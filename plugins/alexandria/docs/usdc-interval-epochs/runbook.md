# Transaction-position epochs runbook

The accepted study is `.hexaemeron/study.md`. This run implements issue #1503 from `41a21f8e065ce086d3ec4355c057b2623b28f205` with Python 3.14.6. Existing raw captures and released identifiers remain immutable. The selected positional receipt is an authorised derived representation. No dependencies, network methods, Solidity, CI configuration or public ABI changes are selected.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 0c47e8f4b35afd10ca1c1f0a8baa26c3cc4871cebefac5590fe96355c618dc2e
candidate | position-boundary
```

All steps retain the twelve study risks. The run uses the tracked checked runner and commit gate. Regenerate portable copies and the Horos boundary/census when their inputs change; their generated outputs are included with the owning changes. Every new prose record follows Sapheneia, Imprimatur, Vulgate and Imprimatur; specifications keep their complete requirements. Fiat owns receipts, App publication and signed commits. Author is Shoggoth; the repository-authorised human publisher signs as committer using the configured key. Each step passes the required checks before commit and remains open until ordered integration.

## Step 1: Preserve the design and scaffold conformance checks

**Goal.** Commit the accepted specification and executable selection evidence, with the harness that will measure production conformance.

**Entry.** The run branch at the recorded starting commit; study and design-lock receipts are accepted; the 642-test Alexandria baseline passes.

**Exit.** Repository copies of the study, runbook, design evidence, selection reports, model generator and observations are present under `plugins/alexandria/docs/usdc-interval-epochs/`, preserving their source bytes and relative report paths. A conformance harness dispatches each of the four accepted resolver names to explicit real assertions and writes a report only after execution; before production support exists those invocations refuse and cannot emit a passing report. Its exact working copy is `.hexaemeron/design/conformance.py`. `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study .hexaemeron/study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py .hexaemeron/runbook.md`, and `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:1` pass. `python3 scripts/run_checks.py --base origin/main` and the Alexandria suite pass. Existing toolchain, licence and CI already supply the scaffold and remain unchanged.

**Files.** Create `plugins/alexandria/docs/usdc-interval-epochs/study.md`, `runbook.md`, `design-evidence.json`, `design/build_design_evidence.py`, `design/model-observations.json`, `design/conformance.py`, and the ten `reports/selection/*.json` files from the accepted design. Create only necessary harness tests under `plugins/alexandria/tests/test_epoch_conformance.py`. Update `tests/check-map-v1.json` if an actual new path needs ownership; regenerate `.agents/skills/promise-machine/runtime/` and `.horos/boundary.json` or `.horos/census.json` where required. The controller-local harness and resolver outputs are not committed as controller state.

**Tests.** Verify selection reports recompute and the production harness refuses missing implementation or a missing/zero-test result. Keep future production conformance pending. For audit fixes the exact runner is `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/epoch-step-1.json`. Run the repository root suite and selected checked-runner scopes as well; the report names only the Alexandria test execution.

**Disciplines.** phylax: the harness reads controlled report paths and must not execute provider text. ephoros: each resolver names the failed criterion and actual result. metron: no performance claim; preserve the existing bounds. elenchus: any harness failure requires reproduction and a guard. hypomnema: preserve the accepted specification and source/report bindings; reserve the representation decision for Step 2.

## Step 2: Derive and verify positional implementation epochs

**Goal.** Make new releases attribute supported proxy logs to transaction-position epochs and verify that ownership offline.

**Entry.** Step 1's reviewed tree, checked design artifacts and conformance harness; the positional candidate remains locked.

**Exit.** New build receipts use `alexandria-interval-receipt/v2`, exclusive position bounds, upgrade transaction indexes and explicit `log_attributions` exactly as the study specifies. All preserved proxy logs reach the shared discovery/attribution validation. First-block upgrades without prior evidence, ordinary emissions in the upgrade transaction, multiple upgrades in a block, malformed or contradictory positions, and unsupported ownership refuse. V1 verification remains explicitly block-only; schema, preserved raw bytes and old release IDs remain unchanged. The old fixture demonstrations use confined fixture-specific reconstruction if needed, with no general legacy-build CLI flag. A before-upgrade assertion is observed red on the unfixed parent and green on the fixed tree; import or infrastructure errors cannot count as that guard. `python3 plugins/alexandria/tests/run_tests.py` and `python3 scripts/run_checks.py --base origin/main` pass. Run all four actual resolver commands and inspect their results; final report copies and demonstration are owned by Step 3.

**Files.** Change `plugins/alexandria/scripts/alexandria_lib/interval.py`, `plugins/alexandria/scripts/usdc_interval.py` and only necessary adjacent validation helpers; add `plugins/alexandria/schemas/interval-receipt-v2.schema.json`; retain the v1 schema. Update `plugins/alexandria/schemas/README.md` if present and `plugins/alexandria/docs/usdc-interval-collector.md`; create `plugins/alexandria/docs/usdc-interval-epochs/decision.md`. Add positional and hostile cases under `plugins/alexandria/tests/test_epoch_positions.py` and adapt existing interval/live tests to distinguish representations. Change only reconstruction code in `plugins/alexandria/examples/usdc-interval-v0/demo.py` and `plugins/alexandria/examples/usdc-interval-live-v0/demo.py` when needed to preserve old IDs. Complete `plugins/alexandria/docs/usdc-interval-epochs/design/conformance.py` and its exact controller-local copy without changing the immutable design selection. Include necessary ownership and generated boundary/census/portable outputs. No raw fixture, released manifest, old expectation identifier or unrelated issue fix is in scope.

**Tests.** Cover literal before/after attribution, an upgrade at the final block, first-block refusal, same-transaction ordinary logs on both sides of the announcement, multi-upgrade refusal, bool/negative/missing indexes, duplicate or unordered coordinates, conflicting transaction hashes/indexes and block hashes, forged ownership and boundary tables rebound through real ingest, code/slot mismatch and restart identity. Existing live/synthetic IDs must reproduce. For audit fixes the exact runner is `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/epoch-step-2.json`. Elenchus uses that runner on the unfixed parent; preserve the fresh fixed-tree counts separately. Test counts are determined by execution, never a target.

**Disciplines.** phylax: validate provider coordinates before comparison and preserve path/byte bounds. ephoros: errors identify bounded positions and the checked receipt semantics. metron: no speed claim; avoid copying whole journals per epoch. elenchus: the observed attribution defect requires a parent assertion failure and passing fixed suite. hypomnema: record v2 semantics, v1 compatibility and unsupported histories at the decision path linked from collector documentation.

## Step 3: Demonstrate attribution and reconcile the frontier

**Goal.** Publish executable evidence for the new ownership and preserve the scope of both receipt versions.

**Entry.** Step 2's audited implementation and compatible old-release readers; all production conformance assertions execute successfully.

**Exit.** `python3 plugins/alexandria/examples/usdc-interval-epochs-v0/demo.py build --output <fresh-directory>` and `verify <same-directory>` demonstrate literal old/new ownership over a synthetic upgrade block and reject altered ownership offline. A second new v2 build from the unchanged live staging bytes verifies with its own recorded ID; both historical examples still reproduce their old IDs. Execute `python3.14 .hexaemeron/design/conformance.py production-attribution`, `legacy-release-identity`, `offline-rederivation`, and `resume-and-refusal` as separate commands; copy the four actual selected-candidate reports into the committed design report tree. `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition integration` passes. Increment Alexandria evolution once, retain generation/epoch, and record one evidenced next job or mature status based on the remaining material gap. Cold-read and reconcile all mutable first-party marketplace prose as VERSIONING requires; preserve unrelated historical records and fixed-template bytes. Refresh generated SOURCES through its owner before integration. `python3 scripts/run_checks.py --base origin/main`, Alexandria tests, the demos, portable check, census and relevant prose lints pass. Fiat saves the required native/Linux checkpoint before the next transition.

**Files.** Create `plugins/alexandria/examples/usdc-interval-epochs-v0/demo.py`, `README.md` and an expectation record for the new representation, plus tests under `plugins/alexandria/tests/test_epoch_positions_demo.py`. Record new live v2 expectations beside this demonstration without altering historical expectations. Add the four `plugins/alexandria/docs/usdc-interval-epochs/reports/conformance/position-boundary-*.json` files and concise `proof.md`. Update `plugins/alexandria/skills/alexandria/EVOLUTION.md`, sibling `SKILL.md`, `plugins/alexandria/AGENTS.md`, `plugins/alexandria/README.md`, collector document, schema pointers and relevant public marketplace descriptions. Include package manifests if packaging rules require a version change. Reconcile other mutable first-party marketplace prose only where its current evidence requires correction, retaining source-owned text. Include the generated `SOURCES.md`, any repaired ledger source links, portable runtime, boundary/census and actual check-map ownership changes. The exact existing raw live/synthetic evidence and old release IDs remain immutable.

**Tests.** Execute both old demonstrations, the new synthetic v2 demonstration, the unchanged-live-source v2 build/check, all four production conformance resolvers, full Alexandria tests, selected checked-runner checks, and controller-owned fresh native/Linux checkpoint proofs. For audit fixes the exact runner is `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/epoch-step-3.json`. Record actual counts and limitations; no result can be inferred from the selection model.

**Disciplines.** phylax: demonstration and verification stay offline and preserve source bytes. ephoros: results identify v1 or v2 and unsupported attribution. metron: no performance claim; retain caps. elenchus: replay the attribution guard and hostile release cases. hypomnema: link the completed proof, compatibility decision, frontier evidence and reasoned carryover dispositions.

### Amendment -- 2026-09-08

**What changed.** Complete replacement Files: Create `plugins/alexandria/docs/usdc-interval-epochs/study.md`, `runbook.md`, `design-evidence.json`, `design/build_design_evidence.py`, `design/model-observations.json`, `design/conformance.py`, and the ten `reports/selection/*.json` files from the accepted design. Create only necessary harness tests under `plugins/alexandria/tests/test_epoch_conformance.py`. Update `tests/check-map-v1.json` if an actual new path needs ownership; regenerate `.agents/skills/promise-machine/runtime/` and `.horos/boundary.json` or `.horos/census.json` where required. The controller-local harness and resolver outputs are not committed as controller state. Add a prose-only decision section to `plugins/alexandria/skills/alexandria/EVOLUTION.md`; retain all frontier headers, versions and history rows until Step 3.

Complete replacement Exit: Repository copies of the study, runbook, design evidence, selection reports, model generator and observations are present under `plugins/alexandria/docs/usdc-interval-epochs/`, preserving their source bytes and relative report paths. A conformance harness dispatches each of the four accepted resolver names to explicit real assertions and writes a report only after execution; before production support exists those invocations refuse and cannot emit a passing report. Its exact working copy is `.hexaemeron/design/conformance.py`. `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study .hexaemeron/study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py .hexaemeron/runbook.md`, and `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:1` pass. `python3 scripts/run_checks.py --base origin/main` and the Alexandria suite pass. Existing toolchain, licence and CI already supply the scaffold and remain unchanged. The ledger records the selected construction and rejected alternative, and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study plugins/alexandria/docs/usdc-interval-epochs/study.md --design-evidence plugins/alexandria/docs/usdc-interval-epochs/design-evidence.json --repo-root .` passes.

**Why.** The receipted study placement amendment requires the selected design in the governed ledger and a checked bridge before the study ships.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.

### Amendment -- 2026-09-08

**What changed.** Complete replacement Files: Change `plugins/alexandria/scripts/alexandria_lib/interval.py`, `plugins/alexandria/scripts/usdc_interval.py` and only necessary adjacent validation helpers; add `plugins/alexandria/schemas/interval-receipt-v2.schema.json`; retain the v1 schema. Update `plugins/alexandria/schemas/README.md` if present and `plugins/alexandria/docs/usdc-interval-collector.md`; link the existing decision section in `plugins/alexandria/skills/alexandria/EVOLUTION.md` without creating a separate decision file. Add positional and hostile cases under `plugins/alexandria/tests/test_epoch_positions.py` and adapt existing interval/live tests to distinguish representations. Change only reconstruction code in `plugins/alexandria/examples/usdc-interval-v0/demo.py` and `plugins/alexandria/examples/usdc-interval-live-v0/demo.py` when needed to preserve old IDs. Complete `plugins/alexandria/docs/usdc-interval-epochs/design/conformance.py` and its exact controller-local copy without changing the immutable design selection. Include necessary ownership and generated boundary/census/portable outputs. No raw fixture, released manifest, old expectation identifier or unrelated issue fix is in scope.

**Why.** The receipted study placement amendment gives the selected design one standing home in the governed ledger.

**Steps touched.** Step 2.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.
