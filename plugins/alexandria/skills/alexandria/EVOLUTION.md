# Alexandria evolution ledger

Policy: [../../../hexaemeron/skills/VERSIONING.md](../../../hexaemeron/skills/VERSIONING.md)

- Current version: `alexandria-v3.7.0`
- Frontier status: `open`
- Frontier revision: `transaction-index-reconciliation`
- Current frontier: Ordinary builds now emit `alexandria-interval-receipt/v2`, which attributes each preserved proxy log to an implementation epoch by block, transaction index and log index, and `check` re-derives every owner offline; reconciliation still compares a log without its transaction index, so a second provider that reports a different index for the same log records `agreed`.
- Next Fiat job: Reconcile each provider log's transaction index as well as its identity, so a second provider that reports a different position for a log the v2 receipt attributes by position is disputed rather than agreed. Accepted when a versioned reconciliation record disputes an otherwise identical log whose providers differ only in `transactionIndex` and keeps both providers' bytes for that shard, identical positions still agree, a malformed position from either provider still leaves the interval unreconciled, every historical release keeps its identifier and recorded comparison meaning, and an executable case over that index-only difference fails without the change.
- Sources: [../../../../SOURCES.md](../../../../SOURCES.md)

## History

| Version | Axis | Frontier revision | Frontier SHA-256 | Evidence | Change |
| --- | --- | --- | --- | --- | --- |
| `alexandria-v0.2.0` | baseline | `usdc-interval-collector` | `d5afa30db4e5769dccded9f28be061dc623e119b328dbbcd87b729567b7eaeff` | [README marketplace-context](../../README.md) | Versioning starts here. The held frontier is adopted from the plugin's marketplace-context block unchanged. |
| `alexandria-v0.3.0` | generation | `usdc-interval-collector` | `d5afa30db4e5769dccded9f28be061dc623e119b328dbbcd87b729567b7eaeff` | [release statement](../../docs/release-statements.md) | Adds the deterministic unsigned release-statement emitter and schema without changing the held frontier or claiming signing, publisher identity, provider completeness, consensus finality or canonical-chain status. |
| `alexandria-v0.4.0` | generation | `usdc-interval-collector` | `d5afa30db4e5769dccded9f28be061dc623e119b328dbbcd87b729567b7eaeff` | [skills#391](https://github.com/wildcat-finance/skills/issues/391) | The Probitas bridge now returns each coverage row's release identities as a `releases` field beside the note that already named them in prose, so a Probitas gate can require an archive row to name what it stands on without parsing another plugin's sentences. A row with no records has no record to derive them from, which is exactly the empty archive row that most needs them. The demonstration's pinned Probitas digests are regenerated for the new evidence schema and its coverage status counts are unchanged. Frontier unchanged: the held USDC interval-collector job is untouched. |
| `alexandria-v1.4.0` | evolution | `usdc-interval-live-boundaries` | `5e225140ccbe4328e07e733ec28f972ff42760f88685109d05a1acd626837372` | [skills#395](https://github.com/wildcat-finance/skills/issues/395), [study](../../docs/usdc-interval-study.md) | The held collector job is delivered. `usdc_interval.py` collects a declared Ethereum USDC Comet interval in bounded shards from an injected transport, is resumable through a checkpointed journal per evidence class with a bounded reorg rewind, discovers code-hash-bound implementation epochs from preserved chain evidence, reconciles the finished interval against a second provider without settling a disagreement, binds its end boundary under a named finality policy, and builds a release the existing `ingest` accepts and a new offline check verifies. The frontier stays open: nothing has run against a live provider, the interval's first block is never read so a scope's finality class is `provider-reported` rather than the policy it names, and no implementation code is preserved, so an epoch's code hash cannot be rechecked from the release. |
| `alexandria-v1.5.0` | generation | `usdc-interval-live-boundaries` | `5e225140ccbe4328e07e733ec28f972ff42760f88685109d05a1acd626837372` | [skills#397](https://github.com/wildcat-finance/skills/issues/397), [proof-backed tests](../../tests/test_proof_backed.py) | `verify` now earns `proof-backed-state` by reconstructing a digest-mapped Lazarus fixture, rerunning its offline verifier and bounding the capture to the proved block and targets. A checked-in synthetic example and executable Promise Machine cases cover acceptance, refusal, overclaim, recovery and side effects. Frontier unchanged: the held live-boundaries job is untouched. |
| `alexandria-v2.5.0` | evolution | `epoch-attribution-by-transaction` | `75ea7f1e4bc4e85c5a223c6d118df6307a96eb05fa9a2e39ad4cb7f6b0b591a5` | [skills#1350](https://github.com/wildcat-finance/skills/issues/1350), [study](../../docs/usdc-interval-live-study.md), [live capture](../../examples/usdc-interval-live-v0/README.md) | The held live-boundaries job is delivered. `collect` now opens with reads of the interval's first block, each epoch boundary's implementation slot and each implementation's runtime code, journaled and checkpointed as a fourth `epoch-evidence` class, so `build` discovers the epoch table from preserved bytes rather than an operator table and the retired `--epochs` argument is gone. The release carries the runtime code as an `implementation-code` component and `check` re-hashes it offline; a plan declares its own evidence classes and its provider class, and a `finalized` or `safe` plan now earns that finality class on every evidence scope with both boundary hashes bound. The collector ran against two live providers over Ethereum mainnet blocks 25,903,935 to 25,905,934: four shards of 500 all complete, 93 logs, two implementation epochs split by an `Upgraded(address)` at block 25,904,935, reconciliation `agreed` over 106 comparisons with none disputed, and the whole capture committed as `examples/usdc-interval-live-v0` rebuilding offline to release identifier `sha256:30c4e9724b1d7bcb32d95fb6090ca4eed51f837a39779ead7d4b723b2e2b3b32`. The frontier stays open: the epoch table tiles the interval by block, so a proxy log emitted earlier in an upgrade block than the upgrade itself would be attributed to the implementation that replaced the one which produced it. This capture instantiates no such log, block 25,904,935 carrying only the upgrade at transaction index 193 and log index 524, so the gap is open in the derivation rather than in these bytes. |
| `alexandria-v3.5.0` | evolution | `transaction-index-reconciliation` | `43f5386a363538011b102d4dc96bebab1821437a7daf7acbbd33f433a4117b71` | [skills#1503](https://github.com/wildcat-finance/skills/issues/1503), [proof](../../docs/usdc-interval-epochs/proof.md), [positional demonstration](../../examples/usdc-interval-epochs-v0/README.md) | The held transaction-attribution job is delivered. Ordinary `build` now emits `alexandria-interval-receipt/v2`: each epoch carries exclusive start and end positions of block, transaction index and log index, `log_attributions` gives every preserved proxy log one owner, and `check` re-derives those owners from the release offline. The guard over an upgrade block with a proxy log in an earlier transaction fails by assertion on the parent `7cfb7a55` and passes at `1609b86d`, and the four production conformance resolvers exit 0 and reproduce their reports byte for byte. Historical v1 releases keep their bytes, identifiers and block-only meaning, and both historical demonstrations still rebuild to their pinned identifiers. `examples/usdc-interval-epochs-v0` pins literal owners over a synthetic upgrade block in release `sha256:b71996c8450d5f28c36d112fab7bafb636b2176d74e6f609646dbe5162983036`, rebuilds the unchanged live staging bytes as v2 release `sha256:42eb1651533a795b25977cec9e0ef683ebecd7c65ff558913077f5a4da2aad3a`, and refuses a re-digested release whose owner or boundary moved. Ordinary logs in the upgrade transaction, an upgrade in the interval's first block and two upgrades in one block still refuse. The frontier stays open: reconciliation compares a log without its transaction index, and a second provider that differs from the first only in one log's index, `0x3` against `0x7`, still records `agreed`. |
| `alexandria-v3.6.0` | generation | `transaction-index-reconciliation` | `43f5386a363538011b102d4dc96bebab1821437a7daf7acbbd33f433a4117b71` | [skills#1731](https://github.com/wildcat-finance/skills/issues/1731), [whole proof](../../docs/wildcat-interval/proof.md), [both estates](../../examples/wildcat-estates-interval-v0/README.md) | Register Wildcat V1 and V2 in the shared collector, preserve both mainnet intervals, and rebuild them offline beside Compound. Per-subject epochs begin at each subject's established deployment or first-code evidence; MAX_EPOCHS bounds each subject separately. The eight conformance cells pass without changing the held transaction-index reconciliation job. Package version 0.7.10. |
| `alexandria-v3.7.0` | generation | `transaction-index-reconciliation` | `43f5386a363538011b102d4dc96bebab1821437a7daf7acbbd33f433a4117b71` | [skills#1888](https://github.com/wildcat-finance/skills/issues/1888), [proof](../../docs/epoch-table-split/proof.md) | A subject-set plan that declares `log_attribution_parts` moves its attribution rows out of the epoch table into `log-attributions.<k>` parts at the plan's journal ranges, under `alexandria-interval-receipt/v4`, and `check` re-derives every part offline. A release now holds 16,384 components and 16,384 captures, manifests and capture plans are read under 134,217,728 bytes and 2,000,000 nodes, and the collector checkpoint under 2,000,000 nodes. A plan without the field keeps its bytes: every pinned demonstration rebuilds to its identifier and both committed releases verify. The held transaction-index reconciliation job is unchanged. Package version 0.7.20. |

## Transaction-position design decision

Accepted for implementation on 2026-09-08 in [#1503](https://github.com/wildcat-finance/skills/issues/1503). Production conformance has since passed: the [proof](../../docs/usdc-interval-epochs/proof.md) records the four resolver reports and the guard's assertion failure on the parent. The [study](../../docs/usdc-interval-epochs/study.md) and [selection evidence](../../docs/usdc-interval-epochs/design-evidence.json) select `position-boundary` over `block-only`: whole-block assignment misattributes earlier proxy logs in an upgrade block and accepts unsupported same-transaction emissions.

The selected receipt is `alexandria-interval-receipt/v2`, with exclusive-end positions ordered by block, transaction index and log index. Nullable index pairs mark block edges. All preserved proxy logs must reach the same attribution rule, and offline checking must reproduce each owner. Ordinary emissions in the upgrade transaction, an upgrade in the first block without preceding implementation evidence, and multiple upgrades in one block refuse. The announcement identifies the new boundary; it does not prove the emitting implementation.

Historical v1 releases keep their exact bytes, identifiers and block-only meaning. New v2 derivations receive new identifiers. This design adds no RPC method or release component and makes no performance claim. The implementation and its conformance evidence delivered the frontier recorded as `alexandria-v3.5.0`.
