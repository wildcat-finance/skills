# Anamnesis evolution ledger

Policy: [../../../hexaemeron/skills/VERSIONING.md](../../../hexaemeron/skills/VERSIONING.md)

- Current version: `anamnesis-v5.1.0`
- Frontier status: `open`
- Frontier revision: `resolved-mapper`
- Current frontier: The curation policy's declared mapper is resolved through a registry keyed by name and version: an unresolved name refuses before any record is written, every assertion records the entry that read the source, and three corpora ship, the third preserving the pilot's same 41 findings read from their synopsis rendering under a second registered implementation.
- Next Fiat job: Admit a corpus whose findings a party other than this repository produced, under a per-source mapper declaration so one corpus can hold both formats. Every source admitted so far was produced here, and the mapper is declared once for the whole curation policy, so one corpus cannot hold a second producer's format beside an existing one. Accepted when the mapper is declared per admitted source rather than once per policy, one corpus admits two sources in different formats under an explicit rights basis for each, and each assertion names the implementation that read its own source.
- Sources: [../../../../SOURCES.md](../../../../SOURCES.md)

```declared-inputs
second-producer-findings | corpus | absent | Audit findings a party outside this repository produced, together with a rights basis that permits redistributing them.
```

## History

| Version | Axis | Frontier revision | Frontier SHA-256 | Evidence | Change |
| --- | --- | --- | --- | --- | --- |
| `anamnesis-v0.1.0` | baseline | `seed-corpus-prototype` | `d03eb92bce14b6c11f2a30d226cd65451a8eadf2e6c37a15fa02f7f404b27ee4` | [the Protasis study](../../docs/study.md) and [its runbook](../../docs/runbook.md) | Versioning starts here. The member exists because no sibling owned custody of audit findings and their remedies: Warden produces one round's record, Elenchus proves one present cause, and Synkrisis compares declared runs. Source admission is the first promise to ship; curation and release are declared and refuse by name. The held job is the curation graph runbook step 2 owes. |
| `anamnesis-v1.1.0` | evolution | `consumer-projections` | `897e50052a6d7dd14b661847c40f6fd35befbd088205fde57a954cf42f0ef664` | [the Step 2 curation suite](../../tests/test_s2_curation.py) and [the pilot release](../../specimens/pilot/release/manifest.json) | The held seed-corpus job completed. The graph keeps submissions, findings, occurrences, remediation attempts and verifications apart and joins them many-to-many, so one change answering four findings and one finding needing two changes are both representable. Rejection and acceptance are facts about a finding and produce no remediation, which stops a rejection and a fix recorded in the same round collapsing into one record. The pilot releases 41 findings across 31 rounds, 12 of them with no findings, and rebuilds to the same release id. The consumer projections are the remaining held job. |
| `anamnesis-v2.1.0` | evolution | `synkrisis-producer-admission` | `427bebd06deb3114fc00c0be50b6de44f2d1f2ebd36ed8788fb826983dea77cf` | [the Step 3 consumer suite](../../tests/test_s3_consumers.py) and [the demo path](../../docs/demo.md) | The held consumer-projection job completed. Both views are closed field sets checked before they leave: the Elenchus view has no verdict field, refuses a remediation carrying a verification state, and states what it does not establish; the Synkrisis view carries every denominator beside every count. A non-public source reaches neither, and the withholding is counted. The demo builds the pilot twice into fresh directories and compares the release id, the file set and every component byte, then reads both views and prints duration and peak memory as baselines with no budget declared. Synkrisis still gates on one producer identity and does not admit this one, so the cohort is produced and not consumed; that is the new held job. |
| `anamnesis-v3.1.0` | evolution | `corpus-scope` | `1da8d2f843cb3b0ff1fd6dac5d41d4cafb5746388635c3f1ba5e41adde1d1f77` | [ADR-005](../../docs/decisions/ADR-005-corpus-projections-outside-the-cohort-boundary.md) and [the Step 5 boundary suite](../../tests/test_s5_boundary.py) | The held synkrisis-producer-admission job completed, and it completed as the second of the two outcomes it allowed. Synkrisis admits one producer identity at two enforcement sites, and a manifest declaring this one refuses SK008 while the same manifest declaring promise-machine-run-observation/v1 passes that gate. Widening it would drop the ten denominators at the boundary and fabricate six of the nine required run fields, and building a sibling cohort kind is a frontier-scale change to a skill whose own ledger is open elsewhere. ADR-005 records that corpus projections sit outside the cohort boundary and names the projection itself, read under its own schema, as what reads them instead. What the corpus should preserve beyond the hand-picked pilot is the new held job. |
| `anamnesis-v4.1.0` | evolution | `declared-scope` | `13c730d9f26091188d16c8c640632c21943fe3f85b2c9363c70b8e0ed499965e` | [ADR-006](../../docs/decisions/ADR-006-declared-corpus-scope.md), [the Step 8 scope suite](../../tests/test_s8_scope.py) and [the Step 9 estate suite](../../tests/test_s9_estate.py) | The held corpus-scope job completed as the first of the two outcomes it allowed. `scope` is a required closed object of the curation policy, which the manifest records and the release id hashes, so a corpus cannot change what it preserves without changing its id; `seed_scope` and its literal bound leave the program, and admission, curation, release and rebuild refuse a source outside the scope, a scope source that was not admitted and a record count outside the declared bounds. The pilot declares `warden-seed-pilot` and is re-released; the estate corpus declares `capture-estate-findings` and rebuilds to its own release id over 17 findings the capture estate recorded about itself, admitted under a written permission as public derived text. What the declared mapper selects is the new held job. |
| `anamnesis-v5.1.0` | evolution | `resolved-mapper` | `df27b276252cc4045a18c202b8b00782fe4dc40835b3eafca3935f48900e13f9` | [ADR-007](../../docs/decisions/ADR-007-resolved-mapper-registry.md), [the Step 12 synopsis suite](../../tests/test_s12_synopsis.py) and [the Step 13 guard suite](../../tests/test_s13_guards.py) | The held declared-scope job completed. The curation policy's `mapper` is resolved through a module-level registry keyed by name and version: a name no entry provides refuses `A078` at curation before any assertion, quarantine entry or release directory exists, and every assertion records the entry that ran rather than the string the policy declared. A second entry reads `fiat-audit-synopsis/v1` and refuses `A079` on a missing or non-matching schema header, so handing one implementation the other's bytes returns a refusal rather than 31 rounds and 0 findings. A third corpus preserves the pilot's same 41 findings from their synopsis rendering and rebuilds to its own release id, and both shipped release ids are unmoved. The corpus can now select an implementation and still holds one producer's findings in two formats; admitting a second producer's, under a per-source mapper declaration, is the new held job. |
