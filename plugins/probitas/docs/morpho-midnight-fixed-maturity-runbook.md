# Runbook: add fail-closed Morpho Midnight fixed-maturity coverage

Derived from `.hexaemeron/study.md`, accepted at SHA-256
`b5f920c96f4fefbbd913f52fc9f85eec3bb16e1515a16d33a9b7c3bce4b7b0e4`.
Four steps follow one capability from durable specification through strict
collection, dossier integration and an operator-visible demonstration.

## Step 1: Scaffold the Midnight specification and audit runner

**Goal.** Commit the accepted design boundary and give every later Probitas audit one source-owned, fail-closed test-report command.

**Entry.** Branch `fiat/390-add-fail-closed-morpho-midnight-fixed-maturi` at immutable base `7e449ba35e1519d28b33f06225c4c4137b548a23`, with the study receipted, a clean tracked tree, and the pinned Python 3.13.15 Probitas baseline green at 276 tests.

**Exit.** The accepted study and this runbook exist byte-for-byte under `plugins/probitas/docs/`; ADR-042 records the due-time, settlement-language, schema-1 evidence and API-completeness decisions named in study item 12; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report tmp/elenchus/probitas-midnight-step-1-exit.json` exits zero and writes one complete `elenchus.unittest.v1` result for all Probitas tests. The same runner refuses absent, duplicate, empty, existing, outside-worktree, parent-escape, symlinked and replaced report targets, cleans up its own incomplete target after a write failure, and never removes a target it no longer owns. Protasis accepts both committed design records, Imprimatur reports 100.0 on every shipped prose file, the root suite exits zero, and `git diff --check` is clean.

**Files.** `plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `docs/decisions/ADR-042-derive-midnight-timeliness-from-debt-units.md`, `plugins/probitas/tests/run_tests.py`, and `plugins/probitas/tests/test_run_tests.py`.

**Tests.** Add reporter tests for a passing suite, an assertion failure, an infrastructure error, zero discovered tests, every hostile target above, secure parent creation, complete counters and interrupted writes; retain all 276 baseline Probitas cases and the root suite. Exact Elenchus runner contract: command `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `tmp/elenchus/probitas-midnight-step-1.json`.

**Disciplines.** phylax: the new caller-named report writer must confine a fresh regular file to the owning worktree and resist path, symlink and replacement races. ephoros: the complete report records test totals, failures, errors, skips and abnormal termination for Warden. metron: none, this step makes no performance claim. elenchus: the eight hostile report-target cases and interrupted writes are reduced to guards before the runner is trusted. hypomnema: ADR-042 and the committed study preserve the due-time and evidence-boundary decisions that would be expensive to reverse.

## Step 2: Build the strict Midnight evidence adapter

**Goal.** Turn bounded Morpho Midnight v0 responses into exact, transaction-cited event and maturity records, or fail before claiming coverage.

**Entry.** Step 1's green exit state, including its committed design records and source-owned Probitas audit runner.

**Exit.** `morpho_midnight.py` walks every Base user-transaction cursor without following redirects, validates the documented event vocabulary and event-specific subject, reads immutable market/token and current-position data, groups exact integer debt-unit deltas by second, includes the maturity second, rejects post-maturity debt increases and negative balances, reconciles the observation balance, and emits separate obligation-state and settlement-mode values. Its coverage note binds Base chain id 8453, cursor exhaustion, observation/index bounds and the unpublished API-history lower bound without claiming archive-chain completeness. Cleared, residual-then-liquidated, not-due and empty specimens produce their named states; unknown or ambiguous secondary close, unknown type, malformed semantic field, float or boolean integer, wrong subject/chain/id/hash, cursor repetition or ceiling, response ceiling, origin redirect, position disagreement and every other study risk return no records through shared error coverage. `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 -m unittest plugins.probitas.tests.test_adapter_morpho_midnight` and the step's Elenchus runner both exit zero, while Phylax and Ephoros report no unresolved defect.

**Files.** `plugins/probitas/scripts/probitas_lib/adapters/morpho_midnight.py`, `plugins/probitas/scripts/probitas_lib/endpoints.py`, `plugins/probitas/tests/test_adapter_morpho_midnight.py`, and source-dated `morpho-midnight.json` specimens beneath `plugins/probitas/tests/fixtures/midnight-cleared/`, `midnight-late/`, `midnight-not-due/`, and `midnight-empty/`.

**Tests.** Add focused cases for every success and refusal in the Exit, including exact equality at maturity, equal-second grouping, current zero after a late liquidation, determining-transaction citations, contributing-record counts, harmless extra fields, deliberately ignored known variants, page and byte ceilings, timeouts and no partial results; run the complete Probitas suite after the focused module. Exact Elenchus runner contract: command `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `tmp/elenchus/probitas-midnight-step-2.json`.

**Disciplines.** phylax: this step opens the evolving REST ingestion boundary, so origin locking, redirect refusal, bounded reads, semantic validation, exact integers and secret-free errors are required. ephoros: coverage and error notes must answer whether cursors exhausted, which observation/index boundary was used and why no claim was emitted. metron: none, bounded pagination is a safety limit rather than a speed claim. elenchus: every observed schema or accounting failure is first reduced to a source-dated fixture mutation and then guarded. hypomnema: the durable accounting choice is already in ADR-042; reversible field mappings and known-ignore reasons stay beside the adapter constants.

## Step 3: Integrate maturity outcomes into the dossier

**Goal.** Register Midnight in the existing collection path and render its obligation state without erasing settlement conduct or coverage gaps.

**Entry.** Step 2's green exit state, with the strict adapter independently passing but `morpho-midnight` still unimplemented in the registry and absent from aggregate collection.

**Exit.** The CLI registers `morpho-midnight` as a distinct Base venue; every aggregate fixture directory contains a deterministic `morpho-midnight.json`; the shared adapter runner turns any Midnight refusal into coverage `error` plus a named gap; and the renderer gives `cleared_by_maturity`, `outstanding_at_maturity`, `settled_late` and `not_due` explicit plain-language paths while keeping primary repayment, secondary close and liquidation distinct. The regenerated example dossier shows checked Midnight coverage, a reproduced maturity outcome and no Midnight gap, and all five verification gates pass. The focused adapter, registry, CLI, rendering, evidence, documentation and demo tests plus the complete step Elenchus runner exit zero; the evidence schema remains 1 and every old venue fixture stays deterministic.

**Files.** `plugins/probitas/scripts/probitas.py`, `plugins/probitas/scripts/probitas_lib/registry.py`, `plugins/probitas/scripts/probitas_lib/adapters/__init__.py`, `plugins/probitas/scripts/probitas_lib/render.py`, `plugins/probitas/tests/test_registry.py`, `plugins/probitas/tests/test_cli.py`, `plugins/probitas/tests/test_render.py`, `plugins/probitas/tests/test_evidence.py`, `plugins/probitas/tests/test_docs.py`, `plugins/probitas/tests/test_demo.py`, every aggregate fixture directory under `plugins/probitas/tests/fixtures/`, and `plugins/probitas/docs/example-dossier.md`.

**Tests.** Extend aggregate coverage counts, registry/adapter equality, error-to-gap behavior, exact evidence values, supported rendering language, gate reconstruction, deterministic bytes and the real documented collect-render-verify subprocess path; assert specifically that a current zero balance cannot rewrite an outstanding-at-maturity result and a liquidation cannot render as voluntary repayment. Exact Elenchus runner contract: command `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `tmp/elenchus/probitas-midnight-step-3.json`.

**Disciplines.** phylax: aggregate fixture selection remains confined and the renderer must not turn untrusted values into Markdown structure. ephoros: the dossier and coverage table expose outcome, settlement mode, source, cursor state and any refusal to the operator. metron: none, this step makes no performance claim. elenchus: integration failures are reduced in the focused adapter, renderer or CLI module before the whole suite is rerun. hypomnema: none new, this step implements ADR-042 without introducing another expensive choice.

## Step 4: Reconcile the marketplace, advance the frontier and demonstrate

**Goal.** Publish the completed capability consistently, advance only Probitas's held frontier once, and prove the shipped operator path from clean inputs.

**Entry.** Step 3's green exit state, with Midnight behavior complete but public marketplace prose, package delivery metadata and the Probitas evolution ledger still at their starting claims.

**Exit.** Every mutable first-party marketplace-context block is cold-read; only claims affected by the completed Probitas job change, and unrelated skill frontiers remain byte-identical. Probitas records exactly one evolution row at `probitas-v1.1.0`, with a recomputed digest and an evidence-backed successor or `None -- mature`; its frontmatter agrees. The Probitas delivery package takes its smallest unused patch version `0.1.2` across both plugin manifests, the root marketplace and the exact-version test, so an installed cache can observe the changed skill. The README, canonical skill and venue reference state Base-only API-scoped coverage, the unpublished lower bound, strict failure behavior and the distinction between due-time state and liquidation. `portable_promise_machine.py sync` leaves canonical and portable copies equal. From a fresh temporary directory, the documented CLI collects the demo fixture, renders the dossier and verifies five passing gates; the dossier contains checked Midnight coverage and no Midnight gap. The complete Probitas and root suites, portable check, Promise Machine check, version/evolution/marketplace tests, Protasis, Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, Agent Skills frontmatter validation, Horos currency scan and `git diff --check` all exit zero on the exact final tree.

**Files.** `plugins/probitas/README.md`, `plugins/probitas/skills/probitas/SKILL.md`, `plugins/probitas/skills/probitas/EVOLUTION.md`, `plugins/probitas/skills/probitas/references/venues.md`, `plugins/probitas/docs/adding-a-venue.md` if its counts or procedure changed, `plugins/probitas/.claude-plugin/plugin.json`, `plugins/probitas/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `plugins/probitas/tests/test_manifests.py`, `tests/test_version_propagation.py`, `tests/test_marketplace_prose.py` where the new final claim needs a guard, and generated Probitas files under `.agents/skills/promise-machine/runtime/` produced by the repository sync command.

**Tests.** Extend version/frontier and marketplace assertions for header/frontmatter agreement, one evolution row, correct evolution arithmetic, a recomputed frontier digest, package version `0.1.2` in all delivery surfaces, canonical-to-portable equality, unchanged unrelated frontiers, documented venue counts and the exact final demo. Record actual final test counts rather than predicting them. Exact Elenchus runner contract: command `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `tmp/elenchus/probitas-midnight-step-4.json`.

**Disciplines.** phylax: rerun the repository-wide off-chain boundary lint after portable synchronization and prove no credential, host or filesystem boundary widened. ephoros: the final demo, report and coverage row provide the operator-visible readback for the whole path. metron: none, no performance claim is published. elenchus: any final-tree failure is reduced in its owning focused test and guarded before the release suites rerun. hypomnema: the signed evolution row records the completed frontier and its evidenced successor or mature closure; ADR-042 remains the decision record for behavior.

### Amendment -- 2026-08-28

**What changed.** Complete replacement Exit: The accepted study and this runbook exist byte-for-byte under `plugins/probitas/docs/`; ADR-042 records the due-time, settlement-language, schema-1 evidence and API-completeness decisions named in study item 12; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report tmp/elenchus/probitas-midnight-step-1-exit.json` exits zero and writes one complete `elenchus.unittest.v1` result for all Probitas tests. The same runner refuses absent, duplicate, empty, existing, outside-worktree, parent-escape, symlinked and replaced report targets, cleans up its own incomplete target after a write failure, and never removes a target it no longer owns. `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py sync` records the two new Probitas document copies in the generated portable runtime, and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py check` exits zero. Protasis accepts both committed design records, Imprimatur reports 100.0 on every shipped prose file, the root suite exits zero, and `git diff --check` is clean. Complete replacement Files: `plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `docs/decisions/ADR-042-derive-midnight-timeliness-from-debt-units.md`, `plugins/probitas/tests/run_tests.py`, `plugins/probitas/tests/test_run_tests.py`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `.agents/skills/promise-machine/runtime/MANIFEST.json`, and `.horos/boundary.json`.

**Why.** The root-suite guard `test_generated_runtime_is_current` observed that committing new Probitas documents requires the repository-mandated portable sync. The generated copies, manifest and Horos boundary are mechanical consequences of that source change, but the original Step 1 Files field did not authorise them.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.

### Amendment -- 2026-08-28

**What changed.** Complete replacement Files: `plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `docs/decisions/ADR-042-derive-midnight-timeliness-from-debt-units.md`, `plugins/probitas/tests/run_tests.py`, `plugins/probitas/tests/test_run_tests.py`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `.agents/skills/promise-machine/runtime/MANIFEST.json`, and `.agents/skills/promise-machine/runtime/.horos/boundary.json`.

**Why.** The portable sync's exact diff places its generated Horos boundary inside `.agents/skills/promise-machine/runtime/`; the preceding amendment incorrectly named the unchanged root `.horos/boundary.json`.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.

### Amendment -- 2026-08-28

**What changed.** Complete replacement Exit: The accepted study and this runbook exist byte-for-byte under `plugins/probitas/docs/`; ADR-042 records the due-time, settlement-language, schema-1 evidence and API-completeness decisions named in study item 12; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report tmp/elenchus/probitas-midnight-step-1-exit.json` exits zero and writes one complete `elenchus.unittest.v1` result for all Probitas tests. The same runner refuses absent, duplicate, empty, existing, outside-worktree, parent-escape, symlinked and replaced report targets, cleans up its own incomplete target after a write failure, and never removes a target it no longer owns. `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py sync` records the two new Probitas document copies in the generated portable runtime; `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/horos/skills/horos/scripts/horos.py scan . --write` records the resulting classified runtime-tree change in the root boundary; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py check` exits zero. Protasis accepts both committed design records, Imprimatur reports 100.0 on every shipped prose file, the root suite exits zero, and `git diff --check` is clean. Complete replacement Files: `plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `docs/decisions/ADR-042-derive-midnight-timeliness-from-debt-units.md`, `plugins/probitas/tests/run_tests.py`, `plugins/probitas/tests/test_run_tests.py`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `.agents/skills/promise-machine/runtime/MANIFEST.json`, `.agents/skills/promise-machine/runtime/.horos/boundary.json`, and `.horos/boundary.json`.

**Why.** A fresh repository Horos scan observed the changed portable runtime directory after its internal boundary and manifest were regenerated. The final generated closure therefore includes both the runtime-local boundary and the root boundary.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.

### Amendment -- 2026-08-28

**What changed.** Complete replacement Exit: The accepted study and this runbook exist byte-for-byte under `plugins/probitas/docs/`; ADR-043 records the due-time, settlement-language, schema-1 evidence and API-completeness decisions named in amended study item 12; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/probitas/tests/run_tests.py --elenchus-report tmp/elenchus/probitas-midnight-step-1-exit.json` exits zero and writes one complete `elenchus.unittest.v1` result for all Probitas tests. The same runner refuses absent, duplicate, empty, existing, outside-worktree, parent-escape, symlinked and replaced report targets, cleans up its own incomplete target after a write failure, and never removes a target it no longer owns. `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py sync` records the two new Probitas document copies in the generated portable runtime; `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 plugins/horos/skills/horos/scripts/horos.py scan . --write` records the resulting classified runtime-tree change in the root boundary; and `/Users/c0rtexzer0/.local/share/uv/python/cpython-3.13.15-macos-aarch64-none/bin/python3.13 scripts/portable_promise_machine.py check` exits zero. Protasis accepts both committed design records, Imprimatur reports 100.0 on every shipped prose file, the root suite exits zero, and `git diff --check` is clean. Complete replacement Files: `plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `docs/decisions/ADR-043-derive-midnight-timeliness-from-debt-units.md`, `plugins/probitas/tests/run_tests.py`, `plugins/probitas/tests/test_run_tests.py`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-study.md`, `.agents/skills/promise-machine/runtime/plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md`, `.agents/skills/promise-machine/runtime/MANIFEST.json`, `.agents/skills/promise-machine/runtime/.horos/boundary.json`, and `.horos/boundary.json`. Complete replacement Disciplines: phylax: the new caller-named report writer must confine a fresh regular file to the owning worktree and resist path, symlink and replacement races. ephoros: the complete report records test totals, failures, errors, skips and abnormal termination for Warden. metron: none, this step makes no performance claim. elenchus: the eight hostile report-target cases and interrupted writes are reduced to guards before the runner is trusted. hypomnema: ADR-043 and the committed study preserve the due-time and evidence-boundary decisions that would be expensive to reverse.

**Why.** Live `origin/main` claimed ADR-042 after this run began. The amended study moves the unchanged decision to free identifier ADR-043, so the step's durable record, file inventory and discipline pointer must agree.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.
