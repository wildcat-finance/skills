# Hypomnema evolution ledger

Policy: [../VERSIONING.md](../VERSIONING.md)

## Held frontier

- Current version: `hypomnema-v5.8.0`
- Frontier status: `open`
- Frontier revision: `duplicate-home-discovery`
- Current frontier: An explicit study mode binds one selected Protasis candidate to exactly one declared ADR or governed-skill ledger and refuses a duplicate the study itself declares, but no check discovers the same decision standing in a second established home when the study declares only one of them.
- Next Fiat job: Close the remaining half of the skills#461 gap: prove mechanically that a selected design's decision stands in exactly one established home across the repository, not only in the one home its study declares. Accepted when a decision recorded in both an ADR and a governed-skill ledger is refused even though its study declares one of them, the check reads only exact declared identifiers rather than inferring a decision from prose, fixtures cover a second home in each direction and a same-home rewrite that is not a duplicate, H008's existing declared-duplicate and single-home results are unchanged, and both suites pass.
- Sources: [../../../../SOURCES.md](../../../../SOURCES.md)

## History

| Version | Axis | Frontier revision | Frontier SHA-256 | Evidence | Change |
| --- | --- | --- | --- | --- | --- |
| `hypomnema-v0.1.0` | baseline | `recorded-reasons-and-their-homes` | `5bcbb5f56863de94e5d141ddb78afc84fcc78aea2e64971dff8e3fd1dc5ceb11` | [skill evolution contract](../VERSIONING.md) | Hypomnema starts here, holding what gets recorded and where it goes. |
| `hypomnema-v1.1.0` | evolution | `adr-shape-check` | `5c69c143dc7adb1380e27931e5440e9772b184b96fc5964f3fb5a722d3ac59f9` | [first-records study](../../../../docs/hypomnema-first-records-study.md), [skills#308](https://github.com/wildcat-finance/skills/pull/308), [skills#309](https://github.com/wildcat-finance/skills/pull/309) | Closes the recorded-reasons-and-their-homes frontier. The marketplace's first deliberate decision records exist under the convention this skill states: ADR-005 records the vendoring boundary around the Pashov suite and ADR-006 the reason skill ledgers are not SemVer, and the lint resolves every pointer in them. The run found the convention's home already alive -- the Promise Machine run had left ADR-001 to ADR-004 under docs/decisions/ in three different status shapes -- so it continued the numbering, normalised all six records to one dated Status shape without moving other content, and named the directory as a running convention in the contract. The successor frontier is the structure the lint cannot yet read: shape drift arrived within a day of the first records, and two records still carry no alternatives section. |
| `hypomnema-v1.2.0` | generation | `adr-shape-check` | `5c69c143dc7adb1380e27931e5440e9772b184b96fc5964f3fb5a722d3ac59f9` | [design bridge study](../../../../docs/hypomnema-design-bridge-study.md), [skills#312](https://github.com/wildcat-finance/skills/pull/312) | A shipped study's chosen design now has to reach a standing record before the step that ships it is receipted. The study names the design and the alternatives that lost, which is the material the record template calls the part that pays, and nothing carried it out of the run artefact once the delivery shipped. The bridge is point-or-write: an ADR under docs/decisions/ for a cross-cutting choice, the skill's EVOLUTION.md row for a governed skill's choice, and never both, because two homes for one decision is the drift the conventions paragraph refuses. The pre-receipt checklist asks for it. The held job's target and acceptance are untouched. |
| `hypomnema-v2.2.0` | evolution | `source-comment-adr-references` | `2aef08cb2c9efa435240ddb41e74f7835e9b93e2579e6a3b492dd84199121915` | [ADR shape check study](../../../../docs/hypomnema-adr-shape-check-study.md), [skills#341](https://github.com/wildcat-finance/skills/pull/341), [skills#342](https://github.com/wildcat-finance/skills/pull/342), [skills#343](https://github.com/wildcat-finance/skills/pull/343) | Closes the adr-shape-check frontier. The lint now holds each record under a decisions directory to the template: H004 for each of the five sections it misses, H005 for a status whose first line is not the dated shape v1.1.0 normalised to, with the existing pragma covering a deliberate exception on the record's first line or the status heading. ADR-002 and ADR-004 carry alternatives sections lifted from the Promise Machine study's options and non-goals, closing the lead the first-records round accepted. Directory walks skip fixtures directories relative to the walked root, because the fixture specimens document faults deliberately; naming a fixtures path directly still reads it. A shape pass inside the existing walk won over a separate lint and a repository test, because the audit round cites one hypomnema command and the rule travels with the plugin. The successor frontier is the walk's reach: skills#317 verified that an ADR cited from a source comment dangles unseen, and the shape rule settled what a record must contain before the walk widens. |
| `hypomnema-v3.2.0` | evolution | `runbook-shape-check` | `e617fc575336ebba63cd074e3e4b93f722a20af2531e822a9bfc41d8958e08cc` | [source-comment references study](../../../../docs/hypomnema-source-comment-references-study.md), [skills#345](https://github.com/wildcat-finance/skills/pull/345), [skills#346](https://github.com/wildcat-finance/skills/pull/346) | Closes the source-comment-adr-references frontier. The walk now reads source files beside the markdown -- `#` comments in Python and shell, `//` comments and `/* */` blocks in Solidity, JavaScript and TypeScript -- and H006 fires for a comment citing a record the index does not hold. A marker counts only at the start of a line's stripped text or preceded by whitespace, which keeps string literals, quoted headings and a URL's double slash unscanned; that boundary was chosen over a per-language tokenizer, and a reference the rule cannot see stays a documented miss. In source files the pragma is the bare `hypomnema: allow <why>` after a comment marker. The index answering both passes is the one the markdown pass builds from record file names, so a record renamed away is caught from either side. The successor frontier is the runbook shape: the contract names the three answers a runbook owes and H003 checks only that the file exists, the same existence-without-shape gap this ledger has now closed twice. |
| `hypomnema-v4.2.0` | evolution | `design-bridge-check` | `d357b0ea34c8661e7dc524e9f4f7e5a894ec11ebf2f0af8de57953ce54e79ebd` | [runbook shape study](../../../../docs/hypomnema-runbook-shape-check-study.md), [skills#318](https://github.com/wildcat-finance/skills/issues/318), [skills#314](https://github.com/wildcat-finance/skills/pull/314) | Closes the runbook-shape-check frontier. H007 now holds only Markdown files below a directory named `runbooks` to the three exact level-two headings `What fired`, `First check` and `Who to wake`, each with a non-empty body outside fenced examples. A reasoned pragma suppresses H007 only from the file's first line or the relevant heading; H000 to H006 retain their existing scope. Fixed headings won over free-form keywords, frontmatter and a second linter because the on-call answers stay visible and the existing audit command keeps owning the gate. The successor is the still-manual design bridge carried forward by skills#314: the record parser now exists, while no check proves that a shipped study's chosen design reaches one standing record without being copied into two homes. |
| `hypomnema-v4.3.0` | generation | `design-bridge-check` | `d357b0ea34c8661e7dc524e9f4f7e5a894ec11ebf2f0af8de57953ce54e79ebd` | [alert annotation study](../../../../docs/ephoros-alert-runbook-annotations-study.md), [skills#319](https://github.com/wildcat-finance/skills/issues/319) | H003 now resolves generic relative Markdown paths from `runbook:` keys in supported block-YAML, outside comments and block scalars. It does not classify alerts or require the annotation; Ephoros E004 owns presence, while H007 keeps its existing Markdown target shape. H000 to H007 keep their codes and prior cases. The held frontier revision, digest, status and next job are unchanged. |
| `hypomnema-v4.4.0` | generation | `design-bridge-check` | `d357b0ea34c8661e7dc524e9f4f7e5a894ec11ebf2f0af8de57953ce54e79ebd` | [quoted specimen study](../../../../docs/hypomnema-quoted-specimen-study.md), [skills#500](https://github.com/wildcat-finance/skills/issues/500) | The Markdown H003 pass now tells a quoted specimen from a live pointer: a `runbook:` keyword inside an inline code span is a mention and earns no finding, while the same keyword in prose is still read and still resolved, so a backticked path after a bare keyword keeps working. Spans pair per line by backtick-run length, an unmatched run stays literal text, an odd-backslash escape opens nothing, and a span crossing a line break is not read as one, which keeps every ambiguous case reported rather than silently skipped. This unblocks `audit/AUDIT.md`, whose append-only contract froze two specimen pointers that three separate rounds recorded as leads and none could fix. The keyword's position won over blanking every span, which would have retired the backticked-path form the existing pattern supports, and over a rule keyed to the audit format, which would have fixed one file rather than the reading. `audit` is deliberately not added to the required lint scope in `AGENTS.md`: that would make the ledger a permanent gate, where one future round pasting an unbackticked missing pointer breaks every later step and the append-only contract forbids the repair. That scope decision is the successor lead. H000 to H002 and H004 to H007 keep their codes and prior cases, the YAML pass is untouched, and the held frontier revision, digest, status and next job are unchanged. |
| `hypomnema-v4.5.0` | generation | `design-bridge-check` | `d357b0ea34c8661e7dc524e9f4f7e5a894ec11ebf2f0af8de57953ce54e79ebd` | [code-span guards](../../tests/test_hypomnema_checker.py), [study](../../../../docs/fiat-host-byline-readback/study.md) | H001 now passes over a relative link inside an inline code span as a quoted specimen, the reading H003 already gave a `runbook:` keyword there; a link outside a span, a link after an unmatched backtick run and a link inside a span that crosses a line break are still read and still resolved. Found when a receipted study quoting a ledger-relative link inside a code span was committed under `docs/` and the tree walk reported it as resolving to nothing. Editing the quoted line out of the receipted study and a pragma on the committed copy both lost: the receipted bytes cannot change, the committed copy must stay exact, and a pragma is an edit. H000 and H002 to H007 keep their codes and prior cases, and the held frontier revision, digest, status and next job are unchanged. |
| `hypomnema-v4.6.0` | generation | `design-bridge-check` | `d357b0ea34c8661e7dc524e9f4f7e5a894ec11ebf2f0af8de57953ce54e79ebd` | [skills#573](https://github.com/wildcat-finance/skills/issues/573) and its left-boundary regression guards | Markdown H003 now requires the `runbook:` keyword to begin the line or follow a character outside the word-and-hyphen set. Word suffixes such as `myrunbook:` and hyphenated forms such as `sub-runbook:` no longer borrow the keyword's pointer semantics, while sentence-initial, list-item and dotted `annotations.runbook:` forms remain live. The path alternatives, code-span reading, YAML pass and H000 to H002 plus H004 to H007 are unchanged. This is a generation change; the held frontier revision, digest, status and next job remain byte-identical. |
| `hypomnema-v5.6.0` | evolution | `duplicate-home-discovery` | `74714f68f73e5a2f4394b7f40d55b77055336c76a1665884e279a8325bb9eed3` | [study](../../docs/hypomnema-design-bridge-check/study.md), [runbook](../../docs/hypomnema-design-bridge-check/runbook.md), [skills#461](https://github.com/wildcat-finance/skills/issues/461) | Closes the design-bridge frontier. H008's explicit study mode reads one bounded stable study and strict Protasis selection envelope, then requires one ordered `hypomnema-design-bridge/v1` block whose candidate matches the checked selection and whose portable record path reaches one ordinary ADR or governed-skill ledger. Missing, malformed, repeated, mismatched, unsafe, dangling, wrong-home, symlinked, special and unstable inputs refuse; valid ADR and skill-ledger homes pass. The selected closed block preserves candidate identity; free-form link inference lost because it cannot prove which record belongs to the selected design. The ordinary walk and H000 through H007 retain their scope, the portable copy is synchronised, and the cold-read found no other mutable first-party Hypomnema description made stale. The frontier does not close: H008 refuses a duplicate the study declares, and skills#461's own gap statement also names a decision copied into two homes, which no check discovers when the study declares one of them. That becomes the held `duplicate-home-discovery` target. |
| `hypomnema-v5.7.0` | generation | `duplicate-home-discovery` | `74714f68f73e5a2f4394b7f40d55b77055336c76a1665884e279a8325bb9eed3` | [skills#888](https://github.com/wildcat-finance/skills/issues/888), the decision `adr/assign-adr-numbers-at-merge-not-at-authoring`, and its [study and runbook](../../../../docs/adr-merge-assignment/) | New cross-cutting records keep one bounded lowercase ASCII slug from draft through final placement and may be cited as `adr/<slug>` from Markdown or supported source comments before a number exists. H008 refuses invalid, misplaced or duplicate stable identities and H009 refuses a dangling stable reference without changing the legacy numbered forms. The native-Git assignment helper derives consecutive numbers above the exact base maximum, ignoring holes and sorting at most 32 slugs by ASCII bytes; it changes only each path and exact first heading, preserves modes and remaining bytes, and binds the transform to one canonical report that plan, apply and replay must reproduce. Replacement objects, inherited Git repointing, shallow or wrong-typed objects, moved bases, hostile paths, drift, dirty worktrees and partial mutation refuse with bounded codes. This is a generation change: the `duplicate-home-discovery` frontier, digest, status and next job remain byte-identical. |
| `hypomnema-v5.8.0` | generation | `duplicate-home-discovery` | `74714f68f73e5a2f4394b7f40d55b77055336c76a1665884e279a8325bb9eed3` | [skills#1247](https://github.com/wildcat-finance/skills/issues/1247) and its [study and runbook](../../../../docs/harness-roster-freshness/) | Repairs the study bridge at the draft-to-final lifecycle boundary. Study mode now accepts the repository's one pre-integration ADR home only when it is a directly nested `docs/decisions/drafts/<slug>.md` with a valid stable slug, `# Decision: <title>` heading, five decision sections and dated status. The issue-1247 study exposed the contradiction: authoring rules required that draft while H008 accepted only a numbered ADR or governed ledger. A focused guard was observed red on the old classifier and green after the repair; malformed, invalid-slug and nested drafts refuse, and the existing numbered-ADR, governed-ledger, duplicate, unsafe-path and wrong-home results remain unchanged. This is a generation change: the `duplicate-home-discovery` frontier, digest, status and next job remain byte-identical. |

## Ledger boundary

A generation row records a wider implementation without moving the held
frontier revision or digest.
