Checkpoint authority fixture home.

The manifest names the selected candidate and four pending conformance gates.
Its empty `cases` and `implemented_criteria` arrays explicitly mean that no
protocol case ran. They cannot produce a passing conformance report.
Later implementing steps add source-bound golden and hostile cases with their
observed results. The historical selection model remains in the public study.
