# Ephoros evolution ledger

Policy: [../VERSIONING.md](../VERSIONING.md)

## Held frontier

- Current version: `ephoros-v2.2.0`
- Frontier status: `open`
- Frontier revision: `span-kinded-index`
- Current frontier: Five rules are executable: E001, E002 and E003 read Python and the TypeScript surface, E004 reads the supported block-YAML subset, and E005 reads Python, supported block-YAML label keys and the TypeScript surface, every TypeScript reading through the shared masked lexer under the 1 MiB fail-closed boundary. The lint runs clean over this marketplace. Over the pinned application clone `wildcat-app-v2` at `564a189b` E001 reports 14 interpolated logger messages, true findings rather than suppressed ones, and E002 and E003 report zero because that tree holds no label container and no mean over a duration, an absence of surface rather than evidence that the rules found it clean.
- Next Fiat job: Thread the lexer's span kinds through the per-file TypeScript index so a recogniser can tell a comment span from a string span, then repair the two families that gap leaves open: E001's concatenation half reads a `+` in the mask beside a quote anywhere in the raw first argument, so `logger.info(a + b /* it's fine */)` fires with no string literal as an operand and an escaped `\${` reads as interpolation; and a string key or string label element preceded by a comment inside the same item, `{ /* c */ "walletAddress": w }` or a `// note` line above `"requestId"` in a `tags:` array, is unrecognised by E002 and E005 and fails toward silence. Accepted when each of those four shapes is held by a TypeScript fixture observed red before the fix, the first two clean and the last two reporting E005 and E002 at the key's own line with a reasoned pragma beside it suppressing, while `logger.info("took " + ms)` still fires, the E001 count over the pinned clone at `564a189b` stays 14, a cap-scale specimen through the widened index is measured before and after against the 2,000 ms budget, the lint stays clean over this marketplace, and both suites pass.
- Sources: [../../../../SOURCES.md](../../../../SOURCES.md)

## History

- `ephoros-v0.1.0` | baseline | `emitted-signal-contract` | `84e954fec8e47067179c8005120b0d5b689f5c89d0216ecfe4d56c0978f885ab` | [phylax secrets rule](../phylax/SKILL.md) | Ephoros starts here, holding the telemetry a step leaves behind once it runs unattended.
- `ephoros-v0.2.0` | generation | `emitted-signal-contract` | `84e954fec8e47067179c8005120b0d5b689f5c89d0216ecfe4d56c0978f885ab` | [alert annotation study](../../../../docs/ephoros-alert-runbook-annotations-study.md), [skills#319](https://github.com/wildcat-finance/skills/issues/319) | E004 now reports each supported block-YAML alert list entry without its own nested `annotations.runbook` Markdown path. Comments, block scalars, top-level pointers and neighbouring entries do not create or satisfy the annotation, and the existing reasoned pragma remains the only exception. Ephoros stops at presence: Hypomnema owns resolution and target shape. The held frontier revision, digest, status and next job are unchanged.
- `ephoros-v1.2.0` | evolution | `typescript-rule-parity` | `02ce0a73818c7a6c3bd9ff967c217b0172312b64a0f7b1776c4c1649bbe66fd7` | [wallet-address telemetry study](../../../../docs/ephoros-wallet-address-telemetry-study.md), [skills#322](https://github.com/wildcat-finance/skills/issues/322) | Closes the emitted-signal-contract frontier. E005 reports an address used as a metric label, a dashboard key or a log index, in Python, in supported block-YAML label mappings and in TypeScript read through the shared masked lexer under phylax's audited input boundary: the 1 MiB cap before lexing, E000 fail-closed, no execution of inspected source. Address-shaped metric labels move from E002 to E005 under a guard test, so one concern carries one code, and the reasoned pragma gains its `//` line-comment form. The ephoros/phylax line over shared TypeScript files is recorded in [ADR-010](../../../../docs/decisions/ADR-010-split-address-telemetry-from-boundary-control.md). Three limits are stated with the rule: the message says wallet address for any address-fragment key such as `ip_address`, recognition under a YAML `labels:` mapping is direct-children-only, and the `s?` suffix family shared with E002 misses `-es` plurals such as `addresses`. The successor is the parity gap this run leaves in hand: the TypeScript surface is read for one rule while E001 to E003 read Python only, and the pinned clone holds a live interpolated logger message an E001 analogue would have to judge.
- `ephoros-v2.2.0` | evolution | `span-kinded-index` | `d1d922633d2121067b6e74c5b667f820fea74f7f4d454ee935ebc34af959294e` | [TypeScript rule parity study](../../../../docs/ephoros-typescript-rule-parity-study.md), [skills#1420](https://github.com/wildcat-finance/skills/issues/1420) | Closes the typescript-rule-parity frontier. E001, E002 and E003 now read the TypeScript surface through the same masked lexer and per-file span index E005 already used, behind the unchanged boundary: the 1 MiB cap before lexing, E000 fail-closed, no execution of inspected source, and a reasoned `//` pragma the only exception. E001 fires on an interpolated template literal or a concatenation involving a string literal; a template literal with no `${}` is a constant string and stays clean, where Python's E001 fires on a placeholder-free f-string, and that divergence is decided in `adr/keep-a-constant-template-literal-outside-e001`. E002 fires on an unbounded key in a label container, leaving address-shaped keys to E005, with each key split into words at a case change and at each `_` and matched against the Python fragments as whole words, so every key carrying `id` as a word fires here, `chainId` as much as `requestId`, and plurals such as `hashes` are words of their own where the Python `s?` suffix passes them. E003 fires on a mean over a duration, a `mean`, `average`, `avg` or `fmean` call or a `reduce` divided by a `.length` standing as the whole right-hand side of a declaration or assignment under a duration word, built as one gate on the existing bracket walk and measured linear across 18 specimen shapes to 1 MiB. The verdict over `wildcat-app-v2` at `564a189b`, the first non-clean verdict this checker records over a real tree: E001 reports 14 findings from 14 of its 21 logger call sites, each a true interpolated message and none suppressed, `useGetLenders.ts` at line 29 among them; E002 and E003 report zero, and both zeros are absence of surface, since the clone holds no label container and no mean over a duration, not evidence that the rules found it clean. The checker module grew from 115 to 160 cases. The label follows the contract's evolution arithmetic from `ephoros-v1.2.0`; the run's runbook wrote a generation token for this row and the arithmetic corrects it here. The successor is the lead every round of this run carried as S2-R1-02: the span kinds are not threaded into the index, so E001's concatenation half reads proximity rather than operands and a comment before a string key or element hides it from E002 and E005.

## Ledger boundary

A generation row records a wider implementation without moving the held
frontier revision or digest.
