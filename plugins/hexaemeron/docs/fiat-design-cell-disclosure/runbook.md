# Disclose a failed integration design cell

Assuming, unless corrected:

1. The receipted study is `.hexaemeron/study.md`, SHA-256 `870a90cd0bed82d1f44a4031c950b65e1d0a14a6658984214d26191c4cf6ce11`, written from starting commit `32a70f9cc3c8b14ed2001dcf9e2e7821b8c112a9` on `main`. Its section 4 selects `disclosed-refusal`; its fifteen risk-register ids govern every audit round.
2. The installed Hexaemeron plugin `1.6.70` is this run's controller for the whole run. No product edit replaces it, so no receipt of this run uses the disclosure route the run builds. That fact is stated again in the run pull request.
3. The study declares no success-criteria fence and no known-failure inventory fence, so the controller takes its no-criteria and implementation-first paths: every step opens at `implement`, no `run-exit` observation is owed, and the step's suites are proved by the Exit commands below and by the controller's own suite binding at `done implement`.
4. Python is `3.14.6` with the standard library only, Git and `gh` are authenticated, and the pinned cosign `3.1.3` binary sits at the path the study names. Every suite runs with `NO_COLOR=1` and `CHECKPOINT_COSIGN` exported as section 3 of the study states.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 600ee2050a5abbfecb4a0a6188f14f3d091f36f01875e006850c61f6d6cc7536
candidate | disclosed-refusal
```

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
protasis | plugins/hexaemeron/skills/protasis/EVOLUTION.md | next-generation-after-integration-base
```

The record carries one conformance cell per candidate, `refusal-guards-green`, blocking `step:4`. Only the selected candidate's cell is consumed. Its pinned resolver is the Step 1 wrapper running the Step 3 guard module; the operator runs that resolver once, after Step 3's audit closes and before the `done push` that closes Step 3, so the report exists when that push checks `step:4`. No step's Exit produces that report, and Mason and Warden never run the resolver.

Every step changes `plugins/hexaemeron/`, so every step raises the package version above its own pull request base: Step 1 to `1.6.72`, then `1.6.73`, `1.6.74`, `1.6.75` and `1.6.76`. Before each push the operator re-checks every remote ref's `.claude-plugin/marketplace.json` and takes a higher number if one is claimed. Each rise edits `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, the hexaemeron entries in `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py`; these six paths are the version surfaces named in each Files field.

Every step that edits `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` or `plugins/hexaemeron/tests/test_protasis_design_evidence.py` walks the digest cascade in order: `tests/promise_machine_coverage.json`, then `docs/promise-machine/obligation-gates/evaluation-run.json` re-tallied from the confined answers with the recorded model string and date and never from a model run, then `docs/promise-machine/obligation-gates/integration-projection.json` and `docs/promise-machine/obligation-gates/integration-projection.md`, then the Horos census. `docs/promise-machine/obligation-gates/demonstration-run.json` and `docs/promise-machine/obligation-gates/demonstration-evidence.md` are preserved records and are not edited. These are the cascade files named in each Files field.

Before every commit, after staging, run the Horos boundary scan with write and the census scan with write, so `.horos/boundary.json`, `.horos/candidates.json` and `.horos/census.json` match the staged tree. Every file under `audit/` and `plugins/hexaemeron/audit/` is prefix-pinned: the run appends its own rounds and edits no existing byte. `plugins/hexaemeron/skills/fiat/checkpoint-authority/native-profile.json` is frozen. No inherited decision record changes a byte, and the new decision stays an unnumbered draft until the allocator numbers it at integration.

Author run commits as `Shoggoth <shoggoth@wildcat.finance>`, commit them as `Dr Laurence E. Day <laurence@wildcat.finance>`, sign with the configured OpenPGP key of fingerprint `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, verify each commit locally before receipt, and end each commit message exactly once with `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`. Prose that ships goes through Sapheneia, Imprimatur, Vulgate with protected-content comparison, then Imprimatur again; Brevitas applies where its contract includes the record. Generated and digest-bound bytes stay exact. Ask before widening the disclosure to a `step:N` cell, touching `hexctl amend`, `halt` or `reset`, or changing the standing decision record's bytes; the study rules each of those out.

## Step 1: Scaffold the delivery and ship the report wrapper

**Goal.** Commit the receipted specification, author the draft decision, and add the Protasis report wrapper with its guard tests, so a resolver's report and the controller's object can never share a path again.

**Entry.** The run branch `fiat/1524-integration-blocked-design-cell` at `32a70f9cc3c8b14ed2001dcf9e2e7821b8c112a9`, the study and design record receipted, no product change yet. The step branch is cut from the run branch by the controller.

**Exit.** Byte-identical copies of the study, this runbook, the design record, its twenty-eight reports and the probe sit under the delivery's documentation directory named in Files, each equal to its receipted original under cmp. The draft decision exists at the path named in Files with a Decision heading, five sections and a dated status, derived from the Surveyor's candidate under the design directory of the run state. The wrapper named in Files runs a list argv with no shell under a 900 second timeout and a 1 MiB captured-output cap, writes one closed design report whose exit is the child's exit and whose command is the quoted argv, derives the value from the exit or from one key of a JSON file the child wrote, and refuses an output path that already exists, is a symlink, lies outside the record's directory or equals any argv element. The package version rises to the Step 1 number on all six version surfaces. The proving commands are `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study plugins/hexaemeron/docs/fiat-design-cell-disclosure/study.md --design-evidence plugins/hexaemeron/docs/fiat-design-cell-disclosure/design-evidence.json --repo-root .`, `python3 scripts/run_checks.py --scope root --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-1-checks.json` and `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/step-1-hexaemeron.json`. All three exit zero.

**Files.** Add `plugins/hexaemeron/docs/fiat-design-cell-disclosure/study.md`, `plugins/hexaemeron/docs/fiat-design-cell-disclosure/runbook.md`, `plugins/hexaemeron/docs/fiat-design-cell-disclosure/design-evidence.json`, `plugins/hexaemeron/docs/fiat-design-cell-disclosure/reports/` with the twenty-eight report files, `plugins/hexaemeron/docs/fiat-design-cell-disclosure/probe.py`, `docs/decisions/drafts/disclose-a-failed-integration-design-cell.md`, `plugins/hexaemeron/skills/protasis/scripts/design_report.py` and `plugins/hexaemeron/tests/test_protasis_design_report.py`. Update the six version surfaces and the three Horos files. Touch no other product file.

**Tests.** A new module with one case per wrapper behaviour: a passing child writes a closed report with exit zero and the quoted command; a failing child records its exit and the checker later reads it as a fail; the value is taken from the exit, and separately from a named key of a child-written JSON file; an output path equal to any argv element refuses before the child runs; an existing output, a symlink, and a path outside the record directory each refuse; a child exceeding the timeout is recorded as a non-zero exit with the cap and timeout named; captured output above 1 MiB is truncated and marked. Expect about ten cases, each failing without the wrapper. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/elenchus-step-1.json`.

**Disciplines.** phylax: the wrapper spawns a child from operator-supplied argv and writes a file, so list argv, no shell, timeout, output cap and the output-path refusals are its boundary. ephoros: none, every command here runs at a terminal inside one invocation and emits nothing unattended. metron: none, no performance claim is made in this step. elenchus: none, no failure is in hand; the wrapper's refusals each get a guard as the study's section 11 requires. hypomnema: the draft decision is authored here at the home section 12 of the study names.

## Step 2: Add the disclosure contract to the design-evidence checker

**Goal.** Teach the Protasis checker one closed disclosure object that admits a failed integration cell against its locked threshold, and record the Protasis generation row.

**Entry.** The Step 1 head, green, audited and pushed. The Step 1 design transition passed at the push that opened this step.

**Exit.** The checker reads one or more disclosure flags at the integration transition only, each naming a record-relative path to a closed disclosure object with exactly the ten fields the study's section 4 names, at most 64 KiB, a non-symlink regular file below the record's directory, with a reason of at most 4,096 printable bytes and no newline. An admitted disclosure removes that cell's D008 and lists the cell in a receipt field named disclosed with candidate, criterion, report path, report digest and disclosure digest; every other D008 stands. A disclosure that is malformed, misplaced, digest-mismatched against the record or the report, names a passing cell, a cell not due at integration, a non-selected candidate, or arrives at another transition refuses under a new code D009. More than thirty-two flags refuse. Without a flag the checker's receipt is byte-for-byte today's five-key receipt. The Protasis skill document gains one paragraph in its design-evidence section naming the disclosure, D009 and the rule that a resolver's own report path and the controller's object path differ, with the wrapper as the route; the Protasis ledger gains one generation row and the skill metadata version moves with it. The before and after real-time measurement of the checker at integration with one disclosure is recorded in the budget file named in Files and stays at or under 1,000 milliseconds. The digest cascade is re-pinned and the package version rises to the Step 2 number. The proving commands are `python3 scripts/run_checks.py --scope root --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-2-checks.json` and `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/step-2-hexaemeron.json`. Both exit zero.

**Files.** Update `plugins/hexaemeron/skills/protasis/scripts/design_evidence.py`, `plugins/hexaemeron/tests/test_protasis_design_evidence.py`, `plugins/hexaemeron/skills/protasis/SKILL.md` and `plugins/hexaemeron/skills/protasis/EVOLUTION.md`. Add `plugins/hexaemeron/docs/fiat-design-cell-disclosure/budget.md`. Update the four cascade files, the six version surfaces and the three Horos files.

**Tests.** Extend the existing eleven cases with one per behaviour: an admitted disclosure clears exactly its D008 and appears in the disclosed field; the receipt without a flag equals the current five-key receipt byte for byte; a missing field, an extra field, a wrong schema, a record digest mismatch, a report digest mismatch, a value or threshold that disagrees with the report or criterion, a passing cell, a cell blocking a step, a non-selected candidate, a disclosure passed at a step transition or at design lock, a symlink, a file above 64 KiB, a reason with a newline, and a thirty-third flag each refuse D009 or their existing code, and none of them changes the record or any report. Expect about twenty-six cases in the module. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/elenchus-step-2.json`.

**Disciplines.** phylax: the disclosure is a hand-written JSON file the checker reads, so the closed key set, size cap, path confinement, digest comparison and printable reason are its controls. ephoros: none, the checker is a terminal subprocess and its receipt already carries what a reader asks. metron: the 1,000 millisecond budget from section 10 of the study is measured before and after this step's change with the command that section names, and the two readings are recorded. elenchus: none, no failure is in hand; each refusal gets a guard. hypomnema: the resolver-and-report separation rule lands in the Protasis skill document because it is a contract rule, and the decision home is the Step 1 draft.

## Step 3: Add hexctl disclose design and replay it at integration

**Goal.** Give Fiat a receipted command that records a disclosure, passes it to the checker at the integration transition, and replays it under verify, without changing the record, the reports, the design-lock block or any earlier receipt.

**Entry.** The Step 2 head, green, audited and pushed. The Step 2 design transition passed at the push that opened this step.

**Exit.** The new command, disclose design, takes a cell as candidate slash criterion, a report path and a reason. It is accepted only when the run carries the design-evidence contract marker, is in the integrate phase before the final merge-step is receipted, is not halted, a dry run of the checker at integration reports D008 for exactly that cell, and no disclosure for that cell exists. It writes the disclosure below the design-disclosures directory of the run state through a temporary file and rename, appends candidate, criterion, path and digest to the study receipt's disclosures list, and commits one ledger event named design colon disclose carrying the closed object; a refusal writes nothing. The integration transition verifies each recorded disclosure's digest and passes it to the checker, and its receipt carries the disclosed cells. Verify replays the transition with the same flags and refuses a disclosure present in state without its event or present in the ledger without its state entry. The existing five-key checker receipt and the new receipt with the disclosed field are both admitted. Every step, design-lock and record digest check is unchanged. The digest cascade is re-pinned and the package version rises to the Step 3 number. After this step's audit closes and before its push, the operator runs the design record's pinned resolver for the selected candidate's conformance cell, which wraps this step's guard module and writes the report at the path the record names; this step does not produce that report. The proving commands are `python3 scripts/run_checks.py --scope root --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-3-checks.json` and `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/step-3-hexaemeron.json`. Both exit zero.

**Files.** Update `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`. Add `plugins/hexaemeron/tests/test_hexctl_design_disclosure.py`. Update the four cascade files, the six version surfaces and the three Horos files.

**Tests.** A new module importing the controller the way the existing hexctl tests do, with one case per behaviour: the positive path writes the file, the state entry and the ledger event, and the integration transition then receipts the disclosed cell; a run without the contract marker, a halted run, a run in a step phase, a run whose final merge-step is already receipted, a cell whose dry run reports no D008, a cell blocking a step, a non-selected candidate, a second disclosure of the same cell, a reason above 4,096 bytes or with a newline, and a missing report each refuse and leave the disclosures directory, state and ledger unchanged; verify refuses a state entry whose event is absent and an event whose state entry is absent; the design-lock, record and report digest checks still refuse drift exactly as before. Expect about fifteen cases. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/elenchus-step-3.json`.

**Disciplines.** phylax: operator text lands on the ledger and a file is written, so the reason bounds, the temporary-file-and-rename write and the phase and marker refusals are its controls. ephoros: the ledger event answers which cell was disclosed, against what and why, the first question in section 8 of the study. metron: none, the command runs once per disclosed cell at a terminal. elenchus: none, no failure is in hand; each refusal gets a guard. hypomnema: none beyond the Step 1 draft, whose decision this step implements.

## Step 4: Surface disclosures in next, status and the integrate body gate

**Goal.** Make the disclosed cells visible from status and the integrate directive, require the run pull request body to repeat them, update the Fiat skill document and its promise, and record the Fiat generation row.

**Entry.** The Step 3 head, green, audited and pushed. The push that opened this step consumed the selected candidate's conformance report at the step 4 design transition.

**Exit.** Status prints one DESIGN line per disclosed cell. The integrate directive from next carries one design-disclosures row per disclosed cell. When the integration transition receipt lists disclosed cells, done integrate requires the run pull request body to carry a Design evidence heading with exactly one row per cell in the shape the study's section 4 gives, refuses extra or missing rows and refuses the heading when no cell was disclosed; the receipt records the rows. A run without the contract marker is unchanged. The Fiat skill document gains one paragraph after byte 29736 and before its amending section naming the route, leaves its lines 415 to 418 byte for byte, and its design-evidence promise stanza names disclosure in its refusals and recovery; the promise history file and the two obligation-gate records that name it are re-pinned, the agent-instruction manifest is re-pinned and the reconciliation proof script is run against the base commit. The Fiat ledger gains one generation row, the skill metadata version moves with it, the checkpoint-compatible controller version set gains the new label, and the runtime fixture that pins the ledger digest is re-pinned. The digest cascade is re-pinned and the package version rises to the Step 4 number. The proving commands are `python3 scripts/run_checks.py --scope root --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-4-checks.json` and `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/step-4-hexaemeron.json`. Both exit zero.

**Files.** Update `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `plugins/hexaemeron/tests/test_hexctl_design_disclosure.py`, `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/hexaemeron/skills/fiat/EVOLUTION.md`, `tests/promise_machine_id_history.json`, `tests/fixtures/agent-instruction-v1/manifest.json`, `tests/fixtures/promise-machine/runtime/fiat-final-integration.json`, the four cascade files, the six version surfaces and the three Horos files.

**Tests.** Extend the Step 3 module with one case per behaviour: status prints the DESIGN line for a disclosed cell and none otherwise; the integrate directive carries the rows; done integrate accepts a body whose rows equal the recorded disclosures, refuses a missing row, an extra row, a changed digest or reason, and the heading with nothing disclosed; a run without the marker takes the unchanged path. The existing skill-metadata, promise-history and evolution-contract tests pass on the moved versions. Expect about eight new cases. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/elenchus-step-4.json`.

**Disciplines.** phylax: the pull request body is text read from the recorded path through the bounded reader that already serves the carried-forward section, with a fixed row grammar and exact set equality. ephoros: the status line and the directive rows answer the second question in section 8 of the study, whether the run pull request said so. metron: none, both surfaces read state already in memory. elenchus: none, no failure is in hand; each refusal gets a guard. hypomnema: the skill paragraph names the route and the ledger row records the generation; the decision home stays the Step 1 draft.

## Step 5: Demonstrate a disclosed integration on a disposable run

**Goal.** Drive the checked-in controller through a disposable Git-backed run whose integration cell fails and reaches done integrate only through a disclosure, and record the observations.

**Entry.** The Step 4 head, green, audited and pushed. The Step 4 design transition passed at the push that opened this step.

**Exit.** A demonstration case in the Step 3 module builds a disposable Git-backed fixture with fixture-only GitHub and remote boundaries, runs the checked-in controller from this tree through init, study, runbook, one step and its merge-step with a record whose integration cell fails, observes D008 at that merge-step, runs disclose design, observes the merge-step receipt with the disclosed cell, and observes done integrate accepting a body with the matching row. The same module records the six refusals the study's section 1 names: an undisclosed D008, a drifted report digest, disclosure of a passing cell, disclosure of a step cell, disclosure on a legacy state, and body rows that do not match. The proof file named in Files records the controller source digest and commit, the positive path and each refusal with its observed message, and claims no sufficiency beyond those observations. If this runbook was amended, its committed copy is re-copied and equals the receipted runbook under cmp. The package version rises to the Step 5 number. The proving commands are `python3 scripts/run_checks.py --scope root --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-5-checks.json` and `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/step-5-hexaemeron.json`. Both exit zero.

**Files.** Update `plugins/hexaemeron/tests/test_hexctl_design_disclosure.py`. Add `plugins/hexaemeron/docs/fiat-design-cell-disclosure/proof.md`. Re-copy `plugins/hexaemeron/docs/fiat-design-cell-disclosure/runbook.md` when an amendment moved the receipted runbook. Update the six version surfaces and the three Horos files.

**Tests.** The demonstration case and six refusal cases above, each asserting the ledger event, the receipt field and the refusal message it observes, plus one case that the proof file names the controller digest the fixture ran. Expect seven or eight new cases. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/elenchus-step-5.json`.

**Disciplines.** phylax: the fixture confines the controller to a temporary repository with fixture-only GitHub and remote boundaries and no network. ephoros: none, the demonstration runs at a terminal and its observations are recorded in the proof file. metron: none beyond the Step 2 recording; the demonstration makes no time claim. elenchus: none, no failure is in hand. hypomnema: the proof file is the record of the demonstration, at the home the study's section 1 names.

At integration the run pull request closes wildcat-finance/skills#1524, states that this run's own controller predates the route it built, and carries one carryover row for each still-open item the study's section 3 rules out, pointing at the issue that already holds it, with a stated reason for anything that earns no issue.
