# Imprimatur causal subject has no formula runbook

## Step 1: Add and demonstrate the causal subject has no family

**Goal.** Teach Imprimatur to report the bounded causal subject `has no` formula, repair the two public Atlas specimens, and preserve the existing rule, masking, evaluation, and frontier boundaries.

**Entry.** Start from run branch `fiat/teach-imprimatur-the-causal-subject-has-no-formu` at `fc0374bcd2d4311a2ce7d1f710e6809e40f00c92`, with the receipted study in `.hexaemeron/study.md`, focused Imprimatur baseline `112/112`, and the exact user specimen observed at score 100.0 with zero defects.

**Exit.** `structural.json` contains one medium, scored `causal_subject_has_no` family matching `because`, one to eight punctuation-free word tokens, then `has no`, including ordinary line wrapping. Focused tests prove the exact user specimen, bounded variants, original coordinates in a supported source comment, quote masking, and clean controls. The two public Atlas passages retain Cline, Roo Code, the manual route, and the absence of a built and checked hand-off without retaining the reported formula. `SKILL.md`, the lexicon rationale, and the Imprimatur generation row explain the diagnostic without claiming factual error or universal reading cost. The skill advances to `imprimatur-v2.4.0`; the held frontier fields and every byte below `evals/labelled-prose-v1/` remain unchanged. Hexaemeron package version 1.6.6 reaches both manifests, both marketplace listings, and the root assertion unless a newer compatible base version must be retained during integration. Copies of the receipted study and runbook ship under `plugins/hexaemeron/docs/imprimatur-causal-subject-has-no/`, providing Step 1's scaffold and the final demonstration in this one-step run. Prove the exit with `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 -m unittest discover -s tests`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 -m unittest tests.test_evolution_contract tests.test_version_propagation`, `python3 scripts/promise_machine.py check`, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, Imprimatur over every changed prose file, Brevitas over the changed public engineering prose but not the completeness-oriented study and runbook, and `git diff --check`. The exact specimen piped to `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py - --format json --max-defects 0` must exit 1 with one medium `causal_subject_has_no` defect; `No checked Atlas hand-off exists for them, so they stay on the manual route.` must not report that family.

**Files.** Change `plugins/hexaemeron/skills/imprimatur/lexicon/structural.json`, `plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `tests/test_shipped_prose_lints.py`, `plugins/hexaemeron/skills/imprimatur/SKILL.md`, `plugins/hexaemeron/skills/imprimatur/references/lexicon-rationale.md`, `plugins/hexaemeron/skills/imprimatur/EVOLUTION.md`, `README.md`, `docs/how-to-help-shoggoth.md`, `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, and `tests/test_version_propagation.py`. Add `plugins/hexaemeron/docs/imprimatur-causal-subject-has-no/study.md` and `plugins/hexaemeron/docs/imprimatur-causal-subject-has-no/runbook.md`. Change `.horos/boundary.json` only if the required Horos scan proves its tracked-tree evidence is stale.

**Tests.** Add focused positive examples for the exact public sentence, a generic singular subject, a plural subject, mixed case, a line-wrapped clause, and a supported-source comment. Add clean controls for the direct rewrite, standalone `has no`, `there is no`, `lacks`, punctuation before `has no`, quoted discussion, code or string content, and a subject longer than eight tokens. Add a root prose-gate assertion that the new family is active rather than fixture-only. The Elenchus runner contract is test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, and report file `.hexaemeron/elenchus/step-1.json`. Run the complete command set in Exit and record the fixed-tree counts rather than predicting them.

**Disciplines.** phylax: the existing caller-prose and source-extraction boundary remains bounded, with no new dependency, command, host, credential, or path use. ephoros: none, since this synchronous terminal lint adds no retained telemetry or unattended path. metron: none, since the change makes no speed claim and the regular expression has a fixed token bound. elenchus: the exact user specimen is the parent-red guard and every audit repair must retain a failing parent witness. hypomnema: the `imprimatur-v2.4.0` ledger row is the standing record for the chosen family, rejected broad expression, parser alternative, and frozen-evaluation boundary.
