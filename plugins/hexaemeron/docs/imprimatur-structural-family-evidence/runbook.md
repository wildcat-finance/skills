# Imprimatur structural prose family evidence catalogue runbook

Derived from the receipted study. Four steps, dependency order, one pull request each, stacked on run branch `fiat/1298-imprimatur-structural-prose-family-evidence` cut from `7d12d63e13fe193fcc1f8827b393f8aa51161731`. The selected design is `jsonl-fixture`: a fixture directory `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/` with `families.jsonl`, `specimens.jsonl`, two schemas, a README, a stdlib checker and a unittest module. The lint script, the three lexicon files, `EVOLUTION.md` and `evals/labelled-prose-v1/` stay byte-identical throughout.

```design-lock
schema | protasis-design-evidence/v1
sha256 | f96f2fb27c16da48ef639eae8e9f41caf6d2f40c6fa7fe8948848c91628746dc
candidate | jsonl-fixture
```

## Step 1: Scaffold the fixture and commit the specification

**Goal.** Create the fixture layout, its two row schemas and its README of decisions, and commit the accepted study and runbook beside the skill, without adding any specimen or checker yet.

**Entry.** Run branch `fiat/1298-imprimatur-structural-prose-family-evidence` at `7d12d63e13fe193fcc1f8827b393f8aa51161731`, study and runbook receipted, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py` reporting `112/112 passed`, `python3 plugins/hexaemeron/tests/run_tests.py` and `python3 scripts/run_checks.py` green.

**Exit.** The directory `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/` exists with `README.md`, `schemas/family.schema.json` and `schemas/specimen.schema.json`. The README records the three expensive decisions from study item 12 (fixture location and row schemas; selection rule, seed string `imprimatur-structural-family-evidence-v1` and universe; tier minimums), the 42-family count with the five non-family observations, the frozen SHA-256 of `scripts/imprimatur.py`, the three lexicon files and `EVOLUTION.md` at the starting ref, and the statement that `EVOLUTION.md` does not change. Both schemas declare every field named in study item 4 for their row. The study and runbook are committed at `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/study.md` and `runbook.md`. The existing repository layout, `.python-version`, `.github` checks and `LICENSE` remain the scaffold; nothing is duplicated. Prove the exit with `python3 -c "import json; json.load(open('plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/family.schema.json')); json.load(open('plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/specimen.schema.json'))"`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md plugins/hexaemeron/docs/imprimatur-structural-family-evidence/study.md plugins/hexaemeron/docs/imprimatur-structural-family-evidence/runbook.md --max-defects 0`, `sha256sum plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/skills/imprimatur/lexicon/hard.json plugins/hexaemeron/skills/imprimatur/lexicon/gated.json plugins/hexaemeron/skills/imprimatur/lexicon/structural.json plugins/hexaemeron/skills/imprimatur/EVOLUTION.md` matching the README's frozen values, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, and `python3 scripts/run_checks.py`.

**Files.** Add `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/family.schema.json`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/specimen.schema.json`, `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/study.md` and `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/runbook.md`. Change `tests/check-map-v1.json` only if `python3 scripts/run_checks.py` refuses the new paths as unowned; register the fixture path and nothing else.

**Tests.** No new test module in this step; the two schemas are checked by loading them and the prose by the Imprimatur lint. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-1.json`. Run the Imprimatur suite, the Hexaemeron suite and the root check runner named in Exit and record the exact counts the clean tree reports.

**Disciplines.** phylax: none, this step adds JSON schemas and prose only, opens no input path and runs no subprocess. ephoros: none, nothing here runs unattended. metron: none, no performance claim is made. elenchus: none, no failure is in hand. hypomnema: the fixture location, row schemas, selection rule and tier minimums are the decisions study item 12 names, and the README is their home, matching where v1 recorded the same three.

## Step 2: Write the checker, its tests and the 42-family catalogue

**Goal.** Ship `check_family_evidence.py`, its unittest module and `families.jsonl` carrying every family from issue #1298 with the issue's own wording, so that a malformed or truncated fixture is refused before any specimen exists.

**Entry.** Start from the controller-recorded Step 1 commit with its Exit commands green and no unreceipted change to the accepted study or runbook.

**Exit.** `plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py` reads only files below `--fixture`, refuses a symlink, a file over 1,048,576 bytes, an unreadable JSON line, a row failing its schema, a duplicate `family_id`, an unknown `evidence_tier`, a specimen naming an unknown family, a span outside its UTF-8 text or splitting a codepoint, a `text_sha256` that does not match, `annotated_before_lint` false, and two positives sharing a `source_group_id`; it exits 0 on a clean fixture, 1 on findings, 2 on bad invocation, opens no socket unless `--verify-sources` is passed, and writes one JSON report with `--report` carrying `families`, `specimens`, `below_minimum` and `rejections_path`. `--allow-below-minimum` turns a tier-minimum shortfall from a finding into a report entry and exists for the build phase only. `families.jsonl` carries 42 rows whose `form`, `reader_cost`, `direct_rewrite`, `boundary` and `disposition` are copied from the issue body, with `evidence_tier` `high-value` for the five named families, `signal` for the eight named families, and `boundary`, `existing-family` or `future` for the rest, and `minimum_positive` and `minimum_negative` set from the tier. Prove the exit with `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1 --allow-below-minimum --report /tmp/family-evidence-step2.json` exiting 0 and `python3 -c "import json; r=json.load(open('/tmp/family-evidence-step2.json')); assert r['families']==42 and len(r['below_minimum'])==13, r"` exiting 0, `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1; test $? -eq 1` exiting 0, `python3 -m unittest discover -s plugins/hexaemeron/tests -t plugins/hexaemeron -p test_imprimatur_family_evidence.py`, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, and `python3 scripts/run_checks.py`.

**Files.** Add `plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/families.jsonl`, an empty `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/specimens.jsonl`, and `plugins/hexaemeron/tests/test_imprimatur_family_evidence.py`. Change `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md` to document the checker's commands and exit codes.

**Tests.** `test_imprimatur_family_evidence.py` carries one `test_refuses_<condition>` per refusal listed in Exit, one clean-fixture test, one test that the 42 `family_id` values and the five copied fields equal the level-three headings and their lines in a checked-in copy of the issue text at `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/issue-1298.md`, and one test that the frozen digests in the README equal the current files. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-2.json`. Record the exact new count the Hexaemeron suite reports rather than predicting it.

**Disciplines.** phylax: the checker reads untrusted JSONL from the fixture directory, so paths resolve only below `--fixture`, symlinks and oversized files are refused, text is handled as bytes and never evaluated or passed to a shell, and no socket opens without `--verify-sources`. ephoros: the `--report` JSON is the one signal a later run reads, answering which families still lack their minimum and why; no event, metric or alert is added. metron: none, the budget in study item 10 is measured in Step 4 on the full fixture. elenchus: none at entry; any refusal found wrong mid-step is reproduced with the exact fixture bytes, reduced to one row and guarded by a new `test_refuses_<condition>`. hypomnema: the exit-code contract and the `--allow-below-minimum` build flag are recorded in the README, the interface v2 reads; no separate decision record is earned.

## Step 3: Collect and annotate specimens for the 13 target families

**Goal.** Fill `specimens.jsonl` with at least two independent positives and two negatives for each high-value family and two positives and one negative for each signal family, drawn from the v1 universe by the study's deterministic selection rule, annotated before any lint runs and provenance-bound to immutable GitHub objects.

**Entry.** Start from the controller-recorded Step 2 commit with its Exit commands green and no unreceipted change to the accepted study or runbook.

**Exit.** `plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py` is the executable form of the study's selection rule: it takes the pinned default-branch heads and the merged skills pull request and issue set as arguments, excludes the 16 v1 source groups, Imprimatur's own files and issue #1298, finds candidate paragraphs by each family's `discovery_phrases`, orders them by `sha256("imprimatur-structural-family-evidence-v1" || source_url || text)`, fetches text only through `gh api` at pinned commits, writes every rejected candidate with its reason to `selection-rejections.jsonl`, and writes `specimens.jsonl` through a temporary file and rename. The annotator records `start_byte`, `end_byte`, `decision`, `reason` and `rewrite` for each selected row before running any lint, and `annotated_before_lint` is true on every row. Every specimen carries `source_url`, `source_commit`, `source_path`, `source_start_line`, `source_end_line`, `source_object`, `source_group_id` and `text_sha256`. Prove the exit with `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1 --report /tmp/family-evidence-step3.json` exiting 0, `python3 -c "import json; r=json.load(open('/tmp/family-evidence-step3.json')); assert r['families']==42 and r['below_minimum']==[], r"` exiting 0, `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1 --verify-sources` exiting 0, `git diff --stat 7d12d63e13fe193fcc1f8827b393f8aa51161731 -- plugins/hexaemeron/skills/imprimatur/evals/labelled-prose-v1 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/skills/imprimatur/lexicon plugins/hexaemeron/skills/imprimatur/EVOLUTION.md` printing nothing, `python3 -c "import json; groups={json.loads(l)['source_group_id'] for l in open('plugins/hexaemeron/skills/imprimatur/evals/labelled-prose-v1/samples.jsonl')}; used={json.loads(l)['source_group_id'] for l in open('plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/specimens.jsonl')}; assert not (groups & used), groups & used"` exiting 0, `python3 -m unittest discover -s plugins/hexaemeron/tests -t plugins/hexaemeron -p test_imprimatur_family_evidence.py`, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, and `python3 scripts/run_checks.py`.

**Files.** Add `plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py` and `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/selection-rejections.jsonl`. Change `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/specimens.jsonl`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/families.jsonl` only to fill `discovery_phrases`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md` to record the pinned heads, the collection date, the counts per family and the discovery-phrase bias, and `plugins/hexaemeron/tests/test_imprimatur_family_evidence.py` to add the collector's offline cases.

**Tests.** Extend `test_imprimatur_family_evidence.py` with offline collector cases: ordering by the seed digest on a fixed candidate list, exclusion of a v1 source group, rejection of a paragraph outside 18 to 180 words, rejection of a five-gram Jaccard duplicate at 0.80, the temporary-file-then-rename write, and refusal to run without pinned commits. Network fetches are not exercised in tests; `--verify-sources` in Exit is the online proof. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-3.json`. Record the exact counts.

**Disciplines.** phylax: the collector opens the one network boundary the study names, through `gh api` with pinned commits and no shell, and treats fetched text as bytes; the checked-in checker stays offline by default. ephoros: none beyond the Step 2 report; the collector prints its rejection counts to standard output and writes them to `selection-rejections.jsonl`, which is the record a later run reads. metron: none, no performance claim; the online step carries no budget by study item 10. elenchus: a specimen whose digest fails `--verify-sources` is a reproduced failure, reduced to the one row, and guarded by a checker test. hypomnema: the pinned heads, collection date, seed and the discovery-phrase bias are recorded in the README because they are what v2 needs to reproduce or distrust the selection.

## Step 4: Demonstrate the fixture, measure the budget and propagate the package

**Goal.** Run the demo path from study item 1, take the Step 4 timing measurement, resolve the pending conformance cell, increment the Hexaemeron package identically everywhere it is pinned, and preserve a proof document.

**Entry.** Start from the controller-recorded Step 3 commit with its Exit commands green and no unreceipted change to the accepted study or runbook.

**Exit.** The demo path `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1 --report /tmp/family-evidence.json` followed by `python3 -c "import json; r=json.load(open('/tmp/family-evidence.json')); print(r['families'], r['below_minimum'])"` prints `42 []`. `/usr/bin/time -p python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1` reports `real` under 2.0 seconds, recorded in the proof. The pending design cell is resolved by `python3 plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py --fixture plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1 --min-independent-positive 2 --tier high-value --report .hexaemeron/reports/jsonl-fixture-two-independent-specimens.json` exiting 0, and `python3 "$PLUGIN_ROOT/skills/protasis/scripts/design_evidence.py" .hexaemeron/design-evidence.json --transition integration` exits 0. The smallest repository-valid Hexaemeron package increment is identical in both plugin manifests, both marketplace listings and `tests/test_version_propagation.py`. `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/proof.md` records the demo output, the timing, the per-family counts, the `--verify-sources` result and date, the frozen digests, the package identities and every gate result. Prove the exit with the demo path above, `python3 scripts/promise_machine.py check`, `python3 -m unittest discover -s tests`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/imprimatur/tests/run_tests.py`, `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/docs/imprimatur-structural-family-evidence/study.md plugins/hexaemeron/docs/imprimatur-structural-family-evidence/runbook.md plugins/hexaemeron/docs/imprimatur-structural-family-evidence/proof.md plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md --max-defects 0`, `python3 scripts/run_checks.py`, and `git diff --check`.

**Files.** Change `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md`. Add `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/proof.md`. Regenerate `.horos/boundary.json` only if the required Horos scan reports that its classified tracked-tree evidence changed.

**Tests.** Re-run every command in Exit after the proof carries final counts. No new test module; `test_version_propagation.py` gains the incremented version. The Elenchus runner contract is: test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`; report file `.hexaemeron/elenchus/step-4.json`. Run `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` only when classified-file drift must be repaired, then rerun the root and Horos checks.

**Disciplines.** phylax: package and proof inputs are repository-owned and add no host, dependency, subprocess, credential or model-output authority; the existing linter still checks the changed Python paths. ephoros: none, packaging and a local proof add no unattended execution. metron: the study's one budget is taken here with the command in study item 10, once on the full fixture, and the number goes in the proof with the machine it was taken on. elenchus: any failed full gate stops publication and is reduced to its causal change before the same gate set is rerun; the runner contract above stays fixed. hypomnema: the proof preserves what was demonstrated and the package manifests remain the home of distribution identity; `EVOLUTION.md` stays byte-identical because no lint generation changed, as the README's opening paragraph records.
### Amendment -- 2026-09-06

**What changed.** Complete replacement Files: Add `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/family.schema.json`, `plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/schemas/specimen.schema.json`, `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/study.md` and `plugins/hexaemeron/docs/imprimatur-structural-family-evidence/runbook.md`. Change `.horos/boundary.json` and `.horos/census.json` by running `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` and the same command with `--census`, because the root suite refuses a tracked tree whose Horos boundary and census do not match a fresh scan. Change `tests/check-map-v1.json` only if `python3 scripts/run_checks.py` refuses the new paths as unowned; register the fixture path and nothing else.
**Why.** `tests/test_joined_front_door.py` and `tests/test_demonstrations.py` compare the committed Horos boundary and census with a fresh scan, and three new Markdown files move the `.md` file count, so Step 1's Exit command `python3 scripts/run_checks.py` cannot exit 0 without the regenerated pair. The study did not name this repository gate; Step 4 named the boundary but not the census and not the earlier steps.
**Steps touched.** Step 1's Files.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.
