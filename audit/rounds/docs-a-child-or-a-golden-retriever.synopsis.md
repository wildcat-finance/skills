Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/docs-a-child-or-a-golden-retriever.md | source_sha256=36fe9aaadd8debd562342958b8394497d2889b267d0c484564ab0183353a2c2f | h2_count=4
## Step 1, round 1 -- 2026-08-27T14:48:54Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: role-confusion=reviewed; current-state-drift=reviewed; mascot-identity=reviewed; generated-text=reviewed; reference-leakage=reviewed; binary-review=reviewed; accessibility-gap=reviewed; layout-overflow=reviewed; source-output-drift=reviewed; toolchain-gap=reviewed; horos-currency=reviewed; link-decay=reviewed; branch-authority=reviewed; scope-creep=reviewed<br><br>Not checked: the Pashov x-ray and solidity-auditor passes, waived because the step ships no Solidity; comprehension by a first-time human reader; PDF/UA conformance, which the package does not claim; resolution of the canonical primer URL after publication, because the requested run branch does not contain the step until integration and returned HTTP 404 before publication; any pull request or merge to main, which the user did not authorise<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | scripts/build_child_or_golden_retriever_primer.py; tests/test_child_or_golden_retriever_primer.py | The current-state-drift row says tests pin current versions and checkpoint claims, but the implementation pinned only definitions, lifecycle, links, and output bytes. A Hexaemeron or Fiat version move, or stale `Fiat does not yet support checkpointing` wording returning to the canonical primer, left the dedicated builder report clean. | fixed in this commit: the `current-state` report check pins Hexaemeron 1.6.5 and Fiat 5.30.1 to their current source files, confirms both values in the shipped study, rejects four stale checkpoint forms, and has the same focused test; reduced version-drift and stale-claim mutations now fail, while the exact runner passes 10 of 10 checks |<br><br>Leads not pursued: all four definitions and all seven lifecycle stages match the contracts and generated views; the two generated source illustrations preserve the angular light mascot, tall ears, narrow yellow eyes, lean proportions, blank cards, and text-free boundary on full-size inspection; the supplied archive is 64,678,409 bytes at SHA-256 `e09eb107921ab52e467bae54e3e605f2e01fa258df7c12529be44fc486d71218`, no kit reference is tracked, and no request-scoped reference directory remains; the Creator-supplied cover is copied byte for byte at SHA-256 `5763ab9da93a3bd3420d2e905eef9525dbeb2e642f3121d8ad76c38d9f9cc32a`; the target-path recovery leaves the required inventory regular, label and cover-text overflow guards pass, and six fresh Poppler page renders have no clipping, overlap, missing glyph, broken hierarchy, or unreadable contrast; the temporary-path PDF repair yields four byte-identical generated outputs; the `pdfimages` cover regex and Git binary detection pass, with all seven new binaries reported as binary by `git diff --numstat`; live main owns ADR-038 at blob `3e7d7df69e5009846ea8940d100f1fc40ca391e6`, so ADR-039 is collision-free and Hypomnema exits 0; the post-audit Horos write records 1,733 tracked files and updates only the boundary count for the two new audit files, its check exits 0, and all seven new binaries are classified; the exact builder exits 0 with 10 of 10 checks, the focused suite passes 12 tests, the root suite passes 414 tests, Phylax and Ephoros exit 0, Imprimatur reports 100.0/100 with zero defects on all six changed prose files, and Brevitas is deferred to the prose phase for the reader-facing draft while the study and runbook remain excluded completeness-oriented specifications; every checked external source URL returns HTTP 200 except the expected pre-publication branch URL recorded under Not checked
## Step 1, round 2 -- 2026-08-27T15:01:40Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: role-confusion=reviewed; current-state-drift=reviewed; mascot-identity=reviewed; generated-text=reviewed; reference-leakage=reviewed; binary-review=reviewed; accessibility-gap=reviewed; layout-overflow=reviewed; source-output-drift=reviewed; toolchain-gap=reviewed; horos-currency=reviewed; link-decay=reviewed; branch-authority=reviewed; scope-creep=reviewed<br><br>Not checked: the Pashov x-ray and solidity-auditor passes, waived because the step ships no Solidity; comprehension by a first-time human reader; PDF/UA conformance, which the package does not claim; the actual GitHub Actions jobs, because this branch is not published yet; resolution of the canonical primer URL after publication, because the requested run branch does not contain the step until integration and returned HTTP 404 before publication; any pull request or merge to main, which the user did not authorise<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | medium | tests/test_child_or_golden_retriever_primer.py; .github/workflows/repo.yml | The focused test fixed its builder interpreter to `/Users/c0rtexzer0/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3` and raised in `setUpClass` before any portable assertion when that private path was absent. The repository workflow runs the root suite on `ubuntu-latest` with Python 3.9 and 3.13, so the step would fail its required gate on a normal checkout before reviewing the shipped package. | fixed in this commit: the test discovers an explicitly configured, current, or home-relative Python only when Pillow, pypdf, and ReportLab import; a host without those optional build dependencies still runs the portable assertions and checks all seven shipped binary digests, raw PDF links, and active-content exclusions, while the two dependency-backed checks skip with exact reasons. A committed host-neutrality guard rejects the old private path. The reduced missing-runtime specimen now passes 12 tests with 2 explicit skips; the capable local path passes all 14 focused tests, the exact builder passes 10 of 10 checks, and the root suite passes 416 tests. |<br><br>Leads not pursued: the round-one version and stale-checkpoint guard passes unchanged; all four definitions and seven lifecycle stages remain byte-bound across the Markdown and generated views; the kit archive remains 64,678,409 bytes at SHA-256 `e09eb107921ab52e467bae54e3e605f2e01fa258df7c12529be44fc486d71218`, no reference asset leaked into Git, both generated source illustrations remain text-free and preserve the angular light mascot, and the Creator-supplied cover remains byte-identical at SHA-256 `5763ab9da93a3bd3420d2e905eef9525dbeb2e642f3121d8ad76c38d9f9cc32a`; all four generated outputs rebuild byte-identically; both PDFs retain horizontal A4 metadata, fixed titles, extracted definitions, lifecycle, annotations, no JavaScript, five plus one pages, and stable SHA-256 digests, and fresh full-page Poppler inspection plus full-size inspection of all five PNGs found no clipping, overlap, missing glyph, unreadable contrast, generated source text, generic-cat drift, or cropped critical content; all seven binary paths remain Git-binary and Horos-classified; the current branch descends from the requested run branch and no main publication action occurred; Phylax, Ephoros, and Hypomnema exit 0; every checked external source URL returns HTTP 200 except the expected pre-publication branch URL recorded under Not checked
## Step 1, round 3 -- 2026-08-27T15:14:44Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: role-confusion=reviewed; current-state-drift=reviewed; mascot-identity=reviewed; generated-text=reviewed; reference-leakage=reviewed; binary-review=reviewed; accessibility-gap=reviewed; layout-overflow=reviewed; source-output-drift=reviewed; toolchain-gap=reviewed; horos-currency=reviewed; link-decay=reviewed; branch-authority=reviewed; scope-creep=reviewed<br><br>Not checked: the Pashov x-ray and solidity-auditor passes, waived because the step ships no Solidity; comprehension by a first-time human reader; PDF/UA conformance, which the package does not claim; the actual GitHub Actions jobs, because this branch is not published yet; resolution of the canonical primer URL after publication, because the requested run branch does not contain the step until integration and returned HTTP 404 before publication; any pull request or merge to main, which the user did not authorise<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R3-01 | medium | docs/a-child-or-a-golden-retriever.md; scripts/build_child_or_golden_retriever_primer.py; tests/test_child_or_golden_retriever_primer.py; plugins/hexaemeron/skills/fiat/references/push-discipline.md | The current-state guard rejected four obsolete no-checkpoint phrases, but the primer still told readers not to `move an unfinished run to another machine`. Current Fiat requires a portable checkpoint after a completed step so another contributor can pick it up, then requires verification before anything else. The absolute prohibition hid that supported recovery path, and both the builder and focused test passed it. | fixed in this commit: the primer now distinguishes an in-progress step moved by hand from a completed step resumed through a verified portable checkpoint; the builder normalises whitespace, rejects the old prohibition, and requires the positive transfer guidance; the focused test mirrors both checks. Applying the changed guard to the unfixed parent made `current-state` fail with 9 of 10 checks passed, while the exact fixed-tree runner passes 10 of 10, the capable focused suite passes 14 tests, and the missing-runtime suite passes 12 tests with 2 explicit dependency skips. |<br><br>Leads not pursued: round 2's portability fix was exercised with the capable runtime, an existing interpreter missing Pillow, pypdf, and ReportLab, and a deliberately missing configured runtime; under missing build dependencies the portable tests still refused injected PDF active content, a removed PDF link, a changed binary digest, and a broken PNG signature rather than passing silently. All four definitions and seven lifecycle stages remain byte-bound; all four generated outputs rebuild byte-identically; five current PNGs and all six fresh Poppler page renders show no clipping, overlap, missing glyph, unreadable contrast, generated source text, generic-cat drift, or cropped critical content; both PDFs retain fixed SHA-256 digests, five plus one pages, horizontal A4 metadata, extracted text, annotations, binary structure, and no active content; all seven binaries remain Git-binary and Horos-classified; the root suite passes 416 tests, and Phylax, Ephoros, and Hypomnema each exit 0; every checked external URL returns HTTP 200 except the expected pre-publication primer URL recorded under Not checked; the stacked branch descends from the requested run branch, and no push, pull request, or merge to main occurred
## Step 1, round 4 -- 2026-08-27T15:26:15Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: role-confusion=reviewed; current-state-drift=reviewed; mascot-identity=reviewed; generated-text=reviewed; reference-leakage=reviewed; binary-review=reviewed; accessibility-gap=reviewed; layout-overflow=reviewed; source-output-drift=reviewed; toolchain-gap=reviewed; horos-currency=reviewed; link-decay=reviewed; branch-authority=reviewed; scope-creep=reviewed<br><br>Not checked: the Pashov x-ray and solidity-auditor passes, waived because the step ships no Solidity; comprehension by a first-time human reader; PDF/UA conformance, which the package does not claim; the actual GitHub Actions jobs, because this branch is not published yet; resolution of the canonical primer URL after publication, because the requested run branch does not contain the step until integration and returned HTTP 404 before publication; the step-checkpoint upload, explicitly waived for this run with the wording `waive the step checkpoint for this run`; any pull request or merge to main, which the user did not authorise<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Fiat 5.30.1's current push discipline distinguishes an in-progress step moved by hand from a completed-step pickup through an available portable checkpoint and requires checkpoint verification before any other action; the primer preserves that beginner-level boundary, while the controller reference retains the detailed `done push`, upload, sidecar, manifest, and clean-machine ceremony. The round-three guard independently refused both the old absolute prohibition and removal of the positive verified-transfer sentence. The exact capable runner passes 10 of 10 checks and all 14 focused tests; an empty-home `/usr/bin/python3` without Pillow, pypdf, or ReportLab still runs 12 portable tests with two explicit dependency skips; the root suite passes 416 tests. All four definitions and seven lifecycle stages remain byte-bound, and all four generated outputs rebuild byte-identically. Full-size review of the five PNGs and fresh 150-DPI Poppler review of all six PDF pages found no clipping, overlap, missing glyph, unreadable contrast, generated source text, generic-cat drift, or cropped critical content. Both PDFs retain fixed SHA-256 digests `833eb12a3ff7977823d39366c706d51b455b8a8a964388a71c44b913655a2a69` and `09c666e63545ebc5b5f69252467a5250fa3a51dc4e1e154aeb36e0a1125c4764`, five plus one pages, horizontal A4 metadata, expected text and URI annotations, binary structure, no encryption, and no catalog, page, or annotation active-content action. The supplied mascot archive remains 64,678,409 bytes at SHA-256 `e09eb107921ab52e467bae54e3e605f2e01fa258df7c12529be44fc486d71218`; no reference asset is tracked; all seven shipped binaries remain Git-binary and Horos-classified. The current remote main and audited tree carry identical Hexaemeron 1.6.5 manifest and Fiat 5.30.1 skill bytes. All local links resolve; every checked external URL returns HTTP 200 except the expected pre-publication primer URL recorded under Not checked. Phylax, Ephoros, Hypomnema, Imprimatur, Brevitas, Horos, audit-synopsis currency, and `git diff --check` pass. Each of the four product and audit commits verifies locally with exactly one required provenance trailer pair; the stacked branch descends from the requested run and step branches, only the requested run anchor exists remotely, and no push, pull request, or merge to main occurred
