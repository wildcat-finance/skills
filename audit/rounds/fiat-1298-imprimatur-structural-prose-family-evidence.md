## Step 1, round 1 -- 2026-09-07T07:39:52Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; holdout-leak=not-applicable; specimen-provenance=not-applicable; span-integrity=not-applicable; independence=not-applicable; annotation-order=not-applicable; partial-write=not-applicable; fetch-boundary=not-applicable; untrusted-text=not-applicable

Not checked: the 42 headings and the 8/5/6/12/11 split come from a live `gh issue view 1298 --repo wildcat-finance/skills` read on 2026-09-07, not from a checked-in copy of the issue, which Step 2 adds; the Pashov pair and `fizz` did not run under the recorded waiver (`waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`); `python3 plugins/hexaemeron/tests/run_tests.py`, named in the step's Exit, was not run in this round; the eight not-applicable ids concern the collector, the specimens and the checker that Steps 2 and 3 add, and no specimen, JSONL row or checker exists at this commit.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the two schemas cannot express `start_byte < end_byte`, `family == family_id`, or a `rewrite` required on an `actionable` positive, so `span-integrity` and `independence` stay with the Step 2 checker. `phylax.py` and `ephoros.py` printed `clean` and exited 0 for a path that does not exist, observed when one joined argument was passed by mistake, while `hypomnema.py` reported `H000 unreadable` for the same argument; that is a boundary of the bundled lints, not a defect in this diff, and the lint exits reported here were re-taken with the seven paths passed separately. Review basis: diff `7d12d63e13fe193fcc1f8827b393f8aa51161731..57c5d29da97dbeaca3830e612b4c4e32b9eed708`, 7 files, 642 insertions, 7 deletions, all inside the amended Files list. Phylax 0; Ephoros 0; Hypomnema 0. Root suite `python3 -m unittest discover -s tests`: `Ran 1623 tests`, `OK`, exit 0. Imprimatur suite `112/112 passed`. `lint-bytes-drift`: `sha256sum` of the five frozen paths equals the README table byte for byte and equals the same five paths at `7d12d63e`. `v1-bytes-drift`: `git diff 7d12d63e -- plugins/hexaemeron/skills/imprimatur/evals/labelled-prose-v1` prints nothing, `candidate-freeze.json` included. `family-count`: the issue body carries 42 `###` headings, each with a `Disposition` line, split 8 "Missing conditions and causal wrappers", 5 "Hidden actors, authority, and intent", 6 "Verb, capability, and purpose shells", 12 "Stacked hedges, emphasis, and redundant markers", 11 "Existential, reference, and scope constructions needing more evidence", matching the README; the section "Ideas that do not yet form a family" carries the five observations the README records as non-rows. `issue-wording`: `Form`, `Reader cost`, `Direct rewrite`, `Boundary` and `Disposition` each appear 42 times, once per family; the `group` enum's five values match the issue's five section headings verbatim; the README's 5 high-value and 8 signal family names match the issue's evidence-packet paragraph. Both schemas load, declare exactly the study item 4 field lists (13 family, 24 specimen) as `properties` and `required` with `additionalProperties: false`, and use only keywords `validate_schema()` in `evaluate_labelled_corpus.py` implements: `type` including union, `const`, `enum`, `required`, `properties`, `additionalProperties`, `items`, `uniqueItems`, `minLength`, `pattern`, `minimum`, `maximum`. The Imprimatur lint on the README, study and runbook reports 0 defects at `--max-defects 0`. Committed `study.md` differs from `.hexaemeron/study.md` only in five `[skill](...)` links rewritten to `../../skills/<name>/SKILL.md`, and all five resolve; `runbook.md` is byte-identical. v1 `samples.jsonl` carries 16 distinct `source_group_id` values, matching the README's excluded-group count; issue #422 is `imprimatur-next -- build labelled-prose-v2`, matching the README's citation. A fresh `horos.py scan . --write` and `--census` reproduce the committed `.horos` pair on the step tree with no untracked file present; the +2 `.md` delta seen first came from this round's own audit files. No new prose file carries a host byline, and the step commit's trailers are `Co-authored-by: Shoggoth` and `Wildcat-Origin: shoggoth`.

## Step 2, round 1 -- 2026-09-07T10:24:15Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; partial-write=reviewed; holdout-leak=not-applicable; specimen-provenance=not-applicable; independence=not-applicable; annotation-order=not-applicable

Not checked: the `--verify-sources` replay against GitHub never ran, because `specimens.jsonl` is empty and both new guards run with `PATH` stripped of `gh`, so neither this round nor any test opened a socket; the `resolved.is_relative_to(fixture)` branch of `resolve_below` has no guard and is unreachable while every read name is a module constant; whether the `boundary`, `existing-family` and `future` split of the 29 non-target families matches an intent no source states, since the issue calls them only "required negative boundaries or future questions"; the Pashov pair and `fizz` did not run under the recorded waiver (`waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`); `tests/test_demonstrations.py::RunnerExecutionBoundaryTests::test_a_grandchild_that_leaves_the_process_group_is_refused` failed once (`0 != 2`) in a greenlight run that overlapped `scripts/run_checks.py`, then passed four times alone and once in a clean greenlight, so its cause was not established, and nothing in this step's diff reaches that runner.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `--verify-sources` replayed every specimen row carrying `specimen_id` and `text`, so a row failing its schema reached `fetch_source_object`. A row without `repository` raised an uncaught `KeyError` and exited 1, the code the amended Exit reserves for a content finding rather than the 2 it reserves for an unsafe read; a `repository` breaking the schema's `^wildcat-finance/` pattern chose the `gh api` target. Both reproduced. | fixed in 20dd6774ee653ac1af9537e3ae0d6d012f6bb182 |
| S2-R1-02 | medium | plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | No test bound the issue's 13 named evidence targets to family ids; only the counts 5 and 8 were asserted, so moving `reason_is_because` to `signal` and `purpose_periphrasis` to `high-value` left all 21 tests green and put the wrong families in the report's `below_minimum`. | fixed in 20dd6774ee653ac1af9537e3ae0d6d012f6bb182 |
| S2-R1-03 | low | plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | `issue_families()` did not close the open family record at a non-family `##` heading, so a `Form`, `Reader cost`, `Direct rewrite`, `Boundary` or `Disposition` line under a later section overwrote the last family's expected value. Appending `Disposition: injected by a later section.` to `issue-1298.md` failed `broad_fact_clause/disposition` against a correct catalogue. | fixed in 20dd6774ee653ac1af9537e3ae0d6d012f6bb182 |
| S2-R1-04 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `--report` is written with `write_text` before findings are collected, so a failed or interrupted write leaves a truncated report where the register's `partial-write` mitigation is a temp file then a rename. | accepted |

Leads not pursued: S2-R1-04 was accepted because no guard test distinguishes an atomic write from a direct one at this boundary, and the register's `partial-write` concern is scoped to the collector Step 3 adds; the report is a build-phase diagnostic, not a released artefact. The `boundary`, `existing-family` and `future` tiers all carry minimums (0, 0), so swapping `broad_fact_clause` to `future` and `so_or_such_that` to `boundary` leaves the whole module green and the report unchanged; the issue draws no line between them, and pinning one would freeze a judgement no source states. `issue-1298.md` is absent from the README's frozen-digest table, so the wording oracle itself is unpinned; its digest is recorded here instead, `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46`, byte-identical to the live body of `wildcat-finance/skills#1298` read on 2026-09-07, which closes the item Step 1's record left open. `run()` returns 1 at the first non-empty finding class and prints only that class: study item 11 states that behaviour and the README documents it, so it is contract rather than defect. The `--report` path is not bounded below `--fixture`, which the step's own Exit command relies on by writing to `/tmp`. `gh_fetch` checks `MAX_FETCH_BYTES` after `capture_output` has buffered all of stdout, so the cap bounds the compared value rather than the memory, and the 30-second timeout is the live bound. Review basis: diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..8df2d7acec857bbc871eb27d2f38901a12078693`, 9 files, 1510 insertions, 13 deletions, all inside the amended Files list. Phylax 0; Ephoros 0; Hypomnema 0, each with the changed paths passed as separate arguments. Root suite `python3 -m unittest discover -s tests` at the step head: `Ran 1623 tests`, `OK`, exit 0; after the fix through `.githooks/greenlight`: `Ran 1623 tests`, `OK`, exit 0. Hexaemeron suite `2456/2456 tests passed`; Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 25 tests`, `OK`, from 21 before the fix. The step's Exit commands hold: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. `family-count`: 42 `###` headings, split 8 / 5 / 6 / 12 / 11, matching the `group` counts in `families.jsonl` and the README. Tiers: `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16, and the 5 and 8 identities equal the issue's evidence packet. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing. `span-integrity`: `span_problem()` refuses a non-integer offset, a negative start, an end past the UTF-8 length, an empty span and a split codepoint, each guarded. `untrusted-text`: specimen text reaches `hashlib`, `str.encode`, `json.dumps` and comparison only, with no `eval`, `exec`, `pickle`, `os.system` or `shell=True` anywhere in the checker. `fetch-boundary`: `subprocess` is imported once and called once, from `fetch_source_object` under `--verify-sources` with a fixed argv list and `shell=False`, and no `socket`, `urllib`, `http` or `requests` import exists, so the default path opens nothing. Each of the 12 refusals the amended Exit lists has a `test_refuses_` guard, and the file cap refuses above 1,048,576 bytes while admitting exactly that many. Commit `20dd6774` carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, is GPG signed, and no changed file carries a host byline.

## Step 2, round 2 -- 2026-09-07T11:04:30Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: no socket opened in this round, so no `--verify-sources` reply from GitHub was compared; the three replay findings below were reproduced against a recording `gh` stub on `PATH`, which fixes the argv the checker builds but not what api.github.com does with it, so whether `repos/wildcat-finance/../evil-org/evil-repo/...` resolves server-side to another organisation is not established; `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`; the Pashov pair and `fizz` did not run under the recorded waiver (`waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`).

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Round 1's narrowing stopped an unvalidated row reaching `fetch_source_object`, but left the schema as the only bound on the endpoint. `^wildcat-finance/` has no anchor after the prefix, so `repository` of `wildcat-finance/../evil-org/evil-repo` clears every local check and is interpolated into the `gh api` path. Reproduced against a recording stub: `api repos/wildcat-finance/../evil-org/evil-repo/commits/<sha>`. | fixed in a41312de7ed1a8ee60a202bec630867fbc2aae1e |
| S2-R2-02 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | An `issue_comment` or `pull_request_comment` specimen replayed the wrong object. The comment id is in the `#issuecomment-<id>` fragment, which `split("#")[0]` discards, leaving the issue number to be read as a comment id. A row at `.../issues/1298#issuecomment-42` fetched `repos/wildcat-finance/skills/issues/comments/1298`, so `specimen-provenance` compared a comment nobody cited. | fixed in a41312de7ed1a8ee60a202bec630867fbc2aae1e |
| S2-R2-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `source_path` carries no schema pattern and was checked only for absolute paths and `..`, so `README.md#x` reached `gh` as `repos/o/r/contents/README.md#x?ref=<sha>`. Under RFC 3986 the `#` opens a fragment and the pinned `ref` is dropped, turning the immutable replay the register's `specimen-provenance` mitigation names into a default-branch read. The README's claim that a bad `source_path` "failed its schema" was false; nothing constrained it. | fixed in a41312de7ed1a8ee60a202bec630867fbc2aae1e |

Leads not pursued: S2-R1-04 stays accepted and does not reach further than round 1 judged: `--report` still writes with `write_text` before findings are collected, its counts are taken from raw rows so a fixture with schema failures still reports them, and both facts are visible beside a non-zero exit; the register's `partial-write` concern remains scoped to the Step 3 collector. The `resolved.is_relative_to(fixture)` branch is not dead code and needs no guard: every read name is a module constant, the `..`, absolute-path and symlink checks ahead of it carry the boundary, and the branch is reachable only if a symlink appears between `is_symlink()` and `resolve()`, which is a time-of-check backstop no test can schedule. The `boundary`, `existing-family` and `future` split holds against 27 of the 29 non-target disposition lines read one by one: all 9 `boundary` rows say "Do not add" or name a negative-specimen role, all 4 `existing-family` rows point at an existing family, and 14 of the 16 `future` rows say "Candidate", "Signal only", "Hold" or "not a current" form. The two that stay arbitrary are `broad_existential_there` ("Do not add without separate evidence", `boundary`) and `so_or_such_that` ("Do not add without a much narrower evidenced form", `future`), which carry the same conditional-negative wording and different tiers; all three tiers have minimums (0, 0), so neither assignment changes a report field or `below_minimum`, and the issue states no rule to pin it. Round 1's flake did not recur: `test_a_grandchild_that_leaves_the_process_group_is_refused` passed in both 1,623-test root-suite runs this round. The first of those two runs failed only `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan` (`.py` bytes 11971441 against 11975624), which was this round's own edits landing on disk while the 497-second run was still walking tracked files, not a defect at the step head; the rerun through `.githooks/greenlight` on the staged fixed tree was `Ran 1623 tests`, `OK`, exit 0. Review basis: diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..a41312de7ed1a8ee60a202bec630867fbc2aae1e`, 11 files, 1,705 insertions, 14 deletions. Phylax 0; Ephoros 0; Hypomnema 0, taken twice with the changed paths passed as separate arguments, at the step head and again after the fix. Hexaemeron suite 2,460 tests, 0 failures, from 2,456 at round 1; Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 29 tests`, `OK`, from 25. Elenchus ran the step's own runner contract against `a41312de`: status `guarded`, detail `the runner report records a parent assertion failure`, 2,460 executed, 4 assertion failures, 0 errors, 0 skipped, exit 0; the four new guards were seen red on the parent and green on the fix before the commit. The step's Exit commands still hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. Round 1's three fixes were re-derived rather than taken on trust. The `--verify-sources` narrowing holds on its first two claims: a row failing its schema, digest, span, family or annotation order was confirmed not to reach `fetch_source_object` in six probes with a stub `gh` that would have recorded any call, and no row that should be replayed was dropped, a negative-polarity `issue_comment` row included. The evidence-target test is load-bearing: moving `reason_is_because` to `signal` and `purpose_periphrasis` to `high-value`, with their minimums kept consistent, fails `test_refuses_an_evidence_tier_the_issue_packet_does_not_name` while `test_tier_minimums_follow_the_evidence_tier` stays green. `issue_families()` loses and duplicates nothing at a section boundary: with and without the `current = None` line it returns the same 42 ids in the same order with all five copied fields present, because every group section in `issue-1298.md` is followed by prose rather than a field line; the first family `causal_subject_has_no` opens under the first group heading and the last, `broad_fact_clause`, is closed by `## Ideas that do not yet form a family`, and the first and last family of each of the five groups parse with their own fields. `family-count`: 42 `###` headings, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl` and the README. Tiers: `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five frozen digests match the current files. `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46` and remains absent from that table. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen` or `shell=True` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list and `shell=False`. Commit `a41312de` is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, carries `Co-authored-by: Shoggoth` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 2, round 3 -- 2026-09-07T12:03:32Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: no socket opened in this round, so every replay probe ran against a recording `gh` stub and establishes the argv the checker builds, not what api.github.com does with it; `--verify-sources` has still never run against GitHub, because `specimens.jsonl` is 0 bytes; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling, since it bounds the segment and is not sourced from one; the Pashov pair and `fizz` did not run under the recorded waiver (`waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`); `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Round 2 pinned `repository` and `source_path` and left `source_commit` to the schema, whose `^[0-9a-f]{40}$` is applied with `re.search` and whose `$` also matches before a trailing newline. A row carrying 40 hex plus `\n` cleared every local check and reached the `gh` argv as `repos/wildcat-finance/skills/commits/aaaa…\n`, exit 0. Reproduced against a recording stub on both the `commits` and the `contents?ref=` endpoint. | fixed in 2806328e24c4369b45c008460fe4939bfb53feec |
| S2-R3-02 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | The `issue_body` and `pull_request_body` branch accepted the URL tail on `str.isdigit()`, which is true for every Unicode decimal digit, so `.../issues/١٢٣` reached `gh` as `repos/wildcat-finance/skills/issues/١٢٣`, exit 0. No `/`, `.`, `..`, `#`, `?`, `%` or control character passes `isdigit()`, so the effect is a non-ASCII homoglyph in a URL path segment rather than a redirected endpoint. | fixed in 2806328e24c4369b45c008460fe4939bfb53feec |
| S2-R3-03 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | The `gh` reply was read as `json.loads(blob)["commit"]["message"]` and `["body"]` with no shape check. A reply missing `commit`, missing `commit.message`, that is not JSON, or that is a JSON array raised an uncaught `KeyError`, `JSONDecodeError` or `TypeError`, printed a traceback and exited 1 -- the code the amended Exit reserves for a content finding, not the 2 it reserves for an unsafe read. This is S2-R1-01's defect on the reply side, which round 1 fixed only on the row side. All four shapes reproduced against the step-head bytes. | fixed in 2806328e24c4369b45c008460fe4939bfb53feec |
| S2-R3-04 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `json.loads` resolves a repeated key by last-wins, so a `families.jsonl` row declaring `evidence_tier` as `boundary` and again as `high-value` was checked as `high-value` while its bytes read as `boundary`. `families.jsonl` carries a frozen digest, so its bytes and its enforced meaning could diverge with nothing on screen to show it. Reproduced: exit 1 on a tier minimum the visible row does not carry. | fixed in 2806328e24c4369b45c008460fe4939bfb53feec |
| S2-R3-05 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Structural. Seven findings across rounds 1 and 2 and four more this round sit in `fetch_source_object`, which built each endpoint by interpolating five row fields and took a guard per field only after a round found it. Three fields were guarded and two were not, and the guards sat in four separate branches, so whether every value was anchored could not be read in one place -- which is how round 2's own fix, whose comment claimed each value was "pinned here rather than trusted to the schema", left `source_commit` trusted to the schema. Named alternative, applied: one `endpoint_segment` gate fullmatching the pattern named for its field in an `ENDPOINT_SEGMENTS` table, so a field with no row there cannot reach `gh` and the question is one table rather than four branches. | fixed in 2806328e24c4369b45c008460fe4939bfb53feec |

Leads not pursued: `read_jsonl_below` splits rows with `str.splitlines()`, which also splits on U+0085, U+2028 and U+2029; a row whose `text` carries one of those raw -- the form `json.dumps(..., ensure_ascii=False)` emits, which the test module's own `jsonl()` helper uses -- is split into two unparseable lines and refused as `unreadable JSON` with a misleading message. It fails closed and cannot fire while `specimens.jsonl` is empty, but a Step 3 collector writing with `ensure_ascii=False` would produce a file this checker refuses; left for Step 3, where the collector is written, and the fix is `split("\n")`. S2-R1-04 stays accepted and was re-derived rather than carried: `--report` is still written with `write_text` before findings are collected, is still not bounded below `--fixture`, and its counts are still taken from raw rows. No line cap bounds a JSONL file, but the 1,048,576-byte file cap bounds the row count and no per-row work is superlinear. A BOM, a JSON array row and a file with no trailing newline were each probed and gave refuse, refuse and accept, which is correct in all three. `--report` with an absent parent directory exits 2 through `main`'s `OSError` handler. Unknown `--tier` exits 2, `--min-independent-positive` exits 2 on a negative value and on a non-integer through argparse, `--fixture` naming a file exits 2, and `--allow-below-minimum` with `--min-independent-positive` exits 0 with both applied. The four guards round 2 added are load-bearing: each was removed from the checker in a scratch copy and its own test went red. `test_every_source_object_kind_replays_its_own_endpoint` is green on the parent by design, because it guards this round's refactor against regressing round 2's fix rather than guarding a new defect; the five `test_refuses_` guards were each seen red on the parent bytes and green on the fix. Round 1's flake recurred and stays unresolved: `tests/test_demonstrations.py::RunnerExecutionBoundaryTests::test_a_grandchild_that_leaves_the_process_group_is_refused` failed `0 != 2` in the greenlight run over the staged record, then passed five times alone. Both observed failures, round 1's and this one, were greenlight runs carrying concurrent load, and the test passed in the round 1, round 2 and round 3 fix greenlights; a load-sensitive timing window is the standing hypothesis and is not established. Nothing in this step's diff reaches `tests/test_demonstrations.py` or that runner, and the record commit's own greenlight was re-taken clean. Review basis: diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..4ab6be86`, 11 files, 1,731 insertions, 14 deletions, then the fix at 2806328e24c4369b45c008460fe4939bfb53feec. Phylax 0; Ephoros 0; Hypomnema 0, taken twice with the changed paths passed as separate arguments, at the step head and again after the fix. Root suite `python3 -m unittest discover -s tests` at the step head: `Ran 1623 tests`, `OK`, exit 0; through `.githooks/greenlight` on the staged fix: `Ran 1623 tests`, `OK`, greenlight exit 0. Hexaemeron suite 2,466 tests, 0 failures, 0 errors, 0 skipped, from 2,460 at round 2; Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 35 tests`, `OK`, from 29. The step's Exit commands hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. All six `source_object` values the schema's enum allows still replay to their own endpoint, checked one by one against the stub, so the pinning dropped nothing it should replay. `family-count`: 42 `###` headings each with a `Disposition` line, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl`; tiers `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16. `issue-wording`: `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46` and remains absent from the README's frozen-digest table. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five frozen digests equal the current files. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen`, `shell=True` or `__import__` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list, `shell=False`, a 30-second timeout and a 4,194,304-byte output cap. The Imprimatur lint reports 0 defects at `--max-defects 0` on the changed README. Rounds 1 and 2 were re-derived rather than taken on trust. Elenchus ran the step's own runner contract against `2806328e24c4369b45c008460fe4939bfb53feec`: status `guarded`, detail `the runner report records a parent assertion failure`, 2,466 executed, 8 assertion failures, 0 errors, 0 skipped, exit 0. Commit 2806328e24c4369b45c008460fe4939bfb53feec is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, carries `Co-authored-by: Shoggoth` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 2, round 4 -- 2026-09-08T17:40:26Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: no socket opened in this round, so every replay probe ran against a recording `gh` stub and establishes the argv the checker builds, not what api.github.com does with it; `--verify-sources` has still never run against GitHub, because `specimens.jsonl` is 0 bytes; whether `blob/<sha>/<path>`, `raw/<sha>/<path>`, `commit/<sha>` and `commits/<sha>` are the only citation forms GitHub serves for those objects, since the accepted pair per kind comes from reading URLs rather than from a documented list, and a legitimate citation in a fifth form would now be refused with exit 2; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling; the Pashov pair and `fizz` did not run under the recorded waiver (`waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`); `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`; `python3 scripts/run_checks.py` with no `--base` selects nothing on a clean tree and returns `outcome nothing-selected`, so its exit 0 establishes nothing about this diff, and the green recorded below is the `--base` run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Round 3's one gate anchors the shape of every endpoint segment, which establishes that the replayed object is one `wildcat-finance` object and not that it is the object the row cites. The endpoint was built from `repository`, `source_commit` and `source_path`, and none was compared with `source_url`. Reproduced against a recording stub, exit 0 each time: a row declaring `wildcat-finance/skills` while citing `wildcat-finance/v2-protocol/issues/7` replayed `repos/wildcat-finance/skills/issues/7`; a `markdown_paragraph` citing `docs/OTHER.md` at `bbbb…` replayed `README.md` at `aaaa…`; a `commit_message` citing `cccc…` replayed `aaaa…`. So `specimen-provenance` compared text against an object nobody cited, which is S2-R2-02's class on the field side rather than the fragment side. | fixed in 8663c546e76df8a7b7427480b4b41fbd2a6cacb5 |
| S2-R4-02 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `group_id_problem`, added one commit earlier at `1f4fada3` to stop two ids that read as one group counting as two, refuses whitespace and the `Cc`, `Cf`, `Cn`, `Co` and `Cs` categories. A combining mark is none of those, so the NFC and NFD spellings of `wildcat-finance/skills:docs/café.md` render identically and compare unequal. Reproduced: two positives for `reason_is_because` from that one document, `--min-independent-positive 2 --tier high-value`, reported `independent_positives: 2`, carrying a high-value family's evidence minimum out of a single source group. | fixed in 8663c546e76df8a7b7427480b4b41fbd2a6cacb5 |
| S2-R4-03 | medium | plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md | The README said `--verify-sources` "replays each specimen against its immutable GitHub object", and the checker's own comment said `source_commit` is checked on every kind so "the pinned ref a replay claims to be immutable is one this checker validated". Probed on all six `source_object` values: only `markdown_paragraph` sends a reference (`contents/<path>?ref=<sha>`) and only `commit_message` addresses its object by sha. `issue_body`, `pull_request_body`, `issue_comment` and `pull_request_comment` replay `issues/<n>`, `issues/comments/<id>` and `pulls/comments/<id>` with no reference at all, so they compare a body GitHub lets its author edit, and the validated commit is never sent. The register's `specimen-provenance` mitigation is stated as replaying an immutable object; it holds for two kinds of six. | fixed in 8663c546e76df8a7b7427480b4b41fbd2a6cacb5 |
| S2-R4-04 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Inside the `--verify-sources` loop, `elif sha256_text(row["text"]) != row["text_sha256"]` was unreachable: a row whose digest disagreed with its own text is refused as `specimen-digest` before it reaches the `verifiable` list. Reproduced: `text_sha256` set to 64 zeroes exits 1 with `specimen-digest` and no `gh` call recorded. The README's "compares `text_sha256`" therefore named a comparison the replay cannot make; the only post-replay comparison is `row["text"] in body`. | fixed in 8663c546e76df8a7b7427480b4b41fbd2a6cacb5 |
| S2-R4-05 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `source_start_line` and `source_end_line` are required by the specimen schema and read by nothing: `grep` finds neither name in the checker. The register's `specimen-provenance` item names "source_url commit path and lines of every specimen". Reproduced: a `markdown_paragraph` row carrying `source_start_line` 999999 and `source_end_line` 1000000 against a two-line replayed file exits 0. | accepted |

Leads not pursued: S2-R4-05 was accepted rather than fixed because the natural repair pins a line contract the amended Exit's refusal list does not name, and Step 3 writes the collector that produces those two fields, so a refusal added here would be a contract that step has not been built against; it is recorded as a finding rather than a lead so the unverified field stays visible. The two comment kinds' `source_object` is still not cross-checked against the kind its own URL names, so an `issue_comment` row citing `pull/1298#discussion_r99` replays `pulls/comments/99`: the fragment rather than `source_object` decides the collection, because a pull request's conversation comment is an issue comment on GitHub and legitimately carries `#issuecomment-`, and the replay follows the citation, so the object fetched is the cited one and only the label disagrees. S2-R1-04 stays accepted and was re-derived rather than carried: `--report` is still written with `write_text` before findings are collected, is still not bounded below `--fixture`, and its counts are still taken from raw rows. `measure_below_minimum` counts independence over raw rows while `collect_findings` applies `group_id_problem` to validated rows only, so the report can name a family whose independence the findings also refuse; both paths exit 1, so nothing passes on the difference. `validate_schema` still applies `pattern` with `re.search`: every patterned field that reaches an endpoint now passes `endpoint_segment` instead, and the two that do not fail closed downstream, a `family_id` carrying a trailing newline through the family join and a `text_sha256` through the digest comparison. `FamilyCatalogueWordingTest.setUp` still reads `families.jsonl` with `str.splitlines()`, the reader `jsonl_rows` replaced in the checker at `1f4fada3`; a smuggled row would make that test see 43 rows and fail, and the checker refuses the same file with exit 2, so both fail closed. Round 3's `str.splitlines()` lead for the Step 3 collector no longer stands: `1f4fada3` replaced the split, and `test_a_separator_inside_specimen_text_stays_one_row` guards a raw U+2028 inside `text`. `gh_fetch` still checks `MAX_FETCH_BYTES` after `capture_output` has buffered stdout, so the cap bounds the compared value rather than the memory, and the 30-second timeout is the live bound. The `resolved.is_relative_to(fixture)` branch is still an unguardable time-of-check backstop reachable only if a symlink appears between `is_symlink()` and `resolve()`. Round 1's flake did not recur: `tests/test_demonstrations.py::RunnerExecutionBoundaryTests::test_a_grandchild_that_leaves_the_process_group_is_refused` passed in this round's root-suite run, its greenlight and the `--base` check run. Review basis: `1f4fada3` at the round-4 head, audited as the whole step diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..1f4fada3`, 11 files, 2,062 insertions, 14 deletions, then the fix at 8663c546e76df8a7b7427480b4b41fbd2a6cacb5, 4 files, 209 insertions, 33 deletions. Phylax 0; Ephoros 0; Hypomnema 0, taken twice with the changed paths passed as separate arguments, at the step head and again after the fix. Root suite `python3 -m unittest discover -s tests` on the fixed tree: `Ran 1623 tests`, `OK`, exit 0, and green again inside `.githooks/greenlight` over the staged fix. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit`: `outcome green`, exit 0, 11 checks selected and passed, `root-suite`, `hexaemeron-suite`, `imprimatur-suite`, `dead-code-suite`, `dead-code-suppressions-check`, `demonstrations-suite`, `front-door-check`, `joined-front-door-suite`, `lint-ephoros`, `lint-hypomnema` and `lint-phylax`. Hexaemeron suite 2,475 tests, 0 failures, 0 errors, 0 skipped, from 2,466 at round 3; Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 44 tests`, `OK`, from 39 methods at the head. Elenchus ran the step's own runner contract against 8663c546e76df8a7b7427480b4b41fbd2a6cacb5: status `guarded`, detail `the runner report records a parent assertion failure`, 2,475 executed, 5 assertion failures, 0 errors, 0 skipped, exit 0; the three new refusal guards were each seen red on the parent bytes with the parent checker in place, and `test_an_agreeing_citation_still_replays_the_cited_file` and `test_a_replayed_object_without_the_text_is_a_finding` pass on both, so those two guard this change rather than a defect. The step's Exit commands hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. The endpoint-shape class round 3 named is closed and was re-derived here rather than taken on trust: the five interpolated values `repository`, `source_path`, `source_commit`, `object_number` and `comment_id` each fullmatch one `ENDPOINT_SEGMENTS` pattern, the only other value in any endpoint is the literal `issues` or `pulls` collection, no sixth row field is interpolated, all six `source_object` kinds were replayed one by one against the stub and each reached its own endpoint, and the reply side carries `reply_value`'s shape check. What round 4 found was the identity of that endpoint rather than its shape. `family-count`: 42 `###` headings each with a `Disposition` line, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl`, whose 42 `family_id` values are distinct and whose `source_issue` is one value; tiers `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16. `issue-wording`: `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46` and remains absent from the README's frozen-digest table. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five frozen digests equal the current files. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen`, `shell=True` or `__import__` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list, `shell=False`, a 30-second timeout and a 4,194,304-byte output cap. The Imprimatur lint reports 0 defects at `--max-defects 0` on the changed README; its three `rule_of_three` signals are the issue's own section headings, which the catalogue copies. Rounds 1, 2 and 3 were re-derived rather than taken on trust. Commit 8663c546e76df8a7b7427480b4b41fbd2a6cacb5 is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 2, round 5 -- 2026-09-09T02:48:46Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: no socket opened in this round, so every replay probe establishes the argv the checker builds rather than what api.github.com does with it, and `--verify-sources` has still never run against GitHub because `specimens.jsonl` is 0 bytes. Which thread a comment belongs to is still not established: GitHub keys a comment by id alone, so `S2-R5-02`'s path check establishes that a citation names a thread, not that it names that comment's thread. Also not checked: whether `blob/<sha>/<path>`, `raw/<sha>/<path>`, `commit/<sha>` and `commits/<sha>` are the only citation forms GitHub serves for those objects, since the accepted pair per kind comes from reading URLs rather than a documented list, so a legitimate citation in a fifth form is refused with exit 2; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling, carried from round 3. `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`. The Pashov pair and `fizz` did not run, under the recorded waiver `waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`. This round's four findings were not re-derived by a fresh review of the whole step diff: their mechanisms are the ones the fixes commit names, and what was re-established here is the mechanical evidence -- `phylax`, `ephoros` and `hypomnema` each exit 0 and print `clean`; Elenchus records a parent assertion failure over 2,481 executed tests with 0 errors and 0 skipped, naming `test_a_citation_carrying_a_query_string_still_replays_its_object` and three parametrisations of `test_refuses_a_comment_citation_that_names_no_thread` as red on the parent bytes; `python3 -m unittest discover -s tests` reports `Ran 1623 tests` and `OK`; and `python3 scripts/run_checks.py --base <the step-1 branch>` reports `outcome green` at exit 0 with 10 checks passed, `hexaemeron-suite` at 511.3s and `root-suite` at 236.7s. The severities below are assigned from those mechanisms rather than carried from the round's own grading.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R5-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `measure_below_minimum` ran before validation and read `source_group_id` into a set element and `evidence_tier` into a dict key. A list or an object in either raised an uncaught `TypeError`, printing a traceback and exiting 1 where the amended Exit reserves 2 for an unsafe read. Both are skipped there now and reported by the validation that follows, which is the shape the `family_id` and polarity guards already had. | fixed in cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a |
| S2-R5-02 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | The two comment kinds read their id from the URL fragment and never read the path before it, so a citation naming a blob, a commit or a release replayed a comment at exit 0. The path has to cite the thread now, and `#discussion_r` under an issue thread is refused. | fixed in cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a |
| S2-R5-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `cited_path` dropped the fragment and kept the query, so GitHub's own `?plain=1` permalink was compared against the path and a citation that agreed was refused with exit 2. | fixed in cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a |
| S2-R5-04 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | A duplicate `specimen_id` was not refused while a duplicate `family_id` was, so two byte-identical negative rows counted as two negatives and carried a high-value family's whole negative minimum out of one document. Independence stays the positives-only rule the register asks for. | fixed in cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a |

Leads not pursued: none of the four sit in the endpoint-shape or endpoint-identity classes rounds 3 and 4 closed, and no new finding reached `fetch_source_object`'s endpoint construction. `S2-R4-05` stays accepted for the reason round 4 recorded: the natural repair pins a line contract the amended Exit's refusal list does not name, and Step 3 writes the collector that produces `source_start_line` and `source_end_line`. `S2-R1-04` stays accepted: `--report` is still written with `write_text` before findings are collected. The `issue_comment` label still disagrees with the collection a pull request's conversation comment is fetched from, because such a comment is an issue comment on GitHub and legitimately carries `#issuecomment-`; the replay follows the citation, so the object fetched is the cited one and only the label disagrees.

## Step 2, round 6 -- 2026-09-09T04:43:38Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: No socket opened in this round, so every replay probe establishes the argv the checker builds rather than what api.github.com does with it, and `--verify-sources` has still never run against GitHub because `specimens.jsonl` is 0 bytes. Two questions this round's own fix raises are open for the same reason: whether `gh api --hostname github.com` takes precedence over `GH_HOST` at the real `gh`, which the fix does not rely on because it also removes `GH_HOST` and `GH_REPO` from the child, and whether `gh` still consults the working directory's remote for a host once `--hostname` is given. Which thread a comment belongs to is still not established: GitHub keys a comment by id alone, so `S2-R5-02`'s path check establishes that a citation names a thread, not that it names that comment's thread. Also not checked: whether `blob/<sha>/<path>`, `raw/<sha>/<path>`, `commit/<sha>` and `commits/<sha>` are the only citation forms GitHub serves for those objects, since the accepted pair per kind comes from reading URLs rather than a documented list, so a legitimate citation in a fifth form is refused with exit 2, and a percent-encoded or space-bearing path is refused the same way because `endpoint_segment`'s `source_path` pattern admits neither; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling, carried from round 3. `issue-1298.md` is still absent from the README's frozen-digest table, so the wording oracle is unpinned and an edit made consistently to both the oracle and the catalogue would leave every test green; closing it needs a network read this round did not make. `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`. The Pashov pair and `fizz` did not run, under the recorded waiver `waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`. `python3 scripts/run_checks.py` with no `--base` selects nothing on a clean tree and returns `outcome nothing-selected`, so the green recorded below is the `--base` run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | The replay's host was never pinned. `fetch_source_object` builds a relative API path and `gh_fetch` ran `gh api <path>` with the process environment inherited, and `gh`'s own documentation gives `GH_HOST` as "the GitHub hostname for commands where a hostname has not been provided, or cannot be inferred from the context of a local Git repository". The checker provided no hostname, so an operator environment or the working directory's remote decided where a specimen's text was sent and compared, while the register's `specimen-provenance` mitigation and the README both read as a replay of one GitHub object. Reproduced against a recording stub: with `GH_HOST=ghe.attacker.example` and `GH_REPO=attacker/repo` the checker exited 0, the recorded argv was `api -H Accept: application/vnd.github.raw repos/wildcat-finance/skills/contents/README.md?ref=0000...` with no hostname, and the child saw `GH_HOST=[ghe.attacker.example]`. The fix runs `gh api --hostname github.com` and removes both variables from the child, so the pin is in the argv and neither variable can name a host or a repository the specimen never cited; the same probe now records `--hostname github.com` and `GH_HOST=[unset]`. | fixed in 5c352bbdbc14a18c142708529104f58fe897a69d |
| S2-R6-02 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | Each family row declares `minimum_positive` and `minimum_negative`, `TIER_MINIMUMS` in the checker enforces them, and nothing joined the two: `grep` finds the row's own two fields nowhere in the checker except as output keys of `below_minimum`, which are filled from the table. The only other copy of the table is a literal at `plugins/hexaemeron/tests/test_imprimatur_family_evidence.py:782`, and the test module runs the checker as a subprocess and imports nothing from it, so no test reads `TIER_MINIMUMS`. Reproduced on a scratch copy: dropping the table's `signal` pair to (1, 0) left the shipped catalogue still declaring 2 and 1, the report printing `minimum_positive: 1, minimum_negative: 0` beside `causal_negative_passive`, `below_minimum` still 13 so the step's own proof command held, and exit 0. The amended Exit already states that these two fields are set from the tier; a new `family-minimum` finding class compares the declared pair with the enforced one, and the drifted row now exits 1. | fixed in 5c352bbdbc14a18c142708529104f58fe897a69d |
| S2-R6-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `--min-independent-positive` overrode the minimum for every tier the run measured, not for one. `boundary`, `existing-family` and `future` require nothing, so the flag on its own lifted them above zero and put all 42 families in `below_minimum` with a `minimum_positive` their tier does not require, in the field the README documents as "the minimums its tier requires". Reproduced against the shipped fixture: `--allow-below-minimum --min-independent-positive 2` reported 42 entries across all five tiers, against 5 for `--min-independent-positive 2 --tier high-value` and 13 for neither flag. The flag now needs `--tier` to name the tier it overrides and is refused with exit 2 without it, which is the pairing the README and the design record's `two-independent-specimens` gate already used. | fixed in 5c352bbdbc14a18c142708529104f58fe897a69d |
| S2-R6-04 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py and plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md | `8663c546` removed "replays each specimen against its immutable GitHub object" from the README under `S2-R4-03` and left the same sentence in the `--verify-sources` argparse help, which is the copy `--help` prints at the command line. Reproduced by reading `8663c546`'s own README diff against the surviving help string. Four of the six kinds send no reference and compare the current body, which is what round 4 established; the help text now names the two that do send one, and a guard asserts that `--help` does not describe the replay as immutable. | fixed in 5c352bbdbc14a18c142708529104f58fe897a69d |

Leads not pursued: Round 5's four status cells each name `cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a`, and no object of that id exists: `git cat-file -e cb6d39bb1ff09a71fc73e6c8bb9b4dc0e3b48c6a^{commit}` fails while the same command succeeds for `cb6d39bb0226cda55ac3254bc2f9b2cceed7efb3`, which is also what `git rev-parse --verify cb6d39bb` resolves to and what that round's controller receipt records. The commit round 5's four findings were fixed in is therefore `cb6d39bb0226cda55ac3254bc2f9b2cceed7efb3`. Round 5's record is receipted and its bytes stay as written, so the correction is carried here; it is a defect in the audit file rather than in the product. `S2-R4-05` was re-derived rather than carried and stays accepted for the reason round 4 recorded: `source_start_line` and `source_end_line` are read by nothing, `grep` finds neither name in the checker, and a `markdown_paragraph` row carrying 999999 and 1000000 against a two-line replayed file exits 0 both locally and through the replay; the natural repair pins a line contract the amended Exit's refusal list does not name, and Step 3 writes the collector that produces those two fields. `S2-R1-04` stays accepted and was re-derived: `--report` is still written with `write_text` at `run()` before `collect_findings` is called, is still not bounded below `--fixture`, and its counts are still taken from raw rows. The `issue_comment` label still disagrees with the collection a pull request's conversation comment is fetched from, because such a comment is an issue comment on GitHub and legitimately carries `#issuecomment-`; the replay follows the citation, so the object fetched is the cited one and only the label disagrees. Eight more required fields are read by nothing, which is `S2-R4-05`'s class and stays accepted with it: `decision`, `rewrite`, `reason`, `origin`, `selection_seed` and `selection_rank_within_group` on the specimen row, and `overlaps` and `discovery_phrases` on the family row. Both family fields were checked against the shipped catalogue by hand rather than by the checker: every `overlaps` entry names a row that exists, 31 of the 42 rows carry an empty list, and the 29 non-target families carry no `discovery_phrases`, which matches the README's account of specimens being optional for them. Step 1's record left "a `rewrite` required on an `actionable` positive" with the Step 2 checker; of the three it named, `start_byte < end_byte` and `family == family_id` landed and this one did not, and the amended Exit's refusal list does not name it either, so it stays with the Step 3 collector alongside `S2-R4-05`. Nothing compares `decision` with `polarity` for the same reason. `measure_below_minimum` still counts independence over raw rows while `collect_findings` applies `group_id_problem` to validated rows only, so the report can name a family whose independence the findings also refuse; both paths exit 1, so nothing passes on the difference. `validate_schema` still applies `pattern` with `re.search`: every patterned field that reaches an endpoint passes `endpoint_segment` instead, and the two that do not fail closed downstream, a `family_id` carrying a trailing newline through the family join and a `text_sha256` through the digest comparison. `FamilyCatalogueWordingTest.setUp` still reads `families.jsonl` with `str.splitlines()`, the reader `jsonl_rows` replaced in the checker at `1f4fada3`; a smuggled row would make that test see 43 rows and fail, and the checker refuses the same file with exit 2, so both fail closed. `gh_fetch` still checks `MAX_FETCH_BYTES` after `capture_output` has buffered stdout, so the cap bounds the compared value rather than the memory, and the 30-second timeout is the live bound. The `resolved.is_relative_to(fixture)` branch is still an unguardable time-of-check backstop reachable only if a symlink appears between `is_symlink()` and `resolve()`. Two `gh` stubs in the test module interpolate a value into a single-quoted shell string, at `recording_gh` and in `test_refuses_a_gh_reply_that_is_not_the_shape_its_kind_expects`; the interpolated values are module constants and payload literals carrying no quote, so neither can break its own quoting today, and the stub this round adds takes its reply and both log paths from the environment instead. The new `family-minimum` check joins the catalogue to the checker's table and not the test module's literal to either, so the two tables can still drift from each other while the catalogue tracks whichever one the checker holds. `--min-independent-positive 0 --tier high-value` still measures that tier's negative minimum, which is the flag's stated scope. Review basis: the whole step diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..9ae1d3036f605d3a0ddf5a2df4047dcba54d47e4`, 11 files, 2,473 insertions, 14 deletions, read in full rather than re-derived from rounds 1 to 5, then the fix at 5c352bbdbc14a18c142708529104f58fe897a69d, 4 files, 193 insertions, 14 deletions. Round 5's four findings were recorded from their fixes commit's message and diff rather than from a review of the whole diff; that review is this round's, and it re-derived rounds 1 to 5 rather than taking them on trust. Phylax 0; Ephoros 0; Hypomnema 0, taken twice with the changed paths passed as separate arguments, at the step head and again after the fix. Root suite `python3 -m unittest discover -s tests` on the fixed tree: `Ran 1623 tests`, `OK`, exit 0, and green again inside `.githooks/greenlight` over the staged fix. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit`: `outcome green`, exit 0, 11 checks selected and passed, `root-suite`, `hexaemeron-suite`, `imprimatur-suite`, `dead-code-suite`, `dead-code-suppressions-check`, `demonstrations-suite`, `front-door-check`, `joined-front-door-suite`, `lint-ephoros`, `lint-hypomnema` and `lint-phylax`. Hexaemeron suite 2,485 tests, 0 failures, 0 errors, 0 skipped, from 2,481 at round 5; Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 54 tests`, `OK`, from 50 methods at the head; `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests` exit 0. Elenchus ran the step's own runner contract against 5c352bbdbc14a18c142708529104f58fe897a69d: status `guarded`, detail `the runner report records a parent assertion failure`, 2,485 executed, 5 assertion failures, 0 errors, 0 skipped, exit 0. Those five are the four guards this round adds -- `test_the_replay_pins_the_github_host`, named in the retained diagnostic, `test_refuses_a_declared_minimum_that_disagrees_with_its_tier`, `test_refuses_an_independent_positive_override_without_a_tier` and `test_help_does_not_call_the_replay_immutable` -- plus `test_a_comment_replays_its_own_comment_id_not_its_issue_number`, whose argv equality this round extended with the host pin and which therefore goes red on the parent for the `S2-R6-01` mechanism rather than for a defect. The step's Exit commands hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. `family-count`: 42 `###` headings each with a `Disposition` line, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl`, whose 42 `family_id` values are distinct and whose `source_issue` is one value; tiers `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16, and every row's declared minimums now equal its tier's enforced pair by the checker's own comparison. `issue-wording`: `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46`, and the issue's own evidence packet still yields the 5 and 8 target identities the catalogue assigns. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five frozen digests equal the current files. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen`, `shell=True` or `__import__` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list, `shell=False`, a 30-second timeout, a 4,194,304-byte output cap, a pinned hostname and a child environment stripped of `GH_HOST` and `GH_REPO`; `os` was added for that strip and reads the environment only. `span-integrity`, `independence`, `annotation-order` and `partial-write` were re-derived at their existing guards and are unchanged by this round's fix. The Imprimatur lint reports 0 defects at `--max-defects 0` on the changed README; its three `rule_of_three` signals are still the issue's own section headings, and its `the endpoint` opener cadence signal predates this round. Commit 5c352bbdbc14a18c142708529104f58fe897a69d is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 2, round 7 -- 2026-09-09T05:51:16Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: No socket opened in this round, so every replay probe establishes the argv the checker builds rather than what api.github.com does with it, and `--verify-sources` has still never run against GitHub because `specimens.jsonl` is 0 bytes. Round 6's two open questions stay open for the same reason: whether `gh api --hostname github.com` takes precedence over `GH_HOST` at the real `gh`, and whether `gh` still consults the working directory's remote for a host once `--hostname` is given. This round's own repair adds three limits of its own. The register and the digest-table exclusion set are both editable, so drift now needs a deliberate edit to a named list with a stated reason rather than an omission; it is not impossible. Three of the six joins are asserted by the test module against the shipped fixture and the checker's own constants, so a run against another `--fixture` directory gets `schema-contract` and `family-overlaps`, which are checker-side, and not the README-table or digest-coverage comparisons. And the eight required fields the register defers are still enforced by nothing: they are declared, reported and asserted, which is a different claim. Rounds 1 to 5's individual findings were not re-derived one by one this round; the class round 6 named was re-derived as a class, the two carried accepted findings were re-derived by probe, and the checker at the round-7 head, 807 lines, both schemas and the whole README were read in full. Which thread a comment belongs to is still not established: GitHub keys a comment by id alone, so `S2-R5-02`'s path check establishes that a citation names a thread, not that it names that comment's thread. Also not checked: whether `blob/<sha>/<path>`, `raw/<sha>/<path>`, `commit/<sha>` and `commits/<sha>` are the only citation forms GitHub serves for those objects, since the accepted pair per kind comes from reading URLs rather than a documented list, so a legitimate citation in a fifth form is refused with exit 2, and a percent-encoded or space-bearing path is refused the same way; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling, carried from round 3. Pinning `issue-1298.md` does not settle whether it is the issue's current body: it raises a consistent edit from two files to three, and closing it needs a network read this round did not make. `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`. The Pashov pair and `fizz` did not run, under the recorded waiver `waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`. `python3 scripts/run_checks.py` with no `--base` selects nothing on a clean tree and returns `outcome nothing-selected`, so the green recorded below is the `--base` run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R7-01 | medium | plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md and plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | The README's tier-minimums table says of itself that "the table above is what it enforces, so a row declaring anything else is a finding rather than a second answer". Nothing held it to that. One contract had four copies -- this table, the checker's `TIER_MINIMUMS`, a literal at `test_imprimatur_family_evidence.py:872`, which round 6 recorded at line 782 before its own fix added lines above it, and every catalogue row -- and round 6 joined only the last two. Reproduced against a mirror of the fixed step head: editing the `signal` row to 1 and 0 while the checker enforced 2 and 1 left `Ran 54 tests`, `OK`. The test module imports `TIER_MINIMUMS` instead of restating it, and `test_the_readme_tier_table_states_the_enforced_minimums` reads the README's four numeric columns against it, so the table is the stated source and the other three are compared with it. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |
| S2-R7-02 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | A schema's `required` list states what a row must carry and the checker states what is checked, and the two were separate lists with nothing between them, so a field could be required and enforced by nothing with that fact visible in neither. This is the mechanism behind `S2-R4-05` and behind round 6's eight further fields, and finding them one at a time is what four rounds did. Reproduced twice: dropping `origin` from the specimen schema's `required` list left `Ran 54 tests`, `OK`, so the declaration itself could shrink unnoticed; adding a required `weight` nothing reads did the same. `FIELD_ENFORCEMENT` now names, for each of the 13 family and 24 specimen required fields, either the check that enforces it beyond its own schema clause or the runbook step that writes it; the `schema-contract` finding class compares that register with each `required` list in both directions, so a field cannot enter or leave a schema without a decision; and `--report` carries the eight deferrals under `unenforced_fields` with their row file and owner. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |
| S2-R7-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `TIERS` and `TIER_MINIMUMS` were two declarations of one five-name set. `collect_findings` refused a tier outside `TIERS` and then indexed `TIER_MINIMUMS` with it, so a name in one and not the other raised an uncaught `KeyError`, printed a traceback and exited 1 -- the code the amended Exit reserves for a content finding, not the 2 it reserves for an unsafe read. Reproduced by adding `promising` to `TIERS` and to the family schema's enum: `exit 1`, `KeyError: 'promising'`. This is `S2-R1-01`, `S2-R3-03` and `S2-R5-01`'s exit-code class inside the checker's own constants. `TIERS = tuple(TIER_MINIMUMS)` makes the divergence unwritable rather than guarded, so no test is owed for it. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |
| S2-R7-04 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `FIXTURE_SEED` in the checker and the specimen schema's `selection_seed` const were two copies of one literal, and the family schema's `evidence_tier` enum a third copy of the tier set. Reproduced: changing `FIXTURE_SEED` left `Ran 54 tests`, `OK`, and the report's `seed` then named a seed no specimen was allowed to declare; adding a sixth `evidence_tier` to the enum left `Ran 54 tests`, `OK`, while the checker would have refused every row carrying it. Both are `schema-contract` comparisons now, and each refuses with exit 1 and no traceback. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |
| S2-R7-05 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `overlaps` declares a link to another family and nothing resolved it, so a row naming a family that does not exist in this catalogue exited 0. Reproduced. Round 6 read the shipped list by hand rather than by the checker. `family-overlaps` is `specimen-unknown-family`'s check applied to the catalogue's own referential field, and it runs after the family loop because it needs every id the catalogue carries. The shipped catalogue is unchanged by it: 11 rows carry `overlaps`, no entry is dangling and none names its own row. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |
| S2-R7-06 | medium | plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md and plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | `issue-1298.md` is the oracle the wording test compares the catalogue against, it carried no frozen digest, and the digest table's own extent was joined to nothing but a hard-coded `assertEqual(len(table), 5)`, so a file could go unpinned by being left out. Reproduced as round 6 described it: changing one `Disposition:` line in the oracle and the matching `disposition` field in `families.jsonl` left `Ran 54 tests`, `OK`. A `### Fixture inputs` table pins `families.jsonl` at `97ec47f13248b60a269123e116e2689a1285b693b14520abb127ec9b7258d8e8`, `issue-1298.md` at `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46`, `schemas/family.schema.json` at `46244a6a6a9386b903aa16731f4b4f30df07945b2e3221320544b243aafa8185` and `schemas/specimen.schema.json` at `ed8de25920f263308ed22928b603dcbd351230595b521af471d1f144dd1700c9`, and `test_the_fixture_digest_table_covers_every_fixture_file` walks the fixture and compares the table with the files actually present, in both directions. Two exclusions are named with their reasons: this README, which cannot hold its own digest, and `specimens.jsonl`, which Step 3 writes. Step 3 also adds `selection-rejections.jsonl` and refills `families.jsonl`, so that test going red is how it learns to update the table. | fixed in b92610e9b7dc89d10d056cfedd47d35cbf043464 |

Leads not pursued: The class round 6 named -- a declaration in one place with nothing joining it to the behaviour that enforces it -- was attacked as a class rather than instance by instance, and what closes it is three properties rather than six repairs. One divergence is now unwritable instead of guarded, `TIERS = tuple(TIER_MINIMUMS)`. Every required field must name its enforcement or its owner, and each schema's `required` list is compared with that register in both directions, so a new unjoined declaration cannot be added and an old one cannot be removed without a red check. The digest table's extent is compared with the files the fixture holds, so a file cannot be unpinned by omission. What is not closed is stated in `Not checked` above rather than claimed here: a deliberate coordinated edit to the register or the exclusion set still passes, three of the six joins are test-side and bind the shipped fixture only, and eight required fields remain enforced by nothing. Five leads in the class were judged out of this step and left: nothing compares `decision` with `polarity`, nothing requires a `rewrite` on an `actionable` positive, nothing requires `source_start_line` below `source_end_line`, nothing requires a `discovery_phrases` entry on a `high-value` or `signal` family, and a row that overlaps itself exits 0. Each was reproduced at exit 0. The first four constrain a specimen row, which is what Step 3's collector writes, so a refusal added here would be a contract that step has not been built against -- the reason round 4 accepted `S2-R4-05` -- and the amended Exit's refusal list names none of them. The fifth is degenerate rather than wrong, no shipped row does it, and the issue states no rule against it. All five are now visible in the register or the report rather than only in a source comment, which was the property that let them be rediscovered five times. `S2-R4-05` stays accepted and was re-derived: `source_start_line` and `source_end_line` are still read by nothing, and a `markdown_paragraph` row carrying 999999 and 1000000 against a two-line replayed file still exits 0. What changed is that both fields are now declared deferrals in `FIELD_ENFORCEMENT` and named in `--report`, with Step 3's Exit sentence as the reason. `S2-R1-04` stays accepted and was re-derived: `--report` is still written with `write_text` at `run()` before `collect_findings` is called, is still not bounded below `--fixture`, and its counts are still taken from raw rows. The `issue_comment` label still disagrees with the collection a pull request's conversation comment is fetched from, because such a comment is an issue comment on GitHub and legitimately carries `#issuecomment-`; the replay follows the citation, so the object fetched is the cited one and only the label disagrees. `measure_below_minimum` still counts independence over raw rows while `collect_findings` applies `group_id_problem` to validated rows only, so the report can name a family whose independence the findings also refuse; both paths exit 1. `validate_schema` still applies `pattern` with `re.search`: every patterned field that reaches an endpoint passes `endpoint_segment` instead, and the two that do not fail closed downstream. `FamilyCatalogueWordingTest.setUp` still reads `families.jsonl` with `str.splitlines()`; a smuggled row would make that test see 43 rows and fail, and the checker refuses the same file with exit 2, so both fail closed. `gh_fetch` still checks `MAX_FETCH_BYTES` after `capture_output` has buffered stdout, so the cap bounds the compared value rather than the memory, and the 30-second timeout is the live bound. The `resolved.is_relative_to(fixture)` branch is still an unguardable time-of-check backstop reachable only if a symlink appears between `is_symlink()` and `resolve()`. Two `gh` stubs in the test module still interpolate a value into a single-quoted shell string, at `recording_gh` and in `test_refuses_a_gh_reply_that_is_not_the_shape_its_kind_expects`, and the interpolated values are module constants and payload literals carrying no quote. One process note belongs here. The fix was first committed as `4373a4f96a4aaf67fff6fce2cdd63eb1e92f689e`, and two of its new tests would have raised `KeyError` and `ValueError` on the parent rather than asserting, which is an error rather than an assertion failure and would have made the Elenchus report mixed and the verdict `inconclusive`. Both were changed to fail on a comparison, greenlight was re-taken and the commit was amended to `b92610e9b7dc89d10d056cfedd47d35cbf043464` before anything referenced the first object; no receipt, record or synopsis names it. Review basis: the whole step diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..adace2118413dc6cd89722ea20f3bd192a3dff92`, 11 files, 2,672 insertions, 14 deletions, then the fix at `b92610e9b7dc89d10d056cfedd47d35cbf043464`, 4 files, 457 insertions, 23 deletions. Phylax 0; Ephoros 0; Hypomnema 0, with the three changed paths passed as separate arguments on the fixed tree; the lints were not re-taken at the step head, where rounds 1 to 6 recorded 0 against a tree this round did not change. Root suite `python3 -m unittest discover -s tests` on the fixed tree: `Ran 1623 tests`, `OK`, exit 0, and green again inside `.githooks/greenlight` over the staged fix, which recorded tree `0f058a772eaa087090dc26d7842f759bf55c1be2`. The same command at the round-7 head before the Horos regeneration failed `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan` on the `.md` byte count, 19411041 against 19414755, which is this round's own README edit and not a defect; `horos.py scan . --write` and `--census --write` after staging reproduced the committed pair. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit`: `outcome green`, exit 0, 11 checks selected and passed, `dead-code-suite`, `dead-code-suppressions-check`, `demonstrations-suite`, `front-door-check`, `hexaemeron-suite` at 380.7s, `imprimatur-suite`, `joined-front-door-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax` and `root-suite` at 193.0s. Hexaemeron suite 2,492 tests, 0 failures, 0 errors, 0 skipped, from 2,485 at round 6, taken twice; its two `0/0 tests passed` lines and one `scheduler-error` object come from tests that drive the runner in a child under a deliberate write-failure fixture, not from this diff. Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 61 tests`, `OK`, from 54 at round 6; `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests` exit 0. Elenchus ran the step's own runner contract against `b92610e9b7dc89d10d056cfedd47d35cbf043464` with `--timeout 2400`, because the contract names no timeout and the script's 900-second default has been exceeded by this suite: status `guarded`, detail `the runner report records a parent assertion failure`, complete, 2,492 executed, 6 assertion failures, 0 errors, 0 skipped, exit 0. The six red-on-parent tests were identified from this round's own diff rather than from Elenchus's output, by overlaying the changed test module onto a mirror of the parent's `skills/imprimatur` tree at `adace2118413dc6cd89722ea20f3bd192a3dff92` and running it there: `test_refuses_a_required_field_no_enforcement_accounts_for`, `test_refuses_a_schema_tier_the_checker_does_not_enforce`, `test_refuses_a_schema_seed_that_is_not_the_fixture_seed`, `test_refuses_an_overlaps_entry_that_names_no_family`, `test_the_report_names_every_field_this_step_does_not_enforce` and `test_the_fixture_digest_table_covers_every_fixture_file`, six failures and 0 errors. The seventh new test, `test_the_readme_tier_table_states_the_enforced_minimums`, passes on the parent by design, because the parent's README and its `TIER_MINIMUMS` still agree; it guards this change rather than a defect. The step's Exit commands hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. `family-count`: 42 `###` headings each with a `Disposition` line, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl`, whose 42 `family_id` values are distinct and whose `source_issue` is one value; tiers `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16, every row's declared minimums equal its tier's enforced pair, and 29 rows carry no `discovery_phrases` while all 13 evidence targets carry some, which is round 6's hand check now made by the checker for the first of those two fields. `issue-wording`: `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46` and is now in a frozen-digest table for the first time. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five sibling frozen digests equal the current files. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen`, `shell=True` or `__import__` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list, `shell=False`, a 30-second timeout, a 4,194,304-byte output cap, a pinned hostname and a child environment stripped of `GH_HOST` and `GH_REPO`; no `socket`, `urllib`, `http` or `requests` import exists. `span-integrity`, `independence`, `annotation-order`, `partial-write` and `specimen-provenance` were re-derived at their existing guards and are unchanged by this round's fix. The Imprimatur lint reports 0 defects at `--max-defects 0` on the changed README, score 100.0; its three `rule_of_three` signals are still the issue's own section headings, and it adds one new `from_x_to_y_sweep` signal at 345:29, `from two files to three,`, which is this round's own sentence about the oracle and is a signal rather than a defect. Commit `b92610e9b7dc89d10d056cfedd47d35cbf043464` is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 2, round 8 -- 2026-09-09T11:51:55Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; family-count=reviewed; issue-wording=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; untrusted-text=reviewed; fetch-boundary=reviewed; specimen-provenance=reviewed; partial-write=reviewed; holdout-leak=not-applicable

Not checked: No socket opened in this round, so every replay probe establishes the argv the checker builds rather than what api.github.com does with it, and `--verify-sources` has still never run against GitHub because `specimens.jsonl` is 0 bytes. Carried and still open: whether `gh api --hostname github.com` takes precedence over `GH_HOST` at the real `gh`, and whether `gh` still consults the working directory's remote once `--hostname` is given; which thread a comment belongs to, because GitHub keys a comment by id alone, so `S2-R5-02`'s path check establishes that a citation names a thread rather than that it names that comment's thread; whether `blob/<sha>/<path>`, `raw/<sha>/<path>`, `commit/<sha>` and `commits/<sha>` are the only citation forms GitHub serves for those objects, so a legitimate citation in a fifth form is refused with exit 2; whether `endpoint_segment`'s `[0-9]{1,20}` bound matches any documented GitHub id ceiling; and whether `issue-1298.md` is the issue's current body, which needs a network read this round did not make. Round 7's three disclosed limits stand unchanged: a deliberate coordinated edit to `FIELD_ENFORCEMENT` or the digest-table exclusion set still passes, three of its six joins are test-side so another `--fixture` directory gets only the checker-side `schema-contract` and `family-overlaps`, and eight required fields remain declared and reported rather than enforced. This round's own repair adds one limit: `SCHEMA_DECLARATIONS` names six clauses rather than every clause `validate_schema` applies, so a schema weakening a clause outside those six -- `text`'s `type`, say -- still reaches a named content finding at exit 1 rather than a refusal. That is correct behaviour today and is a judgement about which reads are unsafe, not a proof that the six are all of them. `S2-R8-01`'s fixes commit message says all seventeen shapes "printed a traceback and exited 1". Re-derived against the parent bytes at `66c48620e8e26c142775d144470c4a228904b7a7`, thirteen of the seventeen shapes constructed here did that, two exited 1 as a named `family-schema` or `schema-contract` finding with no traceback, and two -- a `type` list carrying a non-predicate and a `type` list carrying a non-string -- exited 0, because `any()` short-circuits before it indexes the predicate table. The seventeen that commit counted are not recoverable from the tree, so the two sets may differ; what is established is that all seventeen constructed here exit 2 with no traceback at the round-8 head. Two reading limits of this round: rounds 6 and 7's individual findings were not re-derived one by one, only rounds 1 to 5 as the round's first job asked; and the 1,208-line test module was read at its head, its round-8 addition and the regions each probe needed rather than end to end, while the checker was read in full at 1,060 lines and again after the fix at 1,149, and both schemas in full. `issue-1298.md`'s 581 lines and `families.jsonl`'s 42 rows were re-derived mechanically against the catalogue and the digest table rather than read line by line, as rounds 1 to 4 did. `holdout-leak` is not applicable because `specimens.jsonl` is empty, no specimen is selected in this step, and neither the checker nor its tests names `labelled-prose-v1`. The Pashov pair and `fizz` did not run, under the recorded waiver `waived: the run produces prose, JSON lexicon data and Python tests only; no Solidity`. `python3 scripts/run_checks.py` with no `--base` selects nothing on a clean tree and returns `outcome nothing-selected`, so the green recorded below is the `--base` run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R8-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py and plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | A row's fields pass a schema clause and, where one reaches an endpoint, `endpoint_segment`; a `gh` reply's fields pass `reply_value`. The two schema documents were the third input read from below `--fixture`, and every keyword the validator dereferences was taken on trust. Seventeen valid-JSON shapes were constructed and run against the parent bytes at `66c48620e8e26c142775d144470c4a228904b7a7`: thirteen raised an uncaught `AttributeError`, `KeyError`, `TypeError` or `re.PatternError`, printed a traceback and exited 1 -- the code the amended Exit reserves for a content finding, not the 2 it reserves for an unsafe read -- two exited 1 as a named finding with no traceback, and two exited 0. `read_schema_below` now walks each document once through `require_schema_document` and refuses what the validator cannot read: a document that is not an object, a `type` naming no predicate, `required` or `enum` that is not a list of names, a `pattern` that is not a compilable string, a non-numeric `minLength`, `minItems`, `minimum` or `maximum`, and a `properties` or `items` value that is not an object, at every depth. All seventeen exit 2 with no traceback, and `git diff` over `schemas/` across the whole step prints nothing, so the shipped schemas are unchanged by it. The gate covers everything `validate_schema` dereferences, whose only subschema recursions are `properties/<key>` and `items`. Fixed by a Warden of this round that was lost on a rate limit before it could record; that commit is GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and names `S2-R8-01`. | fixed in d5bc3914fb215f48c52ee7e2bb1530f7bf6ec1e4 |
| S2-R8-02 | low | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py | `SCHEMA_TYPE_NAMES` and `validate_schema`'s own local predicate table were two declarations of one seven-name set. `require_schema_document` admitted any name in the tuple and `validate_schema` then indexed the table with it, so a name in one and not the other raised an uncaught `KeyError`, printed a traceback and exited 1 -- the code the amended Exit reserves for a content finding, not the 2 it reserves for an unsafe read. Reproduced against the round-8 head bytes by adding `date` to `SCHEMA_TYPE_NAMES` and declaring `form` as `date` in the family schema: `exit 1`, `KeyError: 'date'`. This is `S2-R7-03`'s mechanism exactly, reintroduced one round after round 7 closed it for `TIERS`, by `S2-R8-01`'s own gate. `SCHEMA_TYPE_NAMES = tuple(SCHEMA_TYPE_PREDICATES)` over a module-level table makes the divergence unwritable rather than guarded, so no test is owed for it and none is added, which is the judgement round 7 recorded for `TIERS`. | fixed in a660f1ea56a9cb76009e374341489808b982441f |
| S2-R8-03 | medium | plugins/hexaemeron/skills/imprimatur/scripts/check_family_evidence.py, plugins/hexaemeron/tests/test_imprimatur_family_evidence.py and plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md | Gating a schema document made it readable and did not make its clauses load-bearing where the rows are read. Six clauses carry the safety of this checker's own reads: `family_id` on a family row and `specimen_id`, `family_id` and `source_group_id` on a specimen row are hashed, iterated character by character and NFC-normalised here, and `polarity` and `source_object` decide whether a row counts at all and which endpoint replays it. `schema-contract` joins each `required` list, the family schema's `evidence_tier` enum and the specimen schema's `selection_seed` const, and none of these six. All reproduced at the round-8 head with a schema below `--fixture` that `require_schema_document` accepts, because a property declaring no `type` is legal: dropping the four string clauses and supplying a row the weakened schema admits raised `TypeError: cannot use 'dict' as a set element` at `check_family_evidence.py:759` on the family's `family_id` and again on `specimen_id` and the specimen's `family_id`, `TypeError: normalize() argument 2 must be str, not dict` on an object `source_group_id`, and `AttributeError: 'int' object has no attribute 'isspace'` on a list one -- five shapes, each a traceback at exit 1 where 2 is reserved. Dropping `polarity`'s enum was quieter and worse: the row cleared every check, counted as neither polarity, contributed to no independence count, and the fixture exited 0; `source_object`'s enum did the same to the choice of replay endpoint. `require_declarations` reads both schemas before any row is read and refuses all seven shapes with exit 2, `SCHEMA_DECLARATIONS` states the clause each keyed field stands on, and the shipped schemas satisfy it unchanged. Swept and sound in the same pass: `overlaps` and `group` on a family row and `text`, `text_sha256`, `family`, `annotated_before_lint`, `start_byte` and `end_byte` on a specimen row each reach a named finding at exit 1 or are `isinstance`-guarded, so none of them raised. | fixed in a660f1ea56a9cb76009e374341489808b982441f |

Leads not pursued: This round's first job was to re-derive rounds 1 to 5 finding by finding against the current head, which round 7 disclosed it had not done. All 21 were re-derived by probe against the head at `d5bc3914fb215f48c52ee7e2bb1530f7bf6ec1e4`, each with a recording `gh` stub that opens no socket or with `PATH` stripped of `gh`, and none regressed. Nineteen hold repaired: `S2-R1-01`, a specimen row missing `repository` exits 1 with `schema missing keys` and a `repository` outside `^wildcat-finance/` exits 1 with `schema pattern mismatch`, neither reaching the stub; `S2-R1-02`, the catalogue's five `high-value` and eight `signal` identities equal the issue's own evidence packet and the test reads `issue_evidence_targets()` rather than counting; `S2-R1-03`, `issue_families()` returns the same 42 records with and without a `Disposition:` line injected under a later section; `S2-R2-01`, `wildcat-finance/../evil-org/evil-repo` exits 2 with `repository is not one pinned wildcat-finance repository` and no `gh` call; `S2-R2-02`, `.../issues/1298#issuecomment-42` replays `repos/wildcat-finance/skills/issues/comments/42` at exit 0; `S2-R2-03`, `README.md#x` exits 2 with `unusable source_path`; `S2-R3-01`, 40 hex plus a newline exits 2 with `unusable source_commit`; `S2-R3-02`, `.../issues/١٢٣` exits 2 with `cannot read an object number`; `S2-R3-03`, all four reply shapes -- not JSON, missing `commit`, missing `commit.message`, a JSON array -- exit 2 with no traceback; `S2-R3-04`, a row declaring `evidence_tier` twice exits 2 with `duplicate JSON key`; `S2-R3-05`, `ENDPOINT_SEGMENTS` still holds exactly `repository`, `source_path`, `source_commit`, `object_number` and `comment_id`, `fetch_source_object` reads no row field into an endpoint that the table does not gate, and its four `gh_fetch` calls take a fixed argv list; `S2-R4-01`, a row declaring one repository while citing another, a `markdown_paragraph` citing another path and a `commit_message` citing another sha each exit 2 with no `gh` call; `S2-R4-02`, the NFD spelling of `wildcat-finance/skills:docs/café.md` exits 1 with `is not in Unicode normal form NFC`; `S2-R4-03`, the README no longer claims a blanket immutable replay and names the two-of-six split; `S2-R4-04`, the unreachable post-replay digest comparison is gone from the `--verify-sources` loop and the README says `text_sha256` is compared against the row's own `text` before the replay rather than after it; `S2-R5-01`, a raw `source_group_id` list and a raw `evidence_tier` list each exit 1 with no traceback; `S2-R5-02`, `#issuecomment-42` under `blob/<sha>/README.md` exits 2 with no `gh` call; `S2-R5-03`, GitHub's own `?plain=1` permalink replays `repos/wildcat-finance/skills/contents/README.md?ref=<sha>` at exit 0; `S2-R5-04`, two byte-identical negative rows exit 1 with `duplicate specimen_id`. The two accepted findings are unchanged and stay accepted. `S2-R1-04`: `args.report.write_text` still precedes the `collect_findings` call in `run()`, the report is still not bounded below `--fixture`, and its counts are still taken from raw rows. `S2-R4-05`: `source_start_line` and `source_end_line` are still read by nothing, both now carry an `OWNED_ELSEWHERE` row naming `step-3` and `S2-R4-05`, and `--report` lists them under `unenforced_fields`. The `issue_comment` label still disagrees with the collection a pull request's conversation comment is fetched from, because such a comment is an issue comment on GitHub and legitimately carries `#issuecomment-`; the replay follows the citation, so the object fetched is the cited one and only the label disagrees. The round's second job was to sweep for a class outside declaration and enforcement, which round 7 closed. `S2-R8-01` is one instance of a real class and that class is not closed: it is the checker's trust in an input it reads from below `--fixture`, and `S2-R8-03` is the same class one level out. Every input this process reads from outside itself was swept. Row fields: gated by a schema clause, by `endpoint_segment` where one reaches an endpoint, and now by `SCHEMA_DECLARATIONS` where the clause itself is load-bearing. The `gh` reply: gated by `reply_value`, re-derived at `S2-R3-03`. The two schema documents: gated by `require_schema_document` for what the validator reads and by `require_declarations` for what this file keys on. `sys.argv`: a missing fixture directory, `--fixture` naming a file, `--fixture` reached through a symlink, `--report` with an absent parent, `--report` naming a directory, an unknown `--tier`, `--min-independent-positive` without `--tier`, a negative value, a non-integer and a missing `--fixture` each exit 2 with no traceback, ten probes. The environment: `GH_HOST` and `GH_REPO` are removed from the child and the host is pinned in the argv, and `PATH` decides which `gh` runs, where an absent `gh` exits 2 with `gh call failed`, a `gh` exiting non-zero exits 2 with `gh exited 4`, and a reply over the 4,194,304-byte cap exits 2 with `gh returned more than the fetch cap allows`. The fixture directory itself: an absolute path, a `..` component, a symlink on any parent and an oversized file are each refused, and `resolved.is_relative_to(fixture)` remains the unguardable time-of-check backstop reachable only if a symlink appears between `is_symlink()` and `resolve()`. `issue-1298.md` and the README are read by the test module only, and the first is digest-pinned. So the class has one open edge, stated in `Not checked` above: `SCHEMA_DECLARATIONS` is a named list of six clauses rather than a derivation, so a further unsafe read added later without its row would not be refused. Review basis: the whole step diff `fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit..d5bc3914fb215f48c52ee7e2bb1530f7bf6ec1e4`, 11 files, 3,304 insertions, 14 deletions, then the fix at `a660f1ea56a9cb76009e374341489808b982441f`, 4 files, 178 insertions, 17 deletions, leaving the step diff at 11 files, 3,465 insertions, 14 deletions. Phylax 0; Ephoros 0; Hypomnema 0, with the three changed paths passed as separate arguments on the fixed tree; `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests` exit 0. Root suite `python3 -m unittest discover -s tests` inside `.githooks/greenlight` over the staged fix: `Ran 1623 tests`, `OK`, greenlight exit 0, recorded tree `e5ffc2d76b327d7ba6755e76846d1a82a6b6f30b`. The same command before the Horos regeneration failed only `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan`, which is this round's own byte counts and not a defect; the two counts the assertion printed were not captured, and `horos.py scan . --write` and `--census --write` after staging reproduced the committed census. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-1-scaffold-the-fixture-and-commit` on the committed fix: `outcome green`, exit 0, 11 checks selected and passed, `dead-code-suite` at 143.0s, `dead-code-suppressions-check` at 108.0s, `demonstrations-suite` at 33.5s, `front-door-check` at 0.4s, `hexaemeron-suite` at 909.5s, `imprimatur-suite` at 1.2s, `joined-front-door-suite` at 18.5s, `lint-ephoros` at 4.1s, `lint-hypomnema` at 1.9s, `lint-phylax` at 7.0s and `root-suite` at 441.1s, with no `WAI-E-ADAPTER.TIMEOUT` line; it overlapped another repository's `run_checks` run on this machine, which is why those two suites are near twice their round-7 times. Hexaemeron suite `2495/2495 tests passed`, 0 failures, 0 errors, 0 skipped, on the fixed tree, from 2,492 at round 7; it was not re-taken at the round-8 head. Imprimatur suite `112/112 passed`; `test_imprimatur_family_evidence.py` `Ran 64 tests`, `OK`, 64 test methods from 62 at the round-8 head. The step's Exit commands hold on the fixed tree: the report exits 0 with `families` 42 and `below_minimum` 13, the same fixture without `--allow-below-minimum` exits 1, and `--tier promising` exits 2. Elenchus ran the step's own runner contract against `a660f1ea56a9cb76009e374341489808b982441f` with `--timeout 2400`, because the contract names no timeout and this suite exceeds the script's 900-second default: status `guarded`, detail `the runner report records a parent assertion failure`, complete, 2,495 executed, 7 assertion failures, 0 errors, 0 skipped, elenchus exit 0 over a parent test-command exit of 1. The seven red-on-parent results were identified from this round's own diff rather than from Elenchus's output, by restoring the round-8 head checker under the changed test module and running it: the six parametrisations of `test_refuses_a_schema_that_does_not_declare_what_this_checker_keys_on` each failed `0 != 2`, and `test_a_row_an_untyped_schema_admits_reaches_no_unhashable_read` failed `1 != 2` carrying the parent's own `TypeError` at `check_family_evidence.py:759` as the assertion message; 7 failures and 0 errors, so every guard fails on a comparison rather than by raising, which is the shape round 7's amended commit was made to reach. `family-count`: 42 `###` headings in `issue-1298.md`, split 8 / 5 / 6 / 12 / 11, matching `families.jsonl`, whose 42 `family_id` values are distinct and whose `source_issue` is one value, `https://github.com/wildcat-finance/skills/issues/1298`; tiers `high-value` 5, `signal` 8, `boundary` 9, `existing-family` 4, `future` 16. `issue-wording`: `issue-1298.md` is still `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46`. `lint-bytes-drift` and `v1-bytes-drift`: `git diff 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `scripts/imprimatur.py`, `lexicon`, `EVOLUTION.md` and `evals/labelled-prose-v1` prints nothing, and the README's five sibling frozen digests equal the current files, `7522d57632d5ceee515f37355744718853ee82d26c5e549b68571a2dce9ad50a`, `a6ad7adbc6c8e06512032cf460c92749a49a6c139b4f2aee101de8bdc95df844`, `e554ab6f9661d88095f285c6651983c980bd672b854287f74daa288b1dabc34c`, `908e20c6319b587e95fa21de5949a10c0088ed698d546b0a1048686211826240` and `19d88c8bbf1548c99509a964fd0828cc047e6319c4682292ea79973c42a1a606`. The four fixture-input digests also hold unchanged: `families.jsonl` `97ec47f13248b60a269123e116e2689a1285b693b14520abb127ec9b7258d8e8`, `issue-1298.md` `ccff01a9db78693b183a3193b5cd76edbd908f75f3d48b4e25c46fda907f1e46`, `schemas/family.schema.json` `46244a6a6a9386b903aa16731f4b4f30df07945b2e3221320544b243aafa8185` and `schemas/specimen.schema.json` `ed8de25920f263308ed22928b603dcbd351230595b521af471d1f144dd1700c9`. `untrusted-text`: no `eval`, `exec`, `pickle`, `marshal`, `os.system`, `popen`, `shell=True` or `__import__` in the checker. `fetch-boundary`: `subprocess` is still the only outward import and is called once, from `fetch_source_object` under `--verify-sources`, with a fixed argv list, `shell=False`, a 30-second timeout, a 4,194,304-byte output cap, a pinned hostname and a child environment stripped of `GH_HOST` and `GH_REPO`; no `socket`, `urllib`, `http` or `requests` import exists. `span-integrity`, `independence`, `annotation-order`, `partial-write` and `specimen-provenance` were re-derived at their existing guards through the rounds 1 to 5 probes above and are unchanged by this round's fix. The Imprimatur lint reports `score 100.0/100`, `defects 0` at `--max-defects 0` on the changed README, exit 0; its three `rule_of_three` signals are the issue's own section headings and its one `from_x_to_y_sweep` at 370:29 is round 7's own sentence, both signals rather than defects. Commit `a660f1ea56a9cb76009e374341489808b982441f` is GPG signed under key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline. This is round 8 of a ceiling of 8, so the loop leaves no further round.

## Step 3, round 1 -- 2026-09-09T19:28:09Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; holdout-leak=reviewed; specimen-provenance=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; issue-wording=reviewed; partial-write=reviewed; fetch-boundary=reviewed; family-count=reviewed; untrusted-text=reviewed

Not checked: `x-ray`, `solidity-auditor` and `fizz` did not run: the step diff (`2feed2ed4debe8aa014abba7ba14fd17d21c2b74`..`91cfca1d1a8a7b514f363dd5279f499e26c945b0`, 9 files, no `.sol`) carries Python, JSONL, Markdown and `.horos` JSON only, which the recorded waiver covers; the three lints are this round's mechanical part. The collector's network pass was not re-run (about 4,000 `gh` calls), so `specimens.jsonl` (38 rows) and `selection-rejections.jsonl` (600 rows) are the first pass's output plus four `reason` fields relabelled by hand to the string the fixed collector writes for those paths; the shipped bytes were reconciled against the README's collection record instead (per-family Candidates, Judged, Independent positives and Negatives for all 13 target families, source-kind counts 22/8/5/3, origin counts 21/17, all 13 rejection-reason counts, 170 whole-document and 116 paragraph rejections, 352 candidates, and the six fixture-input digests), all equal. Whether GitHub's contents endpoint would serve a dot-prefixed segment was not asked: the four `.agents/` and `.githooks/` documents are outside the universe by the collector's pattern, not by GitHub's answer, and the README's `unusable-source-path` paragraph is what records that bias. The README's figure of 11,702 documents per pass was not recomputed. `--verify-sources` ran once against GitHub over the 38 rows and exited 0, which establishes each row's `text` is a substring of the cited object today; for the 8 thread-kind rows that is the current body, as the README states. The Elenchus verdict is the runner's declared classification and its report bytes are not attested here. `MAX_FETCH_BYTES` is applied after `capture_output` has read the whole reply into memory, so it bounds what is processed, not peak memory; that design is shared with the Step 2 checker and is left as it stands.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py | `fetch_document` interpolated tree-listing paths into `repos/<r>/contents/<path>?ref=<commit>` without `segment`, against the module's rule that every endpoint value fullmatches its `ENDPOINT_SEGMENTS` pattern before the endpoint is built. The tree listing is data from outside the process: a path carrying `?ref=main&x=` would have moved the fetch off the pinned commit onto a moving branch, `#` would have truncated the request, and `docs/Scale Factor.md` was sent with a literal space. The path is gated where the endpoint is built, `corpus_from_network` records a path the pattern refuses without requesting it (the document stays in the list with empty text so `document_rejection` still writes its row), and `test_a_tree_path_cannot_reach_the_contents_endpoint_unchecked` and `test_an_unfetchable_tree_path_is_recorded_rather_than_requested` guard both. Seen to error on the unfixed collector. | fixed in 80a18220e888db4d47a543fc7cbf364c02fcd9e1 |
| S3-R1-02 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py, plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/selection-rejections.jsonl and plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md | `document_rejection` returned `path-carries-whitespace` for a `segment("source_path")` refusal, so four rows (`.agents/skills/promise-machine/PORTABLE.md`, `.agents/skills/promise-machine/SKILL.md`, `.githooks/README.md`, `tests/fixtures/promise-machine/unresolved-router/.agents/skills/promise-machine/SKILL.md`) were recorded under a reason naming a fault they do not have; a reader reproducing the selection would look for a space and find none. A pattern refusal is now `unusable-source-path`, the four rows carry it, the README's reason table reads `path-carries-whitespace` 2 and `unusable-source-path` 4 (the two whitespace rows are `v2-protocol:docs/Scale Factor.md` and `v2-protocol:docs/hooks/templates/Access Control Hooks.md`), the README's `selection-rejections.jsonl` digest moves from `85a8c1ecccbfbcd97717da94aaa88d586434e550bdd2caca25d8aa6ee6d47df9` to `5d63342e63f7c83688652db0153bd73fcaf47e10f2a296b122c951a7ad87f533`, and `test_a_dot_prefixed_path_is_not_recorded_as_carrying_whitespace` compares the shipped file's split with the collector's. Seen to fail on the unfixed collector. | fixed in 80a18220e888db4d47a543fc7cbf364c02fcd9e1 |
| S3-R1-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py | A candidate for a `boundary`, `existing-family` or `future` family left `select` at the `(0, 0)` minimum without a rejection row, against the Exit's "writes every rejected candidate with its reason to `selection-rejections.jsonl`". The shipped file is unaffected because no zero-minimum row in `families.jsonl` carries a `discovery_phrases` entry; the moment one does the record would go silent. Such a candidate is now written as `tier-has-no-minimum`, and `test_a_tier_with_no_minimum_still_records_its_candidates` guards it on all three tiers. Seen to fail on the unfixed collector. | fixed in 80a18220e888db4d47a543fc7cbf364c02fcd9e1 |
| S3-R1-04 | low | plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | `--corpus-in` (`read_corpus`) and `--annotations` (`read_annotations`) are ingestion paths with no malformed-input test, which Phylax names as unfinished. `test_a_recorded_corpus_is_read_back_against_its_own_digest` feeds a row edited after its digest, unreadable JSON, a non-object row, a row without `text` or without `text_sha256`, and a symlink; `test_an_annotation_record_that_cannot_be_keyed_is_refused` feeds a repeated `(family_id, candidate_id)` pair, a record with no `candidate_id` or no `family_id`, a non-string id, a non-object row, unreadable JSON and a symlink. Each is refused with `RefusalError`. The corpus digest sits beside the text in the same file, so it catches corruption and not a recomputed edit; a shipped row's provenance rests on the per-specimen replay, which `--no-verify` skips, and the test docstring says so. No collector change. | fixed in 80a18220e888db4d47a543fc7cbf364c02fcd9e1 |
| S3-R1-05 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py | `fetch_origin` built `repos/<r>/commits?sha=<c>&path=<p>&per_page=1` from `repository`, `commit` and `path` without `segment`. It was safe only by ordering: `build_specimen` calls `replay`, which gates the same three values, before it; a second caller would not inherit that order. The three values are gated where the endpoint is built, and `test_fetch_origin_gates_its_own_endpoint_values` asserts refusal before any `gh` call for a foreign repository, a branch name, a `?ref=main&` path and a path with a space, and the exact argv on a clean call. Seen to fail on the unfixed collector. | fixed in 80a18220e888db4d47a543fc7cbf364c02fcd9e1 |

Leads not pursued: `write_jsonl_atomic` uses the fixed temporary name `<file>.partial`, so two collectors writing one fixture directory at once would race on the rename; the collector is a one-operator build tool with no concurrent use described, and the rename still leaves either the old or the whole new file. `annotated_before_lint` is hard-coded `True` on every row: it records the protocol the README describes and nothing in the collector can verify that no lint ran, which the README already says (the current lint has none of these families). `decision` reads `actionable` on both `redundant_connective_pair` positives while the issue rates the additive shape signal only, because `DECISION_FOR` derives the decision from the tier; the README's Collection record discloses this and leaves it to v2. The thread half of the universe replays a current body, so a body edited after 2026-09-09 would change a later `--verify-sources` answer for the 8 thread-kind rows; the README states this and the fixture's span carries the annotation order. Root suite: 1,623 tests OK under `.githooks/greenlight` on the staged tree `bfb8fa3bec8745905ff57d9b31e0a53318f446e0`. Hexaemeron suite on the fixes commit: 2,516 of 2,516 passed, 0 errors, 0 failures, 0 skipped (`--elenchus-report .hexaemeron/elenchus/step-3-round-1.json`, schema `elenchus.unittest.v1`, complete). Imprimatur suite: 112/112 passed. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-2-write-the-checker-its-tests-and`: outcome green, 11 checks selected and passed (dead-code-suite, dead-code-suppressions-check, demonstrations-suite, front-door-check, hexaemeron-suite, imprimatur-suite, joined-front-door-suite, lint-ephoros, lint-hypomnema, lint-phylax, root-suite). Lints on the fixed tree: `phylax.py plugins tests` exit 0, `ephoros.py plugins tests` exit 0, `hypomnema.py README.md AGENTS.md .agents plugins docs` exit 0; `imprimatur.py` on the fixture README at `--max-defects 0` reports `score 100.0/100`, `defects 0`, exit 0, with three `rule_of_three` signals on the issue's own section headings and one `from_x_to_y_sweep` at 652:29 that are signals, not defects. Exit commands on the fixed tree: the checker with `--allow-below-minimum --report` exits 0 and the report asserts `families == 42` with `below_minimum` exactly `causal_fact_clause_wrapper`, `empty_expletive_case`, `existential_relative_shell`, `reason_is_because`; without `--allow-below-minimum` it exits 1 on those four rows; `git diff --stat 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `evals/labelled-prose-v1`, `scripts/imprimatur.py`, `lexicon` and `EVOLUTION.md` prints nothing and the five frozen digests equal the current files; the holdout assertion passes (16 v1 groups, 31 specimen groups, empty intersection); the focused module runs 85 tests OK. Commit 80a18220e888db4d47a543fc7cbf364c02fcd9e1 is GPG signed under key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 3, round 2 -- 2026-09-10T00:34:05Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; holdout-leak=reviewed; specimen-provenance=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; issue-wording=reviewed; partial-write=reviewed; fetch-boundary=reviewed; family-count=reviewed; untrusted-text=reviewed

Not checked: `x-ray`, `solidity-auditor` and `fizz` did not run: the step diff (`2feed2ed4debe8aa014abba7ba14fd17d21c2b74`..`06910abc17c3fd0127e37d0348c859600fdcd3c5`, 9 files, no `.sol`) carries Python, JSONL, Markdown and `.horos` JSON only, which the recorded waiver covers; the three lints and the fixture README lint are this round's mechanical part. The collector's network pass was not re-run and `--verify-sources` was not re-run: `specimens.jsonl` is byte-identical to round 1's (`406ed81594b9691a20b0c7c5c25e6839d6ba873c4bb616d427ecd2e07d5dcc0a`), so round 1's exit 0 over the 38 rows stands as the online proof; this round replayed offline from the local clone the 22 rows the clone holds (14 `wildcat-finance/skills` Markdown rows at `source_commit:source_path` within `source_start_line`..`source_end_line`, 8 `wildcat-finance/skills` commit messages), all present, and could not replay the 8 `wildcat-docs` rows or the 8 thread rows. The README's 11,702-document figure, its `Judged` semantics beyond the rejection details, and the four short families' bounded absence searches were not recomputed against the universe. `schemas/*.json` digests were not re-hashed by hand; `test_the_fixture_digest_table_covers_every_fixture_file` covers them. The Elenchus verdict is `elenchus.py`'s declared classification from a fresh parent run; its report bytes are not attested here. `MAX_FETCH_BYTES` still applies after `capture_output` has read the reply, as round 1 left it.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | low | plugins/hexaemeron/tests/test_imprimatur_family_evidence.py | `test_a_tree_path_cannot_reach_the_contents_endpoint_unchecked`, the guard S3-R1-01 named for `fetch_document`, passed on the ungated body: with `gh_fetch` unpatched every bad path still raised `RefusalError`, from a stub `gh` answering HTTP 404 (5 of 5 paths, and the request line carried `contents/docs/Scale Factor.md?ref=...`) and from no `gh` on PATH (`gh call failed`), so the assertion held without the fix and would send the very request the fix stops when `gh` is present and authenticated; only `endpoint_path_ok` being absent made the test error on 91cfca1d. The test now patches `gh_fetch`, asserts no call across the five paths, and asserts the exact argv `["api", "-H", "Accept: application/vnd.github.raw", "repos/wildcat-finance/skills/contents/docs/hooks/templates/access.md?ref=<40 zeros>"]` on a clean path. Seen to fail (`RefusalError not raised : docs/Scale Factor.md`) on a copy of the collector with the `segment` call removed from `fetch_document`. | fixed in 06910abc17c3fd0127e37d0348c859600fdcd3c5 |
| S3-R2-02 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py | `fetch_tree`, `thread_ceiling`, `fetch_threads`, `fetch_commits` and `fetch_comments` interpolated `repository` and `commit` as handed to them, gated only by `run` through `parse_head` and the `--threads` pass, so the rule above `ENDPOINT_SEGMENTS` ("every value that becomes part of a gh endpoint fullmatches one pattern named here, before the endpoint is built") held for `fetch_document`, `fetch_origin` and `replay` only, and S3-R1-05's own reason for gating `fetch_origin`, that a second caller would not inherit `run`'s order, applied unchanged to the other five. Each now gates its values where the endpoint is assembled, and `thread_ceiling` refuses a listing whose newest number is below one instead of probing `issues/0`, reading the 404 and returning a ceiling that enumerates no thread. `test_every_endpoint_builder_gates_its_own_values` asserts refusal before any `gh_json` or `gh_fetch_optional` call for four bad repositories (`other-org/repo`, `wildcat-finance/skills?x=`, `-H`, `wildcat-finance/skills/../other`) and four bad commits (`main`, 39 hex, 40 hex plus newline, `?sha=main`) across the five, the exact argv of each clean call, and the below-one refusal. Seen to fail on 2b73ca8d (`Expected 'gh_json' to not have been called. Called 28 times.`). No shipped byte changes. | fixed in 06910abc17c3fd0127e37d0348c859600fdcd3c5 |
| S3-R2-03 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py | The holdout check at the end of `run` intersected the shipped rows' `source_group_id` values with the v1 sample file's, under a comment saying a group derived another way still could not collide; labelled-prose-v1 names its 16 groups `H-TD-01`..`M-TD-03` and this collector names them `<repository>:<path>`, so the two sets are disjoint whatever the rows hold, the check could never fire, and the runbook Exit's `groups & used` assertion is empty for the same reason. The real control is `document_rejection`'s exclusion by `(repository, source_path)` and `(repository, source_commit)`, which `test_a_v1_source_group_is_excluded_whole` guards; offline, no shipped row shares either identity with a v1 row, the largest five-gram Jaccard between any of the 38 texts and any of the 64 v1 sample texts is 0.0079, and all six v1 `skills` document paths still exist at the pinned head `592390722f10df53658906623b15428dbfb88d8f`. `holdout_collisions` now compares the document identities as well as the ids, `run` calls it, and `test_a_shipped_row_from_a_v1_document_is_named_as_a_collision` names a v1 document row and a v1 commit row while passing a Markdown row at a v1 commit and a clean row, keeps the id comparison, and runs the check over the shipped 38 rows against `samples.jsonl`. Seen to fail on 2b73ca8d by assertion (`the collector has no holdout check that reads document identities`). | fixed in 06910abc17c3fd0127e37d0348c859600fdcd3c5 |

Leads not pursued: carried from round 1 and unchanged: `write_jsonl_atomic`'s fixed `<file>.partial` name, `annotated_before_lint` hard-coded `True`, `decision` reading `actionable` on both `redundant_connective_pair` positives, and the 8 thread-kind rows replaying a current body. New this round: `test_the_fixture_is_written_through_a_temporary_file_and_rename` rebinds `os.replace` on the shared `os` module for its duration instead of patching through `unittest.mock`, so any other test in the same process during that window would call the recording function; `gh_call` counts only `HTTP 404` as a hole in the thread sequence, so a deleted thread answered `HTTP 410` would end the whole pass as a refusal; `is_table` rejects any paragraph whose line holds two `|` characters, so prose with a piped inline code span is `table-row`, and no shipped or rejected row carries that reason; `select` treats an annotation with no `select` key as taken; a pass without `--annotations` rewrites the fixture's `selection-rejections.jsonl` with `annotator-rejected` rows for every candidate, so the shipped rejection file is whatever the last pass wrote; a `thread_url` whose tail is not digits is dropped silently in `fetch_comments` rather than recorded. Round-1 fixes re-checked on the current head: S3-R1-01 gate and skip in `fetch_document` and `corpus_from_network`; S3-R1-02 four `unusable-source-path` and two `path-carries-whitespace` rows, `selection-rejections.jsonl` at `5d63342e63f7c83688652db0153bd73fcaf47e10f2a296b122c951a7ad87f533` equal to the README; S3-R1-03 `tier-has-no-minimum` branch in `select`; S3-R1-04 the two ingestion tests present; S3-R1-05 `segment` on all three values in `fetch_origin`. README collection record against the fixture bytes: per-family `Candidates` (17, 30, 18, 14, 16, 93, 35, 20, 13, 25, 33, 15, 23) and `Judged` (9, 3, 18, 14, 3, 3, 3, 8, 4, 9, 3, 15, 23) recomputed from `specimens.jsonl` plus the selection-phase rejections and equal; 170 whole-document and 116 paragraph-rule rejections; 313 `minimum-already-met` of which 237 unannotated, 71 annotated negative and 5 annotated positive; all 13 reason counts equal; source kinds 22/8/5/3 and origin 21/17 equal; `families.jsonl`, `issue-1298.md`, `selection-rejections.jsonl` and `specimens.jsonl` digests equal to the README's table. All 38 rows: `text_sha256` recomputes, `start_byte < end_byte <= len(text)` and the span decodes as UTF-8, `annotated_before_lint` true, `specimen_id` unique, and positives per family on distinct `source_group_id`. Lints on the fixed tree: `phylax.py plugins tests` exit 0, `ephoros.py plugins tests` exit 0, `hypomnema.py README.md AGENTS.md .agents plugins docs` exit 0; `imprimatur.py` on the fixture README at `--max-defects 0` exit 0, `score 100.0/100`, `defects 0`. Exit commands on the fixed tree: the checker with `--allow-below-minimum --report` exits 0 and the report asserts `families == 42` with `below_minimum` exactly `causal_fact_clause_wrapper`, `empty_expletive_case`, `existential_relative_shell`, `reason_is_because`; without the flag it exits 1; `git diff --stat 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `evals/labelled-prose-v1`, `scripts/imprimatur.py`, `lexicon` and `EVOLUTION.md` prints nothing and the five frozen digests equal the current files; the holdout assertion passes (16 v1 groups, 31 specimen groups, empty intersection, which S3-R2-03 shows is by namespace); the focused module runs 87 tests OK (85 before this round); the Imprimatur suite 112/112. Root suite: 1,623 tests OK under `.githooks/greenlight` on the staged tree `f1d6a611ff6730ed8b707e644a22267937696d1c`. Hexaemeron suite on the fixes commit: 2,518 of 2,518 passed, 0 errors, 0 failures, 0 skipped (`--elenchus-report .hexaemeron/elenchus/step-3-round-2.json`, schema `elenchus.unittest.v1`, complete). Elenchus guard check with the runbook's runner contract (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus/step-3.json`) against `06910abc17c3fd0127e37d0348c859600fdcd3c5`: parent report complete, 2,518 executed, 2 assertion failures, 0 errors, 0 skipped, changed test file `plugins/hexaemeron/tests/test_imprimatur_family_evidence.py`, status `guarded`. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-2-write-the-checker-its-tests-and` on the fixes commit, source identity `c2e2de40464527e9ecbdf0472e1c0ab2212519dfd821a327fc85e04fac0c77bc`, ran twice and was red both times for different reasons: the run finishing at 2026-09-09T21:04:46Z returned `outcome red`, `failures test-failure`, `dead-code-suite failed` at 339.2 s with the other 10 checks passed; the run finishing at 2026-09-10T00:27:05Z returned `outcome red`, `failures scheduler-error`, `root-suite failed` at 129.0 s with the other 10 checks passed and `hexaemeron-suite` at 643.6 s. Neither red reproduced and neither cause is established: the human format retains no per-check output or reason, no load measurement exists for the first run, and during the second the 5- and 15-minute load averages read 15.3 and 14.7 at 00:06Z and 21.8 and 22.8 at 00:28Z while other worktrees ran their suites. On the same source identity, `python3 -m unittest tests.test_dead_code -v` alone, finishing 2026-09-10T00:06:09Z, ran 250 tests OK in 49.8 s, and `python3 scripts/run_checks.py --scope root --format json`, 2026-09-10T00:28:07Z to 00:32:45Z, returned `outcome green`, exit 0, 9 checks selected and passed, `root-suite` exit 0 with `Ran 1623 tests` in 270.8 s and `dead-code-suite` 250 tests OK in 53.2 s; `hexaemeron-suite` and `imprimatur-suite` passed in both `--base` runs, so each of the 11 `--base` checks has passed on this source identity, and no single `--base` run has. Commit `06910abc17c3fd0127e37d0348c859600fdcd3c5` is GPG signed under key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 3, round 3 -- 2026-09-10T02:44:27Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; holdout-leak=reviewed; specimen-provenance=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; issue-wording=reviewed; partial-write=reviewed; fetch-boundary=reviewed; family-count=reviewed; untrusted-text=reviewed

Not checked: `x-ray`, `solidity-auditor` and `fizz` did not run: the step diff (`2feed2ed4debe8aa014abba7ba14fd17d21c2b74`..`cb426891b89fd42409e698f0779658f618fee179`, 11 files, no `.sol`) carries Python, JSONL, Markdown and `.horos` JSON only, which the recorded waiver covers; the three lints and the fixture README lint are this round's mechanical part. The collector's network pass was not re-run; `specimens.jsonl` is byte-identical to rounds 1 and 2 (`406ed81594b9691a20b0c7c5c25e6839d6ba873c4bb616d427ecd2e07d5dcc0a`) and `--verify-sources` was re-run over its 38 rows (below). The README's 11,702-document figure, its 27-repository listing and the four short families' absence searches were not recomputed against the universe; the per-family `Candidates` and `Judged` columns were not recomputed this round, because the candidate rows are the same bytes round 2 recomputed them from and S3-R3-01 removes only whole-document rows. The Elenchus verdict is `elenchus.py`'s declared classification from a fresh parent run; its report bytes are not attested here. `MAX_FETCH_BYTES` still applies after `capture_output` has read the reply, as rounds 1 and 2 left it.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R3-01 | low | plugins/hexaemeron/skills/imprimatur/scripts/collect_family_evidence.py, plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/selection-rejections.jsonl and plugins/hexaemeron/skills/imprimatur/evals/structural-family-evidence-v1/README.md | `v1_exclusions` added every v1 row's `source_commit` to the excluded commit set, not only the commit-message rows', so the ten commits v1's twelve Markdown documents were sampled at (their `source_commit` values in `labelled-prose-v1/samples.jsonl`) were excluded from the universe and written as `v1-source-group`; a commit's message is a different document from a file read at that commit and is in no v1 source group, so the README's rule ("Prose from the 16 v1 source groups") and the reason named a fault ten rows do not have, and the universe was ten commit messages narrower than the rule states. Established from the shipped record: the 20 `v1-source-group` commit rows split 11 `v2-protocol`, 8 `skills`, 1 `wildcat-docs`, which is the 10 v1 commit-message shas plus the 10 Markdown `source_commit` values exactly, and v1's commit groups and Markdown commits are disjoint. The ten messages were fetched through `gh api` and walked with the collector's own `paragraphs` and `discovery_phrases`: none carries a phrase, so no candidate, specimen or `Judged` count changes. The set is now built from `source_object == "commit_message"` rows, the ten rows are removed (600 to 590 rows; `v1-source-group` 32 to 22, whole-document rejections 170 to 160; digest `5d63342e63f7c83688652db0153bd73fcaf47e10f2a296b122c951a7ad87f533` to `d23f94f7b927fef41a0980f9fa5b67ac5c75dff464b5909fde4cc539743ddb26`), the README's counts, digest table and Collection record say so, and `test_a_commit_a_v1_paragraph_was_read_at_is_not_a_v1_group` asserts the set on a two-row sample file, `document_rejection` on both commits, and one shipped row per v1 document (12) and per v1 commit-message sha (10). Seen to fail on 0c730f8e241e9fe4af904b30326af47fbcabb1df (`Items in the first set but not the second: ('wildcat-finance/skills', '3333333333333333333333333333333333333333')`). The over-exclusion was conservative for the holdout, so no shipped specimen is affected. | fixed in cb426891b89fd42409e698f0779658f618fee179 |

Leads not pursued: carried from rounds 1 and 2 and unchanged: `write_jsonl_atomic`'s fixed `<file>.partial` name; `annotated_before_lint` hard-coded `True`; `decision` reading `actionable` on both `redundant_connective_pair` positives; the 8 thread-kind rows replaying a current body; `gh_call` counting only `HTTP 404` as a hole so an `HTTP 410` ends the pass as a refusal; `is_table` reading two `|` on one line as a table row (no shipped or rejected row carries `table-row`); `select` treating an annotation with no `select` key as taken; a pass without `--annotations` rewriting `selection-rejections.jsonl` with `annotator-rejected` rows; a `thread_url` whose tail is not digits dropped silently in `fetch_comments`. `test_the_fixture_is_written_through_a_temporary_file_and_rename` rebinding `os.replace` on the shared module is carried with its reason settled: `run_tests.py` runs each shard's tests sequentially in its own worker process, so no other test shares the window. New this round: a whole-document rejection row for a commit message or a thread carries no sha and no thread number (`source_path` and `candidate_id` are null), so the 10 `v1-source-group` commit rows, the 19 `unknown-thread` rows and the 2 `excluded-issue` rows cannot be tied to an object from the record alone, and the ten rows S3-R3-01 removes were identified by repository count rather than by sha; the Exit binds candidates, which all carry `candidate_id`, and the shape is left for a pass that re-collects. `v1_exclusions` keys documents by `(repository, source_path)` whatever commit the path is read at, so a v1 document renamed after its sample would not be excluded by path; all twelve v1 paths exist at the pinned heads and were rejected. `fetch_threads` refuses a reply whose `number` differs from the one requested, which a transferred thread's redirect would trigger and end the pass; the recorded pass completed, so no such thread was in the sequence. Round-1 and round-2 fixes re-checked on the current head: S3-R1-01 `segment("source_path")` in `fetch_document` and the `endpoint_path_ok` skip in `corpus_from_network`; S3-R1-02 four `unusable-source-path` and two `path-carries-whitespace` rows, still present after the S3-R3-01 removal; S3-R1-03 `tier-has-no-minimum` in `select`; S3-R1-04 `test_a_recorded_corpus_is_read_back_against_its_own_digest` and `test_an_annotation_record_that_cannot_be_keyed_is_refused`; S3-R1-05 three `segment` calls in `fetch_origin`; S3-R2-01 `gh_fetch` patched and `assert_not_called` in the tree-path test; S3-R2-02 `segment` where each of the five builders assembles its endpoint and the below-one ceiling refusal; S3-R2-03 `holdout_collisions` reading document identities, called from `run`. All 38 rows: `text_sha256` recomputes, `0 <= start_byte < end_byte <= len(text)` and the span decodes as UTF-8, `annotated_before_lint` true, `selection_seed` the fixture seed, `specimen_id` unique, negatives with `decision` `negative` and no `rewrite`, positives with a `rewrite`, and positives per family on distinct `source_group_id` (2/2 for nine families, 1/1 for `reason_is_because` and `existential_relative_shell`); source kinds 22/8/5/3 and origin 21/17 equal the README. `families.jsonl` differs from `2feed2ed4debe8aa014abba7ba14fd17d21c2b74` in `discovery_phrases` only, on 42 rows. `labelled-prose-v1` samples carry `markdown_paragraph` and `commit_message` rows only, so the document-and-commit exclusion covers every v1 group. `--verify-sources` with `--allow-below-minimum`, 2026-09-10T00:52:18Z to 00:52:35Z: exit 0 over the 38 rows. Lints on the fixed tree: `phylax.py plugins tests` exit 0, `ephoros.py plugins tests` exit 0, `hypomnema.py README.md AGENTS.md .agents plugins docs` exit 0; `imprimatur.py` on the fixture README at `--max-defects 0` exit 0, `score 100.0/100`, `defects 0`. Exit commands on the fixed tree: the checker with `--allow-below-minimum --report` exits 0 and the report asserts `families == 42` with `below_minimum` exactly `causal_fact_clause_wrapper`, `empty_expletive_case`, `existential_relative_shell`, `reason_is_because`; without the flag it exits 1 on those four; `git diff --stat 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `evals/labelled-prose-v1` (with `candidate-freeze.json`), `scripts/imprimatur.py`, `lexicon` and `EVOLUTION.md` prints nothing and the eleven README digests equal the current files; the holdout assertion passes (16 v1 groups, 31 specimen groups, empty intersection); the focused module runs 88 tests OK (87 before this round); the Imprimatur suite 112/112. Root suite: 1,623 tests OK under `.githooks/greenlight` on the fixes tree `f09f632b323c854060a2e5f378fab0ca21a4d779`, which is the tree of cb426891b89fd42409e698f0779658f618fee179. Elenchus on the fixes commit: `elenchus.py` classified `guarded`; the parent run executed 2,519 Hexaemeron tests with 1 assertion failure, `test_a_commit_a_v1_paragraph_was_read_at_is_not_a_v1_group` on the unfixed parent, and 0 errors, and the fixed tree ran the same module green. `python3 scripts/run_checks.py --base fiat/1298-imprimatur-structural-prose-family-evidence-step-2-write-the-checker-its-tests-and --format json` on the fixes tree: outcome green, 11 checks selected and passed (dead-code-suite, dead-code-suppressions-check, demonstrations-suite, front-door-check, hexaemeron-suite, imprimatur-suite, joined-front-door-suite, lint-ephoros, lint-hypomnema, lint-phylax, root-suite) at 2026-09-10T02:43:49Z. Two earlier attempts on the same tree did not reach green: the first ended `unstable-source` because the worktree changed under it, and the second was red on one test, `tests.test_demonstrations.RunnerExecutionBoundaryTests.test_a_grandchild_that_leaves_the_process_group_is_refused` (`0 != 2`), while a greenlight root suite and the Elenchus runner were executing beside it; run alone on a quiet machine the test passes (`Ran 1 test`, OK), and the green run above is the one this record stands on. Commit cb426891b89fd42409e698f0779658f618fee179 is GPG signed under key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, carries `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, and adds no host byline.

## Step 3, round 4 -- 2026-09-10T02:58:59Z

Audit schema: fiat-audit-round/v2

Covered: lint-bytes-drift=reviewed; v1-bytes-drift=reviewed; holdout-leak=reviewed; specimen-provenance=reviewed; span-integrity=reviewed; independence=reviewed; annotation-order=reviewed; issue-wording=reviewed; partial-write=reviewed; fetch-boundary=reviewed; family-count=reviewed; untrusted-text=reviewed

Not checked: `x-ray`, `solidity-auditor` and `fizz` did not run: the step diff (`2feed2ed4debe8aa014abba7ba14fd17d21c2b74`..`5909415fd9400b8fcc4e203d41be12a9ef8785f5`, 11 files, no `.sol`) carries Python, JSONL, Markdown and `.horos` JSON only, which the recorded waiver covers; the three lints and the fixture README lint are this round's mechanical part. The collector's network pass was not re-run; `specimens.jsonl` is byte-identical to rounds 1 to 3 (`406ed81594b9691a20b0c7c5c25e6839d6ba873c4bb616d427ecd2e07d5dcc0a`). The README's 11,702-document figure, its 27-repository listing, the 1,181-thread count and the four short families' whole-universe absence searches were not recomputed against the universe. The README's `14 prose paragraphs across 13 source groups` (`causal_fact_clause_wrapper`) and `15 prose paragraphs across 14 source groups` (`empty_expletive_case`) were checked to a lower bound only: the paragraph counts equal the 14 and 15 candidate rows, and the record holds 9 and 6 distinct Markdown groups beside 4 and 8 non-Markdown candidate rows whose commit or thread identity the rejection rows do not carry, so 13 and 14 hold if the two `pull_request_body` rows of the first and the six `commit_message` rows of the second are distinct objects, which the record cannot show. No fixes commit exists, so Elenchus did not run, and `python3 plugins/hexaemeron/tests/run_tests.py` and `python3 scripts/run_checks.py --base ...step-2-write-the-checker-its-tests-and` were not re-run: `git diff --stat cb426891b89fd42409e698f0779658f618fee179..HEAD` touches `.horos/candidates.json`, `.horos/census.json`, this record and its synopsis only, and both were green on `cb426891`'s tree in round 3. `MAX_FETCH_BYTES` still applies after `capture_output` has read the reply, as rounds 1 to 3 left it.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: each lead carried from rounds 1 to 3 was re-read on `5909415fd9400b8fcc4e203d41be12a9ef8785f5` and decided; none becomes a finding. `write_jsonl_atomic`'s fixed `<file>.partial` name: a one-operator build tool with no concurrent use described, and `os.replace` leaves either the old or the whole new file. `annotated_before_lint` hard-coded `True`: the collector cannot observe whether a lint ran; the README records the protocol and that the current lint has none of these families. `decision` reading `actionable` on both `redundant_connective_pair` positives: `DECISION_FOR` derives it from the tier, `check_annotation` refuses any other value, and the README's Collection record states the cost to a v2 reader; changing it changes the fixture's tier contract, which is the study's. The 8 thread-kind rows replaying a current body: the README states it and `--verify-sources` exited 0 again this round. `gh_call` counting only `HTTP 404` as a hole, so an `HTTP 410` ends the pass: fail-closed, and the recorded pass completed. `is_table` reading two `|` on one line as a table row: no shipped or rejected row carries `table-row`, so the shipped record is unaffected. `select` treating an annotation with no `select` key as taken: `check_annotation` then refuses a wrong polarity, decision, reason, rewrite or span, and annotations are operator-written. A pass without `--annotations` rewriting `selection-rejections.jsonl` with `annotator-rejected` rows: operator tool; the shipped file is the annotated pass's. A `thread_url` whose tail is not digits dropped silently in `fetch_comments`: GitHub's `issue_url` and `pull_request_url` end in the thread number by API shape, not verifiable offline. `test_the_fixture_is_written_through_a_temporary_file_and_rename` rebinding `os.replace`: restored in `finally` at `test_imprimatur_family_evidence.py:1486-1489`, and shards run sequentially per worker. Whole-document rejection rows for commit messages and threads carrying no sha or number: the Exit binds candidates, which all carry `candidate_id`; the consequence is the group-count bound in Not checked, and a shape change would re-pin `selection-rejections.jsonl` without changing a specimen. `v1_exclusions` keying documents by `(repository, source_path)` whatever the commit: the 12 `markdown_paragraph` `v1-source-group` rows equal the 12 v1 document identities exactly. `fetch_threads` refusing a reply whose `number` differs: the recorded pass completed. New this round: `thread_ceiling` probes upward from the listing's newest and stops at the first 404, so a deleted thread directly above an unlisted newer one would lower the ceiling; the hole count the pass printed to stderr is not in the record, so the ceiling reached cannot be checked offline. `read_families`, `read_annotations`, `read_corpus` and the v1 sample read carry no byte cap, unlike the checker's 1,048,576-byte cap; all four read operator-local files. `NOT_PROSE_START` treats a four-space-indented first line as code and an unterminated fence swallows the rest of a document, both biases toward fewer candidates under the README's stated phrase bias. `HEX_LITERAL` matches lowercase hex only. S3-R3-01 re-derived by probe: `v1_exclusions` on `labelled-prose-v1/samples.jsonl` (64 rows: 48 `markdown_paragraph`, 16 `commit_message`) returns 16 groups, 12 documents and 10 commits; the commit set equals the 10 commit-message shas and holds none of the 10 Markdown `source_commit` values, and the two sets are disjoint; `document_rejection` returns `v1-source-group` for a commit-message document at a commit-message sha and `None` at a Markdown sample commit; the 22 `v1-source-group` rows are 12 `markdown_paragraph` (paths equal the v1 document set) and 10 `commit_message` (8 `v2-protocol`, 2 `skills`, equal to the commit-message shas by repository); `holdout_collisions` over the 38 rows is empty and no shipped commit row sits at a v1 Markdown commit. Guards from rounds 1 to 3 present at `test_imprimatur_family_evidence.py`: S3-R1-01 at 1707 and 1744, S3-R1-02 at 1767, S3-R1-03 at 1809, S3-R1-04 at 1834 and 1875, S3-R1-05 at 1916, S3-R2-02 at 1947, S3-R2-03 at 2026, S3-R3-01 at 1363; the six tests the runbook's Tests field names at 1320, 1339, 1416, 1429, 1475 and 1507. Files against the effective Exit: `families.jsonl` differs from `2feed2ed4debe8aa014abba7ba14fd17d21c2b74` in `discovery_phrases` only, on the 13 target-family rows; `stacked_epistemic_modal` carries `could plausibly` and `might plausibly`. README cold-grep against the fixture bytes: 20 pinned heads, collection date 2026-09-09, every `markdown_paragraph` and thread row at its repository's pinned head; per-family `Candidates` (17, 30, 18, 14, 16, 93, 35, 20, 13, 25, 33, 15, 23) and `Judged` (9, 3, 18, 14, 3, 3, 3, 8, 4, 9, 3, 15, 23) recomputed from `specimens.jsonl` plus the 314 selection-phase rejection rows and equal; 160 whole-document and 116 paragraph-rule rejections, 352 candidates, 38 specimens; 313 `minimum-already-met` of which 237 unannotated, 71 annotated negative and 5 annotated positive; all 13 reason counts equal; source kinds 22/8/5/3 and origin 21/17 equal; the one `group-already-used` row is `borrowers.md` line 23 and the `reason_is_because` positive is `borrowers.md` line 156; the six fixture digests and the five frozen digests equal the current files; `issue-1298.md` has 42 `###` headings, each carrying a Disposition line. All 38 rows: `text_sha256` recomputes, `0 <= start_byte < end_byte <= len(text)` and the span decodes as UTF-8, `annotated_before_lint` true, `specimen_id` unique, `decision` follows polarity and tier on every row, negatives carry no `rewrite` and positives carry one, every text inside the 18 to 180 word band and clean under `paragraph_rejection`, positives per family on distinct `source_group_id` (2/2 on nine families, 1/1 on `reason_is_because` and `existential_relative_shell`, 0/2 on `causal_fact_clause_wrapper` and `empty_expletive_case`), and no two rows of one family reach five-gram Jaccard 0.5. Lints on `5909415f`: `phylax.py plugins tests` exit 0, `ephoros.py plugins tests` exit 0, `hypomnema.py README.md AGENTS.md .agents plugins docs` exit 0; `imprimatur.py` on the fixture README at `--max-defects 0` exit 0, `score 100.0/100`, `defects 0`, with three `rule_of_three` signals on the issue's section headings and one `from_x_to_y_sweep` at 663:29, signals not defects. Exit commands: the checker with `--allow-below-minimum --report` exits 0 and the report asserts `families == 42` with `below_minimum` exactly `causal_fact_clause_wrapper`, `empty_expletive_case`, `existential_relative_shell`, `reason_is_because`; without the flag it exits 1; `--verify-sources` with `--allow-below-minimum`, 2026-09-10T02:53:23Z to 02:53:40Z, exit 0 over the 38 rows; `git diff --stat 7d12d63e13fe193fcc1f8827b393f8aa51161731` over `evals/labelled-prose-v1`, `scripts/imprimatur.py`, `lexicon` and `EVOLUTION.md` prints nothing; the holdout assertion passes (16 v1 groups, 31 specimen groups, empty intersection); the focused module runs 88 tests OK; the Imprimatur suite 112/112. Commits `cb426891b89fd42409e698f0779658f618fee179` and `5909415fd9400b8fcc4e203d41be12a9ef8785f5` are GPG signed, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day`, and carry `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`.
