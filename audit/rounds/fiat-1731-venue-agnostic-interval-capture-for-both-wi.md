## Step 1, round 1 -- 2026-09-19T05:17:53Z

Audit schema: fiat-audit-round/v2

Covered: undeclared-venue=reviewed; venue-registry-disagreement=reviewed; registry-pin-bypass=reviewed; registry-source-drift=reviewed; canonical-form-mismatch=not-applicable; market-list-completeness=not-applicable; registered-controller-miscount=not-applicable; shared-subject-double-claim=reviewed; epoch-model-substitution=not-applicable; mid-interval-deployment=not-applicable; per-subject-epoch-tiling=not-applicable; missing-deployment-block=not-applicable; v1-unpinned-code=reviewed; v1-source-identity-overclaim=reviewed; subject-set-attribution=reviewed; plan-version-compatibility=not-applicable; component-budget=reviewed; journal-split-boundary=not-applicable; constructed-staging-claim=reviewed; credential-in-artefact=reviewed; credential-in-error-text=reviewed; trace-reconciliation=not-applicable; unreconciled-count-misread=not-applicable; shard-width-second-transport=not-applicable; v1-archive-depth-unprobed=not-applicable; stale-capture-record=not-applicable; partial-release-write=reviewed; journal-descriptor-leak=not-applicable; venue-test-member-set=reviewed; carried-lead-bind-finality=reviewed; carried-lead-opening-compare=reviewed; carried-lead-undeclared-journal=reviewed

Not checked: `scripts/run_checks.py --base origin/main`, `scripts/promise_machine.py check`, and Imprimatur, Vulgate and Brevitas over the changed prose were not run this round. `hypomnema.py` ran over the two changed Markdown files, not the whole `plugins/alexandria` walk, and its study-mode bridge check was not run: the study carries no `design-bridge` fence and the ordinary walk never infers one. The eight conformance criteria were not executed against venue code, none existing; each was exercised only through its refusal path, so every conformance cell in the design record stays pending and this round establishes none of them. `build_design_evidence.py` was never run against the repository tree, which it would rewrite; it was executed only into temporary copies. Fifteen register ids are marked not-applicable because this step ships no registry, plan, subject set, epoch derivation, journal, release or transport for them to attach to. The overlay resolver was not consulted, the security suite being waived. Elenchus's own exported-tree runs carried 78 infrastructure errors and 2 consequent assertion failures, all from `ModuleNotFoundError: No module named 'eth_hash'`: its closed runner environment sets `PYTHONNOUSERSITE=1` and this host holds the Lazarus runtime dependency only in `/Users/c0rtexzer0/Library/Python/3.14/lib/python/site-packages`. That is why the verdict is inconclusive rather than a judgement on the fix, and the same condition will recur for every Alexandria step on this host. The same suite run in the worktree passes 706 tests.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| W1-R1-01 | medium | plugins/alexandria/tests/test_wildcat_conformance.py | `test_selection_reports_recompute_byte_identically` copied the design record, the model observations and the 24 selection reports into the temporary tree and removed none of them before executing the generator, so the byte comparison read those copies. A generator writing nothing at all passed all 26 paths, and the runbook's obligation that each report recomputes byte-identically from the committed generator was not established. Reproduced: the unamended test passes against a no-op generator; the amended one reports 26 subtest failures. The generator itself was already correct and reproduces all 26 artefacts byte-identically from an emptied tree. | fixed in 9f8c421e |
| W1-R1-02 | low | plugins/alexandria/docs/wildcat-interval/study.md | Section 12 names `docs/adr/<number>-venue-dispatch-for-the-interval-collector.md` as the home of four of the five decisions and says the number is allocated at the scaffold step against the default branch. No `docs/adr` directory exists anywhere in the tree; the established convention is 102 numbered records directly under `docs/decisions/` and 3 unnumbered drafts under `docs/decisions/drafts/`, numbered only by the integration composer. Step 1 is the scaffold step and created no record in either home. Left open rather than fixed here: the study is controller-pinned at SHA-256 67552e500fb7ee17ab082ac6b284f8ead9c5450101224b522abc910f1cd8a713, the step's Exit requires its source bytes preserved, and the runbook assigns the four decisions to Steps 2, 3, 6 and 7, so the repair is a controller study amendment or Step 2's record. | open |

Leads not pursued: fiat-1350's three carried leads stay open by name and unchanged by this step, which touches no collector path: the finality boundary comparison, the first-block header comparison by hash alone, and a manifest journal for a class the plan did not declare. On the report-drift refusal path `conformance.py main` prints its observed record with `"passed": true` and `"reason": null` to stdout before `parser.error` exits 2, so captured stdout alone reads as an establishment while the process refused; not raised, because the assertions did pass, the refusal is about report immutability rather than the criterion, no report file is written, and Protasis consumes the report file rather than stdout. `REJECTED_CANDIDATES` in the test file is a hardcoded triple rather than the record's candidate set minus its selected candidate, so it could go stale against `design-evidence.json`; not raised, because adding or renaming a candidate is already caught by the 56/32/20/4 result-count assertions and by the globbed report filenames. Four tests patch `pathlib.Path.cwd` and `unittest.TestLoader` on the real stdlib modules rather than on module-local aliases, which would be unsafe under an in-process parallel runner; not raised, because `plugins/alexandria/tests/run_tests.py` holds no thread, pool or parallel dispatch. `conformance.py main` inserts a cwd-derived `plugins/alexandria` at `sys.path` position 0; not raised, because that directory exposes exactly one importable top-level name, `tests`, which collides with no stdlib module, and the harness comments the cwd dependency. The published `build_design_evidence.py` writes into its own `parents[1]`, so running the committed copy would rewrite the committed reports; not raised, because its module docstring states the trap and no test executes that copy in place.
