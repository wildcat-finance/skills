## Step 1, round 1 -- 2026-09-23T02:16:58Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The step diff `e9e95b891b3ae642516e017c46b97a18a1480164..fac1493f31e48d259552955563d20c8235620277` carries 0 `.sol` files, so the X-Ray and solidity-auditor in-scope source sets are empty and neither produced analysis output; fizz does not apply because the step ships no Solidity. Byte-equality of each equivalent type to its sealed anchor files, the zero-pass proof for the two excluded test files, the Gate 5 demonstrations and fixture binding of the recorded code hashes belong to later steps and were not checked. Of the 28 profile comparisons, 2 were reproduced independently (`market-lens-app` and `open-access-role-provider` at `v2-e1f7`, both profiles, Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, fresh public clone); the other 26, including both private trees, were not. `run_checks.py` was not run; the root suite and `.githooks/greenlight` were.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `scripts/kickoff_hermes_1355.py` | `validate_custody` matched `^\s*pragma solidity` against the raw bytes only, and SPDX only as a raw substring. A standard-JSON input escapes each source into one string, so `docs/kickoff/1355/evidence/standard-input.json` carrying `"content": "pragma solidity 0.8.25;\ncontract X { uint256 a; }\n"` with no SPDX line passed `check` (reproduced on `fac1493f`). The runbook's Tests clause requires a target or private source file under the docs tree to be refused. Fixed by decoding every `.json` file under the tree and testing each key and string value; a JSON file that does not parse is now refused, and `parse_json` maps `RecursionError` to a named refusal. | fixed in c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7 |
| S1-R1-02 | low | `docs/kickoff/1355/inventory.json` | The `open-access-role-provider` `registry_source_override` was checked only by commit (`5d7f8c889a8d29935838a3906172feb8d9861807` must equal the registry's source commit). Its evidence, the Sourcify source SHA-256 `7a5b57852f433b876f0b43048c74158a740ce0f2708587b31eb682a7c390f84f`, sat in the free-text `reason`, unchecked against `docs/kickoff/1359/evidence/source-match-1590.json`, which records it. The claim itself holds: `src/OpenAccessRoleProvider.sol` at `e1f77540` and `5838b2f3` hashes to that digest in a fresh public clone, and the registry-named private blob `07bc4c91edb64b1255d61173c35e0523ee33f12f49a59d320834256f510e3b9d` differs. Fixed by adding a required `sourcify_source_sha256` field; the checker verifies `source-match-1590.json` against the registry's evidence digest `0bd8d53d351e46082dbb625a499bc7c6cebe1f1f43ab4c9ef2498f23fb79282e`, requires exactly one Sourcify `match` entry at the overridden commit for an inventory address of the type, and requires the field to equal its `source_sha256` and the registry blob to differ. | fixed in c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7 |

Leads not pursued: Reviewed range `e9e95b891b3ae642516e017c46b97a18a1480164..fac1493f31e48d259552955563d20c8235620277`, one signed commit over 29 files; fixes commit `c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7` changes 5 files. Mechanical exits over the 29 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. Root suite `python3 -m unittest discover -s tests` on the fixed tree: 2,189 tests OK, exit 0; `.githooks/greenlight` exit 0; `tests.test_kickoff_hermes_1355` 38 of 38; `scripts/kickoff_hermes_1355.py check` exit 0 with 137 addresses, 17 types, 8 exclusions and 28 of 28 comparisons invariant; `horos.py check .` `boundary matches the tree`. The five later conformance criteria (`owner-handoffs`, `selector-rejection`, `layout-rejection`, `sealed-coverage`, `evidence-custody`) each exit 1 naming the criterion and the transition it blocks. The checker recomputes each blob key as the SHA-256 of Hermes's canonical text, so a map digest is not accepted without content. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json` returned `inconclusive` for `c6d0b088dc9a`: over the whole root suite on the detached parent it counted 2,115 executed, 42 assertion failures, 585 errors and 2 skipped. By hand, the changed test file overlaid on a `git archive` of `fac1493f` ran 38 tests with 3 failures, exactly the three new guards, and 0 errors; on `c6d0b088` it ran 38 OK. The 585 parent-wide errors were not investigated. Four leads were seen and left. First, the capture script `.hexaemeron/design/capture_profiles_1355.py` (SHA-256 `fd17ec67b951eb6cbded967ab8d45a82d2c8c12c0930056a565e9368ea1d36c5`, confirmed) is gitignored, so the committed record names bytes a reader cannot fetch; the README's manual steps were enough to reproduce 2 comparisons. Second, custody refuses by suffix, directory name, private build-input digest and Solidity text, so private test output or private prose under `docs/kickoff/1355` would pass; the tree was grepped and carries none. Third, the fee recipient and role-provider anchor layouts and method maps from private repositories are committed as derived maps, as the README states. Fourth, `.hexaemeron/design-reports/anchor-and-inspect-profile-invariance.json` was written before these fixes and not regenerated; it binds no inventory digest. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 1, round 2 -- 2026-09-23T02:39:35Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The round covers the fixes commit `c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7` and the step as a whole at `2f1f455a032b8943d4654312a72c743dce855c70`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No further profile comparisons were reproduced beyond round 1's 2 of 28. A Solidity source hidden in Markdown through JSON unicode escapes (for example `\u0070ragma`) still passes custody; it needs deliberate obfuscation rather than a copy, and was not fixed. `run_checks.py` was not run.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | low | `scripts/kickoff_hermes_1355.py` | The S1-R1-01 fix decoded `.json` files only. A standard-JSON input pasted into Markdown keeps its escaped newlines, so no line starts with `pragma solidity`, and the SPDX test skips `.md`: `docs/kickoff/1355/evidence/notes.md` holding `{"content": "pragma solidity 0.8.25;\ncontract X {}\n"}` passed `check` on `2f1f455a` (reproduced). Fixed by refusing `pragma solidity` followed by a version constraint anywhere in any file under the tree; a negative control confirms prose naming the keyword alone still passes. | fixed in ce4081de44f16ca0843bdefe4b79ad00242504ce |

Leads not pursued: Fixes commit `ce4081de44f16ca0843bdefe4b79ad00242504ce` changes 4 files. Mechanical exits over the changed paths: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,191 root-suite tests; `tests.test_kickoff_hermes_1355` 40 of 40. The rest of `c6d0b088` held on review: `validate_overrides` needs exactly one Sourcify `match` entry at the overridden commit for an inventory address of the type, the JSON walk is iterative, and a JSON file that fails to parse is refused. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json` returned `inconclusive` for `ce4081de44f1`, counting 2,117 executed, 40 assertion failures, 585 errors and 2 skipped on the detached parent. The cause is the runner's containment, not the parent tree: the round-1 overlay (`c6d0b088`'s test file on a detached worktree at `fac1493f`) run through the same `tests/run_tests.py --elenchus-report` outside Elenchus reported 2,189 run, 0 errors and 4 failures, the 3 round-1 guards plus `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan`, which the overlay's extra bytes trip. Elenchus's runner forbids child processes, and the root suite starts them. By hand, `ce4081de`'s test file on a `git archive` of `2f1f455a` ran 40 tests with 1 failure, the new guard, and 0 errors; on `ce4081de` it ran 40 OK. The two round-1 leads were weighed again and neither is a finding. The gitignored capture script (`fd17ec67b951eb6cbded967ab8d45a82d2c8c12c0930056a565e9368ea1d36c5`) sits outside the step's Files clause, a `.py` file may not ship under `docs/kickoff/1355`, and the README recipe reproduced 2 comparisons. Custody's blindness to private test output or prose is outside the `private-payload` mitigation, which names private source, test source and complete private Hermes directories, all of which the checker refuses, and prose cannot be classified mechanically; the tree carries none. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 1, round 3 -- 2026-09-23T02:45:38Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The round covers the fixes commit `ce4081de44f16ca0843bdefe4b79ad00242504ce` and the step at `9298d3a4d8e33bc6f35b8dfbbc9dda271a015c46`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. A Solidity source hidden in Markdown through JSON unicode escapes (for example `\u0070ragma`) still passes custody; that needs deliberate obfuscation and stays unfixed. Private test output and private prose are not classified. `run_checks.py` was not run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: The custody scan is closed at the `private-payload` and `vendored-source` mitigations. Against `9298d3a4`, six planted specimens under `docs/kickoff/1355` were each refused: test source in a Markdown fence, escaped source in JSON with no SPDX line, escaped source inline in Markdown, a `baseline-sources` directory, a `.sol` file, and a pragma with extra spaces. The unmodified tree passed. Private-source digests stay covered by `test_private_source_digest_under_docs_tree_is_refused`. `ce4081de` adds `PRAGMA_WITH_VERSION` and one alternative in `validate_custody`, and nothing else in the checker. Mechanical exits over the 31 changed paths: phylax 0, ephoros 0, hypomnema 0. `tests.test_kickoff_hermes_1355` 40 of 40; `scripts/kickoff_hermes_1355.py check` exit 0; the five later conformance criteria each exit 1 by name. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 2, round 1 -- 2026-09-23T03:51:37Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=not-applicable; exclusion-loss=not-applicable; compiler-pin-drift=not-applicable; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=not-applicable; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The step diff `bc8390b30bfd0f7b2af118b1e1edf5a9438e2701..d29593fb7b6d3cca4d404a899e77f27fe5c999d0` carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No retained byte recomputes the capture's request, byte and time counts or its attempt list, and the three discarded attempts left no fixture to inspect. The header is self-consistent and matches the registry's recorded hash; whether it is on the canonical chain was not checked. Target-maintainer review of the inventory is not recorded. The comparisons in `validate_fixture_payload` and `validate_release_payload` run only where `.hexaemeron/restricted/` holds the payloads. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | `scripts/kickoff_hermes_1355.py` | The module docstring said `owner-handoffs` conformance "recomputes the committed records from those bytes". It recomputes fixture rows, component digests, plan limits, recorded responses and the release identity, but the capture's request, byte and time counts, UTC time and attempt list are the capture script's own report. On a scratch copy holding the retained payloads, with handoff digests rebound, setting `capture.requests` to 1, `capture.response_bytes` to 1, `capture.elapsed_seconds` to 0.1 or `capture.observed_at` to `2020-01-01T00:00:00Z` each still wrote value `true`. `docs/kickoff/1355/README.md` stated those counts as facts and left them out of its evidence-class section. Fixed by stating in the docstring and README which fields are recomputed and that the capture counts are recorded; compliance with the declared limits rests on the fixture-bound plan and Lazarus's enforcement during capture. | fixed in 800608986ce2edd2637334dbd3ba3b0a975fc825 |

Leads not pursued: Reviewed range `bc8390b30bfd0f7b2af118b1e1edf5a9438e2701..d29593fb7b6d3cca4d404a899e77f27fe5c999d0`, one signed commit over 8 files; fixes commit `800608986ce2edd2637334dbd3ba3b0a975fc825` changes 3 files. Mechanical exits over the 10 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,209 root-suite tests; `tests.test_kickoff_hermes_1355` 58 of 58 in the run worktree, the 2 retained-payload tests included. Re-run from the retained payloads: `lazarus.py verify` exit 0, fixture `460b8f56f11dc1f2dc5e89fa4215b465a75ec411c94a56b50c6cc9985140d4e4`, 137 proof-backed, 1 header-bound, 137 recorded-rpc; `alexandria.py verify` exit 0, `sha256:adab37e8142ab295fab84dbe10509aa51cb9eb722550bef6c1036eef09076551`; offline replay served 137 of 137 code reads equal to the proof records and answered another block with `-32070`. Lazarus's verifier recomputes the header hash, checks each account proof against the state root and each code's keccak against the proved `codeHash`, and requires the RPC records to cover the plan exactly. Of 13 mutation probes on scratch copies with rebound handoff digests, 9 refused under conformance: a row's `code_sha256` or `proof_record_sha256`, `verification.state_root`, `capture.script_sha256`, the Lazarus manifest tool version, the release name, the release time, a capture's `record_count`, and a component's redistribution class; the other 4 are the S2-R1-01 fields. Under conformance no stated digest passes: each release input, handoff artefact, payload component and manifest is hashed from its bytes. `check` alone validates the committed records' shape and cross-record agreement, not the payload. A recorded value labelled `proof-backed`, or a proved value from any source but `proofs.jsonl`, refuses. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-2.json` returned `unguarded` for `800608986ce2`: the commit changes no test file, and a prose correction has no failing test. Five leads were weighed and left. First, the inventory handoff row reads `complete` with reviewer "Warden, step 1 audit rounds 1 to 3" and a note that target-maintainer review is not recorded. The runbook exit asks each row to name producer, reviewer, artefact and digest, and it does, so the report's `true` holds against that contract. Study section 7 names "Warden and target maintainer" for the inventory, baselines and rejections row, "pending the conformance gates", so that review stays open and the `true` does not say it happened. Second, the fixture and release rows name "Warden, step 2 audit" as reviewer, and `.hexaemeron/design-reports/anchor-and-inspect-owner-handoffs.json` was written at 2026-09-23T03:31:25Z, before this round; this round performed that review. Third, `RetainedPayloadTests` skips outside the run worktree, so once that worktree is gone no suite exercises the payload comparisons; the stub-verifier tests reach only refusals that precede them. The payloads are excluded from the tree by custody, so this was left. Fourth, capture attempt 2 went through a loopback relay that hid the RPC URL from Lazarus's secret scan and was discarded unused; the retained tools hold no relay. The capture credential's URL host, URL path segments of 8 or more bytes and bearer were compared byte for byte, without printing, against every file in the run worktree, `.hexaemeron/restricted/` included, and against the step commit's patch: 0 matches. The capture script reads both from the environment and passes neither through argv. Fifth, the five fixture components carry redistribution `permitted` while the registry, source-match and chain-observation inputs carry `unknown`; `permitted` matches the Lazarus fixtures in the tracked Alexandria examples. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 2, round 2 -- 2026-09-23T04:05:18Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=not-applicable; exclusion-loss=not-applicable; compiler-pin-drift=not-applicable; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=not-applicable; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The round covers the fixes commit `800608986ce2edd2637334dbd3ba3b0a975fc825` and the step at `a7dc934665003536a8721a6fdb4de6e8bf64812a`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. The round-1 negative space stands: no retained byte recomputes the capture counts or attempt list, the header's place on the canonical chain was not checked, and the payload comparisons run only where `.hexaemeron/restricted/` holds the payloads. No target-maintainer review of the inventory is recorded. `run_checks.py` was not run.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | low | `docs/kickoff/1355/evidence/owner-handoffs.json` | The inventory handoff row read status `complete` while its note said target-maintainer review of the mapping is not recorded, and the checker accepted only `complete`, so a clean `owner-handoffs` report read as though that review had happened. The orchestrator decided that Step 3 proceeds without it and that the review is carried forward to the run pull request. Fixed by giving the inventory row status `target-maintainer-review-outstanding` with a note naming the carry-forward; the checker accepts that status on the inventory row only, refuses it on any other row, reports `handed_off` 7, `complete` 6 and `review_outstanding` 1, and gates the criterion on every row being handed on. `docs/kickoff/1355/README.md` says the report's `true` does not mean the review happened. | fixed in 69cd15805dcffd3e8e6966ed92876a6040afa339 |

Leads not pursued: `800608986ce2` holds on review: the docstring and README now list what conformance recomputes, place the capture's request, byte and time counts in the recorded class, and tie limit compliance to the fixture-bound plan; the 4 round-1 telemetry probes stay accepted by design and are now described as recorded. Fixes commit `69cd15805dcffd3e8e6966ed92876a6040afa339` changes 5 files. Mechanical exits over the 11 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,211 root-suite tests; `tests.test_kickoff_hermes_1355` 60 of 60 in the run worktree, the retained-payload conformance test included, so the report is still written from the retained payloads under the new status. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-2.json` returned `inconclusive` for `69cd15805dcf`, counting 2,135 executed, 41 assertion failures, 586 errors and 4 skipped on the detached parent; as in Step 1, the runner's containment forbids the child processes the root suite starts. By hand, `69cd1580`'s test file on a `git archive` of `a7dc9346` ran 60 tests with 2 failures, the two new guards, 1 error, `test_committed_tree_passes_check` reading the new `handed_off` key, and 2 skipped; on `69cd1580` it ran 60 OK with the same 2 retained-payload tests skipped, the archive holding no payloads. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 2, round 3 -- 2026-09-23T04:11:18Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=not-applicable; exclusion-loss=not-applicable; compiler-pin-drift=not-applicable; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=not-applicable; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed

Not checked: The round covers the fixes commit `69cd15805dcffd3e8e6966ed92876a6040afa339` and the step at `9ed5fcd7f1db46e9cc6a4df4df2491832678e0de`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. The round-1 negative space stands: no retained byte recomputes the capture counts or attempt list, the header's place on the canonical chain was not checked, and the payload comparisons run only where `.hexaemeron/restricted/` holds the payloads. No target-maintainer review of the inventory is recorded; it is carried forward to the run pull request. `run_checks.py` was not run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `69cd1580` holds on review. Across 21 status probes on scratch copies, one per handoff row for each of `target-maintainer-review-outstanding`, `pending` and `Complete`, `check` accepted only the inventory row with `target-maintainer-review-outstanding` (`handed_off` 7) and refused the other 20. The committed record now hashes to `57de2ade7a074b168bf0187624ecae39ea9b89c8e4a508056f433331159f8200`. From the worktree root, the prior `.hexaemeron/design-reports/anchor-and-inspect-owner-handoffs.json` was moved aside, and `python3 scripts/kickoff_hermes_1355.py conformance --criterion owner-handoffs --candidate anchor-and-inspect --report .hexaemeron/design-reports/anchor-and-inspect-owner-handoffs.json` exited 0 and wrote value `true`, SHA-256 `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, byte-identical to the prior report, with `handed_off` 7, `complete` 6 and `review_outstanding` 1 over both re-verified payloads. Mechanical exits over the 11 changed paths: phylax 0, ephoros 0, hypomnema 0. `tests.test_kickoff_hermes_1355` 60 of 60 in the run worktree. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 3, round 1 -- 2026-09-23T07:17:58Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=reviewed; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The step diff `e053991c03a8918c7f535c7ec16e49d4f2560a31..840fd1d0bd3055b25357b620f075a0a60d79fc28` carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. The five candidate patches are Solidity but are never merged; they were read for rule class and, for `layout-b1-sto04`, for the pack and unpack arithmetic of `TmpEscrowParams`, and were not security-reviewed. No Hermes run on `v2-c7be` was reproduced: the anchor's 795-test pass count, its exclusion's zero-pass property and the `selector-mem16` verify stay recorded. The three Gate 3 attempts were not re-run. The restoration status and stdout digests are recorded only. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | `scripts/kickoff_hermes_1355.py` | `method_check_problems` compared the recorded `before.sha256` with the attempt's Gate 1 `artifact_hashes` but never hashed the committed before map, and accepted any `after.path`; `validate_attempt` bound only the Gate 5 family's before map to Gate 1. On a scratch copy of `840fd1d0` two hand edits passed `check` at exit 0: `after.path` set to `run/method-identifiers/WildcatSanctionsSentinel.before.json` with the supplementary map deleted, and both committed Sentinel method maps replaced by one lacking `WildcatSanctionsEscrowInitcodeHash()`, with `run_files` and `after.sha256` re-pinned and `state.json` untouched. The README said `check` recomputes that the after map equals the map Gate 1 sealed. Fixed: every committed attempt map must hash to its own Gate 1 `artifact_hashes` entry; the check must name `run/method-identifiers/<label>.before.json` and `supplementary/<label>.methods.after.json`, and the committed before bytes are hashed against the sealed entry; an attempt directory holding anything beside `run/` and a declared `supplementary/` is refused. | fixed in ecbe7d78f07b810b9ef43715890c6c63449228b8 |
| S3-R1-02 | low | `scripts/kickoff_hermes_1355.py` | A `verify_argv` without `--rule` made `validate_attempt` raise `ValueError: list.index(x): x not in list` (reproduced: exit 1 with a traceback, no named refusal), against the module's statement that every refusal names the record, the field and the digest. A non-object Gate 2 `rule` or `artifact_hashes` raised the same way. Fixed: `flag_value` reads flag operands, both fields are type-guarded, and any remaining `AttributeError`, `IndexError`, `KeyError`, `TypeError` or `ValueError` in an attempt refuses as `attempts[<n>] is malformed`. | fixed in ecbe7d78f07b810b9ef43715890c6c63449228b8 |

Leads not pursued: Reviewed range `e053991c03a8918c7f535c7ec16e49d4f2560a31..840fd1d0bd3055b25357b620f075a0a60d79fc28`, two signed commits over 72 files; fixes commit `ecbe7d78f07b810b9ef43715890c6c63449228b8` changes 4 files. Mechanical exits over the 72 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,246 root-suite tests; `tests.test_kickoff_hermes_1355` 95 of 95 in the run worktree; `scripts/kickoff_hermes_1355.py check` exit 0. Independent reproduction under `env -i` (`HOME`, `LANG`, `NO_COLOR`, `FOUNDRY_SOLC=0.8.22`, `PATH`; no credential), Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, Python 3.14.6, fresh public clone of wildcat-protocol at `488b30d08c73a93be3e4bf99128c774997411d3a` with all 26 recorded submodule commits: Hermes `baseline` with the recorded argv exited 0 at `baseline_ready`, 348 passed, and its layouts, raw layouts, method maps, source manifest, Forge config, gas snapshot and `artifact_hashes` are byte-equal to `baselines/v1-488b`; the `layout-b1-sto04` patch (`0394583cdf7b83f71d93d0ab4ebe98b3a77c829554a3dd1c2253ef8ae0a9fe77`) with the recorded verify argv passed Gates 1 to 4 and exited 50 with `protected storage layout changed: src/WildcatSanctionsSentinel.sol:WildcatSanctionsSentinel`, its after layout and `gas-comparison.json` byte-equal to the committed ones; `git apply -R` and removing `.gas-snapshot` left a clean status at `488b30d0`. Unexcluded, the same clone ran 354 tests, 348 passed and 6 failed; `test/market/WildcatMarketToken.t.sol` alone ran 0 passed and 6 failed. Gate 3 targets are not cherry-picked: neither selected attempt moved any deterministic measurement up (`layout-b1-sto04` 11 of 236 down by 3,123 each; `selector-mem16` 8 of 500 down by 17 or 22), Gate 3 rejects any positive deterministic delta whatever the target, and the `a1`, `a2` and `a3` regressions were deterministic rows or invariant rows. The selector target `WildcatMarketTest:test_transfer_NotApprovedLender\(\)` is unanchored and matched the `FixedTermWildcatMarketTest` row too; both fell by 22. Rule classes hold: `MEM-16` removes only the unbounded `getMarketsForHooksInstance(address)` overload, and `STO-04` packs three addresses into two words with named offsets, masks and bits 224 to 255 reserved. The committed patches carry hunks only, no whole target file. The three Gate 3 attempts' `state.json` name session-scratchpad run and copy directories: they are Hermes's own bytes, bound by `run_files` digests, the README says so, and they carry no credential, so they were left. The controller runbook (`9c3cd58abd263586e88157bc4ecb56bce01e9890433ca3d87166502aa132d86f`) carries the 2026-09-23 amendment twice, byte-identical; the copy, `RUNBOOK_SHA256`, `documents.runbook` and the owner-handoffs inventory row all name that digest, and `test_runbook_copy_is_pinned_to_the_amended_runbook` guards the copy. The amendment's Why says all three failed attempts ran on v2-protocol `c7be4039`; `layout-a3-sto01` ran on collateral-contract `46dba596fa111f868200358f551796e8f73b5fd7`, as the README and record state. Both anchor baselines' method maps and gas snapshots are recorded; the checker hashes them but cannot recompute them without a build. Before and after the fix, from the worktree root with each prior report moved aside, the four conformance commands exited 0 with value `true` and wrote bytes identical to the prior reports: `owner-handoffs` `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, `selector-rejection` `8d3db84c7ac3476c17da62abc11425ace314af48120c9aae998b6bde07f1f664`, `layout-rejection` `96662f14176a251e50a62bdf6eb251c63be3e109f0ed5d9225a7b0a60ab5782d`, `profile-invariance` `171bf9f516ca099c2c73bd858320da881efed699b732bdfb27b55b0e25f42f1a`. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-3.json` returned `inconclusive` for `ecbe7d78f07b`, counting 2,170 executed, 42 assertion failures, 586 errors and 4 skipped on the detached parent, with no digest rebinds; as in Steps 1 and 2, the runner's containment forbids the child processes the root suite starts. By hand, `ecbe7d78`'s test file on a `git archive` of `840fd1d0` ran 95 tests with 3 failures and 1 error, exactly the four new guards, and 2 skipped; on `ecbe7d78` it ran 95 OK. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 3, round 2 -- 2026-09-23T07:28:46Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=reviewed; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The round covers the fixes commit `ecbe7d78f07b810b9ef43715890c6c63449228b8` and the step at `aa63ab9551c854e912c8219985c12cafa4c92fb9`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. The five candidate patches are never merged and were not security-reviewed. The three Gate 3 attempts were not re-run. The restoration status and stdout digests stay recorded only. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `ecbe7d78` holds on review. A committed attempt file is now tied to its own Gate 1 `artifact_hashes` entry, and that entry to the sealed anchor's, so the committed before map, the attempt's Gate 1 and the anchor form one chain. The two round-1 probes and the missing-`--rule` probe now refuse by name. The catch-all turns only `AttributeError`, `IndexError`, `KeyError`, `TypeError` and `ValueError` inside one attempt into a refusal, so it fails closed. `flag_value` reads the first `--rule`, as the old slice did; a repeated flag is still caught, because Hermes's own Gate 2 `candidate.rule.id` must equal the record's rule. Independent reproduction of `selector-mem16`: `env -i` (`HOME`, `LANG`, `NO_COLOR`, `PATH`; no credential), Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, Python 3.14.6, a fresh public clone of v2-protocol at `c7be4039f8f383a9dda4e45f63331c17d63f9ed9` with all 26 recorded submodule commits. Hermes `baseline` with the recorded argv exited 0 at `baseline_ready` with 795 passed. Every committed `baselines/v2-c7be/run` file other than `state.json` and `result.json`, the `artifact_hashes` and the gas snapshot are byte-equal. The patch (`862b7521efec29a25b341ea7611a4d1f05b5a79be91492d91e59454cb3930354`) under the recorded verify argv passed Gates 1 to 4 and exited 50 with `public method identifiers changed: src/HooksFactory.sol:HooksFactory`. Its `HooksFactory.after.json` and `gas-comparison.json` are byte-equal to the committed ones, and `git apply -R` with `.gas-snapshot` removed left a clean status at `c7be4039`. On the same clone, the unexcluded suite ran 797 tests, 795 passed and 2 failed; `test/vault/Wildcat4626WrapperStandard.t.sol` alone ran 0 passed and 2 failed. Both selected attempts are now reproduced against their anchors. Erratum to round 1: its lead said the controller runbook carries the 2026-09-23 amendment twice, byte-identical. That is wrong. The runbook holds two distinct amendments of that date, Step 3's at line 130 and Step 4's at line 142. Round 1's brief listed the Step 3 amendment twice, which is where the error came from. The Step 3 amendment's Why says all three failed attempts ran on v2-protocol `c7be4039`, but `layout-a3-sto01` ran on collateral-contract `46dba596fa111f868200358f551796e8f73b5fd7`. That prose is controller-pinned, so it is recorded here as an erratum and not changed; the README and the rejection record state the tree correctly. Mechanical exits over the 74 changed paths: phylax 0, ephoros 0, hypomnema 0. `tests.test_kickoff_hermes_1355` 95 of 95; `scripts/kickoff_hermes_1355.py check` exit 0. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 4, round 1 -- 2026-09-23T08:30:29Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=not-applicable; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The step diff `5d321bff238e2307de7d857f3174b55842572947..12cf4a8008634f03ef77c9f1ca49703e0b417450` carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No Gate 1 was reproduced: `collateral-46db`'s 48-test pass count stays recorded, and `fee-ac73` and `role-provider-5d7f` were checked only through `sealed-coverage` over the retained runs. Eight of the eleven equivalence types were not recomputed. The exclusion counts were compared with the surviving Forge logs, not re-run. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | low | `scripts/kickoff_hermes_1355.py` | `check` listed only the top level of `baselines/` and each anchor's `run/`, so anything beside `run/` in an anchor directory passed. On a `git archive` of `12cf4a80` with the retained private runs, copying the retained `fee-ac73` `state.json`, `result.json`, `baseline-source-manifest.json`, `baseline.forge-config.json` and `baseline.gas-rule-corpus.json` into `baselines/fee-ac73/` passed `check` at exit 0; so did a retained `rp-5d7f` `state.json` under `baselines/role-provider-5d7f/gate1/` and an extra file in `baselines/v2-c7be/`. The retained `state.json` names the private tree's file paths, including its test file. The README said `check` refuses a withheld file in a private anchor's public directory. Fixed: each anchor directory may hold only `record.json` and `run/`; in a private anchor, a withheld name is refused as one. | fixed in d2f7e170d5dcc032b2dc282ab29f1870fa3d8215 |
| S4-R1-02 | low | `docs/kickoff/1355/README.md` | The exclusion paragraph said the copies are gone, and the exclusion and equivalence docstrings said the disposable copies no longer exist or hold the recorded fields. The copies and Forge logs were still under `/private/tmp/fiat1355-hermes/exclusions/` and `/private/tmp/fiat1355-hermes/equivalence/`. Fixed: the wording now says no committed or retained byte backs the recorded fields. | fixed in d2f7e170d5dcc032b2dc282ab29f1870fa3d8215 |

Leads not pursued: Reviewed range `5d321bff238e2307de7d857f3174b55842572947..12cf4a8008634f03ef77c9f1ca49703e0b417450`, one signed commit over 62 files; fixes commit `d2f7e170d5dcc032b2dc282ab29f1870fa3d8215` changes 4 files. Independent recomputation under `env -i` (`HOME`, `LANG`, `NO_COLOR`, `PATH`, and `FOUNDRY_SOLC=0.8.22` for V1; no credential), Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, Python 3.14.6, Hermes `canonical_json` and `canonical_storage_layout` from `hermes.py` `36e80da4405645486e4f34caf6fa59ed795d864c685e04a9b37a959bc43c1805`. Fresh public clones with submodules: v2-protocol at `e1f77540fef65736374de6c847743d8ca2233fb4` and wildcat-protocol at `da74452aa7d1a0f024d99efd22cc6d950a8116b7`. For `market-lens-app`, `open-access-role-provider` and `arch-controller`, the raw layout, canonical layout and method map equal the recorded digests and the committed files, and the canonical layout and method map are byte-equal to the sealed `v2-c7be` `MarketLens`, private `role-provider-5d7f` `OpenAccessRoleProvider` and `v1-488b` `WildcatArchController` files. `forge config --json` resolved 0.8.25/cancun and 0.8.22/shanghai without via-IR, as recorded; both clones were left clean. Each capture's recorded forge config digest equals its anchor's (`a691402d9d46c4062f577fe3597b2e8146de349dea7aec6bb2bad844a8011c42` v2, `e3e0d8e07bb6ae4839d418307bb72e123859936f0bfc1806b9afb930e1bbbbab` V1); the checker requires only a SHA-256 there, since the private anchor's tree has its own config. Both `market-lens-app` and `open-access-role-provider` have an empty layout (`5007e3b784841c1839d50e1c7c327054fd93a236126542d5218ef7d82f30c563`). Exclusion rows equal the surviving logs and exit files: `v2-c7be` 0 passed and 2 failed alone, 795 passed and 2 failed unexcluded; `v1-488b` 0 and 6, 348 and 6; copies at `c7be4039` and `488b30d0`. Custody: 566 distinctive tokens from the two private trees' files matched 33 added lines, all from public repositories or the permitted method maps; no private test name, test path or source body is committed, and `.hexaemeron/restricted/` is ignored. The state projection carries identifiers, generic Gate 1 commands and map digests only. `sealed-coverage` computes 17 of 17 types (6 native, 11 equivalent) and 137 of 137 addresses from the inventory and the sealed anchors, refuses a missing retained run by name, and `check` refuses an equivalence on an unsealed anchor. S3-R1-01 holds: each equivalence map is compared with the sealed anchor file, that file with the anchor's sealed hashes, and each private public map with the retained `artifact_hashes` through the projection. S3-R1-02 holds: every new validator runs under `guarded`. `RetainedSealedCoverageTests` skips where `.hexaemeron/restricted/hermes/` is absent, which is every checkout but the run worktree; six synthetic-run tests cover the same code paths, and the committed `sealed-coverage` report is the evidence for the real runs, so that was accepted. `exclusions.json` is not named in the runbook's Files, but its rows are checked for commit, pins, seed, argv, counts and the anchor's pass count, and the README marks the counts as recorded only. Step 3's README and rejection docstring carry the same copies-are-gone wording; that text belongs to Step 3's receipted rounds and was left. Before and after the fix, from the worktree root with each prior report moved aside, the five conformance commands exited 0 with value `true` and wrote identical bytes: `profile-invariance` `171bf9f516ca099c2c73bd858320da881efed699b732bdfb27b55b0e25f42f1a`, `owner-handoffs` `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, `selector-rejection` `8d3db84c7ac3476c17da62abc11425ace314af48120c9aae998b6bde07f1f664`, `layout-rejection` `96662f14176a251e50a62bdf6eb251c63be3e109f0ed5d9225a7b0a60ab5782d`, `sealed-coverage` `3b0543b91ffcf68730300b6ec7b6bf88b69a5df4d7b7351458913901d7ce07b3`. Mechanical exits over the 62 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,276 root-suite tests; `tests.test_kickoff_hermes_1355` 125 of 125; `check` exit 0. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-4.json` returned `inconclusive` for `d2f7e170`, counting 2,199 executed, 41 assertion failures, 585 errors and 5 skipped on the parent; as in Steps 1 to 3, the runner's containment forbids the child processes the root suite starts. By hand, `d2f7e170`'s test file on a `git archive` of `12cf4a80` ran 125 tests with 2 failures, exactly the two new guards, and 3 skipped; on `d2f7e170` it ran 125 OK. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 4, round 2 -- 2026-09-23T08:38:51Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=not-applicable; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The round covers the fixes commit `d2f7e170d5dcc032b2dc282ab29f1870fa3d8215` and the step at `071043f28cd77637eb1ef271880cc01d2a6e553e`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No Gate 1 was reproduced, and the eight equivalence types not recomputed in round 1 stay unrecomputed. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `d2f7e170` holds on review. On a `git archive` of `071043f2` with non-private bytes only, `check` passes clean and refuses by name a dotfile beside `run/` in `baselines/fee-ac73/`, a symlink beside `run/` in `baselines/collateral-46db/`, a `logs/` directory in `baselines/role-provider-5d7f/`, an undeclared `run/state.json` in `baselines/fee-ac73/` and a `baseline.gas-rule-corpus.json` beside `run/` there. The refusal for a withheld directory says "withheld Hermes file"; the README's withheld list uses the same word for both, so it was left. The README custody wording still matches the tree after the coordinator removed every private-repository clone and loose private copy outside `.hexaemeron/restricted/`: the README says copies and run directories sat under `/private/tmp/fiat1355-hermes/`, in the past tense, and that the complete private run directories stay in `.hexaemeron/restricted/hermes/`, which still holds both. The S4-R1-02 wording says no committed or retained byte backs the recorded counts; the public exclusion and equivalence copies still under `/private/tmp/fiat1355-hermes/` are neither committed nor retained by any record. From the worktree root with each prior report moved aside, the five conformance commands exited 0 and wrote identical bytes: `profile-invariance` `171bf9f516ca099c2c73bd858320da881efed699b732bdfb27b55b0e25f42f1a`, `owner-handoffs` `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, `selector-rejection` `8d3db84c7ac3476c17da62abc11425ace314af48120c9aae998b6bde07f1f664`, `layout-rejection` `96662f14176a251e50a62bdf6eb251c63be3e109f0ed5d9225a7b0a60ab5782d`, `sealed-coverage` `3b0543b91ffcf68730300b6ec7b6bf88b69a5df4d7b7351458913901d7ce07b3`. Mechanical exits over the 65 changed paths: phylax 0, ephoros 0, hypomnema 0. `tests.test_kickoff_hermes_1355` 125 of 125; `scripts/kickoff_hermes_1355.py check` exit 0. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 5, round 1 -- 2026-09-23T09:42:49Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The step diff `1b2371ca1c59c844f9e85640369d1b5c1d96d3c3..ddea2871dca04ad29569aa1522b01ec8ce4f4690` carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. Of the seven reproductions, only `col-46db` was re-run here; `v2-c7be`, `v1-488b`, both private anchors and both rejections were not, and the public ones stay recorded only. The private repositories were not cloned. The delivery pull request does not exist yet, so its evidence links and restoration statement were not checked. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | low | `scripts/kickoff_hermes_1355.py` | `verify_reproduction_payload` checked a retained private reproduction's own digests, maps and log count, but not that it was a second run or that its state matched the sealed record. On a scratch copy with the retained payloads, copying the sealed Step 4 `fee-ac73` run into `.hexaemeron/restricted/reproduction/<its state digest>/` and pointing `reproduction.json` at it passed `check` and wrote `evidence-custody` value `true`. Fixed: the retained `state.json` digest must differ from the sealed record's, and its state projection must equal the sealed record's. | fixed in c870a3ead154532321ff63db3bda087253989449 |
| S5-R1-02 | low | `scripts/kickoff_hermes_1355.py` | The custody scan matched only `Suite:function` pairs from a private gas snapshot, while the module docstring said `evidence-custody` refuses private test output. On the same scratch copy, four lines of a retained private Forge test log pasted into the README, and three bare private test function names, each passed at value `true`. The S2-R1-01 class recurs: the docstring claimed more than the code computed. Fixed: suite, function, pair and test file path from each private snapshot and test log are refused as whole tokens unless a public anchor's snapshot names them; the docstring and README state that bound. | fixed in c870a3ead154532321ff63db3bda087253989449 |
| S5-R1-03 | low | `docs/kickoff/1355/README.md` | The reproduction table named anchors `col-46db` and `rp-5d7f` and sent the reader to `baselines/<tree>/record.json`, but those records sit in `baselines/collateral-46db/` and `baselines/role-provider-5d7f/`. "What this establishes" said the second run produced the same digests without saying that five of seven runs are recorded only, and did not say that byte-equal equivalence is a reading of the issue the operator confirmed on 2026-09-23 (`runbook.md`, study assumption 3). Fixed: a record-directory column, and both qualifications. | fixed in c870a3ead154532321ff63db3bda087253989449 |

Leads not pursued: Reviewed range `1b2371ca1c59c844f9e85640369d1b5c1d96d3c3..ddea2871dca04ad29569aa1522b01ec8ce4f4690`, one signed commit over 6 files; fixes commit `c870a3ead154532321ff63db3bda087253989449` changes 4 files. Independent reproduction of `col-46db` under `env -i` (`HOME`, `LANG`, `NO_COLOR`, `PATH`, `FOUNDRY_SOLC=0.8.28`, `FOUNDRY_EVM_VERSION=cancun`; no credential), Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, Python 3.14.6, `hermes.py` `36e80da4405645486e4f34caf6fa59ed795d864c685e04a9b37a959bc43c1805`, fresh public clone of collateral-contract at `46dba596fa111f868200358f551796e8f73b5fd7` with submodules: Hermes `baseline` with the record's argv exited 0 at `baseline_ready`, 48 passed, 0 failed; all six layouts, three method maps, the source manifest and the Forge config are byte-equal to `baselines/collateral-46db/run`. Its `baseline.gas-snapshot` differs from the sealed text only in the reverts counts of the five `SimpleMarketCollateralMultiPartyInvariantTest` invariant rows under the same seed; `layout-a3-sto01` stopped at Gate 3 on five changed invariant rows on this tree, so that stop may be run-to-run variation rather than the candidate. That is Hermes's Gate 3 decision and the attempt is not a selected one, so it was left. After the run the clone held only Hermes's untracked `.gas-snapshot`, as the record's negative observation says. `check` recomputes each verdict from its fields: a hand-set `differs` with reproducing fields, a changed map digest, and a rejection at exit 30 are each refused by name. The public reproductions can only be refused on disagreement with the sealed records, not recomputed; the README and the record's negative observations say so. Removing the not-implemented branch leaves no vacuous pass: every criterion in `CONFORMANCE` has its own evaluator, an unknown one is refused, and `evidence-custody` refuses a missing reproduction or any verdict other than `reproduced`. Deleting `.hexaemeron/restricted/reproduction/` is refused as a named absent payload. The custody scan still compares whole files, whole JSON strings and whole test identifiers, not re-encodings or private source paths; the risk register's private-payload clause names source and test source, and no private source body or test body is committed. A token scan of the fixed range's added lines against the retained private sources, snapshots and logs found 29 shared tokens of eight or more characters: public commit hashes, public contract names, English words and one private test suite name, already in the step's synthetic test fixture as a conventional `<Contract>Test` name, which was left. S2-R2-01 holds: the inventory row stays `target-maintainer-review-outstanding` and the README says so. S3-R1-01, S3-R1-02, S4-R1-01 and S4-R1-02 hold: `check` exit 0 on the fixed tree, `validate_reproduction` runs under `guarded`, and the README keeps the recorded-only wording. Before and after the fix, from the worktree root with each prior report moved aside, the six conformance commands exited 0 with value `true` and wrote identical bytes: `profile-invariance` `171bf9f516ca099c2c73bd858320da881efed699b732bdfb27b55b0e25f42f1a`, `owner-handoffs` `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, `selector-rejection` `8d3db84c7ac3476c17da62abc11425ace314af48120c9aae998b6bde07f1f664`, `layout-rejection` `96662f14176a251e50a62bdf6eb251c63be3e109f0ed5d9225a7b0a60ab5782d`, `sealed-coverage` `3b0543b91ffcf68730300b6ec7b6bf88b69a5df4d7b7351458913901d7ce07b3`, `evidence-custody` `6bcab799fbc03006e22fcedc4de7f3aaeda7315b40a4b29613890addb67c52d1`. Mechanical exits over the 6 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0; Imprimatur and Brevitas clean on the README. `.githooks/greenlight` exit 0 with 2,294 root-suite tests; `tests.test_kickoff_hermes_1355` 143 of 143 with 5 skipped outside the run worktree. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-5.json` returned `inconclusive` for `c870a3ead154`, counting 2,215 executed, 42 assertion failures, 585 errors and 7 skipped on the parent, with no digest rebinds; the runner's containment forbids the child processes the root suite starts. By hand, `c870a3ea`'s test file on a `git archive` of `ddea2871` ran 143 tests with 3 failures, exactly the two new guards and the updated private-test-output case, and 5 skipped; on `c870a3ea` it ran 143 OK. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 5, round 2 -- 2026-09-23T09:58:17Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The round covers the fixes commit `c870a3ead154532321ff63db3bda087253989449` and the step at `df1d3acf7e4a40e755d9017e29e0997c2b038877`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No reproduction was re-run this round. The private repositories were not cloned. The delivery pull request does not exist yet. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R2-01 | low | `scripts/kickoff_hermes_1355.py` | The S5-R1-01 fix called a retained private reproduction "a second run, not the sealed one" in the `verify_reproduction_payload` and `evidence_custody_evidence` docstrings and README custody item 6, but it only refuses a `state.json` byte-identical to the sealed run's. A copy of the sealed run with an edited run directory, re-hashed into `reproduction.json`, still passes; nothing retained shows a fresh execution. `result.json` was also checked only for status and exit, so a result naming another run directory passed. The S2-R1-01 class recurs. Fixed: `result.json` must be Hermes's exact `baseline_ready` result for the state's run directory, as the sealed-run verifier already requires; the refusal says "byte for byte"; the docstrings and README state that a re-hashed copy is not told apart and that fresh execution is recorded only. | fixed in 4f3a6b8ae75b8b5a3ba36797933d1bfa2e0f3917 |

Leads not pursued: Reviewed `c870a3ead154532321ff63db3bda087253989449`, 4 files; fixes commit `4f3a6b8ae75b8b5a3ba36797933d1bfa2e0f3917` changes 4 files. Test-name scan false positives: the retained private snapshots and test logs yield 52 identifiers, the public anchors' committed snapshots 1,626, and the two sets share 0; no private identifier is named as a whole token in any docs file, so `evidence-custody` holds on the delivered tree without the public subtraction doing any work. The subtraction covers a private name a public snapshot also carries, and `test_private_test_name_or_log_suite_under_docs_is_refused` guards it; a private name that coincides with public prose but no public snapshot would still be refused, and none does. Whole-token matching stops a private name matching inside a longer identifier. S5-R1-01 otherwise holds: the byte-identical sealed copy and a changed `git_head` are refused by name. S5-R1-03 holds: the record-directory column matches `baselines/`. Hermes lead, carried by the coordinator as a Hermes item: a fresh `col-46db` Gate 1 under the recorded seed `0x5EED` in round 1 wrote a `baseline.gas-snapshot` (`5a2257128ec41193ae39ab48c17205c20de4290bd62e19c4726c71e6b9feec0f`) that differs from the sealed text (`d485779c19e886548b0eafb875eca91ae958eabb395ad4ad2f3ac392c7a50dd4`) only in the reverts counts of the five `SimpleMarketCollateralMultiPartyInvariantTest` invariant rows (for example `invariant_availableCollateralMatches` 40568 sealed, 40630 fresh), while all maps, the source manifest and the Forge config were byte-equal; `layout-a3-sto01` stopped at Gate 3 on five changed invariant rows on that tree, so its stop may be run-to-run variation rather than the candidate. From the worktree root with each prior report moved aside, the six conformance commands exited 0 with value `true` and wrote identical bytes: `profile-invariance` `171bf9f516ca099c2c73bd858320da881efed699b732bdfb27b55b0e25f42f1a`, `owner-handoffs` `aa95c8a186461f5ccd668f7cf69a79c62df74f68c6e999101c3127d2cc46a3f3`, `selector-rejection` `8d3db84c7ac3476c17da62abc11425ace314af48120c9aae998b6bde07f1f664`, `layout-rejection` `96662f14176a251e50a62bdf6eb251c63be3e109f0ed5d9225a7b0a60ab5782d`, `sealed-coverage` `3b0543b91ffcf68730300b6ec7b6bf88b69a5df4d7b7351458913901d7ce07b3`, `evidence-custody` `6bcab799fbc03006e22fcedc4de7f3aaeda7315b40a4b29613890addb67c52d1`. `check` exit 0. Mechanical exits over the 9 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0; Imprimatur and Brevitas clean on the README. `.githooks/greenlight` exit 0 with 2,295 root-suite tests; `tests.test_kickoff_hermes_1355` 144 of 144 with 5 skipped outside the run worktree. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-5.json` returned `inconclusive` for `4f3a6b8ae75b`, counting 2,216 executed, 41 assertion failures, 585 errors and 7 skipped on the parent, with no digest rebinds; the runner's containment forbids the child processes the root suite starts. By hand, `4f3a6b8a`'s test file on a `git archive` of `df1d3acf` ran 144 tests with 2 failures, the new result-shape guard and the sealed-copy case whose refusal text changed, and 5 skipped; on `4f3a6b8a` it ran 144 OK. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 5, round 3 -- 2026-09-23T10:12:12Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The round covers the fixes commit `4f3a6b8ae75b8b5a3ba36797933d1bfa2e0f3917` and the step at `8e0365bed01861006eb4c120e742d7f4b286be16`, plus one sweep of every module and Step 5 docstring and every README claim against the code. Claims from Steps 1 to 4 were compared with the code, not re-run. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No reproduction was re-run. The private repositories were not cloned. The delivery pull request does not exist yet. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R3-01 | low | `scripts/kickoff_hermes_1355.py` | The one-pass sweep found four claims the code does not compute, the S2-R1-01 class. (1) The module docstring said `evidence-custody` writes `true` only when every anchor and rejection reproduced, and the `validate_reproduction` docstring said each anchor and rejection re-ran and passed; the code recomputes each verdict from recorded fields, and for the public runs nothing shows that they ran. (2) The module docstring and README said every refusal names the digest involved; 99 of the 371 `finding` calls carry none, 7 of them from Step 5, such as a missing payload or an unbacked reference. (3) The `validate_rejection` docstring and the README Gate 5 paragraph said the disposable copies are gone or no longer hold the status; `/private/tmp/fiat1355-hermes/copies/` still holds the five public clones (collateral-contract 1, v2-protocol 2, wildcat-protocol 2), which S4-R1-01's round left as Step 3 text. (4) The docstring and `conformance` help still said "an implemented criterion" after the not-implemented branch was removed. Fixed by rewording each claim to what the code computes. | fixed in 507cd6e126127dc3f6243e35397e29aaa913b7a4 |

Leads not pursued: `4f3a6b8a` holds on review. `result.json` must equal Hermes's exact `baseline_ready` object for the state's run directory, and a non-object state stops before `state.get` through short-circuit evaluation. The refusal names "byte for byte", and the stated limit (a re-hashed copy of the sealed run is not told apart) matches the code. "What this establishes" holds after the fix: 17 types and 137 addresses are computed by `sealed-coverage`; the two Gate 5 rejections are checked from Hermes's committed results, with their maps diffed by the checker and each reproduced independently by a Warden in Step 3; the reproduction bullet says which runs are recomputed and which recorded; the equivalence reading, the outstanding target-maintainer review, the fixture header's canonicity, the unenumerated instances and the Elenchus limit are all stated. Every `.hexaemeron/restricted` path the docs name resolves to a verified payload: `fixture-26006289` and its `proofs.jsonl`, `alexandria-release`, `alexandria-input/capture-plan.json`, both sealed `hermes/` runs and both `reproduction/` runs. The Alexandria release objects are not hashed by `evidence-custody`, which checks the release manifest by digest, and the README names only the manifest and plan, so that was left to `owner-handoffs`, which re-verifies the release. All five attempt `state.json` run directories still exist, so the README's statement about the three Gate 3 attempts holds. Before and after the fix, from the worktree root with each prior report moved aside, the six conformance commands exited 0 with value `true` and wrote identical bytes; `evidence-custody` is `6bcab799fbc03006e22fcedc4de7f3aaeda7315b40a4b29613890addb67c52d1`. `check` exit 0. Mechanical exits over the 9 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0; Imprimatur and Brevitas clean on the README. `.githooks/greenlight` exit 0 with 2,295 root-suite tests; `tests.test_kickoff_hermes_1355` 144 of 144. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-5.json` returned `unguarded` for `507cd6e12612`: the commit changes prose only, docstrings, the `conformance` help string and the README, so no test can fail without it. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.

## Step 5, round 4 -- 2026-09-23T10:18:49Z

Audit schema: fiat-audit-round/v2

Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=not-applicable; wrong-gate-negative=reviewed; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=reviewed; partial-evidence=reviewed

Not checked: The round covers the fixes commit `507cd6e126127dc3f6243e35397e29aaa913b7a4` and the step at `131e7ea687f53a833226a4ab7055d1f4f34782f6`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No reproduction was re-run. The private repositories were not cloned. The delivery pull request does not exist yet. Target-maintainer review of the inventory is still not recorded and stays carried forward. `run_checks.py` was not run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `507cd6e1` changes prose only and each new claim matches the code. A missing file refuses as `record=<name> field=path missing file <path>` with no digest, as the reworded README and module docstring now say. `/private/tmp/fiat1355-hermes/repro/` is absent, so "the public copies and runs were deleted" holds for the reproduction. The Gate 5 restoration status and stdout digest are backed by no committed or retained byte. The `conformance` help reads "write one design report for a conformance criterion". The `validate_reproduction` docstring now says what the entries record, and that the runs happening is recorded only. `check` exit 0; `tests.test_kickoff_hermes_1355` 144 of 144. Mechanical exits over the 9 changed paths: phylax 0, ephoros 0, hypomnema 0. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
