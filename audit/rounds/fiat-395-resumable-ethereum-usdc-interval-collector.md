## Step 1, round 1 -- 2026-08-31T03:56:27Z

Audit schema: fiat-audit-round/v2

Covered: torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; coverage-inflation=not-applicable; reorg-rewind=not-applicable; epoch-gap=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run; the `security_suite` receipt waives them because the run ships no Solidity and no Foundry or Hardhat project. Six of the eleven register concerns sit at boundaries steps 2 to 5 open and no code implementing them exists at this commit, so they were read as not-applicable rather than reviewed; `unbounded-response` is one of them, because its declared boundary is the transport and this step has none, but its staging-side sibling was found and is S1-R1-03 below. The three bundled lints each exited 0 over the step's changed Python and Markdown. The committed study and runbook were not re-audited for content: they are the receipted artefacts, with two surface deviations forced by repository law and recorded in the step commit message. One correction to the implement receipt: its test summary reported the root suite as exit 0, which was the exit code of a `tail` at the end of a pipeline rather than of `unittest`. Measured directly, that suite reports 776 tests with 2 failures, both of them this step's and both listed below.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | `resume()` treated a symlinked checkpoint as an absent one and truncated every journal to zero. Reproduced: one committed shard staged 72 bytes of `logs.jsonl`; replacing `checkpoint.json` with a symlink to an identical file outside the root made `resume()` return `next_shard: 0` and left the journal at 0 bytes. Bytes a completed shard had staged were discarded where a hostile or careless tree should have been refused. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-02 | low | plugins/alexandria/scripts/alexandria_lib/interval.py | A `journals` entry that is a symlink to a regular file raised a bare `FileExistsError` out of `Path.mkdir(exist_ok=True)` instead of a controlled `AlexandriaError`. Reproduced against a constructed staging root. Every other refusal in the module is controlled. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-03 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | `record()` bounded nothing while `entries()` refuses a journal above `MAX_JOURNAL_BYTES`. With the ceiling patched to 64 bytes, one record wrote 273 bytes and the reader then refused the journal it had just written. A long collection could stage evidence no reader will accept, and the writer would not say so. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-04 | low | plugins/alexandria/scripts/alexandria_lib/interval.py | `commit()` before `resume()` wrote a checkpoint whose `records` count understated the journals: measured `records=1` against two staged records. The count is working state a reader uses to judge a resumed run. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-05 | info | plugins/alexandria/scripts/alexandria_lib/interval.py | `DIGEST_RE` was unused, and `validate_checkpoint`'s `plan_digest` parameter shadowed the module function of the same name. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-06 | medium | .agents/skills/promise-machine/runtime | The step commit bdba6f1a20a79c908798eeb0d4109af69515b4ee shipped an incomplete portable mirror. `portable_promise_machine.py sync` builds its universe from `git ls-files`, the six new files were still untracked when it ran, and `check` then agreed with a universe that did not contain them. At bdba6f1a, `plugins/alexandria/scripts/alexandria_lib/interval.py` is tracked and its mirror copy is absent, so `check` fails at that commit. Found by re-running the whole battery rather than the suite the step touched. | fixed in 13147dde938797873dcde124321dd8b298978df0 |
| S1-R1-07 | high | .agents/skills/promise-machine/runtime/MANIFEST.json | The repaired mirror manifests 1001 files. `tests/test_skills_sh_package.py` asserts `file_count` is below `MAX_FILES = 1000`, a figure held because it is the skills CLI's `SKILLS_EXTRACT_MAX_FILES` default, and ADR-054 records the payload decision behind it. `main` at 1c1137898bce9086c34310bd29b5cf8a889f800c and bdba6f1a both manifest 995, so the repository stands four files below that ceiling. Step 1 needs six. The canonical `SKILL.md` links `docs/study.md` and `docs/runbook.md`, so omitting committed design records from the mirror would break the router's dependency closure, and the remaining five steps add roughly seven more mirrored files. The run cannot produce a tree that passes this suite without a repository-level decision about the portable payload or its ceiling. | open |

Leads not pursued: the ceiling in S1-R1-07 is the reason this round does not close. Three routes were measured and none fits inside this run's authority: raising `MAX_FILES` would misreport installability through a route the CLI actually gates; omitting `plugins/*/docs/**` would drop two documents the canonical `SKILL.md` links; and merging the three interval schemas into one file, plus omitting the step 6 example's fixture subtree the way `plugins/alexandria/examples/compound-v3-phase0-v0/input/**` already is, recovers four files against a shortfall of about seven. Issue #940 asked whether the payload should live outside this tree and was closed by ADR-054 in favour of keeping it, which is the decision this finding sits against. Not pursued: whether `portable_promise_machine.py` should refuse a sync whose universe is missing tracked files, which would have turned S1-R1-06 into a loud failure at the moment it happened; that belongs to the script's own owner rather than to an Alexandria step. Not pursued: whether `tests/test_skills_sh_package.py` should run in a workflow, which is the CI gap issue #882 already carries for the Alexandria and Probitas suites. The macOS `/var/folders` alias that finding S1-R1-03 of the release-statement run repaired in a sibling runner was checked here and holds: `resolve_root` resolves before comparison and `test_an_unresolved_alias_still_contains_its_own_children` covers it.

## Step 1, round 2 -- 2026-08-31T04:11:48Z

Audit schema: fiat-audit-round/v2

Covered: torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; coverage-inflation=not-applicable; reorg-rewind=not-applicable; epoch-gap=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable

Not checked: nothing new. The same six concerns still belong to steps 2 to 5 and the Pashov pair remains waived by the `security_suite` receipt. The round re-read the fixed module for faults the repair itself could have introduced: the size ledger `record()` now keeps is reset by `close()` and re-read from `fstat` when a journal is reopened, so a resumed run cannot inherit a stale figure across the truncation `resume()` performs; the symlink refusal added to `resume()` sits in front of an `exists()` call that would otherwise follow a link, and the read below it keeps `O_NOFOLLOW` so a swap between the check and the open is refused at open rather than raced.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the whole battery ran against the fixed tree. The three bundled lints exited 0, the Alexandria suite reported 352 of 352 through the runner step 1's contract names with zero skips, `portable_promise_machine.py check`, `horos.py check .`, `audit_synopsis.py --check .` and `git diff --check` each exited 0, and the working tree is clean. The root suite reports 776 tests with one failure, `test_manifest_binds_every_runtime_file_to_source_bytes`, which is S1-R1-07 and nothing else; the scratch-quiescence failure round 1 recorded alongside it is gone. S1-R1-07 stays open and is accepted rather than repaired here: framework-63, issue #949, moves the generated skills.sh payload into its own repository and lands after framework-65, issue #972, so the ceiling this run stands against is owned upstream and the contributor directed the run to continue locally against it. One thing is recorded rather than fixed: a refused oversized record still leaves an empty journal file behind, because `record()` opens the journal before it weighs the entry. The file is inside the staging root, carries no bytes and is the same file a successful record would have created, so nothing is disclosed and nothing is lost; moving the weight in front of the open would cost a stat per record for no evidence gained.

## Step 2, round 1 -- 2026-08-31T04:48:04Z

Audit schema: fiat-audit-round/v2

Covered: epoch-gap=reviewed; coverage-inflation=reviewed; torn-shard=not-applicable; staging-path-escape=not-applicable; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; reorg-rewind=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run; the `security_suite` receipt waives them because the run ships no Solidity. The staging concerns settled in step 1 are untouched by this step, and the transport, reorg and reconciliation concerns belong to steps 3 and 4. The three bundled lints ran over the changed module, the changed test module and the changed documents and each exited 0, after one recorded exception: `ephoros` reads `log["address"]` as telemetry keyed by a wallet address, and the line now carries `# ephoros: allow` with the reason, because `log` is a JSON-RPC event log a provider returned and `address` is its emitting-contract field. The round then read the diff for the register's concerns the lints cannot see and probed the epoch builder by hand with evidence that disagrees with itself.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | An `Upgraded(address)` log announces the implementation it upgraded to in its second topic, and the EIP-1967 slot read at that block holds the one that went live. Discovery read only the slot, so the two could disagree and the epoch took the slot silently. Reproduced by rewriting the first log's implementation topic to `0xde…de`: the epoch still recorded `0x42f9505a376761b180e27a01ba0554244ed1de7d` and nothing refused. The whole point of the epoch table is that it is bound to evidence, so two pieces of evidence disagreeing has to stop it. | fixed in 8ccf42cf8c2342c67f5fd2c1ca650a608b21720b |
| S2-R1-02 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | The same log carries its own `blockHash`, which was never read and never compared with the preserved block hash for that block. Reproduced by setting it to `0xff…ff`: the epoch's `start_hash` came from the preserved block and the disagreement went unrecorded. A log whose block hash does not match the block it is said to sit in is either from a reorged branch or from another capture. | fixed in 8ccf42cf8c2342c67f5fd2c1ca650a608b21720b |
| S2-R1-03 | low | plugins/alexandria/scripts/alexandria_lib/interval.py | A runtime code read keyed by a checksummed address was reported as `implementation 0x1b0e… has no runtime code read`. The read was present and correct; only its key spelling differed. A message that says evidence is missing when it is there sends a reader looking for the wrong thing. Keys are now normalised to lowercase, and two different bodies under one address refuse. | fixed in 8ccf42cf8c2342c67f5fd2c1ca650a608b21720b |
| S2-R1-04 | info | plugins/alexandria/docs/usdc-interval-runbook.md | Step 2's Entry names branch `fiat/395-resumable-ethereum-usdc-interval-collector-step-1-scaffold-the-collector-s-contracts`. The branch the controller derived and the run actually used ends `-contrac`, because the derived name is truncated. A reader following the document to that ref finds nothing. The runbook is receipted, so the name is recorded here rather than edited. | open |

Leads not pursued: the fixes commit changed tests as well as code. Against `eca364aa1d25ab59447bc4f671c98965cf487998` five of the six new guards fail by assertion and one errors, because `test_a_checksummed_runtime_code_key_still_resolves` asserts a success path the parent cannot reach and raises there instead. The Elenchus contract classifies a mixed assertion and error report as `inconclusive`, so that is what is recorded rather than `guarded`; the fixed tree reports 377 of 377 with zero skips. Two bounded things are recorded rather than fixed. Two upgrade logs in the same block refuse as unordered, which is fail-closed and right for a first collector but would need a rule if a real market ever upgrades twice in one block. And `MAX_EPOCHS` is checked once on the supplied log count and again on the built table, so a 256-log input builds 257 epochs before the second check refuses; the work is bounded and the refusal is correct, but the first check is one short of covering it. S1-R1-07 stays open and accepted.

## Step 2, round 2 -- 2026-08-31T04:50:35Z

Audit schema: fiat-audit-round/v2

Covered: epoch-gap=reviewed; coverage-inflation=reviewed; torn-shard=not-applicable; staging-path-escape=not-applicable; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; reorg-rewind=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable

Not checked: nothing new. The same concerns remain with steps 3 and 4, and the Pashov pair remains waived. The round re-read the repaired builder for faults the repair could have introduced: the code-read mapping is normalised once before the epoch loop rather than per boundary, the block hash for a boundary is resolved before the announcement comparison so a missing hash refuses first, and an upgrade log at the interval's first block still opens the first epoch rather than adding a boundary beside it.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the whole battery ran against the fixed tree. The three bundled lints exited 0, the Alexandria suite reported 377 of 377 with zero skips through the runner step 2's contract names, `portable_promise_machine.py check`, `horos.py check .`, `audit_synopsis.py --check .` and `git diff --check` each exited 0 with a clean working tree, and the design checker exited 0 at `step:3` against the unchanged record. The root suite reports 776 tests with one failure, S1-R1-07 and nothing else. One thing is recorded rather than fixed: `_topic_address` and `_implementation` each carry the same three-line left-padded-address check, one for a log topic and one for a slot read. Folding them into one helper would be tidier, but the two read different evidence and no defect was found in either, so a refactor with no failing case behind it is churn rather than a fix. The two bounded items from round 1 still stand, as does S1-R1-07 and the stale branch name S2-R1-04 records.

## Step 3, round 1 -- 2026-08-31T05:14:24Z

Audit schema: fiat-audit-round/v2

Covered: endpoint-leak=reviewed; silent-truncation=reviewed; unbounded-response=reviewed; reorg-rewind=reviewed; torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; epoch-gap=not-applicable; coverage-inflation=not-applicable; reconciliation-bias=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run; the `security_suite` receipt waives them because the run ships no Solidity. `reconciliation-bias` belongs to step 4 and `coverage-inflation` to step 5, and neither is touched here. The three bundled lints ran over the new command, the changed module and both changed test modules and each exited 0. The live HTTPS path was not exercised against a provider: no endpoint is reachable from this run, so every claim about it is a claim about the boundary rather than about a provider that answered. The round then read the diff for the register's concerns the lints cannot see and drove the collector by hand with a transport built to misbehave.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | high | plugins/alexandria/scripts/usdc_interval.py | An error receipt copied the transport's own exception message into `receipts/errors.jsonl`. The shipped HTTPS transport never puts its endpoint in a message, but a caller-supplied transport can, and the receipt file is durable. Reproduced with a transport whose message carried `https://user:hunter2@rpc.example.invalid/v1/SECRET-KEY`: the whole string, credentials included, was written to disk. The step's own exit says a receipt never carries an endpoint, a header or a credential, and the test that claimed to check it only searched for strings the fixture never produced. | fixed in 023820343441896f3fad153b46aa77cf802d05cb |
| S3-R1-02 | medium | plugins/alexandria/scripts/usdc_interval.py | The receipts directory was created with `mkdir(exist_ok=True)`, which accepts a symlink to a directory, and the receipt file was opened without `O_NOFOLLOW`. Reproduced by pointing `receipts` at another directory: the receipt landed outside the staging root. Confinement is the one thing a staging root is for. | fixed in 023820343441896f3fad153b46aa77cf802d05cb |
| S3-R1-03 | medium | plugins/alexandria/scripts/usdc_interval.py | The finality bind and the boundary re-read raised without writing a receipt, so the two reads that happen before any shard left no record of a failed attempt. Reproduced by failing the finality read: the collection stopped and `receipts/errors.jsonl` did not exist. Those are the reads an operator most needs a record of, because they decide whether the run started at all. | fixed in 023820343441896f3fad153b46aa77cf802d05cb |

Leads not pursued: the fixes commit changed tests as well as code. Against `a50e25a7b33a82833b68366e843b7d2470a96f2a` two of the eight new guards fail by assertion and six error, because they assert behaviour the parent has no code path for at all; the Elenchus contract classifies that mixed report as `inconclusive`, and the fixed tree reports 423 of 423 with zero skips. Three bounded things are recorded rather than fixed. An exception message still reaches the operator on stderr, so a caller-supplied transport that puts its endpoint there is visible to whoever is watching; that is a different surface from a durable file and the shipped transport does not do it. The `confirmations` finality policy asks for the block the plan already names and checks the provider agrees, which confirms the boundary rather than deriving it; deriving it would mean reading a head and subtracting, and the plan is the place that decision belongs. And `trace_filter` is requested per shard with no fallback to `trace_block`, so a provider serving only the latter is refused rather than degraded; Phase 0 measured `trace_filter` on the endpoint this collector targets. S1-R1-07 and S2-R1-04 stay open and accepted.

## Step 3, round 2 -- 2026-08-31T05:21:03Z

Audit schema: fiat-audit-round/v2

Covered: endpoint-leak=reviewed; silent-truncation=reviewed; unbounded-response=reviewed; reorg-rewind=reviewed; torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; epoch-gap=not-applicable; coverage-inflation=not-applicable; reconciliation-bias=not-applicable

Not checked: nothing new. Steps 4 and 5 still own their two concerns and the Pashov pair remains waived. The round re-read the repaired receipt path for faults the repair could have introduced: the symlink check on the receipts directory runs after the `mkdir`, so a symlink to a file raises `FileExistsError` and is converted rather than escaping; the receipt file descriptor is opened once and closed once, with `closefd=False` on the wrapper; and a status that is neither a bounded number nor a short plain string refuses before anything is written.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the whole battery ran against the fixed tree. The three bundled lints exited 0, the Alexandria suite reported 423 of 423 with zero skips, `portable_promise_machine.py check`, `horos.py check .`, `audit_synopsis.py --check .` and `git diff --check` each exited 0 with a clean working tree, and the design checker exited 0 at `step:4` with the reorg conformance report passing on seven cases. The root suite reports 776 tests with one failure, S1-R1-07 and nothing else. One thing is recorded rather than fixed: `_ask` catches `AlexandriaError` from the transport, so a caller-supplied transport that raises something else propagates without a receipt. `HttpsTransport` raises only `TransportError`, and catching everything would swallow a `KeyboardInterrupt` during a long collection, which is the one exception an operator most wants to work. The three items recorded under round 1 still stand, as do S1-R1-07 and S2-R1-04.

## Step 4, round 1 -- 2026-08-31T05:50:08Z

Audit schema: fiat-audit-round/v2

Covered: reconciliation-bias=reviewed; endpoint-leak=reviewed; unbounded-response=reviewed; staging-path-escape=reviewed; torn-shard=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; reorg-rewind=not-applicable; epoch-gap=not-applicable; coverage-inflation=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run; the `security_suite` receipt waives them because the run ships no Solidity. `coverage-inflation` belongs to step 5, where the capture plan is emitted, and this step declares no coverage. The three bundled lints ran over the changed command, the changed module and the changed test module and each exited 0. No live second provider answered: the reconciler was driven against a fixture built to disagree in three declared ways, so every claim here is about the comparison rather than about two providers that both answered. The round read the diff for the register's concerns the lints cannot see and drove the reconciler by hand against an interrupted tree.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | medium | plugins/alexandria/scripts/usdc_interval.py | Reconciliation opened the staging tree with `resume`, which truncates every journal back to its checkpoint. On a completely collected tree that changes nothing, so a reconciliation over a finished collection never showed it. On an interrupted tree it discarded the uncommitted bytes and then refused: measured, the journals went from 1902, 7966 and 2595 bytes to 1425, 5973 and 2595 before `the interval is not completely collected` was raised. A reader that mutates what it reads can destroy evidence on the path that refuses to use it. | fixed in 0d8a0a18dbf74d4519b1885bc3f0deac7324d8c6 |
| S4-R1-02 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | Both journal readers used `load_bytes` at its default 8,388,608-byte control limit while the collector accepts a response up to 67,108,864 bytes. A staged record in that band could be written and never read back: an 8,388,661-byte record refuses with `exceeds the 8388608-byte limit`. The writer and the reader have to agree about what they hold. | fixed in 0d8a0a18dbf74d4519b1885bc3f0deac7324d8c6 |
| S4-R1-03 | low | plugins/alexandria/scripts/usdc_interval.py | An interval left unreconciled reported `compared` and `matched` as zero even when several shards had already agreed before the second provider stopped answering, and `validate_reconciliation` required exactly that. The interval genuinely is unreconciled, but discarding the comparisons already made tells a reader less than the run knew. The record keeps the counts it reached. | fixed in 0d8a0a18dbf74d4519b1885bc3f0deac7324d8c6 |

Leads not pursued: the fixes commit changed tests as well as code, and four guards fail against `51e18329fef4576cfa88a85749c2c22708f332a6` by assertion with no infrastructure error, so the verdict is `guarded` and the fixed tree reports 440 of 440 with zero skips. One thing about how that verdict was reached is worth stating: the guard for S4-R1-02 first let `AlexandriaError` escape, which unittest counts as an error, and a mixed report would have been `inconclusive`. It now catches the refusal and calls `self.fail` with the message, because the refusal is the defect under test rather than a failure to run the test. The earlier draft of that guard was weaker still: it wrote a 12 KB record, which the unfixed reader accepts, so it passed against the parent and proved nothing; it now writes a record deliberately just over the control limit and far under the component limit. Two bounded things are recorded rather than fixed. `_staged` keeps the last entry when a journal somehow holds two for one shard and class, which a completed collection cannot produce and a rewind truncates away. And a shard whose logs disagree is `partial` while one whose boundary disagrees is `failed`, which is the harvest specification's own split; nothing here decides which provider is right. S1-R1-07 and S2-R1-04 stay open and accepted.
