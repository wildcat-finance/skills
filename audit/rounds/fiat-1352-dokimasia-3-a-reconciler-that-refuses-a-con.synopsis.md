Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1352-dokimasia-3-a-reconciler-that-refuses-a-con.md | source_sha256=036cf856791536d56ad2b554ff082b88fc572f5af00263423970085f430d974f | h2_count=1
## Step 1, round 1 -- 2026-09-06T14:11:30Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unattributed-admission=reviewed; rule-dangling=reviewed; attribution-on-draft=reviewed; attribution-drafted=reviewed; attribution-clobber=reviewed; prior-set-migration=reviewed; counts-by-attribution=reviewed; workbook-prose=reviewed; cap-exhaustion=reviewed; partial-write=reviewed; path-traversal=reviewed; subprocess-and-network=reviewed; target-repository-write=reviewed; evidence-digest-binding=reviewed; disposition-closure=reviewed; repository-pins=reviewed<br><br>Not checked: regeneration of the three committed evidence records from the pinned inputs, because `DOKIMASIA_PINNED_APP` and `DOKIMASIA_PINNED_WORKBOOK` are not set in this session; their digests were verified instead by `scripts/demonstrations.py check --root .` at exit 0 and by `scripts/check_public_front_door.py` at exit 0. The 21 selection reports were not read one by one; the generator regenerated them and the design record byte for byte with `git diff --exit-code` at exit 0. The fixture builder was read by grep rather than line by line; `test_the_committed_fixtures_regenerate_from_the_builder` holds every committed fixture byte-equal to its output. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run, and the overlay resolver was not consulted because it resolves nothing for a waived suite.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: `propose()` in `plugins/dokimasia/scripts/dokimasia_lib/propose.py` rebuilds the set from `schema`, `inventory_sha256`, `workbook_sha256` and `dispositions` only, so a regeneration drops the set-level `rules` table while keeping every confirmed entry, and the regenerated set then refuses in `reconcile()` with `confirmed disposition on 'case:ADM-01' names rule 'row-owner-walks-it', which the rules table does not hold`; reproduced against the `closed.json` fixture with 15 preserved, 0 replaced. Nothing is counted wrongly and nothing is written by `demonstrate`, which never calls `propose`, but the reviewer's stated-rule text would be lost from a file `propose --existing` rewrote. Runbook step 2, "Carry attribution through a proposal regeneration", owns this by title and ADR-003's Consequences state that the proposal surface owes it in the step after this one, so it is not a step 1 defect and is carried forward rather than fixed here. Also not pursued: `by_person` keys are the strings the entries carry, so two spellings of one name differing by whitespace or case count as two people; `dispositions-v1.json` bounds `confirmed_by`, `stated_by`, `text` and `rule` by `maxLength` in characters while the reconciler bounds them in UTF-8 bytes, so the reconciler is the stricter gate for a non-ASCII value and the schema alone would admit a 128-character name over 128 bytes; `_check_rules` calls `rule_id.encode` on each table key, which a JSON source always makes a string and a Python caller might not; and `hypomnema.py` reports H009 on `DEMONSTRATION.md:80` for `adr/govern-real-data-demonstrations-separately` only when the changed documents are passed without `docs/decisions` in the index, a line this step does not touch and that resolves to `docs/decisions/ADR-084-govern-real-data-demonstrations-separately.md` with `docs/decisions` supplied, at exit 0. Evidence for the covered concerns: `phylax.py` and `ephoros.py` exit 0 over all 83 paths of the step diff at `08ed9a9ca4fe98ad027e01a271432cf220a65578`, and `hypomnema.py` exits 0 over the seven changed Markdown documents with `docs/decisions` supplied; the root suite reports 1623 of 1623 OK at exit 0 in 217.5 s; `run_checks.py --scope dokimasia` is green with the dokimasia suite, and the three lints, all passed; `design_evidence.py --transition design-lock`, `promise_machine.py coverage --check` at 98 of 98, `demonstrations.py check` at 20 constructed and 1 absent, `check_public_front_door.py` with 4 checked cards, and the Elenchus runner `reconcile --check --candidate rule-table --criterion unattributed-confirmation-refused` writing a `protasis-design-report/v1` with `value` true all exit 0; `horos.py scan . --write` and `scan . --census --write` leave the step tree unchanged. `unattributed-admission`, `rule-dangling` and `attribution-on-draft` are answered by `_check_attribution` and `_check_rules` raising before `assigned`, `by_person`, `applied` or `individual` is touched, each refusal naming the item and the field, and by the eleven refusal tests plus the command-line case asserting exit 2, no traceback and no report file. `attribution-drafted` is answered by `grep` finding neither `confirmed_by` nor `rule` written anywhere under `plugins/dokimasia/scripts` outside `reconcile.py`, with `draft_entry` emitting exactly `item`, `disposition`, `reason`, `oracle`, `confirmed` false and `proposed_sha256`. `prior-set-migration` is answered by `prior-shape.json` refusing on its first confirmed entry naming the item and `'confirmed_by'`, and by the pinned set carrying no default: 261 entries, 202 confirmed, all 202 naming `Laurence Day` and `row-author-owns-walking-it`, the 59 unconfirmed carrying neither field. `counts-by-attribution` is answered by the block being built in the one existing pass, by `check()` and the tests reconciling `people`, `by_person`, `individual` plus `applied` against `disposed` on both the closed and mixed fixtures, and by `coverage_digest` hashing the whole record so a renamed confirmer changes it. `workbook-prose` is answered by the extended test asserting the rule text is present, matches the set, is under 200 characters and that the five column names are absent. `cap-exhaustion` is answered by the four caps being keyword parameters of `reconcile` with module defaults, reported in `caps`, each measured in UTF-8 bytes, with fixtures at 128, 64, 512 and 256 admitted and one over each refused. `partial-write`, `path-traversal`, `subprocess-and-network` and `target-repository-write` are reviewed as unchanged surface: `reconcile.py` adds no I/O, no import beyond `re`, and `dokimasia.py` is not in the diff. `evidence-digest-binding` is answered by `_check_currency` running before `_check_rules` and by the regenerated stale fixtures. `disposition-closure` is answered by the pinned record closing at 202 over 261 with `covered` zero and by the mixed fixture's numerator equalling its attributed confirmations. `repository-pins` is answered by the two schema digests in `tests/promise_machine_coverage.json`, the four DEMONSTRATION.md source digests and the README marker all checking clean. The committed study and runbook differ from `.hexaemeron/study.md` and `.hexaemeron/runbook.md` on exactly one line each, the bold `Next Fiat job.` marker written as the plain words `Next Fiat job` because `test_marketplace_prose` admits the marker only in plugin landing READMEs, and the committed design record is identical to `.hexaemeron/design-evidence.json`. No private repository, branch or path appears in any added line of the diff.
