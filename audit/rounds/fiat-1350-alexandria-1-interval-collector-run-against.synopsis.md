Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1350-alexandria-1-interval-collector-run-against.md | source_sha256=37e2e8eac1194028e359d5d2904e76db30afd4d1ba39afd7bff3548b530e5074 | h2_count=7
## Step 1, round 1 -- 2026-09-06T20:48:12Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=not-applicable; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=not-applicable; preserved-bytes-identity=not-applicable; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: the runbook's own exit commands `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, and Imprimatur and Brevitas over the changed prose were not rerun this round; `hypomnema.py` ran over the three changed Markdown files, not the whole `plugins/alexandria` walk; the missing-fixture-fails path of the new cases was not re-exercised; the receipt produced by an HTTP 403 or 429 on the two new finality reads was not exercised beyond the existing transport-receipt case.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Step head `4365122588167cff7b66f527bceac34eb9f31a66` on `f22de68086ad7265869636903554d09cf751e765`, 46 files, +3027/-105; security suite waived, Python and Markdown only. Gates: phylax exit 0, ephoros exit 0 and hypomnema exit 0 over the 44 changed non-Horos paths (hypomnema over the 3 Markdown files); root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 529 tests; `design_evidence.py ... --transition design-lock` exit 0 over the committed record; `build_design_evidence.py` regenerated the record directory byte for byte; `conformance.py finality-rebinds-after-tag-advance` exit 0 and its report equals the committed copy; `.hexaemeron/design/conformance.py` and `design-evidence.json` equal their committed copies; the committed runbook differs from `.hexaemeron/runbook.md` only at line 110, the study copy is byte-identical; `FinalityRebindTests` on the parent `f22de68086ad7265869636903554d09cf751e765`: 8 tests, 4 failures, 4 errors, so the guard is red without the step; `REQUEST_HEADERS` is `Content-Type` plus `User-Agent: alexandria-usdc-interval/0.5.0`, the version read from `plugin.json` at import, the decoy `USER_AGENT`, `HTTP_USER_AGENT` and `ALEXANDRIA_USER_AGENT` variables reach no header; `record_error` copies no exception text and `ALEXANDRIA_COMPOUND_RPC_URL` is read in `HttpsTransport.from_environment` only and stripped from the conformance child environment; no endpoint hostname, credential, private repository name, branch or absolute path was found in the 46 files. Five leads seen and left. First, `bind_finality` compares the plan hash against the by-number read only: a `finalized` or `safe` tag answering the boundary's own number with another hash is accepted, because the same provider's by-number read already settled the hash and the tag hash would refuse only a self-contradicting provider; the risk register asks for the by-number comparison and the number floor, both present. Second, `check_interval` loads journals for the declared classes only, so a manifest carrying a journal for an undeclared class is not refused, and `_gaps` names no omitted class; runbook step 3 owes both. Third, a plan declaring `boundary-blocks` alone collected 5 shards, reconciled `agreed`, built and checked offline under socket denial when probed by hand; no committed test covers the one-class tree. Fourth, `python3 -m unittest discover -s tests` from `plugins/alexandria` reports 2 pre-existing ImportErrors (`test_index`, `test_runner`, relative imports); the suite runner is `run_tests.py`. Fifth, `Reconciler._counts` counts a non-list `logs` result as 1 where the parent counted 0, matching the collector's own rule. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 1 -- 2026-09-06T22:02:54Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=reviewed; traces-omission-declared=not-applicable; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=reviewed; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py`, `design_evidence.py` and Imprimatur and Brevitas over the changed prose were not rerun this round; `hypomnema.py` ran over the two changed Markdown files, not the whole `plugins/alexandria` walk; an HTTP 403 or 429 on an opening read was not exercised beyond the existing transport-receipt and JSON-RPC-error paths `_ask` already receipts; `build` and `check` do not yet consume the `epoch-evidence` journal, so `start-hash-source`, `code-digest-rebind` and `traces-omission-declared` wait for step 3; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | plugins/alexandria/scripts/usdc_interval.py | `Reconciler.reconcile` gated only on `next_shard == len(shards)`, which the last shard's commit satisfies before any opening read is committed, and `_opening` read the `epoch-evidence` journal file rather than the checkpoint's committed offset; a tree killed on the seventh opening read with that record flushed but not checkpointed (offset 2524, journal 2844 bytes) reconciled `agreed` over 30 comparisons, although the collector truncates and re-issues that read on resume. Fixed in `eb59f4c6da732847a81180b82055cee5eab28c84`: `Staging.committed` reports `offsets`, `Staging.journal_bytes` reads the size, and reconcile refuses any journal longer than its committed offset before comparing; guard `test_an_opening_read_the_checkpoint_has_not_committed_refuses_to_reconcile` fails by assertion on `275f62953e80ea117fc0138ead034ebb701481ce`. | fixed |<br><br>Leads not pursued: Step head `275f62953e80ea117fc0138ead034ebb701481ce` on `8dda00d29fc621f03d4c763358232012191a3898`, 14 files, +1092/-56; security suite waived, Python and Markdown only; fixes range `275f62953e80ea117fc0138ead034ebb701481ce..eb59f4c6da732847a81180b82055cee5eab28c84`, 4 files, +45/-3. Gates before the fix: phylax exit 0, ephoros exit 0 over the 12 changed non-Horos paths, hypomnema exit 0 over the 2 Markdown files; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 561 tests. After the fix: the same three lints exit 0 over the 3 changed files plus the 2 Markdown files; `run_tests.py` exit 0, 562 tests; root suite exit 1 on `HorosCensusCurrencyTests` alone until `horos.py scan . --census --write` refreshed `.horos/census.json`, then exit 0, 1623 tests under `.githooks/greenlight`; Elenchus over `eb59f4c6da732847a81180b82055cee5eab28c84` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json`: parent report complete, 562 executed, 1 assertion failure, 0 errors, status `guarded`. `conformance.py opening-reads-resumable` exit 0 before and after the fix and its report equals the committed copy under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. The Mason's `opening-phase-resume` guard: `OpeningPhaseResumeTests` on the parent `8dda00d29fc621f03d4c763358232012191a3898` is an `AttributeError` (the class is absent there) and the overlaid test module errors on import, so as Elenchus classifies it the guard is not an assertion; against a mutant of the step head that discards the committed `epoch-evidence` entries and re-issues every opening read, 5 of its 8 cases fail by assertion (`re_issues_only_the_reads_past_the_checkpoint`, `on_a_code_read`, `after_the_last_opening_read`, `the_plan_does_not_name`, `opens_no_socket`), so it guards the mechanism the runbook names. The clean one-upgrade collection makes 7 opening reads, ids 16 to 22 under virtual shard 5, in the order first-block-header 15331586, implementation-slot 15331586 and 15331626, epoch-boundary-header 15331626 and 15331625, implementation-code at 15331586 and 15331626; each passes `_ask`'s byte ceiling, truncation, page-limit, envelope and JSON-RPC-error checks, `record_error` copies no exception text, and the endpoint-leak case asserts no scheme, host, credential or User-Agent reaches `receipts/errors.jsonl`. Resume into the opening phase re-reads no shard: `_settle_start` makes one by-number re-read of the last shard's boundary, then `_open_interval` replays committed records against the plan and re-checks each through `OpeningPhase.accept`. The demonstration `demo.py build` reproduces `sha256:5d0762677e64ed899cf6466f7dc2cc9a343931c8e0f972bc9a35fafa690f2de0` with `epochs` 1, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, before and after the fix, and `verify` exits 0; `examples/usdc-interval-v0/fixtures/` is byte-identical to the parent. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 14 changed files; the only URL is the test's `rpc.example.invalid` decoy. Round 1's first lead stays open: `bind_finality` is untouched by this step and still compares the plan hash against the by-number read only. Round 1's second and third leads (undeclared-class journals, the one-class tree) now sit with step 3's `_gaps` and class-agreement check; the fourth and fifth are unchanged. Five leads seen and left. First, a plan omitting `logs` reads one epoch (3 opening reads) with no `Upgraded(address)` source and no slot read at the interval's end to cross-check the last epoch's implementation, so an upgrade inside such an interval is undetectable by construction; the study at lines 413 to 414 says this run's plan declares `boundary-blocks` and `logs`, and step 3 owes the omission naming. Second, `Reconciler._compare_opening` compares the second provider's first-block header by `hash` alone and does not check its `number`. Third, reconcile does not re-ask the epoch-boundary headers: each is bound to the primary's upgrade log `blockHash`, and `log_identity` includes `blockHash`, so a second provider disagreeing about that block surfaces as a `log-identity` dispute on the shard; accepted as the study's design, not a hole. Fourth, an opening refusal receipt carries the refused block number as `status` and `unresolved` `{start, end}`, a bounded integer but a different meaning from the shard receipts' HTTP or JSON-RPC codes. Fifth, `Staging.record` refuses an opening record under a real shard index and a shard record under the virtual index, but nothing refuses a checkpoint whose `epoch-evidence` offset is non-zero while `next_shard` is below the plan, which only `validate_checkpoint` could name. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 2 -- 2026-09-06T22:21:09Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=reviewed; traces-omission-declared=not-applicable; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=reviewed; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py`, `design_evidence.py`, `demo.py build` and `verify`, and Imprimatur and Brevitas over the changed prose were not rerun this round, the tree they would read being byte-identical to the one round 1 ran them on at `eb59f4c6da732847a81180b82055cee5eab28c84`; `hypomnema.py` ran over the two changed Markdown files, not the whole `plugins/alexandria` walk; an HTTP 403 or 429 on an opening read was not exercised beyond the transport-receipt and JSON-RPC-error paths `_ask` receipts; `build` and `check` do not yet consume the `epoch-evidence` journal, so `start-hash-source`, `code-digest-rebind` and `traces-omission-declared` wait for step 3; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Audited tree `eb59f4c6da732847a81180b82055cee5eab28c84` (step head `275f62953e80ea117fc0138ead034ebb701481ce` plus the round-1 fix, 4 files, +45/-3) over the step's full diff `8dda00d29fc621f03d4c763358232012191a3898..eb59f4c6da732847a81180b82055cee5eab28c84`, 14 files, +1135/-57 including the fix; security suite waived, Python and Markdown only; no fixes commit this round. Gates: phylax exit 0 and ephoros exit 0 over the 12 changed non-Horos paths, hypomnema exit 0 over the 2 Markdown files (no H009, so no index root was needed); root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 562 tests; `conformance.py opening-reads-resumable` exit 0 and its report equals the committed copy under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. S2-R1-01 re-verified on the fixed tree: the guard `test_an_opening_read_the_checkpoint_has_not_committed_refuses_to_reconcile` overlaid on `275f62953e80ea117fc0138ead034ebb701481ce` fails by assertion (`AlexandriaError not raised`, 1 test, 1 failure, 0 errors) and passes at `eb59f4c6`; a clean tree with every journal exactly at its committed offset reconciles `agreed` over 30 comparisons; one byte appended to the `epoch-evidence` journal, and separately to the `logs` journal, refuses before any comparison, names the class and writes no `reconciliation.json`; a tree killed on the fourth opening read with the fourth record flushed but not checkpointed refuses, then resumes (`issued` 4, `resumed_from` 3, `total` 7) to journals byte-identical to a clean run and reconciles `agreed`; a tree killed at `shard 2 logs` resumes to byte-identical journals and reconciles `agreed`, so the fix introduced no resume regression. One lead from the fix, left: the refusal names the class but not the two byte counts, and a journal one byte shorter than its committed offset draws the same "holds bytes the checkpoint has not committed" wording (`Staging.committed` does not run `resume`'s shorter-than-offset check); the refusal is correct in both directions and no evidence is destroyed. Round 1's five leads against the fixed tree: first, a plan omitting `logs` still reads one epoch (3 opening reads) with no end-of-interval slot read, open, owed by step 3's `_gaps` omission naming; second, `Reconciler._compare_opening` still compares the first-block header by `hash` alone, a header carrying the right hash under another `number` returns `(True, first-block-hash)`, open, unchanged by the fix; third, epoch-boundary headers not re-asked, accepted as the study's design, unchanged; fourth, an opening refusal receipt still carries the refused block number as `status`, open, unchanged; fifth, `validate_checkpoint` still accepts `next_shard` 2 with a non-zero `epoch-evidence` offset, open, unchanged by the fix, which reads offsets and does not validate them further. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 14 changed files; the only URL is the test's `rpc.example.invalid` decoy. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 1 -- 2026-09-07T01:01:22Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=reviewed; code-digest-rebind=reviewed; opening-phase-resume=reviewed; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=not-applicable; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, `build_design_evidence.py`, and Imprimatur, Vulgate and Brevitas over the changed prose were not run this round; `hypomnema.py` ran over `plugins/alexandria/scripts`, `tests`, `docs` and the example README, not the whole repository walk; no transport, paging or retry path changed, so `rate-limit-refusal` and `silent-truncation` were read but not re-exercised; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only; the four unreachable-by-test refusal branches in `_recheck_implementation_code` (unknown component shape, unknown record shape, non-lowercase address, duplicate address) were read and not exercised; the overlay resolver was not consulted, the security suite being waived.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| W3-R1-01 | low | plugins/alexandria/scripts/usdc_interval.py | The new journal-record loop in `check_interval` shape-checked each record's key set but not its value types, and `_replay_release_opening` and the count derivation then call `.encode()` on `request` and `response`. A self-consistent release whose `epoch-evidence` record carries `"request": 5`, or whose `logs` record carries a non-text `response`, made `check` raise `AttributeError` rather than refuse by name; it still failed closed. Reproduced by hand on the demonstration release with `verify` patched to the manifest's own identity. Fixed in `959c6a7ba272d1818513583e80fe30a3fc9dab88`: both fields are required to be text where the keys are checked, and the `FIXED_COMPONENTS` comment saying "these seven" for a six-name tuple is corrected to six. Guard `test_a_journal_record_whose_request_or_response_is_not_text_is_refused_by_name` asserts the exception type, so it fails by assertion on `bcf73f89f27ea8ec8c7e8072c63905c1530838a7` rather than erroring. | fixed |<br><br>Leads not pursued: Step head `bcf73f89f27ea8ec8c7e8072c63905c1530838a7` on `a680d7887958f280efba3358a450a524975b29c0`, 16 files, +1167/-275; security suite waived, Python and Markdown only; fixes range `bcf73f89f27ea8ec8c7e8072c63905c1530838a7..959c6a7ba272d1818513583e80fe30a3fc9dab88`, 3 files, +35/-3. Gates before the fix: phylax exit 0 and ephoros exit 0 over the 5 changed Python paths, hypomnema exit 0 over `plugins/alexandria/scripts`, `tests`, `docs` and the example README; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 598 tests; `scripts/run_checks.py --scope alexandria --scope root` exit 0, outcome green, 10 checks. After the fix: the same three lints exit 0; `run_tests.py` exit 0, 599 tests; root suite exit 0, 1623 tests under `.githooks/greenlight`; Elenchus over `959c6a7ba272d1818513583e80fe30a3fc9dab88` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json`: `guarded`, the runner report recording a parent assertion failure. The same run under the pre-amend commit `da49d263c7a62871c8484675d33fda04b6eb133a`, whose guard used `assertRaisesRegex`, returned `inconclusive` on an infrastructure error, which is why the guard asserts the type. `conformance.py scope-binds-both-hashes` and `conformance.py code-hash-rechecked-from-component` exit 0 before and after the fix and both reports equal their committed copies under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design_evidence.py .hexaemeron/design-evidence.json --transition step:4` clean, SHA-256 `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`, selected candidate `opening-reads-in-collect`. `demo.py build` reproduces `sha256:7cf794f07cb74c0383ae3fdd270758324b8036705c9845a7724043993dde40aa` with `epochs` 1, `implementations` `0x42f9505a376761b180e27a01ba0554244ed1de7d` to `302225f1bb36d46cee282bbb0fd9eb92931eecc0f1fbe36b87a83b3411ff89e3`, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, and `verify` exits 0, both before and after the fix; the built release carries 10 components and its reconciliation is `agreed` over 28 comparisons, 25 shard and 3 opening-read, which is the README's count; `expected.json` and the README pin that identifier and that digest. `_check_scopes` refuses a scope carrying one hash and not the other, a scope with neither, a finality class the plan does not bind, a start hash other than the replayed first-block read, an end hash other than the last shard's boundary read, and any non-journal capture carrying a boundary hash; `scope_finality` keeps a `confirmations` plan at `provider-reported` while still binding both hashes. `_recheck_implementation_code` re-hashes the `implementation-code` component's own release bytes for the receipt's declared digest and re-hashes each record's runtime code for the epoch's `implementation_code_sha256`, so nothing is copied from the epoch table; a flipped byte, a changed hex digit, a missing implementation and a stray implementation each refuse by name, and `derived_epochs != receipt["epochs"]` then refuses a table the preserved opening reads do not derive. Class agreement is settled three ways: an unexpected component name, an absent expected one, a journal whose `class` is not its component's name, and a record whose `class` is not its journal's. `_gaps` names every omitted evidence class with its reason on every journal capture and on no other, and `check_interval` refuses a journal capture whose coverage omits that naming. No test asserts a digest it also computes through the code path under test: the recheck cases hash the component's bytes and each code body with `hashlib` directly, and `expected.json` pins the digest as a literal. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 16 changed files; the only URLs are the pre-existing `example.invalid` and `fixture.invalid` decoys the leak tests assert against. The seven leads carried from the step-2 rounds: first, the plan omitting `logs` is now named on every evidence scope, `OMISSION_REASONS["logs"]` saying an `Upgraded(address)` inside the interval is undetectable from these bytes, so the lead is closed by declaration while the underlying blindness stands as a declared gap; second, `_compare_opening` now compares the second provider's first-block `number` as well as its `hash`, closed; third, epoch-boundary headers are still not re-asked, accepted as the study's design, unchanged; fourth, an opening refusal receipt still carries a block number as `status` and the new `tag-hash-mismatch` receipt does the same, open, and `record_error` bounds `status` to a number or a short plain string without fixing its meaning; fifth, `validate_checkpoint` now refuses a non-zero `epoch-evidence` offset while `next_shard` is below the plan, and the same under a history entry that is not the plan's last shard, closed; sixth, `require_committed_journals` now names both byte counts in both directions and separates "holds bytes the checkpoint has not committed" from "is shorter than its committed offset", closed; seventh, `bind_finality` now compares the tag's hash when the tag stands exactly at the boundary's own number, closed for that case, and a tag standing above the boundary still cannot name the boundary's hash, which the by-number read already binds. Three leads seen and left. First, `epochs_from_opening` overwrites the opening phase's own header read at the interval's last block with the last shard's `end_hash`, so an upgrade at that exact block would have its two reads silently reconciled in the shard's favour; the path is guarded today only because `discover_epochs` compares the upgrade log's `blockHash` against that hash and refuses, and nothing refuses it where no log announces the block. Second, `check_interval` and `_check_scopes` index `captures[name]` for every journal component, so a manifest carrying a component with no matching capture raises a `KeyError` rather than a named refusal; Alexandria's `verify` is what stands between that and the CLI, and the pattern predates this step. Third, `_capture` names the `implementation-code` collection `implementations` while the component is `implementation-code`, which no check compares. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 2 -- 2026-09-07T01:25:04Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=reviewed; code-digest-rebind=reviewed; opening-phase-resume=reviewed; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=not-applicable; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, `design_evidence.py`, `build_design_evidence.py`, and Imprimatur, Vulgate and Brevitas over the changed prose were not run this round; `hypomnema.py` ran over `plugins/alexandria/scripts`, `tests`, `docs` and the example README, not the whole repository walk; no transport, paging, retry or header path changed, so `rate-limit-refusal`, `silent-truncation` and the header constants were read and not re-exercised; the four unreachable-by-test refusal branches in `_recheck_implementation_code` (unknown component shape, unknown record shape, non-lowercase address, duplicate address) were read and not exercised; a single-block interval where `start` equals `end`, the one residue of the `epochs_from_opening` lead, was settled by reading `opening_prefix` and `discover_epochs` rather than by building a fixture for it; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only; the overlay resolver was not consulted, the security suite being waived.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| W3-R2-01 | low | plugins/alexandria/scripts/usdc_interval.py | Round 1's fix typed a journal record's `request` and `response` but not its `shard`, which the next line makes a set element and the count derivation makes a dictionary key. On the fixed tree a release whose `logs` record carries `"shard": ["x"]` or `{"a": 1}` raised `TypeError: cannot use 'list' as a set element` rather than refusing by name, and `"shard": true` was accepted as shard one wherever a record for shard one existed. Both reproduced by hand on a built release with `verify` patched to the manifest's own identity; both fail closed. Fixed in `8673d12c2f5f00acc28e573902edc09b2faf0a5a`: the shard index must be an `int` and not a `bool` where the other two types are checked. Guard `test_a_journal_record_whose_shard_index_is_not_a_whole_number_is_refused_by_name` asserts the exception type, so on `cc2205f1f2402f51c0869398383259be8f1c2ad3` it fails by assertion in all three subtests (3 failures, 0 errors) rather than erroring. | fixed |<br><br>Leads not pursued: Audited tree `cc2205f1f2402f51c0869398383259be8f1c2ad3` (step head `bcf73f89f27ea8ec8c7e8072c63905c1530838a7` plus the round-1 fix `959c6a7ba272d1818513583e80fe30a3fc9dab88`) over the step's full diff `a680d7887958f280efba3358a450a524975b29c0..959c6a7ba272d1818513583e80fe30a3fc9dab88`, 16 files, +1199/-275; security suite waived, Python and Markdown only; fixes range `cc2205f1f2402f51c0869398383259be8f1c2ad3..8673d12c2f5f00acc28e573902edc09b2faf0a5a`, 3 files, +33/-2. Gates before the fix: phylax exit 0 and ephoros exit 0 over the 5 changed Python paths, hypomnema exit 0 over `plugins/alexandria/scripts`, `tests`, `docs` and the example README; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 599 tests. After the fix: the same three lints exit 0; `run_tests.py` exit 0, 600 tests; `horos.py scan . --census --write` refreshed `.horos/census.json` alone, and the root suite then exits 0, 1623 tests, under `.githooks/greenlight`; Elenchus over `8673d12c2f5f00acc28e573902edc09b2faf0a5a` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json` deleted first: `guarded`, the runner report recording a parent assertion failure. `conformance.py scope-binds-both-hashes` and `conformance.py code-hash-rechecked-from-component` exit 0 and both reports equal their committed copies under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. `demo.py build` reproduces `sha256:7cf794f07cb74c0383ae3fdd270758324b8036705c9845a7724043993dde40aa` with `epochs` 1, `implementations` `0x42f9505a376761b180e27a01ba0554244ed1de7d` to `302225f1bb36d46cee282bbb0fd9eb92931eecc0f1fbe36b87a83b3411ff89e3`, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, `reconciliation` `agreed`, 5 complete shards, and `verify` exits 0, so the round's fix changes no release byte. W3-R1-01 re-verified: round 1's rewritten guard overlaid on `bcf73f89f27ea8ec8c7e8072c63905c1530838a7` fails by assertion in both subtests (2 failures, 0 errors, `AttributeError("'int' object has no attribute 'encode'") is not an instance of AlexandriaError`) and passes at `cc2205f1`; the type check covers both fields the later code calls `.encode()` on, in both branches, because `_replay_release_opening` and the count derivation read only `documents["logs"]` and `documents[OPENING_CLASS]`, both in `journal_names`, and the record loop runs before either; the refusal names the class and the field; a clean release still checks to `epochs` 2, `agreed`, 5 complete shards and both implementations. `check` still refuses by name a flipped byte in `implementation-code`, a changed hex digit in an epoch digest, a missing implementation, a stray implementation, an epoch table the opening reads do not derive, a scope carrying one boundary hash, a scope carrying neither, a wrong finality class, a start hash other than the replayed first-block read, an end hash other than the last shard's boundary read, and boundary hashes on a non-journal capture; `CodeHashRecheckTests` is 14 cases, all green. The three leads round 1 left, decided on this tree. First, `epochs_from_opening` still overwrites `block_hashes[end]` with the last shard's `end_hash`, but `opening_prefix` reads a header only at the interval's start, at each upgrade block and at the block before it, so `phase.hashes` can hold the interval's last block only when an upgrade sits exactly there, where `discover_epochs` refuses on `announced_hash != opening_hash`, or when `start` equals `end`, where the scope's start hash is still taken from `phase.hashes[start]` and not from the overwritten table; not a finding on this tree. Second, `captures[name]` indeed raises `KeyError: 'logs'` for a manifest missing that capture, but only with `verify` patched: through `check_interval`'s own `verify` the same edited manifest refuses with "manifest release identity does not match its content" before any indexing, so the branch is unreachable from the command; not a finding. Third, `_capture` names five collections differently from their component ids -- `epoch-table` to `epochs`, `implementation-code` to `implementations`, `interval-plan` to `interval-plan-shards`, `reconciliation` to `reconciliation-shards`, `registry` to `registry-entries` -- so the `implementations` name is the manifest's convention rather than a drift, and no check is owed; not a finding. Round 1's dispositions of the carried leads hold on this tree: `_compare_opening` still compares the second provider's first-block `number` as well as its `hash`, `validate_checkpoint` still refuses a non-zero `epoch-evidence` offset below the plan's last shard, `require_committed_journals` still names both byte counts in both directions, `bind_finality` still compares the tag's hash at the boundary's own number, and the omitted `logs` class is still named on every evidence scope through `OMISSION_REASONS`, all closed; the refusal receipt carrying a block number as `status`, which the `tag-hash-mismatch` receipt follows, remains open and unchanged by either fix. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 16 changed files or the 3 files the fix touched; the only URLs are the pre-existing `example.invalid`, `fixture.invalid` and `rpc.example.invalid` decoys the leak tests assert against. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 3 -- 2026-09-07T01:54:43Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=reviewed; code-digest-rebind=reviewed; opening-phase-resume=reviewed; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=not-applicable; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, `design_evidence.py`, `build_design_evidence.py`, and Imprimatur, Vulgate and Brevitas over the changed prose were not run this round; `hypomnema.py` ran over `plugins/alexandria/scripts`, `tests`, `docs` and the example README, not the whole repository walk; no transport, paging, retry or header path changed, so `rate-limit-refusal`, `silent-truncation` and the header constants were read and not re-exercised; the enumeration mutated the first element of each list and every dictionary field, not every element of every list, and used six wrong-typed values and field deletion, not every malformed value; the four unreachable-by-test refusal branches in `_recheck_implementation_code` were read and not exercised; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only; the overlay resolver was not consulted, the security suite being waived.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| W3-R3-01 | low | plugins/alexandria/scripts/usdc_interval.py | The `reconciliation` component was the one record shape `check_interval` consumed with no shape check: its fields were indexed straight, and `validate_reconciliation` (`alexandria_lib/interval.py:1148`) returns on a null record, a shape the receipt schema declares legal under `reconciliation.oneOf.null`. On a self-consistent release built through the real unpatched `verify`, an absent `plan_sha256` or `reconciliation` raised `KeyError`, a null `reconciliation` raised `TypeError: 'NoneType' object is not subscriptable` at `check_interval`'s return where the status is read, and a wrong or absent `format` was accepted. The null case reproduced through the `check` CLI: uncaught traceback, exit 1, so it still failed closed. Fixed in `214b360ec8937ca56cbd25a6db77550b710521d5`: the component's key set and format are checked and a null record refuses by name, beside the checks the epoch table and the journals already carry. Guard `test_a_reconciliation_component_of_an_unknown_shape_is_refused_by_name` asserts the exception type over four subtests. | fixed |<br>| W3-R3-02 | medium | plugins/alexandria/scripts/usdc_interval.py | The same construction found the receipt's own copy of the comparison uncompared. `check_interval` validated `documents["reconciliation"]["reconciliation"]` and returned its status, while `receipt["reconciliation"]`, which the builder takes from the same object at `usdc_interval.py:1209`, was read by nothing: deleting any of its five fields was accepted, and a release declaring `status` `agreed` over 9999 comparisons in the shipped `epoch-table` beside a record saying `unreconciled` checked clean. A reader of the receipt alone would believe the fabricated comparison. Fixed in the same commit: the two copies must be equal. Guard `test_a_receipt_declaring_another_comparison_than_the_record_is_refused` asserts the exception type. | fixed |<br><br>Leads not pursued: The round's question was whether the value-type class round 1 and round 2 fixed field by field is closed, settled by construction rather than by reading. A release was built from the `usdc-interval-v0` demonstration and every field of every record shape `check_interval`, `_replay_release_opening`, the count derivation, `_recheck_implementation_code` and `_check_scopes` consume was enumerated: 201 schema paths over the manifest and all ten components, each fed six wrong-typed values (`5`, `["x"]`, `{"a": 1}`, `true`, `null`, `"x"`), 1206 mutations, plus 179 field deletions. Every mutated release was rebuilt self-consistently -- component digests, byte counts, object paths, capture `component_sha256` and the manifest's own `release_id` recomputed -- and run through the real `verify`, a stronger model than the patched `verify` rounds 1 and 2 used. On the audited tree `8673d12c2f5f00acc28e573902edc09b2faf0a5a` exactly three mutations raised rather than refused, all three in the `reconciliation` component, which is W3-R3-01; the receipt's unchecked comparison block, which accepted rather than raised, is W3-R3-02. On the fixed tree `214b360ec8937ca56cbd25a6db77550b710521d5` the same 1385 mutations produce no non-`AlexandriaError` outcome. The class is closed: the journal record's four fields (`class`, `request`, `response`, `shard`) are each typed or compared by equality, the epoch table, the `implementation-code` component, the journals and now the reconciliation component each carry a key-set and format check, and the manifest's own shapes are settled by Alexandria's `validate_manifest` before `check_interval` sees them -- a `components[].name` or `captures[].component` of any non-string type refuses with "component name must be a lowercase stable name" and "capture component must be a lowercase stable name" rather than raising, which retires round 1's second lead in the stronger model as well as the weaker one. Three leads left, none judged worth another round. First, the `registry` and `error-receipts` components are shape-checked by nothing in `check_interval`: deleting `format`, `source`, `entries[].proxy` or any other registry field is accepted. No reader in this file consumes them, so nothing raises and nothing is believed; they are carried bytes, and giving them a shape check is a change the step did not ask for. Second, the refusal-receipt `status` block number carried over from step 2 remains open and unchanged; `record_error` still bounds `status` to a number or a short plain string without fixing its meaning, and the `tag-hash-mismatch` receipt follows it. Third, epoch-boundary headers are still not re-asked, accepted as the study's design. Round 1's and round 2's other dispositions hold unchanged on this tree, none of them touched by this round's fix: `_compare_opening` still compares the second provider's first-block `number` as well as its `hash` (`usdc_interval.py:885`), `validate_checkpoint` still refuses a non-zero `epoch-evidence` offset below the plan's last shard, `require_committed_journals` still names both byte counts in both directions and separates the two cases, `bind_finality` still compares the tag's hash at the boundary's own number, the omitted `logs` class is still named on every evidence scope through `OMISSION_REASONS`, and `epochs_from_opening`'s `block_hashes[end]` overwrite is still unreachable except where `discover_epochs` refuses. Gates before the fix: phylax exit 0 and ephoros exit 0 over the 5 changed Python paths, hypomnema exit 0 over `plugins/alexandria/scripts`, `tests`, `docs` and the example README; `plugins/alexandria/tests/run_tests.py` exit 0, 600 tests. After the fix: the same three lints exit 0; `run_tests.py` exit 0, 602 tests; `horos.py scan . --census --write` exit 0, refreshing `.horos/census.json` alone; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests, under `.githooks/greenlight`, which recorded tree `c055fc34e2c0f64ca08e5e8899bde4ebce390eee`. Elenchus over `214b360ec8937ca56cbd25a6db77550b710521d5` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json` deleted first: `guarded`, the runner report recording a parent assertion failure. `conformance.py scope-binds-both-hashes` and `conformance.py code-hash-rechecked-from-component` exit 0 and both reports equal their committed copies under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. `demo.py build` still reproduces `sha256:7cf794f07cb74c0383ae3fdd270758324b8036705c9845a7724043993dde40aa` with `epochs` 1, `implementations` `0x42f9505a376761b180e27a01ba0554244ed1de7d` to `302225f1bb36d46cee282bbb0fd9eb92931eecc0f1fbe36b87a83b3411ff89e3`, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, `reconciliation` `agreed`, 5 complete shards, and `verify` exits 0, so neither fix changes a release byte. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 18 files of the step's diff or the 3 files the fix touched; the only URLs are the pre-existing `example.invalid`, `fixture.invalid` and `rpc.example.invalid` decoys the leak tests assert against. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 4 -- 2026-09-07T02:29:21Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=reviewed; code-digest-rebind=reviewed; opening-phase-resume=not-applicable; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=not-applicable; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, `design_evidence.py`, `build_design_evidence.py`, and Imprimatur, Vulgate and Brevitas over the changed prose were not run this round; `hypomnema.py` ran over `plugins/alexandria/scripts`, `tests`, `docs` and the example README, not the whole repository walk, and the repository walk still carries seven pre-existing H008 and H009 findings in `DEMONSTRATION.md` and two other runs' audit records, none of them this step's; no transport, paging, retry, header or checkpoint path changed, so `rate-limit-refusal`, `silent-truncation` and `opening-phase-resume` were read and not re-exercised; the enumeration mutated one leaf at a time and never two together, so a release that moved a value and its every reader consistently was not modelled; values inside a journal record's `request` and `response` envelopes were probed by hand at the boundary header and one duplicated logs record, not enumerated; the `registry` component's 621 accepted leaf mutations were counted and not read one by one; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only; the overlay resolver was not consulted, the security suite being waived.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| W3-R4-01 | medium | plugins/alexandria/scripts/usdc_interval.py | The shard table's `end_hash` was never compared with the read that produced it. `declared_classes` refuses a plan without `boundary-blocks`, so every release carries that journal, and it holds one `eth_getBlockByNumber [hex(shard.end), False]` per shard whose result names the block's `number` and `hash`; `_boundary_hash` (`usdc_interval.py:654`) is where the collector takes the declared value from. `check_interval` read `shards[].end_hash` for the epoch-boundary coincidence check and passed `shards[-1]["end_hash"]` into `epochs_from_opening` and `_check_scopes` as the interval's end hash, comparing none of them with the journal. On a self-consistent release built through the real unpatched `check` CLI, a fabricated `end_hash` on any of shards 0 to 3 exited 0; so did a fabricated `hash` inside the preserved header; so did a header whose `number` named block `0xdeadbe`. The last shard's hash was pinned only to the epoch table's own epoch `end_hash`, which `epochs_from_opening` takes from that same declared value, so it was pinned to itself: moving both together exited 0. Fixed in `c0b144a7c05a04fd5bf49931ac911504139f7996`: every shard's `end_hash` is compared with its preserved boundary read, and that read's own block number must name the shard's last block. Guards `test_a_shard_boundary_hash_is_compared_with_its_preserved_read` over five subtests and `test_the_last_shards_boundary_hash_is_compared_with_its_preserved_read`. | fixed |<br>| W3-R4-02 | medium | plugins/alexandria/scripts/usdc_interval.py | The reconciliation record carries its own copy of the shard table and only the two copies' `status` lists were compared, so its `end_hash` and `record_counts` were read by nothing: the receipt's counts are compared with the journals and the receipt's hashes now with the boundary reads, while this copy's were compared with neither. Through the real `check` CLI a release declaring in the reconciliation record that shard 0 held 9999 logs, or naming a fabricated boundary hash on any of its five shards including the last, exited 0, and a reader of the reconciliation alone would believe it. This is round 3's W3-R4 shape in the same component: one table written twice, one copy checked. Fixed in the same commit: `reconciliation["shards"]` must equal the receipt's entry for entry, which subsumes the status comparison. Guard `test_a_reconciliation_shard_table_differing_from_the_receipts_is_refused` over two subtests. | fixed |<br>| W3-R4-03 | low | plugins/alexandria/scripts/usdc_interval.py | `_check_scopes` compared each evidence scope's `start_hash` with the collector's own first-block read and its `end_hash` with the last shard's boundary, and never compared the block range those two hashes sit beside with the plan's interval. Alexandria's `_validate_interval` requires only that `start` does not exceed `end`. Through the real `check` CLI a release whose every evidence scope declared blocks 15331587 to 15331686 exited 0, as did one whose `logs` scope alone declared blocks 1 to 2 while carrying the hashes of 15331586 and 15331685. Fixed in the same commit: each evidence scope's range must be the plan's. Guard `test_an_evidence_scope_declaring_another_block_range_is_refused` over the four journal classes. | fixed |<br>| W3-R4-04 | medium | plugins/alexandria/scripts/usdc_interval.py | The count derivation assigned rather than summed: `derived[record["shard"]][name] = len(result) if isinstance(result, list) else 1` named the last record's size for that shard and class and hid every earlier one. Through the real `check` CLI a release whose `logs` journal carried a sixth record duplicating shard 0's, with three fabricated log entries, exited 0 while the receipt's `record_counts` for shard 0 still said `logs` 3, so the release carried evidence its own receipt did not count. Fixed in the same commit by adding the sizes. Guard `test_a_shard_carrying_one_class_twice_declares_both_reads`. | fixed |<br>| W3-R4-05 | low | plugins/alexandria/scripts/usdc_interval.py | Each component's coverage and scope are read under the component's own name at `check_interval`'s `captures[name]["coverage"]["gaps"]` and throughout `_check_scopes`, and the capture map was indexed straight. A release whose `traces` capture carried the id `x` raised `KeyError: 'traces'`, an uncaught traceback at exit 1, rather than refusing by name; the release passed Alexandria's `verify`, which requires only that a capture id be a name. This is the last outcome of round 3's raise-rather-than-refuse class, found by the 3689-mutation type and deletion sweep rather than the value sweep. Fixed in the same commit: a component with no capture of its name refuses. Guard `test_a_capture_that_names_no_component_of_the_release_is_refused` asserts the exception type. | fixed |<br><br>Leads not pursued: The round asked which values a release declares about itself that `check` accepts without comparing them against the bytes they describe, and settled it by construction rather than by reading. A release was built from the `usdc-interval-v0` demonstration and every scalar leaf of the manifest and all ten components was enumerated: 1111 type- and shape-preserving value mutations, each rebuilt self-consistently -- component digests, byte counts, object paths, capture `component_sha256` and the manifest's own `release_id` recomputed -- and run through the real unpatched `check_interval`. Integers moved by one, block hashes, addresses, hex quantities and SHA-256 digests had a hex digit flipped, decimal block numbers moved by one, and each enumerated status, finality class, access, redistribution, locator and evidence class was swapped for another value its own schema declares legal, so every mutation left a release Alexandria's `verify` accepts. On the audited tree `b6d6c036fdfb5707845a97094d62b2f9bfc4a6f1` 788 mutations were accepted; discounting the 621 in `registry`, the 142 in fields the rebuild recomputes and the 1 in `error-receipts`, 24 were quantities the release declares about its own evidence, and they are W3-R4-01, W3-R4-02 and W3-R4-03. Each was then reproduced as a complete release through the `check` CLI at exit 0, together with two probes of values inside a journal response envelope and one duplicated journal record, which are W3-R4-01's other side and W3-R4-04. A separate sweep applied round 3's model -- six wrong-typed values and field deletion over the same leaves, 3689 mutations outside `registry` -- and found one non-`AlexandriaError` outcome, which is W3-R4-05. On the fixed tree `c0b144a7c05a04fd5bf49931ac911504139f7996` both sweeps are clean: the value sweep accepts nothing in `epoch-table`, `reconciliation`, `implementation-code`, `interval-plan` or any journal, and the type sweep produces no non-`AlexandriaError` outcome. The class is closed on quantities: every count, status, block number, boundary hash, code digest and component digest a release declares is now recomputed from the preserved bytes or compared with another copy that is. Three leads left, none judged worth another round. First, the 400 mutations still accepted are provenance and rights labels with no referent inside the release -- `evidence_class`, `source.kind`, `source.locator_class`, `source.reference`, `components[].access`, `media_type` and `redistribution`, the informational text of a `coverage.gaps` entry, and the `finality` and block range on the six captures that preserve no chain read and are refused boundary hashes by `_check_scopes`. Nothing in a release's own bytes could establish any of them offline, so they are unfalsifiable by construction rather than fail-open, and binding them would need an Ariadne statement rather than a check. Second, round 3's standing lead holds unchanged: the `registry` and `error-receipts` components are shape-checked by nothing in `check_interval` and consumed by nothing there either, which is why 621 registry mutations and the `error-receipts` `format` are accepted; they are carried bytes and giving them a shape check is a change the step did not ask for. Third, the refusal-receipt `status` block number carried over from step 2 remains open, and epoch-boundary headers are still not re-asked, accepted as the study's design. Round 1's, round 2's and round 3's dispositions hold on this tree, none of them touched by this round's fix: the wrong-typed-field class stays closed under the stronger 3689-mutation model, `_compare_opening` still compares the second provider's first-block `number` as well as its `hash` (`usdc_interval.py:885`), `validate_checkpoint` still refuses a non-zero `epoch-evidence` offset below the plan's last shard, `require_committed_journals` still names both byte counts in both directions, `bind_finality` still compares the tag's hash at the boundary's own number, the omitted `logs` class is still named on every evidence scope through `OMISSION_REASONS`, and the reconciliation component's key set, format and null record are still refused by name. Gates before the fix: phylax exit 0 and ephoros exit 0 over `plugins/alexandria/scripts` and `plugins/alexandria/tests`, hypomnema exit 0 over `plugins/alexandria/scripts`, `tests`, `docs` and the example README; root suite exit 0. After the fix: the same three lints exit 0; `plugins/alexandria/tests/run_tests.py` exit 0, 608 tests; `horos.py scan . --census --write` exit 0, refreshing `.horos/census.json` alone; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests, under `.githooks/greenlight`, which recorded tree `b2e3bb57c243ff0ce1c5a291132a788a84b8792c`. Elenchus over `c0b144a7c05a04fd5bf49931ac911504139f7996` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json` deleted first: `guarded`, the runner report recording a parent assertion failure. `conformance.py scope-binds-both-hashes` and `conformance.py code-hash-rechecked-from-component` exit 0 and both reports equal their committed copies under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. `demo.py build` still reproduces `sha256:7cf794f07cb74c0383ae3fdd270758324b8036705c9845a7724043993dde40aa` with `epochs` 1, `implementations` `0x42f9505a376761b180e27a01ba0554244ed1de7d` to `302225f1bb36d46cee282bbb0fd9eb92931eecc0f1fbe36b87a83b3411ff89e3`, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, `reconciliation` `agreed`, 5 complete shards, and `verify` exits 0, so the fix changes no release byte. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 3 files the fix touched; the only URLs in the audited sources are the pre-existing `example.invalid`, `fixture.invalid` and `rpc.example.invalid` decoys the leak tests assert against. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
