Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1350-alexandria-1-interval-collector-run-against.md | source_sha256=d83b70f775dd825d6042b98dfecf056ee0500b2d208e4f62e6f2fa39f88bad27 | h2_count=3
## Step 1, round 1 -- 2026-09-06T20:48:12Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=not-applicable; traces-omission-declared=reviewed; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=not-applicable; preserved-bytes-identity=not-applicable; silent-truncation=not-applicable; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: the runbook's own exit commands `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py` over the committed study and runbook, and Imprimatur and Brevitas over the changed prose were not rerun this round; `hypomnema.py` ran over the three changed Markdown files, not the whole `plugins/alexandria` walk; the missing-fixture-fails path of the new cases was not re-exercised; the receipt produced by an HTTP 403 or 429 on the two new finality reads was not exercised beyond the existing transport-receipt case.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Step head `4365122588167cff7b66f527bceac34eb9f31a66` on `f22de68086ad7265869636903554d09cf751e765`, 46 files, +3027/-105; security suite waived, Python and Markdown only. Gates: phylax exit 0, ephoros exit 0 and hypomnema exit 0 over the 44 changed non-Horos paths (hypomnema over the 3 Markdown files); root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 529 tests; `design_evidence.py ... --transition design-lock` exit 0 over the committed record; `build_design_evidence.py` regenerated the record directory byte for byte; `conformance.py finality-rebinds-after-tag-advance` exit 0 and its report equals the committed copy; `.hexaemeron/design/conformance.py` and `design-evidence.json` equal their committed copies; the committed runbook differs from `.hexaemeron/runbook.md` only at line 110, the study copy is byte-identical; `FinalityRebindTests` on the parent `f22de68086ad7265869636903554d09cf751e765`: 8 tests, 4 failures, 4 errors, so the guard is red without the step; `REQUEST_HEADERS` is `Content-Type` plus `User-Agent: alexandria-usdc-interval/0.5.0`, the version read from `plugin.json` at import, the decoy `USER_AGENT`, `HTTP_USER_AGENT` and `ALEXANDRIA_USER_AGENT` variables reach no header; `record_error` copies no exception text and `ALEXANDRIA_COMPOUND_RPC_URL` is read in `HttpsTransport.from_environment` only and stripped from the conformance child environment; no endpoint hostname, credential, private repository name, branch or absolute path was found in the 46 files. Five leads seen and left. First, `bind_finality` compares the plan hash against the by-number read only: a `finalized` or `safe` tag answering the boundary's own number with another hash is accepted, because the same provider's by-number read already settled the hash and the tag hash would refuse only a self-contradicting provider; the risk register asks for the by-number comparison and the number floor, both present. Second, `check_interval` loads journals for the declared classes only, so a manifest carrying a journal for an undeclared class is not refused, and `_gaps` names no omitted class; runbook step 3 owes both. Third, a plan declaring `boundary-blocks` alone collected 5 shards, reconciled `agreed`, built and checked offline under socket denial when probed by hand; no committed test covers the one-class tree. Fourth, `python3 -m unittest discover -s tests` from `plugins/alexandria` reports 2 pre-existing ImportErrors (`test_index`, `test_runner`, relative imports); the suite runner is `run_tests.py`. Fifth, `Reconciler._counts` counts a non-list `logs` result as 1 where the parent counted 0, matching the collector's own rule. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 1 -- 2026-09-06T22:02:54Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=reviewed; traces-omission-declared=not-applicable; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=reviewed; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py`, `design_evidence.py` and Imprimatur and Brevitas over the changed prose were not rerun this round; `hypomnema.py` ran over the two changed Markdown files, not the whole `plugins/alexandria` walk; an HTTP 403 or 429 on an opening read was not exercised beyond the existing transport-receipt and JSON-RPC-error paths `_ask` already receipts; `build` and `check` do not yet consume the `epoch-evidence` journal, so `start-hash-source`, `code-digest-rebind` and `traces-omission-declared` wait for step 3; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | plugins/alexandria/scripts/usdc_interval.py | `Reconciler.reconcile` gated only on `next_shard == len(shards)`, which the last shard's commit satisfies before any opening read is committed, and `_opening` read the `epoch-evidence` journal file rather than the checkpoint's committed offset; a tree killed on the seventh opening read with that record flushed but not checkpointed (offset 2524, journal 2844 bytes) reconciled `agreed` over 30 comparisons, although the collector truncates and re-issues that read on resume. Fixed in `eb59f4c6da732847a81180b82055cee5eab28c84`: `Staging.committed` reports `offsets`, `Staging.journal_bytes` reads the size, and reconcile refuses any journal longer than its committed offset before comparing; guard `test_an_opening_read_the_checkpoint_has_not_committed_refuses_to_reconcile` fails by assertion on `275f62953e80ea117fc0138ead034ebb701481ce`. | fixed |<br><br>Leads not pursued: Step head `275f62953e80ea117fc0138ead034ebb701481ce` on `8dda00d29fc621f03d4c763358232012191a3898`, 14 files, +1092/-56; security suite waived, Python and Markdown only; fixes range `275f62953e80ea117fc0138ead034ebb701481ce..eb59f4c6da732847a81180b82055cee5eab28c84`, 4 files, +45/-3. Gates before the fix: phylax exit 0, ephoros exit 0 over the 12 changed non-Horos paths, hypomnema exit 0 over the 2 Markdown files; root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 561 tests. After the fix: the same three lints exit 0 over the 3 changed files plus the 2 Markdown files; `run_tests.py` exit 0, 562 tests; root suite exit 1 on `HorosCensusCurrencyTests` alone until `horos.py scan . --census --write` refreshed `.horos/census.json`, then exit 0, 1623 tests under `.githooks/greenlight`; Elenchus over `eb59f4c6da732847a81180b82055cee5eab28c84` with the step's runner `python3 plugins/alexandria/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.hexaemeron/reports/conformance/alexandria-unittest.json`: parent report complete, 562 executed, 1 assertion failure, 0 errors, status `guarded`. `conformance.py opening-reads-resumable` exit 0 before and after the fix and its report equals the committed copy under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. The Mason's `opening-phase-resume` guard: `OpeningPhaseResumeTests` on the parent `8dda00d29fc621f03d4c763358232012191a3898` is an `AttributeError` (the class is absent there) and the overlaid test module errors on import, so as Elenchus classifies it the guard is not an assertion; against a mutant of the step head that discards the committed `epoch-evidence` entries and re-issues every opening read, 5 of its 8 cases fail by assertion (`re_issues_only_the_reads_past_the_checkpoint`, `on_a_code_read`, `after_the_last_opening_read`, `the_plan_does_not_name`, `opens_no_socket`), so it guards the mechanism the runbook names. The clean one-upgrade collection makes 7 opening reads, ids 16 to 22 under virtual shard 5, in the order first-block-header 15331586, implementation-slot 15331586 and 15331626, epoch-boundary-header 15331626 and 15331625, implementation-code at 15331586 and 15331626; each passes `_ask`'s byte ceiling, truncation, page-limit, envelope and JSON-RPC-error checks, `record_error` copies no exception text, and the endpoint-leak case asserts no scheme, host, credential or User-Agent reaches `receipts/errors.jsonl`. Resume into the opening phase re-reads no shard: `_settle_start` makes one by-number re-read of the last shard's boundary, then `_open_interval` replays committed records against the plan and re-checks each through `OpeningPhase.accept`. The demonstration `demo.py build` reproduces `sha256:5d0762677e64ed899cf6466f7dc2cc9a343931c8e0f972bc9a35fafa690f2de0` with `epochs` 1, `interrupted_at` `shard 2 logs`, `resumed_from_shard` 2, before and after the fix, and `verify` exits 0; `examples/usdc-interval-v0/fixtures/` is byte-identical to the parent. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 14 changed files; the only URL is the test's `rpc.example.invalid` decoy. Round 1's first lead stays open: `bind_finality` is untouched by this step and still compares the plan hash against the by-number read only. Round 1's second and third leads (undeclared-class journals, the one-class tree) now sit with step 3's `_gaps` and class-agreement check; the fourth and fifth are unchanged. Five leads seen and left. First, a plan omitting `logs` reads one epoch (3 opening reads) with no `Upgraded(address)` source and no slot read at the interval's end to cross-check the last epoch's implementation, so an upgrade inside such an interval is undetectable by construction; the study at lines 413 to 414 says this run's plan declares `boundary-blocks` and `logs`, and step 3 owes the omission naming. Second, `Reconciler._compare_opening` compares the second provider's first-block header by `hash` alone and does not check its `number`. Third, reconcile does not re-ask the epoch-boundary headers: each is bound to the primary's upgrade log `blockHash`, and `log_identity` includes `blockHash`, so a second provider disagreeing about that block surfaces as a `log-identity` dispute on the shard; accepted as the study's design, not a hole. Fourth, an opening refusal receipt carries the refused block number as `status` and `unresolved` `{start, end}`, a bounded integer but a different meaning from the shard receipts' HTTP or JSON-RPC codes. Fifth, `Staging.record` refuses an opening record under a real shard index and a shard record under the virtual index, but nothing refuses a checkpoint whose `epoch-evidence` offset is non-zero while `next_shard` is below the plan, which only `validate_checkpoint` could name. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 2 -- 2026-09-06T22:21:09Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: finality-tag-drift=reviewed; start-hash-source=not-applicable; code-digest-rebind=not-applicable; opening-phase-resume=reviewed; traces-omission-declared=not-applicable; header-leak=reviewed; endpoint-leak=reviewed; rate-limit-refusal=reviewed; opening-reads-reconciled=reviewed; preserved-bytes-identity=reviewed; silent-truncation=reviewed; live-reads-confined=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed<br><br>Not checked: `scripts/run_checks.py --scope alexandria --scope root`, `scripts/promise_machine.py check`, `protasis.py`, `design_evidence.py`, `demo.py build` and `verify`, and Imprimatur and Brevitas over the changed prose were not rerun this round, the tree they would read being byte-identical to the one round 1 ran them on at `eb59f4c6da732847a81180b82055cee5eab28c84`; `hypomnema.py` ran over the two changed Markdown files, not the whole `plugins/alexandria` walk; an HTTP 403 or 429 on an opening read was not exercised beyond the transport-receipt and JSON-RPC-error paths `_ask` receipts; `build` and `check` do not yet consume the `epoch-evidence` journal, so `start-hash-source`, `code-digest-rebind` and `traces-omission-declared` wait for step 3; no live plan is committed, so `preserved-bytes-identity` was judged on the synthetic `usdc-interval-v0` demonstration only.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Audited tree `eb59f4c6da732847a81180b82055cee5eab28c84` (step head `275f62953e80ea117fc0138ead034ebb701481ce` plus the round-1 fix, 4 files, +45/-3) over the step's full diff `8dda00d29fc621f03d4c763358232012191a3898..eb59f4c6da732847a81180b82055cee5eab28c84`, 14 files, +1135/-57 including the fix; security suite waived, Python and Markdown only; no fixes commit this round. Gates: phylax exit 0 and ephoros exit 0 over the 12 changed non-Horos paths, hypomnema exit 0 over the 2 Markdown files (no H009, so no index root was needed); root suite `python3 -m unittest discover -s tests` exit 0, 1623 tests; `plugins/alexandria/tests/run_tests.py` exit 0, 562 tests; `conformance.py opening-reads-resumable` exit 0 and its report equals the committed copy under `plugins/alexandria/docs/usdc-interval-live/reports/conformance/`; `design-evidence.json` SHA-256 equals the brief's `6af1a6bc04f8d019005d52a7ccc648767c48933de54f9bec82d0a244dfc37a8d`. S2-R1-01 re-verified on the fixed tree: the guard `test_an_opening_read_the_checkpoint_has_not_committed_refuses_to_reconcile` overlaid on `275f62953e80ea117fc0138ead034ebb701481ce` fails by assertion (`AlexandriaError not raised`, 1 test, 1 failure, 0 errors) and passes at `eb59f4c6`; a clean tree with every journal exactly at its committed offset reconciles `agreed` over 30 comparisons; one byte appended to the `epoch-evidence` journal, and separately to the `logs` journal, refuses before any comparison, names the class and writes no `reconciliation.json`; a tree killed on the fourth opening read with the fourth record flushed but not checkpointed refuses, then resumes (`issued` 4, `resumed_from` 3, `total` 7) to journals byte-identical to a clean run and reconciles `agreed`; a tree killed at `shard 2 logs` resumes to byte-identical journals and reconciles `agreed`, so the fix introduced no resume regression. One lead from the fix, left: the refusal names the class but not the two byte counts, and a journal one byte shorter than its committed offset draws the same "holds bytes the checkpoint has not committed" wording (`Staging.committed` does not run `resume`'s shorter-than-offset check); the refusal is correct in both directions and no evidence is destroyed. Round 1's five leads against the fixed tree: first, a plan omitting `logs` still reads one epoch (3 opening reads) with no end-of-interval slot read, open, owed by step 3's `_gaps` omission naming; second, `Reconciler._compare_opening` still compares the first-block header by `hash` alone, a header carrying the right hash under another `number` returns `(True, first-block-hash)`, open, unchanged by the fix; third, epoch-boundary headers not re-asked, accepted as the study's design, unchanged; fourth, an opening refusal receipt still carries the refused block number as `status`, open, unchanged; fifth, `validate_checkpoint` still accepts `next_shard` 2 with a non-zero `epoch-evidence` offset, open, unchanged by the fix, which reads offsets and does not validate them further. No endpoint hostname, credential, private repository name, branch or absolute path was found in the 14 changed files; the only URL is the test's `rpc.example.invalid` decoy. Audit filter `sapheneia:sapheneia` preserved every disposition, count, exit code, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
