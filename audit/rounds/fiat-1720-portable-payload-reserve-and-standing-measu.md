## Step 1, round 1 -- 2026-09-20T15:29:41Z

Audit schema: fiat-audit-round/v2

Covered: measure-drift=not-applicable; over-line-blind=not-applicable; refusal-text=not-applicable; class-overreach=reviewed; link-pullback=reviewed; lazarus-retained=reviewed; dangling-mentions=not-applicable; manifest-truth=not-applicable; figure-prose=reviewed; record-currency=reviewed; resolver-writes=reviewed; workflow-untouched=reviewed; other-suites=reviewed

Not checked: x-ray and solidity-auditor ran their scope check against the step's full diff (41 files, zero `.sol`, no `foundry.toml`/`hardhat.config`) and found no in-scope Solidity material, matching the recorded non-Solidity waiver; fizz does not apply for the same reason. The five not-applicable concerns (measure-drift, over-line-blind, refusal-text, dangling-mentions, manifest-truth) describe `scripts/portable_promise_machine.py`, `PORTABLE.md` and `tests/test_skills_sh_package.py` behaviour this step does not touch, so they have no surface until a later step edits the generator. Imprimatur, Protasis and the full `run_checks.py` root/docs scope are Mason's Exit gate, already green on this clean tree, and were not independently rerun. Live GitHub, merge and PR operations are out of scope for this round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `.hexaemeron/reports/probe-manifest-killed-probe-recovery.json` (schema `protasis-design-report/v1`, candidate `probe-manifest`, unrelated to this resolver's four `CANDIDATES`) sits beside the 32 receipted reports; it is gitignored local scratch (`.hexaemeron/.gitignore` excludes it), untracked, and correctly absent from the packaged `docs/portable-payload-reserve/reports/` (32 files, byte-identical to `.hexaemeron/reports/` apart from this one file). Not pursued: pre-existing scratch outside this step's Files list, not a step defect. Evidence for the covered concerns: `phylax.py docs/portable-payload-reserve/design`, `ephoros.py docs/portable-payload-reserve/design`, `hypomnema.py docs/portable-payload-reserve docs/decisions` and `hypomnema.py --study docs/portable-payload-reserve/study.md --design-evidence docs/portable-payload-reserve/design-evidence.json --repo-root .` all exit 0 clean. `study.md`, `runbook.md` and `design-evidence.json` under `docs/portable-payload-reserve/` are byte-identical to their `.hexaemeron/` originals at `b20a981525c9dd1b37dbc6bdb229d9ca56ab5ccae184b9888fa6445e27d43e04`; the 32 selection reports are byte-identical; `design/resolve.py`, `design/main-measurement.json` and `design/selection-summary.json` match the runbook-stated hashes `9060bf1f6fd8a444315987b6ffcb5d7bf130f0c4855304b5f1d7d8f19d232de1`, `68aa491948d78473289c8864abe70af388d254691ecb5c6aa04d51369d0b8559` and `de1338c4849f195edaddf9dd8648a94f5da1b42918b10d800f9f227c1ee9a99c`. A fresh `resolve.py selection --root . --reports <scratch>` run reproduced every candidate's `package_bytes`/`package_files`/`margin`/`reserve_held` exactly as cited in `study.md` section 4 and the decision draft (example-payload-class 17,790,545/1,404/3,180,975/5,242,880; second-distribution 15,524,133/1,114/5,447,387/5,242,880; lower-reserve 20,381,433/1,550/3,735,815/2,097,152; plugin-budgets 20,381,433/1,550/590,087/5,242,880), and `design-evidence.json`'s recorded gate states (`lower-reserve` fails `reserve-held`, `plugin-budgets` fails `delivery-room`, the other two candidates and gates pass) match those figures with every `report.sha256` verified against the actual report file. `resolve.py`'s subprocess calls are fixed local `git`/`sys.executable` argv with no shell, `write_report` refuses a symlinked path and refuses to overwrite a report with different bytes, and its link-closure walk (`close_links`/`link_targets`) is bounded by the file universe with no network access. `docs/decisions/ADR-040-package-one-dependency-closed-portable-router.md`, `docs/decisions/ADR-090-omit-portable-decorative-portraits.md` and `docs/decisions/drafts/keep-one-complete-lazarus-fixture-in-the-portable-runtime.md`, all named by the new draft decision, exist and are untouched by this step's diff. `distribution/skills-runtime/sync.yml` is untouched. `python3 plugins/horos/skills/horos/scripts/horos.py check .` reports the boundary matches the tree. `tests.test_agent_instruction`, `test_agent_instruction_corpus`, `test_agent_instruction_reconciliation`, `test_portable_duplicate_fixture` and `test_portable_skills` ran 424 tests, 0 failures; `plugins.hexaemeron.tests.test_phylax_model_proxy.ConformanceTests.test_skill_package_marketplace_coverage_and_portable_versions_are_exact` passed.

## Step 2, round 1 -- 2026-09-20T17:48:51Z

Audit schema: fiat-audit-round/v2

Covered: measure-drift=reviewed; over-line-blind=reviewed; refusal-text=reviewed; class-overreach=not-applicable; link-pullback=not-applicable; lazarus-retained=not-applicable; dangling-mentions=not-applicable; manifest-truth=not-applicable; figure-prose=not-applicable; record-currency=not-applicable; resolver-writes=not-applicable; workflow-untouched=reviewed; other-suites=reviewed

Not checked: x-ray and solidity-auditor ran their scope check against the step's diff (5 files, zero `.sol`, no `foundry.toml`/`hardhat.config`) and found no in-scope Solidity material, matching the recorded non-Solidity waiver; fizz does not apply for the same reason. Beyond the scope check, both tools' Python-relevant angles (command injection, path traversal, argument parsing, information disclosure) were applied by hand to the new `measure` surface; see below. The eight not-applicable concerns (class-overreach, link-pullback, lazarus-retained, dangling-mentions, manifest-truth, figure-prose, record-currency, resolver-writes) describe the omission-class predicate, `PORTABLE.md` link-kept exceptions, the Lazarus fixture guard, the package test's `OMISSIONS`/`EXPECTED_OMISSIONS` rows, figure prose in comments/`PORTABLE.md`/decision records, the committed design-time measurement record and `design/resolve.py`; this step's diff touches only `scripts/portable_promise_machine.py`, `tests/test_portable_payload_measurement.py` (new) and `tests/test_skills_sh_package.py`, none of which carries that surface, so they have no surface until a later step. Imprimatur, Protasis and the full `run_checks.py` root/docs scope are Mason's Exit gate and were not independently rerun in full; the promise-machine-adjacent test modules were rerun directly (below). Live GitHub, merge and PR operations are out of scope for this round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `hypomnema.py scripts/portable_promise_machine.py tests/test_portable_payload_measurement.py tests/test_skills_sh_package.py` exits 1 with 3 findings (H006 `ADR-054` citation at `scripts/portable_promise_machine.py:30`; H006 `ADR-054` citation at `tests/test_skills_sh_package.py:76`; H009 stable decision reference `adr/omit-checkpoint-authority-conformance-corpora-from-the-portable-runtime` with no record at `tests/test_skills_sh_package.py:161`), contradicting Mason's Exit-gate report of a passing hypomnema lint; `phylax.py` and `ephoros.py` are clean (exit 0) over the identical three-file scope. All 3 findings are byte-identical to the step's entry tree, confirmed by running the same `hypomnema.py` invocation against a temporary worktree at commit `7d8fccec` (step 1's exit / step 2's entry commit) before any of this step's edits; `tests/test_portable_payload_measurement.py` on its own is hypomnema-clean. Not pursued: debt carried from an earlier step's lineage (issue #1676's ADR-054 and checkpoint-authority references), outside this step's Files list, not a defect this step introduced. Also not pursued as a finding: the study's own metron command for this step, `measure --require-margin 794493`, exits 1 here (package margin 590,087) -- `study.md` section 4's `delivery-room` gate row states `at least 794,493 | ... | 590,087 fail` and section 10 frames the command as exiting 1 "on a tree whose margin is smaller"; the gate is designed to block `step:4`, not this step's `step:3` transition (which needs only `measure-agrees`), so the observed failure is the documented, expected state, not a code defect. Command-injection/path-traversal/argument-parsing/information-disclosure review of the new `measure` surface: `measure_tree` calls `_package_bytes`/`expected_files` exactly as `package`/`check` do, reusing the same fixed-argv `git -C <root> ...` and `[sys.executable, <horos>, "scan", ...]` subprocess calls with no shell and no new subprocess; `--top`/`--require-margin` are parsed by a new `_non_negative_int` argparse type. Adversarial CLI runs: `--top -5`, `--top=-5`, `--require-margin -1` (exit 2, correctly consumed as the option's value, not mis-parsed as a flag); `--top abc`, `--top 3.5`, `--top 0x10` (exit 2, "not an integer"); `--top` missing its value (exit 2, argparse's own "expected one argument"); an unknown action (exit 2); `--top` with `10**36`, with a leading space, a leading `+` and underscore grouping (all exit 0 harmlessly -- `--top` only bounds a list slice); `--require-margin 999999999999999999999999999999` (exit 1, correctly refuses and names both figures); `measure --out <path> --json` (exit 0, confirmed nothing written to `<path>`: the action writes nothing and `--out` is silently unused for `measure`). `--json`'s `largest_default_included` entries are repository-relative POSIX paths only (e.g. `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`), never absolute filesystem paths or user-supplied data; `kept_by_link` is `[]` as the runbook specifies for this step. One cosmetic, non-security observation: piping `measure --json` into a truncating reader (`| head -c N`) surfaces a `BrokenPipeError` traceback on stderr -- standard Python behaviour for any script writing to a closed stdout pipe, discloses nothing beyond the script's own source line numbers, and is not specific to this diff. `python3 plugins/horos/skills/horos/scripts/horos.py check .` reports the boundary matches the tree; `distribution/skills-runtime/sync.yml` is byte-identical to the entry tree. `tests.test_agent_instruction`, `test_agent_instruction_corpus`, `test_agent_instruction_reconciliation`, `test_portable_duplicate_fixture`, `test_portable_skills`, `test_skills_sh_package` and `test_portable_payload_measurement` ran 462 tests, 0 failures; `plugins.hexaemeron.tests.test_phylax_model_proxy.ConformanceTests.test_skill_package_marketplace_coverage_and_portable_versions_are_exact` passed.

## Step 3, round 1 -- 2026-09-20T21:51:59Z

Audit schema: fiat-audit-round/v2

Covered: measure-drift=reviewed; over-line-blind=reviewed; refusal-text=reviewed; class-overreach=reviewed; link-pullback=reviewed; lazarus-retained=reviewed; dangling-mentions=reviewed; manifest-truth=reviewed; figure-prose=not-applicable; record-currency=reviewed; resolver-writes=not-applicable; workflow-untouched=not-applicable; other-suites=reviewed

Not checked: x-ray and solidity-auditor were read in full (digest-verified against `PROMISES.md`'s `hexaemeron-x-ray-preaudit` entry and the resolved overlay) rather than invoked mechanically: both pipelines enumerate only `.sol` files (x-ray's `enumerate.sh`; solidity-auditor's Turn 1 `find` and its `source.md`, built from "ALL in-scope `.sol` files"), so running either against this all-Python, zero-`.sol` diff would scan an empty set and produce no signal, consistent with the directive's recorded `security_suite` waiver. In place of that, the diff's actual attack-surface-shaped logic -- path matching, the regex-based Markdown link scanner, path-traversal/symlink checks on link targets -- got a hand security read using the same adversarial categories those tools' own hacking-agent taxonomy names (access-control, boundary, trust-gap, numerical/gap); see the finding and evidence below. Fizz does not apply (no Solidity, no Foundry/Hardhat project). Live GitHub, merge and PR operations are out of scope for this round. `figure-prose` is not-applicable: the only byte figure this step's `PORTABLE.md` diff carries (`5,242,880`, ADR-090's reserve) is pre-existing and untouched by the diff; the decision draft's simulation figures are explicitly pinned to the starting commit, which is `record-currency`'s domain, reviewed below. `resolver-writes` and `workflow-untouched` are not-applicable: this step's diff (`scripts/portable_promise_machine.py`, `tests/test_skills_sh_package.py`, `tests/test_portable_payload_measurement.py`, `tests/test_portable_duplicate_fixture.py`, `.agents/skills/promise-machine/PORTABLE.md`) does not touch `.hexaemeron/design/resolve.py` or `distribution/skills-runtime/sync.yml`, confirmed absent from `git diff HEAD~2 HEAD --stat` for both paths.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | `scripts/portable_promise_machine.py` (`_link_kept_examples`, `_source_candidates`) | A tracked git path can never sit under a symlinked directory: git records a symlink as one `120000` blob with no tree underneath it, so `_source_candidates`'s existing per-path check (`source.is_symlink() or not source.is_file()`) was never exercised against an intermediate symlinked ancestor -- only ever the leaf. `_link_kept_examples` reads the live filesystem rather than git's tree, so a committed symlink at `plugins/<plugin>/examples` pointing outside the checkout let an ordinary `[text](path)` link from a packaged Markdown document reach a file that check never inspected: on the leaf, `is_symlink()` reports `False` and `is_file()` reports `True`, because only a parent path component is the symlink. Demonstrated end to end in a real git repository built for the probe: `git ls-tree -r HEAD` showed `120000 blob ... plugins/demo/examples` with no separate entry for anything inside it; a genuine link from a packaged Markdown document to a file reached through that symlink was added to `_source_candidates`'s selected set with no refusal (`candidates_containing_leak` non-empty, no `PackageError`), contradicting the runbook's stated promise that a link-kept target "outside the tree refuses generation." Rated medium: exploitation needs a symlinked directory to land inside a merged commit under `plugins/*/examples/`, but a contributor who gets one past review controls the shipped runtime's contents via any file locally reachable through that symlink at package-build time, including files git never tracked. | fixed in `2168bfcc323f5e9937e27cc0ca9c36ecd8548640`: resolves every selected candidate path once against the resolved checkout root (`root.resolve()`) and refuses any whose own resolved path (`source.resolve()`) lands outside it, alongside the existing leaf `is_symlink`/`is_file` checks. No regression: the legitimate cross-plugin file- and directory-shaped link cases (`test_link_targets_that_escape_the_tree_are_never_treated_as_example_links`, `test_directory_link_without_an_already_kept_file_pulls_back_the_smallest_tracked_file`, and a fresh end-to-end probe over a real git repo) keep resolving exactly as before. Guarded by new `tests.test_portable_payload_measurement.LinkKeptSafetyTests.test_intermediate_symlinked_directory_target_refuses_generation`, run by hand against both trees since the mechanical Elenchus check itself returned `inconclusive` (below): overlaying only that test file onto the parent commit `bbf8bc6a` in a detached worktree reproduces `AssertionError: PackageError not raised`; the identical test on the fixed commit passes. `.horos/census.json` was regenerated in the same commit (`HorosCensusCurrencyTests` reddens on any tracked-`.py`-byte-count change; `horos.py check .` still reports "boundary matches the tree"). |

Leads not pursued: the link scan's per-file Markdown read (`_link_kept_examples`) has no absolute byte-size cap -- only the regex's own per-match groups are bounded (`{0,1000}`/`{1,1000}`/`{0,500}`) -- so an extremely large hostile committed Markdown file has no hard ceiling on scan time. Not raised as a finding: independently measured the compiled `MARKDOWN_LINK` pattern against a pure wall of `[` characters at five sizes (125,000/250,000/500,000/1,000,000/2,000,000 chars) and got 0.70s/1.34s/2.79s/5.56s/10.84s -- linear in input size (each doubling roughly doubles time), not the quadratic/exponential blow-up Mason's report says an earlier draft's unbounded `[^\]]*` caused; this satisfies the design's stated bound and Mason's own `test_oversized_or_hostile_markdown_link_scan_completes_quickly` already holds a >1MB hostile fixture (50,000 `[`, 50,000 `(`, 200,000 `[x](y`, 2,000 real links) to a 15s ceiling with comfortable margin. A hand `run_checks.py --base fiat/1720-portable-payload-reserve-and-standing-measu --scope root --scope promise-machine --format json` invocation was started but killed after 20+ minutes of no progress once it became clear it was running concurrently with this round's own `.githooks/greenlight` full-suite invocation in the same worktree, which is exactly the "don't run checks beside a worker" trap the other direction; relied instead on `.githooks/greenlight`'s own `python3 -m unittest discover -s tests` (2,109 tests; one pre-existing-pattern failure, `HorosCensusCurrencyTests`, resolved by regenerating `.horos/census.json` as its own message directs, then a clean rerun that recorded tree `da2dd59c5c2823f53db9ded1539e993cbbd2aa95`, matching the staged/committed tree) plus direct targeted runs, which is a superset of the scoped invocation for this diff. Facts -- 3a (traversal): a link like `../../../../secret.txt` from a nested document is never even recognised as a candidate (`_link_kept_examples` returns an empty set for it) because `posixpath.normpath` cannot produce a result both escaping to a plain `../`-prefixed path and matching `EXAMPLE_ROOT`'s `^plugins/[^/]+/examples/`; an absolute link (`/etc/passwd`) is rejected earlier still by the `link.startswith("/")` guard. A *legitimate* relative traversal that lands back inside a different plugin's `examples/` (e.g. `../../../other/examples/target.json`) is correctly accepted (confirmed via a full `_source_candidates` run over a scaffolded git repo). 3b (symlink): a link-kept leaf that is itself a symlink is refused via the existing `is_symlink()` check (confirmed); the same held for a link naming a symlinked *directory* (misclassified by `_link_kept_examples`'s own directory-detection as neither an ordinary directory nor excluded, but still caught downstream because a symlink-to-directory's `is_symlink()` is `True`); the intermediate-ancestor case above was the actual gap, now S3-R1-01. 3c (ReDoS): current `MARKDOWN_LINK` is exactly `\[[^\]\n]{0,1000}\]\(([^)\s]{1,1000})(?:\s+"[^"\n]{0,500}")?\)` -- read directly from `scripts/portable_promise_machine.py`, matching Mason's stated hardened form -- and confirmed linear-time above; no catastrophic-backtracking shape survives. 3d (false positives): checked all `kept_by_link` entries `measure --json` reports (8 total; `demo.py` via lazarus `EVOLUTION.md` independently verified before this round) -- `aave-v4-demo-v0-agent.json` and `grounded_agent_demo.py` via a genuine link at `plugins/ariadne/skills/ariadne/EVOLUTION.md:20`; `wad-interest-v0/checked-inputs.json` via genuine links at `plugins/homologia/README.md:71` and `EVOLUTION.md:17`; `aave-v4-spoke-v0-release/release.json` via a genuine link at `plugins/lazarus/skills/lazarus/EVOLUTION.md:17`; and three directory-shaped pull-backs (`berean/examples/aave-v4-demo-v0/rebuild.py`, `horos/examples/fixture/lib/dep.py`, `lazarus/examples/multi-provider-anchor-v0/rpc.jsonl`) each traced to a genuine `[text](dir)` link (`plugins/berean/README.md:85`, `plugins/horos/skills/horos/SKILL.md:201`, `plugins/lazarus/README.md:158` and `docs/chain-anchors.md:81`) rather than a fenced-code-block mention -- none is a false positive. Point 4 (class boundary): `measure --json` reports `omission_classes: 10` (was 14) matching the runbook; adversarial `_example_class_matches` probes (`plugins/foo/not-examples/bar.json`, `examples/plugins/foo.json`, `plugins/examples/foo.json`, `plugins/foo/examplesx/bar.json`, `plugins/foo/EXAMPLES/bar.json` case-sensitivity, `plugins/foo/bar/examples/baz.json` one level too deep, and a same-prefix-different-file collision against the retained Lazarus release path) all classified correctly; none reaches outside `plugins/*/examples/`. Other-suites: `tests.test_agent_instruction`/`test_portable_skills` (356 tests), `tests.test_portable_duplicate_fixture`/`test_skills_sh_package`/`test_portable_payload_measurement` (57 tests, includes the new guard), and `plugins.hexaemeron.tests.test_phylax_model_proxy` (161 tests) all ran green post-fix; `phylax.py`, `ephoros.py` and `hypomnema.py` each exit 0 over `scripts tests .agents/skills/promise-machine` / `docs plugins .agents`.

## Step 3, round 2 -- 2026-09-20T22:26:58Z

Audit schema: fiat-audit-round/v2

Covered: measure-drift=reviewed; over-line-blind=reviewed; refusal-text=reviewed; class-overreach=reviewed; link-pullback=reviewed; lazarus-retained=reviewed; dangling-mentions=reviewed; manifest-truth=reviewed; figure-prose=not-applicable; record-currency=reviewed; resolver-writes=not-applicable; workflow-untouched=not-applicable; other-suites=reviewed

Not checked: same waiver and same-agent continuity as round 1 -- x-ray and solidity-auditor stay unread mechanically for the same reason (this diff enumerates zero `.sol` files against pipelines scoped exclusively to Solidity/Foundry/Hardhat projects); fizz does not apply. Live GitHub, merge and PR operations are out of scope. `figure-prose`/`resolver-writes`/`workflow-untouched` stay not-applicable: this round's diff (`scripts/portable_promise_machine.py`, `tests/test_portable_payload_measurement.py`, `.horos/census.json`) touches neither `.hexaemeron/design/resolve.py` nor `distribution/skills-runtime/sync.yml`, and carries no new byte figure in prose.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | low | `scripts/portable_promise_machine.py` (`_link_kept_examples`) | Closing out round 1's "lead not pursued" with a fresh look rather than leaving it open: the link scanner's per-file Markdown read (`(root / relative).read_bytes()`) had no byte ceiling, only the `MARKDOWN_LINK` regex's own match groups (`{0,1000}`/`{1,1000}`/`{0,500}`) were bounded. This kept the scan linear in file size (round 1 measured ~5.4us/byte worst case) but placed no limit on that size itself, contradicting the runbook's own claim that links are "read with one linear pattern over bounded file bytes" and the study risk `link-pullback`'s "bounded input." An arbitrarily large committed Markdown file had no hard stop on the memory or wall-clock time this scan spent on it -- resource-exhaustion-shaped, not an integrity or disclosure defect, and confined to something a merged PR would have to carry conspicuously (megabytes of Markdown), which is why this is rated low rather than medium. | fixed in `9624fcd22e3b55e26f73f8b2b5ce2e59f3c722cd`: adds `MARKDOWN_LINK_SCAN_MAX_BYTES` (8 MiB, the same discipline as Phylax's own `TYPESCRIPT_MAX_BYTES`), reading at most cap+1 bytes and refusing (`PackageError`) rather than reading further. Sized above the largest packaged Markdown file today (272,388 bytes) and above the existing `test_oversized_or_hostile_markdown_link_scan_completes_quickly` fixture (~1.19MB), so neither needed changing. Guarded by two new tests -- `test_oversized_packaged_markdown_refuses_generation` and `test_file_at_exactly_the_cap_is_still_scanned` (an exact-cap-byte file with a real trailing link still scans correctly, proving the boundary is exact) -- confirmed by hand against the round-1 tree (`1cb9a082`): `MARKDOWN_LINK_SCAN_MAX_BYTES` does not exist there, so both new tests error on the unfixed tree (`AttributeError`) and pass on the fix. Elenchus's mechanical check again returned `inconclusive` for the same sandboxed-subprocess reason as round 1 (`report`: 2,038 executed, 39 assertion failures, 584 errors, all in unrelated tests; `detail`: "the runner report records an infrastructure error"). |

Leads not pursued: a fresh, skeptical look at the round-1 fix itself, as asked, rather than assuming it still holds. `resolved_root = root.resolve()` sits once, immediately before the `for relative in ordered:` loop, and `root` is a fixed function parameter never reassigned anywhere in `_source_candidates`'s body -- one computation, not per-iteration, and nothing inside the loop can change what it was computed from. Walked every ancestor directory of this worktree's actual `root` (`/Users/c0rtexzer0/Projects/wildcat-skills/.claude/worktrees/issue-1788-f9ee59/tmp/fiat/fiat-1720-portable-payload-reserve-and-standing-measu` up to `/`) and confirmed none is a symlink on this filesystem, so `root.resolve() == root` here with no macOS case-folding or symlink surprise; `repository_root()` already calls `.resolve()` before any candidate-building code runs, so the `.resolve()` inside `_source_candidates` is a defensive no-op on the real CLI path and only does real work for a caller (a test) that passes an unresolved root. `Path.is_relative_to()` itself is a pure lexical parts-prefix comparison with no filesystem access; since both operands here (`source.resolve()` and `resolved_root`) have already been through `.resolve()` -- which is what actually touches the filesystem, resolving symlinks, `..`, and (on macOS) canonical on-disk casing -- there is no version/platform skew between the two sides being compared, and round 1's own end-to-end probes over real git repos (including one where both the checkout root and the escape target sat under the same macOS `/var/folders/...` symlinked temp-directory mount) already exercised this path successfully. One theoretical, non-actionable note: `is_symlink()`/`is_file()`/`resolve()` are three separate stat calls per candidate, so a TOCTOU window exists in principle between them; this is the same characteristic the pre-existing (pre-round-1) leaf check already had, this is a single-actor build-time packaging run rather than a multi-tenant service processing concurrent hostile input, and nothing in Phylax's boundary-review checklist asks for atomic filesystem checks in that threat model, so it is noted rather than filed. Re-ran the full mechanical and suite evidence after S3-R2-01's fix: `phylax.py`, `ephoros.py` (`scripts tests .agents/skills/promise-machine`), `hypomnema.py` at both the step-scoped (`PORTABLE.md docs/decisions`) and broad (`docs plugins .agents`) invocations, and `imprimatur.py` on `PORTABLE.md` (100.0/100, 0 defects) all exit 0. `tests.test_skills_sh_package`/`test_portable_payload_measurement`/`test_portable_duplicate_fixture` (59 tests), `tests.test_agent_instruction`/`test_portable_skills` (356 tests) and `plugins.hexaemeron.tests.test_phylax_model_proxy` (161 tests) all green. `.horos/census.json` regenerated for the fix's tracked-byte-count change; `horos.py check .` still reports "boundary matches the tree" (one informational "candidate drift" note naming the round-1 synopsis file as evidenced-but-not-yet-in-the-boundary is pre-existing bookkeeping noise, not a check failure -- overall exit 0 both times). `.githooks/greenlight`'s full `python3 -m unittest discover -s tests` recorded tree `e88e7182593ab6bc5401e505cd8f47f7af4ea899`, matching the committed tree, with zero failures. Because this round also carries a finding (fixed same-round, same as round 1), the audit is not yet closeable on a clean round; a round 3 confirming no further gaps is the natural next step, which is for the coordinator to schedule.

## Step 3, round 3 -- 2026-09-20T22:42:05Z

Audit schema: fiat-audit-round/v2

Covered: measure-drift=reviewed; over-line-blind=reviewed; refusal-text=reviewed; class-overreach=reviewed; link-pullback=reviewed; lazarus-retained=reviewed; dangling-mentions=reviewed; manifest-truth=reviewed; figure-prose=not-applicable; record-currency=reviewed; resolver-writes=not-applicable; workflow-untouched=not-applicable; other-suites=reviewed

Not checked: same waiver and same-agent continuity as rounds 1-2. Live GitHub, merge and PR operations are out of scope. `figure-prose`/`resolver-writes`/`workflow-untouched` stay not-applicable: this round makes no code change and touches neither `.hexaemeron/design/resolve.py` nor `distribution/skills-runtime/sync.yml`.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: none new. Full regression, fresh after both fixes (2168bfcc, 9624fcd2): `phylax.py`/`ephoros.py` (`scripts tests .agents/skills/promise-machine`), `hypomnema.py` step-scoped (`PORTABLE.md docs/decisions`) and broad (`docs plugins .agents`), and `imprimatur.py` on `PORTABLE.md` (100.0/100, 0 defects) all exit 0. `python3 -m unittest discover -s tests` (`.githooks/greenlight`'s own root suite) ran 2,111 tests, 0 failures, exit 0 -- no concurrent-worker interference this round, run alone. Deliberately fresh angle away from `_link_kept_examples`/`_source_candidates` (the two functions rounds 1-2 both came from), as asked: (1) OMISSIONS manifest-row consistency -- the examples row's `exceptions` list is built as `["**/*.md", LAZARUS_RETAINED_RELEASE + "**"]` plus the live `kept_by_link` paths, so it derives from the single `LAZARUS_RETAINED_RELEASE` constant rather than a second hardcoded copy in the generator; `expected_files` copies `OMISSIONS` (`[dict(entry) for entry in OMISSIONS]`) and *reassigns* (not mutates) the examples row's `exceptions` key to a freshly built list each call, so repeated in-process calls (as the test suite makes) cannot accumulate duplicate or stale entries across calls. The test file's own `EXAMPLE_CLASS_EXCEPTIONS` is a separately hand-typed literal, but it is asserted as a subset of the *live* manifest built fresh by `setUpModule` (a real `package` subprocess each test run, not a committed copy), so a future drift in `LAZARUS_RETAINED_RELEASE` would fail this assertion loudly rather than pass silently -- checked by construction, not merely inspected. (2) `PORTABLE.md` prose accuracy -- rebuilt a real package (`scripts/portable_promise_machine.py package --out <tmp>`) and checked every factual claim in "Honour the omissions" against it directly: `plugins/lazarus/examples/aave-v4-spoke-v1/` contains only `demo.py` (the six named duplicates genuinely absent), `aave-v4-spoke-v1-release/fixture/` is complete (all eight files including `manifest.json`), and all six Tabularium release directories (`aave-v4-v0`, `aave-v4-v1`, `euler-v1-v0`, `euler-v1-v1`, `euler-v2-v0`, `euler-v2-v1`) contain only `README.md` and `DATA-DICTIONARY.md` -- matching the corrected "Tabularium's v0 and superseding v1 payloads... also require a full checkout" sentence exactly (the pre-step-3 text only said this of v1). A whole-package sweep found zero non-Markdown, non-retained example files absent from `kept_by_link` and zero `kept_by_link` entries missing from the built tree (8 entries, matching rounds 1-2). The `adr/omit-example-payloads-from-the-portable-runtime` reference resolves, confirmed by the passing step-scoped hypomnema exit above (H009 would otherwise catch a dangling reference). (3) `EXPECTED_OMISSIONS`/manifest agreement post-fix -- `tests/test_skills_sh_package.py`'s `setUpModule` always rebuilds the package fresh via subprocess before any assertion runs, so `self.assertEqual({entry["pattern"] for entry in manifest["omissions"]}, EXPECTED_OMISSIONS)` and the `kept_by_link` byte-count cross-check (`row["bytes"] == (RUNTIME / row["path"]).stat().st_size`) were exercised against the current, post-both-fixes tree in this round's own suite run, not against a stale implement-time snapshot. Also gave `tests/test_portable_duplicate_fixture.py`'s renamed `test_only_markdown_link_kept_or_released_files_survive_the_class` a fresh read (flagged in Mason's implement commit as outside the runbook's stated Files list): it correctly asserts `_omitted()` alone -- which has no access to Markdown content -- still classifies `demo.py` as would-be-omitted, and documents that the link-kept override is checked at the built-runtime level by a different test; this is an accurate layering, not a weakened assertion. No further gap found. The audit is ready to close.
