Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-936-report-dead-code-baseline-staleness-instead.md | source_sha256=aea08d63b1945b73a9f7d9980024948ba1a5028c0c9153bc235eae6e5d9ced0f | h2_count=2
## Step 1, round 1 -- 2026-08-30T07:00:29Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: currency-as-validity=reviewed; recompute-invariance=reviewed; publication-commit-identity=reviewed; git-argv=reviewed; path-report-rendering=reviewed; pipefail-masking=reviewed; workflow-silence=reviewed; schema-stability=reviewed; suppression-path=reviewed; coverage-binding-drift=reviewed; receipted-artefact-immutability=reviewed; self-hosting-order=reviewed<br><br>Not checked: the security suite is waived and recorded, so `x-ray`, `solidity-auditor` and `fizz` did not run and this round makes no Solidity claim; the diff is one Python function, its tests, a workflow and prose. Refusals 7 and 8 were not reached from a constructed history. A merge whose record blob disagrees with the commit `git rev-list -1` returns could not be built, because default history simplification follows a parent that is TREESAME for the path, and `read_commit_regular` has already established the blob at the checkout before the discovery runs; both were driven only through the step's own `run_git` substitution, which replaces the discovery and leaves the refusal under test in production code. The workflow was parsed by Ruby's YAML and its summary block was extracted and executed against a synthetic capture on this host; no GitHub runner rendered it, so `$RUNNER_TEMP`, `$GITHUB_STEP_SUMMARY` and the rendered markdown were not observed where they run. Whether this branch is pushed before any other is Fiat's to hold, so this round establishes that the branch carries the changed command and not that CI ran it.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `scripts/dead_code.py:4183` | `baseline --check` reads `.dead-code/suppressions.json` only at the recorded source commit, and dropping the source-to-checkout path comparison leaves nothing reading that file at the checkout. On the synthetic fixture repository, deleting `.dead-code/suppressions.json` and committing an entry for a finding that does not exist each leave the command at exit 0 with `status matched` and the file named on the `currency` line, while the same entry at the source commit still refuses with `.dead-code/suppressions.json entry 0 is unused; finding sha256:0000... is absent`. `command_report` never calls `load_suppressions`, so no other command in the workflow reads it either. Study item 4.11 and runbook item 2.11 both scope the refusal to the source commit, so this is the written contract; what is lost is the transitive cover the removed refusal gave by accident, and the `transition` sentence in `tests/promise_machine_coverage.json`, "with only exact current suppressions accepted", now overstates what the command establishes. Closing it needs a second recompute at the checkout, which the 46-second budget in exit item 9 does not hold. | recorded, not fixed |<br>| S1-R1-02 | low | `.github/workflows/dead-code.yml:97` | The step summary filtered the captured output to `currency` and `status`, so the `published` line naming the commit that wrote the checked-out record reached no reader. Study item 8's second on-call question sends a reader to the summary for how far behind the record is, and study item 8, ADR-054's Consequences and the operator guide's recovery entry all describe that commit as sitting on the `currency` line, where `_baseline_summary` puts it on its own `published` line. The filter now names `published`, `currency` and `status`, and ADR-054 and `docs/promise-machine/dead-code-v1.md` name the line each value sits on. The receipted study keeps its wording, because exit item 10 proves its tracked copy byte-identical. | fixed and guarded in `8b6631be7946c4e0a93b19d51621488d0b8b8532` |<br>| S1-R1-03 | informational | `.github/workflows/dead-code.yml:97` | The captured lines were interpolated into a markdown list item, so a tracked path carrying a backtick, an angle bracket, a vertical bar, an underscore or an asterisk reached the GitHub step summary as markup rather than as the path the command named. Driven on the fixture repository with six such names, each of which reached the currency line unchanged. `validate_repository_path` rejects NUL, control characters, backslashes and absolute paths, so no line break, directive or shell string was reachable and the exposure is a misread path list rather than an injection; the `path-report-rendering` control nonetheless states that paths are printed without markdown interpretation. The summary now reproduces the captured lines verbatim inside a fenced block. | fixed and guarded in `8b6631be7946c4e0a93b19d51621488d0b8b8532` |<br>| S1-R1-04 | informational | `docs/dead-code-baseline-currency/study.md:103` | Study item 4 and runbook item 2 present the eleven refusals "in the order it evaluates" and place `compare_baseline_documents` tenth and `load_suppressions` eleventh. `command_baseline` calls `load_suppressions` at `scripts/dead_code.py:4183` and `compare_baseline_documents` at `:4190`. A forged record published over a source commit whose suppressions name an absent finding refuses on `.dead-code/suppressions.json entry 0 is unused` rather than on the drift it also carries, so the eleventh evaluates before the tenth. The order predates this diff, and refusal 11 is unreachable for any record `baseline --write` produced, because the write ran the same call at the same commit and returned. Not fixed: exit item 10 proves both tracked copies byte-identical to the receipted `.hexaemeron/` sources with `cmp -s`, so the wording cannot be corrected in place. | recorded, not fixable in this run |<br><br>Leads not pursued: all eleven refusals were driven rather than read, against the step branch at `7965e6c3829a8356abe7da0f5eaef796f63513fe`. Refusals 1, 2 as both an absent and a symlinked record, 3, 4, 5 and 11 were exercised by probes on the synthetic fixture repository and each named its own message; 6, 7, 8, 9, 10 and the reported exit-0 case are covered by the step's replacement tests, which pass. `source_commit` reaches `git diff` and `git merge-base` as data from a committed file, and `validate_baseline_document` puts it through `_require_oid` at line 3619 before `require_baseline_publication` runs, so no record can shape a git argument. `_changed_paths` refuses when either diff exceeds `MAX_GIT_OUTPUT_BYTES`, so a repository 16 MiB of paths past its publication commit turns a currency question into a hard refusal; the same ceiling applied to the single diff before the change, so it is not a regression. `_require_oid` demands 40 lowercase hexadecimal characters, so the command refuses in a SHA-256 object-format repository; that constraint is shared with `resolve_commit` and `resolve_tree` and predates this step. The summary step still prints a fixed `- changes: added=0 resolved=0 stale_suppressions=0` while `_baseline_summary` emits a `changes` line of its own; `workflow-silence` names the status line alone, so this sits outside the register and outside this round. The three mapped lints exit 0, and `phylax` and `ephoros` were additionally run over `scripts`, which no mapped lint reads, and are clean there too. `python3 scripts/dead_code.py baseline --check` exits 0 on the fixes commit in 26.6 seconds against the study's 43.6-second before-measurement and 46-second budget, printing `published 41bea02dd9ea4cfe2ba32342f4e08de62b50ce0e` and `currency  stale; 9 path(s) changed after publication`, which is exit item 8 and the acceptance demonstration. The affected-scope closure selected seven checks, of which `dead-code-suite`, `root-suite`, `imprimatur-suite`, `lint-phylax`, `lint-ephoros` and `lint-hypomnema` passed and `hexaemeron-suite` failed; that suite fails on clean `main` at `7e97b5195d5b0e43146b4200f26cd41b89003413` and is held by `#944`, and the run was made before this round's operator-guide amendment, so the closure at the round's tip goes to the controller with the round. `#944` and `#939` are held out of scope by the packet and were not chased.
## Step 1, round 2 -- 2026-08-30T07:19:55Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: currency-as-validity=reviewed; recompute-invariance=reviewed; publication-commit-identity=reviewed; git-argv=reviewed; path-report-rendering=reviewed; pipefail-masking=reviewed; workflow-silence=reviewed; schema-stability=reviewed; suppression-path=reviewed; coverage-binding-drift=reviewed; receipted-artefact-immutability=reviewed; self-hosting-order=reviewed<br><br>Not checked: the security suite stays waived and recorded, so `x-ray`, `solidity-auditor` and `fizz` did not run and this round makes no Solidity claim; the diff is one Python function, its tests, a workflow and prose. No GitHub runner rendered the workflow. The summary block was extracted from `.github/workflows/dead-code.yml` with Ruby's YAML, so it is the parsed step body rather than a re-typed copy, and executed on this host against a synthetic capture; `$RUNNER_TEMP` and `$GITHUB_STEP_SUMMARY` were not observed where they run. The step's exit-status behaviour was driven under `bash -e` with a stand-in command that writes to both streams and exits 2, not on a runner; GitHub's documented default shell for a `run:` step without a `shell:` key is `bash -e {0}`, and that document was not fetched this round, so the shell is an assumption carried from documentation rather than an observation. Fence integrity was established by locating every line in the rendered bytes whose content opens with a fence, not by GitHub's markdown renderer. Refusals 7 and 8 were again driven only through the step's `run_git` substitution; the shallow-clone route to refusal 7 was tested and does not reach it, and no other constructed real history was built. Whether this branch is pushed before any other is Fiat's to hold, so this round establishes that the branch carries the changed command and not that CI ran it.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the four leads the packet set for this round were driven and none produced a finding. The round appends no fixes commit, so Elenchus was not run and the verdict field is `null` rather than one of the four classifications; round 1's `guarded` stands unamended and this round neither re-ran nor relabelled it. `_baseline_currency` at `scripts/dead_code.py:3809` was called directly at 0, 1, 4, 5, 6, 7 and 100 paths; 0 renders `current; no tracked path changed after publication`, 5 renders all five with no suffix, and 6, 7 and 100 render five paths then ` and 1 more`, ` and 2 more` and ` and 95 more`. Rendering the same counts through `require_clean_tree` at `:494` gives a byte-identical `[:5]` slice and the same `and N more` suffix, so the borrowed truncation convention behaves the same way. The production path was driven too: `python3 scripts/dead_code.py baseline --check` exits 0 at `ed37b871` printing `published 41bea02dd9ea4cfe2ba32342f4e08de62b50ce0e` and `currency  stale; 11 path(s) changed after publication: ... and 6 more`, which is exit item 8 and the `>5` branch in production. That run took 23.69 seconds real against the 46-second budget in exit item 9, the study's 43.6-second before-measurement and round 1's 26.6 seconds, so the Metron budget holds with one measurement and no campaign. That measurement is also the `recompute-invariance` evidence the study asks for: the 26 lines of `compare_baseline_documents` at `scripts/dead_code.py:3693` are byte-identical between `7e97b5195d5b0e43146b4200f26cd41b89003413` and `ed37b871`, sha256 prefix `d1e23d429dff8452` at both ends, so the only changed line the diff places near it is the insertion of `_changed_paths` after its last line; the recompute still runs at the recorded source commit rather than at the checkout, and a wall time under the before-measurement rules out the edit having reached into it. A path carrying a newline or a NUL cannot reach the line at all, because `validate_repository_path` at `:514` refuses control characters, and `decode_output` at `:433` turns non-UTF-8 git output into `git stdout output is not UTF-8` rather than a traceback, so both fail closed at exit 2. ADR-054 and `docs/promise-machine/dead-code-v1.md` were read claim by claim against the build. Every ADR-054 statement holds after round 1's amendment, including the `published` line sitting above `currency` in `_baseline_summary` at `:3844`; its Decision paragraph names nine of the eleven refusals and the following paragraph names the other two, so the set is complete across the two. The one imprecision is ADR-054 calling refusal 9 "a publication commit that changed more than its owned record" where `:3804` refuses on `set(published) != BASELINE_PUBLICATION_PATHS`, an inequality rather than a superset test; the difference is unreachable, because the excluded branch needs the record blob identical at source, publication and checkout, which makes the recorded `git_tree` the hash of a tree containing that same record and is a fixed point no writer produces. Not recorded as a finding. The guide's "Recover from refusal" section names neither new refusal message, `no commit reachable from the checkout published .dead-code/baseline.json` nor `.dead-code/baseline.json at the checkout differs from the record published by <commit>`; that index was already partial at `7e97b5195d5b0e43146b4200f26cd41b89003413`, where refusals 2, 4, 5, 6 and 9 had no entry either, both new messages name their own subject, and runbook exit item 6 asked for one edit to that section and it was made, so this is a lead seen and left rather than a finding. Study item 8's second on-call question still says the `currency` line "names the publication commit"; that is the receipted study, already carried by S1-R1-02, and exit item 10 keeps it byte-identical. The workflow was driven as executed rather than read. Under `bash -e` a redirected command exiting 2 ends the step at 2, `cat` and every later line are not reached, and the refusal still reaches the log because `main` at `:4292` writes `dead_code.py: <refusal>` to stderr, which the `>` redirect does not capture; the redirect therefore preserves the exit status in the failure case as well as the success case, and `pipefail` is not load-bearing because no pipeline was introduced. The captured file holds only partial stdout after a refusal, and no reader sees it, because the summary step carries `if: success()` and the failure branch runs instead. Round 1's own fix was re-driven: the extracted block was run against a capture holding the full eleven-line summary with six hostile path names on the `currency` line, including a backtick, a triple backtick, a vertical bar, an underscore and an asterisk. The filter keeps `published`, `currency` and `status` and drops the header, `source`, `tree`, `universe`, `analysers`, `findings`, `changes` and `suppressed`; the rendered output holds exactly two fence-opening lines, the opener and the closer, so a triple backtick inside a path cannot close the block because no path can begin a line. `git rev-list -1 <current> -- .dead-code/baseline.json` was probed against a purpose-built shallow clone: at depth 1 it returns the shallow root rather than nothing, so refusal 7 is not reachable that way, and any depth deep enough to hold `3c67a6e293accc9ea2c00b4231c32a4894d83c80` necessarily holds its child `41bea02dd9ea4cfe2ba32342f4e08de62b50ce0e`, while a shallow clone missing the source refuses first at refusal 6. Refusal 7 is therefore a defensive guard whose only coverage remains the substitution, which is correct fail-closed code rather than a defect. `load_suppressions` has exactly two call sites, `:4136` on the write path and `:4183` at the source commit on the check path, so no suppression became advisory and the gap remains `#962`'s and not this round's. `.dead-code/`, `schemas/`, `docs/dead-code/`, `tests/check-map-v1.json` and ADR-053 are byte-unchanged from the base ref, so `schema-stability` and `receipted-artefact-immutability` hold. All five `dead_code` digests in `tests/promise_machine_coverage.json` recompute against the files they name and all fifteen selectors resolve to a `def` in `tests/test_dead_code.py`, so `coverage-binding-drift` holds; `test_workflow_summary_names_tree_universe_analyser_and_non_gating_state` still exists beside the new `test_workflow_summary_carries_the_publication_commit_as_plain_text`, so exit item 5 holds. The three mapped lints exit 0 and report `clean`, and `phylax` and `ephoros` over `scripts` and `hypomnema` over ADR-054 and the operator guide are clean too. `#944`, `#962`, `#939` and `#950` are held out of scope by the packet and were not chased.
