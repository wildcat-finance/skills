Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-499-grade-router-selection-not-just-resolution.md | source_sha256=650a3be519f9b83f68572b21056fb1ddddf6b3c6e202a8bccdf95b7045318386 | h2_count=10
## Step 1, round 1 -- 2026-08-27T19:37:21Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, since this run ships no Solidity and the `.sol` files in the tree are vendored fizz templates and fixtures it does not touch; how any agent actually routes, which step 3's recorded run owns; whether the corpus is representative of real requests; step 2's hard cases, ambiguity rule and two guard fixtures; step 3's recorded run block, so the `runs` branch of `test_a_recorded_run_block_matches_the_corpus_digest` and the `prompt_template_sha256` binding stay untested against real data; native Windows execution; and the 1,379-test Hexaemeron suite as a step gate, which no step's Exit owes, though Elenchus ran it at 1,379 of 1,379.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | tests/test_router_selection.py | An empty or whitespace-only `deciding_sentence.text` satisfied the occurrence check for every section of every file, because the empty string occurs in all of them, and the field validator required non-empty strings only for `id`, `family`, `request` and `not_established`. Setting every case's and every pair's sentence to `{"path": "AGENTS.md", "section": "", "text": ""}` left all six methods green, so the promise's load-bearing prose binding was voidable entry by entry. | fixed in this commit; `section` and `text` are now required non-empty and an empty quotation fails where it is quoted |<br>| S1-R1-02 | medium | tests/test_router_selection.py | The `pairs` block was never held to a shape and `PAIR_FIELDS` was defined but referenced nowhere, so a pair of arbitrary shape (`{"nonsense": true}`) and a pair whose `deciding_sentence.path` was `../../../etc/passwd` both passed every method; `test_every_deciding_sentence_occurs_in_the_file_it_names` skipped an out-of-set path rather than failing on it. The promise's stated refusal of a quoted path outside the closed set therefore held for cases and not for pairs. No filesystem exposure: the checker performs three reads, at lines 89, 110 and 226 pre-fix, and every one derives from a module constant, never from corpus data. | fixed in this commit; cases and pairs share one validator and an out-of-set path fails by name |<br>| S1-R1-03 | low | tests/test_router_selection.py | `collapsed` joins a section's lines with single spaces, so a quotation may span two adjacent table rows. The string `` `metron` \| \| Decide what explanation or decision record belongs where `` occurs in the collapsed `## Select one runtime contract` section, so a case could quote it and read as evidence that `metron` is selected for hypomnema's request; the splice across the imprimatur and vulgate rows matches the same way. No case in the shipped corpus does this, and all 24 quote a single cell or a single prose clause. | accepted; constraining the span needs a rule for what counts as one sentence, which step 2's ambiguity rule owns, and the cost is now named in the module docstring |<br>| S1-R1-04 | low | .agents/skills/promise-machine/SKILL.md | A whitespace-only source edit can change what the router means while every quotation still matches. Inserting one newline inside a table row stops it being a Markdown table row, and a four-space indent turns the router's selection instruction into a literal code block; both leave the check green. | accepted; the collapse is required by the 14 cases quoting hard-wrapped `AGENTS.md` prose, and the trade is now named in the module docstring |<br>| S1-R1-05 | low | tests/test_router_selection.py | The malformed-corpus guard exercised the real `parse_corpus`, which `load_corpus` calls, so the two cannot drift on the parse path; but its five hostile inputs reached only five of the parser's nine refusal branches. The missing and mistyped `pairs` and `runs` branches were unguarded, while the promise's `Refuses:` line claims a missing or mistyped `cases`, `pairs` or `runs` key. `load_corpus`'s own `OSError` branch stays unexercised. | fixed in this commit for the four parser branches; the `OSError` branch remains unguarded and is recorded as a lead |<br>| S1-R1-06 | low | tests/fixtures/router-selection/cases.json | `RS-24`'s deciding sentence quotes the router's Request cell, which selects the row rather than the skill: the row's selection cell is the phrase `The named upstream Pashov skill`, and the sentence resolving it to `x-ray` is the selection table at `plugins/hexaemeron/AGENTS.md` line 26, which is outside `PROSE_SOURCES`; `AGENTS.md` names no Pashov skill at all. `contested: []` also understates a row that leaves `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor` open, the four of 28 discovered canonical skills with no case. Expecting `x-ray` is defensible and `refuse: ambiguous` would be wrong here, because the router prescribes two-step resolution and the Hexaemeron runtime contract's table resolves audit-readiness to exactly one skill; a request naming none of the three is the ambiguous case. | accepted; the `contested` field is step 2's scope by its Files entry, and the qualification is recorded here rather than changed |<br>| S1-R1-07 | medium | PROMISE_MACHINE.md | The block declares `Evidence classes: checked, recorded`, which is exactly what runbook step 1's Exit requires at its line 81, but the receipted study assigns the run block the evidence class `measured` at its line 554 and names the delivery's verbs as measures and records at line 452. The law defines `measured` as a value observed under a recorded method and environment, which fits a score the promise's `Authorises:` line permits citing; six of the repository's seven `measured` promises pair it with `recorded`, so the answer is in addition to, never instead of. Step 3's Files field cannot touch `PROMISE_MACHINE.md`, so step 1 is the only place the class can be declared. `recomputed` has a parallel unraised case for the digest clause, which the Promise and Evidence lines both call recomputed while the contract document assigns it to `checked`. Nothing gates on the list today: the root-law promise is absent from the records `scripts/promise_machine.py` checks coverage classes against, and the `router_selection` coverage entry carries no `evidence_class` field. | open; the step-1 Exit pins the two-class list, so this needs a receipted runbook amendment rather than a Warden edit, and is escalated to the controller |<br><br>Leads not pursued: the step's Exit line `brevitas.py PROMISE_MACHINE.md docs/promise-machine/router-selection-v1.md docs/decisions/ADR-041-...md` is a recorded deviation and not a finding, confirmed by running it (exit 2, `unrecognized arguments`, since `brevitas.py` takes one optional positional `draft`) and by running each of the three documents separately (exit 0 each), which are every changed document that line intends, the receipted study and runbook copies being Imprimatur-linted rather than Brevitas-budgeted and the 14 generated law copies being byte-identical to the root; four canonical skills have no case, `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor`, so the register's `case-coverage-decay` concern is met for all 24 router rows but not for 24 of 28 canonical skills, which step 2's coverage checks will surface; the corpus exercises neither the `refuse` expectation branch nor `REFUSALS` nor the contested-name branch, all 24 cases being `select` with `contested: []`, so those shipped paths run against no data until step 2; `test_every_bound_capability_selector_exists_in_its_test_file` checks pinned selectors against the file and not the reverse, so a new method omitted from `selectors` is caught only indirectly through the `tests.sha256` re-pin, which is pre-existing repository behaviour and not this step's; step 2 adds two guard fixture paths the checker must open, and parameterising `load_corpus` to reach them would introduce the first caller-supplied path component into a read the step's phylax discipline requires to carry none, so the fixed-constant rule should travel with them; `section_of` ends a section at any line beginning with `#`, which a shell comment inside a fenced block would truncate, unreached by the two quotable files today; the runbook's projected suite counts of 448 at step 2 and 449 at step 3 are consistent with the 444 observed here plus the four and one methods those steps add, so they are a projection and not a discrepancy; and neither the contract document nor the promise records that a router row with no case is undetected today, which is an omission rather than a false statement, since no shipped surface claims the 24-of-24 coverage that holds by enumeration.
## Step 1, round 2 -- 2026-08-27T20:09:27Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 records; how any agent actually routes, and whether a recorded score would repeat, both of which step 3 owns; whether the corpus is representative of real requests; step 2's hard cases, ambiguity rule and two guard fixtures; a run block against real data, since `runs` is still empty; the completeness of a run block and the shape of each failure entry, which step 3's `test_the_recorded_run_block_is_complete_and_names_its_failures` owns by its Tests field; the date and prompt-template digest formats, which no step's plan names; native Windows execution; and the 1,379-test Hexaemeron suite as a step gate, which no step's Exit owes, though Elenchus ran it at 1,379 of 1,379. The corpus, `AGENTS.md` and `.agents/skills/promise-machine/SKILL.md` are byte-unchanged this round, so round 1's prose-binding review stands on the same bytes.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | low | tests/test_router_selection.py | `test_a_recorded_run_block_matches_the_corpus_digest` validated a run block's field set and its `corpus_sha256` and nothing else, so nine incoherent blocks were accepted: 24 passed and 24 failed out of 24 cases, 99 passed out of 24, a failure count of -5, counts recorded as the strings `"24"` and `"0"`, a boolean count, a `failures` list shorter or longer than `failed`, and a `failures` value that was not a list. Only a digest from another corpus was refused. Declaring those counts `measured`, which the law defines as a value observed under a recorded method and environment, makes their internal coherence load-bearing, and a block whose counts cannot all be true observed nothing. The exposure is future: `runs` is empty, the reporter prints `not-run`, and no block exists yet. | fixed in this commit; the study's arithmetic is enforced and guarded, so every case is passed or failed and a run records every failing case id, while completeness and failure-entry shape stay with step 3's method |<br><br>Leads not pursued: round 1's S1-R1-07 is closed by this commit under runbook amendment `d875ed2c`, the promise now naming `checked`, `measured`, `recomputed` and `recorded` and never `proved`, with the Evidence line extended to name the run block that `recorded` already depended on and the contract document reconciled to match; round 1's `brevitas.py` arity deviation is closed by the same amendment, which names the three documents one per line, the form that was run; `PM036` validates only that a declared evidence class is a recognised name, so nothing anywhere requires a declared class ever to be exercised, which is repository-wide and not this step's to fix; four canonical skills still have no case, `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor`, so coverage holds for all 24 router rows but for 24 of 28 canonical skills; the corpus still exercises neither the `refuse` expectation branch nor `REFUSALS` nor the contested-name branch, all 24 cases being `select` with `contested: []`; `test_every_bound_capability_selector_exists_in_its_test_file` still checks pinned selectors against the file and not the reverse, caught only indirectly through the `tests.sha256` re-pin, which is pre-existing repository behaviour; step 2's two guard fixture paths must keep the fixed-constant rule rather than parameterising `load_corpus`; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; `load_corpus`'s `OSError` branch is still unguarded; the runbook's projected counts of 448 at step 2 and 449 at step 3 remain consistent with the 444 observed here, since this round added no method; and neither the contract document nor the promise records that a router row with no case is undetected today, which stays an omission rather than a false statement.
## Step 1, round 3 -- 2026-08-27T20:32:04Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 records; the 1,379-test Hexaemeron suite, not rerun on its own, since this round's Elenchus overlay executed it at 1,379 of 1,379 with no assertion failure, error or skip and the controller permits relying on that; how any agent routes and whether a recorded score would repeat, both step 3's; whether the corpus is representative of real requests; step 2's hard cases, ambiguity rule and two guard fixtures; a run block against real data, since `runs` is still empty; the completeness of a block and the shape of each failure entry, which step 3's `test_the_recorded_run_block_is_complete_and_names_its_failures` owns; the date and prompt-template digest formats, which no step's plan names; and native Windows execution. The corpus, the checker, `AGENTS.md` and `.agents/skills/promise-machine/SKILL.md` are byte-unchanged since round 2, so rounds 1 and 2 reviewed the bytes that still stand.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R3-01 | low | PROMISE_MACHINE.md | Rounds 1 and 2 strengthened the checker and left both `Refuses` declarations behind. The promise's line and the contract document's `## Refuses` section named twelve refusals between them and none of the ten the two fixes added, each confirmed refused by the committed validators: an empty or whitespace-only quotation, a pair of an unknown shape, a pair separating one skill, a pair with an empty id, a run block whose field set is wrong, counts that are not counts, `passed` plus `failed` disagreeing with `cases`, `failures` disagreeing with `failed`, and `failures` that is not a list. A consumer reading either surface could be refused for a reason neither states. The divergence under-promised rather than over-promised, which is the safer direction. | fixed in this commit on both surfaces; no guard is written, because a test asserting that prose names a refusal would match on wording, which is the fragility `S1-R1-03` and `S1-R1-04` already record against |<br><br>Leads not pursued: the new coherence guards were driven against thirteen honest step-3 blocks and rejected none, including 24 of 24 with zero failures, 0 of 24, a subset run over 8 hard cases, a 30-case corpus after step 2 grows it, failure entries as objects or as pairs, and two blocks recorded in one corpus, so they constrain internal coherence alone and never the corpus size or the entry shape; a correction to round 1, which counted "nine refusal branches" where the module has seven `raise CorpusError` sites, six in `parse_corpus` and one in `load_corpus`, the nine being hostile inputs rather than branches, and the committed guard drives all six `parse_corpus` sites; step 3's red-first instruction to observe a block missing `model` go red will show two methods red rather than one, since `run_faults` already refuses that field set; the checker still accepts a duplicate failing case id and a run over zero cases, both of which step 3's own method can close; `load_corpus`'s `OSError` branch is still the one unguarded refusal site; four canonical skills still have no case, `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor`, so coverage holds for all 24 router rows but for 24 of 28 canonical skills; the corpus still exercises neither the `refuse` expectation branch nor `REFUSALS` nor the contested-name branch, all 24 cases being `select` with `contested: []`; step 2's two guard fixture paths must keep the fixed-constant rule rather than parameterising `load_corpus`; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; `PM036` still validates only that a declared evidence class is a recognised name, so nothing requires a declared class ever to be exercised, which is repository-wide; and round 1's `brevitas.py` arity deviation is closed by amendment `d875ed2c`, which names the three documents one per line, so it is no longer an open lead.
## Step 1, round 4 -- 2026-08-27T21:11:43Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 records; the 1,379-test Hexaemeron suite, not rerun on its own, since this round's Elenchus overlay executed it at 1,379 of 1,379 with no assertion failure, error or skip and the controller permits relying on that; how any agent routes and whether a recorded score would repeat, both step 3's; whether the corpus is representative of real requests; step 2's hard cases, ambiguity rule and two guard fixtures; a run block against real data, since `runs` is still empty; the completeness of a block and the shape of each failure entry, which step 3's own method owns; the date and prompt-template digest formats, which no step's plan names; and native Windows execution. The corpus, the checker, `AGENTS.md` and `.agents/skills/promise-machine/SKILL.md` are byte-unchanged since round 2, so rounds 1 and 2 reviewed the bytes that still stand, and the `Promise`, `Evidence`, `Boundary`, `Authorises` and `Recovery` fields were read but not re-derived, this round scoping itself to `Refuses`.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R4-01 | low | PROMISE_MACHINE.md | Round 3 corrected the ten refusals its own fixes had added and compared no further, so five refusals that predate every round stayed unnamed on both surfaces: a corpus declaring an unsupported schema, which is the checker's first gate and its likeliest real refusal; a required case field present but empty, for any of `id`, `family`, `request` and `not_established`; a `contested` that is not a list of canonical names; a quotation that is not the three keys the schema names; and a pair `id` present but empty, the symmetric case for pairs. Both declarations named the case field set, the repeated id and the expectation, and none of these five. All five are original code untouched by rounds 1 to 3, which is why a comparison scoped to the newly added refusals did not reach them. Two faults that looked like gaps are not, and are recorded as covered rather than fixed: an `expect` that is not an object is the named expectation refusal, and an absent named section is the named sentence refusal, since a sentence in a section that is gone is not contained by it. | fixed in this commit on both surfaces; no guard, for the reason `S1-R3-01` gives |<br><br>Leads not pursued: the two-direction check is complete on the fixed tree and clean in both directions, thirty-six distinct faults having been driven against the committed validators, every one refused and every one named on both surfaces, and every clause named before this commit driven and refused too, so neither round 3's fix nor this one over-states; the schema has no field for a case that a grading run could not answer, so `passed` plus `failed` equalling `cases` is forced by the schema rather than by the guard, which is a limit of the run block rather than a defect of this step; round 1 counted "nine refusal branches" where the module has seven `raise CorpusError` sites, six in `parse_corpus` and one in `load_corpus`, the nine being hostile inputs rather than branches; step 3's red-first instruction to observe a block missing `model` go red will show two methods red rather than one, since `run_faults` already refuses that field set; the checker still accepts a duplicate failing case id and a run over zero cases, both closable by step 3's own method; `load_corpus`'s `OSError` branch is still the one unguarded refusal site, though it is named on both surfaces; four canonical skills still have no case, `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor`, so coverage holds for all 24 router rows but for 24 of 28 canonical skills; the corpus still exercises neither the `refuse` expectation branch nor `REFUSALS` nor the contested-name branch, all 24 cases being `select` with `contested: []`; step 2's two guard fixture paths must keep the fixed-constant rule rather than parameterising `load_corpus`; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; `PM036` still validates only that a declared evidence class is a recognised name, so nothing requires a declared class ever to be exercised, which is repository-wide; and round 1's `brevitas.py` arity deviation stays closed by amendment `d875ed2c`.
## Step 1, round 5 -- 2026-08-27T21:24:45Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the two-direction comparison of the `Refuses` surfaces against the validators, not repeated, since round 4 drove all thirty-six distinct faults on these exact bytes and found every one refused and every one named, and this round rests on that result; the waived Pashov Solidity suite, for the reason round 1 records; the 1,379-test Hexaemeron suite, last executed at 1,379 of 1,379 by round 4's Elenchus overlay with no assertion failure, error or skip, and this round makes no fixes commit and so runs no overlay of its own; how any agent routes and whether a recorded score would repeat, both step 3's; whether the corpus is representative of real requests; step 2's hard cases, ambiguity rule and two guard fixtures; a run block against real data, since `runs` is still empty; the completeness of a block and the shape of each failure entry, which step 3's own method owns; the date and prompt-template digest formats, which no step's plan names; and native Windows execution. The corpus, the checker, `AGENTS.md` and `.agents/skills/promise-machine/SKILL.md` are byte-unchanged since round 2, and the `Promise`, `Evidence`, `Evidence classes`, `Boundary`, `Authorises`, `Consequence`, `Recovery` and `Exceptions` fields were read but not re-derived, this round scoping itself to the round-4 fix.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the round-4 fix landed coherently and the double re-pin left nothing behind, the superseded documentation digests `37503ac5`, `a05b9b91`, `5ecd676c` and `b267c884` appearing in no tracked file, the final `13dc0580` in exactly one, and the pinned documentation, tests and fixture digests agreeing across the pin, the committed blob and the worktree; the promise block is byte-identical to the root on all 29 generated surfaces in both the worktree and the commit, carries all nine required fields, and the round-4 record, its commit message and the landed bytes agree that five refusals were named, each of the five verifiable in the committed line; `load_corpus`'s `OSError` is the one refusal site still unguarded, though now named on both surfaces, the module having seven `raise CorpusError` sites rather than the nine round 1 recorded, six of them in `parse_corpus` and driven by the guard, the nine having been hostile inputs rather than branches; the schema has no field for a case a grading run could not answer, so `passed` plus `failed` equalling `cases` is forced by the schema rather than by the guard; step 3's red-first instruction to observe a block missing `model` go red will show two methods red rather than one, since `run_faults` already refuses that field set, and the checker still accepts a duplicate failing case id and a run over zero cases, both closable by step 3's own method; four canonical skills still have no case, `fizz`, `fizz-convert`, `fizz-sync` and `solidity-auditor`, so coverage holds for all 24 router rows but for 24 of 28 canonical skills; the corpus still exercises neither the `refuse` expectation branch nor `REFUSALS` nor the contested-name branch, all 24 cases being `select` with `contested: []`; step 2's two guard fixture paths must keep the fixed-constant rule rather than parameterising `load_corpus`; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; and `PM036` still validates only that a declared evidence class is a recognised name, so nothing requires a declared class ever to be exercised, which is repository-wide and not this step's.
## Step 2, round 1 -- 2026-08-27T22:41:48Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 records; the 1,379-test Hexaemeron suite on its own, since this round's Elenchus overlay executed it and the controller permits relying on that; how any agent routes, and whether a recorded score would repeat, both step 3's; whether the corpus is representative of real requests, and whether a case expects the selection a reader would agree with; a run block against real data, `runs` being empty, and the completeness of a block and the shape of each failure entry, which step 3's own method owns; the date and prompt-template digest formats, which no step's plan names; native Windows execution; and `docs/router-selection/runbook.md`, which is one amendment behind the receipted bytes, is a step 1 file outside step 2's Files, and the controller refreshes in step 3, so it was read as known rather than raised. Three canonical skills have no case, `fizz`, `fizz-convert` and `fizz-sync`; all three sit behind the one unnamed Pashov row, which two cases cover, so coverage holds for 24 of 24 router rows and 25 of 28 canonical skills. `.horos/candidates.json` is unchanged by this commit because the candidate record did not move. The Elenchus overlay reports on the Hexaemeron runner alone, which does not execute this round's guards, since they live in the root suite; that suite was run separately at 448 of 448, exit 0, with no failure, error or skip, and the overlay recorded 1,379 of 1,379 executed with none of the three either.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | tests/promise_machine_coverage.json | The `router_selection` capability entry pinned the corpus, both guard fixtures, the checker and the contract document, and did not pin `tests/emit_router_selection_report.py`, which the promise's `Authorises` line names as the surface reporting is authorised through. The reporter is the file whose bytes decide whether a `request` or a `deciding_sentence` reaches a printed line, which is the `grader-contamination` concern the register names, and nothing recomputed a digest over it. Verified against the current bytes rather than by reading: over all 36 requests, 36 case sentences and 11 pair sentences, the 39-line report carries no whole request, no whole sentence and no four-word fragment of either. That was a fact about today's file with nothing holding it. Ruled that the entry gains a reporter binding rather than that the `Authorises` claim be narrowed, on two grounds. Narrowing removes the only named report surface and leaves step 3's recorded run with nowhere authorised to print. The slot already exists: `run_observation_binding` carries `reporter` as `path` plus `sha256`, and `test_every_bound_capability_digest_matches_the_file_it_names` recomputes every such field by construction, so the binding needs no new test and adds no refusal to the checker. | fixed in this commit; `reporter` pinned at `24c1bbd5` |<br>| S2-R1-02 | low | tests/test_router_selection.py | `row_coverage_faults` matched a named row by the canonical skill in its selection cell, so two rows selecting the same skill were covered by one case and the second row's request predicate was graded by nothing. Driven: adding one row selecting `elenchus` to the real 24 produced no fault before the fix and one after. Mason's third branch does close every selection-cell shape, which was driven too, but shape is not the axis this gap sits on. Nothing else forbids the duplicate: `tests/test_router_selection.py` is the repository's only parser of the router's selection table, and `test_canonical_skill_names_match_parent_directories_and_are_unique` holds the uniqueness of names on disk rather than of selection cells. The register's `case-coverage-decay` mitigation is that a router row with no case fails the suite instead of being noticed later, so this is a miss against the register. The unnamed Pashov row needs no equivalent rule, because its branch matches the row's own request predicate and a second row carrying that phrase already faults, which was also driven. | fixed in this commit; one branch in `row_coverage_faults` and one subTest inside the existing guard, so the method count and the root suite total both stay at 448 |<br>| S2-R1-03 | low | docs/promise-machine/router-selection-v1.md | `## Quoting` said a pair quotes the whole sentence that separates its members. That is false for one of the eleven pairs. `elenchus-metron` quotes `When neither sentence excludes the other, stop at inspection and report which two rows matched and which sentence you read.`, which is the ambiguity rule's disposition rather than a separation. A refutation was attempted and failed: `elenchus` and `metron` occur nowhere in `AGENTS.md`, and in the router each occurs only in its own table row, so no sentence of either quotable file separates them and `RS-33` records a real ambiguity. The pair belongs, because the study names `elenchus` against `metron` as an intra-Hexaemeron near neighbour the boundaries section does not cover, so the document's claim was the thing that was wrong. The checker cannot see this: it holds a quotation to its occurrence in the named section and reads nothing about separation. | fixed in this commit; `## Quoting` now states the gap case and names it, and the documentation digest is re-pinned `2b441df2` to `5778f64c` |<br>| S2-R1-04 | low | PROMISE_MACHINE.md | The promise's `Evidence` line still described step 1's evidence surface. It opened with `The one fixed repository-relative corpus path` and named `the malformed-corpus guard`, while this step added two more fixed fixture paths behind the closed `GUARD_CORPORA` dict and two more guards that read them. Every refusal was named on both surfaces, which is what the standing rule requires; the field that enumerates what the pass rests on was one step behind. | fixed in this commit; `Evidence` now names the malformed-corpus guard and the two degraded-corpus guards read from a closed set of fixture constants |<br><br>Leads not pursued: the two-direction check was re-derived over the current checker rather than inherited, and is clean in both directions on the fixed tree, 61 distinct faults driven against the committed validators with every one refused and every one named on both surfaces, and three negative controls confirming the same functions pass over the corpus on disk; the fixed-constant rule holds on all five filesystem reads in the two modules, which are the corpus constant, the fixed `plugins/` glob, the closed quotable set, the closed `GUARD_CORPORA` dict and nothing in the reporter, a corpus-supplied path being compared rather than joined, driven with `../../etc/passwd`; the ambiguity rule is written for exactly two matching rows while four of the eleven pairs separate three skills and four cases contest three each, but `contested` is documented as the skills whose boundary the request sits near rather than as rows that match, and the runbook's receipted exit specifies the two-row form, so widening the rule to two or more was left to the controller rather than taken as an audit fix; row coverage still has no reverse direction, so a case expecting a skill whose row was later retired would keep grading a claim the router no longer makes, harmless today with all 24 rows live and the mirror of the gap `S2-R1-02` closes; every one of the 47 quotations was checked for the join the module's own docstring warns about and none relies on it, each occurring exactly once and each occupying lines of a single kind, the two router-sourced pair sentences being one table row and one three-line paragraph; `load_corpus`'s `OSError` branch is still the one refusal site with no guard, though it is named on both surfaces, carried from step 1 round 5; the checker still accepts a duplicate failing case id and a run over zero cases, both closable by step 3's own method, carried; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files, carried; and `PM036` still validates only that a declared evidence class is a recognised name, which is repository-wide and not this step's, carried.
## Step 2, round 2 -- 2026-08-27T22:47:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the full 61-site fault inventory, not re-derived, since round 1 drove it against these validators and this round changed none of them, so only the sites round 1's four fixes touched were driven again; the waived Pashov Solidity suite, for the reason round 1 of step 1 records; the 1,379-test Hexaemeron suite, last executed at 1,379 of 1,379 by round 1's Elenchus overlay with no assertion failure, error or skip, and this round makes no fixes commit and so runs no overlay of its own; how any agent routes, and whether a recorded score would repeat, both step 3's; whether the corpus is representative of real requests, and whether a case expects the selection a reader would agree with; a run block against real data, `runs` being empty, and the completeness of a block and the shape of each failure entry, which step 3's own method owns; the date and prompt-template digest formats, which no step's plan names; and native Windows execution. `docs/router-selection/runbook.md` remains one amendment behind the receipted bytes and remains the controller's, refreshed in step 3, so it is read as known rather than raised. Three canonical skills still have no case, `fizz`, `fizz-convert` and `fizz-sync`; all three sit behind the one unnamed Pashov row, which two cases cover, so coverage holds for 24 of 24 router rows and 25 of 28 canonical skills.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: round 1's four fixes were driven rather than reread, and all four hold. The reporter binding recomputes three ways, the worktree file, the committed blob and the entry all reading `24c1bbd5`, and it is enforced rather than merely present: corrupting the recorded digest and separately appending one line to the reporter each failed `test_every_bound_capability_digest_matches_the_file_it_names`, which named the slot as `router_selection.reporter` in both, and the second took the whole root suite to 448 with one failure. The duplicate-selection branch faults a second row selecting an already-covered skill, once and order-independently, faults twice more when the repeated skill is itself uncovered, and leaves the honest corpus at zero faults over all 24 rows; disabling the branch fails the guard at the subTest labelled `two rows selecting the same canonical skill`, so the branch is driven rather than shipped unrun. The corrected pair-quotation claim matches the data and the code: `elenchus-metron` is the only pair quoting `## Preserve the selected promise`, `elenchus` and `metron` occur zero times in `AGENTS.md` and once each in the router at lines 58 and 61, `RS-33` expects `refuse` with reason `ambiguous` over exactly those two names, its `deciding_sentence` is byte-identical to the pair's, and no code path reads separation, only shape and occurrence. The two-direction property holds after those fixes: all ten coverage sites, the nine round 1 inherited and the one it added, were driven, every one refused and every one named on both the promise and the contract document, with the honest corpus still clean. The controller ruled on the ambiguity-rule lead and the router is not edited: the rule stays at two rows, because `contested` is documented as the canonical skills whose boundary the request sits near rather than the rows that matched, so a three-skill pair does not imply three matching rows; the rule is silent on three or more rather than wrong about them; no case in the corpus grades a three-row ambiguity; and inventing prose for an untested case is the move the study already refused when it rejected a deterministic matcher, so a later run that produces a three-row case can widen the rule and requote against it. The orphan-row lead was checked rather than assumed and one assumption was wrong: retiring a row in the router leaves its orphan cases silent for 23 of the 24 rows, but retiring the `lemma` row is caught, and only incidentally, because `guard-missing-row.json` is the corpus's row cases with `lemma` removed, so with that row gone the fixture yields zero faults and the guard fails with a message about the guard rather than about the retired row; no surface claims the converse of row coverage, so nothing is false, and binding a case to a row is a schema change for a later run. Round 1's own fix introduced one new lead: the contract document now names `elenchus-metron` and `RS-33` literally, and nothing binds document prose to corpus ids, so the document's digest catches an edit to the document and would not catch a rename in the corpus. Carried unchanged from step 1: `load_corpus`'s `OSError` branch is the one refusal site with no guard, though it is named on both surfaces; the checker still accepts a duplicate failing case id and a run over zero cases, both closable by step 3's own method; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; and `PM036` still validates only that a declared evidence class is a recognised name, which is repository-wide and not this step's.
## Step 3, round 1 -- 2026-08-27T23:15:51Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 of step 1 records. Whether the recorded score would repeat, on this model or another, and whether 36 of 36 would survive a per-request regrade: no regrade was run, and the checker admits one prompt template across all blocks, so a second run under a corrected prompt is not appendable, which `S3-R1-08` records. Whether the corpus is representative of real requests, and whether a case expects the selection a reader would agree with. The 61-site fault inventory step 2 round 1 drove over `parse_corpus`, `sentence_faults`, `pair_faults`, `quotation_faults`, `row_coverage_faults` and `pair_coverage_faults` was not re-derived, because this round changed none of those validators; the 18 run-block sites were, below. The 1,379-test Hexaemeron suite on its own, since this round's Elenchus overlay executed it. Native Windows execution. Three canonical skills still have no case, `fizz`, `fizz-convert` and `fizz-sync`; all three sit behind the one unnamed Pashov row, which two cases cover, so coverage holds for 24 of 24 router rows and 25 of 28 canonical skills. `.horos/boundary.json` moved with the mirror and `.horos/candidates.json` moved with this log, which its blob entry sizes, so the `scan --write` in this commit writes both; `horos.py check .` reports the committed boundary matching the tree, run after staging so the count is the post-staging one.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | high | tests/emit_router_selection_report.py | The reporter raises on any run block that recorded a failure. Step 3 closed the failure entry to an object of `case` and `selected`; line 94 still joined `failures` as a list of strings, which is what the shape was before this step defined it. Driven against the exact shape `FAILURE_FIELDS` and the receipted Exit both specify, `[{"case": "RS-30", "selected": "pandects"}]`: `TypeError: sequence item 0: expected str instance, dict found`. The perfect score is what hid it. `failures` is `[]`, `",".join([])` is the empty string, `or "none"` supplies a word, and the line reads `failures=none`, so today's report is correct for the one input it can take. The surface it takes out is the only one the promise authorises for citing a run, and the one the amended Exit calls the demo path, and it fails on exactly the run a reader would reach for it to read. No test executed the reporter anywhere in the repository before this round; step 2 round 1 pinned its bytes and added no behaviour. | fixed in this commit; `failures_field` renders each entry as case id and selection, and three subTests inside `test_the_recorded_run_block_is_complete_and_names_its_failures` drive the failing-run render, the run line's identifiers and the no-echo property, so the method count and the root suite total both stay at 449 |<br>| S3-R1-02 | medium | tests/fixtures/router-selection/prompt-template.txt | The response-format example names two real labels and gives the outcome class of both. It reads `Q01: <canonical-name>` and `Q02: REFUSE - <reason>`, and `Q01` and `Q02` are labels in the list below it, not placeholders. Both illustrations match ground truth: `Q01` is `RS-19`, which expects `select ephoros`, and `Q02` is `RS-36`, which expects `refuse uncovered`. The corpus holds two refusals in 36 cases, `RS-33` and `RS-36`, so the instructions handed the graded context one of the two, and refusal is the judgement a router is hardest to get right and the one a perfect score most conceals. 34 of 36 labels were uncued; `Q01`'s canonical name was not given, only that it resolves. Blind is a fair description of 34 labels and not of `Q02`. Not fixable in this round: the template's bytes are pinned by the recorded block's `prompt_template_sha256`, the completeness method holds the two equal, and correcting the example means a regrade, which is the controller's. | accepted; the template is committed, so the cue is retrievable rather than lost, and the Boundary on both surfaces binds the score to this template |<br>| S3-R1-03 | medium | docs/router-selection/study.md | The recorded run batched all 36 requests into one context where the study's re-run recipe specifies one per context. The study, at `e4273b0f`, says "Each request goes to a fresh agent context loaded with the router, `AGENTS.md` and the plugin runtime contracts". The committed template presents all 36 under labels `Q01`-`Q36` to a single context. A batched context can eliminate across the set, can see the answer distribution, and can infer that refusals exist in the set at all, none of which a per-request context has. The template's own instruction, "do not try to spread your answers evenly across the available skills", is the author's acknowledgement that batching creates that pressure, and it does not remove the information. The amended Exit's phrase "each `request` field alone was presented to a fresh agent context" does not distinguish the two readings. Nothing committed states the procedure except the template, which shows the batch plainly, so the record is auditable; committing the template is what made this finding possible. | accepted; the deviation from the study's recipe and the reading it permits are recorded here, and a per-request regrade is the controller's |<br>| S3-R1-04 | low | tests/test_router_selection.py | `run_faults` truncated both digests to `[:12]` in its mismatch message, so a divergence past the twelfth character printed the recorded value and the value on disk as the same string. Driven with two digests agreeing on twelve characters: `recorded against corpus 48ebe808dac6 and the cases on disk digest to 48ebe808dac6`. The message exists to send a reader to a regrade rather than to an edit of the block, and in that form it argues for the edit: it reads as a check that has broken rather than as a corpus that has moved. Mason found it, left it as outside its authorised scope, and printed both digests in full in the new code rather than copying it, so the two run-block validators disagreed on the same question. | fixed in this commit; both digests print whole, driven by one subTest inside `test_a_recorded_run_block_matches_the_corpus_digest`, which fails on the truncated form and names the recorded and on-disk digests separately |<br>| S3-R1-05 | low | PROMISE_MACHINE.md | The study bounds what a graded case shows and neither shipped surface carried the bound. The study's own boundary says a passing run does not establish "that a case the model got right was got right for the boundary the case names". The promise's `Boundary` and the contract document's `## Boundary` both stop at how any agent routes, representativeness, whether a reader would agree with an expectation, and whether a run would repeat. Measured rather than asserted, over the whole graded reading surface of `AGENTS.md`, the router and the fourteen `plugins/*/AGENTS.md`: three of the eleven contested cases carry a token occurring under exactly one plugin's runtime contract, and in all three that plugin is the expected one. `RS-27` carries `replays`, unique to Lazarus. `RS-31` carries `reader`, unique to Sapheneia, and restates the pair's own recorded deciding sentence, "It does not change another skill's facts or gates.", as "change no fact and no gate". `RS-30` carries `hook`, `observe` and `economic`, all three unique to Janus, including the token the case uses as its Pandects distractor, and its core clause repeats the AGENTS.md Janus clause that `RS-07` also repeats without a distractor. Each of the three has an uncontested twin carrying the same signal: `RS-08` for `RS-27`, `RS-12` for `RS-31`, `RS-07` for `RS-30`. Negative result, preserved: the corpus is not uniformly soft. A content-word bag-of-words matcher over the router's 24 row predicates, argmax with ties counted as no answer, scores 23 of 36 overall and 5 of 11 on the contested cases, and is defeated by `RS-26`, `RS-29`, `RS-32`, `RS-34` and `RS-35`; `RS-32` and `RS-35` defeat it by construction, excluding siblings by negation and by carrying the distractor as the head phrase. Not fixable in the corpus: `corpus_digest` covers the whole `cases` array, so editing a case's `not_established` invalidates the recorded block. | fixed in this commit on the two surfaces that dropped it; both now state that a contested case establishes that the corpus declares a boundary, not that the request could only be answered by reading the sentence that separates it, and the contract document names the plugin-unique-token case and says the run does not distinguish the two readings |<br>| S3-R1-06 | low | PROMISE_MACHINE.md | `Authorises` permitted citing a run with three of the four identifiers `Boundary` binds a score to. `Boundary` says a recorded score is evidence about one model, one prompt template, one corpus digest and one date; `Authorises` said "the model, date and corpus digest it names attached", and the reporter's run line printed model, date and counts with no template digest and no corpus digest of its own. Step 3 is the step that made the template digest resolve to retrievable bytes, so leaving the citation surface at three contradicts the step's own argument. Driven: dropping the field from the run line fails the new subTest by name. | fixed in this commit; `Authorises` on both surfaces names the prompt-template digest, and the run line carries `prompt_template_sha256` and `corpus_sha256` so a pasted line stands alone, which is the module's stated design rule |<br>| S3-R1-07 | low | PROMISE_MACHINE.md | The checker refuses a `failures` that is present but is not a list, and neither surface named that refusal. The field set check passes, because `failures` is in `RUN_FIELDS`; the type check is separate and fires on `failures: "none"`. The promise's run-block clause named the field set, the digest and the counts; the contract document's named the same three and expanded the counts. Found by re-deriving the inventory over the current checker rather than inheriting step 2 round 1's count. | fixed in this commit; both surfaces name it in the run-block clause |<br>| S3-R1-08 | low | tests/test_router_selection.py | `run_completeness_faults` holds every recorded block's `prompt_template_sha256` equal to the digest of the one committed template, so the corpus admits one prompt template across all runs. Driven: appending a second block dated 2026-09-01 under a different template digest faults on `runs[1]`. The consequence is that a regrade under a corrected or evolved prompt cannot join the record, only replace it, which is the opposite of what a corpus accumulating graded evidence over time needs, and it is what makes `S3-R1-02` unfixable in this round. The rule itself is right and this round does not weaken it. Closing the consequence is a schema change. | documented in this commit; the contract document states that the corpus carries one prompt template at a time and that a regrade under a different prompt replaces the earlier block rather than joining it. The schema limit is carried |<br>| S3-R1-09 | low | docs/promise-machine/router-selection-v1.md | A failure entry's `selected` accepts canonical skill names only, so two failure classes cannot be recorded truthfully: a case expecting a selection that the run refused, which has no canonical name to give, and a case expecting one refusal reason that the run refused for the other, which `selected` cannot express either. `REFUSE` in that field faults as a selection no `SKILL.md` declares, driven. This run produced neither, so nothing recorded is false. Ruled a carried item rather than something this run must close: the receipted Exit specifies `failures` as "each failing case id with the canonical skill actually selected", and widening `selected` contradicts a receipted exit, which is the controller's to change and not an audit round's, the same disposition step 2 round 2 gave the ambiguity rule. Widening is digest-safe, since `runs` sits outside `corpus_digest`, so the cost is the schema string and both surfaces rather than a regrade. | accepted; the contract document now names both unrecordable classes and says the schema cannot carry them until a schema change, so the gap is stated rather than silent |<br>| S3-R1-10 | low | docs/router-selection/study.md | The study's re-run recipe names `tests/emit_router_selection_report.py --requests`, which prints "one request per line with its case id and nothing else". No committed tool provides the flag and Mason read the requests from the corpus directly. Ruled: the recipe is unfollowable as written and must stay that way. The flag would make the reporter echo request text, which is the contamination the reporter exists to avoid, stated in its own docstring and in the contract document's `## Authorises`, and the reason step 2 round 1 pinned its bytes; building it would put the route the pin was added to close behind a flag. The re-run path that does exist is complete without it: the committed template carries all 36 requests in their graded order, and that order reproduces exactly from `random.Random(499).shuffle` over the corpus index range, verified label by label for all 36, so a later reader can reissue the exact prompt and map every answer back by exact request text without the seed, the flag or the corpus mapping. | accepted; the study is source-bound and the correction is the controller's. The honest record is that the recipe's step 1 is superseded by the committed template, not merely unbuilt |<br><br>Leads not pursued: the two-direction property was re-derived over the run-block half of the current checker rather than inherited, and holds after the two prose fixes. All 18 refusal sites in `run_faults`, `run_completeness_faults` and `prompt_template_digest` were driven, every one refused, every one now named on both the promise and the contract document, with two negative controls, the honest block faulting nowhere and `prompt_template_digest` recomputing to the block's recorded `3f6904e6`. Eleven of the eighteen are Mason's; the twelfth, `failures` type, is `S3-R1-07`. The fixed-constant rule holds over the current module: `load_corpus` and `prompt_template_digest` take no argument and resolve `CORPUS_PATH` and `PROMPT_TEMPLATE_PATH`, `guard_corpus` indexes the closed two-entry `GUARD_CORPORA` dict, and the reporter opens nothing of its own. No surface calls a score proved or a gate, checked across the promise, the contract document, the runbook, the study, the corpus, the checker and the reporter; `proved` occurs in the contract document only in the two sentences that refuse it. The reporter echoes no request and no deciding sentence, now driven rather than read, over all 36 case requests, 36 case sentences and 11 pair sentences against a report rendering two failures. The committed runbook digests to the receipted `9e636cf3` and the committed study to `e4273b0f`, so both source-bound documents match. Carried unchanged and not re-driven: `load_corpus`'s `OSError` branch remains the one refusal site with no guard, though it is named on both surfaces; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; row coverage still has no reverse direction; the contract document names `elenchus-metron` and `RS-33` literally with nothing binding document prose to corpus ids; and `PM036` still validates only that a declared evidence class is a recognised name, which is repository-wide. Two leads this round opened and left: the ambiguity rule reading of `RS-25`, whose request matches both the Alexandria row, on the loan tape, and the Probitas row, on the declared addresses, so a reader applying the two-row rule literally could reach a refusal where the corpus expects `probitas`, which the case's own `not_established` half concedes and which the controller's step 2 round 2 ruling on the two-row rule already covers; and the absence of any committed record of the grading procedure beyond the template itself, no seed, no subagent transcript and no statement of the batch, which `S3-R1-03` records as a finding against the study's recipe rather than as a missing artefact this step owed.
## Step 3, round 2 -- 2026-08-27T23:38:13Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 of step 1 records. Whether the regraded score would repeat, on this model or another; one run per request is one observation per request, and nothing here estimates variance. The 61-site inventory step 2 round 1 drove over the six validators this round did not touch. The 1,379-test Hexaemeron suite on its own, since this round's Elenchus overlay executed it. Native Windows execution. The widened answer field's two refusal forms are driven by the guard and unexercised by the recorded data: `RS-33`'s failure is a canonical name, so no run to date has produced the failure class the seventh amendment was written for. The graded contexts' reasoning was not inspected, only their one-line answers; each returned the required form and none was reformatted. Three canonical skills still have no case, `fizz`, `fizz-convert` and `fizz-sync`; all three sit behind the one unnamed Pashov row, which two cases cover, so coverage holds for 24 of 24 router rows and 25 of 28 canonical skills. `docs/router-selection/runbook.md` now carries the seventh amendment's receipted bytes at `f0f22029`, replacing `9e636cf3`.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R2-01 | low | PROMISE_MACHINE.md | The prompt-template digest names the prompt the grading supplied and not the context the graded agent ran in, and no surface said so. A graded context also receives a harness system prompt, whatever repository instruction files that harness loads, and the definitions of the tools it may call. None of that is under any digest this contract names, and all of it was present in both gradings. The gap is the corpus's own subject one level up: a digest that resolves to retrievable bytes, cited for a claim wider than the bytes it covers. It bounds rather than invalidates, because the part a later reader must reissue to repeat the run is exactly the part the digest does cover. | fixed in this commit; the `Boundary` on both surfaces states that the digest reaches the supplied prompt and stops there, and names what else reaches the graded context |<br>| S3-R2-02 | low | tests/fixtures/router-selection/prompt-template.txt | The template names the worktree path, which contains the string `fiat`, and `fiat` is the canonical name `RS-14` expects. Audited rather than assumed: over the corrected template, `fiat` is the only one of the 27 expected canonical names occurring anywhere in it, no graded label survives, no case id, no request text, no deciding sentence, and the request slot occurs exactly once. The token is a constant of every one of the 36 contexts and was present in the superseded template too, so it cannot explain any per-case result and it does not distinguish the two runs. Removing it means renaming the worktree, which moves the template again and costs a third grading. | accepted; recorded so the constant is visible to anyone reading the score |<br>| S3-R2-03 | low | docs/router-selection/runbook.md | The committed runbook carries one Imprimatur defect, `513:40 medium spatial: 'scaffolding' [no concrete referent]`, at score 99.5 of 100 with exit 0. Checked against the superseded bytes, which scored 99.4 with the same defect at the same site, so it predates the seventh amendment and this commit did not introduce it. The file is the controller's source-bound copy and this round refreshed it byte-for-byte from `.hexaemeron/runbook.md`; correcting the wording would break that identity. | accepted; the exit the amended list requires is zero and is met, and the wording is the controller's to amend |<br><br>Leads not pursued: this log now carries two gradings of the same 36 cases and the first is superseded, not deleted. The first, recorded in step 3's commit `da2c2312` and audited in round 1, scored 36 of 36 under template `3f6904e6`, whose response-format example named the real labels `Q01` and `Q02` and gave the outcome class of both, and it presented all 36 requests to one batched context rather than one per request as the study's recipe and the receipted exit both specify. Round 1 recorded those as `S3-R1-02` and `S3-R1-03` and the controller ruled a regrade. The second, recorded here under template `af3cb4c8`, removed both aids and scored 35 of 36. The block was replaced rather than joined, because the checker holds every recorded block against the one committed template, which is `S3-R1-08`. The regrade met the study's recipe and the receipted exit, and the conditions were checked rather than asserted. Thirty-six separate general-purpose contexts, one request each, none carrying another's question or answer, none given the corpus, the study, a case id, an expected selection, a `contested` list, a deciding sentence or the number of refusals; the presentation order was `random.Random(907)` over the corpus index range, recorded here because per-request isolation makes the order inert rather than load-bearing; the subagents inherited this session's model, which is why the block still records `claude-opus-5[1m]`. The recorded block matches the answers as returned: 35 passed, 1 failed, `RS-33` answering `elenchus`, under the corrected template at `af3cb4c8`, and no answer was adjusted, reformatted or re-asked. `RS-33` was examined rather than accepted as a miss, and the case looks right: the router's rule is to select only when a boundary sentence excludes what the other row claims, step 2 round 1 established that `elenchus` and `metron` occur nowhere in `AGENTS.md` and only in their own rows, so no such sentence exists and the rule disposes of the request by stopping at inspection. The isolated context selected one of the two rows it was meant to find tied, which is a miss against the rule as written, not against a preference. One near-miss is left open: the request names no reproduction and no change, so a reader could hold that neither row matches and that the refusal owed is `uncovered` rather than `ambiguous`; either way the corpus refuses and the answer given fails, and the distinction is now recordable because the answer field admits both refusal forms. Round 1's `S3-R1-01` is load-bearing on this run rather than hypothetical: the report line reads `failures=RS-33:elenchus`, and on the unfixed reporter this exact block raises `TypeError`, so the amended exit's demo path would have failed on the first run that measured anything. All four guards were re-driven and all four fail without their fix: the truncated digest, the join over dicts, the dropped template digest, and the answer field narrowed back to canonical names, which refuses `refuse:ambiguous`. The contested split moved from 11 of 11 to 10 of 11 and the uncontested from 25 of 25 to 25 of 25, so the whole of the drop sits on the one case whose expected answer is a refusal. Nothing regressed: root suite 449 of 449, every command of the amended exit list zero, all seven pinned digests recomputing against their committed blobs, no surface calling a score proved or a gate, and the reporter still echoing no request and no deciding sentence, driven over all 36 requests and 47 sentences against a report rendering a real failure. Carried unchanged: `load_corpus`'s `OSError` branch is the one refusal site with no guard, though it is named on both surfaces; `section_of` still ends a section at any line beginning with `#`; row coverage still has no reverse direction; document prose names `RS-33` literally with nothing binding it to corpus ids, which this round's failure makes slightly more load-bearing; `PM036` still validates only that a declared evidence class is a recognised name; and `S3-R1-08` stands, one prompt template at a time, which this regrade exercised by replacing the earlier block rather than joining it.
## Step 3, round 3 -- 2026-08-27T23:53:39Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: corpus-prose-drift=reviewed; grader-contamination=reviewed; score-overclaim=reviewed; digest-selfreference=reviewed; mirror-drift=reviewed; router-link-shape=reviewed; coverage-binding-gap=reviewed; interpreter-split=reviewed; boundary-currency=reviewed; case-coverage-decay=reviewed; partial-corpus-write=reviewed<br><br>Not checked: the waived Pashov Solidity suite, for the reason round 1 of step 1 records. Both fault inventories, cited rather than re-derived on the controller's instruction: step 2 round 1's 61 sites over the six validators no round since has touched, and round 1 of this step's 18 run-block sites, of which round 2 changed one, the answer field, and that one is driven below. The regrade, not repeated. The 1,379-test Hexaemeron suite, last executed at 1,379 of 1,379 by round 2's overlay; this round makes no fixes commit and runs no overlay of its own. Whether the regraded score would repeat: one run per request is one observation per request and nothing here estimates variance. The graded contexts' reasoning, only their one-line answers. Native Windows execution. Three canonical skills still have no case, `fizz`, `fizz-convert` and `fizz-sync`, all three behind the one unnamed Pashov row that two cases cover, so coverage holds at 24 of 24 router rows and 25 of 28 canonical skills.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the round was a verification pass over head `15d0c626` and it found nothing to fix. All four guards were re-driven and every one fails without its fix: the digest mismatch printing `48ebe808dac6` twice where both digests should print whole; the reporter's join over dicts raising `TypeError: sequence item 0: expected str instance, dict found`; the run line missing `prompt_template_sha256=af3cb4c8`; and the answer field narrowed to canonical names refusing `refuse:ambiguous`. The controller's question about the widened field is settled and the answer is that both forms are carried by the test, not one. Driven separately: with `REFUSAL_ANSWERS` holding only `refuse:ambiguous` the guard fails naming `refuse:uncovered`, and with it holding only `refuse:uncovered` the guard fails naming `refuse:ambiguous`, so each form is independently load-bearing and each failure names the form that broke. The two forms remain unexercised by recorded data, because `RS-33`'s failure is a canonical name and no run to date has produced the class the seventh amendment was written for; that is the honest state and no data was manufactured for it. Round 2's new Boundary clause holds in both directions: it is present on the root promise, on the contract document in its own wording, on all fourteen generated plugin copies and on the portable mirror's root plus its fourteen copies, thirty surfaces in all, with `sync --check` and `portable check` both zero, and no surface anywhere claims the digest covers more than the supplied prompt. The recorded block was replayed against the 36 answers as returned rather than reread: 36 cases, 35 passed, 1 failed, `RS-33` answering `elenchus`, every field matching, and the reporter prints that block without raising. Register evidence at this head: 24 of 24 router rows named with no row-coverage fault, 11 of 11 pairs contested with no pair-coverage fault, no quotation fault across all 47 sentences, the corpus digest and the template digest both recomputing to the values the block names, all seven pinned digests equal across the capability entry, the committed blob and the worktree, `horos check .` matching the tree, the pinned CPython at 3.13.15, and `proved` occurring on these surfaces only in the repository-wide evidence-class table and in the sentence that refuses it. Standing leads, unchanged and none worth another round: `RS-33` names no reproduction and no change, so a reader could hold that neither row matches and the owed refusal is `uncovered` rather than `ambiguous`, which changes nothing about the failure and is now recordable either way; the template names the worktree path, which contains `fiat`, a canonical name `RS-14` expects, constant across all 36 contexts and across both gradings so it explains no per-case result, and removable only by renaming the worktree and grading a third time; `docs/router-selection/runbook.md` carries one Imprimatur defect at `513:40`, `spatial: 'scaffolding'`, scoring 99.5 of 100 at exit zero, in receipted bytes that predate the seventh amendment and belong to the controller; `load_corpus`'s `OSError` branch is still the one refusal site with no guard, though it is named on both surfaces; `section_of` still ends a section at any line beginning with `#`, unreached by the two quotable files; row coverage still has no reverse direction; the contract document names `RS-33` literally with nothing binding document prose to corpus ids, which this step's one failure makes marginally more load-bearing; `PM036` still validates only that a declared evidence class is a recognised name, which is repository-wide; and `S3-R1-08` stands, one prompt template at a time, exercised by the regrade replacing the earlier block rather than joining it. Step 3 is ready to close.
