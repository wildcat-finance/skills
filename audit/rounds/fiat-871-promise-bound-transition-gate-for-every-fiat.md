## Step 1, round 1 -- 2026-09-20T05:56:35Z

Audit schema: fiat-audit-round/v2

Covered: grant-preimage-binding=not-applicable; writer-discovery-drift=not-applicable; unmapped-handler=not-applicable; gate-purity=not-applicable; transaction-crash-window=not-applicable; pending-writer-coexistence=not-applicable; transaction-concurrency=not-applicable; generic-resume-overreach=not-applicable; config-container-bypass=not-applicable; round-nine-alias=reviewed; legacy-loop-preservation=not-applicable; loop-number-continuity=not-applicable; finding-carryover-omission=not-applicable; authority-as-evidence=not-applicable; archive-loop-field=not-applicable; replacement-exit-regression=not-applicable; handover-prose-divergence=not-applicable; handover-hostile-input=not-applicable; gate-integrity-bootstrap=not-applicable; same-account-tamper=reviewed; self-repin=not-applicable; publication-command-smuggling=not-applicable; installer-fail-open=not-applicable; self-hosting-confusion=reviewed; pin-cascade-drift=reviewed

Not checked: Solidity tools were waived; the step ships no Solidity. The Hexaemeron suite was not rerun, because no file under `plugins/hexaemeron/` changed between `aededf66434ed4b4e3994bbaab5f1005fe10b453` and `175b2a482df123fd8e157cbd24e109eea2aa4d1b`. `scripts/run_checks.py` was not rerun. The 12 pending conformance cells have no product to measure. Study claims outside this list were not re-derived: PR #681, #1168, #1169, #1775, #1784 and #1785 content, the audit-synopsis searches, and the earlier run's study. The first root-suite run was red, 34 of 2,056 tests, each on `tree-oversized: plugins/hexaemeron/skills/x-ray exceeds its 512-entry cap (513)`; 13 ignored `__pycache__` directories under `plugins/hexaemeron/skills` caused it, and whether `aededf66` reproduces it was not tested.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: (1) `docs/fiat-transition-gate/design/resolve_gate.py` derives its root as `Path(__file__).resolve().parents[2]`, which is `docs/` from the published path, so `--criterion gate-call-sites --candidate per-handler-gate` exits 1 with a `FileNotFoundError` traceback and every cell that loads `hexctl.py` cannot be reproduced there; it reproduces from `.hexaemeron/design/` (`per-handler-gate` `crash-window-labelled` returned `false`, matching the report). Not pursued: the step requires byte-identical copies, every report's `command` names the `.hexaemeron/design/` path, and no document claims the published path runs. (2) `build_record.py` creates `.hexaemeron/design/reports` under the current directory before its resolver call can fail, so a run outside a Fiat worktree leaves an empty ignored directory; it refuses when the record exists and opens it with mode `x`. (3) The governed-skill walk in `scripts/shoggoth_topology.py` counts ignored bytecode toward its 512-entry cap, so running any tree lint reddens 34 root tests until the caches are removed; the step changed no file that walk reads. Evidence for the zero row: `phylax.py docs/fiat-transition-gate docs/decisions/drafts/gate-fiat-mutations-and-continue-audit-loops.md .horos/boundary.json .horos/census.json` exit 0; `ephoros.py` over the same paths exit 0; `hypomnema.py docs/fiat-transition-gate docs/decisions` exit 0 and its `--study` bridge mode exit 0, all from plugin 1.6.64; Imprimatur and both Protasis modes exit 0; `python3 -m unittest discover -s tests` ran 2,081 tests OK with `PYTHONDONTWRITEBYTECODE=1` after the caches were removed; `cmp` and `diff -r` found the study, runbook, design record, 3 scripts and 32 reports identical to `.hexaemeron/`, at SHA-256 `69540ed8...3dcc`, `7895b0db...9e6b` and `d991b74e...d438`; 32 report digests match the record; `horos check` and both `scan --write` passes left no diff. Verified against `hexctl.py` (`5a69457a...a729`, 31,196 lines): 19 `MUTATING` names, 14 under `gate_recovery_preflight`, `commit()` at 3040 to 3042 with no `fsync`, the five bypass writes at 19325, 19427, 19436, 20515 and 20540 and no sixth, `loop = len(rounds)` at 29100, the ADR-047 allowlist, 8 `MODULE_BINDINGS`, 1,303 commits of distance, and the 29736 range end. The draft record's claims about ADR-028 (lines 9 to 11 and 108 to 122), ADR-047 and issue 871 match their sources, and it carries decisions 1 to 3, three alternatives and the same-account limit.

## Step 2, round 1 -- 2026-09-20T07:44:23Z

Audit schema: fiat-audit-round/v2

Covered: grant-preimage-binding=reviewed; writer-discovery-drift=not-applicable; unmapped-handler=reviewed; gate-purity=reviewed; transaction-crash-window=not-applicable; pending-writer-coexistence=reviewed; transaction-concurrency=not-applicable; generic-resume-overreach=reviewed; config-container-bypass=reviewed; round-nine-alias=reviewed; legacy-loop-preservation=not-applicable; loop-number-continuity=not-applicable; finding-carryover-omission=not-applicable; authority-as-evidence=not-applicable; archive-loop-field=not-applicable; replacement-exit-regression=reviewed; handover-prose-divergence=not-applicable; handover-hostile-input=not-applicable; gate-integrity-bootstrap=not-applicable; same-account-tamper=reviewed; self-repin=not-applicable; publication-command-smuggling=not-applicable; installer-fail-open=not-applicable; self-hosting-confusion=reviewed; pin-cascade-drift=reviewed

Not checked: Solidity tools were waived; the step ships no Solidity. Nothing calls the gate yet, so every dispatcher, writer, lock and crash-window property is Step 3's and was not exercised. Rule directive sets were compared with handler source for `cmd_config`, `cmd_record`, `cmd_halt`, `cmd_resume`, `cmd_reset`, both amendments, `done inoculate`, `done sync-run`, `done resolve-versions`, `done merge-step`, `done integrate`, `cmd_run_exit`, both checkpoint commands, both carryover commands and both replacement commands; `cmd_init`, `cmd_observe`, `cmd_retain_guard`, `cmd_audit_round` and `done` study, runbook, implement, audit, prose and push were taken from the Mason's reading and the `_next_directive` vocabulary only. The per-field value types a handler expects were not compared with the gate. `hexctl verify` was not run.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | With `evidence.recovery` set, the owner was still held to its row's directives. `recover_version_resolution` (`state_hash == after`) and `_finish_no_known_transaction` run after the state write, where `_next_directive` returns `integrate`, or `implement` or `run-exit`; the gate refused `directive-not-authorised` there while `load_state` refuses every other command, so under Step 3's wiring the run could not clear its own pending record. Breaks risk `pending-writer-coexistence`. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | `done sync-run` was granted only at `integrate`. The `resolve-versions` directive names the base sync as its recovery, and `done_sync_run` admits it there through `_integrate_directive(check_resolution=False)`; the gate refused it. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | A fresh `amend study` was granted at `blocked`, where `require_no_amendment_block` refuses it and the directive's recovery names only the runbook repair. It is now granted at `blocked` only under `recovery: amendment`. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | `evaluate` let exceptions other than `Refusal` escape: `TypeError` for a list or dict `covers` and a list `to`, `ValueError` for an integer over 4,300 digits in a command value or `ledger_count`. A `dict` subclass overriding `get` and `str` subclasses were read as plain values. Fail-closed, but outside the one-grant-or-one-refusal contract. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-05 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | `grant["command"]` shared its list values with the caller; appending to `acknowledge_sync_paths` after the grant changed what the grant named. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-06 | medium | plugins/hexaemeron/tests/test_transition_gate.py | Each rule had one refused directive, so a widened rule passed: `cmd_carryover_bind` mutated to grant at `halted` left 116 of 116 green. `AdmittedDirectiveTests` now decides all 29 rules against every directive, 1,421 decisions, from a table written in the test; the same mutant fails 16 subtests. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |
| S2-R1-07 | low | plugins/hexaemeron/tests/test_transition_gate.py | The AST purity test did not close `getattr`, dunder attribute access or writes to module state, so a forbidden attribute or a poisoned table was reachable without tripping it. Called names are now pinned and function stores limited to local names and `self.report`. | fixed in c6376dde3fd5eec3c07fe68b5883814c23e769e6 |

Leads not pursued: (1) Elenchus returned `inconclusive -- the report is incomplete` on the runbook's command, the known result when the 12-worker coordinator cannot spawn under its boundary. By hand, the fixed test file against the pre-fix gate and reference at `6a792008eeabf44b879d5ce83907100fe89b257e` fails 23 by assertion and 0 by error; on the fix it passes 119 of 119. (2) For Step 3: `evidence.recovery` and `evidence.tail_event` are caller claims and an omitted `recovery` grants as if no record were live, so the dispatcher must read both from disk; hexctl's `halted` directive carries no `covers`, so the dispatcher must compute it; `replacement.resume` commits three times in one invocation (`replacement:guarded`, `replacement:promoting`, `replacement:admitted`), which one preimage-bound grant cannot cover. Not pursued: outside this step's files. (3) The gate types no field: `findings` true or -5, an empty required string and any `record` key are granted, and the handler and argparse remain the only check. Not pursued: the exit asks for closed field names, and no study claim covers field types. (4) `promise-unknown` tells the caller to name a Promise declared in `SKILL.md`, yet `fiat-design-evidence`, `fiat-joined-controller-demonstration` and `fiat-decision-assignment-composition` are declared there and refuse as unknown because no rule names them. Wording only. (5) Mason deviations judged within the step: the four extra grant fields, the closed directive projection, the optional evidence claims, and `round` held to 1 through 8, which refuses a legacy run whose `max_rounds` exceeds 8 at round 9 as risk `round-nine-alias` requires. `step` is optional on every directive and `absent` with a `step` passes. `cmd_run_exit` is granted only at `run-exit`, tighter than its handler, which also admits a re-observation at `implement`; `cmd_init` at `absent` cannot tell the `Fiat-Required: 0` route, which writes nothing. (6) Mutation check: `cmd_reset` widened to `implement`, `LOOP_ROUND_MAX` 16, an `audit.` config prefix and a bare `resume` at an exhausted halt failed 3, 5, 7 and 6 tests. (7) Hexaemeron suite on the fix, inside `run_checks.py --scope root`: the three standing cosign reds and no other, 3,107 tests in four shards, `scheduler-error` from the `DemonstrationTests` setUpClass shard; the other 14 selected checks passed, root suite included.

## Step 2, round 2 -- 2026-09-20T08:23:12Z

Audit schema: fiat-audit-round/v2

Covered: grant-preimage-binding=reviewed; writer-discovery-drift=not-applicable; unmapped-handler=reviewed; gate-purity=reviewed; transaction-crash-window=not-applicable; pending-writer-coexistence=reviewed; transaction-concurrency=not-applicable; generic-resume-overreach=reviewed; config-container-bypass=reviewed; round-nine-alias=reviewed; legacy-loop-preservation=not-applicable; loop-number-continuity=not-applicable; finding-carryover-omission=not-applicable; authority-as-evidence=not-applicable; archive-loop-field=not-applicable; replacement-exit-regression=reviewed; handover-prose-divergence=not-applicable; handover-hostile-input=not-applicable; gate-integrity-bootstrap=not-applicable; same-account-tamper=reviewed; self-repin=not-applicable; publication-command-smuggling=not-applicable; installer-fail-open=not-applicable; self-hosting-confusion=reviewed; pin-cascade-drift=reviewed

Not checked: Solidity tools were waived; the step ships no Solidity. Nothing calls the gate yet, so every dispatcher, writer, lock and crash-window property is Step 3's and was not exercised. `done_implement`, `done_prose`, `done_push`, `done_study` and `done_runbook` were read for their phase, halt and amendment-block guards only, not for their evidence checks. `require_global_phase` was not opened; its halt behaviour is assumed from its name and its callers. How Step 3 will project a stale version resolution, where `_next_directive` dies, into a canonical directive is not decided anywhere this round could read. The fuzz KeyError seen on the pre-round-1 gate at `6a792008eeabf44b879d5ce83907100fe89b257e` was not localised; 300,000 seeded calls on the round 1 gate and 10,000 on the fixed gate raised nothing but `Refusal`.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | `done resolve-versions` was granted only at `resolve-versions`. After a base sync the recorded resolution is stale, `active_version_resolution` dies inside `_next_directive`, and `done_resolve_versions` renews it while `_integrate_directive(check_resolution=False)` reads `integrate`; the gate refused that renewal unless a pending record was claimed. The row now admits `integrate`. | fixed in 1a9c30cb0ef37f74e9c86b0eb9eb9bdcbb82664b |
| S2-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/transition_gate.py | `done audit` with `no_further_leads` and a reason was granted at `audit-round` with `round` 1, where `done_audit` refuses with `no audit rounds recorded`. It now refuses with the new code `audit-close-needs-a-round`; the reference lists 24 codes. | fixed in 1a9c30cb0ef37f74e9c86b0eb9eb9bdcbb82664b |
| S2-R2-03 | medium | plugins/hexaemeron/tests/test_transition_gate.py | Round 1's `test_a_live_record_widens_no_other_directive` read the admitted set from `gate.RECOVERY_PATHS`, the table it checked, so the `version-resolution` row widened to `done` and `study` passed 119 of 119. The recovery rows are now decided against every directive, 196 decisions, from a table written in the test; the row widened to `done` fails 2 subtests and `amend runbook` widened to `halted` fails 17. | fixed in 1a9c30cb0ef37f74e9c86b0eb9eb9bdcbb82664b |

Leads not pursued: (1) Elenchus returned `inconclusive -- the report is incomplete` on the runbook's command. By hand, the fixed test file against the round 1 gate and reference at `0de86f3928377780e6a4d69514df8900fe296e16` fails 6 by assertion and 0 by error; on the fix it passes 123 of 123. (2) Round 1 lead 3 decided as a lead for Step 3, not a finding. The step's exit says `an unknown command, field, Promise id, directive shape, recovery path or consequence level refuses` and study section 9 says `unknown commands, fields, Promise ids and directive shapes refuse`; neither asks that a known field of the wrong type refuse, and the join names a `normalised command`. The gate types every field its own decision reads (`path`, `to`, `no_further_leads`, `reason`, `tail_event`, `promise`, `consequence`, `recovery`) and carries the rest as data, so `findings` true or -5 and an empty `reason` on `halt` are granted and left to argparse and the handler. Step 3 must build `command` from the parsed namespace, never from raw argv. (3) A caller claiming `recovery` with no record live gains three grants beyond the rows: `amend study` at `blocked`, and `done inoculate` at `implement` and `run-exit`; the handlers refuse all three with no pending file. A recovery name belonging to another command refuses `recovery-pending`. Closing it needs the dispatcher to read the pending files, which is Step 3's. (4) Tighter than the handler and left so, because `next` sends the operator elsewhere: `cmd_audit_round` refuses at `close-audit` though the handler would record a further round after a clean one; `done implement` refuses at `run-exit`. (5) Round 1 fixes re-attacked without a finding: `bool`, `bytes`, `float`, NaN, tuples, sets and subclasses refuse wherever an `int`, `str`, `list` or `dict` is meant; zero and negative `step`, `round`, `ledger_count` and `consequence` refuse; digests of wrong length, upper case or with a trailing newline refuse. The 1,421-decision table is literal in the test and shares no constant with the gate. The pinned call names are exact, so none is unused. (6) Deliverables: each exit bullet is met. Beyond the Files list, the step carries the runbook re-copy at `docs/fiat-transition-gate/runbook.md`, which the controller ordered and disclosed, and nothing else. (7) `hexctl verify`: `ok: 18 ledger entries, chain intact, state consistent`. `run_checks.py --scope root` on the fix: 14 of 15 selected checks passed, root suite included; `hexaemeron-suite` shows the three standing cosign reds and no other, 3,110 tests in four shards.

## Step 2, round 3 -- 2026-09-20T09:05:13Z

Audit schema: fiat-audit-round/v2

Covered: grant-preimage-binding=reviewed; writer-discovery-drift=not-applicable; unmapped-handler=reviewed; gate-purity=reviewed; transaction-crash-window=not-applicable; pending-writer-coexistence=reviewed; transaction-concurrency=not-applicable; generic-resume-overreach=reviewed; config-container-bypass=reviewed; round-nine-alias=reviewed; legacy-loop-preservation=not-applicable; loop-number-continuity=not-applicable; finding-carryover-omission=not-applicable; authority-as-evidence=not-applicable; archive-loop-field=not-applicable; replacement-exit-regression=reviewed; handover-prose-divergence=not-applicable; handover-hostile-input=not-applicable; gate-integrity-bootstrap=not-applicable; same-account-tamper=reviewed; self-repin=not-applicable; publication-command-smuggling=not-applicable; installer-fail-open=not-applicable; self-hosting-confusion=reviewed; pin-cascade-drift=reviewed

Not checked: Solidity tools were waived; the step ships no Solidity. Nothing calls the gate yet, so every dispatcher, writer, lock and crash-window property is Step 3's and was not exercised. A new Warden ran this round and did not rerun rounds 1 and 2. Rule fields were compared with each handler's `args` reads by an AST walk to call depth 3 over all 29 rows; deeper reads and reads through `vars(args)` were not followed. Handler bodies opened: `cmd_record`, `cmd_config`, `cmd_halt`, `cmd_resume`, `cmd_reset`, `done_audit`, `_checkpoint_boundary`, both carryover and both replacement commands; the rest rests on rounds 1 and 2. That an amendment at `audit-verdict` leaves round state alone rests on a search of `hexctl.py` lines 20635 to 21100 for `rounds`, which matched nothing. `run_checks.py --scope hexaemeron` was not run; the root scope selected the Hexaemeron suite. The design record was checked by digest (`d991b74e649768433f1e50a403702ea9e3c0e276a6d43e97329f5d1dcc32d438`) and its grant sentence in the study; its 32 reports were not reopened.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | plugins/hexaemeron/tests/test_transition_gate.py | No test held a rule's admitted or required command fields, so the exit's closed field table was unguarded. Three mutants passed 123 of 123: `findings` no longer required on `cmd_audit_round`, `key` and `value` no longer required on `cmd_record`, and `max_rounds` admitted as a field of `cmd_audit_round`. `CommandFieldTests` now decides all 29 rules against every field name and six hostile ones, each field absent and null, and `tail_event` on every rule, from tables written in the test; the mutants fail 2, 4 and 1 tests. | fixed in ee89158cff485de1b489b19fdfa04e53162de240 |
| S2-R3-02 | low | plugins/hexaemeron/tests/test_transition_gate.py | The accepted checkpoint tail events were tested against four other values only; `config-set` added to `CHECKPOINT_TAIL_EVENTS` passed 123 of 123. Twenty ledger event names now refuse at both checkpoint rules, and the mutant fails 2 tests. | fixed in ee89158cff485de1b489b19fdfa04e53162de240 |

Leads not pursued: (1) Elenchus returned `inconclusive -- the report is incomplete` on the runbook's command: 12 workers, each `launch failed: [Errno 1] Operation not permitted`. The fix is test-only and the gate is unchanged, so the parent counterfactual cannot fail by construction: the fixed test file passes 128 of 128 on both trees. The guard evidence is by mutation in an ignored scratch copy: 10 mutants against the round 2 test file, 4 passed; 13 against the fixed file, 0 passed (field added to `cmd_audit_round` and to `cmd_halt`, three `required` sets dropped, `cmd_reset` Promise swapped, tail events widened, `tail_event` admitted everywhere, an `audit.l` config prefix, `ledger_count` unbound, ledger tail unchecked, the `audit-verdict` tail condition off, `promise-mismatch` off). (2) `evaluate` attacked at the exhausted loop with every rule, all three recovery claims and both tail events. At `halted` covering `audit-verdict` only `config set` on the allowlist, `halt`, `observe`, `record`, `reset` and `resume` with `to` `audit-verdict` are granted; at `audit-verdict` those without `resume` and `reset`, plus both amendments, both carryover commands, both checkpoint commands and `done audit` with the waiver. `audit.max_rounds`, `audit`, a bare `resume`, round 9, `start-audit-loop` and `cmd_audit_round` refuse. `git.`, `git..` and `git.` followed by NUL are granted, as `config_path_is_mutable` admits them; the handler then dies on `config path not found`. No grant was found that the handler refuses beyond those rounds 1 and 2 listed. (3) Check order is rule lookup, directive, preimage, command, evidence, admission, the rule's check, size, as the reference states; every structural test precedes the first field read, no code depends on set or dict iteration, and two key orders of one directive give one canonical grant. (4) The grant binds state digest, ledger tail and count, directive, handler, subcommand and command in sorted canonical JSON. It does not carry `evidence`, so a grant made under a `recovery` claim is byte-identical to one made without, and the pending files sit outside the state digest. For Step 3: the writer cannot tell from the grant which was claimed, so the claim must be read and the write made under one lock. A `sync-run` with 600 paths of 100 bytes is granted at 62,269 bytes; the budget test lands on 65,536 exactly. (5) Rule table against `hexctl.py` and `SKILL.md`: 19 `MUTATING` names, each with a rule and no other handler with one; 12 Promises, each consequence equal to its declaration; `fiat-decision-assignment-composition`, `fiat-design-evidence` and `fiat-joined-controller-demonstration` declared and unused; `start-audit-loop` absent from both and refused `command-unknown`. The AST walk found no field a handler reads that its row lacks, and none a row admits that its handler does not read. (6) Reference: its 24 codes equal `REFUSALS` in both directions, both schemas are given field by field, and the same-account section says tamper evidence and deterministic refusal, not privilege isolation. (7) The seeded fuzz test makes 10,000 calls in 0.03 s, but 6,818 stop at `directive-shape-unknown`, 1,266 at `command-unknown`, 4 are granted and a rule's own check refuses twice; it holds the no-other-exception contract at the outer checks and adds little beyond them. The module ran 123 tests in 0.31 s before the fix and 128 in 0.33 s after, 0.24 s of it parsing `hexctl.py`. (8) Scope: 15 files differ from `98af61c3955033577339ca8ff58d886e7f07a776`, all in the step's Files list, the controller-ordered runbook copy, or this log and its synopsis; `hexctl.py` is unchanged; `plugin_release.py --base 98af61c3955033577339ca8ff58d886e7f07a776 --head HEAD` exits 0 at one rise, 1.6.64 to 1.6.65. (9) On the fix, `run_checks.py --scope root` reports `red`, 14 of 15 checks passed, root suite 2,081 tests OK. `hexaemeron-suite` shows the three standing cosign reds and `test_checkpoint_signing_formats.CheckpointOpenPgpSigningTests.test_export_inspect_restore_without_producer_keys`, which passed alone in 8.9 s; it resembles the load failure filed as skills#1781, and that issue was not opened this round; shards ran 779, 779, 778, 778 and 774 tests and one returned no result. Phylax and Ephoros over the gate and its test, Hypomnema over `plugins/hexaemeron/skills/fiat/references docs/fiat-transition-gate docs/decisions` and Imprimatur over the reference exit 0 from plugin 1.6.64. `hexctl verify`: `ok: 19 ledger entries, chain intact, state consistent`.

## Step 2, round 4 -- 2026-09-20T10:03:15Z

Audit schema: fiat-audit-round/v2

Covered: grant-preimage-binding=reviewed; writer-discovery-drift=not-applicable; unmapped-handler=reviewed; gate-purity=reviewed; transaction-crash-window=not-applicable; pending-writer-coexistence=reviewed; transaction-concurrency=not-applicable; generic-resume-overreach=reviewed; config-container-bypass=reviewed; round-nine-alias=reviewed; legacy-loop-preservation=not-applicable; loop-number-continuity=not-applicable; finding-carryover-omission=not-applicable; authority-as-evidence=not-applicable; archive-loop-field=not-applicable; replacement-exit-regression=reviewed; handover-prose-divergence=not-applicable; handover-hostile-input=not-applicable; gate-integrity-bootstrap=not-applicable; same-account-tamper=reviewed; self-repin=not-applicable; publication-command-smuggling=not-applicable; installer-fail-open=not-applicable; self-hosting-confusion=reviewed; pin-cascade-drift=reviewed

Not checked: Solidity tools were waived; the step ships no Solidity. Nothing calls the gate yet, so every dispatcher, writer, lock and crash-window property is Step 3's and was not exercised. A new Warden ran this round and did not rerun rounds 1 to 3. The mutation run is first-order only: one change per mutant, none in pairs, none to a `REFUSALS` key, a `Rule` default or the `_done` helper, and a mutant counts as killed whichever test failed. Rule Promises were compared with the Promise, Boundary and Authorises lines of the 15 Promises in `SKILL.md`; of the handlers only `cmd_record` was reopened, and what each other handler mutates rests on rounds 1 to 3. `run_checks.py --scope hexaemeron` was not run; the root scope selected the Hexaemeron suite. The design record was checked by digest (`d991b74e649768433f1e50a403702ea9e3c0e276a6d43e97329f5d1dcc32d438`); its 32 reports were not reopened.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | low | plugins/hexaemeron/tests/test_transition_gate.py | No test held a rule's evidence fields beyond `tail_event` on every rule and `authority` on `cmd_halt`. `authority` admitted as evidence passed 128 of 128 on each of the other 28 rules, `cmd_resume` and `cmd_config` `set` among them. Every rule is now decided against seven hostile evidence names and `tail_event`, 232 decisions; the 28 mutants fail. | fixed in 44ba94af5cd25e37ebf384012357a0b9f501d3ff |
| S2-R4-02 | low | plugins/hexaemeron/tests/test_transition_gate.py | The only assertion on a recovery line compared it with `gate.REFUSALS`, the table it came from, so 22 of 24 lines replaced by `x` passed 128 of 128, and a line could go stale against the constant it names. Each line must now be a distinct string of at least five words, and the lines naming the round limit, the grant budget, the consequence range, the ADR-047 paths, the typed exit, `hexctl next` and `hexctl verify` are held to those values; the 22 mutants fail. | fixed in 44ba94af5cd25e37ebf384012357a0b9f501d3ff |
| S2-R4-03 | low | plugins/hexaemeron/tests/test_transition_gate.py | `canonical` was measured only against itself. With `sort_keys` off, or with default separators, 128 of 128 passed, though its docstring says it serialises as the controller fingerprints state (`hexctl.py` line 1780) and the 65,536-byte budget is counted in it. It is now held to a literal and to one form for two key orders of one grant; both mutants fail. | fixed in 44ba94af5cd25e37ebf384012357a0b9f501d3ff |
| S2-R4-04 | low | plugins/hexaemeron/tests/test_transition_gate.py | Eight boundary mutants passed 128 of 128: `ledger_count >= 1` as `> 1` and as `>= 2`, which refuses every command on a run holding only its `init` entry; the lower integer bound as `<`; `type(reason) is str` dropped or true in the audit close, which grants a waiver whose reason is `True`, 7 or a list; `type(tail_event) is not str` dropped or false, which lets a list escape as `TypeError` and grants a `str` subclass; and `<=` as `<` on the directive field set. Each value now has a case; the eight fail. | fixed in 44ba94af5cd25e37ebf384012357a0b9f501d3ff |
| S2-R4-05 | low | plugins/hexaemeron/skills/fiat/references/transition-gate.md | The reference said an integer outside the signed 64-bit range refuses. The gate's bound is symmetric, -(2^63 - 1) through 2^63 - 1, so -2^63 refuses although that range holds it, and the test pins the refusal. The reference now states the bound the code enforces. | fixed in 44ba94af5cd25e37ebf384012357a0b9f501d3ff |

Leads not pursued: (1) Elenchus returned `inconclusive -- the report is incomplete` on the runbook's command: 12 workers, none returned a result. The fix changes the test and one reference sentence and leaves the gate alone, so the parent counterfactual cannot fail by construction: the fixed test file passes 134 of 134 on the gate and reference at `7385311f96bc9c368b38ea9a5595c2ad3056063d`. The guard evidence is a generated mutation run in an ignored scratch copy, one mutant per run of the module: 3,507 generated; against the round 3 test file 3,428 killed and 79 survived; against the fixed file 3,488 killed and 19 survived; 0 timed out and 0 failed to import. By class: 2,649 on rule rows (each foreign directive added and each member dropped, each field added from 53 names and dropped, each required field dropped and added, `tail_event` and `authority` as evidence, each of the 11 other Promises, the transition, each check); 196 on the vocabularies, Promise consequences and recovery rows; 162 on recovery lines, refusal codes swapped, grant and refusal field values, the digest pattern and `canonical`; 500 on comparisons, boolean operators, conditions, integer constants and predicates. (2) The 19 survivors are equivalent: 16 change a `str | None` annotation, never evaluated under `from __future__ import annotations`; 3 remove or shift `ledger_count < 0` at line 477, which the two returns below it already decide (`== 0` when absent, `>= 1` otherwise). (3) Two kills rest on one pin each. 244 of the 319 Promise swaps fail only the reference-row comparison in `test_reference_states_both_schemas_every_code_and_the_limit`; every other Promise assertion reads `rule.promise` from the gate. The digest class widened to `[0-9a-z]` fails only the source pin on the pattern text; no case refuses a digest carrying `g` to `z`. Not pursued: both are killed, and the reference table is written out independently of the gate. (4) Promise against handler, no finding. Each row's Promise is one whose Authorises line covers the receipt the handler writes, and each consequence equals its declaration. Two rows could name a second Promise: `done sync-run` with `decision_assignments` is also the recorded sync of `fiat-decision-assignment-composition`, and `done study` and `done runbook` on a design-evidence run also establish `fiat-design-evidence`; all are consequence 2, and a row carries one Promise by design. `done sync-run` and `done merge-step` publish nothing, so consequence 3 stays with `done integrate` alone. (5) Reference against code: beyond S2-R4-05 every statement read held, including the check order, the 24 codes, the three recovery rows and the four row checks. The `Directives` column of its rule table is compared by no test, which reads the first four columns; read by hand, its 29 rows match `RULES`. The argument table and the `command-value-malformed` row still say `64-bit integer`; the paragraph below the argument table carries the exact bound. (6) Scope: `hexctl.py` equals `98af61c3955033577339ca8ff58d886e7f07a776`; `plugin_release.py --base 98af61c3955033577339ca8ff58d886e7f07a776 --head HEAD` exits 0 at one rise, 1.6.64 to 1.6.65; the brief equals the `next` envelope's at state `905a2ecc27e17eda369b8003cc73c5b923dc503a442350b3cddfe083e2ff36c4`, and the study, runbook and design record match their brief digests. (7) On the fix, `run_checks.py --scope root` reports `red`, 14 of 15 checks passed, root suite 2,081 tests OK under greenlight. `hexaemeron-suite` shows the three standing cosign reds and `test_checkpoint_signing_formats.CheckpointOpenPgpSigningTests.test_export_inspect_restore_without_producer_keys`, which passed alone in 7.9 s and resembles skills#1781; shards ran 780, 780, 780, 779 and 775 tests. Phylax and Ephoros over the gate and its test, Hypomnema over `plugins/hexaemeron/skills/fiat/references docs/fiat-transition-gate docs/decisions` and Imprimatur over the reference exit 0 from plugin 1.6.64. `hexctl verify`: `ok: 20 ledger entries, chain intact, state consistent`.
