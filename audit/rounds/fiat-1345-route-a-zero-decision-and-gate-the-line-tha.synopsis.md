Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md | source_sha256=e923644f2dee58f26aa09e89aa5d69ee97825e33ba72761163e4098936002b9c | h2_count=14
## Step 1, round 1 -- 2026-09-06T23:07:26Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=not-applicable; partial-write-of-the-receipt=not-applicable; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the skills#1337 edit count and logins were not re-queried and are carried from the receipted study at .hexaemeron/study.md:132 and :160; the Solidity suite is waived by the brief, since the step produces no Solidity and no Foundry or Hardhat project; phylax and ephoros read neither Markdown nor JSON, so their exit 0 covers no byte this step added; hexctl init behaviour is unchanged and untested this round; the study's and runbook's content beyond the claims this step's Exit binds was receipted in their own phases and not re-audited here.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:62-67 | Study item 12 gives decision 3, that the gate reads no editor identity, the home "the same record's Consequences, alongside the measured fact that all four edits on #1337 carry one login". The record put it in Decision, and Consequences carried neither the decision nor the measurement. The paragraph moved unchanged: the file's sorted-word SHA-256 is e1a50bfa4088a31a79865afd3a9d3302dbebcf6667344c2fa1397b43fb49dfc6 before and after. | fixed in 9e314efff63f17b0958acadef488b27cf192cef4 |<br>| S1-R1-02 | medium | docs/route-a-zero-decision-study.md:1 | The shipped study carries no design-bridge fence, so hypomnema 5.8.0 study mode reports "H008 study has no design bridge block" and exits 1 against --design-evidence .hexaemeron/design-evidence.json. That fence would bind the selected candidate decision-age-window to docs/decisions/drafts/route-a-filed-zero-as-an-answer.md, which is the join this step exists to make. The repair is the controller's: the fence has to enter .hexaemeron/study.md through hexctl amend, because the Exit pins the docs copy to the receipted study with only the five link changes. The runbook Tests field names the ordinary walk alone, and that exits 0. | open |<br><br>Leads not pursued: Elenchus reports the fix unguarded because the commit changed no test files, which is expected for a prose-only repair and is recorded as that skill requires. Study item 12 gives decision 2 the home "its own section" and the record gives it a bold-led paragraph inside Decision rather than a sixth heading; not raised, because a sixth heading breaks the five-section shape every record under docs/decisions/ uses, and the rejected alternative and its reason are both present in Alternatives. The study names --filing-decision-override at docs/route-a-zero-decision-study.md:178 while rejecting it, and the register scopes that concern to the refusal, the routed directive and the reference, none of which this step builds; revisit when init emits bytes. ADR-067 carries no supersession marker because the study requires it byte-identical, so the supersession claim sits on the draft alone and no contradiction exists until the code changes.
## Step 1, round 2 -- 2026-09-07T00:47:45Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=not-applicable; partial-write-of-the-receipt=not-applicable; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: step 1 still changes no Python, so graphql-transport, prior-body-in-memory, receipt-forgery and partial-write-of-the-receipt have no bytes to audit and are not-applicable rather than passed; refusal-names-its-own-bypass, routed-exit-mistaken-for-success and window-undiscriminated-read were reviewed as prose commitments in the record and study, not as emitted init bytes, which do not exist until step 2. held-job-drift and reviewed-span-drift were checked as byte-identity only: git diff f22de680 HEAD is empty for plugins/hexaemeron/skills/fiat/EVOLUTION.md and for its SKILL.md, and no generation row is owed yet. The skills#1337 edit count and logins were not re-queried and are carried from the receipted study. The Solidity suite is waived by the brief. phylax and ephoros parse Python, TypeScript and block-YAML only, so their exit 0 covers no byte this step added. Imprimatur 100.0 out of 100 with 0 defects and a clean Brevitas on the study are the operator's readings taken after d89603af and were not re-run here. run_checks was not re-run: the operator's --base run over the whole step branch is green, 11 of 11 checks passed with root-suite at 175.3s, and the seven changed paths it reports match git diff --name-only f22de680 HEAD exactly.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:105 | Carried from round 1 and re-verified. Decision 3 now sits inside Consequences, which opens at line 85 with no later heading. The move is byte-exact: the removed and added blocks share SHA-256 1b79e24645ad3df58cbecfd42e399379adf81253ee6aa4365aa4e383466ed651. Nothing else shifted: the file's sorted-line SHA-256 is 333e387dac6c3881035f916df038556696136f465ed80eda009b54b76ff15cb9 at 7c6f5ca8, at 9e314eff and at HEAD, so the file is a permutation of its own lines, and git diff 9e314eff HEAD on the path is empty. | verified fixed in 9e314efff63f17b0958acadef488b27cf192cef4 |<br>| S1-R1-02 | medium | docs/route-a-zero-decision-study.md:306 | Carried from round 1 and re-verified. Study mode against --design-evidence .hexaemeron/design-evidence.json prints clean and exits 0. The fence carries three ordered rows: schema hypomnema-design-bridge/v1, decision decision-age-window, record docs/decisions/drafts/route-a-filed-zero-as-an-answer.md. That decision equals selection.candidate in the design-evidence record 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a, whose field is named selection.candidate rather than a top-level selected. The Exit binding survives the amendment: substituting the plugins/hexaemeron/skills/ prefix into the five sibling-skill links of .hexaemeron/study.md reproduces the docs copy byte-for-byte, 5 occurrences and exactly 5 differing lines at unshifted numbers 235, 245, 260, 285 and 299. The amendment is a pure append at byte 32055 of 33054: bytes 0 to 32055 hash to the receipted prefix 2d651b80ef222a2b6cdd694631eb5ca7ae45ef948a746b9c70879601df824680, the remainder to 0d9f565e, the whole file to 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524. The ordinary walk exits 0. | verified fixed in d89603af4337ddc822c2e24e4a0c3efdd4a0efaa |<br>| S1-R2-01 | low | docs/route-a-zero-decision-runbook.md:57 | The Exit proves itself by python3 scripts/run_checks.py exiting 0. Run bare on a clean tree that command exits 0 with outcome nothing-selected, zero checks selected and root-suite among the omitted, so the clause as written is satisfiable without executing a check; skills#1429 records the tooling defect. The step is green in fact, but only under the operator's --base run. Runbooks under docs/ name --full five times and --base or --plan elsewhere, so the bare form is this runbook's departure from the house convention. The same clause stands at lines 95, 134, 174 and 219 for steps 2 to 5. Not repaired here: the shipped copy is byte-identical to .hexaemeron/runbook.md, which Exit clause 2 requires, so the repair is a controller amendment spanning all five steps. | open |<br>| S1-R2-02 | low | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:53 | Study item 12 gives decision 2, that no override exists, the home "the same record, as its own section". The record gives it a bold-led paragraph inside Decision. Item 12 names decision 3's home as Consequences, a level-two section, so section in that sentence denotes a level-two heading. Round 1 left this unpursued on the ground that a sixth heading breaks the five-section shape every record under docs/decisions/ uses; that ground is measurably false in both halves. A probe copy carrying a sixth heading drew no shape finding from hypomnema, only three H001 link failures caused by the probe path, and 12 of the 83 records under docs/decisions/ already carry six or more level-two sections. Not repaired here: the rejected alternative and its reason are both present in Alternatives, so the gap is heading level alone, and promoting a published record's heading is the run owner's call. It does not block the Exit, which requires the record to record the three decisions and does not quote item 12's homes. | open |<br><br>Leads not pursued: no fix was applied this round, so there is no commit to classify and the verdict is null; round 1's unguarded stands on 9e314eff and is not re-classified. The study still names --filing-decision-override at docs/route-a-zero-decision-study.md:178 while rejecting it, and the register scopes that concern to the bytes init emits; init is unchanged, so the lead is still unreachable and becomes reachable at step 2. ADR-067 is byte-identical to the base as study item 12 requires, so its supersession claim sits on the draft alone and AGENTS.md:117 still matches the code; stale-claim-elsewhere becomes reachable at step 2, when the code stops refusing a 0. The record's Decision section holds two decisions and their reasoning against a template that asks for one sentence; folded into S1-R2-02 rather than raised separately, because one repair settles both.
## Step 1, round 3 -- 2026-09-07T01:15:48Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=not-applicable; partial-write-of-the-receipt=not-applicable; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: step 1 still changes no Python, and git diff --name-only f22de680 HEAD over '*.py' is empty, so graphql-transport, prior-body-in-memory, receipt-forgery and partial-write-of-the-receipt have no bytes to audit and are not-applicable rather than passed, and refusal-names-its-own-bypass, routed-exit-mistaken-for-success and window-undiscriminated-read were reviewed as prose commitments in the record and the study rather than as emitted init bytes, which do not exist until step 2. held-job-drift, reviewed-span-drift and stale-claim-elsewhere were checked as byte-identity against the base only: git diff f22de680 HEAD is empty for plugins/hexaemeron/skills/fiat/EVOLUTION.md, for its SKILL.md, for AGENTS.md, for ADR-067 and for ADR-078, and AGENTS.md:117 still describes the refusal the code still performs. The skills#1337 edit count and logins were not re-queried and are carried from the receipted study. The Solidity suite is waived by the brief, since the step produces no Solidity and no Foundry or Hardhat project. phylax and ephoros parse Python, TypeScript and block-YAML only, so their clean exit 0 covers no byte this step added. run_checks was not started here: the operator's --base run over the step branch at 0fb5eebc was read from its output, and the seven changed paths it reports match git diff --name-only f22de680 HEAD exactly. Steps 2 to 5 were read only for the amendment composition in S1-R3-01; no Entry or Exit condition of theirs was audited.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:109 | Carried from rounds 1 and 2 and re-verified, because 0fb5eebc edits the same file. Decision 3 still stands inside Consequences at line 109 with its measurement that all four edits on skills#1337 carry the one login. The round-3 edit is confined to lines 47 to 70 and the removed Alternatives bullet, and changes no Consequences byte: git diff d89603af 0fb5eebc on the path reports one hunk at 47 and one at 72, neither reaching line 89 where Consequences opens. | verified fixed in 9e314efff63f17b0958acadef488b27cf192cef4 |<br>| S1-R1-02 | medium | docs/route-a-zero-decision-study.md:306 | Carried and re-verified. The shipped study is unchanged since the fix: git diff d89603af HEAD on the path is empty. Study mode against --design-evidence .hexaemeron/design-evidence.json prints clean and exits 0, and that record still hashes to 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a, the value the brief pins. The runbook amendments did not touch the study: it still differs from .hexaemeron/study.md by exactly the five link re-bases, 5 differing lines at 235, 245, 260, 285 and 299, and substituting the plugins/hexaemeron/skills/ prefix into the artefact reproduces the shipped copy byte for byte. | verified fixed in d89603af4337ddc822c2e24e4a0c3efdd4a0efaa |<br>| S1-R2-01 | low | docs/route-a-zero-decision-runbook.md:252 | Repaired by six receipted runbook amendments, f28625a2 to fb82755a, and the shipped copy is byte-identical to .hexaemeron/runbook.md at fb82755a. Each of steps 1 to 5 carries exactly one Complete replacement Exit and no step was missed; all six record replacement_fields Exit alone, so no other field moved. Every replacement is the full Exit rather than a fragment: no baseline Exit sentence other than the proof clause is absent from its replacement, and each names --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha and the token outcome green. The command the repaired clause names ran green over the step branch at 0fb5eebc, 11 of 11 checks passed with root-suite at 239.3s, base f22de680 and 7 changed paths. The bare form cannot reach that token: run_checks.py:2986 sets outcome nothing-selected with exit_code 0 when nothing is selected, and outcome green is produced only at line 3090 after checks have run. The baseline clause is not deleted, because hexctl amendments append and source_runbook_step composes baseline plus amendments at hexctl.py:11710, so each packet still opens with the bare-command Exit and the replacement follows it; that is the tool's shape, not a defect of this repair. | verified fixed in 0fb5eebc7b0cd08a58dba5cf14e7de81996d92b6 |<br>| S1-R2-02 | low | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:50 | Repaired in 0fb5eebc. `## No override exists` is a level-two heading at line 50, sitting between Decision and Alternatives, and carries all three parts study item 12 asks for: the decision, its reason, and the rejected override alternative with the reason for rejecting it. The five template sections survive, at Status 5, Context 11, Decision 40, Alternatives 69 and Consequences 89. Nothing was lost in the move: a word-multiset comparison of the record at d89603af against 0fb5eebc loses only the bold and bullet markers, the clause 'Rejected for the reason the Decision states', the word 'above' and the word 'window', and gains the heading plus one new sentence giving a second reason for the rejection. Hypomnema's ordinary walk and its study mode are both clean at exit 0, so the sixth heading draws no shape finding. Imprimatur scores 100.0 out of 100 with 0 defects on the record and Brevitas exits 0 with no output. | verified fixed in 0fb5eebc7b0cd08a58dba5cf14e7de81996d92b6 |<br>| S1-R3-01 | low | docs/route-a-zero-decision-runbook.md:348 | The correction amendment names Steps touched Step 1, and source_runbook_step at hexctl.py:11691-11710 admits an amendment to a step packet only when that step number is in its steps_touched. Reproducing that composition over the six receipted amendments gives step 1 two amendments and the token nothing-selected twice, and gives steps 2, 3, 4 and 5 one amendment each carrying the false claim that the bare command reports `outcome green` for no scope, with nothing-selected zero times. The correction's own Why says the true token 'now sits in step 1's Exit where a reader meets it' and its Steps touched says step 1 carries it 'on behalf of all five blocks'; both are true of step 1's packet and false of the other four, which is where the false claim is delivered. The claim is load-bearing rather than cosmetic: it names outcome green as what a vacuous run yields, and outcome green is the token the repaired Exit requires, so as written it argues that the repair those four blocks make is as empty as the clause it replaced. Amendment 6 states that the conclusion and the installed Exit clauses are unaffected and that only the output token was wrong, which is true of the words and understates the consequence. Not repaired here: .hexaemeron/ is wholly ignored at .hexaemeron/.gitignore:1, so the artefact cannot enter a commit, and editing the shipped copy alone would break the byte-identity Exit clause 2 requires. The repair is four further hexctl amend runbook calls, one Complete replacement Exit per step 2 to 5, which moves the runbook receipt off fb82755a and is the controller's to make. | open |<br><br>Leads not pursued: Elenchus reports 0fb5eebc unguarded because the commit changed no test files, which is what that skill specifies for a commit with no test change and is expected for a prose-only repair. Amendment 6's Why dates the outcome nothing-selected observation to 2026-09-06 against commit 7c6f5ca8; no durable record corroborates that provenance, since round 1's record at 2026-09-06T23:07:26Z does not name the token and the only record that does is round 2's S1-R2-01 row, written 2026-09-07T00:47:45Z and citing docs/route-a-zero-decision-runbook.md:57 rather than a commit. Not raised, because the token itself is established independently from run_checks.py:2986 and refuting the date would need the operator's transcript. Study item 12's 'with the alternative and the reason for rejecting it' was met by moving the override bullet out of Alternatives rather than duplicating it, so Alternatives drops from six bullets to five and a reader scanning that section alone no longer meets the override option; not raised, because item 12 names the new section as its home and both hypomnema modes are clean. All six runbook amendments carry study_sha256 83ae2f35, equal to the current study, so all six are live in their packets; a further hexctl amend study would move that value and silently drop every one of them, returning all five Exits to the bare command, and no study amendment is owed. The study still names --filing-decision-override at docs/route-a-zero-decision-study.md:178 while rejecting it, and the register scopes that concern to the bytes init emits, so the lead stays unreachable and becomes reachable at step 2, as does stale-claim-elsewhere when the code stops refusing a 0.
## Step 1, round 4 -- 2026-09-07T01:39:34Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=not-applicable; partial-write-of-the-receipt=not-applicable; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: step 1 still changes no Python, and git diff --name-only f22de680 HEAD over '*.py' is empty, so graphql-transport, prior-body-in-memory, receipt-forgery and partial-write-of-the-receipt have no bytes to audit and are not-applicable rather than passed, and refusal-names-its-own-bypass, routed-exit-mistaken-for-success and window-undiscriminated-read were reviewed as prose commitments in the record and the study rather than as emitted init bytes, which do not exist until step 2. held-job-drift, reviewed-span-drift and stale-claim-elsewhere were checked as byte-identity against the base only: git diff f22de680 HEAD is empty for plugins/hexaemeron/skills/fiat/EVOLUTION.md, for its SKILL.md, for AGENTS.md, for ADR-067 and for ADR-078, and AGENTS.md:117 still describes the refusal the code still performs. The skills#1337 edit count and logins were not re-queried and are carried from the receipted study. The Solidity suite is waived by the brief, since the step produces no Solidity and no Foundry or Hardhat project. phylax and ephoros parse Python, TypeScript and block-YAML only, so their clean exit 0 covers no byte this step added. The --base run over the step branch at c214822a was read from the operator's output rather than started here: 11 of 11 checks passed with root-suite at 192.4s, base f22de680, outcome green, and the 8 changed paths it reports match git diff --name-only f22de680 HEAD exactly. Steps 2 to 5 were read only for their Exit composition and correction text; no Entry or Exit condition of theirs was audited and none of their implementation exists. The 2026-09-06 provenance amendment 6 gives its own nothing-selected observation is still uncorroborated by any durable record and was not chased again.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:109 | Carried from rounds 1 to 3 and re-verified. c214822a touches only .horos/census.json and docs/route-a-zero-decision-runbook.md, and git diff 0fb5eebc HEAD on this path is empty, so decision 3 still stands inside Consequences with its measurement that all four edits on skills#1337 carry the one login. | verified fixed in 9e314efff63f17b0958acadef488b27cf192cef4 |<br>| S1-R1-02 | medium | docs/route-a-zero-decision-study.md:306 | Carried and re-verified. Hypomnema study mode against --design-evidence .hexaemeron/design-evidence.json prints clean and exits 0, and that record still hashes to 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a. The ten runbook amendments did not reach the study: both copies hold 324 lines, exactly lines 235, 245, 260, 285 and 299 differ, each by the plugins/hexaemeron/skills/ prefix alone, and substituting that prefix into all five occurrences in .hexaemeron/study.md reproduces the shipped copy byte for byte. The artefact still hashes to 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524. | verified fixed in d89603af4337ddc822c2e24e4a0c3efdd4a0efaa |<br>| S1-R2-01 | low | docs/route-a-zero-decision-runbook.md:252 | Carried and re-verified across every packet rather than step 1 alone. Each of steps 1 to 5 composes to exactly two amendments, each replacing Exit alone, and each final Exit names --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha and the token outcome green. A word diff of each baseline Exit against its final replacement reports one substitution, of the bare proof clause, for steps 1 to 4, and that substitution plus one appended sentence for step 5, whose baseline proof clause carried a second conjunct about the recorded demo commands reproducing byte for byte; that conjunct survives intact. No other baseline Exit sentence is absent from any replacement, and no replacement carries another step's material. | verified fixed in 0fb5eebc7b0cd08a58dba5cf14e7de81996d92b6 |<br>| S1-R2-02 | low | docs/decisions/drafts/route-a-filed-zero-as-an-answer.md:50 | Carried and re-verified. The record is untouched since the fix: git diff 0fb5eebc HEAD on the path is empty. Hypomnema's ordinary walk over the paths the runbook's Tests field names and its study mode are both clean at exit 0, so the sixth heading still draws no shape finding. | verified fixed in 0fb5eebc7b0cd08a58dba5cf14e7de81996d92b6 |<br>| S1-R3-01 | low | docs/route-a-zero-decision-runbook.md:334-436 | Repaired by four further amendments, receipt fb82755a to 34adeafc, ten in total. Reproducing hexctl.py:11695-11700, which admits an amendment to a step packet only when steps_touched names that step and study_sha256 equals the current study, gives each of steps 1 to 5 exactly two amendments and two Complete replacement Exit clauses. All ten record replacement_fields Exit alone and their prose names no other replaced field. The ten byte spans are contiguous from 14084 to 34276, every amendment_sha256 recomputes from its own span, and the prior-to-new chain is unbroken with a final value equal to the file, 34adeafc31347fa54a2551295ad385a579dee42dbf48ac8ba6c8b1b3b7957f5e. The appended sentence is byte-identical in all five, SHA-256 1aeb7d4d1f149deba70f7f74ffc365fc9628c6b416c1231313a0d6f8af8c548c. Token counts confirm the operator's: the correction block carries outcome nothing-selected once raw and twice whitespace-normalised for steps 2 to 5 and twice both ways for step 1, and the one wrapped span sits in Why, never in the Exit clause, which carries the token unbroken in all five. The appended sentence is true first-hand: bare python3 scripts/run_checks.py on a clean tree printed outcome nothing-selected with 0 changed paths and no scope or check selected and exited 0, and run_checks.py sets that outcome at :2986 with exit_code 0 at :2991 while outcome green is reached only at :3090, after results exist. The new correction text holds: all five original blocks do carry the quoted phrase, outcome green is the exact token every repaired Exit requires, and Steps touched now names only the step whose own packet carried the claim. | verified fixed in c214822a0ba7466ab49eca53d37f8b79b7e41b2d |<br>| S1-R4-01 | informational | docs/route-a-zero-decision-runbook.md:334-352 | The correction-text repair reaches four packets, not five. Amendment 6 cannot be edited, so step 1's packet still reads that "only the output token they name was wrong", the understatement round 3 recorded, and that step 1's Exit "carries the correction on behalf of all five blocks", which amendments 7 to 10 have since made false a second way: steps 2 to 5 each carry their own correction now, so step 1 carries nothing on their behalf. Steps 2 to 5 read the repaired text. c214822a's message says both defects are corrected in the new text, which is true of that text and silent about the one packet keeping the old. No Exit clause is affected: step 1's operative clause carries the true token, and every packet a worker is delegated from here carries the repaired reasoning. Not repaired: an eleventh amendment is the controller's, moves the receipt off 34adeafc again, and is itself immutable, so it would leave the same residue one block later. | open |<br><br>Leads not pursued: the hard line wrap inside the Why code span of steps 2 to 5 is not worth a finding. It renders as one span because a Markdown code span joins a line break as a space, the operative Exit clause carries outcome nothing-selected unbroken on one line in all five blocks, Imprimatur scores the shipped runbook 100.0 out of 100 with 0 defects, Brevitas exits 0 with no output and both hypomnema modes are clean, so only a raw substring search is affected and it under-counts by one per block. The repaired Why still opens "The five amendment blocks above", which is true of the runbook file and resolves to one block inside the packet that reads it; not raised, because the referent is a superset containing that reader's own block and it asserts no coverage the packet lacks, unlike the claim S1-R3-01 named. .horos/candidates.json is the eighth changed path and step 1's Files field names only boundary and census; it entered at 8787e370 because this run's own synopsis crossed the blob threshold at mean line length above 400, which is the mandated Horos scan recording the audit loop's own output, and it will move again when this round is appended. All ten amendments carry study_sha256 83ae2f35, equal to the current study, so all ten are live in their packets; a further hexctl amend study would move that value and hexctl.py:11697 would silently drop every one of them, returning all five Exits to the bare command, and no study amendment is owed. The study still names --filing-decision-override at docs/route-a-zero-decision-study.md:178 while rejecting it, and the register scopes that concern to the bytes init emits, so the lead stays unreachable and becomes reachable at step 2, as does stale-claim-elsewhere when the code stops refusing a 0.
## Step 2, round 1 -- 2026-09-07T10:24:58Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=not-applicable; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's security_suite field, since the run produces no Solidity and no Foundry or Hardhat project. Operator evidence was read rather than reproduced: python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha at ab1b6e53 reporting outcome green over 13 checks with hexaemeron-suite 850.9s and root-suite 580.0s, the 2437-test 12-shard Hexaemeron run, and the live probe at .hexaemeron/notes/demo-observed.md with its directive at .hexaemeron/notes/demo-1337-routed.json. The probe's directive bytes were read and reconcile with routed_filing_directive field for field, but neither clone was re-run here. phylax and ephoros exit 0 over the three changed Python paths, which covers only their eight and five parser rules and no byte of JSON. hypomnema's ordinary walk over docs and plugins exits 0; adding audit reports two H008 at audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128 and its synopsis:8, which belong to another run's byte-pinned record, were last written at f1678e6e and are untouched by ab1b6e53. Study mode was not run, because step 2 ships no study. The held_lock concurrency reasoning is analytic, from hexctl.py:2202-2300 and its single call site at :17604: no two-process test was written and none exists, so S2-R1-02 and S2-R1-03 are read from the code rather than observed. The GitHub API was not queried, so the skills#1337 edit count and logins stay carried from the receipted study. window-undiscriminated-read, graphql-transport and prior-body-in-memory name a REST updated_at read, a GraphQL request and a userContentEdits body read that ab1b6e53 does not add, so they are not-applicable rather than passed. This round applies no fix, so the Elenchus verdict field is null; the guard check the brief asked for was run against the step's own commit instead, and S2-R1-08 records what it returned and why.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | informational | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2279-2292 | The teardown comment presents a routed zero as the invocation reaching it. Every successful init from a checkout without `.hexaemeron/` reaches it too: `held_lock` locks `state_root(args.dir)`, a run's state is written to `state_root(worktree)`, and `cmd_init`'s only `makedirs` calls are the worktree home and `state_root(worktree)`, so `state_path(args.dir)` never exists and `os.listdir(root) == ["lock"]` holds at teardown. The guard clause the comment states, created the root and left it otherwise empty, is accurate; the framing around it is not. Behaviour is correct today because the directory removed holds only the lock. The first future write into `state_root(args.dir)` during init changes the teardown with no test covering it. | open |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2286-2291 with :2228-2238 | The teardown gives `os.open` a benign failure it does not distinguish. The holder unlinks `lock` under its flock and then `rmdir`s the root; a contender that passed `os.path.isdir(root)` before the rmdir and reaches `os.open(path, O_CREAT\|O_RDWR\|O_NOFOLLOW)` after it gets ENOENT, which the pre-existing `except OSError` converts to `die("run lock is not a safe regular file", 1)`. The contender fails closed and no exclusion is lost, but the operator is told the lock is unsafe when it is absent. Before this commit `O_CREAT` could fail only for a genuine safety reason, so the message was accurate. | open |<br>| S2-R1-03 | informational | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2224-2226 | `created_root` records that this invocation observed the root absent, not that it created it: `os.makedirs(root, exist_ok=True)` is a no-op on an existing directory, so two inits racing on a fresh checkout both set it True. Exclusion still holds, because only the flock winner reaches the teardown and the loser dies at `flock`, so the flag is sound for the guard it protects. The name, and the commit message's removes a state root it created, assert more than it carries, and no test exercises two concurrent inits. | open |<br>| S2-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5090-5096 | `routed_filing_directive`'s docstring states that every issue-derived string passes `clean`. The row comprehension cleans string values only: keys are not cleaned and non-string values pass through. That holds today solely because `carryover_row_faults` (:4674-4678) emits exactly the three fixed keys `id`, `disposition` and `reference` with string values, after `_contains_nonprinting_character` has already refused the whole row. Nothing pins that coupling, and `test_the_directive_strips_a_control_character_from_every_field` exercises only string values under those three keys, so a future row shape with a derived key or a nested value falsifies the docstring with no test failing. | open |<br>| S2-R1-05 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2679-2686 | A routed zero and a started run are indistinguishable by exit status: both exit 0. The directive's `"do": "pull-request"` is the only discriminator and it is on stdout, while `test_a_zero_routes_before_any_state_worktree_or_branch_exists` now pins stderr empty. A caller written as `hexctl init ... && hexctl next` proceeds to `next`, which fails on absent state. The step meets the register line's stated criterion, exit 0 with one directive and nothing built, and adds nothing an exit-code-only caller can see, which is the risk `routed-exit-mistaken-for-success` names. | open |<br>| S2-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 2's third amendment, Files | The Complete replacement Files names four paths; ab1b6e53 edits five. The fifth is `.horos/census.json`, forced by the same criterion the amendment used to admit `tests/promise_machine_coverage.json`: a derived artefact that a tracked-file edit reddens, here `HorosCensusCurrencyTests` rather than `test_audit_synopsis_recovery`. Step 5's Files names `.horos/census.json` and `.horos/boundary.json`, so the runbook holds the path elsewhere. Not repaired here: a runbook amendment is controller-pinned and the brief reserves `hexctl amend` to the orchestrator. | open |<br>| S2-R1-07 | low | AGENTS.md:117, docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md:63, plugins/hexaemeron/skills/fiat/SKILL.md:181-187 and :798 | The contradiction `stale-claim-elsewhere` anticipated now exists. AGENTS.md:117 states that init refuses to start a run against a 0 before it creates any state, worktree or branch; ADR-067:63 states that `Fiat-Required: 0` refuses with the pull-request route named; SKILL.md:181-187 states that `Fiat-Required: 0` refuses and still carries the bypass sentence naming the edit to `Fiat-Required: 1`. All five documents are byte-identical to the run base f22de680. The register names AGENTS.md:117, ADR-067 and ADR-078 and omits SKILL.md, which docs/route-a-zero-decision-study.md:65 identifies as the document carrying the sentence the 6 September agent followed. Step 5's Exit names both SKILL.md sites, so the delivery covers it and the register line does not. | open |<br>| S2-R1-08 | low | docs/route-a-zero-decision-runbook.md, step 2 Disciplines | The Disciplines clause requires that each new case is observed red against this step's entry tree before its behaviour lands. One of the six is green there. Overlaying the three changed test files onto 979a213d and running each case: `test_a_filed_zero_exits_zero_and_prints_one_directive`, `test_a_routed_zero_names_the_route_the_issue_and_what_closing_needs`, `test_a_routed_zero_leaves_no_state_worktree_or_branch` and `test_nothing_emitted_names_a_mechanism_that_grants_this_run` fail; `test_the_directive_strips_a_control_character_from_every_field` errors, because `routed_filing_directive` does not exist on the parent; `test_an_issue_body_carrying_a_control_character_never_reaches_a_run` passes, because it asserts parser behaviour the step did not change. The rewritten `test_issue_filing_contract.InitFilingGateTests.test_a_zero_routes_before_any_state_worktree_or_branch_exists` fails. Five of seven changed cases are guards, one is green on both trees, one errors. That mix is why the runbook's own Elenchus runner contract returns inconclusive rather than guarded for this step: `python3 $PLUGIN_ROOT/skills/elenchus/scripts/elenchus.py --ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-2.json` printed `inconclusive -- the runner report records an infrastructure error` and exited 0, naming test_hexctl.py, test_issue_filing_contract.py and promise_machine_coverage.json. A step that adds a function cannot earn guarded through this contract, because the case exercising that function errors on a tree where it is absent. | open |<br><br>Leads not pursued: the held_lock teardown is safe as written and its symptom is not cosmetic, so neither became a finding against the change itself. Exclusion is preserved because the unlink happens under the exclusive flock on that inode: a contender that opened the path first holds the same inode and dies at `flock`, and one that opens after the unlink creates its own inode, so the holder's `rmdir` fails ENOTEMPTY and is swallowed. No live root is reachable, because a run's root is `state_root(worktree)` while the teardown removes `state_root(args.dir)`, and does so only when it holds no `state.json` and exactly `lock`. That symptom reaches past appearance: no tracked gitignore rule covers `.hexaemeron`, and the self-ignoring `*` file is written into `state_root(worktree)` at hexctl.py:2764-2766 and never into `state_root(args.dir)`, so a leftover lock on a fresh checkout is untracked, shows in `git status --short`, fails this step's Exit and blocks the next run through the dirty-tree preflight hexctl.py:2731-2735 names. A cheaper alternative exists and was not raised as a finding because the current code is correct: write that same self-ignoring `.gitignore` when `held_lock` creates the root, which satisfies the Exit with no unlink, no rmdir and no teardown race, at the cost of leaving an empty directory that `test_a_routed_zero_leaves_no_state_worktree_or_branch` currently forbids. Step 1 round 4's open lead closes: `--filing-decision-override` appears 0 times in hexctl.py, so the study's rejected flag never reached emitted bytes, and the three other bypass tokens the Exit names, override, --force and Fiat-Required: 1, appear in hexctl.py only at :237, :12020, :12024 and :12211, which are a legacy config override docstring, the audit-log path guard and `git worktree remove --force`, none on the filing path. plugins/hexaemeron/skills/fiat/EVOLUTION.md:71 still reads that a 0 refuses with the pull-request route named, and was left alone as a dated ledger row for fiat-v5.47.1 rather than a live claim, which is the treatment hypomnema prescribes for superseded records. No fix was applied this round: S2-R1-01, S2-R1-03 and S2-R1-04 are comment and docstring accuracy in one file whose whole-file digest is bound in eleven `tests/promise_machine_coverage.json` bindings that `test_audit_synopsis_recovery.py::test_integrated_controller_digest_reaches_every_promise_binding` enforces, so repairing them costs eleven re-pins, a Horos census rescan and a root-suite gate on a file step 5 must re-pin again; S2-R1-02 is a code change whose value depends on whether the teardown survives at all; S2-R1-06 and S2-R1-08 need a controller-pinned runbook amendment the brief reserves to the orchestrator; and S2-R1-07 is step 5's declared work. This record's own prose gates: Imprimatur scores 100.0 out of 100 with 0 defects, and Brevitas reports B010, B001 and B027, all three explained rather than repaired. B010 and B001 fire identically on the already-receipted step 1 round 4 record, because the fiat-audit-round/v2 structure fixes one heading and a long field ahead of the table. B027 matches the occurrence verb at offsets 1473 and 1651, used literally about grep counts rather than as a hedge, which is the line-anchored false-positive class skills#1338 records for B022. .horos/candidates.json moves again when this round is appended, and step 2's Files field names no .horos path at all, which S2-R1-06 records.
## Step 2, round 2 -- 2026-09-07T11:21:00Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=not-applicable; graphql-transport=not-applicable; prior-body-in-memory=not-applicable; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's security_suite field, since the run produces no Solidity and no Foundry or Hardhat project. Operator evidence was read rather than reproduced: the 2438-test 12-shard Hexaemeron run at 8b176610 exiting 0 with zero failures; the controller digest re-pin across 11 bindings from e272b3ba to 6cf4a645; and python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha at 8b176610 reporting outcome green, 13 of 13 checks passed against base f22de680 over 12 changed paths, with hexaemeron-suite 1012.9s and root-suite 390.8s. That is the exact token this step's amended Exit names, and the 12 paths it reports match git diff --name-only f22de680 HEAD, so the proof clause is met at 8b176610 before this round is appended. The eight cases the step changes were re-run here, alone, at HEAD and against the entry tree, which is where S2-R2-02's measurement comes from. The GitHub API was not queried, so the skills#1337 edit count and logins stay carried from the receipted study, and the live demo path was not re-run; the routed-zero path was exercised through the eight unit cases and through held_lock directly, never against the live API. window-undiscriminated-read, graphql-transport and prior-body-in-memory name a REST updated_at read, a GraphQL request and a userContentEdits body read that 8b176610 does not add, so they are not-applicable rather than passed. No two-process test exists: the ENOENT window was forced inside one process by making os.path.isdir report a root that was already absent, which observes the message mapping and not a real race, so the exclusion reasoning in S2-R1-02 and S2-R1-03 is still read from the code. phylax and ephoros exit 0 over the three changed Python paths, which covers only their eight and five parser rules and no byte of JSON. hypomnema's ordinary walk over docs and plugins exits 0; adding audit exits 1 with exactly two H008, at audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128 and its synopsis:8, which is the pre-existing defect skills#1451 records and is unchanged by this step. Study mode was not run, because step 2 ships no study. Steps 3, 4 and 5 were read for their Exit and Files alone, to settle where S2-R1-05 belongs; none of their implementation exists. This round applies no fix, so the Elenchus verdict field is null.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | informational | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2295-2309 | The framing is repaired. The comment now states that every `init` from a checkout without `.hexaemeron/` leaves this directory holding nothing but the lock, whether it routed a filed `0` or built a run, and gives the reason removal matters rather than the mechanism. Each restated fact holds at this tree: `held_lock` takes `state_root(args.dir)` from its one call site at :17640; `cmd_init` writes the run's state and its self-ignoring `.gitignore` into `state_root(worktree)` at :2779-2786, with `WORKTREE_HOME` = `tmp/fiat` at :12002, so the two roots cannot coincide. Observed rather than inferred: `held_lock(repo, "cmd_init")` on a fresh checkout held exactly `['lock']` inside the lock, left no state root afterwards, and `git status --short` was empty; `held_lock(repo, "cmd_next")` on the same checkout yielded and created nothing. One clause of the replacement is a new finding, S2-R2-01. | verified fixed in 8b176610 |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2244-2255 | Repaired, and observed rather than reasoned. `except FileNotFoundError` sits at :2244 and `except OSError` at :2254; the specific clause must come first because `FileNotFoundError` is an `OSError` subclass, and it does. `die` at :1094-1096 calls `sys.exit`, so neither clause falls through to `os.fstat(fd)` at :2256. Measured against the same open flags: a missing parent directory raises `FileNotFoundError`, ENOENT 2; a dangling symlink and a symlink to a regular file both raise ELOOP 62 and a directory at the lock path raises `IsADirectoryError`, EISDIR 21, none of them `FileNotFoundError`, so every genuine safety failure still reaches `run lock is not a safe regular file`. Forcing the window produced exit 1 and `the run state directory went away while this command was starting; nothing was changed, so run it again`. | verified fixed in 8b176610 |<br>| S2-R1-03 | informational | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2224-2232 with :2310 | Repaired. The flag is `root_was_absent` at :2230, :2232 and :2310, and `created_root` survives nowhere in the tree but this file's round 1 row and its synopsis, which are byte-pinned records rather than readers. The comment at :2224-2229 states that `makedirs(exist_ok=True)` is a no-op on a directory another process made, that two racing inits both set the flag, and that exclusion rests on the flock and the teardown's own guard instead. | verified fixed in 8b176610 |<br>| S2-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5111-5132 | Repaired. `scrubbed` cleans strings, cleans keys through `clean(str(key))`, recurses into dict values and list elements, and returns everything else unchanged; `clean` is `CONTROL_RE.sub(" ", text)` at :16884. Over the domain a decoded contract can carry, that reaches every string the docstring claims. Passing integers, `None` and booleans through is right rather than tolerated: none can carry a control byte, and coercing them would change the directive's type contract, which the new case pins by asserting `row["count"] == 3` and `row["nested"]["deep "] == ["a   bell", 7, None, True]`. `test_the_directive_strips_control_characters_from_keys_and_nesting` is the seventh new case in `test_hexctl.py`, matching the amended Tests count, and passes at this tree. | verified fixed in 8b176610 |<br>| S2-R1-05 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2701-2708 | Carried and re-verified; open, and the disposition assigned to it does not hold. Exit status still does not separate a routed `0` from a started run: `sys.exit(0)` at :2708 after one directive on stdout. No remaining step's Exit closes it. Step 5 declares `AGENTS.md`, the two `SKILL.md` sites, the `EVOLUTION.md` generation row, the re-pinned instruction chain, version agreement and the demo record, and its Files names no controller path. Steps 3 and 4 do edit `hexctl.py`, but declare a receipt provenance block and a window refusal, neither of which an exit-code-only caller can read. It therefore belongs in the run pull request's `## Carried forward`, not to a step. | open |<br>| S2-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 2's fourth amendment, Files | Repaired. The Complete replacement Files names five paths and `ab1b6e53` edits exactly those five: `hexctl.py`, `test_hexctl.py`, `test_issue_filing_contract.py`, `tests/promise_machine_coverage.json` and `.horos/census.json`. `.horos/candidates.json` is not owed: it moves only in `ad0d6d5f`, the round 1 record commit, which is the audit loop's own output rather than the step's work. The amendment reaches step 2's packet under the admission rule at :2460-2466, because its `steps_touched` names 2 and its `study_sha256` equals the current study `83ae2f35`. | verified fixed in runbook 011705a9 |<br>| S2-R1-07 | low | AGENTS.md:117, docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md:63, plugins/hexaemeron/skills/fiat/SKILL.md:181-187 and :798-799 | Carried and re-verified; open. All four documents and ADR-078 are byte-identical to the run base `f22de680`. The register still names three of the four at `docs/route-a-zero-decision-study.md:196`, and neither copy can be repaired in this run: `hexctl amend study` moves `study_sha256`, and the admission rule at :2463 then silently drops all thirteen runbook amendments, returning five step Exits and step 2's Files, Tests and Disciplines to baseline; while step 1's own Exit binds the shipped copy to `.hexaemeron/study.md` with the five sibling-skill links prefixed and nothing else changed, so editing `:196` there breaks a receipted Exit. The risk itself is closed by delivery: the criterion column is universal, every prose claim that init refuses a 0, and step 5's Exit names both `SKILL.md` sites. What ships uncorrected is the scope column, narrower than its own criterion and five lines above `:65`, where the study's prose already names `SKILL.md` and both byte offsets. | open |<br>| S2-R1-08 | low | docs/route-a-zero-decision-runbook.md, step 2 Disciplines | Repaired in kind. The clause no longer requires each new case to be observed red against the entry tree; it records a measured per-case outcome instead, which is the only thing a step that adds a function can deliver. The replacement's own count is wrong, carried as S2-R2-02. | verified fixed in runbook 011705a9 |<br>| S2-R2-01 | informational | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2299-2301 | The replacement teardown comment asserts `Nothing tracked ignores it there`, giving as its reason that the self-ignoring `.gitignore` goes to the worktree's root and not this one. That reason establishes only that `hexctl` does not ignore the directory; whether anything tracked does is the target repository's decision, and the comment at :2755-2759 declines that same assumption in as many words, writing the home's `.gitignore` rather than leaning on the target repository's own rules so the promise holds whichever repository the run was started in. True in this repository: no tracked rule covers `.hexaemeron`, and `git check-ignore -v` matches only `.hexaemeron/.gitignore`, the untracked self-ignoring file this worktree's own run wrote. Behaviour is unaffected, because removal is right whether or not the target ignores it. Not repaired: this file's whole-file digest is bound in eleven `tests/promise_machine_coverage.json` bindings, so one comment clause costs eleven re-pins, a census rescan and a root-suite gate, and re-committing `hexctl.py` would invalidate the `--base` run started against `8b176610` for this round. No later step's Files names this path. | open |<br>| S2-R2-02 | low | docs/route-a-zero-decision-runbook.md, step 2's fourth amendment, Disciplines | The replacement Disciplines records six outcomes across a set of seven. It reads four of the seven fail on the parent, one errors, one passes, which totals six, and names `test_the_directive_strips_a_control_character_from_every_field` as the single erroring case. The seventh case is `test_the_directive_strips_control_characters_from_keys_and_nesting`, which the same amendment added for S2-R1-04, and it errors for the identical reason. Measured: the three changed files from `8b176610` overlaid on entry tree `979a213d`, each case run alone, gives four failures, two errors and one pass, both errors reading `AttributeError: module 'hexctl_under_test' has no attribute 'routed_filing_directive'`; the rewritten `test_issue_filing_contract.InitFilingGateTests.test_a_zero_routes_before_any_state_worktree_or_branch_exists` fails, which the clause does not misstate. The amendment counted the new case in Tests, moving six to seven, and not in Disciplines. Not repaired: a runbook amendment is controller-pinned and the brief reserves `hexctl amend` to the orchestrator. | open |<br><br>Leads not pursued: `scrubbed` can collide two keys, because `clean` maps a control character to a space, so `a\x00b` and `a b` reduce to one key and the later value wins. Not raised: `carryover_row_faults` at :4630 refuses such a row before the builder is reached, the docstring claims cleaning rather than key preservation, and the hazard is strictly narrower than the parser coupling S2-R1-04 removed. The same comment's phrase written into the worktree's root elides state, recoverable from the sentence before it, which contrasts it with the calling checkout's state root; not raised. The clean-tree preflight that comment names is an agent-followed instruction, `git status --short  # must be clean` at SKILL.md:195 with :203 saying to stop if the tree is dirty, and not a controller gate: the only `git status --porcelain` call sites are `worktree_is_clean` at :12266, reached from the integrate receipt at :10724 and worktree removal at :17393, and `_checkpoint_restore_origin` at :15774. The comment claims the preflight stops the next run rather than that `hexctl` enforces it, and ADR-012:44, the comment at :2757 and the `WORKTREE_HOME` docstring at :12007 all make the same claim, so it is inherited rather than introduced here. The new emitted bytes were read against `refusal-names-its-own-bypass`: the ENOENT message says to run the same command again, which for a `Fiat-Required: 0` issue produces the directive again, so it names no mechanism that grants this run, and `--filing-decision-override` still appears 0 times in `hexctl.py`. `partial-write-of-the-receipt` holds because the new refusal fires at lock acquisition, before any state write. The shipped study and `.hexaemeron/study.md` still differ by exactly five lines, 235, 245, 260, 285 and 299, each by the `plugins/hexaemeron/skills/` prefix alone, which is what step 1's Exit requires and step 1 round 4 verified; neither copy is touched here. No fix was applied this round, so there is no commit to classify and the verdict is null; S2-R2-01 is the only new finding a worker could repair, and repairing it would cost the operator's in-flight `--base` evidence for one informational comment clause. This record's own prose gates: Imprimatur scores 100.0 out of 100 with 0 defects, and Brevitas reports B010, B001 and B027, all three explained rather than repaired. B010 and B001 fire identically on the already-receipted step 2 round 1 record, because the fiat-audit-round/v2 structure fixes one heading and a long field ahead of the table. B027 counts two qualifiers inside `Leads not pursued`, which the schema puts on one line, and both carry evidence: the key collision is strictly narrower than the coupling it replaces, and the preflight is agent-followed rather than a controller gate. `.horos/candidates.json` moves again when this round is appended.
## Step 3, round 1 -- 2026-09-07T16:25:35Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's security_suite field, since the run produces no Solidity and no Foundry or Hardhat project. The GitHub API was not queried. That matters here more than in earlier rounds, because `userContentEdits` newest-first ordering is what makes `revisions[1]` the prior body rather than the one before it, and the harness returns whatever a case hands it, so no test can discriminate the two readings. The evidence is one live observation of one issue: `.hexaemeron/notes/step-3-boundary-measured.md` records skills#1337 at totalCount 4 with editedAt 2026-09-06T09:17:50Z on the oldest node, equal to the REST `created_at`, and 2026-09-06T10:08:38Z on the newest, equal to `updated_at`. The oldest node therefore carries the creation body and the newest the current one, which settles the reading in the code's favour; the note is untracked and the claim survives in the commit message alone. Operator evidence read rather than reproduced: the 2445-test 12-shard Hexaemeron run and the 1623-test root suite at `f4f0d4b8`, and the three root-suite failures repaired before that commit. `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` was started by the operator against `f4f0d4b8`'s clean tree and was still running when this round's edits reached the worktree; a dirty tree is refused by name by `dead-code-suppressions-check`, so its thirteen checks straddle the edit and its result is evidence for neither tree: `hexaemeron-suite` passed in 1297.9s and `root-suite` failed in 603.1s while both were reading a worktree whose `hexctl.py` had moved and whose `tests/promise_machine_coverage.json` had not yet been re-pinned, `dead-code-suppressions-check` failed in 2.0s because it refuses a dirty tree by name, which is skills#1342, and `demonstrations-suite` failed in 46.1s; `outcome red`, failures `command-failure, test-failure`, base f22de680, 14 changed paths. It is reported here and read as nothing. The Exit's proof clause is therefore met by nothing this round observed. The operator's evidence for `f4f0d4b8` is the 2445-test 12-shard Hexaemeron run and a greenlight root suite of 1623 tests at LAST_GREEN tree 6e394b357a566c02589bba7ad89810d61b9b0a7a, and neither is the command the clause names nor produces the word `outcome green`; the only run of that command touching this step is the straddled one above. The clause is unproven at `f4f0d4b8` and at the audit head alike, and the command is owed against the audit head after this commit, where this round's own greenlight covers the root suite and nothing wider. Elenchus was run under the runbook's exact contract, and the per-case measurements beside it were taken by extracting each tree with `git archive` and running each case alone, which is a different instrument from the one whose four-state verdict is recorded above. phylax and ephoros exit 0 over the four changed Python paths, which covers only their eight and five parser rules and no byte of JSON or Markdown. hypomnema's ordinary walk over docs and plugins exits 0; adding audit exits 1 with exactly two H008, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, which is the pre-existing defect skills#1451 records and is untouched by this step. Study mode was not run, because step 3 ships no study. The `-F` typed-parameter reading below was not exercised: the fake `gh` ignores `-f` and `-F` alike, so what gh sends for an all-digit owner or repository name is read from its documented behaviour and not observed.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17536-17546 with :17585-17612 | `filing_decision_divergence` compared `fiat_required`, `sha256`, `updated_at` and `edit_count`, and printed every divergence under `the filing decision has moved since this run read it`. `ISSUE_EDITS_QUERY`'s own docstring at :5115-5127 gives the reason this reader asks GraphQL at all: `userContentEdits` is the only surface that says whether the body changed, as opposed to `updated_at`, which a comment or a label also moves, at :5122. The comparison used that field and dropped `last_edited_at`, which the same re-read already computes and which only a body edit moves. Observed against the stubbed API: body byte-identical at sha256 6bfabe87fbe38e667cdb0d10273b7a7cbe993325f0a34404bf1e436a4b6efbc0, edit history identical at `edit_count` 1 and `last_edited_at` 2026-09-06T10:08:38Z, `updated_at` moved to 2026-09-07T11:00:00Z alone; verify printed `the filing decision has moved since this run read it` and exited 1. Naming an undiscriminated read as a body edit is what `window-undiscriminated-read` refuses at `init`, arriving on a surface that line does not name. Repaired: `last_edited_at` joins the compared tuple at :17540-17544; `UNDISCRIMINATED_DIVERGENCE_FIELDS` at :17549 holds `updated_at` alone; an `updated_at`-only delta now reads `the issue has been touched since this run read it, and nothing that records the body has moved`, and that row carries `(a comment or a label moves this too, so on its own it is not a body edit)`. Exit 1 is unchanged, because the recorded value did diverge. | fixed in this round's commit |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5200-5210 | The GraphQL response was parsed under `except (UnicodeDecodeError, ValueError)`. `RecursionError` derives from `RuntimeError` and not from `ValueError`, so a deeply nested array escaped the guard the same function writes for every other malformed response and aborted the controller with a traceback instead of returning `unknown` with a reason. Observed: 400000 bytes of `[` and `]` gives `RecursionError: Stack overflow (used 16352 kB) while decoding a JSON array from a unicode string`, and 400000 sits well inside the 2097152-byte `GIT_OUTPUT_MAX` cap, so the cap is not what bounds this. `init` makes the read before its first mutation, so a crash left no state naming a decision it had not read; what failed is the Exit's `recorded as unknown with its reason, never omitted`, not fail-closure. Repaired at :5202 by adding `RecursionError` to that clause. | fixed in this round's commit |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5219-5232 | `isinstance(total, int)` accepted `True`, because `bool` subclasses `int`, and `True > ISSUE_EDIT_NODES_MAX` is `False`, so the ceiling did not catch it either. `"edit_count": true` was written into `.hexaemeron/state.json`. `True != 1` is `False`, so in `filing_decision_divergence` a recorded `true` compared equal to a real count of 1 and verify printed `the filing decision stands as recorded` and exited 0 over a moved count. Both halves observed against the stubbed API. Repaired at :5227 by rejecting `bool`, so such a response falls to `the GraphQL response did not carry an edit history`, which is the reject-rather-than-coerce shape the rest of the reader already uses. | fixed in this round's commit |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2702 | The comment above the two pre-mutation reads still said a `0` verdict `must cost the operator nothing but the read`, singular, which step 3 stopped being true. `read_task_issue_contract` builds the provenance block at :5093-5109 before `cmd_init` routes the value at :2714, so a filed `0` now pays for a GraphQL request whose answer `routed_filing_directive` never carries and can wait `GIT_TIMEOUT` twice rather than once. Observed: `init` on a `Fiat-Required: 0` issue makes both calls, and the directive carries twelve fixed keys and no provenance. Building the block unconditionally is the right design, because routing on the value inside the reader would put the decision about a `0` in two places, which the note at :5086-5089 refuses; the comment was the thing that was wrong, so the comment is what changed. | fixed in this round's commit |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:103-122 | `test_prior_body_text_reaches_no_recorded_surface` names four surfaces, stdout, stderr, `state.json` and `ledger.jsonl`, and drives one path, `init` on a `Fiat-Required: 1`. Two further places hold a prior body and format output from it, and neither carried an assertion: `routed_filing_directive` at :5268, whose directive an agent consumes on the `0` route, and the divergence printer at :17585-17612. Swept rather than enumerated: a marker planted in the prior revision appears in no byte of any file the run leaves, on either route and after `verify --check-filing-decision`. The complete set `init` leaves is `.hexaemeron/{.gitignore,lock,worktree}` in the calling checkout and `.hexaemeron/{.gitignore,ledger.jsonl,state.json}` in the run worktree, so the four named surfaces are every one that can carry issue text, plus both streams; the lock and the breadcrumb hold a pid and a path. This is a guard gap and not a live leak. Repaired by two cases covering the `0` route's directive and the divergence report; both pass on the unfixed parent by construction, which is what a coverage extension does. | fixed in this round's commit |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3's third and fourth amendments, Files | Files names eight paths and `f4f0d4b8` edits eight, but not the same eight. `.horos/boundary.json` is edited and not named; `plugins/hexaemeron/tests/test_hexctl.py` is named and not edited, its net change being zero because the fourth amendment moved the seven cases out of it before the commit was made, leaving the file at 259222 bytes, which is the figure that amendment states. `boundary.json` moves in the step commit itself rather than in the audit loop's own output, which is the distinction S2-R1-06 used to hold `.horos/candidates.json` unowed, so it is owed here. Not repaired: a runbook amendment is controller-pinned and the brief reserves `hexctl amend` to the orchestrator. | open |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract is `elenchus.py --ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json`, and it names no `--timeout`. `elenchus.py:275` defaults that to 900 seconds and :327-331 returns `inconclusive` with `the run did not finish inside 900s` when the command exceeds it. The suite the contract names does not finish inside it on this repository: `run_checks` timed `hexaemeron-suite` at 1297.9s in the run described above, and this round's contract run took 904 seconds of wall clock, 2026-09-07T15:36:36Z to 15:51:40Z, exiting 0 with exactly that detail and an empty report. The contract can therefore return no verdict about a guard for any commit that changes a test file; step 1 round 1's `unguarded` came from :278-280, which answers before the command runs at all. A second reason stands behind the clock: `classify` at :231-238 returns `inconclusive` whenever `errors` exceeds zero, tested before assertion failures, and this round's six new cases give three assertion failures, one error and two passes on the parent. A second run of the same contract with `--timeout 2400`, started at 2026-09-07T15:53:23Z, was killed before it reported, so that second reason is read from the source and from the per-case counts and was not observed end to end. Not repaired: the runner contract is part of the runbook's Tests clause and a runbook amendment is controller-pinned. | open |<br><br>Leads not pursued: `gh api graphql -F owner=... -F name=...` sends a typed parameter, so an all-digit owner or repository name is sent as an integer against `$owner:String!` and `$name:String!` and the read returns `unknown` with `the GraphQL response carried errors` for the life of that run. It degrades exactly as the register requires, `-f` for both would be the repair, and the fake `gh` types nothing, so a fix here would ship unguarded; recorded rather than applied. `ISSUE_EDIT_NODES_MAX` bounds one thing and the docstring claims only that thing: a `totalCount` above 20 is refused rather than truncated into a smaller count than the issue has, observed at 21. It does not bound `len(nodes)`; `GIT_OUTPUT_MAX` does, and a 3200145-byte 50000-node response only parsed here because the probe called the reader directly and bypassed `bounded_probe`. The step's own case `test_a_readable_history_records_the_count_time_and_prior_value` pins `totalCount` 4 against two nodes, which `first:20` cannot produce from GitHub, so it fixes trusting `totalCount` over the node list without that being the response it names. The argv is pinned and list-shaped with `shell=False`, a 30-second `GIT_TIMEOUT` and the 2 MiB cap, and `GITHUB_ISSUE_RE` at :13156 confines the repository to `[A-Za-z0-9_.-]` and the number to `[1-9][0-9]*`, which closes gh's `@file` reading of a `-F` value; each of those was read at its source and the argv was observed in the transport log. The `reason` field carries one string for three separate unknowns, so a `last_edited_at` that is null because `editedAt` was missing is explained by whatever the prior revision did, and a null `last_edited_at` with a null reason is reachable; not raised, because the Exit's `never omitted` binds the unreachable case and that case fills every field. `stale-claim-elsewhere` still omits `fiat/SKILL.md` from the register's scope column, which skills#1454 records and step 5 closes; noted, not re-raised, and `AGENTS.md`, `docs/decisions/` and both `SKILL.md` sites are byte-identical from `da8d5326` through this commit. `docs/route-a-zero-decision-runbook.md:524` carries a Brevitas B022 on a hard wrap putting `reading` at a line start; it is the documented false positive skills#1338, Brevitas exits 0, and the bytes sit inside an amendment block. The seven cases the step adds are red at entry tree `da8d5326`, seven of seven, but each on the same `KeyError: 'provenance'` raised by the shared `start()` helper before any assertion runs, so the redness is one observation repeated seven times rather than seven independent ones, and it is an error rather than an assertion failure in every case. This round's own prose gates: Imprimatur scores 100.0 out of 100 with 0 defects. Brevitas reports B010 and B001 and exits 1, both artefacts of the fiat-audit-round/v2 structure, which fixes one heading and puts a long field ahead of the table; they fire identically on the already-receipted rounds above, and B027 does not fire here. `.horos/candidates.json` moves again when this round is appended.
## Step 3, round 2 -- 2026-09-07T21:58:01Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's security_suite field, since the run produces no Solidity and no Foundry or Hardhat project. The GitHub API was not queried; every observation below is against the fixture's fake `gh` in `plugins/hexaemeron/tests/hexctl_harness.py:540`. The `userContentEdits` newest-first ordering is therefore still unverified, exactly as round 1 recorded: the harness returns whatever a case hands it, so no test can discriminate `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 was read from round 1's record rather than re-taken. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was not run this round and is still owed against the audit head; what this round ran is `python3 -m unittest discover -s tests`, 1623 tests, OK in 238.507s at the tree this commit carries. A first attempt at that suite reported one failure, `test_dead_code.ShippedSurfaceTests.test_checked_dead_code_scope_selects_suite_and_live_validator` refusing with `unknown-ownership: changed paths have no declared owner .rootsuite.log`, which was this round's own log file sitting untracked in the worktree; the file was removed and the suite re-run clean, and the failure is reported here as an artefact of the instrument rather than of the diff. phylax and ephoros exit 0 over the two changed Python paths, which covers only their eight and five parser rules and no byte of JSON or Markdown. hypomnema's ordinary walk over `docs plugins` exits 0; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. Two of this round's observations were driven below the command rather than through it: `--observations --check-filing-decision` was exercised with `verify_run`, `load_state` and `verify_observation_bindings` stubbed, because a passing `--observations` needs a bound observation prefix this module does not otherwise build, and the absent-provenance case was driven by calling `filing_decision_divergence` on a constructed state, because no state written by a pre-`f4f0d4b8` controller exists here to run against. Round 1's `-F` typed-parameter reading is still unexercised for the same reason it was then: the fake `gh` ignores `-f` and `-F` alike. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against `118007ec`, 2026-09-07T21:44:08Z to 21:49:38Z, 330 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. Round 1's 904-second timeout does not recur, so the 2400-second replacement is now observed rather than argued. `118007ec` is this round's fixes commit as it stood before the record and its synopsis were written, and it is the object every measurement above was taken against; it was amended into the commit that carries this record, the amend adding only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`. None of the three is a test file, so the changed-test-file set the Elenchus comparison used is identical in the commit that carries this record.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17544 at a972cd1f, :17548-17634 with :17648-17734 after the fix | `filing_decision_divergence` compared every field with one `if was != now` at a972cd1f:17544, and `unknown` is a value that comparison reads. `unknown_filing_provenance` writes that sentinel into `edit_count`, `last_edited_at` and `prior_fiat_required` so a receipt can tell "no edit history" from "could not look", and the comparison spent the distinction in both directions. Four shapes observed against the stubbed API, each with the body byte-identical and the edit history untouched: GraphQL readable at `init` and unreachable at verify prints `edit_count: recorded 1, now unknown` and `last_edited_at: recorded 2026-09-06T10:08:38Z, now unknown` under `the filing decision has moved since this run read it`, exit 1; unreachable at `init` and readable at verify prints the same two rows reversed, same headline, exit 1; unreachable at both compares the two sentinels equal and prints `the filing decision stands as recorded`, exit 0, over an edit history neither end had read, which is the `graphql-transport` line's `an unavailable API recorded as unknown rather than treated as a pass`, in the environment the reader's own docstring calls ordinary -- GraphQL needs a token scope REST does not and `diff` needs write access; and a receipt carrying no `provenance` key, which is every run initialised before `f4f0d4b8`, has the absence turned into `{}` by `as_dict`, so all three provenance fields compare `None` against a real value and an untouched issue reads as three divergences and exit 1. Naming an unread field as a body edit is the reading `window-undiscriminated-read` refuses at `init` and S3-R1-01 removed from `updated_at`, arriving through the transport rather than through the field. Repaired: `FILING_PROVENANCE_UNKNOWN` at :5129; `filing_decision_divergence` at :17548 returns a third list, and a field whose recorded or current side is the sentinel, or whose recorded block is absent, becomes an uncomparable entry at :17618-17630 instead of a divergence; `uncomparable_filing_field` at :17532 names which end failed; `print_uncomparable_filing_fields` at :17726 prints `<field>: not compared, because <why>` beside a divergence as well as instead of one; the line that read `stands as recorded` now reads `stands as recorded on every field this run could compare, and N fields could not be compared`. Exit 1 is unchanged for a real divergence and an uncomparable-only result exits 0, matching the shipped `skipped` branch, so the text and not the exit code carries the claim. `rest_filing_stamps` at :5139 closes the same class on the REST half: `init` coerced a non-string `updated_at` to `None` and the divergence reader did not, so the two builders of one block disagreed about one response; both now go through it. | fixed in this round's commit |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17575 at a972cd1f, :17648-17667 after the fix | `cmd_verify`'s `--observations` branch ended with `return` at a972cd1f:17575, above the `--check-filing-decision` block. `hexctl verify --observations --check-filing-decision` therefore printed `ok: 1 ledger entries, chain intact, state consistent; 1 observation prefix verified` and exited 0 with the filing decision never compared and no line saying it had been dropped. Observed with `verify_run`, `load_state` and `verify_observation_bindings` stubbed: `filing_decision_divergence` is called 0 times. Not observed through the fixture, because with no observation binding the command refuses by name with FOB001 before either branch reports, which is a refusal and not the silent pass. Repaired at :17648-17667: the observation branch records that it reported and falls through, and the unconditional closing line prints only when neither branch did. | fixed in this round's commit |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5244 at a972cd1f, :5264-5276 after the fix | `github_issue_edit_provenance` wrote `the body has one revision, so it has no prior value` at a972cd1f:5244 for every `len(revisions) < 2`, a count it never read. `totalCount` and `nodes` are validated separately and nothing relates them. Observed against the stubbed API: `totalCount` 0 with an empty node list records `edit_count: 0` beside that sentence, about a body the reader saw no revision of; `totalCount` 4 with one node records `edit_count: 4` beside it, so one provenance block asserts four revisions and one revision at once. The step's Goal is `Make a run say what it read`, and this is the receipt saying what it did not. Repaired at :5264-5276: the reason counts the nodes carried, `the response carried 0 revisions, so it carries no prior value` and `the response carried 1 revision, so it carries no prior value`, which cannot contradict `edit_count` because it no longer claims what `edit_count` claims. The existing assertion in `test_no_readable_field_is_ever_absent_rather_than_unknown` moves from `one revision` to `carried 1 revision`. Not repaired: the `totalCount`-against-`len(nodes)` disagreement itself, which round 1 recorded as a lead, which `first:20` under the `ISSUE_EDIT_NODES_MAX` ceiling makes unreachable from a well-behaved API, and which the step's own `test_a_readable_history_records_the_count_time_and_prior_value` pins at totalCount 4 against two nodes. | fixed in this round's commit |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17536-17546 with :17585-17612 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Carried from round 1; `last_edited_at` joins the compared tuple, `UNDISCRIMINATED_DIVERGENCE_FIELDS` holds `updated_at` alone, and the qualifier rides beside that row. Re-checked this round: `test_a_moved_updated_at_alone_is_not_called_a_body_edit` and `test_a_moved_body_still_reads_as_a_moved_decision` both pass at `118007ec`, and the S3-R2-01 repair does not weaken either, because an uncomparable field is classified before the undiscriminated test is reached. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5200-5210 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown` with a reason. Carried from round 1; `RecursionError` is in the except clause and `test_a_deeply_nested_response_records_unknown_rather_than_crashing` passes at `118007ec`. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5219-5232 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was written to `.hexaemeron/state.json` as `"edit_count": true`, and compared equal to a real count of 1. Carried from round 1; `bool` is rejected at the boundary and `test_a_boolean_edit_count_is_refused_rather_than_recorded` passes at `118007ec`. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2702 | The comment above the two pre-mutation reads still said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Carried from round 1; the comment names both reads and why the block is built unconditionally. Re-read at `118007ec` and still true: this round changed no ordering in `cmd_init`. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:103-122 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Carried from round 1; both have cases. Re-checked against this round's new printer: `print_uncomparable_filing_fields` emits a fixed field name and reader-authored text only, and `test_no_prior_body_text_reaches_the_divergence_report` passes at `118007ec`. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Repaired by the fifteenth runbook amendment, whose replacement Files names `hexctl.py`, `hexctl_harness.py`, `test_filing_decision_provenance.py`, `test_issue_filing_contract.py`, `tests/promise_machine_coverage.json`, `.horos/census.json`, `.horos/boundary.json` and `docs/route-a-zero-decision-runbook.md`, which is exactly the set `f4f0d4b8` edits. Verified this round: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, which is the digest the packet's `runbook_step.sha256` carries. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default and returned `inconclusive` reading `the run did not finish inside 900s` for a suite `run_checks` timed at 1297.9s. Repaired by the fifteenth runbook amendment, whose replacement Tests names `test_filing_decision_provenance.py` as the file the cases land in and adds `--timeout 2400`, chosen against that measurement. Verified this round in the same byte-identical runbook, and exercised: this round ran the amended contract end to end. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2, "Did the filing decision change under the run?", with `hexctl verify`, which the third amendment made false: the comparison needs `--check-filing-decision` and plain `verify` makes no request at all. It is the same item 8 claim that amendment's Why deliberately left standing, and it prices the alternative: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. The two files differ only in six rewritten relative links, so the shipped copy is controller-pinned in substance; recorded rather than raised. `AGENTS.md:117-118` still says `hexctl init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, which step 2 made false; `AGENTS.md`, `docs/decisions/` and `plugins/hexaemeron/skills/fiat/SKILL.md` are byte-identical from `da8d5326` through `118007ec`, step 5 owns the `stale-claim-elsewhere` sweep, and skills#1454 records the register scope-column gap. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. `created_at` and `prior_fiat_required` are recorded and never compared: `created_at` cannot move on GitHub, and `prior_fiat_required` can only move when `edit_count` or `last_edited_at` moves, both of which are compared; read and left. The `reason` field still carries one string for three separate unknowns, as round 1 recorded, and this round's uncomparable rows read the sentinel rather than the reason, so a reader still cannot tell which of the three a single reason explains. `verify --check-filing-decision` still refuses outright when REST is unreachable, because `github_rest` calls `github_unreachable`; that is a refusal of the diagnostic rather than a false answer, and it is the house fail-closed shape. The exit code for an uncomparable-only result is 0, which is the trade this repair takes: a caller reading only the exit code still learns nothing about the unread fields, and the alternative would fail a diagnostic that found no divergence in every environment without GraphQL scope. `cmd_record` still refuses `task_issue_contract` at :6045-6052, and `plugins/hexaemeron/skills/fiat/EVOLUTION.md` is byte-identical from `da8d5326` through `118007ec`; both read, neither raised. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings and all eleven moved from ad0b180609923a7b7117f44a06892999535ece31e1ca82cf2febab16b85caaca to 14ba917da7497e9cf0ad63feb54d5e60da59dc54b178bc7fa85f411555fda988 in this commit. `.horos/candidates.json` did not move this round; only `--census --write` was run.
## Step 3, round 3 -- 2026-09-07T22:49:57Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's `security_suite` field, since the run produces no Solidity and no Foundry or Hardhat project. This is a fresh reader on rounds 1 and 2; nothing below is carried from their context, and every carried row was re-checked against the tree rather than copied. The GitHub API was not queried. The `userContentEdits` newest-first ordering is therefore still unverified, as rounds 1 and 2 both recorded: the fake `gh` at `plugins/hexaemeron/tests/hexctl_harness.py:580` returns whatever a case hands it, so no test discriminates `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 is read from round 1's record. Round 1's `-F` typed-parameter reading is still unexercised for the same reason: the fake `gh` ignores `-f` and `-F` alike. Two of this round's three findings were measured by stubbing `bounded_probe` and calling `filing_decision_divergence` and `github_rest` directly, because the fixture's `gh` cannot return a 400000-byte nested array or a 262145-byte body; the third, the non-text body, was driven end to end through the fixture's existing `FAKE_GH_MODE=issue-body-not-text`. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was not run this round and is still owed against the audit head; what this round ran is `python3 -m unittest discover -s tests`, 1623 tests, OK in 318.078s at the staged tree `8918f16596adf2270ace8e0776fa49bf7254bc2e`, which is the tree `58fe36d2` carries. phylax and ephoros exit 0 over `hexctl.py` and `test_filing_decision_provenance.py`, which covers only their eight and five parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against `58fe36d2`, 2026-09-07T22:41:59Z to 22:46:45Z, 286 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. `58fe36d2` is this round's fixes commit as it stood before the record and its synopsis were written; it was amended into the commit that carries this record, the amend adding only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`. None of the three is a test file, so the changed-test-file set the Elenchus comparison used is identical in the commit that carries this record. The four new cases were also run against parent tree `1bf662a7` by `git archive` with only the changed test file overlaid: four failures, zero errors, which is the shape `classify` needs, since it returns `inconclusive` whenever `errors` exceeds zero before assertion failures are read.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17616 with :5025-5057 and :5100 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, so the two builders of one provenance block disagreed about one response -- the mechanism `rest_filing_stamps` was added for in S3-R2-01, left open on the body half. The re-read did `body = body if isinstance(body, str) else ""` where the init reader calls `github_unreachable(label, path, "returned a body that is not text")`, and applied no `ISSUE_BODY_BYTES_MAX` cap where the init reader dies by name. Observed at `1bf662a7` against the fixture's `FAKE_GH_MODE=issue-body-not-text`: `verify --check-filing-decision` exits 1 printing `the filing decision has moved since this run read it:`, `fiat_required: recorded 1, now None` and `sha256: recorded 6bfabe87fbe38e667cdb0d10273b7a7cbe993325f0a34404bf1e436a4b6efbc0, now e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. That second digest is the SHA-256 of the empty string, reported as the issue's current body digest for a response the reader read no body out of, which is the reading `window-undiscriminated-read` refuses at `init` and S3-R1-01 removed from `updated_at`. Observed separately against a stubbed `bounded_probe`: a 262145-byte body returns divergences where `init` dies. A null body is not this case and is unchanged: GitHub sends it for an issue whose body is empty, so `""` is what the response says. Repaired by `admitted_issue_body` at :5025, which is `read_task_issue_contract`'s own three rules extracted, called at :5100 and :17616. Guarded by `test_a_body_that_is_not_text_is_not_a_moved_decision` and `test_a_body_above_the_cap_is_refused_rather_than_hashed`, both failing by assertion at `1bf662a7`. | fixed in this round's commit |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17618 at 1bf662a7, :17625-17636 after the fix | `now_record, _faults = issue_contract_faults(body, label)` computed the current body's faults and dropped them, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`, which is a third thing the issue does not say and one `init` refuses by name. Observed at `1bf662a7` through the fixture: a body declaring `Fiat-Required` at both 1 and 0 prints `fiat_required: recorded 1, now None` beside a real `sha256` divergence. Observed the same for `Fiat-Required: 2` and for the line deleted. Repaired at :17625 by reading `fiat_required_value` for the filing fault alone, not the carryover or status-block faults, and at :17638-17667 by making the field uncomparable rather than a divergence, carrying the fault `init` would have printed, so a body declaring the line twice now reads: fiat_required: not compared, because the issue's body does not declare one readable filing decision -- task issue wildcat-finance/example#74 declares `Fiat-Required` 2 times, so it has made no decision. The fault copies a value out of the body and `FIAT_REQUIRED_LINE_RE` at :98 matches `(?P<value>.*?)` to the line end, so it is passed through `clean` and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 at :5151 before it reaches stdout. `sha256` still diverges and the exit code is still 1. Guarded by `test_an_unreadable_decision_now_is_not_reported_as_no_decision`, failing by assertion at `1bf662a7`. | fixed in this round's commit |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12558 | `github_rest` parsed its response under `except ValueError`. `RecursionError` derives from `RuntimeError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. It is the REST sibling of the GraphQL parser S3-R1-02 repaired at :5253, three functions and one transport apart, and this step gave it a second call site at :17615 inside `filing_decision_divergence`. Observed against a stubbed `bounded_probe`: 400000 bytes of `[` gives `RecursionError: Stack overflow (used 16352 kB) while decoding a JSON array from a unicode string`, and 400000 sits well inside the 2097152-byte `GIT_OUTPUT_MAX`, so the cap is not what bounds this. `init` makes this read before its first mutation, so a crash left no state naming a decision it had not read; what failed is the transport shape, not fail-closure. Repaired at :12558 by adding `RecursionError` to the clause, which routes to the existing `returned a response that is not JSON`. Guarded by `test_a_deeply_nested_rest_response_refuses_rather_than_crashing`, which catches the exception rather than letting it propagate, so the parent result is an assertion failure and not the error `classify` reads as inconclusive. | fixed in this round's commit |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17697 with :17638-17692 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-checked at `58fe36d2`: `UNDISCRIMINATED_DIVERGENCE_FIELDS` holds `updated_at` alone, `last_edited_at` is still the fifth entry in the compared tuple, and `test_a_moved_updated_at_alone_is_not_called_a_body_edit` and `test_a_moved_body_still_reads_as_a_moved_decision` both pass. This round's fifth tuple element does not weaken either: an unreadable field is classified before the undiscriminated test is reached, and `fiat_required` is the only field it is ever set for. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5253 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback. Re-checked at `58fe36d2`: `RecursionError` is in the clause and `test_a_deeply_nested_response_records_unknown_rather_than_crashing` passes. The repair was applied to one of two parsers on the same transport pair, which is S3-R3-03. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5278 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling and compared equal to a real count of 1. Re-checked at `58fe36d2`: `isinstance(total, bool)` rejects at the boundary and `test_a_boolean_edit_count_is_refused_rather_than_recorded` passes. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2706 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at `58fe36d2`: the comment names both reads, the `GIT_TIMEOUT` paid twice, and why the block is built unconditionally. This round changed no ordering in `cmd_init` and added no request; `admitted_issue_body` parses a response `github_rest` had already returned. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:104-123 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at `58fe36d2`: both cases pass, and `test_no_prior_body_text_reaches_the_divergence_report` covers this round's new printer input. The `why` string S3-R3-02 adds carries text from the *current* body, never a prior revision, and reaches stdout `clean`ed and bounded at 200 characters. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight. Re-verified at `58fe36d2`: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, which is the digest the packet's `runbook_step.sha256` carries, and its replacement Files names exactly the set `f4f0d4b8` edits. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, and exercised again: this round's contract run finished in 286 seconds inside the 2400-second replacement, against round 2's 330 and round 1's 904-second timeout. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17638-17692 with :5158 and :5168 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at `58fe36d2`: `FILING_PROVENANCE_UNKNOWN` at :5158, the third returned list, `uncomparable_filing_field` and `print_uncomparable_filing_fields` all stand, `rest_filing_stamps` at :5168 is called by both builders, and `test_an_unreadable_history_now_is_not_called_a_moved_decision`, `test_an_unreadable_history_then_is_not_called_a_moved_decision`, `test_two_unreadable_reads_do_not_read_as_a_history_that_stands` and `test_a_receipt_with_no_provenance_block_is_not_a_moved_decision` all pass. This round extends that machinery rather than replacing it: S3-R3-02's field joins the same uncomparable list. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17716-17734 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-checked at `58fe36d2`: the branch sets `reported` at :17728 and falls through, the closing line is guarded by `if not reported`, and `test_the_observations_flag_does_not_swallow_the_filing_check` passes. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5303-5315 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-checked at `58fe36d2`: the reason counts the nodes carried and `test_the_reason_names_the_revision_count_the_response_carried` passes. The `totalCount`-against-`len(nodes)` disagreement behind it is still unrepaired and still a lead. | fixed in 1bf662a7 |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from round 2 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended. `github_rest` is one of 27 `json.loads` call sites in `hexctl.py` and, after this round, one of two whose guard names `RecursionError`; the other 25 are inherited from the run base and a repository-wide sweep is not this step's Files. A `last_edited_at` recorded as `None` because a node carried no `editedAt` is an unread field compared as a value, which is the S3-R2-01 class one level below the sentinel; round 1 recorded it and this round's repair does not reach it, because the Exit's `never omitted` binds the unreachable case and that case fills every field with the sentinel. `FILING_PROVENANCE_UNKNOWN in (was, now)` is tested for `fiat_required` and `sha256` as well as the three provenance fields, but neither can hold the string `unknown`: `issue_contract_faults` at :4888 writes `None` or `int(value)` for the first and a hex digest for the second, and `FIAT_REQUIRED_VALUES` is `("0", "1")`. Read and left. The `reason` field still carries one string for three separate unknowns, so a reader cannot tell which of the three a single reason explains; recorded in rounds 1 and 2 and unchanged. `init`'s own contract-fault refusal at :5102-5108 echoes an uncleaned `Fiat-Required` value to stderr, which this round's stdout path now bounds and cleans; it is inherited from the run base rather than added by this step, and stderr on a refusal the operator's own issue caused is a narrower surface than the diagnostic's stdout. `--filing-decision-override` still appears 0 times in `hexctl.py`, and none of this round's new emitted bytes names a mechanism that grants this run: the strings added are `not compared, because ...` and `the issue's body does not declare one readable filing decision`. `cmd_record` still refuses `task_issue_contract` at :6105-6112, and no write path was touched, so `receipt-forgery` and `partial-write-of-the-receipt` hold as rounds 1 and 2 read them. `plugins/hexaemeron/skills/fiat/EVOLUTION.md`, `AGENTS.md`, `plugins/hexaemeron/skills/fiat/SKILL.md` and `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` are byte-identical to the run base `f22de680` at blobs 91111113, df705057, a96980de and 53eefaa4, so `held-job-drift`, `reviewed-span-drift` and `stale-claim-elsewhere` are unmoved; step 5 owns the sweep and skills#1454 records the register's scope-column gap. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2, and left to step 5. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and still belongs in the run pull request's `## Carried forward`; nothing this round touched `cmd_init`'s exit path. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings and all eleven moved from 14ba917da7497e9cf0ad63feb54d5e60da59dc54b178bc7fa85f411555fda988 to 09f55551f11ee7b50fb7c24108820a52078636780f999a4229397f9469967bb6 in this commit. `plugins/hexaemeron/tests/test_hexctl.py` is unchanged at 259222 bytes, inside its 262144-byte bound, and the four new cases went into `test_filing_decision_provenance.py`, now 23409 bytes. This record's own prose gates: Imprimatur scores 100.0 out of 100 with 0 defects. Brevitas reports B010 and B001 and exits 0, both artefacts of the fiat-audit-round/v2 structure, which fixes one heading and puts a long field ahead of the table; they fire identically on the already-receipted rounds above, and neither B027 nor B022 fires here. `.horos/candidates.json` did not move this round; only `--census --write` was run.
## Step 3, round 4 -- 2026-09-12T15:16:13Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's `security_suite` field, since the run produces no Solidity and no Foundry or Hardhat project. This record is committed after two of the three fixes it describes, which reverses the order rounds 1 to 3 used. `5bbdf212` and `f92ddbf7` were made by two earlier sessions that ended before writing a record: the log ended at `## Step 3, round 3` and the controller still counted three rounds when this round began. This reader re-checked both commits against the tree rather than carrying their claims forward, found and repaired S3-R4-04, and appends the record afterwards. The severities on S3-R4-01, S3-R4-02 and S3-R4-03 are this round's, assigned against the bands rounds 1 to 3 used, because neither commit message states one. The failing observations those two commits record were not re-taken; what this round took is the repaired behaviour at `76eb1288` and the guard cases. This is a fresh reader on rounds 1 to 3, nothing below is carried from their context, and every carried row was re-checked against the tree rather than copied. The GitHub API was not queried. The `userContentEdits` newest-first ordering is therefore still unverified, as rounds 1 to 3 all recorded: the fake `gh` at `plugins/hexaemeron/tests/hexctl_harness.py:550` returns whatever a case hands it, so no test discriminates `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 is read from round 1's record. Round 1's `-F` typed-parameter reading is still unexercised for the same reason: the fake `gh` ignores `-f` and `-F` alike. Two instruments produced the observations below. The 28 cases in `plugins/hexaemeron/tests/test_filing_decision_provenance.py` ran through the fixture, OK in 52.762s. Every provenance-block value quoted for S3-R4-01 to S3-R4-04 was taken by calling `github_issue_edit_provenance`, `rest_filing_stamps` and `uncomparable_filing_field` directly with `bounded_probe` stubbed, because the fixture's `gh` cannot deliver a `totalCount` that disagrees with its own node list. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was not run this round and is still owed against the audit head; what this round ran is `python3 -m unittest discover -s tests`, 1623 tests, OK in 193.914s, at staged tree `eab786638cab9a4611528108d4fee01039094c3a`, which is the tree `76eb1288` carries. phylax and ephoros exit 0 over `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and `plugins/hexaemeron/tests/test_filing_decision_provenance.py`, which covers only their eight and five parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. Of the design record's two pending conformance criteria, `reviewed-span-unchanged` was settled offline by running its own declared resolver, which prints True at `76eb1288`; `guard-test-fails-without-the-fix` is the Elenchus verdict above. Both block integration rather than this step, and `.hexaemeron/design-evidence.json` was read at sha256 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a and not written to. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against `76eb1288`, 2026-09-12T15:09:00Z to 15:14:15Z, 315 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. `76eb1288` is this round's fixes commit as it stood before this record and its synopsis were written; it was amended into the commit that carries them, the amend adding only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`. None of the three is a test file, so the changed-test-file set the comparison used is identical in the commit that carries this record. No Elenchus verdict exists for `5bbdf212` or `f92ddbf7`: the sessions that made them ended first, and this round did not re-run the contract against either.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R4-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5342-5352 | `github_issue_edit_provenance` took `last_edited_at` from the newest node's `editedAt` and wrote `None` when it was absent or not text, so a field the reader could not read entered the one compared field that carries the strong headline on its own: an issue with a byte-identical body and an unchanged `edit_count` reported `the filing decision has moved since this run read it` on `last_edited_at: recorded None, now 2026-09-06T10:08:38Z`, exit 1, observed in both directions. `last_edited_at` is not in `UNDISCRIMINATED_DIVERGENCE_FIELDS`. The sibling field on the same node, `diff`, already took the sentinel when withheld. Repaired at :5342-5352 as three states: `None` for a response carrying no revision at all, because that was read, and `FILING_PROVENANCE_UNKNOWN` with a reason for a newest node whose `editedAt` is unreadable. `reason` becomes a joined list at :5417, because one block can carry two unread fields. Re-checked at `76eb1288` against a stubbed `bounded_probe`: a two-node response whose newest node carries no `editedAt` records `last_edited_at: unknown` beside `edit_count: 2` and `prior_fiat_required: 0`; a one-node response whose `editedAt` is `17` records `unknown` and joins two reasons; a zero-revision response still records `None`. `test_an_unreadable_edit_time_is_not_called_a_moved_decision` passes. The failing observation is `5bbdf212`'s own record and was not re-taken. | fixed in 5bbdf212 |<br>| S3-R4-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5198-5204 | `rest_filing_stamps` coerced a non-string `created_at` or `updated_at` to `None`. Both builders agreed, which is what S3-R2-01 asked of it, and the comparison then read the absence as data: a run whose `init` got `"updated_at": 17` printed `the issue has been touched since this run read it` over an issue nothing had touched, exit 1. Low rather than medium because `updated_at` is the sole member of `UNDISCRIMINATED_DIVERGENCE_FIELDS`, so the false report carries the weak headline and its qualifier rather than a body-edit claim. Repaired at :5198-5204: an unread stamp takes `FILING_PROVENANCE_UNKNOWN`, which the comparison already declines to read as data. Re-checked at `76eb1288`: `{"updated_at": 17}` records `unknown`, an absent stamp records `unknown`, and two strings pass through unchanged. `test_an_unread_rest_stamp_is_not_called_a_touched_issue` passes. The failing observation is `5bbdf212`'s own record and was not re-taken. | fixed in 5bbdf212 |<br>| S3-R4-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5327-5331 | `totalCount` and `nodes` were validated separately and never against each other, so one response answered how many revisions the body has twice and was believed both times. A response claiming 3 revisions and carrying none recorded `edit_count: 3` beside `last_edited_at: None`, and that `None` is the reader's word for read-and-no-last-edit-time, which the response did not say: against the same three revisions delivered, `verify --check-filing-decision` printed `the filing decision has moved since this run read it` on `last_edited_at: recorded None, now 2026-09-06T10:08:38Z` and exited 1 over a body whose digest, decision, `updated_at` and `edit_count` were all identical, observed in both directions. `first:` equals `ISSUE_EDIT_NODES_MAX` and a larger `totalCount` is already refused at :5303, so a well-formed response carries exactly `total` nodes. Repaired at :5327-5331: any disagreement makes the whole block `unknown` with a reason naming both numbers, which also refuses a negative `totalCount` that passed the type check and the ceiling and recorded `edit_count: -3`. Re-checked at `76eb1288` against a stubbed `bounded_probe`: `totalCount` 3 with no nodes, `totalCount` -3, and `totalCount` 2 with one node each return every field `unknown` under `the GraphQL response claimed N revisions and carried M, so its edit history was not read`. Two existing cases that declared a `totalCount` against a shorter node list now carry counts that agree. `test_a_count_without_its_nodes_is_not_a_history_that_was_read` and `test_a_count_and_a_node_list_that_disagree_are_never_read_as_values` pass. The failing observation is `f92ddbf7`'s own record and was not re-taken. | fixed in f92ddbf7 |<br>| S3-R4-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5391-5409 with :4856 | `fiat_required_value` at :4873 returns `None` for three different prior bodies -- one declaring no `Fiat-Required` line, one declaring it more than once, and one declaring a value that is neither 0 nor 1 -- and `github_issue_edit_provenance` recorded `prior_fiat_required: None` for all three beside the one sentence `the prior revision declared no Fiat-Required line`. `None` is this reader's word for read-and-there-is-no-prior-value, so the last two named a body the reader could not read a decision out of as an absence, and the sentence beside them was false. Observed at `f92ddbf7` against a stubbed `bounded_probe`: a prior revision declaring the line twice, and one declaring `Fiat-Required: 7`, each record `prior_fiat_required: None` under that sentence, byte-identical to what a prior revision declaring nothing records, while `fiat_required_value` on the same two bodies returns `declares Fiat-Required 2 times, so it has made no decision` and `declares Fiat-Required: 7, which is neither 1 nor 0`. It is S3-R4-01's reading reached through the prior revision rather than through `editedAt`, and S3-R3-02's reached on the prior body rather than the current one. Low, not medium: `prior_fiat_required` is recorded and never compared, so no headline and no exit code moves; what moves is the receipt's account of what it read, against the step's Goal. Repaired at :5391-5409 -- `None` only for a body declaring no line, `FILING_PROVENANCE_UNKNOWN` for the other two -- with `fiat_required_declarations` at :4856 extracting the parse both readers now share, for the reason `admitted_issue_body` and `rest_filing_stamps` were extracted. The reason names the shape and quotes nothing, because `init`'s own fault at :4893-4897 copies the declared value out of the body and a prior body reaches no recorded surface. Guarded by `test_an_unreadable_prior_decision_is_not_recorded_as_no_decision`, failing by assertion at `f92ddbf7` with `AssertionError: None != 'unknown'` and zero errors, and covering the `None` half as a regression guard. | fixed in this round's commit |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17790 with :17677-17789 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-checked at `76eb1288`: `UNDISCRIMINATED_DIVERGENCE_FIELDS` holds `updated_at` alone, `last_edited_at` is still compared beside it, and `test_a_moved_updated_at_alone_is_not_called_a_body_edit` and `test_a_moved_body_still_reads_as_a_moved_decision` both pass. This round's three repairs feed that comparison the sentinel rather than an absence, and an uncomparable field is classified at :17780 before the undiscriminated test is reached, so neither is weakened. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5276 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown`. Re-checked at `76eb1288`: the clause reads `except (UnicodeDecodeError, ValueError, RecursionError)` and `test_a_deeply_nested_response_records_unknown_rather_than_crashing` passes. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5301 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was recorded as `"edit_count": true`, and compared equal to a real count of 1. Re-checked at `76eb1288`: `isinstance(total, bool)` rejects at the boundary and `test_a_boolean_edit_count_is_refused_rather_than_recorded` passes. S3-R4-03's `len(nodes) != total` rule at :5327 stands behind it and refuses the same response a second way, since `True` cannot equal a list length of 2 or more. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2705 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at `76eb1288`: the comment names both reads, the `GIT_TIMEOUT` paid twice, and why the block is built unconditionally. Neither round-4 commit nor `76eb1288` changed any ordering in `cmd_init` or added a request; all three edit readers `github_rest` and `bounded_probe` had already returned from. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:107-126 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at `76eb1288`: both cases pass, and the three reason strings this round adds carry integers and reader-authored text alone -- `the GraphQL response claimed N revisions and carried M`, `the newest revision carried no readable editedAt`, `the prior revision declared Fiat-Required N times` and `a Fiat-Required value that is neither 0 nor 1` -- so no new byte of a prior body can reach a recorded surface. S3-R4-04's repair declines the one fault that would have carried body text. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Re-verified at `76eb1288`: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, which is the digest the packet's `runbook_step.sha256` carries, and its replacement Files names every path this round writes. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, and exercised again: this round ran the amended contract end to end, against round 3's 286 seconds, round 2's 330 and round 1's 904-second timeout. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17677-17789 with :5171 and :5181 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at `76eb1288`: `FILING_PROVENANCE_UNKNOWN` at :5171, the third returned list, `uncomparable_filing_field` at :17661 and `print_uncomparable_filing_fields` at :17881 all stand, `rest_filing_stamps` at :5181 is called by both builders, and `test_an_unreadable_history_now_is_not_called_a_moved_decision`, `test_an_unreadable_history_then_is_not_called_a_moved_decision`, `test_two_unreadable_reads_do_not_read_as_a_history_that_stands` and `test_a_receipt_with_no_provenance_block_is_not_a_moved_decision` all pass. Round 4 extends this machinery rather than replacing it: every field its three repairs could not read now takes the sentinel this repair taught the comparison to decline. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17821 with :17877 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-checked at `76eb1288`: the branch sets `reported` at :17821 and falls through, the closing line is guarded by `if not reported` at :17877, and `test_the_observations_flag_does_not_swallow_the_filing_check` passes. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5361-5365 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-checked at `76eb1288`: the reason counts the nodes carried and `test_the_reason_names_the_revision_count_the_response_carried` passes on both halves. The `totalCount`-against-`len(nodes)` disagreement this repair deliberately left standing, and which rounds 1 and 2 both recorded as a lead, is closed by S3-R4-03; the empty-list half remains the discriminating input, since `first:20` under the ceiling now makes the other half unreachable. | fixed in 1bf662a7 |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5038 called at :5113 and :17709 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, substituting `""` for a body that is not text and applying no `ISSUE_BODY_BYTES_MAX` cap, so verify reported the SHA-256 of the empty string as the issue's current body digest under the moved-decision headline. Re-checked at `76eb1288`: `admitted_issue_body` at :5038 is the single rule and both call sites use it, and `test_a_body_that_is_not_text_is_not_a_moved_decision` and `test_a_body_above_the_cap_is_refused_rather_than_hashed` pass. | fixed in fca9bd17 |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17718-17730 | `now_record, _faults = issue_contract_faults(body, label)` dropped the current body's faults, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`. Re-checked at `76eb1288`: `fiat_required_value` is read for the filing fault alone at :17718, the field becomes uncomparable carrying the fault `init` would have printed, the fault is `clean`ed and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 at :5164 before it reaches stdout, and `test_an_unreadable_decision_now_is_not_reported_as_no_decision` passes. S3-R4-04 is this same defect on the prior body, where the fault must not be carried at all. | fixed in fca9bd17 |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12651 | `github_rest` parsed its response under `except ValueError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. Re-checked at `76eb1288`: the clause reads `except (ValueError, RecursionError)` and `test_a_deeply_nested_rest_response_refuses_rather_than_crashing` passes, catching the exception so the parent result is an assertion failure rather than the error `classify` reads as inconclusive. | fixed in fca9bd17 |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from rounds 2 and 3 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended; `.hexaemeron/study.md` is byte-identical at sha256 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524, the digest the packet's `risk_register.sha256` carries. The class this step keeps returning to is enumerated rather than spot-checked. Step 3 adds or touches ten reader sites and one comparison site. `admitted_issue_body` at :5038 refuses a non-text body and dies above the cap, and never coerces. `rest_filing_stamps` at :5181 sends an unread stamp to the sentinel. `unknown_filing_provenance` at :5207 fills every field with the sentinel and omits none. Inside `github_issue_edit_provenance` at :5223, seven sub-sites take the whole block to the sentinel -- an identity GraphQL cannot be asked about, a transport failure, a parse failure including `RecursionError`, a response carrying `errors`, a `totalCount` that is not an int or is a `bool` beside a `nodes` that is not a list, a `totalCount` above `ISSUE_EDIT_NODES_MAX`, and a `totalCount` that disagrees with the node list -- and three field-level sites apply the same rule: `editedAt` unreadable takes the sentinel and a response carrying no revision takes `None`, an unreadable prior `diff` takes the sentinel, and, after this round, a prior body whose decision cannot be read takes the sentinel while one declaring no line takes `None`. `github_rest` at :12622 refuses in the transport shape. The one comparison site, `filing_decision_divergence` at :17677, applies four rules in order: an unreadable current filing decision is uncomparable, a receipt with no provenance block is uncomparable, either side holding the sentinel is uncomparable, and only then is a difference a divergence. Every site now applies the rule, so the class is closed by enumeration rather than by absence of a further finding, and every field that is compared applies it at both ends. Two residues of that rule are named next. The sentinel is the literal string `unknown`, so a genuinely read `updated_at` or `last_edited_at` carrying that exact text is indistinguishable from a field the reader could not read: observed at `76eb1288`, `rest_filing_stamps` records `unknown` for `{"updated_at": "unknown"}` and `uncomparable_filing_field` then reports `this run recorded it as unknown, so it read no value to compare`. Round 3 examined this collision for `fiat_required` and `sha256` and found neither can hold the string; the three provenance fields are free-form API text and can. It hides no body edit, because `sha256` and `edit_count` still compare, and GitHub sends ISO timestamps, so the input needs a `gh` or an API that does not. And `recorded_provenance` at :17696 tests that the recorded block is present while `was_provenance.get(...)` reads a field out of it, so a block present but missing one key would compare `None` against a real value -- the shape S3-R2-01 closed for the absent block. No controller version writes a partial block: `rest_filing_stamps` always returns two keys and `github_issue_edit_provenance` always returns four, unchanged since `f4f0d4b8`, so reaching it needs a hand-edited or foreign-written `state.json`. Both read and left. `admitted_issue_body`'s `ISSUE_BODY_BYTES_MAX` cap governs the current body only: a prior revision's `diff` is parsed by `fiat_required_value` at whatever length the response carries, bounded by `GIT_OUTPUT_MAX` at 2097152 rather than 262144. It records no body text and the parse is bounded, so it is a cost rather than a leak. Divergence rows print `clean(str(entry['recorded']))` with no length bound, where the unreadable-decision fault beside them is bounded at 200 by `UNREADABLE_DECISION_DETAIL_MAX`; the values there are API-generated stamps rather than filer-authored text, so the quarter-megabyte case that bound exists for does not arise from an issue body. A `totalCount` cannot break the reason that quotes it: `json.loads` builds it with `int()`, which refuses above 4300 digits in this interpreter, and `str()` refuses at the same limit, so any count that parses can be formatted at :5308-5311. The `reason` field still carries one joined string for what are now up to two unread fields, and neither the comparison nor the printer reads it, so `verify` output shows the sentinel and not the reason; recorded in rounds 1 to 3, and the join narrows it without closing it. `created_at` and `prior_fiat_required` are recorded and never compared: `created_at` cannot move on GitHub, and `prior_fiat_required` moves only when `edit_count` or `last_edited_at` moves, both of which are compared. S3-R4-04 lands entirely inside that gap, which is why it is low. `github_rest` and the GraphQL parser are two of 27 `json.loads` call sites in `hexctl.py` and the only two whose guard names `RecursionError`; the other 25 are inherited from the run base and a repository-wide sweep is not this step's Files. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. `--filing-decision-override` appears 0 times in `hexctl.py`, and none of the bytes this round adds names a mechanism that grants this run: the new strings are `the GraphQL response claimed N revisions and carried M, so its edit history was not read`, `the newest revision carried no readable editedAt`, `the prior revision declared Fiat-Required N times, so it made no decision` and `the prior revision declared a Fiat-Required value that is neither 0 nor 1`, and each carries integers and reader-authored text alone. `cmd_record` still refuses `task_issue_contract` at :6198-6205, and `routed_filing_directive` and every state write path are byte-identical to `f4f0d4b8` across all four rounds, so `receipt-forgery`, `routed-exit-mistaken-for-success` and `partial-write-of-the-receipt` hold as rounds 1 to 3 read them. `plugins/hexaemeron/skills/fiat/EVOLUTION.md` at blob 91111113, `AGENTS.md` at df705057, `plugins/hexaemeron/skills/fiat/SKILL.md` at a96980de, `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` at 53eefaa4 and `docs/decisions/ADR-078-echo-fiat-required-as-a-filer-set-label.md` are all byte-identical to the run base `f22de680`, so `held-job-drift`, `reviewed-span-drift` and `stale-claim-elsewhere` are unmoved. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2, and left to step 5; skills#1454 records the register's scope-column gap. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and belongs in the run pull request's `## Carried forward`; nothing this round touched `cmd_init`'s exit path. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings, and all eleven moved from 04f7d4bbb39615c815343f2fcb6192e3c25f29e4107dcca16490142edcfd9d4f to eea78aded1a96ca20a679e8d3b17975d99be9b3fd142db81ed56c617e8fec879 in `f92ddbf7` and from there to 9410b04a1a2d182bc3370b1c446797884465f90b1cf4b76c2367a7689612cc84 in this commit. `plugins/hexaemeron/tests/test_hexctl.py` is unchanged at 259222 bytes, inside its 262144-byte bound, and `test_filing_decision_provenance.py` is now 32688 bytes. This record's own prose gates, run on the draft before it was appended: Imprimatur scores 100.0 out of 100 with 0 defects, and reports two rule-of-three signals and four repeated openers as known false positives. Brevitas reports B010, B001 and B027 and exits 0. B010 and B001 are artefacts of the fiat-audit-round/v2 structure, which fixes one heading and puts a long field ahead of the table, and fire identically on the already-receipted rounds above; B027 fires once on the leads paragraph, on a claim carrying two scope-bounding qualifiers that are kept, where rounds 1 and 3 recorded it as not firing. B022 does not fire here. `.horos/candidates.json` did not move this round; only `--census --write` was run.
## Step 3, round 5 -- 2026-09-12T15:50:22Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: The Solidity suite is waived by the packet's `security_suite`, since the run produces no Solidity and no Foundry or Hardhat project. This is a fresh reader on rounds 1 to 4. Nothing below is carried from their context, and all seventeen rows were re-checked against the tree at `f50298fb` rather than copied; every `file:line` those rows cite was resolved in the current file. No new finding was raised, so this round writes no fix and the Elenchus verdict above is `null` rather than a result. The commit carrying this record adds only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`; `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` stays at sha256 9410b04a1a2d182bc3370b1c446797884465f90b1cf4b76c2367a7689612cc84, which all eleven `tests/promise_machine_coverage.json` bindings still carry, `plugins/hexaemeron/tests/test_hexctl.py` at 259222 bytes inside its 262144-byte bound and `test_filing_decision_provenance.py` at 32688 bytes. The GitHub API was not queried. The `userContentEdits` newest-first ordering is still unverified, as rounds 1 to 4 all recorded: the fake `gh` branches on `args[:2] == ["api", "graphql"]` at `plugins/hexaemeron/tests/hexctl_harness.py:580` and returns whatever `FAKE_GH_EDITS` hands it at :587, so no test discriminates `revisions[1]` from the revision before it, and round 1's `-F` typed-parameter reading stays unexercised because that branch reads neither `-f` nor `-F`. Three instruments produced the observations below. The 28 cases in `plugins/hexaemeron/tests/test_filing_decision_provenance.py` ran through the fixture, OK in 43.333s. The root suite `python3 -m unittest discover -s tests` was selected and passed inside `run_checks` as the check `root-suite`, and is re-run at the staged tree this record's commit carries before that commit is made. Every value quoted for the two residues was taken by calling `rest_filing_stamps`, `uncomparable_filing_field`, `unknown_filing_provenance` and `filing_decision_divergence` directly with `github_rest` and `github_issue_edit_provenance` stubbed, because the fixture's `gh` cannot deliver a provenance block whose keys disagree with what the builders return. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was owed against the audit head after round 4 and is settled here. The run at `f50298fb` on a clean tree, 2026-09-12T15:34:17Z to 15:42:38Z, 501 seconds of wall clock and exit 0, reports `outcome green` against base f22de68086ad7265869636903554d09cf751e765 over 14 changed paths. It selected 13 checks -- dead-code-suite, dead-code-suppressions-check, demonstrations-suite, front-door-check, harness-roster-check, harness-roster-freshness, hexaemeron-suite, imprimatur-suite, joined-front-door-suite, lint-ephoros, lint-hypomnema, lint-phylax and root-suite -- every one passed, and `failure_classes` is empty. phylax and ephoros exit 0 over `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `plugins/hexaemeron/tests/hexctl_harness.py`, `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `plugins/hexaemeron/tests/test_issue_filing_contract.py`, which is every Python path the step writes and two more than round 4 read, and covers only their eight and five parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0. Study mode was not run, because step 3 ships no study. Of the design record's two pending conformance criteria, neither was re-settled here: `reviewed-span-unchanged` was settled offline in round 4 and `guard-test-fails-without-the-fix` has no new guard to classify. Both block integration rather than this step, and `.hexaemeron/design-evidence.json` was read at sha256 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a and not written to. `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, the digest the packet's `runbook_step.sha256` carries. `.hexaemeron/study.md` is byte-identical at sha256 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524, the digest the packet's `risk_register.sha256` carries; `docs/route-a-zero-decision-study.md` at f25ea0c58a01feb09cc97c2d27d96899778e591658b191f85a6ae1f83cb6ae3c differs from it in five relative link paths and in nothing else, and line 230 is identical in both.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17790 with :17677-17789 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-checked at `f50298fb`: `UNDISCRIMINATED_DIVERGENCE_FIELDS` at :17790 holds `updated_at` alone, the note at :17836 attaches to that field alone, and `edit_count`, `last_edited_at` and `sha256` are compared beside it. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5276 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown`. Re-read at `f50298fb`: :5276 reads `except (UnicodeDecodeError, ValueError, RecursionError)`. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5301 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was recorded as `"edit_count": true`, and compared equal to a real count of 1. Re-read at `f50298fb`: `isinstance(total, bool)` rejects at :5301, and S3-R4-03's `len(nodes) != total` at :5327 refuses the same response a second way. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2705 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at `f50298fb`: the comment names both reads and the `GIT_TIMEOUT` paid twice. The only `cmd_init` hunk between `f4f0d4b8` and `f50298fb` is this comment; no ordering, request or state write in `cmd_init` moved. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:107-126 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at `f50298fb`: the three reason strings at :5397-5409 carry integers and reader-authored text alone, `fiat_required_value`'s faults on a prior body are bound to `_faults` at :5391 and discarded, and `routed_filing_directive` at :5421 reads `issue`, `repository`, `number` and `carryover` and never `provenance`. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Re-verified at `f50298fb` in the runbook byte-identical at f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, which carries `--timeout 2400`. Not exercised this round, because this round has no fixes commit to classify. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17677-17789 with :5171 and :5181 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at `f50298fb`: `FILING_PROVENANCE_UNKNOWN` at :5171, the third returned list, `uncomparable_filing_field` at :17661 and `print_uncomparable_filing_fields` at :17881 all stand. Driven directly, a receipt and a re-read both holding the sentinel on `updated_at`, `edit_count` and `last_edited_at` returned three uncomparable fields and zero divergences, and the same pair with a moved body returned the `sha256` divergence beside them. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17821 with :17877 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-read at `f50298fb`: the branch sets `reported` at :17821 and falls through, and the closing line is guarded by `if not reported` at :17877. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5361-5365 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-read at `f50298fb`: the reason counts `len(revisions)`, the nodes carried. The `totalCount`-against-`len(nodes)` disagreement it left standing is closed by S3-R4-03 at :5327, so the empty-list half is the discriminating input. | fixed in 1bf662a7 |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5038 called at :5113 and :17709 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, substituting `""` for a body that is not text and applying no `ISSUE_BODY_BYTES_MAX` cap, so verify reported the SHA-256 of the empty string as the issue's current body digest under the moved-decision headline. Re-read at `f50298fb`: `admitted_issue_body` at :5038 is the single rule and both call sites, :5113 and :17709, use it. | fixed in fca9bd17 |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17718-17730 | `now_record, _faults = issue_contract_faults(body, label)` dropped the current body's faults, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`. Re-read at `f50298fb`: `fiat_required_value` is read for the filing fault alone at :17718, the field becomes uncomparable, and the fault is `clean`ed and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 at :5164 before it reaches stdout. `issue_contract_faults` at :4911 still writes `None` for all three unreadable shapes; this guard is what keeps that `None` out of the comparison. | fixed in fca9bd17 |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12651 | `github_rest` parsed its response under `except ValueError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. Re-read at `f50298fb`: :12651 reads `except (ValueError, RecursionError)`. | fixed in fca9bd17 |<br>| S3-R4-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5342-5352 | `github_issue_edit_provenance` took `last_edited_at` from the newest node's `editedAt` and wrote `None` when it was absent or not text, so a field the reader could not read entered the one compared field that carries the strong headline on its own. Re-read at `f50298fb`: three states stand at :5342-5352, `None` for a response carrying no revision and `FILING_PROVENANCE_UNKNOWN` with a reason for a newest node whose `editedAt` is unreadable, and `reason` is a joined list at :5417. | fixed in 5bbdf212 |<br>| S3-R4-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5198-5204 | `rest_filing_stamps` coerced a non-string `created_at` or `updated_at` to `None`, and the comparison read the absence as data. Re-checked at `f50298fb` by direct call: a payload carrying no `updated_at` records `unknown`, and one carrying the string `unknown` records `unknown`, which is the collision named under the leads below. | fixed in 5bbdf212 |<br>| S3-R4-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5327-5331 | `totalCount` and `nodes` were validated separately and never against each other, so one response answered how many revisions the body has twice and was believed both times. Re-read at `f50298fb`: `len(nodes) != total` at :5327 takes the whole block to the sentinel under a reason naming both numbers, which also refuses a negative `totalCount` that passed the type check and the ceiling. | fixed in f92ddbf7 |<br>| S3-R4-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5391-5409 with :4856 | `fiat_required_value` returns `None` for three different prior bodies, and `github_issue_edit_provenance` recorded `prior_fiat_required: None` for all three beside the one sentence `the prior revision declared no Fiat-Required line`. Re-read at `f50298fb`, which is the commit that carries the repair: `None` only for a body declaring no line at :5395-5399, the sentinel for a body declaring the line more than once at :5400 and for one declaring a value that is neither 0 nor 1 at :5405, with `fiat_required_declarations` at :4856 supplying the shape. `test_an_unreadable_prior_decision_is_not_recorded_as_no_decision` at :575-622 exercises all three branches and asserts the neither-0-nor-1 reason quotes no value out of the body. | fixed in f50298fb |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from rounds 2, 3 and 4 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended. Round 4's enumeration was re-derived from the diff `da8d5326..f50298fb` rather than read, and it is complete for every site where the rule can fail. Three sites the step adds or touches are absent from it, and none of the three can fail. `fiat_required_declarations` at :4856 returns a list, and an empty list is read-and-none rather than a coercion. `fiat_required_value` at :4873 still collapses three shapes to `None`, and every live caller discriminates: `github_issue_edit_provenance` at :5391-5409 by S3-R4-04's repair, `filing_decision_divergence` at :17718-17730 by S3-R3-02's, and `read_task_issue_contract` at :5115 by dying on faults before any `None` is recorded. `read_task_issue_contract` at :5073 records `fiat_required: None` and `sha256: None` for a tracker it never read, at :5098-5107, which is the sentinel rule's shape; those bytes are `c148ab9a`, an ancestor of the run base `f22de680`, they carry a `reason` field and a stderr warning in place of a sentinel, and `filing_decision_divergence` short-circuits at :17703 before reading a field of that receipt, because `github_issue_identity` returns `None` for the same URL. The count in that enumeration does not reconcile with the list it introduces: ten reader sites are claimed, and the paragraph names eight locations, or fourteen counting the seven block-level and three field-level sub-sites inside `github_issue_edit_provenance` separately. The number carries no evidence and the list does, and the list is complete, so this is recorded rather than raised. One invariant the four earlier rounds do not state is what makes the closure hold rather than merely go unfalsified: `sha256` is always in the compared set. `recorded.get("sha256")` and `now_record["sha256"]` are both `hashlib.sha256(text.encode("utf-8")).hexdigest()` outputs from :4914, neither can equal the sentinel, and the only receipt shape whose `sha256` is `None` is the non-GitHub one at :5104, which never reaches the loop. Observed directly: with `updated_at`, `edit_count` and `last_edited_at` unread at both ends, a moved body still returned one divergence on `sha256`, which is not in `UNDISCRIMINATED_DIVERGENCE_FIELDS`, so the strong headline still fires. Round 4's two residues were re-taken and both remain leads. The sentinel is the literal string `unknown`, so a genuinely read `updated_at` or `last_edited_at` carrying that exact text is indistinguishable from a field the reader could not read: observed at `f50298fb`, `rest_filing_stamps({"created_at": "2026-09-01T00:00:00Z", "updated_at": "unknown"})` records `unknown`, and `uncomparable_filing_field` then returns `this run recorded it as unknown, so it read no value to compare`. Every direction of the collision produces an uncomparable field and never a false divergence, the `sha256` invariant above means no body edit can hide behind it, and GitHub sends ISO timestamps, so the input needs a `gh` or an API that does not. And `recorded_provenance` at :17736 tests that the recorded block is present while `was_provenance.get(...)` reads a field out of it. The failure mode was taken rather than reasoned: a block present but missing `updated_at` returns the divergence `updated_at: recorded None, now 2026-09-05T00:00:00Z` over an issue nothing had touched, which is the shape S3-R2-01 closed for the absent block. It stays a lead on two guards. No controller version writes a partial block: `rest_filing_stamps` at :5198-5204 returns exactly `created_at` and `updated_at` from a fixed tuple, `unknown_filing_provenance` at :5215-5220 exactly four keys, and the success return at :5410-5418 exactly four, and the three key sets were read back from the running module. And `filing_decision_divergence` has exactly one caller, `cmd_verify` at :17824, which runs `verify_run` at :17792 first, and `verify_run` dies at :17568-17571 when `state_fingerprint(state)` does not match the last ledger entry, a SHA-256 over the whole canonical state including `receipts` at :2336-2337. A hand-edited partial block therefore cannot reach the comparison in the same process; reaching it needs a foreign controller that writes the partial block and a matching ledger chain together. Both read and left. Three leads carry forward unchanged. The `reason` field carries one joined string that neither the comparison nor the printer reads, so `verify` output shows the sentinel and not the reason; recorded in rounds 1 to 4. `created_at` and `prior_fiat_required` are recorded and never compared, `created_at` because it cannot move on GitHub and `prior_fiat_required` because a change in it with `edit_count` and `last_edited_at` both unmoved is not reachable through GitHub. An issue above `ISSUE_EDIT_NODES_MAX` = 20 revisions takes the whole block to the sentinel permanently, which is the bounded read stated in that constant's own docstring rather than a defect in it, and `sha256` still compares. `hypomnema` over `audit` reports six findings rather than the two round 4 named: the two H008 at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, which are the pre-existing defect skills#1451, and four H009, each naming one stable decision reference that has no record, at `audit/rounds/fiat-shoggoth-front-door-derived.md:62` and :83 and its synopsis:5 and :6. That reference is not reproduced here: hypomnema reads a quoted reference inside an audit record as live, so naming it would make this record carry the same H009, which the first draft of this paragraph did. All four files are byte-identical to the run base `f22de680`, they belong to another run's record, and this record adds no stable decision reference of its own. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. `--filing-decision-override` appears 0 times in `hexctl.py`, and no line the step adds names a mechanism that grants this run. `cmd_record` still refuses `task_issue_contract` at :6198-6205, and `routed_filing_directive` and every state write path are byte-identical to `f4f0d4b8`: the diff `f4f0d4b8..f50298fb` over `hexctl.py` touches sixteen hunks and none of them is in `save_state`, `append_ledger`, `routed_filing_directive` or `cmd_init`'s write path. `plugins/hexaemeron/skills/fiat/EVOLUTION.md` at blob 91111113, `AGENTS.md` at df705057, `plugins/hexaemeron/skills/fiat/SKILL.md` at a96980de, `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` at 53eefaa4 and `docs/decisions/ADR-078-echo-fiat-required-as-a-filer-set-label.md` at 9ea97245 are all byte-identical to the run base `f22de680`. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2; step 5's Exit owns that sentence by name, so the `stale-claim-elsewhere` residue has a named owner rather than being deferred to nobody. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and belongs in the run pull request's `## Carried forward`. This record's own prose gates, run on the draft before it was appended: Imprimatur scores 100.0 out of 100 with 0 defects and reports `clean`. Brevitas reports B010, B001 and B027 and exits 0. B010 and B001 are artefacts of the fiat-audit-round/v2 structure, which fixes one heading and puts a long field ahead of the table, and fire identically on the already-receipted rounds above; B027 fires once on the leads paragraph, on a claim carrying two scope-bounding qualifiers that are kept. B022 does not fire.
## Step 3, round 6 -- 2026-09-12T16:30:33Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's `security_suite` field, since the run produces no Solidity and no Foundry or Hardhat project. This is a fresh reader on rounds 1 to 5: nothing below is carried from their context, every carried row was re-checked against the tree rather than copied, and round 4's enumeration was tested by calling each site it names rather than trusted. Round 5's table carries the same seventeen ids as round 4's, at the same severities and the same statuses, and contributes no id of its own, so the nineteen rows below are those seventeen plus this round's two. Rounds 5 and 6 overlapped: round 5's record was written at 15:50:22Z and committed as `8bc3ff7e` at 15:56:59Z, after this reader had begun and against the same tree `f50298fb`. Its bytes are untouched, its findings count stands, and this section appends after it. This record is committed after the fixes commit it describes, the order round 4 used. The GitHub API was not queried, so the `userContentEdits` newest-first ordering rounds 1 to 5 all recorded is still unverified: the fake `gh` at `plugins/hexaemeron/tests/hexctl_harness.py:587` returns whatever `FAKE_GH_EDITS` hands it, so no test discriminates `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 is read from round 1's record. Round 1's `-F` typed-parameter reading is unexercised for the same reason: the fake `gh` ignores `-f` and `-F` alike. Three instruments produced the observations below. Every branch of `rest_filing_stamps`, `github_issue_edit_provenance`, `read_task_issue_contract`, `cmd_issue_check` and `filing_decision_divergence` was called directly with `bounded_probe` and `github_rest` stubbed, because the fixture's `gh` can deliver neither a `totalCount` that disagrees with its own node list, nor a body that is not text to `issue-check`, nor a control character on a `Fiat-Required` line. The 30 cases in `plugins/hexaemeron/tests/test_filing_decision_provenance.py` ran through the fixture, OK in 38.072s. `tests/test_promise_machine_contract`, `tests/test_run_observation_capture` and `plugins/hexaemeron/tests/test_issue_filing_contract` ran 179 tests, OK in 52.244s. `python3 -m unittest discover -s tests` ran 1623 tests, OK in 159.760s, at the fix-only tree, before this record and its synopsis were written. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was settled by round 5 against `f50298fb`, 13 of 13 checks passed. It was not run this round, and this round's commit moves the audit head past the tree round 5 proved, so it is owed again there. What this round ran instead is the root suite below, which `run_checks` selects as `root-suite`, and the Hexaemeron suite through the Elenchus contract. phylax and ephoros exit 0 over `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and `plugins/hexaemeron/tests/test_filing_decision_provenance.py`, which covers only their parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. Of the design record's two pending conformance criteria, both of which block integration rather than this step, `reviewed-span-unchanged` holds because `plugins/hexaemeron/skills/fiat/SKILL.md` is byte-identical to the run base `f22de680` at blob a96980de, and `guard-test-fails-without-the-fix` is the Elenchus verdict above; `.hexaemeron/design-evidence.json` was read at sha256 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a, selected candidate `decision-age-window`, and not written to. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against the fix-only tree that became this round's commit by amendment, 2026-09-12T16:25:37Z to 16:29:29Z, 232 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. That amendment added only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`. None of the three is a test file, so the changed-test-file set the comparison used is identical in the committed object. The runner was pointed at a commit the amendment replaced, so that id is named nowhere here: it would resolve to nothing for a later reader.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R6-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:6373 with :5038 | `cmd_issue_check --issue` is the third reader of one issue response, and it agreed with neither of the other two. `read_task_issue_contract` at :5113 and `filing_decision_divergence` at :17735 both go through `admitted_issue_body`; the branch for a repository outside the publication contract kept `payload.get("body") or ""`, which makes the `isinstance` check on the next line unreachable for a falsy non-string and applies no `ISSUE_BODY_BYTES_MAX` cap. Observed at `f50298fb` with `github_rest` stubbed: a body of `[]`, `0`, `False` or `{}` was read as the empty string and reported as "some/other#9 declares no `Fiat-Required` line", exit 1, which is a claim about a body this reader never read, where `admitted_issue_body` refuses the same payload as "returned a body that is not text", exit 2; a 300044-byte body was parsed and its filing decision read, where the `--body` sibling at :6338 and `admitted_issue_body` both refuse above 262144. It is S3-R3-01's reading reached through `issue-check` rather than through `verify`. Low: both inputs still refuse, so nothing is granted that should not be, and `issue-check` is a pre-filing lint rather than a gate on a run; what moves is the reason given and the length parsed. Round 4's enumeration does not list this site, and its scope, the sites step 3 adds or touches, does not reach it. Repaired at :6373 by calling `admitted_issue_body`. Re-checked at this round's tree: all four falsy non-strings and the truthy `17` refuse in the transport shape, and the 300044-byte body refuses at the cap. `test_issue_check_reads_a_body_under_the_rule_init_reads_it_under` passes, failing by assertion at `f50298fb` with zero errors. | fixed in this round's commit |<br>| S3-R6-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5123-5125 with :17750-17752 | The filing refusal printed body-derived text raw and unbounded. The fault sentence quotes the value copied out of the issue body, and step 3 gave that sentence a second destination: `filing_decision_divergence` cleans it and bounds it at `UNREADABLE_DECISION_DETAIL_MAX` = 200 before stdout, while `read_task_issue_contract`'s refusal, the destination it has always had, did neither. Observed at `f50298fb` with `github_rest` stubbed: a body whose `Fiat-Required` line carries `ESC [2J ESC [31m` and `BEL` put those bytes on stderr unchanged, and a 250000-character value printed 250515 bytes, where the same two bodies through `verify --check-filing-decision` produce 195 and 269 characters with no control byte. Low: `init` refuses either way at exit 2, so no decision and no gate moves; what moves is what an operator's terminal renders and how much of it. The class is the one `stale_body_report` at :4834 already names for a crafted title. Repaired at :5123-5125 with `clean` and `ISSUE_FAULT_DETAIL_MAX` = 2000 at :5181, chosen against the 399-character longest reader-authored fault set `issue_contract_faults` produces, so no filer-facing instruction is cut. Re-checked at this round's tree: neither `ESC` nor `BEL` reaches stderr, the refusal for the 250000-character value is 2187 bytes, and "start the run again" survives the bound. `test_the_filing_refusal_carries_no_control_characters_from_the_body` passes, failing by assertion at `f50298fb` with zero errors. | fixed in this round's commit |<br>| S3-R4-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5361-5372 | `github_issue_edit_provenance` took `last_edited_at` from the newest node's `editedAt` and wrote `None` when it was absent or not text, so an issue with a byte-identical body and an unchanged `edit_count` reported `the filing decision has moved since this run read it` on `last_edited_at: recorded None, now 2026-09-06T10:08:38Z`, exit 1. Re-checked at this round's tree against a stubbed `bounded_probe`: a two-node response whose newest node carries no `editedAt` records `last_edited_at: unknown` beside `edit_count: 2` and `prior_fiat_required: 0`; a one-node response whose `editedAt` is `17` records `unknown` and joins two reasons at :5436; a zero-revision response still records `None`. `last_edited_at` is still absent from `UNDISCRIMINATED_DIVERGENCE_FIELDS` at :17816. `test_an_unreadable_edit_time_is_not_called_a_moved_decision` passes. | fixed in 5bbdf212 |<br>| S3-R4-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5218-5222 | `rest_filing_stamps` coerced a non-string `created_at` or `updated_at` to `None`, and the comparison read the absence as data: a run whose `init` got `"updated_at": 17` printed `the issue has been touched since this run read it` over an issue nothing had touched, exit 1. Re-checked at this round's tree: `{"updated_at": 17}` records `unknown`, an absent stamp records `unknown`, `{"created_at": None}` records `unknown`, and two strings pass through unchanged. Both builders still call it, at :5143 and :17758. `test_an_unread_rest_stamp_is_not_called_a_touched_issue` passes. | fixed in 5bbdf212 |<br>| S3-R4-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5346-5350 | `totalCount` and `nodes` were validated separately and never against each other, so a response claiming 3 revisions and carrying none recorded `edit_count: 3` beside `last_edited_at: None`, and `verify --check-filing-decision` printed the moved-decision headline over a body whose digest, decision, `updated_at` and `edit_count` were identical. Re-checked at this round's tree against a stubbed `bounded_probe`: `totalCount` 3 with no nodes, `totalCount` -3, and `totalCount` 2 with one node each return every field `unknown` under `the GraphQL response claimed N revisions and carried M, so its edit history was not read`. `test_a_count_without_its_nodes_is_not_a_history_that_was_read` and `test_a_count_and_a_node_list_that_disagree_are_never_read_as_values` pass. | fixed in f92ddbf7 |<br>| S3-R4-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5410-5428 with :4856 | `fiat_required_value` returns `None` for three different prior bodies -- one declaring no `Fiat-Required` line, one declaring it more than once, and one declaring a value that is neither 0 nor 1 -- and `github_issue_edit_provenance` recorded `prior_fiat_required: None` for all three beside the one sentence `the prior revision declared no Fiat-Required line`. Re-checked at this round's tree against a stubbed `bounded_probe`: a prior revision declaring the line twice records `unknown` under `declared Fiat-Required 2 times, so it made no decision`, one declaring `Fiat-Required: 7` records `unknown` under `a Fiat-Required value that is neither 0 nor 1`, one declaring nothing records `None`, and one whose `diff` is withheld records `unknown`. `fiat_required_declarations` at :4856 is the shared parse and neither reason quotes a body byte. `test_an_unreadable_prior_decision_is_not_recorded_as_no_decision` passes. | fixed in f50298fb |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17816 with :17703-17815 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-checked at this round's tree: `UNDISCRIMINATED_DIVERGENCE_FIELDS` holds `updated_at` alone, `edit_count` and `last_edited_at` are compared beside it, the weak headline and its per-row note are chosen at :17860-17878, and `test_a_moved_updated_at_alone_is_not_called_a_body_edit` and `test_a_moved_body_still_reads_as_a_moved_decision` both pass. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5295 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown`. Re-checked at this round's tree: the clause reads `except (UnicodeDecodeError, ValueError, RecursionError)` and `test_a_deeply_nested_response_records_unknown_rather_than_crashing` passes. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5319 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was recorded as `"edit_count": true`, and compared equal to a real count of 1. Re-checked at this round's tree: `isinstance(total, bool)` rejects at the boundary, a `true` count returns every field `unknown` under `the GraphQL response did not carry an edit history`, and `test_a_boolean_edit_count_is_refused_rather_than_recorded` passes. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2705 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at this round's tree: the comment names both reads, the `GIT_TIMEOUT` paid twice, and why the block is built unconditionally. No commit in this step changed an ordering in `cmd_init` or added a request; this round's commit edits `cmd_issue_check` and `read_task_issue_contract`, both downstream of `github_rest`, and adds no call. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:109-128 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at this round's tree: the cases at :215 and :237 both pass, and no reason string added since carries a body byte -- each holds integers and reader-authored text alone. The two strings this round adds reach stderr rather than a recorded surface, and the current body's declared value they carry is the one `init` has always printed. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Re-verified at this round's tree: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, the digest the packet's `runbook_step.sha256` carries, and its replacement Files names every path this round's commit writes. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, and exercised again: this round ran the amended contract end to end, against round 4's 315 seconds, round 3's 286, round 2's 330 and round 1's 904-second timeout. Round 5 had no fixes commit, so it ran no contract and recorded no verdict. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17703-17815 with :5190 and :5200 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at this round's tree: `FILING_PROVENANCE_UNKNOWN` at :5190, the third returned list, `uncomparable_filing_field` at :17687 and `print_uncomparable_filing_fields` at :17907 all stand, and the comparison applies four rules in order at :17796-17813 -- an unreadable current filing decision, a receipt with no provenance block, either side holding the sentinel, and only then a difference. `test_an_unreadable_history_now_is_not_called_a_moved_decision`, `test_an_unreadable_history_then_is_not_called_a_moved_decision`, `test_two_unreadable_reads_do_not_read_as_a_history_that_stands` and `test_a_receipt_with_no_provenance_block_is_not_a_moved_decision` all pass. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17847 with :17903 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-checked at this round's tree: the branch sets `reported` at :17847 and falls through, the closing line is guarded by `if not reported` at :17903, and `test_the_observations_flag_does_not_swallow_the_filing_check` passes. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5380-5384 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-checked at this round's tree: the reason counts the nodes carried, singular and plural, and `test_the_reason_names_the_revision_count_the_response_carried` passes on both halves. The `totalCount`-against-`len(nodes)` disagreement it deliberately left standing is closed by S3-R4-03. | fixed in 1bf662a7 |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5038 called at :5113 and :17735 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, substituting `""` for a body that is not text and applying no `ISSUE_BODY_BYTES_MAX` cap, so verify reported the SHA-256 of the empty string as the issue's current body digest under the moved-decision headline. Re-checked at this round's tree: `admitted_issue_body` at :5038 is the single rule, and this round adds the third call site at :6373 rather than a fourth copy of it. `test_a_body_that_is_not_text_is_not_a_moved_decision` and `test_a_body_above_the_cap_is_refused_rather_than_hashed` pass. | fixed in fca9bd17 |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17744-17756 | `now_record, _faults = issue_contract_faults(body, label)` dropped the current body's faults, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`. Re-checked at this round's tree: `fiat_required_value` is read for the filing fault alone at :17744, the field becomes uncomparable carrying the fault `init` would have printed, and the fault is `clean`ed and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 at :17750-17752 before it reaches stdout. `test_an_unreadable_decision_now_is_not_reported_as_no_decision` passes. S3-R6-02 is this row's `clean` and bound owed back to the destination the same sentence has at `init`. | fixed in fca9bd17 |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12677 | `github_rest` parsed its response under `except ValueError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. Re-checked at this round's tree: the clause reads `except (ValueError, RecursionError)` and `test_a_deeply_nested_rest_response_refuses_rather_than_crashing` passes. | fixed in fca9bd17 |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from rounds 2, 3, 4 and 5 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended; `.hexaemeron/study.md` is byte-identical at sha256 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524, the digest the packet's `risk_register.sha256` carries. The shipped copy is not byte-identical to it, at sha256 f25ea0c58a01feb09cc97c2d27d96899778e591658b191f85a6ae1f83cb6ae3c; the whole difference is five relative link targets rewritten for its own path, both files are 323 lines, and line 230 is the same byte string in each, so nothing in the divergence is a claim. Round 4's enumeration, and round 5's verdict on it, were tested against the tree rather than trusted. On the sites it lists, it holds: `admitted_issue_body` at :5038, `rest_filing_stamps` at :5200, `unknown_filing_provenance` at :5226, `github_rest` at :12648, ten block-level returns inside `github_issue_edit_provenance` at :5242 -- an identity GraphQL cannot be asked about, `start`, `timeout`, `output-cap`, a non-zero exit, a parse failure including `RecursionError`, a response carrying `errors`, a `totalCount` that is not an int or is a `bool` beside a `nodes` that is not a list, a `totalCount` above `ISSUE_EDIT_NODES_MAX`, and a `totalCount` that disagrees with the node list, which round 4 groups as seven by collapsing the four transport returns into one -- three field-level sites, and the one comparison site at :17703 with its four ordered rules. Every one was called directly this round and returned the sentinel or the absence the rule requires, including a `nodes` list of two nulls under a `totalCount` of 2, which records `edit_count: 2` beside `last_edited_at: unknown` and `prior_fiat_required: unknown`. What does not hold is the scope, and this round falsifies round 5 on exactly that. Round 5's record states that round 4's enumeration `is complete for every site where the rule can fail`, unqualified, and supports it by naming three further sites the step adds or touches and showing that none of the three can fail: `fiat_required_declarations` at :4856, `fiat_required_value` at :4873 with every live caller discriminating, and `read_task_issue_contract`'s null-recording branch at :5098-5107. Each of those three readings is correct and was re-checked here. The claim above them is not, because its evidence is drawn from the diff `da8d5326..f50298fb` and its scope is therefore the sites step 3 adds or touches, while the sentence asserts every site where the rule can fail. Two sites outside that diff can fail it and did: `cmd_issue_check` at :6373, which read a body it never read as `""` and reported a `Fiat-Required` line absent from it, and `read_task_issue_contract`'s refusal at :5123, whose fault sentence reached stderr uncleaned and unbounded while the same sentence reached stdout cleaned and bounded. Both were observed at `f50298fb`, the tree round 5 read, and both are repaired in this round's commit. Round 5 examined `read_task_issue_contract` at :5115 and found correctly that it dies on faults before any `None` is recorded, which is a statement about the value and not about what the refusal prints; that is where the second site sits. S3-R6-01 is the class as rounds 1 to 5 named it, a reader naming an unread thing as a value. S3-R6-02 is not a reader at all: it is the parity this step's own repair created and left one-sided, one fault sentence with two destinations and a `clean` and a bound on only the newer one. Round 4's sentence that the class is closed by enumeration, and round 5's that the enumeration is complete for every site where the rule can fail, are both true of step 3's own surface and neither is true of the filing-decision surface. Round 5's finding count of zero stands for the scope it read; this round read wider and found two. A fourth body reader, `issue_publication_from_payload` at :5019, is also unlisted and does apply both rules, inline rather than by calling `admitted_issue_body`; it is a duplicated rule that agrees today and is left as a lead rather than folded in, because it serves the publication contract rather than the filing decision and the step's Files does not reach it. `verify_pull_request_topology` at :13702 carries the same `payload["body"] or ""` shape over a pull request body, where no filing decision is read; out of this class and untouched. Of round 4's two named residues, neither has become a finding. The sentinel is still the literal string `unknown`, and `rest_filing_stamps` still records `unknown` for `{"updated_at": "unknown"}`, after which `uncomparable_filing_field` reports `this run recorded it as unknown, so it read no value to compare`; the collision needs an API or a `gh` that returns the literal word where GitHub returns an ISO timestamp, it reaches only `created_at`, `updated_at` and `last_edited_at`, since `edit_count` is an int and `prior_fiat_required` is an int or `None`, and `sha256` and `edit_count` still compare, so it hides no body edit. Round 5 states the invariant behind that, which the four earlier rounds do not: `sha256` is always in the compared set, since it is never `from_provenance` and a 64-character hex digest cannot hold the sentinel. Read again and carried, not restated as this round's own. And `recorded_provenance` at :17762 still tests block presence while `was_provenance.get(...)` reads per field; re-verified this round that no controller version writes a partial block, because `rest_filing_stamps` returns exactly `created_at` and `updated_at` on every path and `github_issue_edit_provenance` returns exactly `edit_count`, `last_edited_at`, `prior_fiat_required` and `reason` on every one of its return paths, so the merged block always carries six keys and reaching the gap needs a hand-edited or foreign-written `state.json`. Both read again and left. The `reason` field still carries one joined string, and neither the comparison nor the printer reads it, so `verify` names an uncomparable field without saying why the read failed; recorded in rounds 1 to 5 and unchanged. `created_at` and `prior_fiat_required` are recorded and never compared. `admitted_issue_body`'s cap governs the current body only: a prior revision's `diff` is parsed at whatever length the response carries, bounded by `GIT_OUTPUT_MAX` at 2097152 rather than 262144, and `fiat_required_value` and `fiat_required_declarations` each walk it, so that body is parsed twice. Divergence rows still print `clean(str(entry['recorded']))` with no length bound, over API-generated stamps rather than filer-authored text. `github_rest` and the GraphQL parser are two of 27 `json.loads` call sites in `hexctl.py` and the only two whose guard names `RecursionError`. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. `--filing-decision-override` appears 0 times in `hexctl.py`, and this round adds no emitted string at all: it routes `issue-check` onto `admitted_issue_body`'s existing refusals and cleans the refusal `init` already printed. `cmd_record` still refuses `task_issue_contract` at :6222, `save_state` at :2327 is byte-identical to the run base `f22de680` and still writes through `os.replace`, and `routed_filing_directive` at :5440 carries no provenance field, so `receipt-forgery`, `partial-write-of-the-receipt` and `routed-exit-mistaken-for-success` hold as rounds 1 to 5 read them. `plugins/hexaemeron/skills/fiat/EVOLUTION.md` at blob 91111113, `AGENTS.md` at df705057, `plugins/hexaemeron/skills/fiat/SKILL.md` at a96980de, `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` at 53eefaa4 and `docs/decisions/ADR-078-echo-fiat-required-as-a-filer-set-label.md` at 9ea97245 are all byte-identical to the run base `f22de680`, so `held-job-drift`, `reviewed-span-drift` and `stale-claim-elsewhere` are unmoved. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2, and left to step 5; skills#1454 records the register's scope-column gap. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and belongs in the run pull request's `## Carried forward`; nothing this round touched `cmd_init`'s exit path. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings, and all eleven moved from 9410b04a1a2d182bc3370b1c446797884465f90b1cf4b76c2367a7689612cc84 to 545791f00cf646f80ecd18077e86214cc059ca4ff8e2e16b883970013bc7fb39 in this round's commit. `plugins/hexaemeron/tests/test_hexctl.py` is unchanged at 259222 bytes, inside its 262144-byte bound, and `test_filing_decision_provenance.py` is now 38012 bytes. `.horos/candidates.json` did not move this round; only `--census --write` was run.
## Step 3, round 7 -- 2026-09-13T00:04:13Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's `security_suite` field, since the run produces no Solidity and no Foundry or Hardhat project. This is a fresh reader on rounds 1 to 6: nothing below is carried from their context, and every carried row was re-checked against this round's commit tree rather than copied, with each cited `file:line` resolved in the current file. This record is committed by amendment into the fixes commit it describes, the order rounds 4 and 6 used. The GitHub API was not queried, so the `userContentEdits` newest-first ordering rounds 1 to 6 all recorded is still unverified: the fake `gh` at `plugins/hexaemeron/tests/hexctl_harness.py:587` returns whatever `FAKE_GH_EDITS` hands it, so no test discriminates `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 is read from round 1's record. Round 1's `-F` typed-parameter reading is unexercised for the same reason. This round's enumeration is stated in the leads below with its own scope; it does not query GitHub either, and it establishes what the code does with a payload rather than what GitHub sends. Three instruments produced the observations. `inspect_pull_request`, `cmd_issue_check`, `issue_publication_from_payload`, `admitted_issue_body`, `rest_filing_stamps`, `github_issue_edit_provenance`, `filing_decision_divergence`, `checked_login`, `checked_identity`, `message_coauthors` and `stale_body_report` were each called directly with `github_rest`, `bounded_probe` and `target_repository` stubbed, because the fixture's `gh` delivers neither a pull request body that is not text nor a control character on a `Fiat-Required` line. The 33 cases in `plugins/hexaemeron/tests/test_filing_decision_provenance.py` ran through the fixture, OK in 78.476s, against 30 at round 6. `python3 -m unittest discover -s tests` ran 1623 tests, OK in 247.648s, 2026-09-12T23:52:07Z to 23:56:15Z, at the fix-only tree, and again at the staged tree this record's commit carries, before that commit was made. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was settled by round 5 against `f50298fb` at 13 of 13 checks and owed again after round 6; it was not run this round either, and this round's commit moves the audit head past `99e0410a`, so it remains owed. phylax and ephoros exit 0 over `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and `plugins/hexaemeron/tests/test_filing_decision_provenance.py`, before and after this round's edits, which covers only their parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0 both times; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. Of the design record's two pending conformance criteria, both of which block integration rather than this step, `reviewed-span-unchanged` holds because `plugins/hexaemeron/skills/fiat/SKILL.md` is byte-identical to the run base `f22de680` at blob a96980de, and `guard-test-fails-without-the-fix` is the Elenchus verdict above; `.hexaemeron/design-evidence.json` was read at sha256 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a, selected candidate `decision-age-window`, and not written to. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against the fix-only tree that became this round's commit by amendment, 2026-09-12T23:57:20Z to 2026-09-13T00:02:33Z, 313 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. The amendment that folds this record in adds only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`; none is a test file, so the changed-test-file set the comparison used is identical in the committed object. The runner was pointed at a commit the amendment replaced, so no commit id of this round's is named anywhere in this section and both new rows carry `fixed in this round's commit`, the phrase rounds 4 and 6 use, which survives a further amend where a SHA does not.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R7-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5122, :5186, :6395 with plugins/hexaemeron/tests/test_filing_decision_provenance.py:670, :713, :750 | Six in-source citations named finding ids this run never issued. Round 6's repairs are S3-R6-01 and S3-R6-02 in the audit record at lines 265 to 297 and in `99e0410a`'s own commit message, and all six citations that commit added named a round 5 id instead, three in `hexctl.py` and three in `test_filing_decision_provenance.py`. Round 5 of this run recorded zero findings and contributed no id, so `S3-R5-` occurs zero times in `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`. The ids are not dangling, which would be the milder fault: grepped across the repository they resolve to another run's step 3 round 5, a low finding about the commit gate's index-anchoring control recorded at `docs/commit-gate/runbook.md:384` and cited at `tests/test_commit_gate.py:1417` and `tests/test_harness_manifest.py:2166`, `:2194`, `:2249` and `:2309`. A reader following the citation reaches a real record that says something else. Finding ids are per-run and the repository holds several runs at step 3 round 5, which is what turns a typo into a mis-citation. It is the `stale-claim-elsewhere` shape inside a file this run had already edited, the same shape the fifth runbook amendment repaired in `test_issue_filing_contract.py`'s docstring. Low: no behaviour and no gate moves; what moves is where the next reader lands. Repaired by replacing all six with S3-R6-01 and S3-R6-02. Guarded by `test_every_finding_a_comment_cites_names_this_run_s_own_record` at `test_filing_decision_provenance.py:940`, which assembles the prefix rather than spelling it, because a case asserting a string is absent from its own file cannot write it; failing by assertion at `99e0410a` on the `hexctl.py` subtest with zero errors. | fixed in this round's commit |<br>| S3-R7-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:13741 with :13746 and :13757 | `inspect_pull_request` read `payload["body"] or ""`, which made the `isinstance` check on the next line unreachable for a falsy non-string. Observed at `99e0410a` with `github_rest` and `target_repository` stubbed: a body of `[]`, `0`, `False` or `{}` returned a complete topology record at `state: OPEN` and exit 0, while a truthy `17` refused, which is what kept the gap invisible. Two consumers then read the substitution. `HOST_BYLINE_RE.search(body)` at :13746 searched `""`, found nothing and passed the runtime-host byline gate over a body this reader never read. `pull_request_closing_reference` at :13757, when `expected_closing_issue` is supplied, would report `pull request body has no recognised closing reference for the recorded task_issue; add \`Closes #N\` before merge`, which is a claim about that same unread body and an instruction that cannot fix it. It is S3-R6-01's reading reached through the pull request body rather than the issue body. Round 6 named this site as `verify_pull_request_topology` at :13702 and ruled it out of the class because no filing decision is read here; that is a statement about what the body is for rather than about whether the reader read it, and the class every round states is a reader naming an unread thing as a value. The function's name is `inspect_pull_request`; `pull request topology` is the label string it passes to `github_rest`. Low, on round 6's own banding: a non-text body needs an API or a `gh` that does not send what GitHub sends, the same reachability S3-R6-01 was raised at, and no receipt records the substituted body. Repaired at :13741-13745 with the three states `admitted_issue_body` uses -- `None` is read-and-empty because REST spells an empty body that way, a non-string refuses, and a string passes -- under the topology vocabulary rather than the issue one, so the truthy half's refusal text moves from `pull request topology is missing its body` to `pull request topology returned a body that is not text`; no test asserted the former. The neighbouring `head`/`base` read at :13767 was examined and left: `head.get("ref") if isinstance(head, dict) else None` names an unread head object as `None` in the refusal text, but all three callers pass a non-`None` `expected_head` and `expected_base`, at :7879, :10387 and :11038, so it refuses in every direction, records nothing and moves no gate. Guarded by `test_the_pull_request_body_reader_refuses_a_body_it_did_not_read` at `test_filing_decision_provenance.py:843`, failing by assertion at `99e0410a` on four subtests with zero errors, and covering `None`, `""` and the truthy half as regression guards. | fixed in this round's commit |<br>| S3-R7-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5189 with :6411-6413 and :11034 | The body-derived fault sentence has four destinations, not the two round 6 enumerated. `fiat_required_value` at :4895 quotes the value copied out of the issue body into `declares \`Fiat-Required: {value}\``, and that sentence reaches `read_task_issue_contract`'s refusal, the divergence report, `cmd_issue_check`'s fault printer and the filed carryover refusal in `done integrate`. Round 6 repaired the first and recorded the second as already repaired by S3-R3-02, and described the sentence as having two destinations. Observed at `99e0410a` with `github_rest` stubbed: through `cmd_issue_check --issue`, a `Fiat-Required` line carrying `ESC [2J ESC [31m` and `BEL` reached stderr with 2 escape sequences and 1 BEL byte for byte in 171 bytes, and a 250000-character value printed 250156 bytes; through `issue_publication_from_payload` into the `done integrate` refusal, the same two bodies produced 148 and 250133 bytes with the same 2 escapes and 1 BEL. `issue-check --issue` is the wider exposure of the two, because its non-skills branch accepts any canonical GitHub issue URL, so the text is a stranger's by design. Low, on round 6's banding for S3-R6-02: `issue-check` exits 1 and `done integrate` dies either way, so no decision and no gate moves; what moves is what an operator's terminal renders and how much of it. Repaired by extracting `bounded_issue_fault_detail` at :5189, which applies `clean` and one bound, and routing all four destinations through it: `read_task_issue_contract` at :5123 and `filing_decision_divergence` at :17792 keep their existing bounds of `ISSUE_FAULT_DETAIL_MAX` = 2000 and `UNREADABLE_DECISION_DETAIL_MAX` = 200 and are byte-identical in output, `cmd_issue_check` at :6411-6413 bounds each fault line rather than the join because that destination is a list a filer works down, and `done integrate` at :11034 bounds the join. `CARRYOVER_ROWS_MAX` = 128 bounds the number of lines `issue-check` can print, so its worst case is 128 lines of 2003 characters; the longest reader-authored fault set `issue_contract_faults` produces is 399 characters, so nothing a filer needs is cut. Guarded by `test_every_destination_of_the_filing_fault_cleans_and_bounds_it` at `test_filing_decision_provenance.py:874`, failing by assertion at `99e0410a` with zero errors. The `done integrate` destination is pinned two ways rather than driven -- the shared rule is exercised, and the call site is read for the name of it -- because reaching that line needs a whole integrating run; that is weaker than the other three and is recorded as such. | fixed in this round's commit |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17857 with :17744-17856 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-read at this round's commit: `UNDISCRIMINATED_DIVERGENCE_FIELDS` at :17857 holds `updated_at` alone, the note attaches to that field alone at :17903, and `edit_count`, `last_edited_at` and `sha256` are compared beside it. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5318 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown`. Re-read at this round's commit: :5318 reads `except (UnicodeDecodeError, ValueError, RecursionError)`. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5343 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was recorded as `"edit_count": true`, and compared equal to a real count of 1. Re-read at this round's commit: `isinstance(total, bool)` rejects at :5343, and S3-R4-03's `len(nodes) != total` at :5369 refuses the same response a second way. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2706 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at this round's commit: the comment names both reads and the `GIT_TIMEOUT` paid twice. `cmd_init` is byte-identical to `f4f0d4b8` apart from that comment; no ordering, request or state write in it moved, and this round touched none of it. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:109-126 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at this round's commit: the case stands at :109, the six reason strings at :5392, :5403, :5411, :5439, :5443 and :5448 carry integers and reader-authored text alone, `fiat_required_value`'s faults on a prior body are bound to `_faults` at :5433 and discarded, and `routed_filing_directive` at :5463 reads `issue`, `repository`, `number` and `carryover` and never `provenance`, cleaning every string it does carry at any depth. This round's three repairs add no emitted string that quotes a body: two are reader-authored refusals and one is a bound on text already emitted. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Re-verified at this round's commit: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, the digest the packet's `runbook_step.sha256` carries, and its replacement Files names every path this round writes. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, and exercised again: this round ran the amended contract end to end in 313 seconds, against round 6's 232, round 4's 315, round 3's 286, round 2's 330 and round 1's 904-second timeout. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17744-17856 with :5213 and :5223 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at this round's commit: `FILING_PROVENANCE_UNKNOWN` at :5213, the third returned list, `uncomparable_filing_field` at :17728 and `print_uncomparable_filing_fields` at :17948 all stand, and `rest_filing_stamps` at :5223 is called by both builders. Driven directly, a receipt and a re-read both holding the sentinel on `updated_at`, `edit_count` and `last_edited_at` returned three uncomparable fields and zero divergences, and the same pair with a moved body returned the `sha256` divergence beside them. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17888 with :17944 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-read at this round's commit: the branch sets `reported` at :17888 and falls through, and the closing line is guarded by `if not reported` at :17944. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5403-5407 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-read at this round's commit: the reason counts `len(revisions)`, the nodes carried, and pluralises on it. The `totalCount`-against-`len(nodes)` disagreement it left standing is closed by S3-R4-03 at :5369, so the empty-list half is the discriminating input. | fixed in 1bf662a7 |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5038 called at :5113, :6396 and :17776 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, substituting `""` for a body that is not text and applying no `ISSUE_BODY_BYTES_MAX` cap, so verify reported the SHA-256 of the empty string as the issue's current body digest under the moved-decision headline. Re-read at this round's commit: `admitted_issue_body` at :5038 is the single rule and now has three call sites, `:5113`, `:6396` from S3-R6-01's repair and `:17776`. | fixed in fca9bd17 |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17785-17797 | `now_record, _faults = issue_contract_faults(body, label)` dropped the current body's faults, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`. Re-read at this round's commit: `fiat_required_value` is read for the filing fault alone at :17785, the field becomes uncomparable carrying the fault `init` would have printed, and the fault is cleaned and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 before it reaches stdout -- now through `bounded_issue_fault_detail` at :17792, which this round's S3-R7-03 repair routes it through with the same bound and byte-identical output. `issue_contract_faults` at :4901 still writes `None` for all three unreadable shapes; this guard is what keeps that `None` out of the comparison. | fixed in fca9bd17 |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12705 | `github_rest` parsed its response under `except ValueError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. Re-read at this round's commit: :12705 reads `except (ValueError, RecursionError)`. It and the GraphQL parser at :5318 are still the only two of the `json.loads` call sites in this file whose guard names `RecursionError`. | fixed in fca9bd17 |<br>| S3-R4-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5384-5394 | `github_issue_edit_provenance` took `last_edited_at` from the newest node's `editedAt` and wrote `None` when it was absent or not text, so a field the reader could not read entered the one compared field that carries the strong headline on its own. Re-read at this round's commit: three states stand at :5384-5394, `None` for a response carrying no revision and `FILING_PROVENANCE_UNKNOWN` with a reason for a newest node whose `editedAt` is unreadable, and `reason` is a joined list at :5459. | fixed in 5bbdf212 |<br>| S3-R4-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5240-5246 | `rest_filing_stamps` coerced a non-string `created_at` or `updated_at` to `None`, and the comparison read the absence as data. Re-checked at this round's commit by direct call: a payload carrying no `updated_at` records `unknown`, `{"updated_at": 17}` records `unknown`, two strings pass through unchanged, and one carrying the literal string `unknown` records `unknown`, which is the collision named under the leads below. | fixed in 5bbdf212 |<br>| S3-R4-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5369-5373 | `totalCount` and `nodes` were validated separately and never against each other, so one response answered how many revisions the body has twice and was believed both times. Re-read at this round's commit: `len(nodes) != total` at :5369 takes the whole block to the sentinel under a reason naming both numbers, which also refuses a negative `totalCount` that passed the type check and the ceiling. | fixed in f92ddbf7 |<br>| S3-R4-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5433-5451 with :4856 | `fiat_required_value` returns `None` for three different prior bodies -- one declaring no `Fiat-Required` line, one declaring it more than once, and one declaring a value that is neither 0 nor 1 -- and `github_issue_edit_provenance` recorded `prior_fiat_required: None` for all three beside the one sentence `the prior revision declared no Fiat-Required line`. Re-read at this round's commit: `None` only for a body declaring no line at :5437-5441, the sentinel for a body declaring the line more than once at :5442 and for one declaring a value that is neither 0 nor 1 at :5447, with `fiat_required_declarations` at :4856 supplying the shape, and the reason quoting no value out of the body. | fixed in f50298fb |<br>| S3-R6-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:6396 with :5038 | `cmd_issue_check --issue` is the third reader of one issue response, and it agreed with neither of the other two: the branch for a repository outside the publication contract kept `payload.get("body") or ""`, which makes the `isinstance` check on the next line unreachable for a falsy non-string and applies no `ISSUE_BODY_BYTES_MAX` cap, so a body of `[]`, `0`, `False` or `{}` was read as the empty string and reported as declaring no `Fiat-Required` line. Re-read at this round's commit: the branch calls `admitted_issue_body` at :6396 and `test_issue_check_reads_a_body_under_the_rule_init_reads_it_under` at `test_filing_decision_provenance.py:703` passes. S3-R7-02 is this same reading reached through the pull request body, which round 6 examined and ruled out of the class. | fixed in 99e0410a |<br>| S3-R6-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5123 with :17792 | The filing refusal printed body-derived text raw and unbounded, while `filing_decision_divergence` cleaned the identical sentence and bounded it at `UNREADABLE_DECISION_DETAIL_MAX` = 200 before stdout. Re-read at this round's commit: the refusal is cleaned and bounded at `ISSUE_FAULT_DETAIL_MAX` = 2000, now through `bounded_issue_fault_detail` at :5123, and `test_the_filing_refusal_carries_no_control_characters_from_the_body` at `test_filing_decision_provenance.py:741` passes. Round 6 read this sentence as having two destinations; S3-R7-03 names the other two and gives all four one rule. | fixed in 99e0410a |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from rounds 2, 3, 4, 5 and 6 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended; `.hexaemeron/study.md` is byte-identical at sha256 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524, the digest the packet's `risk_register.sha256` carries, and the shipped copy stands at f25ea0c58a01feb09cc97c2d27d96899778e591658b191f85a6ae1f83cb6ae3c, differing only in five relative link targets rewritten for its own path. THE ENUMERATION, AND ITS SCOPE. Rounds 4, 5 and 6 each enumerated from a diff and each stated a scope narrower than the sentence built on it. Round 4 enumerated the ten reader sites and one comparison site step 3 adds or touches and called the class closed by enumeration. Round 5 restated that as complete for every site where the rule can fail. Round 6 falsified round 5 from two sites outside the diff `da8d5326..f50298fb`, then scoped both of its own claims the same way: it read the filing fault sentence as having two destinations and it ruled the pull request body reader out of the class. Round 7 enumerates from the transports forward instead of from a diff, and states its scope before its result. SCOPE: every expression in `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` at this round's commit that takes a value out of a decoded GitHub API response, plus every site that prints to stdout or stderr or records into `state.json`, the ledger or a directive a value derived from one. Two transports carry every such response and there are no others in the file: `github_rest` at :12676, whose `bounded_probe` at :12688 runs `gh api --method GET`, and the one GraphQL `bounded_probe` at :5286, which runs `gh api graphql`. `"gh"` occurs at exactly those two argv sites, :5288 and :12689. Eight call sites lie between them: `read_task_issue_contract` at :5110, `filed_issue_publication_records` at :5619, `cmd_issue_check` at :6378, `github_repository` at :13578, `inspect_pull_request` at :13712, `github_commit_payload` at :13833, `filing_decision_divergence` at :17775, and the GraphQL request inside `github_issue_edit_provenance`. Twenty-four sites take a field out of one of those responses, and this round called each: :4814 and :4815 in `stale_body_report`, :4996 in `issue_label_names`, :5023 and :5024 in `issue_publication_from_payload`, :5056 in `admitted_issue_body`, :5242 in `rest_filing_stamps`, :5333, :5334, :5387 and :5409 in `github_issue_edit_provenance`, :13579 in `github_repository`, :13682 in `pull_request_state`, :13717, :13741, :13763, :13769, :13794 and :13800 in `inspect_pull_request`, :13838 in `github_commit_payload`, :13851 in `require_github_verified`, and :13869, :13885 and :13890 in `commit_attribution`. Twenty-three of the twenty-four refuse what they could not read or record the sentinel for it. The twenty-fourth is :13741, raised as S3-R7-02. OUT OF SCOPE, NAMED: this enumeration is over one file, and it excludes three neighbouring surfaces that read like it. Local Git output at :13331, where `HOST_BYLINE_RE` searches a commit message read through `_exact_commit_git`, is not a GitHub payload. `carried_forward_fault` at :5529 and `carryover_triage` over the run's own pull request body read a local file this run wrote. The design-evidence reads at :6917 to :6944 parse a local JSON artefact. None was audited here and none is claimed clean. The enumeration also says nothing about what GitHub actually sends: every observation was taken with the transport stubbed, and no round of this step has queried the API. THE TWO SITES ROUND 6 NAMED. `issue_publication_from_payload` at :5019 is not a finding. It applies both halves of the body rule inline rather than through `admitted_issue_body` -- `None` becomes `""` because REST spells an empty body that way, a non-string refuses through `github_unreachable`, and a body above `ISSUE_BODY_BYTES_MAX` dies -- and the two refusal sentences differ only in naming the publication contract rather than the filing decision, which each destination owes. `title` at :5023 is passed on untyped and `issue_queue_contract` at :4919 returns `{}` and one fault for a title that is not text rather than coercing it, and `issue_label_names` at :4994 refuses labels that are not an array and a label without a text name. It remains a duplicated rule that agrees, and is left rather than folded in for the reason round 6 gave. One residue: when the title is not text the merged record carries no `queue`, `owner`, `title` or `labels` key, and both consumers refuse on the fault before reading one, at :6414 and :11031, so the partial record reaches no surface. Read and left. `verify_pull_request_topology` at :13702 IS a finding, raised as S3-R7-02; the function is named `inspect_pull_request` and `pull request topology` is its `github_rest` label. Round 6 ruled it out because no filing decision is read there. That is true and is not the rule: the class is a reader naming an unread thing as a value, and this reader named `[]`, `0`, `False` and `{}` as an empty body, then carried the runtime-host byline gate over the substitution. Round 6's finding count of two stands for the scope it read; this round read the file rather than a diff and found three. WHAT THIS ROUND DOES NOT CLAIM. The class is not declared closed. The enumeration is complete for the scope stated above and for no wider one, and three of the four surfaces it excludes are named. Round 6's two findings and this round's three all lay outside the previous round's scope, so the honest reading is that the class has been bounded once per round by whatever the round chose to read, and this round's contribution is a scope statement the next round can falsify directly rather than reconstruct. THE STANDING RESIDUES, RE-TAKEN. The sentinel is still the literal string `unknown`: `rest_filing_stamps` records `unknown` for `{"updated_at": "unknown"}`, after which `uncomparable_filing_field` reports `this run recorded it as unknown, so it read no value to compare`. It reaches only `created_at`, `updated_at` and `last_edited_at`, since `edit_count` is an int and `prior_fiat_required` an int or `None`, it needs an API or a `gh` returning the literal word where GitHub returns an ISO timestamp, and `sha256` and `edit_count` still compare, so it hides no body edit. `sha256` is always in the compared set, since it is never `from_provenance` and a 64-character hex digest cannot hold the sentinel. Unreachable and carried, not raised. `recorded_provenance` at :17803 still tests block presence while `was_provenance.get(...)` reads per field; `rest_filing_stamps` returns exactly two keys on every path and `github_issue_edit_provenance` exactly four on every return, so the merged block always carries six and reaching the gap needs a hand-edited or foreign-written `state.json`. Unreachable and carried, not raised. The `reason` field still carries one joined string that neither the comparison nor the printer reads, so `verify` names an uncomparable field without saying why the read failed. `created_at` and `prior_fiat_required` are recorded and never compared. `admitted_issue_body`'s cap governs the current body only: a prior revision's `diff` is parsed at whatever length the response carries, bounded by `GIT_OUTPUT_MAX` at 2097152 rather than 262144, and `fiat_required_value` and `fiat_required_declarations` each walk it, so that body is parsed twice. Divergence rows at :17922-17925 still print `clean(str(entry['recorded']))` with no length bound, over API-generated stamps rather than filer-authored text. `inspect_pull_request` applies no length cap of its own to a pull request body: `HOST_BYLINE_RE.search` and `markdown_prose_lines` walk whatever the transport delivered, bounded only by `GIT_OUTPUT_MAX`, where the issue readers stop at `ISSUE_BODY_BYTES_MAX`. `cmd_issue_check`'s clean path prints each carryover row's `id`, `disposition` and `reference` to stdout with no length bound; control characters are refused per row at :4663 and the row count at `CARRYOVER_ROWS_MAX` = 128, and the reason field is capped at `CARRYOVER_REASON_BYTES_MAX` = 512, so the unbounded remainder is an id or a reference the row shape already constrains. All read and left. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. THE REGISTER'S OTHER LINES. `--filing-decision-override` appears 0 times in `hexctl.py`, and this round's three emitted strings are `pull request topology returned a body that is not text`, one truncation marker and no third, none of which names a mechanism that would grant a run. `cmd_record` still refuses `task_issue_contract` at :6231, `save_state` at :2327 is byte-identical to the run base `f22de680` and still writes through `os.replace`, and `routed_filing_directive` at :5463 carries no provenance field, so `receipt-forgery`, `partial-write-of-the-receipt` and `routed-exit-mistaken-for-success` hold as rounds 1 to 6 read them. `plugins/hexaemeron/skills/fiat/EVOLUTION.md` at blob 91111113, `AGENTS.md` at df705057, `plugins/hexaemeron/skills/fiat/SKILL.md` at a96980de, `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` at 53eefaa4 and `docs/decisions/ADR-078-echo-fiat-required-as-a-filer-set-label.md` at 9ea97245 are all byte-identical to the run base `f22de680`, so `held-job-drift`, `reviewed-span-drift` and `stale-claim-elsewhere` are unmoved in those five files; S3-R7-01 is the `stale-claim-elsewhere` shape found instead in two files this run does edit. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2, and left to step 5; skills#1454 records the register's scope-column gap. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and belongs in the run pull request's `## Carried forward`; nothing this round touched `cmd_init`'s exit path. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings, and all eleven moved from 545791f00cf646f80ecd18077e86214cc059ca4ff8e2e16b883970013bc7fb39 to ce1a9122d51572bd12b0fdf65fd36556a9ca254fc406fef9e2b1ee6af285acb7 in this round's commit. `plugins/hexaemeron/tests/test_hexctl.py` is unchanged at 259222 bytes, inside its 262144-byte bound, and `test_filing_decision_provenance.py` is now 46544 bytes against 38012 at round 6. `.horos/candidates.json` did not move; only `--census --write` was run.
## Step 3, round 8 -- 2026-09-13T00:49:57Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: refusal-names-its-own-bypass=reviewed; routed-exit-mistaken-for-success=reviewed; window-undiscriminated-read=reviewed; graphql-transport=reviewed; prior-body-in-memory=reviewed; receipt-forgery=reviewed; partial-write-of-the-receipt=reviewed; held-job-drift=reviewed; reviewed-span-drift=reviewed; stale-claim-elsewhere=reviewed<br><br>Not checked: the Solidity suite is waived by the packet's `security_suite` field, since the run produces no Solidity and no Foundry or Hardhat project. This is a fresh reader on rounds 1 to 7: nothing below is carried from their context, every carried row was re-checked against this round's commit tree rather than copied, and each cited `file:line` was resolved in the current file rather than shifted. That resolution corrected four citations in the carried S3-R7-02 row, which the shift alone would have carried wrong: the three callers that supply a non-`None` `expected_head` and `expected_base` are at :7920, :10428 and :11079 and not at the three lines round 7 recorded, `pull_request_closing_reference` is called at :13764, and the neighbouring `head`/`base` read is at :13780. Every claim those numbers support was re-taken and holds; the numbers alone moved. This record is committed by amendment into the fixes commit it describes, the order rounds 4, 6 and 7 used. The GitHub API was not queried, so the `userContentEdits` newest-first ordering rounds 1 to 7 all recorded is still unverified: the fake `gh` at `plugins/hexaemeron/tests/hexctl_harness.py:587` returns whatever `FAKE_GH_EDITS` hands it, so no test discriminates `revisions[1]` from the revision before it, and the one live observation of skills#1337 at totalCount 4 is read from round 1's record. Round 1's `-F` typed-parameter reading is unexercised for the same reason. Every observation in this step has been taken with the transport stubbed; that boundary is round 7's and it is not closed here. Three instruments produced this round's observations. `carried_forward_fault`, `carried_forward_record`, `filed_issue_publication_records`, `carried_forward_section`, `carried_forward_lines` and `carryover_triage` were called directly against files written for the purpose, because a completed integrating run does not produce a run-level pull request body that is not UTF-8 or one carrying a 250000-character carryover id. `verify_local_commit`, `_exact_commit_git`, `tool_text` and `HOST_BYLINE_RE` were driven against a scratch Git repository holding six commits: clean, valid UTF-8 non-ASCII, raw latin-1, 3 MiB, a runtime-host byline, and an unresolvable SHA. The 35 cases in `plugins/hexaemeron/tests/test_filing_decision_provenance.py` ran through the fixture, OK in 31.351s, against 33 at round 7. `python3 -m unittest discover -s tests` ran 1623 tests, OK in 157.921s, 2026-09-13T00:35:17Z to 00:37:55Z, exit 0, at the staged tree this round's commit carries, before that commit was made. The Exit's proof clause, `python3 scripts/run_checks.py --base fiat/1345-route-a-zero-decision-and-gate-the-line-tha` reporting `outcome green`, was settled by round 5 against `f50298fb` at 13 of 13 checks and owed again after rounds 6 and 7; it was not run this round either, and this round's commit moves the audit head past `b9eb5a31`, so it remains owed. phylax exits 0 and ephoros exits 0 over `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and `plugins/hexaemeron/tests/test_filing_decision_provenance.py`, which covers only their parser rules and no byte of JSON or Markdown. hypomnema over `docs docs/decisions plugins` exits 0; `audit` was not added, and the two H008 round 1 recorded there, at `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md:128` and its synopsis:8, are the pre-existing defect skills#1451 and are untouched. Study mode was not run, because step 3 ships no study. Of the design record's two pending conformance criteria, both of which block integration rather than this step, `reviewed-span-unchanged` holds because `plugins/hexaemeron/skills/fiat/SKILL.md` is byte-identical to the run base `f22de680` at blob a96980de, and `guard-test-fails-without-the-fix` is the Elenchus verdict above; `.hexaemeron/design-evidence.json` was read at sha256 88c6d36590c92b497113e3958697b7dc0b464a39bdabda2bcf930cb08cc3992a, selected candidate `decision-age-window`, and not written to. Those reads are also one of this round's three subjects and are recorded under the leads below. The Elenchus verdict above is the runner's declared result and is not an attestation of the report bytes. It was taken under the fifteenth amendment's exact contract, `--ref HEAD --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .elenchus/step-3.json --timeout 2400`, against the fix-only tree that became this round's commit by amendment, 2026-09-13T00:39:52Z to 00:43:46Z, 234 seconds of wall clock and exit 0, reading `guarded -- the runner report records a parent assertion failure` over changed test files `plugins/hexaemeron/tests/test_filing_decision_provenance.py` and `tests/promise_machine_coverage.json`. Both new guards were written to fail by assertion rather than by error: `classify` returns `inconclusive` whenever `errors` exceeds zero, tested before assertion failures are read, and the decode guard would otherwise have failed by the very `UnicodeDecodeError` it exists to refuse. Observed at `b9eb5a31`, the pair fails with two failures and zero errors. The amendment that folds this record in adds only `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`, its `.synopsis.md` and `.horos/census.json`; none is a test file, so the changed-test-file set the comparison used is identical in the committed object. The runner was pointed at a commit the amendment replaced, so no commit id of this round's is named anywhere in this section and both new rows carry `fixed in this round's commit`.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R8-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5542 with :5529 and :11035 | `carried_forward_fault` caught `OSError` alone. `UnicodeDecodeError` derives from `ValueError`, not `OSError`, so a run-level pull request body that is not UTF-8 left the reader as an uncaught exception while an absent file, a directory and every other unreadable body earned the sentence the same handler returns. Observed at `b9eb5a31` by direct call on a body reading `## Carried forward` then a `carryover` block whose one row carries the byte `0xe9`: `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 50: invalid continuation byte`, raised out of `fh.read()` at :5541, against the fault sentence the same function returns for `[Errno 2] No such file or directory` and `[Errno 21] Is a directory`. `done integrate` calls this reader at :11035 and dies on what it returns, so the traceback replaces a refusal that names the path and the phase that writes it. It is not the coercion class: nothing is named as a value here, and the failure is closed rather than open. It is S3-R1-02's and S3-R3-03's exception-class shape, reached through a local file rather than a GraphQL response, and it is banded low with them. The same `except OSError` alone stands at five neighbouring read sites named under the leads below, all outside this round's stated scope. Repaired at :5542 by naming both classes; the exception's own text names the byte and its offset and quotes no file content, which the guard asserts. Guarded by `test_the_run_pull_request_reader_refuses_a_body_it_cannot_decode` at `plugins/hexaemeron/tests/test_filing_decision_provenance.py:999` in the new `LocalSourceReaderScopeTests`, failing by assertion at `b9eb5a31` with zero errors, and covering the absent file and the readable body as regression guards. | fixed in this round's commit |<br>| S3-R8-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5572 with :5189 and :11035 | The body-derived fault sentence has a fifth destination. `carryover_row_faults` quotes a row's id and its disposition straight out of the run-level pull request body into `row {n} id {item!r} is not kebab-case` and `row {n} disposition {disposition!r} is not one of ...`, and `carried_forward_fault` joined those raw on the way to `die` at :11035. Observed at `b9eb5a31` by direct call: one row carrying a 250000-character id produced a 250308-byte refusal, and 128 rows carrying 20000-character ids produced 2578383 bytes. Control characters were already refused per row at :4663 without being echoed, and `CARRYOVER_ROWS_MAX` = 128 already bounded the row count; length alone was unbounded. It is not the coercion class either. It is S3-R6-02's and S3-R7-03's shape reached through the one surface round 7 excluded, and it is narrower than either: this body is the run's own, written by the prose phase, where `cmd_issue_check --issue` accepts a stranger's issue by design. Low on round 6's banding for S3-R6-02: `done integrate` dies either way, so no decision and no gate moves; what moves is how much an operator's terminal renders. Repaired at :5572 by routing each fault through `bounded_issue_fault_detail` at :5189, bounded per row rather than over the join -- the shape `cmd_issue_check` takes at :6421, because this is a list a filer works down and the row count is already bounded. Bounding the join instead would have cut the tail of a 128-row list at `ISSUE_FAULT_DETAIL_MAX` = 2000 characters. The same two inputs now produce 2174 and 256809 bytes, the worst case is 128 lines of 2003 characters, and the longest reader-authored fault a row can earn is 107 characters, the reserved-id sentence, which quotes nothing out of the body, so nothing a filer needs is cut. The helper now has five call sites, :5123, :5572, :6421, :11044 and :17801, which is the fifth destination its own docstring anticipated. Guarded by `test_the_carryover_fault_bounds_every_row_it_quotes` at `plugins/hexaemeron/tests/test_filing_decision_provenance.py:1041`, failing by assertion at `b9eb5a31` with zero errors, and asserting that all 128 rows still earn a line, that the control-character refusal still echoes nothing, and that a reader-authored fault set is not cut. | fixed in this round's commit |<br>| S3-R7-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5122, :5186, :6405 with plugins/hexaemeron/tests/test_filing_decision_provenance.py:671, :714, :751 | Six in-source citations named finding ids this run never issued. Round 6's repairs are S3-R6-01 and S3-R6-02 in the audit record at lines 265 to 297 and in `99e0410a`'s own commit message, and all six citations that commit added named a round 5 id instead, three in `hexctl.py` and three in `test_filing_decision_provenance.py`. Round 5 of this run recorded zero findings and contributed no id, so `S3-R5-` occurs zero times in `audit/rounds/fiat-1345-route-a-zero-decision-and-gate-the-line-tha.md`. The ids are not dangling, which would be the milder fault: grepped across the repository they resolve to another run's step 3 round 5, a low finding about the commit gate's index-anchoring control recorded at `docs/commit-gate/runbook.md:384` and cited at `tests/test_commit_gate.py:1417` and `tests/test_harness_manifest.py:2166`, `:2194`, `:2249` and `:2309`. A reader following the citation reaches a real record that says something else. Finding ids are per-run and the repository holds several runs at step 3 round 5, which is what turns a typo into a mis-citation. It is the `stale-claim-elsewhere` shape inside a file this run had already edited, the same shape the fifth runbook amendment repaired in `test_issue_filing_contract.py`'s docstring. Low: no behaviour and no gate moves; what moves is where the next reader lands. Repaired by replacing all six with S3-R6-01 and S3-R6-02. Guarded by `test_every_finding_a_comment_cites_names_this_run_s_own_record` at `test_filing_decision_provenance.py:941`, which assembles the prefix rather than spelling it, because a case asserting a string is absent from its own file cannot write it; failing by assertion at `99e0410a` on the `hexctl.py` subtest with zero errors. | fixed in b9eb5a31 |<br>| S3-R7-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:13751 with :13756 and :13764 | `inspect_pull_request` read `payload["body"] or ""`, which made the `isinstance` check on the next line unreachable for a falsy non-string. Observed at `99e0410a` with `github_rest` and `target_repository` stubbed: a body of `[]`, `0`, `False` or `{}` returned a complete topology record at `state: OPEN` and exit 0, while a truthy `17` refused, which is what kept the gap invisible. Two consumers then read the substitution. `HOST_BYLINE_RE.search(body)` at :13756 searched `""`, found nothing and passed the runtime-host byline gate over a body this reader never read. `pull_request_closing_reference` at :13764, when `expected_closing_issue` is supplied, would report `pull request body has no recognised closing reference for the recorded task_issue; add \`Closes #N\` before merge`, which is a claim about that same unread body and an instruction that cannot fix it. It is S3-R6-01's reading reached through the pull request body rather than the issue body. Round 6 named this site as `verify_pull_request_topology` at :13712 and ruled it out of the class because no filing decision is read here; that is a statement about what the body is for rather than about whether the reader read it, and the class every round states is a reader naming an unread thing as a value. The function's name is `inspect_pull_request`; `pull request topology` is the label string it passes to `github_rest`. Low, on round 6's own banding: a non-text body needs an API or a `gh` that does not send what GitHub sends, the same reachability S3-R6-01 was raised at, and no receipt records the substituted body. Repaired at :13751-13755 with the three states `admitted_issue_body` uses -- `None` is read-and-empty because REST spells an empty body that way, a non-string refuses, and a string passes -- under the topology vocabulary rather than the issue one, so the truthy half's refusal text moves from `pull request topology is missing its body` to `pull request topology returned a body that is not text`; no test asserted the former. The neighbouring `head`/`base` read at :13780 was examined and left: `head.get("ref") if isinstance(head, dict) else None` names an unread head object as `None` in the refusal text, but all three callers pass a non-`None` `expected_head` and `expected_base`, at :7920, :10428 and :11079, so it refuses in every direction, records nothing and moves no gate. Guarded by `test_the_pull_request_body_reader_refuses_a_body_it_did_not_read` at `test_filing_decision_provenance.py:844`, failing by assertion at `99e0410a` on four subtests with zero errors, and covering `None`, `""` and the truthy half as regression guards. | fixed in b9eb5a31 |<br>| S3-R7-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5189 with :6421-6423 and :11044 | The body-derived fault sentence has four destinations, not the two round 6 enumerated. `fiat_required_value` at :4895 quotes the value copied out of the issue body into `declares \`Fiat-Required: {value}\``, and that sentence reaches `read_task_issue_contract`'s refusal, the divergence report, `cmd_issue_check`'s fault printer and the filed carryover refusal in `done integrate`. Round 6 repaired the first and recorded the second as already repaired by S3-R3-02, and described the sentence as having two destinations. Observed at `99e0410a` with `github_rest` stubbed: through `cmd_issue_check --issue`, a `Fiat-Required` line carrying `ESC [2J ESC [31m` and `BEL` reached stderr with 2 escape sequences and 1 BEL byte for byte in 171 bytes, and a 250000-character value printed 250156 bytes; through `issue_publication_from_payload` into the `done integrate` refusal, the same two bodies produced 148 and 250133 bytes with the same 2 escapes and 1 BEL. `issue-check --issue` is the wider exposure of the two, because its non-skills branch accepts any canonical GitHub issue URL, so the text is a stranger's by design. Low, on round 6's banding for S3-R6-02: `issue-check` exits 1 and `done integrate` dies either way, so no decision and no gate moves; what moves is what an operator's terminal renders and how much of it. Repaired by extracting `bounded_issue_fault_detail` at :5189, which applies `clean` and one bound, and routing all four destinations through it: `read_task_issue_contract` at :5123 and `filing_decision_divergence` at :17802 keep their existing bounds of `ISSUE_FAULT_DETAIL_MAX` = 2000 and `UNREADABLE_DECISION_DETAIL_MAX` = 200 and are byte-identical in output, `cmd_issue_check` at :6421-6423 bounds each fault line rather than the join because that destination is a list a filer works down, and `done integrate` at :11044 bounds the join. `CARRYOVER_ROWS_MAX` = 128 bounds the number of lines `issue-check` can print, so its worst case is 128 lines of 2003 characters; the longest reader-authored fault set `issue_contract_faults` produces is 399 characters, so nothing a filer needs is cut. Guarded by `test_every_destination_of_the_filing_fault_cleans_and_bounds_it` at `test_filing_decision_provenance.py:875`, failing by assertion at `99e0410a` with zero errors. The `done integrate` destination is pinned two ways rather than driven -- the shared rule is exercised, and the call site is read for the name of it -- because reaching that line needs a whole integrating run; that is weaker than the other three and is recorded as such. | fixed in b9eb5a31 |<br>| S3-R1-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17867 with :17754-17866 | `filing_decision_divergence` reported a moved `updated_at` under `the filing decision has moved since this run read it`, naming an undiscriminated read as a body edit. Re-read at this round's commit: `UNDISCRIMINATED_DIVERGENCE_FIELDS` at :17867 holds `updated_at` alone, the note attaches to that field alone at :17913, and `edit_count`, `last_edited_at` and `sha256` are compared beside it. | fixed in e4c012cb |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5318 | `RecursionError` derives from `RuntimeError`, not `ValueError`, so a deeply nested GraphQL array aborted the controller with a traceback instead of returning `unknown`. Re-read at this round's commit: :5318 reads `except (UnicodeDecodeError, ValueError, RecursionError)`. | fixed in e4c012cb |<br>| S3-R1-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5343 | `bool` subclasses `int`, so `"totalCount": true` passed the type check and the ceiling, was recorded as `"edit_count": true`, and compared equal to a real count of 1. Re-read at this round's commit: `isinstance(total, bool)` rejects at :5343, and S3-R4-03's `len(nodes) != total` at :5369 refuses the same response a second way. | fixed in e4c012cb |<br>| S3-R1-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:2692-2706 | The comment above the two pre-mutation reads said a `0` verdict `must cost the operator nothing but the read`, singular, which the provenance block made false. Re-read at this round's commit: the comment names both reads and the `GIT_TIMEOUT` paid twice. `cmd_init` is byte-identical to `f4f0d4b8` apart from that comment; no ordering, request or state write in it moved, and this round touched none of it. | fixed in e4c012cb |<br>| S3-R1-05 | low | plugins/hexaemeron/tests/test_filing_decision_provenance.py:110-127 | `test_prior_body_text_reaches_no_recorded_surface` drove one path; the routed `0` directive and the divergence report also hold a prior body and neither carried an assertion. Re-checked at this round's commit: the case stands at :110, the six reason strings at :5392, :5403, :5411, :5439, :5443 and :5448 carry integers and reader-authored text alone, `fiat_required_value`'s faults on a prior body are bound to `_faults` at :5433 and discarded, and `routed_filing_directive` at :5463 reads `issue`, `repository`, `number` and `carryover` and never `provenance`, cleaning every string it does carry at any depth. This round's three repairs add no emitted string that quotes a body: two are reader-authored refusals and one is a bound on text already emitted. | fixed in e4c012cb |<br>| S3-R1-06 | low | docs/route-a-zero-decision-runbook.md, step 3 Files | Files named eight paths and `f4f0d4b8` edits eight, but not the same eight: `.horos/boundary.json` edited and unnamed, `plugins/hexaemeron/tests/test_hexctl.py` named with a net change of zero. Re-verified at this round's commit: `.hexaemeron/runbook.md` and `docs/route-a-zero-decision-runbook.md` are byte-identical at sha256 f9d2dcba037946a75bb8e9292231021b98be4377c242d7e7827393185cb96447, the digest the packet's `runbook_step.sha256` carries, and its replacement Files names every path this round writes. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R1-07 | low | docs/route-a-zero-decision-runbook.md, step 3 Tests, Elenchus runner contract | The declared contract named no `--timeout`, so it inherited `elenchus.py`'s 900-second default for a suite `run_checks` timed at 1297.9s. Re-verified in the same byte-identical runbook, and exercised again: this round ran the amended contract end to end in 313 seconds, against round 6's 232, round 4's 315, round 3's 286, round 2's 330 and round 1's 904-second timeout. | closed by the fifteenth runbook amendment, shipped in a972cd1f |<br>| S3-R2-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17754-17866 with :5213 and :5223 | `filing_decision_divergence` compared every field with one `if was != now`, and `unknown` is a value that comparison reads, so a failed GraphQL read at either end reported a body edit and two failed reads compared equal and reported a history that stands. Re-checked at this round's commit: `FILING_PROVENANCE_UNKNOWN` at :5213, the third returned list, `uncomparable_filing_field` at :17738 and `print_uncomparable_filing_fields` at :17958 all stand, and `rest_filing_stamps` at :5223 is called by both builders. Driven directly, a receipt and a re-read both holding the sentinel on `updated_at`, `edit_count` and `last_edited_at` returned three uncomparable fields and zero divergences, and the same pair with a moved body returned the `sha256` divergence beside them. | fixed in 1bf662a7 |<br>| S3-R2-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17898 with :17954 | `cmd_verify`'s `--observations` branch ended with `return` above the `--check-filing-decision` block, so both flags together dropped the comparison in silence. Re-read at this round's commit: the branch sets `reported` at :17898 and falls through, and the closing line is guarded by `if not reported` at :17954. | fixed in 1bf662a7 |<br>| S3-R2-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5403-5407 | `github_issue_edit_provenance` wrote `the body has one revision` for every `len(revisions) < 2`, a count it never read. Re-read at this round's commit: the reason counts `len(revisions)`, the nodes carried, and pluralises on it. The `totalCount`-against-`len(nodes)` disagreement it left standing is closed by S3-R4-03 at :5369, so the empty-list half is the discriminating input. | fixed in 1bf662a7 |<br>| S3-R3-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5038 called at :5113, :6406 and :17786 | `filing_decision_divergence` admitted an issue body `read_task_issue_contract` refuses, substituting `""` for a body that is not text and applying no `ISSUE_BODY_BYTES_MAX` cap, so verify reported the SHA-256 of the empty string as the issue's current body digest under the moved-decision headline. Re-read at this round's commit: `admitted_issue_body` at :5038 is the single rule and now has three call sites, `:5113`, `:6406` from S3-R6-01's repair and `:17786`. | fixed in fca9bd17 |<br>| S3-R3-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:17795-17807 | `now_record, _faults = issue_contract_faults(body, label)` dropped the current body's faults, so a body that no longer declares one readable decision reported `fiat_required: recorded 1, now None`. Re-read at this round's commit: `fiat_required_value` is read for the filing fault alone at :17795, the field becomes uncomparable carrying the fault `init` would have printed, and the fault is cleaned and bounded by `UNREADABLE_DECISION_DETAIL_MAX` = 200 before it reaches stdout -- now through `bounded_issue_fault_detail` at :17802, which this round's S3-R7-03 repair routes it through with the same bound and byte-identical output. `issue_contract_faults` at :4901 still writes `None` for all three unreadable shapes; this guard is what keeps that `None` out of the comparison. | fixed in fca9bd17 |<br>| S3-R3-03 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12715 | `github_rest` parsed its response under `except ValueError`, so a deeply nested array walked the caller off the interpreter's stack instead of refusing through `github_unreachable`. Re-read at this round's commit: :12715 reads `except (ValueError, RecursionError)`. It and the GraphQL parser at :5318 are still the only two of the `json.loads` call sites in this file whose guard names `RecursionError`. | fixed in fca9bd17 |<br>| S3-R4-01 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5384-5394 | `github_issue_edit_provenance` took `last_edited_at` from the newest node's `editedAt` and wrote `None` when it was absent or not text, so a field the reader could not read entered the one compared field that carries the strong headline on its own. Re-read at this round's commit: three states stand at :5384-5394, `None` for a response carrying no revision and `FILING_PROVENANCE_UNKNOWN` with a reason for a newest node whose `editedAt` is unreadable, and `reason` is a joined list at :5459. | fixed in 5bbdf212 |<br>| S3-R4-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5240-5246 | `rest_filing_stamps` coerced a non-string `created_at` or `updated_at` to `None`, and the comparison read the absence as data. Re-checked at this round's commit by direct call: a payload carrying no `updated_at` records `unknown`, `{"updated_at": 17}` records `unknown`, two strings pass through unchanged, and one carrying the literal string `unknown` records `unknown`, which is the collision named under the leads below. | fixed in 5bbdf212 |<br>| S3-R4-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5369-5373 | `totalCount` and `nodes` were validated separately and never against each other, so one response answered how many revisions the body has twice and was believed both times. Re-read at this round's commit: `len(nodes) != total` at :5369 takes the whole block to the sentinel under a reason naming both numbers, which also refuses a negative `totalCount` that passed the type check and the ceiling. | fixed in f92ddbf7 |<br>| S3-R4-04 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5433-5451 with :4856 | `fiat_required_value` returns `None` for three different prior bodies -- one declaring no `Fiat-Required` line, one declaring it more than once, and one declaring a value that is neither 0 nor 1 -- and `github_issue_edit_provenance` recorded `prior_fiat_required: None` for all three beside the one sentence `the prior revision declared no Fiat-Required line`. Re-read at this round's commit: `None` only for a body declaring no line at :5437-5441, the sentinel for a body declaring the line more than once at :5442 and for one declaring a value that is neither 0 nor 1 at :5447, with `fiat_required_declarations` at :4856 supplying the shape, and the reason quoting no value out of the body. | fixed in f50298fb |<br>| S3-R6-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:6406 with :5038 | `cmd_issue_check --issue` is the third reader of one issue response, and it agreed with neither of the other two: the branch for a repository outside the publication contract kept `payload.get("body") or ""`, which makes the `isinstance` check on the next line unreachable for a falsy non-string and applies no `ISSUE_BODY_BYTES_MAX` cap, so a body of `[]`, `0`, `False` or `{}` was read as the empty string and reported as declaring no `Fiat-Required` line. Re-read at this round's commit: the branch calls `admitted_issue_body` at :6406 and `test_issue_check_reads_a_body_under_the_rule_init_reads_it_under` at `test_filing_decision_provenance.py:704` passes. S3-R7-02 is this same reading reached through the pull request body, which round 6 examined and ruled out of the class. | fixed in 99e0410a |<br>| S3-R6-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:5123 with :17802 | The filing refusal printed body-derived text raw and unbounded, while `filing_decision_divergence` cleaned the identical sentence and bounded it at `UNREADABLE_DECISION_DETAIL_MAX` = 200 before stdout. Re-read at this round's commit: the refusal is cleaned and bounded at `ISSUE_FAULT_DETAIL_MAX` = 2000, now through `bounded_issue_fault_detail` at :5123, and `test_the_filing_refusal_carries_no_control_characters_from_the_body` at `test_filing_decision_provenance.py:742` passes. Round 6 read this sentence as having two destinations; S3-R7-03 names the other two and gives all four one rule. | fixed in 99e0410a |<br><br>Leads not pursued: `.hexaemeron/study.md:230` and its shipped copy `docs/route-a-zero-decision-study.md:230` answer on-call question 2 with `hexctl verify`, which the third amendment made false -- the comparison needs `--check-filing-decision`. Carried unchanged from rounds 2, 3, 4, 5, 6 and 7 and priced there: a study amendment moves `study_sha256` and silently drops all fifteen runbook amendments. Recorded, not raised, and the study is not amended; `.hexaemeron/study.md` is byte-identical at sha256 83ae2f351ef9e07b8f6bc2e2f9ab7b8396bd9f81559d70cb780d80abc24b7524, the digest the packet's `risk_register.sha256` carries, and the shipped copy stands at f25ea0c58a01feb09cc97c2d27d96899778e591658b191f85a6ae1f83cb6ae3c, differing only in five relative link targets rewritten for its own path. THE ENUMERATION, AND ITS SCOPE. Round 7 enumerated from the two transports forward, stated its scope before its result, and named three surfaces it had excluded and did not claim clean. Those three are this round's whole subject, and this round adds no fourth of its own. SCOPE: the three surfaces round 7 named, read end to end under the rule the class is stated in -- unread takes the sentinel, read-and-absent takes `None`, never coerce -- together with every function each one calls to obtain its bytes and every destination each one's derived text reaches. It does not re-enumerate round 7's twenty-four GitHub payload expressions, and it opens no new surface. A ninth round can falsify it by naming a fourth surface these three call, or a destination of theirs this round did not follow. SURFACE ONE, LOCAL GIT OUTPUT: not in the class, and not a finding. `verify_local_commit` at :13257 reads the commit message at :13324 through `_exact_commit_git` at :12989 into `bounded_git` at :12390, `bounded_tool` at :12365 and `bounded_run` at :12337, and `HOST_BYLINE_RE.search(body)` at :13341 is the reader round 7 named. Every failure is closed and none is substituted: a non-zero git exit dies at the named refusal, a start failure dies, a timeout dies at `GIT_TIMEOUT`, output above `GIT_OUTPUT_MAX` = 2097152 dies at the cap rather than truncating, and `tool_text` at :12887 dies on bytes that are not UTF-8. Driven at six inputs against a scratch repository: a clean message and a valid UTF-8 non-ASCII message both read whole with one co-author trailer and no byline match; a 3 MiB message exits at `git --no-replace-objects exceeded 2097152-byte output cap`; an unresolvable SHA exits at the caller's refusal; a message carrying `Generated with Claude Code` matches the byline gate. A raw latin-1 message does not reach `tool_text`'s refusal, because `git commit` transcodes it: the byte `0xe9` was written and the commit object holds `0xc3 0xa9`, so that branch is defensive against a hand-written object rather than against `git commit`. Read and left. One property is read and left rather than raised: `bounded_probe` at :12263 merges the child's stderr into stdout, so a git warning would enter `body`. It can only add lines, and the gates below it refuse a byline or a trailer count that is not exactly 1, so the only direction it moves is refusal. SURFACE TWO, THE RUN'S OWN PULL REQUEST BODY: in the class in one place, which is unreachable, and the source of both this round's findings, neither of which is the class. The class shape is at `carried_forward_record`, which substitutes `carried_forward_section(text) or ""` at :5584 and `len(carried_forward_lines(text) or [])` at :5588, and at `filed_issue_publication_records`, which substitutes the same `or ""` at :5611. An absent carried-forward section reaching either would be recorded in the integrate receipt as `lines: 0` with an empty `carryover`, `filed` and `duplicates` -- a run reported as carrying nothing forward on the strength of a body that never answered the question, and distinguishable in the receipt from a real `none | none | ...` row only by that row's absence. Observed by direct call at `b9eb5a31`: `carried_forward_record` on a body with no section returns exactly that record. It is unreachable through `done integrate`, which calls `carried_forward_fault` at :11035 and dies on the absent section at :11036 before either runs, and those are the only two call sites, at :11038 and :11088. Unreachable and carried, not raised, on the terms rounds 6 and 7 carried `recorded_provenance`. `carryover_triage` at :4710 and `carried_forward_section` at :4538 both apply the rule the step is about correctly and deliberately: `fenced_block_rows` returns `None` for a block that never opened and an empty list for one that opened and holds nothing, and its docstring says why. One further property is read and left: `done integrate` opens the same path three times, at :5540, :5582 and :5609, with `inspect_pull_request` and `verify_github_commits` between the first and the third. A body edited in that window would be gated on one read and receipted from another. The receipt's `sha256` is taken from the bytes it recorded rows from, so it is self-consistent about what it read; the gate is what could have passed on different bytes. Local file, operator-owned, one command. Read and left. SURFACE THREE, THE DESIGN-EVIDENCE READS: not in the class, and not a finding. `_checked_design_transition` at :6988 lstats the artefact and dies if it is unavailable, refuses a symlink or a non-regular file, reads it through `read_bounded_source`, digests it, refuses a digest that differs from the receipted one, re-reads it after the checker runs and refuses `second != first`, and refuses a selected candidate that changed after design-lock. `_design_checker_receipt` at :6901 decodes the checker's output under `(UnicodeDecodeError, ValueError, TypeError)` and dies, then refuses a payload that is not a dict or whose key set is not exactly the five expected, findings that are not a list, a wrong schema, a wrong transition, a selected candidate that is not a kebab-case string, a consumed list above `DESIGN_CONSUMED_MAX`, a report whose key set is wrong, a malformed identity, unordered reports, an unsafe or absolute report path, and a digest that is not 64 hex characters. `receipted_design_evidence` at :7055 re-digests the artefact against the receipt on every delegation. Nothing is coerced and no absent field is named as a value at any of them. THE NEIGHBOURING SITES, NAMED AND OUT OF SCOPE. `carried_forward_fault`'s `except OSError` alone was not unique to it. Five sibling local-file readers in the same file carry it: `ledger_version` at :2887, `frontier_close_fault` at :5720, the ledger read inside `_subtracted_published_versions` at :5695, `raw_breadcrumbs` at :12590 and `read_breadcrumbs` at :12606. Three of those five are worse than S3-R8-01 in the direction of the class rather than the traceback, because they answer with a value instead of dying: `ledger_version` returns `None`, and `raw_breadcrumbs` and `read_breadcrumbs` return `[]`, so an unreadable file and an absent one give the same answer as a file that genuinely holds nothing. Against them, six `except` clauses in this file already pair a file read with the decode class, at :2117, :5542, :5953, :11763, :11788 and :17173, and `UnicodeDecodeError` is guarded at 45 places overall, so the house rule exists and these five deviate from it. None of the five is one of round 7's three surfaces and none reads the run-level pull request body, so none is repaired here; naming them with their exact lines is what stops the next round reconstructing the sweep. This is the one place this round declines work it can see, and it declines it to keep the scope statement above falsifiable rather than elastic. THE STANDING RESIDUES, RE-TAKEN. The sentinel is still the literal string `unknown`: `rest_filing_stamps` at :5223 records `unknown` for `{"updated_at": "unknown"}`, after which `uncomparable_filing_field` at :17738 reports `this run recorded it as unknown, so it read no value to compare`. It reaches only `created_at`, `updated_at` and `last_edited_at`, it needs an API or a `gh` returning the literal word where GitHub returns an ISO timestamp, and `sha256` and `edit_count` still compare, so it hides no body edit. Unreachable and carried, not raised. `recorded_provenance` at :17813 still tests block presence while `was_provenance.get(...)` reads per field; `rest_filing_stamps` returns exactly two keys on every path and `github_issue_edit_provenance` exactly four on every return, so reaching the gap needs a hand-edited or foreign-written `state.json`. Unreachable and carried, not raised. The `reason` field still carries one joined string that neither the comparison nor the printer reads. `created_at` and `prior_fiat_required` are recorded and never compared. `admitted_issue_body`'s cap governs the current body only, so a prior revision's `diff` is parsed at `GIT_OUTPUT_MAX` rather than `ISSUE_BODY_BYTES_MAX` and is walked twice. The divergence row at :17934 still prints `clean(str(entry['recorded']))` with no length bound, over API-generated stamps. `inspect_pull_request` applies no length cap of its own to a pull request body, so `HOST_BYLINE_RE.search` and `markdown_prose_lines` walk whatever the transport delivered. `cmd_issue_check`'s clean path prints each carryover row's `id`, `disposition` and `reference` to stdout with no length bound, its rows bounded at 128 and its reasons at `CARRYOVER_REASON_BYTES_MAX` = 512. All read and left. Round 1's `-F` typed-parameter lead stands unchanged and unapplied, because the fake `gh` types nothing and the repair would ship unguarded. THE REGISTER'S OTHER LINES. `--filing-decision-override` appears 0 times in `hexctl.py`, and this round's emitted strings are the existing unreadable-body sentence now reached by a second exception class and one truncation marker, neither of which names a mechanism that would grant a run. `cmd_record` still refuses `task_issue_contract` at :6250, `save_state` at :2327 is byte-identical to the run base `f22de680` and still writes through `os.replace` at :2333, and `routed_filing_directive` at :5463 carries no provenance field, so `receipt-forgery`, `partial-write-of-the-receipt` and `routed-exit-mistaken-for-success` hold as rounds 1 to 7 read them. The two `gh` argv sites stand at :5288 and :12699 and there are no others. `plugins/hexaemeron/skills/fiat/EVOLUTION.md` at blob 91111113, `AGENTS.md` at df705057, `plugins/hexaemeron/skills/fiat/SKILL.md` at a96980de, `docs/decisions/ADR-067-gate-a-run-on-what-its-issue-filed.md` at 53eefaa4 and `docs/decisions/ADR-078-echo-fiat-required-as-a-filer-set-label.md` at 9ea97245 are all byte-identical to the run base `f22de680`, so `held-job-drift`, `reviewed-span-drift` and `stale-claim-elsewhere` are unmoved in those five files. `S3-R5-` occurs 0 times in `hexctl.py` and 0 times in `test_filing_decision_provenance.py`, so S3-R7-01 stays closed, and every finding id this round's new comments cite -- S3-R1-02, S3-R6-01, S3-R7-02, S3-R8-01 and S3-R8-02 -- is issued by this run and resolves in this record. `AGENTS.md:117-118` still says `init` `refuses to start a run against a 0 before it creates any state, worktree or branch`, false since step 2, and left to step 5; skills#1454 records the register's scope-column gap. S2-R1-05, that exit status does not separate a routed `0` from a started run, is still open and belongs in the run pull request's `## Carried forward`; nothing this round touched `cmd_init`'s exit path. `tests/promise_machine_coverage.json` carries the controller digest in eleven bindings, and all eleven moved from ce1a9122d51572bd12b0fdf65fd36556a9ca254fc406fef9e2b1ee6af285acb7 to 6b70e10e5c9f779e46bcfa568ed816215db6ab2ce39ed8df1f5f91cb5f1610f8 in this round's commit. `plugins/hexaemeron/tests/test_hexctl.py` is unchanged at 259222 bytes, inside its 262144-byte bound, and `test_filing_decision_provenance.py` is now 52585 bytes against 46544 at round 7. `.horos/candidates.json` did not move; only `--census --write` was run. WHAT THIS ROUND CLAIMS, AND WHAT IT DOES NOT. The three surfaces round 7 excluded have now been read. Two are clean and are recorded as clean rather than as unexamined. The third carried two defects, both repaired, and neither of them is the coercion class: that class is untouched on all three surfaces, and its one shape here is unreachable behind a guard three lines above it. On the evidence of rounds 7 and 8 together, the class is now bounded by a scope statement rather than by a diff, and this round found nothing in it. That is the condition three earlier rounds asserted without the enumeration to carry it. This round does not assert it either; it records that the enumeration round 7 stated and round 8 completed has no remaining unread surface inside its own bounds, and names the five sibling readers outside them as the work a ninth round would take if one is called.
