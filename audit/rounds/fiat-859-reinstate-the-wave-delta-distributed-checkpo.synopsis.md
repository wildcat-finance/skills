Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.md | source_sha256=0f028238e0a923378c8a979d7fcef10aa14762d1c5ba58850b0d8955a2093077 | h2_count=15
## Step 1, round 1 -- 2026-09-02T07:51:26Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity and no Foundry or Hardhat project. Three of the nine register concerns have no surface at this commit: ADR-070 through ADR-073 do not exist, so `retired-body-drift` has nothing to diff; no issue body was edited and `docs/wave-delta-issue-estate-2026-09-02.md` does not exist, so `issue-body-publication` has nothing to read back; and neither fresh programme document exists, so `banner-inversion` has no pair to invert. Also not checked: whether the four successors, the estate rewrite, the fresh programme pair and `docs/wave-delta-reinstatement-demo.py` do what the runbook says, all of which are steps 2 through 7 exits and cannot be stood in for here. Also not checked: whether GitHub hides pull request #569 rather than lacking it; `gh api repos/wildcat-finance/skills/pulls/569` answers HTTP 404, which does not separate a deleted item from a hidden one. Also not checked: the ADR-028 amendment's forward tense. It states that ADR-029 through ADR-032 "stay Retired" and each "gains one standing successor" while no successor exists at this commit; that is the register a decision record is written in and ADR-069 carries the timing ("They land in the two steps after this one"), so it is recorded as looked-at rather than as a finding.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | docs/wave-delta-reinstatement-study.md | Section 7 Sources cites `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `cmd_checkpoint_export` line 12629, `cmd_checkpoint_restore` line 13754, subparsers at line 14801. The first two are exact. The third is not: `sp.add_subparsers(dest="checkpoint_action", required=True)` is at line 14803, the `sub.add_parser("checkpoint", ...)` call opens at line 14799, and line 14801 is that call's closing parenthesis. A reader who jumps to 14801 lands on punctuation. The file is outside this delivery's write set and unchanged between `ff47f3070c8dce05c767b6c0dad65234c56870de` and `c76c2b722fcf1f4a754ca06f1fe8bbb8d9bf90be`, so the citation was wrong when written rather than overtaken. | open; repair route named below |<br>| S1-R1-02 | low | docs/wave-delta-reinstatement-study.md | Section 2 states "Each also records that PR #569 first published it under a number between ADR-024 and ADR-027". `gh issue view 569` and `gh api repos/wildcat-finance/skills/pulls/569` both answer that 569 does not resolve. The study's own `dead-issue-links` control requires that "one that does not resolve is named as unresolved in the text rather than linked". Half the control holds: #569 is plain prose, not a link. The other half does not: it is not named as unresolved, while #547 is named that way in the same document's Sources section ("#547 does not resolve"), so the convention was applied unevenly. The underlying claim is accurate and attributed -- `docs/decisions/ADR-029-separate-the-checkpoint-protocol-from-its-authority-service.md:10` reads "PR #569 published this record as ADR-024" -- so this is a missing qualification, not a false statement. | open; repair route named below |<br><br>Leads not pursued: both findings land in `docs/wave-delta-reinstatement-study.md`, which is byte-identical to the receipted controller artefact `.hexaemeron/study.md` at SHA-256 `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec`, the digest the round's own packet pins. Hand-editing the docs copy would break that identity and create the drift this round was asked to check for, and the amend path that repairs the controller artefact is a mutating controller command reserved to the orchestrator. Both are therefore left open with the route named rather than fixed on the stacked branch: amend `.hexaemeron/study.md` through the controller, then re-copy to `docs/`. Neither is worth a second round on its own. Second lead, examined and not filed as a finding against this delivery: `plugins/hexaemeron/tests/run_tests.py` answered a concurrent invocation with `status: scheduler-error` and `testsRun: 0`. That path is fail-closed -- `coordinator_main` returns 3 whenever `scheduler_errors` is non-empty, at `plugins/hexaemeron/tests/run_tests.py:3843`, before the pass/fail return at 3845 -- so a contention refusal cannot be read as a green suite, which is the failure mode that would have made it serious. What remains is that a step exit running the runner beside another invocation can take a spurious exit 3; that is a usability property of a file under `plugins/`, which the runbook's scope guard excludes from every step of this delivery, so it belongs to the Fiat frontier rather than here. Third lead, out of scope: `tests/test_decision_records.py:139` reads `if added and True:`, a no-op conjunct in the default-branch collision check; the test is not in this diff and its behaviour is correct, so it is noted rather than touched. Evidence for the covered concerns: the three lints exit 0 over all five changed paths and report `clean` having read real paths; the repository's declared root suite, `python3 -m unittest discover -s tests` from `tests/check-map-v1.json` `root-suite`, reports `Ran 1116 tests`, `OK`, exit 0, with no skips, so `test_no_number_collides_with_one_already_on_the_default_branch` compared rather than skipping. `adr-number-collision`: `git ls-tree -r --name-only origin/main -- docs/decisions` tops out at ADR-068, leaving 069 through 073 free, and `origin/main` is at the base commit `ff47f3070c8dce05c767b6c0dad65234c56870de`. `adr-028-clause-drift`: the slice `test_adr_supersedes_remote_transport_with_mandatory_local_handoff` reads, from the mandatory-local heading to the next `\n## `, is byte-identical across the base and the audited commit at 1089 characters and SHA-256 prefix `981913c56fc926a3`; the whole ADR-028 change is a pure 952-byte append after `## Consequences`, with the new `## Amendment: distributed layer reinstated (2026-09-02)` heading at line 241 and the mandatory-local slice still bounded by `## Alternatives` at line 189. `pinned-artefact-digest`: `EXPECTED_DIGESTS` covers only `docs/fiat-controller-checkpoint-study.md` and `docs/fiat-controller-checkpoint-runbook.md`, neither is in the diff, and `python3 -m unittest tests.test_decision_records tests.test_fiat_checkpoint_decision_record` runs 10 tests OK. `capsule-overclaim`: every capability ADR-069 and study section 2 assign to the reopened layer is disclaimed verbatim by `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md` -- "These commands do not create or verify the Git bundle, package an archive, handle keys or mint a semantic checkpoint identity" at its `## Outer recovery boundary`, and "not the semantic checkpoint identity, service acceptance or outer archive identity owned by the remaining Wave Delta work" at its manifest table -- and every capability the study says already ships is present there as "ref-to-commit map", "ledger prefix", and the `## Relocation transaction` section. `scope-creep-to-code`: `! git diff --name-only ff47f3070c8dce05c767b6c0dad65234c56870de..HEAD | grep -q '^plugins/'` exits 0, the diff is five files, and the sole deletion is `.horos/boundary.json` `files_walked` 2465 becoming 2468, which matches the three files added exactly. Design conformance against the locked candidate `additive-successors` at `.hexaemeron/design-evidence.json` SHA-256 `6a171b6f72d1a651ebd9b7dd32af331ba6a9aaff600f02100c328907d1b9d755`: ADR-069 states the reinstatement route, ADR-028 keeps Status Accepted with a pure append, and ADR-029 through ADR-032 are absent from the diff and retain "Retired, 2026-08-27". Artefact drift: `docs/wave-delta-reinstatement-study.md` equals `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` and `docs/wave-delta-reinstatement-runbook.md` equals `.hexaemeron/runbook.md` at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`, both matching the packet pins, with `diff` reporting no difference on either pair. Shipped-state claims all hold: `hexctl.py` declares exactly `export` and `restore` under `checkpoint`; `cmd_checkpoint_export` is at line 12629 and `cmd_checkpoint_restore` at line 13754 as cited; `fiat-v5.49.1` is the current version in `plugins/hexaemeron/skills/fiat/EVOLUTION.md`; rows `fiat-v5.35.1`, `fiat-v5.43.1` and `fiat-v5.44.1` say what section 2 says they say, and `fiat-v5.44.1` ends "until a distributed checkpoint framework is accepted", which is the hook ADR-069 uses; `tests/test_evolution_contract.py:222` is `self.assertIn("ADR-028", capsule["evidence"])` as cited; `tests/test_shipped_prose_lints.py:66` excludes `docs`; `docs/decisions/` holds ADR numbers 1 through 68 missing only 026 and 027, 66 files before this delivery's ADR-069. Estate: #859 through #867, #508, #899 and #901 all resolve OPEN under milestone 64, "Wave Δ — distributed Fiat checkpoints"; #508's title is "bind exhausted-audit carryover, delegated writes, and runbook gates", matching assumption 9's description of its lane; #899 and #901 are both `hexctl` controller defects by title, matching assumption 7; #547 does not resolve, exactly as Sources states; PR #1072 merged 2026-08-31 and PR #997 merged 2026-08-30 as cited. Links: both relative Markdown links in the diff resolve (ADR-069 to ADR-028, ADR-028 to ADR-069), and every absent path the two documents name is a step 2 through 7 output the runbook creates. Gates re-run independently and green: `protasis.py --study` clean, `protasis.py` on the runbook clean, `hypomnema.py --study --design-evidence` clean, `imprimatur.py` 100.0/100 with 0 defects on all four changed prose files, `promise_machine.py check` and `coverage --check` both exit 0.
## Step 1, round 2 -- 2026-09-03T15:14:20Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity and no Foundry or Hardhat project. Whether GitHub reports `verified: true` and `reason: valid` for the five stacked commits cannot be observed in this round, because nothing is pushed; that is the push receipt's own gate. What is observed locally is that all five carry a good signature from key `B83B60AE16F5DD1A`, author `Shoggoth <shoggoth@wildcat.finance>` and committer `Dr Laurence E. Day <laurence@wildcat.finance>`, which is the shape GitHub reported `verified: true`, `reason: valid` for `db31d809` on `main`. Three register concerns still have no surface at this commit: ADR-070 through ADR-073 do not exist, `docs/wave-delta-issue-estate-2026-09-02.md` does not exist and no issue body was edited, and neither fresh programme document exists, so `retired-body-drift`, `issue-body-publication` and `banner-inversion` have nothing to read. Whether the four successors, the estate rewrite, the fresh programme pair and `docs/wave-delta-reinstatement-demo.py` do what the runbook says remains a steps 2 through 7 exit and is not stood in for here. Five study claims were re-read against `origin/main` at `01a17bed45058a1fc20875bb19765fdf91cb293a`: the current Fiat version, the `checkpoint` subcommand surface, the `docs/decisions/` numbering, the ADR-028 mandatory-local slice, and `tests/test_evolution_contract.py` line 222. The twelve issue numbers and the two merged pull requests the study cites were read at the base on 2026-09-02 and were not re-read against the moved branch. Whether the run branch merges cleanly into the advanced base was not tested; the base moved 58 commits and that composition belongs to the integrate phase. #569 remains unresolvable rather than proved absent: `gh api repos/wildcat-finance/skills/pulls/569` answers HTTP 404, which does not separate a deleted item from a hidden one.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | medium | docs/decisions/ADR-069-reinstate-the-distributed-checkpoint-layer-above-the-local-store.md | ADR-069 listed a semantic checkpoint identity distinct from archive bytes among the things the reopened layer adds, and the study's section 2 said `hexctl.py` defines exactly two checkpoint subcommands with no intake, publication, acceptance, revocation or lineage-resolution command. Both hold at the base `ff47f3070c8dce05c767b6c0dad65234c56870de` and neither holds on `origin/main`, which is 58 commits ahead at `01a17bed45058a1fc20875bb19765fdf91cb293a`. Upstream commit `482172e7` adds `cmd_checkpoint_identity` at line 13661 and the `identity` subparser at line 16206, help text `print one verified semantic checkpoint identity`, and amends ADR-028 with `## Amendment: Immutable run anchors and checkpoint identity (2026-08-29)`, which derives that identity from the verified run anchor and separates it from exact capsule and archive bytes. A governing record that merges into that branch would claim as still-owed a capability already accepted there, which is the `capsule-overclaim` concern reached through a moved base rather than through prose. | fixed in 5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f |<br>| S1-R2-02 | low | docs/wave-delta-reinstatement-study.md | Sections 2 and 7 cited `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` by line: `cmd_checkpoint_export` 12629, `cmd_checkpoint_restore` 13754, subparsers 14803. All three are exact at the base and none survives the move: on `origin/main` the same file has changed by 1444 insertions and 36 deletions, and those three lines hold a `die` call, an `os.stat` call and a `for` statement, while the cited definitions sit at 13971, 15096 and 16205. This is the same class as S1-R1-01, caused by the base advancing rather than by a mis-read. | fixed in 5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f |<br>| S1-R2-03 | high | commit metadata on the stacked branch | The three stacked commits written after the receipted implement commit carried committer `Shoggoth <shoggoth@wildcat.finance>` while signed with Laurence's key `B83B60AE16F5DD1A`: `9ab3fd5d`, `aeba6bac` and `693d2b9c`. The receipted implement commit `c76c2b722fcf1f4a754ca06f1fe8bbb8d9bf90be` carries committer `Dr Laurence E. Day <laurence@wildcat.finance>`, so the step held two identity shapes at once. Fiat refuses a push receipt unless GitHub reports `verified: true` and `reason: valid` for every commit in the pushed range, and GitHub ties a GPG signature to an email verified on the account holding the key; no commit with committer `shoggoth@wildcat.finance` appears in the last 300 commits of `origin/main`. Discovered at `done push` this costs the branch, because re-signing rewrites receipted SHAs. Nothing was pushed and none of the three was receipted, so the repair was free here. | fixed by rewriting 9ab3fd5d, aeba6bac and 693d2b9c as 71f0df1d, 69112d0b and 0bac93c2, tree identical |<br><br>Leads not pursued: The docs copy of the study now differs from the receipted controller artefact in four hunks rather than two. `.hexaemeron/study.md` stays at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` and `docs/wave-delta-reinstatement-study.md` is at `0a435548a7c529ad9c3cba7e962ff047f921389264d75e675b41ead368ea07a4`. The controller amend path appends a dated block and cannot revise a sentence in place, so the receipted artefact remains the record of what was specified at receipt time and the docs copy is the governing document; steps 4 and 6 take their account of the shipped half from the docs copy, and this record is where that instruction lives. The runbook pair stays byte-identical at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`. Second lead: the upstream ADR-028 amendment inserts before `## Amendment: Mandatory local checkpoint hand-off (2026-08-30)` while this step's amendment appends at end of file, so the two edits occupy different regions and a three-way merge has no overlap to resolve; the resulting order of amendment sections was reasoned about rather than proved by a trial merge, which belongs to the integrate phase. Third lead, unchanged from round 1 and outside this diff: `tests/test_decision_records.py:139` reads `if added and True:`. Fourth lead, also unchanged: `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures; the real invocation in this round finished `status: passed` with 2204 tests, 0 failures, 0 errors and 0 skipped, and the runner is fail-closed at `plugins/hexaemeron/tests/run_tests.py:3843`, so a contention refusal cannot read as a green suite. Evidence for the covered concerns: the three lints exit 0 over all seven changed paths and report `clean` having read real paths; the declared root suite `python3 -m unittest discover -s tests` from `tests/check-map-v1.json` `root-suite` reports `Ran 1116 tests`, `OK`, exit 0, with no skips, so `test_no_number_collides_with_one_already_on_the_default_branch` compared rather than skipped. `adr-number-collision`: `git ls-tree -r --name-only origin/main -- docs/decisions` tops out at ADR-068 and holds no ADR-069, so 069 through 073 remain free on the moved branch. `adr-028-clause-drift`: this step's ADR-028 change is still a pure 17-line append after the last section, the upstream change to the same file lands 68 lines earlier, and `python3 -m unittest tests.test_decision_records tests.test_fiat_checkpoint_decision_record` runs 10 tests OK. `pinned-artefact-digest`: `docs/fiat-controller-checkpoint-study.md` and `docs/fiat-controller-checkpoint-runbook.md` are absent from the seven-path diff. `capsule-overclaim`: S1-R2-01 is its finding and the repair leaves every remaining item in ADR-069's list absent from both `controller-checkpoint.md` and upstream `hexctl`. `dead-issue-links`: the two relative links in the diff resolve, and the qualification convention now covers #569 and #547 alike. `scope-creep-to-code`: `! git diff --name-only ff47f307..HEAD | grep -q '^plugins/'` exits 0. Prose and record gates re-run green on the fixed tree: `imprimatur` scores 100.0/100 with 0 defects on all four changed prose files, `protasis --study` and `protasis` on the runbook are clean, `hypomnema --study --design-evidence` is clean, `promise_machine check` reports 18 plugins and `coverage --check` 94 of 94, and `tests.test_boundary_currency` with `tests.test_audit_synopsis_currency` run 46 tests OK. Identity evidence: `scripts/check_commit_identity.py` against a bare replica of the rewritten branch reports `status: passed`, `commit_count: 5`, `shoggoth_author_count: 5`, `human_author_count: 0`. Elenchus: `5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f` is `unguarded`, detail `the commit changed no test files`, which is correct for three prose repairs and one metadata rewrite; no test can guard a citation's accuracy, and the boundary counts that can be guarded are covered by the 46 currency tests above.
## Step 1, round 3 -- 2026-09-03T15:20:56Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the seven stacked commits is still unobservable because nothing is pushed; locally all seven carry a good signature from key `B83B60AE16F5DD1A`, author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`, and `scripts/check_commit_identity.py` against a bare replica reports `status: passed`, `commit_count: 7`, `shoggoth_author_count: 7`, `human_author_count: 0`. `retired-body-drift`, `issue-body-publication` and `banner-inversion` still have no surface: ADR-070 through ADR-073, `docs/wave-delta-issue-estate-2026-09-02.md` and the fresh programme pair do not exist and no issue body was edited. Steps 2 through 7 exits are not stood in for here. The twelve issue numbers and two merged pull requests the study cites were read at the base on 2026-09-02 and are still not re-read against `origin/main`. No trial merge into the advanced base was attempted; that composition is integrate-phase work. #569 remains unresolvable rather than proved absent, since HTTP 404 does not separate a deleted item from a hidden one.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R3-01 | low | docs/wave-delta-reinstatement-study.md | Round 2 corrected section 2 and ADR-069 and left two later passages in the same study contradicting them. The sentence closing section 2's shipped half still opened `So the distributed programme still needs, on top of the capsule: a semantic checkpoint identity distinct from archive bytes`, and the `Distributed layer` glossary seed still listed `semantic identity` first among what the programme adds. Both are false on `origin/main` at `01a17bed45058a1fc20875bb19765fdf91cb293a` for the same reason S1-R2-01 was, and internally inconsistent with lines 102 to 113 of their own document. Incomplete repair rather than a new defect. | fixed in 0677fefab8baf00dbfd0d3cca64912f57220388d |<br><br>Leads not pursued: The docs copy of the study now differs from the receipted controller artefact `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` in five hunks; the docs copy is at `028bdee21a07a672ca04ecba084dc9ee55f9b71162ed3e3fe50e372ff290e9a9`. The disposition is unchanged from round 2: the controller amend path appends and cannot revise in place, so the receipted artefact records what was specified and the docs copy governs. `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md` is byte-identical between the base and `origin/main`, so the study's quotation of its manifest-digest disclaimer at lines 118 to 121 still holds, and that disclaimer stays true beside the new `hexctl checkpoint identity`: the manifest digest names exact controller bytes and the identity is a separate output. Two long-standing leads outside this diff are unchanged: `tests/test_decision_records.py:139` reads `if added and True:`, and `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures while the real invocation passes. Evidence for the covered concerns at this commit: the three lints exit 0 over all seven changed paths reading real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, no skips; `origin/main` holds no ADR-069 and tops out at ADR-068; this step's ADR-028 change is still a pure append after the final section while the upstream amendment lands 68 lines earlier; the two digest-pinned checkpoint documents are absent from the seven-path diff; the `plugins/` scope guard exits 0; and imprimatur scores 100.0/100 with 0 defects on all four changed prose files, with protasis, hypomnema and 56 currency and pinned-record tests clean. Elenchus reports `unguarded` for `0677fefab8baf00dbfd0d3cca64912f57220388d`, detail `the commit changed no test files`, which is correct for a prose repair no test can guard.
## Step 1, round 4 -- 2026-09-03T15:25:41Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the eight stacked commits remains unobservable because nothing is pushed; locally all eight carry a good signature from key `B83B60AE16F5DD1A` with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. `retired-body-drift`, `issue-body-publication` and `banner-inversion` still have no surface at this commit. Steps 2 through 7 exits are not stood in for here. Every comparison in this round is against `origin/main` at `01a17bed45058a1fc20875bb19765fdf91cb293a`, re-fetched immediately before the round and still 58 commits ahead of the base; a later advance is integrate-phase work and no trial merge was attempted. #569 and #547 remain unresolvable rather than proved absent, since HTTP 404 does not separate a deleted item from a hidden one.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R4-01 | low | docs/wave-delta-reinstatement-study.md | Assumption 9 recorded the maintainer's 2026-09-02 decision that #508 no longer gates #860 and that #860 is startable once the reinstatement records land, and said nothing about #860's controller half having landed. Upstream `482172e7`, committed 2026-09-02 09:47:52 +0100, 48 minutes after this step's implement commit `c76c2b722fcf1f4a754ca06f1fe8bbb8d9bf90be`, reapplies pull request #1069: init resolves one immutable starting commit, an init-owned `fiat-run-anchor/v1` receipt joins the run identity to it, and `hexctl checkpoint identity` derives one semantic checkpoint identity. That is #860's stated subject, and #860 is still open. This is the estate half of S1-R2-01, read through the issues rather than through `hexctl`. The maintainer's recorded decision text is unchanged; one sourced sentence is added after it, pointing steps 4 through 6 at the commit rather than the issue title. | fixed in a5f45c944d151808a820943d0d41c555600a8850 |<br><br>Leads not pursued: Round 3's open gap is closed rather than carried: every number the study cites was re-read from GitHub in this round. #859 through #867, #508, #899 and #901 are all OPEN under milestone `Wave Δ — distributed Fiat checkpoints`; #547 and #569 both answer HTTP 404; pull request #1072 merged 2026-08-31T23:01:06Z and #997 merged 2026-08-30T23:10:43Z, as the study states. `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md` is still byte-identical between the base and `origin/main`, so the study's quotation of its manifest-digest disclaimer holds. The docs copy of the study now differs from the receipted controller artefact `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` in six hunks and is at `8d9ba18bdd7b9ffd3d4b45aedf34246c0f7178eedfba0406c7c0ae19a89edace`; the disposition is unchanged from round 2, and the runbook pair stays byte-identical at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`. Two long-standing leads outside this diff are unchanged: `tests/test_decision_records.py:139` reads `if added and True:`, and `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures while the real invocation passes. Evidence for the covered concerns at this commit: the three lints exit 0 over all seven changed paths reading real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, with no skips; `origin/main` holds no ADR-069 and tops out at ADR-068; this step's ADR-028 change is still a pure append after the final section while the upstream amendment lands 68 lines earlier; the two digest-pinned checkpoint documents are absent from the seven-path diff; the `plugins/` scope guard exits 0; and imprimatur holds 100.0/100 with 0 defects on the changed study, with protasis and hypomnema clean. Elenchus reports `unguarded` for `a5f45c944d151808a820943d0d41c555600a8850`, detail `the commit changed no test files`, which is correct for one sourced sentence no test can guard.
## Step 1, round 5 -- 2026-09-03T15:30:54Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the ten stacked commits remains unobservable because nothing is pushed; locally all ten carry a good signature with committer `laurence@wildcat.finance`. `retired-body-drift`, `issue-body-publication` and `banner-inversion` still have no surface at this commit, and steps 2 through 7 exits are not stood in for here. What load produced the timeout in S1-R5-01 was not identified: this Mac runs concurrent sessions and the runner records no contention signal for a same-process unittest run, so the record states the observation and not a cause beyond the expired budget. No trial merge into the advanced base was attempted. #547 and #569 remain unresolvable rather than proved absent.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R5-01 | low | tests/test_agent_instruction.py | The declared root suite exited 1 on this tree: `test_agent_instruction.MeasurementTests.test_fake_tokenizer_model_mismatch_refuses` expected refusal `WAI-E-TOKENIZER.MISMATCH` and got `WAI-E-ADAPTER.TIMEOUT` at `tests/test_agent_instruction.py:2547` through `assertRefusal` at line 583. Localised, not guessed: the profile that test uses sets `timeout_seconds` to `2` at line 2252, while the sibling `test_fake_timeout_refuses` at line 2556 sets it to `1` and asserts the timeout code, so the module treats a two-second budget as the expected-success case. Under load the two-second budget expires too and the mismatch assertion sees the timeout refusal instead. Reproduced green in isolation immediately afterwards: the single test runs 1 test OK in 0.130s and the whole module runs 310 tests OK in 9.276s. The same tree ran `Ran 1116 tests`, `OK`, exit 0 three times in rounds 2, 3 and 4, and the only change since round 4 is the round-4 audit record and its synopsis. The file is not in this delivery's seven-path diff, and the runbook's scope guard confines every step of this delivery to records and documents, so widening the budget or making the case load-independent is not this step's work. | accepted; load-sensitive budget in a module this delivery does not own, reproduced green in isolation |<br><br>Leads not pursued: The route for S1-R5-01 is a separate issue against the measurement harness rather than a fix here: the two-second budget at `tests/test_agent_instruction.py:2252` decides a refusal-code assertion, so any machine slow enough turns one expected refusal into another, and the fix belongs with whoever owns that module. It earns a `## Carried forward` row at integration. Round 4's issue-number gap stays closed and nothing re-opened it. The docs copy of the study still differs from the receipted controller artefact `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` in six hunks, with the disposition unchanged from round 2. Checked in this round and holding: ADR-069's Decision clause says the ADR-028 amendment is appended after that record's `## Consequences` section, and it is, at line 241 directly after `## Consequences` at line 220, with `## Alternatives` at 189 and the two earlier amendments at 125 and 168; the upstream amendment on `origin/main` inserts before `## Amendment: Mandatory local checkpoint hand-off (2026-08-30)` so this delivery's section stays last after a merge. `tests/test_shipped_prose_lints.py` line 66 reads `if parts[0] in ("audit", "docs"):` identically at the base and on `origin/main`. `docs/decisions/` holds 66 ADR records at both the base and `origin/main`, so the study's count of records holds; the directory holds one additional non-ADR file on the moved branch, `draft-fix-the-issue-status-block-markers.md`, which does not bear a number and does not change the numbering claim it supports. The three cited `EVOLUTION.md` rows `fiat-v5.35.1`, `fiat-v5.43.1` and `fiat-v5.44.1` are all still present upstream and `fiat-v5.49.1` is still the current version. Two long-standing leads outside this diff are unchanged: `tests/test_decision_records.py:139` reads `if added and True:`, and `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures while the real invocation passes. Mechanical evidence for this round: the three lints exit 0 over all seven changed paths reading real paths; imprimatur scores 100.0/100 with 0 defects on all four changed prose files; `promise_machine check` reports 18 plugins and `coverage --check` 94 of 94.
## Step 1, round 6 -- 2026-09-03T15:33:29Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the eleven stacked commits stays unobservable until the push phase, because nothing is pushed; locally every one carries a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. `retired-body-drift`, `issue-body-publication` and `banner-inversion` have no surface at this commit: ADR-070 through ADR-073, `docs/wave-delta-issue-estate-2026-09-02.md` and the fresh programme pair do not exist and no issue body was edited. Whether the four successors, the estate rewrite, the fresh programme pair and `docs/wave-delta-reinstatement-demo.py` do what the runbook says are steps 2 through 7 exits and are not stood in for here. No trial merge into the advanced base was attempted; `origin/main` at `01a17bed45058a1fc20875bb19765fdf91cb293a` is 58 commits ahead of the base and that composition is integrate-phase work. #547 and #569 remain unresolvable rather than proved absent, since HTTP 404 does not separate a deleted item from a hidden one.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: S1-R5-01 stays accepted and open: the two-second adapter budget at `tests/test_agent_instruction.py:2252` sits outside this delivery's seven-path diff and earns a `## Carried forward` row at integration rather than an edit here. The declared root suite is green on this exact tree, `Ran 1116 tests`, `OK`, exit 0, no skips, which is the fourth green run of it on this branch against one red run whose single failure reproduced green in isolation. The three lints exit 0 over all seven changed paths and report `clean` having read real paths. Every finding from rounds 1 through 4 is fixed on this branch: S1-R1-01 and S1-R1-02 in `69112d0b`, S1-R2-01 and S1-R2-02 in `5a5490a7`, S1-R2-03 by rewriting three unpushed commits as `71f0df1d`, `69112d0b` and `0bac93c2`, S1-R3-01 in `0677fefa`, and S1-R4-01 in `a5f45c94`. Nothing new was found in this round. The docs copy of the study remains six hunks away from the receipted controller artefact `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec`, with the disposition recorded in round 2, and the runbook pair stays byte-identical at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`. Two leads outside this diff are unchanged: `tests/test_decision_records.py:139` reads `if added and True:`, and `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures while the real invocation passes.
## Step 2, round 1 -- 2026-09-03T17:04:14Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=reviewed; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships two Markdown records. `adr-028-clause-drift` has no surface here: ADR-028 is absent from the three-path diff and its amendment landed in step 1. `issue-body-publication` and `banner-inversion` still have none: no issue body was edited and neither fresh programme document exists. Whether ADR-072 and ADR-073 carry ADR-031 and ADR-032 correctly is step 3's exit and is not stood in for here. GitHub verification of the step-2 commits is unobservable until the push phase; locally all three carry a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. Whether the claims ADR-070 and ADR-071 make about a future service are good engineering was not assessed; the round checked that they carry their sources faithfully, that nothing they call outstanding is already specified, and that they authorise nothing. `origin/main` was read at `01a17bed45058a1fc20875bb19765fdf91cb293a` and no fresh fetch was taken in this round.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | docs/decisions/ADR-070-separate-the-checkpoint-protocol-from-its-authority-service.md | The Dropped paragraph carried #563 and #564 out of ADR-029 as the entry gates that record named. Neither resolves: `gh api repos/wildcat-finance/skills/issues/563` and the same call for 564 both answer HTTP 404 on 2026-09-03. The study's `dead-issue-links` control requires a number that does not resolve to be named as unresolved in the text rather than linked, which is the treatment #547 and #569 already carry. Same class as S1-R1-02, and the only two numbers either new record cites. | fixed in 618208f99a3c63d9fb42f644757bf6ab1b7c6f90 |<br>| S2-R1-02 | low | .horos/boundary.json | The root suite went red after the implement commit: `test_horos_boundary_is_current_for_the_scaffold` at `tests/test_agent_instruction.py:116` and `test_the_committed_boundary_matches_a_fresh_scan` at `tests/test_boundary_currency.py:164` both reported `.horos/boundary.json#counts` drift. Adding the two records moved `files_walked` from 2470 to 2472. Neither test fires before the commit, because an untracked file is not walked, so the implement phase saw a green suite and the audit round saw a red one on the same content. Step 2's runbook Files field says `Changed: none`, so the repair is a recorded deviation rather than a silent edit; step 1 hit the same mechanism at `0bac93c2`. | fixed in b18b3f02c0f576d13db5d16ca8b28cdccac662d1 by the regeneration command the failure names |<br>| S2-R1-03 | low | .hexaemeron/runbook.md | The Exit field of steps 2, 3, 4 and 5 each carries a retired-record check built from `git diff --name-only` over a range and two paths, piped into `wc -l` and then into `grep -qx '0'`, at runbook lines 169, 218, 267 and 316. That command cannot exit 0 on this machine: BSD `wc -l` right-pads its count to eight columns, so the line `grep -qx` receives is eight characters of padding followed by a zero, and the anchored match fails. The check's subject holds for step 2 either way. Zero files differ for ADR-029 and ADR-030 across `HEAD~1..HEAD` and across `ff47f307..HEAD`, and inserting `tr -d` of a space between the count and the match exits 0. Every future step carrying this pattern will meet it again. | accepted; substance verified by the trimmed variant, carried forward at integration rather than amended |<br><br>Leads not pursued: The route for S2-R1-03 is either a runbook amendment or a carried-forward row, and this round chose the row. `hexctl amend runbook` accepts at most one `Complete replacement Exit:` clause per amendment, checked at `_complete_replacement_fields` in the controller, so correcting four steps means four separate controller transitions for a shell portability defect whose subject is already verified by an equivalent command. A reviewer who disagrees can amend; the finding records the exact lines and the passing form either way. Evidence for the covered concerns: the three lints exit 0 over all three changed paths and report `clean` having read real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, no skips, after the boundary repair. `retired-body-drift`: ADR-029 and ADR-030 are byte-identical to the base, zero files differ across `ff47f307..HEAD`, and each successor carries an explicit three-way split. ADR-070 names what it carries verbatim, the three-way ownership, the pinned protocol handshake, the no-secrets rule and Ariadne as the acceptance boundary; what it rebases on `fiat-v5.49.1`, three of ADR-029's six protocol items having shipped in part, so the owed list narrows to acceptance, revocation, resolution and redacted-discovery schemas, identity rules past one run's controller state, pinned keys with typed transitions, the publication fence and compatibility fixtures; and what it drops, the Proposed framing and the two unresolvable numbers. ADR-071 names its carried durability rules, its three rebases, an acceptance statement binding outer archive digest, capsule manifest digest and semantic checkpoint identity, a validator sized against a real 94 MB step checkpoint, and every clause read as a requirement on an unauthorised service; and its drop, the DigitalOcean and AWS commitments, left in ADR-030 as the worked example. `capsule-overclaim`: every item either record calls outstanding is absent from `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md`, which specifies the capsule manifest, the read boundary, atomic no-replace publication, restore and the relocation transaction, and which states in its own words that it does not replace the standing Git bundle, signature proof, local archive, outer sidecar or hand-off. ADR-071's 94 MB figure is this run's own step-1 checkpoint at 93,975,335 bytes. `adr-number-collision`: `origin/main` holds no ADR-070 or ADR-071 and tops out at ADR-068. `pinned-artefact-digest`: neither digest-pinned checkpoint document is in the diff, and the two pinned decision-record modules run 10 tests OK. `scope-creep-to-code`: the `plugins/` guard exits 0 and the diff is three files. Prose gates: imprimatur scores 100.0/100 with 0 defects on both records, after two defects in ADR-071 were repaired before the implement commit, and hypomnema over `docs/decisions` is clean. Elenchus reports `unguarded` for `b18b3f02c0f576d13db5d16ca8b28cdccac662d1`, detail `the commit changed no test files`, which is correct for a prose repair and a regenerated counts file. One lead not pursued: `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` also rewrites `.horos/candidates.json`, whose byte totals follow the growing audit record; that file is not gated by any test in the root suite and was restored rather than committed, so this step's diff keeps the shape its runbook declares.
## Step 2, round 2 -- 2026-09-03T17:09:47Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=reviewed; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the step-2 commits is unobservable until the push phase; locally all five carry a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. `adr-028-clause-drift`, `issue-body-publication` and `banner-inversion` have no surface at this commit. Steps 3 through 7 exits are not stood in for here. Whether the engineering judgement inside ADR-070 and ADR-071 is right was not assessed; the round checked carriage, overclaim and authority. `origin/main` was read at `01a17bed45058a1fc20875bb19765fdf91cb293a`, unchanged since step 1's round 4 fetch, and no fresh fetch was taken in this round.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R2-01 | low | docs/decisions/ADR-070-separate-the-checkpoint-protocol-from-its-authority-service.md | Round 1's repair at `618208f9` replaced a sentence without rewrapping what followed, leaving one prose line at 142 columns in a document whose other prose lines wrap near 78. No lint catches it: imprimatur scored 100.0/100 with the long line present, and hypomnema is a pointer check. The corpus does carry longer lines where a Markdown link target or a fenced register row cannot wrap, which is why this one is a regression rather than a house-style complaint: it is ordinary prose in the middle of a wrapped paragraph. | fixed in d931098c0e8f8c5fea9d52032081a6825ca12b2a, content unchanged |<br><br>Leads not pursued: The same class survives in a step-1 file this step may not touch: `docs/wave-delta-reinstatement-study.md:154` runs to 100 columns, left by step 1's round-1 repair at `69112d0b`, and that file is outside step 2's declared write set and already published in pull request 1162. It earns a carried-forward row at integration rather than an edit here. The pre-existing 138-column line at study:144 and the 264-column fenced register row at study:406 are not in that class; neither was written by this run's repairs and a fenced row cannot wrap. S2-R1-03 stays accepted and open: the retired-record check in the Exit fields of steps 2 through 5 cannot exit 0 under BSD `wc`, and the trimmed variant confirms its subject, zero files differing for ADR-029 and ADR-030 across `ff47f307..HEAD`. Evidence for the covered concerns at this commit: the three lints exit 0 over all five changed paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, no skips; imprimatur holds 100.0/100 with 0 defects on both records and hypomnema over `docs/decisions` is clean; `origin/main` holds no ADR-070 or ADR-071; the two digest-pinned checkpoint documents are absent from the diff and the pinned modules run 10 tests OK; the boundary and synopsis currency modules run 56 tests OK with the refreshed counts; and the `plugins/` scope guard exits 0. Elenchus reports `unguarded` for `d931098c0e8f8c5fea9d52032081a6825ca12b2a`, detail `the commit changed no test files`, which is correct for a rewrap that changed no content.
## Step 2, round 3 -- 2026-09-03T17:12:13Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=reviewed; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the six step-2 commits waits on the push phase; locally each carries a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. `adr-028-clause-drift`, `issue-body-publication` and `banner-inversion` have no surface at this commit. Steps 3 through 7 exits are not stood in for here, and no trial merge into the advanced base was attempted. `origin/main` was read at `01a17bed45058a1fc20875bb19765fdf91cb293a` and no fresh fetch was taken in this round.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Two items stay open and accepted, both carried forward at integration rather than fixed here. S2-R1-03: the retired-record check in the Exit fields of steps 2 through 5 cannot exit 0 under BSD `wc`, and its subject is confirmed by the trimmed variant, zero files differing for ADR-029 and ADR-030 across `ff47f307..HEAD`. S2-R2-01's sibling at `docs/wave-delta-reinstatement-study.md:154`, a 100-column line left by step 1's round-1 repair, sits in a file outside this step's write set and already published in pull request 1162. Nothing new was found in this round. Every finding this step raised is fixed: S2-R1-01 in `618208f9`, S2-R1-02 in `b18b3f02`, S2-R2-01 in `d931098c`. Evidence at this commit: the three lints exit 0 over all five changed paths and report `clean` having read real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, with no skips, the third green run of it on this step; ADR-070 now carries no prose line over 85 columns outside its two link lines; ADR-029 and ADR-030 remain byte-identical to the base; `origin/main` holds no ADR-070 or ADR-071 and tops out at ADR-068; the two digest-pinned checkpoint documents are absent from the five-path diff; and the `plugins/` scope guard exits 0.
## Step 3, round 1 -- 2026-09-03T18:15:08Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=reviewed; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships two Markdown records. `adr-028-clause-drift`, `issue-body-publication` and `banner-inversion` have no surface at this commit. Steps 4 through 7 exits are not stood in for here. GitHub verification of the step-3 commits waits on the push phase; locally both carry a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. Whether the fence and the graph these records describe are the right engineering was not assessed; the round checked that they carry their sources, that nothing they call outstanding is already specified, that they authorise nothing, and that they do not contradict ADR-069 through ADR-071. ADR-073's claim that nothing prevents two restores of one capsule was checked against the restore contract's own refusals, which are all local to one origin, and not by performing two restores. `origin/main` was read at `01a17bed45058a1fc20875bb19765fdf91cb293a` and no fresh fetch was taken in this round.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | low | docs/decisions/ADR-072-fence-external-fiat-transitions-on-signed-checkpoint-acceptance.md | The Decision's first clause read as an immediate rule: an external run's transition is complete only once an independent validator has signed an acceptance, and until then the run does not advance. No validator exists or is authorised, so that clause read alone blocks every external run from today, while the paragraph two below it says the mandatory local checkpoint is the whole of the fence until a service is authorised. A governing record carrying both readings is a defect in the record rather than in the delivery around it. | fixed in 37f42d11afff3f051507e23f1bfed1d40f0d920a; the clause now opens on its condition and states the current position |<br><br>Leads not pursued: Two items from earlier steps stay accepted and open, both carried forward at integration: S2-R1-03, the retired-record check in the Exit fields of steps 2 through 5 that BSD `wc` cannot satisfy, and the 100-column line at `docs/wave-delta-reinstatement-study.md:154` in a step-1 file this step may not touch. Step 3's own boundary refresh was taken inside the implement commit rather than left for this round, because step 2 established the mechanism at S2-R1-02 and an audit round is for what is not yet known; the commit message records the deviation from the runbook's `Changed: none` and the count moving from 2472 to 2474. Evidence for the covered concerns: the three lints exit 0 over all three changed paths and report `clean` having read real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, with no skips, both before and after the fix. `retired-body-drift`: ADR-031 and ADR-032 are untouched, zero files differ across `HEAD~1..HEAD` under the trimmed check, and each successor carries the three-way split. ADR-072 names what it carries verbatim, the execution class recorded and never inferred, compulsory publication for external runs, the ask-first class change, the idempotency key, no offline bypass, a rejection that preserves refusal and archive identity, and an acceptance statement outside the archive it signs; what it rebases, ADR-031's first two steps having shipped as one of two exact export boundaries and `hexctl checkpoint export`, a statement naming ADR-071's three identities, and the fence stated as the external half of ADR-028's local rule; and what it drops, the two state-name literals and the assumption that an unpublished external run implies an outage. ADR-073 names its carried graph, stage rule, immutable edges, refusals, advisory claims, typed resolution, reconciliation as new checked work and append-only revocation; its three rebases, the scope tuple and node name now existing through `fiat-run-anchor/v1` and `hexctl checkpoint identity`, the fork case having a concrete local cause in two restores of one capsule, and revocation naming which identity it revokes; and its drop, the named database product. Successor closure checked mechanically: ADR-070 names ADR-029, ADR-071 names ADR-030, ADR-072 names ADR-031, ADR-073 names ADR-032, one each and no source with two. `capsule-overclaim`: everything either record calls outstanding, upload, independent validation, signing, a transition freeze, sibling nodes, resolutions and revocation, is absent from `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md`, and both records name what that contract does specify rather than describing it as missing. Upstream claims re-read at `origin/main`: `cmd_checkpoint_identity` is defined and ADR-028 there carries `fiat-run-anchor/v1`. `dead-issue-links`: neither record cites an issue or pull-request number, so the control has nothing to resolve. `adr-number-collision`: `origin/main` holds no ADR-072 or ADR-073. `pinned-artefact-digest`: neither digest-pinned checkpoint document is in the diff and the pinned modules run 10 tests OK. `scope-creep-to-code`: the `plugins/` guard exits 0. Elenchus reports `unguarded` for `37f42d11afff3f051507e23f1bfed1d40f0d920a`, detail `the commit changed no test files`, correct for a prose clarification.
## Step 3, round 2 -- 2026-09-03T18:27:29Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=reviewed; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=reviewed; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the three step-3 commits waits on the push phase; locally each carries a good signature with author `shoggoth@wildcat.finance` and committer `laurence@wildcat.finance`. `adr-028-clause-drift`, `issue-body-publication` and `banner-inversion` have no surface at this commit, and steps 4 through 7 exits are not stood in for here. No trial merge into the advanced base was attempted. `origin/main` was read at `01a17bed45058a1fc20875bb19765fdf91cb293a` and no fresh fetch was taken in this round.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Nothing new was found. S3-R1-01 is fixed in `37f42d11` and the round-1 repair introduced no wrap regression, which is what round 2 of step 2 caught last time: no prose line in either record exceeds 85 columns outside the two Markdown link lines in each Status section. Two items from earlier steps stay accepted and open for the integration carryover: S2-R1-03, the retired-record check that BSD `wc` cannot satisfy in the Exit fields of steps 2 through 5, and the 100-column line at `docs/wave-delta-reinstatement-study.md:154`. Evidence at this commit: the three lints exit 0 over all five changed paths and report `clean` having read real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, with no skips, the third green run on this step; imprimatur scores 100.0/100 with 0 defects on both records and hypomnema over `docs/decisions` is clean; ADR-031 and ADR-032 remain untouched; `origin/main` holds no ADR-072 or ADR-073; the two digest-pinned checkpoint documents are absent from the diff; and the `plugins/` scope guard exits 0. With this step the four retired records each have exactly one standing successor, which is the condition the `additive-successors` candidate was scored on at design lock.
## Step 4, round 1 -- 2026-09-03T19:11:14Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=not-applicable; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=reviewed<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the step-4 commits waits on the push phase. The dispositions above cite the run study's register, the one every round of this run cites. `issue-body-publication` has no surface, because no issue body was edited and the estate rewrite is step 6, and `adr-number-collision`, `adr-028-clause-drift` and `retired-body-drift` have none either, because this step adds no record and touches no ADR. The fresh programme study seeds its own seven-concern register for the deliveries that follow it; that register governs no round of this run. The fresh runbook is step 5, so the historical runbook still carries its banner without a forward pointer, which is that step's exit rather than a gap here. Whether the programme this study describes is the right programme was not assessed; the round checked that every claim about shipped behaviour is true at a named commit, that nothing is called absent which the capsule contract specifies, and that the twelve items are answered. Issue numbers were re-read on 2026-09-03 for #859 and the estate; #547, #563, #564 and #569 were confirmed unresolvable earlier in this run and not re-read here.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S4-R1-01 | low | docs/wave-delta-checkpoint-programme-study.md | The em-dash repair taken before the implement commit left one prose line at 86 columns in item 12, where the document wraps near 78. The seven other long lines are risk-register rows inside a fenced block, which the Protasis parser requires to be one concern per line and which cannot wrap, so they are not in this class. | fixed in this round's commit; content unchanged and both protasis --study and imprimatur stay clean |<br>| S4-R1-02 | medium | tests/test_agent_instruction.py | The adapter-timeout family is intermittently red under full-suite load on this machine, and it is no longer one test. Three runs of the declared root suite against this exact tree: the first failed with `test_fake_unavailable_model_refuses` and errored on `test_fake_requests_are_isolated_one_message_jobs`, the second errored on `test_fake_malformed_answer_refuses`, and the third reported `Ran 1116 tests`, `OK`. Every one of those tests passes in isolation: `ParityAdapterTests` runs 34 tests OK twice and the whole module runs clean. The cause is the one localised at S1-R5-01, the two-second budget the profile sets at line 2252 which `_run_bounded` enforces at `scripts/agent_instruction.py:1376` by refusing with `WAI-E-ADAPTER.TIMEOUT`. What is new is the scope: this is not one load-sensitive assertion but a family, and it makes the root suite non-deterministic on this machine, which weakens the exit evidence of every step in this delivery rather than one. | accepted; the fix is code in a module this records-only delivery may not open, so it is carried forward with the escalated evidence |<br><br>Leads not pursued: S4-R1-02 changes what a green suite run means here and the honest position is worth stating plainly: a single `Ran 1116 tests, OK` on this machine is evidence that the suite passed that time, not that the tree is deterministic. Every step in this delivery has recorded at least one green run, and steps 1 through 3 recorded three or four each; where a run went red, the failure was reproduced green in isolation before the round closed. The route stays a carried-forward row rather than an edit, because `scripts/agent_instruction.py` and `tests/test_agent_instruction.py` are code and this delivery is records and documents. Also carried: S2-R1-03, the retired-record check BSD `wc` cannot satisfy, which this step's Exit repeats at runbook line 267, and the 100-column line at `docs/wave-delta-reinstatement-study.md:154`. Evidence for the covered concerns: `stale-shipped-claim`, every claim in the fresh study about shipped behaviour names the commit it was read at, `ff47f3070c8dce05c767b6c0dad65234c56870de` for the capsule contract and `482172e7` for the identity command, and both were re-read at `origin/main` in step 3's round 1. `banner-inversion`, the fresh study carries no no-longer-governs banner, the historical study keeps its banner and gains a dated forward pointer whose relative link resolves, and the forward pointer is an append that changes no earlier byte. `pinned-digest-drift` and `pinned-artefact-digest`, `docs/fiat-controller-checkpoint-study.md` and its runbook show zero files differing across `ff47f307..HEAD` and the module holding their digests runs 10 tests OK. `dead-issue-links`, #859's title ends `[epic]` as the study claims, and the estate titles for #861 through #867 match the study's one-line descriptions. `capsule-overclaim`, the three unbuilt capabilities the study names, upload, acceptance and discovery, are each absent from `controller-checkpoint.md`, and what that contract does specify is cited rather than described as missing. `scope-creep-to-code`, the `plugins/` guard exits 0 and the diff is three files. The three lints exit 0 over all three changed paths, protasis --study is clean, and imprimatur scores 100.0/100 with 0 defects on both documents.
## Step 4, round 2 -- 2026-09-03T19:14:37Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=not-applicable; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=reviewed<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the step-4 commits waits on the push phase. The four not-applicable concerns have no surface: no ADR is added or touched and no issue body was edited. The historical runbook still carries its banner without a forward pointer, which is step 5's exit. Steps 5 through 7 are not stood in for here, and no trial merge into the advanced base was attempted.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Nothing new was found. S4-R1-01 is fixed and left no wrap regression: no prose line in the fresh study exceeds 85 columns outside the seven fenced register rows, which cannot wrap. S4-R1-02 stays accepted and open, and this round's suite run is the fourth on this step's content: `Ran 1116 tests`, `OK`, exit 0, with no skips and no failure in the adapter family. That is one more green observation, not a claim that the family stopped being load-sensitive; the finding stands as recorded. Also carried forward: S2-R1-03, the retired-record check BSD `wc` cannot satisfy, and the 100-column line at `docs/wave-delta-reinstatement-study.md:154`. Evidence at this commit: the three lints exit 0 over all three changed paths and report `clean` having read real paths; imprimatur scores 100.0/100 with 0 defects on the fresh study and on the historical one after its forward pointer; protasis --study is clean, so all twelve items are answered and the register block parses; the digest-pinned checkpoint pair shows zero files differing across the run; and the `plugins/` scope guard exits 0.
## Step 5, round 1 -- 2026-09-03T19:36:08Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=not-applicable; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=reviewed<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the step-5 commits waits on the push phase. The four not-applicable concerns have no surface: no ADR is added or touched and no issue body was edited. Whether the eight packets are the right decomposition of the programme was not assessed; the round checked that each names one target repository and one authority gate, that the document's own two rules hold in every step, that the three statements step 5's exit requires are present, and that the historical runbook keeps its banner and gains a working forward pointer. The commands each packet's Exit names are future work in repositories that mostly do not exist; none was run, and none could be. Steps 6 and 7 of this run are not stood in for here.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S5-R1-01 | low | docs/wave-delta-checkpoint-programme-runbook.md | The runbook opens with two rules, that no packet authorises one agent to write Skills, the service and Atlas together, and that a packet names exactly one target repository. Step 6 then named `wildcat-finance/skills` and `wildcat-finance/fiat-checkpoints` and said so plainly, `which makes this two packets in practice rather than one`, and step 7 named two as well. A rule the same document breaks two steps later is worse than no rule. | fixed in this round's commit; step 6 is the protocol half with the index half as a dependent packet, step 7 is the drills with fixtures landing first |<br>| S5-R1-02 | low | docs/wave-delta-checkpoint-programme-runbook.md | Step 6's Entry lost its `Authority gate:` clause in the S5-R1-01 rewrite, so seven of the eight packets named a gate and one did not. Checked mechanically across all eight entries rather than by reading. | fixed in the same commit; the gate reads none beyond the ordinary Fiat run for the protocol half |<br>| S5-R1-03 | low | the step-5 branch name | The step branch was cut as `...-step-5-write-the-fresh-programme-runbook` when the controller's directive named `...-step-5-write-the-fresh-programme-runboo`, one character shorter under the 48-character slug limit. `done implement` refused the receipt and named the expected value. Nothing was pushed, so the repair was a local rename. The controller's refusal is the guard that makes this cheap, and Fiat's rule is to take the branch from the directive rather than to derive it. | fixed by renaming the local branch before any push; the receipt then accepted |<br><br>Leads not pursued: The adapter-timeout family surfaced a third time in this delivery: one run of the root suite after the step-5 implement commit failed on `test_fake_chat_extra_field_refuses`, the whole module then passed in isolation, and the next two full runs were green, including this round's. That is the accepted finding S4-R1-02 with one more data point rather than a new finding, and it stays carried forward. Also carried: S2-R1-03, the retired-record check BSD `wc` cannot satisfy, whose form step 5's Exit repeats at runbook line 316, and the 100-column line at `docs/wave-delta-reinstatement-study.md:154`. Evidence for the covered concerns: the three lints exit 0 over all three changed paths and report `clean` having read real paths; the declared root suite reports `Ran 1116 tests`, `OK`, exit 0, no skips. `banner-inversion`: the fresh runbook carries no banner, the historical runbook keeps its own and gains a dated forward pointer whose two relative links resolve, and the append changes no earlier byte. The three statements step 5's exit requires are present and checked: #860 startable once the reinstatement records land, creating `wildcat-finance/fiat-checkpoints` a separate authorisation, and no packet authorising one agent across Skills, the service and Atlas. All eight packets name one target repository and one authority gate, verified by parsing each Entry rather than by reading. `pinned-artefact-digest`: the digest-pinned checkpoint pair shows zero files differing across the run and the module holding their digests runs 10 tests OK. `capsule-overclaim`: the runbook describes future packets and calls nothing absent that `controller-checkpoint.md` specifies; step 2's packet is explicitly the outer archive the capsule contract says it does not replace. `dead-issue-links`: the eight issue numbers #860 through #867 were read on 2026-09-03 and all resolve open. `scope-creep-to-code`: the `plugins/` guard exits 0. protasis is clean on the step schema and imprimatur scores 100.0/100 with 0 defects on both runbooks. Elenchus reports `unguarded` for the fixes commit, correct for a prose repair.
## Step 5, round 2 -- 2026-09-03T19:38:51Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: adr-number-collision=not-applicable; adr-028-clause-drift=not-applicable; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=reviewed<br><br>Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run. GitHub verification of the step-5 commits waits on the push phase. The four not-applicable concerns have no surface at this commit. The commands the eight packets name are future work in repositories that mostly do not exist; none was run. Steps 6 and 7 of this run are not stood in for here, and no trial merge into the advanced base was attempted.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Nothing new was found. All three round-1 findings are fixed and the repair introduced no wrap regression: no prose line in the fresh runbook exceeds 85 columns. The packet contract was re-checked mechanically rather than by reading, by parsing each of the eight Entry fields: every one names exactly one target repository and one authority gate. Carried forward unchanged: S4-R1-02, the adapter-timeout family, whose fourth data point in this delivery is this round's green run; S2-R1-03, the retired-record check BSD `wc` cannot satisfy; and the 100-column line at `docs/wave-delta-reinstatement-study.md:154`. Evidence at this commit: the three lints exit 0 over all three changed paths; the root suite reports `Ran 1116 tests`, `OK`, exit 0, no skips; protasis is clean on the step schema; imprimatur holds 100.0/100 with 0 defects on both runbooks; the digest-pinned checkpoint pair shows zero files differing; and the `plugins/` scope guard exits 0.
