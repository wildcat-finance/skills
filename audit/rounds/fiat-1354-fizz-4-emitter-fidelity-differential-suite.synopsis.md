Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1354-fizz-4-emitter-fidelity-differential-suite.md | source_sha256=3092110262a3cee6bda65bf849d6a629b45352cc05461017ae6242e45a37e348 | h2_count=4
## Step 1, round 1 -- 2026-09-13T01:28:31Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: comparison-cannot-fail=not-applicable; reference-is-not-authority=not-applicable; oracle-derived-from-assembly=not-applicable; vendored-source-drift=not-applicable; vendored-boundary-write=reviewed; pairing-by-name=reviewed; unpaired-emitter=reviewed; narrowed-fuzz-domain=reviewed; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-disabled=not-applicable; check-map-ownership=reviewed<br><br>Not checked: The step's diff at `0ead2ed5d7bcaa64de4c16276a1e63ddbde50523` is three Markdown files plus the two Horos JSON files and holds no Solidity, so `x-ray`, `solidity-auditor` and `fizz` were each read from plugin root `hexaemeron/1.6.30` and given the full diff and found no contract in scope; no `x-ray/` output, audit bundle or fuzz suite was produced, and the twelve register ids marked not-applicable describe a harness that does not exist yet. The study's claim that `forge build` exits 1 with `Error: Lint failed` on the whole protocol tree was not reproduced, because the protocol clone was read only through `git show` and its `lib/` submodules are not reachable that way; only the closure half of that claim was reproduced. The decision-assignment planner was dry-run against `refs/remotes/origin/main` at `b9f8e36b8b6210bcd023a68059ecb46da3e35769` and refused `object-ancestry` before evaluating placement, because that base is not an ancestor of the product, so S1-R1-01 rests on the planner's code path and on main's own history rather than on a replayed `draft-placement` refusal. CI workflows were not run. No fixes commit exists, so the step-1 runner contract was not exercised.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | high | docs/decisions/draft-deliver-the-emitter-fidelity-suite-as-a-first-party-harness.md | The draft sits directly under `docs/decisions/` as `draft-<slug>.md`, but the base-owned allocator `plugins/hexaemeron/skills/hypomnema/scripts/decision_assignments.py` reads drafts only from `docs/decisions/drafts/` (`DRAFTS`, line 49), classifies any other non-`ADR-` Markdown file there as `misplaced` (lines 476 to 478) and refuses `draft-placement` when the product adds one the base did not carry (lines 602 to 604), then `draft-missing` when `drafts/` holds nothing (lines 605 to 606). Main already moved to that convention: `753a48f38f0c85b02f11c0b595a54084979ae69e` renamed five `docs/decisions/drafts/<slug>.md` files into ADR-081 to ADR-085, and the two `draft-*.md` files still under `docs/decisions/` at `e7d0fdea1a636e06725fc55a55d02c24f74a64ee` are tolerated only as inherited bytes. `hypomnema.py --study docs/emitter-fidelity-differential-suite-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` reports `H008 study has no design bridge block`, and study mode accepts a draft only at `docs/decisions/drafts/<slug>.md`. The runbook's step-1 Entry and Exit 2, the study's section 12 and the draft's own Status ("Whoever merges this record gives it a number by hand") all state the legacy placement, so the repair is a rename plus a runbook amendment, both controller-owned. | open |<br>| S1-R1-02 | medium | docs/emitter-fidelity-differential-suite-study.md | Section 4 quotes the `source-resolution-ms` column as `0`, `1193`, `577`, `0` for skills-harness, fizz-inline, spec-only and fixture-project, in the matrix and again as "the pinned 0, 1193, 577 and 0". The reports that `.hexaemeron/design-evidence.json` (SHA-256 `4ec55f9ff970320a4009ecebfbc057d8985ba863c39d869b365aca1e41ec05ce`) binds by digest, every one of the 20 digests matching on disk, hold `0`, `1063`, `956` and `1`. The sentence "Nothing in the matrix is transcribed from this prose, and the reverse is also true" is false for that column. The three gate columns and `carried-source-bytes` match the record exactly, and a fresh `--table` gave `1`, `184`, `213`, `0` for the timing column, so the selection is unaffected; the study is receipted and byte-pinned, so the correction is a controller amendment. | open |<br>| S1-R1-03 | medium | docs/emitter-fidelity-differential-suite-runbook.md | Exit 6 of step 1, and the repository-wide command every later step's exit names, is the bare `python3 scripts/run_checks.py`. On the committed head with a clean tree that command plans nothing: `--format json` reports `outcome: nothing-selected`, `changed_paths: []`, `selected_checks: []` and exits 0 with `root-suite` among 35 omitted checks. The exit condition is therefore satisfiable without running a check; the implement receipt's green came from `--base main`, which the runbook never names. | open |<br>| S1-R1-04 | low | docs/emitter-fidelity-differential-suite-study.md | Two counts stated without a source are wrong at the starting ref `e7d0fdea1a636e06725fc55a55d02c24f74a64ee`: section 12 and the runbook's step-1 Entry say `docs/` holds "twelve paired" study and runbook files, and `git ls-tree` there lists 51 `-study.md` and 50 `-runbook.md` files; section 2 says `plugins/janus/harness/` holds "fifteen tracked `.sol` files under `src/` and `test/`", and `git ls-tree -r` there lists 20, with a twenty-first at `adapters/ManifestFuzz.sol`. | open |<br><br>Leads not pursued: `.hexaemeron/reports/probe-manifest-killed-probe-recovery.json` (candidate `probe-manifest`, criterion `killed-probe-recovery`, dated 2026-09-07 18:24) sits beside the 20 bound reports, is named by no result in the design record and is ignored by `.hexaemeron/.gitignore`; it looks like another run's leftover and was not traced. Study line 295 cites the absolute local path of the protocol clone in a public document. The clone's `fiat/fizz-4-emitter-fidelity-differential-suite-step-2-build-the-differential-harness` branch is checked out in a linked worktree; the study's statement about the main checkout stays true and nothing there was touched. Everything else the brief asked to re-derive held: `cmp -s` exits 0 for both documents; `protasis.py --study` and `protasis.py` print `clean`; `imprimatur.py` scores 100.0/100 with 0 defects on each of the three documents; `brevitas.py` and the ordinary `hypomnema.py` walk exit 0; `horos.py check .` prints `boundary matches the tree`; `git diff --check` exits 0; a grep for the bare stable-slug prefix over the three documents finds nothing. At protocol ref `f5a26146987926f4811b72a795d662813dedfe85`, read only through `git show`: 22 plus 5 `emit_` free functions, 14 `log1`, 9 `log2`, 4 `log3`, 0 `log4`, no `logN` opcode in any other `src/` file, 23 plus 5 `event` declarations, `AccountSanctioned` declared at `IMarketEventsAndErrors.sol:127` with no emitter, `uint32 expiry` at `MarketEvents.sol:69` against `uint256 expiry` at `IMarketEventsAndErrors.sol:101`, an eleven-file closure of 46,799 bytes with no non-relative import, and `git diff --stat v2.1.0` naming five non-Solidity files. `measure_design.py --facts` reproduces every one of those numbers, `--emit-reports --out .hexaemeron/reports` exits 2 printing `refusing to overwrite`, `--emit-reports` without `--out` exits 2, a scratch `--out` regenerates all 16 gate and byte reports byte-identical, `state.json` and `ledger.jsonl` mtimes are unchanged, and `RUN_DOC_PREFIXES` excludes `.hexaemeron/` from `tracked_files()`. The eleven closure files with a copy of the Janus profile in a scratch directory: `forge build` under forge 1.7.1 (`4072e48705af9d93e3c0f6e29e93b5e9a40caed8`) exits 0 in 311 ms cold with exactly 32 `warning[unsafe-typecast]` lints, all in `src/libraries/SafeCastLib.sol`, and 0 errors. `plugins/hexaemeron/skills/fizz/VERSION` is `1`, no `EVOLUTION.md` exists there, `git ls-files` shows no `plugins/hexaemeron/EVOLUTION.md`, and `plugins/hexaemeron/harness/` does not exist. `tests/check-map-v1.json` holds no `hexaemeron-forge-*` check, one forge group `pandects-forge`, and a `hexaemeron` scope of `hexaemeron-suite` and `imprimatur-suite`. The draft's use of `ADR-005-vendor-the-pashov-suite-whole-and-ungoverned` is fair: the record's Decision and Consequences say byte-for-byte unmodified and the loop's conventions stop at the vendored boundary, which is exactly the constraint the draft cites, and the executed `S2-R1-05` check corroborates it. Evidence for this round: `phylax.py` and `ephoros.py` from plugin root `hexaemeron/1.6.30` each printed `clean` and exited 0 over the five changed paths, `hypomnema.py` from the same root printed `clean` and exited 0 over the three documents; `python3 scripts/run_checks.py --base main --format json` against local `main` at `b9cafd196b1d8f8b74b207320836532c14e78bab` returned `outcome: green` with 35 of 35 checks passed, `root-suite` (`python3 -m unittest discover -s tests`) exit 0 in 341.8 s. The bounded Sapheneia pass preserved the heading, the four schema fields, all sixteen risk ids and dispositions, every named unchecked item, the four finding ids with severity, file and status, every digest, count, line number and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 1, round 2 -- 2026-09-13T02:22:02Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: comparison-cannot-fail=not-applicable; reference-is-not-authority=not-applicable; oracle-derived-from-assembly=not-applicable; vendored-source-drift=not-applicable; vendored-boundary-write=reviewed; pairing-by-name=not-applicable; unpaired-emitter=not-applicable; narrowed-fuzz-domain=not-applicable; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-disabled=not-applicable; check-map-ownership=reviewed<br><br>Not checked: The fixes commit `78c2caae9e0c8b26dc83d16018099e561b5e2b95` changes one renamed draft, the two refreshed document copies and `.horos/census.json`, and holds no Solidity, so `x-ray`, `solidity-auditor` and `fizz` were given the full diff and found no contract in scope; the register ids marked not-applicable describe a harness that does not exist yet, and the protocol-repository facts checked in round 1 were not re-read because no amended byte changes them. The decision allocator was not replayed against a real base: every ancestor base that carries the product's numbered set also carries `docs/decisions/drafts/check-framework-numbers-rather-than-allocate-them.md` from main, which `decision_assignments.py` refuses as `base-draft` (lines 593 to 594) before placement is evaluated, so the placement result below comes from a scratch clone with a synthetic draft-free base. The `hexctl next` packet's effective step digest `b9fc575b187000d9bd921ec8fa6f36d17c117f2cd21948c4f8cf99772b2dcd63` was not recomputed; the effective step was read from the runbook's step-1 block plus its dated amendment. CI workflows were not run.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | high | docs/decisions/drafts/deliver-the-emitter-fidelity-suite-as-a-first-party-harness.md | The draft now lives under `docs/decisions/drafts/` with first heading `# Decision: Deliver the emitter-fidelity suite as a first-party harness`; the rename changed only the heading and the Status paragraph (`git diff -M`, similarity 91%%), and the new Status keeps the proposed date, the unnumbered state, the `tests/test_decision_records.py` comparison, the collision risk, the stable slug and "supersedes nothing", replacing the by-hand numbering with the allocator and dropping only the sentence that explained the old `draft-` filename. The amended study carries one `design-bridge` fence naming `skills-harness` and that path, and `hypomnema.py --study ... --design-evidence ... --repo-root .` prints `clean`. In a scratch clone with a synthetic base holding no draft, `decision_assignments.py plan` on a product adding only this draft returned `outcome: planned`, `mapping_count: 1`, number 87 against that base's ADR-086 maximum; the same base with the round-1 path replayed `draft-placement`. | fixed in 78c2caae9e0c8b26dc83d16018099e561b5e2b95 |<br>| S1-R1-02 | medium | docs/emitter-fidelity-differential-suite-study.md | The dated amendment states the timing column as 0, 1063, 956 and 1, which are the values in the four digest-bound `source-resolution-ms` reports, and withdraws the transcription sentence for that column. | fixed in 78c2caae9e0c8b26dc83d16018099e561b5e2b95 |<br>| S1-R1-03 | medium | docs/emitter-fidelity-differential-suite-runbook.md | Four dated amendments replace every step's Exit so the repository-wide command is `python3 scripts/run_checks.py --base main --format json` with `outcome` `green`; a word-level diff of each replacement against the baseline field shows that command, the `outcome` clause, and for step 1 only the intended path, heading, `dated`, `alternatives`, `hypomnema.py --study` and runner-contract insertions, with no other change. The study amendment states the same command in section 3. | fixed in 78c2caae9e0c8b26dc83d16018099e561b5e2b95 |<br>| S1-R1-04 | low | docs/emitter-fidelity-differential-suite-study.md | The amendment states 20 tracked `.sol` files under `plugins/janus/harness/src/` (13) and `test/` (7) plus `adapters/ManifestFuzz.sol`, and 51 `-study.md` and 50 `-runbook.md` files at the top level of `docs/`, all at `e7d0fdea1a636e06725fc55a55d02c24f74a64ee`; `git ls-tree` there returns exactly those counts. The runbook's replacement Entry carries the same 51 and 50. | fixed in 78c2caae9e0c8b26dc83d16018099e561b5e2b95 |<br>| S1-R2-01 | low | docs/emitter-fidelity-differential-suite-study.md | The amendment's fourth correction cites `decision_assignments.py` "lines 476 to 478" for the `misplaced` classification; at `78c2caae9e0c8b26dc83d16018099e561b5e2b95` that classification is lines 485 to 487 (`misplaced[path] = entry` at 487), and lines 476 to 478 are the `identity-duplicate` refusal inside the `drafts/` branch. The `DRAFTS` line 49 and the `draft-placement` refusal at lines 602 to 604 are cited correctly. The round-1 record above carries the same wrong 476 to 478 reference, which this row corrects; that record is append-only and is not rewritten. | open |<br><br>Leads not pursued: The fixes commit's committer identity is Shoggoth rather than the signing account, unlike the round-1 record commit; the signature verifies `G` under `B83B60AE16F5DD1A` and both trailers are present, and the controller's range gate owns that check. Every real ancestor base refuses the allocator with `base-draft` because main itself carries an unnumbered draft, so integration composition depends on main numbering that draft first; this is outside the step's products and was not traced further. The step-1 Tests amendment supersedes the preamble's `unittest-text-v1` sentence without rewriting it, so the preamble still reads that format, and a reader who stops at the preamble sees a runner Elenchus refuses. Everything else the brief asked to re-derive held on `78c2caae9e0c8b26dc83d16018099e561b5e2b95`: `cmp -s` exits 0 for both copies against `.hexaemeron/study.md` (SHA-256 `12d0df56a508bbaf92a647ee8e78ddac8721949d4544fab8045a001aa3a5a7b5`) and `.hexaemeron/runbook.md` (`ff30dab0c528c81ff663256959fc775db279ab8a9b6957ea1a31746f70b490c0`); `protasis.py --study` and `protasis.py` print `clean`; `imprimatur.py` scores 100.0/100 with 0 defects on each of the three documents; `brevitas.py` exits 0 on each; the ordinary `hypomnema.py` walk prints `clean`; `horos.py check .` prints `boundary matches the tree`; `git diff --check` over the fix range exits 0; no document contains the bare stable-slug prefix. Evidence for this round: `phylax.py` and `ephoros.py` from plugin root `hexaemeron/1.6.30` each printed `clean` and exited 0 over the three documents and `.horos/census.json`, `hypomnema.py` from the same root printed `clean` and exited 0 over the three documents; `python3 scripts/run_checks.py --base main --format json` against local `main` at `97b74e5d1adda5d86f59c8936e32d5af6840e0d0` returned `outcome: green` with 35 of 35 checks passed, `root-suite` (`python3 -m unittest discover -s tests`) exit 0 in 333.5 s; Elenchus on `78c2caae9e0c8b26dc83d16018099e561b5e2b95` under the amended step-1 runner (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json`) returned `status: unguarded`, `detail: the commit changed no test files`, `tests: []`, the honest value for a repair that touches only documents and a census. The bounded Sapheneia pass preserved the heading, the four schema fields, all sixteen risk ids and dispositions, every named unchecked item, the five finding ids with severity, file and status, every digest, count, line number and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 1, round 3 -- 2026-09-13T04:00:23Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: comparison-cannot-fail=not-applicable; reference-is-not-authority=not-applicable; oracle-derived-from-assembly=not-applicable; vendored-source-drift=not-applicable; vendored-boundary-write=reviewed; pairing-by-name=not-applicable; unpaired-emitter=not-applicable; narrowed-fuzz-domain=not-applicable; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-disabled=not-applicable; check-map-ownership=reviewed<br><br>Not checked: The fixes commit `008da57cda062a3bc3072a771ea48f49b6e0563a` changes the two refreshed document copies and `.horos/census.json` only, and holds no Solidity, so `x-ray`, `solidity-auditor` and `fizz` were given the full diff and found no contract in scope; the register ids marked not-applicable describe a harness that does not exist yet, and the protocol-repository facts and the allocator placement checked in rounds 1 and 2 were not re-run because no amended byte touches them. The controller's own `run_checks.py --base main --format json` on this commit, reported as 34 of 35 with `hexaemeron-suite` failed on `timeout` and zero bytes of output while a peer Fiat run drove the same suite with 11 workers, was not reproduced here: in this round's own `run_checks.py` pass, run with nothing else on this tree but beside a peer session's `run_checks.py --full --jobs 12`, `hexaemeron-suite` passed in 791.8 s and `root-suite` went red instead, on one test; both reds are host-load results on a diff that touches no test, as the suite line below sets out, and neither is evidence about the tree. CI workflows were not run.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: S1-R2-01 is closed by the second study amendment: at `008da57cda062a3bc3072a771ea48f49b6e0563a`, `plugins/hexaemeron/skills/hypomnema/scripts/decision_assignments.py` line 49 is `DRAFTS = b"docs/decisions/drafts/"`, lines 476 to 478 are the `identity-duplicate` refusal, lines 485 to 487 end in `misplaced[path] = entry`, and lines 602 to 604 are the `draft-placement` refusal, exactly as the amendment now states. The four re-issued runbook amendments carry What changed, Steps touched and Still holding fields byte-identical to their first issue and a Why that extends the first by one sentence naming the study digest move to `aef775a6`, with no other change. The fixes commit's committer identity is Shoggoth rather than the signing account, as in `78c2caae9e0c8b26dc83d16018099e561b5e2b95`; the signature verifies `G` under `B83B60AE16F5DD1A` and both trailers are present. Every real ancestor base still refuses the allocator with `base-draft` because main carries an unnumbered draft of its own; outside the step's products. The runbook preamble still reads `unittest-text-v1`, superseded by the Tests field and not rewritten. Everything else the brief asked to re-derive held on `008da57cda062a3bc3072a771ea48f49b6e0563a`: `cmp -s` exits 0 for both copies against `.hexaemeron/study.md` (SHA-256 `aef775a60e65b3a35317ebdd66440ad9e3d67a5f99cf5daeb13c91c6fc16ea96`) and `.hexaemeron/runbook.md` (`39c70fa8a57124069ae3de1239e8ddbbaa19edac26abbd8de70ee644f3dcabf0`), both digests pinned in `.hexaemeron/state.json`; `protasis.py --study` and `protasis.py` print `clean`; `imprimatur.py` scores 100.0/100 with 0 defects on each of the three documents; `brevitas.py` exits 0 on each; the ordinary `hypomnema.py` walk and `hypomnema.py --study ... --design-evidence ... --repo-root .` print `clean`; `horos.py check .` prints `boundary matches the tree`; `git diff --check` over the fix range exits 0; no document contains the bare stable-slug prefix. Evidence for this round: `phylax.py` and `ephoros.py` from plugin root `hexaemeron/1.6.30` each printed `clean` and exited 0 over the three documents and `.horos/census.json`, `hypomnema.py` from the same root printed `clean` and exited 0 over the three documents; `python3 scripts/run_checks.py --base main --format json` against local `main` at `97b74e5d1adda5d86f59c8936e32d5af6840e0d0` returned `outcome: red`, 34 of 35 checks passed, `hexaemeron-suite` passed in 791.8 s, and `root-suite` failed (exit 1, 502.7 s) on exactly one test, `test_demonstrations.RunnerExecutionBoundaryTests.test_a_grandchild_that_leaves_the_process_group_is_refused` with `AssertionError: 0 != 2`, a process-group timing test in a file this step never touched; that test then passed 3 of 3 times alone, and the declared argv `python3 -m unittest discover -s tests` re-run alone on this tree returned `Ran 1623 tests`, `OK` in 380.4 s, exit 0; Elenchus on `008da57cda062a3bc3072a771ea48f49b6e0563a` under the amended step-1 runner (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json`) returned `status: unguarded`, `detail: the commit changed no test files`, `tests: []`, the honest value for a repair that touches only documents and a census. The bounded Sapheneia pass preserved the heading, the four schema fields, all sixteen risk ids and dispositions, every named unchecked item, the zero-finding row, every digest, count, line number and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 2, round 1 -- 2026-09-13T09:28:14Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: comparison-cannot-fail=not-applicable; reference-is-not-authority=not-applicable; oracle-derived-from-assembly=not-applicable; vendored-source-drift=reviewed; vendored-boundary-write=reviewed; pairing-by-name=reviewed; unpaired-emitter=not-applicable; narrowed-fuzz-domain=reviewed; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-disabled=reviewed; check-map-ownership=reviewed<br><br>Not checked: The eleven files under `plugins/hexaemeron/harness/src/vendor/` are upstream protocol code vendored byte-identical, so every lead the suite raised inside them is upstream's, outside this run's fix scope under the study's Never rule, and is listed below with that scope rather than as a finding against the step. `forge coverage` was not run. `x-ray`, `solidity-auditor` and `fizz` were read from plugin root `hexaemeron/1.6.30` and given the full diff and every contract it touches; the twelve `solidity-auditor` role agents ran on `sonnet` because this session has no `AskUserQuestion`. The `fizz` pipeline stopped after its Steps 1 and 2 in a scratch copy of the harness: `forge` 1.7.1, `medusa` 1.5.1 and `echidna` 2.3.3 are installed, `extract_abis.js` found one contract (`SphereXConfig`, 4 functions), and no suite was generated because the effective Exit 6 requires no test beyond the smoke test and the Files clause names no `fizz` path; the runbook places `fizz_data/PROPERTIES.md` and `campaign.json` in step 4. `ensure_foundry.sh` attempted `forge install` of `forge-std`, which in the tree would add a submodule under `lib/` against `libs = []`; step 3 needs a hand-declared `Vm` interface in the shape of `plugins/janus/harness/src/Vm.sol`, which no runbook clause names. The CI workflows were not executed; `run_checks.py` marks a `requires_executable: forge` check `status: unavailable` with no failure class where `forge` is absent, so a host without Foundry reports green for the two new checks, and only `.github/workflows/hexaemeron-forge.yml` and the `plugins.yml` shard, which installs Foundry `v1.7.1`, enforce them. The ten register ids marked not-applicable describe suite behaviour that arrives in steps 3 and 4. No fixes commit exists, so the step-2 runner contract was not exercised.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | pull request 1568 body | The proof section states `forge build` reports 32 `unsafe-typecast` lints "(all `SafeCastLib.sol`)". A clean `forge build` at `9d0e5e6cd2dccec1e16aade30912d79a5c998543` reports 32 such warnings and 0 errors, of which 31 sit in `src/vendor/libraries/SafeCastLib.sol` and 1 at `src/vendor/libraries/FeeMath.sol:169:42`, the raw `uint32(timestamp)` cast. The count and exit code hold; the attribution does not. The body is not in the tree, so the correction belongs to the prose phase. | open |<br><br>Leads not pursued: Evidence for this round: `python3 scripts/run_checks.py --base main --format json` was started once in the background against local `main` at `97b74e5d1adda5d86f59c8936e32d5af6840e0d0` and its process disappeared without writing a byte of output while a peer session's `run_checks.py --jobs` and five `run_tests.py` workers were up, so it is not evidence either way; `python3 scripts/run_checks.py --scope root --format json` was then run alone in the foreground and returned `outcome: green`, 9 of 9 checks passed, `root-suite` passed (exit 0, 206.1 s) with `Ran 1623 tests`, `OK`, and the plan for the two forge checks is the `--plan --base main` result above; `forge build` from the harness exits 0 with 32 warnings and 0 errors and `forge test` exits 0 with 3 passed. Re-derived on `9d0e5e6cd2dccec1e16aade30912d79a5c998543`: each of the eleven vendored files matches its `PROVENANCE.json` byte count and SHA-256 and is byte-identical to `git show f5a26146987926f4811b72a795d662813dedfe85:<upstream path>` in the read-only protocol clone, 46,799 bytes in total, 0 mismatches, and no twelfth file sits under `src/vendor/`; the eleven are exactly the transitive import closure of `src/libraries/MarketEvents.sol`, `src/spherex/SphereXProtectedEvents.sol`, `src/interfaces/IMarketEventsAndErrors.sol` and `src/spherex/SphereXConfig.sol` at that ref, computed from the upstream imports, and every import among them is relative and resolves inside the directory; the `tag_note` holds, because all eleven files, and every file under upstream `src/`, are identical between that ref and tag `v2.1.0` at `c7be4039f8f383a9dda4e45f63331c17d63f9ed9`. `foundry.toml` declares `libs = []`, `solc = "0.8.25"`, `evm_version = "cancun"`, `optimizer_runs = 200`, `bytecode_hash = "none"`, no `ffi`, and one `fs_permissions` entry, `read-write` on `./out`; `test/` contains no `vm.ffi`, `vm.readFile` or `vm.writeFile`. `SphereXConfig(address(this), address(this), address(0))` in `Closure.t.sol:15` is sound by construction rather than coincidence: `_setSphereXEngine` at `SphereXConfig.sol:154-158` skips the `supportsInterface` staticcall only for the zero engine, and any non-zero engine, including `address(this)` mid-construction, would revert `SphereXNotEngine`; the three cases reach two `emit_` free functions through external calls, the three role getters and the `MarketState` struct, and `forge test` passes 3 of 3. The `ClosureProbe` constructor emits three assembly logs (`SphereXConfig.sol:29,32,35`) during `new ClosureProbe()`, so a step-3 case that opens `vm.recordLogs` before deployment would record five entries rather than two; the smoke test's `setUp` deployment before the recording window is the pattern to keep. `plugins/hexaemeron/tests/test_harness_provenance.py` passes 5 of 5 on the tree and, in a scratch copy, fails on one appended byte in `Errors.sol` (digest test), on an extra `Extra.sol` and on a removed `ISphereXEngine.sol` (inventory test), and on `total_bytes` set to 46800 (total test); its `os.walk` would also fail on a stray dotfile under `src/vendor/`, which is strictness, not a defect. `tests/check-map-v1.json` against `90702fa1c16be5faf48fc0e51c2d16b1c0097fe9` adds exactly the two checks, one group and two scope entries and changes nothing else at the object level; `run_checks.py --plan --base main --format json` selects 37 checks including `hexaemeron-forge-build` and `hexaemeron-forge-test` and refuses no path. `tests/test_skills_sh_package.py` moves `MAX_FILES` from 1,300 to 1,400 with a paragraph that follows the file's three earlier moves; a fresh `portable_promise_machine.py package` gives `MANIFEST.json` `file_count` 1300 and `total_bytes` 24,887,987, which is 94.94%% of 25 MiB, so `assertLess(1300, 1300)` was the red the amendment names; the harness contributes 14 files and 51,951 bytes, leaving 100 files and 1,326,413 bytes for step 3. `git diff --check 90702fa1c16be5faf48fc0e51c2d16b1c0097fe9 9d0e5e6cd2dccec1e16aade30912d79a5c998543` exits 2 on `SphereXConfig.sol:111` only; no hook, check-map entry or test in this repository gates whitespace, and `.gitattributes` already uses `-whitespace` for a generated tree, so a `linguist-vendored -whitespace` line for `plugins/hexaemeron/harness/src/vendor/**` would silence it and corroborate Horos, which today lists that directory only as a `candidate` ("directory name vendor alone, uncorroborated") and keeps `bytes_vendored` at 94; the later steps' clause that `git diff --check` exits 0 is true only in its bare form on a clean tree and false over any range containing this commit, which the controller owns. `horos.py check .` prints `boundary matches the tree`, and the runbook copy is byte-identical to `.hexaemeron/runbook.md` at SHA-256 `d3aafd991b5d0a43e80623da459834a7a4ff3188b100e1c32006d32d580fa626`. Inputs for step 3 read from the closure: 22 `emit_` functions in `MarketEvents.sol` and 5 in `SphereXProtectedEvents.sol` against 23 events in `IMarketEventsAndErrors.sol` and 5 in `SphereXConfig.sol`, so `AccountSanctioned` (`IMarketEventsAndErrors.sol:127`) has no emitter and a "27 events" count means 27 emitters; the only emitter narrower than its declaration is `emit_SanctionedAccountAssetsQueuedForWithdrawal` (`uint32 expiry` at `MarketEvents.sol:69` against `uint256` at `IMarketEventsAndErrors.sol:101`), while `emit_SanctionedAccountWithdrawalSentToEscrow` matches its `uint32` declaration at line 168, so step 3's "one case" clause is right; all 27 hard-coded topic0 literals were recomputed by the role agents and match; three emitters borrow `0x40` and restore it, `emit_InterestAndFeesAccrued` writes 0xc0 bytes at `mload(0x40)` without advancing it, and `WithdrawalBatchCreated` and `WithdrawalBatchClosed` emit zero-length data. Upstream leads, out of this run's fix scope: the two role-agent FINDINGs were demoted at the judging gates, the delinquency clock skipped when `delinquencyFeeBips == 0` (`FeeMath.sol:156`) because `WildcatMarketBase.sol:58` at the ref declares that value `immutable`, and the unmasked `uint32 expiry` in `mstore` (`MarketEvents.sol:75,238`) because it needs a dirty word from assembly in a caller outside the closure and the step-3 cases pass fuzzed values through an external boundary that cleans them; the remaining leads are `MarketState.sol:91` and `:109` checked subtractions beside `satSub` siblings, `FeeMath.sol:169` raw `uint32(timestamp)`, `FeeMath.sol:36,63` unguarded timestamp ordering, `FeeMath.sol:19-28` duplicating `MathUtils.calculateLinearInterestFromBips` with one call site each, `MathUtils.sol:71-76` `satAdd` returning a wrapped sum below `maxValue` on overflow with no caller in scope, `SphereXConfig.sol:27-36` accepting a zero admin or operator, `SphereXConfig.sol:143` letting the operator alone zero the engine, `SphereXConfig.sol:170-171` emitting `NewAllowedSenderOnchain` after a call to a code-less engine, `MarketEvents.sol:122` storing a `bool` unmasked, and the three `0x40`-borrowing emitters leaving their words above the restored pointer. The X-Ray run produced the four outputs in operator working material outside the tree with `mode: full`, reason `dependency-cycle` (`MarketState.sol` and `FeeMath.sol` import each other), 958 nSLOC, 12 guards, 5 single-contract and 1 cross-contract invariant, verdict FRAGILE on unit-only tests. The bounded Sapheneia pass preserved the heading, the four schema fields, all sixteen risk ids and dispositions, every named unchecked item, the one finding with its severity and status, every digest, count, line number and quoted string, the null verdict, and the unpursued leads without changing any claim.
