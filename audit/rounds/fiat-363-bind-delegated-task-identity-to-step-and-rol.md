## Step 1, round 1 -- 2026-09-13T19:29:23Z

Audit schema: fiat-audit-round/v2

Covered: untrusted-handle-input=reviewed; warden-round-continuity=reviewed; envelope-determinism=reviewed; brief-shape-pins=reviewed; controller-digest-cascade=reviewed; topic-only-runs=reviewed; inline-directive-handle=not-applicable; host-rename-boundary=reviewed

Not checked: the security suite is waived on the `security_suite` receipt because the run ships no Solidity, so x-ray, solidity-auditor and fizz did not run. What did run, on `9982e0d54a4cbb8986a82d7803b156cafef0c0c0`: phylax, ephoros and hypomnema from the 1.6.36 plugin root over the eight changed paths, each exit 0 `clean`; the Exit's `phylax.py plugins tests` and `ephoros.py plugins tests`, each exit 0; `test_task_identity` 32 tests OK; `promise_machine.py check` exit 0 with 18 plugins and 18 copies; `git diff --check` exit 0 on the worktree and on `97b74e5d..9982e0d5`; both Horos scans exit 0 with no tracked change; the root suite on a detached worktree, 1882 tests OK in 960.1 s; and the Hexaemeron suite on another, 2659 tests with 0 failures, 0 errors and 0 skipped in 967.1 s at 12 automatic jobs. On fixes commit `370866012205b6d0378e656ecc089b390b435db4`, tree `111eb253caacdc197898349b4b545398a2b6db65`: the three lints over its seven changed paths and the two Exit lint forms, each exit 0; `test_task_identity` 35 tests OK, where the unfixed code gave 3 assertion failures and 0 errors in the same 35; `promise_machine.py check` exit 0; `git diff --check` exit 0 on `9982e0d5..37086601`; greenlight in the run worktree, 1882 tests OK in 792.4 s, recording that tree in `LAST_GREEN`; the root suite on a detached worktree of that tree, 1882 tests OK in 770.9 s; and the Hexaemeron suite on another, 2662 tests with 0 failures, 0 errors and 0 skipped in 750.0 s. Each suite ran detached and was waited on in the foreground, because at 750 to 967 s under other sessions' load none fits the 600 s foreground tool limit. Elenchus ran as `elenchus.py --ref 370866012205b6d0378e656ecc089b390b435db4 --test-command "python3 -m unittest plugins.hexaemeron.tests.test_task_identity -v" --format json` with no `--report-format` or `--report-file`, because the runbook step declares neither (S1-R1-05), and returned `inconclusive`, detail "declare both --report-format and --report-file", without running a test; the red-then-green guard observation above is not an Elenchus classification. Not exercised: Step 2's `next` envelope, `--task-handle` argv path, inline-directive refusal and checkpoint restore, which do not exist yet; Windows wide-character argv; `isprintable` and `isspace` under a Unicode database other than 16.0.0; hosted CI, because nothing is pushed; the `step:3` and integration conformance cells; and the prose masks, which belong to the prose phase.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:13818` | At 9982e0d5 `task_handle_refusal` counts bytes with `observed.encode("utf-8", "surrogateescape")`, which raises `UnicodeEncodeError` for a lone surrogate outside U+DC80..U+DCFF. Its character check at `:13832` names all of U+D800..U+DFFF, so for 1920 of those 2048 code points the function raises instead of returning the `(character)` refusal its docstring promises and the step's ephoros discipline requires; reproduced with U+D800, U+DBFF, U+DC00 and U+DFFF. POSIX argv decoding yields only U+DC80..U+DCFF, so the argv path Step 2 wires cannot reach it, but a JSON `\uD800` escape or a direct caller can. The exception names one code point and its position, so nothing is echoed unbounded. Guard `ObservedHandleRefusalTests.test_every_lone_surrogate_is_refused_rather_than_raised`. | fixed in 370866012205b6d0378e656ecc089b390b435db4 |
| S1-R1-02 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:13758` | `task_identity_task` returns the topic slug as it is, and a slug of digits alone is the `<task>` an issue-backed run with that number derives. At 9982e0d5 a `none` or `external` anchor, or no anchor, with topic `363` or `#363` derives `fiat-363-step-1-warden`, byte-identical to issue 363's Warden handle, so `task_handle_refusal` accepts a handle minted for issue 363 in that topic-only run, and the reverse: the cross-issue reuse this run exists to refuse. It contradicts the docstring at `:13765-13767` and the invariant `test_topic_only_run_never_fabricates_an_issue_number` asserts at `plugins/hexaemeron/tests/test_task_identity.py:155`, which passes only because its fixture topic contains letters. It needs an operator-chosen digits-only topic. Fixed by prefixing such a slug `topic-`; every other topic handle and every issue handle is unchanged. For digits-only slugs alone this departs from the `<task>` wording at `study.md:223-224` and `study.md:290-291` and from prototype criterion 1's `fiat-<topic-slug>-...` form at `study.md:58-60`, so the Step 3 prose and the integration decision record should name the prefix. Guard `TopicOnlyRunTests.test_a_digits_only_topic_cannot_take_an_issue_handle`. | fixed in 370866012205b6d0378e656ecc089b390b435db4 |
| S1-R1-03 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:13828` | At 9982e0d5 the character check refuses `isspace()`, C0, DEL, C1 and surrogates but passes Unicode format, private-use and unassigned code points, which the equality refusal at `:13839-13843` then echoes. U+200B, U+200E, U+202E, U+2066, U+FEFF, U+180E, U+E000 and U+0378 each returned `(equality)` with the character inside the diagnostic, so a zero-width or bidi character can print two handles that look identical, and the refusal names equality where the cause is a character class. No handle is accepted this way, the echo stays within 200 bytes, and the refused set was already wider than the study section 9 `clean` convention. Fixed by refusing `isspace() or not isprintable()`, which on Python 3.14.6 with Unicode 16.0.0 is exactly general categories C and Z plus U+0020; on the fixed tree every code point placed before and after the expected handle, 2228224 values, raised 0 times and produced no diagnostic carrying a non-printable character. Guard `ObservedHandleRefusalTests.test_non_printable_characters_are_refused_on_character_without_echo`. | fixed in 370866012205b6d0378e656ecc089b390b435db4 |
| S1-R1-04 | informational | `plugins/hexaemeron/tests/test_task_identity.py:3` | The module docstring says a Warden continued under a handle minted for another issue. Issue 363's observed failure and study section 1 name the Mason resumed as `issue318_step2`, which the module's own `test_stale_handle_from_another_issue_is_refused_on_equality` checks against `fiat-320-step-2-mason`. Corrected to Mason; prose only, no guard. | fixed in 370866012205b6d0378e656ecc089b390b435db4 |
| S1-R1-05 | medium | `.hexaemeron/runbook.md:50` | No step declares an audit-fix runner, report format or report file. The Tests fields of Step 1 (`runbook.md:50-54`, which the 2026-09-13 amendment leaves in force), Step 2 (`:97-102`) and Step 3 (`:156-162`) name tests only, where this repository states the runner in Tests, for example `docs/adr-merge-assignment/runbook.md:25`. Study section 11 (`study.md:378-382`) names `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report {report}` with a `wildcat.hexaemeron-run.v1` report, which is not an Elenchus `--report-format`; Elenchus accepts `unittest-json-v1`, `forge-junit-v1` and `node-test-json-v1`. So every audit fix in this run classifies `inconclusive`, "declare both --report-format and --report-file", as this round's did, and study assumption 4's basis for running while #453 is open (`study.md:26-34`), each fix guarded red on its step parent and green on the step tree under that convention, cannot be recorded as `guarded`. Not fixed: the runbook changes only through `hexctl amend runbook`, one step per amendment. | open |
| S1-R1-06 | low | `.hexaemeron/study.md:397` | Study decision 12.3 (`study.md:397-399`) says Step 1 commits the study and runbook under `docs/<run>/`. 9982e0d5 adds neither, and no step's Files lists them: not Step 1's baseline (`runbook.md:46-48`) or its 2026-09-13 complete replacement (`runbook.md:175`), nor Step 2's or Step 3's. The repository keeps such copies at `docs/<slug>/study.md` and `docs/<slug>/runbook.md`, 52 of each tracked, so as written this run integrates with no committed copy of its design inputs. Not fixed: the copies fall outside Step 1's Files, and where and when they land is a runbook decision for `hexctl amend runbook`. | open |

Leads not pursued: five, none changing code. Topic-only runs whose topics share a slug still share handles, and after S1-R1-02 topics `363` and `Topic 363` both derive `topic-363`; slug identity for topic-only runs is the selected design's, and this round closes only equality with an issue-backed handle. A state with no run anchor derives the topic slug even when `receipts.task_issue` names an issue, which `test_derivation_reads_only_topic_and_anchor` pins on purpose. Printable homoglyphs such as Cyrillic U+0430 pass the character check and can appear in an equality diagnostic within 200 bytes; the refusal still holds. `task_identity` does not cross-check role against phase, so a `surveyor` identity with a step, or a `round` on a Mason, is derivable; Step 2's `delegation_packet` supplies both. The register's `controller-digest-cascade` control asks for re-pins in their own commit, but 9982e0d5 and the fixes commit each carry code and re-pins together, because the commit gate needs every commit's tree green and a code-only commit reddens the root suite on the coverage pin. The cascade was recomputed independently at both commits instead: the coverage pin matches `hexctl.py`, `1c9983244f9c7ca40076d25383a8a3996d1e5b2028470c5a8c6076ba666d3c17` at the step and `6b99db3cb8f479c7595010340277e1c4d7ea43a07182050d87c4fb2a82cffa9c` at the fix; evaluation packets emitted from `97b74e5d`, the step and the fix differ only in `manifest.json` `tree_sha256`, `3a57932876d496fd9071437659571d59bd357dc98e2930d22daf91ac19cc5081`, `2195fde7f189ffdb0491c9df9dd84b3d15bb62c2562be9acd5decef3711b37a5` and `868db8af58c04c442abf37bd179cb2e8c25c7a484cd4ef66162cfb9484e5567b`, with identical `corpus_sha256`, `prompt_template_sha256` and 11 prompts; `tally` from the unchanged `evaluation-answers.json` with model `ollama/qwen3.8-uncensored:Q6_K@sha256:79b2ee0918f76b4dd182bdd34c2ff14d1e89ec149311a9167e4d58450a472bce` and date `2026-08-31` reproduces the step's record byte for byte and, at the fix, differs from it only in `tree_sha256` and is the committed record; `verify` exits 0 at both, 11 cases, 55 passed, 0 failed; all 9 digest entries in `demonstration-run.json` and all 9 in `demonstration-evidence.md` recompute at both; and at the fix Horos moved only `.horos/census.json` byte totals. The step leaves `delegation_packet` and the four pinned brief key sets untouched and their tests pass in both Hexaemeron runs, which is the `brief-shape-pins=reviewed` evidence; `inline-directive-handle` is not applicable because no `--task-handle` flag or directive wiring exists before Step 2; and `host-rename-boundary` is reviewed because nothing in the step claims to rename or read a host handle and `task_handle_refusal` only accepts or refuses the string it is given.

## Step 1, round 2 -- 2026-09-13T20:42:49Z

Audit schema: fiat-audit-round/v2

Covered: untrusted-handle-input=reviewed; warden-round-continuity=reviewed; envelope-determinism=reviewed; brief-shape-pins=reviewed; controller-digest-cascade=reviewed; topic-only-runs=reviewed; inline-directive-handle=not-applicable; host-rename-boundary=reviewed

Not checked: the security suite is waived on the `security_suite` receipt because the run ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Audited: the step at `48e2c5f523c53db2ffcfef34092956275b3fe6cd`, including round 1's fixes in `370866012205b6d0378e656ecc089b390b435db4`, against Step 1's effective source `68d28766643b4c57264c9fe42540ddbb93e7474d345277d6474feb56101788e1`, study `9e033dbcc0b2cdd02536723f02a3dc7c87867dbc5040084408c9aac871396798`, runbook `504b508da4b8edb9845d366ae5cc0adf8e51a5072e611563919990816ecb4275` and design record `59fb40505301c90732cc7def91b629fddff00ac2b24437bf3bbac9c900ad2d59`. The effective source recomputes as the baseline plus amendments `2960e276442e69176cf03024ceb932b98e299b2b99f5ab281ea260d3c0b68ee7` and `a842687cb4146f68ade82696ab002044b163ee160b0f8aae3aeb15f3540ac84d`, and all four runbook amendment digests and study amendment `47c0f3ac45a2bca7594e775c410a1a9f8f4c85c4cb16add2e620186471771783` recompute from the canonical bytes. What ran: phylax, ephoros and hypomnema from the 1.6.36 plugin root on the four paths `4d1b934adf8a9caaa3a761e8dac671fc4a4568a8` changes and the two `2b582f1141f15ef4a1de8b11633bb6fb1b6cf739` changes, each exit 0 `clean`. The amended Exit list ran in the run worktree at `2b582f11`: `test_task_identity -v` 35 tests OK; `promise_machine.py check` exit 0 with 18 plugins and 18 copies; `phylax.py plugins tests` and `ephoros.py plugins tests` exit 0; both `cmp` lines exit 0; Hypomnema on the two copies exit 0 `clean`; both Horos scans exit 0 with no tracked change; and `git diff --check` exit 0. Greenlight passed at `4d1b934a`'s tree `9eda10e8244415b29fae4f903e1d27c2cb1a689d`, 1882 tests OK in 384.7 s, and at `2b582f11`'s tree `8effb1b037eac7aafaa60483c6dbfccbb09510fc`, 1882 tests OK in 481.9 s, each recorded in `LAST_GREEN`. On detached worktrees of that second tree, the root suite ran 1882 tests OK in 476.3 s and the Hexaemeron suite 2662 tests with 0 failures, 0 errors and 0 skipped in 426.8 s at 12 automatic jobs. Each suite ran detached and was waited on in the foreground. Elenchus ran with the declared contract, `--test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-363-step-1.json`, at its 900 s default timeout. On `4d1b934a` it returned `unguarded`, "the commit changed no test files", without running a test. On `2b582f11` it returned `passed`, "the runner report records that the guard passed on the parent", with 2662 executed, 0 assertion failures, 0 errors and 0 skipped. The `Elenchus verdict` below is `2b582f11`'s. Not exercised: Step 2's `next` envelope, `--task-handle` argv path, inline-directive refusal and checkpoint restore, which do not exist yet; Windows wide-character argv; a Unicode database other than 16.0.0; hosted CI, because nothing is pushed; the `step:3` and integration conformance cells; the prose masks; and the Step 2 and Step 3 amendments' Files, Exit and Tests claims beyond their runner contracts, which describe work not yet built.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-05 | medium | `.hexaemeron/runbook.md:50` | No step declared an audit-fix runner, report format or report file, so every fix classified `inconclusive`. No product change was needed. Runbook amendments `a842687cb4146f68ade82696ab002044b163ee160b0f8aae3aeb15f3540ac84d` (Step 1, `runbook.md:220`), `2efc503b4cdf128e4a60ae959b6ae87292b1dfa9bede1d30096fd661b2488852` (Step 2, `:250`) and `834cd8e67d7c90e887a0b941517d25467fef9c7d5e614b8cb6a1a742b07651d1` (Step 3, `:284`) declare `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1` and a per-step `.elenchus/` report file. Study amendment `47c0f3ac45a2bca7594e775c410a1a9f8f4c85c4cb16add2e620186471771783` corrects section 11 (`study.md:411`). Verified: that runner writes `elenchus.unittest.v1` (`plugins/hexaemeron/tests/run_tests.py:302`), the schema `unittest-json-v1` requires (`plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:124`); root `tests/run_tests.py:118` writes the same; `wildcat.hexaemeron-run.v1` is the run event (`run_tests.py:62`); and `/.elenchus/` is ignored (`.gitignore:73`). Under the Step 1 contract this round's fixes classified `unguarded` and `passed`. | resolved by runbook amendments a842687cb4146f68ade82696ab002044b163ee160b0f8aae3aeb15f3540ac84d, 2efc503b4cdf128e4a60ae959b6ae87292b1dfa9bede1d30096fd661b2488852 and 834cd8e67d7c90e887a0b941517d25467fef9c7d5e614b8cb6a1a742b07651d1; no product change |
| S1-R1-06 | low | `.hexaemeron/study.md:397` | Study decision 12.3 gives Step 1 the study and runbook copies, and no step created them. Study amendment `47c0f3ac45a2bca7594e775c410a1a9f8f4c85c4cb16add2e620186471771783` item 3 (`study.md:412`) names the flat paths, and runbook amendment `a842687cb4146f68ade82696ab002044b163ee160b0f8aae3aeb15f3540ac84d` adds them to Step 1's Files (`runbook.md:201`) and their `cmp` and Hypomnema checks to its Exit (`runbook.md:203`). Fixed: `docs/fiat-delegated-task-identity-study.md` and `docs/fiat-delegated-task-identity-runbook.md` are `cp` copies of `.hexaemeron/study.md` at `9e033dbcc0b2cdd02536723f02a3dc7c87867dbc5040084408c9aac871396798` and `.hexaemeron/runbook.md` at `504b508da4b8edb9845d366ae5cc0adf8e51a5072e611563919990816ecb4275`. Both `cmp` lines exit 0, and Hypomnema exits 0 `clean` on both copies, whose only relative links are five `../plugins/hexaemeron/skills/*/SKILL.md` links that resolve from `docs/`. The copies moved `.horos/boundary.json` `files_walked` from 3257 to 3259 and `.horos/census.json` by 2 files and 50547 `.md` bytes; `.horos/candidates.json` did not move. Elenchus: `unguarded`, because the commit changed no test files. | fixed in 4d1b934adf8a9caaa3a761e8dac671fc4a4568a8 |
| S1-R2-01 | low | `plugins/hexaemeron/tests/test_task_identity.py:366` | The Tests field of runbook amendment `a842687cb4146f68ade82696ab002044b163ee160b0f8aae3aeb15f3540ac84d` (`runbook.md:220`) says `test_task_identity` covers refusal of a lone-surrogate observed handle "without raising and without echoing it". At `4d1b934a`, `test_lone_surrogate_is_refused_on_character` (`:361`) and `test_every_lone_surrogate_is_refused_rather_than_raised` (`:366-377`) assert the `(character)` refusal, the code point, the index and the absence of a raise, but not the absence of an echo; every other class the field names asserts it. Both passed, with 0 failures and 0 errors, against a variant of `task_handle_refusal` whose character refusal quotes the observed value. Behaviour was already correct: all 6144 lone-surrogate probes, U+D800..U+DFFF in three positions, were refused on character with 0 raised and 0 echoed. Fixed by asserting that neither the surrogate nor the expected handle appears in the message. The same variant now fails that test with an assertion, and Elenchus returned `passed`, with 2662 executed, 0 assertion failures and 0 errors. | fixed in 2b582f1141f15ef4a1de8b11633bb6fb1b6cf739 |

Leads not pursued: five, none changing code. First, study section 9 (`study.md:350-351`) still describes the character control as refusal of control and whitespace characters through the existing `clean` convention, while the step refuses whitespace and non-printable characters and never calls `clean`. The study amendment's three corrections (`study.md:410-412`) leave it, and `4d1b934a` commits that text. It was not raised because the amended Step 1 Exit binds the stronger rule and a study is a run input; a committed copy that describes the implemented control needs `hexctl amend study`. Second, the Step 1 amendment's Why (`runbook.md:222`) says the Exit and Tests carry the reader rules for S1-R1-01 and S1-R1-03 and the slug rule for S1-R1-02, "which the study amendment of 2026-09-13 records". That amendment records only the slug rule, so the clause holds only when read as attaching to the slug rule. Third, the `topic-` prefix gives topics `363` and `Topic 363` the same `<task>`, `topic-363`. Topic-only runs whose topics share a slug already share handles under the selected design. A separator no slug can produce, such as a double hyphen, would avoid this case, but the amended study fixes `topic-363`. Fourth, when another lone surrogate is present the surrogatepass fallback counts escaped argv bytes U+DC80..U+DCFF at three bytes each, so U+DCFF 66 times plus U+D800, 69 argv-equivalent bytes, is refused on length as 201 bytes; the value is still refused and the diagnostic still bounded. Fifth, the two `cmp` Exit lines read `.hexaemeron/`, which exists only in the run worktree, so a detached snapshot or CI cannot prove them; they ran in the run worktree. Evidence behind `Covered`: round 2 changes no product code, so `envelope-determinism`, `warden-round-continuity` and `brief-shape-pins` rest on the 2662-test Hexaemeron run at `2b582f11`, which includes their tests. `controller-digest-cascade` holds because `hexctl.py` still hashes to its pin `6b99db3cb8f479c7595010340277e1c4d7ea43a07182050d87c4fb2a82cffa9c` and the round moved only Horos artefacts. `untrusted-handle-input` and `topic-only-runs` rest on S1-R2-01, the surrogate probes and the prefix review above. `host-rename-boundary` holds because nothing new claims to rename or read a host handle, and `inline-directive-handle` is not applicable before Step 2. The study copy names Python 3.14.6 without reddening the runtime-prose check, because that check skips names ending `-study.md` and `-runbook.md` (`tests/test_python_contract.py:219`).

## Step 2, round 1 -- 2026-09-13T23:46:14Z

Audit schema: fiat-audit-round/v2

Covered: untrusted-handle-input=reviewed; warden-round-continuity=reviewed; envelope-determinism=reviewed; brief-shape-pins=reviewed; controller-digest-cascade=reviewed; topic-only-runs=reviewed; inline-directive-handle=reviewed; host-rename-boundary=reviewed

Not checked: the security suite is waived on the `security_suite` receipt because the run ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Audited: the step at `1510dabc6554e95f4faf66cbcd29786b337e2ba5`, tree `0db5626522b0e74eede6344af7dc1430a2729179`, two commits above Step 1's head `cc8ee17417bd8c96b70c35effb909ed724ead032`, against Step 2's effective source `2656f5662fe33ecd7b04f25cc79dedbae3834d0f1f706038ff3a756499e405f3`, study `9e033dbcc0b2cdd02536723f02a3dc7c87867dbc5040084408c9aac871396798`, runbook `a6c5863c51e500c2ce1beca41f3e69db2f7237c04505994710032d346141c5e7` and design record `59fb40505301c90732cc7def91b629fddff00ac2b24437bf3bbac9c900ad2d59`. At `1510dabc`: phylax, ephoros and hypomnema from the 1.6.36 plugin root over the 15 paths `cc8ee174..1510dabc` changes, each exit 0 `clean`, and hypomnema on its two Markdown paths, exit 0. In the run worktree on that tree: `promise_machine.py check` exit 0 with 18 plugins and 18 copies; `phylax.py plugins tests` and `ephoros.py plugins tests` exit 0; both `cmp` lines exit 0; both Horos scans exit 0 with no tracked change; `git diff --check` exit 0 on the worktree and on `cc8ee174..1510dabc`. On detached worktrees of that tree: the Exit's `test_task_identity` and `test_hexctl` command ran 446 tests OK in 1419.5 s; the root suite ran 1882 tests OK in 352.3 s; the Hexaemeron suite ran 2670 tests at 12 jobs with 1 failure, 0 errors and 0 skipped, exit 1, in 316 s. The failure is `test_metron_check.TimeCommandTests.test_an_escaped_reader_keeps_its_pipe_descriptors`, in a file the step does not touch; its class then ran 33 tests OK in each of three isolated runs on the same tree, consistent with skills#1586. On the fixes commit `7cecc319595364080450636c46132fe6a20dcee6`, tree `963a5b6c3601a6f97e47457fca481e48cf7150cc`: the three lints over its 10 changed paths, hypomnema on its Markdown path and the two Exit lint forms, each exit 0; `promise_machine.py check` exit 0; both `cmp` lines exit 0; `git diff --check` exit 0 on the staged tree; and the Horos scans moved only `.horos/census.json`, by the 313 bytes the test gained. On detached worktrees of that tree the root suite ran 1882 tests OK in 397.3 s, the green `LAST_GREEN` records; the Exit's `test_task_identity` and `test_hexctl` command ran 446 tests OK in 2418.2 s; and the Hexaemeron suite ran 2670 tests with 2 failures, 1 error and 0 skipped, exit 1, in 352 s, all three in `test_metron_check.TimeCommandTests`, whose class then ran 33 tests OK in each of three isolated runs on the same tree. Elenchus ran as `elenchus.py --ref 7cecc319595364080450636c46132fe6a20dcee6 --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-363-step-2.json` at its 900 s default timeout, overlaying the six changed test paths on `1510dabc`, and returned `inconclusive`, "the run did not finish inside 900s", with no runner report; at 23:28Z, with other sessions' suites running, the machine's load averages were 64.20, 49.84 and 40.11. The red-then-green guard observation in S2-R1-01 is not an Elenchus classification. Each suite ran detached and was waited on in the foreground. Not exercised: the `next` wall-clock bound, a Step 3 measurement; the `step:3` and integration conformance cells; `scripts/portable_promise_machine.py check`, a Step 3 Exit command, which exited 1 on a fresh snapshot because its generated runtime is absent before `sync`; a handle above the macOS argument limit, where a 1 MiB value failed with Errno 7 before hexctl started; Windows argv; a Unicode database other than 16.0.0; hosted CI, because nothing is pushed; and the prose masks.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | `plugins/hexaemeron/tests/test_hexctl.py:5923` | `TestTaskIdentity.test_identity_is_identical_across_processes_and_after_reload` compares the restored `next` output only after `after.replace(restored, os.path.realpath(self.target)).replace(now, was)`, which rewrites the restored root and state digest inside `task_identity` as well as in the brief. At 1510dabc, with `delegate` changed to add a `root` key holding the absolute run root, or a `state` key holding `state_fingerprint(state)`, to every delegated identity, both `TestTaskIdentity` tests passed, 2 run and 0 failed, although the identity then differs across the restore. `.hexaemeron/resolve_conformance.py` resolves `stale-handle-regression-guarded` from that class alone and `resume-identity-reproducible` from that test alone, so both `step:3` cells would resolve true for a controller whose identity changes across a restore, which study criterion 3 (`study.md:64-65`) forbids, including one carrying the absolute path the `envelope-determinism` control excludes. Behaviour was already correct: a probe comparing the parsed identities across a restore found them equal, with only `brief.design_evidence.path`, `brief.runbook_step.path` and `state_sha256` differing. The same two mutations fail `EnvelopeIdentityTests` and the literal-dict test, and a worktree path added inside `task_identity()` also fails Step 1's `DeterminismTests`, but neither cell runs them. Step 3's Files do not list `test_hexctl.py`. Fixed by asserting that the emitted `task_identity` equals the one before the restore, with handle `fiat-320-step-2-mason`, before normalising; the fixed test fails by assertion under both mutations and passes on the unmodified controller. | fixed in 7cecc319595364080450636c46132fe6a20dcee6 |

Leads not pursued: five, none changing code. First, a refused `next --brief-out PATH` leaves an existing `PATH` as it was: a probe that wrote a brief under a matching handle, then ran a stale handle against the same path, found its 1201 bytes unchanged, so an orchestrator that ignores exit 2 can still hand the stale handle an earlier brief; the controller writes nothing on refusal, which is what the Exit requires. Second, a handle carries no run discriminator, so a delegate left from an earlier run for the same issue, after a reset and re-init or in another clone, passes the check; the grammar is the selected design's, and study acceptance 1 (`study.md:82`) addresses reuse across issues. Third, the register asks for the literal test (`brief-shape-pins`) and the re-pins (`controller-digest-cascade`) in their own commits, but `1510dabc` and the fixes commit each carry them with the change that moved them, as Step 1's commits did, because the commit gate needs every commit's tree green. Fourth, the length and character refusals name the expected handle and the observed byte count, or code point and index, but not the observed value, where study section 9 (`study.md:352-353`) describes a bounded copy of it; Step 1's amended Tests (`runbook.md:220`) require a malformed handle never to be echoed, and the step follows them. Fifth, `test_hexctl.py` is 258,813 bytes after the fix, 3,331 under the 262,144-byte bound, and Step 3's Files exclude it; that bound is enforced, contrary to the Mason's report: `check_coverage` reads each coverage row's test path through `read_markdown` (`scripts/promise_machine.py:7749-7751`), which caps at `MAX_MARKDOWN_BYTES` (`:26`) and refuses PM003 (`:1123-1133`), and with the bound set one byte below the file's size `promise_machine.py check` exited 1 on PM003 for `test_hexctl.py`. Evidence behind `Covered`, at `1510dabc` unless it says otherwise. `untrusted-handle-input`: a non-UTF-8 byte, a 262,144-byte handle, a 201-byte multibyte handle, U+0085, U+2028 and U+200B each exited 2 with one stderr line of 139 to 149 bytes ending `; expected fiat-test-topic-step-1-mason` and not containing the value; `--task-handle=-fiat` refused on equality, and `--task-handle -fiat` stopped in argparse with exit 2 and no stdout. Refusal writes: for the surveyor, Mason, Warden and Scribe envelopes and the `runbook`, `resolve-security-suite` and `close-audit` inline directives, `next` with a stale handle and `--brief-out .hexaemeron/briefs/deep/probe.json` exited 2 with empty stdout and left every file's bytes, mode and mtime, every directory and every ref under the run root unchanged, while a matching handle wrote that path; moving the check after the `--brief-out` write gave 10 `EnvelopeIdentityTests` failures. `inline-directive-handle`: those three inline directives each refused `(delegate)` by name, and letting an inline directive accept gave 2 failures. `warden-round-continuity`: adding `round` to the handle failed 2 tests. `envelope-determinism`: S2-R1-01 and the probe above. `brief-shape-pins`: `task_identity` is added only beside `agent`, `test_fiat_skill.py` is unchanged, the literal dict gains only that key (`test_version_relations.py:2911-2918`), and the brief key-set tests passed in both Hexaemeron suite runs. `controller-digest-cascade`: every pin at `1510dabc` and at the fixes commit recomputes from tree bytes, and no superseded digest remains outside `audit/`; evaluation packets differ only in `tree_sha256`, `868db8af58c04c442abf37bd179cb2e8c25c7a484cd4ef66162cfb9484e5567b` at `cc8ee174`, `1e0b490be0dd5ee4ce44b83f9dfc28d97f06a1c9ce250b6d08734fea659470eb` at `1510dabc` and `4d20ebbb9d52fc6059a3fa114cbfe0584e6c4daf01c50655a3238e9dc21b7e79` at the fix, with `corpus_sha256` `b9b6dfa8d1993c8bf7d7fea38adc3af765ac089b4efb7ce97d0c5e4f73ced427`, `prompt_template_sha256` `bb58f1dc5b6da0c37ae4303415f210d5158344516b51cc24f20bc623909eb5c2` and 11 byte-identical prompts; `tally` from the unchanged answers with model `ollama/qwen3.8-uncensored:Q6_K@sha256:79b2ee0918f76b4dd182bdd34c2ff14d1e89ec149311a9167e4d58450a472bce` and date `2026-08-31` reproduced `1510dabc`'s record byte for byte and differed at the fix only in `tree_sha256`; `verify` exited 0 at both with 11 cases, 55 passed and 0 failed; and all 9 digest entries in each demonstration record recompute at both. `topic-only-runs`: `EnvelopeIdentityTests` fixtures are topic-only runs deriving `fiat-test-topic-` handles, `TestTaskIdentity` derives `fiat-320-step-2-mason` from an issue anchor, and the derivation is unchanged since Step 1. `host-rename-boundary`: the `--task-handle` help and the new docstrings claim only to accept or refuse the handle shown, and no host call is added. The `task_identity: null` key on inline envelopes matches Step 2's Tests, an inline directive carrying nulls, and study section 3's deferral of identity for inline directives (`study.md:199-201`); it adds 23 bytes to an inline envelope, a delegated envelope gains 146 bytes for this run's Warden and at most 245 for a 48-character topic, inside the design's 512-byte gate, and no non-test code parses the envelope's key set. All 9 guards the step added or changed fail against the Step 1 controller with the step's tests overlaid, 11 failures and 5 errors.
