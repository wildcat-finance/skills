Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-ship-the-markdown-outline-extractor-the-census-n.md | source_sha256=4518967e7486125af6178a53df209c83b6b9e4e152a6329528af4410c3c574e0 | h2_count=3
## Step 1, round 1 -- 2026-09-05T01:36:44Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=not-applicable; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=reviewed; ledger-row=not-applicable; no-execution=not-applicable<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. The step's diff is the two verbatim spec copies under `plugins/horos/docs/markdown-outline/` and `.horos/boundary.json`; no source, test or skill file changed, so the eight outliner ids, `census-regeneration`, `fixture-repin`, `ledger-row` and `no-execution` are not-applicable. This round ran inline by the controller. The receipted `.hexaemeron/study.md`, `.hexaemeron/runbook.md` and `.hexaemeron/design-evidence.json` (`c8879b32c9405b0d91ae6b5a0789059457a54df2b81e217b081c30357b451256`) were left unedited; both copies `cmp` equal to them.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | .horos/boundary.json | The step's boundary regeneration ran before the two copies were `git add`ed, so the scan's tracked universe did not include them and the committed boundary stayed at `files_walked` 2542 against a fresh scan's 2544. In a clean detached snapshot of the implement head `a899e4ff` the root suite was `Ran 1233 tests`, `FAILED (failures=2)`: `test_boundary_currency.test_the_committed_boundary_matches_a_fresh_scan` and `test_agent_instruction.test_horos_boundary_is_current_for_the_scaffold`, both on `.horos/boundary.json#counts`. The implement receipt's test summary claimed that snapshot green in error; this record corrects it. Fixed at `4319ba9432a24d6456c489eaf37b56397ade6853`: `scan . --write` over the tree with the copies tracked, `#counts` only, `horos.py check .` prints `boundary matches the tree` exit 0. Elenchus on `4319ba9432a24d6456c489eaf37b56397ade6853` under the step's runner (`python3 plugins/horos/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json`): `unguarded -- the commit changed no test files`, the honest value for a regenerated artefact. | fixed in 4319ba9432a24d6456c489eaf37b56397ade6853 |<br><br>Leads not pursued: issue 1228 carried: inside the run worktree the root suite stays red on the two `test_agent_instruction_corpus` tests; in a detached clean snapshot of `4319ba9432a24d6456c489eaf37b56397ade6853` it is `Ran 1233 tests`, `OK`. The study's five discipline links resolve from the committed location. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22`, each printed `clean` and exited 0 over the changed paths; horos suite `Ran 239 tests`, `OK`; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the finding id with severity, file and status, every digest, count and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 1, round 2 -- 2026-09-05T01:38:51Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=not-applicable; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=reviewed; ledger-row=not-applicable; no-execution=not-applicable<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. This round re-checked the tree with round 1's fix applied and ran inline by the controller; the only change since round 1 is the regeneration `4319ba9432a24d6456c489eaf37b56397ade6853` and the round 1 record.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: issue 1228 carried unchanged: in a detached clean snapshot of `40013792c2c551f68644bb4843a91b93daea362d` the root suite is `Ran 1233 tests`, `OK`; inside the run worktree it stays red on the two `test_agent_instruction_corpus` tests. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22`, each printed `clean` and exited 0 over the changed paths; horos suite `Ran 239 tests`, `OK`; `horos.py check .` prints `boundary matches the tree`, exit 0; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, and the unpursued leads without changing any claim.
## Step 2, round 1 -- 2026-09-05T02:39:19Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: fence-shadowed-heading=reviewed; html-hidden-heading=reviewed; setext-under-lazy-line=reviewed; container-prefix=reviewed; front-matter-phantom=reviewed; unterminated-fence=reviewed; hostile-input=reviewed; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=not-applicable; ledger-row=not-applicable; no-execution=reviewed<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. CommonMark conformance was held against the study's oracle (markdown-it-py, CommonMark preset, `.hexaemeron/design-reports/oracle.json` over the 895 files of `corpus-list.txt`) and five hand cases under the same parser, not against the CommonMark spec suite. `census-regeneration`, `fixture-repin`, `prose-reconciliation` and `ledger-row` belong to steps 3 and 4 and nothing in this diff touches their surfaces. The diff `78ea5a4d..e1b1f748` changes no existing extractor: `git diff --stat` names only `languages/markdown/`, one `.md` line in `languages/__init__.py`, the two `map` help strings in `horos.py`, `GUIDE.md`, `test_md_outline.py`, `.horos/boundary.json#counts` and `tests/promise_machine_coverage.json`. The brief's `design_evidence` digest `c8879b32c9405b0d91ae6b5a0789059457a54df2b81e217b081c30357b451256`, study `588d3a5685bcb80cd8f2c6dc1089d55d52f263bc029e7532c0972f4fc8247009` and runbook `96e4f9f09e2599e647a3b43a4797d4e186c0dcc6d96d0d4e9a417d0d100b7818` match the files on disk; the shipped module is the study's `line-scanner` candidate byte for byte below its docstring.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | plugins/horos/skills/horos/scripts/languages/markdown/markdown.py | `hostile-input`: one crafted line drives `map` quadratic, and the study's hostile set never held that shape. On the step head, `* ` x 20,000 then `x` (40,002 bytes) took 9.1 s, `# a` + 30,000 spaces + `b` 4.4 s and the same with a closing ` #` 4.4 s; the 2 MiB `* ` line did not finish in 300 s. Four mechanisms: the ATX regex's lazy `.*?` before `[ \t]*$` and the closing-hash regex each rescanned the tail per step; the thematic-break regex backtracked over its repeated group; the container-opening loop sliced the line once per list marker and re-checked the remainder for a thematic break each time; `_strip_containers` re-counted the indent per open item. Fixed: greedy ATX with `_atx_text`, `_thematic` answered from the line's uniform tail, position scanning with unanchored `QUOTE` and `LIST_ITEM`. After the fix the 2 MiB lines outline in 525 ms (markers then `x`), 162 ms (markers), 3 ms (ATX spaces), 2 ms (closing hashes), 262 ms (2 MiB of `>`), 344 ms (`- > ` mix); the eight hostile inputs of `resolve.py` run with zero exceptions at 0, 0, 0, 0, 1, 0, 94 and 75 ms (empty, cr-only, binary, unterminated-fence, one-long-line, deep-quotes, deep-list, many-blocks; deep-list was 4,753 ms on the step head, Mason recorded 5,186 ms). Guard: `test_a_long_line_of_markers_and_spaces_is_outlined_in_linear_time`, red on the parent at 9.1 s, 4.4 s and 4.4 s against a 2 s bound. | fixed in f6b11b058d0eb692283aa29ce104c821cccff821 |<br>| S2-R1-02 | low | plugins/horos/skills/horos/scripts/languages/markdown/markdown.py | `container-prefix`, `html-hidden-heading`, `setext-under-lazy-line`: three container-close rules diverged from the oracle, each losing a heading or a fence range, never inventing one. An HTML block of start kinds 1 to 5 inside a blockquote or list item did not close with its container, so `> <!--` then `# h` hid the heading and confessed lines 1-2 where the oracle reads an HTML block at line 1 and an h1 at line 2; a blank line inside a blockquote-held fence did not close the fence, so a blockquote-marked backtick fence, a blank line, a second blockquote-marked backtick fence and `# h` gave one fence 1-3 where the oracle reads fences 1-1 and 3-3; and `para[2]` stayed true after any lazy line, so `> Foo`, `bar`, `> ===` and `- foo`, `bar`, `  ---` outlined no heading where the oracle reads an h1 and an h2 at line 1. Fixed: one `closes` test (a container the line did not continue, where a blank line keeps an item and closes a blockquote) shared by the HTML and fence branches, and the lazy flag dropped; a lazy underline is still refused by `starts_block`. Guards: `test_a_container_closing_closes_the_html_block_or_fence_it_holds` and `test_an_underline_at_the_paragraph_depth_after_a_lazy_line_is_a_heading`, both red on the parent. The corpus differential is 0 missed, 0 extra, 0 fence_missed, 0 fence_extra, 0 crashes over 895 files before and after the fix. | fixed in f6b11b058d0eb692283aa29ce104c821cccff821 |<br><br>Leads not pursued: (1) Correction to the step 1 round 2 record above: it states that a clean detached snapshot of `40013792c2c551f68644bb4843a91b93daea362d` ran 1233 tests OK. It did not. That commit's boundary regeneration ran before its own audit log and synopsis were tracked; in a clean snapshot of it this round `horos.py check .` exits 1 with `drift: .horos/boundary.json#counts` (`files_walked` 2544 committed against 2546 fresh, `1 path(s) drifted`) and `test_boundary_currency.test_the_committed_boundary_matches_a_fresh_scan` and `test_agent_instruction.test_horos_boundary_is_current_for_the_scaffold` fail. The round 2 commit `78ea5a4d` regenerated with the audit files tracked; its clean snapshot, and the step head `e1b1f748`, are `Ran 1233 tests`, `OK` with `boundary matches the tree`. (2) `e1b1f748` re-pins `tests/promise_machine_coverage.json` `runtime.horos-boundary-scan.sha256` to `09edab05d87777db3457e6d9a687be0e06ad1d5e8e06e05240dc81e4b6c9ace3`, a file outside the step's Files field: accepted, the register binds `horos.py` by whole-file digest and the help-string edit the Files field names moved it; without the re-pin the root suite the exit requires is red. (3) Elenchus under the host's default `TMPDIR` (`/var/folders/...`, resolving through `/private/var`) returned `inconclusive -- the runner did not create its report` because `run_tests.py` resolves its worktree root strictly and refuses the unresolved report path with `REPORT must stay inside the current worktree`; with `TMPDIR` set to the scratchpad (already a `/private` path) the same command on the same commit returned `guarded -- the runner report records a parent assertion failure` (executed 258, assertion_failures 6, errors 0). The recorded verdict is that second run; the runner's path check is outside this step's Files and is not re-found here. (4) `_strip_containers` is still O(open containers) per line; a 4,000-deep list outlines in 343 ms, so no bound is added. (5) Issue 1228 carried: inside the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `test_agent_instruction_corpus.test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`; in clean detached snapshots of `e1b1f748` and of `f6b11b058d0eb692283aa29ce104c821cccff821` it is `Ran 1233 tests`, `OK`. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22` each printed `clean` and exited 0 over the changed paths on the step head and again on the fix; horos suite `Ran 255 tests`, `OK` on the step head and `Ran 258 tests`, `OK` on the fix, in the worktree and in the clean snapshot; `horos.py check .` prints `boundary matches the tree` on both; `scan . --write` after the fix left `git status --short .horos/` empty; `map plugins/horos/skills/horos/SKILL.md` exits 0 with `module: Horos`, `front matter: lines 1-6`, `declarations: 16` and `unparsed: 1 region(s): lines 8-10` before and after; the pinned `GUIDE.md` outline is unchanged; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, both finding ids with severity, file and status, every digest, count, timing and quoted string, the exact verdict word, and the five unpursued leads without changing any claim.
