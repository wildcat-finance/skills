## Step 1, round 1 -- 2026-09-04T01:43:34Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=not-applicable; probe-subprocess=not-applicable; absent-versus-unavailable=reviewed; stale-manifest=not-applicable; partial-manifest-write=not-applicable; network-absent=reviewed; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: the three recorded suite ids found nothing because this step's diff gives them nothing to read. It carries no Solidity and touches no Foundry or Hardhat project, so the auditor's own scoped find returns zero files and its source bundle would be empty, the X-Ray reuse adapter refuses the honest empty scope by name with `scope.sources must be a non-empty array`, and fizz's precondition is absent. The 73 `.sol` files that survive the auditor's exclude pattern elsewhere in the tree belong to other plugins' specimens, harnesses and fixtures, and none is touched here. That zero is an absence of scope, not a pass over contracts. No harness client was run, no account was created and no Atlas route was touched, so the six per-harness observations the study records were read rather than reproduced. The schema was exercised against hand-written documents and the shipped cases only, never against a manifest a probe wrote, because no probe exists until step 2. No measurement was taken, so nothing here carries a performance claim. Whether ADR-074's acceptance-condition-2 reading is the correct reading is a judgement the record makes visible and this round does not settle; its two checkable claims were confirmed, that `tests/test_marketplace_prose.py` lines 331 and 332 assert `pull/479` and `PR #479` are absent from both `README.md` and `docs/how-to-help-shoggoth.md`, and that commit `daa64e5f` removed the link.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | schemas/harness-classification-v1.json | The `classification` description read "Atlas launcher and tested local route are earned classes: neither may be recorded without a client run". The schema performs no such refusal. Driven against the shipped document with jsonschema 4.25.1, an entry carrying `Atlas launcher` with `client_present` false, `client_version` null, no `probe` and a blocker validates clean, and so does the same entry under `tested local route`. The runbook puts that guarantee in step 2's classifier, not here, and the design record's `earned-class-only` gate reads a declared boolean `permits_tested_class_without_a_client_run` out of `candidates.json` rather than measuring any built artefact, so nothing upstream closes the gap either. The document is published under `$id` `https://wildcat.finance/schemas/harness-classification-v1.json`, so a reader validating a third-party manifest against it was told a refusal it would not get. ADR-074 states the same rule correctly, attributing only the closed enumeration to the schema. Fixed by rewording the description to say the classifier enforces the rule, that the schema fixes the vocabulary and refuses an unknown name, and that validation here is not evidence a class was earned. | fixed in 45d20fcb |
| S1-R1-02 | medium | tests/test_harness_manifest.py | The module docstring stated "Each case asserts against the schema document itself, which always runs, and then asserts the same rule behaviourally when the library happens to be importable. A host without the library still checks the declared rule; it does not quietly skip." Three of the eight cases held only `assert_valid` or `assert_refused` calls, and both return immediately when `validator()` is None, so `test_each_classification_name_is_admitted`, `test_a_harness_entry_missing_any_observation_field_is_refused` and `test_a_null_client_version_is_admitted_when_the_client_is_absent` asserted nothing at all on a host without `jsonschema`, which the file itself declares a Lazarus dependency rather than a root one. Proved by mutation: making `client_version` non-nullable failed 7 of the 8 cases with the library present and passed all 8 without it, in 0.003s against 0.195s. Fixed by giving each of the three a document-level assertion, so the same mutation now fails 1 case with the library absent, and mutations removing `client_present` from `required` or dropping `tested local route` from the enum each fail 2 cases whether the library is present or not. Still eight cases, as the step's Tests contract names. | fixed in 45d20fcb |
| S1-R1-03 | low | tests/test_harness_manifest.py | `test_each_classification_name_is_admitted` asserted that a manifest whose only entry carried `Atlas launcher` with `client_present` false, `client_version` null, no `probe` and the blocker "absent from this host and unauthenticated" is valid, because it reused the unearned default record for all four names. The assertion is true of the schema, and it is the one shipped statement in the repository that reads as endorsement of an unearned earned class, in a file whose purpose is to hold the four names. Fixed by a `shaped()` helper that gives the two earned names a recorded client run, so no case in the file asserts an unearned earned class is a valid record. | fixed in 45d20fcb |
| S1-R1-04 | low | docs/atlas-harness-handoff/study.md | Line 498 records the home for the schema decision as "a new ADR under `docs/adr/`". That directory does not exist and never has; all 73 decision records live in `docs/decisions/`, which is where ADR-074 was correctly written, and which the study's own `hypomnema-design-bridge/v1` block names. It is the only occurrence of `docs/adr/` in the repository. Not fixed here: the file is a byte-identical copy of the controller-pinned `.hexaemeron/study.md` at sha256 `17c2fad756475a9557f94804b571a498d8c9c423cf67fc9e2565b60b7cab1099`, byte-identity is the step's own Exit condition, and a hand edit to either copy would break that condition and the pin at once. It needs a controller amendment to the study, then a re-copy. | accepted |
| S1-R1-05 | high | docs/decisions/ADR-074-generate-the-harness-roster-from-one-probed-manifest.md | The number 074 is taken. `origin/main` carries `ADR-074-shape-every-written-record-through-sapheneia.md`, landed in commit `d611ae4b` at 2026-09-04T01:23:35Z and merged as pull request #1181 at 01:40:30Z, after this round's first two gate runs had already passed. `tests/test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` now fails, and `tests/test_wave_delta_reinstatement.ReinstatementConditionTests.test_the_pinned_decision_record_tests_pass` fails behind it, so `python3 scripts/run_checks.py --full` is red with root-suite failed. The step branch head `b192b259` fails the same case on its own in a detached worktree, so this is the step's defect and not the audit fixes'. The fix the test names is to renumber to the next free number, 075, and move every reference with it. Not done here: six of those references live in `.hexaemeron/runbook.md` and `.hexaemeron/study.md`, including the `hypomnema-design-bridge/v1` block whose `record` key names the file by path and whose whole purpose was to bind the design to a standing record. Those two artefacts are controller-pinned, and the step's own Exit requires the `docs/atlas-harness-handoff/` copies to stay byte-identical to them, so a hand rename would leave the bridge pointing at a file that does not exist and break the pin. It needs a controller amendment renumbering the study and the runbook, then the rename, the re-copy, and the schema description's pin line. | open, needs a controller amendment |

Leads not pursued: four checks came back negative and are recorded as evidence rather than as findings. The runbook's Files field names "the check-map entry that binds the new paths to a declared scope" and no check-map change was made; none was needed, because `run_checks.owner_of` resolves every new path to exactly one existing scope, `schemas/harness-classification-v1.json` to `schemas`, the three documents to `docs`, and `tests/test_harness_manifest.py` to `root` with `root-suite`. The `schemas` and `docs` scopes declare no checks of their own, so scope-selected runs exercise the new schema only through the root suite; the step's Exit uses `--full`, which is unaffected. `.horos/boundary.json` moved `files_walked` from 2492 to 2497, exactly the five new tracked files. A credential sweep over every added line of the diff, for GitHub, OpenAI, Slack, AWS, bearer, private-key, api-key and session-id shapes, returned zero hits, and the published documents carry no absolute home path, no personal identifier and no private repository name. The two register rows marked reviewed beyond `unearned-class` hold: `absent-versus-unavailable` is met by `client_present` and `auth_configured` being separate required booleans under `additionalProperties` false, so absence, an unconfigured client and a configured client cannot collapse into one field, and `network-absent` is met by `launcher_contract` being a required non-empty string, so an unreadable contract cannot be omitted or nulled and its reason has to be written. One lead belongs to the base rather than to this step. Regenerating the Horos boundary for the two new tracked files, which the boundary-currency test demanded by name, also showed `.horos/candidates.json` missing a 90,295-byte blob candidate, `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md`, that already existed at the base commit `8dc3aca5`. That regeneration was reverted and only the `files_walked` bump from 2497 to 2499 was committed, because the staleness predates this step and nothing unrelated should ride along in an audit fix; the boundary tests and the 236-test Horos suite are green either way. Three leads stay open. The schema still admits an unearned earned class, and adding an `allOf` conditional keyed on the earned names would give defence in depth beneath step 2's classifier; recorded rather than built, because moving the enforcement boundary is a design decision belonging to the runbook and ADR-074 rather than to an audit fix, and step 2's Exit already claims the classifier side. Nothing stops a ninth case asserting that every case in the file carries an assertion that survives `jsonschema` being absent, which is the property S1-R1-02 restored by hand; recorded rather than built, because the step's Tests contract names eight cases. Three shapes the schema admits that later steps may care about were left alone as step-3 and step-4 surface: `recorded.date` is pattern-checked only, so `2026-13-45` validates; `harnesses` has `uniqueItems` but no key on `name`, so two entries can name the same harness; and an authentication that was attempted and rejected has no field of its own, reaching the record only as `blocker` prose beside `auth_configured` false. Three exits were read directly on this round's tree: phylax, ephoros and hypomnema each returned 0 over the changed paths before the fixes and over the two changed files after them, `python3 -m unittest discover -s tests` returned 0, and `python3 scripts/run_checks.py --full` was run three times. It returned 0 with outcome green on the committed step head `b192b259` with root-suite at 280.9s, and 0 again on the fixes commit `45d20fcb` with every check passing and root-suite at 259.0s. The third run, on this round's final tree, returned 1 with outcome red and root-suite failed at 216.6s, for the reason S1-R1-05 records: the default branch took ADR number 074 between the second run and the third. The first two greens were true when they were taken and are not true now, and no fix in this round caused the change. The fixed tree runs 1140 of 1140 tests at exit 0 under the step's own runner contract, `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-856-step-1.json`, writing an `elenchus.unittest.v1` report. The Elenchus verdict is `passed` rather than `guarded` for a reason worth stating: all three fixes correct a document claim or strengthen an assertion, none repairs a behaviour, so the changed test file applied to the parent runs clean and no guard could have gone red there.

## Step 1, round 2 -- 2026-09-04T03:19:21Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=not-applicable; probe-subprocess=not-applicable; absent-versus-unavailable=reviewed; stale-manifest=not-applicable; partial-manifest-write=not-applicable; network-absent=reviewed; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: the negative space from round 1 stands unchanged. The three recorded suite ids still have no Solidity to read, since this round's diff is one decision record; no harness client was run, no account created and no Atlas route touched; the schema has still never been handed a manifest a probe wrote, because no probe exists until step 2; and no measurement was taken, so nothing here carries a performance claim. Two claims in the renumber commit were read rather than driven: that issue 888 is the systemic answer to the numbering race, which is a judgement about another issue's scope, and that pull request 1185 will keep ADR-075, which is true of an open pull request and not of a merged one. Whether the four superseded ADR-074 mentions left in the base text of `docs/atlas-harness-handoff/runbook.md` are legible to a reader who stops before the amendments was not tested on a reader; the amendment mechanism is append-only by design and this round checked only that every one of them is covered by a complete replacement.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | low | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | The Consequences paragraph enumerates seven readers of the field names, the schema, the manifest, the renderer, three wording surfaces and the suite, then said renaming one is "a change to all six at once". The numeral arrived with the Imprimatur repair in `b46e53a1` that replaced `load-bearing`: the sentence it had before, "a change to all of them at once", carried no count and could not disagree with its own list. Corrected to seven. | fixed in 796d61fb |
| S1-R2-02 | low | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | The disclosure section said pull request 1181 merged the colliding record into `main` "about ninety minutes later". No reading of this run reaches ninety. This record's own commit `b192b259` is 2026-09-04T00:45:16Z and the merge `6acbcb38` is 01:40:30Z, fifty-five minutes; the ledger's `done:implement` is 00:57:46Z, forty-three minutes before the merge; and the colliding record was authored in `d611ae4b` at 01:23:35Z, thirty-eight minutes after this one. A wrong quantity in the one section whose whole job is to be checkable is worse than no quantity. Corrected to fifty-five minutes with both clock times named so a reader can check it. The same "ninety minutes" stands in the receipted runbook amendment at `docs/atlas-harness-handoff/runbook.md:261`, which is append-only and not editable here, so the two now disagree. | fixed in 796d61fb |
| S1-R2-03 | low | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | The same section said "The repository suite does not run study mode, so nothing here goes red on it". The suite does run study mode: `plugins/hexaemeron/tests/test_hypomnema_checker.py:752` and four call sites in `plugins/hexaemeron/tests/test_protasis_checker.py` invoke `--study`. Neither names `docs/atlas-harness-handoff/study.md`, and a grep for `atlas-harness-handoff` in both files returns zero, so the conclusion holds while the premise does not. Reworded to say the suite exercises study mode only against its own fixtures and never against this study. | fixed in 796d61fb |
| S1-R2-04 | medium | docs/atlas-harness-handoff/study.md | The `hypomnema-design-bridge/v1` block at line 524 permanently names `docs/decisions/ADR-074-generate-the-harness-roster-from-one-probed-manifest.md`, which no longer exists, so `hypomnema --study` reports `H008 record ... is unavailable or not an ordinary non-symlink file` against a shipped document, and will do so for good. Both escape routes were driven inside the repository rather than taken on trust: appending a second `design-bridge` fence naming ADR-076 refuses with `H008 study declares more than one design bridge home`, and deleting the block refuses with `H008 study has no design bridge block`. Study amendments are append-only, so neither a repoint nor a removal is reachable, and a stub or alias at the old number does not help because the collision test reads the number from the filename and walks `git ls-tree -r`. Accepted as a decided and disclosed defect: ADR-076's "Numbering, and one stale pointer this leaves behind" section states it and points the reader at the right file, and it takes a carryover row and its own issue at integration. | accepted |

Leads not pursued: the Elenchus verdict is `unguarded` because the fixes commit changed no test file, which the reference says to carry here rather than treat as a failure; all three corrections are to prose quantities in a decision record, and this repository has no check that asserts ADR prose against the facts it cites, which is why three wrong ones shipped inside a commit whose own lints were clean. Round 1's fixes were re-driven against the renamed tree rather than assumed: the reworded `classification` description survives with the def still holding exactly `description` and `enum`, so the structural assertion in `test_an_unknown_classification_name_is_refused` still bites; the eight cases pass with `jsonschema` present and absent; and the three mutation proofs return exactly their round 1 post-fix figures, five failures present and one absent for a non-nullable `client_version`, two and two for removing `client_present` from `required`, and two and two for dropping `tested local route` from the enum. The earned-class gap is unchanged and still documented rather than enforced, which is the design's own division of labour with step 2's classifier. Round 1's S1-R1-05 is resolved and was verified rather than accepted: `tests.test_decision_records` runs five tests green, ADR-076 is absent from `origin/main` and unique on this branch, the heading matches the filename, and every one of the four ADR-074 mentions left in the base text of the runbook copy is covered by a complete replacement in one of the two appended amendments. The two pinned copies are byte-identical to their `.hexaemeron/` originals again, the study digest is unchanged at `17c2fad7`, the runbook is `e418396e`, and the effective step source `b3ef2320` reproduces as step 1's section joined to the first amendment by a blank line. The renumber commit's Files claim was driven too: `python3 scripts/run_checks.py --plan` exits 0 with no unowned path, so no check-map row was owed, which is round 1's reasoning now true in the runbook rather than only in the audit file. Round 1's S1-R1-04 stands unchanged and is not re-listed above: `docs/atlas-harness-handoff/study.md:498` still records the ADR home as `docs/adr/`, a directory that has never existed, and it is unfixable for the same append-only reason as S1-R2-04 and belongs in the same amendment if one is ever made. Two open leads carry from round 1 untouched: an `allOf` conditional keyed on the earned names would give defence in depth beneath step 2's classifier, and a ninth case asserting that every case survives `jsonschema` being absent would guard by machine what S1-R1-02 restored by hand; both stay recorded rather than built, the first because it moves a design boundary and the second because the step's Tests contract names eight cases. The three shapes the schema still admits are unchanged and remain step-3 and step-4 surface: a calendar-invalid `recorded.date`, two entries naming the same harness, and an authentication attempted and rejected reaching the record only as `blocker` prose. Exits read directly on this round's tree: phylax, ephoros and hypomnema each returned 0 over the changed paths and again over the edited record, Imprimatur scores the record 100.0 out of 100 with zero defects, `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-856-step-1-r2.json` reports 1140 of 1140 under the step's runner contract, `python3 -m unittest discover -s tests` returned 0 at 1140 tests, and `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing twice, once on the fixes commit `796d61fb` and again on this round's final tree with the record and synopsis committed.

## Step 1, round 3 -- 2026-09-04T04:12:30Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=not-applicable; probe-subprocess=not-applicable; absent-versus-unavailable=reviewed; stale-manifest=not-applicable; partial-manifest-write=not-applicable; network-absent=reviewed; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: the negative space from rounds 1 and 2 stands unchanged, with no Solidity for the three recorded suite ids to read, no harness client run, no account created, no Atlas route touched, no manifest a probe wrote, and no measurement taken. Round 2's corrections were re-derived from the repository rather than read back, but two of the study's claims this round did read rather than drive: that the Copilot desktop app reached general availability on 17 June 2026, and that `GET /api/job` answered with 119 eligible jobs and a 1481-character prompt, both of which need a network read this round did not make. The Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` was not fetched, so the study's count of six test files and its claim that none covers either route remain its own. Whether the four superseded ADR-074 mentions in the runbook's base text are legible to a reader who stops before the amendments is still untested on a reader. The study's standing claim that no audit round here has ever reached a verdict on an Atlas hand-off was sampled by grep across `audit/` and not re-established record by record.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R3-01 | low | docs/atlas-harness-handoff/study.md | The Audit records paragraph at lines 119 to 123 says the synopsis check "ran from the target root and exited zero, with all 62 records reporting `budget=pass` and `committed=match`", and that the synopsis "was the view read for the 55 per-run records under `audit/rounds/`". Both counts overstate. Re-derived by running `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check` against a detached worktree at the run's own base commit `8dc3aca5`, the command emits 60 lines, all of them `committed=match`, and `git ls-tree -r` finds 52 non-synopsis files under `audit/rounds/` there. The current tree reports 61 because this run added one record of its own. The paragraph also disagrees with itself: 55 per-run plus the six it names as plugin-local is 61, not the 62 it claims. The conclusion the paragraph supports, that no audit round in this repository has ever reached a verdict on an Atlas hand-off, is not challenged by these counts; it was sampled rather than re-established here, and every `Atlas` mention the sample found in the existing records concerns other subjects. Not fixed here: the study is byte-pinned, the `docs/` copy must stay byte-identical to it as the step's Exit requires, and correcting prose in a live run's study is a controller amendment. Unlike S1-R2-04 this one is reachable by an ordinary appended amendment, because it needs no second design-bridge fence. | accepted |

Leads not pursued: round 2's three corrections were re-derived rather than read, and all three hold. The Consequences sentence enumerates seven readers, the schema, the manifest, the renderer, three wording surfaces and the suite, and now says seven. The interval is right: `b192b259` has commit date 2026-09-04T00:45:16Z, `6acbcb38` has 01:40:30Z, and the gap is 55.23 minutes, while the ledger's `done:implement` at 00:57:46Z is 43 minutes before the merge and the colliding record `d611ae4b` was authored at 01:23:35Z, 38 minutes after this one, so no reading reaches ninety. One trap in checking it is worth recording: `b192b259` has an author date of 00:25:51Z, nineteen minutes before its commit date, and `git show` prints the author date by default, so a reader re-deriving the interval with the obvious command gets 74.65 minutes and would judge the record wrong. The record's phrase "this record's own commit" is accurate for the commit date and this is a lead rather than a defect, but naming the field would remove the trap. The study-mode claim was verified exhaustively rather than by sampling: every `--study` call site in the repository is `test_hypomnema_checker.py:752` and four in `test_protasis_checker.py`, all against `FIXTURES` or `DESIGN_BRIDGE_FIXTURES` paths, no check-map check carries `--study` in its argv, and the only non-test caller is `hexctl.py:9867`, which runs Protasis rather than Hypomnema over a temporary amendment candidate and so cannot emit H008 at all. Protasis study mode over the shipped copy is clean at exit 0, and `H008` appears in no checker but `hypomnema.py`, so the disclosed finding is the only thing that reports and nothing in the suite or the controller goes red on it. Two accepted items carry forward unchanged and are restated here rather than relisted as new findings, because their ids belong to the rounds that raised them. S1-R2-04, medium, accepted: the `hypomnema-design-bridge/v1` block at `docs/atlas-harness-handoff/study.md:524` still names the ADR-074 path, `hypomnema --study` still reports `H008 record ... is unavailable or not an ordinary non-symlink file`, and it is structurally unfixable because a second fence refuses with `H008 study declares more than one design bridge home` and deletion refuses with `H008 study has no design bridge block`. S1-R1-04, low, accepted: `docs/atlas-harness-handoff/study.md:498` still records the ADR home as `docs/adr/`, which does not exist against 73 files in `docs/decisions/`, 72 of them numbered; like S1-R3-01 it is reachable by an ordinary appended amendment. The `runbook.md:261` divergence is confirmed exactly as round 2 described it and is the statement to quote in the carryover: the receipted amendment reads "into `main` ninety minutes later" while the corrected `ADR-076` reads "into `main` fifty-five minutes later, at 2026-09-04T01:40:30Z against this record's own commit at 00:45:16Z", the amendment is append-only so the two stand disagreeing, and `ninety` occurs nowhere else in the run's files. Round 1's and round 2's fixes were re-driven against this tree: the `classification` def still holds exactly `description` and `enum`, the root description still pins ADR-076, the eight cases pass with `jsonschema` present and absent, and the three mutation proofs return their established figures of five and one, two and two, and two and two. External state was re-read because it moved twice in this loop: `origin/main` has advanced to `a46f3b50` and still carries neither ADR-075 nor ADR-076, pull request 1185 is still open holding ADR-075, issue 888 is still open, and `tests.test_decision_records` runs five tests green. The three open leads carry unchanged, the earned-class conditional, the ninth case, and the three shapes the schema admits. Exits read on this round's tree, the branch tip `94758036` with no fixes commit added: phylax, ephoros and hypomnema each returned 0, Imprimatur scores `ADR-076` 100.0 out of 100 with zero defects, `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-856-step-1-r3.json` reports 1140 of 1140 at exit 0 on a fresh report path, and `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing. There is no fixes commit this round, so there is no Elenchus verdict to record.

## Step 1, round 4 -- 2026-09-04T07:32:22Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=not-applicable; probe-subprocess=not-applicable; absent-versus-unavailable=reviewed; stale-manifest=not-applicable; partial-manifest-write=not-applicable; network-absent=reviewed; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: this round was scoped to the amendment's replacement figures, the pinned copy and the invariants, so it is not a fresh sweep of the study. The amendment's other re-derivations were spot-checked rather than repeated in full: the three badges in the README block at lines 313 to 324, Codex at line 273, the six data rows in the guide table at lines 126 to 139, the exclusion sentence at lines 137 to 139, the builder's `Manual only` label at line 386 with the hardcoded harness string at 389, and the two prose assertions at lines 331 and 332 were each read and hold. The Copilot general-availability date, the live `GET /api/job` figures and the Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` still need a network read this round did not make. The negative space from earlier rounds is otherwise unchanged: no Solidity for the three recorded suite ids, no harness client run, no account created, no Atlas route touched, no manifest a probe wrote, and no measurement taken.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R4-01 | low | docs/atlas-harness-handoff/study.md | Line 89 states "The strings `go/copilot` and `tested local route` appear nowhere in the tree." That was true when written: at the base ref `8dc3aca5` neither string occurs in any tracked file. It is false of the tree that ships it. `tested local route` now occurs in five tracked files, `docs/atlas-harness-handoff/runbook.md`, `docs/atlas-harness-handoff/study.md`, `docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md`, `schemas/harness-classification-v1.json` and `tests/test_harness_manifest.py`, and `go/copilot` occurs in `docs/atlas-harness-handoff/study.md`, so the sentence sits in a file containing the strings it denies. This step falsified it by committing the schema, the tests and the study copy. The sharper defect is the round 4 amendment at line 566, which lists this among claims that "all hold as written": that assertion was made against the current tree, where it does not hold, and the amendment scoped its recomputed counts to the base ref but not this one. One clause naming the ref would fix it. Not fixed here: the study is controller-pinned and append-only, and byte-identity with it is the step's Exit. | accepted |
| S1-R4-02 | low | audit/rounds/fiat-856-framework-13-test-the-remaining-atlas-hand-o.md | Round 3's finding S1-R3-01 gave the corrected per-run record count as 52. That figure is wrong; the count is 54. The cause is this Warden's own command: a `git ls-tree -r --name-only 8dc3aca5 audit/rounds/` listing piped through `grep -v synopsis`, which matches the substring rather than the `.synopsis.md` suffix, so it also discarded two records whose names contain the word, `audit/rounds/fiat-369-read-audit-synopsis-resigned.md` and `audit/rounds/fiat-429-audit-record-schema-timestamp-synopsis.md`. Re-derived by suffix, `audit/rounds/` holds 108 files at the base ref, exactly 54 of them `.synopsis.md` and 54 records, which is also why round 3's other figure of 60 was right: 54 plus five plugin-local plus one root. The wrong number is the same class of defect round 3 raised against the study, produced by the round raising it, and it stands in the log because audit history is append-only. Corrected here and in the study amendment; round 3's record keeps its bytes. | accepted |

Leads not pursued: every replacement figure in the amendment was re-derived independently and all six hold. `audit/rounds/` holds 108 files at the base ref `8dc3aca5` with exactly 54 `.synopsis.md` siblings, giving 54 per-run records. Five plugin trees carry an `audit/AUDIT.md`, for ariadne, hexaemeron, pandects, probitas and tabularium, which is the exact five the amendment names. One root `audit/AUDIT.md` exists. 54 plus 5 plus 1 is 60, and running the synopsis check against a detached worktree at that base ref emits 60 lines independently. `docs/decisions/` holds 72 files there, 71 of them ADR-numbered. The invariants hold too: exactly one `design-bridge` fence remains, the study digest moved from `17c2fad7` to `43e02474` and both copies carry it, `cmp` reports the study and the runbook byte-identical to their `.hexaemeron/` originals, Protasis `--study` is clean at exit 0, and the amendment commit `c6639dfd` is signed `G` with one of each trailer and touches only `docs/atlas-harness-handoff/study.md` at 45 added lines. S1-R1-04 and S1-R3-01 are resolved and were confirmed rather than assumed: the amendment states that decision records live in `docs/decisions/` and names the record this run wrote, and it replaces 62 with 60, 55 with 54 and six with five, each with its recomputation. Neither is carried. S1-R2-04 is the one accepted item still standing, medium: the `hypomnema-design-bridge/v1` block at line 524 still names the ADR-074 path, `hypomnema --study` still reports `H008 record ... is unavailable or not an ordinary non-symlink file`, and it stays structurally unfixable because a second fence refuses with `H008 study declares more than one design bridge home` and deletion refuses with `H008 study has no design bridge block`. One imprecision was noticed and not raised as a finding: the amendment cites "the prose test at line 323" where line 323 is the paths tuple and the test's `def` is at 322, which points inside the right test and leaves its two load-bearing assertions exactly located. The amendment's record of the author-date trap is accurate, naming 00:25:51Z, 00:45:16Z, 01:40:30Z and 74.65 minutes against the 55 stated in ADR-076. The three open leads carry unchanged, the earned-class conditional, the ninth case, and the three shapes the schema admits. Round 1 and round 2 fixes were not re-driven this round because no code changed since round 3 drove them. Exits read on this round's tree, the amendment commit `c6639dfd` with no fixes commit added: phylax, ephoros and hypomnema each returned 0 over the changed path, Imprimatur scores the study 100.0 out of 100 with zero defects, `python3 -m unittest discover -s tests` returned 0 at 1140 tests, `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-856-step-1-r4.json` reports 1140 of 1140 at exit 0 on a fresh report path, and `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing. There is no fixes commit this round, so there is no Elenchus verdict to record.

## Step 1, round 5 -- 2026-09-04T08:26:34Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=not-applicable; probe-subprocess=not-applicable; absent-versus-unavailable=reviewed; stale-manifest=not-applicable; partial-manifest-write=not-applicable; network-absent=reviewed; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: this round was scoped to the one appended amendment, the invariants and the gates, so the study was not re-swept and the counts corrected in round 4 were not derived a third time. The Copilot general-availability date, the live `GET /api/job` figures and the Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` still need a network read no round of this step has made. Rounds 1 and 2 fixes were not re-driven, because no code has changed since round 3 drove them. The negative space is otherwise unchanged: no Solidity for the three recorded suite ids, no harness client run, no account created, no Atlas route touched, no manifest a probe wrote, and no measurement taken. This is the last round on this step, so the three findings below are recorded for the close and the carryover rather than for a further round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R5-01 | low | docs/atlas-harness-handoff/study.md | Line 607 reads "No quantity is restated here." and then, in the next sentence, states three: "Four wrong checkable quantities have been produced in this document across four audit rounds, three of them inside a correction rather than in the original work". Neither reading of it resolves. Read as the study alone, the wrong checkable quantities are the 62, the 55 and the six plugin-local trees, which is three rather than four, and all three stood in the original study text rather than inside a correction, so both halves fail. Read across the run's documents, they are the study's three, ADR-076's "all six" and "about ninety minutes", the runbook amendment's repeat of that interval, and this log's 52 and five, which is seven or eight depending on whether one wrong interval in two files counts once or twice, of which four or five arrived inside a correction. The amendment's own paragraph is the sentence that proves its point: a restatement is a fresh chance to be wrong, and this one took it while disclaiming that it had. | accepted |
| S1-R5-02 | low | docs/atlas-harness-handoff/study.md | The same amendment says "This step committed the schema, the manifest tests, the decision record and the pinned copies of these two artefacts, and those files use both strings as their working vocabulary." Only one of the five named files uses both. `docs/atlas-harness-handoff/study.md` carries `tested local route` six times and `go/copilot` four; `schemas/harness-classification-v1.json`, `tests/test_harness_manifest.py`, `docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md` and `docs/atlas-harness-handoff/runbook.md` carry `tested local route` two, two, one and two times and `go/copilot` zero times each. Taken collectively the set does use both strings, which is the reading that supports the conclusion, so this is an ambiguity with one false reading rather than a flat error. What it obscures is that `go/copilot` reaches the shipped tree through a single file, and that file is the study stating the string appears nowhere. | accepted |
| S1-R5-03 | low | audit/rounds/fiat-856-framework-13-test-the-remaining-atlas-hand-o.md | Round 4's finding S1-R4-01 states that `tested local route` "now occurs in five tracked files" and lists five. The five listed are correct but the count is not the tree's. The measuring command excluded `audit/`, and the record did not say so; at the tree it was measured against, `c6639dfd`, the string was already in seven tracked files, because this log and its synopsis had carried it since round 1. The `go/copilot` half of the same finding was accurate when measured, at one file, and reached three only once round 4's own record was committed. This is the second wrong quantity this Warden has put in this log, after the 52 of S1-R4-02, and it stands because audit history is append-only. | accepted |

Leads not pursued: the amendment does what round 4 asked of it and the correction itself is sound: it scopes the section 2 sentence to the surveyed ref rather than recounting anything, it states plainly that the previous amendment was wrong to list the claim as holding as written, and its substantive claims check out. Neither string occurs in any tracked file at `8dc3aca54adeca49387a2bdfc174cf6e72d02a11`, both occur on the shipped tree, this step did commit the schema, the manifest tests, the decision record and the two pinned copies, and this log does name both strings. The two findings against it are a self-contradicting disclaimer and one ambiguous sentence, not a wrong correction. Every invariant holds: the study digest moved from `43e02474` to `e226078d` and both copies carry it, `cmp` reports the study and the runbook byte-identical to their `.hexaemeron/` originals, exactly one `design-bridge` fence remains, Protasis `--study` is clean, and the amendment commit `de3baa49` is signed `G` with one of each trailer, touching only `docs/atlas-harness-handoff/study.md` at 37 added lines. S1-R4-01 is resolved and was confirmed rather than assumed. The accepted carry set closing with this step is five items, all recorded and none fixable from here: S1-R2-04, medium, the `hypomnema-design-bridge/v1` block at line 524 naming the ADR-074 path, which keeps `hypomnema --study` reporting H008 for good because a second fence refuses with `H008 study declares more than one design bridge home` and deletion refuses with `H008 study has no design bridge block`; S1-R4-02 and S1-R5-03, both low, the two wrong quantities this Warden wrote into this append-only log, 52 for a count that is 54 and five for a count that was seven; and S1-R5-01 and S1-R5-02, both low, the disclaimer and the ambiguity in the final amendment. The three open leads carry unchanged into the run: the earned-class conditional beneath step 2's classifier, a ninth case asserting every case survives `jsonschema` being absent, and the three shapes the schema admits, a calendar-invalid `recorded.date`, two entries naming the same harness, and a rejected authentication reaching the record only as `blocker` prose. The `runbook.md:261` divergence stands as round 3 stated it, the receipted amendment reading ninety minutes against ADR-076's corrected fifty-five. One systemic item is the reason six of this step's fifteen findings exist and belongs in the carryover with its own issue, those six being S1-R2-01, S1-R2-02, S1-R3-01, S1-R4-02, S1-R5-01 and S1-R5-03: no check in this repository asserts a prose quantity against the artefact it counts, so every figure in a study, decision record or audit record rests on its author's arithmetic alone, and a correction pass restating one is about as likely to introduce an error as to remove it. Exits read on this round's tree, the amendment commit `de3baa49` with no fixes commit added: phylax, ephoros and hypomnema each returned 0 over the changed path, Imprimatur scores the study 100.0 out of 100 with zero defects, `python3 -m unittest discover -s tests` returned 0 at 1140 tests, `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-856-step-1-r5.json` reports 1140 of 1140 at exit 0 on a fresh report path, and `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing. There is no fixes commit this round, so there is no Elenchus verdict to record.

## Step 2, round 1 -- 2026-09-04T11:25:06Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; stale-manifest=not-applicable; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: no Solidity entered this round's scope, so neither Pashov skill produced a finding. 376 `.sol` files exist in this repository, none of them in `ac81a656..be5ded32`, and the only `foundry.toml` is `plugins/pandects/foundry.toml` in another plugin's tree, so `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope, and no twelve-role report was produced or implied. `fizz` did not run for the same reason: this step ships no Solidity under Foundry or Hardhat. `metron` had no measurement to take, because the step's Disciplines field declares none. No harness client was run for real, no account was created, no plan was enrolled in, and no network call was made from the probe or from this round, so the Copilot seat entitlement that `auth_configured` cannot see stays unread, and no manifest a probe actually wrote was produced or read. Step 1's five accepted carry items were not reopened and no quantity corrected in an earlier round was re-derived. Nothing under `audit/` or `.hexaemeron/` was edited.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | scripts/probe_harnesses.py | `probe_client` read `completed.returncode` only inside `if version is None`, so a client that exited non-zero while printing any dotted number was recorded `answered` with that fragment as its `client_version`, and `recorded_client_run` then returned true. Four real failure shapes reach it, each observed on this tree: `Error: connect ECONNREFUSED 127.0.0.1:8080` at exit 1 yields version `127.0.0.1`, `node:internal/errors 4.18.2 unhandled rejection` at exit 1 yields `4.18.2`, `fatal: credentials expired at 2026.09.04` at exit 1 yields `2026.09.04`, and `unknown flag --version; see cursor.com/cli/0.1.2` at exit 2 yields `0.1.2`. On a host with the client installed and the declared key set, Cursor and Gemini CLI then earn `tested local route` from a client that failed. `Atlas launcher` is not reachable this way, because Copilot is the only row with `launcher_published` true and it declares no local authentication signal. The register's property survives literally, since a client did run, and fails in substance, since the class asserts an answer nobody got. `ClassifierTests` could not see it: its sweep varies `classify`'s inputs and never `probe_client`'s output, `RecordingRunner` defaults to `returncode=0`, no case in the file set it otherwise, and `RESULT_EXIT` therefore had no coverage at all. The exit status is now read first and is decisive, and `RESULT_EXIT` no longer claims the absence of a token it no longer consults. | fixed in 5251cb18 |
| S2-R1-02 | medium | schemas/harness-classification-v1.json | The step writes `UNREAD_VERSION`, the literal `unread`, into `client_version` for a present client that did not answer, while the schema described that field as "The exact version the client reported. Null only where no client was present to report one." A consumer written against the published contract therefore reads the sentinel as a version, which is the `absent-versus-unavailable` collapse moved one level down: the distinction survives only for a reader who knows to string-match a value the schema does not enumerate. `probe.status` carries `unread` structurally, but the `probe` block is optional in the entry, so nothing may rely on it. Mason's third deferred judgement asks whether this should be a schema field, and it should: the schema is the documentation where an interface crosses a process boundary, and three wording surfaces are about to be generated from this one. The description now names the sentinel and a declared `version_read` boolean carries the same fact as a field, false both where no client was present and where a present client did not answer. The defect was never a wrong value -- the sentinel cannot collide with a real version, because `recognise_version` only ever returns a digit-led `VERSION_TOKEN` match -- only an undocumented one. | fixed in 5251cb18 |
| S2-R1-03 | medium | tests/test_harness_manifest.py | `KilledProbeTests.tearDownClass` wrote `value: false` and `exit: 1` whenever `passed` did not equal all four `CASES`, and `passed` is only reached by a case that ran. Running one case by name passes and rewrites the report to `value: false`, observed on this tree with `test_a_killed_probe_leaves_nothing_where_there_was_no_manifest`. That report is the `killed-probe-recovery` resolver Fiat's design checker reads at the `step:3` transition, so a green partial run blocks the next step, and its `exit: 1` names a status nothing exited. An `attempted` set now gates the write, so only a run that attempted every case may speak for the gate. The fix and the property both live in this file, so Elenchus's parent overlay cannot go red on this one; `GateReportTests` drives `tearDownClass` directly instead, and sits in its own class so it cannot enter `KilledProbeTests.attempted` and suppress the real write. | fixed in 5251cb18 |
| S2-R1-04 | low | schemas/harness-classification-v1.json | `testable_here` is `client_present and auth_configured`, while the schema described it as "Whether an authenticated read-only client run was possible on this host." A present client that never answered is `testable_here` true on the exact observation that it did not run, and S2-R1-01's fix widens that state rather than narrowing it, because a non-zero exit now lands in `unread` as well. The field is worth keeping as it computes, so the description now says what it computes and points at `classification` for what the run earned. | fixed in 5251cb18 |
| S2-R1-05 | low | scripts/probe_harnesses.py | `_run` calls `subprocess.run(capture_output=True)`, which buffers the whole of the child's output before `recognise_version` truncates it. A child printing one line of 1,000,000 characters was captured at 1,000,001 characters against a `MAX_CLIENT_OUTPUT_CHARS` of 4,096, measured on this tree, so both caps bound what reaches the record and neither bounds what is read into memory. Phylax asks for a size cap as well as a time cap and only time is capped here, by the 10s default and the 120s ceiling. Accepted rather than fixed: the exposure is an operator's own local binary answering `--version`, the timeout bounds it in practice, and a correct bounded read means replacing `subprocess.run` with a manual `Popen` loop whose deadlock and orphan risks exceed the exposure they would close. Left open with the mechanism named rather than closed with a rewrite this round cannot test properly. | accepted |
| S2-R1-06 | low | .hexaemeron/runbook.md | Mason's carried gap is real and both halves reproduce. The runbook's false-red clause reads "A `run_checks.py` failure reported as `WAI-E-ADAPTER.TIMEOUT` is parallel load, not a defect; rerun with `--jobs 1`", which names an adapter-level failure. What Mason saw was a check-level `command-failure`: `hexaemeron-suite failed at 1800.4s`, on the 1800s check-map ceiling, with that suite alone green at 2270 of 2270 in 675.8s. The signatures differ, so the documented clause does not cover the observed red and the next reader hitting it has no written ground to call it load. The contention diagnosis is confirmed independently: on the same commit `be5ded32`, `python3 scripts/run_checks.py --full` returned 0 with outcome green and `hexaemeron-suite` at 346.3s, against load averages of 6.51, 9.30 and 31.40 on 18 CPUs where Mason's run had 31.6, 63.3 and 90.9. Open rather than fixed: the runbook is controller-pinned, and this correction is a Fiat amendment Warden may not write. | open, needs a Fiat runbook amendment |

Leads not pursued: Mason's four deferred judgements, in order. Windsurf staying off `product_withdrawn` is right, because `classify` makes that flag terminal ahead of every other branch and `unsupported`'s standing meaning here is Roo Code's -- sunset, repository archived, no successor named -- while Cascade inside Devin Desktop exists and is testable once somebody decides which product the row describes; the blocker carries that question to the reader who can settle it. Copilot declaring no local authentication signal is right for the reason the step's Exit gives, that seat entitlement is a network fact and the probe makes none, and inventing a placeholder variable would manufacture a signal; the consequence needs saying, because step 4's `atlas-claim` row rests on it. Driving the whole roster at best case, with every client present, every client answering and every declared authentication variable set, returns `manual route` for GitHub Copilot, Windsurf and Cline, `tested local route` for Cursor and Gemini CLI, and `unsupported` for Roo Code, so three of the four class names are reachable and `Atlas launcher` is reachable for no row at all: Copilot is the only harness with `launcher_published` true and it can never satisfy the authentication branch. `test_every_class_the_classifier_returns_is_one_of_the_schema_names` proves all four reachable from `classify`'s input space, which is a different claim from reachable on this roster. The third judgement is S2-R1-02 above. The `.horos` deferral is sound and was checked rather than assumed: `git status --porcelain .horos/` and `git diff HEAD -- .horos/candidates.json` are both empty on this branch, so no drift went uncarried, and `files_walked` moving 2499 to 2500 is exactly the one file the diff adds, `scripts/probe_harnesses.py`, since `tests/test_harness_manifest.py` shipped in step 1. Three of step 1's open leads land in this step's code and none is closed. `manifest_document` validates `date` against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and nothing else, so `2026-13-45`, `2026-02-31` and `0000-00-00` were all accepted on this tree; only an operator `--date` reaches it, because `main` defaults to `datetime.date.today().isoformat()`. Neither the schema's `uniqueItems` nor `manifest_document` refuses two entries naming the same harness, since `uniqueItems` compares whole entries and `ROSTER` is fixed, so only a caller passing its own roster reaches it. An authentication that exists and is rejected by the service stays indistinguishable from a working one, appearing only as `blocker` prose, and it stays that way for as long as the probe makes no network call, which is structural rather than fixable. The `jsonschema`-absent lead is answered in the shape the test file's own docstring asks for: `test_a_probe_written_document_validates_against_the_schema` checks the declared field set unconditionally and skips only the behavioural half, by name, where the library is missing. Two leads of this round's own, neither pursued. `write_log` writes `recorder.lines()` without sweeping the concatenation; every event was swept individually as it was recorded, and no pattern in `CREDENTIAL_PATTERNS` was found to span the newline join, because each requires `[:=]` immediately after its keyword where a record instead ends `"}` -- defence in depth with no reachable instance, so it was left alone. `_sentence` returns a bare `.` for an empty reason, which no roster row can produce today because all six `standing_blocker` strings and every `RESULT_` template are non-empty. Positive results worth recording, since a clean row is evidence too. The command boundary is the strongest part of the step and nothing in it moved: every `probe_argv` is a tuple of shell-metacharacter-free strings resolved from `PATH` by exact name, `_run` passes it as a list with no shell, `stdin` closed and `env` reduced to `PATH` alone so a client cannot echo an inherited credential, and `test_no_probe_argument_is_read_from_a_manifest` plants a `sh -c touch` command in a manifest sitting at the destination and proves the file is never created. The credential sweep allowlists rather than filters, runs per event at record time and again over the serialised manifest, and fails closed with nothing written; `write_manifest` precedes `write_log` in `main`, so a leak stops both surfaces. The atomic write is real: `KilledProbeTests` kills a genuine child with `SIGKILL` at both the `fsync` and the `os.replace` points and asserts a return code of -9, so no case there can pass by the child quietly finishing, and `read_manifest` refuses both a torn file and a foreign schema. The unearned-class hazard was hunted the four ways the packet asked and S2-R1-01 is where it was: no path bypasses `classify`, because `entry_document` holds the only `"classification"` assignment in the module and derives its value, and no manifest field supplies one; a hand-built `Observation` carrying a forged `ProbeRecord` is a caller in the same trust domain rather than an input path; and the hand-edited manifest a renderer could be handed is a boundary the schema states in its own `classification` description, which step 3 inherits rather than this step introducing. Exits, all read on a committed tree. At `be5ded32` before the fix: phylax 0, ephoros 0 and hypomnema 0 over the changed paths, `python3 -m unittest discover -s tests` 0 at 1160 tests, and `python3 scripts/run_checks.py --full` 0 with outcome green at 28 of 28 checks. At `5251cb18` after the fix: phylax 0, ephoros 0, hypomnema 0, `python3 -m unittest discover -s tests` 0 at 1168 tests, and `python3 scripts/run_checks.py --full` 0 with outcome green at 28 of 28 checks and `hexaemeron-suite` at 350.7s. The eight added cases are the whole difference between 1160 and 1168, and `tests.test_harness_manifest` moved from 28 cases to 36 against a runbook expectation of around eighteen. Elenchus ran the step's own runner contract, `python3 tests/run_tests.py --elenchus-report {report}` at report schema `elenchus.unittest.v1` through the adapter the script names `unittest-json-v1`, on the fresh report path `.elenchus/fiat-856-step-2-r1.json` the controller directed because `.elenchus/fiat-856-step-2.json` already exists and `run_tests.py` refuses an occupied target; it returned `guarded` on a parent assertion failure in `tests/test_harness_manifest.py`, at exit 0. The fixes commit is `5251cb186f08857df758e6659dff7e1e6e31a943`, signed `G`, authored Shoggoth, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer and touching three files. The four step-3 and step-4 register rows are not-applicable because no surface exists yet to check them against: `stale-manifest` needs a recorded manifest, `roster-drift` and `atlas-claim` need the three generated wording surfaces, and `pdf-nondeterminism` needs the PDF builder. Two findings stay open into the next round, S2-R1-05 accepted with its mechanism named and S2-R1-06 waiting on an amendment only Fiat can write.

## Step 2, round 2 -- 2026-09-04T13:45:00Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; stale-manifest=not-applicable; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: no Solidity entered this round's scope either, so neither Pashov skill produced a finding. `ac81a656..0178984b` carries no `.sol` path, 94 `.sol` files are tracked at both `be5ded32` and `0178984b`, and all four tracked `foundry.toml` files sit in other plugins' trees, so `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope and no twelve-role report was produced or implied. `fizz` did not run for the same reason: this step ships no Solidity under Foundry or Hardhat. Both suite documents and the reuse reference were read in full this round, because this is a new Warden for step 2 and a round number is not evidence that anything was read; the X-Ray preconditions were recomputed and matched, `skills/x-ray/SKILL.md` at `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41` and `lib/xray_reuse.py` at `23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10` against the `hexaemeron-x-ray-preaudit` overlay, and `skills/solidity-auditor/SKILL.md` at `1c1cf4e99d042e7aadc56b622d97a07d3286f4786838a05510697c814d1e983f`. No X-Ray reuse cache was consulted, so no reuse plan, scope manifest or cache verdict exists to report. `metron` had no measurement to take, because the step's Disciplines field declares none. No harness client was run for real, no account was created and no network call was made from the probe or from this round, so the Copilot seat entitlement `auth_configured` cannot see stays unread; the probe was run once end to end against this host, writing to a temporary directory, and every one of the six rows came back `manual route` or `unsupported` with `client_present` false. Nothing under `audit/` or `.hexaemeron/` was edited. Round 1's four fixed findings were re-checked rather than assumed fixed, and S2-R1-01's fix was re-driven against all four of its recorded failure shapes. The `attempted`-gate regression hunt did not establish that any break confined to the two subtest-wrapped cases leaves the other two green, so the false-green gate is proved as a mechanism and its reachability from a single realistic break is not established.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | schemas/harness-classification-v1.json | Round 1 answered S2-R1-02 by adding `version_read`, and added it to the entry's `properties` only. `harness.required` was not touched, so a conforming producer may omit the field entirely, and a consumer written against the published contract is then back to string-matching the `unread` sentinel -- the state S2-R1-02 called defective, one level down. The schema also permitted the two fields to contradict each other. Four shapes validated against `jsonschema` 4.25.1 on this tree: `version_read` omitted, `testable_here` omitted, `client_version` `"unread"` beside `version_read` true, and `client_version` `"1.2.3"` beside `version_read` false. The control refusal fired, so `additionalProperties: false` was working and the gap was `required`, not validation. The test file already held the mechanism -- `test_a_harness_entry_missing_any_observation_field_is_refused` asserts `required` membership and comments that being listed there "is the mechanism that turns the omission below into a refusal" -- and `OBSERVATION_FIELDS` was a five-tuple the new field never joined, so the fix landed outside a guard in the same file it edited. `version_read` is now in `required` and the pairing is two `allOf` conditionals. `testable_here` has the identical gap and deliberately does not get the identical treatment; see S2-R2-04. | fixed in 0178984b |
| S2-R2-02 | medium | tests/test_harness_manifest.py | The `killed-probe-recovery` gate derives `value` from a `passed` set each case adds itself to in a trailing statement, and three of the four cases reach that statement after a `subTest` loop. `subTest` records a failure and lets the block continue, so the trailing statement runs on a failed case. Driven on this tree with the kill assertion failing inside a `subTest` in every case, the run was red at five failures and `tearDownClass` wrote `value: true` and `exit: 0`. Fiat's design checker reads that file at the `step:3` transition, so this direction admits a broken atomic write, where S2-R1-03 only blocked a sound step. The exposure was survivable before this round only because `test_a_killed_probe_leaves_no_partial_file_the_reader_would_accept` makes its kill assertion outside any `subTest` and therefore raises: with the four cases failing the same way but that one raising, the report correctly said `value: false`. The gate's soundness rested on that one case's bracket placement, which nothing declared or tested. Attribution: `passed` and its four `add` calls predate round 1, present at `be5ded32`; S2-R1-03's fix added `attempted` and closed the false-negative direction only. A failure inside a `subTest` is now remembered by a `checked_subtest` wrapper, `_passed` is gated on it, and `resolved` requires both a complete attempt and an empty `failed` set. | fixed in 0178984b |
| S2-R2-03 | low | scripts/probe_harnesses.py | `recognise_version` applied `stream[:MAX_CLIENT_OUTPUT_CHARS]` before searching, so a token the bound cut in half was returned as the version. Measured on this tree: a client printing 4,090 filler characters then `1.23456789` had `1.234` recorded as `client_version`, with `version_read` true and `classification` `tested local route`, under a schema description calling the field "The exact version the client reported". This is S2-R1-01's family -- a `client_version` nobody reported -- reached by the bound rather than by an exit status, and much narrower: it needs more than 4,096 characters of output ahead of the version, which no client on this roster was observed to produce, and a cut landing one character earlier returns `None` and falls closed to `unread`. A match that runs to the truncation point is now discarded. The guard is deliberately conservative where the line bound rather than the character bound dropped the tail, which withholds a version rather than inventing one. This does not touch S2-R1-05: the whole of the child's output is still buffered before any of this runs. | fixed in 0178984b |
| S2-R2-04 | low | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | The record pinning this schema enumerates the entry as `name`, `classification`, "the five observation fields `client_present`, `client_version`, `auth_configured`, `launcher_contract` and `blocker`, with optional `testable_here` and `probe`", and adds "Objects are closed: an undeclared field is refused rather than ignored". `version_read` appears nowhere in it, so round 1's fix put a field in a closed object the pinning record does not name, and S2-R2-01 has now made that field required. This was found by attempting the symmetric fix: `testable_here` carries the same optional-field defect S2-R2-01 describes, and requiring it would contradict an explicit word in the record, so it was reverted and its optionality is now asserted by `test_the_fields_adr_076_calls_optional_stay_optional` instead. What remains is a required field absent from the enumeration that pins it. The remedy is an ADR-076 update, which is a decision-record change and not an audit fix; the step's Disciplines field declares `hypomnema: none`, so it is out of this step's declared scope as well. Left open rather than written, and named here so the divergence is not silently inherited by the three wording surfaces step 3 renders from this schema. | open, needs an ADR-076 update |
| S2-R2-05 | low | audit/rounds/fiat-856-framework-13-test-the-remaining-atlas-hand-o.md | Two quantities in round 1's own Not-checked field do not re-derive. It records "376 `.sol` files exist in this repository" and "the only `foundry.toml` is `plugins/pandects/foundry.toml`". Re-derived here: 94 `.sol` files are tracked at `be5ded32`, the commit round 1 audited, and 94 at `0178984b`; four `foundry.toml` files are tracked at `0178984b`, at `plugins/ariadne/tests/fixtures/forge-project/v1`, the same under `v2`, `plugins/janus/harness` and `plugins/pandects`. The `.sol` figure is not merely wrong but unstable, and its instability is the likelier cause: `/tmp/` is gitignored at `.gitignore:38` and `tmp/check-runner/<hash>/snapshot/` is a transient copy of the repository a check run creates and removes, so the same `find` invocation returned 236 at 13:00Z and 472 at 14:10Z on this tree, with 376 under `./tmp` alone during a live run -- round 1's figure is the transient snapshot's contribution rather than a count of the repository. The conclusion both numbers support is untouched and was re-established independently: no `.sol` path appears in `ac81a656..0178984b`, there is no `foundry.toml` or Hardhat config at the worktree root, and every tracked one sits in another plugin's tree. Not corrigible: `audit/` bytes are append-only and pinned, and the runbook's preamble states that no step edits any file under `audit/`. The correction is this row. | accepted |
| S2-R2-06 | low | .hexaemeron/study.md | A third false red, which the study's corrected constraints do not name. `hypomnema.py` over the three changed paths alone exits 1 with `scripts/probe_harnesses.py:93: H006 comment cites 'ADR-076', which does not exist`. The record does exist, tracked at `0178984b` and at `be5ded32`. The mechanism is invocation scope, read from the lint's source: `main` builds its index as `adr_index(files)` from the walked path list, and `adr_index` collects `ADR-NNN` only out of the filenames it is handed, so a path list carrying no `docs/decisions/ADR-*.md` treats every cited record as absent. Adding `docs/decisions` to the same invocation returns `clean` at exit 0. Only line 93 is reported because `_source_findings` scans `#` comment segments alone, leaving the identical citations in the module docstring at line 6 and in the `classify` docstring at line 541 unread. This round does not owe the three lint exits as controller fields, because `security_suite` records the Pashov ids, so nothing was blocked by it. Recorded rather than amended: naming it in the study is a Fiat amendment, and every study amendment un-binds every runbook repair, which is the carryover defect below. Whether that cost is worth paying is Fiat's decision, not Warden's. | accepted |
| S2-R1-05 | low | scripts/probe_harnesses.py | Carried from round 1, re-assessed and not reopened. `_run` still calls `subprocess.run(capture_output=True)`, which buffers the whole of the child's output before `recognise_version` bounds it, so neither `MAX_CLIENT_OUTPUT_CHARS` at 4,096 nor `MAX_CLIENT_OUTPUT_LINES` at 16 bounds what is read into memory; only the 10s default and the 120s ceiling bound it at all. Round 1's judgement was checked rather than re-litigated and it holds: the exposure is an operator's own local binary answering `--version`, and a correctly bounded read means a manual `Popen` loop whose deadlock and orphan risks exceed what they would close. S2-R2-03's fix narrows what a truncated read may be recorded as and does not change the memory bound, so this row is unmoved by it. | accepted |
| S2-R1-06 | low | .hexaemeron/runbook.md | Carried from round 1, and partially fixed rather than closed. Fiat acted: `hexctl amend study` appended a dated amendment stating and correcting both false reds, moving the study digest `e226078d` to `8b7721b6`, and `hexctl amend runbook` re-issued step 2's Disciplines replacement bound to it, moving the runbook digest `fa03bfd2` to `7c79ff0a`. Both were verified here against the packet rather than taken from it, and the study now carries the corrected pair at its lines 630 to 647. The residue is real and was confirmed from source. The offending clause sits in the runbook's shared preamble at lines 42 to 44, before `## Step 1` at line 56, and still reads that a failure "reported as `WAI-E-ADAPTER.TIMEOUT` is parallel load"; no amendment can reach it, because `hexctl.py:9192` declares `RUNBOOK_FIELDS = ("Goal", "Entry", "Exit", "Files", "Tests", "Disciplines")` and `COMPLETE_REPLACEMENT_RE` matches only those six, with `protasis.py:93` carrying the identical list. So the corrected pair lives only in the study and the runbook's narrower sentence stands as receipted. The four source claims behind that correction were re-derived rather than accepted: `scripts/run_checks.py` contains zero `WAI-` strings; all three `WAI-E-ADAPTER.TIMEOUT` refusals are raised at `scripts/agent_instruction.py:1376`, `:1408` and `:1413`, reaching suite output through `tests/test_agent_instruction.py:2560`; the check-level ceiling breach is `failure_class="command-failure"` with `reason="timeout"` at `scripts/run_checks.py:2292` against `DEFAULT_TIMEOUT_SECONDS = 1_800` at `:51`; and a genuinely failing suite is `test-failure` at `:2316`. Neither red is a defect in this diff, and this round met neither. | partially fixed, preamble unreachable by amendment |

Leads not pursued: The study-digest treadmill is carried forward, not answered, and it is a controller defect rather than step 2 work. `hexctl.py:10117` drops an amendment whose recorded `study_sha256` is not the current one, so any study amendment un-binds every runbook repair regardless of subject; step 2's Disciplines repair has now been re-issued three times for that reason, and S2-R2-06's remedy would make it four. Two `version_read` questions were looked at and left. A hand-built `Observation` can still carry a `ProbeRecord` whose status is `answered` with a null version, which would make `entry_document` emit `version_read` false beside `client_present` true and a null `client_version`; the schema's older conditional refuses that document, and no input path reaches it, because `probe_client`'s only `STATUS_ANSWERED` return passes a checked non-None token -- a caller in the same trust domain rather than an input, which is where round 1 left the equivalent forged-record lead. The `VERSION_TOKEN` bound of one to three dot groups plus an optional 32-character build suffix was checked against `CREDENTIAL_PATTERNS` and cannot reach any of them, since every keyword pattern needs a `[:=]` and `sk-`, `gh[pousr]_` and the JWT shape all require a non-digit lead the token cannot have. Three of round 1's leads were re-checked and none is closed. `manifest_document` still validates `date` against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and nothing else, so `2026-13-45` and `2026-02-31` remain acceptable, reachable only through an operator `--date`; the schema's `recorded.date` carries the same pattern and no more, so step 3's `stale-manifest` row inherits it. Neither `uniqueItems` nor `manifest_document` refuses two entries naming the same harness, reachable only by a caller passing its own roster. An authentication that exists and is rejected by the service stays indistinguishable from a working one, which is structural for as long as the probe makes no network call. Two of this round's own leads, neither pursued. `main` writes the manifest before the log, so a `--log` naming a directory that does not exist leaves a valid manifest on disk and returns 1 from `_atomic_write_text`'s refusal; the manifest is not wrong and the register's `partial-manifest-write` row is about the manifest file, so it was left alone rather than reordered. `_sentence` still returns a bare `.` for an empty reason, which no roster row can produce, because all six `standing_blocker` strings and every `RESULT_` template are non-empty. Positive results, since a clean row is evidence too. The command boundary did not move and is still the strongest part of the step: every `probe_argv` is a tuple of metacharacter-free strings resolved from `PATH` by exact name, `_run` passes it as a list with no shell, `stdin` closed and `env` reduced to `PATH` alone, and `test_no_probe_argument_is_read_from_a_manifest` plants a `sh -c touch` command in a manifest at the destination and proves the file is never created. S2-R1-01's fix was re-driven and holds: all four recorded failure shapes now return `unread` with a null version and `manual route` even with the client present and `CURSOR_API_KEY` set, and a zero-exit client still reaches `answered` at `1.2.3`. The kill hook is more robust than it looks, established by trying to defeat it: replacing `os.replace(temporary, target)` with `Path(temporary).replace(target)` does not evade the child's `probe.os.replace = die`, because `probe.os` is the singleton `os` module and patching its attribute patches the one `pathlib` calls, so that attempted regression produced four green cases rather than the false-green gate it was built to demonstrate. The probe was run end to end for the first time in this loop and its manifest validates against the tightened schema with no errors and all ten declared fields present. No manifest is committed anywhere in the tree, so requiring `version_read` constrains what step 3 must produce and breaks nothing that exists. Exits, all read on the committed tree at `0178984b` unless stated. `python3 -m unittest discover -s tests` returned 0 at 1173 tests, against 1168 at round 1's `5251cb18`; the five added cases are the whole difference, and `tests.test_harness_manifest` moved from 36 to 41 against a runbook expectation of around eighteen. `python3 tests/run_tests.py --elenchus-report {report}` was driven by Elenchus at report schema `elenchus.unittest.v1` through the adapter the script names `unittest-json-v1`, on the fresh path `.elenchus/fiat-856-step-2-r2.json` because `.elenchus/fiat-856-step-2.json` is occupied and `run_tests.py` refuses an occupied target; it returned `guarded` on a parent assertion failure in `tests/test_harness_manifest.py`, at exit 0. That report is written inside Elenchus's detached parent worktree and is not an artefact of this tree, and `.elenchus/` is gitignored at `.gitignore:73` in any case. `phylax` and `ephoros` each returned 0 over the three changed paths; `hypomnema` returned 1 over those paths alone and 0 with `docs/decisions` added, which is S2-R2-06 and not a defect in this diff. This round does not owe those three as controller fields, because `security_suite` records the Pashov ids rather than a waiver. `python3 scripts/run_checks.py --full` returned 0 with outcome green at 28 of 28 checks and nothing failed, the same 28 round 1 recorded. Every wall-clock figure in this record is inflated by contention and none of them is a measurement of this diff: `hexaemeron-suite` passed at 1432.2s against 346.3s for round 1's green run, and `root-suite` passed at 538.3s inside the parallel plan against 139.9s for the same suite run on its own here. The host carried five other concurrent agent sessions plus continuous `mds_stores` and `fseventsd` indexing throughout, on 18 CPUs: load averages were 142.67, 123.41 and 71.44 at 14:20 local, 66.14, 87.63 and 80.65 at 14:37, and 13.86, 58.94 and 70.12 at 14:40, against 6.51, 9.30 and 31.40 for round 1's green run and 31.6, 63.3 and 90.9 for the run that produced its false red. So the one-minute load here exceeded the false red's at times and the durations should be read as upper bounds. No red was met, so neither documented false red was diagnosed rather than encountered; the margin was small, because 1432.2s sits 367.8s under the 1800s check-map ceiling whose breach is the `command-failure` with `reason="timeout"` the study now names. Had a red appeared, its `failure_class` and `reason` would have classified it, not its duration. The fixes commit is `0178984b5d885c566dd0d1673723a35798f4bbb5`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer and touching the same three files. The four step-3 and step-4 register rows stay not-applicable for round 1's reason, unchanged this round: no surface exists yet to check them against. Two findings stay open into any next round, S2-R2-04 waiting on a decision record only its owner should change and S2-R1-06's preamble half structurally unreachable, and four are accepted with their mechanisms named.

## Step 2, round 3 -- 2026-09-04T14:20:00Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; stale-manifest=not-applicable; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: no Solidity entered this round's scope, so neither Pashov skill produced a finding. `ac81a656..fa09ef43` carries no `.sol` path, 94 `.sol` files are tracked at `fa09ef43` as at `be5ded32`, and all four tracked `foundry.toml` files sit in other plugins' trees, so `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope and no twelve-role report was produced or implied. `fizz` did not run for the same reason. This is the same Warden as round 2, so the suite documents were not re-read; their digests were recomputed and still match the `hexaemeron-x-ray-preaudit` overlay, `skills/x-ray/SKILL.md` at `b23bb945`, `lib/xray_reuse.py` at `23e628b7` and `skills/solidity-auditor/SKILL.md` at `1c1cf4e9`. No X-Ray reuse cache was consulted, so no reuse plan or cache verdict exists to report. `metron` had no measurement to take. No harness client was run for real and no network call was made from the probe, so the Copilot seat entitlement stays unread; the probe was run end to end against this host into a temporary directory and all six rows came back `manual route` or `unsupported` with `client_present` false. Nothing under `audit/` was edited, and no controller-pinned artefact was touched: the study, runbook and design-evidence digests are unchanged from the packet at the end of the round. S2-R1-05 and S2-R2-05 were carried on the controller's instruction and deliberately not re-derived. The ADR collision below was not diagnosed from the failing assertion alone; what could not be established is whether any consumer of `docs/harness-classification.json` outside this repository already reads the roster record by its current filename, because nothing in this tree names such a consumer.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-05 | high | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | The root suite is red at 2 failures and the step's exit contract is not met. `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` reports that this branch adds `ADR-076-generate-the-harness-roster-from-one-probed-manifest.md` while the default branch already holds `ADR-076-digest-neutral-measured-corpus.md`, and `test_wave_delta_reinstatement.ReinstatementConditionTests.test_the_pinned_decision_record_tests_pass` is that failure again through a subprocess. Neither is a false red and neither is caused by any audit round: the roster record has carried this number since `be5ded32`, checked at all six commits from `be5ded32` to `fa09ef43`, and this round edited its body without touching its name. What moved is the default branch. `origin/main` gained the colliding record in commit `530efcec`, dated 2026-09-04 11:07:49 +0100 and not an ancestor of this branch, and the ref only reached this clone at the `14:48:42` fetch recorded in its reflog: at the previous `13:40:20` fetch `origin/main` was `70dfef7b`, whose `docs/decisions` holds `ADR-075` and no `ADR-076`, and at `52df51b0` it holds both. So round 2's green root suite was correct when it ran and this red is correct now, and the test's verdict turns on when the shared remote-tracking ref was last fetched rather than on anything in the diff. The next free number is `ADR-077`; the highest in use across this branch and `origin/main` is 076. Not fixed, and not fixable from here, because the renumber and the controller repair have to land together: `.hexaemeron/runbook.md` names `ADR-076` seven times, four of them the full filename, and its effective Disciplines field is the amendment bound at `1a05701c` against runbook `ead01b11`; renaming the file makes that field name a path that does not exist, which is the exact defect the amendment chain was re-issued three times to repair. `.hexaemeron/study.md` names it twice. `docs/atlas-harness-handoff/study.md` and `runbook.md` name it two and three times and must stay byte-identical to the pinned originals, so they cannot move first either. The record itself, `schemas/harness-classification-v1.json`, `scripts/probe_harnesses.py` and `tests/test_harness_manifest.py` hold one, one, three and five references that would move with a renumber. The 13 and 5 references in this run's audit log and synopsis stay as they are, being append-only and correct about what was true when written. | open, needs a renumber to ADR-077 sequenced with a Fiat runbook amendment |
| S2-R3-01 | low | scripts/probe_harnesses.py | A regression in round 2's own fix, found by re-deriving its boundary rather than trusting it. The guard treated a match reaching the end of a truncated line as a possible fragment, which is the wrong question: what decides it is the character the cut removed. Measured on this tree at `MAX_CLIENT_OUTPUT_CHARS` of 4,096, a client printing `1.2.3` followed by a newline had its version withheld and landed in `unread`, and so did `1.2.3` followed by a space, while `1.2.3` followed by `)` was read, because that cut left the match short of the line end. The failure direction was safe -- it withheld a version rather than inventing one, which is why round 2's own record called the guard deliberately conservative -- but it under-reported a client that had answered correctly. The decision now consults the first dropped character through `_extends_version`: a digit, dot, dash or plus could have continued a `VERSION_TOKEN`, and nothing else could. Verified both ways on this tree: `1.2.3\n`, `1.2.3 `, `1.2.3)` and `1.2.3,` are read, and `1.23456789`, `1.2.3.4`, `1.2.3-rc1` and `1.2.3+build` cut at the bound stay withheld. | fixed in fa09ef43 |
| S2-R3-02 | low | scripts/probe_harnesses.py | `main` reports an operator-facing failure as one `probe_harnesses: <reason>` line and exit 1, and did so for every path its own checks enumerate, but not for one the filesystem rejects instead. `--out` naming an existing directory passes `_checked_out`, passes `_atomic_write_text`'s `is_dir` check on the parent, and reaches `os.replace`, which raises `IsADirectoryError`; `except ProbeError` does not catch an `OSError`, so the run ended in a traceback carrying the operator's path back out rather than in a refusal. Observed on this tree, beside the adjacent case that already refused cleanly: `--out` inside a directory that does not exist returns 1 through the `ProbeError` path. This is the phylax boundary the step's Disciplines field names, and the inconsistency was in the reporting rather than in the write, which stayed atomic and left nothing behind. `main` now catches `OSError` as well and reports the error type without the path. | fixed in fa09ef43 |
| S2-R3-03 | low | scripts/probe_harnesses.py | `read_manifest` is described in its own docstring as the reader that accepts a manifest "or refuses it", and it is the oracle step 3's renderer reads the roster through. Every refusal in the module is a `ProbeError` except the two a renderer is likeliest to meet: an absent manifest raised `FileNotFoundError` and a directory raised `IsADirectoryError`, both straight from `target.stat()`, so a renderer catching the documented type would miss exactly the cases it was written for. Observed on this tree before the fix and after. Both now raise `ProbeError` naming the error type, and `read_text` is covered the same way. The distinction matters at the step boundary rather than here, which is why it is worth closing before step 3 renders anything through it. | fixed in fa09ef43 |
| S2-R3-04 | low | tests/test_harness_manifest.py | The guards written for S2-R3-02 and S2-R3-03 did not register as guards. Run against the unfixed parent, both failed by propagating the very exception their fix exists to catch -- `assertRaises(ProbeError)` does not catch an `OSError`, and `main` raised before returning -- so the parent report carried three uncaught errors beside four assertion failures, and Elenchus classified the mixture `inconclusive` on `08adf767` rather than `guarded`. This is the same shape as S2-R2-02: a check whose signal cannot reach the thing that reads it. Both guards now catch broadly and assert on the type, so the unfixed parent produces seven assertion failures and no errors, and Elenchus returns `guarded` on `fa09ef43`. The finding is recorded rather than quietly corrected because the mechanism generalises: a guard whose failure mode on the unfixed tree is an escaped exception is invisible to the mechanical guard check the run relies on, whatever it proves to a person reading it. | fixed in fa09ef43 |
| S2-R2-04 | low | docs/decisions/ADR-076-generate-the-harness-roster-from-one-probed-manifest.md | Carried from round 2, in scope this round by the amendment bound at `1a05701c`, and fixed. The record enumerated a closed entry as `name`, `classification` and five observation fields "with optional `testable_here` and `probe`", never naming `version_read`, which step 2's schema had made required; it therefore described a manifest the schema refuses. The enumeration now names `version_read` among the required fields, states that `testable_here` and `probe` are the only optional ones, and gives the reason the field is required rather than optional, which is the reason round 2 found it: a field a conforming producer may omit is one a consumer cannot rely on, leaving it recognising the sentinel after all. It also records what `testable_here` means, since round 2 rewrote that description in the schema. `test_every_declared_field_is_either_required_or_named_optional` now asserts the partition in both directions, so a field added to `properties` without a decision on its status fails a case rather than drifting out of the record again. The `testable_here` optionality assertion round 2 added is kept and unchanged. | fixed in fa09ef43 |
| S2-R2-06 | low | .hexaemeron/study.md | Carried from round 2, and resolved as an invocation rather than a defect. The amendment states the remedy: invoke `hypomnema.py` with `docs/decisions` in its path list, because it builds its decision-record index from the paths it is handed. Confirmed on this tree at `fa09ef43`: `phylax`, `ephoros` and `hypomnema` each return 0 over `scripts/probe_harnesses.py`, `tests/test_harness_manifest.py`, `schemas/harness-classification-v1.json` and `docs/decisions`, and `hypomnema` returns 0 over `docs/decisions` alone, which also checks the ADR edited above as a record in its own right. The H006 report round 2 met is not reachable from the stated invocation, so nothing is left accepted here. Recorded as the invocation the round ran, per the amendment. | fixed by the stated invocation |
| S2-R1-05 | low | scripts/probe_harnesses.py | Carried from round 2 unchanged, on the controller's instruction not to redo the judgement. `_run` still calls `subprocess.run(capture_output=True)`, buffering the whole of the child's output before any cap applies, so neither `MAX_CLIENT_OUTPUT_CHARS` at 4,096 nor `MAX_CLIENT_OUTPUT_LINES` at 16 bounds what is read into memory; only the 10s default and the 120s ceiling bound it. S2-R3-01's change to `recognise_version` alters which tokens may be recorded and touches the memory bound not at all, so this row is unmoved by it. | accepted |
| S2-R2-05 | low | audit/rounds/fiat-856-framework-13-test-the-remaining-atlas-hand-o.md | Carried from round 2 unchanged, on the controller's instruction not to redo it. Round 1's Not-checked field records "376 `.sol` files" and "the only `foundry.toml`" where 94 and four are tracked, the `.sol` figure being the transient gitignored `tmp/check-runner/` snapshot rather than a count of the repository. The conclusion both numbers support is untouched and was re-established independently again this round. Not corrigible, because `audit/` bytes are append-only; the correction stands in round 2's record. | accepted |
| S2-R1-06 | low | .hexaemeron/runbook.md | Carried from round 2 unchanged, and confirmed unreachable by the controller. The study carries the corrected false-red pair; the offending sentence sits in the runbook's shared preamble at lines 42 to 44, ahead of `## Step 1`, where no amendment can reach it, because `hexctl.py:9192` and `protasis.py:93` both restrict a replacement to `Goal`, `Entry`, `Exit`, `Files`, `Tests` and `Disciplines`. Left recorded as it stands, on instruction. This round met neither documented false red, and the red it did meet is neither of them. | partially fixed, preamble unreachable by amendment |

Leads not pursued: The study-digest treadmill stays carried and was not triggered this round, which is worth recording as a positive: the amendment that put S2-R2-04 and S2-R2-06 in scope was made in the runbook, the study digest is unchanged at `8b7721b6`, and `next` reports two bound amendments for the step rather than none. `hexctl.py:10117` still drops an amendment whose recorded `study_sha256` is not current, so the defect is unfixed rather than absent, and the next study amendment will un-bind both of step 2's repairs. Round 2's three fixes were re-driven rather than assumed, and one held completely: every observation shape `observe` can actually reach was pushed through `entry_document` and validated, fourteen in all, across absent, answered, non-zero exit, zero exit with no version, timeout, `OSError` and no-argv-declared, each at both authentication states, and none was refused by the tightened schema, so making `version_read` required broke no reachable generator output. The `checked_subtest` mechanism was probed for holes and none was found: `unittest.SkipTest` is re-raised rather than recorded as a failure, `failed` is keyed by method name so one case cannot suppress another, and `GateReportTests` patches `attempted`, `passed` and `failed` together, so driving `tearDownClass` directly cannot disturb the real class. The gate report on a full green run is still `value: true` and `exit: 0`, and writing it leaves the tree clean because the bytes are identical. Three leads of this round's own, none pursued. `write_log` is still called after `write_manifest` in `main`, so a `--log` naming a missing directory leaves a valid manifest on disk beside exit 1; the manifest is not wrong and the register's row concerns the manifest file, so the ordering was left alone, and S2-R3-02's `OSError` catch now reports that case as one line rather than a traceback. `manifest_document` still validates `date` against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and nothing more, so `2026-13-45` and `2026-02-31` remain acceptable through an operator `--date`, and the schema's `recorded.date` carries the same pattern, which step 3's `stale-manifest` row inherits. `_sentence` still returns a bare `.` for an empty reason, which no roster row can produce. Positive results, since a clean row is evidence too. The command boundary did not move: every `probe_argv` is a tuple of strings carrying no shell metacharacter, checked by pattern across the whole roster this round, `_run` passes it as a list with no shell and an environment holding only `PATH`, and `test_no_probe_argument_is_read_from_a_manifest` still plants a `sh -c touch` command in a manifest at the destination and proves the file is never created. The credential sweep was run over both surfaces a real probe wrote and found nothing, and the log carries no absolute path. Exits, all on the committed tree at `fa09ef43` unless stated. `python3 -m unittest tests.test_harness_manifest` returned 0 at 45 cases, against 41 at round 2 and a runbook expectation of around eighteen. `python3 -m unittest discover -s tests` returned 1 at 1,177 tests with 2 failures, both S2-R3-05; the four added cases are the whole difference from round 2's 1,173. `phylax`, `ephoros` and `hypomnema` each returned 0 over the four paths named in S2-R2-06. Elenchus ran the step's own runner contract, `python3 tests/run_tests.py --elenchus-report {report}` at report schema `elenchus.unittest.v1` through the adapter the script names `unittest-json-v1`, on the fresh path `.elenchus/fiat-856-step-2-r3.json` because `.elenchus/fiat-856-step-2.json` is occupied and `run_tests.py` refuses an occupied target; it returned `inconclusive` on `08adf767` and `guarded` on `fa09ef43` after S2-R3-04, both at exit 0, and that report is written inside Elenchus's detached parent worktree rather than into this tree, which is gitignored at `.gitignore:73` in any case. `python3 scripts/run_checks.py --full` returned 1 with outcome red at 28 checks, one of them failed: `root-suite` at 201.1s. That red is S2-R3-05 and neither documented false red, established rather than assumed. `root-suite` is declared `kind: "suite"` in `tests/check-map-v1.json`, so a non-zero return takes the `scripts/run_checks.py:2316` branch and is recorded `failure_class="test-failure"`; the check-level ceiling breach the study names is the `:2292` branch, `failure_class="command-failure"` with `reason="timeout"`, and it cannot apply here because 201.1s leaves 1,598.9s of headroom under the `DEFAULT_TIMEOUT_SECONDS = 1_800` at `:51`. The adapter-level `WAI-E-ADAPTER.TIMEOUT` signature cannot apply either, since `run_checks.py` carries no `WAI-` string at all. Load was low throughout and is not a candidate explanation: one-minute load averages were 5.09 at the start and 11.23 at the end on 18 CPUs, against 66.14 to 142.67 during round 2's green run, and `root-suite` took 201.1s here against 538.3s inside round 2's parallel plan. The other 27 checks passed. The fixes commit is `fa09ef435e8b0ce81d6cc6d458da97f7d396057f`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer across three files. The four step-3 and step-4 register rows stay not-applicable for the reason rounds 1 and 2 gave, unchanged: no surface exists yet to check them against. One finding stays open, S2-R3-05, and it is the one that matters, because the step's exit contract is unmet until the roster record is renumbered and the runbook amendment naming its path is re-issued to match. Three are carried at their previous status on instruction, and five were fixed here.

## Step 2, round 4 -- 2026-09-04T14:50:00Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; stale-manifest=not-applicable; roster-drift=not-applicable; atlas-claim=not-applicable; pdf-nondeterminism=not-applicable

Not checked: no Solidity entered this round's scope, so neither Pashov skill produced a finding. `ac81a656..eb92833c` carries no `.sol` path, 94 `.sol` files are tracked at `eb92833c` as at `be5ded32`, and all four tracked `foundry.toml` files sit in other plugins' trees, so `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope; no twelve-role report was produced or implied, and `fizz` did not run for the same reason. This is the same Warden as rounds 2 and 3, so the suite documents were not re-read; their digests were recomputed and still match the `hexaemeron-x-ray-preaudit` overlay. No X-Ray reuse cache was consulted, so no reuse plan or cache verdict exists to report. `metron` had no measurement to take. No harness client was run for real and no network call was made from the probe, so the Copilot seat entitlement stays unread. This is the run's final audit round for step 2, so nothing is deferred: every open item below carries an explicit disposition rather than an implied next round. Two things were not established and are named rather than assumed. Which test Elenchus actually saw fail on its own detached parent is inferred from a reconstruction, not read from its report, because the report is written inside a worktree it removes; the reconstruction was made faithful before it was trusted, and S2-R4-03 says what was and was not observed. Whether any consumer outside this repository reads the roster record by its old filename could not be established, because nothing in this tree names such a consumer, so the renumber's blast radius is bounded by what this tree contains and no further.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-05 | high | docs/decisions/ADR-077-generate-the-harness-roster-from-one-probed-manifest.md | Carried from round 3 and closed. The roster record's number collided with the default branch, and the root suite was red at two failures. The record is renumbered to ADR-077 and the collision is gone: `tests.test_decision_records` returns OK at 5 cases and the `test_the_pinned_decision_record_tests_pass` cascade returns OK, both re-run against `origin/main` at `166d3e21` after a fresh fetch, and `python3 -m unittest discover -s tests` returns 0 at 1,177 tests. Ten occurrences moved across four files; the `git mv` is recorded as a rename at 99% similarity. The number was rechecked immediately before this record was written, because 075 was already lost to a pending PR earlier in this run: `origin/main`'s numbered records run 071 to 076 with no 077, none of 218 remote branches carries a competing ADR-077 file, and none of 49 open pull requests does either. The runbook amendment bound at `fb4631e4` already names ADR-077, so the effective Disciplines field and the file on disk now agree, which was the ordering constraint round 3 could not satisfy alone. | fixed in eb92833c |
| S2-R4-01 | medium | docs/atlas-harness-handoff/study.md | The two shipped copies were stale against the artefacts they are required to equal, so every correction this run has made reached no repository reader. Step 1's Exit calls `docs/atlas-harness-handoff/study.md` and `docs/atlas-harness-handoff/runbook.md` byte-identical copies of the two `.hexaemeron/` artefacts. They were not: the study copy was 34,231 bytes against 39,598 pinned and the runbook copy 17,362 against 24,666, with `cmp -s` failing on both. The study copy's digest was `e226078d6d4a78490498461387f2f9e1a440b0ad42a88fb8144ed4fc5659cc8c`, which is the study digest recorded before this run's first amendment, so the copies had been stale since that amendment and carried none of it: not the false-red correction, not either ADR correction, not the renumber. Both are now byte-identical, `cmp -s` clean at 39,598 and 24,666 bytes, with digests `e858c4c9` and `18d3c53f` matching the artefacts this round's packet named. The refreshed copies carry ADR-074 four and nine times, ADR-076 six and ten times and ADR-077 three and two times, all inherited from the pinned artefacts and correct, because byte-identity is the requirement and those references are historical amendment prose together with the two uncorrectable ones. | fixed in eb92833c |
| S2-R4-02 | low | tests/ | Nothing asserts the identity S2-R4-01 violated, which is why three green rounds passed over it. `grep -rln atlas-harness-handoff tests/` returns nothing and exits 1, and no other check names the pair either, so the copies can go stale again at the next amendment with every gate still green. Recorded and deliberately not built: step 2's declared `Files` are `scripts/probe_harnesses.py` and `tests/test_harness_manifest.py`, so a docs-identity test sits outside this step, and adding one here would put a file outside the step's contract into an audit fixes commit. The durable fix needs its own carryover item, and this is the disposition rather than a deferral, because there is no round 5 to carry it to: without that item the refresh done above is a one-off, and the next amendment re-opens the same gap silently. | carried forward, needs a carryover item |
| S2-R4-04 | low | tests/test_harness_manifest.py | The reference count in this round's brief was short, and following it literally would have left the tree red. The brief named four tracked files carrying one occurrence each; the tracked occurrence counts are ten across four files, being one in the record's own heading, one full path in the schema description, three in the probe at the module docstring, the classifier comment and the `classify` docstring, and five in the test module, which the brief did not name at all. Four of the test module's five sit in `#` comments or an assertion string, which is exactly what `hypomnema`'s H006 scans, so leaving them would have reported `ADR-076` as a record that does not exist and turned a green lint red on the renumber itself. The brief's audit-file counts were also short, at eight and five against twenty-five and twenty-five. Verified rather than assumed, as the brief asked. All ten moved, and the audit files were left alone. | fixed in eb92833c |
| S2-R4-03 | low | tests/test_harness_manifest.py | The `guarded` verdict this round records is substantively right and its attribution is misleading, which is worth stating beside the receipt. Elenchus reports `guarded -- the runner report records a parent assertion failure` and names `tests/test_harness_manifest.py`, but this commit's only changes to that module are five comment and string edits renaming ADR-076 to ADR-077, none load-bearing, and the changed module overlaid on a faithful detached parent worktree at `eb92833c^` returns OK at 45 cases. The test that does fail there is `tests.test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`, in a file this commit never touched, and it passes on the fixed tree, so a test genuinely fails without the fix and the verdict holds. What is named is the overlay target rather than the guard. Established on a real `git worktree add --detach`, after a first reconstruction by `git archive` produced ten unrelated failures because a tar extraction is not a git repository and the prose, boundary-currency and dead-code cases inspect git state; that reconstruction was discarded rather than reported. Accepted rather than fixed: the attribution is in the vendored adapter's one-line output, not in this diff, and the receipt's own evidence is the verified fixes range rather than this string. | accepted |
| S2-R1-05 | low | scripts/probe_harnesses.py | Carried at its previous status on the controller's instruction not to redo the judgement, and given a final disposition here rather than left open. `_run` still calls `subprocess.run(capture_output=True)`, buffering the whole of the child's output before any cap applies, so neither `MAX_CLIENT_OUTPUT_CHARS` at 4,096 nor `MAX_CLIENT_OUTPUT_LINES` at 16 bounds what is read into memory; only the 10s default and the 120s ceiling bound it. The exposure is an operator's own local binary answering `--version`, and a correctly bounded read means a manual `Popen` loop whose deadlock and orphan risks exceed what they would close. Accepted for the life of the step, not pending. | accepted |
| S2-R2-05 | low | audit/rounds/fiat-856-framework-13-test-the-remaining-atlas-hand-o.md | Carried at its previous status on instruction, with a final disposition. Round 1's Not-checked field records "376 `.sol` files" and "the only `foundry.toml`" where 94 and four are tracked, the `.sol` figure being the transient gitignored `tmp/check-runner/` snapshot rather than a count of the repository. The conclusion both numbers support is untouched and was re-established independently in each of rounds 2, 3 and 4. Not corrigible, because `audit/` bytes are append-only; the correction stands in round 2's record and is not reopened. | accepted |
| S2-R1-06 | low | .hexaemeron/runbook.md | Carried at its previous status on instruction. The study carries the corrected false-red pair; the offending sentence sits in the runbook's shared preamble at lines 42 to 44, ahead of `## Step 1`, where no amendment can reach it, because `hexctl.py:9192` and `protasis.py:93` both restrict a replacement to `Goal`, `Entry`, `Exit`, `Files`, `Tests` and `Disciplines`. This round met neither documented false red. The disposition is the controller's carryover block rather than a further round, and it is final for step 2. | carried forward, preamble unreachable by amendment |

Leads not pursued: Every item here has a disposition, because this is step 2's last round. Four are the controller's and were excluded from this round by instruction: S2-R1-06's preamble half above, the study-digest treadmill, and the two ADR references that cannot be corrected. The treadmill is unfixed rather than absent and fired again this round as expected, the study amendment un-binding the two previous runbook repairs so that `fb4631e4` is the only bound one, which is correct because the newest supersedes; `hexctl.py:10117` still drops an amendment whose recorded `study_sha256` is not current, and it stays a controller defect in the carryover block. The two uncorrectable references were confirmed from source rather than accepted: the study's fenced `design-bridge` block at lines 521 to 525 names `docs/decisions/ADR-074-generate-the-harness-roster-from-one-probed-manifest.md`, two renumbers behind, and step 1's baseline `Exit` and `Files` name the ADR-076 path, which no amendment can rewrite because `hexctl.py:9453` dies with `amendment cannot rewrite completed step(s)` and `.hexaemeron/state.json` records step 1 as `done`. Both are carried, and a reader who finds ADR-074 or ADR-076 in either place should read the current record's own heading instead. Four leads of this round's own, each disposed of rather than carried. The audit log and its synopsis keep twenty-five ADR-076 references each, deliberately: they are append-only history, they were correct when written, and the runbook's preamble forbids a step editing anything under `audit/`; they are named here as history so a later reader does not take them for live references. `main` still writes the manifest before the log, so a `--log` naming a missing directory leaves a valid manifest beside exit 1; the manifest is not wrong, the register's row concerns the manifest file, and S2-R3-02's `OSError` catch now reports that case as one line, so it is accepted as it stands. `manifest_document` still validates `date` against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and nothing more, so `2026-13-45` and `2026-02-31` remain acceptable through an operator `--date`, and the schema's `recorded.date` carries the same pattern; this is step 3's `stale-manifest` row to close, and it is handed to that step rather than accepted here. `_sentence` still returns a bare `.` for an empty reason, unreachable because all six `standing_blocker` strings and every `RESULT_` template are non-empty, so it is accepted. Positive results, since a clean row is evidence too. One suspicion was chased down and dismissed rather than left hanging: the synopsis is 97,669 bytes against a 97,035-byte source, which looks wrong for a bounded view, and it is not, because the view is line-collapsed at one record per line with each newline written `<br>`; the 634-byte difference is accounted for exactly by a 212-byte header plus 143 `<br>` substitutions at three bytes each, and the generator's own budget is per-record lines, at nine against 158. The command boundary did not move: every `probe_argv` is a tuple of strings carrying no shell metacharacter, checked by pattern across the whole roster, `_run` passes it as a list with no shell and an environment holding only `PATH`, and `test_no_probe_argument_is_read_from_a_manifest` still plants a `sh -c touch` command in a manifest at the destination and proves the file is never created. The credential sweep was run over both surfaces a real probe wrote and found nothing, and the log carries no absolute path. Round 2's and round 3's fixes were re-driven rather than assumed and all held. Exits, all on the committed tree at `eb92833c`. `python3 -m unittest tests.test_harness_manifest` returned 0 at 45 cases, unchanged from round 3 because this round's edits to that module are comment text. `python3 -m unittest discover -s tests` returned 0 at 1,177 tests, against exit 1 and two failures at the same count in round 3; the renumber is the whole difference. `python3 scripts/run_checks.py --full` returned 0 with outcome green at 28 of 28 checks and nothing failed, which is step 2's exit condition met on a committed tree: `root-suite` passed at 201.8s, `hexaemeron-suite` at 608.7s against the 1,800s ceiling, `dead-code-suite` at 77.2s on a clean tree, with one-minute load averages of 5.59 at the start and 12.30 at the end on 18 CPUs, so neither documented false red was in play and none was met. `phylax`, `ephoros` and `hypomnema` each returned 0 over the probe, the test module, the schema, `docs/decisions` and `docs/atlas-harness-handoff`, which is the invocation the amendment states and which also checks the renamed record and both refreshed copies. Elenchus ran the step's own runner contract on the fresh path `.elenchus/fiat-856-step-2-r4.json` and returned `guarded` at exit 0, qualified by S2-R4-03. The fixes commit is `eb92833c0964eb98166d14e461d7827e1ad1ffde`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer across six files including the rename. The four step-3 and step-4 register rows stay not-applicable for the reason every round has given: no surface exists yet to check them against, and step 3 is where `stale-manifest` and `roster-drift` first become checkable. Nothing is left open that this round could have closed. One item is carried forward with a named owner, S2-R4-02's missing docs-identity assertion, which needs a carryover item because it sits outside step 2's declared `Files`; three are accepted with their mechanisms named; and the controller's four remain in its carryover block.

## Step 3, round 1 -- 2026-09-04T18:19:23Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; stale-manifest=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; roster-drift=reviewed; atlas-claim=not-applicable; pdf-nondeterminism=reviewed

Not checked: no Solidity entered this step, so neither Pashov skill produced a finding. `c0524f0c..16831d4b` carries no `.sol` path, 94 `.sol` files are tracked at `16831d4b` as at `eb92833c`, there is no `foundry.toml` or Hardhat configuration at the worktree root, and all four tracked `foundry.toml` files sit in other plugins' trees, at `plugins/ariadne/tests/fixtures/forge-project/v1`, the same under `v2`, `plugins/janus/harness` and `plugins/pandects`. So `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope; no twelve-role report was produced or implied, and `fizz` did not run because this step ships no Solidity under Foundry or Hardhat. A `find` over the worktree returns 516 `.sol` files against 94 tracked, which is the transient gitignored `tmp/check-runner/` snapshot step 2's round 2 diagnosed rather than a count of the repository, and it is named here so the figure is not read as a third count. This is a new Warden for step 3, so all three suite documents and the reuse reference were read in full rather than assumed from a round number; the X-Ray preconditions were recomputed and matched, `skills/x-ray/SKILL.md` at `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41` and `lib/xray_reuse.py` at `23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10` against the `hexaemeron-x-ray-preaudit` overlay, with `skills/solidity-auditor/SKILL.md` at `1c1cf4e99d042e7aadc56b622d97a07d3286f4786838a05510697c814d1e983f` and `skills/fizz/SKILL.md` at `62a60df4cec160511b8ef36433eef7c8805d0b4a398491293eb4542ab73539bd` matching their own overlay entries. No X-Ray reuse cache was consulted, so no reuse plan, scope manifest or cache verdict exists to report. No harness client was run and no network call was made from this round, so the six per-harness observations landed in `docs/harness-classification.json` were read rather than reproduced, and the Copilot seat entitlement `auth_configured` cannot see stays unread. `atlas-claim` is marked not-applicable on the controller's assignment of that row to step 4 rather than because the surface is absent, and the distinction matters now that a surface exists: the sentence the row names stands at `docs/how-to-help-shoggoth.md:117`, reading that the ChatGPT and Claude routes "are covered by the Atlas launcher tests", this step did not touch it, and the Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` was not fetched, so whether it holds a route test is unestablished here exactly as it was in every earlier round. Four things are named as unknown rather than assumed. That the committed PDF was built by reportlab 4.4.5 specifically is Mason's report and not something this round read: the file establishes only that its producer is not 5.0.1's, so S3-R1-01 states the producer string it can prove and attributes the version number. Whether any reader outside this repository consumes the roster from the surfaces rather than the manifest could not be established, so the blast radius of a wording change is bounded by this tree and no further. Whether the two Markdown surfaces read correctly to a person who stops at the generated block was not tested on a reader. And the parent-overlay reconstruction behind the Elenchus verdict was run here on a real detached worktree, but which test the vendored adapter itself saw is inferred from that reconstruction rather than read from its report, which is written inside a worktree it removes.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | docs/pdf/how-to-help-shoggoth.pdf | The committed PDF was built by a `reportlab` this host does not have, and rebuilding it here turns the tree red. The file's `/Producer` reads `ReportLab PDF Library - www.reportlab.com`; `reportlab` 5.0.1, the only one installed, hardcodes `_default_producer = "ReportLab PDF Library - (opensource)"` at `pdfbase/pdfdoc.py:1552` with matching markers at `:189` and `:894`, so 5.0.1 cannot have produced these bytes. The sharper evidence is the parent: the PDF this step replaced, at `c0524f0c`, is 2702627 bytes and carries the `(opensource)` producer, so the step moved the repository off the producer family it had been using rather than onto it. Measured consequence: the committed file is 2702710 bytes and a 5.0.1 rebuild is 2702681, 29 fewer, and `.horos/boundary.json` records the exact count, so a rebuild fails `tests.test_boundary_currency.BoundaryCurrencyTests.test_the_committed_boundary_matches_a_fresh_scan` at 1 of 8 and `tests.test_agent_instruction.AgentInstructionScaffoldTests.test_horos_boundary_is_current_for_the_scaffold` at 1 of 310, both naming `.horos/boundary.json#counts` and the PDF. Regenerating the surfaces is the step's own documented operation and its own `wording-regen-budget` gate, so the next person to run it inherits a red tree from a build nobody can reproduce, the venv being gone. Fixed by rebuilding under 5.0.1 and refreshing the boundary in the same commit, moving `bytes_binary` 43942306 to 43942277 and the entry 2702710 to 2702681 with `files_walked` unchanged at 2502; both cases are green. `scripts/build_contributor_guide.py` now prints its `reportlab` version on every run, because nothing in the tree said which version produced the committed bytes and no requirements file pins one. | fixed in 5d669ee8 |
| S3-R1-02 | medium | scripts/render_harness_roster.py | `check` compared the harness page by containment, so a page showing a longer roster than the manifest read as clean. `pdf_roster_line` joins the manual-route names with `  /  `, and a substring test cannot tell a whole line from the head of a longer one. Driven against the committed page: delete `Cline`, the last manual-route entry, from the manifest and all three expectations are still contained, so the PDF half of the check passes on a page that goes on advertising a harness the roster no longer carries. Deleting the first entry, `GitHub Copilot`, does the same from the other end; a middle deletion is caught, which is why the hole survived the step's own cases. The two Markdown surfaces are compared by equality through `rendered_surface` and do not have it. This is the `roster-drift` row's stated property, that the check fails when any surface differs from what the manifest renders, and containment does not implement `differs`. A second shape sits beside it: `pdf_roster_line` returns an empty string when no harness holds `manual route`, and an empty string is contained in every page ever written, so the check would pass vacuously. Fixed by `pdf_drift`, which requires a matched roster line to be delimiter-bounded and reports an empty expectation as drift. Both ends and the empty case are now cases; the guard's first draft normalised the separator to a bare `/` and silently never fired, which is recorded because that failure mode is invisible. | fixed in 5d669ee8 |
| S3-R1-03 | medium | README.md | The generated README block claimed a probe "read every client below". No client was read. All six rows record `client_present` false with `version_read` false, five of them because the binary did not resolve on `PATH` so the command was not run, and Roo Code because no client binary is declared for it at all. The manifest is precise and the guide's parallel sentence is precise, saying a row cannot reach an earned class "without a client run somebody got an answer from"; the README, the surface most people read, overstated it, and it is generated, so every future render would repeat it. This is the `unearned-class` overstatement one level up from the classification field: not a class nobody earned, but a probe action nobody performed. Fixed by deriving the clause from `version_read` rather than asserting it, so the sentence reads "no client answered there" today and states a count on the day one answers. | fixed in 5d669ee8 |
| S3-R1-04 | low | scripts/render_harness_roster.py | `recorded.date` was matched against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and never parsed, so a calendar-invalid date reached all three published surfaces. Driven on this tree through the renderer: `2026-13-45`, `2026-02-31` and `0000-00-00` each produced a README sentence, a guide table footer and a PDF roster card label carrying the string, for instance `Manual only - probed darwin-arm64, 2026-13-45`. Step 2's round 4 handed this here by name as "step 3's `stale-manifest` row to close", having established that only an operator `--date` reaches it because `main` defaults to `datetime.date.today().isoformat()`. Fixed in `recorded`, which is the last gate before the string is published three times at once, rather than in `probe_harnesses.manifest_document`, which belongs to step 2's declared Files; the probe's own shape contract is left alone and a date the renderer will not publish is now a manifest defect rather than a rendering one. The rest of the `stale-manifest` property was checked and holds without change: host and date are embedded in both provenance comments, in both bodies of prose and in the PDF label, so a manifest recorded against a different host or day drifts all three surfaces at once. | fixed in 5d669ee8 |
| S3-R1-05 | low | docs/how-to-help-shoggoth.md | The hand-authored sentence above the generated block read "Every route below is manual: read `AGENTS.md`, then send the exact `job.prompt`." One row below it, the generated table classifies Roo Code `unsupported`, and its generated blocker says the product is sunset, its repository archived, and "there is nothing to test". The sentence sits outside the markers, so nothing regenerates it and nothing checks it against the manifest; it was true of the roster the step's author had in mind and false of the roster the step shipped. Fixed by saying that a row with a route has a manual one and that an `unsupported` row has none. The general gap stays open and is recorded as a lead: prose outside the markers can contradict the block below it with every gate green. | fixed in 5d669ee8 |
| S3-R1-06 | low | scripts/render_harness_roster.py | The provenance comment published inside both surfaces claimed the file "carries no harness name outside them", and the module docstring made the same claim. Counted on the shipped tree, the guide names Codex three times and Claude Code four times outside the markers, under its own `### Local harnesses` heading, and the README names Codex once. The claim is true only if "harness" is stipulated to mean "row in the probed roster", which is the reading the design intends and neither the comment nor the docstring stated. It matters because that sentence is the one a reader is given for trusting the block, and because the step's own case comments lean on the narrower property when they say the first occurrence of a name is the generated one. Fixed by claiming what is true, that neither surface names a harness from the probed roster outside the markers, and naming Codex and Claude Code as deliberate exceptions; a case now holds the narrower property rather than leaving it to a comment. | fixed in 5d669ee8 |

Leads not pursued: the `inconclusive` verdict has a shape worth stating rather than carrying as a bare string. Reconstructed here on a real `git worktree add --detach` at `16831d4b` with this commit's `tests/test_harness_manifest.py` overlaid, the five added cases split. `test_a_recorded_date_that_is_not_a_calendar_date_is_refused` fails as an assertion at all three subtests and `test_the_readme_states_how_many_clients_answered` fails as an assertion, so two genuine guards were seen red on the unfixed parent. `test_a_page_showing_a_longer_roster_than_the_manifest_is_drift` and `test_a_manifest_rendering_an_empty_roster_line_is_drift` raise `AttributeError: module 'render_harness_roster' has no attribute 'pdf_drift'`, because the function they guard is the one this commit introduces. Four failures and two errors is a mixed report, which the adapter classifies `inconclusive` by rule. The two erroring cases were deliberately left as they are rather than reshaped into assertions that would survive on the parent, because restructuring a test to move a classifier's verdict is exactly what the reference warns against; the verdict records that the report was mixed, not that nothing was guarded. `.elenchus/fiat-856-step-3.json` was free, so the runbook's pinned report path was used unchanged rather than given a round suffix, and `.elenchus/` is gitignored at `.gitignore:73`. The one thing that stays inferred is which test the vendored adapter itself saw, since its report is written inside a worktree it removes. Positive results, since a clean row is evidence too. The `pdf-nondeterminism` mitigation works exactly as the register claims and was driven from both sides: `--check` exits 0 against the committed page built by one `reportlab` and against a 5.0.1 rebuild of the same page, so the harness-page text comparison is tolerant across a `reportlab` major version, and a creation timestamp cannot fail it. What the row does not cover is the other half, and S3-R1-01 is that half: the check is text-based and tolerant while `.horos/boundary.json` records the file's exact byte count, so a rebuild that changes nothing the page says still reddens two cases through a different test. The mitigation is real and partial, and naming which test a PDF rebuild actually breaks is the durable form of it. `unearned-class` holds into the surfaces: no landed entry carries an earned class, `_version` reads the `version_read` boolean rather than string-matching the `unread` sentinel, and the guide's own sentence states the earned-class rule correctly. `credential-leak` holds one boundary further out than step 2 checked it: `refuse_leak` runs `credential_findings` over the PDF expectations and over each Markdown body before anything is written, a planted `bearer` token in a blocker refuses the whole write with all four staged files byte-identical afterwards, and a sweep over every added line of this step's diff for GitHub, OpenAI, Slack, AWS, bearer, private-key and api-key shapes returned zero, with no absolute home path in any added line. `probe-subprocess` was read against the one subprocess this step spawns rather than waived: `build_pdf` builds a fixed argv list with no shell, `stdin` closed and a 600s timeout, and `_checked_path` refuses an empty path, a path containing a NUL and a value starting with `-` for every operator option including `--python`. `partial-manifest-write` names the renderer explicitly in its second half, so it was driven here: a manifest truncated to half its bytes, an empty file and a foreign schema are each refused by `load_manifest` through the probe's own `read_manifest`, at `manifest is not readable JSON` and `manifest does not declare harness-classification/v1`. `absent-versus-unavailable` survives into the guide table, which carries `client_present`, the version and `auth_configured` as three separate columns, so absence, an unread version and an unconfigured client cannot collapse into one cell. Step 2's carried S2-R2-04 is resolved and was verified rather than assumed: `ADR-077` now enumerates `version_read` as a derived required field at its line 56 and names exactly `testable_here` and `probe` as the two optional ones, which matches the schema's own `required` list of eight. Both design gates were run with the runbook's pinned commands verbatim and both pass. `count_roster_copies.py` reports one hand-maintained roster copy against at most one allowed, at exit 0. `time_wording_regen.py` returns runs of 343, 345 and 342 milliseconds with a slowest of 345 against the 60000 ceiling, at exit 0, and it answers the interpreter question directly: `_interpreter` tries `sys.executable` first and the host `python3` now imports `reportlab`, so the gate runs with `WILDCAT_PDF_PYTHON` unset and the pinned command needs no variable. The gate's own restore property was checked rather than trusted, since it rebuilds a PDF under a `reportlab` that changes the bytes: `git status --porcelain` is empty and the PDF digest is unchanged after both gates, and `.hexaemeron/` is fully gitignored at `.hexaemeron/.gitignore:1`, so nothing under it was staged. Three judgements about files outside the step's declared `Files`. Committing `.horos/boundary.json` is acceptable and required rather than a finding: `test_the_committed_boundary_matches_a_fresh_scan` demands it by name, and the step's own move is fully accounted for, `files_walked` 2500 to 2502 being exactly the two added tracked files and `bytes_binary` plus 83 being exactly the PDF's growth from 2702627 to 2702710. This round's own refresh is two lines and nothing else. `.horos/candidates.json` picked up three blob candidates, two of them this run's audit log and its synopsis crossing the mean-line-length threshold, and the third is the 90295-byte `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md` that step 1's round 1 found stale at the base commit and deliberately reverted rather than carry. It rides along here because `horos.py scan --write` regenerates the whole file and no smaller refresh exists, so a pre-existing staleness is closed as a side effect of a required regeneration rather than by decision; it is recorded so the change is not read as this step's own drift. This round did not refresh `candidates.json` again after appending the record, which leaves the log's recorded size one round behind, as every earlier round in this file also does, and no check reads it for currency. Five leads of this round's own, none pursued. `write` calls `path.write_text` for each Markdown surface and lets the builder save the PDF, none of it atomic, so a render killed mid-write leaves a truncated README where the probe in the same design goes to real trouble to leave either the old manifest or nothing; the register's row is about the manifest and the exposure here is a local regeneration a person watches, so it is named rather than rewritten. `build_pdf` does not reduce the child's environment the way `_run` does in the probe, where `env` is cut to `PATH` alone so a client cannot echo an inherited credential; the child here is the repository's own builder and the only way to change it is `--python`, which an operator who could set it could run directly, so the asymmetry is recorded rather than closed. `launcher_contract` reaches no surface: the guide table has five columns and none of them is the contract, so the field the `network-absent` row protects lives in the manifest and in the blocker prose but is not published in its own right, which is a design choice worth stating rather than a defect. The general form of S3-R1-05 stays open, that prose outside the markers can contradict the generated block with every gate green, and closing it means either deciding what outside prose may claim or moving those claims inside the markers, which is a design decision rather than an audit fix. Finally, the new delimiter guard reads the roster line's neighbours in the page's own normalised text, which couples the check a little more tightly to the order `build_contributor_guide.py` draws the card in; the order was already declared by `pdf_expectations`, and adjacency is a stronger claim than order, so a builder that later draws a string between the label and the roster line would make the guard report drift that is not there. Exits, all read on this round's own tree unless stated. Before the fixes, at the step head `16831d4b`: `phylax` 0, `ephoros` 0 and `hypomnema` 0 over the nine changed paths with `docs/decisions` added to the `hypomnema` list, and `python3 -m unittest discover -s tests` 0 at 1192 tests. After the fixes, at `5d669ee8`: `phylax` 0, `ephoros` 0, `hypomnema` 0 and `python3 -m unittest discover -s tests` 0 at 1197 tests, the five added cases being the whole difference, with `tests.test_harness_manifest` moving 60 to 65 against a runbook expectation of around twelve for the step. Each new guard was mutation-proved against its own fix rather than assumed: restoring the stripped separator inside `_bounded` fails 2 cases, dropping the empty-expectation branch fails 1, dropping the calendar parse fails 3, and restoring the old README sentence fails 6, with the unmutated file green at 65. `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing on the fixed tree, `root-suite` at 110.9s, `hexaemeron-suite` at 342.3s and `dead-code-suite` at 42.5s, run from 18:12:13Z to 18:18:13Z against load averages of 2.94, 4.77 and 6.41 at the start and 8.25, 7.43 and 7.17 at the end. Neither documented false red was met, and the durations are not inflated the way step 2's contended runs were, so `hexaemeron-suite` sits 1457.7s under the 1800s check-map ceiling whose breach is the `command-failure` with `reason="timeout"` rather than the `WAI-E-ADAPTER.TIMEOUT` shape. The tree was clean before that run, which `dead-code-suite` requires; the renderer's write mode and both design gates were driven earlier in the round and each left the tree clean, verified by an empty `git status --porcelain` and an unchanged PDF digest. The fixes commit is `5d669ee8bde4a31ac1bedeaa9a28bfc534757016`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer and touching seven files.

## Step 3, round 2 -- 2026-09-04T18:51:37Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; stale-manifest=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; roster-drift=reviewed; atlas-claim=not-applicable; pdf-nondeterminism=reviewed

Not checked: no Solidity entered this step, so neither Pashov skill produced a finding. `c0524f0c..c67df955` carries no `.sol` path against 94 tracked in the tree, there is no `foundry.toml` or Hardhat configuration at the worktree root, and all four tracked `foundry.toml` files sit in other plugins' trees, at `plugins/ariadne/tests/fixtures/forge-project/v1`, the same under `v2`, `plugins/janus/harness` and `plugins/pandects`. So `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope, no twelve-role report was produced or implied, and `fizz` did not run because this step ships no Solidity under Foundry or Hardhat. This is a fresh Warden that did not carry round 1's context, so all three suite documents and the reuse reference were read here in full rather than inherited from a round number; the X-Ray preconditions were recomputed and matched, `skills/x-ray/SKILL.md` at `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41` and `lib/xray_reuse.py` at `23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10` against the `hexaemeron-x-ray-preaudit` overlay, with `skills/solidity-auditor/SKILL.md` at `1c1cf4e99d042e7aadc56b622d97a07d3286f4786838a05510697c814d1e983f` matching its own entry. No X-Ray reuse cache was consulted, so no reuse plan, scope manifest or cache verdict exists to report. No harness client was run and no network call was made, so the six per-harness observations in `docs/harness-classification.json` were read rather than reproduced, and the Copilot seat entitlement `auth_configured` cannot see stays unread. `atlas-claim` is not-applicable on the controller's assignment of that row to step 4 and not because the surface is absent: the sentence stands at `docs/how-to-help-shoggoth.md:117`, reading that the ChatGPT and Claude routes "are covered by the Atlas launcher tests", this step did not touch it, and the Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` was not fetched in this round either, so whether it holds a route test is unestablished here exactly as before. Three of round 1's four named unknowns stand. That the PDF this step replaced was built by reportlab 4.4.5 specifically remains Mason's report rather than something any round read; the parent file establishes only that its producer was not 5.0.1's. Whether any consumer outside this tree reads the roster from the surfaces rather than the manifest is still unestablished, so the blast radius of a wording change is bounded by this tree and no further. Whether the two Markdown surfaces read correctly to a person who stops at the generated block was not tested on a reader. The fourth unknown is closed rather than carried: round 1 could only infer which test the vendored adapter saw, because the report is written inside a worktree the adapter removes, and this round read both case names out of the adapter's own returned diagnostics, which the reference retains for a person and excludes from classification. Two new limits of this round's own. The detail guard added below was driven against the committed page and against synthesised pages, never against a PDF a different builder wrote. And no measurement here bounds how long a person would take to notice the drift S3-R2-01 admits, only that `--check` exits 0 on it.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | medium | scripts/render_harness_roster.py | The containment hole S3-R1-02 closed on the roster line was still open one field over, and round 1's own fix commit `5d669ee8` did not reach it. `pdf_detail` appends `UNSUPPORTED_CLAUSE` only when some harness is unsupported, so dropping the last unsupported name removes the clause rather than shortening it: the manifest then renders bare `PDF_DETAIL` and the page's longer string still contains it. Nothing else moves, because an unsupported entry is in neither `pdf_roster_line` nor `pdf_label`, so the delimiter bound S3-R1-02 installed never fires. Driven against the committed page: drop `Roo Code`, the only `unsupported` row, and all three expectations are still contained, `pdf_roster_line` and `pdf_label` are byte-identical, `pdf_drift` returns `[]`, and the whole `check` returns no drift line and would exit 0 on a page still reading `Unsupported: Roo Code.` Dropping one of several was already caught, because the list is comma-separated and full-stopped so `Unsupported: Cline.` is not contained in `Unsupported: Cline, Roo Code.`; only the fall to zero hides, and the landed manifest carries exactly one unsupported row, so the reachable case is the hidden one. This is the `roster-drift` row's stated property again, that check mode fails when any surface differs from what the manifest renders. Fixed by `_terminal`, which refuses a matched detail that is the head of a longer one, built on the same rebuilt-separator idiom as `_bounded` and carrying the same warning about the silent failure. | fixed in b6f4f69f |
| S3-R2-02 | low | scripts/render_harness_roster.py | `write` swept each surface for credential shapes as that surface's turn came round, so the refusal was only as early as whichever surface carried the token. A `blocker` reaches `guide_block` and no other body, and a blocker is where captured client output lands, so a token planted there refused at the guide after the README had already been rewritten. Measured on staged copies: with a token in `harnesses[0].blocker` and `recorded.base_ref` also moved so the README body genuinely differs, the refusal names the guide region and `README.md` is modified on disk before it fires. No token reaches disk in any placement tried, since the guide and the PDF are the surfaces that never get written and a token in `recorded.host` or `recorded.base_ref` refuses at the harness-page or README sweep with nothing modified; what was left behind was one surface regenerated from a manifest the renderer went on to refuse and two stale, which the next `--check` reports as drift. Round 1 recorded the opposite positive result, that a planted token in a blocker "refuses the whole write with all four staged files byte-identical afterwards", and the existing case cannot establish it: `test_a_credential_in_the_manifest_never_reaches_a_surface` plants the token in a blocker and changes nothing else, so the README it renders equals the staged one and goes unwritten whichever order the sweep runs in, leaving its "nothing was written at all" assertion vacuous for that surface. `refuse_leak`'s own docstring makes the same claim, that it fails the write closed. Fixed by sweeping both bodies before either is written; the claim is now true rather than untested. | fixed in b6f4f69f |

Leads not pursued: round 1's six fixes, all recorded fixed in `5d669ee8bde4a31ac1bedeaa9a28bfc534757016`, were each re-driven on this tree rather than assumed, and all six hold. S3-R1-01 holds more strongly than the record claimed: the committed PDF is 2702681 bytes at sha256 `a6b87be17a33625d058cbc2325c2bff6c20d943bdb56f4b2dadf7af5d365c475` with `/Producer (ReportLab PDF Library - \(opensource\))`, matching 5.0.1's hardcoded default, and two independent rebuilds under the installed reportlab 5.0.1 on Python 3.14.6 each produced that exact digest, so the file now reproduces byte for byte here rather than only carrying a plausible producer. `rl_config` is set `invariant=1` at `scripts/build_contributor_guide.py:573` and the file's `/CreationDate` is `D:20000101000000+00'00'`, which is why. `.horos/boundary.json` records `bytes_binary` 43942277, `files_walked` 2502 and the PDF entry at 2702681, matching the file, and `tests.test_boundary_currency` runs 8 of 8 and `tests.test_agent_instruction` 310 of 310 green. S3-R1-02 holds at both ends and the empty case: deleting `GitHub Copilot` and deleting `Cline` each leave all three expectations contained yet each now reports exactly one drift line, deleting `Gemini CLI` from the middle is caught by containment as before, and a manifest whose every row is `unsupported` reports the empty-string line. S3-R1-03 holds, the block reading "no client answered there" with "read every client" absent and all six rows still `version_read` false. S3-R1-04 holds and covers every surface: `2026-13-45`, `2026-02-31` and `0000-00-00` are each refused by `readme_block`, `guide_block`, `pdf_label`, `pdf_expectations`, `_provenance` and `pdf_drift`, while `2026-09-04` still renders, so no path reaches a published surface with a date the calendar refuses. S3-R1-05 holds, the guide's hand-authored sentence now reading that a row with a route has a manual one and that an `unsupported` row has none, which agrees with the generated Roo Code row. S3-R1-06 holds by count: zero of the six roster names appear outside the markers in either surface, with Codex once in the README and three times in the guide and Claude Code four times in the guide, exactly the exceptions the provenance comment now names. Positive results, since a clean row is evidence too. `pdf-nondeterminism` is stronger than the register's mitigation claims: the build is byte-deterministic under a fixed reportlab because of `invariant=1`, so the text-based page comparison is defence in depth rather than the only thing standing, and it remains the half that tolerates a reportlab major version while `.horos/boundary.json` does not. `partial-manifest-write` was driven rather than waived: a manifest truncated to half its bytes and an empty file are each refused at `manifest is not readable JSON` and a foreign schema at `manifest does not declare harness-classification/v1`, all through the probe's own `read_manifest`. `probe-subprocess` holds on the one subprocess this step spawns: `build_pdf` builds a fixed argv list with no `shell=`, `stdin` closed and a 600s timeout, and `_checked_path` refuses an empty path, a path containing a NUL and a value starting with `-` for every operator option including `--python`. `network-absent` holds, every one of the six entries carrying a non-empty `launcher_contract`. `unearned-class` holds, the landed classes being exactly `manual route` and `unsupported` with no earned class anywhere. `absent-versus-unavailable` holds into the guide table's three separate columns. Both design gates were run with the runbook's pinned commands verbatim: `count_roster_copies.py` reports one hand-maintained roster copy against at most one allowed at exit 0, and `time_wording_regen.py` returns 315, 315 and 322 milliseconds with a slowest of 322 against the 60000 ceiling at exit 0, under `/opt/homebrew/opt/python@3.14/bin/python3.14` with `WILDCAT_PDF_PYTHON` unset. Both left the tree clean, verified by an empty `git status --porcelain` and an unchanged PDF digest, as did the two rebuilds, which were written outside the repository. Eight leads, none pursued. Two are this round's own and both sit inside the guards it just added: `_bounded` and `_terminal` each accept the first occurrence that satisfies them, so a page drawing the exact roster line or the exact detail twice, once standing alone and once extended, reports no drift, which needs a builder that draws the card twice and none does; and `harness_page_text` returns the first content stream carrying `THE ATLAS AND FIAT ROUTE`, so a second page bearing the same heading would never be compared. Six carry from round 1 unchanged. `write` is still not atomic across surfaces, so a render killed between the README and the guide leaves them disagreeing, which S3-R2-02 narrows but does not close, having moved only the sweep and not the writes. `build_pdf` still does not reduce the child's environment the way the probe's `_run` does. `launcher_contract` still reaches no surface in its own right, the guide table having five columns and none of them the contract. The general form of S3-R1-05 stays open, that prose outside the markers can contradict the generated block with every gate green, and closing it is a design decision rather than an audit fix. The delimiter guard still reads the roster line's neighbours in the page's own normalised text, which couples it to the order the builder draws the card in. And `.horos/candidates.json` still records this log at its size one round back, as every earlier round in this file also does; the log is absent from `.horos/boundary.json` entries and appears in `candidates.json` only, no check reads it for currency, and `test_the_committed_boundary_matches_a_fresh_scan` is green regardless. Exits, all read on this round's own tree. Before the fixes, at `c67df955`: `phylax` 0, `ephoros` 0 and `hypomnema` 0 over the eleven changed paths with `docs/decisions` added to the `hypomnema` list, and `python3 -m unittest discover -s tests` 0 at 1197 tests, confirming rather than inheriting the controller's own reading of the same four. After the fixes, at `b6f4f69f`: `phylax` 0, `ephoros` 0, `hypomnema` 0 and the root suite 0 at 1199 tests, the two added cases being the whole difference, with `tests.test_harness_manifest` moving 65 to 67. `render_harness_roster.py --check` exits 0 on the committed tree both before and after, the three surfaces being byte-unchanged by this round, so no PDF rebuild and no surface regeneration was owed. Each new guard was mutation-proved against its own fix: dropping the `_terminal` call from `pdf_drift` fails 1 case, stripping the clause's surrounding spaces in `_terminal` fails 1, and restoring the interleaved sweep in `write` fails 1, with the unmutated file green at 67. The Elenchus verdict is `guarded` on the runbook's pinned runner contract, `python3 tests/run_tests.py --elenchus-report {report}` in `elenchus.unittest.v1` written to the fresh path `.elenchus/fiat-856-step-3-r2.json`, `.elenchus/` being gitignored at `.gitignore:73`. The adapter's report is complete with 1199 executed, 2 assertion failures, 0 errors and 0 skipped, and the two failures on the unfixed parent are this round's two new cases failing as assertions rather than erroring, which is deliberate: neither names `_terminal` or `UNSUPPORTED_CLAUSE`, the symbols the fix introduces, so neither can raise the `AttributeError` that made round 1's report mixed and its verdict `inconclusive`. The fixes commit is `b6f4f69fab2aba4d7020db4cd1f2291f7ad238f6`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer and touching two files. Load averages were 6.53, 6.31 and 6.64 at the first suite run and 5.52, 5.44 and 6.02 at the last; neither documented false red was met and `python3 scripts/run_checks.py --full` was not re-run in this round, the controller having read it green at 28 of 28 on the tree this round started from and this round changing two files that the root suite covers.

## Step 3, round 3 -- 2026-09-04T19:46:34Z

Audit schema: fiat-audit-round/v2

Covered: unearned-class=reviewed; credential-leak=reviewed; probe-subprocess=reviewed; absent-versus-unavailable=reviewed; stale-manifest=reviewed; partial-manifest-write=reviewed; network-absent=reviewed; roster-drift=reviewed; atlas-claim=not-applicable; pdf-nondeterminism=reviewed

Not checked: no Solidity entered this step, so neither Pashov skill produced a finding. `c0524f0c..30e53655` carries no `.sol` path against 94 tracked in the tree, across Mason's `7f2885ba`, `862f4f4e` and `16831d4b`, round 1's fixes `5d669ee8` and record `c67df955`, and round 2's fixes `b6f4f69f` and record `30e53655`. There is no `foundry.toml` or Hardhat configuration at the worktree root, and all four tracked `foundry.toml` files sit in other plugins' trees, at `plugins/ariadne/tests/fixtures/forge-project/v1`, the same under `v2`, `plugins/janus/harness` and `plugins/pandects`. So `x-ray`'s source-read, entry-point and invariant passes and `solidity-auditor`'s twelve roles each had an empty scope, no twelve-role report was produced or implied, and `fizz` did not run because this step ships no Solidity under Foundry or Hardhat. This is a third Warden, holding neither round 1's nor round 2's context, so all three suite documents and the reuse reference were read here in full rather than inherited from a round number; the X-Ray preconditions were recomputed and matched, `skills/x-ray/SKILL.md` at `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41` and `lib/xray_reuse.py` at `23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10` against the `hexaemeron-x-ray-preaudit` overlay, with `skills/solidity-auditor/SKILL.md` at `1c1cf4e99d042e7aadc56b622d97a07d3286f4786838a05510697c814d1e983f` matching its own entry. No X-Ray reuse cache was consulted, so no reuse plan, scope manifest or cache verdict exists to report. No harness client was run and no network call was made, so the six per-harness observations in `docs/harness-classification.json` were read rather than reproduced, and the Copilot seat entitlement `auth_configured` cannot see stays unread. `atlas-claim` is not-applicable on the controller's assignment of that row to step 4 and not because the surface is absent: the sentence stands at `docs/how-to-help-shoggoth.md:117`, reading that the ChatGPT and Claude routes "are covered by the Atlas launcher tests", this step did not touch it, and the Atlas repository at `ce866e3d7e8b489fcb8b70c608f7af72d9b7a673` was not fetched in this round either, so whether it holds a route test is unestablished here exactly as before. Round 1's three surviving unknowns stand unchanged. That the PDF this step replaced was built by reportlab 4.4.5 specifically remains Mason's report rather than something any round has read; the parent file establishes only that its producer was not 5.0.1's. Whether any consumer outside this tree reads the roster from the surfaces rather than the manifest is still unestablished, so the blast radius of a wording change is bounded by this tree and no further. Whether the two Markdown surfaces read correctly to a person who stops at the generated block was not tested on a reader. One unknown is this round's own and it is named rather than closed: S3-R3-01 refuses the earned-class manifest rather than rendering one, so what the three surfaces should say on the day a client answers is still unwritten, and nothing here establishes what a person would want them to say. Two limits on this round's own evidence. The earned-class hole was driven by hand-editing the landed manifest into states `probe_harnesses.classify` would have to produce, not by running a probe that produced them, because no client on this host answers. And the `--manifest` passthrough was driven against the repository's own builder only, never against a builder that reads the option differently.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R3-01 | medium | scripts/render_harness_roster.py | The third instance of the containment shape rounds 1 and 2 each closed one field at a time, and the widest: this one drops a whole harness rather than a clause. `names_in_class` is asked for `manual route` and for `unsupported` and for nothing else, while the schema's `classification` enum admits four names and `probe_harnesses.classify` returns `Atlas launcher` or `tested local route` as soon as one client answers. A harness holding either earned class therefore reaches `guide_block`, which walks every entry, and reaches `readme_block`'s bullets and the harness page not at all. Driven on a mirror of this tree with the landed `Cline` row moved to `Atlas launcher` and its observation fields made consistent: `--write` exited 0 and wrote all three surfaces, `Cline` was absent from the README's bullets and absent from the page's text entirely, and `--check` exited 0 printing `three surfaces match 6 recorded harnesses` against a README naming five and a page naming four. The same run at `tested local route` behaves identically. Worse than the omission, four hand-written claims go on standing over it: `readme_block` opens "No local harness holds a checked one-click Atlas launcher", `pdf_label` opens "Manual only", `PDF_DETAIL` closes "No checked Atlas launcher here", and the guide's own prose above the markers, which S3-R1-05 rewrote, says a row with a route has a manual one. None is derived from the manifest, an earned class falsifies all four at once, and the guide table one column over displays the class that falsifies them. This is `unearned-class` inverted: that row protects against a class outrunning its observation, and here an observation outruns the surfaces. The test module already asserts the property in `test_every_harness_the_manifest_names_reaches_every_surface`, which passes only because `landed()` carries neither earned class. Fixed by `refuse_unpublished_class`, called from `load_manifest`, which every path including the builder's harness page goes through: a manifest carrying a class these surfaces do not publish is refused by name and class rather than rendered short. Refusing rather than inventing wording is deliberate and is the trade `recorded` already makes for a date the calendar refuses, and it turns those four claims into preconditions the renderer checks. Its cost is stated in the docstring: on the day a client answers, the roster does not render until somebody writes the wording, which is a person's decision rather than a rendering one. | fixed in db67400d |
| S3-R3-02 | low | scripts/render_harness_roster.py | `--manifest` reached two surfaces of three. `build_pdf` built the argv `[python, BUILDER_PATH, "--output", target]` with no manifest, and `build_contributor_guide.draw_harness_page` called `render.load_manifest()` with no argument, so the page was always drawn from `docs/harness-classification.json` whatever document the renderer had been told to use. Measured on a mirror whose default manifest still carried `Cline` and whose `--manifest` did not: `--write --manifest other.json` exited 0 and printed `rendered 6 harnesses into three surfaces`, the README bullet dropped `Cline` and the page went on showing it, and the immediately following `--check --manifest other.json` exited 1 reporting drift the write had just made. So the write silently disobeyed its own option and the module docstring's claim that the PDF "cannot drift from the manifest without the manifest moving first" held only for the default document. The one guard that caught it was `_bounded`, from S3-R1-02, and only because the difference happened to fall at the end of the roster line. Fixed by passing the manifest on the argv and teaching the builder a `--manifest` it defaults to `None`, so an omitted option reads the same document as before: a default rebuild is byte-identical at 2702681 bytes and sha256 `a6b87be17a33625d058cbc2325c2bff6c20d943bdb56f4b2dadf7af5d365c475`, `.horos/boundary.json` is untouched, and the argv stays a fixed list of strings with no shell. After the fix the same sequence exits 0 on both commands and the page drops `Cline` with the README. | fixed in db67400d |

Leads not pursued: round 1's six fixes, recorded fixed in `5d669ee8`, and round 2's two, recorded fixed in `b6f4f69f`, all eight, were re-driven on this tree rather than assumed, and all eight hold. S3-R1-01 holds: the committed PDF is 2702681 bytes at sha256 `a6b87be17a33625d058cbc2325c2bff6c20d943bdb56f4b2dadf7af5d365c475` with `/Producer (ReportLab PDF Library - \(opensource\))`, `.horos/boundary.json` records `bytes_binary` 43942277, `files_walked` 2502 and the PDF entry at 2702681 against the file, which is what `test_the_committed_boundary_matches_a_fresh_scan` requires and why that file is committed although it sits outside this step's declared `Files`, as rounds 1 and 2 and steps 1 and 2 each judged before this one. A rebuild under the installed reportlab 5.0.1 on Python 3.14.6 reproduced that exact digest again here, which is a third independent reproduction after round 2's two and re-establishes `pdf-nondeterminism` from this round's own evidence rather than by carrying it; `rl_config` is `invariant=1` at `scripts/build_contributor_guide.py:573`, which is why. S3-R1-02 holds at both ends and the empty case: dropping `GitHub Copilot` and dropping `Cline` each leave the shorter `pdf_roster_line` contained in the page yet each reports exactly one drift line, a middle deletion of `Gemini CLI` is caught by containment as before, and a manifest whose every row is `unsupported` renders `pdf_roster_line` empty and reports the empty-string line. S3-R1-03 holds, the block reading "no client answered there" with "read every client" absent and all six rows still `version_read` false. S3-R1-04 holds across every surface: `2026-13-45`, `2026-02-31` and `0000-00-00` are each refused by `readme_block`, `guide_block`, `pdf_label`, `pdf_expectations`, `_provenance` and `pdf_drift`, while `2026-09-04` still renders. S3-R1-05 holds, the guide's hand-authored sentence reading that where a row has a route at all it is a manual one and that an `unsupported` row has none, which agrees with the generated Roo Code row; S3-R3-01 records that this same sentence is one of the four an earned class would falsify. S3-R1-06 holds by count: zero of the six roster names appear outside the markers in either surface, with `Codex` once in the README and three times in the guide and `Claude Code` four times in the guide and none in the README, exactly the exceptions the provenance comment names. S3-R2-01 holds: with the only `unsupported` row removed, `pdf_detail` drops `UNSUPPORTED_CLAUSE` rather than shortening it and falls to bare `PDF_DETAIL`, which is still contained in the committed page, and `_terminal` reports the drift. S3-R2-02 holds and was driven in the shape round 2 built it for, a token in `harnesses[0].blocker` with `recorded.base_ref` also moved so the README body genuinely differs: the write refuses at the guide region with all three staged surfaces byte-identical afterwards. Positive results, since a clean row is evidence too. `probe-subprocess` holds on the one subprocess this step spawns and on the argument this round added to it: `build_pdf` has no `shell=`, `stdin` is closed, the timeout is 600s, the appended `--manifest` is the path `_checked_path` already admitted, and `_checked_path` still refuses an empty path, a path containing a NUL and a value starting with `-` for every operator option. `partial-manifest-write` was driven rather than waived: a manifest truncated to half its bytes and an empty file are refused at `manifest is not readable JSON` and a foreign schema at `manifest does not declare harness-classification/v1`, all through the probe's own `read_manifest`. `credential-leak` holds, and a sweep over every added line of `c0524f0c..30e53655` outside the PDF for GitHub, OpenAI, Slack, AWS, bearer, private-key and api-key shapes returned zero, with no absolute home path in any added line outside `audit/`. `network-absent` holds, every one of the six entries carrying a non-empty `launcher_contract`. `absent-versus-unavailable` holds into the guide table's three separate columns, `client_present`, the version and `auth_configured`. `unearned-class` holds in the landed record, the classes being exactly `manual route` and `unsupported` with no `version_read` true anywhere, which is also why S3-R3-01 is latent rather than live today. Both design gates were run with the runbook's pinned commands verbatim: `count_roster_copies.py` reports one hand-maintained roster copy against at most one allowed at exit 0, and `time_wording_regen.py` returns 372, 349 and 363 milliseconds with a slowest of 372 against the 60000 ceiling at exit 0, under `/opt/homebrew/opt/python@3.14/bin/python3.14` with `WILDCAT_PDF_PYTHON` unset. Both left the tree clean, verified by an empty `git status --porcelain` and an unchanged PDF digest, and the second of them exercised the changed `build_pdf` through its own `write` path with the manifest omitted, which is the default-preserving half of S3-R3-02's fix. Seven leads, none pursued. One is this round's own and sits beside S3-R3-01: `readme_block` and `pdf_detail` both build a conditionally present clause, and `pdf_label`'s "Manual only" is unconditional, so the surfaces have no vocabulary for a roster that is not entirely manual; writing that vocabulary is a design decision and the refusal names it rather than guessing at it. Six carry from earlier rounds unchanged. `write` is still not atomic across surfaces, so a render killed between the README and the guide leaves them disagreeing, which S3-R2-02 narrows but does not close, having moved only the sweep and not the writes. `build_pdf` still does not reduce the child's environment the way the probe's `_run` does. `launcher_contract` still reaches no surface in its own right, the guide table having five columns and none of them the contract. The general form of S3-R1-05 stays open, that prose outside the markers can contradict the generated block with every gate green, and S3-R3-01 shows the same gap from the other side. `_bounded` and `_terminal` each accept the first occurrence that satisfies them, and `harness_page_text` returns the first content stream carrying `THE ATLAS AND FIAT ROUTE`, so a builder drawing the card or the heading twice would go unreported; none does. And `.horos/candidates.json` still records this log at its size one round back, as every earlier round in this file also does, with no check reading it for currency. One structural observation, recorded rather than raised as a finding: `render_harness_roster.py --check` appears in neither `tests/check-map-v1.json` nor `scripts/run_checks.py` nor `.github/`, so the step's Exit sentence about `--check` exiting non-zero on drift is proved by `RenderTests` calling `check` in process on staged copies rather than by any CI invocation of the command; drift between the committed manifest and the committed surfaces is genuinely covered by `test_check_passes_on_the_surfaces_the_renderer_wrote`, so this is a description of where the gate lives and not a hole. Exits, all read on this round's own tree. Before the fixes, at `30e53655`: `phylax` 0, `ephoros` 0 and `hypomnema` 0 over the eleven changed paths with `docs/decisions` added to the `hypomnema` list, and `python3 -m unittest discover -s tests` 0 at 1199 tests, confirming rather than inheriting the controller's own reading of the same four. After the fixes, at `db67400d`: `phylax` 0, `ephoros` 0, `hypomnema` 0 and the root suite 0 at 1202 tests, the three added cases being the whole difference, with `tests.test_harness_manifest` moving 67 to 70. `render_harness_roster.py --check` exits 0 on the committed tree both before and after, the three surfaces being byte-unchanged by this round, so no PDF rebuild and no surface regeneration was owed. Each new guard was mutation-proved against its own fix: dropping the `refuse_unpublished_class` call from `load_manifest` fails 2 cases, widening `PUBLISHED_CLASSIFICATIONS` to all four schema names fails 3, dropping the `--manifest` passthrough from `build_pdf` fails 1, and reverting `draw_harness_page(pdf, manifest_path)` to `draw_harness_page(pdf)` fails 1, with the unmutated file green at 70. `python3 scripts/run_checks.py --full` returned 0 with outcome green and 28 of 28 checks passing on the committed tree, `root-suite` at 187.7s, `hexaemeron-suite` at 491.2s and `dead-code-suite` at 72.0s, run from 19:33:33Z to 19:42:13Z against load averages of 5.62, 5.53 and 5.51 at the start and 8.48, 9.29 and 7.79 at the end; neither documented false red was met, and `hexaemeron-suite` sits 1308.8s under its 1800s ceiling. The tree was clean before that run, which `dead-code-suite` requires. The Elenchus verdict is `inconclusive` on the runbook's pinned runner contract, `python3 tests/run_tests.py --elenchus-report {report}` in `elenchus.unittest.v1` written to the fresh path `.elenchus/fiat-856-step-3-r3b.json`, `.elenchus/` being gitignored at `.gitignore:73`. The adapter's report is complete with 1202 executed, 2 assertion failures, 2 errors and 0 skipped, and its shape is read from the adapter's own returned diagnostics rather than reconstructed: `test_a_class_these_surfaces_do_not_publish_is_refused` fails as an assertion at both subtests with `AssertionError: RenderError not raised`, which is a genuine guard seen red on the unfixed parent, while `test_the_two_published_classes_still_render` errors with `AttributeError: module 'render_harness_roster' has no attribute 'PUBLISHED_CLASSIFICATIONS'` and `test_the_pdf_builder_is_given_the_manifest_the_renderer_was_told_to_use` errors with `TypeError: build_pdf() got an unexpected keyword argument 'manifest'`, each naming a symbol this commit introduces. Two errors make the report mixed, which the adapter classifies `inconclusive` by rule. The two erroring cases were deliberately left as they are rather than reshaped into assertions that would survive on the parent, which is round 1's disposition of the identical shape and what the reference warns against; the verdict records that the report was mixed, not that nothing was guarded. The fixes commit is `db67400d607533fdaaed7402b5269256047b9aaa`, signed `G`, authored Shoggoth and committed by Laurence, carrying one `Co-authored-by` and one `Wildcat-Origin` trailer and touching three files, `scripts/render_harness_roster.py`, `scripts/build_contributor_guide.py` and `tests/test_harness_manifest.py`. It supersedes `8bd8a4d8`, which carried the same two fixes and was amended before this record was written to replace an `assertEqual(x != y, True)` with `assertNotEqual` in one of the added cases; Elenchus was re-run against the amended commit on its own fresh report path and returned the same verdict from the same four cases, so no verdict was moved by the amendment.
