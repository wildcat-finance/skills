## Step 1, round 1 -- 2026-09-05T21:45:20Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: The implementations and receipts assigned to Steps 2 through 5; this round reviewed only their Step 1 interfaces. Live GitHub publication, non-macOS secure reads, same-UID mutation after the final identity check, and Solidity were not checked. Solidity is waived by the packet because issue 453 changes no Solidity source or behaviour.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | high | `docs/known-failure-inoculation-study.md:1`; `plugins/hexaemeron/docs/known-failure-inoculation/runbook.md:1447` | The shipped study has no closed `hypomnema-design-bridge/v1` block. The exact study-mode Hypomnema command exits 1 with `H008 study has no design bridge block`. A minimal bridge to `docs/decisions/drafts/require-inoculation-before-implementation.md` also exits 1 because study mode admits a numbered ADR or governed-skill ledger, not that draft. The effective Step 1 Exit runs only the ordinary Hypomnema walk and says Hypomnema checks all five shipped documents, so it does not establish the selected design's join to its record before the study ships. | open; repair requires controller-receipted study and runbook amendments plus a literal Hypomnema generation before another audit round |
| S1-R1-02 | high | `plugins/hexaemeron/docs/known-failure-inoculation/runbook.md:1449`; `plugins/brevitas/skills/brevitas/scripts/brevitas.py:286` | The effective Exit sends the required audit record through Brevitas report mode. This `fiat-audit-round/v2` record has the one required Step heading and one row per real finding, so the exact command exits 1 with B010, `draft has 1 section headings; minimum is 3`, and B011, `table has 2 data rows and 5 real-data columns; minimum is 3x3`. Adding headings or dummy rows would corrupt the audit grammar and finding count. Brevitas's evidence exception does not waive either structural rule. The Exit therefore requires an unattainable shape for this round and every zero-finding round. | open; repair requires a controller-receipted runbook amendment and an audit-record-aware Brevitas contract before another audit round |

Leads not pursued: No repair was applied in this round. Early ADR numbering conflicts with ADR-077's integration-time assignment and can collide before integration. A governed-skill ledger is the wrong home for this cross-cutting decision. The first repair lead is an exact `adr/<slug>` bridge resolved to one canonical draft or final record after the controller receipts append-only study and runbook amendments. The second is a literal Brevitas generation that recognises the fixed Fiat audit-record structure without weakening its evidence budgets. Before this record was written, the inventory suite passed 37 of 37 tests; the added mutation campaign returned all five expected outcomes; the exact Hexaemeron audit runner passed 2,376 of 2,376 tests with no failures, errors, skips, expected failures, or unexpected successes; the clean detached root suite passed 1,330 of 1,330 tests and its 1,258-case inoculation census had zero crashes and zero unexpected-clean cases; Phylax, Ephoros, and the ordinary Hypomnema walk exited 0; and the exact mapped command selected nine checks, passed all nine, and reported `outcome green`. Specialised study-mode Hypomnema and audit-record Brevitas remain at exit 1 as S1-R1-01 and S1-R1-02.

## Step 1, round 2 -- 2026-09-05T23:11:44Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: The implementations and receipts assigned to Steps 2 through 5; this round reviewed only their Step 1 interfaces. Live GitHub publication, non-macOS secure reads, same-UID mutation after the final identity check, and Solidity were not checked. Solidity is waived exactly as `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: The two round-1 findings are fixed by signed product commit `1019fd36326b7e1c51765f3f0d5a0ef57805304a`, whose sole parent is `3253af2a873028d87111237bd3638905626956d4`, and signed no-fast-forward merge `9b5c4418e1a9ec51ba54fb22b46e7d64f51b04b2`, whose parents are the round-1 record `0d58501d6fb3b3e073f3bd9c900b3424928f8ff5` and the product fix in that order. The source-bound H008 guard returned `guarded` with 2,389 executed tests, 13 assertion failures, zero errors, and zero skips; the Brevitas guard returned `guarded` with 91 executed tests, four assertion failures, zero errors, and zero skips. Both parent runners exited 1 on the recorded assertion failures. On the fixed tree, the Hexaemeron report at `.elenchus/fiat-453-step-1-audit.json` passed 2,389 of 2,389 tests and has SHA-256 `c578a814100b50ca1be72d648eec5d21282b8cf7a57907bbae174c79183cff13`; the Brevitas report passed 91 of 91 tests and has SHA-256 `4537e936d4864a2a6b1c142645a7c01dcfc3a9de578afe32983f39501720d160`. Focused inventory, design-bridge, allocator, and contract suites passed 37, 39, 41, and 85 tests. The clean detached root suite passed 1,332 tests, its 1,258-case inoculation census had zero crashes and zero unexpected-clean cases, and the mapped command selected and passed all ten checks with `outcome green`. The committed study and runbook equal their receipted bytes at SHA-256 `b761db434c406dcce1f24bee00a374403c16aa57ea4b462bd4372f1828a966cb` and `0827c534ddffc70c8b6df96f0b80be1042a4410793ebe4296bb30a78c29145fd`. The inventory remains exactly `kf-453-01` through `kf-453-07`; the stable bridge accepts one canonical bounded draft or three-digit final and refuses missing, duplicate, malformed, aliased, special, oversized, or unstable candidates; explicit `fiat-audit-record` mode suppresses only B010 and B011 after the synopsis check, while auto, answer, and report modes remain unchanged. Full diff review and the named refusal tests found no route through any of the twelve covered risks. A scan on the audit branch sees the two inherited Warden files and would move only `.horos/boundary.json#counts` from 2,576 to 2,578 files; the amended contract assigns Horos and root-tree currency to exact product F, where both are clean, and assigns the synopsis, audit Imprimatur input, and `fiat-audit-record` lint to this audit branch. The generated boundary change was not retained in A2. Next action: receipt this zero-finding round with the product fix and exact `guarded` verdict; `done audit` remains a later controller gate and has not run.

## Step 2, round 1 -- 2026-09-06T03:06:17Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under its non-Solidity waiver; external repositories, network services, credentials, native Windows, CI, controller receipt, push, pull request, merge, publication and issue closure; Step 3 report retention, manifest admission and guard-manifest proof; model, measurement, parity, tokenizer, Ollama and recorded-adapter processes, which the amended Step 2 contract explicitly disables; and an Elenchus fix replay, because Warden made no product repair and this round's verdict is null

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | `tests/test_promise_machine_contract.py` | The candidate changes this repository-level contract test to require the new `fiat-known-failure-inoculation` promise and raise the closed runtime-binding count from 47 to 48, but the effective Step 2 Complete replacement Files clause omits the path. The signed product range therefore exceeds its source-bound runbook authority even though the change is mechanically necessary. | open; amend the Step 2 Files clause to include this path and refresh the tracked runbook mirror |

Leads not pursued: none

## Step 2, round 2 -- 2026-09-06T03:57:09Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under its non-Solidity waiver; external repositories, network services, credentials, native Windows, CI, controller receipt, push, pull request, merge, publication and issue closure; Step 3 report retention, manifest admission and guard-manifest proof; and model, measurement, parity, tokenizer, Ollama and recorded-adapter processes, which the amended Step 2 contract explicitly disables. No prohibited command or model process ran in this round.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `S2-R1-01` is resolved by signed product fix `aaf65c0e1a7a390c4ade31e3a5278c8431320ca6`, whose sole parent is original candidate `96f558698140cdf4bd6d3d866de157940b649f1b`, and signed no-fast-forward merge `79ddc0d95ebd1f355df0db89298af8352d843c3c`, whose parents are round-1 audit head `fb863b9a9e90632d1647ea0fc954df7205b6bd43` and that product fix in order. The fix changes only the tracked runbook, adding the omitted `tests/test_promise_machine_contract.py` authority without changing a test file. The exact Elenchus fix check therefore returned `unguarded -- the commit changed no test files`; this closed verdict is recorded without converting it to `guarded`. Full review of the 22-path `13a2c9f22065c89e85d031bd5bb084b36a487670..aaf65c0e1a7a390c4ade31e3a5278c8431320ca6` product range found every path inside the amended Step 2 inventory, all frozen paths unchanged, and no route through any of the twelve covered risks. On the clean detached product tree, the `kf-453-02` report passed 1 of 1 tests and has SHA-256 `fc26652c99ed7e19ca1ccbcc13a328867602f55e0b0b36f6d67094d9e895de99`; inventory reported zero findings; focused inventory, lifecycle, Hexctl, Fiat, promise-contract, and instruction/corpus/naming suites passed 40, 32, 401, 117, 2, and 350 tests. Offline reconciliation returned `nothing-to-reconcile`; the structural check accepted three fixtures, 15 aggregate bindings including seven Fiat bindings, nine questions, 14 mutations, and 20 passes; both frozen-tree comparisons were clean. The full Hexaemeron report passed 2,426 of 2,426 tests with zero failures, errors, skips, expected failures, or unexpected successes and has SHA-256 `3365602551f46ffeffa6891dcfd705fbbdb0fb9b6277e243122fe5d8de1acd3b`. The root suite passed 1,332 tests; its 1,258-case inoculation census had zero crashes and zero unexpected-clean cases; and the mapped runner selected and passed all ten checks with `outcome green`. Phylax, Ephoros, ordinary and study-mode Hypomnema, Imprimatur, the four product Brevitas reports, the audit-record Brevitas mode, synopsis check, Horos product-tree currency, and diff checks all exited clean. Managed study and runbook bytes equal their tracked copies at SHA-256 `e9c33a44737a83b24badfcc21817abef6a97016020d77e31586453d43b82561e` and `4228006564fd0e36f66b091a97dc578344dd183d5b651ec636f7538f424fcbab`. The audit branch retains the inherited Warden files only; no audit-branch Horos count drift is retained. Next action is the separate controller receipt gate; it has not run.

## Step 3, round 1 -- 2026-09-06T13:06:29Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; external repositories, network services, credentials, native Windows, CI, controller receipt, push, pull request, merge, publication and issue closure; the Step 4 checkpoint, restore, `verify`, cross-worktree, post-compaction, final-green and successor-Warden gates; malicious loader execution beyond the non-executing environment-retention proof; and the disclosed same-UID Darwin executable-swap interval. Model, measurement, parity, tokenizer, recorded-family adapter and Ollama paths were explicitly disabled and remained untouched. No Elenchus fix replay ran because Warden made no product repair and this round's verdict is null.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | high | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:3323`; `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:3343`; `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:3356`; `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:7500` | `_native_relation_environment` copies every caller variable except names beginning `GIT_`, so `LD_PRELOAD`, `LD_LIBRARY_PATH`, `DYLD_INSERT_LIBRARIES`, and `DYLD_LIBRARY_PATH` reach the absolute Git used for relation, ancestry, guard-object, exact-zero and signature reads. A non-executing sentinel probe confirmed that all four survive. On a host that honours one of those loader channels, injected code can falsify Git output despite the absolute executable and `--no-replace-objects`. This breaks the Step 3 caller-environment substitution refusal and the `parent-substitution` trust boundary. | open; use a closed environment containing only required locale, home and verifier inputs, or reject and scrub every loader/runtime injection channel, then add hostile-loader regressions through relation, guard and signature reads |
| S3-R1-02 | medium | `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:133`; `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:151`; `plugins/hexaemeron/tests/test_hexctl.py:2207`; `plugins/hexaemeron/tests/test_hexctl.py:2225` | Both new native-graph helpers inherit the caller environment: some calls pass `env=None`, and the remaining calls extend `os.environ.copy()`. The `hash-object -w` call is one explicit no-environment invocation. Ambient `GIT_DIR`, `GIT_OBJECT_DIRECTORY`, or `GIT_ALTERNATE_OBJECT_DIRECTORIES` can therefore redirect the object and ref work away from the disposable repository, making the required local native-graph qualification host-dependent and able to write outside its test root. | open; give every fixture Git call one closed environment, add only the private index and deterministic identity fields where needed, and add a hostile-Git-environment regression that proves all objects, refs and pushes remain in the disposable repository and bare origin |

Leads not pursued: The missing durable-evidence replay in `verify_run` is a real `resume-loss` gap, but the effective Step 3 contract expressly assigns `verify`, checkpoint, cross-worktree and post-compaction replay to Step 4, so it is not a Step 3 finding. The Darwin pathname-exec race is real but is excluded verbatim by `elenchus-parent-guard-evidence` at `plugins/hexaemeron/skills/elenchus/SKILL.md:439`. Changing a disposable manifest to `model_evidence_status: active` can reach the retained future generator path, but the shipped manifest is disabled, the structural Promise refuses a non-disabled result, and Step 3 requires no machine receipt for a future separately authorised source change. One earlier Hexaemeron run overlapping the five-worker repository runner reported 1 failure in `test_legacy_runner_rejects_an_executable_changed_during_the_run`, expecting `executable changed` but receiving `the test command could not be started`; two solo, 20 sequential, the complete 12-test class, 80 focused concurrent and a later full run did not reproduce it, and no mechanism was established. Warden's exact runner passed 2,506 of 2,506 tests with exact accounting and produced `.elenchus/fiat-453-step-3-audit.json` at SHA-256 `c26c2b12fa82270831fc13a54185553d63da0ef53dc13fdb3fb3ef673e501493`. The clean detached root suite passed 1,339 tests and its 1,258-case inoculation census had zero crashes and zero unexpected-clean cases. The structural instruction check accepted three round trips, 17 bindings including nine Fiat bindings, nine questions, 14 mutations and disabled model evidence; both frozen reports retain SHA-256 `1f1f168fc66489d497464772946caa5affc71656c41b2325843c9a4297fa158f` and `5c628fe267bb9862cc165848be1380b545b68c416cf9d914c608da71670687c7`. Full review covered every path in the 58-path product range `aaf65c0e1a7a390c4ade31e3a5278c8431320ca6..8f3a491bfc2f90db0eb4d59774b480a126c04c32`; the 18 generated Promise copies equal the root contract, the tracked study and runbook equal their receipted sources, and the frozen evidence paths are unchanged. Phylax, Ephoros, the effective Step 3 Hypomnema command, four product Brevitas report checks, Promise copy checks and diff checks exited clean. No repair, controller transition, commit or publication was made. Next action: repair both findings under a controller-receipted round-2 packet, then rerun this audit.

## Step 3, round 2 -- 2026-09-06T14:07:27Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; external repositories, network services, credentials, native Windows, hosted CI, a live SSH remote, controller receipt, push, pull request, publication and issue closure; the Step 4 checkpoint, restore, `verify`, cross-worktree, post-compaction, final-green and successor-Warden gates; and the remainder of the full root, mapped, Promise, frozen-byte, source, Horos and tree-lint Exit after the first confirmed finding, because Warden stops a round on a finding. Model, measurement, parity, tokenizer, recorded-family adapter and Ollama paths remained untouched.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | medium | `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:78`; `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:79`; `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:292`; `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:293`; `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py:294`; `plugins/hexaemeron/tests/hexctl_harness.py:1071` | `FrontierReceiptCase` still performs five native graph mutations through inherited `HexctlCase.git`, which invokes caller-resolved `git` without an `env` argument. With hostile `GIT_DIR` and `GIT_WORK_TREE`, the exact add and commit calls exited zero, left the intended HEAD unchanged, and created foreign commit `5c03c97175737f42103642bddd58a708639bf6ad`. With hostile `GIT_OBJECT_DIRECTORY`, new HEAD `04e502709110ced40fa0e74c8d59452454bffba5` was absent from the intended object store and present in the foreign store. The new hostile regression wraps only `native_git` and `native_commit`, so it never traverses these five calls. This violates the amended requirement that every native fixture Git call start from an empty environment and that all graph writes remain in the disposable repository or local bare origin. | open; route all five calls through the closed native helper, add only the required fixed identity and date fields, and make the hostile regression traverse the baseline, step and branch mutations |
| S3-R2-02 | low | `plugins/hexaemeron/tests/test_step_branch_extensions.py:64`; `plugins/hexaemeron/tests/test_step_branch_extensions.py:68`; `plugins/hexaemeron/tests/test_step_branch_extensions.py:78` | `NativeGraphCase` gives its two `git commit` calls no fixed author or committer dates and gives `commit-tree` identity fields without dates. Two otherwise identical graph builds two seconds apart produced different three-commit object sets, so the fixture does not meet the amended deterministic-date construction rule even though its relation assertions currently pass. | open; pass fixed author and committer dates only to the three commit commands and assert their exact environment-key sets |

Leads not pursued: Signed product fix `6021e664d76a46160eebadddefa0fbfc77d7346b` has sole parent `8f3a491bfc2f90db0eb4d59774b480a126c04c32`; signed no-fast-forward audit merge `5d07af1fdef0d5d6e9bc338e029a381f53e50c5f` has parents `cc70d00bc7c1bd9bebe739588e600102eb87b200` and that fix in order. Production environment isolation closes `S3-R1-01`: relation, guard and signature calls use the required closed key sets, all 12 Promise coverage bindings name controller SHA-256 `dbba194533a2a7037d44bd0c8ce338d482fec4286a5cbc5119cf41c6b88586ab`, and the isolated source-bound production regression returned `guarded` with one executed test, one assertion failure, zero errors and zero skips; its fixed-tree counterpart passed one of one. An attempted source-bound invocation of the complete parallel runner returned `inconclusive` because Elenchus's required no-descendant sandbox refused its worker processes; that infrastructure result was not presented as product evidence. Warden's direct exact runner passed 2,510 of 2,510 tests with exact accounting and produced `.elenchus/fiat-453-step-3-audit.json` at SHA-256 `c45d364451f6e476e3251ee731c33d98a7a9dc83e0a018fa899cc35602e2b1f8`. The three added hostile regressions passed three of three, the affected fixture classes passed 53 of 53, and a production-focused set passed 133 tests, but those greens do not exercise either residual. One earlier nonexistent-class selector was an immediately corrected operator typo, not a product finding. Full review covered all seven changed paths in `8f3a491bfc2f90db0eb4d59774b480a126c04c32..6021e664d76a46160eebadddefa0fbfc77d7346b`; production native-environment isolation, the two append-only prose amendments, and the 12 digest bindings showed no additional defect. `git diff --check` and focused Phylax exited clean. No product repair, push, controller transition or publication was made. Next action: repair both fixture findings under a fresh controller-receipted round-3 packet, then rerun this audit.

## Step 3, round 3 -- 2026-09-06T15:20:44Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; external repositories, network services, credentials, native Windows, hosted CI, a live SSH remote, controller receipt, push, pull request, publication and issue closure; and the Step 4 checkpoint, restore, `verify`, cross-worktree, post-compaction, final-green and successor-Warden gates. Model, measurement, parity, tokenizer, recorded-family adapter and Ollama paths remained untouched.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `S3-R2-01` and `S3-R2-02` are resolved by signed product fix `050b3d2c969fd94fb2900e52e4b4ef70bbf52e0f`, whose sole parent is `6021e664d76a46160eebadddefa0fbfc77d7346b`. Signed guard commit `a9facb8891d7705ce760f4a9796e7115c7644683` has the same sole parent and changes only the reporter plus the two source-contract test files. Each source-bound Elenchus command returned `guarded` with one executed test, one assertion failure, zero errors and zero skips. Signed no-fast-forward audit merge `ba95cef709e8e021e152faa33a0f5c7f2967b63f` has parents `ca77e424a9a9e368b163fb8b652271fff29b5db1` and the product fix in that order. All three commits verify with key `B83B60AE16F5DD1A`. Full review covered all six paths in `6021e664d76a46160eebadddefa0fbfc77d7346b..050b3d2c969fd94fb2900e52e4b4ef70bbf52e0f`: the study and runbook append-only amendments, `emit_issue_453_guard_report.py`, `test_hexctl.py`, `test_hexctl_frontier_receipt.py`, and `test_step_branch_extensions.py`. The prior study and runbook prefixes remain exact at 83,484 bytes and SHA-256 `fd2bff8fd93f17f214a59316c1635957d4fc6f0524c41d99cf15a9d179155e93`, and 239,232 bytes and SHA-256 `8a4e01a946b5e168423ce64e6096429995ab784bf91776efe8512773aa8ac61d`. The final files have SHA-256 `c220ea89949ace129c135ac81c9d5876a0fa5a8bc65b41c5d94ba347c07aa92d` and `73c5f3cab5e144075e317a5c3baf158643f6211d58f1c8872844bd130311f2ef`. On the fixed tree, the two strengthened contracts passed two of two, their individual reports passed one of one each at SHA-256 `fc26652c99ed7e19ca1ccbcc13a328867602f55e0b0b36f6d67094d9e895de99`, the hostile publication fixture passed 35 of 35, the hostile Frontier fixture passed seven of seven, Frontier discovery and module execution were exactly eight of eight, and the branch-extension module passed 13 of 13. Warden's exact runner passed 2,512 of 2,512 with exact accounting and no failures, errors, skips, expected failures or unexpected successes. Its 162-byte report `.elenchus/fiat-453-step-3-audit.json` has SHA-256 `0c895095ef11d65695f71198647c22b277220e1c219675b708482b766939a42e`; runner source SHA-256 is `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, and manifest SHA-256 is `81905e8429c4697f97639e143d6f56705deaea2e4451aec1b3b86f725cdb6536`. The exact detached product root suite passed 1,339 of 1,339; its 1,258-case inoculation census had zero crashes and zero unexpected-clean cases. The mapped run selected and passed all 29 checks with `outcome green`. An extra audit-branch root run passed 1,337 of 1,339 and failed only its two Horos count assertions because that branch adds exactly this Warden record and synopsis to the 2,598-path product tree; the exact product-tree Horos checks passed. Inventory, both Protasis checks, deterministic reconciliation, the structural instruction check, all Promise checks, Phylax, Ephoros, Hypomnema and diff checks exited clean. The structural check accepted nine Fiat bindings, 17 aggregate bindings, nine questions and 14 mutations with model evidence disabled. Frozen measurement and parity bytes remain SHA-256 `1f1f168fc66489d497464772946caa5affc71656c41b2325843c9a4297fa158f` and `5c628fe267bb9862cc165848be1380b545b68c416cf9d914c608da71670687c7`. Two independent read-only reviews found no additional defect. No controller transition, product edit, push or publication occurred. Next action is the separate controller receipt gate; it has not run.

## Step 4, round 1 -- 2026-09-11T02:07:40Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; `hexctl` itself, which the operator holds, so no controller transition, receipt, push, pull request, merge, publication or issue closure was exercised in this round; the new strict capture path, read as source but never executed, because this run's runbook receipt carries no `known_failure_inventory`, so `receipted_known_failure_inventory` returns None at `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:7101` and every reader takes the legacy branch; Step 5; external repositories, network services, credentials, non-macOS hosts, native Windows and hosted CI; and the inherited same-UID mutation window after the final identity check.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | medium | `.hexaemeron/runbook.md` Step 4 Exit, generation 13; `plugins/brevitas/skills/brevitas/scripts/brevitas.py:432`; `plugins/brevitas/skills/brevitas/scripts/brevitas.py:435` | Generation 13's replacement Exit folds the Warden-owned audit record into the five-draft Brevitas `--mode report` loop. That exact command exits 1 with seven B011 findings, at lines 11, 28, 44, 60, 76, 93 and 110, each a table with 1 or 2 data rows against 5 real-data columns where the minimum is 3x3. The same bytes under `--mode fiat-audit-record` exit 0, because lines 432 and 435 skip `_section_issues` and `_table_issues` for that mode alone. Steps 2 and 3 ran a four-draft report loop plus a separate `audit_synopsis.py --check .` and a separate `--mode fiat-audit-record` command; the runbook still holds that pair at lines 1730, 2052, 2395 and 2998. This is S1-R1-02 recurring after its step-1 repair shipped. Warden cannot repair it: all seven failing tables are append-only history from steps 1 to 3, the five-column shape is the `fiat-audit-round/v2` grammar, and padding rows would invent findings and corrupt the count. | open; repair requires a controller-receipted runbook amendment restoring `--mode fiat-audit-record` for this file, as steps 2 and 3 ran it |
| S4-R1-02 | low | `.hexaemeron/runbook.md` Step 4 Exit, baseline and generation 13; `plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py` | Neither the baseline nor the amended Step 4 Exit names `audit_synopsis.py --check`, which steps 2 and 3 ran immediately before the record's Brevitas mode; the step-1 round-2 record states that the mode suppresses B010 and B011 "after the synopsis check". Run directly here it exits 0 and reports `committed=match` for this run's pair, source_lines=114, synopsis_lines=8, source_sha256 `f4eb07f650427f0b3b35066aeffc01faa8f00cb8a1d9510e70f609f65b175263`. Severity is low because the controller's own `audit-round` receipt independently renders the log through the same bounded synopsis code and refuses a missing, stale, lossy, oversized or over-budget view, so the omission drops a redundant check rather than the only one. | open; repair requires a controller-receipted runbook amendment restoring the command |

Leads not pursued: Both guard verdicts were re-derived rather than read. `kf-453-06` and `kf-453-07` each returned `guarded` against signed guard-only commit `c76cc4be487a525a635c7d62ff06034de7acd425` with detail "the runner report records a parent assertion failure", complete, one executed test, one assertion failure, zero errors and zero skips. The captured parent tracebacks name the genuine cases `test_kf_453_06_resume_preserves_remaining_and_no_findings` and `test_kf_453_07_red_guard_cannot_finish_without_fixed_tree_green`, not the surface-absence fallback. The packet's stated report locations `.elenchus/issue-453-kf-453-06.json` and `.elenchus/issue-453-kf-453-07.json` hold no file, because Elenchus replaces `{report}` with a path inside its detached parent worktree, so those reports never persist in the live tree; the packet's verdicts and counters are nonetheless exact. `c76cc4be` is signed with key `laurence.e.day (github-signer)`, authored by Shoggoth, has sole parent `73c2b4996ba2ecccbc5f5cedd4892d9723df8ca0` which is Step 3's exit, and its four changed paths are exactly the `kf-453-06` and `kf-453-07` guard-path union with no product path. Both green reports are 159 bytes at SHA-256 `fc26652c99ed7e19ca1ccbcc13a328867602f55e0b0b36f6d67094d9e895de99` with `complete` true, `testsRun` 1 and zero failures, errors, skips, expected failures and unexpected successes; neither `green_command` could be re-run in place, because the shared emitter refuses an occupied REPORT target and exits 2. Study, runbook and design-evidence digests equal the packet at `c220ea89949ace129c135ac81c9d5876a0fa5a8bc65b41c5d94ba347c07aa92d`, `0ed7432ce2179c4e1bc66a79adb7ccbd19fe2487f22ecdf023d2c4275cac3b27` and `0af3099f87a07e6a51c08e8fe0bffd9fa73780711561817aa35fc18727239473`, and the tracked mirror `plugins/hexaemeron/docs/known-failure-inoculation/runbook.md` is byte-identical to `.hexaemeron/runbook.md`. The pinned Exit generation moves 12 to 13 with a 13-entry distribution, and the runbook holds 13 `Complete replacement Exit:` clauses and 77 `Known-failure assignment:` lines, which is 11 generations of seven. Inventory guard paths are shape-validated only and never resolved on disk at `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:6760`, and three earlier modules are already absent at their historical inventory paths from Steps 1 to 3, so keeping `test_issue_453_recovery.py` there is the established pattern. Exits: Phylax 0, Ephoros 0 and Hypomnema 0, the last run before the ignored portable payload was regenerated as the amended ordering requires; recovery with Fiat-skill 135 of 135; checkpoint and checkpoint-identity suites 0; portable sync 0; portable check 0; promise check 0 over 18 plugins and 18 copies; the Hexaemeron suite 2,533 of 2,533 with exact accounting and zero failures, errors, skips, expected failures and unexpected successes; the clean detached root suite at `4106a886` 1,339 of 1,339, its 1,258-case inoculation census at zero crashes and zero unexpected-clean cases; `run_checks --base 5bc2494c` 0 with 29 checks passed and `outcome green`; Imprimatur 0 at `--max-defects 0`; the four product Brevitas drafts 0; and `git diff --check` 0. The dead-code lane reported `currency current; no tracked path changed after publication`, so `.horos` needs no refresh, and nothing was written into the detached worktree, which stayed clean. Product review of the 22-path range `73c2b499..4106a886` found no route through any of the twelve covered risks. `_final_green_run` builds a closed 13-key child environment, so no `LD_PRELOAD`, `LD_LIBRARY_PATH`, `DYLD_INSERT_LIBRARIES` or `DYLD_LIBRARY_PATH` reaches it, which is the production form `S3-R1-01` demanded; `_final_green_executable` substitutes `sys.executable` for the declared bare interpreter with no `PATH` lookup, argv stays a list, no shell runs, and the runner is bounded at 5,400 seconds. `_retain_final_green` refuses an occupied report path, records a start timestamp and refuses a report whose mtime precedes it, publishes the report first and the manifest last with no replacement, and the manifest binds `finding_id`, `green_command`, `green_argv`, `report_file` and `final_commit`, so the shared green-report digest is not a substitution vector. `final_green_admission_counters` admits only a closed-key `elenchus.unittest.v1` document that is complete, positive and free of every rejection counter beside a zero runner exit, and names itself Fiat's own admission rather than an Elenchus verdict. Suite discovery is fail-open at implement and closed past it, where both `root-suite` and `hexaemeron-suite` rows are required at `hexctl.py:10976`; both are declared in `tests/check-map-v1.json`. Past inoculate an explicit no-known-findings claim is required at `hexctl.py:10987`, so an empty successful run cannot stand for emptiness, and `require_final_green_admission` gates audit, prose, push, completion and checkpoint through one contract while returning None on the legacy path. Three leads were left. First, `missing_surface_suite` in `plugins/hexaemeron/tests/emit_issue_453_guard_report.py` substitutes a synthetic surface-absence assertion whenever any REQUIRED_SURFACE path is missing, and that yields the same counters a genuine guard yields, which Elenchus classifies `guarded`; it was not exercised here, and repairing the emitter is unrelated to this round's two findings. Second, four changed paths sit outside the amended Files field: the tracked runbook mirror, `tests/test_evolution_contract.py`, `tests/fixtures/agent-instruction-v1/manifest.json`, and three `.agents/skills/fiat-study-runbook-phase/` corpus files; Steps 1, 2 and 3 shipped the same consequential paths under receipted Files fields that also omitted them, at `dfd380b8`, `96f55869` and `8f3a491b`, and `S2-R1-01` already established the amend-the-clause remedy, so this is receipted precedent rather than Step 4 drift. Third, Fiat's `SKILL.md`, `EVOLUTION.md` and root contract pin all agree at `fiat-v5.53.1`, the projection `version_relations` names, and exact label resolution stays with the post-stack gate. An unrelated concurrent session ran its own `run_checks --base 0bc39f27` and Hexaemeron workers throughout this round, lengthening wall time without changing a result. No product repair, commit, controller transition, push or publication was made. Next action: put both findings to the controller for one runbook amendment, then rerun this audit.

## Step 4, round 2 -- 2026-09-11T02:49:01Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; `hexctl`, which the operator holds, so no controller transition, receipt, push, pull request, merge, publication or issue closure was exercised here; the strict capture path, which round 2 does not change, because the runbook receipt still carries no `known_failure_inventory`, so `receipted_known_failure_inventory` returns None at `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:7101` and every reader still takes the legacy branch, leaving those gates read as source and unexercised; the `missing_surface_suite` fallback in `S4-R2-01`, whose mechanism is established by source reading and was never executed; the precise scheduler cause behind `S4-R2-02`; Step 5; external repositories, network services, credentials, non-macOS hosts, native Windows and hosted CI; and the inherited same-UID mutation window after the final identity check.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R2-01 | low | `plugins/hexaemeron/tests/emit_issue_453_guard_report.py`; `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:231` | `missing_surface_suite` raises `AssertionError` when any of the twelve `REQUIRED_SURFACE` paths is absent, so unittest records a failure rather than an error, and `classify` tests `errors > 0` before `assertion_failures > 0` and returns `guarded`. The counters are then identical to a genuine guard's, at one executed test, one assertion failure, zero errors and zero skips, so a surface-absence condition is indistinguishable from a guarded finding in the report alone. The step's rename changed one `REQUIRED_SURFACE` entry, which brings the item inside this step's diff. No exposure occurred: both Step 4 guards were re-derived against their genuine named cases, `test_kf_453_06_resume_preserves_remaining_and_no_findings` and `test_kf_453_07_red_guard_cannot_finish_without_fixed_tree_green`, read from the parent tracebacks, and the fallback also names the absent paths in its own message, so the operator-visible diagnostic distinguishes what the counters do not. | accepted; guard-evidence admission is Step 3's receipted subject through `kf-453-03` and `kf-453-04`, and the Step 4 Files field authorises this file only for the case-map and required-surface repointing, which is complete and correct, so repairing it here would reopen a closed contract and ride an unrelated change into a fixes commit; the remedy for a later step or a follow-up issue is to raise a non-assertion exception in `required_surface_is_present`, which `classify` reports as `inconclusive` |
| S4-R2-02 | low | `.hexaemeron/runbook.md` Step 4 Tests; `plugins/hexaemeron/tests/run_tests.py:3822` | The step's declared Warden runner contract, `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` at `unittest-json-v1` into `.elenchus/fiat-453-step-4-audit.json`, returns exactly `inconclusive -- the report is incomplete` when run against fix `ae1901b5`, and returns the same with `--jobs 1`, so parallel worker descendants are not the cause. The runner writes `complete=complete and not scheduler_errors`, so an incomplete or scheduler-faulted run on the overlaid parent cannot yield the complete report `guarded` requires. A future Warden fix in this step would therefore receipt a weaker verdict than its guard earns. Step 3 round 2 recorded the same class of infrastructure `inconclusive` and likewise declined to present it as product evidence. The guard property itself does hold here, established independently of Elenchus: the overlaid pin assertion reads the tracked mirror and evaluates `final_generation` 13 at parent `9e1ec6f9` against its assertion of 14, and 14 at `ae1901b5`, so the fix carries a test that is red without it. | accepted; the verdict is recorded untranslated and is non-blocking by the audit-loop's own terms, this round made no Warden fix, so nothing in it depended on that contract, and the precise scheduler cause inside the detached parent worktree was not established and would be separate work |

Leads not pursued: Both round-1 findings are closed by signed fix `ae1901b5cb1e4917a2c446e2eba9a5580cd4bba7`, whose sole parent is the round-1 record `9e1ec6f98f3fb87607e19f7e50d56b2c898934ad`, signed with key `B83B60AE16F5DD1A` by `laurence.e.day (github-signer)`, authored by Shoggoth with the repository identity as committer and both trailers. It changes exactly two paths, the tracked runbook mirror at 82 insertions and zero deletions and `plugins/hexaemeron/tests/test_known_failure_inventory.py` at three lines each way, and touches no product path. Runbook amendment `dc95819a8f7e3a6b19b9803dd0eeff0980d2bef32210f46201244d0858b61ca1` replaces the Exit field only, moving the runbook from `0ed7432ce2179c4e1bc66a79adb7ccbd19fe2487f22ecdf023d2c4275cac3b27` to `b9510f3ead0a231508f50b69c7cec98d5c8e9191cd051c9455f969f88c7effe6` between byte 253,597 and byte 259,047; the mirror is append-only with its 253,597-byte prefix byte-identical and 5,450 bytes appended, and it equals `.hexaemeron/runbook.md` exactly. They are closed rather than moved. The effective Exit's Brevitas `report` loop now names the four product drafts only, and the two commands Steps 2 and 3 ran are restored as its lines 16 and 17: `audit_synopsis.py --check .` exits 0 and reports `committed=match`, and the record under `--mode fiat-audit-record` exits 0. `report` mode on the record still reports eight `B011` findings, which is the same structural incompatibility as before and is now unreachable from the Exit, so the defect removed was the routing rather than the record. The Exit prose the amendment adds is accurate about the grammar and not only about this file: a single-round log split from this record fails both `B010`, "draft has 1 section headings; minimum is 3", and `B011` under `report` mode, and exits 0 under the dedicated mode, so a fresh run's first record would trip both minimums exactly as the prose states. The assignment restatement is faithful: the runbook now holds 14 `Complete replacement Exit:` clauses, 29 unchanged `Complete replacement Files:` clauses and 84 `Known-failure assignment:` lines, the per-generation distribution recomputed from the shipped document is `[0, 0]` followed by twelve sevens, all twelve non-empty generations carry one single distinct assignment block with the newest two byte-identical, and the pinned inventory suite passed 40 of 40. On whether that pin should be generation-sensitive at all: moving 13 to 14 was the correct minimal response, because the literal is what detects an unintended extra generation, and the repository already governs its Fiat ledger row the same way in `tests/test_evolution_contract.py`, which this step also had to move. The coupling is real, since any future Exit amendment to any step forces this edit, and the same invariant could be written generation-insensitively as `[0, 0] + [7] * (final_generation - 2)` while `test_final_exit_generation_never_falls_back` already guards the no-fallback rule on synthetic fixtures; that is a test-convention change for whoever owns the convention, not an audit finding, and it is recorded here rather than acted on. Exits on the fixed tree: Phylax 0, Ephoros 0 and Hypomnema 0, the last run before the ignored portable payload was regenerated as the Exit ordering requires; recovery with Fiat-skill 135 of 135; checkpoint with checkpoint-identity 81 of 81; portable sync 0; portable check 0; promise check 0 over 18 plugins and 18 copies; the Hexaemeron suite 2,533 of 2,533 with exact accounting, executed once, and zero failures, errors, skips, expected failures and unexpected successes; the clean detached root suite at `ae1901b5` 1,339 of 1,339 with the dead-code lane reporting `currency current; no tracked path changed after publication`; `run_checks --base 5bc2494c` 0 with 29 checks passed and `outcome green`; Imprimatur 0 at `--max-defects 0`; the four product Brevitas drafts 0; and `git diff --check` 0. Nothing was written into the detached worktree, which stayed clean. Review of the four-path range `4106a886..ae1901b5`, at 104 insertions against four deletions, found both durable documents append-only, no product path touched, and no route through any of the twelve covered risks. The controller's own round-1 receipt independently matches what that round reported, at `log_end_offset` 38,159, `record_timestamp` `2026-09-11T02:07:40Z`, `entry_sha256` `c99095a93bec787c9902b22f5def7f5c0569bc741827759e0d5e8f8f4bd1e35f`, `synopsis_sha256` `ba9511e5bff67b92f3c2af9bd1cc6dd6ac342d67caeec6d1b013c1dd2f0c354a`, findings 2, all three lint exits 0, and both `fixes_commit` and `elenchus_verdict` null. The four consequential paths outside the amended Files field from round 1 are unchanged in this range and remain receipted precedent from `dfd380b8`, `96f55869` and `8f3a491b` under the `S2-R1-01` remedy. Both of this round's findings are recorded as accepted rather than open, because neither is repairable inside this step's authority and neither blocks it. Round 2 makes no Warden repair, so it records no fixes commit and no verdict of its own, and the `inconclusive` above belongs to `S4-R2-02` rather than to this round's verdict field. Because round 1 found two, the audit close owes fixes evidence, and `ae1901b5` is the commit that carries it. Next action: close the audit phase with that fixes evidence and both accepted reasons, or reopen a round for the one-token `missing_surface_suite` remedy.

## Step 5, round 1 -- 2026-09-11T13:48:28Z

Audit schema: fiat-audit-round/v2

Covered: inventory-omission=reviewed; source-view-drift=reviewed; parent-substitution=reviewed; guard-path-escape=reviewed; report-substitution=reviewed; verdict-confusion=reviewed; partial-inoculation=reviewed; preedit-bypass=reviewed; resume-loss=reviewed; empty-success=reviewed; red-step-handoff=reviewed; self-hosting-overclaim=reviewed

Not checked: the Pashov Solidity suite under the exact waiver `waived: no Solidity source or Solidity behaviour is in the issue 453 scope`; `hexctl`, which the operator holds, so no controller transition, receipt, push, pull request, merge, publication or issue closure was exercised here; this delivery's own strict capture path, because the runbook receipt still carries no `known_failure_inventory`, so `receipted_known_failure_inventory` returns None at `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:7101` and every reader in this run still takes the legacy branch -- the strict readers do run, inside the proof's disposable repository, and only there; `partial-inoculation` beyond a single assigned id, because each proof lane declares at most one finding, so receipt atomicity over a multi-id set is exercised nowhere in this step; `resume-loss` beyond `halt`, `resume`, `next` and `verify`, because no lane drives `status`, `restore` or a compacted continuation; the refusal half of `red-step-handoff`, because no lane reaches `done step` or a push, so the proof shows the clean exit and not the refusal; the `missing_surface_suite` fallback named by `S4-R2-01`, still unexecuted and still outside this step's Files field; the precise scheduler cause behind `S4-R2-02`; `proof.py` on a host whose `git` or `ssh-keygen` sits outside the child `PATH` `/usr/bin:/bin:/usr/sbin:/sbin`; and external repositories, network services, credentials, non-macOS hosts, native Windows and hosted CI.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | low | `plugins/hexaemeron/docs/known-failure-inoculation/proof.md:4`; `plugins/hexaemeron/docs/known-failure-inoculation/proof.py:4`, `:532`, `:1375`, all at `fe2e601d` | The transcript and two `proof.py` docstrings say the proof drives `two complete lanes`. The run drives three, `guarded`, `no-known` and `unguarded`: it prints `proof: 3 lanes, 17 phases, 12 refusals, 16 bound tokens`, the record block binds three `lane` rows, and `493c6b15`'s own commit message says three. The lane count is a bound fact of the run, and `check` resolved every bound field while reading none of the prose around them, so the stale figure passed the transcript's own check on every run since the third lane was added. The repair corrects all three statements and adds one rule to `check` that resolves the stated lane word against `len(record.lanes)` and refuses a mismatch or an unstated count; run against `fe2e601d`'s transcript that rule answers `the transcript states no lane count the run can answer`. | fixed in `d316fc6bfaba139cdd491db0e7cb3d3f42b48285` |
| S5-R1-02 | low | `plugins/hexaemeron/docs/known-failure-inoculation/proof.md:7`; `plugins/hexaemeron/docs/known-failure-inoculation/proof.py:1378`, both at `fe2e601d` | `Nothing below was typed by hand` claims more of the record block than the run establishes. The `question`, `case`, `class` and `result` columns are literal arguments to `Record.phase` and `Record.refusal`, and every `verdict=` value is the third element of a `VARIANTS` row at `proof.py:511` or a literal beside it; none is read back from the controller. `proof.md:57` says of that same column that it is `inferred from the bound counters under Elenchus's published classification, not read out of the refusal`, so the opening sentence contradicts the document two sections later. This is the `self-hosting-overclaim` class, in the one document whose purpose is to bound what the disposable run proves. The preceding sentence, enumerating number, command, exit status, path and digest, is accurate and is kept. No mechanical guard applies; the repair names what the file authored. | fixed in `d316fc6bfaba139cdd491db0e7cb3d3f42b48285` |

Leads not pursued: `proof.py` was audited in full against Phylax, and its named boundaries hold. The ephemeral ed25519 signer is generated into `tempfile.mkdtemp(prefix="fiat-inoculation-proof-")`, only its public path reaches `user.signingkey`, and neither key nor passphrase is printed; `refusal_text` substitutes `<object-id>` for every 40-hex and `<digest>` for every 64-hex token; state, ledger, manifest, study and inventory digests reach the transcript only as `@name` tokens whose actual values stay in memory. Cleanup is unconditional: forcing `atomic_write` to fail by pointing `--transcript` at a directory left no `fiat-inoculation-proof-*` under `TMPDIR`, unlinked the staged `.proof-*.md`, and exited 1. Three leads stay open there. `run_bounded` caps combined stdout and stderr at 1 MiB after `subprocess.run` has already buffered both, so the cap bounds what the proof reads rather than what it holds; every child is first-party. `shutil.which` resolves `git` and `ssh-keygen` against the parent `PATH` while children search the fixed `/usr/bin:/bin:/usr/sbin:/sbin`, so a host with either elsewhere passes the preflight and raises `FileNotFoundError` in the child. That failure and an `OSError` from `atomic_write` leave `main` as a traceback rather than the bounded `proof.py:` line, which the forced-failure run above showed; cleanup still ran. `--keep-temporary` retains the disposable repository with its private key inside and prints only the directory, which is the flag's stated purpose. Determinism was tested rather than accepted: four runs, two on `fe2e601d` and two on `d316fc6b`, produced `39d905c6772e1029f98fd2a4bd01b96fae37ee19dcc4d8780a087088661d6b26` twice and `9aba0ee3d1be109d334dbb04337db531ca58df567c87e38d47b29accf43805d3` twice, and the first of those reproduced Mason's committed bytes exactly. The regeneration changed the opening paragraph only; every `source`, `lane`, `phase`, `refuse` and `bind` row is byte-identical across the repair. Both limitations the transcript records were checked at their mechanism: `_base_result` at `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:1577` returns the four keys `ref`, `status`, `tests` and `detail`, the `unguarded` early return at `:1729` uses it, and `hexctl.py:11935` refuses any result whose key set is not the exact eight, so `no-test-blob`, `incomplete-report` and `absent-report` share one refusal text and the transcript's `verdict` column is inferred from the bound counters rather than read; and this delivery's guard and final-green evidence came from the manual bootstrap procedure, which `proof.md` states. The step's own no-known-findings obligation is met before its release edits: `.hexaemeron/bootstrap/step-5/no-known-findings.json`, recorded at `2026-09-11T07:13:29Z` against step parent `229152750ceeac83ca043537732204cdfc7a5fbd` and ahead of the `09:21` build commit, binds study `c220ea89949ace129c135ac81c9d5876a0fa5a8bc65b41c5d94ba347c07aa92d`, inventory `be3f0d7898933d85197fb8976d80cb40263f3f8308555fbfdeaaa803bf3e776a`, all seven ids with their consuming steps and all three source-view pairs, each of which still matches the tree; the empty `assigned_to_step_5` array stands beside that claim rather than instead of it. Release surfaces were checked rather than taken: 1.6.24 moves to 1.6.25 in both plugin manifests and both marketplace records, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` follow, the three declared ledgers keep 58, 16 and 5 data rows with the existing candidate row edited in place, and a walk of all 81 `{path, sha256}` pairs in `tests/promise_machine_coverage.json` found zero stale digests, which is why it needed no change. `.horos/boundary.json` and `.horos/candidates.json` are generator-produced: `horos scan . --write` left the tree unchanged before and after the repair, and `.horos/census.json` needed none. The controller's two Exit amendments were reviewed as new surfaces. `336e8ece` names the four maintained modules and `docs/known-failure-inoculation-study.md`, all of which exist and pass; `ebfd1769` cuts the Brevitas `report` loop to nine drafts and restores `audit_synopsis.py --check .` and `--mode fiat-audit-record`. Both claims hold on this tree: the nine drafts each exit 0, and the four removed drafts each exit 1 under `report` mode, the study on `B023`, `B022` and `B006`, the runbook on `B022`, `docs/agent-instruction-language-v1.md` on `B011` and this record on `B011`, so generation 15's Exit could not have passed. All four stay inside the Imprimatur list, which exits 0 at `--max-defects 0` over 16 drafts. Two changed paths sit outside Step 5's Files field, `plugins/hexaemeron/docs/known-failure-inoculation/runbook.md` and `plugins/hexaemeron/tests/test_known_failure_inventory.py`; both come from the controller's own amendment commits `5a3ae005` and `fe2e601d`, and Mason's build `493c6b15` matches the Files field exactly, so this is the receipted precedent `S2-R1-01` established rather than build drift. `S4-R2-01` stays accepted: `plugins/hexaemeron/tests/emit_issue_453_guard_report.py` is not in this step's diff and this step's Files field does not authorise it either, so the one-token remedy in `required_surface_is_present` still belongs to a later step or a follow-up issue. `S4-R2-02`'s class recurred in its own way. The declared runner `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` at `unittest-json-v1` into `.elenchus/fiat-453-step-5-audit.json` never ran, because Elenchus answered `unguarded -- the commit changed no test files` at exit 0 with `tests` empty, the fix having changed only `proof.py` and `proof.md`. The guard it earns is real and lives inside `proof.py`'s own checker, observed red against `fe2e601d`'s transcript before the repair; the declared runner does not read that file as a test, and no repository test pins `proof.md`'s bytes, so nothing translates the verdict upward. Exits on the fixed tree `d316fc6b`: `proof.py` 0 with 3 lanes, 17 phases, 12 refusals, 16 bound tokens and all six replay mutations refused; the four focused modules 137 of 137; portable sync 0, portable check 0, promise check 0 over 18 plugins and 18 copies; Horos scan and check 0 with the tree unchanged; the Hexaemeron suite 2,533 of 2,533, exact accounting, executed once, zero failures, errors, skips, expected failures and unexpected successes; the clean detached root suite 1,340 of 1,340 with its 1,258-case inoculation census at zero crashes and zero unexpected-clean, its dead-code lane reporting `currency current; no tracked path changed after publication`, and nothing written into that worktree, which stayed clean; version, evolution and proxy 181 of 181; `run_checks --base 5bc2494c` 0 with 29 checks passed and `outcome green`; both Protasis calls 0; Phylax 0, Ephoros 0 and Hypomnema 0, from the bundled and the repository copies alike, with Hypomnema run before `portable_promise_machine.py sync` had materialised the ignored payload; Imprimatur 0 at `--max-defects 0`; the nine-draft Brevitas loop 0; `audit_synopsis.py --check .` 0 with `committed=match`; the record under `--mode fiat-audit-record` 0; and `git diff --check` 0. The same set ran green on `fe2e601d` before the repair, with the root suite at 1,340 of 1,340 and `run_checks` green over 29 checks. No push, pull request, merge, publication or controller transition was made. Next action: receipt this round with `--findings 2`, `--fixes-commit d316fc6bfaba139cdd491db0e7cb3d3f42b48285`, `--elenchus-verdict unguarded`, the three lint exits at 0 and `--audit-filter sapheneia:sapheneia`, then run round 2 against the fixed tree.
