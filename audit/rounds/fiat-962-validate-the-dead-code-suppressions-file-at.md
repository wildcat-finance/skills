## Step 1, round 1 -- 2026-08-31T02:41:52Z

Audit schema: fiat-audit-round/v2

Covered: checkout-binding=reviewed; missing-suppressions=reviewed; stale-suppression=reviewed; partial-report=reviewed; baseline-semantics=reviewed; runtime-budget=reviewed; check-map-ownership=reviewed; report-contract-drift=reviewed; repository-substitution=reviewed; interrupted-validation=reviewed; advisory-authority=reviewed; operator-recovery=reviewed

Not checked: the security suite was waived exactly as `waived: no Solidity; this run changes the Python dead-code command, workflow, tests, and prose only`, so `x-ray`, `solidity-auditor`, and `fizz` did not run and this round makes no Solidity claim. No hosted GitHub Actions runner executed. External analyser semantics were not independently proved; the audit checked the local fixed `python,repository` implementation, emitted records, guards, and checked-runner wiring. Live Linux and Windows behaviour, remotes, GitHub objects, push, merge, and controller mutation were neither authorised nor performed.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: This independent Warden audited the complete logical diff `c79d6781e2278642d1653d50671acdabb5867ef8..302381aef18a08cf1442b2b563abc5f0bb7fab24` and the relevant current source. Commit `302381aef18a08cf1442b2b563abc5f0bb7fab24` has a good signature from key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A` and exactly the required co-author and origin trailers. The source-bound study, runbook, effective Step 1, and design-evidence SHA-256 values match `5709f08b2374f2792bf63ebf8e62a493213b0003b4bba01e7c5cffafdbe9d462`, `484e2160be5c4069cea83daf3b38114db0dda14bad48bf253efa0071c45191f1`, `1d1e497f8d8b8478c97c219ffa38c13a9a06244beb7461b6830cf0e1581aba61`, and `1d61f2c933cd6872cdc9127a5a0b0d7a40467ad3aa485ccbe8a9126d62dba1d8`; the selected candidate is `checkout-suppressions-command`, and both shipped source documents are byte-identical to their bound packet copies. The focused dead-code module passes 233/233. The live command emits exactly one 148-byte line at current commit, reports analyser states `python=ran,repository=degraded`, 439 findings, and zero suppressions, and exits zero. Standalone samples complete the suppression check in 29.05 s and the unchanged baseline check in 27.64 s, both within the 46 s budget. Both `report` forms exit zero. The `dead-code` scope selects and passes all five owned checks. The complete canonical changed-path closure selects and passes all 26 checks; an earlier unqualified closure passed 25 and failed only Lazarus because inherited `TMPDIR=/var/folders/...` traversed macOS's `/var` symlink, while the exact Lazarus suite passes 599/599 with ten skips and the full closure is green when `TMPDIR` uses the canonical `/private/var/folders/...` spelling. Phylax, Ephoros, Hypomnema, both Protasis checks, Imprimatur over all four shipped prose files, Brevitas over each of the ADR and operator document, Horos, `git diff --check`, the Promise Machine copy check, the portable Promise Machine check, and the pre-existing audit-synopsis check each exit zero. Imprimatur also exits zero on this audit record. Brevitas report mode exits one with B010 because the owning Warden schema permits one round heading and B011 because that schema requires the exact one-row zero-finding table; this record does not claim Brevitas-clean structure, and changing either protected host requirement was not pursued. Static review confirmed descriptor-bound Git reads, strict suppression parsing, exact analyser-state handling, commit-bound baseline loading, final repository-identity and clean-tree checks, no product write before validation, and advisory-only operator recovery prose; no source defect or authorised fix followed. No fixes commit exists, so the exact Elenchus runner was not invoked and the verdict remains `null`. The bounded Sapheneia pass preserved this heading, schema, all twelve register dispositions, waiver, unknowns, hashes, counts, timings, commands, verdict, zero-finding row, and unpursued leads. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof.
