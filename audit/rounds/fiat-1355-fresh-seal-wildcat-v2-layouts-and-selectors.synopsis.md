Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1355-fresh-seal-wildcat-v2-layouts-and-selectors.md | source_sha256=d8403416aef95ae76002a15cced22d98bb55f7b8221de2df21f0910227da6216 | h2_count=5
## Step 1, round 1 -- 2026-09-23T02:16:58Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed<br><br>Not checked: The step diff `e9e95b891b3ae642516e017c46b97a18a1480164..fac1493f31e48d259552955563d20c8235620277` carries 0 `.sol` files, so the X-Ray and solidity-auditor in-scope source sets are empty and neither produced analysis output; fizz does not apply because the step ships no Solidity. Byte-equality of each equivalent type to its sealed anchor files, the zero-pass proof for the two excluded test files, the Gate 5 demonstrations and fixture binding of the recorded code hashes belong to later steps and were not checked. Of the 28 profile comparisons, 2 were reproduced independently (`market-lens-app` and `open-access-role-provider` at `v2-e1f7`, both profiles, Forge 1.7.1 `4072e48705af9d93e3c0f6e29e93b5e9a40caed8`, fresh public clone); the other 26, including both private trees, were not. `run_checks.py` was not run; the root suite and `.githooks/greenlight` were.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `scripts/kickoff_hermes_1355.py` | `validate_custody` matched `^\s*pragma solidity` against the raw bytes only, and SPDX only as a raw substring. A standard-JSON input escapes each source into one string, so `docs/kickoff/1355/evidence/standard-input.json` carrying `"content": "pragma solidity 0.8.25;\ncontract X { uint256 a; }\n"` with no SPDX line passed `check` (reproduced on `fac1493f`). The runbook's Tests clause requires a target or private source file under the docs tree to be refused. Fixed by decoding every `.json` file under the tree and testing each key and string value; a JSON file that does not parse is now refused, and `parse_json` maps `RecursionError` to a named refusal. | fixed in c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7 |<br>| S1-R1-02 | low | `docs/kickoff/1355/inventory.json` | The `open-access-role-provider` `registry_source_override` was checked only by commit (`5d7f8c889a8d29935838a3906172feb8d9861807` must equal the registry's source commit). Its evidence, the Sourcify source SHA-256 `7a5b57852f433b876f0b43048c74158a740ce0f2708587b31eb682a7c390f84f`, sat in the free-text `reason`, unchecked against `docs/kickoff/1359/evidence/source-match-1590.json`, which records it. The claim itself holds: `src/OpenAccessRoleProvider.sol` at `e1f77540` and `5838b2f3` hashes to that digest in a fresh public clone, and the registry-named private blob `07bc4c91edb64b1255d61173c35e0523ee33f12f49a59d320834256f510e3b9d` differs. Fixed by adding a required `sourcify_source_sha256` field; the checker verifies `source-match-1590.json` against the registry's evidence digest `0bd8d53d351e46082dbb625a499bc7c6cebe1f1f43ab4c9ef2498f23fb79282e`, requires exactly one Sourcify `match` entry at the overridden commit for an inventory address of the type, and requires the field to equal its `source_sha256` and the registry blob to differ. | fixed in c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7 |<br><br>Leads not pursued: Reviewed range `e9e95b891b3ae642516e017c46b97a18a1480164..fac1493f31e48d259552955563d20c8235620277`, one signed commit over 29 files; fixes commit `c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7` changes 5 files. Mechanical exits over the 29 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. Root suite `python3 -m unittest discover -s tests` on the fixed tree: 2,189 tests OK, exit 0; `.githooks/greenlight` exit 0; `tests.test_kickoff_hermes_1355` 38 of 38; `scripts/kickoff_hermes_1355.py check` exit 0 with 137 addresses, 17 types, 8 exclusions and 28 of 28 comparisons invariant; `horos.py check .` `boundary matches the tree`. The five later conformance criteria (`owner-handoffs`, `selector-rejection`, `layout-rejection`, `sealed-coverage`, `evidence-custody`) each exit 1 naming the criterion and the transition it blocks. The checker recomputes each blob key as the SHA-256 of Hermes's canonical text, so a map digest is not accepted without content. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json` returned `inconclusive` for `c6d0b088dc9a`: over the whole root suite on the detached parent it counted 2,115 executed, 42 assertion failures, 585 errors and 2 skipped. By hand, the changed test file overlaid on a `git archive` of `fac1493f` ran 38 tests with 3 failures, exactly the three new guards, and 0 errors; on `c6d0b088` it ran 38 OK. The 585 parent-wide errors were not investigated. Four leads were seen and left. First, the capture script `.hexaemeron/design/capture_profiles_1355.py` (SHA-256 `fd17ec67b951eb6cbded967ab8d45a82d2c8c12c0930056a565e9368ea1d36c5`, confirmed) is gitignored, so the committed record names bytes a reader cannot fetch; the README's manual steps were enough to reproduce 2 comparisons. Second, custody refuses by suffix, directory name, private build-input digest and Solidity text, so private test output or private prose under `docs/kickoff/1355` would pass; the tree was grepped and carries none. Third, the fee recipient and role-provider anchor layouts and method maps from private repositories are committed as derived maps, as the README states. Fourth, `.hexaemeron/design-reports/anchor-and-inspect-profile-invariance.json` was written before these fixes and not regenerated; it binds no inventory digest. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
## Step 1, round 2 -- 2026-09-23T02:39:35Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed<br><br>Not checked: The round covers the fixes commit `c6d0b088dc9a0d78c4cab7c2014c4bb832a6b1d7` and the step as a whole at `2f1f455a032b8943d4654312a72c743dce855c70`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No further profile comparisons were reproduced beyond round 1's 2 of 28. A Solidity source hidden in Markdown through JSON unicode escapes (for example `\u0070ragma`) still passes custody; it needs deliberate obfuscation rather than a copy, and was not fixed. `run_checks.py` was not run.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | low | `scripts/kickoff_hermes_1355.py` | The S1-R1-01 fix decoded `.json` files only. A standard-JSON input pasted into Markdown keeps its escaped newlines, so no line starts with `pragma solidity`, and the SPDX test skips `.md`: `docs/kickoff/1355/evidence/notes.md` holding `{"content": "pragma solidity 0.8.25;\ncontract X {}\n"}` passed `check` on `2f1f455a` (reproduced). Fixed by refusing `pragma solidity` followed by a version constraint anywhere in any file under the tree; a negative control confirms prose naming the keyword alone still passes. | fixed in ce4081de44f16ca0843bdefe4b79ad00242504ce |<br><br>Leads not pursued: Fixes commit `ce4081de44f16ca0843bdefe4b79ad00242504ce` changes 4 files. Mechanical exits over the changed paths: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,191 root-suite tests; `tests.test_kickoff_hermes_1355` 40 of 40. The rest of `c6d0b088` held on review: `validate_overrides` needs exactly one Sourcify `match` entry at the overridden commit for an inventory address of the type, the JSON walk is iterative, and a JSON file that fails to parse is refused. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json` returned `inconclusive` for `ce4081de44f1`, counting 2,117 executed, 40 assertion failures, 585 errors and 2 skipped on the detached parent. The cause is the runner's containment, not the parent tree: the round-1 overlay (`c6d0b088`'s test file on a detached worktree at `fac1493f`) run through the same `tests/run_tests.py --elenchus-report` outside Elenchus reported 2,189 run, 0 errors and 4 failures, the 3 round-1 guards plus `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan`, which the overlay's extra bytes trip. Elenchus's runner forbids child processes, and the root suite starts them. By hand, `ce4081de`'s test file on a `git archive` of `2f1f455a` ran 40 tests with 1 failure, the new guard, and 0 errors; on `ce4081de` it ran 40 OK. The two round-1 leads were weighed again and neither is a finding. The gitignored capture script (`fd17ec67b951eb6cbded967ab8d45a82d2c8c12c0930056a565e9368ea1d36c5`) sits outside the step's Files clause, a `.py` file may not ship under `docs/kickoff/1355`, and the README recipe reproduced 2 comparisons. Custody's blindness to private test output or prose is outside the `private-payload` mitigation, which names private source, test source and complete private Hermes directories, all of which the checker refuses, and prose cannot be classified mechanically; the tree carries none. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
## Step 1, round 3 -- 2026-09-23T02:45:38Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: equivalence-overreach=reviewed; exclusion-loss=reviewed; compiler-pin-drift=reviewed; profile-assumption=reviewed; protected-set-gap=reviewed; role-provider-source=reviewed; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=reviewed; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed<br><br>Not checked: The round covers the fixes commit `ce4081de44f16ca0843bdefe4b79ad00242504ce` and the step at `9298d3a4d8e33bc6f35b8dfbbc9dda271a015c46`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. A Solidity source hidden in Markdown through JSON unicode escapes (for example `\u0070ragma`) still passes custody; that needs deliberate obfuscation and stays unfixed. Private test output and private prose are not classified. `run_checks.py` was not run.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: The custody scan is closed at the `private-payload` and `vendored-source` mitigations. Against `9298d3a4`, six planted specimens under `docs/kickoff/1355` were each refused: test source in a Markdown fence, escaped source in JSON with no SPDX line, escaped source inline in Markdown, a `baseline-sources` directory, a `.sol` file, and a pragma with extra spaces. The unmodified tree passed. Private-source digests stay covered by `test_private_source_digest_under_docs_tree_is_refused`. `ce4081de` adds `PRAGMA_WITH_VERSION` and one alternative in `validate_custody`, and nothing else in the checker. Mechanical exits over the 31 changed paths: phylax 0, ephoros 0, hypomnema 0. `tests.test_kickoff_hermes_1355` 40 of 40; `scripts/kickoff_hermes_1355.py check` exit 0; the five later conformance criteria each exit 1 by name. The root suite ran through `.githooks/greenlight` before this record was committed. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
## Step 2, round 1 -- 2026-09-23T03:51:37Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: equivalence-overreach=not-applicable; exclusion-loss=not-applicable; compiler-pin-drift=not-applicable; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=not-applicable; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed<br><br>Not checked: The step diff `bc8390b30bfd0f7b2af118b1e1edf5a9438e2701..d29593fb7b6d3cca4d404a899e77f27fe5c999d0` carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. No retained byte recomputes the capture's request, byte and time counts or its attempt list, and the three discarded attempts left no fixture to inspect. The header is self-consistent and matches the registry's recorded hash; whether it is on the canonical chain was not checked. Target-maintainer review of the inventory is not recorded. The comparisons in `validate_fixture_payload` and `validate_release_payload` run only where `.hexaemeron/restricted/` holds the payloads. `run_checks.py` was not run; the root suite ran through `.githooks/greenlight`.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | `scripts/kickoff_hermes_1355.py` | The module docstring said `owner-handoffs` conformance "recomputes the committed records from those bytes". It recomputes fixture rows, component digests, plan limits, recorded responses and the release identity, but the capture's request, byte and time counts, UTC time and attempt list are the capture script's own report. On a scratch copy holding the retained payloads, with handoff digests rebound, setting `capture.requests` to 1, `capture.response_bytes` to 1, `capture.elapsed_seconds` to 0.1 or `capture.observed_at` to `2020-01-01T00:00:00Z` each still wrote value `true`. `docs/kickoff/1355/README.md` stated those counts as facts and left them out of its evidence-class section. Fixed by stating in the docstring and README which fields are recomputed and that the capture counts are recorded; compliance with the declared limits rests on the fixture-bound plan and Lazarus's enforcement during capture. | fixed in 800608986ce2edd2637334dbd3ba3b0a975fc825 |<br><br>Leads not pursued: Reviewed range `bc8390b30bfd0f7b2af118b1e1edf5a9438e2701..d29593fb7b6d3cca4d404a899e77f27fe5c999d0`, one signed commit over 8 files; fixes commit `800608986ce2edd2637334dbd3ba3b0a975fc825` changes 3 files. Mechanical exits over the 10 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,209 root-suite tests; `tests.test_kickoff_hermes_1355` 58 of 58 in the run worktree, the 2 retained-payload tests included. Re-run from the retained payloads: `lazarus.py verify` exit 0, fixture `460b8f56f11dc1f2dc5e89fa4215b465a75ec411c94a56b50c6cc9985140d4e4`, 137 proof-backed, 1 header-bound, 137 recorded-rpc; `alexandria.py verify` exit 0, `sha256:adab37e8142ab295fab84dbe10509aa51cb9eb722550bef6c1036eef09076551`; offline replay served 137 of 137 code reads equal to the proof records and answered another block with `-32070`. Lazarus's verifier recomputes the header hash, checks each account proof against the state root and each code's keccak against the proved `codeHash`, and requires the RPC records to cover the plan exactly. Of 13 mutation probes on scratch copies with rebound handoff digests, 9 refused under conformance: a row's `code_sha256` or `proof_record_sha256`, `verification.state_root`, `capture.script_sha256`, the Lazarus manifest tool version, the release name, the release time, a capture's `record_count`, and a component's redistribution class; the other 4 are the S2-R1-01 fields. Under conformance no stated digest passes: each release input, handoff artefact, payload component and manifest is hashed from its bytes. `check` alone validates the committed records' shape and cross-record agreement, not the payload. A recorded value labelled `proof-backed`, or a proved value from any source but `proofs.jsonl`, refuses. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-2.json` returned `unguarded` for `800608986ce2`: the commit changes no test file, and a prose correction has no failing test. Five leads were weighed and left. First, the inventory handoff row reads `complete` with reviewer "Warden, step 1 audit rounds 1 to 3" and a note that target-maintainer review is not recorded. The runbook exit asks each row to name producer, reviewer, artefact and digest, and it does, so the report's `true` holds against that contract. Study section 7 names "Warden and target maintainer" for the inventory, baselines and rejections row, "pending the conformance gates", so that review stays open and the `true` does not say it happened. Second, the fixture and release rows name "Warden, step 2 audit" as reviewer, and `.hexaemeron/design-reports/anchor-and-inspect-owner-handoffs.json` was written at 2026-09-23T03:31:25Z, before this round; this round performed that review. Third, `RetainedPayloadTests` skips outside the run worktree, so once that worktree is gone no suite exercises the payload comparisons; the stub-verifier tests reach only refusals that precede them. The payloads are excluded from the tree by custody, so this was left. Fourth, capture attempt 2 went through a loopback relay that hid the RPC URL from Lazarus's secret scan and was discarded unused; the retained tools hold no relay. The capture credential's URL host, URL path segments of 8 or more bytes and bearer were compared byte for byte, without printing, against every file in the run worktree, `.hexaemeron/restricted/` included, and against the step commit's patch: 0 matches. The capture script reads both from the environment and passes neither through argv. Fifth, the five fixture components carry redistribution `permitted` while the registry, source-match and chain-observation inputs carry `unknown`; `permitted` matches the Lazarus fixtures in the tracked Alexandria examples. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
## Step 2, round 2 -- 2026-09-23T04:05:18Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: equivalence-overreach=not-applicable; exclusion-loss=not-applicable; compiler-pin-drift=not-applicable; profile-assumption=not-applicable; protected-set-gap=reviewed; role-provider-source=not-applicable; chain-evidence-class=reviewed; wrong-gate-negative=not-applicable; candidate-class-mixing=not-applicable; source-restoration=not-applicable; private-payload=reviewed; vendored-source=reviewed; ffi-execution=not-applicable; partial-evidence=reviewed<br><br>Not checked: The round covers the fixes commit `800608986ce2edd2637334dbd3ba3b0a975fc825` and the step at `a7dc934665003536a8721a6fdb4de6e8bf64812a`. The diff still carries 0 `.sol` files, so X-Ray and solidity-auditor had an empty in-scope source set and fizz does not apply. The round-1 negative space stands: no retained byte recomputes the capture counts or attempt list, the header's place on the canonical chain was not checked, and the payload comparisons run only where `.hexaemeron/restricted/` holds the payloads. No target-maintainer review of the inventory is recorded. `run_checks.py` was not run.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R2-01 | low | `docs/kickoff/1355/evidence/owner-handoffs.json` | The inventory handoff row read status `complete` while its note said target-maintainer review of the mapping is not recorded, and the checker accepted only `complete`, so a clean `owner-handoffs` report read as though that review had happened. The orchestrator decided that Step 3 proceeds without it and that the review is carried forward to the run pull request. Fixed by giving the inventory row status `target-maintainer-review-outstanding` with a note naming the carry-forward; the checker accepts that status on the inventory row only, refuses it on any other row, reports `handed_off` 7, `complete` 6 and `review_outstanding` 1, and gates the criterion on every row being handed on. `docs/kickoff/1355/README.md` says the report's `true` does not mean the review happened. | fixed in 69cd15805dcffd3e8e6966ed92876a6040afa339 |<br><br>Leads not pursued: `800608986ce2` holds on review: the docstring and README now list what conformance recomputes, place the capture's request, byte and time counts in the recorded class, and tie limit compliance to the fixture-bound plan; the 4 round-1 telemetry probes stay accepted by design and are now described as recorded. Fixes commit `69cd15805dcffd3e8e6966ed92876a6040afa339` changes 5 files. Mechanical exits over the 11 changed paths on the fixed tree: phylax 0, ephoros 0, hypomnema 0. `.githooks/greenlight` exit 0 with 2,211 root-suite tests; `tests.test_kickoff_hermes_1355` 60 of 60 in the run worktree, the retained-payload conformance test included, so the report is still written from the retained payloads under the new status. Elenchus under `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-2.json` returned `inconclusive` for `69cd15805dcf`, counting 2,135 executed, 41 assertion failures, 586 errors and 4 skipped on the detached parent; as in Step 1, the runner's containment forbids the child processes the root suite starts. By hand, `69cd1580`'s test file on a `git archive` of `a7dc9346` ran 60 tests with 2 failures, the two new guards, 1 error, `test_committed_tree_passes_check` reading the new `handed_off` key, and 2 skipped; on `69cd1580` it ran 60 OK with the same 2 retained-payload tests skipped, the archive holding no payloads. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead.
