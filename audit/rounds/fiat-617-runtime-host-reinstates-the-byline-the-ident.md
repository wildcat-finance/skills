## Step 1, round 1 -- 2026-08-26T06:41:50Z

Audit schema: fiat-audit-round/v2

Covered: no-gate-relaxed=reviewed; byline-widening-scope=reviewed; no-host-identity-accepted=reviewed; cause-text-guarded=reviewed; message-no-echo=reviewed; readback-transport=reviewed; footer-reappearance=reviewed; no-receipt-shape-change=reviewed; procedure-rest-only=not-applicable; prose-pins-kept=not-applicable; human-authorship-preserved=reviewed; settings-file-shape=not-applicable; settings-file-scope=not-applicable; prose-matches-docs=not-applicable; ledger-integrity=not-applicable; version-surfaces=not-applicable; runtime-digest-pins=reviewed; partial-run=reviewed; interceptor-drift=reviewed

Not checked: the Pashov x-ray, solidity-auditor and fizz passes, waived because the step ships no Solidity; live GitHub behaviour (a real gh pr create, a REST body read-back against github.com, a claude[bot] author, the cloud session footer), reached only through the suite's fake git and fake gh; the Hexaemeron suite on Python 3.9.6, run here only on 3.12.13 under uv inside the Node 26 wrapper; the step-2 surfaces the not-applicable ids name (push-discipline.md, the Fiat SKILL.md and EVOLUTION.md, .claude/settings.json, INSTALL.md, docs/how-to-help-shoggoth.md and the four version manifests), which this step does not touch and which still read 1.6.2 and fiat-v5.26.1; the Interceptor repository, read only through the study's account of it

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | plugins/hexaemeron/tests/test_hypomnema_checker.py | The five H001 code-span guards pin a quoted link, a bare link, a quoted and a live link on one line, an unmatched backtick run, a span crossing a line break and the study's line 805, but none pins the common form whose link text is itself a code span (150 links of that shape, external targets included, sit in the required lint scope) or a span holding only part of a link. The checker reads both correctly because `_within` tests the offset of the opening bracket, yet a rule that skipped any link overlapping a span passes all five guards while dropping every code-text link from H001, with fewer findings and no red test. | fixed in this commit: two guards pin that a link whose text is a code span is still read and resolved, that a bracket inside a span opens no link, and that a bracket outside a span whose span swallows the target is still read; both fail under the overlap mutation, which the step's own guards do not detect |

Leads not pursued: the widened HOST_BYLINE_RE searches unanchored, so descriptive prose such as `the fixtures were generated with Copilot's help` or `regenerated with Claude` in a governed commit or pull-request body is now refused beside the attribution line, `co-authored by Claude Smith` in prose was refused at the base already, and the trailer `Co-Authored-By: Claude Smith <claude.smith@example.com>` still passes both gates; the study's section 4 accepts the widening as deliberate and its Never line forbids relaxing a refusal, so the cost stands recorded and the new cause clause tells the operator to reword; `Generated with GitHub Copilot`, `Generated by GitHub Copilot` and `generated with Cursor` pass HOST_BYLINE_RE because its alternation names `copilot` but not `github copilot` and omits `cursor`, `aider`, `devin`, `openai` and `anthropic`, which HOST_IDENTITY_NAMES carries; present at the base, and the runbook exit fixes the alternation as unchanged, so it is a later run's widening; the step's own pull-request body must not quote a refused specimen verbatim, whether a `Generated with` or `Generated by` line naming a host or the specimens in this record, because inspect_pull_request cannot tell a quotation from a byline and done push would refuse the step it describes; the five step commit messages were run through the widened expression and the co-author gate and none matches; the fixes commit changes only a test file, so the Elenchus parent overlay runs guards that already pass on the parent and returns passed rather than guarded; the red observation for S1-R1-01 is the overlap mutation run in this round, not the parent tree; the study's section 11 names the Elenchus report file `.elenchus/hexaemeron-unittest.json` while the receipted runbook step names `.elenchus/fiat-host-byline-readback-step-1.json`; the runbook is the source-bound contract and was used; the hypomnema-v4.5.0 row says H002 to H007 keep their codes and prior cases and does not name H000, which is also unchanged; the v4.4.0 row's H001 promise is superseded by this generation row and by the pointer-gate Boundary, not silently; horos.py check . names one synopsis candidate (fiat-503) it does not hard-classify and still reports that the boundary matches the tree; present at the base; the study's section 12 leads stand for the run pull request's carried-forward section: the Claude-Session trailer, the claude User login outside HOST_PR_LOGINS, the Interceptor's copied expression sharing the generated-with gap, and the unverified cloud footer suppression

## Step 1, round 2 -- 2026-08-26T06:58:00Z

Audit schema: fiat-audit-round/v2

Covered: no-gate-relaxed=reviewed; byline-widening-scope=reviewed; no-host-identity-accepted=reviewed; cause-text-guarded=reviewed; message-no-echo=reviewed; readback-transport=reviewed; footer-reappearance=reviewed; no-receipt-shape-change=reviewed; procedure-rest-only=not-applicable; prose-pins-kept=not-applicable; human-authorship-preserved=reviewed; settings-file-shape=not-applicable; settings-file-scope=not-applicable; prose-matches-docs=not-applicable; ledger-integrity=not-applicable; version-surfaces=not-applicable; runtime-digest-pins=reviewed; partial-run=reviewed; interceptor-drift=reviewed

Not checked: the Pashov x-ray, solidity-auditor and fizz passes, waived because the step ships no Solidity; live GitHub behaviour (a real gh pr create, a REST body read-back against github.com, a claude[bot] author, the cloud session footer), reached only through the suite's fake git and fake gh; the full Hexaemeron suite on this exact tree, not rerun because no test input changed since the round-1 Elenchus overlay ran 1297/1297 on the same test tree, which lacked only the two audit files; the Hexaemeron suite on Python 3.9.6, run only on 3.12.13 under uv inside the Node 26 wrapper; the step-2 surfaces the not-applicable ids name (push-discipline.md, the Fiat SKILL.md and EVOLUTION.md, .claude/settings.json, INSTALL.md, docs/how-to-help-shoggoth.md and the four version manifests), which this step does not touch and which still read 1.6.2 and fiat-v5.26.1; the Interceptor repository, read only through the study's account of it

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the widened HOST_BYLINE_RE searches unanchored, so descriptive prose such as `the fixtures were generated with Copilot's help` or `regenerated with Claude` in a governed commit or pull-request body is refused beside the attribution line, `co-authored by Claude Smith` in prose was refused at the base already, and the trailer `Co-Authored-By: Claude Smith <claude.smith@example.com>` still passes both gates; the study's section 4 accepts the widening as deliberate and its Never line forbids relaxing a refusal, so the cost stands recorded and the cause clause tells the operator to reword; `Generated with GitHub Copilot`, `Generated by GitHub Copilot` and `generated with Cursor` pass HOST_BYLINE_RE because its alternation names `copilot` but not `github copilot` and omits `cursor`, `aider`, `devin`, `openai` and `anthropic`, which HOST_IDENTITY_NAMES carries; present at the base, and the runbook exit fixes the alternation as unchanged, so it is a later run's widening; the step's own pull-request body must not quote a refused specimen verbatim, whether a `Generated with` or `Generated by` line naming a host or the specimens in both records of this log, because inspect_pull_request cannot tell a quotation from a byline and done push would refuse the step it describes; the six commits on the stacked branch were run through the widened expression and the co-author gate and none matches; the round-1 fix for S1-R1-01 at 7185fa9 changed only a test file, so its receipted Elenchus verdict is passed and the guards' red observation remains the overlap mutation run in round 1, not a parent tree; this round re-ran the 98-test checker module green and re-read both guards against the record's claims; the study's section 11 names the Elenchus report file `.elenchus/hexaemeron-unittest.json` while the receipted runbook step names `.elenchus/fiat-host-byline-readback-step-1.json`; the runbook is the source-bound contract and was used; the hypomnema-v4.5.0 row says H002 to H007 keep their codes and prior cases and does not name H000, which is also unchanged; the v4.4.0 row's H001 promise is superseded by this generation row and by the pointer-gate Boundary, not silently; horos.py check . names one synopsis candidate (fiat-503) it does not hard-classify and still reports that the boundary matches the tree; present at the base; the study's section 12 leads stand for the run pull request's carried-forward section: the Claude-Session trailer, the claude User login outside HOST_PR_LOGINS, the Interceptor's copied expression sharing the generated-with gap, and the unverified cloud footer suppression

## Step 2, round 1 -- 2026-08-26T08:07:41Z

Audit schema: fiat-audit-round/v2

Covered: no-gate-relaxed=reviewed; byline-widening-scope=reviewed; no-host-identity-accepted=reviewed; cause-text-guarded=reviewed; message-no-echo=reviewed; readback-transport=reviewed; footer-reappearance=reviewed; no-receipt-shape-change=reviewed; procedure-rest-only=reviewed; prose-pins-kept=reviewed; human-authorship-preserved=reviewed; settings-file-shape=reviewed; settings-file-scope=reviewed; prose-matches-docs=reviewed; ledger-integrity=reviewed; version-surfaces=reviewed; runtime-digest-pins=reviewed; partial-run=reviewed; interceptor-drift=reviewed

Not checked: the Pashov x-ray, solidity-auditor and fizz passes, waived because the step ships no Solidity; live GitHub behaviour (a real gh pr create, a REST body read-back against github.com, a claude[bot] author, the cloud session footer), reached only through the suite's fake git and fake gh; whether Claude Code honours .claude/settings.json in any session, terminal or cloud, which no command in this round observes; the Hexaemeron suite on Python 3.9.6, run only on 3.12.13 under uv inside the Node 26 wrapper; the Interceptor repository, read only through the study's account of it; the run pull request's `## Carried forward` section, which the prose phase has not written yet

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | docs/how-to-help-shoggoth.md | The attribution paragraph said a `Generated with` or `Generated by` line "naming Claude Code, Codex or another host" is refused as a runtime-host byline, in the paragraph that sends Cursor, Windsurf and GitHub Copilot contributors to remove such lines by hand. `HOST_BYLINE_RE` reads claude, claude code, codex, chatgpt, copilot and gemini (code assist) only: driven through the expression this round, `Generated with Cursor`, `Generated by Windsurf`, `Generated with GitHub Copilot`, `Generated with Aider` and `Generated by Devin` pass, so the guide promised a refusal the controller does not make for the harnesses it names, and a contributor on one of them would trust a gate that lets the line through. | fixed in this commit: the sentence names the six hosts the expression reads and says a line naming any other host passes the gate and still has to go; HostGuidanceTests in test_fiat_skill.py reads the hosts the sentence names, drives each through HOST_BYLINE_RE with both verbs and pins that "or another host" is gone; on the parent prose its two tests fail with 3 assertion failures and 0 errors |
| S2-R1-02 | low | plugins/hexaemeron/skills/fiat/references/push-discipline.md | The `Read the body back` list called the `_Generated by [Claude Code](https://claude.ai/code/session_...)_` specimen "the session link a cloud or Remote Control session appends". The study withholds that identification: section 2 says it "does not claim" the #615 footer is the `sessionUrl` link rather than something the web product adds on its own, section 6 says the footer "has that shape", and INSTALL.md in this same step says only that it "matches the documented session link and no other documented default"; the procedure stated as fact what the source-bound study records as unverified and the guidance beside it hedges. | fixed in this commit: the bullet names the footer by where it was seen, pull request #615, and says it has the shape of the documented session link; a pin in BodyReadBackTests holds both phrases and fails on the parent prose with 1 assertion failure |

Leads not pursued: the nine controller ids (no-gate-relaxed, byline-widening-scope, no-host-identity-accepted, cause-text-guarded, message-no-echo, footer-reappearance, no-receipt-shape-change, human-authorship-preserved, runtime-digest-pins) are reviewed as unchanged since step 1: the 14-file step-2 diff touches none of hexctl.py, test_hexctl.py, host_identity_cases.py, tests/promise_machine_coverage.json, tests/test_contributors.py or scripts/contributors.py, and this round re-ran TestCommitVerification and TestPublicationBindings (40 OK), the root suite holding tests.test_contributors (399 OK on 3.12.13 and on 3.9.6), promise_machine.py check (clean: 14 plugin(s), 14 copy/copies) and coverage --check (clean: promises=71 coverage_rows=71 coverage_selected=71), and found the four HOST_ names in hexctl.py to be the three sets and the expression; every specimen the procedure lists was driven through HOST_BYLINE_RE: the terminal line with and without the robot emoji, the #615 footer with the literal `...` and with a real session id, and all 48 combinations of `Generated by`, `Generated with`, `Authored by` and `Co-authored by` with Claude, Claude Code, Codex, ChatGPT, Copilot and Gemini as bare text and as a Markdown link are refused, while `Generated with pytest`, `co-authored by a colleague`, the two provenance trailers, `Co-Authored-By: Claude Smith <claude.smith@example.com>` and the `Claude-Session:` trailer pass; the host trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` passes the expression and is refused by the co-author gate on its address, and a model-named trailer with a non-host address passes both gates, as at the base; `Generated with GitHub Copilot`, `Generated by GitHub Copilot`, `generated with Cursor`, `Generated by Windsurf`, `Generated with Aider` and `Generated by Devin` pass the expression and `Generated with Gemini CLI` is refused through its `gemini` prefix; present at the base and recorded in step 1 as a later run's widening under the ask-first tier, so S2-R1-01 corrects the prose to the expression rather than the expression to the prose; INSTALL.md defines the third default as both the `Claude-Session` commit trailer and the description link while its refusal sentence names only "the description's session link"; the sentence is accurate as written and the commit trailer passes the range gate unread (study assumption 9 and section 12), so it stays a carried-forward item rather than a guidance edit; push-discipline.md says "the `--json` forms go over GraphQL", and `gh pr view` goes over GraphQL with or without `--json`, so the sentence under-describes rather than misstates; the #495 observation traces to the study's section 2 account of pull request #615 and to the fiat-v5.25.1 row; not changed; the guidance names only the session link's suppression as "documented and not observed"; no default's suppression was observed from here, and the run's commits carry no host trailer because the repository rule was followed, which is not evidence the setting held; the study draws the same line; not changed; the live settings reference (380,320 bytes, downloaded this round) carries the `attribution.commit`, `attribution.pr` and `attribution.sessionUrl` sentences the study's section 2 quotes word for word, including "Claude Code adds the link as a `Claude-Session` trailer on commits and as a link in pull request descriptions", and the cloud page carries "To change settings for a cloud session, use environment variables or commit settings files to the repository"; this corroborates the quotes and observes no behaviour; INSTALL.md's "a cloud session, which the documentation says reads committed settings files" traces to that sentence, and its `sessionUrl` sentence restates the quoted one, which covers both the commit trailer and the description link, so neither over-claims; the read-back command is spelled `gh api repos/<owner>/<repo>/pulls/<n> --jq .body` and the section says why `--json` is not used; `.claude/settings.json` is 83 bytes ending in one LF, parses to the attribution object, and the three root tests assert whole-document equality, both key sets, `assertIs(False)` on sessionUrl and the single LF; nothing else in the repository reads it: hexctl.py has no `.claude` reference, tests/test_marketplace_prose.py skips `.claude` in its README sweep and plugin-surface walk, the Horos boundary holds no entry for it and `check .` still matches the tree, the phylax and ephoros `plugins tests` scopes and the Hypomnema argv exclude it, and git does not ignore it; the fiat-v5.27.1 row keeps revision `state-shape-validation` and digest `e413d6041edb34b3807a54019489605814a591f60547755f8f66f01830f643aa` byte-identical to v5.26.1, the step-2 diff moves only `Current version` in the frontmatter and adds the row, status `open` and the `Next Fiat job` line are unchanged, the evidence links resolve under the 4.5.0 Hypomnema, all eight refusal sites carry a CAUSE_ constant (hexctl.py lines 6245, 6290, 6452, 6468, 6473, 6610, 6620 and 6704), options B to E stand in the committed study's section 4, and ADR-016 is byte-identical to the base; the four manifests and `DELIVERY_PACKAGE_VERSIONS` read 1.6.3 and the SKILL.md frontmatter 5.27.1; test_fiat_skill.py runs 99 OK (96 plus the three new pins); BodyReadBackTests pins the new sentences flattened like the label pins, its placement test relies on each anchor occurring once, which holds, and it pins neither the GraphQL reason nor the footer-reappears sentence, as the label pins are partial too; INSTALL.md's ADR-016 link and how-to-help's `../INSTALL.md#claude-code` anchor (the `### Claude Code` heading) resolve; the Hexaemeron suite on the fixed tree ran 1307/1307 tests passed under the wrapper; the fixes commit changes one test file and two prose files, so the Elenchus verdict is over the parent overlay of test_fiat_skill.py, where the three new tests fail with 4 assertion failures and 0 errors, and no root-only guard is recorded; the five step-2 commits were run through the widened expression, the co-author gate and the trailer count: none matches and each carries the two trailers once; the audit commit's message quotes no specimen; the step's pull-request body must not quote a refused specimen verbatim, including the specimens in this record, because inspect_pull_request cannot tell a quotation from a byline; the first phylax and ephoros invocations reported one P000/E000 `unreadable` finding because the shell passed the fourteen changed paths as a single argument; the recorded exits are the reruns with explicit argv (clean, 0) and the runbook's `plugins tests` spelling (clean, 0); the plugin-root Hypomnema 4.4.0 reports the committed study's line 805 as expected, while the worktree 4.5.0 checker is clean on the runbook argv and on INSTALL.md; horos.py check . names the fiat-503 synopsis candidate it does not hard-classify and reports the boundary matches the tree; present at the base; the study's section 11 names `.elenchus/hexaemeron-unittest.json` while the runbook step names `.elenchus/fiat-host-byline-readback-step-2.json`; the runbook was used; the study's section 12 leads stand for the run pull request's carried-forward section: the Claude-Session trailer, the claude User login outside HOST_PR_LOGINS, the Interceptor's copied expression sharing the generated-with gap, and the unverified cloud footer suppression

## Step 2, round 2 -- 2026-08-26T10:11:14Z

Audit schema: fiat-audit-round/v2

Covered: no-gate-relaxed=reviewed; byline-widening-scope=reviewed; no-host-identity-accepted=reviewed; cause-text-guarded=reviewed; message-no-echo=reviewed; readback-transport=reviewed; footer-reappearance=reviewed; no-receipt-shape-change=reviewed; procedure-rest-only=reviewed; prose-pins-kept=reviewed; human-authorship-preserved=reviewed; settings-file-shape=reviewed; settings-file-scope=reviewed; prose-matches-docs=reviewed; ledger-integrity=reviewed; version-surfaces=reviewed; runtime-digest-pins=reviewed; partial-run=reviewed; interceptor-drift=reviewed

Not checked: the Pashov x-ray, solidity-auditor and fizz passes, waived because the step ships no Solidity; live GitHub behaviour (a real gh pr create, a REST body read-back against github.com, a claude[bot] author, the cloud session footer), reached only through the suite's fake git and fake gh; whether Claude Code honours .claude/settings.json in any session, terminal or cloud, which no command in this round observes; the form Claude Code gives the documented session link in a pull-request description, which the settings reference does not quote and which no cloud session was opened to observe; the full Hexaemeron suite on this exact tree, not rerun because no test input changed since round 1 ran it on the fixed tree (1307/1307 tests passed under the wrapper, 46 seconds before afd6e2f was committed from that tree) and its Elenchus overlay (1307 executed, 4 assertion failures, 0 errors, complete), and this round changes only the audit record and its synopsis, which no Hexaemeron test reads; the Hexaemeron suite on Python 3.9.6, run only on 3.12.13 under uv inside the Node 26 wrapper; the settings reference, not re-downloaded this round, so its corroboration of the study's section 2 quotes rests on round 1's copy; the Interceptor repository, read only through the study's account of it; the run pull request's `## Carried forward` section, which the prose phase has not written yet

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the nine controller ids (no-gate-relaxed, byline-widening-scope, no-host-identity-accepted, cause-text-guarded, message-no-echo, footer-reappearance, no-receipt-shape-change, human-authorship-preserved, runtime-digest-pins) are reviewed as unchanged since step 1: the range 9bf1846..afd6e2f touches none of hexctl.py, test_hexctl.py, host_identity_cases.py, test_issue_429_recovery.py, tests/promise_machine_coverage.json, tests/test_contributors.py or scripts/contributors.py, the three HOST_ frozenset blocks hash byte-identical to the base at 5489863 and the runbook's grep against that base prints 0, the four HOST_ names in hexctl.py are the three sets and the expression, and this round re-ran TestCommitVerification and TestPublicationBindings (40 OK), the root suite holding tests.test_contributors (399 OK on 3.12.13 and on 3.9.6), promise_machine.py check (clean: 14 plugin(s), 14 copy/copies) and coverage --check (clean: promises=71 coverage_rows=71 coverage_selected=71); the round-1 fixes were re-read against their status cells: HostGuidanceTests reads Claude, Claude Code, Codex, ChatGPT, Copilot and Gemini from the guide's sentence and each is refused with `Generated with` and `Generated by` (12 of 12, and 48 of 48 with `Authored by` and `Co-authored by` added, bare and as a Markdown link); `or another host` occurs nowhere in the guide, and the pin asserts `or another host, refused` absent and `a line naming any other host passes the gate and still has to go` present; the #615 bullet carries `the footer that reached pull request #615` and `it has the shape of the session link`; the head test file run against the parent prose at e25bd42, extracted with git archive rather than a worktree, fails 4 of 10 (two subtests and one test in HostGuidanceTests, one footer pin), which is the record's 3 and 1 and the round-1 Elenchus overlay's 4 assertion failures and 0 errors; the expression's negatives were re-run: `Generated with pytest`, `co-authored by a colleague`, the two provenance trailers, `Co-Authored-By: Claude Smith <claude.smith@example.com>` and the `Claude-Session:` trailer pass; the host trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` passes the expression and is refused by the co-author gate on its address; `Generated with Cursor`, `Generated by Windsurf`, `Generated with GitHub Copilot`, `Generated by GitHub Copilot`, `Generated with Aider` and `Generated by Devin` pass and `Generated with Gemini CLI` is refused through its `gemini` prefix, present at the base and recorded in step 1 as a later run's widening under the ask-first tier; the terminal line with and without the robot emoji and the #615 footer with the literal `...` and with a real session id are refused; INSTALL.md's `#### Attribution` and the guide say Fiat refuses the description's session link as a runtime-host byline; what the round drove through HOST_BYLINE_RE is the #615 footer form, refused through `Generated by [Claude Code]`, and a session link rendered without a generated-by or authored-by verb before a host name, a bare `https://claude.ai/code/session_...` URL or one after `Session:`, passes the expression; the settings reference does not quote the link's form, the study's section 1 draws the same conclusion ("The web session link is caught") and its section 4 requires the guidance to say Fiat refuses the three defaults by name either way, while its section 2 declines to identify the #615 footer with the sessionUrl link, so both sentences follow the source-bound study and the residue is that unverified identification; INSTALL.md's "matches the documented session link and no other documented default" drops the study's "Its shape" qualifier but sits between "documented and not observed" and "has not been checked from here"; not changed, recorded for the carried-forward section beside the unverified suppression; INSTALL.md defines the third default as both the `Claude-Session` commit trailer and the description link while its refusal sentence names only "the description's session link"; the sentence is accurate as written and the commit trailer passes the range gate unread (study assumption 9 and section 12), so it stays a carried-forward item rather than a guidance edit; push-discipline.md says "the `--json` forms go over GraphQL", and `gh pr view` goes over GraphQL with or without `--json`, so the sentence under-describes rather than misstates; the #495 observation traces to the study's section 2 account of pull request #615 and to the fiat-v5.25.1 row; not changed; the guidance names only the session link's suppression as "documented and not observed"; no default's suppression was observed from here, and the run's commits carry no host trailer because the repository rule was followed, which is not evidence the setting held; the study draws the same line; not changed; every other statement of host behaviour in the five prose files was traced again: INSTALL.md's session scope to the settings page's "Shared project settings" line and the cloud page's "commit settings files to the repository" sentence, its three per-key sentences to the `attribution.commit`, `attribution.pr` and `attribution.sessionUrl` quotes in section 2, its "every commit" and "every pull-request description" wording to section 1's observations, the five harnesses without a documented switch to assumption 10, and push-discipline.md's "the default line a terminal session adds" to section 1's "the terminal default"; the read-back command is spelled `gh api repos/<owner>/<repo>/pulls/<n> --jq .body`, the section says why `--json` is not used and that a failed read is not a clean body, and BodyReadBackTests pins those sentences flattened like the label pins, its placement test relying on each anchor occurring once, which holds; `.claude/settings.json` is 83 bytes ending in one LF, parses to the attribution object, is not ignored by git, is read by nothing in the repository (hexctl.py has no `.claude` reference), and the three root tests assert whole-document equality, both key sets, `assertIs(False)` on sessionUrl and the single LF, passing on both interpreters; the fiat-v5.27.1 row keeps revision `state-shape-validation` and digest `e413d6041edb34b3807a54019489605814a591f60547755f8f66f01830f643aa` byte-identical to v5.26.1, the step-2 diff of EVOLUTION.md moves only `Current version` and adds the row, status `open` and the `Next Fiat job` line are unchanged, tests.test_evolution_contract and tests.test_version_propagation pass on both interpreters, the four manifests and `DELIVERY_PACKAGE_VERSIONS` read 1.6.3 with no 1.6.2 left on those surfaces, the SKILL.md frontmatter reads 5.27.1, and ADR-016 hashes identical to the base; test_fiat_skill.py runs 99 OK, tests.test_marketplace_prose and tests.test_host_settings 23 OK, the four root modules 115 OK on both interpreters; Imprimatur scores 100/100 with no defects on the seven demo-path files, Brevitas exits 0 on push-discipline.md, Protasis is clean on both committed copies, audit_synopsis.py --check . shows 17 pairs at committed=match before this append, and git diff --check is silent over the range; horos.py check . now names two synopsis candidates it does not hard-classify, fiat-503 as at the base and this run's fiat-617 synopsis since the step-2 round-1 record grew it, and still prints `boundary matches the tree` at exit 0, so the boundary is not regenerated; the plugin-root Hypomnema 4.4.0 reports the committed study's line 805 as expected while the worktree 4.5.0 checker is clean on the runbook argv and on INSTALL.md, and the recorded exit is the worktree checker's; phylax and ephoros ran with the 16 changed paths as explicit argv and with the runbook's `plugins tests` spelling, all clean; the six commits on the stacked branch were run through the widened expression, the co-author gate and the trailer count: none matches, each names Shoggoth alone as co-author and carries the two trailers once, and each verifies against the contributor's SSH key; the message of the commit carrying this record was run through the same gates before it was made; the step's pull-request body must not quote a refused specimen verbatim, including the specimens in this log, because inspect_pull_request cannot tell a quotation from a byline; the study's section 11 names `.elenchus/hexaemeron-unittest.json` while the runbook step names `.elenchus/fiat-host-byline-readback-step-2.json`; the runbook was used in round 1 and no fix this round called for Elenchus; the study's section 12 leads stand for the run pull request's carried-forward section: the Claude-Session trailer, the claude User login outside HOST_PR_LOGINS, the Interceptor's copied expression sharing the generated-with gap, and the unverified cloud footer suppression
