## Step 1, round 1 -- 2026-09-06T14:11:30Z

Audit schema: fiat-audit-round/v2

Covered: unattributed-admission=reviewed; rule-dangling=reviewed; attribution-on-draft=reviewed; attribution-drafted=reviewed; attribution-clobber=reviewed; prior-set-migration=reviewed; counts-by-attribution=reviewed; workbook-prose=reviewed; cap-exhaustion=reviewed; partial-write=reviewed; path-traversal=reviewed; subprocess-and-network=reviewed; target-repository-write=reviewed; evidence-digest-binding=reviewed; disposition-closure=reviewed; repository-pins=reviewed

Not checked: regeneration of the three committed evidence records from the pinned inputs, because `DOKIMASIA_PINNED_APP` and `DOKIMASIA_PINNED_WORKBOOK` are not set in this session; their digests were verified instead by `scripts/demonstrations.py check --root .` at exit 0 and by `scripts/check_public_front_door.py` at exit 0. The 21 selection reports were not read one by one; the generator regenerated them and the design record byte for byte with `git diff --exit-code` at exit 0. The fixture builder was read by grep rather than line by line; `test_the_committed_fixtures_regenerate_from_the_builder` holds every committed fixture byte-equal to its output. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run, and the overlay resolver was not consulted because it resolves nothing for a waived suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `propose()` in `plugins/dokimasia/scripts/dokimasia_lib/propose.py` rebuilds the set from `schema`, `inventory_sha256`, `workbook_sha256` and `dispositions` only, so a regeneration drops the set-level `rules` table while keeping every confirmed entry, and the regenerated set then refuses in `reconcile()` with `confirmed disposition on 'case:ADM-01' names rule 'row-owner-walks-it', which the rules table does not hold`; reproduced against the `closed.json` fixture with 15 preserved, 0 replaced. Nothing is counted wrongly and nothing is written by `demonstrate`, which never calls `propose`, but the reviewer's stated-rule text would be lost from a file `propose --existing` rewrote. Runbook step 2, "Carry attribution through a proposal regeneration", owns this by title and ADR-003's Consequences state that the proposal surface owes it in the step after this one, so it is not a step 1 defect and is carried forward rather than fixed here. Also not pursued: `by_person` keys are the strings the entries carry, so two spellings of one name differing by whitespace or case count as two people; `dispositions-v1.json` bounds `confirmed_by`, `stated_by`, `text` and `rule` by `maxLength` in characters while the reconciler bounds them in UTF-8 bytes, so the reconciler is the stricter gate for a non-ASCII value and the schema alone would admit a 128-character name over 128 bytes; `_check_rules` calls `rule_id.encode` on each table key, which a JSON source always makes a string and a Python caller might not; and `hypomnema.py` reports H009 on `DEMONSTRATION.md:80` for `adr/govern-real-data-demonstrations-separately` only when the changed documents are passed without `docs/decisions` in the index, a line this step does not touch and that resolves to `docs/decisions/ADR-084-govern-real-data-demonstrations-separately.md` with `docs/decisions` supplied, at exit 0. Evidence for the covered concerns: `phylax.py` and `ephoros.py` exit 0 over all 83 paths of the step diff at `08ed9a9ca4fe98ad027e01a271432cf220a65578`, and `hypomnema.py` exits 0 over the seven changed Markdown documents with `docs/decisions` supplied; the root suite reports 1623 of 1623 OK at exit 0 in 217.5 s; `run_checks.py --scope dokimasia` is green with the dokimasia suite, and the three lints, all passed; `design_evidence.py --transition design-lock`, `promise_machine.py coverage --check` at 98 of 98, `demonstrations.py check` at 20 constructed and 1 absent, `check_public_front_door.py` with 4 checked cards, and the Elenchus runner `reconcile --check --candidate rule-table --criterion unattributed-confirmation-refused` writing a `protasis-design-report/v1` with `value` true all exit 0; `horos.py scan . --write` and `scan . --census --write` leave the step tree unchanged. `unattributed-admission`, `rule-dangling` and `attribution-on-draft` are answered by `_check_attribution` and `_check_rules` raising before `assigned`, `by_person`, `applied` or `individual` is touched, each refusal naming the item and the field, and by the eleven refusal tests plus the command-line case asserting exit 2, no traceback and no report file. `attribution-drafted` is answered by `grep` finding neither `confirmed_by` nor `rule` written anywhere under `plugins/dokimasia/scripts` outside `reconcile.py`, with `draft_entry` emitting exactly `item`, `disposition`, `reason`, `oracle`, `confirmed` false and `proposed_sha256`. `prior-set-migration` is answered by `prior-shape.json` refusing on its first confirmed entry naming the item and `'confirmed_by'`, and by the pinned set carrying no default: 261 entries, 202 confirmed, all 202 naming `Laurence Day` and `row-author-owns-walking-it`, the 59 unconfirmed carrying neither field. `counts-by-attribution` is answered by the block being built in the one existing pass, by `check()` and the tests reconciling `people`, `by_person`, `individual` plus `applied` against `disposed` on both the closed and mixed fixtures, and by `coverage_digest` hashing the whole record so a renamed confirmer changes it. `workbook-prose` is answered by the extended test asserting the rule text is present, matches the set, is under 200 characters and that the five column names are absent. `cap-exhaustion` is answered by the four caps being keyword parameters of `reconcile` with module defaults, reported in `caps`, each measured in UTF-8 bytes, with fixtures at 128, 64, 512 and 256 admitted and one over each refused. `partial-write`, `path-traversal`, `subprocess-and-network` and `target-repository-write` are reviewed as unchanged surface: `reconcile.py` adds no I/O, no import beyond `re`, and `dokimasia.py` is not in the diff. `evidence-digest-binding` is answered by `_check_currency` running before `_check_rules` and by the regenerated stale fixtures. `disposition-closure` is answered by the pinned record closing at 202 over 261 with `covered` zero and by the mixed fixture's numerator equalling its attributed confirmations. `repository-pins` is answered by the two schema digests in `tests/promise_machine_coverage.json`, the four DEMONSTRATION.md source digests and the README marker all checking clean. The committed study and runbook differ from `.hexaemeron/study.md` and `.hexaemeron/runbook.md` on exactly one line each, the bold `Next Fiat job.` marker written as the plain words `Next Fiat job` because `test_marketplace_prose` admits the marker only in plugin landing READMEs, and the committed design record is identical to `.hexaemeron/design-evidence.json`. No private repository, branch or path appears in any added line of the diff.

## Step 2, round 1 -- 2026-09-06T15:18:17Z

Audit schema: fiat-audit-round/v2

Covered: unattributed-admission=reviewed; rule-dangling=reviewed; attribution-on-draft=reviewed; attribution-drafted=reviewed; attribution-clobber=reviewed; prior-set-migration=reviewed; counts-by-attribution=reviewed; workbook-prose=reviewed; cap-exhaustion=reviewed; partial-write=reviewed; path-traversal=reviewed; subprocess-and-network=reviewed; target-repository-write=reviewed; evidence-digest-binding=reviewed; disposition-closure=reviewed; repository-pins=reviewed

Not checked: the pinned set regenerating byte-identical from its own inputs, because `DOKIMASIA_PINNED_APP` and `DOKIMASIA_PINNED_WORKBOOK` are not set in this session and no checkout or workbook was found under the home directory; `test_the_pinned_set_regenerates_against_its_own_inputs_byte_for_byte` skips with a message naming both variables, so the 261 scoped, 202 preserved, 202 attributed, 59 replaced and 1 rule row it asserts are the step's claim, not this round's observation. The pinned set is not in the diff and its digests check clean through `scripts/demonstrations.py check --root .` at exit 0. `regeneration-input.json` was read by shape rather than line by line: 14 entries, 4 attributed, a 2-row table, taken from the command-line probe below, and the root suite holds every committed fixture byte-equal to the builder's output. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run, and the overlay resolver was not consulted because it resolves nothing for a waived suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `propose()` carries a confirmed entry whose `confirmed_by` is blank, a confirmed entry naming a `rule` with no `confirmed_by`, and a `rules` table over the reconciler's 256-row cap, probed at 302 rows, and writes each; the reconciler then refuses the regenerated file by name, none of the three shapes is created by `propose`, the study and `dispositions-v1.json` place those caps in the reconciler, and the copy is bounded by the 8 MiB `MAX_FILE_BYTES` read cap, so this is carried as the reviewer's own bytes rather than fixed; a `rule` of null is refused at the schema check before the write. `test_the_verb_reports_the_preserved_and_attributed_counts_on_stderr` writes `plugins/dokimasia/docs/evidence/regeneration-test-<pid>.dispositions.json` inside the tracked tree and unlinks it on cleanup, so a killed run leaves an untracked file that the dead-code check's clean-tree requirement reports until it is removed. `_moved` in `propose.py` and `moved` in `test_propose.py` are the same function. The one repository URL the privacy grep found is `dokimasia-study.md:211`, unchanged by this step. Evidence for the covered concerns: `phylax.py` and `ephoros.py` exit 0 over the eight changed non-Horos paths of the step diff at `92f0aea79cc3c19de4ebac87224e733d49367fd6`, and `hypomnema.py` exits 0 over the three changed Markdown documents with `docs/decisions` supplied; the root suite reports 1623 of 1623 OK at exit 0 in 220.0 s; `test_propose.py` runs 36 tests, 35 OK and 1 skipped, up from 24; `demonstrations.py check` at 4 real-data, 2 mixed, 20 constructed and 1 absent, and `check_public_front_door.py` with 4 checked cards, both exit 0; the program digest `ac322ddd1a79e7cb52b0c764506dd32f022c1cc3b12d53a4f68efc01a468d88d` in DEMONSTRATION.md matches `dokimasia.py` and the README marker carries `27b344d92ceec3caaf4ff55ea32e5f5d73e4c91d5df32631ace88584c7c71526`; the Elenchus runner `propose --check --candidate rule-table --criterion attribution-preserved-on-regeneration` writes a `protasis-design-report/v1` with `value` true at exit 0 to a scratch path, no fix having been made; `horos.py scan . --write` and `scan . --census --write` leave the step tree unchanged. A command-line probe against `closed.json` and an inventory with its last item removed exits 2 with `the existing set attributes 'route:src/app/markets/[marketId]/lenders/page.tsx' to a person and the item is no longer scoped, so the attribution cannot be carried forward; remove the entry by hand`, leaves the file byte-identical and no `.tmp` beside it; the same probe against `regeneration-input.json` exits 0 and reports `14 scoped; 4 preserved, of which 4 attributed; 10 replaced, 0 added, 1 dropped; rules table carried with 2 rows`. `unattributed-admission`, `rule-dangling` and `attribution-on-draft` are answered by `_carried_table` refusing a non-object table, `_check_carried` refusing a rule the carried table lacks, the unscoped-item branch refusing an attributed entry, and `_check_drafted` refusing an unconfirmed entry carrying either field, every one raised inside `propose()` before `write_set` is reached, and by the five refusal tests. `attribution-drafted` is answered by `DRAFT_FIELDS` being the six draft keys, `ATTRIBUTION_FIELDS` and `RULES_FIELD` being read from `reconcile_lib` constants, the source test stripping docstrings and comments and asserting neither quoted literal beside `covered`, the driven-branch test holding every drafted entry to exactly `DRAFT_FIELDS` across a first draft, a regeneration, a moved inventory and a dropped item, and a monkeypatched `draft_entry` adding either field being refused with `no draft may`. `attribution-clobber` is answered by the table being deep-copied through a JSON round trip and every attributed entry carried as the same object, compared canonically against the same and a moved inventory in `_check_attribution_carried` and the two byte-for-byte tests; the step-1 lead is closed by `test_a_regenerated_attributed_set_reconciles_to_its_confirmations` and the `closed` clause of the check regenerating `closed.json` with every entry preserved, 0 replaced, the table intact and `closure_ratio.closed` true. `prior-set-migration` is answered by a confirmed entry lacking `confirmed_by` being carried unchanged through `_touched` and never defaulted, for the reconciler to refuse by name. `counts-by-attribution` is answered by `attributed`, `rule_rows` and `rules_carried` joining the counts, `attributed` never exceeding `preserved`, and the stderr line printing both. `workbook-prose` is answered by `reconcile.py` and the coverage record being outside the diff. `cap-exhaustion` is answered by the existing caps and the lead above. `partial-write` is answered by `propose()` raising before `write_set`, which stages `.<name>.<pid>.tmp` and `os.replace`s it, and by the probe. `path-traversal` is answered by `SAFE_LABEL`, the `..` refusal and the symlink refusals being unchanged and no new path being built. `subprocess-and-network` is answered by `propose.py` importing `hashlib`, `json`, `os` and `pathlib` only; the `subprocess` in the test drives the verb. `target-repository-write` is answered by `propose` reading the two records and writing only under the evidence root. `evidence-digest-binding` is answered by the record carrying the digests of the records it was built from, so the moved inventory's regenerated set reconciles against the moved digest. `disposition-closure` is answered by the check asserting the regenerated set's numerator equals the attributed count and a fresh draft's equals zero. `repository-pins` is answered by the program digest, README marker, boundary and census all current. Attribution values in the diff's added lines are the builder's `Reviewer One` and `Reviewer Two`; `Laurence Day` and `row-author-owns-walking-it` appear only in the unchanged pinned set, and the test asserts counts alone.

## Step 3, round 1 -- 2026-09-06T16:33:43Z

Audit schema: fiat-audit-round/v2

Covered: unattributed-admission=reviewed; rule-dangling=reviewed; attribution-on-draft=reviewed; attribution-drafted=reviewed; attribution-clobber=reviewed; prior-set-migration=reviewed; counts-by-attribution=reviewed; workbook-prose=reviewed; cap-exhaustion=reviewed; partial-write=reviewed; path-traversal=reviewed; subprocess-and-network=reviewed; target-repository-write=reviewed; evidence-digest-binding=reviewed; disposition-closure=reviewed; repository-pins=reviewed

Not checked: regeneration of the pinned scrutiny from the pinned inputs, because `DOKIMASIA_PINNED_APP` and `DOKIMASIA_PINNED_WORKBOOK` are not set in this session; `test_the_committed_evidence_regenerates_from_the_pinned_inputs` skips naming both variables, so the 202 over 261, `people` 1, `applied` 202, `individual` 0 and the 281 ms observation are the step's claim, verified here only as far as the committed files carry them: the coverage record is byte-identical to step 2's, the committed scrutiny's `confirmations` equals the coverage record's, its `scrutiny_sha256` `3359490b2e2f848ab16b0bae3973fd8b7d7ae82ae0970d9270685d83ab684928` recomputes from its canonical bytes, and `demonstrations.py check --root .` and `check_public_front_door.py` exit 0 over the new digests. The 281 ms figure exists only in the step's commit message, since the committed scrutiny drops `timing` by design. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run, and the overlay resolver was not consulted because it resolves nothing for a waived suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: a rule `text` or `stated_by` holding `|` renders a malformed table row in "Who decided" that `prose_omissions` still accepts, because the same string is rendered and searched, and a `text` holding a newline followed by `## ` truncates the section so the committed check refuses its own regenerated prose by name; neither value is refused by `_check_rules` or `dispositions-v1.json`, which bound bytes only, no figure is misreported, and the pinned rule text carries neither, so this is carried rather than fixed. `_committed_evidence_failures` reads `record["confirmations"]`, `record["closure_ratio"]` and `record["counts"]` without a schema check first, so a hand-truncated coverage record would traceback from `demonstrate --check` rather than refuse by name; the two older reads predate this step. The "Current frontier" line in `plugins/dokimasia/skills/dokimasia/SKILL.md:29`, `README.md:8` and `AGENTS.md:4` still ends "What the record cannot say is who confirmed those 202 or under what rule", which the regenerated scrutiny now says; study section 12 places that move in the last step with the ledger row. `coverage-contract.md` names no scrutiny field and step 3's Files field does not list it, so no sentence was owed there. Evidence for the covered concerns: `phylax.py` and `ephoros.py` exit 0 over the nine changed non-Horos paths of the step diff at `a8e716ca3f39a171e9658dcfa048347471204172`, and `hypomnema.py` exits 0 over the three changed Markdown documents with `docs/decisions` supplied; the root suite reports 1623 of 1623 OK at exit 0 in 183.2 s; `test_demonstration.py` runs 49 tests, 48 OK and 1 skipped, up from 32; `run_checks.py --scope dokimasia` is green; `demonstrations.py check` at 4 real-data, 2 mixed, 20 constructed and 1 absent, `check_public_front_door.py` with 4 checked cards, and `promise_machine.py coverage --check` at 98 of 98 all exit 0; the Elenchus runner `demonstrate --check --candidate rule-table --criterion pinned-confirmations-attributed` writes a `protasis-design-report/v1` with `value` true at exit 0 to a scratch path, no fix having been made. `unattributed-admission` and `prior-set-migration` are answered end to end: `demonstrate --dispositions` over the `confirmed-without-person.json` and `prior-shape.json` fixtures exits 2 with `confirmed disposition on 'action:src/actions/update-market.ts' carries no 'confirmed_by' field, so nothing states whose judgement admits it` and the `guard:src/components/RequireAuth.tsx` equivalent, no traceback and no report file, because `_check_attribution` raises inside `reconcile()` before `by_person`, `applied` or `individual` is touched and `scrutinise()` copies `coverage["confirmations"]` only after `reconcile()` returns. `rule-dangling` and `attribution-on-draft` are answered by the unchanged `_check_rules` and `_check_attribution`, and `render_confirmations` lists a rule applied zero times rather than dropping it. `attribution-drafted` and `attribution-clobber` are answered by `propose.py` and `reconcile.py` being outside the diff, and by the byte-for-byte regeneration test on the attributed prose. `counts-by-attribution` is answered by the block being deep-copied through a JSON round trip into the scrutiny, by `canonical_bytes` excluding only `subject` and `timing` so the digest covers it, by `test_the_confirmations_are_inside_the_digest`, by the schema requiring the block with `additionalProperties` false, and by the committed check refusing a scrutiny whose block disagrees with its coverage. `workbook-prose` is answered by the section carrying only `people`, `individual`, rule id, `stated_by`, `applied` and `text`, and by the existing five-column-name test still passing. `cap-exhaustion` is answered by the schema bounding `text` at 512 and `stated_by` at 128 characters beside the reconciler's byte caps, with no new cap and no module-level value. `partial-write`, `path-traversal`, `subprocess-and-network` and `target-repository-write` are reviewed as unchanged surface: `demonstrate.py` adds no import, no I/O and no path, and `_committed_evidence_failures` reads the three paths it is handed through `read_json` and `read_text`. `evidence-digest-binding` is answered by the scrutiny keeping `coverage_sha256` and the check comparing it against the record beside it. `disposition-closure` is answered by the pinned `by_rule.row-author-owns-walking-it.applied` equalling the numerator 202 with `individual` 0. `repository-pins` is answered by the three DEMONSTRATION.md digests matching `455efc68d3f781a751c9d3b813d4f28e50942a930fe1775c527744239a31e600`, `199add2d972912ec249329c297eca0fefcb414857dd4a226320b75b861ababe5` and `01f7365dee6041badbf93f61d5b5eff9fbfee61f05d67e53d0b8d087c342f999`, the README marker at `e791905ea2c1d9bd1dfbb109cfe4caa896672482ebad1602a2a5fb5ad0b0dd69`, the `scrutiny-v1.json` binding re-pinned to `c530268f6c9d90b61a0d6ca14dcdf693df979ec7049ae8c53edb5e335bca1064`, the schema set at five, the observation and demo frontier lines unchanged, and the census current at the step head. No private repository, branch or path appears in any added line of the diff; the only name added to committed prose is `Laurence Day` in the regenerated pinned scrutiny.

## Step 4, round 1 -- 2026-09-06T17:25:31Z

Audit schema: fiat-audit-round/v2

Covered: unattributed-admission=reviewed; rule-dangling=reviewed; attribution-on-draft=reviewed; attribution-drafted=reviewed; attribution-clobber=reviewed; prior-set-migration=reviewed; counts-by-attribution=reviewed; workbook-prose=reviewed; cap-exhaustion=reviewed; partial-write=reviewed; path-traversal=reviewed; subprocess-and-network=reviewed; target-repository-write=reviewed; evidence-digest-binding=reviewed; disposition-closure=reviewed; repository-pins=reviewed

Not checked: regeneration of the pinned scrutiny from the pinned inputs, because `DOKIMASIA_PINNED_APP` and `DOKIMASIA_PINNED_WORKBOOK` are not set in this session, so the version move in the two evidence files is verified only as far as the committed bytes carry it: the diff touches `skill_version` and `scrutiny_sha256` in `wildcat-app-v2.scrutiny.json` and the `Skill version` line in `wildcat-app-v2-scrutiny.md` and nothing else, `wildcat-app-v2.coverage.json` and `wildcat-app-v2.dispositions.json` are byte-identical to step 3's, and `881ac25514daa197f3b54c00ba3ef904f5295f318386f091dcf403d3d812841d` recomputes from `canonical_bytes` over the record without its own digest field. The three pull requests the ledger row links, #1415, #1417 and #1419, resolve and are open, not merged, at audit time; the integration merge owns closing them. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run, and the overlay resolver was not consulted because it resolves nothing for a waived suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: `plugins/dokimasia/docs/evidence/wildcat-app-v2.scrutiny.json` and `wildcat-app-v2-scrutiny.md` moved with the version although step 4's Files field names only `DEMONSTRATION.md` and the root `README.md` marker for a `scripts/dokimasia.py` change; `demonstrate --check` requires the committed evidence to regenerate from the tree at `VERSION`, so the move is forced by the check the Exit field names, the DEMONSTRATION.md digests `eb0ad475c7015c4ac6f08ebf6ab1cec2bed1de125bb8e472fd4fc850519fb920`, `8b7a7190e54cb981680ff07cac426e25212d1454853d5d952f4186204a154fdd` and `df2b88ed9b06fb7dc6d0f5cc98b3322d87bbad1e8de4937ccbe2728f62276e35` and the README marker `0f2d1b7edbc86ca2bea2ce5f39b564ba8c563110c718487535920ff467484e1d` follow it, and the runbook is controller-pinned, so this is an omission in the Files field rather than a tree defect and is carried. The `SKILL.md` frontmatter description still opens "Inspect or build the scaffold that compiles", unchanged since `67a01a6c` at `dokimasia-v0.1.0`; the term is kept by the `dokimasia-scaffold-identity` promise and `test_scaffold.py`, and the Files field names the metadata version, the marketplace block and the closure promise rather than the description, so it is carried under the cold-read obligation for the next frontier run to judge. `.agents/plugins/marketplace.json` gains a `version` key for `dokimasia` where 14 of 18 entries carry none and only `hexaemeron`, `sapheneia` and `ariadne` carried one before; `test_the_version_agrees_across_every_surface_that_states_it` now reads it, and `tests/test_version_propagation.py` passes, so it is consistent with the three that state one. `test_the_long_description_refuses_to_promise_a_verdict` lowers its first assertion to a case-insensitive match and adds three, so no assertion was dropped. The step-3 lead is closed: the sentence "What the record cannot say is who confirmed those 202 or under what rule" is absent from `README.md`, `AGENTS.md` and `SKILL.md`, and the `Current frontier` text hashes to `cb4392dff6141a0f079db74d9183177de4c773d40f891864a395873e6b6dd3cb` on all three surfaces and appears once in the ledger header. Evidence for the covered concerns: `phylax.py` and `ephoros.py` exit 0 over the 15 changed non-Horos paths of the step diff at `63da0e3e577bc48afc68c9413a7098b4adcb93e3`, and `hypomnema.py` exits 0 over the seven changed Markdown documents with `docs/decisions` supplied; the root suite reports 1623 of 1623 OK at exit 0 in 177.5 s; the step's Exit chain exits 0 in order: `run_checks.py` over the four scopes green with 12 checks passed, the three repository modules 51 of 51 OK, `promise_machine.py coverage --check` at 98 of 98, the Elenchus runner `selftest --candidate rule-table --criterion frontier-row` writing a `protasis-design-report/v1` with `value` true to a scratch path, no fix having been made, and the four demo commands from study section 1, `reconcile --check`, `propose --check`, `demonstrate --check` and `run_checks.py --scope dokimasia`, each at exit 0; `test_scaffold.py` runs 40 tests, up from 37; `demonstrations.py check` at 20 constructed and 1 absent and `check_public_front_door.py` with 4 checked cards exit 0. `repository-pins` is answered by the ledger row `dokimasia-v3.1.0` on the `evolution` axis at revision `browser-observed-dispositions`, whose digest `35764aae6bec3e62f85cb9ac496f03c34a56e000efc7846cdfa737b9c0f32c15` recomputes from the canonical `open|browser-observed-dispositions|{current frontier}|{next Fiat job}` line with its final newline, by the counter moving 2.1.0 to 3.1.0 with generation and epoch retained, by `ADR-003`, the scrutiny and the attribution design record resolving from the row, by `3.1.0` read from both host manifests, both marketplace entries, the skill metadata, `scripts/dokimasia.py`, `test_scaffold.py` and `test_version_propagation.py` with no `2.1.0` left outside the ledger history, and by the census current at the step head. The README `Next Fiat job` line reads `Use /hexaemeron:fiat to drive a browser` followed by the fixed cold-read sentence, its five acceptance clauses in the ledger each name a check a test can make, and the next job's direction is stated as the maintainer's of 6 September 2026 with the programme note and surveys named only as held by the maintainer; no private repository, branch or path appears in any added line. The cold-read edits describe the tree as it is: the README's "does not run today" paragraph, the `AGENTS.md` "does not yet do" section and the Codex `longDescription` and `defaultPrompt` drop the scaffold-era claims that no verb was built, keep "reads a checkout and never writes to it", "runs no browser, holds no signing key and reaches no chain" and "never mark an item covered on its own", and add that a name is a claim the set makes which nothing verifies, and the `dokimasia-disposition-closure` promise now names `confirmed_by`, the `rules` table and the `confirmations` block in its Promise, Evidence, Boundary and Refuses. `unattributed-admission`, `rule-dangling`, `attribution-on-draft`, `attribution-drafted`, `attribution-clobber`, `prior-set-migration`, `counts-by-attribution`, `workbook-prose`, `cap-exhaustion`, `partial-write`, `path-traversal`, `subprocess-and-network`, `target-repository-write`, `evidence-digest-binding` and `disposition-closure` are reviewed as unchanged surface: the only executable change is `VERSION` in `scripts/dokimasia.py`, `reconcile.py`, `propose.py` and `demonstrate.py` are outside the diff, and the pinned scrutiny still reports `people` 1, `by_person` 202, `by_rule.row-author-owns-walking-it.applied` 202 and `individual` 0 over 202 of 261.
