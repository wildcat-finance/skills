## Step 1, round 1 -- 2026-08-30T16:08:52Z

Audit schema: fiat-audit-round/v2

Covered: over-deletion=reviewed; historical-record-edit=reviewed; readme-gap-filled=reviewed; readme-lint-regression=reviewed; adr-number-collision=reviewed; adr-039-deleted=reviewed; boundary-staleness=reviewed; baseline-publication-shape=reviewed; mirror-drift=reviewed; outcome-misread=reviewed; tmpdir-symlink=reviewed; generated-artefact-orphan=reviewed

Not checked: the security suite. The controller waived it as `waived: the run removes prose, images, a builder script and its test; it adds no Solidity`, so x-ray, solidity-auditor and fizz did not run and this round establishes nothing about Solidity. Also not checked: whether removing the primer with no replacement is the right product decision, which `ADR-060` records and this round does not second-guess; the byte contents of the five deleted PNGs and two deleted PDFs; Git history, which was not rewritten and was read only for the fifteen tracked paths at base `840d8dd3596fd6394901ba85a693bea00c69bf25`; whether GitHub in fact drops the co-author attribution described in `S1-R1-01`, which is inferred from `git interpret-trailers --parse` output and GitHub's documented final-paragraph rule rather than observed on github.com; whether the split-trailer pattern extends past the 300 `origin/main` commits sampled; and the rendered appearance of `README.md` after the paragraph was removed, which was read as bytes and not viewed.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | commit `6dff9da9c9228b27fdb957fb8022499fe769969a`, message line 17 | A blank line sits between `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, so the two provenance lines form two paragraphs instead of one trailer block. `git interpret-trailers --parse` on that message returns only `Wildcat-Origin: shoggoth`. On `d34f8c1404e6219828ec5267005bad795b2a05f7`, where the two lines are adjacent, it returns both. Shoggoth's co-authorship is not a Git trailer on the implementation commit, and GitHub reads co-authors from the final paragraph. Both gates accept the commit because `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:10067` and `scripts/check_commit_identity.py:268` count exact whole lines rather than parsing a trailer block, so nothing surfaces it. | accepted, not fixed. Repair means recreating `6dff9da9`, which the controller has already receipted: `.hexaemeron/state.json` lists it under `steps[0].receipts.implement.verified_commits` and pins the step head at `d34f8c14`. A Warden fix commit on the audit branch cannot reach an earlier commit, and rewriting it would invalidate the implement receipt. The repair belongs to Fiat or to a separate issue. |

Leads not pursued: the twelve risks were each measured rather than argued. The deleted set is exactly fifteen paths, every one matching `child.or.a.golden.retriever|child_or_golden_retriever`, and `git ls-files` at the tip returns nothing on that pattern; `git diff --name-status 840d8dd3..d34f8c14` shows four modifications, three additions and fifteen deletions and no other path. The `README.md` diff removes base lines 29 to 32 and their trailing blank line and adds no line, confirmed by `grep '^+[^+]'` returning nothing; imprimatur `--strict` scores `README.md` 100.0 with zero defects. `ADR-039` is byte-identical to the base ref from `## Context` to end of file, and its new status block matches the two-paragraph superseded form used by `ADR-038`; `ADR-011` uses the older single-paragraph form, so both forms exist and the step chose the recent one. `ADR-060` is free: `origin/main` tops out at `ADR-058`, a sweep of all 86 `refs/remotes/origin` refs plus local heads finds `ADR-059` only on `origin/fiat/936-report-dead-code-baseline-staleness-instead` and `ADR-060` only on this step branch. The tracked study copy re-derives exactly: joining `.hexaemeron/study.md` lines 313 and 314 with a single space, then rewriting five `(../<name>/SKILL.md)` links to `(../../plugins/hexaemeron/skills/<name>/SKILL.md)`, produces `docs/remove-the-beginner-primer/study.md` byte for byte, so thirteen lines differ and no third edit rode in; `cmp .hexaemeron/runbook.md docs/remove-the-beginner-primer/runbook.md` is silent. Two independent sweeps of tracked content, one on the path patterns and one on the words `primer` and `beginner`, return the declared set and nothing else: nine surviving documents, four audit records (`fiat-700` by path, `fiat-390`, `fiat-557` and `fiat-774` by description), the `.agents/` mirror of `plugins/hexaemeron/docs/fiat-author-publisher-separation/runbook.md`, `plugins/hexaemeron/skills/fiat/references/prose-pass.md` with its generic use, and the run's own new files. `docs/ci-plugin-suite-gate/runbook.md:34` and `docs/fiat-sync-resolution-guard-runbook.md:25,33,37` still instruct a reader to regenerate primer sources and PDFs; both are historical delivery records outside this step's Files list, they carry no link to a deleted path, and correcting them was rejected as the history rewrite the study's non-goals exclude. Nothing live points at a deleted path: `tests/check-map-v1.json`, `tests/promise_machine_coverage.json`, `.github/workflows/`, `README.md`, `INSTALL.md`, `CONTRIBUTORS.md`, `AGENTS.md`, `SHOGGOTH.md`, `PROMISE_MACHINE.md`, `FUTUREPROOFING.md`, `skills.sh.json` and every plugin and marketplace manifest name none of the fifteen. No check-map scope emptied: resolving all tracked paths by longest matching prefix leaves `repo-lints` at zero, and it was already zero at the base ref because it is a whole-tree scope owning no path; `docs` falls 250 to 242 and `root` 209 to 205, both arithmetically accounted for by the deletions and additions, and no owner prefix now matches nothing. No promise binding lost its subject: `coverage --check` reports promises 80, coverage rows 80, selected 80. No generated artefact was orphaned: each of the seven surviving `docs/assets` images and two surviving `docs/pdf` documents is still named by at least one tracked text file. The boundary carries 128 entries and `bytes_binary` 43,883,426 as the amended runbook requires, `files_walked` measures 2,120 against the study's superseded prediction of 2,117, and `horos.py check .` prints `boundary matches the tree`. `dead_code.py baseline --check` reports `status matched` with source `6dff9da9`, and `git diff --name-only HEAD~1..HEAD` at the step tip named `.dead-code/baseline.json` alone. This round's own record, its synopsis and the rescanned boundary form a third commit on the audit branch, so that one-path assertion no longer holds at the audit tip by construction. That commit also makes the baseline stale, not because the added paths change the dead-code universe, which is Python and leaves Markdown and JSON out of it, but because the baseline pins the commit whose tree it describes and any later commit moves past it; `dead_code.py baseline --check` then reports `baseline is stale; source changed after publication` naming those three paths, and exits 0 because `#936` made staleness report-only. So the baseline does need republishing again after any commit that follows its publication, and this round republishes it alone in its last commit, the shape the implementation used at `d34f8c14`, after which `baseline --check` reports matched at the audit tip. `promise_machine.py check`, `promise_machine.py coverage --check`, `portable_promise_machine.py check`, `audit_synopsis.py --check .` and `git diff --check` all exit 0, and the portable mirror needed no action because its source was left unchanged. `TMPDIR` was `/private/tmp/fiat-975` throughout, which has no symlinked parent, so no suite result here is a path artefact. `run_checks.py --base 840d8dd3596fd6394901ba85a693bea00c69bf25 --jobs 2 --format json` reports `outcome` `green` read out of the JSON record rather than from exit status, with `failure_classes` empty and all five selected checks passed: `dead-code-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax` and `root-suite`. `python3 -m unittest discover -s tests` reports `Ran 765 tests` and `OK`, against 765 and one `setUpClass` error at entry; the absent `deterministic-rebuild` failure is the subject of the issue and is not treated as a finding. Phylax, Ephoros and Hypomnema each exit 0 across the full tree, protasis passes both tracked copies, and hypomnema on `docs/remove-the-beginner-primer/study.md` is clean, which is the whole reason the derived copy exists. Both step commits carry a good GPG signature, author `Shoggoth <shoggoth@wildcat.finance>`, and exactly one of each provenance line. `S1-R1-01` is not unique to this run: over the last 300 `origin/main` commits, 227 carry the exact co-author line and 33 of those fail to parse it as a trailer, so the systemic fix is a trailer-block check in `verify_local_commit` and `check_commit_identity.py` rather than a repair to this step, and that belongs in its own issue. Issues `#944`, `#939`, `#950`, `#962` and `#936` were named out of scope and were not chased.
