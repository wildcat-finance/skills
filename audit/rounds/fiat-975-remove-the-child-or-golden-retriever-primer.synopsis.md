Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-975-remove-the-child-or-golden-retriever-primer.md | source_sha256=a7a61f75093a3e6e11bfef11989ccd514b19631a990524a759992255d094714c | h2_count=2
## Step 1, round 1 -- 2026-08-30T16:08:52Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: over-deletion=reviewed; historical-record-edit=reviewed; readme-gap-filled=reviewed; readme-lint-regression=reviewed; adr-number-collision=reviewed; adr-039-deleted=reviewed; boundary-staleness=reviewed; baseline-publication-shape=reviewed; mirror-drift=reviewed; outcome-misread=reviewed; tmpdir-symlink=reviewed; generated-artefact-orphan=reviewed<br><br>Not checked: the security suite. The controller waived it as `waived: the run removes prose, images, a builder script and its test; it adds no Solidity`, so x-ray, solidity-auditor and fizz did not run and this round establishes nothing about Solidity. Also not checked: whether removing the primer with no replacement is the right product decision, which `ADR-060` records and this round does not second-guess; the byte contents of the five deleted PNGs and two deleted PDFs; Git history, which was not rewritten and was read only for the fifteen tracked paths at base `840d8dd3596fd6394901ba85a693bea00c69bf25`; whether GitHub in fact drops the co-author attribution described in `S1-R1-01`, which is inferred from `git interpret-trailers --parse` output and GitHub's documented final-paragraph rule rather than observed on github.com; whether the split-trailer pattern extends past the 300 `origin/main` commits sampled; and the rendered appearance of `README.md` after the paragraph was removed, which was read as bytes and not viewed.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | commit `6dff9da9c9228b27fdb957fb8022499fe769969a`, message line 17 | A blank line sits between `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`, so the two provenance lines form two paragraphs instead of one trailer block. `git interpret-trailers --parse` on that message returns only `Wildcat-Origin: shoggoth`. On `d34f8c1404e6219828ec5267005bad795b2a05f7`, where the two lines are adjacent, it returns both. Shoggoth's co-authorship is not a Git trailer on the implementation commit, and GitHub reads co-authors from the final paragraph. Both gates accept the commit because `plugins/hexaemeron/skills/fiat/scripts/hexctl.py:10067` and `scripts/check_commit_identity.py:268` count exact whole lines rather than parsing a trailer block, so nothing surfaces it. | accepted, not fixed. Repair means recreating `6dff9da9`, which the controller has already receipted: `.hexaemeron/state.json` lists it under `steps[0].receipts.implement.verified_commits` and pins the step head at `d34f8c14`. A Warden fix commit on the audit branch cannot reach an earlier commit, and rewriting it would invalidate the implement receipt. The repair belongs to Fiat or to a separate issue. |<br><br>Leads not pursued: the twelve risks were each measured rather than argued. The deleted set is exactly fifteen paths, every one matching `child.or.a.golden.retriever|child_or_golden_retriever`, and `git ls-files` at the tip returns nothing on that pattern; `git diff --name-status 840d8dd3..d34f8c14` shows four modifications, three additions and fifteen deletions and no other path. The `README.md` diff removes base lines 29 to 32 and their trailing blank line and adds no line, confirmed by `grep '^+[^+]'` returning nothing; imprimatur `--strict` scores `README.md` 100.0 with zero defects. `ADR-039` is byte-identical to the base ref from `## Context` to end of file, and its new status block matches the two-paragraph superseded form used by `ADR-038`; `ADR-011` uses the older single-paragraph form, so both forms exist and the step chose the recent one. `ADR-060` is free: `origin/main` tops out at `ADR-058`, a sweep of all 86 `refs/remotes/origin` refs plus local heads finds `ADR-059` only on `origin/fiat/936-report-dead-code-baseline-staleness-instead` and `ADR-060` only on this step branch. The tracked study copy re-derives exactly: joining `.hexaemeron/study.md` lines 313 and 314 with a single space, then rewriting five `(../<name>/SKILL.md)` links to `(../../plugins/hexaemeron/skills/<name>/SKILL.md)`, produces `docs/remove-the-beginner-primer/study.md` byte for byte, so thirteen lines differ and no third edit rode in; `cmp .hexaemeron/runbook.md docs/remove-the-beginner-primer/runbook.md` is silent. Two independent sweeps of tracked content, one on the path patterns and one on the words `primer` and `beginner`, return the declared set and nothing else: nine surviving documents, four audit records (`fiat-700` by path, `fiat-390`, `fiat-557` and `fiat-774` by description), the `.agents/` mirror of `plugins/hexaemeron/docs/fiat-author-publisher-separation/runbook.md`, `plugins/hexaemeron/skills/fiat/references/prose-pass.md` with its generic use, and the run's own new files. `docs/ci-plugin-suite-gate/runbook.md:34` and `docs/fiat-sync-resolution-guard-runbook.md:25,33,37` still instruct a reader to regenerate primer sources and PDFs; both are historical delivery records outside this step's Files list, they carry no link to a deleted path, and correcting them was rejected as the history rewrite the study's non-goals exclude. Nothing live points at a deleted path: `tests/check-map-v1.json`, `tests/promise_machine_coverage.json`, `.github/workflows/`, `README.md`, `INSTALL.md`, `CONTRIBUTORS.md`, `AGENTS.md`, `SHOGGOTH.md`, `PROMISE_MACHINE.md`, `FUTUREPROOFING.md`, `skills.sh.json` and every plugin and marketplace manifest name none of the fifteen. No check-map scope emptied: resolving all tracked paths by longest matching prefix leaves `repo-lints` at zero, and it was already zero at the base ref because it is a whole-tree scope owning no path; `docs` falls 250 to 242 and `root` 209 to 205, both arithmetically accounted for by the deletions and additions, and no owner prefix now matches nothing. No promise binding lost its subject: `coverage --check` reports promises 80, coverage rows 80, selected 80. No generated artefact was orphaned: each of the seven surviving `docs/assets` images and two surviving `docs/pdf` documents is still named by at least one tracked text file. The boundary carries 128 entries and `bytes_binary` 43,883,426 as the amended runbook requires, `files_walked` measures 2,120 against the study's superseded prediction of 2,117, and `horos.py check .` prints `boundary matches the tree`. `dead_code.py baseline --check` reports `status matched` with source `6dff9da9`, and `git diff --name-only HEAD~1..HEAD` at the step tip named `.dead-code/baseline.json` alone. This round's own record, its synopsis and the rescanned boundary form a third commit on the audit branch, so that one-path assertion no longer holds at the audit tip by construction. That commit also makes the baseline stale, not because the added paths change the dead-code universe, which is Python and leaves Markdown and JSON out of it, but because the baseline pins the commit whose tree it describes and any later commit moves past it; `dead_code.py baseline --check` then reports `baseline is stale; source changed after publication` naming those three paths, and exits 0 because `#936` made staleness report-only. So the baseline does need republishing again after any commit that follows its publication, and this round republishes it alone in its last commit, the shape the implementation used at `d34f8c14`, after which `baseline --check` reports matched at the audit tip. `promise_machine.py check`, `promise_machine.py coverage --check`, `portable_promise_machine.py check`, `audit_synopsis.py --check .` and `git diff --check` all exit 0, and the portable mirror needed no action because its source was left unchanged. `TMPDIR` was `/private/tmp/fiat-975` throughout, which has no symlinked parent, so no suite result here is a path artefact. `run_checks.py --base 840d8dd3596fd6394901ba85a693bea00c69bf25 --jobs 2 --format json` reports `outcome` `green` read out of the JSON record rather than from exit status, with `failure_classes` empty and all five selected checks passed: `dead-code-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax` and `root-suite`. `python3 -m unittest discover -s tests` reports `Ran 765 tests` and `OK`, against 765 and one `setUpClass` error at entry; the absent `deterministic-rebuild` failure is the subject of the issue and is not treated as a finding. Phylax, Ephoros and Hypomnema each exit 0 across the full tree, protasis passes both tracked copies, and hypomnema on `docs/remove-the-beginner-primer/study.md` is clean, which is the whole reason the derived copy exists. Both step commits carry a good GPG signature, author `Shoggoth <shoggoth@wildcat.finance>`, and exactly one of each provenance line. `S1-R1-01` is not unique to this run: over the last 300 `origin/main` commits, 227 carry the exact co-author line and 33 of those fail to parse it as a trailer, so the systemic fix is a trailer-block check in `verify_local_commit` and `check_commit_identity.py` rather than a repair to this step, and that belongs in its own issue. Issues `#944`, `#939`, `#950`, `#962` and `#936` were named out of scope and were not chased.
## Step 1, round 2 -- 2026-08-30T16:52:03Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: over-deletion=reviewed; historical-record-edit=reviewed; readme-gap-filled=reviewed; readme-lint-regression=reviewed; adr-number-collision=reviewed; adr-039-deleted=reviewed; boundary-staleness=reviewed; baseline-publication-shape=reviewed; mirror-drift=reviewed; outcome-misread=reviewed; tmpdir-symlink=reviewed; generated-artefact-orphan=reviewed<br><br>Not checked: the security suite. The controller waived it as `waived: the run removes prose, images, a builder script and its test; it adds no Solidity`, so x-ray, solidity-auditor and fizz did not run and this round establishes nothing about Solidity. Also not checked: whether removing the primer with no replacement is the right product decision, which `ADR-060` records and this round does not second-guess; `#978`, the split trailer on `6dff9da9`, which round 1 filed and which this round was told not to re-raise or repair; whether the `#936` run halted on the three paths `ADR-060` names, taken from the study rather than re-derived from `origin/fiat/936-report-dead-code-baseline-staleness-instead`; the pixel counts, bounding boxes and 1672 by 941 canvas `ADR-060` quotes for the two mismatched PNGs, which describe deleted binaries this round cannot rebuild; whether a `git fetch` would move `origin/main` and change the `ADR-060` uniqueness result, since the sweep read the 90 refs already present; the rendered appearance of `README.md` and `ADR-060`, both read as bytes and not viewed; and the byte contents of the five deleted PNGs and two deleted PDFs.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | low | `docs/decisions/ADR-060-remove-the-beginner-primer-and-its-generator.md:45` | `ADR-060` commits to leaving every historical reference alone and puts the number at nine studies and runbooks. Ten cite the primer. Nine sit under `docs/`, the figure the study establishes at `docs/remove-the-beginner-primer/study.md:260`; the tenth is `plugins/hexaemeron/docs/fiat-author-publisher-separation/runbook.md:17`, which the study handles separately under *The reference the issue got wrong* and then places in the same class as the nine. Its `.agents/` mirror carries the same citation on an eleventh tracked path and is byte-identical to its source, confirmed by `cmp`. Every one of those files is in fact untouched by this step, so nothing dangles and no gate fails; the record's number is short, and the file it omits is the only citation outside `docs/`, which is the one a reader checking the promise would most easily miss. | fixed in this round: commit `fc3b476010073421fbd167018d65b7dd66f9a1e7` raises the count to ten, says nine of the ten sit under `docs/`, names the tenth and states that its `.agents/` mirror is byte-identical and needs no edit of its own. Hypomnema and imprimatur `--strict` on the edited record both stay clean, imprimatur scoring 100.0 with zero defects. |<br>| S1-R2-02 | low | `docs/remove-the-beginner-primer/runbook.md:62,211` | The runbook says to stage `.horos/boundary.json` into the commit after `git add`, lists `` `.horos/boundary.json`, by scan `` among the changed files, and closes with *No other tracked path is touched*. `horos.py scan . --write` writes two tracked manifests. This round's synopsis crosses the 16,384-byte `BLOB_MIN_BYTES` floor at `plugins/horos/skills/horos/scripts/horos.py:31`, so the scan adds it to `.horos/candidates.json` as a `blob` candidate with the evidence `mean line length above 400 in the first 4096 bytes` and raises `bytes_blob` from its committed 16,134,137. The synopsis byte count and the resulting `bytes_blob` total are deliberately not quoted: the synopsis is derived from this record, so writing either number into it changes that number, and both should be read from the committed `.horos/candidates.json`. Round 1's synopsis was 9,393 bytes and stayed under the floor, which is why this is the first round in the run to move that file. A Warden following the Files list literally leaves a tracked generated manifest dirty at the audit tip. Sibling runbooks state the convention the other way: `plugins/hexaemeron/docs/xray-source-reuse/runbook.md:50` and `plugins/alexandria/docs/release-statement-runbook.md:11` both name `.horos/boundary.json` and `.horos/candidates.json` together for the required staged-tree scan. | accepted, not fixed. This round commits `.horos/candidates.json` with its record, because the alternative is a tracked manifest that no longer describes the tree, and `plugins/horos/docs/scoped-entry/study.md:20` records that the file stays advisory and never binds an agent, so including it bypasses no gate; `horos.py check .` reports `boundary matches the tree` either way, so nothing surfaced this. The runbook cannot be repaired here: `docs/remove-the-beginner-primer/runbook.md` has to stay byte-identical to the receipted `.hexaemeron/runbook.md`, which the step's exit asserts with `cmp` and which a Warden fix commit cannot amend. The correction belongs to Fiat or to a separate issue. |<br><br>Leads not pursued: round 1 measured the deletion surface, so this round took the four openings it left. First, the added artefacts on their own terms. `ADR-060`'s checkable numbers hold: the fifteen deleted paths total 10,946,434 bytes and the seven deleted binaries 10,802,141, both read from `git ls-tree -r -l 840d8dd3`; the removed `README.md` paragraph carried exactly three links; `docs/fiat-integration-path-bound-study.md:134` sits inside the passage that records the CI half and calls the gap structurally invisible; and `hexctl.py:6820` requires each integration revalidation check to record exit 0 while `:6855` refuses a set that does not cover every affected path, which is what the record cites them for. Its causal claim about `docs/assets/` and `docs/pdf/` survives a check that could have broken it: `tests/check-map-v1.json` maps both directories to the `docs` scope, which declares no checks of its own, but `run_checks.py` prints `repo-lints` and `root` as consumers of `docs`, so a changed path under either directory selects `root-suite`, and `root-suite` was the check that could not pass while the primer test was in it. The tracked study and runbook copies are readable on their own: `protasis` passes both, `hypomnema` is clean on the study copy, and the runbook copy carries its own 2026-08-30 amendment declaring the two rewrites, so a reader who finds the derived study finds the explanation beside it. Second, indirect references. The repository has no docs index, decisions index, table of contents or site configuration to update, checked by `git ls-files` over `docs/*README*`, `docs/index*`, `docs/decisions/*README*`, `*mkdocs*` and `docs/*TOC*`. A full relative-link resolution over all 1,103 tracked Markdown files checked 1,584 links and found none broken that names the primer; of the 149 broken links in the tree, 43 sit outside the `.agents/` portable mirror, whose copies resolve at a different depth by design, and none of the 149 is in a file this step added or changed, the one apparent hit at `docs/remove-the-beginner-primer/study.md:313` being the literal text `[ADR-011](...)` inside a code span rather than a link. The five rewritten discipline links in the study copy all resolve to `plugins/hexaemeron/skills/<name>/SKILL.md`, and every remaining `README.md` link resolves. Every `docs/assets` and `docs/pdf` path named anywhere in `scripts`, `tests`, `plugins`, `.github`, `*.json` and `*.toml` exists, and each of the seven surviving images and two surviving PDFs is named at least once, so the deletion left no orphan and broke no reference in either direction. Third, `README.md` after the cut. The removed lines were a self-contained paragraph between the Interceptor sentence and the install instruction, so the surviving prose reads without an orphan heading, a dangling connective or a now-false sentence; imprimatur `--strict` scores it 100.0 with zero defects; and a sweep for `new here`, `start here`, `five.minute`, `quick.start`, `first.time reader` and `infographic` across `README.md`, `INSTALL.md`, `CONTRIBUTORS.md`, `AGENTS.md`, `SHOGGOTH.md`, `PROMISE_MACHINE.md`, `FUTUREPROOFING.md` and `docs/how-to-help-shoggoth.md` returns one unrelated line about cron intervals, so no surviving document points at the entry route the step deliberately left unspoken. Fourth, both manifests at the tip. `horos.py check .` prints `boundary matches the tree` with 128 entries, `bytes_binary` 43,883,426 and `files_walked` 2,122, the two above round 1's measured 2,120 being that round's own record and synopsis. `dead_code.py baseline --check` reports `status matched` with source `1f03eb24feb3d2f1108cbd62386bf1a1a5133485` at the round 1 tip. The two round 1 correction commits changed the record's bytes without rescanning the boundary and the boundary still matches, which is consistent: `audit/rounds/` carries no boundary entry. Fifth, whether anything deleted was load-bearing in a way a name grep would miss. The dead-code baseline falls from 439 findings to 437 and no longer contains the string `child_or_golden_retriever` anywhere, exactly the two deleted Python files and nothing else; no tracked file names `find_builder_python`, `deterministic primer`, `deterministic-rebuild` or `build_child`; `repo_contract.py` names neither the primer nor those directories; and no tracked file under `tests/` names `scripts/build_` at all, so nothing paired the deleted builder with a surviving fixture. `promise_machine.py check`, `coverage --check` at promises 80, coverage rows 80 and selected 80, `portable_promise_machine.py check`, `audit_synopsis.py --check .` and `git diff --check` all exit 0. The citing set is unchanged and unmodified: ten studies and runbooks, nine of them under `docs/` and the tenth at `plugins/hexaemeron/docs/fiat-author-publisher-separation/runbook.md`, plus four audit records, `fiat-700` by path and `fiat-390`, `fiat-557` and `fiat-774` by description, plus the byte-identical `.agents/` mirror and the generic use in `plugins/hexaemeron/skills/fiat/references/prose-pass.md`. `ADR-039` remains byte-identical to the base ref from `## Context` to end of file and its status link to `ADR-060` resolves; `ADR-060` is still the only file of that number across all 90 local and remote refs, and `ADR-058` is the highest other number in `docs/decisions`. Phylax, Ephoros and Hypomnema each exit 0 over their mapped scopes. `run_checks.py --base 840d8dd3596fd6394901ba85a693bea00c69bf25` was read from its `outcome` line rather than its exit status and reports `green` at `fc3b4760`, over 24 changed paths, with `dead-code-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax` and `root-suite` all passed; it was run twice, once before this round's fix and once after, green both times, and never while anything was writing the tree. `TMPDIR` was `/private/tmp/fiat-975` throughout, which has no symlinked parent. Elenchus returns `unguarded` on `fc3b4760` with the detail `the commit changed no test files`, which is the correct reading of a prose correction to a decision record: no test can guard a count in an ADR, and the verdict is evidence for this round rather than a defect in the fix. That commit carries a good GPG signature, author `Shoggoth <shoggoth@wildcat.finance>`, committer from configuration, and both provenance lines adjacent, so `git interpret-trailers --parse` returns `Co-authored-by` and `Wildcat-Origin` together and `#978`'s defect is not reproduced. The record commit that follows makes the baseline stale for the reason round 1 gave, so this round republishes it alone in a final commit. Issues `#978`, `#944`, `#939`, `#950`, `#962`, `#936` and `#976` were named out of scope and were not chased, and the `TMPDIR` symlink was treated as an environment constraint rather than a repository defect. Brevitas over the whole record reports `B010` at line 1, two section headings against a minimum of three; `B011` at line 11 and at line 27, round 1's table with one data row and this round's with two, both against a minimum of three by three; `B001` at line 12; and `B027` at line 32, two stacked qualifiers in this round's evidence paragraph against a maximum of one. Fiat's required structure gives each round exactly one heading and as many data rows as it has findings, and the qualifiers `B027` counts are caveats on evidence, so the record was not padded with a third heading, a third row or a dropped caveat to satisfy that shape.
