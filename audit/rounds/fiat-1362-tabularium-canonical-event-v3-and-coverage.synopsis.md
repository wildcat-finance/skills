Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1362-tabularium-canonical-event-v3-and-coverage.md | source_sha256=6f8df61db2b8484eb30bdd30bca4712b55645ea65374e84b1d5b7c67ddc57e81 | h2_count=2
## Step 1, round 1 -- 2026-09-19T04:37:37Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: nothing under `plugins/tabularium/scripts/` builds or verifies a schema 3 release at this head, so the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests were reviewed as absent rather than exercised; those belong to later steps. The v2 read path was checked by validating the shipped documents, not by a rejection fixture. `rebuild.py` temporary directories are unchanged by this step and were not re-reviewed. The prose sweep over `README.md`, `SKILL.md`, the dictionaries, the adapter guide, the release policy and `AGENTS.md` is step 4 and none of those files changed here. The Pashov pair did not run under the recorded waiver: the step ships Python, JSON Schema and Markdown and no Solidity. `docs/tabularium-schema-v3/design-probe.py` was read and run but its 25 committed reports were compared by bytes, not recomputed. What was completed: the three lints exit 0 over the 42 changed paths, before and after the fix; the root suite ran 2,049 tests OK with exit 0 at both `edad99508b505abf21dc4a57e8466fd6e390777f` and `60eaf491f91acce4ab893949150ec58de1ef58a7`; `python3 scripts/run_checks.py --scope tabularium` is green at both heads, with the Tabularium suite at 160 tests before the fix and 161 after; the step's own exit commands each exit 0 at the step head, namely `plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` reporting `tabularium: 0.3.3 -> 0.3.4`, both Protasis runs, Imprimatur at `--max-defects 0`, and `design_evidence.py .hexaemeron/design-evidence.json --transition step:1`; the fix moved the Tabularium tree above the step commit, so `plugin_release.py --base edad99508b505abf21dc4a57e8466fd6e390777f --head HEAD` demanded a package above `0.3.4` and the four version surfaces went to `0.3.5` in the same signed fixes commit.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `plugins/tabularium/tests/test_schemas.py` | The tuple-drift guard built its table from the fixed list `ADAPTER_MODULES = (aave_v4, euler_v1, euler_v2)` rather than from the `release_v2.ADAPTERS` registry the study names as the way a value enters the admitted vocabulary. Demonstrated at `edad99508b505abf21dc4a57e8466fd6e390777f`: with a fourth adapter registered in `release_v2.ADAPTERS` and absent from both v3 documents, all 17 `test_schemas` tests stayed green, so the enums and `oneOf` branches were unchecked for it. The table now reads the registry, and one new test, `test_every_registered_adapter_reaches_the_tuple_table_and_the_v3_schemas`, fails on that case naming the schema file, the field and the value, for example `canonical-event-v3.json: properties/venue/enum disagrees with the adapter tuple table; missing from schema: ['morpho-v1']`. | fixed in 60eaf491f91acce4ab893949150ec58de1ef58a7 |<br>| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |<br>| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |<br><br>Leads not pursued: `provenance.source_kind` is `{"type": "string", "minLength": 1}` in `canonical-event-v3.json` although each adapter emits one constant, `consensus-log`, `ethereum-json-rpc-log` and `hosted-indexer-event`, so a v3 event may name a venue and a source kind from different adapters; the runbook pins five provenance fields per `oneOf` branch and `test_event_v3_keeps_the_v2_envelope_outside_the_tuple_fields` deliberately holds this field equal to v2, so it is design rather than drift. The corrected v2 documents remain a cross-product over the tuple columns, as they were before the step, so `canonical-event-v2.json` admits `euler-v1` beside `adapter_version` `2.0.0`; only v3 binds the tuple, and the runbook asks v2 to admit what the v2 Python validator admitted rather than to bind it. `coverage-manifest-v3.json` accepts an empty `versions.mapping_rules` because `items` is vacuous on an empty array; the branch stays pinned by `evidence_class`, `protocol_generation`, `source_api` and `versions.adapter`, so no cross-branch ambiguity follows. `.agents/plugins/marketplace.json` is named in the step's Files field and carries no `version` key for `tabularium`, so no bump was owed there. No test pins the twelve v0 release digests at this head; `git diff --stat 1d131b98 edad9950 -- plugins/tabularium/examples` is empty, which is the evidence this round has. Negative results worth keeping: `plugins/tabularium/tests/run_tests.py` is byte-identical to `plugins/alexandria/tests/run_tests.py` apart from the docstring, so the phylax boundary it was modelled on is unweakened, with a worktree-bound root and dev/ino recheck, an `O_DIRECTORY|O_NOFOLLOW` directory walk, an `O_EXCL` create at mode 0600 and identity-checked cleanup; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` are byte-identical to their `.hexaemeron/` counterparts and the 25 committed reports match, with `.hexaemeron/study.md` at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17`, `.hexaemeron/runbook.md` at `0b8a2aadc24d7f757e48494127096ef1ddf1d7527dd850dc5c7e4dd4f728f08b` and the design record at `512a0b64fd17b5e46768e250dca7c9ace5f6c69febf4fc800c326313187cb595`; under `jsonschema` 4.25.1 both v3 documents pass `check_schema` and the `oneOf` rejected every mixed tuple tried, namely `aave-v4` with `adapter_version` `1.0.0`, with `source_api` `euler-v3`, with `adapter` `euler-v2`, with `protocol_generation` `euler-v1`, with `mapping_rule` `euler-v2.repay.v1`, `venue` `euler-v1` over aave provenance, `schema_version` 2 and an extra top-level key, while the coverage document rejected `adapter.version` `1.0.0`, `adapter.name` `euler-v2`, a swapped `evidence_class`, `source_api` or `protocol_generation`, a euler-v2 rule, `versions.event_schema` 2 and a three-item `known_gaps`; the corrected v2 documents admit every shipped row, 500 of 500 aave-v4 rows, 1 euler-v1 row, 2 euler-v2 rows and all three `coverage.json` manifests, where at `1d131b98` all 500 aave rows and the aave manifest were invalid and the euler rows were already valid; with `jsonschema` blocked by an import stub the Tabularium suite still ran 161 tests with 0 skips and 0 failures, so no parity test passes by skipping; and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/tabularium-schema-v3/study.md --design-evidence docs/tabularium-schema-v3/design-evidence.json --repo-root .` exits 0, binding the declared decision `superseding-releases` to `plugins/tabularium/skills/tabularium/EVOLUTION.md`.
## Step 1, round 2 -- 2026-09-19T04:54:09Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: the round-1 fix changed no production code, so nothing round 1 deferred became reachable: `git diff --name-only edad9950 e448a1e3 -- plugins/tabularium/schemas plugins/tabularium/scripts` is empty, and the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests are still absent rather than exercised. No Elenchus run was made this round because the round produced no fixes commit, so the verdict is null rather than the `inconclusive` S1-R1-03 records for a fix on this step. The subprocess census behind S1-R1-03 was read by grep over `plugins/tabularium/tests/*.py`, not by running the declared runner against each module. `plugins/tabularium/tests/run_tests.py` did not change between `edad9950` and `e448a1e3`, so its boundary was re-confirmed by that absence rather than re-read line by line. The 25 committed selection reports were compared by bytes, not recomputed, for the reason S1-R1-02 records. The Pashov pair did not run under the recorded waiver: no Solidity. The prose sweep remains step 4 and none of its files changed. What was completed: the three lints exit 0 over the nine paths changed between `edad9950` and `e448a1e3`; the root suite ran 2,049 tests OK with exit 0 at `e448a1e3406e9835ace53dab507b32d0bc0523b1`; `python3 scripts/run_checks.py --scope tabularium` is green with the Tabularium suite at 161 tests; `plugin_release.py` exits 0 against both `edad99508b505abf21dc4a57e8466fd6e390777f` (`tabularium: 0.3.4 -> 0.3.5`) and `1d131b98a78c888b571f302e5b4c899aa2e47caf` (`0.3.3 -> 0.3.5`); both Protasis runs, Imprimatur at `--max-defects 0`, `design_evidence.py .hexaemeron/design-evidence.json --transition step:1` and the Hypomnema study-mode bridge each exit 0; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree left the worktree clean, with `tests.test_demonstrations` passing 114 tests including `HorosCensusCurrencyTests`.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |<br>| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |<br><br>Leads not pursued: the S1-R1-01 fix binds `ADAPTER_MODULES` once at module import, while the new test reads `release_v2.ADAPTERS` live, so the two can diverge if an adapter is registered after the test module is imported. Both orders still catch the drift and neither is reachable in the tree, where `release_v2.ADAPTERS` is a module-level literal: registering a fourth adapter before the import fails 14 of 18 `test_schemas` tests, and registering it after the import fails exactly 1, the new test, with `the tuple table does not cover every adapter in release_v2.ADAPTERS` and the snapshot at 3 against a live registry of 4. Deriving the table inside `tuple_table()` would make the two orders identical; it was not pursued because no behaviour in the tree is wrong and the guard holds either way. S1-R1-03 names `tests/test_scaffold.py` as the subprocess caller; a grep of `plugins/tabularium/tests/*.py` shows four modules calling `subprocess`, also `test_builder_cli.py`, `test_release.py` and `test_euler_releases.py`, so the carried row understates the breadth without changing its finding, and the row is carried unedited. The round-1 leads still stand unchanged: free-string `provenance.source_kind` in `canonical-event-v3.json` against three per-adapter constants; the corrected v2 documents remaining a cross-product over the tuple columns; `coverage-manifest-v3.json` accepting an empty `versions.mapping_rules` with no cross-branch ambiguity; `.agents/plugins/marketplace.json` carrying no `version` key for `tabularium`; and no test pinning the twelve v0 digests. Negative results this round: `git diff --name-only 1d131b98 e448a1e3 -- plugins/tabularium/examples` returns zero files, so no `examples/*-v0/` byte moved across the step and its fix; the four version surfaces all read `0.3.5` and `tests.test_version_propagation` passes 8 tests; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` remain byte-identical to their `.hexaemeron/` counterparts and the 25 reports still match; under `jsonschema` 4.25.1 both v3 documents still pass `check_schema` and reject 8 of 8 mixed-tuple events and 8 of 8 mixed-tuple manifests; the corrected v2 documents still admit 503 of 503 shipped rows and all three `coverage.json` manifests; and with `jsonschema` blocked by an import stub the Tabularium suite still runs 161 tests with 0 skips and 0 failures.
