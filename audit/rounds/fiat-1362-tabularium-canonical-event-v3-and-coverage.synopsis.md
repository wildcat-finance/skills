Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1362-tabularium-canonical-event-v3-and-coverage.md | source_sha256=a24388026e5b1eb1dea119371594d8571d4ba50641bb359c89517582af619823 | h2_count=4
## Step 1, round 1 -- 2026-09-19T04:37:37Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: nothing under `plugins/tabularium/scripts/` builds or verifies a schema 3 release at this head, so the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests were reviewed as absent rather than exercised; those belong to later steps. The v2 read path was checked by validating the shipped documents, not by a rejection fixture. `rebuild.py` temporary directories are unchanged by this step and were not re-reviewed. The prose sweep over `README.md`, `SKILL.md`, the dictionaries, the adapter guide, the release policy and `AGENTS.md` is step 4 and none of those files changed here. The Pashov pair did not run under the recorded waiver: the step ships Python, JSON Schema and Markdown and no Solidity. `docs/tabularium-schema-v3/design-probe.py` was read and run but its 25 committed reports were compared by bytes, not recomputed. What was completed: the three lints exit 0 over the 42 changed paths, before and after the fix; the root suite ran 2,049 tests OK with exit 0 at both `edad99508b505abf21dc4a57e8466fd6e390777f` and `60eaf491f91acce4ab893949150ec58de1ef58a7`; `python3 scripts/run_checks.py --scope tabularium` is green at both heads, with the Tabularium suite at 160 tests before the fix and 161 after; the step's own exit commands each exit 0 at the step head, namely `plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` reporting `tabularium: 0.3.3 -> 0.3.4`, both Protasis runs, Imprimatur at `--max-defects 0`, and `design_evidence.py .hexaemeron/design-evidence.json --transition step:1`; the fix moved the Tabularium tree above the step commit, so `plugin_release.py --base edad99508b505abf21dc4a57e8466fd6e390777f --head HEAD` demanded a package above `0.3.4` and the four version surfaces went to `0.3.5` in the same signed fixes commit.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `plugins/tabularium/tests/test_schemas.py` | The tuple-drift guard built its table from the fixed list `ADAPTER_MODULES = (aave_v4, euler_v1, euler_v2)` rather than from the `release_v2.ADAPTERS` registry the study names as the way a value enters the admitted vocabulary. Demonstrated at `edad99508b505abf21dc4a57e8466fd6e390777f`: with a fourth adapter registered in `release_v2.ADAPTERS` and absent from both v3 documents, all 17 `test_schemas` tests stayed green, so the enums and `oneOf` branches were unchecked for it. The table now reads the registry, and one new test, `test_every_registered_adapter_reaches_the_tuple_table_and_the_v3_schemas`, fails on that case naming the schema file, the field and the value, for example `canonical-event-v3.json: properties/venue/enum disagrees with the adapter tuple table; missing from schema: ['morpho-v1']`. | fixed in 60eaf491f91acce4ab893949150ec58de1ef58a7 |<br>| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |<br>| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |<br><br>Leads not pursued: `provenance.source_kind` is `{"type": "string", "minLength": 1}` in `canonical-event-v3.json` although each adapter emits one constant, `consensus-log`, `ethereum-json-rpc-log` and `hosted-indexer-event`, so a v3 event may name a venue and a source kind from different adapters; the runbook pins five provenance fields per `oneOf` branch and `test_event_v3_keeps_the_v2_envelope_outside_the_tuple_fields` deliberately holds this field equal to v2, so it is design rather than drift. The corrected v2 documents remain a cross-product over the tuple columns, as they were before the step, so `canonical-event-v2.json` admits `euler-v1` beside `adapter_version` `2.0.0`; only v3 binds the tuple, and the runbook asks v2 to admit what the v2 Python validator admitted rather than to bind it. `coverage-manifest-v3.json` accepts an empty `versions.mapping_rules` because `items` is vacuous on an empty array; the branch stays pinned by `evidence_class`, `protocol_generation`, `source_api` and `versions.adapter`, so no cross-branch ambiguity follows. `.agents/plugins/marketplace.json` is named in the step's Files field and carries no `version` key for `tabularium`, so no bump was owed there. No test pins the twelve v0 release digests at this head; `git diff --stat 1d131b98 edad9950 -- plugins/tabularium/examples` is empty, which is the evidence this round has. Negative results worth keeping: `plugins/tabularium/tests/run_tests.py` is byte-identical to `plugins/alexandria/tests/run_tests.py` apart from the docstring, so the phylax boundary it was modelled on is unweakened, with a worktree-bound root and dev/ino recheck, an `O_DIRECTORY|O_NOFOLLOW` directory walk, an `O_EXCL` create at mode 0600 and identity-checked cleanup; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` are byte-identical to their `.hexaemeron/` counterparts and the 25 committed reports match, with `.hexaemeron/study.md` at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17`, `.hexaemeron/runbook.md` at `0b8a2aadc24d7f757e48494127096ef1ddf1d7527dd850dc5c7e4dd4f728f08b` and the design record at `512a0b64fd17b5e46768e250dca7c9ace5f6c69febf4fc800c326313187cb595`; under `jsonschema` 4.25.1 both v3 documents pass `check_schema` and the `oneOf` rejected every mixed tuple tried, namely `aave-v4` with `adapter_version` `1.0.0`, with `source_api` `euler-v3`, with `adapter` `euler-v2`, with `protocol_generation` `euler-v1`, with `mapping_rule` `euler-v2.repay.v1`, `venue` `euler-v1` over aave provenance, `schema_version` 2 and an extra top-level key, while the coverage document rejected `adapter.version` `1.0.0`, `adapter.name` `euler-v2`, a swapped `evidence_class`, `source_api` or `protocol_generation`, a euler-v2 rule, `versions.event_schema` 2 and a three-item `known_gaps`; the corrected v2 documents admit every shipped row, 500 of 500 aave-v4 rows, 1 euler-v1 row, 2 euler-v2 rows and all three `coverage.json` manifests, where at `1d131b98` all 500 aave rows and the aave manifest were invalid and the euler rows were already valid; with `jsonschema` blocked by an import stub the Tabularium suite still ran 161 tests with 0 skips and 0 failures, so no parity test passes by skipping; and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/tabularium-schema-v3/study.md --design-evidence docs/tabularium-schema-v3/design-evidence.json --repo-root .` exits 0, binding the declared decision `superseding-releases` to `plugins/tabularium/skills/tabularium/EVOLUTION.md`.
## Step 1, round 2 -- 2026-09-19T04:54:09Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: the round-1 fix changed no production code, so nothing round 1 deferred became reachable: `git diff --name-only edad9950 e448a1e3 -- plugins/tabularium/schemas plugins/tabularium/scripts` is empty, and the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests are still absent rather than exercised. No Elenchus run was made this round because the round produced no fixes commit, so the verdict is null rather than the `inconclusive` S1-R1-03 records for a fix on this step. The subprocess census behind S1-R1-03 was read by grep over `plugins/tabularium/tests/*.py`, not by running the declared runner against each module. `plugins/tabularium/tests/run_tests.py` did not change between `edad9950` and `e448a1e3`, so its boundary was re-confirmed by that absence rather than re-read line by line. The 25 committed selection reports were compared by bytes, not recomputed, for the reason S1-R1-02 records. The Pashov pair did not run under the recorded waiver: no Solidity. The prose sweep remains step 4 and none of its files changed. What was completed: the three lints exit 0 over the nine paths changed between `edad9950` and `e448a1e3`; the root suite ran 2,049 tests OK with exit 0 at `e448a1e3406e9835ace53dab507b32d0bc0523b1`; `python3 scripts/run_checks.py --scope tabularium` is green with the Tabularium suite at 161 tests; `plugin_release.py` exits 0 against both `edad99508b505abf21dc4a57e8466fd6e390777f` (`tabularium: 0.3.4 -> 0.3.5`) and `1d131b98a78c888b571f302e5b4c899aa2e47caf` (`0.3.3 -> 0.3.5`); both Protasis runs, Imprimatur at `--max-defects 0`, `design_evidence.py .hexaemeron/design-evidence.json --transition step:1` and the Hypomnema study-mode bridge each exit 0; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree left the worktree clean, with `tests.test_demonstrations` passing 114 tests including `HorosCensusCurrencyTests`.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |<br>| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |<br><br>Leads not pursued: the S1-R1-01 fix binds `ADAPTER_MODULES` once at module import, while the new test reads `release_v2.ADAPTERS` live, so the two can diverge if an adapter is registered after the test module is imported. Both orders still catch the drift and neither is reachable in the tree, where `release_v2.ADAPTERS` is a module-level literal: registering a fourth adapter before the import fails 14 of 18 `test_schemas` tests, and registering it after the import fails exactly 1, the new test, with `the tuple table does not cover every adapter in release_v2.ADAPTERS` and the snapshot at 3 against a live registry of 4. Deriving the table inside `tuple_table()` would make the two orders identical; it was not pursued because no behaviour in the tree is wrong and the guard holds either way. S1-R1-03 names `tests/test_scaffold.py` as the subprocess caller; a grep of `plugins/tabularium/tests/*.py` shows four modules calling `subprocess`, also `test_builder_cli.py`, `test_release.py` and `test_euler_releases.py`, so the carried row understates the breadth without changing its finding, and the row is carried unedited. The round-1 leads still stand unchanged: free-string `provenance.source_kind` in `canonical-event-v3.json` against three per-adapter constants; the corrected v2 documents remaining a cross-product over the tuple columns; `coverage-manifest-v3.json` accepting an empty `versions.mapping_rules` with no cross-branch ambiguity; `.agents/plugins/marketplace.json` carrying no `version` key for `tabularium`; and no test pinning the twelve v0 digests. Negative results this round: `git diff --name-only 1d131b98 e448a1e3 -- plugins/tabularium/examples` returns zero files, so no `examples/*-v0/` byte moved across the step and its fix; the four version surfaces all read `0.3.5` and `tests.test_version_propagation` passes 8 tests; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` remain byte-identical to their `.hexaemeron/` counterparts and the 25 reports still match; under `jsonschema` 4.25.1 both v3 documents still pass `check_schema` and reject 8 of 8 mixed-tuple events and 8 of 8 mixed-tuple manifests; the corrected v2 documents still admit 503 of 503 shipped rows and all three `coverage.json` manifests; and with `jsonschema` blocked by an import stub the Tabularium suite still runs 161 tests with 0 skips and 0 failures.
## Step 2, round 1 -- 2026-09-19T07:19:54Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: no jsonschema document run was made against a built schema-3 ledger; the parity this round checked is the required-field lists of `canonical-event-v3.json` and `PROVENANCE_FIELDS` compared field by field, not `jsonschema` applied to the rows, which the register routes to the reporter work. `validate_event_row` still checks presence and tuple membership rather than the closed key set the documents enforce with `additionalProperties: false`, so an extra `provenance` key, a dropped top-level `id` and a dropped `native_record` are each refused only by `canonical bytes do not match an offline source rebuild`; all three were tried through the CLI and each exits 1. No size cap was added or asked for: `_artifact_bytes` reads each declared artefact whole and `_parse_jsonl` materialises every row before the row-count check, so a 400,000-row ledger against a manifest declaring 500 rows is refused at exit 1 only after 13.1 s and a full parse. Hypomnema was not run against changed documents because the step changed none: `git diff --name-only 9cc8a8ec ef9c5a8f` lists no Markdown, and over the whole worktree the lint exits 1 with four H008/H009 findings, all in `audit/rounds/fiat-1086-gate-study-and-runbook-links-before-their-d.md`, its synopsis, `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md` and its synopsis, which belong to other runs and are unchanged by this step. The Pashov pair did not run under the recorded waiver: no Solidity. prose-drift is step 4 and none of its files changed. The Elenchus verdict below is the declared classifier result, not an attestation of the report bytes; the counterfactual behind S2-R1-01 was run by hand for the reason S1-R1-03 records.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | `plugins/tabularium/scripts/tabularium_lib/release_v2.py` | `PROVENANCE_FIELDS` named six of the eleven `provenance` fields that `canonical-event-v2.json` and `canonical-event-v3.json` both require, so a row missing `source_kind`, `source_contract`, `source_entity`, `source_id` or `supporting_selectors` was refused only by `canonical bytes do not match an offline source rebuild` at the end of `_verify_release`, not by row and field. Each of the five was dropped from row 1 of a schema-3 aave-v4 release and verified through the CLI at exit 1 with that generic message. No release is admitted that should not be; what is lost is the named diagnostic this step exists to give and the schema-parity the register tracks. | fixed in 398d43550544285443a5227ceaafae7085aa9e08 |<br>| S2-R1-02 | low | `plugins/tabularium/scripts/tabularium_lib/verifier.py` | The comment the step added above the row loop said the rows are checked "before any byte comparison", while `_release_artifacts` already compares each artefact's SHA-256 and byte count against the manifest's declared claim in `_artifact_bytes` before the loop runs. The runbook's own requirement, that the validator run before the rebuild, does hold. `docs/tabularium-schema-v3/study.md:266` carries the same "before any byte comparison" wording, so the comment restated a controller-pinned artefact rather than inventing the claim; the study was not edited. | fixed in 398d43550544285443a5227ceaafae7085aa9e08 |<br><br>Leads not pursued: `validate_event_row(row, adapter_module, schema_version, index=1)` defaults the row number to 1, and the one caller in `_verify_release` passes the real index, so no refusal can misname a row today; a future caller that omits it would label every row 1. Not pursued because a default on a single-caller function is not worth a change on this step. The whole-file read in `_artifact_bytes` and the full parse in `_parse_jsonl` both predate this step and neither is named in its Files field, so the 400,000-row cost above is recorded rather than fixed. `docs/tabularium-schema-v3/study.md:120` still records `EVENT_SCHEMA_VERSION = 1` and `ADAPTER_VERSION = "1.0.0"` as present in `tabularium_lib/__init__.py`, which this step removed; the study is pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and describes the tree as it was found, so no edit was attempted. Negative results worth keeping. Removed constants: `EVENT_SCHEMA_VERSION`, `ADAPTER_VERSION` and `MANIFEST_SCHEMA_VERSION` have no reference in any `.py`, `.md` or `.json` in the worktree outside that one study sentence, and `docs/tabularium-schema-v3/design-probe.py`, the only module outside `plugins/tabularium/` that imports `release_v2`, uses `EVIDENCE_CLASSES` alone and touches no changed signature. v0 immutability: `git diff --name-only 9cc8a8ec ef9c5a8f -- plugins/tabularium/examples` lists exactly `aave-v4-v0/rebuild.py`, `euler-v1-v0/rebuild.py` and `euler-v2-v0/rebuild.py`, and all three rebuilds report their published digests `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7`, `4034622f8b34147dead8a87d7c16b2a7c7197ed6417809fec41716a8028552aa` and `f563baa00c737384a3901f1bb3a7ae977f68f52a813eae9d02071eb2f4d0a5fe` both at `ef9c5a8f` and after the fix. tuple-drift: registering a fourth adapter in `release_v2.ADAPTERS` before `test_schemas` imports turns 14 of its 18 tests red, including `test_every_registered_adapter_reaches_the_tuple_table_and_the_v3_schemas` at `4 != 3`, so the step-1 guard still holds over the rewritten `release_v2.py`. version-mixing: a manifest at 3 with row 250 at 2, a manifest at 2 with rows at 3, and `versions.event_schema` 2 under `schema_version` 3 are each refused at exit 1, naming `canonical row 250`, `canonical row 1` and both manifest numbers. Named refusals reaching stderr at exit 1 through the CLI: `venue`, `provenance.adapter`, `provenance.adapter_version`, `provenance.protocol_generation`, `provenance.source_api`, `provenance.mapping_rule`, a missing `provenance.mapping_rule` and a row that is not an object. path-confinement: a symlinked `events.jsonl`, a `../rel/events.jsonl` and an absolute `/etc/passwd` in `canonical.path` are refused with `canonical path uses a symlink` and `canonical path is not a safe relative path`. Adversarial input: a 200,000-deep JSON array, the bytes `\xff\xfe` and a duplicated `schema_version` key are each refused as a `TabulariumError` at exit 1 with no traceback, because `loads_json` already catches `RecursionError` and `UnicodeDecodeError`. legacy-write: a build with no flag wrote `schema_version` 3 in the manifest and in every row, the only `--event-schema 2` callers in the tree are the three rebuild demos, and `--event-schema 4` is refused by argparse against `choices` `[2, 3]`. partial-write: the two `write_bytes_atomic` calls in `builder.py` are unchanged and still follow `validate_manifest_v2`. skip-invisibility: the Elenchus report records 181 tests with 0 skipped, 0 expected failures and 0 unexpected successes. Gates: phylax and ephoros each exit 0 over the 22 paths changed between `9cc8a8ec` and `ef9c5a8f` and again over the three files the fix changed; the root suite ran 2,049 tests OK with exit 0 on the fixed tree; `python3 scripts/run_checks.py --scope tabularium --format json` reports outcome green over 4 checks; the Tabularium suite is 180 tests at `ef9c5a8f` and 181 after the fix; and `plugin_release.py --base ef9c5a8f --head 398d4355` reports `tabularium: 0.3.6 -> 0.3.7` at exit 0 after the four version surfaces were bumped in the fixes commit.
## Step 2, round 2 -- 2026-09-19T07:33:35Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable<br><br>Not checked: still no `jsonschema` document run against a built ledger, and the three rejection fixtures the register names do not exist in the tree: `plugins/tabularium/tests/fixtures/` holds `compound-debt-transfer.json`, `minimal-capture-manifest.json` and `minimal-snapshot.json` only. The closed key set both documents declare with `additionalProperties: false`, on the row and on `provenance`, is still unenforced in Python, so an extra `provenance` key, a dropped top-level `id` and a dropped `native_record` are refused only by `canonical bytes do not match an offline source rebuild`; all three were retried at this head, at schema 3 and schema 2, and each exits 1. That gap is decided below rather than carried. No size cap was added: a 400,000-row ledger against a manifest declaring 500 rows is still refused at exit 1 only after a full parse, measured at 7.6 s this round against 13.1 s in round 1, which is machine load rather than a behaviour change. The Pashov pair did not run under the recorded waiver: no Solidity. prose-drift is step 4 and the only Markdown this range changed is this run's own audit record and its synopsis. No new Elenchus run was made because this round produced no fixes commit, so the verdict is null rather than the `inconclusive` round 1 records for its fix; S1-R1-03's cause is unchanged, and `elenchus.py` was not re-invoked.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: two round-1 leads are now decided with evidence rather than carried. The `index=1` default on `validate_event_row` is unreachable at this head: `validate_event_row` has exactly three references in every `.py` file in the worktree, its definition at `release_v2.py:351`, the import at `verifier.py:12` and the one call at `verifier.py:114`, which passes the real `index`; no test calls it directly, so no refusal can misname a row and the default is not a finding. The closed key set is routed to the parity work rather than recorded as a step-2 finding: `canonical-event-v2.json` and `canonical-event-v3.json` both set `additionalProperties: false` on the row and on `provenance`, and `release_v2._exact` is the library's own closed-key helper applied to every manifest and capture object, so the tool exists and the row is the one untrusted object it is not applied to; against that, step 2's Tests field enumerates the refusals it owes and a closed key set is not among them, its Files field names no fixture directory, the three rejection fixtures the risk register pairs with this work are absent from the tree, and nothing is admitted that should not be, because the rebuild comparison refuses every one of these rows at exit 1. The decision is that the diagnostic belongs with the reporter the register routes it to, and a second behavioural change to the row validator in a regression round would add over-refusal risk for a message improvement. One new lead: `test_every_schema_required_provenance_field_is_refused_by_name` derives its list from `canonical-event-v3.json` alone, so if the v2 document's `provenance.required` ever diverged from the v3 document's the test would not see it; both require the same eleven fields today, and the same `PROVENANCE_FIELDS` tuple serves both schema versions, so nothing is wrong in the tree. Negative results worth keeping. Over-refusal, which is the failure this round existed to catch, did not occur: building and verifying a release end to end for each of the three adapters at each of schema 2 and schema 3 gives six pairs that all exit 0, the three schema-2 builds reproduce the published digests `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7`, `4034622f8b34147dead8a87d7c16b2a7c7197ed6417809fec41716a8028552aa` and `f563baa00c737384a3901f1bb3a7ae977f68f52a813eae9d02071eb2f4d0a5fe`, the three schema-3 builds give `81d416a10b70ab0f3d9a3bd41c0680e235b3b64f4f06cc36b4f4292c81136492`, `5b1016a9bc143f42e9bea46de71b3d1917bf6731d93b4c49f974df3660bc8595` and `f2b227058f53cd644c11359e911c8494924d6fef7da7072e8a33a4baf952d02a`, the three v0 `rebuild.py` demos each exit 0 on their published digest, and the three committed `coverage.json` files verify offline at schema 2 on those same digests. The widened tuple behaves identically on both schema versions: the five newly named fields are named on a schema-2 release exactly as on a schema-3 one. Every named refusal round 1 recorded still holds at this head, at exit 1 through the CLI: `venue`, `provenance.adapter`, `provenance.adapter_version`, `provenance.protocol_generation`, `provenance.source_api`, `provenance.mapping_rule`, a missing `provenance.mapping_rule`, a row that is not an object, `canonical row 250` at schema 2 under a manifest at 3, `canonical row 1` at schema 3 under a manifest at 2, `versions.event_schema` 2 under `schema_version` 3, a symlinked `events.jsonl`, a `../rel/events.jsonl`, an absolute `/etc/passwd`, a 200,000-deep JSON array, the bytes `\xff\xfe` and a duplicated `schema_version` key. The step-1 tuple-drift guard still holds over the twice-rewritten `release_v2.py`: registering a fourth adapter before `test_schemas` imports fails 14 of its 18 tests, the same count as at `ef9c5a8f`. The round-1 guard still fails on the parent six-field tuple at this head and passes on the fixed tree. Scope and gates: `git diff --name-only 9cc8a8ec 614de545 -- plugins/tabularium/examples` still lists exactly `aave-v4-v0/rebuild.py`, `euler-v1-v0/rebuild.py` and `euler-v2-v0/rebuild.py`, and `git diff --name-only ef9c5a8f 614de545 -- plugins/tabularium/examples` is empty, so the fix moved no example byte; `git diff --stat ef9c5a8f 614de545` is empty for `builder.py`, the three adapters and `tabularium.py`, which is the evidence for partial-write and legacy-write this round; phylax, ephoros and hypomnema each exit 0 over the eleven paths changed between `ef9c5a8f` and `614de545`, and hypomnema has a real target this round, this record and its synopsis, rather than the empty set round 1 reported; the root suite ran 2,049 tests OK with exit 0; `python3 scripts/run_checks.py --scope tabularium --format json` reports outcome green over 4 checks each at exit 0; the Tabularium suite is 181 tests with 0 skipped, 0 expected failures and 0 unexpected successes; `plugin_release.py` exits 0 against both bases, `tabularium: 0.3.6 -> 0.3.7` from `ef9c5a8f` and `0.3.5 -> 0.3.7` from `9cc8a8ec`, and all four surfaces read `0.3.7`; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree leaves the worktree clean.
