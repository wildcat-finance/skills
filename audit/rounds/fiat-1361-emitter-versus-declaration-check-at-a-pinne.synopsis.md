Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1361-emitter-versus-declaration-check-at-a-pinne.md | source_sha256=7764f25fd6fdf9c93bd02d0a1ba1339d4ef25961a3c50ddf58a0ad5f9626e37d | h2_count=4
## Step 1, round 1 -- 2026-09-23T02:01:04Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: emitter-parse-miss=not-applicable; declaration-miss=not-applicable; oracle-independence=not-applicable; keccak-correctness=reviewed; abi-view-disagreement=not-applicable; deployed-binding=not-applicable; source-drift=not-applicable; vendored-source=reviewed; subprocess-git=not-applicable; partial-write=not-applicable; md-json-drift=not-applicable; runtime-overclaim=not-applicable; subword-note=not-applicable; horos-currency=reviewed<br><br>Not checked: CI (`.github/workflows/repo.yml` not run). The diff `e9e95b891b3ae642516e017c46b97a18a1480164..40a2c9594c87b0b2497f6a21bd5c08d295830d20` holds no `.sol` file: X-Ray's `enumerate.sh` reports nSLOC 0 and its git analysis counts 0 of 4200 commits touching Solidity source, and the solidity-auditor's default `find` returns 0 in-scope files, so neither had a Solidity scope and no auditor agent pass ran in this round. fizz did not run: no Solidity, no Foundry or Hardhat project in scope. The Python review is this Warden's own, plus the leads of an earlier Warden's review sub-agents on the same head; one of those returned no output and counts as unreviewed, not clean, and every lead used here was re-verified on the tree. The scanner, table, ABI and source-binding ids are not-applicable because this step ships only the skeleton, the parser that refuses with exit 2, and the Keccak core. `docs/emitter-declaration-check-study.md` and `docs/emitter-declaration-check-runbook.md` `cmp` equal to the receipted `.hexaemeron/study.md` and `.hexaemeron/runbook.md`; `design_evidence` SHA-256 `3e98bb53aafb080cf7de9f2f0990cab9dfc9783d7354c526ca817c1e9bdbd703` (selected `source-and-abi`) matches the file on disk, and its `committed-table-regenerates` conformance cell blocks integration, not this step.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | scripts/emitter_declarations.py | `keccak-correctness`: `keccak256` appended `0x01`, then `(_RATE - len(padded) %% _RATE - 1) %% _RATE` zero bytes, then `0x80` as separate bytes (lines 76-79 at `40a2c959`). When `len(data) %% 136 == 135` one byte of the block remains, so pad10*1 needs the single byte `0x81`; the code wrote `0x01` there and pushed `0x80` into a second, 135-zero block, hashing two blocks instead of one. `keccak256(b"a" * 135)` returned `8567c1de67db78fe5aeaf45043709897a116a2e7890b51657528ef2a7c33baba`; `openssl dgst -keccak-256` and `cast keccak` give `34367dc248bbd832f4e3e69dfaac2f92638bd0bbd18f2912ba4ef454919cf446`. A sweep of lengths 0 to 699 against correct padding over the same permutation fails at exactly 135, 271, 407, 543 and 679; the permutation itself matches `hashlib.sha3_256` at every length 0 to 409 under the `0x06` suffix. Later steps hash event signatures with this function, so a signature of such a length would get a wrong expected topic0. Fixed: padding moved into `_sponge(data, suffix)`, which writes the one byte `suffix + 0x80` (`0x81` for Keccak) when one byte remains; `keccak256` calls it with `0x01`. Guard `test_padding_boundary_vectors` pins the 135-, 136- and 271-byte vectors from `openssl`. | fixed in the commit that adds this record |<br>| S1-R1-02 | low | tests/test_emitter_declarations.py | `keccak-correctness`: the tests pinned only the 0-byte and 33-byte vectors the risk register names, both single-block inputs far from the boundary, so S1-R1-01 passed the suite, while the source comment called the copy "pinned by tests/test_emitter_declarations.py". Fixed: `test_sponge_matches_sha3_across_three_blocks` requires `_sponge(data, 0x06)` to equal `hashlib.sha3_256(data).digest()` at every length 0 to 409, crossing the one-byte-remaining boundary at 135, 271 and 407, and the source comment now says the padding differs from the Tabularium copy. Both guards fail on the parent: the Elenchus runner report there reads 6 executed, 2 assertion failures, 0 errors. | fixed in the commit that adds this record |<br><br>Leads not pursued: (1) `plugins/tabularium/scripts/tabularium_lib/keccak.py:59-67` carries the same padding defect, returning `8567c1de67db78fe5aeaf45043709897a116a2e7890b51657528ef2a7c33baba` for 135 bytes; it is outside this step's Files contract and left unedited, and its only caller, `keccak.py:87`, hashes a 64-byte slot preimage, which never reaches the defect. (2) `tests/emit_emitter_declarations_report.py:51` resolves `REQUIRED_SURFACE` under `Path.cwd()` while `MODULES` import from `REPOSITORY_ROOT` via `__file__`, so a run from another directory would check one tree and test another; the pattern is inherited from `tests/emit_run_observation_report.py:290` and `tests/emit_contributors_report.py:45`, no Fiat call site uses a different cwd, and the runner is digest-registered and not editable here, so accepted. (3) The runner parses argv twice, `build_parser` then `report_target`; `-- -r.json` fails closed in the second parser and no bypass was found. (4) `test_study_and_runbook_copies_are_committed` checks only existence and non-zero size; no digest ties the `docs/` copies to the receipted study and runbook. That matches the runbook's "a test that the committed study and runbook copies exist", and both copies are byte-equal today. Mechanical gates on the fixed tree: `phylax.py`, `ephoros.py` over the three changed Python files and `hypomnema.py` over the two docs copies and this record each exit 0; the runbook's repository `phylax.py scripts tests` and `imprimatur.py` over the two docs copies exit 0. Root suite on the fixed tree before the Horos regeneration: `Ran 2157 tests`, 1 failure, `HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan`, the stale census this commit regenerates; the commit gate's `greenlight` then passed the root suite on the exact staged tree.
## Step 1, round 2 -- 2026-09-23T02:22:33Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: emitter-parse-miss=not-applicable; declaration-miss=not-applicable; oracle-independence=not-applicable; keccak-correctness=reviewed; abi-view-disagreement=not-applicable; deployed-binding=not-applicable; source-drift=not-applicable; vendored-source=reviewed; subprocess-git=not-applicable; partial-write=not-applicable; md-json-drift=not-applicable; runtime-overclaim=not-applicable; subword-note=not-applicable; horos-currency=reviewed<br><br>Not checked: X-Ray and solidity-auditor's literal tools again have no Solidity scope on this head (0 `.sol` files, same as round 1), so neither ran as a tool. The Python review for this round is this Warden's own reproduction of the fixed tree, together with verification of the review sub-agents an earlier, host-stopped Warden of this same step left running: each of their claims against `_sponge`/`keccak256`, the ABI cross-check, `write_report`'s symlink and pre-existing-path handling, and the CLI refusals was independently re-run on this tree before being logged here. One of those sub-agents was interrupted by a classifier before its planned check completed; that check counts as unchecked, not clean. fizz did not run: no Solidity, no Foundry or Hardhat project in scope.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | low | tests/emit_emitter_declarations_report.py | `missing_surface_suite` raises `AssertionError` inside a `unittest.FunctionTestCase` when the required surface is absent; Fiat's unittest guard admission accepts that assertion as a real fix-guard, so a round whose parent lacked the surface would read "guarded" with no guard test actually run. Reproduced at parent `40a2c9594c87b0b2497f6a21bd5c08d295830d20`. The same pattern is inherited from `tests/emit_run_observation_report.py:566-569` and `tests/emit_contributors_report.py`. Unreachable in this run: round 1's parent already carries the required surface and every later step builds on it, and the runner's `build_parser` is digest-registered (`b8dc87bf6311782a38587771bb917fe395765185fd80126fd0197c9764e5cfc3`) outside this step's Files contract, so it is not editable here. | accepted; tracked as https://github.com/wildcat-finance/skills/issues/1861 (framework-39) covering the shared pattern |<br><br>Leads not pursued: (1) the round 1 cwd-vs-import-root split (`tests/emit_emitter_declarations_report.py:51`) reproduced again from a directory carrying only decoy stub surface files: the surface check reads `Path.cwd()` and passes trivially, but the actual suite still imports and runs the real `tests.test_emitter_declarations` via the `REPOSITORY_ROOT` insertion into `sys.path`, so the reported result reflects the genuine repo state and not the decoy on every path tried; status unchanged from round 1 -- accepted, runner not editable here, no controller call site uses a different cwd. (2) `python3 tests/emit_emitter_declarations_report.py -- -h` (also `-- -x.json`) exits 0 via argparse's own `-h` handling before `report_target`'s confinement parser ever runs, printing help and writing no report; this is a different argument shape than round 1's lead (3), which tested `-- -r.json` and found the second parser fails closed. Elenchus always invokes the runner with an absolute report path and no `--`, so no controller route reaches it; not fixed, runner not editable here. (3) the subTest testsRun-undercount pattern (a test with several failing `subTest`s reports more failures than `testsRun`) is already tracked as https://github.com/wildcat-finance/skills/issues/869 (elenchus-wish, open); this step's own `test_emitter_declarations.py` uses `subTest` in `SubcommandTests` and `SpecificationCopyTests`, both currently passing with no multi-failure observed on this tree. (4) `tests/emit_run_observation_report.py` (SHA-256 `c18dffd0f05fa8b652380af9534b9b2a660699447aef4aa7072c4d7ba208e63f`) supplies `report_target`, `result_payload` and `write_report` to the digest-registered runner but sits outside the runbook's `command-interfaces` fence, which pins only `build_parser` of `tests/emit_emitter_declarations_report.py`; a change to that shared module would not trip the pin. Step 2 pointer for any runbook amendment touching the runner's registration. (5) Step 2 design pointers, no Step 1 fix expected: study assumption 3 and design option 1 say "every event declaration under `src/`", while the single-epoch evidence pins only the eight files the scan reads, so a declaration outside those eight is not covered by that claim; the glossary (section 6) names seven mismatch classes but the five specimens omit `data-order` and `parameter-count`; the study never states how contract, interface, enum, or user-defined-value-type parameters map to ABI canonical types (for example `IERC20` to `address`, an enum to `uint8`), so a scanner could hash the wrong canonical signature; and study sections 9 and 11 say the ref comes only from the source block's table, while demo 2 and Runbook Step 3 run `check --ref bea503c2...` expecting exit 1 naming moved digests, and `bea503c2` also adds two files (`IWildcatMarketRevolving.sol`, `WildcatMarketRevolving.sol`) the eight-file block does not name, so a v2.5 `build` needs a mode that derives its own fresh source block for a named ref while `check` keeps using the table's. Mechanical gates on this tree: `phylax.py` and `ephoros.py` over `scripts tests`, and `hypomnema.py` over the two docs copies, each exit 0. Root suite on the fixed tree, after removing the stopped round's untracked `x-ray.md` and `.audit-*` staging that had reddened `test_dead_code.ShippedSurfaceTests.test_checked_dead_code_scope_selects_suite_and_live_validator` with `unknown-ownership`: `Ran 2157 tests`, `OK`.
## Step 2, round 1 -- 2026-09-23T03:40:11Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: emitter-parse-miss=reviewed; declaration-miss=reviewed; oracle-independence=reviewed; keccak-correctness=not-applicable; abi-view-disagreement=reviewed; deployed-binding=reviewed; source-drift=reviewed; vendored-source=reviewed; subprocess-git=reviewed; partial-write=reviewed; md-json-drift=reviewed; runtime-overclaim=reviewed; subword-note=reviewed; horos-currency=reviewed<br><br>Not checked: X-Ray's own pipeline (`enumerate.sh`, phase 2/3) has no Solidity scope here -- 0 `.sol` files in `scripts/emitter_declarations.py` and `tests/test_emitter_declarations.py`, the same finding Step 1 round 1 recorded -- so it did not run as a tool. The solidity-auditor-style review ran regardless, as directed, since Step 1's equivalent pass found a real defect despite the same absence of Solidity: this Warden's own line-by-line read plus manual repro scripts, and a general-purpose sub-agent applying the solidity-auditor SKILL.md method to this Python file (run twice -- once backgrounded, once synchronous after the backgrounded run was slow to return -- both converging on the same primary finding). `keccak-correctness` is not-applicable: this diff does not touch `_sponge`/`keccak256`, fixed in Step 1. fizz did not run: no Solidity, no Foundry or Hardhat project in scope. Independent verification beyond the suite: a scratch clone of `wildcat-finance/v2-protocol` (deleted after use) reproduced the Mason's reported `build` figures exactly -- 27 emitters, 38 rows, 0 mismatch rows, 0 unreviewed, `check` exit 0 in 0.76s -- and `check --ref` against the unreleased candidate `bea503c2736d47de7fd34130c64f10783dc35b39` correctly refused (exit 1, 108 source differences, two added-file rows among them); three computed topic0 values (`Transfer`, `Moved`, `Approval` signatures) matched `cast keccak` byte for byte.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | scripts/emitter_declarations.py | `emitter-parse-miss`/`oracle-independence`: `_parse_emitter` (lines 556-568, pre-fix) always reads `args[2]` of a `logN` call as the `topic0` signature literal whenever `arity >= 1`, without knowing at parse time whether the paired declaration is anonymous. A genuinely anonymous `logN` call reserves no topic0 slot -- every topic word is an indexed value. Common trigger (first indexed value a plain identifier): already fails closed, `unreviewed`, with a reason that reads as a data problem rather than an anonymity mismatch -- unchanged, still safe. Narrower, previously undetected trigger: when that first indexed value happens to be a 32-byte hex literal or resolves through a declared `constant` (a realistic assembly idiom, e.g. a hard-coded tag), it was silently absorbed into `topic0_literal` and dropped from `topic_args`. In `compare()`, the resulting one-short `topic_args` length then failed the `len(...) == len(indexed)` guard, so `indexed-order` was never evaluated for that row -- a real swapped-or-substituted indexed value in a genuinely anonymous emitter would pass with only `anonymous-declared` recorded, not the `indexed-order` mismatch it should also carry. Confirmed independently by this Warden (manual repro) and by both solidity-auditor-style passes. Fixed: `compare()` now reconstructs the full anonymous topic sequence (`[topic0_literal] + topic_args`) before the indexed-order comparison whenever the declaration is anonymous and a `topic0_literal` was read; the non-anonymous path is untouched. Guard `test_anonymous_indexed_order_is_not_silently_skipped` builds a genuinely anonymous `Sent` emitter whose first topic is a stray 32-byte literal where `from` belongs: fails on the parent commit (`indexed-order` absent from `['abi:WildcatMarket:log-arity', 'abi:WildcatMarket:topic0', 'anonymous-declared']`), confirmed both by direct `unittest` against the parent tree and by `elenchus.py --ref` against this round's fixes commit (`{"status": "guarded", "detail": "the runner report records a parent assertion failure", "report": {"executed": 49, "assertion_failures": 1}}`); passes on the fixed tree (49/49). | fixed in the commit that adds this record |<br><br>Leads not pursued: (1) the same region's common trigger (first indexed value a plain identifier) still reports `"topic0 '<name>' is not a literal"` as the unreviewed reason -- fails closed correctly, but the text reads as a literal-parsing problem rather than naming the anonymity case; wording-only, not fixed, since no test pins the current string and the glossary (study section 6) already defines arity as always including a topic0 slot. (2) `TypeResolver`'s struct-member resolution (`scripts/emitter_declarations.py:462-464`) treats a `detail` of `[]` (zero members) the same as `None` via `(detail or [None])`, so it can never produce the canonical empty tuple `"()"` that `_abi_type` produces for a zero-input ABI event; unreachable, since a Solidity struct cannot have zero members -- informational, unfixed. (3) the same class's cycle guard (`:449`) keys `stack` by the literal spelled type string rather than a resolved (owner, name) identity; both solidity-auditor-style passes tried adversarial mutual-cycle and qualifier-mismatch fixtures and could not construct a reachable bypass, since a struct cannot contain itself and a UDVT's underlying type must be elementary -- informational, no confirmed input. (4) `check --from-https` cannot detect a source file added under `src/` at a later `--ref`, since `HttpsReader.list_sources` is `None` and the added/removed-file drift comparison in `read_table_sources` is skipped entirely for that reader; disclosed in the module docstring ("a raw host cannot list a tree") and consistent with "check reads ref and paths only from the table," but a real coverage gap specific to the HTTPS-only workflow, and no test in this step exercises `check --from-https` at all (only `build --from-https` has one, `test_https_build_refuses_a_digest_mismatch`); noted for a future step or study amendment, not fixed here, since resolving it needs an independent tree listing over HTTPS, out of this step's Files contract. (5) `deployed-binding` (`in_builds`/`binding_differs` in `assemble()`) has no specimen test among this step's ~48, despite being a named risk-register item; manually verified correct behaviour by constructing a deliberately diverged `WildcatMarket` standard-input against the real read source -- `in_builds` correctly read `{"WildcatMarket": "differs", "HooksFactory": "absent"}` and `binding_differs` correctly named the file; no code defect found, coverage gap only, not pursued as a fix this round. (6) `GitReader.read` (buffers a local blob fully via `subprocess.run(capture_output=True)` before the `MAX_FILE_BYTES` check) and `assemble` (accumulates `MAX_TOTAL_BYTES` only after every file is already read) bound memory less tightly than `HttpsReader`, which caps the socket read directly; both require an already-trusted local clone and are not independently exploitable -- informational. Mechanical gates on the fixed tree: `phylax.py` and `ephoros.py` over `scripts tests` each exit 0; `hypomnema.py` over `scripts tests` exits 0 (10 pre-existing findings, none touching this step's two files). Root suite on the fixed tree, after regenerating `.horos/census.json` and `.horos/boundary.json` for the byte-count and reading-boundary change this fix introduced: `Ran 2200 tests`, `OK`.
## Step 2, round 2 -- 2026-09-23T04:04:16Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: emitter-parse-miss=reviewed; declaration-miss=reviewed; oracle-independence=reviewed; keccak-correctness=not-applicable; abi-view-disagreement=reviewed; deployed-binding=reviewed; source-drift=reviewed; vendored-source=reviewed; subprocess-git=reviewed; partial-write=reviewed; md-json-drift=reviewed; runtime-overclaim=reviewed; subword-note=reviewed; horos-currency=reviewed<br><br>Not checked: no auditor-agent pass ran this round; the round's own scope is the S2-R1-01 fix and its guard test, already read line by line at commit `cd0894189174e887aa6cde17fe7334eff25ae56f`, not a fresh diff needing X-Ray or a solidity-auditor-style pass. `keccak-correctness` is not-applicable, unchanged since Step 1. fizz did not run: no Solidity, no Foundry or Hardhat project in scope. Correction to the round 1 record: this Warden's round 1 report of `hypomnema exit:0` reflected `tail`'s exit code, not hypomnema's; hypomnema actually exited 1 there (10 pre-existing findings in unrelated repository files, none in this step's own paths) because it was run over the broad `scripts tests` directories rather than the changed docs. This round scopes hypomnema correctly to the changed doc, `audit/rounds/fiat-1361-emitter-versus-declaration-check-at-a-pinne.md` (and its generated `.synopsis.md`), matching Step 1's own precedent and the audit-loop's `<changed docs>` instruction; both are clean, exit 0.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: regression check: `compare()`'s non-anonymous path is `topic_values = emitter["topic_args"]` unchanged by S2-R1-01 -- the `anonymous and topic0_literal is not None` branch only reconstructs the list for anonymous declarations, so no behavioural change reaches a non-anonymous emitter; all 49 tests pass, including every pre-existing non-anonymous specimen (`topic0`, `log-arity`, `indexed-order`, `data-length`, `data-order`, `parameter-count`). Pin re-verified from a fresh scratch clone (deleted after use): `build` gives 27 emitters, 38 rows, 0 mismatch rows, 0 unreviewed; `check` exits 0. All six round 1 leads re-checked against the unchanged code regions they name and found unchanged: (1) the misleading `"topic0 ... is not a literal"` reason text, (2) `TypeResolver`'s empty-tuple struct resolution, (3) its cycle-guard key granularity, (4) `check --from-https`'s inability to detect an added source file at a later `--ref`, (5) `deployed-binding`'s missing specimen coverage, (6) `GitReader`/`assemble`'s looser memory-bounding versus `HttpsReader` -- none is touched by this round's diff (`.horos/*.json` byte-count and reading-boundary regeneration only), so none escalates to a finding. Mechanical gates: `phylax.py` and `ephoros.py` over `scripts tests` each exit 0; `hypomnema.py` over the changed doc exits 0. Root suite, run once in the foreground: `Ran 2200 tests`, `OK`.
