## Step 1, round 1 -- 2026-09-04T02:51:00Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=not-applicable; hook-index-inheritance=not-applicable; hook-config-inheritance=not-applicable; worktree-record-crossing=not-applicable; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=not-applicable; ci-cannot-see-activation=reviewed

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity and no Foundry or Hardhat project. Five register concerns have no surface at `3afc109b9a3ebbc8f7d3511e36cd7fc0dc123c46`: `.githooks` holds `README.md` alone, with no `pre-commit`, no `greenlight` and no green record anywhere, so `stale-green-different-tree`, `hook-index-inheritance`, `hook-config-inheritance` and `worktree-record-crossing` have no code to read, and `executable-bit-loss` has no script whose mode could be asserted. Also not checked: the five acceptance conditions themselves, which steps 2 through 5 deliver. Also not checked: ruleset `21830871` and the workflow states the study reports from the GitHub API on 2026-09-04, neither re-read this round, so the study's claim that the ruleset requires only `identity` and `invariants` in `evaluate` mode is carried as recorded rather than checked. Also not checked: whether run #856 still claims ADR-074 on `origin/main`. Also not checked: the fixtures under the untracked `.hexaemeron/design/`, which were not re-executed, so the 289.3-second, 22-millisecond, 497-byte and 40-byte figures were verified only against the shipped reports that carry them. Also not checked: issues #888 and #582 and the ADR-050 and ADR-024 history the decision record's Status section cites.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | docs/commit-gate/study.md | The two shipped run documents cite the gitignored run-state directory ten times: `docs/commit-gate/study.md` lines 5, 256, 411, 412 and 467, and `docs/commit-gate/runbook.md` lines 3, 43, 83, 178 and 189. `.hexaemeron/.gitignore` is `*`, so no clone carries the path, and `git check-ignore -v .hexaemeron/study.md` confirms it. The sharpest instance is study.md:411-412, which sends a reader to `.hexaemeron/reports/` for the forty reports that ship beside it at `docs/commit-gate/reports/`. This is the class `tests/test_contributors.py:242` `test_no_published_file_cites_the_run_state_directory` already names, whose docstring reads that `.hexaemeron` is fully gitignored so a clone never has it; its `SPEC` list does not reach `docs/commit-gate/`. Hypomnema H001 resolves Markdown links, not inline code spans, and all ten citations are backticked, so both gates pass. Not fixable inside this step's amended Exit, which pins the shipped runbook to its source and the shipped study to a five-line link-target delta with no other byte differing. | open, needs a controller amendment on the same route the H001 link rewrite used |
| S1-R1-02 | low | .githooks/README.md:16 | The line read "Until it is set, the root suite fails and names this command", stating as current behaviour an assertion runbook step 4 delivers. At `3afc109b` no test reads `core.hooksPath`; `grep -rn "core.hooksPath" tests/` returns nothing, `git config --get core.hooksPath` exits 1 in this worktree, and the root suite ran 1138 tests OK with it unset. The file's own "What lands here" section already writes about step 2 in the future tense, so this one sentence was the outlier, and it is the first thing a fresh clone reads. | fixed in 5b0acf156ea7be93f0c3dab1832a887952287b1b |
| S1-R1-03 | low | docs/commit-gate/runbook.md:14 | The citation `tests/test_decision_records.py:108` names a blank line. The collision check is `def test_no_number_collides_with_one_already_on_the_default_branch` at line 110, at both `ab611eb96a6a` and the base `8dc3aca54adeca49387a2bdfc174cf6e72d02a11`, so the file did not move and the citation was wrong when written; `docs/remove-the-beginner-primer/study.md:471` already cites `:110` for the same check. Separately, study.md:79 cites `scripts/run_checks.py:3091` and `:3128` for the green and red exits, which are one line past `outcome = "green" if not classes and not not_started else "red"` at :3090 and `return 0 if outcome == "green" else 1` at :3127; that file did change between `ab611eb96a6a` and the base, so those two are drift rather than error. The remaining citations checked are exact: `tests/test_boundary_currency.py:57-73` brackets the 1487 figure at :61, `:104` opens `GitEnvironmentIsolation`, `:126` is `test_the_helper_leaves_the_outer_staged_state_alone`, and `AGENTS.md:242` opens the checks section. | open, pinned bytes |
| S1-R1-04 | low | docs/commit-gate/runbook.md:82-83 | The shipped copy's step-1 Exit still reads that `docs/commit-gate/study.md` and `docs/commit-gate/runbook.md` are "byte-identical copies of the two `.hexaemeron/` artefacts", which the same commit makes false for the study. The amendment that replaces that Exit is 2567 bytes the shipped copy does not carry: `docs/commit-gate/runbook.md` is a strict byte prefix of `.hexaemeron/runbook.md`, whose SHA-256 is `1d11b527f1571bf3ddd823a8d1304c6ae26b29e92ec7f5119f15510f04deea8f`, and the tail is exactly the `### Amendment -- 2026-09-04` block. A reader of `docs/commit-gate/` cannot see the correction. The refresh is scheduled rather than absent: runbook steps 4 and 5 both list `docs/commit-gate/runbook.md` under Files, and the amendment's Why scopes the claim to the artefact as it stood when this step was built. | accepted, refresh scheduled at step 4 |
| S1-R1-05 | low | docs/commit-gate/study.md:61 | Acceptance condition 1 names `docs/decisions/ADR-074-*.md` as its criterion and `python3 -m unittest tests.test_decision_records -v` as the command that proves it. The record ships unnumbered by design, `tests/test_decision_records.py:68` globs `ADR-*.md` so that suite never sees a draft, and `tests/test_commit_gate.py:113` asserts that no `ADR-074-*.md` exists. The study's own row-1 command therefore passes without reading the record it names, and its criterion is deliberately false. The divergence is recorded at `docs/commit-gate/runbook.md:9-26`, which moves the proof to `DecisionRecordTests`. Filed so the run's close reports the moved proof rather than the study's literal row 1. | accepted, recorded at runbook.md:9-26 |

Leads not pursued: `.horos/candidates.json` was left stale, and that is right for this step. `python3 plugins/horos/skills/horos/scripts/horos.py check .` prints exactly one candidate drift, `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md`, then "boundary matches the tree" and exits 0. That file is tracked at the base `8dc3aca5` and absent from the base's `candidates.json`, so the drift is run #859's omission and predates this diff; `git diff --name-only 8dc3aca5..3afc109b -- .horos/` returns `.horos/boundary.json` alone. Candidate classification is advisory in `horos.py` check, which gates on `diff_boundary_documents` only, and `tests/test_boundary_currency.py` compares boundary documents only, so leaving it costs no gate, while refreshing it here would import another run's drift into this step's diff. It belongs to whoever closes #859. Second lead: the study carries no `design-bridge` fence, so Hypomnema's H008 join is unavailable, and it stays unavailable while the record is unnumbered, because H008 accepts only an ADR-numbered Markdown file below a `decisions` directory or an `EVOLUTION.md` beside a `SKILL.md`. `docs/wave-delta-reinstatement-study.md:62` is the only shipped fence in the repository, the ordinary walk never infers a bridge, and nothing in this step's contract asks for one, so this is a consequence of the unnumbered draft rather than a defect in it. Third lead: no lint enforces the run-state-citation rule of S1-R1-01 outside `tests/test_contributors.py`'s `SPEC` list; widening it is a repository change beyond this step. Fourth lead, per the Elenchus contract: the fixes commit `5b0acf156ea7be93f0c3dab1832a887952287b1b` changed no test files, so the guard check returns `unguarded` with detail "the commit changed no test files". The fix corrects one prose sentence in `.githooks/README.md`, and the assertion that would guard it is runbook step 4's `ActivationTests`; writing a test here would land step 4's work early. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 over all 48 changed paths and print `clean`; the repository's declared root suite from `tests/check-map-v1.json` `root-suite`, `python3 -m unittest discover -s tests`, reports `Ran 1138 tests in 121.963s`, `OK`, exit 0; and the step's own Exit command `python3 scripts/run_checks.py --full` reports `outcome green` at exit 0 with all 28 checks passed and no `WAI-E-ADAPTER.TIMEOUT`. The 1138 figure reconciles with the study's measured 1132 at section 3 plus this step's six new cases. `green-record-self-evidence`: the decision record's Consequences opens "The green record is a convenience rather than proof that the suite ran", names audit finding S1-R1-01 on the check runner as the shape, and `test_the_consequences_call_the_green_record_a_convenience` holds the literal string. `bypass-token-drift`: `FIAT_SKIP_PRECOMMIT` appears once in the tree, at `.githooks/README.md:21`, and no test asserts it yet because the tracked gate it would read arrives at step 2. `scratch-quiescence`: `tests/test_commit_gate.py` builds no temporary tree, and `tests/test_scratch_quiescence.py` is green inside the root suite. `hooks-path-absolute`: every occurrence of the activation value in the tree is the relative `.githooks`. `ci-cannot-see-activation`: study.md:331 states that hosted CI cannot see whether a contributor activated the gate, and the decision record's Consequences repeats it and restricts the CI half to the tracked bytes. The six `DecisionRecordTests` cases were mutation-tested rather than read: run against the precedent draft `docs/decisions/draft-fix-the-issue-status-block-markers.md`, the alternatives and consequences cases fail, confirming Mason's report; against a synthetic `# ADR-074:` heading only the heading case fails; against a record missing `## Context` only the sections case fails; against alternatives naming all three options without the word "loses" only the alternatives case fails; and against an absent record with an `ADR-074-something.md` present, all six fail. No case passes vacuously. The check-map entry is required rather than convenient: `owner_of` at `scripts/run_checks.py:545-549` matches on component boundaries, so `.githooks/README.md` matched no prefix before the entry and `run_checks.py` would have refused its plan with `unknown-ownership`, which is what study.md:242-243 predicts; `root` is also the owner that runs the tests asserting on `.githooks`, where `ci` declares no checks at all. All 2554 tracked paths resolve to exactly one scope, with no duplicate owner prefix and no undeclared scope, and `python3 scripts/run_checks.py --plan --base 8dc3aca5` accepts the plan at exit 0 with `.githooks/README.md` resolving to `root`. The shipped study's delta was proved by reconstruction rather than eyeballed: substituting `](../<skill>/SKILL.md)` for `](../../plugins/hexaemeron/skills/<skill>/SKILL.md)` on the five phase skills turns `.hexaemeron/study.md`, SHA-256 `4584dfe388e41a7aa0898347b02c61b1561a59e9c09c71ec2b57c40a41cc624b` and matching the packet pin, into the shipped copy byte for byte; both files are 519 lines, no claim, number, caveat or command changed, all five rewritten targets resolve, all five sibling targets under `docs/` are absent so H001 would indeed have reported them, and the rewritten form is the convention ten other shipped documents already use. `docs/commit-gate/design-evidence.json` and all forty reports are byte-identical to their `.hexaemeron/` sources; all forty cited reports resolve with matching SHA-256 digests, no report on disk is uncited, and the four remaining result rows are declared `pending` on `index-mutation-regression` blocking `step:4` with the resolver command the record names. The `design-lock` fence at runbook.md:67-71 carries `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` and candidate `hooks-path-plus-visibility`, matching the packet's design evidence and its selection. The record's measured figures match the reports that carry them: 497 bytes for `installed-hooks`, 40 for `hooks-path` and `hooks-path-plus-visibility`, 22 milliseconds for the chosen design. Its Context claim was verified directly: the clone's `.git/hooks` holds 14 files and 0 non-`.sample` entries. No `ADR-074-*.md` exists anywhere in `docs/decisions/`, and every `ADR-074` occurrence in the tree is prose. The commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. No file under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, and `.github/` is untouched, so ruleset `21830871` is not affected.

## Step 1, round 2 -- 2026-09-04T03:05:35Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=not-applicable; hook-index-inheritance=not-applicable; hook-config-inheritance=not-applicable; worktree-record-crossing=not-applicable; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=not-applicable; ci-cannot-see-activation=reviewed

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. The gate surface has not moved since round 1, so the same five register concerns still have nothing to read: `.githooks` holds `README.md` alone, with no `pre-commit`, no `greenlight` and no green record. Also not checked: the five acceptance conditions, which steps 2 through 5 deliver. Also not checked: `python3 scripts/run_checks.py --full`, which round 1 ran green on `3afc109b` at 28 checks; this round ran the declared `root-suite` twice instead, and the three commits since do not touch a path outside the `root` scope. Also not checked, unchanged from round 1: ruleset `21830871` and the workflow states, carried as recorded; whether run #856 still claims ADR-074 on `origin/main`; the untracked `.hexaemeron/design/` fixtures; and issues #888 and #582 with the ADR-050 and ADR-024 history the decision record cites. Also not checked: whether the step 4 refresh will in fact carry every amendment recorded by then, which only step 4 can show.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-05 | high | .horos/boundary.json | Round 1's own record commit `01e637ca` added two tracked files, `audit/rounds/fiat-857-framework-16-the-commit-gate-lives-in-one-cl.md` and its synopsis, without regenerating the reading boundary. Any added tracked path moves `files_walked` whether or not it is a candidate, which `tests/test_boundary_currency.py:57-73` states in its own comment. The repository suite therefore exited 1 on the stacked branch this round, `Ran 1138 tests`, `FAILED (failures=2)`, both on `.horos/boundary.json#counts`: `test_boundary_currency.BoundaryCurrencyTests.test_the_committed_boundary_matches_a_fresh_scan` and `test_agent_instruction.AgentInstructionScaffoldTests.test_horos_boundary_is_current_for_the_scaffold`. Round 1's receipted exit 0 was true of the tree it ran against and stopped being true when the record landed on top of it, which is the gap between a green receipt and a green branch. `files_walked` moves 2538 to 2540, exactly the two files. | fixed in 5ea0449691a0f07652af4c7946c75f6b0002077f |
| S1-R2-01 | medium | docs/commit-gate/study.md | S1-R1-01 is not closed, on three counts. First, the remedy is authored only in `.hexaemeron/study.md`, which is self-ignored and never reaches the repository; the shipped copies still carry their pre-amendment bytes by design and the refresh is step 4's work, so nothing a reader holds has changed at `01e637ca`. Second, the enumeration is short: the shipped study carries seven `.hexaemeron/` references, at lines 5, 256, 329, 411, 412, 465 and 467, and the amendment names five of them. Line 329 is the `design_evidence.py --transition design-lock` command and line 465 is the `evaluate_commit_gate.py` command; both open their code span on the preceding line, which is why a grep anchored on a backtick missed them and why round 1's own count of ten was itself short. The true count across both shipped documents is twelve, not ten, and this record corrects round 1 rather than restating it. Third, the runbook's own five references, at `docs/commit-gate/runbook.md` lines 3, 43, 83, 178 and 189, receive no mapping from either amendment, and the step 4 amendment's Why narrows the finding to "the shipped study sends a reader to `.hexaemeron/` five times", so the runbook half is out of the remedy's scope as written. What the amendment does do is sound: the mapping rule is correct for the design record, the forty reports and the runbook, and it is right that the evaluator is unshipped, which `git ls-files` confirms at zero hits for `evaluate_commit_gate`. | open, remedy authored and partly scoped; needs the runbook half and the two unnamed lines |
| S1-R2-02 | medium | .hexaemeron/runbook.md | The round 2 Warden packet has lost step 1's amendment. Round 1's packet carried `amendments` of length 1 with `effective_sha256` `380f26ff` against baseline `020efe2a`, and its step 1 markdown carried the `### Amendment -- 2026-09-04` block. Round 2's packet reports `amendments` empty and `effective_sha256` equal to the baseline `020efe2a`, and its step 1 markdown carries no amendment block, so it hands back the superseded Exit "byte-identical copies of the two `.hexaemeron/` artefacts". The committed tree does not satisfy that Exit and was never meant to: the shipped study differs on five link targets and the shipped runbook is now 5247 bytes short of its source. The amendment itself is intact in the artefact at `.hexaemeron/runbook.md:290-298`, ending `**Steps touched.** Step 1.`, so this is the step view rather than the record. Inferred, not established: the first amendment records `runbook_sha256` `1d11b527`, which the second amendment superseded, and a view that matches an amendment against the current runbook digest would drop it exactly here. | open, controller-side; the artefact is correct and the packet is not |
| S1-R2-03 | low | .hexaemeron/runbook.md | Step 4's replaced Exit is only half provable by a command. It names `python3 -m unittest tests.test_commit_gate -v` and `python3 scripts/run_checks.py --full`, neither of which reads `docs/commit-gate/`, and then proves the refresh itself by "a byte comparison of each refreshed copy against its source reported in the step's commit message". No command can make that comparison from the repository, because `.hexaemeron/` is gitignored and absent from every clone, so the evidence is prose written by the party it certifies. That is the shape the run's own decision record warns about at its Consequences, where a record that supplies its own expected value verifies against itself. A repository-side assertion is available instead: a test that reads `docs/commit-gate/study.md` and `docs/commit-gate/runbook.md` and requires each to carry the amendment headings it must carry needs nothing outside the tree. Writing it here would land step 4's work early, so it is named rather than done. | open, route named |
| S1-R2-04 | low | .hexaemeron/study.md | The study amendment says "the budget command at line 467 cannot be re-run from the repository alone". Line 467 is the report path `.hexaemeron/reports/hooks-path-plus-visibility-commit-overhead.json`; the command is at lines 465 and 466, `python3 .hexaemeron/design/evaluate_commit_gate.py --candidate hooks-path-plus-visibility --criterion commit-overhead`. The substantive claim holds and the line number does not. For contrast, the other unnamed command, at lines 328 and 329, is re-runnable after the mapping is applied, because `design_evidence.py` ships at `plugins/hexaemeron/skills/protasis/scripts/design_evidence.py` and its argument maps to `docs/commit-gate/design-evidence.json`. | open, pinned bytes |

Leads not pursued: the four round 1 findings this round did not re-open are carried as follows, each re-checked rather than assumed. S1-R1-02 stays fixed: `.githooks/README.md` on the stacked branch reads that the root suite is to fail and names step 4 as the step that delivers the assertion. S1-R1-03 is answered as an erratum rather than a repair, which is the right disposition for a preamble line, and the controller independently confirmed `tests/test_decision_records.py:110` at the base, at `3afc109b` and in the working tree. S1-R1-04 is now scheduled rather than merely accepted: step 4's replaced Files list is `tests/test_commit_gate.py`, `AGENTS.md`, the draft decision record, `docs/commit-gate/study.md`, `docs/commit-gate/runbook.md` and `.horos/boundary.json`, so both shipped copies are named in the step that refreshes them, and its replaced Exit says they are refreshed to the bytes receipted at that step's entry. The residual is recorded by the amendment itself and is not filed again here: an amendment recorded after step 4 stales the refreshed copies once more, and step 5 already lists the runbook copy among its files. S1-R1-05 is unchanged and stays accepted. Second lead, unchanged from round 1 and re-checked: `.horos/candidates.json` is left as it stands, because the only entry a fresh scan adds is `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md`, tracked at the base and absent from the base's `candidates.json`, so it is run #859's drift; this round regenerated `.horos/boundary.json` and restored `candidates.json` rather than carrying that entry in, which is the same choice the step commit made. Third lead: the study still carries no `design-bridge` fence and cannot while the record is unnumbered, since H008 accepts only an ADR-numbered record or a skill `EVOLUTION.md`. Fourth lead, per the Elenchus contract: the fixes commit `5ea0449691a0f07652af4c7946c75f6b0002077f` changed no test files, so the guard check returns `unguarded` with detail "the commit changed no test files"; the guard already exists and was the thing that caught this, `tests/test_boundary_currency.py` and `tests/test_agent_instruction.py` both failing on the stale boundary, so a new test would duplicate a working guard rather than add one. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 over all 50 changed paths and print `clean`; the declared `root-suite` `python3 -m unittest discover -s tests` exited 1 on the tree as received, `Ran 1138 tests in 177.073s`, `FAILED (failures=2)`, and exits 0 on the fixed tree, `Ran 1138 tests in 100.503s`, `OK`, with the same 1138 cases in both runs so nothing was skipped into green. `python3 plugins/horos/skills/horos/scripts/horos.py check .` now prints "boundary matches the tree" at exit 0, and the two previously failing cases pass when run alone. Both amendments were verified append-only rather than taken on trust: the amended study is the pre-amendment source plus 1757 bytes, reconstructed by reversing the five link rewrites on the shipped copy and confirming the amended source starts with the result, and the amended runbook starts with the shipped copy byte for byte with 5247 bytes appended. The risk register is byte-identical between the round 1 and round 2 packets at all ten ids, so no concern was added, dropped or reworded under the amendments. The design lock is unchanged: `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. The shipped `docs/commit-gate/` tree is byte-identical to round 1 in every file, which is what "neither amendment touched the tree" means and is why S1-R2-01 cannot yet be closed. Both round 2 commits are authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carry exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, and `.github/` is untouched, so ruleset `21830871` is unaffected.

## Step 1, round 3 -- 2026-09-04T03:15:04Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=not-applicable; hook-index-inheritance=not-applicable; hook-config-inheritance=not-applicable; worktree-record-crossing=not-applicable; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=not-applicable; ci-cannot-see-activation=reviewed

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. The gate surface has not moved since round 1, so the same five register concerns still have nothing to read: `.githooks` holds `README.md` alone. Also not checked: the five acceptance conditions, which steps 2 through 5 deliver, and `ShippedCopyTests` itself, which does not exist until step 4 writes it, so this round judges the specification rather than the code. Also not checked: `python3 scripts/run_checks.py --full`, last run green on `3afc109b` at 28 checks; the four commits since touch no path outside the `root` scope, which is an argument rather than a run. Also not checked, unchanged: ruleset `21830871` and the workflow states, carried as recorded; whether run #856 still claims ADR-074 on `origin/main`; the untracked `.hexaemeron/design/` fixtures; and issues #888 and #582 with the ADR-050 and ADR-024 history.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: this round opened nothing and closed four of the five findings it inherited, each re-checked against the tree rather than against the amendment text. S1-R2-05, high, is closed: `.horos/boundary.json` is current at the branch tip, `horos check` prints "boundary matches the tree" at exit 0, and per commit the walked-file count sits at its baseline offset of 16 below the tracked count at `5b0acf15`, `5ea04496` and `9f52bcff`, and at 18 only at `01e637ca`, which is the one commit the finding named. That commit stays historically stale and no later commit can change it; a bisect through this branch still lands on it. The new Exit clause requiring the boundary to stay regenerated across the audit rounds' commits is the right instruction, but it is an instruction rather than a guard: nothing rescans at commit time, the tracked gate that would is step 2's work, and `tests/test_boundary_currency.py` fires only when someone runs the suite, so the clause holds exactly as long as each round runs the suite after its own commits, which this round did. S1-R2-04, low, is closed: the study amendment now reads "at lines 465 and 466; line 467 is its report path". S1-R2-02, medium, is closed for this run: the packet reports `amendments` of length 1 with `effective_sha256` `24bbdec0` against baseline `020efe2a`, its step 1 markdown carries the amendment block and the boundary clause, and the recorded mechanism, a runbook amendment bound to the study digest current when it was recorded, matches the evidence round 2 gathered. The standing hazard is not closed and is not this step's to close: any future study amendment unbinds every runbook amendment recorded before it, so the ordering discipline holds only while someone remembers it. That belongs to the Fiat frontier rather than to this run, and it is named here because the run is where it was observed. S1-R2-03, low, is closed as filed: step 4's Exit now proves the refresh through `ShippedCopyTests`, three cases that read only files inside the repository, so a stranger with a clone alone can run `python3 -m unittest tests.test_commit_gate -v` and get the answer, which the previous byte comparison in a commit message could not give. Two limits on that proof are worth stating and neither is worth a round. The two heading-count cases carry a literal expected value written by the same step that performs the refresh, so they catch a refresh that fails to land at step 4 and not a copy that goes stale after it, which is the residual the amendment already records and step 5's file list already touches. The third case, which requires every `.hexaemeron/` reference in the shipped study to be accompanied by the mapping in the same file, is the strongest of the three and is scoped to the study; the shipped runbook's own two mapped references, at its lines 3 and 83, get no equivalent assertion and their mapping lives in a sibling file. After the step 4 refresh a diligent reader is covered, because line 3 names a study that sits beside it and line 83 is followed in the same file by the amendment blocks that replace it, but nothing asserts that. S1-R2-01, medium, is answered rather than applied, and this record does not call it closed. Both parts the round 2 review left open are now settled on paper: the enumeration is complete and correct at seven, verified independently against the shipped study, whose `.hexaemeron/` references sit at lines 5, 256, 329, 411, 412, 465 and 467 and nowhere else; and the ruling on the shipped runbook's three remaining references is right. Lines 43, 178 and 189 name `.hexaemeron/reports/hooks-path-plus-visibility-index-mutation-regression.json`, and the controller's own design record carries a pending row for the selected candidate whose record-relative report is `reports/hooks-path-plus-visibility-index-mutation-regression.json`, which against `.hexaemeron/design-evidence.json` resolves to exactly that path, so step 3 must write there for the design checker to clear the `step:4` block. Those three are working paths for the run, not citations offered as evidence, and they are correct as they stand. What remains is only that the shipped tree at this commit still carries the defect: `docs/commit-gate/study.md` holds its seven references with no mapping, because the refresh is step 4's work and this step's Exit forbids editing the shipped copies. It is accepted and scheduled rather than open for a further round, with step 4's Files naming both copies and `ShippedCopyTests` guarding the result. One forward-looking gap this round found and did not file as a finding, because it does not touch step 1's diff or its Exit: no step's Files list refreshes `docs/commit-gate/design-evidence.json` or `docs/commit-gate/reports/`, so once step 3 resolves `index-mutation-regression` in the controller's record the shipped copy will keep four `pending` rows the controller no longer has, and the report path each names under `docs/commit-gate/reports/` will never land. Nothing is misrepresented, because a pending row is declared pending and carries a resolver command a clone can run, and step 1's Exit is untouched, since the four pending rows carry a plain string rather than the `path` and `sha256` citation the forty resolved rows carry. The route, if the controller wants it closed, is to add both paths to step 3's or step 4's Files. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 over all 50 changed paths and print `clean`; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1138 tests`, `OK`, run against the tree as it stands with this record committed rather than before it. Both new amendments were verified append-only rather than taken on trust: the study is its previous state plus one block and the runbook its previous state plus two, with every earlier byte unchanged, and the risk register is byte-identical across all three packets at all ten ids, so no concern has been added, dropped or reworded across the five transitions. The design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. The shipped `docs/commit-gate/` tree is byte-identical to round 1 in every file, which is what "neither amendment touched the tree" continues to mean. The round 1 and round 2 records above are unchanged beneath this append. Nothing under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, and `.github/` is untouched, so ruleset `21830871` is unaffected.

## Step 2, round 1 -- 2026-09-04T07:27:52Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression is not written here and is not this step's to write, so `hook-index-inheritance` and `hook-config-inheritance` are reviewed against the shipped scripts rather than closed by a guard; `ci-cannot-see-activation` is step 3's hosted half and has nothing to read at this commit. Also not checked: greenlight invoked through a symlink or from `PATH`, where `dirname "$0"` names the link's directory rather than the repository, which the documented `.githooks/greenlight` invocation does not reach; the gate under `git rebase`, `git merge` and `git cherry-pick`, none of which run `pre-commit`; behaviour under a non-POSIX `/bin/sh`; and a record larger than memory, since `recorded="$(cat "$record")"` reads it whole. Carried from step 1 under `--no-further-leads` and not re-litigated: the shipped copies under `docs/commit-gate/` still cite `.hexaemeron/` paths no clone holds, which step 4 refreshes, and the four named judgements.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | .githooks/greenlight | `cd "$(git rev-parse --show-toplevel)"` is not an anchor. Git exports `GIT_DIR` into every hook and every command it runs, and with it set `git rev-parse --show-toplevel` returns the caller's directory while `git write-tree` and `git rev-parse --git-dir` still resolve through `GIT_DIR` to the other repository. Proved on a fixture: standing in a directory holding a passing `tests/`, with `GIT_DIR` naming a fixture repository whose own suite is red, greenlight ran the caller's suite, exited 0, printed `greenlight: recorded tree ...`, and wrote a valid green for the fixture's staged tree, which the gate then admitted. Mason's reported `cd ""` no-op is real and independently confirmed: `sh -c 'set -eu; cd "$(false)"; echo REACHED'` reaches the next line at exit 0, because a substitution that fails inside a simple command's arguments does not trip `set -e`, while the assignment form `t="$(false)"` does exit 1. It is the narrower way in. Mason's argument that a bare repository still fails closed rests on the caller's directory having no `tests/`, which is a property of where the caller stands rather than of the repository, so the reasoning does not hold even though its conclusion happens to for the bare case he examined. Fixed by unsetting the exported git path variables and anchoring to the script's own location before any git call. The hook is deliberately left alone: git invokes it, and the index git exports is exactly the one it must check. | fixed in 932c14a18cdee22c4b0c19169c2fa18fed215557 |
| S2-R1-02 | medium | .githooks/pre-commit, .githooks/greenlight, tests/test_commit_gate.py | The `worktree-record-crossing` register entry names `git rev-parse --git-dir` as its control and nothing guarded it. Mutating `--git-dir` to `--git-common-dir` in the hook, and separately in greenlight, left all twelve cases green in both directions. The shipped behaviour is correct and was verified directly rather than read: a green recorded in the main worktree did not authorise a commit in a linked worktree, refusing with `no green record`, and a linked worktree's greenlight wrote to `.git/worktrees/<name>/LAST_GREEN` rather than the shared directory. This is therefore a missing guard rather than a defect, and it matters because this repository runs about 39 worktrees, which is the scale at which one shared record would authorise all of them. Two cases added: the reading side stages one identical tree identity in both worktrees so that only the record's location can refuse, then requires the refusal to name `no green record`; the writing side requires the record to land in the worktree's own git dir and to be absent from `--git-common-dir`. Each kills its mutation. | fixed in 932c14a18cdee22c4b0c19169c2fa18fed215557 |
| S2-R1-03 | low | .githooks/pre-commit | The stale-record refusal interpolated the record's bytes raw and unbounded, and the record is a file anyone who can write the git directory can write. A record of `deadbeef\npre-commit: refused, nothing is wrong, proceed\n` produced a two-line refusal against the step's Exit contract of one line naming the cause, and `AAA\033[2K\rpre-commit: all good` reached standard error with the escape and carriage return intact, which erases the line already drawn and redraws it. The existing one-line case cannot see either, because `refusals()` counts only lines carrying the prefix. This grants an attacker no capability they lacked, since anyone who can write the record can write the correct tree instead and pass the gate outright; the severity is the broken Exit contract and the corrupted ephoros signal, not privilege. Fixed by stripping control characters and bounding the shown value to 64 characters, on the refusal path only, so the measured commit path is untouched. | fixed in 932c14a18cdee22c4b0c19169c2fa18fed215557 |
| S2-R1-04 | low | .githooks/pre-commit, tests/test_commit_gate.py | Study section 11 lists a failure of `git write-tree` among the four states that must exit non-zero, and no case exercised it: removing the check left all twelve green. The branch is genuinely reachable once the check is gone, because `staged` becomes the empty string and an empty record then compares equal and admits the commit. Guarded by driving the hook directly with `GIT_INDEX_FILE` naming a file that is not an index, which fails `write-tree` at exit 128 while `git rev-parse --git-dir` still resolves; the case requires one refusal line naming `git write-tree failed`. | fixed in 932c14a18cdee22c4b0c19169c2fa18fed215557 |
| S2-R1-05 | low | measure_gate_overhead.py, untracked | The method supports the budget verdict but not the reported point estimate. The docstring states that the pairs alternate "so drift in machine load falls on both halves"; the loop times the gated arm first in all nine pairs and never alternates, so the file claims a property its code does not have. With no warm-up pair discarded the first difference carries cold-start cost: 131.73 ms as reported, and 292.86 ms, 498.09 ms and 298.18 ms across three runs here, each of the three above the 200 ms budget the measurement exists to check. Mason's cold `sh` exec attribution, which he labelled an inference, is confirmed rather than accepted: an alternated variant discarding three warm-up pairs drops the maximum to 62.55 ms and 78.71 ms and holds the median at 49.82 ms and 48.97 ms. The budget conclusion stands at every reading taken, 16.45 ms reported, 22 ms recorded at study section 10 and about 49 ms here under load, against 200 ms; the median is machine-dependent and study section 10 records it as a bare number with no conditions or dispersion beside it. | accepted, the script is untracked so no branch byte carries the defect, and the budget verdict is unaffected |

Leads not pursued: mutation testing drove thirteen mutations, each applied to a restored tree and reverted before the next, and it is the evidence for the two findings whose guards pass on the unfixed parent. Nine were killed by the twelve shipped cases: dropping the stale-tree comparison, which independently confirms Mason's report and is caught by `test_a_commit_of_any_other_tree_is_refused` and `test_each_refusal_names_its_own_cause_on_one_line`; dropping the no-record check; dropping the unreadable-record check; renaming the bypass token; making the gate exit 0 unconditionally; recording before the suite runs; ignoring the suite's exit with `|| true`; and dropping `set -e` from greenlight. Four survived, and are S2-R1-02 and S2-R1-04: `--git-dir` to `--git-common-dir` in the hook, the same in greenlight, dropping the `git write-tree` failure check, and deleting greenlight's `cd` line outright, which is untested because every case runs greenlight with the repository as its working directory. All four are killed on the fixed tree. Fail-closed was proved rather than inferred at nine record states the cases do not reach: empty, whitespace-only, `not-a-tree-id`, the correct tree followed by a second line, the correct tree with a leading space, a dangling symlink, mode 000, and a truncated twenty-character prefix all refuse; the correct tree followed by extra blank lines is admitted, which is right, because command substitution strips trailing newlines. A suite failing after two cases pass writes nothing and leaves the earlier green byte-identical rather than updating it to the untested tree, and the commit of that tree is refused. `git commit -a` was checked because it is the common contributor path and builds a temporary index: with a green naming the index tree and an unstaged modification, the hook reads the temporary index git exports and refuses on the tree actually being committed, and admits it once that tree is green. That is the case for leaving the hook's inherited environment alone, and is why S2-R1-01 changes greenlight only. `hook-config-inheritance` was probed against the fixed greenlight rather than left to step 4: `core.worktree` supplied through `GIT_CONFIG_COUNT` with `GIT_CONFIG_KEY_0` and `GIT_CONFIG_VALUE_0`, and again through `GIT_CONFIG_PARAMETERS`, no longer redirects it, because the anchor is established before any git call; the step 4 regression is still owed and is not claimed here. Three leads were seen and left: greenlight reached through a symlink or `PATH`, where `dirname "$0"` names the link's directory, which the documented `.githooks/greenlight` invocation does not reach; a green record larger than memory, since `recorded="$(cat "$record")"` reads it whole, which needs a git directory writer who already has the stronger move; and `.horos/boundary.json`, which this round did not need to regenerate, since it lists 128 sink entries and neither the two script edits nor this record earns one, confirmed by the currency cases passing in both suite runs. On the Elenchus verdict, one distinction is worth keeping: `guarded` rests on four assertion failures with zero errors across 1157 executed cases on the detached parent, and those four are the guards for S2-R1-01 and S2-R1-03, which were behaviour defects. The guards for S2-R1-02 and S2-R1-04 pass on the parent by design, because those findings are missing coverage over already-correct behaviour, so their evidence is the mutation result above rather than the parent run. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean`, run twice, once against the tree as received and once against the fixed tree, over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 on the tree as received at `Ran 1150 tests in 207.045s`, `OK`, and exits 0 on the fixed tree at `Ran 1157 tests in 138.627s`, `OK`, the seven added cases accounting for the difference exactly; and the step's other Exit command, `python3 scripts/run_checks.py --full`, reports `outcome green` at exit 0 over 28 checks with `root-suite passed 294.0s`, run against the tree as it stands with the fixes committed. The design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. Both tracked scripts remain `100755`. The fixes commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, and every fixture ran under the vetted scratch helpers, leaving the outer tree clean.

## Step 2, round 2 -- 2026-09-04T08:12:48Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression and step 3's hosted half remain their steps' work. Two of round 1's three unchecked items are now checked rather than carried: the gate behaves identically under `/bin/dash`, `/bin/ksh` and `/bin/bash` across ten cases including the anchor and the sanitising, and both scripts parse under all four shells present, so the non-POSIX `/bin/sh` concern is closed; greenlight reached through a symlink or `PATH` is characterised below and judged not worth a fix. The third, a green record larger than memory, is named and left: `recorded="$(cat "$record")"` reads the file whole, but it needs a git-directory writer who already holds the stronger move of writing the correct tree, and the failure is the commit not completing, which is the direction the gate is meant to fail. One mechanism is inferred rather than observed and is labelled so in S2-R2-02: the recurrence signature points at the operating system's first-execution assessment of a new executable, and no `syspolicyd` instrumentation was taken to confirm it; the measurements themselves are recorded, not inferred. All figures come from one loaded macOS arm64 machine.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | .githooks/greenlight, tests/test_commit_gate.py | Round 1's fix for S2-R1-01 has two halves, and only the anchor was guarded. Removing the `unset` while keeping `here="$(dirname "$0")"` and the `cd` left all twenty-five cases green, and the line is not redundant: the anchor settles which suite runs, and nothing else settles which index `git write-tree` reads. Reproduced on two fixtures, A carrying a passing suite and B carrying an untested staged tree `e8276bcbdaf1ad8be431ba7a899079ff82813adf`: running A's greenlight from A with `GIT_DIR` naming B exited 0, reported `Ran 1 test`, `OK`, and wrote B's untested tree into B's record. The shipped script does not do this, recording A's own tree `755804afd7f18488364bf794cdeffa89bc1ceaf9` in A's own record and leaving B's absent, so this is a missing guard over correct behaviour rather than a live defect. It is the same class as S2-R1-01 and reaches the same end, a green for a tree no suite validated, which is why it is filed at the same severity. The new case runs greenlight in its own repository with `GIT_DIR` naming a second one and requires the second's record to stay absent and the first's to name its own tree. | fixed in d2e67deac3d3aaa00f64689be01854b262e83434 |
| S2-R2-02 | medium | .hexaemeron/study.md section 10, budget clause | The budget as written is breached, and it is the gate that is cold rather than the harness. The clause reads "The gate adds at most 200 milliseconds to a commit whose tree is already recorded green" and carries no qualification to a median or a steady state, so a first commit falls inside its own terms. Three separations were run rather than argued. Timing the ungated arm first in every pair leaves the outlier on the gated arm, 52.45 ms ungated against 682.89 ms gated at pair 0, so it follows the gate and not the position, and round 1's non-alternating loop is a real harness defect but not the cause. A cold `sh` exec costs 26.41 ms against a warm median of 14.86 ms, an eleven-millisecond difference, so Mason's cold-exec attribution is short by an order of magnitude and does not explain the number it was offered for. Across three independent fresh repositories, each given five ungated warm-up commits before the gate was activated, the first gated commit cost 219.61 ms, 253.05 ms and 322.49 ms over the warm ungated baseline, all three above 200 ms, against a steady-state overhead of 52.48 ms. The cost is per hook file rather than per session: rewriting `.githooks/pre-commit` as a new file mid-run brought it back at 275.40 ms and 429.28 ms while neighbouring commits sat at 73 to 92 ms. It is not caused by this run's fixes and no gate design avoids it: a hook whose entire body is `exit 0` costs 182.79 ms on its first execution, the step-2 original pre-commit 306.39 ms, and the round 1 fixed pre-commit 200.01 ms, with steady-state overhead 31.91 ms and 32.49 ms respectively, so the fix is free in the steady state that matters. The contributor consequence is that the charge falls on the first commit after a clone and again after any pull that changes the hook, which is exactly the moment the budget exists to protect. The remedy is to state the budget the numbers support rather than to change the gate, and the study is controller-pinned and append-only, so this is not a branch edit: scope the clause to the steady state and record the one-time cost beside it, naming the measured range and the fact that a do-nothing hook pays it too. | open, controller-side; the study's budget clause needs amending through the controller, and no branch byte carries the defect |

Leads not pursued: the four round 1 fixes were re-driven by the method that found them rather than read off the diff, and all four hold. The `GIT_DIR` reproduction that forged a green in round 1 now exits 1 and writes nothing, against a fixture whose own suite is red, under `GIT_DIR` alone, under `GIT_DIR` with `GIT_INDEX_FILE` and `GIT_PREFIX`, and under `GIT_WORK_TREE` pointed at the caller; the control, the same fixture made green, still records its own tree `2974ff7ba7e69b27421dfb0b073d11c929fac57b`. A forged two-line record and an `AAA\033[2K\rpre-commit: all good` record each produce exactly one line with no ESC and no CR present, and a 4000-byte record yields a 259-byte line. A non-index `GIT_INDEX_FILE` is refused naming `git write-tree failed`. Two worktrees staging one identical identity `1a9b504a0dd2dd98ef818ea2cfbc3ef4395f9811` still refuse the linked commit with `no green record`, which is the record's location refusing rather than a tree mismatch. The nine-state fail-closed battery reproduces round 1 exactly, and the tree-plus-junk state now renders on one line, which is the S2-R1-03 fix visible in the battery. This round corrects one piece of its predecessor's evidence rather than restating it. Round 1 reported greenlight's `--git-dir` to `--git-common-dir` mutation among its four survivors and then reported all four killed on the fixed tree. Both readings were artefacts of the harness: the substitution was unanchored and matched the first occurrence, which in both the original and the fixed greenlight is the comment at line 11, so the mutation never reached the code at line 48, and the round 1 confirmation run additionally restored to a HEAD that did not yet carry the fixes, so its kills came from the unfixed script rather than from the mutation. Re-run against the code line on the fixed tree, the mutation is killed by `test_greenlight_records_into_the_worktrees_own_git_dir`. The conclusion round 1 drew is therefore correct and the evidence it drew it from was not. Round 2's mutation set is fifteen: all eight against `pre-commit` are killed, including un-sanitising the shown value, which `test_a_stale_refusal_stays_one_line_whatever_the_record_holds` and `test_a_stale_refusal_carries_no_control_characters` catch; six of seven against greenlight are killed; the sole survivor was the `unset` line, which is S2-R2-01 and is killed by the case added this round. One observation on the guards rather than the gate: mutating away the script anchor while keeping the `unset` is caught, but slowly, because the fixtures live under the repository's own `tmp/` and an unanchored greenlight walks up to the real repository and runs its full 1157-case suite inside the test. That is a property of the mutation and not of the shipped script, and it is recorded so that a future round reading a five-minute timeout does not mistake it for a hang. Three leads were examined this round and closed by judgement rather than by a fix. Greenlight through a symlink or `PATH` fails closed in every variant but one: `$0` carries the resolved path of the link rather than of the script, so a link outside any repository and a bare-name `PATH` invocation both exit 128 with nothing recorded, and only a link placed one level inside a second repository succeeds, whereupon it greenlights that second repository. That case is honest, because the suite it ran and the tree it recorded are the same repository, so no record names a tree its suite did not validate, which is the property the gate exists to hold and the one S2-R1-01 broke; the invocation also names the second repository on disk. A portable fix would mean hand-rolled symlink resolution in POSIX `sh`, which has no `readlink -f`, added to code that runs on every contributor's machine, and that trade buys complexity in exchange for closing a path that is already truthful. Shell portability is now checked rather than deferred: both scripts parse under `sh`, `bash`, `ksh` and `zsh`, and the ten-case behavioural battery is identical under `/bin/dash`, `/bin/ksh` and `/bin/bash`, which covers the Debian and Ubuntu `/bin/sh` a contributor or runner is most likely to differ on. The oversized record is named and not pursued, for the reason given under Not checked. On the Elenchus verdict, `passed` is the value the runner returned and it is carried unchanged: the guard added this round passes on the parent, because S2-R2-01 is missing coverage over behaviour that is already correct, so there is no parent failure for it to assert and `guarded` would be the wrong word. Its evidence is the mutation result and the two-fixture reproduction above. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; `python3 -m unittest tests.test_commit_gate` is green at 26 cases; and the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 on the tree as it stands with both this round's commits applied, at `Ran 1158 tests in 216.683s`, `OK`. The Elenchus report records 1158 executed cases, 0 assertion failures, 0 errors, 0 skipped, complete. The design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`, both tracked scripts remain `100755`, and `.horos/boundary.json` needed no regeneration, confirmed by `horos check` printing "boundary matches the tree" at exit 0. Both round 2 commits are authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carry exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, every fixture ran under the vetted scratch helpers, and the working tree was verified byte-identical to HEAD after each mutation run.

## Step 2, round 3 -- 2026-09-04T08:30:02Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression and step 3's hosted half remain their steps' work. This round re-drove S2-R2-01 and audited the study amendment that answers S2-R2-02; it did not re-run round 2's full fifteen-mutation set or the nine-state fail-closed battery, both of which passed against these same bytes one round ago and neither script has changed since `d2e67dea`, confirmed by `git diff`. The oversized-record lead stays where round 2 left it. Two limits on this round's own measurements are stated rather than left implied: the machine was substantially quieter than in round 2, which is itself one of the findings below, and the attribution of the first-execution cost to the operating system remains inferred from the per-file recurrence signature with no instrumentation of the responsible service, exactly as the amendment records.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | .hexaemeron/study.md, budget amendment | The steady-state pair is misattributed, and it is cited for a claim it cannot carry either way. The amendment reads "Steady state measured 31.91 milliseconds for a do-nothing hook and 32.49 for the shipped gate on the same machine, so no gate design avoids the first-execution cost and the shipped gate adds nothing measurable to it". Round 2 measured 31.91 ms as the steady-state overhead of the step-2 **original** `pre-commit`, not of a do-nothing hook; the do-nothing hook measured 10.55 ms in that same run. Re-measured this round over three fresh repositories each, a hook whose whole body is `exit 0` carries a steady-state overhead of 6.49, 2.13 and 7.03 ms, median 6.49, against the shipped gate's 18.97, 23.20 and 20.57 ms, median 20.57. What the pair 31.91 and 32.49 does support is the narrower and still useful claim that round 1's fix costs nothing over the gate it replaced. It does not support "adds nothing measurable": the gate's own logic costs about 14 ms today and about 21 ms in round 2 over a hook that does nothing. Separately, and true under either reading of "to it", steady-state figures cannot evidence a first-execution claim, which is the claim the sentence attaches them to. The budget verdict is untouched, because 20.57 ms sits far inside the 200 ms steady-state clause. Round 2's own wording invited the misreading and this record says so: it listed three hooks and then wrote "steady-state overhead 31.91 ms and 32.49 ms respectively", leaving the antecedent ambiguous. | open, controller-side; the sentence needs the correct attribution and the narrower claim |
| S2-R3-02 | low | .hexaemeron/study.md, budget amendment | The scoping phrase does not match the measured behaviour and contradicts the sentence after it. The corrected clause reads "once the hook file has been executed at least once on that machine". The cost is per file instance, not per machine and not per content: three clones of one origin on this machine, every `pre-commit` byte-identical at SHA-256 `b7571a0ad138`, each paid the first-execution cost again at 159.05, 165.57 and 152.28 ms against the origin's own 158.53 ms. Round 2's three independent fresh repositories showed the same thing from the other direction. The next sentence states the behaviour correctly, that the cost is paid again whenever the hook file is rewritten so a contributor meets it after a clone, so the amendment carries both the wrong rule and the right one two sentences apart, and a reader who stops at the budget clause takes the wrong one. Dropping "on that machine" and scoping to that hook file resolves it. | open, controller-side |
| S2-R3-03 | low | .hexaemeron/study.md, budget amendment | The first-execution range is stated without the load condition that produced it. Round 2's 219.61 to 322.49 ms were measured while this machine was running the 1150-case root suite and concurrent work. Re-measured this round with the machine quiet, the shipped gate's first-execution overhead ran 150.13 to 250.40 ms and a do-nothing hook's 137.11 to 140.75 ms, so part of the range now sits inside the 200 ms budget. The breach itself stands and is not in question: round 2's three trials all exceeded 200 ms and this round still produced a 250.40 ms first execution. What overstates is the precision, because a bare "219.61 to 322.49 milliseconds" reads as the cost rather than as one loaded machine's cost. The amendment attaches "one loaded macOS arm64 machine" to the attribution sentence only; the range needs it too. | open, controller-side |
| S2-R3-04 | low | .hexaemeron/study.md, budget amendment | `**Steps touched.** Step 5` names a step whose Files cannot carry the correction. Step 5's Files are `docs/commit-gate/demonstration.md`, `docs/commit-gate/runbook.md` and `.horos/boundary.json`, and a study amendment reaches a reader only through `docs/commit-gate/study.md`, which step 4 refreshes. Step 4's re-issued Complete replacement Files does list `docs/commit-gate/study.md`, and its replacement Exit requires every artefact under `docs/commit-gate/` to be refreshed to the bytes receipted at that step's entry, which this amendment precedes. Nothing is therefore lost operationally and the correction will ship: this is an accounting line pointing at the wrong step rather than a gap in the work. It is filed because this run has twice been bitten by amendment routing, at S1-R1-01 and S1-R2-02, and a later reader tracing the budget correction to step 5 will find nothing there. | open, controller-side |

Leads not pursued: S2-R2-01's fix was re-driven by the two-fixture reproduction that found it rather than read off the diff, and it holds. Rebuilding the mutant from the current shipped bytes and running repository A's greenlight from A with `GIT_DIR` naming repository B, the mutant exits 0 and writes B's untested tree `e8276bcbdaf1ad8be431ba7a899079ff82813adf` into B's record; the shipped script under identical conditions records A's own tree `755804afd7f18488364bf794cdeffa89bc1ceaf9` in A's own record and leaves B's absent. The guard added last round still kills that mutation, `test_another_repositorys_index_cannot_be_recorded_by_this_suite`. Neither script has changed since `d2e67dea`, so round 2's fifteen-mutation set and nine-state fail-closed battery still describe these bytes and were not re-run; that is stated under Not checked rather than implied. The four findings above come from auditing the study amendment against the measurements it cites, which is what this round was asked to do, and two of them required re-measurement rather than re-reading. For S2-R3-01 the do-nothing hook was re-timed over three fresh repositories at 6.49, 2.13 and 7.03 ms of steady-state overhead with first executions of 140.75, 137.11 and 140.43 ms, against the shipped gate's 18.97, 23.20 and 20.57 ms steady with first executions of 250.40, 150.45 and 150.13 ms. For S2-R3-02 the clone test was built specifically to test the phrase "on that machine": one origin, three clones, every `pre-commit` byte-identical at `b7571a0ad138`, first-execution overheads of 159.05, 165.57 and 152.28 ms against the origin's 158.53 ms, with steady states of 18.82, 18.62 and 20.13 against 25.23. A first attempt at that test was discarded rather than reported, because it staged file names the clone already tracked and the commit was refused with nothing to commit; the rebuilt version uses per-clone tags. What the amendment gets right is worth recording as plainly as what it does not, because three of the four findings are wording rather than substance. The ruling itself is carried faithfully: the breach is stated as a breach, the clause is scoped rather than deleted, the first-execution cost stands beside it rather than replacing it, and the honesty marker separating recorded measurement from inferred attribution is carried in substance and not diluted. The figures at 219.61, 253.05 and 322.49 ms, at 182.79 ms for a bare-success hook, at 52.45 against 682.89 ms for the reversed-order pair, at about 11 ms for a cold shell exec, and at 275.40 and 429.28 ms with neighbours from 73 to 92 ms are each exactly what round 2 measured and are attributed to round 2 rather than asserted as universal. The step 4 re-issue was checked as well and is sound: its Complete replacement Files now carries `docs/commit-gate/study.md`, `docs/commit-gate/design-evidence.json` and `docs/commit-gate/reports/`, and its `ShippedCopyTests` gains a fourth case requiring every report the shipped design record cites to resolve under `docs/commit-gate/reports/` at the digest the record names, which closes the lead step 1's audit carried forward; the decision record's consequences now carry the first-execution cost, which is the right home for it, since a record stating a 200 ms budget without it would mislead exactly the reader it exists for. Two things this round looked at and did not file. The newest study and runbook amendments both open "Still holding" at Step 2 rather than Step 1, unlike every earlier block; step 1 is closed under `--no-further-leads`, so dropping a closed step reads as convention rather than omission, and no step 2 evidence turns on it. And step 5's Files still omit `docs/commit-gate/study.md`, which is correct as things stand because step 4 refreshes it and this amendment precedes step 4, but it is the mechanism behind S2-R3-04 and would become a real gap for any study amendment recorded after step 4 closes. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; `python3 -m unittest tests.test_commit_gate` is green at 26 cases; and the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1158 tests in 283.918s`, `OK`. There is no fixes commit this round and therefore no Elenchus verdict, which is why the field reads null: every finding is a correction to a controller-pinned append-only document that no branch byte can carry. The study digest was verified as received at `990fd843c9a6fb3540cd77fe323f443f48a7008335bb9dff64dd0961cb458bcc` and the runbook at `1caa2bac054dfe5089204eb6aad556bcb16fd1df066bd22e0e5012bce823a827`, the step 2 markdown is unamended with `effective_sha256` equal to its baseline `3d1f0ce0c92d4c3b9b89e41a353f53594f90c1eb83f1247aad49f3be5cb624c4`, and the design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. Both tracked scripts remain `100755`, `.horos/boundary.json` needed no regeneration, and the working tree was verified byte-identical to HEAD after the mutation run. The record commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, no existing test's assertions changed, and every fixture ran under the vetted scratch helpers.

## Step 2, round 4 -- 2026-09-04T08:44:49Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression and step 3's hosted half remain their steps' work. This was a confirmation round against a document, so no gate behaviour was re-measured and no mutation set was re-run: `git diff d2e67dea..HEAD -- .githooks/ tests/test_commit_gate.py` is empty, so round 2's fifteen-mutation set, round 2's nine-state fail-closed battery and round 3's two-fixture reproduction all still describe these exact bytes. No new measurement was taken because the amendment introduces no figure that was not already recorded; every number in it was matched to a recorded value before this record was written. The oversized-record lead stays where round 2 left it.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | low | .hexaemeron/study.md, fourth amendment | The load condition is stated as recorded fact twice, and the first statement is contradicted by this run's own timeline. The amendment reads "Those figures were taken while the machine was running the 1150-case suite" and, of round 3's figures, "On a quiet machine". Neither round instrumented load. Worse for the first: round 2's budget probes ran at 08:57 to 08:59, round 2's declared root-suite run finished at 09:12:15 having been started after the guard commit that followed those probes, and round 1's root suite finished at 07:57:06, an hour earlier, so no 1150-case run of this Warden's was in flight when those figures were taken. Something else may have loaded the machine and nothing establishes it. The second statement is circular in the same way: round 3 inferred a quiet machine from the lower timings and then offered quietness as their cause. This finding is this Warden's error before it is the controller's, because round 3's record introduced the wording verbatim and it was carried in good faith. What the evidence supports is narrower and still enough for the caveat the correction exists to add: the range is not reproducible across runs on this machine, round 2 measured 219.61 to 322.49 and round 3 measured 150.13 to 250.40 for the same quantity, and neither round established why. The breach is untouched, because it rests on the measurements rather than on their cause. | open, controller-side; the two causal clauses need replacing with the variation itself |
| S2-R4-02 | low | .hexaemeron/study.md, fourth amendment | "Three of its four findings are in that amendment's wording rather than in the gate" miscounts. All four of round 3's findings were in the budget amendment and none was in the gate: S2-R3-01 the misattributed pair, S2-R3-02 the scoping clause, S2-R3-03 the unconditioned range and S2-R3-04 the touched step. Round 3's own record said three of four were "wording rather than substance", distinguishing the substantive S2-R3-01 from the other three; transposing that to "rather than in the gate" changes the axis and makes the count wrong. Cosmetic beside the other corrections, and named only because it is one clause in an amendment that is being written anyway. | open, controller-side |

Leads not pursued: S2-R3-01 and S2-R3-02 are answered, and answered properly rather than softened. On the first, the amendment records that 31.91 milliseconds was the step 2 original hook's steady state rather than a do-nothing hook's, carries round 2's 10.55 alongside round 3's 6.49, 2.13 and 7.03 against the shipped gate's 18.97, 23.20 and 20.57, states that the 31.91 and 32.49 pair supports only that round 1's fix costs nothing over the gate it replaced, withdraws the "adds nothing measurable" claim in those words, and gives the gate's own logic as roughly 14 milliseconds today and roughly 21 in round 2 over a hook that does nothing. Each of those is the recorded value, and the derived figures check: 20.57 less 6.49 is 14.08, 32.49 less 10.55 is 21.94, and 32.49 less 31.91 is 0.58, which is what "costs nothing over the gate it replaced" means. The one arithmetic remark worth making and not worth a finding is that 21.94 is nearer 22 than 21, and the amendment carries "roughly 21" because round 3's record wrote "about 21 ms"; the word is "roughly" and the underlying pair is printed beside it, so a reader can do the subtraction. On the second, "on that machine" is struck, the rule is stated as per file instance rather than per machine or per content, and the three-clone evidence is carried at 159.05, 165.57 and 152.28 against the origin's 158.53 with the hooks byte-identical. S2-R3-04 is answered and the amendment's own touched step reads Step 4, which is the step whose Files carry `docs/commit-gate/study.md`. S2-R3-03 is answered in substance, in that the range now carries a condition, the quiet-run figures 150.13 to 250.40 and 137.11 to 140.75 are recorded, and the breach is restated as resting on round 2's three trials exceeding 200 and a later run still producing 250.40; what it introduces is S2-R4-01 above, which is a defect in the condition rather than in the correction. Every number in the fourth amendment was matched one by one to a value this Warden recorded in round 2 or round 3 before this record was written, and all twenty-one matched; the only unmatched numeral is the year in the heading. No figure in it is new, which is why this round took no fresh measurement and says so under Not checked rather than implying a re-run it did not do. The two findings above are one clause each in an amendment that is being written anyway, and neither touches the gate, the budget verdict or any branch byte. S2-R4-01 is recorded as this Warden's error first: round 3 asserted a cause it had not checked, the controller carried it in good faith, and the timeline that contradicts it was reconstructed this round from scratchpad artefact times, 08:57:13, 08:57:59 and 08:58:55 for the three budget probes against 09:12:15 for round 2's root-suite output and 07:57:06 for round 1's. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; `python3 -m unittest tests.test_commit_gate` is green at 26 cases; and the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1158 tests in 201.791s`, `OK`. There is no fixes commit and therefore no Elenchus verdict, which is why the field reads null: both findings are corrections to a controller-pinned append-only document. The study was verified as received at `2504b7fa32fc9f77b80c2e5666a1ef354b735133118d3ef13bf961f1a817b3db` and the runbook at `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3`, both matching the directive; the study now carries four amendment blocks; the step 2 markdown remains unamended with `effective_sha256` equal to its baseline `3d1f0ce0c92d4c3b9b89e41a353f53594f90c1eb83f1247aad49f3be5cb624c4`; and the design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. The runbook re-issue was checked as unchanged in substance from the block round 3 read, which is what a third re-issue for digest rebinding should be. Both tracked scripts remain `100755`, `.horos/boundary.json` needed no regeneration, and the working tree was clean at entry and after the record. The record commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

## Step 2, round 5 -- 2026-09-04T08:54:16Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression and step 3's hosted half remain their steps' work. No gate behaviour was measured and no mutation was run this round, by instruction and because `git diff d2e67dea..HEAD -- .githooks/ tests/test_commit_gate.py` is empty: round 2's fifteen-mutation set, its nine-state fail-closed battery and round 3's two-fixture reproduction all still describe these exact bytes. The one thing this round did examine beyond the amendment's text is the artefact timeline round 4 supplied, because the amendment now rests on it. The oversized-record lead stays where round 2 left it. One state fact recorded rather than actioned: this study amendment unbinds step 4's runbook amendment a fourth time, the runbook stayed at `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3` and the controller has deferred the re-issue until step 4 opens so it happens once against a settled study. Step 2's own view is unaffected and was checked: zero amendments, `effective_sha256` equal to its baseline.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R5-01 | low | .hexaemeron/study.md, fifth amendment | The replacement claim is stronger than the artefact times can carry, and the citation that supports it names the furthest of four prior runs rather than the nearest. Both halves come from round 4's record and are this Warden's error carried forward a second time. First, "No suite run of this run's own was in flight" asserts an absence that these artefacts cannot establish, because a run killed by timeout writes no output file and two mutation runs were killed that way in round 2, one at ten minutes and one at five. Round 2 also established that an unanchored greenlight under mutation walks up to the real repository and runs its full suite inside the test, so an orphaned child of a killed mutation run is exactly the kind of load that would leave no trace. Nothing shows one was running; the point is that nothing could. Second, the amendment says round 1's suite "had finished at 07:57:06, an hour earlier", which is true of the run it names and omits three nearer completions: Elenchus at 08:01:48, `run_checks --full` at 08:26:24 and round 1's final root suite at 08:32:39. The nearest completion before the first probe is therefore 24.6 minutes away, not 60.1, and the gap the sentence conveys is two and a half times the real one. What the artefacts do establish, and what the sentence should say, is that no recorded completion of any suite, check or Elenchus run of this run's own falls between 08:32:39 and 09:07:27, and the three probes at 08:57:13, 08:57:59 and 08:58:55 sit inside that window, with the 09:07:27 run started after the guard commit that followed them. The amendment's conclusion is untouched and if anything better supported: the range moved, and nothing here establishes why. | open, controller-side; the absence claim needs narrowing to what the artefacts show, and the nearest prior completion is 08:32:39 |

Leads not pursued: S2-R4-02 is answered exactly and closes. All four of round 3's findings are recorded as having been in the amendment with none in the gate, and the distinction is put back on its own axis, the one substantive finding against the three that were wording. Nothing in that correction needs narrowing. S2-R4-01 is answered in substance and the substance is the part that mattered: both causal statements are withdrawn in terms, the 1150-case attribution and the quiet-machine inference alike, the circularity is named as circularity, and the claim is reduced to irreproducibility across runs at 219.61 to 322.49 in round 2 against 150.13 to 250.40 in round 3 for the same quantity with neither round establishing why. The stated reason for withdrawing is right and worth keeping: a study shipping a cause nobody measured invites a reader to plan around a mechanism that may not exist. What S2-R5-01 raises is the evidence the withdrawal now leans on, not the withdrawal. Every figure in the fifth amendment was matched to a value this Warden recorded before this record was written: the three probe times, the 09:12:15 and 07:57:06 completions, and both first-execution ranges. One frame is worth stating because the amendment declares it and a later reader should not have to reconstruct it: those probe times are the mtimes of the three probe scripts, which were written and executed inside the same command, so they mark each probe's start within seconds rather than being independently recorded run times, and the amendment says "reconstructed from the run's own artefact times" immediately before using them, which is the right qualifier. The timeline this round rebuilt is denser than round 4's: measured from the first probe at 08:57:13, round 1's post-fix suite finished 60.1 minutes earlier, its Elenchus 55.4, `run_checks --full` 30.8 and its final root suite 24.6, while round 2's Elenchus completed 10.2 minutes after and its root suite 15.0, both started after the guard commit. Round 4 checked one of those four and reported it as though it were the nearest, which is how the hour reached the study. That is the second time this Warden has handed the controller a claim it then carried in good faith, after the load attribution itself, and both were caught only by going back to the artefacts rather than to the prose; the lesson this run keeps relearning is that a reconstruction is evidence only for what it enumerates exhaustively. This round did not widen beyond the amendment and the timeline under it, by instruction: no gate behaviour was measured, no mutation was run, and the reason is recorded under Not checked rather than left to inference. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; and the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1158 tests in 112.263s`, `OK`. There is no fixes commit and therefore no Elenchus verdict, which is why the field reads null: the single finding is a correction to a controller-pinned append-only document and no branch byte carries it. The study was verified as received at `24854e21c469e8d6e8cbb9a4e38fdadf06bd636a55c92dfa46bd08dbe58548be` and now carries five amendment blocks; the runbook is unchanged at `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3`; the step 2 markdown remains unamended with `effective_sha256` equal to its baseline `3d1f0ce0c92d4c3b9b89e41a353f53594f90c1eb83f1247aad49f3be5cb624c4`; and the design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. Both tracked scripts remain `100755`, `.horos/boundary.json` needed no regeneration and `horos check` prints "boundary matches the tree", and the working tree was clean at entry and after the record. The record commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

## Step 2, round 6 -- 2026-09-04T09:06:03Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity. Step 4's polluted-environment regression and step 3's hosted half remain their steps' work. No gate behaviour was measured and no mutation was run, by instruction and because `git diff d2e67dea..HEAD -- .githooks/ tests/test_commit_gate.py` is empty, so round 2's fifteen-mutation set, its nine-state fail-closed battery and round 3's two-fixture reproduction all still describe these exact bytes. What this round did examine is the artefact record under the amendment, and it widened the enumeration rather than the scope: round 5 read the scratchpad, and this round also read the repository's ignored scratch anchor, which is where the finding below came from. Nothing establishes whether a process was executing during the probes, and this record does not claim otherwise. The oversized-record lead stays where round 2 left it.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | low | .hexaemeron/study.md, sixth amendment | "Nothing shows one was running, and nothing could" is wrong in both halves, and a class of artefact nobody had enumerated shows why. `gate_repository()` builds each fixture under `tempfile.TemporaryDirectory` in the repository's ignored `tmp/`, so a fixture that survives means the owning process died before its context manager ran; a normal run of the module was re-run this round and left no new directory, before and after both 26, which is what makes the inference sound. Twenty-six orphaned `tmp/commit-gate-*` fixtures survive, created continuously from 08:39:40 to 08:55:52, the last of them 81 seconds before the first budget probe at 08:57:13. They are the killed mutation runs, and they are exactly the evidence the sentence says does not exist: something does show gate-module runs being terminated abnormally in the seventeen minutes up to the probes, and something could, because orphaned fixtures record an abnormal end where no completion file is ever written. This does not establish that a process was executing during the probes and this finding does not claim it. No fixture was created after 08:55:52, though that excludes little, since the mutation class round 2 identified spends 100 to 290 seconds inside one test running the full suite without creating another fixture. The correct statement is narrower than the amendment's and wider than nothing: no completion record falls in the window, and twenty-six orphaned fixtures show killed runs ending 81 seconds before it, so an untracked load during the probes has a named candidate and remains unestablished. The amendment's conclusion is untouched and better supported: the range moved and nothing establishes why. The 26 directories are left in place rather than swept, so the finding stays checkable; `tmp/` is ignored at `.gitignore:38` and the working tree is clean. | open, controller-side; the sentence needs to become the bounded window plus the orphan evidence |

Leads not pursued: every other claim in the sixth amendment was checked against the artefacts rather than the prose, and all of them hold. The four intervals are right, measured from the first probe at 08:57:13: round 1's post-fix suite 60.1 minutes earlier, its Elenchus run 55.4, the full check run 30.8 and round 1's final root suite 24.6. The bounded window is right and was verified by enumerating every file under the session directory rather than a chosen subset, which is what round 5 said a reconstruction owes: no completion of any suite, check or Elenchus run falls between 08:32:39 and 09:07:27, and the single file whose modification time lands inside it, `err.txt` at 08:36:48, is a zero-byte stderr capture written by the fail-closed battery's fixture commits rather than a run of that class. The three probes at 08:57:13, 08:57:59 and 08:58:55 sit inside the window. The claim that the 09:07:27 run started after the commit that followed the probes was checked against git metadata rather than recalled ordering: `d2e67dea` carries a commit time of 09:03:09, which is after the last probe and before that run. The probe-times frame is carried correctly. The two killed mutation runs are correctly described, one at ten minutes and one at five, and the unanchored-greenlight mechanism is round 2's finding restated accurately. The subtractive method was the right call and it worked: the amendment adds no new causal claim, and the one finding above is not a new claim it introduced but an old one it kept, "nothing could", which was inherited from round 5's record and is this Warden's wording before it is the controller's. That makes three inherited over-strong statements across rounds 4, 5 and 6, each caught by going back to a wider set of artefacts than the round before it read, and the pattern is worth naming rather than repeating: round 4 enumerated one completion where four existed, round 5 enumerated the scratchpad but not the repository's ignored scratch anchor, and this round enumerated both. There is no reason to think the enumeration is now complete, only that it is wider; a seventh round reading somewhere neither has looked could find the same shape again, and that is an argument for stating bounded windows rather than absences, which is what the correction above asks for. This round did not widen its scope: no gate behaviour was measured and no mutation was run, recorded under Not checked rather than left to inference. Evidence for the covered concerns and the mechanical part: the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py`, `.horos/boundary.json` and the two documents; `python3 -m unittest tests.test_commit_gate` is green at 26 cases and left no new orphaned fixture, which is the observation the finding's inference rests on; and the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1158 tests in 177.922s`, `OK`. There is no fixes commit and therefore no Elenchus verdict, which is why the field reads null: the single finding is a correction to a controller-pinned append-only document and no branch byte carries it. The study was verified as received at `7eb7307f83419fa449374290e01482fba85000011dae82afc22e0a391a50977e` and now carries six amendment blocks; the runbook is unchanged at `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3`, its step 4 amendment still unbound by design until step 4 opens; the step 2 markdown remains unamended with `effective_sha256` equal to its baseline `3d1f0ce0c92d4c3b9b89e41a353f53594f90c1eb83f1247aad49f3be5cb624c4`; and the design lock is unchanged at `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. Both tracked scripts remain `100755`, `.horos/boundary.json` needed no regeneration and `horos check` prints "boundary matches the tree", and the working tree was clean at entry and after the record, with the 26 orphaned fixtures sitting under the ignored `tmp/` anchor where `scratch-quiescence` intends fixture churn to land. The record commit is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

## Step 3, round 1 -- 2026-09-04T13:01:07Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run and their documents were not read; this step ships no Solidity and no Foundry or Hardhat project, so `fizz` has no target either. No shell linter ran over either script: `shellcheck` is not installed on this machine, so the two scripts were reviewed by reading and by the behavioural probes recorded below, and a lint class those probes do not reach would not have been seen. The `safe.directory` measurement below forced git's dubious-ownership path with `GIT_TEST_ASSUME_DIFFERENT_OWNER=1` rather than using a genuinely foreign-uid checkout, and whether any contributor to this repository actually needs `safe.directory` was not established; the mechanism is measured, the population is not. `.hexaemeron/design-evidence.json` was read and verified but not amended: its `hooks-path-plus-visibility` / `index-mutation-regression` result still reads `state: "pending"` with `report` as a bare string rather than a path-and-digest object, and flipping it is the controller's operation, not this round's. Step 4's work and the hosted half remain their steps'. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | .githooks/pre-commit, .githooks/greenlight, at ce3b1af2 | The step's implement receipt does not meet its own Exit, and the bytes that close it were sitting uncommitted. Exit requires that the gate "starts no process other than fixed-argument `git` invocations against its own repository and reads no configuration an inherited override could redirect". Measured against ce3b1af2's exact committed bytes on git 2.50.1 (Apple Git-155), `core.fsmonitor` supplied through a configuration *file* runs its command during the gate's own `git write-tree`, on both halves, down all four routes: `GIT_CONFIG_GLOBAL`, `GIT_CONFIG_SYSTEM`, `$HOME/.gitconfig` with both selectors absent, and `$XDG_CONFIG_HOME/git/config`. Eight of eight probes returned `fsmonitor_ran=True`. ce3b1af2's message left the two selectors alone because they "name the caller's own configuration files rather than smuggling a value in-band"; that reasoning is about provenance and the Exit clause is about effect. The working tree at round entry already carried a fix, uncommitted, from an interrupted session: a `gate_git` wrapper pinning both selectors at `/dev/null` through which every gate git call passes, two new cases driving each half down the three selector routes, and `staged_state` widened from the index alone to the index, the working tree's porcelain status and every ref. This round measured those bytes rather than writing them. Under them the same eight probes return `fsmonitor_ran=False`, `git config --list --show-scope` reports `local` alone, and `git write-tree` returns the identical tree `eca00a84cf9e` with the wrapper and without it, so the control changes what the gate reads and not what it answers. A configuration path that does not exist behaves exactly as `/dev/null` does, so the control degrades safely where `/dev/null` is unavailable. | fixed by 612e0b64c708cc067989303d6981b5888529ca16 on the audit branch; ce3b1af2 is left exactly as it is so what it omitted stays visible |
| S3-R1-02 | medium | .githooks/pre-commit:58 | An inherited `GIT_DIR` still redirects which repository configuration the hook half reads, and that repository's own `core.fsmonitor` runs. Measured on the fixed bytes at 612e0b64: with `GIT_DIR` pointed at another repository whose `.git/config` carries `core.fsmonitor`, `pre-commit` returned `fsmonitor_ran=True`; the identical probe against `greenlight` returned `False`, because greenlight unsets `GIT_DIR` at line 53 and pre-commit unsets only `GIT_CONFIG_PARAMETERS` and `GIT_CONFIG_COUNT` at line 58. `GIT_DIR` is an inherited override and it redirects which configuration git reads, so this is the Exit's own second clause, unclosed on one route, in the half the step names. Nothing guards it: `polluted_at()` sets `GIT_INDEX_FILE` and `GIT_PREFIX` and nothing else, so no case in `HookIndexMutationTests` drives `GIT_DIR`. Reachability is this run's own documented incident class, not a hypothetical: `tests/test_boundary_currency.py:57-73` records "A hook, `git bisect run`, a rebase `exec` line: all of them set GIT_DIR", and both hook scripts are executable files a person or another repository's hook can invoke directly. Bounded on the other side: in every measurement the outer repository's staged state was byte-identical afterwards, no green record appeared in it, and the hook still refused at rc=1. Not fixed here. pre-commit cannot simply unset `GIT_DIR` for the reason its own comment gives about `GIT_INDEX_FILE` -- git names the commit's own index and git dir there -- so closing this is a design choice about how the hook half anchors itself, not a line. | open, controller-side; the choice is whether the hook anchors to its own script location as greenlight does, or the Exit clause narrows to the two variables the register names |
| S3-R1-03 | low | .hexaemeron/runbook.md, step 3 Tests field | "Expect five cases"; `HookIndexMutationTests` ships seven. This is a runbook-amendment obligation rather than acceptable audit growth. The two extra cases are new named methods, `test_the_hook_reads_no_configuration_a_file_selector_could_redirect` at line 1023 and `test_greenlight_reads_no_configuration_a_file_selector_could_redirect` at line 1027, and they implement a control the pinned documents name nowhere: `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` appear in neither the step's Goal, Exit, Files nor Tests, and the study's `hook-config-inheritance` register row names only `GIT_CONFIG_PARAMETERS` and `GIT_CONFIG_COUNT`. Growth of that shape is a widening of the step, and the step is where a later reader reconciles what was built against what was asked for. The runbook is unamended: `amendments` is empty and `effective_sha256` equals `baseline_sha256` at `b3c5d12834f7e239816197ebba41030ce9769abc336c47eed8a52a776e2d40b7`, with the document at `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3`. Both pinned documents are append-only through the controller, so nothing on the branch can carry this. | open, controller-side; `hexctl amend runbook` for the count and the file-selector routes, and the same question put to the study's `hook-config-inheritance` row |
| S3-R1-04 | low | .githooks/pre-commit:46-49, 67-70 | The comment names the `safe.directory` trade and claims the refusal "names the cause"; the refusal names the symptom and prescribes a remedy that also fails. Measured with `GIT_TEST_ASSUME_DIFFERENT_OWNER=1` against a fixture whose `$HOME/.gitconfig` carries `safe.directory`: without the control `git rev-parse --git-dir` exits 0, with the control it exits 128 with "detected dubious ownership". The shipped hook then emits exactly one refusal line, `pre-commit: refused, git rev-parse --git-dir failed, so the green record cannot be located; run .githooks/greenlight on the staged tree, or commit with FIAT_SKIP_PRECOMMIT=1`, and git's own stderr is suppressed on that branch by design, so "detected dubious ownership" never reaches the contributor there. The first remedy it prescribes, `.githooks/greenlight`, exits 128 under the same conditions. That is recoverable rather than fatal, and by a route worth stating: greenlight does not suppress git's stderr, so a contributor who follows the failing first remedy sees the cause and git's own `git config --global --add safe.directory ...` line, and the bypass token works throughout. So the trade is real, it fails closed as the comment intends, and the gap is diagnosability on one branch rather than a lost escape. Not fixed here: changing the refusal text is a behaviour change to a gate the runbook does not put in scope this step, and making it would repeat exactly what S3-R1-03 records. | open, controller-side; either the rev-parse branch names the ownership cause or stops suppressing git's stderr, and the comment's "names the cause" narrows to what the line actually says |

Leads not pursued: the greenlight half runs `python3 -m unittest discover -s tests` at line 69 rather than the repository's parallel runner, and this repository's own guidance is that plain discovery can swallow an ImportError and read as a clean suite. It is step 1's byte, not this step's, and it does not bite on this tree: `python3 -m unittest discover -s tests` and `python3 tests/run_tests.py` both report 1165 on the same commit, so the two agree here and the lead is a latent divergence rather than a present one. The XDG route is closed and unguarded: `$XDG_CONFIG_HOME/git/config` fires the command with the control removed and does not with it in place, on both halves, but no shipped case drives it, and it is left alone because a change that reopens XDG while keeping `GIT_CONFIG_GLOBAL` pinned is not readily constructible, so the two shipped cases already cover the class. The oversized-record lead stays where round 2 left it; `horos check` names `audit/rounds/fiat-857-framework-16-the-commit-gate-lives-in-one-cl.md`, its synopsis and `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md` as candidate drift, and this record grows the first of the three again. Elenchus and mutation evidence for the guard the Disciplines clause requires: Elenchus at the fixes commit returns `guarded`, exit 0, `executed` 1165, `assertion_failures` 2, `errors` 0, parent exit code 1, detail "the runner report records a parent assertion failure", from test command `python3 tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1` for the runbook's `elenchus.unittest.v1`, report file `.elenchus/fiat-857-step-3.json`. Four mutations were driven against a scratch copy of the two scripts and the test module, each restored afterwards and the run tree never touched: removing the `gate_git` wrapper fails both new cases; removing ce3b1af2's `unset GIT_CONFIG_PARAMETERS GIT_CONFIG_COUNT` fails both in-band cases; pinning `GIT_CONFIG_GLOBAL` alone and leaving `GIT_CONFIG_SYSTEM` unpinned still fails both new cases on the `GIT_CONFIG_SYSTEM` route, so each half of the pin is load-bearing; and `--git-dir` to `--git-common-dir` in pre-commit fails the worktree case. The unmutated scratch copy is green at 7 cases, matching the run tree. Evidence for the covered concerns and the mechanical part: the design record's resolver command `python3 -m unittest tests.test_commit_gate.HookIndexMutationTests -v` exits 0 at `Ran 7 tests in 3.776s`, `OK`; `.hexaemeron/reports/hooks-path-plus-visibility-index-mutation-regression.json` is 269 bytes and one closed `protasis-design-report/v1` object naming candidate `hooks-path-plus-visibility`, criterion `index-mutation-regression`, command `python3 -m unittest tests.test_commit_gate.HookIndexMutationTests -v` exactly as the design record's `resolver` declares it, `exit` 0, `unit` boolean and `value` true, and the whole `.hexaemeron` directory is ignored at `.hexaemeron/.gitignore:1`, so the artefact is run state rather than a tracked byte; the step's runner contract `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-857-step-3.json` exits 0 at 1165/1165 in 104.25s and writes `{"complete": true, "errors": 0, "expectedFailures": 0, "failures": 0, "schema": "elenchus.unittest.v1", "skipped": 0, "testsRun": 1165, "unexpectedSuccesses": 0}`; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1165 tests in 104.456s`, `OK`; `python3 scripts/run_checks.py --full` exits 0 with outcome green over 28 checks in 459.16s, run on the clean committed tree at the fixes commit rather than on the dirty tree the round opened with, with no adapter timeout; and the three bundled lints each exit 0 and print `clean` over `.githooks/pre-commit`, `.githooks/greenlight`, `tests/test_commit_gate.py` and `.horos/boundary.json`. `.horos/boundary.json` needed no regeneration and was not rescanned: `horos check .` exits 0 and prints "boundary matches the tree", the boundary's 128 entries name no `.githooks` path and no `tests/test_commit_gate.py`, and the three files this step changes are ordinary source rather than token sinks, so no entry is earned or lost by changing them; the file is named in the step's Files and is correctly untouched, last written at `e820a3d4`. Both tracked scripts remain `100755`. The bypass token is present as the literal `FIAT_SKIP_PRECOMMIT` at `.githooks/pre-commit:9`, `:24` and `:65`, at `.githooks/README.md:23` and as `BYPASS_TOKEN` at `tests/test_commit_gate.py:138`. The activation stays relative at `ACTIVATION = ("config", "core.hooksPath", ".githooks")`, `tests/test_commit_gate.py:142`. Scratch quiescence holds: `tmp/` held 26 orphaned `commit-gate-*` fixtures and `check-runner` at entry and the same 26 and one at exit, so round 6's count is unchanged and nothing this round ran left a fixture behind, and the working tree is clean. The three pinned documents were verified as received: study `8431d3853b98a7dc6ea8f28cce8909e5c3557cf4e47ea1beff3e74c52a658014`, runbook `d1b1cbb68bb8ac851c260cb654bcf3c112f0121d378c60f1b07b8f27fa5d90c3`, design lock `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. One state fact recorded rather than actioned: `core.hooksPath` is unset in this worktree, so the gate under audit did not run against this round's own commits. Both commits are authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carry exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline.

## Step 3, round 2 -- 2026-09-05T07:40:03Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run and their documents were not read; this step ships no Solidity and no Foundry or Hardhat project. `shellcheck` is still not installed on this machine, so no shell linter ran over either script; both were reviewed by reading and by the behavioural probes recorded below. The dubious-ownership path was again forced with `GIT_TEST_ASSUME_DIFFERENT_OWNER=1` rather than a foreign-uid checkout. What git exports into a hook for `commit -a` and for a commit from a subdirectory was measured with an environment-dumping hook rather than by a shipped case, and no shipped case drives `commit -a`. `GIT_INTERNAL_SUPER_PREFIX`, which greenlight drops at `.githooks/greenlight:55` and pre-commit does not, was not measured. `.hexaemeron/design-evidence.json` was verified as received and not amended: its `hooks-path-plus-visibility` / `index-mutation-regression` result still reads `state: "pending"` with `report` as a bare string, and flipping it is the controller's operation. `.elenchus/fiat-857-step-3.json` still holds round 1's report at `testsRun` 1165: `python3 tests/run_tests.py --elenchus-report` refuses an existing target with exit 2, so this round's report at the same format went to `.elenchus/fiat-857-step-3.round-2.json`, which is ignored, and Elenchus wrote its own inside the detached parent worktree. Step 4's work and the hosted half remain their steps'. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-02 | medium | .githooks/pre-commit:85-87 | Closed by c07679e8: the hook drops `GIT_DIR`, `GIT_WORK_TREE`, `GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES` and `GIT_NAMESPACE`, keeps `GIT_INDEX_FILE` and `GIT_PREFIX`, and does not change directory; `HookIndexMutationTests` gains the repository-selector case and the gates-under-inherited-`GIT_DIR` case, nine in all; `staged_state` measures with `core.fsmonitor=false`. Re-measured this round: against c1ca8b50's hook bytes with the current test module exactly those two cases fail and the other seven pass, `Ran 9 tests, FAILED (failures=2)`; against 5f7e0e1d all nine pass. What git exports into `pre-commit` was measured on git 2.50.1 with an environment-dumping hook: a plain commit sets `GIT_INDEX_FILE=.git/index`, relative, and no `GIT_DIR`; `commit -a` sets `GIT_INDEX_FILE` to the absolute `.git/index.lock` and no `GIT_DIR`; a commit from a subdirectory runs from the root with `GIT_PREFIX=sub/` and no `GIT_DIR`; a commit in a linked worktree sets `GIT_DIR` to `<common>/worktrees/<name>` and an absolute `GIT_INDEX_FILE` under it; a caller's `GIT_DIR=.git` left behind yields the relative `GIT_INDEX_FILE=.git/index`, so not changing directory is load-bearing. A split work tree with no `.git` of its own, driven through `--git-dir` and `--work-tree` or through `GIT_DIR` and `GIT_WORK_TREE` from a location outside any repository, is refused on one line carrying `not a git repository` and `FIAT_SKIP_PRECOMMIT=1` admits it; the same shape inside another repository is S3-R2-01. | fixed by c07679e850d254808e231cb6b9535264b6ec7e0d on the audit branch |
| S3-R1-03 | low | .hexaemeron/runbook.md, step 3 Exit and Tests | Closed by the study amendment dated 2026-09-04, which reads the `hook-config-inheritance` trigger as five variables and records the `GIT_DIR` decision and the refusal-line consequence, and by the runbook amendment of the same date replacing step 3's Exit and Tests, both verified as received: study `f2919b822f181b2544f29e7495e5d94bb31b99c16920bdadc51ec8cd1c67eac4`, runbook `214bf63a459ab19cf267f66fe3dbb9d30e699f899690d1b17ca1f81e748edeb2`, amendment `c95a0b35158730a4dbb98de08b534ff27fd1373bb8910f6a71524a9ce27d9a1a`, effective step `9383edd3363b4168c3472fc3d4cfba15f09c1333cbe8fbad1fd6744c398f95e7`. The tree matches the replacement: `HookIndexMutationTests` has nine cases, `RefusalLineTests` gained one, and the step range changes nothing in `tests/test_boundary_currency.py`, so `GitEnvironmentIsolation` is left exactly as it is. | closed by the two amendments; no branch byte changed for it |
| S3-R1-04 | low | .githooks/pre-commit:96-106 | Closed. On the `git rev-parse --git-dir` refusal path the one line now carries git's first standard-error line as `git said: <line>`, control characters stripped, cut at 256 characters, taken from a second run of the same command, with `nothing on standard error` when the capture is empty; the header comment and the `safe.directory` paragraph now say what the line carries instead of that it "names the cause". Measured on git 2.50.1: started outside any repository the line carries `fatal: not a git repository (or any of the parent directories): .git`; under `GIT_TEST_ASSUME_DIFFERENT_OWNER=1` with `$HOME/.gitconfig` carrying `safe.directory = *` and the gate's global pin at `/dev/null` it carries `fatal: detected dubious ownership in repository at '<path>'`; in both cases standard error is one line, under `LC_ALL=C` and under `en_US.UTF-8` with the non-ASCII path component `dépôt` intact; with a path long enough that git's line exceeds the bound, the cause segment is exactly 256 characters, the remedy segment survives, and standard error is still one line. The new `RefusalLineTests` case fails against c07679e8's hook bytes, `'not a git repository' not found`, and passes against 5f7e0e1d, `Ran 3 tests, OK`; `test_each_refusal_names_its_own_cause_on_one_line` still tells the three other causes apart. The write-tree refusal is unchanged. | fixed by 5f7e0e1dca3b7f54886676eea77e588ed74f43e8 on the audit branch |
| S3-R2-01 | low | .githooks/pre-commit:71-74, :96-115; .githooks/greenlight:62-73 | The split-work-tree trade that c07679e8, the hook comment and the study amendment record holds only while no repository encloses the work tree. The comment says that for a work tree with no `.git` of its own "discovery finds nothing there". Measured on git 2.50.1 with a fixture repository E whose configuration carries `core.fsmonitor`, and inside E's working tree a repository S whose work tree T sits beside it with no `.git`: a commit driven through `GIT_DIR=<S>/.git GIT_WORK_TREE=<T>`, or through the flags, runs the hook from T, and once the hook drops the selectors `git rev-parse --git-dir` answers E's git directory, `--show-toplevel` E, and `--show-prefix` `inner/tree/`. Four consequences, each measured. E's `core.fsmonitor` command ran once per hook invocation, during the hook's own `git write-tree`, which is the process class the amended Exit forbids "through a repository selector". The record consulted is E's: with S's index carrying the cache tree `git commit` always prepares before running the hook, the refusal reads `no green record at <E>/.git/LAST_GREEN`, and without it `git write-tree failed`, because E's object store lacks S's blobs, `error: invalid object 100755 42a0f9df... for '.githooks/pre-commit'`. With E's `LAST_GREEN` naming S's staged tree the commit is admitted at rc 0 on E's green, so one checkout's record authorised another's commit, for an identical tree. `greenlight` started from T anchors to T, `cd`s to E, runs E's `tests/` and writes E's own tree into E's `LAST_GREEN`. Bounded on the other side: E's index, loose objects, refs and index mtime were byte-identical after every gated commit and every direct hook run, so the phantom-mutation class does not reopen, and `FIAT_SKIP_PRECOMMIT=1` admits throughout. Reachability needs a `.git`-less split work tree inside another repository; a plain checkout, a linked worktree and a split tree outside any repository are unaffected, the last refused with `not a git repository`. One control would close it before any index is read and name the cause: refuse when `git rev-parse --show-prefix` is non-empty, which is empty at a repository root and `inner/tree/` here, at the cost of one more fixed-argument git call and one guard case. Not fixed here: the amended Tests fixes `HookIndexMutationTests` at nine cases and `RefusalLineTests` at one added case, so a guarded fix needs an amendment first, and an unguarded one would repeat what the Disciplines clause refuses. | open, controller-side; either amend the runbook to admit the `--show-prefix` control and its guard, or record the enclosed split tree in the study as an accepted trade with the fsmonitor and record-crossing consequences stated |

Leads not pursued: `PATH` and `GIT_EXEC_PATH` are inherited, so which `git` the gate runs is the caller's; both halves share that and it is not closable in a shell hook without pinning a path. The XDG route stays as round 1 left it, closed and unguarded. The oversized-record lead stays where round 1 left it: `horos check .` exits 0 with "boundary matches the tree" and still names `audit/rounds/fiat-857-framework-16-the-commit-gate-lives-in-one-cl.md`, its synopsis and `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md` as candidate drift, and this record grows the first again. The greenlight half still runs `python3 -m unittest discover -s tests` at `.githooks/greenlight:69` rather than the parallel runner; both report 1168 on this commit. Evidence for the covered concerns and the mechanical part, all on the committed tree at 5f7e0e1d with a clean working tree: the design record's resolver command `python3 -m unittest tests.test_commit_gate.HookIndexMutationTests -v` exits 0 at `Ran 9 tests in 3.493s`, `OK`; `python3 -m unittest tests.test_commit_gate -v` exits 0 at `Ran 36 tests in 7.841s`, `OK`; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1168 tests in 105.256s`, `OK`; `python3 tests/run_tests.py` exits 0 at `1168/1168 tests passed` in 109 s; `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-857-step-3.round-2.json` exits 0 in 121 s and writes `{"complete": true, "errors": 0, "expectedFailures": 0, "failures": 0, "schema": "elenchus.unittest.v1", "skipped": 0, "testsRun": 1168, "unexpectedSuccesses": 0}`; `python3 scripts/run_checks.py --full` exits 0 with outcome green over 28 checks in 518 s, with no adapter timeout; the three bundled lints each exit 0 and print `clean`, phylax and ephoros over `.githooks/pre-commit`, `.githooks/greenlight` and `tests/test_commit_gate.py`, hypomnema over those three and over this record and its synopsis; Elenchus at 5f7e0e1d returns `guarded`, exit 0, `executed` 1168, `assertion_failures` 1, `errors` 0, `skipped` 0, parent exit code 1, detail "the runner report records a parent assertion failure", changed tests `tests/test_commit_gate.py`, in 125 s, from test command `python3 tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1` for the runbook's `elenchus.unittest.v1`, report file `.elenchus/fiat-857-step-3.json`. `.hexaemeron/reports/hooks-path-plus-visibility-index-mutation-regression.json` is unchanged at 269 bytes, one closed `protasis-design-report/v1` object naming candidate `hooks-path-plus-visibility`, criterion `index-mutation-regression`, the resolver command exactly as the design record declares it, `exit` 0 and `value` true. `.horos/boundary.json` is untouched and this round adds no tracked file. Both tracked scripts remain `100755`. The bypass token is present as the literal `FIAT_SKIP_PRECOMMIT` at `.githooks/pre-commit:9`, `:28` and `:94`, at `.githooks/README.md:23` and as `BYPASS_TOKEN` at `tests/test_commit_gate.py:138`. The activation stays relative at `ACTIVATION = ("config", "core.hooksPath", ".githooks")`, `tests/test_commit_gate.py:142`. Scratch quiescence: at entry `tmp/` held the 26 orphaned `commit-gate-*` fixtures, `check-runner` and two `mut-parent-*` scratch copies from earlier in this round, 29 entries; this round's eleven `warden-r2-*` probe trees and the two `mut-parent-*` copies were removed at exit, leaving the 26 and `check-runner`, 27 entries, so no fixture from this round's test runs survives; the non-nested split measurement ran at the session scratchpad under `/private/tmp`, verified `not a git repository`, and was removed too. The three pinned documents were verified as received: study `f2919b822f181b2544f29e7495e5d94bb31b99c16920bdadc51ec8cd1c67eac4`, runbook `214bf63a459ab19cf267f66fe3dbb9d30e699f899690d1b17ca1f81e748edeb2`, design lock `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. One state fact recorded rather than actioned: `core.hooksPath` is unset in this worktree, so the gate under audit did not run against this round's own commits. Both fix commits, c07679e8 and 5f7e0e1d, are authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carry exactly one `Co-authored-by` and one `Wildcat-Origin` trailer with no host byline.

## Step 3, round 3 -- 2026-09-05T08:35:42Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run and their documents were not read; this step ships no Solidity and no Foundry or Hardhat project. `shellcheck` is still not installed on this machine, so no shell linter ran over either script; both were reviewed by reading and by the probes below. The dubious-ownership path was not re-measured; rounds 1 and 2 stand. `GIT_INTERNAL_SUPER_PREFIX`, which greenlight drops at `.githooks/greenlight:55` and pre-commit does not, is still unmeasured. The greenlight half under a nested `.git`-less work tree was not re-measured either: round 2's measurement stands, that it anchors to `$0`, `cd`s to the enclosing repository, runs that repository's `tests/` and writes that repository's own tree into that repository's `LAST_GREEN`, which is self-consistent rather than a crossing, and the amended Exit puts the control in the hook half alone. Whether any contributor keeps a `.git`-less work tree nested inside another repository was not established: the mechanism is measured, the population is not. `.hexaemeron/design-evidence.json` was verified as received and not amended: its `hooks-path-plus-visibility` / `index-mutation-regression` result still reads `state: "pending"` with `report` as a bare string and `blocks` as `step:4`, so flipping it is the controller's operation. `.elenchus/fiat-857-step-3.json` still holds round 1's report at `testsRun` 1165: `python3 tests/run_tests.py --elenchus-report` refuses an existing target with exit 2, so this round's direct run went to the ignored `.elenchus/fiat-857-step-3.round-3.json`, and Elenchus wrote its own inside the detached parent worktree. Step 4's work and the hosted half remain their steps'. Nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | low | .githooks/pre-commit:100-124; tests/test_commit_gate.py:1171 | Closed by 1edd679f. Reproduced first rather than taken from round 2, on git 2.50.1 (Apple Git-155): with an enclosing repository E carrying `core.fsmonitor`, a repository S inside E's working tree and S's work tree T at `E/inner/tree` with no `.git`, a commit driven through `GIT_DIR` and `GIT_WORK_TREE` ran the hook from T, where `git rev-parse --git-dir` answered E, `--show-toplevel` E and `--show-prefix` `inner/tree/`; E's `core.fsmonitor` ran during the hook's own `git write-tree`, and with E's `LAST_GREEN` naming S's staged tree `4a0efb9a` the commit was admitted at rc 0 and S took a commit. The hook now refuses on a non-empty `git rev-parse --show-prefix`, `.githooks/pre-commit:121-124`, through `gate_git` so the new call carries the same `/dev/null` configuration pins, and before a record is located and before an index is read. Re-measured against the same fixture: rc 1, one refusal line carrying `inner/tree/`, `fsmonitor_ran=False`, and S still at no HEAD. The study amendment's recorded trade holds for the nested case as it stands: `FIAT_SKIP_PRECOMMIT=1` admitted that same commit at rc 0, monitor still not run. `HookIndexMutationTests` gains its tenth case at `tests/test_commit_gate.py:1171`, whose enclosing repository holds the nested work tree's bytes; the case asserts that rather than assuming it, because an enclosing repository one blob short refuses for want of an object and such a fixture would pass without the control. With the control's four lines cut from a scratch copy, 378 bytes, that case fails at rc 0, "the enclosing repository's green record admitted a commit into a repository it does not hold", and the other nine pass. | fixed by 1edd679fce1b6a08631234c239cacc25c18061e2 on the audit branch |
| S3-R3-01 | low | .githooks/pre-commit:123 | Introduced by this round's own first fix and found in this round's review of it. The refusal line 1edd679f shipped read "so this work tree sits inside another repository", and a second path reaches a non-empty `--show-prefix` with no other repository present: the script started by hand from a subdirectory, which answers `sub/`. Measured on git 2.50.1, that invocation refused on a line asserting an enclosing repository that does not exist, which is the class S3-R1-04 closed once already on the rev-parse branch, a refusal naming a cause that is not the cause. f359f94c makes the line say what git answered and what the gate then did, "so this is not the root of the working tree git found and no green record is read from here", true on both paths, and the comment above it names both routes and claims neither over the other. The ordinary shapes were measured as real gated commits rather than inferred: a plain commit, a commit from a subdirectory, `commit -a` from a subdirectory, and both of those in a linked worktree each answer an empty `--show-prefix` and are admitted at rc 0 with no refusal line, while `--show-prefix` from a subdirectory answers `sub/` in the main checkout and in the linked worktree alike. No case drives the by-hand invocation: the amended Tests fixes `HookIndexMutationTests` at ten cases and `RefusalLineTests` at its one added case, and an eleventh is the widening round 1 recorded as S3-R1-03. | fixed by f359f94ce036a7f618ca4c84d172defb00c9fd53 on the audit branch |

Leads not pursued: the by-hand invocation from a subdirectory is now refused where it was gated before. That is a behaviour change on a path git never takes, since git runs a hook from the root of the working tree, and the bypass token recovers; it is left unguarded because the amended Tests fixes both case counts. `greenlight` has no prefix control and is not given the same one: started from a nested `.git`-less work tree it anchors to `$0`, runs the enclosing repository's suite and records that repository's own tree in that repository, and the amended Exit scopes the control to the hook half. `PATH` and `GIT_EXEC_PATH` are inherited, so which `git` the gate runs is the caller's; both halves share that and it is not closable in a shell hook without pinning a path. The XDG route stays as rounds 1 and 2 left it, closed and unguarded. The oversized-record lead stays where they left it: `horos check .` exits 0 with "boundary matches the tree" and still names `audit/rounds/fiat-857-framework-16-the-commit-gate-lives-in-one-cl.md`, its synopsis and `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md` as candidate drift, and this record grows the first again. `.githooks/greenlight:69` still runs `python3 -m unittest discover -s tests` rather than the parallel runner; both report 1169 on this commit. Evidence for the covered concerns and the mechanical part, all on the committed tree at f359f94c with a clean working tree: the design record's resolver command `python3 -m unittest tests.test_commit_gate.HookIndexMutationTests -v` exits 0 at `Ran 10 tests in 6.260s`, `OK`; `python3 -m unittest tests.test_commit_gate -v` exits 0 at `Ran 37 tests in 13.487s`, `OK`; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1169 tests in 150.641s`, `OK`; `python3 tests/run_tests.py` exits 0 at `1169/1169 tests passed` in 131 s; the step's runner contract `python3 tests/run_tests.py --elenchus-report .elenchus/fiat-857-step-3.round-3.json` exits 0 in 130 s and writes `{"complete": true, "errors": 0, "expectedFailures": 0, "failures": 0, "schema": "elenchus.unittest.v1", "skipped": 0, "testsRun": 1169, "unexpectedSuccesses": 0}`; `python3 scripts/run_checks.py --full` exits 0 with outcome green over 28 checks in 432 s, with no adapter timeout; `python3 plugins/horos/skills/horos/scripts/horos.py check .` exits 0; the three bundled lints each exit 0 and print `clean`, phylax and ephoros over `.githooks/pre-commit`, `.githooks/greenlight` and `tests/test_commit_gate.py`, hypomnema over those three, `.githooks/README.md`, and this record and its synopsis. Elenchus at the round's fixes commit f359f94c returns `unguarded`, exit 0, detail "the commit changed no test files", because that commit changes `.githooks/pre-commit` alone; at 1edd679f, which carries the guard, it returns `guarded`, exit 0, `executed` 1169, `assertion_failures` 1, `errors` 0, `skipped` 0, parent exit code 1, detail "the runner report records a parent assertion failure", changed tests `tests/test_commit_gate.py`, in 144 s. Both from test command `python3 tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1` for the runbook's `elenchus.unittest.v1`, report file `.elenchus/fiat-857-step-3.json`. `.hexaemeron/reports/hooks-path-plus-visibility-index-mutation-regression.json` is unchanged at 269 bytes, one closed `protasis-design-report/v1` object naming candidate `hooks-path-plus-visibility`, criterion `index-mutation-regression`, the resolver command exactly as the design record declares it, `exit` 0 and `value` true. `.horos/boundary.json` is untouched and this round adds no tracked file. Both tracked scripts remain `100755`. The bypass token is present as the literal `FIAT_SKIP_PRECOMMIT` at `.githooks/pre-commit:9`, `:28` and `:98`, at `.githooks/README.md:23` and as `BYPASS_TOKEN` at `tests/test_commit_gate.py:138`. The activation stays relative at `ACTIVATION = ("config", "core.hooksPath", ".githooks")`, `tests/test_commit_gate.py:142`. Scratch quiescence: `tmp/` held 27 entries at entry, the 26 orphaned `commit-gate-*` fixtures and `check-runner`, and holds the same 27 at exit; this round's `warden-r3-repro` and `warden-r3-mut` trees were removed. The four pinned documents were verified as received: runbook `d7b39a4fef87334c46c91d3431810f2ebc864bcfcb7e4ec164730d8030695779`, latest amendment `e501ff5870332f3903494c2af676bf0bd48b1b82a01461b4f781dc58a1a497f9`, study `f2919b822f181b2544f29e7495e5d94bb31b99c16920bdadc51ec8cd1c67eac4`, design lock `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. One state fact recorded rather than actioned: `core.hooksPath` is unset in this worktree, so the gate under audit did not run against this round's own commits. Both fix commits are authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carry exactly one `Co-authored-by` and one `Wildcat-Origin` trailer, adjacent and last, with no host byline.

## Step 3, round 4 -- 2026-09-05T09:01:07Z

Audit schema: fiat-audit-round/v2

Covered: green-record-self-evidence=reviewed; stale-green-different-tree=reviewed; hook-index-inheritance=reviewed; hook-config-inheritance=reviewed; worktree-record-crossing=reviewed; bypass-token-drift=reviewed; scratch-quiescence=reviewed; hooks-path-absolute=reviewed; executable-bit-loss=reviewed; ci-cannot-see-activation=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run and their documents were not read; this step ships no Solidity and no Foundry or Hardhat project. `shellcheck` is still not installed on this machine, so no shell linter ran over either script; both were reviewed by reading and by the probes below. Elenchus did not run and this round has no verdict, because it has no fixes commit; `.elenchus/fiat-857-step-3.json` still holds round 1's report at `testsRun` 1165 and was not touched. The dubious-ownership path was not re-measured; rounds 1 and 2 stand. `GIT_INTERNAL_SUPER_PREFIX`, which greenlight drops at `.githooks/greenlight:55` and pre-commit does not, is still unmeasured. The greenlight half was exercised only in the coincident-root shape below; rounds 2 and 3 stand for the nested shape. Whether any contributor points a second git directory at a checkout that is itself a repository was not established: the mechanism below is measured, the population is not. The candidate control S3-R4-01 names was measured across the seven shapes listed there and no further -- `commit --amend -a`, a commit under `rebase --exec` and a submodule checkout were not driven -- and nothing was implemented or guarded. `.hexaemeron/design-evidence.json` was verified as received and not amended: its `hooks-path-plus-visibility` / `index-mutation-regression` cell still reads `state: "pending"`, `report` the bare string `reports/hooks-path-plus-visibility-index-mutation-regression.json` and `blocks: step:4`; the file that string names exists and is correct, so the Exit's report clause is met and flipping the cell is the controller's operation. Step 4's work and the hosted half remain their steps'. This round changes no tracked file; nothing under `audit/` was read from or written to except this record and its synopsis, and no existing test's assertions changed.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R4-01 | low | .githooks/pre-commit:100-125 | The `--show-prefix` control closes the enclosing-repository crossing only where the `.git`-less work tree is a proper subdirectory of the enclosing repository, and the amended Exit states the consequence more broadly than the control it names: "A work tree with no `.git` of its own nested inside another repository is therefore refused rather than gated by the enclosing repository's green record". Measured on git 2.50.1 (Apple Git-155) against the committed tree at 72469e5d, with repository E carrying the tracked gate and `core.fsmonitor`, repository S outside E, and S's work tree pointed at E's own root through `--git-dir` and `--work-tree`. Git runs the hook from that root, so once the hook drops the six repository selectors discovery answers E and `git rev-parse --show-prefix` answers empty; the control at `.githooks/pre-commit:121-125` does not fire, and every consequence S3-R2-01 measured returns one directory up. Admitted twice: with E's record written by hand, tree `e5afd827` was admitted at rc 0 and S took commit `536dcaac`; and through the ordinary workflow with nothing hand-written, `.githooks/greenlight` run in E recorded tree `a493adb5` in E's `LAST_GREEN`, after which the commit into S was admitted at rc 0 with no refusal line and S took commit `6a283fe7`, whose tree is `a493adb5`. One repository's green authorised a commit into a repository it does not hold, and S has no green record of its own. E's `core.fsmonitor` ran during the gate's own `git write-tree` on every coincident-root attempt, admitted or refused, which is the process class the amended Exit forbids. Bounded on the other side: E's index, working-tree status and refs were byte-identical after every attempt, so the phantom-mutation class does not reopen; `FIAT_SKIP_PRECOMMIT=1` admits at rc 0 with the monitor not run; the same fixture one directory down refuses on one line carrying `inner/tree/` with the monitor not run, so the shipped control is correct for the case it names; and the admit needs E's object store to hold every staged blob, since otherwise `git write-tree` fails and the refusal is `git write-tree failed`. One candidate control, measured but not shipped: refuse when the directory holding `GIT_INDEX_FILE` is not the git directory discovery answers. Across seven shapes driven as real gated commits the two agree and each was admitted at rc 0 with no refusal line -- plain commit `.git/index`, commit from a subdirectory `.git/index`, `commit -a` from a subdirectory `<R>/.git/index.lock`, `commit --amend` `.git/index`, a linked worktree nested inside its own main checkout and a subdirectory of it both `<R>/.git/worktrees/inside/index` against git dir `<R>/.git/worktrees/inside`, and a commit reached through a symlinked path -- while in the crossing they differ, `<S>/.git/index` against `<E>/.git`. Not fixed here: the amended Tests fixes `HookIndexMutationTests` at ten cases and `RefusalLineTests` at its one added case, so a guarded fix needs an amendment first, and an unguarded one is what the Disciplines clause refuses. | open, controller-side; either amend the runbook to admit a control that reaches the coincident-root shape and its guard case, or narrow the Exit's consequence sentence to the prefix the control actually reads and record the coincident-root work tree in the study as an accepted trade with the fsmonitor and record-crossing consequences stated |

Leads not pursued: the gate starts processes that are not git -- `cat` on the admit path, `tr`, `cut` and `head` on refusal paths -- resolved through the caller's `PATH`, which the Exit's "starts no process other than fixed-argument `git` invocations" does not literally allow; they are named by the script rather than chosen by an inherited override, which is that clause's subject, and `PATH` and `GIT_EXEC_PATH` are inherited for `git` itself and not closable in a shell hook without pinning a path. Two refusal lines print the record path unstripped and unbounded, `.githooks/pre-commit:149` and `:154`, while `:164` strips and bounds the record's own bytes and `:122` does the same for the prefix; a repository path carrying a control character would split the one-line contract. Those are step 2's bytes and the path is the contributor's own rather than an inherited value, so it is left where the step's contract leaves it. The by-hand invocation from a subdirectory stays refused, as round 3 left it, unguarded because the amended Tests fixes both case counts. `greenlight` still has no prefix control and is not given one; started from a coincident-root work tree it anchors to `$0`, runs the enclosing repository's suite and records that repository's own tree in that repository, which is self-consistent, and it is that record S3-R4-01 then crosses with. The XDG route stays closed and unguarded as rounds 1 to 3 left it. The oversized-record lead stays where they left it: `python3 plugins/horos/skills/horos/scripts/horos.py check .` exits 0 with "boundary matches the tree" and still names `audit/rounds/fiat-857-framework-16-the-commit-gate-lives-in-one-cl.md`, its synopsis and `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.synopsis.md` as candidate drift, and this record grows the first again. `.githooks/greenlight:69` still runs `python3 -m unittest discover -s tests` rather than the parallel runner; both report 1169 on this commit. Evidence for the covered concerns and the mechanical part, all on the committed tree at 72469e5d with a clean working tree: the design record's resolver command `python3 -m unittest tests.test_commit_gate.HookIndexMutationTests -v` exits 0 at `Ran 10 tests in 6.336s`, `OK`; `python3 -m unittest tests.test_commit_gate -v` exits 0 at `Ran 37 tests in 16.716s`, `OK`; the declared `root-suite` `python3 -m unittest discover -s tests` exits 0 at `Ran 1169 tests in 160.950s`, `OK`; `python3 tests/run_tests.py` exits 0 at `1169/1169 tests passed` in 185 s; `python3 scripts/run_checks.py --full` exits 0 with outcome green over 28 checks in 727 s, with no adapter timeout, the longer wall time against round 3's 432 s being this round's probes running beside it; `python3 plugins/horos/skills/horos/scripts/horos.py check .` exits 0; the three bundled lints each exit 0 and print `clean`, phylax and ephoros over `.githooks/pre-commit`, `.githooks/greenlight` and `tests/test_commit_gate.py`, hypomnema over those three, `.githooks/README.md`, and this record and its synopsis. `.hexaemeron/reports/hooks-path-plus-visibility-index-mutation-regression.json` is unchanged at 269 bytes, one closed `protasis-design-report/v1` object naming candidate `hooks-path-plus-visibility`, criterion `index-mutation-regression`, the resolver command exactly as the design record declares it, `exit` 0, `unit` boolean and `value` true. `.horos/boundary.json` is untouched and this round adds no tracked file. Both tracked scripts remain `100755`. The bypass token is present as the literal `FIAT_SKIP_PRECOMMIT` at `.githooks/pre-commit:9`, `:28` and `:98`, at `.githooks/README.md:23` and as `BYPASS_TOKEN` at `tests/test_commit_gate.py:138`. The activation stays relative at `ACTIVATION = ("config", "core.hooksPath", ".githooks")`, `tests/test_commit_gate.py:142`. Five of the six refusal causes were re-measured on one fixture and are distinct in wording: no record, stale record naming the recorded tree, and unreadable record, each as a gated commit; non-empty prefix carrying `sub/` and `git rev-parse --git-dir` failed carrying `fatal: not a git repository (or any of the parent directories): .git`, each by starting the hook by hand, since git reaches neither through a commit. The sixth, `git write-tree failed`, was measured on the coincident-root fixture above, and `StagedTreeTests` holds it in the suite. Scratch quiescence: `tmp/` held 27 entries at entry, the 26 orphaned `commit-gate-*` fixtures and `check-runner`, and holds the same 27 at exit; this round created no fixture there, ran every probe under the session scratchpad outside the run tree, and removed them. The four pinned documents were verified as received: runbook `d7b39a4fef87334c46c91d3431810f2ebc864bcfcb7e4ec164730d8030695779`, latest amendment `e501ff5870332f3903494c2af676bf0bd48b1b82a01461b4f781dc58a1a497f9`, study `f2919b822f181b2544f29e7495e5d94bb31b99c16920bdadc51ec8cd1c67eac4`, design lock `6e7a5ca5397d6b84951d79d6f5b1ea3a6cedd724824ba0f28434f82a72b36a44` with candidate `hooks-path-plus-visibility`. One state fact recorded rather than actioned: `core.hooksPath` is unset in this worktree, so the gate under audit did not run against this round's own commit. Every commit in the step range -- ce3b1af2, 612e0b64, c1ca8b50, c07679e8, 5f7e0e1d, 9e875f6d, 1edd679f, f359f94c and 72469e5d -- is authored `Shoggoth <shoggoth@wildcat.finance>`, committed by the configured signer with a good signature, and carries exactly one `Co-authored-by` and one `Wildcat-Origin` trailer, adjacent and last, with no host byline.
