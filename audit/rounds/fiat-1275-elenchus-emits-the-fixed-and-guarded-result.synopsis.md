Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1275-elenchus-emits-the-fixed-and-guarded-result.md | source_sha256=55415e7fe4c566e6c70fd40db05c9f7bd6339e9e59727e9bc3617bae423c6863 | h2_count=10
## Step 1, round 1 -- 2026-09-05T20:02:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245` and `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | `docs/elenchus-fixed-and-guarded-record/study.md:560` | Section 9 ends "No network is reached, no subprocess is started, and no dependency is added". The emitter starts two fixed-argv `git` subprocesses in emit mode: `git -C <repo> rev-parse <ref>^` in `parent_of` and `git -C <repo> ls-files --error-unmatch -- <path>` in `tracked`. `--check` starts none. The study's own field table at line 76 and derivation paragraph at lines 88-94 require the first, and section 9's boundary 3 ("is not tracked") requires the second, so the code follows the study's substance and the sentence alone is false. | open -- the repair belongs to the sentence, not the code. The committed copy is byte-identical to the controller artefact `.hexaemeron/study.md`, both SHA-256 `12e5c1a84157d1b036a5e3533781cf83cd77aa77727a189b8fcad0122b7ef58b`, so a `hexctl amend study` is owed; Warden did not run it |<br>| S1-R1-02 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:606` | `emit` derived `unfixed_parent.commit` from `result["ref"]` without binding that ref to the commit the draft names as the repair, so a record could carry a parent belonging to another commit. Demonstrated on a scratch repository: a result naming an unrelated commit produced a record whose `unfixed_parent.commit` equalled its own `fixed_tree.commit`, and `--check` reported it clean, asserting a guard seen red on the parent and green on the fixed tree while naming one commit for both. | fixed in `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`; one `git rev-parse <ref> <ref>^` read now returns the resolved commit and its parent, F010 refuses a resolved commit that is not the drafted repair, and `test_a_ref_that_is_not_the_drafted_repair_is_refused` guards it |<br>| S1-R1-03 | low | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:483` | `parent_of` interpolated the untrusted `result["ref"]` into `git rev-parse` argv with no leading-dash refusal and no `--`, against the rule phylax states, "a ref beginning with a dash is an option, not a name", and against the `--` guard `tracked` already uses in the same file. Five hostile refs were probed through the emitter (`--since=2020-01-01`, `--branches=main`, `--all`, `-e`, `--local-env-vars`) and all five refused at F008, because the appended `^` poisons every value-carrying option; no input yielding a valid but wrong commit was constructed. `--end-of-options` is not usable here, since `git rev-parse` echoes it as an output line. | fixed in `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`; F006 refuses a ref beginning with a dash before it reaches argv, guarded by `test_a_ref_beginning_with_a_dash_is_refused` |<br>| S1-R1-04 | low | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:621` | `write_staged` was called outside any handler, so an `OSError` at write time escaped `main` as a Python traceback rather than a coded refusal, contradicting the module docstring, "Every refusal names its code and its field on stderr and writes nothing", and the step's ephoros clause. Reproduced with an existing destination directory at mode 500, which `prepare_output_path` accepts because `mkdir(exist_ok=True)` succeeds: exit 1, a `PermissionError` traceback, no code and no field. | fixed in `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`; F009 covers the failed write with a static message, the staged file is still removed, and `test_a_destination_that_cannot_be_written_names_its_code` guards it |<br>| S1-R1-05 | low | `docs/elenchus-fixed-and-guarded-record/study.md:79` | The field table gives `verdict` as "one of the four states, and the runner contract that produced it", sourced to `elenchus.py`. That result prints no runner contract; a runner contract is the test command, report format and report file triple the runbook step declares. What `elenchus.py --format json` prints is `detail`, the runner-report account `classify` produces. The shipped record carries `{status, detail}` untranslated, and the decision draft at line 58 already documents it accurately as "the runner-report account that produced it". | open -- the shipped resolution is faithful and the code needs no change; the study sentence overstates what its named source carries, so the `hexctl amend study` that S1-R1-01 owes should carry this clause too |<br>| S1-R1-06 | low | `.hexaemeron/runbook.md` step 1, Tests | The step declares its Elenchus runner contract as test command `python3 tests/run_tests.py --elenchus-report {report}`, whose loader discovers only `tests/` with the repository as top level. Every test module this step ships lives in `plugins/hexaemeron/tests/`, which that command cannot collect, so no repair to this step's files can be guarded through the declared contract. This round's own fix shows it: the parent comparison executed 1,337 tests with 0 assertion failures and classified `passed`, while all four affected cases in `test_elenchus_fixed_and_guarded.py` are red against the unrepaired emitter, two FAIL and two ERROR. | accepted -- the runner contract is a built step's runbook field and is no longer amendable for step 1. Recorded so `passed` reads as a contract mismatch rather than an absent guard, and carried to later steps' contracts |<br><br>Leads not pursued: `checker-interface-drift` was verified against the branch point rather than taken on report: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at both `0fefcc986107ed66ff43c6572b7aa1c7351f12f4` and `d95a089976c3da1d815e3d39c960cd760029c245`, so neither file changed. `secret-in-reproduction-output` was verified end to end: a sentinel credential placed in the result's `output` reached neither the written record nor stdout nor stderr, and the record held exactly `schema` and the nine fields. The `output-path-escape` register line bundles "the operator-supplied output and draft paths" under one control, but only the output path is held to a symlink-free relative worktree descendant, and a draft read from outside the worktree is accepted, which was verified; study section 9 boundary 1 and the runbook exit both constrain only the output, and forcing operator scratch into the tree would be worse. `read_json`'s `stat.S_ISLNK(before.st_mode)` term is unreachable, because a symlink's `lstat` mode never satisfies the preceding `S_ISREG` test, and `O_NOFOLLOW` closes the final-component race, so the redundancy is harmless. A repeated `--out` is silently last-wins under argparse, where `tests/run_tests.py`, the prior art the study names, refuses two report paths in one invocation; the study's "one output path per invocation" clause holds in effect. The four report counters and `reproduction.output_bytes` accept an unbounded integer, but the record stays bounded by the 1 MiB input cap, so `unbounded-record-size` holds. `verdict.detail` is untrusted free text in a hand-written result and one of `classify`'s four fixed strings in a genuine one; separating the two would need the comparison re-run, which the emitter deliberately does not do. `repair.files` and `fixed_tree.report` are operator assertions the emitter never checks against git or a rerun, which the study states as their source rather than hides. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface before and after the fix; `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0 on both trees; `python3 scripts/run_checks.py --base origin/main` was green on the fixed tree with all 10 selected checks passing and no adapter timeout; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 43 tests green.
## Step 1, round 2 -- 2026-09-05T20:45:05Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; whether the run integrates onto `origin/main`, which advanced to `1f4111d82c43fba69c01742947b392efb6454c4b` after the run started and which the controller owns at integration, so no rebase was made here; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245`, `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68` and `c327d138b996c4db8c842e4a2b174c65480be01b`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | medium | `.horos/boundary.json` | The round 1 record commit `63f0a865c1b663e95ac978a5cc1148ed46ddaaae` added `audit/rounds/fiat-1275-elenchus-emits-the-fixed-and-guarded-result.md` and its synopsis without refreshing the boundary, so committed `files_walked` stayed 2580 against a tree earning 2582; `entries` and every byte total matched a fresh scan. `tests/test_boundary_currency.py:164` and `tests/test_agent_instruction.py:118` both failed on `['.horos/boundary.json#counts']`, `python3 -m unittest discover -s tests` exited 1 with 2 failures in 1,337 tests, and `python3 scripts/run_checks.py --base origin/main` returned outcome red on `root-suite` alone, its nine other selected checks passing with no adapter timeout. Round 1 could not have seen it: it ran both suites before committing its record, and the step exit requires `run_checks.py` at exit zero on the committed tree. | fixed in `c327d138b996c4db8c842e4a2b174c65480be01b`; `horos.py scan . --write` regenerated `files_walked` to 2582 as a one-line change with no entry touched, and the round 2 record commit adds no tracked file, so the count holds |<br>| S1-R2-02 | low | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:501` | S1-R1-02's fix compared the drafted repair against `git rev-parse <ref>`, which does not peel: an annotated tag resolves to its own tag object. `elenchus.py` accepts any ref, peels it for both `diff-tree` and `<ref>^`, and echoes it unresolved into its result at `elenchus.py:268`, so a tag reaches the emitter inside an otherwise correct `guarded` result. Constructed on a scratch repository: the emitter refused at F010, naming the tag object as though it were a wrong commit, while the parent it had derived was correct. A lightweight tag was accepted, isolating the defect to annotated tags. The refusal fails closed, so no wrong record was written and no accepted record is affected. | fixed in `c327d138b996c4db8c842e4a2b174c65480be01b`; the first argument peels with `^{commit}`, guarded by `test_an_annotated_tag_naming_the_repair_is_accepted`, seen red against the unpeeled call and green after |<br>| S1-R2-03 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:382` | `record_findings` checks each field's shape and no relation between two fields, so `--check` reports `clean` at exit 0 on a record whose `verdict.status` is `guarded` while its own `unfixed_parent.report` records `assertion_failures` 0 and `errors` 0. Constructed by editing that one object in a genuine emitted record. The Promise's Refuses clause names "a guard that never failed without the fix", and those two fields settle it with no input beside the record. The emitter cannot produce the shape, because `result_findings` requires a `guarded` result whose report `elenchus.py` classified, so the exposure is `--check`, which the runbook exit and study section 4 both describe as validating a record on its own. | open -- closing it adds a fifth refusal, and study section 11 says "Four refusals decide whether a record exists at all, and each follows from the Promise's own clauses rather than from taste". A `hexctl amend study` is owed before the code may exceed its own specification; Warden did not run it |<br>| S1-R2-04 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:382` | `--check` also reports `clean` at exit 0 on a record whose `unfixed_parent.commit` equals its `fixed_tree.commit`, the exact shape S1-R1-02 demonstrated and recorded `--check` as accepting. Constructed by editing a genuine record's `unfixed_parent.commit` to its own `fixed_tree.commit`. `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68` closed the production path at F010 and left the validator unchanged, so the record shape stays acceptable from any other producer, or from an edit after emission. Study section 3 contemplates a second producer, and issue 1222's producer does not yet exist. | open -- a sixth refusal, owed to the same `hexctl amend study` as S1-R2-03 |<br>| S1-R2-05 | low | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:8` and `:630`, `docs/decisions/drafts/emit-the-fixed-and-guarded-result-as-a-closed-record.md:55` and `:64` | `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68` changed the derivation to `git rev-parse <ref> <ref>^` and left three renderings of the pre-fix `git rev-parse <ref>^` standing: the module docstring, the F008 diagnostic, which named on stderr a command the process never ran, and the decision draft's field table and derivation paragraph, which called it "the same `git rev-parse <ref>^` call". It is not the same call `elenchus.py` makes, and it returns two values rather than one. | fixed in `c327d138b996c4db8c842e4a2b174c65480be01b`; each renders `git rev-parse <ref>^{commit} <ref>^`, and the draft states what the first argument binds |<br>| S1-R2-06 | low | `docs/elenchus-fixed-and-guarded-record/study.md:90` and `:662` | The same stale rendering stands in the study body's derivation paragraph, which calls it "`git rev-parse <ref>^`, the same call `elenchus.py` makes", and in the dated amendment written to correct the study about the code. The amendment's three checkable claims hold, verified rather than taken on report: emit mode starts exactly two `git` processes, counted through a PATH shim as `git -C <repo> rev-parse <ref> <ref>^` and `git -C <repo> ls-files --error-unmatch -- <path>`; `--check` started zero; and `compose` at `fixed_and_guarded.py:575` takes `verdict` as `{status, detail}` straight from the result with no translation. What does not hold is the first call's argv: the amendment renders `git rev-parse <ref>^`, omitting the operand `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68` added, which is the one binding the resolved commit to the drafted repair. `c327d138b996c4db8c842e4a2b174c65480be01b` widens the gap by peeling that operand to `<ref>^{commit}`. | open -- the study is controller-owned and Warden must not hand-edit it. The committed copy is byte-identical to the amended `.hexaemeron/study.md`, both SHA-256 `0788a1b0fad48f30228b0099bfcc3aa51131d12100f1d47a351ab16fa5c5b3b0`, so a further `hexctl amend study` is owed and should carry S1-R2-03 and S1-R2-04's refusals if those are taken |<br><br>Leads not pursued: Round 1's two open findings are answered. The controller's `hexctl amend study` moved `.hexaemeron/study.md` from SHA-256 `12e5c1a84157d1b036a5e3533781cf83cd77aa77727a189b8fcad0122b7ef58b` to `0788a1b0fad48f30228b0099bfcc3aa51131d12100f1d47a351ab16fa5c5b3b0`, and its dated amendment corrects both sentences S1-R1-01 and S1-R1-05 named; `c327d138b996c4db8c842e4a2b174c65480be01b` copies those bytes over the stale committed copy without reflowing one, `cmp -s` agreeing. `docs/elenchus-fixed-and-guarded-record/runbook.md` was checked rather than assumed and was already byte-identical to `.hexaemeron/runbook.md`, both SHA-256 `6e442febe521226182545e26c6074eb615c6927993408c427730150ab30302de`. S1-R2-06 records what the amendment still gets wrong. The Elenchus `guarded` verdict's provenance was traced rather than reported: the parent comparison recorded 2 assertion failures in 1,337 executed tests, and both were the S1-R2-01 boundary tests that `c327d138b996c4db8c842e4a2b174c65480be01b` repaired, not the overlaid `test_elenchus_fixed_and_guarded.py`, which `python3 tests/run_tests.py` still cannot collect. S1-R1-06's accepted contract mismatch therefore stands, and `guarded` here is honest about the fix commit while saying nothing about this step's own guard. `checker-interface-drift` was verified against the branch point rather than taken from round 1: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at both `0fefcc986107ed66ff43c6572b7aa1c7351f12f4` and `c327d138b996c4db8c842e4a2b174c65480be01b`, `git diff` names neither file across the stack, and `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_checker` runs 30 tests green with no case edited. Round 1's three fixes were re-tested against the fixed tree rather than trusted: `secret-in-reproduction-output` end to end, a sentinel credential in the result's `output` reaching neither record nor stdout nor stderr, and the record holding exactly `schema` and the nine fields; S1-R1-04's reproduction, an unwritable destination directory, refusing at F009 with no traceback and an empty destination directory; and S1-R1-03's five hostile refs, `--all`, `-e`, `--local-env-vars`, `--since=2020-01-01` and `--branches=main`, each refused at F006 before argv. Round 1's standing leads were re-read and none changed: a draft read from outside the worktree is still accepted while only the output path is held to a symlink-free relative descendant; the four report counters and `reproduction.output_bytes` still accept an unbounded integer inside the 1 MiB input cap; a repeated `--out` is still last-wins under argparse; `repair.files` and `fixed_tree.report` are still operator assertions the emitter never checks. `mechanism-recurrence-claim` and `guard-name-unbound` were re-read against the diff and neither key set nor F007 changed. Hypomnema's study mode was not run: neither `.hexaemeron/study.md` nor its committed copy carries a `design-bridge` fence, so `--study` would report H008, but no runbook field, step exit or audit-loop obligation declares that mode for this run, the ordinary walk never infers a bridge, and study section 12 names both decision homes in prose instead. Raising it against a built step's own contract would be inventing a requirement. The peel introduced at `fixed_and_guarded.py:501` was not probed against every ref grammar `git rev-parse` accepts; `<ref>^{commit}` on a non-commit object fails the whole read into F008, which is the existing refusal, so no new acceptance was created. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface before and after the fix; on the fixed tree `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0 and `python3 scripts/run_checks.py --base origin/main` returned outcome green with all 10 selected checks passing and no adapter timeout, against the red result S1-R2-01 records for those same two commands on the round 1 tree; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 44 tests green, one more than round 1's 43.
## Step 1, round 3 -- 2026-09-05T21:30:07Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; whether the run integrates onto `origin/main`, which has advanced past this run's start and which the controller owns at integration, so no rebase was made here; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245`, `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`, `c327d138b996c4db8c842e4a2b174c65480be01b` and `da6fca5adc9abd2069055c13e40c1c65275ab01c`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict remains the declared runner result and not an attestation of its bytes; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R3-01 | medium | `docs/elenchus-fixed-and-guarded-record/study.md` and `docs/elenchus-fixed-and-guarded-record/runbook.md` | Both committed run documents were stale against the controller artefacts, so the step exit's byte-identity condition did not hold on the tree round 3 received. Four controller amendments since round 2 moved the study from SHA-256 `0788a1b0fad48f30228b0099bfcc3aa51131d12100f1d47a351ab16fa5c5b3b0` to `910886e1cef1c5b238ee872a4636ff42b98ad7cd6d257673d196e94e80784ebd` and the runbook from `6e442febe521226182545e26c6074eb615c6927993408c427730150ab30302de` to `96e0b9c8612b98493fc829510cf36027889331e87061a62f306b6169cc73a414`; `cmp -s` disagreed on both before the round. | fixed in `da6fca5adc9abd2069055c13e40c1c65275ab01c`; both copied from the controller artefacts without reflowing a byte, `cmp -s` now agreeing and both committed digests equal to the artefacts' |<br>| S1-R3-02 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:384` | `--check` reports `clean` at exit 0 on a record whose `verdict.status` is `guarded` while its own `unfixed_parent.report` records `assertion_failures` 0 and `errors` 1. `classify` at `elenchus.py:231-237` returns `inconclusive` for any report with `errors > 0` and never `guarded`, so the record's own evidence contradicts its verdict; the shape is one counter away from the one F012 refuses. Constructed by editing that single counter in a genuine emitted record and checking it through the shipped command as a subprocess. | open -- the second 2026-09-05 study amendment authorises exactly the zero-assertion-failures-and-zero-errors rule, which does not reach `errors > 0`. A seventh refusal is owed to a further `hexctl amend study`; Warden did not run it, because writing a rule ahead of the study is what produced S1-R1-01 |<br>| S1-R3-03 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:384` | `--check` also reports `clean` at exit 0 on a record whose `fixed_tree.report` records 2 assertion failures while `verdict.status` is `guarded`. The Promise requires a regression test "observed to fail on the unfixed parent and pass on the fixed tree", and `fixed_tree.report` is one of the nine evidence fields, so a fixed tree whose own report still fails contradicts the clause the record exists to evidence. `classify` reads only the parent report, so `elenchus.py` cannot settle it either. Constructed the same way, from a genuine emitted record. | open -- an eighth refusal, owed to the same `hexctl amend study` as S1-R3-02 |<br><br>Leads not pursued: Round 2's two open code findings are closed. `da6fca5adc9abd2069055c13e40c1c65275ab01c` adds F011 for S1-R2-04, refusing a record whose `unfixed_parent.commit` equals its `fixed_tree.commit`, and F012 for S1-R2-03, refusing a `guarded` record whose own parent report records zero assertion failures and zero errors; the second 2026-09-05 study amendment authorises both, naming six refusals rather than four. Each guard was seen red against the unrepaired validator before the fix, `--check` exiting 0 with empty stderr on both hostile records, and green after: `test_a_record_naming_one_commit_as_both_trees_is_refused` and `test_a_guarded_record_whose_parent_report_never_failed_is_refused`. Two further tests bound the change rather than the defect: `test_a_record_whose_two_trees_differ_and_whose_parent_failed_is_accepted` holds the emitted record against both new rules, and `test_a_malformed_record_is_not_also_charged_the_relation_refusals` holds the short-circuit, which returns the per-field findings alone when any exist so a shape the schema already refuses is not also charged a relation; that follows the pattern `_report_findings` at `fixed_and_guarded.py:237-239` already used for its own cross-field count rule. Neither new refusal can fire on a record the emitter composes, verified rather than assumed: `classify` at `elenchus.py:231-237` returns `guarded` only when `assertion_failures` exceeds 0, and a commit's first parent is never the commit itself, both derived in one `rev-parse`. Both therefore reach only `--check` in practice, although `record_findings` runs on the composed record at `fixed_and_guarded.py:675` before any file is touched, so no partial record can result from either. The `verdict.status == GUARDED` term in F012 is currently unreachable as false, because `_verdict_findings` refuses every other status at F004 or F002 and the relations run only on an otherwise clean record; it is kept as a defensive statement of the rule, in the same class as round 1's redundant `stat.S_ISLNK` term. The zero-executed-tests case is covered incidentally, since `executed` 0 forces both counters to 0, and was confirmed refused at F012. S1-R2-06 is answered: the amendment now renders the exact shipped argv, and the shipped code was confirmed to match rather than taken on report -- `fixed_and_guarded.py:501` builds `git -C <repo> rev-parse <ref>^{commit} <ref>^` and `:511` builds `git -C <repo> ls-files --error-unmatch -- <path>`, with the same two renderings in the module docstring at `:8`, the F008 diagnostic at `:631`, and the decision draft at `:55` and `:64`. The study body still reads "Four refusals" and section 9 still carries its superseded sentence; that is the appended-amendment mechanism working as designed, not a defect, so no finding was raised for it. S1-R1-06 is answered by the runbook amendment, and this round's `guarded` verdict was traced rather than reported: the run used `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `da6fca5adc9abd2069055c13e40c1c65275ab01c`, overlaid exactly `plugins/hexaemeron/tests/test_elenchus_fixed_and_guarded.py` onto the parent, executed 2,391 tests there and recorded 2 assertion failures with 0 errors on a complete report, the failures being this round's own two new guards rather than the boundary tests round 2's verdict rested on. The report path `.elenchus/fiat-1275-step-1.json` was fresh and resolved inside the detached parent worktree, so nothing was written into the run worktree. The parent run's exit code 1 is retained as a bounded diagnostic and did not classify the result, and this records the declared runner result without attesting its bytes. The contract's declared report format `elenchus.unittest.v1` was checked against both ends rather than assumed: it is the schema `run_tests.py` writes at `run_tests.py:302` and the one `parse_unittest_report` requires at `elenchus.py:124`, selected by the `--report-format unittest-json-v1` flag value, so the contract is coherent and no finding was raised. `checker-interface-drift` was verified against the branch point rather than taken from earlier rounds: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at `0fefcc986107ed66ff43c6572b7aa1c7351f12f4`, at `9e78bab8107b5714604ea52f48be91b0e9ab54f7` and in the working tree carrying this round's edits, and `git diff` across the stack names neither file. The emitter was exercised end to end through the shipped command as a subprocess on a scratch repository, not only through the test harness: a genuine record emitted at exit 0 and `--check` reported `clean`, the record held exactly `schema` and the nine fields, and a sentinel credential placed in the result's `output` reached neither the record nor stdout nor stderr, so `secret-in-reproduction-output` holds against the changed validator. `mechanism-recurrence-claim` was re-verified the same way: a record carrying an added cross-record key is refused at F001 by the closed key set. Three further register controls were exercised through the same shipped command rather than read: `guard-name-unbound` refuses a guard naming `t/a.py`, absent from the repair's changed test files, at F007; `output-path-escape` refuses both an absolute `--out` and a traversing `../` one at F009; and `unbounded-record-size` refuses a 5,000-byte `causal_mechanism.account` at F003. `partial-record-write` holds across all of them: no refusal in this round's probing left a file in the destination directory, which held only the one genuine record throughout. No committed `elenchus-fixed-and-guarded/v1` record file exists anywhere in the tree, so neither new refusal retroactively rejects an existing record; the exposure the study contemplates remains a second producer that does not yet exist. Round 1's and round 2's standing leads were re-read against this diff and none changed: a draft read from outside the worktree is still accepted while only the output path is held to a symlink-free relative descendant; the four report counters and `reproduction.output_bytes` still accept an unbounded integer inside the 1 MiB input cap; a repeated `--out` is still last-wins under argparse; `repair.files` and `fixed_tree.report` are still operator assertions the emitter never checks against git or a rerun, which S1-R3-03 now records as reaching the verdict as well; `guard-name-unbound` F007 and the closed key sets are unchanged by this diff; Hypomnema study mode was again not run, for the reason round 2 gave. `.horos/boundary.json` was re-checked after staging this round's record commit rather than only before it, the failure mode S1-R2-01 recorded. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 48 tests green, four more than round 2's 44; `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, the same count rounds 1 and 2 recorded, because that loader collects only `tests/` and this round's four new cases live under `plugins/hexaemeron/tests/`; and `python3 scripts/run_checks.py --base origin/main` returned outcome green at exit 0 on the fixes-commit tree, all 10 selected checks passing with no adapter timeout, `dead-code-suite` among them and judged against a clean worktree. A concurrent Fiat run was executing its own parallel suite on this machine throughout, at load averages reaching 183, which lengthened every run here without changing any outcome.
## Step 1, round 4 -- 2026-09-05T22:24:25Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; whether the run integrates onto `origin/main`, which has advanced past this run's start and which the controller owns at integration, so no rebase was made here; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245`, `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`, `c327d138b996c4db8c842e4a2b174c65480be01b`, `da6fca5adc9abd2069055c13e40c1c65275ab01c` and `a3652d2ff0686e53fce3eaf551f8b8630fb33d79`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict remains the declared runner result and not an attestation of its bytes; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R4-01 | medium | `docs/elenchus-fixed-and-guarded-record/study.md` and `docs/elenchus-fixed-and-guarded-record/runbook.md` | Both committed run documents were stale against the controller artefacts, so the step exit's byte-identity condition did not hold on the tree round 4 received. Four controller amendments since round 3, one on the study and three on the runbook, moved the study from SHA-256 `910886e1cef1c5b238ee872a4636ff42b98ad7cd6d257673d196e94e80784ebd` to `93c1b9ee6affbc10f142b69fb10854675bb8296f2ec1a8e8ed322f5306168091` and the runbook from `96e0b9c8612b98493fc829510cf36027889331e87061a62f306b6169cc73a414` to `0875e6c7cbeef446d44acaa40a7b24d780f9b59ffdcde185e303b6e43fc37111`; `cmp -s` disagreed on both before the round. This is the second consecutive round to find it, after S1-R3-01, and the amendment count was verified against both artefacts rather than taken on report. | fixed in `a3652d2ff0686e53fce3eaf551f8b8630fb33d79`; both copied from the controller artefacts without reflowing a byte, `cmp -s` now agreeing and both committed digests equal to the artefacts' |<br>| S1-R4-02 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:613` and `:386` | `fixed_tree.commit` is bound to nothing. `compose` takes it straight from the draft, F010 binds only the result's resolved ref to `repair.commit`, and F011 compares `unfixed_parent.commit` only against `fixed_tree.commit`, so no rule on either path reads `fixed_tree.commit` against `repair.commit`. Constructed on a scratch repository through the shipped command: a draft naming `repair.commit` `86ff65d94b38` with `fixed_tree.commit` an unrelated third commit `1e704584e428` emitted at exit 0, and `--check` reported the written record clean. The record then asserts its guard passed on a tree that is not the repair, decided from two fields it already carries. Unlike S1-R2-03, S1-R3-02 and S1-R3-03, this shape reaches the emit path as well as `--check`. | open -- a ninth member of section 11's enumeration, which is closed and currently holds eight. The rule does not authorise a refusal nobody has written down, so a `hexctl amend study` is owed before the code may exceed its own specification; Warden did not run it, because writing a rule ahead of the study is what produced S1-R1-01 |<br>| S1-R4-03 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:386` and `:670` | `--check` reports `clean` at exit 0 on a record whose `guard.file` is absent from `repair.files`. F007 tests `guard.file` against `result["tests"]` on the emit path alone, and `--check` has no result beside it; the record's own account of the fix commit's changed files is `repair.files`, which no rule reads. Constructed the same way: a draft whose `repair.files` held only `src/w.py` while `result.tests` held `tests/test_widget.py` emitted at exit 0, and `--check` reported it clean. Enumeration member 3 refuses a guard naming a test absent from the fix commit's changed test files, and that rule is not applied to the record's own statement of those files. | open -- a tenth member, owed to the same `hexctl amend study` as S1-R4-02 |<br><br>Leads not pursued: Round 3's two open findings are closed. The third 2026-09-05 study amendment restates section 11's refusal set as a rule with a closed enumeration rather than a count, and authorises exactly two more members; `a3652d2ff0686e53fce3eaf551f8b8630fb33d79` adds F013 for S1-R3-02, refusing a `guarded` record whose `unfixed_parent.report` records any errors above zero, and F014 for S1-R3-03, refusing a `guarded` record whose `fixed_tree.report` records any assertion failures or errors above zero. The authorising text was read from the study rather than from the directive, and the two differ: the study's eighth member covers errors as well as assertion failures on the fixed tree, so `test_a_guarded_record_whose_fixed_tree_report_failed_is_refused` sub-tests both counters and F014 reads both. Each guard was seen red against the unrepaired validator before the fix, `--check` exiting 0 with empty stderr on all three hostile records, and green after: `test_a_guarded_record_whose_parent_report_errored_is_refused` and the two sub-tests of `test_a_guarded_record_whose_fixed_tree_report_failed_is_refused`. Two existing cases were widened rather than left behind the change: `test_a_malformed_record_is_not_also_charged_the_relation_refusals` now holds the short-circuit against all four relation codes, and `test_a_record_whose_two_trees_differ_and_whose_parent_failed_is_accepted` now asserts parent `errors` 0 and `fixed_tree` failures plus errors 0 on the emitted record. `_relation_findings` gained an early return on a status other than `guarded`, which keeps F012's defensive status term, adds the same term to F013 and F014, and leaves F011 status-independent, since one commit named as both trees is wrong under any verdict. Neither new refusal can fire on a record the emitter composes from a genuine result, verified rather than assumed: `classify` at `elenchus.py:231-237` returns `guarded` only for a report with `errors` 0 and `assertion_failures` above 0, so F013's condition is unreachable through `elenchus.py`, which the Elenchus skill states as the mixed-report case being `inconclusive`. F014 reads `fixed_tree.report`, the operator's rerun that the emitter never checks, so it can fire on emission; the emit-path probe confirms it refuses at F014 with nothing written and an untouched destination directory, which is the fail-closed direction. Four legitimate variants were checked clean through the shipped command: skips above zero on both reports, a second `suites` entry recording exit code 1, eleven parent assertion failures, and a one-executed-test pair. The asymmetry worth naming is that `suites` still accepts a non-zero exit code while `fixed_tree.report` may no longer carry a failure; the study's Boundaries clause requires both suites green before a commit and the amendment authorises exactly this rule, so no finding was raised. No committed `elenchus-fixed-and-guarded/v1` record exists anywhere in the tree, so neither new refusal retroactively rejects an existing record. Two further cross-field contradictions were probed and are the round's two open findings; a third candidate, `reproduction.output_bytes` 0 beside a digest that is not the empty string's, was not pursued, because settling it needs a constant carried from outside the record and the study's rule reads nothing outside it. Task C was confirmed from the packet rather than assumed: the source-bound `runbook_step` names test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `elenchus.unittest.v1`, report file `.elenchus/fiat-1275-step-1.json`, and its recorded `study_sha256` equals the current study digest, so the repair is bound rather than un-bound again. The runbook now carries six amendments, two byte-identical sets of three covering steps 1, 2 and 3; the second set's own rationale states that the first was un-bound when the study amendment changed the digest every runbook amendment records, so the duplication is the amend mechanism documenting itself and no finding was raised. The study body still reads "Four refusals" against an enumeration that now holds eight; that is the appended-amendment mechanism working as designed, the judgement round 3 reached on the same sentence. `checker-interface-drift` was verified against the branch point rather than taken from earlier rounds: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at `0fefcc986107ed66ff43c6572b7aa1c7351f12f4` and in the working tree carrying this round's edits, `git diff` across the stack names neither file, and `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_checker` runs 30 tests green with no case edited. That command also prints a `scheduler-error` block from a child the checker's own write-failure case starts, under an `invocation_root` named `hexaemeron-write-failure`; it is stdout noise from a deliberately constructed failure rather than a red suite, and `test_elenchus_checker.py` is a file this delivery must not edit, so it was left alone. Six register controls were re-exercised through the shipped command as a subprocess rather than only through the harness: `draft-input-untrusted` refuses a duplicate draft key at F000 and an unknown result key at F006; `mechanism-recurrence-claim` refuses an added cross-record key by the closed key set; `unbounded-record-size` refuses a 5,000-byte and a non-printable `causal_mechanism.account` at F003; `guard-name-unbound` refuses a guard naming `t/a.py` at F007; `output-path-escape` refuses an absolute, a traversing and a tracked `--out` at F009; and `secret-in-reproduction-output` holds end to end, a sentinel credential in the result's `output` reaching neither record nor stdout nor stderr while the record held exactly `schema` and the nine fields. `partial-record-write` holds across all of them: no refusal in this round's probing left a file in a destination directory. Round 2's annotated-tag acceptance was re-checked against the changed validator and still holds. Earlier rounds' standing leads were re-read against this diff and none changed: a draft read from outside the worktree is still accepted while only the output path is held to a symlink-free relative descendant; the four report counters and `reproduction.output_bytes` still accept an unbounded integer inside the 1 MiB input cap; a repeated `--out` is still last-wins under argparse; `repair.files` and `fixed_tree.report` are still operator assertions the emitter never checks against git or a rerun, which F014 now constrains on one side only; Hypomnema study mode was again not run, for the reason round 2 gave. This round's `guarded` verdict was traced rather than reported: the run used `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `a3652d2ff0686e53fce3eaf551f8b8630fb33d79`, overlaid exactly `plugins/hexaemeron/tests/test_elenchus_fixed_and_guarded.py` onto the parent, executed 2,393 tests there and recorded 3 assertion failures with 0 errors on a complete report, those three being this round's own two new guards counted as one failure and two sub-test failures. The report path `.elenchus/fiat-1275-step-1.json` was fresh and resolved inside the detached parent worktree, leaving no `.elenchus` directory in the run worktree; the parent run's exit code 1 is retained as a bounded diagnostic and did not classify the result. `.horos/boundary.json` was re-checked after staging this round's commits rather than only before them, the failure mode S1-R2-01 recorded. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 50 tests green, two more than round 3's 48; `python3 -m unittest plugins.hexaemeron.tests.test_evolution` runs 11 tests green; `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py` exits 0 on all three shipped documents; `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, the same count every earlier round recorded, because that loader collects only `tests/` and this round's new cases live under `plugins/hexaemeron/tests/`; and `python3 scripts/run_checks.py --base origin/main` returned outcome green at exit 0 on the fixes-commit tree, all 10 selected checks passing with no adapter timeout, `dead-code-suite` among them and judged against a clean worktree.
## Step 1, round 5 -- 2026-09-05T23:12:48Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; whether the run integrates onto `origin/main`, which has advanced to `1f4111d82c43fba69c01742947b392efb6454c4b` and which the controller owns at integration, so no rebase was made here; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245`, `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`, `c327d138b996c4db8c842e4a2b174c65480be01b`, `da6fca5adc9abd2069055c13e40c1c65275ab01c`, `a3652d2ff0686e53fce3eaf551f8b8630fb33d79` and `9bfb88277ca0916dce15ec79dba8b9cd21e311cf`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict remains the declared runner result and not an attestation of its bytes; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R5-01 | medium | `docs/elenchus-fixed-and-guarded-record/study.md` and `docs/elenchus-fixed-and-guarded-record/runbook.md` | Both committed run documents were stale against the controller artefacts, so the step exit's byte-identity condition did not hold on the tree round 5 received. Two controller amendments since round 4, one on the study and one on the runbook, moved the study from SHA-256 `93c1b9ee6affbc10f142b69fb10854675bb8296f2ec1a8e8ed322f5306168091` to `b3986fa9e4ce6d1795f180c513491bd66d5e32287095c9ede05a9cbf3aa8b154` and the runbook from `0875e6c7cbeef446d44acaa40a7b24d780f9b59ffdcde185e303b6e43fc37111` to `2fe854f86a18145910a0094edd77554bf115ed08e7de667e11fd3e7739a5c754`; `cmp -s` disagreed on both before the round. This is the third consecutive round to find it, after S1-R3-01 and S1-R4-01, and the amendment count was verified against both artefacts rather than taken on report: the study carries four amendments against the three round 4 committed, and the runbook seven against six. | fixed in `9bfb88277ca0916dce15ec79dba8b9cd21e311cf`; both copied from the controller artefacts without reflowing a byte, `cmp -s` now agreeing and both committed digests equal to the artefacts' |<br>| S1-R5-02 | medium | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:438` and `:635` | A `guarded` record whose `fixed_tree.report` records `executed` 0 is accepted. F014 refuses only counters above zero, and `compose` takes `fixed_tree` straight from the draft, so no rule reads the fixed tree's executed count. The Boundary clause names a zero-test comparison explicitly, and `classify` at `elenchus.py:233` returns `inconclusive` for a report recording no executed tests, so the record's own evidence contradicts its verdict. Constructed through the shipped command on a scratch repository: a draft whose `fixed_tree.report` was `complete` true, `executed` 0 and every counter 0 emitted at exit 0 with empty stderr, and `--check` reported the written record clean. Like S1-R4-02 this reaches the emit path, because `fixed_tree` is the operator's own rerun the emitter never checks. The parent side has no such gap: `unfixed_parent.report` with `executed` 0 forces both counters to 0 and is refused at F012, verified rather than taken from round 3. | open -- an eleventh member of section 11's enumeration, which is closed and now holds ten. The rule does not authorise a refusal nobody has written down, so a `hexctl amend study` is owed before the code may exceed its own specification; Warden did not run it, because writing a rule ahead of the study is what produced S1-R1-01 |<br><br>Leads not pursued: Round 4's two open findings are closed. The fourth 2026-09-05 study amendment adds section 11's ninth and tenth members and states the enumeration stays closed at ten; the authorising text was read from the study rather than from the directive. `9bfb88277ca0916dce15ec79dba8b9cd21e311cf` adds F015 for S1-R4-02, refusing a record whose `fixed_tree.commit` is not `repair.commit`, and F016 for S1-R4-03, refusing a record whose `guard.file` is absent from `repair.files`. Both sit in `_relation_findings` beside F011 rather than behind its `guarded` early return, because each is wrong under any verdict; that placement also gives F015 the emit path the amendment's ninth member requires, since `emit` calls `record_findings` on the composed record at `fixed_and_guarded.py:711` before any file is touched. Each guard was seen red against the unrepaired validator before the fix and green after: `test_a_record_whose_fixed_tree_is_not_the_repair_is_refused` and `test_a_record_whose_guard_is_absent_from_the_repairs_files_is_refused`, four sub-tests across the two, every one failing `0 != 1` with empty stderr, which is the unfixed emitter writing the record at exit 0 and `--check` calling it clean. Two existing cases were widened rather than left behind the change: `test_a_malformed_record_is_not_also_charged_the_relation_refusals` now holds the short-circuit against all six relation codes, and `test_a_record_whose_two_trees_differ_and_whose_parent_failed_is_accepted` now asserts `fixed_tree.commit` equals `repair.commit` and `guard.file` is among `repair.files` on the emitted record. Neither refusal fires on a legitimate record, checked through the shipped command as a subprocess rather than only through the harness: seven variants emitted and checked clean, a plain genuine record, an annotated tag naming the repair, thirty-one repair files with the guard last, a repair touching only the guard file, the guard listed first in `repair.files`, two changed test files with the guard among them, and a guard nested three directories deep. Round 2's annotated-tag acceptance was the specific regression re-checked there and still holds. Both refusals were also driven hostile through the shipped command on both paths, each naming its code and field on stderr at exit 1, and `partial-record-write` held throughout: no refusal left a file or a staged remnant in a destination directory. F016 now fires on emission as well, where F007 previously settled the guard against `result.tests` alone, so a draft whose `repair.files` omits the guard is refused rather than written; that is the rule the amendment states and the fail-closed direction, so no finding was raised. Eight candidate eleventh cases were probed through the shipped command; one is S1-R5-02 and the other seven were not pursued. `verdict.detail` carrying `classify`'s `passed` string beside a `guarded` status is accepted, but the Promise names no clause about the detail's content and separating a hand-written detail from a genuine one needs the comparison re-run, which the emitter deliberately does not do. A non-zero `suites[].exit_code` beside a `guarded` verdict is accepted; the Promise's Evidence clause names both suite results as evidence without requiring them green, and the study's own Boundaries section is a delivery-process rule for this run's authors rather than a clause of the Promise the record evidences, which is round 4's judgement on the same asymmetry. `reproduction.output_bytes` 0 beside a digest that is not the empty string's is accepted, and settling it needs a constant carried from outside the record, which round 4 also declined. `minimal_case.path` outside `repair.files` and `causal_mechanism.site` in a file the repair never touched are both accepted and neither is a contradiction: a reduced case need not be a repaired file, and a fix can be applied away from where the mechanism starts. `fixed_tree.report` recording `skipped` equal to `executed` is accepted; whether a skipped test sits inside `executed` is an adapter detail the record does not carry, so it cannot be decided from carried fields. `unfixed_parent.commit` not being the parent of `repair.commit` is accepted under `--check` and is undecidable from carried fields by the rule's own terms, since settling it means reading the repository; it does not reach the emit path, where the parent is derived. With F015 in place, F011 is unreachable on emission, because `fixed_tree.commit` now equals the resolved repair and a commit's first parent is never the commit itself; it is kept as a defensive statement of the rule, in the same class as F012's status term and round 1's redundant `stat.S_ISLNK` term. No committed `elenchus-fixed-and-guarded/v1` record file exists anywhere in the tree, so neither new refusal retroactively rejects an existing record. Task C was confirmed from the packet rather than assumed: the source-bound `runbook_step` names test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `elenchus.unittest.v1`, report file `.elenchus/fiat-1275-step-1.json`, and its amendment's recorded `study_sha256` `b3986fa9e4ce6d1795f180c513491bd66d5e32287095c9ede05a9cbf3aa8b154` equals the current study digest, so the repair is bound rather than un-bound a third time. Seven register controls were re-exercised against the changed validator through the shipped command as a subprocess: `draft-input-untrusted` refuses a duplicate draft key at F000 and an unknown result key at F006; `unbounded-record-size` refuses a 5,000-byte and a non-printable `causal_mechanism.account` at F003; `guard-name-unbound` refuses a guard naming `t/a.py` at F007; `output-path-escape` refuses an absolute, a traversing and a tracked `--out` at F009; `record-overclaims-verdict` refuses `passed`, `unguarded` and `inconclusive` at F004; `mechanism-recurrence-claim` refuses an added cross-record key at F001 by the closed key set; and `secret-in-reproduction-output` holds end to end, a sentinel credential in the result's `output` reaching neither record nor stdout nor stderr while the record held exactly `schema` and the nine fields. `checker-interface-drift` was verified against the branch point rather than taken from earlier rounds: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at `0fefcc986107ed66ff43c6572b7aa1c7351f12f4` and in the working tree carrying this round's edits, `git diff` across the stack names neither file, and the four state strings at `elenchus.py:233-238` and `:281` are unchanged. Earlier rounds' standing leads were re-read against this diff and none changed: a draft read from outside the worktree is still accepted while only the output path is held to a symlink-free relative descendant; the four report counters and `reproduction.output_bytes` still accept an unbounded integer inside the 1 MiB input cap; a repeated `--out` is still last-wins under argparse; `repair.files` and `fixed_tree.report` are still operator assertions the emitter never checks against git or a rerun, which F015, F016 and S1-R5-02 now bound on three further sides; Hypomnema study mode was again not run, for the reason round 2 gave. The study body still reads "Four refusals" against an enumeration that now holds ten; that is the appended-amendment mechanism working as designed, the judgement rounds 3 and 4 reached on the same sentence. This round's `guarded` verdict was traced rather than reported: the run used `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `9bfb88277ca0916dce15ec79dba8b9cd21e311cf`, overlaid exactly `plugins/hexaemeron/tests/test_elenchus_fixed_and_guarded.py` onto the parent, executed 2,395 tests there and recorded 4 assertion failures with 0 errors on a complete report, those four being this round's own two new guards at two sub-tests each, the emit and `--check` paths of both. The report path `.elenchus/fiat-1275-step-1.json` was fresh and resolved inside the detached parent worktree, leaving no `.elenchus` directory in the run worktree; the parent run's exit code 1 is retained as a bounded diagnostic and did not classify the result. That run's captured output also carries `hexctl: error:` lines from children the hexctl cases deliberately construct, stdout noise from constructed failures rather than a red suite, the same class round 4 recorded for the checker's write-failure case. `.horos/boundary.json` was re-checked after staging this round's commits rather than only before them, the failure mode S1-R2-01 recorded. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 52 tests green, two more than round 4's 50; `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py` exits 0 on all three shipped documents, each scoring 100.0 with 0 defects; `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, the same count every earlier round recorded, because that loader collects only `tests/` and this round's new cases live under `plugins/hexaemeron/tests/`; and `python3 scripts/run_checks.py --base origin/main` returned outcome green at exit 0 on the fixes-commit tree against base `1f4111d82c43fba69c01742947b392efb6454c4b`, all 10 selected checks passing with no adapter timeout, `dead-code-suite` among them and judged against a clean worktree.
## Step 1, round 6 -- 2026-09-05T23:43:57Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=not-applicable; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity; the `elenchus-v1.4.0` ledger row and the four Hexaemeron re-pin surfaces that `frontier-drift` names, which later steps own, so that id is not-applicable here; the end-to-end demo path, which the last step owns; a SHA-256 object-format repository, where `COMMIT` accepts 64 hex and no case exercises it; durability of the `os.replace` rename across a power loss, since the destination directory is never fsynced; a race between the output path's symlink walk and the staged write, which needs write access to the operator's own worktree; whether the run integrates onto `origin/main`, which has advanced to `c5c5f36217d3ab812a547175266a80dd4d945a78` and which the controller owns at integration, so no rebase was made here; behaviour outside signed commits `d95a089976c3da1d815e3d39c960cd760029c245`, `f2d385cbfd2f4ddac2e0e42e05dfbb8d2d075b68`, `c327d138b996c4db8c842e4a2b174c65480be01b`, `da6fca5adc9abd2069055c13e40c1c65275ab01c`, `a3652d2ff0686e53fce3eaf551f8b8630fb33d79`, `9bfb88277ca0916dce15ec79dba8b9cd21e311cf` and `f5ffcd1fd0a77ece8274632fc74c2c16dea15b0c`; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict remains the declared runner result and not an attestation of its bytes; whether issue 1222's producer adopts these key names, unknowable while that producer does not exist<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R6-01 | medium | `docs/elenchus-fixed-and-guarded-record/study.md` and `docs/elenchus-fixed-and-guarded-record/runbook.md` | Both committed run documents were stale against the controller artefacts, so the step exit's byte-identity condition did not hold on the tree round 6 received. Two controller amendments since round 5, one on each, moved the study from SHA-256 `b3986fa9e4ce6d1795f180c513491bd66d5e32287095c9ede05a9cbf3aa8b154` to `70e44498b74ec3d430f32d918d5f11230aadca5f1c76a505e76aaef00e024291` and the runbook from `2fe854f86a18145910a0094edd77554bf115ed08e7de667e11fd3e7739a5c754` to `696eb54a36a9b47181639a52861ddfacfa14a64b861c2eb3f632bd6d32e910bb`; `cmp -s` disagreed on both before the round. This is the fourth consecutive round to find it, after S1-R3-01, S1-R4-01 and S1-R5-01, and the amendment count was verified against both artefacts rather than taken on report: the study carries five amendments against the four round 5 committed, and the runbook eight against seven. | fixed in `f5ffcd1fd0a77ece8274632fc74c2c16dea15b0c`; both copied from the controller artefacts without reflowing a byte, `cmp -s` now agreeing and both committed digests equal to the artefacts' |<br>| S1-R6-02 | low | `plugins/hexaemeron/skills/elenchus/scripts/fixed_and_guarded.py:696` and `:647` | `repair.files` is never read against `result["tests"]`, the git-derived changed test files the emitter holds at emit time. F007 checks only that `guard.file` is among `result.tests` and F016 only that it is among `repair.files`, so a draft whose `repair.files` omits a test file `result.tests` names is written at exit 0 and `--check` reports it clean. Constructed through the shipped command on a scratch repository whose repair touched `src/widget.py`, `tests/test_widget.py` and `tests/test_other.py`: a draft listing the first two emitted cleanly, and the written `repair.files` then omits a file the commit touched, against the study field table's `repair` row, "the commit that repaired the mechanism, and the files it touched". This reaches the emit path with a genuine `elenchus.py` result rather than a hand-authored one. The guard claim stays sound, because F007 binds `guard.file` to the git-derived list and F015 binds `fixed_tree.commit` to `repair.commit`, so the defect is an incomplete evidence listing and not a false guard, which is why it is low rather than medium. | open -- it does not fit section 11's rule, which decides a refusal from carried fields alone and `result.tests` is not carried; closing it needs a rule of enumeration member 3's emit-path family instead of a twelfth carried-field member. A `hexctl amend study` is owed before the code may exceed its own specification; Warden did not run it, because writing a rule ahead of the study is what produced S1-R1-01 |<br><br>Leads not pursued: Round 5's open finding is closed. The 2026-09-06 study amendment adds section 11's eleventh member and states the enumeration stays closed at eleven; the authorising text was read from the study rather than from the directive. `f5ffcd1fd0a77ece8274632fc74c2c16dea15b0c` adds F017 for S1-R5-02, refusing a `guarded` record whose `fixed_tree.report` records zero executed tests. It sits after `_relation_findings`' `guarded` early return beside F014, and reaches the emit path because `compose` at `fixed_and_guarded.py:644` takes `fixed_tree` straight from the draft and `emit` calls `record_findings` on the composed record at `:720` before any file is touched. Both sub-tests of `test_a_guarded_record_whose_fixed_tree_executed_no_tests_is_refused` were seen red against the unrepaired emitter, each failing `0 != 1` with empty stderr, which is the unfixed emitter writing the record at exit 0 and `--check` calling it clean, and green after. The amendment's parent-side claim was verified rather than assumed: `test_a_parent_report_executing_no_tests_needs_no_eleventh_counterpart` holds that `executed` 0 forces both parent counters to 0 under `_report_findings`' count rule and is then refused at F012, and that a parent claiming one assertion failure it never executed is refused earlier at F002; that test is green before and after the fix, because it records a property rather than guards the change. Two existing cases were widened rather than left behind: `test_a_record_whose_two_trees_differ_and_whose_parent_failed_is_accepted` now asserts `fixed_tree.report.executed` above zero, and `test_a_malformed_record_is_not_also_charged_the_relation_refusals` now holds the short-circuit against all seven relation codes. The refusal cannot fire on a legitimate record, checked through the shipped command as a subprocess rather than only through the harness: four variants emitted and checked clean, a plain genuine record, a one-executed-test fixed tree, a three-executed three-skipped fixed tree, and a 2,395-executed 17-skipped one. Driven hostile through the same command it refuses on both paths at exit 1, naming F017 and `verdict.status`, with nothing written and the destination directory holding only the genuine records; `partial-record-write` held throughout, no refusal leaving a file or a staged remnant. The interaction question was answered by sweep rather than by reading. All seven relation codes are individually reachable under `--check`, and exactly two pairs never co-fire: F012 with F013, whose conditions contradict, and F014 with F017, because `executed` 0 forces both fixed-tree counters to 0 and a record carrying `executed` 0 beside a counter above zero is refused earlier at F002 by the count rule. Every other relation pair co-fires, and `report` at `fixed_and_guarded.py:750` prints every finding in list order, each naming its own code and field, so no refusal is masked by an earlier one and there is no single first refusal to get wrong. Emit-path reachability was probed the same way: F011 fires on emission only alongside F015, never alone, which is round 5's claim restated as what the probe shows; F007 and F016 are independent, a guard listed in `repair.files` but absent from `result.tests` firing F007 alone and a guard absent from `repair.files` firing F016 alone; F012 and F013 reach emission only through a hand-written result, since `classify` at `elenchus.py:231-238` never returns `guarded` for those shapes; and F014, F015, F016 and F017 are the four a genuine producer can trip, all on draft-held fields. The report-versus-verdict class is now closed on both sides, established by exhaustive sweep rather than by argument: over 16,384 parent-and-fixed report pairs drawn from `executed`, `assertion_failures` and `errors` in 0 to 3 and `skipped` in 0 to 1, the validator accepts 72 and every one has `classify(parent)` `guarded` and `classify(fixed)` `passed`, with zero disagreements. The converse sweep found 36 pairs `classify` would call clean that the validator refuses, all at F002 "outcome counts exceed executed tests" on a parent recording more failures than executed tests; that is the schema's own count rule shipped in round 1, refusing an incoherent report in the fail-closed direction, so no finding was raised. Round 5's seven declined candidate members were re-read before anything new was proposed and none changed: `verdict.detail` carrying a `passed` string beside a `guarded` status, a non-zero `suites[].exit_code`, `reproduction.output_bytes` 0 beside a digest that is not the empty string's, `minimal_case.path` outside `repair.files`, `causal_mechanism.site` in a file the repair never touched, and `unfixed_parent.commit` not being the parent of `repair.commit` under `--check` all stand on the reasons round 5 recorded. `fixed_tree.report` recording `skipped` equal to `executed` was re-checked against F017 specifically: `executed` 0 with `skipped` 4 is now refused, because `skipped` sits outside the count rule's sum, while `executed` 5 with `skipped` 5 is still accepted and round 5's reason is untouched, so it was not re-raised. The one new candidate is S1-R6-02, which narrows the standing lead every round since round 1 has recorded, that `repair.files` and `fixed_tree.report` are operator assertions the emitter never checks against git or a rerun; the new evidence is that for the changed-test subset the emitter does hold the git-derived counterpart in `result.tests` at the moment it writes, which is why it was promoted from a lead to a finding rather than recorded again as a lead. `guard.test`, the test name inside the guard file, is still checked against nothing, and settling it needs reading the file rather than any carried field or either input, so it stays a lead. `checker-interface-drift` was verified against the branch point rather than taken from earlier rounds: `elenchus.py` is blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` at `0fefcc986107ed66ff43c6572b7aa1c7351f12f4` and in the working tree carrying this round's edits, `git diff --name-only` across the stack names neither file, the four state strings at `elenchus.py:233-238` and `:281` are unchanged, and `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_checker` runs 30 tests green with no case edited. Ten register controls were re-exercised against the changed validator through the shipped command as a subprocess: `draft-input-untrusted` refuses a duplicate draft key at F000 and an unknown result key at F006; `unbounded-record-size` refuses a 5,000-byte and a non-printable `causal_mechanism.account` at F003; `guard-name-unbound` refuses a guard naming `t/a.py` at F007; `output-path-escape` refuses an absolute, a traversing and a tracked `--out` at F009; `record-overclaims-verdict` refuses `passed`, `unguarded` and `inconclusive` at F004; `mechanism-recurrence-claim` refuses an added cross-record key at F001 by the closed key set; and `secret-in-reproduction-output` holds end to end, a sentinel credential in the result's `output` reaching neither record nor stdout nor stderr while the record held exactly `schema` and the nine fields. No committed `elenchus-fixed-and-guarded/v1` record file exists anywhere in the tree, so F017 retroactively rejects no existing record. Task C was confirmed from the packet rather than assumed: the source-bound `runbook_step` names test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `elenchus.unittest.v1`, report file `.elenchus/fiat-1275-step-1.json`, and its amendment's recorded `study_sha256` `70e44498b74ec3d430f32d918d5f11230aadca5f1c76a505e76aaef00e024291` equals the current study digest, so the repair is bound rather than un-bound a fourth time. The study body still reads "Four refusals" against an enumeration that now holds eleven, and section 9 still closes "no subprocess is started" against an amendment that replaced it; both are the appended-amendment mechanism working as designed, the judgement rounds 3, 4 and 5 reached on the same sentences. Hypomnema study mode was again not run, for the reason round 2 gave. This round's `guarded` verdict was traced rather than reported: the run used `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `f5ffcd1fd0a77ece8274632fc74c2c16dea15b0c`, overlaid exactly `plugins/hexaemeron/tests/test_elenchus_fixed_and_guarded.py` onto the parent, executed 2,397 tests there and recorded 2 assertion failures with 0 errors on a complete report, those two being this round's own new guard at one sub-test per path. The report path `.elenchus/fiat-1275-step-1.json` was fresh and resolved inside the detached parent worktree, leaving no `.elenchus` directory in the run worktree; the parent run's exit code 1 is retained as a bounded diagnostic and did not classify the result. `.horos/boundary.json` was re-checked after staging this round's commits rather than only before them, the failure mode S1-R2-01 recorded: a fresh scan reports `files_walked` 2582 and 134 entries against a committed 2582, and `python3 -m unittest tests.test_boundary_currency tests.test_agent_instruction` runs 337 tests green. Mechanical evidence: Phylax, Ephoros and Hypomnema each exited 0 over the changed surface; `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` runs 54 tests green, two more than round 5's 52; `python3 -m unittest plugins.hexaemeron.tests.test_evolution` runs 11 tests green; `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py` exits 0 on all three shipped documents, each scoring 100.0 with 0 defects; `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, the same count every earlier round recorded, because that loader collects only `tests/` and this round's new cases live under `plugins/hexaemeron/tests/`; and `python3 scripts/run_checks.py --base origin/main` returned outcome green at exit 0 on the fixes-commit tree against base `c5c5f36217d3ab812a547175266a80dd4d945a78`, all 10 selected checks passing with no adapter timeout, `dead-code-suite` among them and judged against a clean worktree.
## Step 2, round 1 -- 2026-09-06T02:03:20Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=reviewed; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity and the `security_suite` receipt records that waiver; the emitter's runtime behaviour, not re-exercised on a scratch repository, because step 2 adds no executable behaviour and `elenchus.py` blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2`, `fixed_and_guarded.py` blob `b830c8ad825bc348d45f0ce55a813250eec1273e` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` are each identical at the branch point `c7d1900a` and at this round's head; step 1's ten open findings (S1-R1-01, S1-R1-05, S1-R2-03, S1-R2-04, S1-R3-02, S1-R3-03, S1-R4-02, S1-R4-03, S1-R5-02, S1-R6-02), which step 1's closed audit owns; the study and runbook bodies, controller-owned and byte-pinned, so the study's own open S1-R1-01 "no subprocess is started" and S1-R1-05 "the runner contract that produced it" sentences were read and not repaired; `guard.test`, still checked against nothing, the standing lead round 6 recorded; step 3's demonstration path, which the last step owns; whether the run integrates onto `origin/main`, which has advanced past this run's start and which the controller owns at integration, so no rebase or rewrite of the signed stack was made here; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict stays the declared runner result and not an attestation of its bytes<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | `plugins/hexaemeron/skills/elenchus/SKILL.md:392` | The new `## Emit the result as a record` section splits the eighteen refusal codes into section 11's closed eleven-member enumeration and "the other seven", then characterises those seven as three families: an input that will not read as one bounded closed object, a parent that cannot be soundly derived from the result's own `ref`, and a destination the emitter will not write to. The eleven were verified as exactly the codes `record_findings` can raise at `fixed_and_guarded.py:456-477` together with `_relation_findings` at `:389-453`: F001, F002, F003, F004, F011, F012, F013, F014, F015, F016 and F017. The remaining seven are therefore F000, F005, F006, F007, F008, F009 and F010, and the three families account for six of them, F000, F005 and F006, then F008 and F010, then F009. F007 fits none: `emit` at `:696` refuses a well-formed draft beside a well-formed result when `draft["guard"]["file"]` is absent from `result["tests"]`, the git-derived changed test files the comparison used, which is the one emit-path refusal reading the comparison's own substance rather than a shape, the git graph or the destination. S1-R6-02 established the same separation from the other side, that `result.tests` is not carried and F007's family sits outside section 11's rule. F007 is named accurately three lines above in the four-refusal list and in the field table's `guard` row, so a reader is not left ignorant of it; what the sentence misstates is the shape of the refusal surface, which is why it is low. | fixed in `37fd18a039e1d1f22d78682add8f1eed03da09ce`; the sentence now names "a guard absent from the changed test files the comparison used" beside the other three families, no other line of the section changed, and imprimatur still scores 100.0 with 0 defects on the file |<br><br>Leads not pursued: Four leads were opened and left. First, the section's "An emitted record establishes what the Promise says and no more". Seven of the nine fields come from the operator draft, and `compose` at `fixed_and_guarded.py:639` copies `reproduction`, `causal_mechanism`, `minimal_case`, `repair`, `guard`, `fixed_tree` and `suites` straight through, so the emitter never checks that the reproduction ran, that the causal account is true, or that the suites exited as claimed; the study's own wording at line 238 is the narrower "the record establishes what the run recorded". Not raised, because the same section already says the draft holds "the seven fields the operator had to establish before the Promise could be claimed at all", the Promise's Evidence classes are `checked, inferred`, and the next sentence narrows the report counts explicitly. Second, study constraint 4's four-file Hexaemeron re-pin from `1.6.24` across `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`. The Mason's conclusion that bumping a manifest would redden `test_promise_machine_delivery_versions_are_exact_and_current` was checked against the test rather than accepted, and holds: `DELIVERY_PACKAGE_VERSIONS["hexaemeron"]` is the literal `"1.6.24"` at `tests/test_version_propagation.py:47`, and `test_the_three_manifests_agree` and `test_hexaemeron_version_reaches_both_marketplaces` would redden beside it, so the re-pin needs those four files and that dict in one commit. Not raised as a finding, because no runbook step's Exit assigns the re-pin, step 2's Exit names only `SKILL.md` and `EVOLUTION.md`, and closing it needs a `hexctl amend runbook` the Warden must not run. Third, the section names `docs/elenchus-fixed-and-guarded-record/study.md`, a path that does not resolve inside the portable runtime package. Not raised, because it is inline code rather than a markdown link and `tests/test_skills_sh_package.py:203` parses links alone, and because the `elenchus-v1.4.0` ledger row links the same document by a relative path that does resolve. Fourth, `result_findings`' docstring at `fixed_and_guarded.py:499` says the result is "read for four values only" while five keys are read, `ref`, `status`, `tests`, `detail` and `report`, with `exit_code` and `output` accepted and never read; the new section's "five of its keys are read" is the accurate statement and the docstring is the stale one. Not raised, because repairing it edits `fixed_and_guarded.py`, which this step's exit requires identical to the branch point.
## Step 2, round 2 -- 2026-09-06T03:06:26Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=reviewed; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity and the `security_suite` receipt records that waiver; the emitter's runtime behaviour, not re-exercised on a scratch repository, because this round adds no executable behaviour and `elenchus.py` blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2`, `fixed_and_guarded.py` blob `b830c8ad825bc348d45f0ce55a813250eec1273e` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` are each identical at the branch point `c7d1900a`, at `9adb7111` and in the worktree; step 1's ten open findings (S1-R1-01, S1-R1-05, S1-R2-03, S1-R2-04, S1-R3-02, S1-R3-03, S1-R4-02, S1-R4-03, S1-R5-02, S1-R6-02), which step 1's closed audit owns; the study body, controller-owned and byte-pinned; `guard.test`, still checked against nothing, the standing lead round 6 recorded; step 3's demonstration path, which the last step owns; whether run 1276 merges before or after this run, which neither run controls and which decides whether S2-R2-02 does harm; whether the run integrates onto `origin/main`, which had advanced to `c5c5f36217d3ab812a547175266a80dd4d945a78` by this round and which the controller owns at integration, so no rebase or rewrite of the signed stack was made here; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict stays the declared runner result and not an attestation of its bytes<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R2-01 | low | `docs/elenchus-fixed-and-guarded-record/runbook.md` | Step 2's Exit and Files, as replaced by the two 2026-09-06 amendments, require this file byte-identical to the controller artefact `.hexaemeron/runbook.md`. At `9adb7111` the committed copy hashed `801e3b30b6464bbd0944f4f76e022c49be0bbb3c01b4b5ceace8fab547481410` against the artefact's `0b000093f7fe86758e79d8aa8d33e8c2239a635f358b39523be4cc6ad504da60`, twenty lines short: both amendments were absent, so the shipped run document omitted the re-pin assignment and the sixth-literal correction that between them produced this round's whole diff. Nothing mechanical reports it. No test compares the two paths, and `python3 scripts/run_checks.py --base origin/main` was green across all ten selected checks with the stale copy in place. `docs/elenchus-fixed-and-guarded-record/study.md` was checked the same way and was already identical at `70e44498b74ec3d430f32d918d5f11230aadca5f1c76a505e76aaef00e024291`. | fixed in `ffa9b9da6a61e72f67bb208f2d3f0d4e126ebd46`; `.hexaemeron/runbook.md` copied over it with no byte rewritten, `cmp -s` reports identical, and the committed file now hashes `0b000093f7fe86758e79d8aa8d33e8c2239a635f358b39523be4cc6ad504da60` |<br>| S2-R2-02 | medium | `plugins/hexaemeron/.claude-plugin/plugin.json:4` | The re-pin at `5ee9bf8f` claims `1.6.25`, and a concurrent run claims the same number. `origin/main` at `c5c5f36217d3ab812a547175266a80dd4d945a78` and every `refs/remotes/origin/` head carry `1.6.24`; the only other ref claiming `1.6.25` is run 1276 at `ec3c11772505f101144cd22bbd8ae6c101f12369`, on local `fiat/1276-declare-a-held-job-s-required-inputs-in-the-step-4-instruct-the-ranking-advance-the` and its `--audit` sibling `02e37855`, which moves the same six places from `1.6.24` to `1.6.25` behind a kronos skill change. Both runs branch from `5bc2494c`. `git merge-tree --write-tree 9adb7111 ec3c1177` writes tree `aa40119751543257bfbe224d9f6b1319d327c471` and conflicts on `.horos/boundary.json` alone: all six version-bearing files auto-merge, because both sides make the identical `1.6.24` to `1.6.25` edit, and the merged tree carries both skill payloads under one package version. `.github/workflows/sync-skills-marketplace.yml` mirrors every branch and tag to `wildcat-finance/skills-marketplace` on a five-minute schedule, so whichever run merges second ships a changed skill file under a version an installed copy has already been offered, which is the outcome study constraint 4 at `docs/elenchus-fixed-and-guarded-record/study.md:300` and this step's amended Exit exist to prevent. No test compares the number against another branch and the merge raises no conflict, so nothing in the repository reports it. Run 1276 stands at step 4 against this run's step 2. | open; the amended Exit names `1.6.25` verbatim, so moving the number needs a `hexctl amend runbook` the Warden must not run, and amending the study instead would revert every step's amended field to baseline. The controller owns the choice between rebinding this step to a higher number and re-picking at integration |<br><br>Leads not pursued: The mechanical part ran clean on the fixed tree: phylax, ephoros and hypomnema each exit 0 over this step's changed paths, `python3 -m unittest discover -s tests` ran 1337 tests at exit 0, `python3 plugins/hexaemeron/tests/run_tests.py` passed 2397 of 2397 at exit 0, and `python3 scripts/run_checks.py --base origin/main` was green across all ten selected checks. Round 1's own fix was re-verified and holds: `37fd18a039e1d1f22d78682add8f1eed03da09ce` changed one sentence at `plugins/hexaemeron/skills/elenchus/SKILL.md:392` and no other line, and its four families now account for all seven codes outside section 11's eleven, F000, F005 and F006 for an input that will not read as one bounded closed object, F007 for a guard absent from the changed test files the comparison used, F008 and F010 for a parent that cannot be soundly derived from the result's own `ref`, and F009 for a destination the emitter will not write to, against the `F000` to `F017` docstring at `fixed_and_guarded.py:13-31` and the raise sites at `:484`, `:506`, `:593`, `:680`, `:687`, `:698`, `:706`, `:713`, `:731` and `:743`. Four questions were then checked and returned negative, and three leads carry forward. First, the re-pin obliges no ledger row. `plugins/hexaemeron/skills/VERSIONING.md` states that the contract governs a skill and not a plugin, and `tests/test_version_propagation.py`'s module docstring says a plugin's version is not its skills' version and that requiring the two to track each other would invent a rule the contract denies, citing Horos at plugin `0.1.0` carrying `horos-v9.2.3`. `plugins.hexaemeron.tests.test_evolution` ran 11 tests at exit 0, and the `elenchus-v1.4.0` frontier retention still recomputes: SHA-256 over `mature|observed-failure-root-cause|<current frontier>|None -- mature` plus a final newline is `08e77bae576b3351d6f38e60ce9da88327014bcaa7459e319b8e51d79caeda8b`, byte-identical to `elenchus-v1.3.0`'s. Second, no regenerated runtime package tracks the version here. `.gitignore:73` excludes `.agents/skills/promise-machine/runtime/` because `wildcat-finance/skills-runtime` publishes the package under ADR-066, `git ls-files .agents/skills/promise-machine` returns only `SKILL.md`, `PORTABLE.md` and `scripts/verify_runtime.py`, and no `MANIFEST.json` is tracked; `.agents/plugins/marketplace.json` sits in `PACKAGE_AUTHORED` at `scripts/portable_promise_machine.py:30`, so the bump reaches that manifest when the package is generated, with nothing to commit here. Third, no manifest in the tree binds a moved file. `tests/promise_machine_coverage.json`'s `elenchus-fixed-and-guarded` runtime entry binds `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py` at `9069e2a4266bf349c963aaad6e22cf2966d14bb34d05d025b72c9e77017d39c5`, which still matches the file; unlike the `ephoros-observability-review` entry it binds no `SKILL.md`, and it names none of the six re-pinned paths. `.horos/boundary.json` is untouched: `counts.files_walked` is 2582 over 134 classified entries, and this round adds and removes no tracked file and moves no classification. Fourth, study constraint 4's four-file wording is accurate and owes no `hexctl amend study`. The four it names state the version as fact and are what an installed copy reads; `tests/test_version_propagation.py:47` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py:5679` are pinned expectations asserting those four agree, which the module docstring frames as walking the layers a plugin declares. The file-level obligation belongs to the runbook, whose third amendment names all six, and amending the study would revert every step's amended field to baseline and un-bind the Exit this step was built under. Three of round 1's four leads stay open and were not re-raised: the section's claim that an emitted record establishes what the Promise says and no more; the inline `docs/elenchus-fixed-and-guarded-record/study.md` path that does not resolve inside the portable runtime package, which `tests/test_skills_sh_package.py:203` does not read because it parses links alone; and the stale `result_findings` docstring at `fixed_and_guarded.py:499`, still saying the result is read for four values only while five keys are read, which cannot be repaired without editing a file this step's exit requires identical to `c7d1900a`. Round 1's fourth lead, study constraint 4's unassigned re-pin, is closed by the two amendments and by `5ee9bf8f` and `9adb7111`. The packet's `elenchus.unittest.v1` report format against the `unittest-json-v1` value `elenchus.py:29` accepts was read as one format and not re-raised, because `elenchus.py:124` requires that parser's report to carry `schema` `elenchus.unittest.v1`, exactly as round 1 recorded.
## Step 2, round 3 -- 2026-09-06T03:45:38Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=reviewed; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity and the `security_suite` receipt records that waiver; the emitter's runtime behaviour, not re-exercised on a scratch repository, because this round adds no executable behaviour and `elenchus.py` blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2`, `fixed_and_guarded.py` blob `b830c8ad825bc348d45f0ce55a813250eec1273e` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` are each identical at the branch point `c7d1900a`, at this round's head and in the worktree; step 1's ten open findings (S1-R1-01, S1-R1-05, S1-R2-03, S1-R2-04, S1-R3-02, S1-R3-03, S1-R4-02, S1-R4-03, S1-R5-02, S1-R6-02), which step 1's closed audit owns; the study body, controller-owned and byte-pinned; `guard.test`, still checked against nothing, the standing lead round 6 recorded; step 3's demonstration path, which the last step owns; any hexaemeron version claimed only in a clone outside this repository, since `1.6.25` on run 1276 is visible here only because both runs share this repository's ref namespace, and a branch held in another checkout would not appear in the 563 refs enumerated; whether run 1276 merges before or after this run, which neither run controls; whether the run integrates onto `origin/main`, which advanced from `c5c5f362` to `66f43a0e` during this round when the enumeration required a fetch, and which the controller owns at integration, so no rebase or rewrite of the signed stack was made here; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict stays the declared runner result and not an attestation of its bytes<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R3-01 | low | `docs/elenchus-fixed-and-guarded-record/runbook.md` | Step 2's Exit requires this file byte-identical to the controller artefact `.hexaemeron/runbook.md`. The fourth amendment landed after round 2 committed its refresh at `ffa9b9da`, so the shipped copy was stale again: `0b000093f7fe86758e79d8aa8d33e8c2239a635f358b39523be4cc6ad504da60` against the artefact's `690a53d7e9132881a03900c2cacbf0c7ceb21323cadf683642ecf7d47eb04cdc`, 271 lines against 281 and 2,611 bytes short, with the entire checked-property amendment that produced this round absent from the document the run ships. Nothing mechanical reports it: `python3 scripts/run_checks.py --base c5c5f362` was green across all ten selected checks with the stale copy in place. `docs/elenchus-fixed-and-guarded-record/study.md` was checked the same way and was already identical at `70e44498b74ec3d430f32d918d5f11230aadca5f1c76a505e76aaef00e024291`. | fixed in `ad4a8c069013d18674bd1b7d964edd4bdcce8c97`; `.hexaemeron/runbook.md` copied across with no byte rewritten, a pure ten-line addition, `cmp -s` reports identical for both run documents, and the committed file now hashes `690a53d7e9132881a03900c2cacbf0c7ceb21323cadf683642ecf7d47eb04cdc` |<br>| S2-R3-02 | medium | `.hexaemeron/runbook.md` step 2 Exit | The 2026-09-06 amendment that answered S2-R2-02 replaces the frozen `1.6.25` with a checked property: the number "must exceed every hexaemeron version claimed by any local or remote ref, it is re-checked immediately before the run's integration push, and a collision found by that check moves all six places to the next free number without a further amendment". Three defects. First, no actor performs that re-check. Fiat's only integration-time version machinery is `hexctl done resolve-versions` under the `fiat-version-resolution` Promise, which needs a `version-relations` fenced block in the runbook before Step 1 (`plugins/hexaemeron/skills/fiat/scripts/hexctl.py:3068`); this runbook carries one fenced block, `design-lock` at lines 18 to 22, `receipts.runbook` in `.hexaemeron/state.json` holds no `version_relations` key, and `hexctl.py:16961` names such a run literal-only. `fiat-integration` at `plugins/hexaemeron/skills/fiat/SKILL.md:949` refuses only "a missing or stale declared version resolution", so an undeclared one is never required, and the relation it would apply, `next-generation-after-integration-base` at `hexctl.py:456`, compares against the integration base alone, which carries `1.6.24`, so it would not have reported run 1276's `1.6.25` either. Nothing will run the check the Exit records. Second, "the next free number" contradicts the exceed-every-ref rule in the same sentence: seven patch numbers inside the claimed `1.6.x` range are unclaimed by any of the 563 refs, 2, 4, 6, 7, 9, 15 and 21, and each is below `origin/main`'s published `1.6.24`, so the literal reading selects a version regression on the five-minute marketplace mirror, a worse outcome than the collision the amendment exists to prevent. The intended reading, the next free number above the maximum, is available and obvious, which is why this half alone would be low. Third, the check reserves nothing: it is a read with no lock, so two runs performing it concurrently observe the same maximum and select the same successor, reproducing S2-R2-02 one number higher. `fiat-version-resolution`'s Boundary states that its receipt "does not reserve a label", so the framework does not claim otherwise; this wording does not acknowledge it. No test reports any of the three. | open; repair needs `hexctl amend runbook`, which the Warden must not run, and amending the study instead would revert every step's amended field to baseline. The controller owns the choice between naming the actor and the above-the-maximum rule in a further amendment, and accepting the clause as guidance the integration step reads by hand |<br>| S2-R3-03 | low | `docs/elenchus-fixed-and-guarded-record/runbook.md` | No check anywhere binds a run's shipped run documents to the controller artefacts they copy, so every runbook amendment silently breaks its step's byte-identity exit and only a Warden's manual comparison catches it. `.hexaemeron/runbook.md` appears in the repository's own code at `plugins/hexaemeron/tests/test_hexctl_checkpoint.py:85` and `:904` alone, both about checkpoint archiving; nothing under `scripts`, `tests` or `plugins/hexaemeron/tests` names `docs/elenchus-fixed-and-guarded-record` at all, and `tests/check-map-v1.json` declares no check over the pairing. The recurrence is evidenced, not predicted: this run refreshed the same file at `ffa9b9da` and again at `ad4a8c069013d18674bd1b7d964edd4bdcce8c97`, and run 857 refreshed `docs/commit-gate/runbook.md` at `033a1fe90a9e2153e62cdd0cf5193ea5320a51b4` and `3d6b443ec46ba34def78c0aa70f87efbf672c39d`, each a ten-line addition, both now on `origin/main`. S2-R2-01 and S2-R3-01 are two instances of the gap rather than two defects. | open; closing it adds a check to `tests/check-map-v1.json` or a test module, both outside this step's byte-pinned Files list, and adding a tracked file would move `.horos/boundary.json` as well. A carryover row or a filed issue is the disposition available to the controller |<br><br>Leads not pursued: The mechanical part ran clean on the fixed tree: phylax, ephoros and hypomnema each exit 0 over this step's changed paths given as separate arguments, `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, `python3 plugins/hexaemeron/tests/run_tests.py` passed 2,397 of 2,397 at exit 0, `python3 -m unittest plugins.hexaemeron.tests.test_evolution` ran 11 tests at exit 0, and `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/hexaemeron/skills/elenchus/SKILL.md` exits 0 with cadence signals only. `python3 scripts/run_checks.py` was green across all ten selected checks against `c5c5f362` and refused against `66f43a0e` with `unknown-ownership: changed paths have no declared owner` naming `.githooks/README.md`, `.githooks/greenlight` and `.githooks/pre-commit`. Not raised as a finding: those three paths exist on the advanced base and not on this stack, `scripts/run_checks.py:639` diffs two-dot as `{resolved}..HEAD` so a base-only path reads as changed, run 857's `.githooks/` tree merged into `main` between `c5c5f362` and `66f43a0e`, and the run's own pinned base in `.hexaemeron/state.json` is the immutable `0fefcc986107ed66ff43c6572b7aa1c7351f12f4`, which the fetch did not disturb. Naming `origin/main` in a step's exit proof is the framework's convention and `fiat-integration` already demands revalidation when the base advanced, so the observation belongs to the controller at integration rather than to this step. Round 2's own fix was re-verified and holds: `ffa9b9da` wrote blob `baad53630cb3a57ac36deabf35df7a3863cc9689` and that blob is unchanged at this round's head and in the worktree, so nothing reverted it; what re-broke the obligation is the artefact moving under it, which is S2-R3-01. The collision claim was re-checked independently across all 563 refs and all six version-bearing places, reading each ref's own bytes: `1.6.26` is claimed by exactly one ref, this run's stacked audit branch, `1.6.25` by exactly two, run 1276's `fiat/1276-declare-a-held-job-s-required-inputs-in-the-step-4-instruct-the-ranking-advance-the` at `ec3c1177` and its `--audit` sibling now at `bfca7589`, and `1.6.24` by 42 refs including `origin/main` and `refs/heads/main`. Run 1276 has not moved off `1.6.25` and no third run has taken `1.6.26`, so the number `1d41f96f` chose is free and the move at that commit is complete and internally consistent: all six places read `1.6.26`, and `git grep` for `1.6.24` or `1.6.25` in tracked source outside the audit record returns only `docs/elenchus-fixed-and-guarded-record/runbook.md:255` and `:267`, historical amendment prose byte-pinned to the artefact, and `docs/elenchus-fixed-and-guarded-record/study.md:22` and `:300`, which name `1.6.24` correctly as the version the re-pin moves from. One ref disagrees with itself across the four manifests, `refs/remotes/origin/chore/brevity-compress` at `1.0.0` against `1.0.0+codex.20260816145806`; not raised, because it predates this run and touches no file this step ships. The frontier retention recomputes: SHA-256 over `mature`, `observed-failure-root-cause`, the current-frontier sentence and `None -- mature` joined by pipes with a final newline is `08e77bae576b3351d6f38e60ce9da88327014bcaa7459e319b8e51d79caeda8b`, byte-identical to the `elenchus-v1.3.0` row's, and `.horos/boundary.json` still reports `counts.files_walked` 2582 because this round adds and removes no tracked file. The section's own claims were re-read against the code for the register ids the lints cannot see: `compose` at `fixed_and_guarded.py:651` writes `verdict` as the result's `status` and `detail` untranslated, `:591` requires the output to be a free symlink-free relative worktree descendant behind `F009`, the closed nine-field key set is at `:69`, and `MAX_TEXT_BYTES` 4096, `MAX_PATH_BYTES` 512 and `MAX_INPUT_BYTES` 1048576 back `F003`. Three of round 1's leads stay open and were not re-raised: the section's claim that an emitted record establishes what the Promise says and no more; the inline `docs/elenchus-fixed-and-guarded-record/study.md` path that does not resolve inside the portable runtime package, which `tests/test_skills_sh_package.py:203` does not read because it parses links alone; and the stale `result_findings` docstring at `fixed_and_guarded.py:499`, still saying the result is read for four values only while five keys are read, which cannot be repaired without editing a file this step's exit requires identical to `c7d1900a`.
## Step 3, round 1 -- 2026-09-06T05:47:02Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-overclaims-verdict=reviewed; partial-record-write=reviewed; output-path-escape=reviewed; draft-input-untrusted=reviewed; secret-in-reproduction-output=reviewed; mechanism-recurrence-claim=reviewed; frontier-drift=reviewed; checker-interface-drift=reviewed; unbounded-record-size=reviewed; guard-name-unbound=reviewed<br><br>Not checked: the waived Pashov suite (x-ray, solidity-auditor, fizz), because the step ships no Solidity and the `security_suite` receipt records that waiver; step 1's ten open findings (S1-R1-01, S1-R1-05, S1-R2-03, S1-R2-04, S1-R3-02, S1-R3-03, S1-R4-02, S1-R4-03, S1-R5-02, S1-R6-02) and step 2's three (S2-R2-02, S2-R3-02, S2-R3-03), which those closed audits own; the study and runbook bodies, controller-owned and byte-pinned, so the study's own open S1-R1-01 and S1-R1-05 sentences were read and not repaired; the demonstration's own commit ids `20ded6d7b3416abc5d822509f22ad4fc49e55c47` and `08ad9798ae5e5df3bb34094c9b6bca5367e25870`, which no re-run can reproduce because a git commit id binds its time and author, so the re-run below binds the demonstration by its transcript, digest and byte count instead; the staged-rename durability `partial-record-write` names, read at `fixed_and_guarded.py:659` and not exercised by killing an emit mid-write, and the destination directory is never fsynced; whether any concurrent run takes `1.6.26` after this round, since the 565 refs were enumerated once; whether the run integrates onto `origin/main`, which stands at `66f43a0e07865daf526036ad1efbf98e3e27deac` carrying a `.githooks/` tree this stack does not own, and which the controller owns at integration, so no rebase or rewrite of the signed stack was made here; hosted CI, the controller receipt, the push and the publication; the report-byte binding issue 453 owns, so this round's verdict stays the declared runner result and not an attestation of its bytes<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | low | `docs/elenchus-fixed-and-guarded-record/demonstration.md:219` | The boundary section credits the emit path with refusing "a guard absent from the changed test files the comparison used". The field either path reads is `guard.file`: F007 at `fixed_and_guarded.py:696` tests `draft["guard"]["file"]` against `result["tests"]` and reports on `guard.file`, and F016 at `:412` tests `record["guard"]["file"]` against `record["repair"]["files"]`. Its sibling `guard.test`, which names the regression test the Promise turns on, is read by no rule on either path: `_guard_findings` at `:340` checks it for shape alone, `_text` within `MAX_PATH_BYTES` and `TEST_NAME.fullmatch`, while its own F002 message says it "must name the regression test inside that file". Demonstrated on the scratch repository built for the re-run below: a draft whose `guard.test` was `NoSuchClass.test_this_test_does_not_exist_anywhere` emitted at exit 0 and the emitted record carried that name, and `--check` on it printed `clean` at exit 0. This is the one sentence in the document whose job is to bound what the emit path adds, and the step's Exit requires the demonstration to say what the record does not establish. It is low because no number is misstated and the code side is step 1's standing `guard.test` lead against a file this step's exit freezes. | fixed in `f760622dc32af1b2ffa4ea567220ee803d062bec`; the four-item list now names `guard.file`, one added sentence states that `guard.test` is checked for shape alone and that a draft naming a test that exists in no file is written at exit 0 and read back `clean`, no other line changed, `fixed_and_guarded.py` stays byte-identical to `c7d1900a`, and phylax, ephoros, hypomnema and imprimatur are each clean on the edited file with imprimatur at 100.0 and 0 defects |<br><br>Leads not pursued: The demonstration was re-run rather than read. A scratch repository was built from the same five base files at `/private/tmp/fixed-and-guarded-demo-TcDTVH`, base `0b7c4ebf64027179a9d0c4b136206c674653b6ab` and repair `32e877323b68dd8a9902db20dec31cd0fa277ff0`, and every command in the document was executed against it. The document's entry state `a9eea7a9844dc2f969f92c64723d55fd436e4738` was confirmed as step 2's head. Section 1 exited 1 and printed 738 bytes carrying `AssertionError: ValueError not raised`, the document's own figure. The strongest available check on whether the document records a real run is that its quoted 738-byte block at lines 57 to 71 hashes to `40513c5ad0355ea25ee140001fc7506e531a2da136c03b89c347e025d1cd65d9`, exactly the digest it claims two lines below, and my own output differs from that block only in the six-character scratch suffix and the elapsed-time string `0.001s` against `0.000s`; normalising those two makes the files identical and reproduces the same digest. Section 2's `git diff-tree --no-commit-id --name-only -r` named `src/widget.py` and `tests/test_widget.py`. Section 3 exited 0 twice and `runner.py` wrote a report byte-identical to the JSON quoted at line 106. Section 4 exited 0 with `status` `guarded`, `tests` `["tests/test_widget.py"]`, `detail` `the runner report records a parent assertion failure`, `report` 1 executed, 1 assertion failure, 0 errors, 0 skipped, and `exit_code` 1, matching every value quoted at lines 127 to 138. Section 5's emit exited 0 printing the absolute destination and `written`, `--check` exited 0 printing the relative path and `clean`, the record held `schema` and the nine evidence fields, `unfixed_parent.commit` was the base commit the emitter re-derived, `unfixed_parent.report` equalled the result's report and `verdict` equalled the result's `status` and `detail` untranslated, `fixed_tree.report` read 1 executed and 0 failures as line 109 says, and no `AssertionError` byte appeared in the written file. Section 6's three refusals each exited 1 with zero bytes on stdout, and their three stderr lines are byte-identical to the document's lines 184 to 186, both files hashing `6651000418e92c97aeef4ad2ab371ca85808adfcbb36e7845641494b4a4a72f9`. Six further document claims were checked and hold: F001 names `schema` and not the missing key for each of the nine fields removed in turn, all nine exiting 1 with that one line; a record whose commits and digest were invented but mutually consistent read `clean` at exit 0 from a directory that is not a git repository, so the line about invented counts is exact; `classify` at `elenchus.py:231` reads only the report counters and never `exit_code`, so line 29's account of why the scratch repository owns its runner holds; the defect behaves as line 42 says, `Widget(width=-1)` returning an instance with width -1 and `area(2)` returning -2; the emitter derives the parent by `git rev-parse <ref>^{commit} <ref>^` at `fixed_and_guarded.py:577`, as lines 147 to 149 state; and `elenchus.py` stages its detached worktree under `tempfile.mkdtemp(prefix="elenchus-")` at `:296` and removes it at `:361` and `:365`, with no `elenchus-*` directory left behind by the re-run, so line 14's cleanup claim holds. `secret-in-reproduction-output` is the one register id the new case guards directly: it asserts `AssertionError` is absent from the written record beside a comment naming a credential in a stack trace as what the digest keeps out, and the re-run confirmed the same against a real 738-byte trace. The test was mutated rather than trusted. A repair leaving the failure red fails the case at the fixed-tree run with `AssertionError: ValueError not raised`, and a repair changing nothing fails it at the changed-file list with `['tests/test_widget.py'] != ['src/widget.py', 'tests/test_widget.py']`, which is exactly what the document's last two lines claim. A third mutation was probed and returned negative: a repair that adds the bound check after `self.width = width`, leaving the source ordering the causal account names, passes the case, because the guard binds the observable the Promise is about, that `ValueError` is raised, rather than the line order. Not raised, because that repair closes the observed failure and the demonstration's sentence describes the two mutations actually run rather than a class. The test depends on no run-specific constant: it computes `hashlib.sha256(observed).hexdigest()` and `len(observed)` from its own run rather than asserting 738 or `40513c5a`, reads `DEFECT_LINE` from the source instead of counting by hand, uses `sys.executable` with `shlex.quote`, and sets `commit.gpgsign` false both locally and per commit against this host's global `true`. Its `--out records/fixed-and-guarded.json` resolves against `--repo` and not the working directory, at `prepare_output_path` reading `repo.resolve(strict=True)`, so nothing is written into the repository under audit; the worktree was clean before and after. Two Mason observations were judged rather than accepted, and both dispositions were right. F001 naming `schema` and not the removed key is recorded at lines 191 to 195 with its consequence for a reader stated, and repairing it would edit `fixed_and_guarded.py`, which this step's exit freezes and which would need a runbook amendment the Warden must not run. The run-specific reproduction digest is recorded at lines 74 to 76 and the suite correctly does not depend on it. Drift and identity all hold: `elenchus.py` blob `97e485925e1d6ed76af419f4c5f1edf4c8fb26f2`, `fixed_and_guarded.py` blob `b830c8ad825bc348d45f0ce55a813250eec1273e` and `test_elenchus_checker.py` blob `6c3bcc46dc47e37076d684cd4fd74c14a62e0b49` are each identical at `c7d1900a`, at this round's head `a509feaf` and in the worktree; `cmp -s` reports both run documents identical to the controller artefacts, `docs/elenchus-fixed-and-guarded-record/study.md` at `70e44498b74ec3d430f32d918d5f11230aadca5f1c76a505e76aaef00e024291` and the runbook copy at `690a53d7e9132881a03900c2cacbf0c7ceb21323cadf683642ecf7d47eb04cdc`, so the recurrence S2-R3-03 names did not fire this step; the `elenchus-v1.4.0` frontier retention recomputes to `08e77bae576b3351d6f38e60ce9da88327014bcaa7459e319b8e51d79caeda8b` over `Frontier status`, `Frontier revision`, `Current frontier` and `Next Fiat job` joined by pipes with a final newline, byte-identical to `elenchus-v1.3.0`; the six-place re-pin reads `1.6.26` at `plugins/hexaemeron/.claude-plugin/plugin.json:4`, `plugins/hexaemeron/.codex-plugin/plugin.json:3`, `.claude-plugin/marketplace.json:35`, `.agents/plugins/marketplace.json:21`, `tests/test_version_propagation.py:47` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py:5679`, and across 565 refs read one by one the only claimants of `1.6.26` are this run's own step 2 branch, its audit sibling, this step 3 branch and the pushed remote of step 2, so no other ref has taken it. The mechanical part ran clean: phylax, ephoros and hypomnema each exit 0 over this step's three changed paths given as separate arguments and again over the edited document, `python3 -m unittest discover -s tests` ran 1,337 tests at exit 0, `python3 plugins/hexaemeron/tests/run_tests.py` passed 2,398 of 2,398 at exit 0, `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_fixed_and_guarded` ran 55 tests at exit 0 against the module's 54 before this step, `tests.test_boundary_currency` ran 8 tests at exit 0 with `.horos/boundary.json` reporting `files_walked` 2583 over 134 classified entries, and `python3 scripts/run_checks.py --base c5c5f362` was green across all ten selected checks. The verdict is the declared runner result for a documentation-only fix: `elenchus.py` against `f760622dc32af1b2ffa4ea567220ee803d062bec` with test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1` whose parser requires schema `elenchus.unittest.v1`, and report file `.elenchus/fiat-1275-step-3.json` exited 0 reporting `unguarded`, `tests` empty and detail `the commit changed no test files`. Two observations carry forward rather than being raised here. `python3 scripts/run_checks.py --base origin/main`, the command step 3's own Exit names, exits 2 with `unknown-ownership: changed paths have no declared owner` naming `.githooks/README.md`, `.githooks/greenlight` and `.githooks/pre-commit`; those three exist on the advanced base and not on this stack, `scripts/run_checks.py:639` diffs two-dot so a base-only path reads as changed, and the run's own pinned base in `.hexaemeron/state.json` is the immutable `0fefcc986107ed66ff43c6572b7aa1c7351f12f4`, so this is the same base-advance S2-R3 recorded and belongs to the controller at integration. And `guard.test` remains bound to nothing in code, which S3-R1-01 documents but does not close; closing it edits `fixed_and_guarded.py` behind a runbook amendment. Three leads from step 2 stay open and were not re-raised: the SKILL.md section's claim that an emitted record establishes what the Promise says and no more; the inline `docs/elenchus-fixed-and-guarded-record/study.md` path that does not resolve inside the portable runtime package, which `tests/test_skills_sh_package.py:203` does not read because it parses links alone; and the stale `result_findings` docstring at `fixed_and_guarded.py:499`, still saying the result is read for four values only while five keys are read. One new lead was opened and left: nothing in the shipped skill tree points at `docs/elenchus-fixed-and-guarded-record/demonstration.md`, which is named only by the runbook's Exit and Files and by the test module's docstring at `:818`, while `SKILL.md` and the `elenchus-v1.4.0` ledger row point at the study instead. Not raised, because the step's Exit requires no pointer and its Files list is the two files it ships. One formatting liberty was checked and returned negative: the result JSON quoted at lines 127 to 138 compacts the `report` object onto three lines where `elenchus.py:403` prints `json.dumps(result, indent=2)` over six, and elides `output`. Not raised, because the document names the elision and its second sentence claims exactness for exit codes, digests, byte counts and refusal lines rather than for the whitespace of every quoted block, and every value in that block is exact.
