## Step 1, round 1 -- 2026-09-19T04:37:37Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable

Not checked: nothing under `plugins/tabularium/scripts/` builds or verifies a schema 3 release at this head, so the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests were reviewed as absent rather than exercised; those belong to later steps. The v2 read path was checked by validating the shipped documents, not by a rejection fixture. `rebuild.py` temporary directories are unchanged by this step and were not re-reviewed. The prose sweep over `README.md`, `SKILL.md`, the dictionaries, the adapter guide, the release policy and `AGENTS.md` is step 4 and none of those files changed here. The Pashov pair did not run under the recorded waiver: the step ships Python, JSON Schema and Markdown and no Solidity. `docs/tabularium-schema-v3/design-probe.py` was read and run but its 25 committed reports were compared by bytes, not recomputed. What was completed: the three lints exit 0 over the 42 changed paths, before and after the fix; the root suite ran 2,049 tests OK with exit 0 at both `edad99508b505abf21dc4a57e8466fd6e390777f` and `60eaf491f91acce4ab893949150ec58de1ef58a7`; `python3 scripts/run_checks.py --scope tabularium` is green at both heads, with the Tabularium suite at 160 tests before the fix and 161 after; the step's own exit commands each exit 0 at the step head, namely `plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` reporting `tabularium: 0.3.3 -> 0.3.4`, both Protasis runs, Imprimatur at `--max-defects 0`, and `design_evidence.py .hexaemeron/design-evidence.json --transition step:1`; the fix moved the Tabularium tree above the step commit, so `plugin_release.py --base edad99508b505abf21dc4a57e8466fd6e390777f --head HEAD` demanded a package above `0.3.4` and the four version surfaces went to `0.3.5` in the same signed fixes commit.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `plugins/tabularium/tests/test_schemas.py` | The tuple-drift guard built its table from the fixed list `ADAPTER_MODULES = (aave_v4, euler_v1, euler_v2)` rather than from the `release_v2.ADAPTERS` registry the study names as the way a value enters the admitted vocabulary. Demonstrated at `edad99508b505abf21dc4a57e8466fd6e390777f`: with a fourth adapter registered in `release_v2.ADAPTERS` and absent from both v3 documents, all 17 `test_schemas` tests stayed green, so the enums and `oneOf` branches were unchecked for it. The table now reads the registry, and one new test, `test_every_registered_adapter_reaches_the_tuple_table_and_the_v3_schemas`, fails on that case naming the schema file, the field and the value, for example `canonical-event-v3.json: properties/venue/enum disagrees with the adapter tuple table; missing from schema: ['morpho-v1']`. | fixed in 60eaf491f91acce4ab893949150ec58de1ef58a7 |
| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |
| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |

Leads not pursued: `provenance.source_kind` is `{"type": "string", "minLength": 1}` in `canonical-event-v3.json` although each adapter emits one constant, `consensus-log`, `ethereum-json-rpc-log` and `hosted-indexer-event`, so a v3 event may name a venue and a source kind from different adapters; the runbook pins five provenance fields per `oneOf` branch and `test_event_v3_keeps_the_v2_envelope_outside_the_tuple_fields` deliberately holds this field equal to v2, so it is design rather than drift. The corrected v2 documents remain a cross-product over the tuple columns, as they were before the step, so `canonical-event-v2.json` admits `euler-v1` beside `adapter_version` `2.0.0`; only v3 binds the tuple, and the runbook asks v2 to admit what the v2 Python validator admitted rather than to bind it. `coverage-manifest-v3.json` accepts an empty `versions.mapping_rules` because `items` is vacuous on an empty array; the branch stays pinned by `evidence_class`, `protocol_generation`, `source_api` and `versions.adapter`, so no cross-branch ambiguity follows. `.agents/plugins/marketplace.json` is named in the step's Files field and carries no `version` key for `tabularium`, so no bump was owed there. No test pins the twelve v0 release digests at this head; `git diff --stat 1d131b98 edad9950 -- plugins/tabularium/examples` is empty, which is the evidence this round has. Negative results worth keeping: `plugins/tabularium/tests/run_tests.py` is byte-identical to `plugins/alexandria/tests/run_tests.py` apart from the docstring, so the phylax boundary it was modelled on is unweakened, with a worktree-bound root and dev/ino recheck, an `O_DIRECTORY|O_NOFOLLOW` directory walk, an `O_EXCL` create at mode 0600 and identity-checked cleanup; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` are byte-identical to their `.hexaemeron/` counterparts and the 25 committed reports match, with `.hexaemeron/study.md` at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17`, `.hexaemeron/runbook.md` at `0b8a2aadc24d7f757e48494127096ef1ddf1d7527dd850dc5c7e4dd4f728f08b` and the design record at `512a0b64fd17b5e46768e250dca7c9ace5f6c69febf4fc800c326313187cb595`; under `jsonschema` 4.25.1 both v3 documents pass `check_schema` and the `oneOf` rejected every mixed tuple tried, namely `aave-v4` with `adapter_version` `1.0.0`, with `source_api` `euler-v3`, with `adapter` `euler-v2`, with `protocol_generation` `euler-v1`, with `mapping_rule` `euler-v2.repay.v1`, `venue` `euler-v1` over aave provenance, `schema_version` 2 and an extra top-level key, while the coverage document rejected `adapter.version` `1.0.0`, `adapter.name` `euler-v2`, a swapped `evidence_class`, `source_api` or `protocol_generation`, a euler-v2 rule, `versions.event_schema` 2 and a three-item `known_gaps`; the corrected v2 documents admit every shipped row, 500 of 500 aave-v4 rows, 1 euler-v1 row, 2 euler-v2 rows and all three `coverage.json` manifests, where at `1d131b98` all 500 aave rows and the aave manifest were invalid and the euler rows were already valid; with `jsonschema` blocked by an import stub the Tabularium suite still ran 161 tests with 0 skips and 0 failures, so no parity test passes by skipping; and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/tabularium-schema-v3/study.md --design-evidence docs/tabularium-schema-v3/design-evidence.json --repo-root .` exits 0, binding the declared decision `superseding-releases` to `plugins/tabularium/skills/tabularium/EVOLUTION.md`.

## Step 1, round 2 -- 2026-09-19T04:54:09Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=not-applicable; path-confinement=reviewed; prose-drift=not-applicable

Not checked: the round-1 fix changed no production code, so nothing round 1 deferred became reachable: `git diff --name-only edad9950 e448a1e3 -- plugins/tabularium/schemas plugins/tabularium/scripts` is empty, and the v1 directory build, its `write_bytes_atomic` use, the cross-file refusal of a manifest at 3 beside a row at 2, the three rejection fixtures, the v1 `source.json` and `capture.json` reuse, the `legacy-v0-verify` report and a test pinning the twelve v0 digests are still absent rather than exercised. No Elenchus run was made this round because the round produced no fixes commit, so the verdict is null rather than the `inconclusive` S1-R1-03 records for a fix on this step. The subprocess census behind S1-R1-03 was read by grep over `plugins/tabularium/tests/*.py`, not by running the declared runner against each module. `plugins/tabularium/tests/run_tests.py` did not change between `edad9950` and `e448a1e3`, so its boundary was re-confirmed by that absence rather than re-read line by line. The 25 committed selection reports were compared by bytes, not recomputed, for the reason S1-R1-02 records. The Pashov pair did not run under the recorded waiver: no Solidity. The prose sweep remains step 4 and none of its files changed. What was completed: the three lints exit 0 over the nine paths changed between `edad9950` and `e448a1e3`; the root suite ran 2,049 tests OK with exit 0 at `e448a1e3406e9835ace53dab507b32d0bc0523b1`; `python3 scripts/run_checks.py --scope tabularium` is green with the Tabularium suite at 161 tests; `plugin_release.py` exits 0 against both `edad99508b505abf21dc4a57e8466fd6e390777f` (`tabularium: 0.3.4 -> 0.3.5`) and `1d131b98a78c888b571f302e5b4c899aa2e47caf` (`0.3.3 -> 0.3.5`); both Protasis runs, Imprimatur at `--max-defects 0`, `design_evidence.py .hexaemeron/design-evidence.json --transition step:1` and the Hypomnema study-mode bridge each exit 0; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree left the worktree clean, with `tests.test_demonstrations` passing 114 tests including `HorosCensusCurrencyTests`.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-02 | low | `docs/tabularium-schema-v3/design-probe.py` | The committed probe cannot run from its committed path: `ROOT = Path(__file__).resolve().parents[1]` resolves to `docs/`, so `python3 docs/tabularium-schema-v3/design-probe.py --output-dir <dir>` exits 1 with `ModuleNotFoundError: No module named 'tabularium_lib'`. It is byte-identical to `.hexaemeron/design-probe.py`, which is not tracked, and the 25 committed reports name that untracked absolute path as their command, so no tracked artefact reproduces the selection stage. | accepted: the committed bytes are the preserved evidence that produced the 25 reports, and editing them would make the committed probe differ from the probe those reports record; the remedy is a provenance note in step 4 prose, not a change to preserved bytes |
| S1-R1-03 | low | `docs/tabularium-schema-v3/runbook.md` | The step's declared Elenchus runner, `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, discovers a suite whose `tests/test_scaffold.py` calls `subprocess.run`, and the Elenchus runner boundary permits no child process. This round's run returned `inconclusive` with 161 executed, 0 assertion failures, 15 errors and `PermissionError: [Errno 1] Operation not permitted`, so no audit fix on this step can reach `guarded` or `passed` through the declared runner. | open: the Tests field is runbook-pinned and only the controller may amend it; the counterfactual for S1-R1-01 was run by hand instead and is recorded in that row |

Leads not pursued: the S1-R1-01 fix binds `ADAPTER_MODULES` once at module import, while the new test reads `release_v2.ADAPTERS` live, so the two can diverge if an adapter is registered after the test module is imported. Both orders still catch the drift and neither is reachable in the tree, where `release_v2.ADAPTERS` is a module-level literal: registering a fourth adapter before the import fails 14 of 18 `test_schemas` tests, and registering it after the import fails exactly 1, the new test, with `the tuple table does not cover every adapter in release_v2.ADAPTERS` and the snapshot at 3 against a live registry of 4. Deriving the table inside `tuple_table()` would make the two orders identical; it was not pursued because no behaviour in the tree is wrong and the guard holds either way. S1-R1-03 names `tests/test_scaffold.py` as the subprocess caller; a grep of `plugins/tabularium/tests/*.py` shows four modules calling `subprocess`, also `test_builder_cli.py`, `test_release.py` and `test_euler_releases.py`, so the carried row understates the breadth without changing its finding, and the row is carried unedited. The round-1 leads still stand unchanged: free-string `provenance.source_kind` in `canonical-event-v3.json` against three per-adapter constants; the corrected v2 documents remaining a cross-product over the tuple columns; `coverage-manifest-v3.json` accepting an empty `versions.mapping_rules` with no cross-branch ambiguity; `.agents/plugins/marketplace.json` carrying no `version` key for `tabularium`; and no test pinning the twelve v0 digests. Negative results this round: `git diff --name-only 1d131b98 e448a1e3 -- plugins/tabularium/examples` returns zero files, so no `examples/*-v0/` byte moved across the step and its fix; the four version surfaces all read `0.3.5` and `tests.test_version_propagation` passes 8 tests; `docs/tabularium-schema-v3/study.md`, `runbook.md`, `design-evidence.json` and `design-probe.py` remain byte-identical to their `.hexaemeron/` counterparts and the 25 reports still match; under `jsonschema` 4.25.1 both v3 documents still pass `check_schema` and reject 8 of 8 mixed-tuple events and 8 of 8 mixed-tuple manifests; the corrected v2 documents still admit 503 of 503 shipped rows and all three `coverage.json` manifests; and with `jsonschema` blocked by an import stub the Tabularium suite still runs 161 tests with 0 skips and 0 failures.

## Step 2, round 1 -- 2026-09-19T07:19:54Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable

Not checked: no jsonschema document run was made against a built schema-3 ledger; the parity this round checked is the required-field lists of `canonical-event-v3.json` and `PROVENANCE_FIELDS` compared field by field, not `jsonschema` applied to the rows, which the register routes to the reporter work. `validate_event_row` still checks presence and tuple membership rather than the closed key set the documents enforce with `additionalProperties: false`, so an extra `provenance` key, a dropped top-level `id` and a dropped `native_record` are each refused only by `canonical bytes do not match an offline source rebuild`; all three were tried through the CLI and each exits 1. No size cap was added or asked for: `_artifact_bytes` reads each declared artefact whole and `_parse_jsonl` materialises every row before the row-count check, so a 400,000-row ledger against a manifest declaring 500 rows is refused at exit 1 only after 13.1 s and a full parse. Hypomnema was not run against changed documents because the step changed none: `git diff --name-only 9cc8a8ec ef9c5a8f` lists no Markdown, and over the whole worktree the lint exits 1 with four H008/H009 findings, all in `audit/rounds/fiat-1086-gate-study-and-runbook-links-before-their-d.md`, its synopsis, `audit/rounds/fiat-1276-declare-a-held-job-s-required-inputs-in-the.md` and its synopsis, which belong to other runs and are unchanged by this step. The Pashov pair did not run under the recorded waiver: no Solidity. prose-drift is step 4 and none of its files changed. The Elenchus verdict below is the declared classifier result, not an attestation of the report bytes; the counterfactual behind S2-R1-01 was run by hand for the reason S1-R1-03 records.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | `plugins/tabularium/scripts/tabularium_lib/release_v2.py` | `PROVENANCE_FIELDS` named six of the eleven `provenance` fields that `canonical-event-v2.json` and `canonical-event-v3.json` both require, so a row missing `source_kind`, `source_contract`, `source_entity`, `source_id` or `supporting_selectors` was refused only by `canonical bytes do not match an offline source rebuild` at the end of `_verify_release`, not by row and field. Each of the five was dropped from row 1 of a schema-3 aave-v4 release and verified through the CLI at exit 1 with that generic message. No release is admitted that should not be; what is lost is the named diagnostic this step exists to give and the schema-parity the register tracks. | fixed in 398d43550544285443a5227ceaafae7085aa9e08 |
| S2-R1-02 | low | `plugins/tabularium/scripts/tabularium_lib/verifier.py` | The comment the step added above the row loop said the rows are checked "before any byte comparison", while `_release_artifacts` already compares each artefact's SHA-256 and byte count against the manifest's declared claim in `_artifact_bytes` before the loop runs. The runbook's own requirement, that the validator run before the rebuild, does hold. `docs/tabularium-schema-v3/study.md:266` carries the same "before any byte comparison" wording, so the comment restated a controller-pinned artefact rather than inventing the claim; the study was not edited. | fixed in 398d43550544285443a5227ceaafae7085aa9e08 |

Leads not pursued: `validate_event_row(row, adapter_module, schema_version, index=1)` defaults the row number to 1, and the one caller in `_verify_release` passes the real index, so no refusal can misname a row today; a future caller that omits it would label every row 1. Not pursued because a default on a single-caller function is not worth a change on this step. The whole-file read in `_artifact_bytes` and the full parse in `_parse_jsonl` both predate this step and neither is named in its Files field, so the 400,000-row cost above is recorded rather than fixed. `docs/tabularium-schema-v3/study.md:120` still records `EVENT_SCHEMA_VERSION = 1` and `ADAPTER_VERSION = "1.0.0"` as present in `tabularium_lib/__init__.py`, which this step removed; the study is pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and describes the tree as it was found, so no edit was attempted. Negative results worth keeping. Removed constants: `EVENT_SCHEMA_VERSION`, `ADAPTER_VERSION` and `MANIFEST_SCHEMA_VERSION` have no reference in any `.py`, `.md` or `.json` in the worktree outside that one study sentence, and `docs/tabularium-schema-v3/design-probe.py`, the only module outside `plugins/tabularium/` that imports `release_v2`, uses `EVIDENCE_CLASSES` alone and touches no changed signature. v0 immutability: `git diff --name-only 9cc8a8ec ef9c5a8f -- plugins/tabularium/examples` lists exactly `aave-v4-v0/rebuild.py`, `euler-v1-v0/rebuild.py` and `euler-v2-v0/rebuild.py`, and all three rebuilds report their published digests `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7`, `4034622f8b34147dead8a87d7c16b2a7c7197ed6417809fec41716a8028552aa` and `f563baa00c737384a3901f1bb3a7ae977f68f52a813eae9d02071eb2f4d0a5fe` both at `ef9c5a8f` and after the fix. tuple-drift: registering a fourth adapter in `release_v2.ADAPTERS` before `test_schemas` imports turns 14 of its 18 tests red, including `test_every_registered_adapter_reaches_the_tuple_table_and_the_v3_schemas` at `4 != 3`, so the step-1 guard still holds over the rewritten `release_v2.py`. version-mixing: a manifest at 3 with row 250 at 2, a manifest at 2 with rows at 3, and `versions.event_schema` 2 under `schema_version` 3 are each refused at exit 1, naming `canonical row 250`, `canonical row 1` and both manifest numbers. Named refusals reaching stderr at exit 1 through the CLI: `venue`, `provenance.adapter`, `provenance.adapter_version`, `provenance.protocol_generation`, `provenance.source_api`, `provenance.mapping_rule`, a missing `provenance.mapping_rule` and a row that is not an object. path-confinement: a symlinked `events.jsonl`, a `../rel/events.jsonl` and an absolute `/etc/passwd` in `canonical.path` are refused with `canonical path uses a symlink` and `canonical path is not a safe relative path`. Adversarial input: a 200,000-deep JSON array, the bytes `\xff\xfe` and a duplicated `schema_version` key are each refused as a `TabulariumError` at exit 1 with no traceback, because `loads_json` already catches `RecursionError` and `UnicodeDecodeError`. legacy-write: a build with no flag wrote `schema_version` 3 in the manifest and in every row, the only `--event-schema 2` callers in the tree are the three rebuild demos, and `--event-schema 4` is refused by argparse against `choices` `[2, 3]`. partial-write: the two `write_bytes_atomic` calls in `builder.py` are unchanged and still follow `validate_manifest_v2`. skip-invisibility: the Elenchus report records 181 tests with 0 skipped, 0 expected failures and 0 unexpected successes. Gates: phylax and ephoros each exit 0 over the 22 paths changed between `9cc8a8ec` and `ef9c5a8f` and again over the three files the fix changed; the root suite ran 2,049 tests OK with exit 0 on the fixed tree; `python3 scripts/run_checks.py --scope tabularium --format json` reports outcome green over 4 checks; the Tabularium suite is 180 tests at `ef9c5a8f` and 181 after the fix; and `plugin_release.py --base ef9c5a8f --head 398d4355` reports `tabularium: 0.3.6 -> 0.3.7` at exit 0 after the four version surfaces were bumped in the fixes commit.

## Step 2, round 2 -- 2026-09-19T07:33:35Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable

Not checked: still no `jsonschema` document run against a built ledger, and the three rejection fixtures the register names do not exist in the tree: `plugins/tabularium/tests/fixtures/` holds `compound-debt-transfer.json`, `minimal-capture-manifest.json` and `minimal-snapshot.json` only. The closed key set both documents declare with `additionalProperties: false`, on the row and on `provenance`, is still unenforced in Python, so an extra `provenance` key, a dropped top-level `id` and a dropped `native_record` are refused only by `canonical bytes do not match an offline source rebuild`; all three were retried at this head, at schema 3 and schema 2, and each exits 1. That gap is decided below rather than carried. No size cap was added: a 400,000-row ledger against a manifest declaring 500 rows is still refused at exit 1 only after a full parse, measured at 7.6 s this round against 13.1 s in round 1, which is machine load rather than a behaviour change. The Pashov pair did not run under the recorded waiver: no Solidity. prose-drift is step 4 and the only Markdown this range changed is this run's own audit record and its synopsis. No new Elenchus run was made because this round produced no fixes commit, so the verdict is null rather than the `inconclusive` round 1 records for its fix; S1-R1-03's cause is unchanged, and `elenchus.py` was not re-invoked.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: two round-1 leads are now decided with evidence rather than carried. The `index=1` default on `validate_event_row` is unreachable at this head: `validate_event_row` has exactly three references in every `.py` file in the worktree, its definition at `release_v2.py:351`, the import at `verifier.py:12` and the one call at `verifier.py:114`, which passes the real `index`; no test calls it directly, so no refusal can misname a row and the default is not a finding. The closed key set is routed to the parity work rather than recorded as a step-2 finding: `canonical-event-v2.json` and `canonical-event-v3.json` both set `additionalProperties: false` on the row and on `provenance`, and `release_v2._exact` is the library's own closed-key helper applied to every manifest and capture object, so the tool exists and the row is the one untrusted object it is not applied to; against that, step 2's Tests field enumerates the refusals it owes and a closed key set is not among them, its Files field names no fixture directory, the three rejection fixtures the risk register pairs with this work are absent from the tree, and nothing is admitted that should not be, because the rebuild comparison refuses every one of these rows at exit 1. The decision is that the diagnostic belongs with the reporter the register routes it to, and a second behavioural change to the row validator in a regression round would add over-refusal risk for a message improvement. One new lead: `test_every_schema_required_provenance_field_is_refused_by_name` derives its list from `canonical-event-v3.json` alone, so if the v2 document's `provenance.required` ever diverged from the v3 document's the test would not see it; both require the same eleven fields today, and the same `PROVENANCE_FIELDS` tuple serves both schema versions, so nothing is wrong in the tree. Negative results worth keeping. Over-refusal, which is the failure this round existed to catch, did not occur: building and verifying a release end to end for each of the three adapters at each of schema 2 and schema 3 gives six pairs that all exit 0, the three schema-2 builds reproduce the published digests `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7`, `4034622f8b34147dead8a87d7c16b2a7c7197ed6417809fec41716a8028552aa` and `f563baa00c737384a3901f1bb3a7ae977f68f52a813eae9d02071eb2f4d0a5fe`, the three schema-3 builds give `81d416a10b70ab0f3d9a3bd41c0680e235b3b64f4f06cc36b4f4292c81136492`, `5b1016a9bc143f42e9bea46de71b3d1917bf6731d93b4c49f974df3660bc8595` and `f2b227058f53cd644c11359e911c8494924d6fef7da7072e8a33a4baf952d02a`, the three v0 `rebuild.py` demos each exit 0 on their published digest, and the three committed `coverage.json` files verify offline at schema 2 on those same digests. The widened tuple behaves identically on both schema versions: the five newly named fields are named on a schema-2 release exactly as on a schema-3 one. Every named refusal round 1 recorded still holds at this head, at exit 1 through the CLI: `venue`, `provenance.adapter`, `provenance.adapter_version`, `provenance.protocol_generation`, `provenance.source_api`, `provenance.mapping_rule`, a missing `provenance.mapping_rule`, a row that is not an object, `canonical row 250` at schema 2 under a manifest at 3, `canonical row 1` at schema 3 under a manifest at 2, `versions.event_schema` 2 under `schema_version` 3, a symlinked `events.jsonl`, a `../rel/events.jsonl`, an absolute `/etc/passwd`, a 200,000-deep JSON array, the bytes `\xff\xfe` and a duplicated `schema_version` key. The step-1 tuple-drift guard still holds over the twice-rewritten `release_v2.py`: registering a fourth adapter before `test_schemas` imports fails 14 of its 18 tests, the same count as at `ef9c5a8f`. The round-1 guard still fails on the parent six-field tuple at this head and passes on the fixed tree. Scope and gates: `git diff --name-only 9cc8a8ec 614de545 -- plugins/tabularium/examples` still lists exactly `aave-v4-v0/rebuild.py`, `euler-v1-v0/rebuild.py` and `euler-v2-v0/rebuild.py`, and `git diff --name-only ef9c5a8f 614de545 -- plugins/tabularium/examples` is empty, so the fix moved no example byte; `git diff --stat ef9c5a8f 614de545` is empty for `builder.py`, the three adapters and `tabularium.py`, which is the evidence for partial-write and legacy-write this round; phylax, ephoros and hypomnema each exit 0 over the eleven paths changed between `ef9c5a8f` and `614de545`, and hypomnema has a real target this round, this record and its synopsis, rather than the empty set round 1 reported; the root suite ran 2,049 tests OK with exit 0; `python3 scripts/run_checks.py --scope tabularium --format json` reports outcome green over 4 checks each at exit 0; the Tabularium suite is 181 tests with 0 skipped, 0 expected failures and 0 unexpected successes; `plugin_release.py` exits 0 against both bases, `tabularium: 0.3.6 -> 0.3.7` from `ef9c5a8f` and `0.3.5 -> 0.3.7` from `9cc8a8ec`, and all four surfaces read `0.3.7`; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree leaves the worktree clean.

## Step 3, round 1 -- 2026-09-19T09:50:18Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable

Not checked: no built schema-3 ledger was validated through `jsonschema` this round, because none is shipped until step 4; the parity checked here is the three committed v0 releases at `schema_version` 2 and the three rejection fixtures at 3. The Pashov pair did not run under the recorded waiver: no Solidity. `jsonschema` is imported by `support.py`, `prove_schema_v3.py` and `test_schemas.py` and by nothing under `plugins/tabularium/scripts/`, so the JSON Schema is a published contract the suite enforces and never a runtime gate; no shipping path exists where one validator admits a document the other refuses. Hypomnema had no Markdown target in the step diff: `git diff --name-only 50432db9 c3f298d4` lists twelve paths and none ends `.md`, so it was run over those same paths for its Python comment rules and again over this record and its synopsis. The step's own `--report` path was not rewritten: `.hexaemeron/` was read and never written, so the committed report at `.hexaemeron/reports/superseding-releases-rejection-parity.json` is still the one the Mason produced, and S3-R1-01's fix was checked against it by comparing the string `resolver_command` builds for that exact path rather than by overwriting it. The Elenchus verdict below is the declared classifier result, not an attestation of the report bytes. One first-pass `python3 scripts/run_checks.py --scope schemas --format json` returned exit 3 with outcome `unstable-source`, zero checks run: it was racing this round's own probes editing the worktree, so it is an artefact of the audit rather than a result, and the scope was rerun on a clean tree.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | `plugins/tabularium/tests/prove_schema_v3.py` | The report's `command` was built as `"python3 " + " ".join(sys.argv)`, so it named the host process rather than the reporter whenever `main(argv)` was called in process. `design_evidence.py:382` reads that field and the step's Exit pins it byte-for-byte to the design record's resolver, so it is evidence. Observed: with the guard applied to the parent reporter the field read `python3 plugins/tabularium/tests/run_tests.py` beside `"value": true`, a command that never computed that value; a host invoked as `python3 - unrelated-host-arg --host-flag=secret` put both arguments verbatim into the written report. It is now built from the parsed arguments and the script's repository-relative path, and the string it produces for the record's resolver is byte-identical to it. | fixed in d15425b04acad951e12203ebfeccee985731ddbc |
| S3-R1-02 | low | `plugins/tabularium/tests/support.py` | `SHIPPED_RELEASES` was defined and read by nothing in the worktree, while the Exit's claim over every shipped `events.jsonl` row and `coverage.json` rested on three hand-written cases naming their releases one at a time. A fourth canonical release would have been covered by nothing and failed nothing. The constant is now bound to the example directories carrying a `coverage.json` and to the case names; a fourth directory fails the new test by name, observed red against a planted `fake-v9-v0`. `examples/compound-v3-phase0-v0` carries no `coverage.json` and stays outside the set, which is correct: it is the Phase 0 witness, not a canonical release. | fixed in d15425b04acad951e12203ebfeccee985731ddbc |
| S3-R1-03 | low | `plugins/tabularium/scripts/tabularium_lib/release_v2.py` | The closed key set both schema documents declare with `additionalProperties: false`, on the row and on `provenance`, is still unenforced in Python: `validate_event_row` checks presence and tuple membership and enumerates no keys, so an extra `provenance` key, an extra top-level key and a dropped top-level `amounts` are each named by `jsonschema` alone. Measured at this head against a re-serialised `aave-v4-v0`, whose unmutated control the verifier accepts: all three mutations are refused at `canonical bytes do not match an offline source rebuild`, none is admitted. The divergence is in which layer refuses and whether the field is named, not in what the ledger admits, and `verifier.py:105` already states that `validate_event_row` is the naming layer in front of that comparison. Decided, not carried: not fixed here because step 3's Files field does not reach `release_v2.py` and a step does not silently widen; the Mason's `test_a_row_only_one_validator_refuses_fails_the_parity_check` holds the disagreement red, which is what the step's elenchus discipline asks of a parity mismatch. Fiat owns the routing: either a runbook amendment putting `release_v2.py` into a later step's Files, or a filed issue carried at integration. | accepted |
| S3-R1-04 | low | `.hexaemeron/study.md` | Study section 11 names the Elenchus fix command `python3 plugins/tabularium/tests/prove_schema_v3.py --case <finding-id> --report {report}` with format `unittest-json-v1`, while runbook line 26 names `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}` and states that the reporter is not an audit runner. The Mason built the runbook's interface and was right to: the reporter has no `--case` flag, it writes a `protasis-design-report/v1` object carrying no executed-test, failure, error or skip count, and `elenchus.py` classifies from exactly those counts, so the study's command could not return anything but `inconclusive`. This round used the runbook's runner. Recorded rather than repaired: the study is controller-pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and an `amend study` reverts every step's amended runbook field to baseline, so the correction is Fiat's to sequence, not a mid-round edit. | accepted |

Leads not pursued: the reporter's `--candidate` is a label, never a measurement. `rejection_parity()` reads `support.REJECTION_FIXTURES` and ignores `args.candidate`, so the same three observations are written under whatever candidate is asked for; running it as `--candidate totally-made-up` exits 0 and writes `"value": true`. Not pursued because `design_evidence.py:367` refuses a report whose identity does not match the cell it resolves, and the record's selection is locked to `superseding-releases`, so the three losing candidates' conformance cells are never resolved. The residue the identity check cannot see is a report resolved for a losing candidate's cell, whose label would match while the measurement came from this tree; that is Protasis's operator-trust model rather than a defect this step introduced. `prove_schema_v3.py` is unimportable in any process where a `tests` package is already bound from an earlier `sys.path` entry: its module-level `from tests import support` resolves to the repository-root `tests` package and raises `ImportError: cannot import name 'support' from 'tests'`, reproduced directly. No declared command reaches it, because `run_tests.py` discovers with `top_level_dir=plugins/tabularium` and the root suite never imports the reporter, so the import shape the suite depends on was left alone. `write_report` opens with `O_TRUNC` and writes without staging, so an interrupted write leaves a truncated report; not pursued because the result is invalid JSON that every reader refuses, and `partial-write` in the register names the build rather than the reporter. `_UNEXPECTED` matches `'<name>' ... was unexpected` and so misses the plural `were unexpected` jsonschema emits for two or more extra keys at once, which would drop the field name to its parent path; each committed fixture carries exactly one defect, so nothing in the tree reaches it. The design record binds `shipped-ledgers-validate-v3`, `legacy-v0-verify` and `suite-wall-time` to this same script, and its `--criterion` accepts only `rejection-parity` today, so each of those three exits 2 against `choices`; they block step:5 and integration, not step:3, so the reporter owes them later and not here. Negative results worth keeping. Path confinement: the reporter refuses a symlinked directory component and a symlinked final component at exit 2 naming the component, refuses a report directory that does not exist and never creates one, and refuses an unknown `--criterion` through argparse at exit 2. It does write to an absolute path outside the run worktree, through `..`, and over an existing regular file, all at exit 0; that is the discipline as written, which binds it to the path it is given rather than to a directory, and the path is operator argv rather than external data. Skip invisibility: `run_tests.py --elenchus-report` records 192 tests with 0 failures, 0 errors, 0 skipped, 0 expected failures and 0 unexpected successes, so this host skips nothing; shadowing the module with a `jsonschema.py` that raises `ImportError` makes the CLI print exactly `jsonschema is not installed` to stderr, exit 1, and leave no report. Parity, re-derived independently of the suite: `aave-v4-v0` 500 rows, `euler-v1-v0` 1 row and `euler-v2-v0` 2 rows all declare `schema_version` 2, all 503 rows and all three `coverage.json` files validate against `canonical-event-v2.json` and `coverage-manifest-v2.json` with zero errors, and `validate_event_row` refuses none of them. Each of the three fixtures is refused by both validators naming exactly one field and not a superset: `unknown-value` at `provenance.mapping_rule`, `wrong-version` at `schema_version`, `malformed-provenance` at `provenance.source_selector`, with `support.parity_observation` requiring `schema_fields == [expected_field]` rather than membership. The reporter cannot report a vacuous pass: `REJECTION_FIXTURES` is a three-element module constant, so the `all(...)` behind `"value": true` is never taken over an empty sequence, and the summary line records `"fixtures": 3`, though the report object itself carries no count because the `protasis-design-report/v1` key set is closed. Scope: `git diff --name-only 50432db9 c3f298d4 -- plugins/tabularium/examples` and the same range over `plugins/tabularium/scripts` are both empty, and the fix moves no byte under either, which is the evidence for v0-immutability, source-reuse, legacy-write and partial-write this round. The Mason's `test_the_reporter_refuses_a_symlinked_report_component` is kept rather than dropped: the step's phylax discipline names that refusal, the Tests field says the expected count is observed rather than predicted, and a control with a hostile-input test is established where one that merely exists is asserted. Gates: phylax, ephoros and hypomnema each exit 0 over the twelve paths changed between `50432db9` and `c3f298d4` and again over the two files the fix changed; the root suite ran 2,049 tests OK at exit 0 on the fixed tree; `run_checks.py --scope tabularium --format json` reports outcome green over 4 checks and `--scope schemas` green over 9, both at exit 0 on a clean tree; `design_evidence.py .hexaemeron/design-evidence.json --transition step:3` prints `clean` at exit 0; and `plugin_release.py --base c3f298d4 --head d15425b0` reports `tabularium: 0.3.8 -> 0.3.9` at exit 0 after the four version surfaces were bumped in the fixes commit. One guard of this round's own was caught before receipt rather than after: the first form of the S3-R1-01 test also asserted that no `sys.argv` entry appeared in the command, which the repository's Tabularium suite failed on `-s` from `unittest discover`, a substring of the scratch report path; the assertion was removed and the exact equality, which is the real guard, kept.

## Step 3, round 2 -- 2026-09-19T10:17:47Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=not-applicable; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable

Not checked: this round audits `c3f298d4..26a3db14`, which is round 1's own fixes and record, so version-mixing, legacy-write and source-reuse are marked not-applicable rather than reviewed: no builder, adapter, verifier, capture or example byte is in the range, and `git diff --name-only c3f298d4 26a3db14 -- plugins/tabularium/examples plugins/tabularium/scripts` is empty. No built schema-3 ledger was validated through `jsonschema`; none ships until step 4. The Pashov pair did not run under the recorded waiver: no Solidity. `.hexaemeron/` was read and never written, so the committed report is still the Mason's and the reporter's output was checked against scratch paths throughout. The Elenchus verdict below is the declared classifier result, not an attestation of the report bytes; its overlay applies both changed reporter and test files to the parent, so it is not the counterfactual for either finding, and both counterfactuals were run by hand. One premise in the round-2 brief does not hold and is corrected here rather than answered: S3-R1-02 did not remove a name from `support.py`. `git diff --name-only c3f298d4 26a3db14 -- plugins/tabularium/tests/support.py` is empty; the fix left the constant in place and added the test that reads it, so no test lost coverage and nothing else referenced it to begin with. That same question asked of the reporter's own evidence set is what S3-R2-02 below records.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | low | `plugins/tabularium/tests/prove_schema_v3.py` | `write_report` opened the report with `O_TRUNC`, emptying it before the first byte was written, so any failure after the open destroyed a valid report rather than leaving it alone. Observed against the round-1 reporter with `os.write` refusing `ENOSPC`: a 61-byte prior report became 0 bytes and no longer parsed as JSON. The risk register's `partial-write` entry names `write_bytes_atomic` as the convention for this class and the reporter was the one writer in the tree outside it. Introduced by the step rather than by round 1's fix, and carried as a round-1 lead; promoted here because the reporter is the file everything downstream trusts and the register already names the rule. It now stages under `O_EXCL` in the report's own directory, re-checks the final component and renames into place; the symlinked-directory, symlinked-final-component and missing-directory refusals are unchanged at exit 2 and no staged file survives a failure. | fixed in bc378e5f6f1224a1c478b93a19548ab6cd9d1bc8 |
| S3-R2-02 | medium | `plugins/tabularium/tests/support.py` | `REJECTION_FIXTURES` is the evidence set the reporter walks to compute the value it attests, and it had exactly two references in the worktree: its definition and `prove_schema_v3.py:114`. No test read it. Emptied, the Tabularium suite stayed green at 192 tests while the reporter exited 0 and wrote `"value": true` over no fixtures at all, because `all(())` is true and the closed `protasis-design-report/v1` key set has nowhere to record a count; the three rejection cases pass because they name their fixtures as literals rather than reading the tuple. This is the one shape that makes the report attest a pass with no evidence behind it. The set is now bound to the committed fixture files and to the case names, and an emptied tuple fails by name. | fixed in bc378e5f6f1224a1c478b93a19548ab6cd9d1bc8 |
| S3-R1-03 | low | `plugins/tabularium/scripts/tabularium_lib/release_v2.py` | The closed key set both schema documents declare with `additionalProperties: false`, on the row and on `provenance`, is still unenforced in Python: `validate_event_row` checks presence and tuple membership and enumerates no keys, so an extra `provenance` key, an extra top-level key and a dropped top-level `amounts` are each named by `jsonschema` alone. Measured at this head against a re-serialised `aave-v4-v0`, whose unmutated control the verifier accepts: all three mutations are refused at `canonical bytes do not match an offline source rebuild`, none is admitted. The divergence is in which layer refuses and whether the field is named, not in what the ledger admits, and `verifier.py:105` already states that `validate_event_row` is the naming layer in front of that comparison. Decided, not carried: not fixed here because step 3's Files field does not reach `release_v2.py` and a step does not silently widen; the Mason's `test_a_row_only_one_validator_refuses_fails_the_parity_check` holds the disagreement red, which is what the step's elenchus discipline asks of a parity mismatch. Fiat owns the routing: either a runbook amendment putting `release_v2.py` into a later step's Files, or a filed issue carried at integration. | accepted |
| S3-R1-04 | low | `.hexaemeron/study.md` | Study section 11 names the Elenchus fix command `python3 plugins/tabularium/tests/prove_schema_v3.py --case <finding-id> --report {report}` with format `unittest-json-v1`, while runbook line 26 names `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}` and states that the reporter is not an audit runner. The Mason built the runbook's interface and was right to: the reporter has no `--case` flag, it writes a `protasis-design-report/v1` object carrying no executed-test, failure, error or skip count, and `elenchus.py` classifies from exactly those counts, so the study's command could not return anything but `inconclusive`. This round used the runbook's runner. Recorded rather than repaired: the study is controller-pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and an `amend study` reverts every step's amended runbook field to baseline, so the correction is Fiat's to sequence, not a mid-round edit. | accepted |

Leads not pursued: two round-1 leads are now decided with evidence rather than carried again, one promoted and one settled. `_UNEXPECTED` is settled and not promoted. Measured at this head, `jsonschema` emits `Additional properties are not allowed ('operator_note' was unexpected)` for one extra key, which the pattern reads as `provenance.operator_note`, and `('operator_note', 'second_note' were unexpected)` for two, which it misses on the plural so the field coarsens to `provenance`. In both cases `agreed` is false and `library_field` is `None`, so the gap can only coarsen a named field and produce a false disagreement, never a false agreement, and no committed fixture carries two extra keys. The direction of failure is what settles it: it cannot move `value` to true. `write_report` was the other lead and is promoted above as S3-R2-01. Two round-1 leads stay open unchanged: `prove_schema_v3.py` remains unimportable in any process where a `tests` package is already bound from an earlier `sys.path` entry, and `--candidate` remains a label rather than a measurement, bounded by the identity check at `design_evidence.py:367`. One new lead: a report path containing a space is written into the `command` field unquoted, so the recorded string is not re-runnable verbatim; the binding still holds because the design record's resolver would carry the same unquoted form, the property is unchanged from the pre-fix code, and no path in this run contains a space. A missing or unparseable fixture raises `FileNotFoundError` or `JSONDecodeError` out of `rejection_parity()` before any report is written, so it fails closed but with a traceback rather than the named refusal the library uses; not pursued because the reporter is a test-tree script and no report is left behind either way. Negative results worth keeping. The round-1 fix generalises rather than fitting its one case: `resolver_command` was checked against every pending result the design record declares, sixteen of them, four criteria across four candidates, and each is byte-identical to the recorded resolver string. The three criteria the record binds to this script but its `--criterion` does not accept, `shipped-ledgers-validate-v3`, `legacy-v0-verify` and `suite-wall-time`, each exit 2 against `choices` and write no report, so none can attest a command that was not run. The reporter still cannot be made to attest a false pass: a fixture both validators accept gives exit 1 and a report recording `"value": false`, which is a recorded false criterion rather than a passing report; a removed fixture and a corrupted fixture each raise before the write and leave no report; only an emptied `REJECTION_FIXTURES` produced a vacuous true, which S3-R2-02 now guards. Path confinement is unchanged by the new writer: a symlinked directory component, a symlinked final component and a missing directory are each refused at exit 2 naming the component, the symlink targets are never written through, an absolute path outside the run worktree, a `..` path and an existing regular file are each still written at exit 0 as the discipline allows, and no `.tmp` staging file survives anywhere under the probe root. Skip invisibility: `run_tests.py --elenchus-report` records 194 tests with 0 failures, 0 errors, 0 skipped, 0 expected failures and 0 unexpected successes. Scope and gates: phylax, ephoros and hypomnema each exit 0 over the ten paths changed between `c3f298d4` and `26a3db14` and again over the two files this round's fix changed; the root suite ran 2,049 tests OK at exit 0; `run_checks.py --scope tabularium --format json` reports outcome green over 4 checks and `--scope schemas` green over 9, both at exit 0 on a quiescent tree, with none of the `unstable-source` result round 1 recorded and no `test_demonstrations` timing flake observed; `design_evidence.py .hexaemeron/design-evidence.json --transition step:3` prints `clean` at exit 0; `plugin_release.py --head bc378e5f` reports `tabularium: 0.3.8 -> 0.3.10` against `c3f298d4` and `0.3.7 -> 0.3.10` against `50432db9`, both at exit 0, with all four version surfaces reading `0.3.10`; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree leaves the worktree clean.

## Step 3, round 3 -- 2026-09-19T10:47:33Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=not-applicable; legacy-write=not-applicable; v0-immutability=reviewed; source-reuse=not-applicable; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=not-applicable

Not checked: this round audits `bc378e5f..0e4c39e1`, which is round 2's own record and its regenerated `.horos/` artefacts, so the four paths in the range carry no product change and version-mixing, legacy-write and source-reuse are not-applicable rather than reviewed. No built schema-3 ledger was validated through `jsonschema`; none ships until step 4. The Pashov pair did not run under the recorded waiver: no Solidity. `.hexaemeron/` was read and never written. The Elenchus verdict below is the declared classifier result, not an attestation of the report bytes; its overlay applies both changed files to the parent, so it is not the counterfactual for either finding, and both were run by hand. The vacuity sweep below is bounded: it enumerates the module-level collections that drive a loop body or a comparison in the Tabularium suite and empties each at source, which is what a careless future edit looks like. It does not establish that no vacuous shape exists outside that enumeration, and a loop over a locally built collection would not appear in it. One process note, recorded because it cost a result rather than because it changed one: the first run of that sweep restored each mutation with `git checkout`, which also discarded this round's own uncommitted tests, so its first three counterfactuals ran against the unpatched file and reported a pass that was not one. The tell was the test count, 194 where the patched tree runs 197. The sweep was rewritten to restore from copies and every counterfactual in this record is from that second run.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R3-01 | low | `plugins/tabularium/tests/test_schemas.py` | `ALL_SCHEMAS` and `DEPRECATED` drive the loops that carry every assertion in `SchemaDocumentValidityTests` and in `test_v2_documents_keep_their_id_and_are_marked_superseded`, and nothing bound either to the tree. Emptied at source, each left the Tabularium suite green at 194 tests with its loop body never entered: the same shape as S3-R2-02, one layer out from the reporter. They govern the schema documents rather than the attested design-report value, which is why this is low where S3-R2-02 was medium. Both are now bound to what is on disk: `ALL_SCHEMAS` to the versioned canonical family matched by name, and `DEPRECATED` to the documents carrying `"deprecated": true`, which is exactly `canonical-event-v2.json` and `coverage-manifest-v2.json`, with each successor required to exist and not itself be deprecated. The Compound v3 Phase 0 documents are not versioned canonical documents and stay outside the set. | fixed in 3160e574ad6c369a2db63337adb69cec91c17c6a |
| S3-R3-02 | low | `plugins/tabularium/tests/prove_schema_v3.py` | The `command` field joined its arguments with plain spaces, so a report path containing one was recorded as two arguments nobody passed: `shlex.split` of the recorded string returned `parity.json` rather than the path. Carried as a round-2 lead and settled here rather than a third time, because the fix is provably free: `shlex.join` leaves a token needing no quoting exactly as it is, so all sixteen resolver strings the design record declares remain byte-identical, checked after the change, and a spaced path is quoted instead. | fixed in 3160e574ad6c369a2db63337adb69cec91c17c6a |
| S3-R1-03 | low | `plugins/tabularium/scripts/tabularium_lib/release_v2.py` | The closed key set both schema documents declare with `additionalProperties: false`, on the row and on `provenance`, is still unenforced in Python: `validate_event_row` checks presence and tuple membership and enumerates no keys, so an extra `provenance` key, an extra top-level key and a dropped top-level `amounts` are each named by `jsonschema` alone. Measured at this head against a re-serialised `aave-v4-v0`, whose unmutated control the verifier accepts: all three mutations are refused at `canonical bytes do not match an offline source rebuild`, none is admitted. The divergence is in which layer refuses and whether the field is named, not in what the ledger admits, and `verifier.py:105` already states that `validate_event_row` is the naming layer in front of that comparison. Decided, not carried: not fixed here because step 3's Files field does not reach `release_v2.py` and a step does not silently widen; the Mason's `test_a_row_only_one_validator_refuses_fails_the_parity_check` holds the disagreement red, which is what the step's elenchus discipline asks of a parity mismatch. Fiat owns the routing: either a runbook amendment putting `release_v2.py` into a later step's Files, or a filed issue carried at integration. | accepted |
| S3-R1-04 | low | `.hexaemeron/study.md` | Study section 11 names the Elenchus fix command `python3 plugins/tabularium/tests/prove_schema_v3.py --case <finding-id> --report {report}` with format `unittest-json-v1`, while runbook line 26 names `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}` and states that the reporter is not an audit runner. The Mason built the runbook's interface and was right to: the reporter has no `--case` flag, it writes a `protasis-design-report/v1` object carrying no executed-test, failure, error or skip count, and `elenchus.py` classifies from exactly those counts, so the study's command could not return anything but `inconclusive`. This round used the runbook's runner. Recorded rather than repaired: the study is controller-pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and an `amend study` reverts every step's amended runbook field to baseline, so the correction is Fiat's to sequence, not a mid-round edit. | accepted |

Leads not pursued: every lead this step carried is now settled, and none is carried to a fourth round. The unquoted report path is promoted and fixed as S3-R3-02. The `tests` import fragility is settled and not promoted: `prove_schema_v3.py` remains unimportable in a process where a `tests` package is already bound from an earlier `sys.path` entry, but no declared command reaches it, because `run_tests.py` discovers with `top_level_dir=plugins/tabularium` and the root suite never imports the reporter, and the repair would change the import shape three files depend on for no reachable gain. `--candidate` as a label rather than a measurement is settled on round 2's evidence, unchanged: `design_evidence.py:367` refuses a report whose identity does not match the cell, and the record's selection is locked to `superseding-releases`. A missing or unparseable fixture is settled: it raises before any report is written, and since S3-R2-02 the suite also fails on it, so the traceback is the only residue and it leaves nothing behind. `_UNEXPECTED`'s missing plural is settled on round 2's evidence: it can only coarsen a named field and produce a false disagreement, never a false pass. S3-R1-03 and S3-R1-04 remain accepted, carried verbatim above; S3-R1-03's home is a filed issue in the run's carryover block at integration, which is settled and not reopened here. Negative results worth keeping. The vacuity question asked generally, as a checked claim over an enumerated set rather than an impression: eleven module-level collections that drive a loop body or a comparison were emptied at source one at a time, and after this round's fix every one is caught by a named test. `SHIPPED_RELEASES` and `REJECTION_FIXTURES` fail their own binding cases; `ALL_SCHEMAS` and `DEPRECATED` fail `test_the_declared_document_sets_match_the_schemas_directory`; `ADAPTER_MODULES` fails `test_coverage_v3_enums_consts_and_branches_equal_the_tuple_table`; `PROVENANCE_FIELDS` fails `test_a_missing_provenance_field_is_refused_by_name`; `KNOWN_GAPS`, `EVIDENCE_CLASSES` and `ADAPTERS` fail `test_a_symlink_swapped_in_after_the_alias_check_cannot_rewrite_source`; `SUPPORTED_EVENT_SCHEMAS` and `aave_v4.MAPPINGS` fail `test_borrow_and_repay_read_the_established_data_words`. No remaining vacuous-pass path was found within that enumeration. Emptying the committed fixture directory at runtime rather than at source is also caught, at one failure and five errors. The last shape, a guard skipped rather than run, is visible rather than silent: with `jsonschema` hidden the suite still exits OK and twelve tests skip, and the runner's report records `skipped: 12`, against `skipped: 0` on this host, which is the number the step's Exit pins and the reporter's own exit 1 reinforces. The atomic write holds under both failures that can be induced: with `os.write` refusing `ENOSPC` and with `os.replace` refusing `EIO`, the earlier report is byte-identical afterwards and the report's directory holds only the report, with no staged file left. Path confinement is unchanged at this head: a symlinked directory component, a symlinked final component and a missing directory are each refused at exit 2, the symlink target is never written through, and an existing file, a path outside the run worktree and an ordinary path are each written at exit 0 as the discipline allows, with no `.tmp` file anywhere. Scope and gates: phylax, ephoros and hypomnema each exit 0 over the four paths changed between `bc378e5f` and `0e4c39e1` and again over the two files this round's fix changed; the root suite ran 2,049 tests OK at exit 0; `run_checks.py --scope tabularium --format json` reports outcome green over 4 checks and `--scope schemas` green over 9, both at exit 0 on a quiescent tree; `run_tests.py --elenchus-report` records 197 tests with 0 failures, 0 errors and 0 skipped; `design_evidence.py .hexaemeron/design-evidence.json --transition step:3` prints `clean` at exit 0; `plugin_release.py --head 3160e574` reports `tabularium: 0.3.8 -> 0.3.11` against `c3f298d4` and `0.3.7 -> 0.3.11` against `50432db9`, both at exit 0, with all four version surfaces reading `0.3.11`; and regenerating `.horos/boundary.json` and `.horos/census.json` on the committed tree leaves the worktree clean.

## Step 4, round 1 -- 2026-09-19T13:07:32Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=reviewed; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=reviewed

Not checked: this round audits `12f9d3a2..31ee4804`, 50 files, on a clean tree at `31ee48043dda4e68c223e3bb6c21b8d8c00688c8`. The Pashov pair did not run under the recorded waiver: no Solidity. `.hexaemeron/` was read and never written. `partial-write` and `path-confinement` are reviewed from unchanged code rather than from new fault injection: no file under `plugins/tabularium/scripts/` is in this range, so round 3's induced `ENOSPC` and `EIO` results and its symlink refusals still describe this head; what was observed live this round is the runner refusing an `--elenchus-report` path outside the worktree, and three new `rebuild.py` demos that build only inside a `tempfile.TemporaryDirectory`. `skip-invisibility` is reviewed from an observed `skipped: 0` on this host and unchanged reporter code; `jsonschema` was not hidden again this round. No round of this step was run with the suite's dependency lock absent. The portable-package measurements below come from the generator's own `expected_files` with `require_byte_headroom` stubbed, which is the same payload the manifest counts, not from a second installation of the published package. No claim is made that the published `skills-runtime` artefact was rebuilt or installed. The Elenchus verdict is `null` because this round has no fixes commit, not because a classifier ran and returned nothing.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | medium | `scripts/portable_promise_machine.py` | Three files outside step 4's Files field changed: this one, `tests/test_skills_sh_package.py` and `plugins/tabularium/tests/support.py`. The first two add a repository-wide omission removing `plugins/tabularium/examples/*-v1/{source.json,capture.json,coverage.json,events.jsonl,rebuild.py}` from the portable runtime, which changes what every installer of `skills-runtime` receives. Disclosed by the Mason, which is not the same as being in scope. Measured, not accepted: with the omission the payload is 1,497 files and 20,210,242 bytes; without it, 1,512 files and 21,925,922 bytes, a delta of 15 files and 1,715,680 bytes. The two breached constraints are `MAX_FILES = 1_500` under a strict `assertLess`, and `MAX_BYTES - total >= MIN_HEADROOM` with `MAX_BYTES = 26,214,400` and `MIN_HEADROOM = 5,242,880`, an effective ceiling of 20,971,520. Both are genuinely breached without the omission, so the step had no green path inside its Files field. Two corrections to the forcing claim. The file figure is 1,512, not 1,500. More material: 21,925,922 is 4,288,478 bytes **under** the CLI's own 25 MiB default, so what the payload breaches is the 5 MiB reserve issue #1538 adopted below that default, not the default itself, and the byte cap framework-109 owns is not crossed. The alternative the same file's own recorded reasoning points at was therefore available at the same scope cost: raise `MAX_FILES`, which that reasoning has done five times on the stated ground that "shipped package content is not trimmed to hold a file count", and spend or re-decide the #1538 reserve. Both routes are repository decisions taken in a plugin-scoped step. Not reverted: reverting reddens the root suite and breaks the step's own Exit. Fiat owns the routing: a runbook amendment naming the three files, and the omission-versus-caps decision filed for the reserve's owner. | accepted |
| S4-R1-02 | low | `plugins/tabularium/skills/tabularium/SKILL.md` | The omission leaves shipped documents naming files the runtime does not contain. `SKILL.md:98-100` and `plugins/tabularium/README.md:209-211` list `python3 plugins/tabularium/examples/{aave-v4,euler-v1,euler-v2}-v1/rebuild.py` among seven rebuild commands with nothing distinguishing them, and each v1 `README.md` tables the SHA-256 of `source.json`, `capture.json`, `events.jsonl` and `coverage.json`, and its `DATA-DICTIONARY.md` documents `events.jsonl` field by field. Three qualifications, each checked. All seven commands are written repository-root-relative and none runs from the package tree, where the payload sits under `runtime/`, so the omission's stated reason that the documents say to run them in the full source checkout is accurate. `plugins/alexandria/examples/compound-v3-phase0-v0/README.md` names `source/`, `input/`, `release/` and its own rebuild command under the cited precedent's identical shape, so this is consistent with repository practice rather than new. No unique evidence is lost: each v1 `source.json` is byte-identical to the retained v0 source, each v1 `capture.json` differs from the retained v0 capture in `release` alone, and the retained `tabularium_lib` builder reproduces the v1 bytes, which the three v1 rebuild demos confirm at exit 0 against canonical digests `81d416a10b70ab0f3d9a3bd41c0680e235b3b64f4f06cc36b4f4292c81136492`, `5b1016a9bc143f42e9bea46de71b3d1917bf6731d93b4c49f974df3660bc8595` and `f2b227058f53cd644c11359e911c8494924d6fef7da7072e8a33a4baf952d02a`. Two residues stand. The omission drops `rebuild.py`, where the cited Alexandria precedent keeps it, and the exact-precedent option is blocked by three files: 1,497 kept plus three `rebuild.py` is 1,500 against a strict `< 1500`. And the reason's wording, that shipping both payloads "would carry the same evidence twice", is exact for `source.json`, near-exact for `capture.json`, and overstated for `events.jsonl` and `coverage.json`, which are different bytes under different canonical digests -- the v0 Aave ledger is `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7` -- and are derivable rather than duplicate. Not fixed here: a repair belongs in the packaging surface or in a note, and either widens the step further; the plugin prose edit would also restart the promise-machine digest cascade. Carried for Fiat to file at integration. | accepted |
| S4-R1-03 | low | `.hexaemeron/runbook.md` | The step 4 Exit clause "each v1 `capture.json` differs from v0 only in `release` and keeps the `request` object with `"id": 1`", and the study's `source-reuse` risk row that states the same, generalise a Euler v1 fact. Measured at this head: all three v1 captures differ from their v0 captures in `release` and in no other key, and the `request` object carries `"id": 1` in `euler-v1-v1` only; `aave-v4-v1` and `euler-v2-v1` carry a `request` object with no `id` member at all, exactly as their published v0 captures do. The clause as written is unsatisfiable for two of three releases without editing published v0 bytes, which `v0-immutability` forbids, so the step is right and the record is wrong. Not repaired here: the study is controller-pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and an `amend study` reverts every step's amended runbook field to baseline, so the correction is Fiat's to sequence. | accepted |

Leads not pursued: the promise-machine digest cascade was checked as a claim rather than accepted, and it holds. `docs/promise-machine/obligation-gates/evaluation-run.json` changed on exactly one line, `tree_sha256` `46d21d02d900a202294a6832050ace51daae78b9a904542df8086fdeb12affe7` to `ee771f3f377131763902e7e203ed8ad9fd765e6a56b9dcdcf9391f983641506e`. `corpus_sha256` `b9b6dfa8d1993c8bf7d7fea38adc3af765ac089b4efb7ce97d0c5e4f73ced427`, `prompt_template_sha256` `bb58f1dc5b6da0c37ae4303415f210d5158344516b51cc24f20bc623909eb5c2`, `model`, `date` `2026-08-31`, `counts`, `answers` and `failures` are all unchanged, and `docs/promise-machine/obligation-gates/evaluation-answers.json` is absent from the range with its digest `3ebdb3a8e7b86dc7fd8c7be77acd72632b966c107aa2034668702e96e12b8855` unmoved in the projection. No answer moved and no model ran, so no evaluation result is fabricated. The rest of the cascade is digest re-pinning only: both runtime fixtures change nothing but the recorded SHA-256 of `plugins/tabularium/tests/test_release.py`, `992977f6...` to `e89fedb8...`, in every evidence-class, source and selector slot, and `tests/promise_machine_coverage.json` re-pins those two fixture digests and the same source digest. v0 immutability holds and the suite asserts it rather than only this round: the only v0 paths in `1d131b98..31ee4804` are the three `README.md` and the three `rebuild.py`, the step-4 range touches the three `README.md` alone and adds a supersession note alone to each, and all twelve v0 data files match their `1d131b98a78c888b571f302e5b4c899aa2e47caf` digests, recomputed here and asserted by `test_release.py:175-196` over `support.PUBLISHED_V0_DIGESTS` with the key set derived from `LEGACY_RELEASES`, and again by the `legacy-v0-verify` criterion through `verification_observation`'s `digest_drift`. The ledger row is a generation bump: frontier revision `compound-v3-phase-1` and frontier digest `c3aac484a13f97742a45f07a4d3ca42cec29e4e4a9666cfd2701d825e4752d6e` are byte-identical to the `tabularium-v0.3.0` row, the `Current frontier` line and the held Compound v3 Phase 1 `Next Fiat job` are outside the diff, and the digest was recomputed independently here from `{status}|{frontier revision}|{current frontier}|{next Fiat job}` with its final newline and reproduces `c3aac484` exactly. The row's stated trade, 1,710,643 added bytes, was measured and is exact for the twelve new data files; the four version surfaces read `0.4.0`. The two new reporter criteria hold all three properties round 3 bought: `command` comes from `resolver_command(args)` rather than `sys.argv`, the write is staged under `O_EXCL|O_NOFOLLOW` and renamed with no truncation and no report written on a `ReportRefused` path, and an empty evidence collection now fails, because `value = bool(observations) and all(...)` and `document_observation` sets `agreed` to `bool(rows) and ...` so a release with no rows is refused rather than counted as agreement. The README test figure is observed rather than asserted: `run_tests.py` ran 228 tests with 0 failures, 0 errors and 0 skipped and a complete report, against `plugins/tabularium/README.md:124` reading 228. `plugins/tabularium/AGENTS.md` names no schema v2 and was correctly left alone; the four historical documents are absent from the range. The remaining v2 mentions in swept prose are deliberate references to the superseded schema. Three leads are recorded and not pursued. The package margin is now thin, at three files and 761,278 bytes below its two constraints, so the next delivery adding shipped content meets S4-R1-01's decision again rather than inheriting it. `plugins/tabularium/tests/support.py` is folded into S4-R1-01 rather than raised alone: its change is what the Tests field asks for and its content is sound, including the recorded rather than Git-read v0 digests, but the Files field names four test modules and not this one. `DEMONSTRATION.md:67` carries an inherited dangling decision reference that predates this run; left for the run's integration block, as briefed. Scope and gates: `phylax.py`, `ephoros.py` and `hypomnema.py` each print `clean` at exit 0 over the range's 50 paths and 15 changed Markdown files; the root suite ran 2,049 tests OK at exit 0 in 251.6s; `run_checks.py --scope tabularium --format json` reports outcome green over 4 checks and `--scope schemas` green over 9, both exit 0 on a quiescent tree, with no occurrence of the known `RunnerBoundaryTests` timing flake; `imprimatur.py ... --max-defects 0` prints `clean` at exit 0 over the seven documents the Exit names; `design_evidence.py .hexaemeron/design-evidence.json --transition step:4` prints `clean` at exit 0; the three v1 `rebuild.py` demos each exit 0 with a matching digest.

## Step 4, round 2 -- 2026-09-19T13:45:02Z

Audit schema: fiat-audit-round/v2

Covered: tuple-drift=reviewed; version-mixing=reviewed; legacy-write=reviewed; v0-immutability=reviewed; source-reuse=reviewed; schema-parity=reviewed; skip-invisibility=reviewed; partial-write=reviewed; path-confinement=reviewed; prose-drift=reviewed

Not checked: the product tree is unchanged. This round audits `31ee48043dda4e68c223e3bb6c21b8d8c00688c8` against an amended step 4, with `HEAD` at `c90178006584c33caaff7681457adb8d5d1c696e`, round 1's record commit, whose range is four paths and carries no product change. Every risk-register verdict above therefore rests on round 1's evidence over the same fifty-path range, re-confirmed by re-running every gate rather than re-derived: no file under `plugins/tabularium/` moved between the two rounds. The effective step source changed and the artefact did not, which is what this round is for. The Pashov pair did not run under the recorded waiver: no Solidity. `.hexaemeron/` was read and never written. The eleven-path coverage result below is mechanical, matching every backticked path in the replacement clause against the range; it does not judge whether an unnamed path ought to have been named, which is the finding's own argument. One process note, recorded because it cost a run rather than because it changed a result: the first root-suite run of this round was made while this Warden ran `protasis.py`, `imprimatur.py` and `hypomnema.py` against the amended runbook in the same shell, and it came back with one error, `RunnerSpecimenTests.test_a_child_past_its_timeout_is_killed_with_its_process_group` at `test_demonstrations.py:806`, `ValueError: invalid literal for int() with base 10: ''`, a grandchild whose pid had not reached `stdout_tail`. That is the same timing family as the known `RunnerBoundaryTests` flake and it was this Warden's breach of quiescence, not a property of the tree. Re-run alone, the suite is green, and the green run is the one this record reports. No claim is made that the published `skills-runtime` artefact was rebuilt or installed, and the package figures below come from the generator's own `expected_files` with `require_byte_headroom` stubbed. Whether a further Files amendment would be accepted was inferred from the first replacement's shape, not tested; only Fiat can run `amend runbook`.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | medium | `scripts/portable_promise_machine.py` | Three files outside step 4's Files field changed: this one, `tests/test_skills_sh_package.py` and `plugins/tabularium/tests/support.py`. The first two add a repository-wide omission removing `plugins/tabularium/examples/*-v1/{source.json,capture.json,coverage.json,events.jsonl,rebuild.py}` from the portable runtime, which changes what every installer of `skills-runtime` receives. Disclosed by the Mason, which is not the same as being in scope. Measured, not accepted: with the omission the payload is 1,497 files and 20,210,242 bytes; without it, 1,512 files and 21,925,922 bytes, a delta of 15 files and 1,715,680 bytes. The two breached constraints are `MAX_FILES = 1_500` under a strict `assertLess`, and `MAX_BYTES - total >= MIN_HEADROOM` with `MAX_BYTES = 26,214,400` and `MIN_HEADROOM = 5,242,880`, an effective ceiling of 20,971,520. Both are genuinely breached without the omission, so the step had no green path inside its Files field. Two corrections to the forcing claim. The file figure is 1,512, not 1,500. More material: 21,925,922 is 4,288,478 bytes **under** the CLI's own 25 MiB default, so what the payload breaches is the 5 MiB reserve issue #1538 adopted below that default, not the default itself, and the byte cap framework-109 owns is not crossed. The alternative the same file's own recorded reasoning points at was therefore available at the same scope cost: raise `MAX_FILES`, which that reasoning has done five times on the stated ground that "shipped package content is not trimmed to hold a file count", and spend or re-decide the #1538 reserve. Both routes are repository decisions taken in a plugin-scoped step. Not reverted: reverting reddens the root suite and breaks the step's own Exit. Fiat owns the routing: a runbook amendment naming the three files, and the omission-versus-caps decision filed for the reserve's owner. | accepted |
| S4-R1-02 | low | `plugins/tabularium/skills/tabularium/SKILL.md` | The omission leaves shipped documents naming files the runtime does not contain. `SKILL.md:98-100` and `plugins/tabularium/README.md:209-211` list `python3 plugins/tabularium/examples/{aave-v4,euler-v1,euler-v2}-v1/rebuild.py` among seven rebuild commands with nothing distinguishing them, and each v1 `README.md` tables the SHA-256 of `source.json`, `capture.json`, `events.jsonl` and `coverage.json`, and its `DATA-DICTIONARY.md` documents `events.jsonl` field by field. Three qualifications, each checked. All seven commands are written repository-root-relative and none runs from the package tree, where the payload sits under `runtime/`, so the omission's stated reason that the documents say to run them in the full source checkout is accurate. `plugins/alexandria/examples/compound-v3-phase0-v0/README.md` names `source/`, `input/`, `release/` and its own rebuild command under the cited precedent's identical shape, so this is consistent with repository practice rather than new. No unique evidence is lost: each v1 `source.json` is byte-identical to the retained v0 source, each v1 `capture.json` differs from the retained v0 capture in `release` alone, and the retained `tabularium_lib` builder reproduces the v1 bytes, which the three v1 rebuild demos confirm at exit 0 against canonical digests `81d416a10b70ab0f3d9a3bd41c0680e235b3b64f4f06cc36b4f4292c81136492`, `5b1016a9bc143f42e9bea46de71b3d1917bf6731d93b4c49f974df3660bc8595` and `f2b227058f53cd644c11359e911c8494924d6fef7da7072e8a33a4baf952d02a`. Two residues stand. The omission drops `rebuild.py`, where the cited Alexandria precedent keeps it, and the exact-precedent option is blocked by three files: 1,497 kept plus three `rebuild.py` is 1,500 against a strict `< 1500`. And the reason's wording, that shipping both payloads "would carry the same evidence twice", is exact for `source.json`, near-exact for `capture.json`, and overstated for `events.jsonl` and `coverage.json`, which are different bytes under different canonical digests -- the v0 Aave ledger is `490d3f6399f84af8a81a5401b3cc92bf7ecfbe98a6bb02f07215b9099625ccf7` -- and are derivable rather than duplicate. Not fixed here: a repair belongs in the packaging surface or in a note, and either widens the step further; the plugin prose edit would also restart the promise-machine digest cascade. Carried for Fiat to file at integration. | accepted |
| S4-R1-03 | low | `.hexaemeron/runbook.md` | The step 4 Exit clause "each v1 `capture.json` differs from v0 only in `release` and keeps the `request` object with `"id": 1`", and the study's `source-reuse` risk row that states the same, generalise a Euler v1 fact. Measured at this head: all three v1 captures differ from their v0 captures in `release` and in no other key, and the `request` object carries `"id": 1` in `euler-v1-v1` only; `aave-v4-v1` and `euler-v2-v1` carry a `request` object with no `id` member at all, exactly as their published v0 captures do. The clause as written is unsatisfiable for two of three releases without editing published v0 bytes, which `v0-immutability` forbids, so the step is right and the record is wrong. Not repaired here: the study is controller-pinned at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and an `amend study` reverts every step's amended runbook field to baseline, so the correction is Fiat's to sequence. | accepted |
| S4-R2-01 | low | `.hexaemeron/runbook.md` | The amended Files field closes the three files round 1 named and leaves eleven paths of the step's fifty unnamed, checked mechanically by matching every backticked path in the replacement clause against the range. One is discretionary source the Exit itself mandates: `plugins/tabularium/tests/prove_schema_v3.py` gained the `shipped-ledgers-validate-v3` and `legacy-v0-verify` criteria that two Exit commands invoke by name, and neither the baseline nor the amendment names the file. The other ten are mechanically forced surfaces with no covering clause, where `.horos/` has one. Four are the version bump the Exit's `plugin_release.py` clause compels: `.claude-plugin/marketplace.json`, `plugins/tabularium/.claude-plugin/plugin.json`, `plugins/tabularium/.codex-plugin/plugin.json` and `tests/test_version_propagation.py`. Six are the promise-machine re-pin that editing the named `test_release.py` compels: `tests/promise_machine_coverage.json`, both `tests/fixtures/promise-machine/runtime/tabularium-release-{build,verification}.json`, `docs/promise-machine/obligation-gates/evaluation-run.json` and `integration-projection.{json,md}`. Remedy is one further `Complete replacement Files:` clause naming the reporter and adding a regeneration sentence for the other ten; the Files grammar demonstrably accepts a clause of this shape, since the first replacement carried about forty code spans and was received clean. Round 1 understated this at three files by following the Mason's disclosure rather than enumerating the range, which is this round's correction to its own earlier row. | accepted |
| S4-R2-02 | low | `docs/tabularium-schema-v3/` | Step 1's `S1-R1-02` was accepted with an explicit remedy: "the remedy is a provenance note in step 4 prose, not a change to preserved bytes". Step 4 delivered no such note. `git grep design-probe` over tracked prose returns the Ephoros run's files and nothing from this run, the step-4 range contains no mention of the probe or the selection reports, and `docs/tabularium-schema-v3/` holds no equivalent of `docs/ephoros-typescript-rule-parity/design-probes/README.md`, which is the precedent for exactly this note. The step could not have delivered it in scope: no Files field version, baseline or amended, reaches `docs/tabularium-schema-v3/`, so the acceptance assigned work to a step whose scope does not hold the file. The cheap remedy needs no amendment: step 5 creates `docs/tabularium-schema-v3/demonstration.md` and its Files field already names it, so the note can go there. Recorded rather than fixed because this step's scope does not reach the path. | accepted |
| S4-R2-03 | low | `docs/tabularium-schema-v3/runbook.md` | The published runbook copy trails the controller copy. Measured: the published file is `0b8a2aadc24d7f757e48494127096ef1ddf1d7527dd850dc5c7e4dd4f728f08b`, unchanged since `edad9950` where step 1 created it, and `.hexaemeron/runbook.md` is now `27160ce051bdb453700231f7a2b532682bf9c85089f2e6ed4291b717bd766b3b`. `.hexaemeron/` is gitignored at `.hexaemeron/.gitignore` with the single pattern `*` and `git ls-files .hexaemeron` returns zero paths, so the published copy is the only tracked record of this run's runbook and it now states a Files field the run has superseded, with nothing in the tree saying so. No test binds the pair: `git grep tabularium-schema-v3` outside that directory returns only the audit log, its synopsis and an unrelated temporary-directory prefix in `test_schemas.py:50`, and the controller contract writes an amendment to the canonical controller path alone. Two qualifications. `study.md` and `design-evidence.json` have not drifted, both matching at `0f357a59...` and `512a0b64...`, so step 5's existing drift-repair clause has nothing to repair. And Hypomnema's own contract calls a study a run artefact rather than a record and requires the decision to live in a standing home, which here is the `tabularium-v0.4.0` ledger row, and that row is current; the drift misleads about the Files field, not about the decision. The stale copy is meanwhile the evidence that the amendment appended rather than edited: the published bytes are the exact byte prefix of the controller copy, the 2,467-byte suffix hashes to the receipted amendment digest `36586d6e719c4feef78464a66d5f2353cb570db833140e4190f8f4e8b62ccacf`, and the whole file hashes to the receipted `27160ce0...`, all recomputed here. Recommendation, and the routing is Fiat's: re-copy in step 5. It needs one `Complete replacement Files:` clause on a pending step, and the three gates step 1's Exit put on the published pair all pass on the amended bytes, measured this round -- `protasis.py` clean at exit 0, `imprimatur.py` over the published study and the amended runbook at `--max-defects 0` exit 0, and `hypomnema.py` clean. The trade is that the pre-amendment prefix stops being a tracked artefact, which costs nothing because `edad9950` still holds those bytes and the appended block stays visible in the re-copied file. Disclosure alone is defensible on the standing-record argument above, and is the answer if Fiat judges a second amendment not worth its digest move. | accepted |
| S4-R2-04 | low | `.hexaemeron/runbook.md` | The amendment's `Why` attributes the package move to "the 1,715,680 bytes this step adds". Measured: 1,715,680 is what the omission removes, the fifteen payload files; the step adds twenty-one files totalling 1,733,938 bytes to the package candidates, the extra 18,258 being the six v1 `README.md` and `DATA-DICTIONARY.md` the omission keeps. The arithmetic closes on the larger figure and on no other: the payload without the omission is 21,925,922 bytes and 1,512 files, with the omission 20,210,242 and 1,497, so the pre-step payload was exactly 20,191,984 bytes and 1,491 files, 20,191,984 plus 1,733,938 is 21,925,922, and every existing file's size delta nets to zero. A third figure is in play and is right where it stands: the ledger row's recorded trade of 1,710,643 is the twelve data files alone, excluding the three `rebuild.py`. Nothing in the conclusion moves; the pre-step payload was already only 779,536 bytes below the reserve line. Not worth its own amendment: carry the correction in the `Why` of the next amendment if S4-R2-01 produces one. | accepted |

Leads not pursued: the amendment is sound on byte continuity and on its caps, checked rather than accepted. The published step-1 runbook is the exact byte prefix of the amended controller copy, the 2,467-byte suffix hashes to the receipted amendment digest `36586d6e719c4feef78464a66d5f2353cb570db833140e4190f8f4e8b62ccacf`, and the whole file hashes to the receipted `27160ce051bdb453700231f7a2b532682bf9c85089f2e6ed4291b717bd766b3b`, so no baseline byte was edited and the replacement was appended as the contract requires. Its three caps numbers reproduce exactly as written: 1,512 files against a strict cap of 1,500 and 21,925,922 bytes against 20,971,520, with only the attribution of the added bytes carried as S4-R2-04. Its `What changed` names the reserve rather than calling the CLI default breached, which is the correction round 1 asked for. Three round-1 leads are settled rather than carried a third time. `plugins/tabularium/tests/support.py` is closed: the amendment names it, and its content was already reviewed sound. The package margin is unchanged at three files and 761,278 bytes and is now carried as a filed issue rather than as a lead. `.agents/plugins/marketplace.json` is settled for this run as it was for step 1: its `tabularium` entry carries no `version` key, re-read at this head, so no bump was ever owed there and the four version surfaces are the whole set. Step 1's `S1-R1-03` is not settleable here and is not restated: the declared Elenchus runner still cannot return anything but `inconclusive`, and this round produced no fixes commit, so the verdict is `null` for the second round running. `DEMONSTRATION.md:67` remains the run's integration-block carry, as briefed, and was not touched. Negative results worth keeping. `docs/tabularium-schema-v3/study.md` and `design-evidence.json` are byte-identical to their controller copies at `0f357a59df6c2f35ee0d2e864d71f923df92db34b90e378dc7bb14d233626d17` and `512a0b64fd17b5e46768e250dca7c9ace5f6c69febf4fc800c326313187cb595`, so the runbook is the only member of the published triple that drifted. No test, script or configuration binds the published copy to the controller copy, established by grepping every tracked file. The amended runbook passes all three gates step 1's Exit put on the published pair. Scope and gates: `phylax.py` and `ephoros.py` print `clean` at exit 0 over the fifty-four paths of the step range and round 1's record range together, and `hypomnema.py` prints `clean` at exit 0 over that union's seventeen Markdown files; the root suite ran 2,049 tests OK at exit 0 in 246.7s on a quiescent tree; `run_checks.py --scope tabularium --format json` reports outcome green over 4 checks and `--scope schemas` green over 9, both exit 0 and neither showing the known `RunnerBoundaryTests` flake.
