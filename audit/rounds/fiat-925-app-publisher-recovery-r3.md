## Step 1, round 1 -- 2026-09-13T03:28:42Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=not-applicable; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=not-applicable; readback-substitution=not-applicable; cleanup-gap=not-applicable; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; Step 2 socket, service-identity, PEM, token, HTTP, POST, readback, retry, and cleanup behaviour; Step 3 deployment and live-isolation behaviour; live service installation; credential access; network or GitHub mutation; a successful greenlight; commit, controller receipt, push, merge, and publication

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:34`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:301`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:389`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:636` | Admission enforced its own queue approximation instead of the repository publication contract. The framework opening omitted the required second sentence; `framework-0`, `framework-01`, and a double-space title survived queue checking; and a candidate without the unfenced `Fiat-Required` decision, exact `carryover` block, or matching decision label could pass into the judgement gates. The three-test parent guard exited 1 with five assertion failures. | fixed in the audit candidate; all three candidates now receive the exact opening, canonical queue-title grammar, one unfenced `Fiat-Required: 0` or `1`, matching `only-pr-needed` or `fiat-run-needed`, one bounded canonical `carryover` block, and a bounded optional status block before authority or Imprimatur. Direct comparison with `hexctl.issue_publication_contract_faults` and focused refusal guards cover the repair. |
| S1-R1-02 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:338`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:386`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:437` | Conformance compared fixture names and metadata but did not execute the queue and rejection cases it claimed to cover. Replacing `admit_request` with an unconditional success therefore left fixture verification green, so a green design report did not establish its stated admission cases. | fixed in the audit candidate; conformance constructs and executes four accepted queues and eleven expected refusals, checks the exact refusal code and zero mint and POST attempts, and probes both sides of the member, string, and request-byte bounds. A bypassed `admit_request` now fails at `GIP199 conformance.rejection-cases`. |
| S1-R1-03 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/canonical.py:92` | The JSON work bound applied the 262,144-byte string limit to values but not object keys. A canonical request with a 262,145-byte key was accepted, despite the normative per-string ceiling. | fixed in the audit candidate; keys and values share one UTF-8 scalar and byte-bound check. A 262,144-byte key passes and a 262,145-byte key refuses with `GIP103 request.string`; focused and conformance guards cover both sides. |
| S1-R1-04 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:493`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:519` | The report writer rejected a symlink only at the final parent component. An intermediate `.hexaemeron` symlink was followed, allowing a conformance report to be written outside the working tree; the isolated parent probe returned `outside-write True outside`. | fixed in the audit candidate; the writer opens every relative parent component through directory descriptors with `O_DIRECTORY` and `O_NOFOLLOW`, then retains the single-link regular-destination and atomic-replacement checks. The subprocess guard refuses the intermediate symlink at `GIP199 conformance.report`, records zero mint and POST attempts, and leaves no outside file. |
| S1-R1-05 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:255`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:362` | The repository contract skips one valid top status block before checking the exact framework opening, but frozen admission required that opening at byte zero. A root-valid observation with its status block before the filing prose therefore refused at `GIP141 frozen.body_opening`, making the status check unreachable for that allowed shape. The exact single-test guard first exited 1 with one error at that refusal. This failed closed and did not admit unchecked prose. | fixed in the audit candidate; a body without a status block retains the original byte-zero rule, while a body with one validated top block must carry the exact frozen opening as its first visible filing-prose line after the close. The block cannot replace the opening, and all three candidates and ordered inventory checks remain in force. The same guard exits 0, and positive observation conformance now exercises the status-block shape. |
| S1-R1-06 | low | `.horos/boundary.json`; `.horos/census.json` | Horos was regenerated while the new audit source and synopsis were still untracked, so staging made two Markdown files visible after the census had frozen. `.githooks/greenlight` on staged tree `2fa38ea43a9e271ad146b9e5ede1863424c0736f` exited 1 with exactly four Horos failures: the fresh tracked tree had 3,302 files against 3,300 recorded, Markdown had 1,097 files against 1,095 recorded, and the boundary `#counts` had drifted. No new gate record or commit was created; the pre-existing `LAST_GREEN` remained the original HEAD tree `0c1f6df62fe2417548795b3d6710db8273822470`, with mtime `2026-09-13T00:38:41+0100`. | fixed in the audit candidate; the 13 original paths stayed staged while `horos.py scan . --write` regenerated the boundary and `horos.py scan . --write --census` regenerated the census against the index-visible audit paths. The result records 3,302 files and 106,363,654 bytes, including 1,097 Markdown files and 21,371,152 Markdown bytes; the existing Horos currency tests guard this ordering, and a fresh Horos check and affected root pass follow the updated record. |

Leads not pursued: The round entered on signed Step 1 commit `fefc489e0287396f4fb4b73d31df4fd9535cd59f`, parent `b9f8e36b8b6210bcd023a68059ecb46da3e35769`, tree `0c1f6df62fe2417548795b3d6710db8273822470`, and controller directive `4227a38f7362dee068862db45e42ef0c639612b3e379ecb51e667cb0dbc2ef9b`; its exact 42-path diff has 3,531 insertions and 14 deletions. The commit has a good signature from `laurence.e.day (github-signer) <laurence@wildcat.finance>` with RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and uses a numberless decision draft, so signer-confusion and adr-collision have no finding. All 22 risk-register rows were dispositioned. Closed schemas, canonical JSON, depth and member caps, duplicate and unknown-field refusals, frozen-inventory order, candidate and frozen digests, four fixed stage ids, stage subjects and source joins, authority subject and route, both in-process Imprimatur executions, and the exact #855 digest-pinned fixture were checked; no distinct defect survived after the six findings above. The Step 1 production surface remains exactly the CLI plus `github_issue_publisher_lib/{__init__,canonical,errors,policy}.py`; its AST imports none of `http`, `requests`, `socket`, `ssl`, `subprocess`, or `urllib`, and its source contains no `/access_tokens`, `api.github.com`, private-key marker, or App PEM path. Every exercised admission refusal and conformance refusal reports zero mint and POST attempts. This is static and test evidence that Step 1 has no credential, signer, subprocess, or HTTP capability; it is not evidence of live service isolation, which later steps own. The bounded fixture reader can follow an intermediate directory symlink, but every allowed fixture name is code-owned and every resulting byte string must match the closed manifest SHA-256 before use; replacing that path cannot change an admitted predicate, leak bytes into the closed report, or widen the fixed destination, so no cause remained. Before S1-R1-05, the source-bound 12-worker Hexaemeron run produced a complete green `elenchus.unittest.v1` report for 2,667 of 2,667 tests with zero failures, errors, skips, expected failures, or unexpected successes at SHA-256 `97f0156bcc897e4bf1f9ca1bcdc48243267fcfeae6247eed2d0199d324972bf5`; it did not cover the fifth repair. The fresh current-tree 12-worker run covers the first five repairs and produced a complete green report for 2,668 of 2,668 tests with the same zero-result categories at SHA-256 `740b9247d741cbf8916ae549c329483377408feb33a791aa9ec770ce9a9f46e2`. The current 53-test focused publisher suite is green. The three selected conformance resolvers exit 0 and emit `true boolean`, `256 count`, and `1048576 bytes`. Before S1-R1-05, Phylax, Ephoros, the full prescribed Hypomnema tree lint, the Hypomnema Study bridge, the Protasis Study check, the Step 1 design-evidence transition, portable synchronization and parity, and the audit-synopsis check exited 0; all affected required gates follow this updated record. The first 1,882-test root run had one Horos census-currency failure after the guarded files changed and no other failure. After census regeneration, a second 1,882-test root run had only `RunnerExecutionBoundaryTests.test_a_grandchild_that_leaves_the_process_group_is_refused`: it observed exit 0 instead of 2 under host contention, while the exact test and three immediate isolated repetitions all exited 0 and passed in 10.084, 10.080, 10.072, and 10.079 seconds. No Step 1 or audit path changes that test or runner. One further root retry was stopped after 60 seconds without a verdict when host load exceeded 268. After contention fell, the exact current-tree root suite passed 1,882 of 1,882 tests in 227.337 seconds. The checked runner at source `9401037fe519ffd4527d40b852762d1ad7e64c78f601ed50d997d876bd1dd4b1` then selected 11 checks; nine passed, including its 2,668-test Hexaemeron suite, 1,882-test root suite, Imprimatur, all three lints, dead-code, front-door, and joined-front-door checks. Its dead-code suppression check reproduced the already tracked issue #1342 dirty-snapshot refusal, and its concurrent demonstrations suite failed without retaining the failed method in the human report; the exact immediate demonstrations rerun passed 114 of 114 in 28.796 seconds, and the checked runner's concurrent root suite passed the same tests, so no Step 1 cause survived and the scheduling attribution remains bounded. The fixed-base 13-of-13 green checked-runner recovery is external base evidence. After those passes, `.githooks/greenlight` on staged tree `2fa38ea43a9e271ad146b9e5ede1863424c0736f` exited 1 with the four Horos currency failures recorded as S1-R1-06 and created no gate record or commit. The preliminary portable-copy drift and narrow Hypomnema `H009` result were generated-parity and input-selection diagnostics, not product findings. A preliminary multi-path Brevitas invocation returned argparse exit 2 because its interface accepts one draft; the corrected per-file run exits 0 for the Phylax reference and reports only `B010` at exit 1 for the mandatory one-heading audit record and its generated synopsis. Adding headings would violate `fiat-audit-round/v2`, so the host form is preserved. The runbook-pinned signed-ref Elenchus comparison over the complete repair range used pre-correction signed audit candidate `2301a7f4ccfb9dbe2b513b0148921e2f8b429d45` as its observed input and returned tool exit 0 with status `inconclusive` and detail `the runner report records an infrastructure error`. Its test command exited 1; the complete `unittest-json-v1` report recorded 2,616 executed tests, zero assertion failures, one error, and zero skipped. The sole error was an overlay import-boundary failure: the current overlaid test module imported `MAX_STRING_BYTES` from the unfixed parent's `github_issue_publisher_lib`, where that name does not exist. The comparison therefore cannot decide whether the six repairs are guarded against that parent; it is not a guarded verdict or a product-test failure. That SHA identifies the pre-correction observation, not the final fixes head; a controller receipt must bind the amended signed candidate. The Sapheneia durable-record shape preserves the action, current candidate state, evidence, limits, and next gate. Vulgate is a surface-only no-op because the candidate is already plain; title structure, six findings, severities, statuses, paths, hashes, counts, waiver, unknowns, negative evidence, and boundary statements remain byte-identical. This finding-bearing round requires a further independent audit round and authorises neither greenlight nor commit.

## Step 1, round 2 -- 2026-09-13T06:22:35Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=not-applicable; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=not-applicable; readback-substitution=not-applicable; cleanup-gap=not-applicable; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; Step 2 socket, service-identity, PEM, token, HTTP, POST, readback, retry, and cleanup behaviour; Step 3 deployment and live-isolation behaviour; live service installation; credential access; network or GitHub mutation; controller receipt, push, merge, and publication

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/policy.py:255`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:320` | Frozen opening validation admitted an opening at byte zero or after a top status block, but the root publication contract also skips leading blank lines and whole-line HTML metadata comments. A root-valid observation beginning `<!-- wildcat-origin: shoggoth -->` therefore refused at `GIP141 frozen.body_opening`. The exact parent guard first exited 1 with one error. This was a false refusal, and no unchecked prose reached authority. | fixed in the audit candidate; opening validation now requires the exact first visible unfenced filing-prose line after leading blanks, whole-line metadata comments, and the single bounded optional status block. It still checks source, Sapheneia, and final candidates, and the opening cannot be replaced by a comment. Before guard correction, the expected parent `PublisherError` escaped as an infrastructure error. The corrected guard retains only its primitive refusal fields outside the frozen exception context: the isolated parent now exits 1 with one assertion failure and zero errors, while the fixed current test exits 0. Both paths retain zero mint and POST attempts. |
| S1-R2-02 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/canonical.py:168`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher.py:406`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:752` | Single-line text validation rejected carriage return, category-C characters, line feed, and tab, but admitted other non-printing Unicode. Titles containing U+00A0 NO-BREAK SPACE, U+2028 LINE SEPARATOR, or U+2029 PARAGRAPH SEPARATOR passed admission even though `hexctl.issue_publication_contract_faults` rejects each as a title control character. The three-subtest parent guard first exited 1 with three failures. | fixed in the audit candidate; every non-printing character now refuses at `GIP110 source.title` before Imprimatur, while multiline body handling is unchanged. The three guarded code points and conformance probes pass; each refusal records zero Imprimatur, mint, and POST attempts. |

Leads not pursued: Round 2 entered on signed fixed head `2d4d63f8d37c81b7a18a315282f77b1a24938539`, tree `31dd02795fab6c7fed45b9e6ae2d6c1ac7bfa8a6`, parent `fefc489e0287396f4fb4b73d31df4fd9535cd59f`, controller directive `ef9a2e1a05115a291c587490aa63d651739fe1700dc4861741d038ba74ec6e8d`, and packet `b49a3a3846dfcdf14f708960b4470145fec0e4843b3619d6750694ba2eafa544`. The commit has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author, and carries the complete 44-path Step 1 tree. The audit candidate changes no path outside that tree; its exact base range is 44 paths, 4,427 insertions, and 14 deletions. All six Round 1 repairs were rechecked: root queue and body enforcement, executed conformance cases, bounded object keys, descriptor-walk report output, top-status opening reachability, and stage-before-Horos ordering. A fresh 45-case differential found 16 root-clean and 13 publisher-clean cases, no root-invalid publisher admission, no unexpected false refusal, and only the three intentional ADR-078 publisher refusals for missing, wrong, or both decision labels; every result kept mint and POST attempts at zero. Final report symlink and hardlink subprocess probes each exited 2 at `GIP199 conformance.report`, wrote zero stdout bytes, left the outside sentinel unchanged, and reported zero mint and POST attempts. The full parser, schema, queue, inventory, digest, stage, authority, and report joins were re-read against all 22 risk rows. Request bytes are capped before parsing; canonical JSON, UTF-8, NFC, depth, aggregate members, string keys and values, duplicates, unknowns, booleans-as-integers, floating values, and trailing bytes fail closed. Queue rules are recomputed from every candidate; protected items remain ordered; four fixed gate ids, subjects, versions, source joins, frozen digest, authority subject, and both local Imprimatur executions remain exact. The production surface is still only the CLI and `github_issue_publisher_lib/{__init__,canonical,errors,policy}.py`. AST and literal guards find no `http`, `requests`, `socket`, `ssl`, `subprocess`, `urllib`, access-token route, GitHub API host, private-key marker, or App PEM path. This proves only credential-free Step 1 code and refusal behaviour, not the live isolation later steps own. The receipted Round 1 issue #1342 dirty-snapshot defect and fixed-base 13-of-13 recovery remain external base evidence, not Step 1 findings. The final focused suite passes 55 of 55 tests. The exact selected conformance resolvers exit 0 with `true boolean`, `256 count`, and `1048576 bytes`. Protasis accepts the Study, Runbook, and Step 1 design transition; the Hypomnema Study bridge, Promise Machine check, portable parity after synchronization, Phylax lint, Ephoros lint, full Hypomnema tree lint, 262-test Horos suite, and `git diff --check` exit 0. The preliminary portable check refused the expected generated-mirror drift after the audit fixes; `portable_promise_machine.py sync` refreshed that ignored mirror and the repeated check passed, so this was a required parity step rather than a product defect. The exact 12-worker Hexaemeron run passes 2,670 of 2,670 tests with no failures, errors, skips, or fixture blocks, exact accounting, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `37273971fe46d60149b72ab7cec62c68f285fcf6e2673153ebae8c31319f1463`, and wall time 729.504 seconds. Final root, synopsis, Horos, and checked-runner evidence follows on the complete staged tree and is not claimed by this record. The runbook-pinned comparison against pre-correction signed audit candidate `eab50113b427b4d50de57a7de118e46f3a058bcf` returned tool exit 0, status `inconclusive`, detail `the runner report records an infrastructure error`, and test-command exit 1. Its first complete report recorded 2,670 executed tests, three assertion failures, two errors, and zero skipped. The three failures were the U+00A0, U+2028, and U+2029 subtests, establishing S1-R2-02 against the unfixed parent. One observed error was `plugins/hexaemeron/tests/test_metron_check.py:950` raising `ValueError` while parsing an empty pid file; the bounded output did not expose the other. An exact Warden replay against the same signed candidate retained the status, detail, exit, 2,670 tests, three failures, and zero skips but reported one error and did not reproduce Metron. The isolated overlaid S1-R2-01 guard identified that error as the parent's `GIP141:frozen.body_opening` refusal escaping the success path. On the unchanged signed tree, the Metron test passed four isolated runs in 0.176, 0.180, 0.189, and 0.185 seconds; no #925 path changes Metron, so its host-load attribution remains bounded and no cause or edit is claimed. The pre-correction guard exited 1 with one error. Because its frozen `PublisherError` also turns a chained assertion into `FrozenInstanceError` under Python 3.14, the corrected guard retains only primitive refusal fields outside the exception context; it exits 1 on the parent with one assertion failure and zero errors, while the fixed current test exits 0. Candidate `eab50113b427b4d50de57a7de118e46f3a058bcf` is therefore historical pre-correction input, not the final fixes commit. Staged tree `ef31f5d916c5ac9f431af4750e40d1ebd69f11a7` passed `.githooks/greenlight`, then became unreceipted amended candidate `d54c0c72b88a607449abb52d29534dda9767b831`, parent `2d4d63f8d37c81b7a18a315282f77b1a24938539`, with the same tree, a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, Shoggoth author, and Dr Laurence E. Day committer. The exact runbook-pinned Elenchus comparison against that replacement returned tool exit 0, status `guarded`, detail `the runner report records a parent assertion failure`, and test-process exit 1. Its runner-owned parent report was complete with 2,670 executed tests, four assertion failures, zero errors, and zero skipped; the fixed side passed. The four failures agree with the isolated one-failure S1-R2-01 guard and three-failure S1-R2-02 guard. The command declared `.hexaemeron/reports/conformance/fiat-925-r3-step-1.json`; Elenchus substituted its runner-owned detached-parent report path for `{report}`. Sapheneia durable-record shape preserves the action, candidate state, findings, evidence, limits, and next gate. Vulgate surface parity is a no-op because the record is already plain; every title, severity, status, path, code point, count, hash, waiver, unknown, and negative result remains fixed. This finding-bearing round authorises no push, merge, or publication and requires a further independent audit round after receipt.

## Step 1, round 3 -- 2026-09-13T09:56:09Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=not-applicable; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=not-applicable; readback-substitution=not-applicable; cleanup-gap=not-applicable; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; Step 2 socket, service-identity, PEM, token, HTTP, POST, readback, retry, and cleanup behaviour; Step 3 deployment and live-isolation behaviour; live service installation; credential access; network or GitHub mutation; controller receipt, push, merge, and publication

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Round 3 entered on signed fixed head `0ae1fdac480f8581f0485fa9722f57a635eb3d02`, tree `74a4d3b72598f04ffa2886a40c2bb2c205214c49`, parent `2d4d63f8d37c81b7a18a315282f77b1a24938539`, controller directive `375cdfb17e739aba75e016135713d686a308ee834ccc6e5ac80f03ff5c04a25c`, and packet `b73d7dad3f07d436659939788ded22db88b35c546ecf133dd7828fd4111c51b7`. The commit has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and carries the complete 44-path Step 1 range over `b9f8e36b8b6210bcd023a68059ecb46da3e35769`: 4,427 insertions and 14 deletions. All six Round 1 and both Round 2 repairs were rechecked: repository-contract queue and filing-prose enforcement, executed conformance cases, bounded object keys, descriptor-walk report output, top-status opening reachability, stage-before-Horos ordering, leading metadata-comment opening reachability, and rejection of non-printing Unicode titles. The full parser, closed schema, queue recomputation, protected inventory, candidate and frozen digests, four stage ids and subject/source joins, authority gate, two local Imprimatur runs, fixture contract, and report writer were re-read against all 22 risk-register rows. Request bytes are capped before parse; UTF-8, NFC, canonical JSON, duplicate and unknown fields, booleans-as-integers, floating values, trailing bytes, depth, aggregate members, and all string keys and values fail closed at their declared bounds. A fresh 15-case differential exercised root-valid and root-invalid combinations of leading blanks, whole-line metadata comments, one bounded top status block, and exact framework prose: seven root-valid cases were admitted, eight root-invalid cases were refused, no false admission or false refusal survived, and every case retained zero mint and POST attempts. The focused publisher suite passes 55 of 55 tests, and the two exact Round 2 guards pass. The three selected conformance resolvers exit 0 with `true boolean`, `256 count`, and `1048576 bytes`. The production surface remains only the CLI and `github_issue_publisher_lib/{__init__,canonical,errors,policy}.py`; its AST and literal guard finds no `http`, `requests`, `socket`, `ssl`, `subprocess`, `urllib`, access-token route, GitHub API host, private-key marker, or App PEM path. This is bounded code and test evidence that Step 1 has no credential, signer, subprocess, or HTTP capability and that its tested refusals make zero signer or POST attempts; it is not evidence for the live service isolation, token use, request execution, readback, cleanup, or deployment that later steps own. The exact root suite passes 1,882 of 1,882 tests in 277.717 seconds. The source-bound 12-worker Hexaemeron run passes 2,670 of 2,670 tests with zero failures, errors, skips, fixture blocks, expected failures, or unexpected successes, exact accounting and exactly-once execution, wall time 357.947 seconds, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, and manifest SHA-256 `37273971fe46d60149b72ab7cec62c68f285fcf6e2673153ebae8c31319f1463`. Promise Machine, portable Promise Machine, the Protasis Study, Runbook, design-lock and Step 2 transition checks, the Hypomnema Study bridge, the 262-test Horos suite, the current-tree Horos check, `git diff --check`, and the prescribed Phylax, Ephoros, and full-tree Hypomnema lints exit 0. The existing changed prose passes Imprimatur at score 100 with zero defects. The receipted Round 1 issue #1342 dirty-snapshot defect and its fixed-base 13-of-13 recovery remain external base evidence, not Step 1 findings. No new cause survived manual review, differential comparison, or the required suites, so there is no fix commit and the Elenchus verdict is null. Sapheneia durable-record shape preserves the action, signed candidate state, zero result, evidence, limits, and next gate. Vulgate surface parity is a no-op because the record is already plain; every heading, risk disposition, zero row, hash, count, waiver, qualification, negative result, and boundary statement remains fixed. Final synopsis, Horos, candidate-root, and staged-tree evidence follows outside this record. This zero-finding round authorises only the controller's `hexctl audit-round --findings 0` transition; it does not authorise a commit, receipt, push, merge, publication, credential access, or network or GitHub mutation.

## Step 2, round 1 -- 2026-09-13T17:56:23Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; a real App PEM, signer, installation token, network request, GitHub issue, or public readback; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/server.py:51` | The production macOS peer reader called `connection.getpeereid`, but Python 3.14.6 socket objects on the target Mac do not expose that method. A real local `socketpair()` therefore refused at `GIP221 peer.identity` before admission, so the default service path could accept no request. Both the injected ABI guard and live local socket-pair guard failed against the Step 2 parent. | fixed in the audit candidate; the reader obtains Darwin `LOCAL_PEERCRED`, validates the 76-byte `xucred` version and group count, and uses its effective UID and first group. The local socket-pair check now returns UID 501 and GID 20, equal to `geteuid()` and `getegid()` on this Mac. No credential or network was used. |
| S2-R1-02 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/server.py:111` | `SOCKET_TIMEOUT_SECONDS` was declared but never applied to an accepted connection. An admitted-group client could hold the one request boundary indefinitely before the runtime budget began. The parent guard observed no `settimeout` call. | fixed in the audit candidate; the server requires and applies the code-owned 60-second I/O timeout before peer lookup and frame reading, maps setup failure to `GIP220 socket.timeout`, and retains close-on-every-path behaviour. The focused guard observes exactly one `settimeout(60.0)` call. |
| S2-R1-03 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:74`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:272` | The client accepted request-shaped strings, objects, booleans, and values above one in diagnostic attempt counts. It also accepted mutually impossible terminal fields, including `published` with no issue, no attempts, no readback, incomplete cleanup, and a failure code. Those forms could make an invalid service response look like a trustworthy publication result. The parent guards accepted all eight first-captured malformed cases. | fixed in the audit candidate; diagnostic attempts are exact integers in `{0,1}`, POST cannot precede mint, and a refusal cannot carry `GIP000`. Result validation now enforces monotone attempts, issue and readback joins, cleanup-code ownership, and the exact field combinations each one-shot outcome can emit. Ten malformed diagnostic and terminal cases refuse at `GIP400`; valid runtime outcomes remain green. |
| S2-R1-04 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/transport.py:81`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/transport.py:156` | The claimed 16 KiB live response-header ceiling ran only after `http.client` had parsed and retained the headers. The standard-library parser could therefore read its larger per-line and header-count bounds first. The parent probe requested 65,537 bytes from the stream and returned `GIP301` rather than the declared `GIP302`. | fixed in the audit candidate; the live response class wraps the socket file before `HTTPResponse.begin()`, limits status and header line reads, counts aggregate raw header bytes, and raises `GIP302 transport.headers` before the parser retains byte 16,385. The guard observes no read request above 16,385 bytes, and the connection closes on refusal. |

Leads not pursued: The round entered on signed Step 2 head `c4d322a0e0f7b968e21b70255fd0ff6e1d74248d`, tree `e29f93d783bd2d9a80f5231f7cb1e2f2d24b4779`, parent `9b5838963bff1f5d8d4aecd52afd8687f24005cf`, immutable brief SHA-256 `182d7b8a22adf365613b256310854b6dce7c85fa8328b65c39d53447ece7f8e1`, Runbook SHA-256 `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, Study SHA-256 `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and design-evidence SHA-256 `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. Its exact 16-path parent diff has 3,341 insertions and 56 deletions. The head has a good signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, carries exactly the required two provenance trailers, and retains the numberless decision draft. All 22 risk-register rows and the full Step 2 diff were inspected. Request framing remains length-first, closed by EOF, bounded to one frame, and rejects short, trailing, concatenated, unsafe JSON, duplicate, unknown, deep, or excessive input before signer access. Queue, protected inventory, judgement records, both in-service Imprimatur runs, fixed stage order, final digest, and authority remain joined before signing. The signer path, executable, arguments, environment, input, timeout, output checks, App identity, installation token request, permissions, repository, host, routes, methods, API version, TLS context, redirect refusal, one POST, authenticated and anonymous exact-byte readbacks, response close paths, cleanup, and content-free schemas were traced. No distinct defect survived after the four findings. The client-side path check precedes connect and can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators are outside the promise, while Step 3 owns the deployed owner, group, mode, identity, and denial checks. The fixed OpenSSL process and key path bound signer output provenance; no real PEM was read and no production signer was invoked, so this round makes no live key, signature, process-memory erasure, or isolation claim. Socket and HTTPS timeouts are inactivity controls, while the injected elapsed-time budget rejects a returned stage or lifecycle beyond its declared ceiling; deliberate cooperative slow-drip wall-clock cancellation was not established as a live-host property. The current focused publisher suite passes 100 of 100 tests. The source-bound 12-worker Hexaemeron run passes 2,715 of 2,715 tests with no failures, errors, skips, fixture blocks, expected failures, or unexpected successes, exact accounting and exactly-once execution, wall time 593.062 seconds, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `45287868fc889e0403929e3bd9c3c2da755109f3e3a192f754688605d1cd856d`, and report SHA-256 `136348c7879aab6e0db9b0cd99b1ca0be23d5857e04089f5b5f71501425dd079`. The selected `signer-and-post-boundary` conformance command exits 0 with `true boolean`; its report SHA-256 is `650c86234139c3f13cf29d5eca00a99ced008e268b7ca5c8f0070f6e940db9cb`. Portable Promise Machine synchronization and parity, `git diff --check`, Imprimatur on the changed decision and reference, and the prescribed Phylax, Ephoros, and full-tree Hypomnema lints exit 0. The exact root suite passed 1,882 of 1,882 tests on the untouched signed Step 2 tree; a fresh final-tree result, synopsis check, Horos currency, greenlight, candidate signature, and Elenchus comparison follow this preliminary record. Sapheneia durable-record shape preserves the action, candidate state, four findings, evidence, limits, unknowns, and next gate. Vulgate is a surface-only no-op because the record is already plain; every heading, risk disposition, finding id, severity, path, count, digest, code, waiver, qualification, negative result, and boundary statement remains fixed. This finding-bearing round authorises neither controller mutation nor push, merge, publication, credential access, network use, or deployment; after the exact guarded verdict is bound into this record, it requires another independent audit round.

## Step 2, round 2 -- 2026-09-13T19:20:43Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; whether the single nonzero OpenSSL child reached or opened the fixed key path internally; a successful production signature; an installation token; a live network request, GitHub issue, or public readback; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/signer.py:47`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:833` | The default signer used `subprocess.run` with `stdout=PIPE`, so it retained all child output before applying the 4,096-byte ceiling. The parent guard emitted 1 MiB and wrote its after-output sentinel before returning `GIP211 signer.output`; the exact two-test run exited 1 with one assertion failure and one pass. | fixed in the audit candidate; the runner retains at most 4,097 stdout bytes, then kills and reaps the child at the 4,096-byte ceiling. The injected-child output and timeout guards pass, and the signer class passes 8 of 8 tests. |
| S2-R2-02 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/runtime.py:75`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/runtime.py:177`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1519` | If terminal event retention failed after token exchange or the issue POST, the runtime raised a diagnostic with zero mint and POST attempts. The parent guard completed four injected HTTP requests, including one POST, but the server returned `GIP401` with counts `0/0`; the exact test exited 1 with one assertion failure. A caller could mistake a completed POST for a safe pre-mutation refusal. | fixed in the audit candidate; terminal event failures close every component and retain the observed token and POST attempt counts in the public diagnostic. The same injected path now returns counts `1/1`, reports four closed responses, and exits 0. |

Leads not pursued: Round 2 entered on clean signed fixed head `44f536de3b764463137fa90640840da2a8574003`, tree `b6dfe0507df0c24579efb9d2e6a975517ef9b6e4`, parent `c4d322a0e0f7b968e21b70255fd0ff6e1d74248d`, and immutable brief SHA-256 `af60e10ca183f5b489c58e811487606e1883d24a228cfc46817f2d5dee5748a6`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The complete Step 2 range over `9b5838963bff1f5d8d4aecd52afd8687f24005cf` is 19 paths, 3,746 insertions, and 61 deletions; the Round 1 repair range is 10 paths, 422 insertions, and 22 deletions. The head has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and carries exactly the required two provenance trailers. S2-R1-01 through S2-R1-04 were re-read and their six exact regressions pass. All 22 risk-register rows and the complete current Step 2 tree were inspected. Framing, peer and socket checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and process controls, token containment, fixed destination, one POST, two exact readbacks, terminal cleanup, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls rather than proved cooperative slow-drip wall-clock cancellation. Result validation does not separately compare a returned request digest with locally sent request bytes, but the one-request server computes that result behind the authenticated root-owned socket boundary; a compromised service or root remains outside this step, so no distinct cause survived. During the first S2-R2-01 test edit, one stale `subprocess.run` mock failed to intercept the new `Popen` implementation. One invocation of command class `/usr/bin/openssl dgst -sha256 -sign /var/db/wildcat-github-issue-publisher/shoggoth-wildcat-labs.pem` returned nonzero and surfaced only `GIP212 signer.exit`. The invocation and nonzero result were observed. No stdout, stderr, PEM bytes, successful signature, network request, or GitHub mutation was observed. The PEM was not statted, opened, or read to investigate, and the production signer was not invoked again. Every default signer test now substitutes an injected Python child; a `Popen` guard rejects the exact production argv and any argv containing the fixed PEM path. The full signer reference audit found no other default-runner call outside that guarded class; the conformance path injects its runner. This boundary incident is test-harness evidence attached to S2-R2-01, not a third product finding. The focused publisher suite passes 104 of 104 tests. The source-bound 12-worker Hexaemeron run passes 2,719 of 2,719 tests with zero failures, errors, skips, fixture blocks, expected failures, or unexpected successes, exact accounting and exactly-once execution, wall time 900.365 seconds, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `cae6750e7802c2bac6d07c22f9f3c014636908481dc4e880ba34c371cbd8ef25`, and report SHA-256 `39dcf6dd0b20422e159c263eeff1ecdeac5e56db243cb34e8186aba9e84d5a4a`. An earlier invocation with `/tmp/fiat-925-r3-step2-r2-current-warden.json` was refused by argparse before discovery because the report path was outside the worktree; it changed nothing and ran no test. The selected `signer-and-post-boundary` conformance command exits 0 with `true boolean` and report SHA-256 `650c86234139c3f13cf29d5eca00a99ced008e268b7ca5c8f0070f6e940db9cb`. Portable synchronization and parity, `git diff --check`, and the prescribed Phylax, Ephoros, and full-tree Hypomnema lints exit 0. Brevitas exits 0 for the changed Phylax reference and 1 for the governed audit source and generated synopsis: the source reports `B011` on the existing one-row and two-row v2 finding tables, including this two-finding round, while the generated synopsis reports `B027`. Adding rows would invent findings, and editing generator-owned synopsis bytes is outside the prose pass, so neither diagnostic changes the governed artefacts. The first exact 1,882-test root run had one failure and no other failure or error: the Horos census retained 21,428,172 Markdown bytes while the changed prose produced 21,428,527. Files remained 1,097 Markdown and 3,310 total. After the audit source and synopsis were staged and Horos was regenerated, the second exact root run passed 1,882 of 1,882 tests in 807.466 seconds. The synopsis and Horos checks exited 0. Greenlight on preliminary tree `8b346484330381324dcc90cd0112c8cda09f3d40` passed its 1,882 tests in 526.293 seconds and produced signed preliminary commit `6c31bd649c39886695e0c700747f809dbdc26419`, whose parent is `44f536de3b764463137fa90640840da2a8574003`. The runbook-pinned Elenchus comparison against that commit returned tool exit 0, status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report recorded 2,719 executed tests, three assertion failures, zero errors, and zero skipped; its output included the unfixed parent's after-output sentinel for S2-R2-01 and zero retained attempt counts for S2-R2-02. The amended signed candidate and repeated final-SHA Elenchus comparison follow outside this record. Sapheneia durable-record shape preserves the heading, 22 risk dispositions, two findings, severities, paths, commands, hashes, counts, waiver, boundary incident, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. After the exact guarded verdict is bound into this record, Step 2 requires another independent audit round.

## Step 2, round 3 -- 2026-09-13T20:49:07Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/runtime.py:60`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1465` | The 60-second total lifecycle deadline was enforced at bounded operation starts and finishes, but terminal cleanup and receipt events only called `elapsed_ms`, which clamped overruns to `60000`. After four injected responses, a close hook advanced the clock from `0` to `60.001`; head `b9907614ee1377a9467412202445486e762f1269` still returned `published` with `GIP000`, mint and POST counts `1/1`, and cleanup and receipt times `60000`. The focused parent guard exited 1 with one assertion failure, and the source-bound 2,720-test parent run exited 1 with that sole failure and no errors or skips. | fixed in the audit candidate; terminal elapsed-time sampling now refuses an overrun with `GIP330 deadline.total` instead of clamping it into an accepted event. The same injected path retains mint and POST counts `1/1`, closes all four responses, signer, transport, and receipt sink, and passes alone and in the 105-test publisher suite. The fixed source-bound run passes 2,720 of 2,720 tests. |

Leads not pursued: Round 3 entered on clean signed fixed head `b9907614ee1377a9467412202445486e762f1269`, tree `e27c54cc290b91199aa34750c03939930685c678`, parent `44f536de3b764463137fa90640840da2a8574003`, and immutable brief SHA-256 `c848e27caa3c0a959b2725a9494f0453f908d2c940870221e6d4cae9416267c7`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The entry Step 2 range over `9b5838963bff1f5d8d4aecd52afd8687f24005cf` has 19 paths, 3,980 insertions, and 61 deletions. The head has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and carries exactly the required two provenance trailers. S2-R1-01 through S2-R1-04 and S2-R2-01 through S2-R2-02 were re-read; their selected regression and signer-containment set passes 10 of 10 tests, and the full publisher suite passes 105 of 105. All 22 risk-register rows and the complete current Step 2 tree were inspected. Framing, macOS peer identity, socket timeout and access checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and bounded child controls, token containment, fixed destination, one POST, two exact readbacks, response limits and closure, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The signer tests apply a class-wide `Popen` guard that rejects the exact production argument vector and every vector containing the fixed PEM path; every default-runner case substitutes an injected Python child, and conformance injects its runner. No production signer invocation occurred in this round. The Round 2 single nonzero OpenSSL boundary incident remains historical evidence only; the PEM was not read, opened, statted, or investigated. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`, and root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls; the elapsed-time budget rejects operations that return after their ceiling, but this round did not establish pre-emptive cancellation of a component that never returns. Result validation still trusts the service-computed request digest behind the authenticated root-owned socket boundary; a compromised service or root remains out of scope. The parent-red source-bound report is `elenchus.unittest.v1`, records 2,720 tests, one assertion failure, zero errors or skips, and has SHA-256 `4b4e4b43233e39e8cbe98cf68f3fa13de4f45e01b20800cc1ada5b131ca6b599`. The fixed report records 2,720 tests with zero failures, errors, skips, expected failures, or unexpected successes and has SHA-256 `9c44012faf67bc22eab818baab70f4af5f566b687f89772192509cefe3083d99`; both runs used runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f` with exact accounting and exactly-once execution. The exact root suite passed 1,882 of 1,882 tests in 306.539 seconds. Phylax, Ephoros, Hypomnema, the synopsis check, Horos currency, portable parity, and `git diff --check` exited 0. Greenlight passed 1,882 of 1,882 tests in 414.953 seconds on six-path tree `1c2a08268758392f48a23f32d5645d8836365763`, producing signed preliminary commit `6aea30957a7c8a42e10c6edee33f7b3fd195a736` over parent `b9907614ee1377a9467412202445486e762f1269`. That commit has a good signature from the governed RSA key, the governed author and committer, and exactly the required two trailers. The first runbook-pinned Elenchus invocation against that preliminary commit returned tool exit 0, exact status `inconclusive`, test path `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py`, and detail `the run did not finish inside 900s`; the timed-out isolated run produced no report or result counts. Exact Elenchus against amended signed commit `e0cffa9a234ea552eb5dc02695126ea83c14ceab` returned tool exit 0, exact status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report records 2,720 executed tests, one assertion failure, zero errors, and zero skips. The reconciled signed candidate and repeated final-SHA Elenchus comparison follow outside this record. Sapheneia durable-record shape preserves the heading, 22 risk dispositions, finding, severity, paths, codes, counts, hashes, waiver, signer containment, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. The preliminary inconclusive result remains historical evidence; the exact guarded verdict is the round result. Step 2 requires another independent audit round.

## Step 2, round 4 -- 2026-09-13T23:56:06Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a real credential or token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/runtime.py:301`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1465` | The create-stage deadline check shared the `try` block that caught an unknown create result. When the POST returned an exact issue after its 20-second stage ceiling but before the 60-second total ceiling, `budget.finish` raised `GIP330` after the issue had been retained; the handler relabelled it `create-indeterminate`, performed both readbacks, and reached an impossible result shape. `result_document` then raised `GIP400 receipt.value` with false mint and POST counts `0/0`, despite one token exchange, one POST, four requests, and four closed responses. The receipt sink stayed open. A deterministic scan found the defect at 3 of 210 clock placements. The exact parent guard exited 1 with one assertion failure: expected `created-but-unverified`, `GIP330`, `1/1`, but observed `refused`, `GIP400`, `0/0`. | fixed in the audit candidate; the stage deadline check now runs only after the create call returns without entering its unknown-create exception handler. A returned exact issue that overruns the stage ceiling reaches the existing known-issue terminal path, performs no readback, and returns `created-but-unverified` with `GIP330`, issue `9250`, mint and POST counts `1/1`, authenticated and anonymous readback counts `0/0`, two requests, all delivered responses closed, signer and receipt sink closed, and a written receipt. The exact guard and all 210 clock placements now pass. |

Leads not pursued: Round 4 entered on clean signed repaired head `2b23169510e0b9acc3cfa8c27ba9d74c1429b174`, tree `3091cbbf7b40036680a3779206a85b00eda191a6`, parent `b9907614ee1377a9467412202445486e762f1269`, and immutable brief SHA-256 `669f65fd255bd20efd8eb77cf69d95eca2838db48b37a2444ec7b4abaed15ee7`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The complete Step 2 range over `9b5838963bff1f5d8d4aecd52afd8687f24005cf` has 19 paths, 4,033 insertions, and 61 deletions. Every commit in that range has a good signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`; the entry head names Shoggoth as author and Dr Laurence E. Day as committer and carries exactly the required two provenance trailers. S2-R1-01 through S2-R1-04, S2-R2-01 through S2-R2-02, and S2-R3-01 were re-read against the full repaired tree. Their selected regression set, the production argument-vector and key-path containment guard, and the new cause guard pass 11 of 11 tests; the full publisher suite passes 106 of 106. The signer tests apply a class-wide `Popen` guard that rejects the exact production argument vector and every vector containing the fixed PEM path; every default-runner case substitutes an injected Python child, conformance injects its runner, and the repository-wide signer reference audit found no other default-runner call. No production signer was invoked in this round. The real PEM was not read, opened, statted, or investigated. No real credential, token, network request, or GitHub action occurred. The Round 2 single nonzero OpenSSL boundary incident remains historical evidence only. All 22 risk-register rows and the complete repaired Step 2 tree were inspected. Framing, macOS peer identity, socket timeout and access checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and bounded child controls, token containment, fixed destination, one POST, two exact readbacks, response limits and closure, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls, and the audit did not establish pre-emptive cancellation of a component that never returns. Result validation still trusts the service-computed request digest behind the authenticated root-owned socket boundary; a compromised service or root remains outside Step 2. A receipt-event clock overrun after a successful receipt write returns `GIP330 deadline.total` with correct mutation counts `1/1`, closes every component and the sink, and leaves a truthful already-written terminal record; it did not reproduce S2-R4-01 and exposes the bounded distributed-terminal evidence already documented by the runtime contract. The entry source-bound 12-worker suite passed 2,720 of 2,720 tests with zero failures, errors, skips, expected failures, or unexpected successes, exact accounting and exactly-once execution, wall time 560.011 seconds, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `fee62f89ccf62f2a2e918cd4348073979cf6387af492a2f4cb9229729dbab1be`, and report SHA-256 `9c44012faf67bc22eab818baab70f4af5f566b687f89772192509cefe3083d99`. The fixed source-bound suite passed 2,721 of 2,721 tests with the same zero-result categories, exact accounting and exactly-once execution, wall time 607.792 seconds, the same runner SHA-256, manifest SHA-256 `e1a63c7598eeb9c877a6f5e0c0be3d047601c6088499bcdc71a2b426d5f3979a`, and report SHA-256 `63af216697ae0c6a8c01dd79625f930ba10f5b6f97ad592d745094f27be674f1`. The selected injected `signer-and-post-boundary` conformance command exits 0 with `true boolean` and report SHA-256 `650c86234139c3f13cf29d5eca00a99ced008e268b7ca5c8f0070f6e940db9cb`. The exact root suite passed 1,882 of 1,882 tests in 423.132 seconds. Phylax, Ephoros, Hypomnema, portable parity, synopsis freshness, Horos currency, and `git diff --check` exit 0. Brevitas exits 1 only for `B011` on the governed one-finding v2 tables and `B027` on the generated synopsis qualifier stack; adding rows would invent findings, and editing generator-owned synopsis bytes is outside the prose pass. Greenlight passed 1,882 of 1,882 tests in 332.457 seconds on six-path tree `b05c89d47ce8d52bacf503dfccbdbadaf8f5f64f`, producing signed preliminary commit `b0f3070a8b1a8e33cab113e20cd6bc1529550cf3` over parent `2b23169510e0b9acc3cfa8c27ba9d74c1429b174`. The runbook-pinned Elenchus comparison against that commit returned tool exit 0, exact status `guarded`, test path `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report records 2,721 executed tests, one assertion failure, zero errors, and zero skips. The amended signed candidate and repeated final-SHA Elenchus comparison follow outside this record. Sapheneia durable-record shape preserves the action, current candidate state, 22 risk dispositions, finding, severity, paths, codes, counts, hashes, waiver, signer containment, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. Step 2 requires another independent audit round after this verdict is bound into the signed record.

## Step 2, round 5 -- 2026-09-14T07:41:20Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a real credential or token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R5-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/transport.py:29`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1539` | The response boundary admitted an unbounded positive issue number. A reduced injected create response with a 1,757-digit all-nine number occupied 3,946 bytes, below the 8,192-byte remote-response ceiling, and completed the token request, one POST, and both readbacks: four requests with mint and POST counts `1/1`. Serialising the terminal result then exceeded its 4,096-byte ceiling and raised `GIP400 receipt.bytes` with false public counts `0/0`. The four responses, signer, and transport closed, but the receipt sink stayed open, so a caller could mistake a completed POST for a safe pre-mutation refusal. | fixed in signed comparison candidate `65e76fee75316df0c2363b34e3d34f206cfa688e`; returned issue numbers and caller-selected readback routes are bounded to positive signed 64-bit values. The reduced case now returns `create-indeterminate` with `GIP320`, accurate counts `1/1`, two requests and no readbacks; every delivered response, signer, transport, and receipt sink closes, and the receipt is written. The exact guard and 107-test publisher suite pass. Exact source-bound Elenchus against preliminary signed commit `9304ddfc77a7b6a49f18c96113ad010a60d09a16` returned tool exit 1 under `--require-guard`, status `inconclusive`, detail `the runner report records an infrastructure error`, and test-command exit 1. Its complete report recorded 2,722 executed tests, one assertion failure, one error, and zero skipped. The intended S2-R5-01 parent assertion was present, but a sibling route guard referred to the new module constant and raised parent `AttributeError`; the parent-compatible replacement spells the same overflow value as `1 << 63`. Exact Elenchus against `65e76fee75316df0c2363b34e3d34f206cfa688e` returned tool exit 0, status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete report recorded 2,722 executed tests, two assertion failures, zero errors, and zero skipped. The two failures are the oversized returned-issue guard and the signed 64-bit readback-route guard. |

Leads not pursued: Round 5 entered on clean signed repaired head `9417c8dd2bef44cd1db977826f010b628066e443`, tree `ecff32d8acc0f6a90b45509eeea1564a69872550`, parent `2b23169510e0b9acc3cfa8c27ba9d74c1429b174`, and immutable brief SHA-256 `38275854ca04589dc94ab1bb6df2ea58a38d01b4dec78b6de480b2526ed769aa`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The entry head has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and carries exactly the required two provenance trailers. S2-R1-01 through S2-R1-04, S2-R2-01 through S2-R2-02, S2-R3-01, and S2-R4-01 were re-read against the full repaired tree. Their selected regression set, the production argument-vector and key-path containment guard, and the new cause guard pass 12 of 12 tests; the full publisher suite passes 107 of 107. The signer tests apply a class-wide `Popen` guard that rejects the exact production argument vector and every vector containing the fixed PEM path; every default-runner case substitutes an injected Python child, conformance injects its runner, and the repository-wide signer reference audit found no other default-runner call. No production signer was invoked in this round. The real PEM was not read, opened, statted, or investigated. No real credential, token, network request, or GitHub action occurred. During Round 2, one stale `subprocess.run` mock failed to intercept the new `Popen` implementation. One invocation of command class `/usr/bin/openssl dgst -sha256 -sign /var/db/wildcat-github-issue-publisher/shoggoth-wildcat-labs.pem` returned nonzero and surfaced only `GIP212 signer.exit`. The invocation and nonzero result were observed. No stdout, stderr, PEM bytes, successful signature, network request, or GitHub mutation was observed. The PEM was not statted, opened, or read to investigate, and the production signer was not invoked again. That historical incident did not recur in Round 5. All 22 risk-register rows and the complete repaired Step 2 tree were inspected. Framing, macOS peer identity, socket timeout and access checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and bounded child controls, token containment, fixed destination, one POST, two exact readbacks, response limits and closure, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls, and the audit did not establish pre-emptive cancellation of a component that never returns. Result validation still trusts the service-computed request digest behind the authenticated root-owned socket boundary; a compromised service or root remains outside Step 2. The initial exact 1,800-digit unit guard failed on the entry implementation with one assertion failure: expected returned `create-indeterminate`, `GIP320`, counts `1/1`, but observed raised `GIP400`, `receipt.bytes`, counts `0/0`. The reduced 1,757-digit entry probe made four requests, closed the four responses and signer, left the sink open, and reproduced the same defect. A repaired-tree scan of nine injected event-failure positions and 256 monotone clock-jump placements preserves terminal counts and closure without another `GIP400`. The source-bound 12-worker repaired-tree suite passes 2,722 of 2,722 tests with zero failures, errors, skips, expected failures, or unexpected successes; its complete report has SHA-256 `868bffc65acb3f26c0760d0c49256f7d42446a54c6075bfd6d3fc5489687ed62`, and the unchanged runner has SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`. The remaining selected conformance, publication, repository, root, staged-tree, and source-bound comparison gates follow outside this preliminary record and are not claimed here. Sapheneia durable-record shape preserves the action, candidate state, 22 risk dispositions, finding, severity, paths, numeric limits, codes, counts, hashes, waiver, signer containment, historical boundary incident, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. Step 2 requires another independent audit round after the exact Elenchus verdict is bound into this signed record.

## Step 2, round 6 -- 2026-09-14T09:36:55Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a real credential or token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | medium | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:18`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:238`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1836` | Terminal-result validation accepted a syntactically consistent `published` result carrying issue number `9223372036854775808`, even though the transport and caller-selected readback route refuse any issue number above signed 64-bit maximum. The reduced entry-tree result was 622 bytes, below the 4,096-byte result ceiling, and `parse_closed_result` accepted it. A client could therefore treat an impossible lifecycle identity from the service boundary as a trustworthy publication result. | fixed in signed comparison candidate `428c0986cd2dd19cc436fc4043aa8ac5042fd059`; result construction and client parsing now share the transport's positive signed 64-bit ceiling through one constant. The exact boundary guard accepts `9223372036854775807`, refuses `9223372036854775808` at `GIP400 receipt.value`, and remains compatible with the unfixed parent. The guard, 14 selected prior-and-current regressions, and the 108-test publisher suite pass. Exact source-bound Elenchus against that commit returned tool exit 0, status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report recorded 2,723 executed tests, one assertion failure, zero errors, and zero skips; the failure is the S2-R6-01 guard accepting the oversized terminal issue on the unfixed parent. |

Leads not pursued: Round 6 entered on clean signed repaired head `54c697c06076f2304e53c62863759734a231595b`, tree `5d3b66cb9af71bc148bfec74cd89242f8281d179`, parent `9417c8dd2bef44cd1db977826f010b628066e443`, and immutable brief SHA-256 `fe00e2e40ff229069db4cb9e16f34bdff9dba472fd4e66cd711ab6888fb400b8`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The entry head has a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, names Shoggoth as author and Dr Laurence E. Day as committer, and carries exactly the required two provenance trailers. S2-R1-01 through S2-R1-04, S2-R2-01 through S2-R2-02, S2-R3-01, S2-R4-01, and S2-R5-01 were re-read against the full repaired tree. Their selected regression set, the production argument-vector and key-path containment guard, and S2-R6-01 pass 14 of 14 tests; the full publisher suite passes 108 of 108. The signer tests apply a class-wide `Popen` guard that rejects the exact production argument vector and every vector containing the fixed PEM path; every default-runner case substitutes an injected Python child, conformance injects its runner, and the repository-wide signer reference audit found no other default-runner call. No production signer was invoked in this round. The real PEM was not read, opened, statted, or investigated. No real credential, token, network request, or GitHub action occurred. During Round 2, one stale `subprocess.run` mock failed to intercept the new `Popen` implementation. One invocation of command class `/usr/bin/openssl dgst -sha256 -sign /var/db/wildcat-github-issue-publisher/shoggoth-wildcat-labs.pem` returned nonzero and surfaced only `GIP212 signer.exit`. The invocation and nonzero result were observed. No stdout, stderr, PEM bytes, successful signature, network request, or GitHub mutation was observed. The PEM was not statted, opened, or read to investigate, and the production signer was not invoked again. That historical incident did not recur in Round 6. All 22 risk-register rows and the complete repaired Step 2 tree were inspected. Framing, macOS peer identity, socket timeout and access checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and bounded child controls, token containment, fixed destination, one POST, two exact readbacks, response limits and closure, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls, and the audit did not establish pre-emptive cancellation of a component that never returns. Result validation still trusts the service-computed request digest behind the authenticated root-owned socket boundary; a compromised service or root remains outside Step 2. An injected receipt sink that advanced the lifecycle clock beyond 60 seconds after a successful write produced public `GIP330 deadline.total` with accurate mint and POST counts `1/1`, while the sink retained a truthful `published GIP000` terminal receipt; all four requests and responses, signer, transport, and sink closed. This is the documented conservative reconciliation after a terminal event cannot be retained, not another S2-R4-01 lifecycle-shape failure. The repaired-tree 12-worker suite passes 2,723 of 2,723 tests with zero failures, errors, skips, expected failures, or unexpected successes, exact accounting and exactly-once execution, wall time 538.536 seconds, 12 shards, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `e0a8a0d2f660cb38b60bba7e65c18236b519ac4edab919f293b89f4d2b9bd85b`, and report SHA-256 `41d842e59244e404f4c2b785f533a23b191f94a15738b49131618b3ae8044af8`. The selected injected `signer-and-post-boundary` conformance command exits 0 with `true boolean` and report SHA-256 `650c86234139c3f13cf29d5eca00a99ced008e268b7ca5c8f0070f6e940db9cb`. Portable synchronization and parity, Python compilation, `git diff --check`, the 250-test dead-code suite, front-door check, 13-test joined-front-door suite, and 112-test Imprimatur suite exit 0. The dirty-tree dead-code suppression check refused before analysis and the first 114-test demonstration suite reported only the expected Horos census byte drift after the changed reference. After the audit source, generated synopsis, and Horos data were frozen, the demonstration suite passed 114 of 114; the clean-commit dead-code suppression check exited 0. Phylax, Ephoros, Hypomnema, Horos currency, synopsis freshness, and portable parity exited 0. The exact root suite passed 1,882 of 1,882 tests in 274.985 seconds. Greenlight passed 1,882 of 1,882 tests in 235.578 seconds on eight-path tree `f2585f8ebbfcda0e6e5ed5393271e82437732ca9`, producing signed preliminary commit `428c0986cd2dd19cc436fc4043aa8ac5042fd059` over parent `54c697c06076f2304e53c62863759734a231595b`. That commit has a good signature from the governed RSA key, the governed author and committer, and exactly the required two trailers. Exact source-bound Elenchus against that commit returned tool exit 0, status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report records 2,723 executed tests, one assertion failure, zero errors, and zero skips. The amended signed candidate and repeated final-SHA Elenchus comparison follow outside this record. Sapheneia durable-record shape preserves the action, current candidate state, 22 risk dispositions, finding, severity, paths, numeric limits, codes, counts, hashes, waiver, signer containment, historical boundary incident, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. Step 2 requires another independent audit round after this verdict is bound into the signed record.

## Step 2, round 7 -- 2026-09-14T10:49:11Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a real credential or token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R7-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:52`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:110`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:324`; `plugins/hexaemeron/tests/test_phylax_github_issue_publisher.py:1836` | The client checked count types, monotone lifecycle order, and broad outcome shape without joining a declared failure code to the attempt boundary that can emit it. It accepted a 539-byte `refused` terminal result with `GIP212` and zero signer, token, or POST attempts. The reduced parent-compatible test also admitted `GIP311` below token exchange and `GIP321` below the issue POST. The last form could make a malformed service envelope describe an issue-byte failure as a safe pre-mutation refusal and invite a caller to retry after a POST that the code itself says was reached. The first exact guard exited 1 with one test, three assertion failures, and zero errors. | fixed in signed comparison candidate `a790ecfdd99e1a1fb9d14c90f2a09a1053fe2a7a`; the result boundary requires declared signer failures after one signer attempt, transport and token failures after one token attempt, and returned-issue failures after one POST attempt. The diagnostic form enforces the token and POST floors it exposes. The expanded cause guard passes, the selected prior-and-current set passes 15 of 15 tests, and the full publisher suite passes 109 of 109. Exact source-bound Elenchus against `a790ecfdd99e1a1fb9d14c90f2a09a1053fe2a7a` returned tool exit 0 under `--require-guard`, status `guarded`, detail `the runner report records a parent assertion failure`, and test-command exit 1. Its complete `unittest-json-v1` report records 2,724 executed tests, five assertion failures, zero errors, and zero skips; the five failures cover the three terminal-result floors and two diagnostic floors. |

Leads not pursued: Round 7 entered on clean signed repaired head `36a8da02cb68fa50ee29f160348db3493dbbeddf`, tree `e1765f909cf0c04e6d7631baa72d896ace2eb34e`, parent `54c697c06076f2304e53c62863759734a231595b`, and immutable brief SHA-256 `fa94adb63814ef1f904498edde997e9fb23d9b2862cd56ae5741932238c44761`. The Runbook, Study, and design-evidence SHA-256 values remain `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The entry head and every commit in the Step 2 range have a good Laurence signature from RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`; the entry names Shoggoth as author and Dr Laurence E. Day as committer and carries exactly the required two provenance trailers. The complete candidate range over `9b5838963bff1f5d8d4aecd52afd8687f24005cf` has 19 paths, 4,331 insertions, and 61 deletions. S2-R1-01 through S2-R1-04, S2-R2-01 through S2-R2-02, S2-R3-01, S2-R4-01, S2-R5-01, and S2-R6-01 were re-read against the full repaired tree. Their selected regression set, the production argument-vector and key-path containment guard, and S2-R7-01 pass 15 of 15 tests; the full publisher suite passes 109 of 109. The signer tests apply a class-wide `Popen` guard that rejects the exact production argument vector and every vector containing the fixed PEM path; every default-runner case substitutes an injected Python child, conformance injects its runner, and the repository-wide signer reference audit found no other default-runner call. No production signer was invoked in this round. The real PEM was not read, opened, statted, or investigated. No real credential, token, network request, or GitHub action occurred. During Round 2, one stale `subprocess.run` mock failed to intercept the new `Popen` implementation. One invocation of command class `/usr/bin/openssl dgst -sha256 -sign /var/db/wildcat-github-issue-publisher/shoggoth-wildcat-labs.pem` returned nonzero and surfaced only `GIP212 signer.exit`. The invocation and nonzero result were observed. No stdout, stderr, PEM bytes, successful signature, network request, or GitHub mutation was observed. The PEM was not statted, opened, or read to investigate, and the production signer was not invoked again. That historical incident did not recur in Round 7. All 22 risk-register rows and the complete repaired Step 2 tree were inspected. Framing, macOS peer identity, socket timeout and access checks, queue and protected-inventory joins, judgement limits, both local Imprimatur passes, stage order, immutable final bytes, signer input and bounded child controls, token containment, fixed destination, one POST, two exact readbacks, response and header limits, component closure, content-free diagnostics, the public client boundary, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft remain guarded. The declared signer, token, transport, and issue failure families are now joined to their minimum visible attempt boundary; the version-1 code field remains syntactically bounded for future fixed codes rather than claiming semantics for an undeclared code. The client path check can race only with an identity able to replace an entry in root-owned `/var/run`; root and administrators remain outside the promise. Socket and HTTPS timeouts remain inactivity controls, and this round did not establish pre-emptive cancellation of a component that never returns. Result validation still trusts the service-computed request digest behind the authenticated root-owned socket boundary; a compromised service or root remains outside Step 2. The exact injected `signer-and-post-boundary` conformance command exits 0 with `true boolean` and report SHA-256 `650c86234139c3f13cf29d5eca00a99ced008e268b7ca5c8f0070f6e940db9cb`. Python compilation, the client forbidden-import scan, the production-signer call-site scan, and `git diff --check` exit 0. The 12-worker repaired-tree suite passes 2,724 of 2,724 tests with zero failures, errors, skips, expected failures, unexpected successes, or fixture blocks, exact accounting and exactly-once execution, 12 shards, wall time 307.633 seconds, runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, manifest SHA-256 `fb72bcbe0250cccb2f672bee2190c307710f6d9261c4e4e6d001719bf02e47b9`, and report SHA-256 `1430f1d8ff66094a2facf9ef45e940e4fe69da8e9635626e5b847d14b1863c16`. The resumed publisher suite passes 109 of 109 tests in 3.085 seconds. Phylax, Ephoros, Hypomnema, portable parity, synopsis freshness, Horos currency, clean-commit dead-code suppression validation, both Imprimatur passes, and `git diff --check` exit 0. The mandatory staged greenlight passes 1,882 of 1,882 root tests in 647.327 seconds on tree `d95c44dd57109653b5e4b41bdbbb7a337134594a`, producing the signed comparison candidate above with the governed author, committer, signer, and trailers. The first checked-runner selection was superseded when that commit changed its source identity; its retry returned `nothing-selected`, which is not claimed as completed selected-check evidence. After the final signed amendment, the complete selection will rerun with explicit base `36a8da02cb68fa50ee29f160348db3493dbbeddf` and no concurrent source mutation. The final staged greenlight and exact final-SHA Elenchus comparison also follow outside this record. Brevitas reports B011 on the required Fiat tables and B027 on the generated synopsis; the owning fixed table format, append-only history, and generated-byte rules take precedence, so those bytes remain unchanged by the structural pass. Sapheneia durable-record shape preserves the action, entry state, 22 risk dispositions, finding, severity, paths, codes, attempt floors, byte and test counts, hashes, waiver, signer containment, historical boundary incident, qualifications, unknowns, negative evidence, limits, and next gate. Vulgate is a surface-only no-op because the record is already plain; every protected item remains fixed. This finding-bearing round authorises no controller mutation, push, pull request, merge, publication, credential access, network use, or deployment. Round 7 is the only current scope; no later audit round begins here.

## Step 2, round 8 -- 2026-09-14T12:22:51Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=not-applicable; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=not-applicable; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=not-applicable

Not checked: Solidity-only Pashov X-Ray, Solidity Auditor, and Fizz under the exact waiver `waived: this run produces and changes no Solidity`; the real App PEM or its contents; the production signer; a successful signature or installation token; a real credential or token; a live network request, GitHub issue, or public readback; hard cancellation of a component that never returns; a deployed service account, group, socket, key, LaunchDaemon, helper replacement, or live-isolation receipt; Step 3 infrastructure; root or administrator exclusion; controller receipt or mutation; push, pull request, merge, publication, or deployment

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Round 8 entered on clean signed head `be60696cba3505687f0515c13fac76cab3ba7864`, tree `639b7201ff332fb0b2ea9a61fd461a0437b7f2c5`, with brief SHA-256 `892aeddbdc783e1466f6cfed93a5e7eab791a36a4c7a3097f0b15792afc1447a`. The Study, Runbook, and design-evidence SHA-256 values remain `45981759eb011b4c82515127b3124d792342ab03903e7a0a8c8f53fc50f78706`, `0e6a4fc2cdc805cfded6f3a8dac88ad8563c0319d397a6cff4ace998acf5d583`, and `d74fd663f1ec76d8169fe3bfa536749d3440127b3e00ec3bd2c6894ae0280587`. The entry signature verifies with RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, Shoggoth as author, Dr Laurence E. Day as committer, and the two required provenance trailers. The repaired Step 2 range over `9b5838963bff1f5d8d4aecd52afd8687f24005cf` contains 19 paths, 4,348 insertions, and 61 deletions. All 22 risk dispositions and the full repaired lifecycle were reviewed again. S2-R1-01 through S2-R1-04, S2-R2-01 through S2-R2-02, and S2-R3-01 through S2-R7-01 remain repaired. Framing, socket and peer checks, ordered admission and protected-inventory joins, both publisher-executed Imprimatur passes, immutable final bytes, signer access and child limits, fixed HTTPS destination and narrowed scope, one POST, two exact readbacks, response bounds and closure, terminal attempt counts, failure-code attempt floors, content-free output, the public client, donor reconciliation, stale-receipt treatment, authorship, and the numberless decision draft produced no new finding. The declared failure families constrain minimum attempt counts; undeclared future codes retain only their syntactic bound. Socket and HTTPS timeouts remain inactivity controls, and pre-emptive cancellation of a component that never returns was not established. Socket replacement by an identity controlling root-owned `/var/run`, root and administrator access, and a compromised service remain outside the promise. The client continues to trust the service-computed digest behind that socket boundary. The historical Round 2 signer-boundary incident remains exactly as recorded in prior entries; it did not recur. No real PEM was read, opened, statted, or investigated; no production signer, real credential, live network request, or GitHub mutation was used. The class-wide production-argument and key-path guard remains active in signer tests, and conformance uses injected components. The fresh publisher suite passes 109 of 109 tests in 1.972 seconds; the exact injected `signer-and-post-boundary` command exits 0 with `true boolean`. Fresh Phylax, Ephoros, and Hypomnema lints exit 0. Runtime and test bytes are unchanged from Round 7, so its 2,724-test repaired-tree evidence and final 11-check green report are reused only for those unchanged sources: report SHA-256 values `1430f1d8ff66094a2facf9ef45e940e4fe69da8e9635626e5b847d14b1863c16` and `23a0892f78606e92935b49b919d00078aa46f0d2086f48d41ea70fd46ce73a1b`. The new audit-only delta receives fresh root and selected repository checks after its bytes are frozen, with explicit base `be60696cba3505687f0515c13fac76cab3ba7864`; their final report and mandatory staged greenlight follow outside this record, before signing. No fixes commit or new Elenchus verdict is claimed. Sapheneia durable-record shape preserves the zero-finding structure, all risk dispositions, identifiers, hashes, counts, prior-finding status, waiver, limits, unknowns, evidence reuse, and remaining gates. Vulgate changes no content. The required zero-finding table and generated synopsis retain their owning formats. The seven receipted rounds remain byte-identical. This round makes no controller, later-round, publication, or deployment decision.

## Step 3, round 1 -- 2026-09-14T14:36:26Z

Audit schema: fiat-audit-round/v2

Covered: same-uid-bypass=reviewed; socket-spoofing=reviewed; frame-smuggling=reviewed; queue-substitution=reviewed; inventory-loss=reviewed; judgement-forgery=reviewed; imprimatur-substitution=reviewed; stage-reordering=reviewed; byte-mutation=reviewed; signer-early-access=reviewed; token-crossing=reviewed; destination-widening=reviewed; duplicate-create=reviewed; readback-substitution=reviewed; cleanup-gap=reviewed; service-misdeployment=reviewed; public-helper-regression=reviewed; donor-drift=reviewed; stale-receipt=reviewed; signer-confusion=reviewed; adr-collision=reviewed; root-bypass=reviewed

Not checked: Solidity under `waived: this run produces and changes no Solidity`; live service installation, resource-limit changes, OS identity or group changes, production signer or PEM access, live token or GitHub publication, helper retirement, persistent server receipts, live isolation, root or administrator exclusion, interpreter libraries, unknown credential copies, or pre-emptive cancellation of a component that never returns.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | high | `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/deployment.py:57`; `plugins/hexaemeron/skills/phylax/deployment/macos/publisher-service.py:23` | The system LaunchDaemon set `NumberOfFiles=64` and `NumberOfProcesses=8` in both limit dictionaries. The local Apple `launchd.plist(5)` manual says these keys also change `kern.maxfiles`, `kern.maxfilesperproc`, `kern.maxproc`, and `kern.maxprocperuid`, risking host-wide exhaustion during installation. No daemon was loaded. Two guards reproduced the unsafe template and absent local limits twice as two assertion failures with zero errors. | fixed in `9f15dc01d720e1d88f0dcb71c1a3a04d55daa82b`; the template keeps CPU 60 and Core 0, while the service applies soft and hard file/process limits 64/8 through `resource.setrlimit` before constructing the runtime. Tests mock every resource call. |
| S3-R1-02 | low | `plugins/hexaemeron/skills/phylax/references/github-issue-publisher-v1.md:258`; `plugins/hexaemeron/skills/phylax/references/github-issue-publisher-v1.md:296`; `plugins/hexaemeron/skills/phylax/scripts/github_issue_publisher_lib/receipts.py:372` | The interface still named seven fixtures and four criteria after the complete manifest grew to nine and five. The memory-sink docstring also promised a production file sink that the kit does not supply, leaving receipt retention unclear. The count guard failed twice on the old text. | fixed in `9f15dc01d720e1d88f0dcb71c1a3a04d55daa82b`; counts agree with the closed code, and the reference and kit require caller retention of request and terminal output. Disconnect before the response remains an unknown outcome requiring GitHub reconciliation before another request. No persistent store was added. |

Leads not pursued: Entry `ed1611ebf21b969930b253052fcdf34b8d804719`, tree `e5a3da48f3ca0ecd3190b1fd3cec234deacec6d0`, and brief SHA-256 `47283f491d23812127a864e0c36ceda3ba0c261c5ef112413eb9c2b6521f8df7` identify this round. The full Step 3 diff over `eccc384ace4b550e1fff6e400327f404a8368262`, all 22 study risks, new verifier, daemon and shims, manifest execution, inherited admission/framing/runtime/client boundaries, public imports, release closure, package propagation, and Promise Machine evidence were reviewed. Deployment checks establish only named metadata and reviewed release bytes; live service state remains unestablished. The pinned Python 3.14.6 executable is 34,640 bytes, below the 1,048,576-byte verifier cap; its libraries remain an operator-reviewed exclusion. Source-bound Git author/signature, controller receipts and ADR assignment remain Fiat gates, not claims established by analogous hostile rows. All 11 evaluation prompts and preserved-answer tallies replay identically against Step 2 except the recomputed tree binding; no new model observation is claimed. The implementation's first aggregate failure and same-source green retry remain preserved; this round does not establish their race cause. The first new test attempt had one assertion failure and one import error; after fixing only test imports, both launchd guards failed twice without errors. Fixed focused tests pass 132/132. Fresh Hexaemeron passes 2747/2747 with zero failures, errors or skips, and the staged root gate passes 1882 tests. Phylax, Ephoros and Hypomnema exit 0; both Imprimatur passes report zero defects. Fresh conformance passes 32 cases, zero unexpected outcomes, zero #855 signer/POST attempts, exact POST/readbacks, and `live_isolation: not-established`. Exact source-bound Elenchus on the fixes commit reports `guarded`, 2747 parent tests, three assertion failures, zero errors and skips. Its report is `.hexaemeron/reports/step-3-round-1-elenchus.json`; raw reproductions, the Apple manual, and lint/verification outputs are preserved with digests under `.hexaemeron/reports/step-3-round-1-evidence/`. The original 97,886 audit bytes remain unchanged. Sapheneia preserves both findings, severities, statuses, all risk dispositions, counts, identifiers, negative evidence and limits; Vulgate changes no content. The required table and generated synopsis retain their owner formats. The final audit-only staged gate and frozen complete-product aggregate follow outside this record before handoff. This round makes no controller, later-round, publication or deployment decision.
