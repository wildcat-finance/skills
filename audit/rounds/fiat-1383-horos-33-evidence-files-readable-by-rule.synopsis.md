Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1383-horos-33-evidence-files-readable-by-rule.md | source_sha256=26d57ca77c817fc67f535e2d80ee6641427707c46a54ccf208d763cba5bfe3e9 | h2_count=3
## Step 1, round 1 -- 2026-09-17T03:11:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: maintainer-authority=not-applicable; git-resolution-parity=not-applicable; directory-split=not-applicable; near-miss-names=not-applicable; committed-boundary-compatibility=reviewed; nested-attribute-reads=not-applicable; readable-cost=not-applicable; scoped-check=not-applicable; boundary-regeneration=reviewed; pinned-files=reviewed; generation-row=reviewed; security-review-exception=reviewed; guard-capacity=not-applicable; private-target-confidentiality=reviewed<br><br>Not checked: X-Ray, solidity-auditor and fizz were neither read nor run: the `security_suite` receipt is a waiver because this run ships no Solidity. CI was not run; only local suites ran. The eight not-applicable ids concern classifier, test and fixture behaviour, and `plugins/horos/skills/`, `plugins/horos/tests/`, `plugins/horos/examples/`, `plugins/horos/docs/evidence/` and `tests/` are unchanged between `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9` and `90637afdd8a0b67d6ec643cc7b86bda53bec9200`. For `pinned-files`, `generation-row` and `security-review-exception` only the clauses step 1 can affect were reviewed; the `SKILL.md` re-pin and PM104, the generation row, `done resolve-versions` and the three conformance reports belong to later steps and integration. Phylax reads only `.py`, `requirements*.txt`, `.ts` and `.tsx`, and Ephoros only `.py`, `.ts`, `.tsx`, `.yaml` and `.yml`, so their exit 0 over this step's four `.md` and `.json` paths checked no rule. Suite runs carried `NO_COLOR=1` with `FORCE_COLOR` empty or unset, not the study's `GIT_CONFIG_*` variables that set `commit.gpgsign=false`. The confidentiality review read only the public text of issues 1356 and 1483; the private handoff was not opened. The audit-record commit's own gate run and lints postdate this record.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `plugins/horos/docs/readable-families/study.md` | Step 1's exit requires each copy to be byte-identical to its receipted artefact, and the study copy was not. The `hexctl amend study` of 2026-09-17 appended a 1,000-byte block (SHA-256 `1f71d21891be69cc958c5ed2cf356af87e33c4038c3b18eabd519ce282494df4`) to `.hexaemeron/study.md`, moving it from SHA-256 `97ba74025a6bb57b14b4405f69be6544f229b285076fd9258da1d084c1f2517e` to `10e87ac04aabccb01817f9ccb57b71343d64d23b486af92849a1d9427afd9cb4`; the copy stayed at the 50,971 pre-amendment bytes, and `cmp` reported EOF on it. Fix: the canonical bytes copied over the copy, so lines 820 to 837 are restored; `cmp` exits 0 for both copies; imprimatur on both copies (score 100.0/100, defects 0), hypomnema on the directory and protasis on the runbook copy each exit 0; `.horos/census.json` regenerated (`.md` bytes and `total_bytes` up 1,000). Elenchus on `90637afdd8a0b67d6ec643cc7b86bda53bec9200` under the step's runner contract: `unguarded`, the commit changed no test files. | fixed in 90637afdd8a0b67d6ec643cc7b86bda53bec9200 |<br>| S1-R1-02 | low | `plugins/horos/docs/readable-families/runbook.md:20-23` | The runbook copy is byte-identical to the receipted runbook (SHA-256 `b8684c4e8a3211b26184a5a2a0a89f086cc9e00deb0a7d9b2782f0c416260351`), and its first convention bullet says a bare `unittest` run inside the run worktree fails the two tests issue 1228 names. That does not reproduce: `python3 -m unittest discover -s tests` inside the run worktree ran 2017 tests OK at `5bd4bbe118e34fa505912dc76ced6edc26de2ee0` and 2017 tests OK on the fix tree. The preamble is not a step field, so `hexctl amend runbook` cannot replace it, and step 1's exit keeps the copy byte-identical. The study amendment of 2026-09-17, now in the committed study copy, records the correction and names this bullet superseded. | accepted: no repair path in this run; superseded by the study amendment |<br><br>Leads not pursued: Phylax and Ephoros pass a named path whose suffix they do not read, without opening it: `phylax.py no/such/path.md` and `ephoros.py no/such/path.json` each print `clean` and exit 0, while the same probe on a `.py` path exits 1 with P000 or E000. This round's first lint call passed the four paths as one unsplit zsh argument; Phylax and Ephoros passed it and only Hypomnema refused it (H000), so the calls were rerun with separate arguments. Not pursued: the lints are outside this step's diff. The runbook's derivation digest `97ba74025a6bb57b14b4405f69be6544f229b285076fd9258da1d084c1f2517e` names the pre-amendment study and equals the SHA-256 of the fixed study copy's first 50,971 bytes, so it is left as written. Evidence, plugin root `hexaemeron/1.6.49`: at `5bd4bbe118e34fa505912dc76ced6edc26de2ee0` (tree `d7bff09452fd1bc08ba270ba1ef7bb17d08f2779`), `horos.py check .` printed `boundary matches the tree`, exit 0; `scan . --write` and `scan . --census --write` left all three `.horos` artefacts unchanged; imprimatur on both copies, hypomnema on `plugins/horos/docs/readable-families` and protasis on the runbook copy each exited 0; the root suite inside the run worktree printed `Ran 2017 tests in 274.717s`, `OK`, exit 0; against `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9` the root boundary changes only `counts.files_walked` (3836 to 3838), no entry, and `.horos/candidates.json`, the fixture and scoped-entry boundaries and the five frozen copies under `plugins/horos/docs/evidence/` are unchanged. The fix commit's gate printed `Ran 2017 tests in 274.684s`, `OK`, recording tree `acbc88279faf0486c69dc9acebf3cab5ddd8047c`. At `90637afdd8a0b67d6ec643cc7b86bda53bec9200`: `python3 scripts/run_checks.py --base fiat/1383-horos-33-evidence-files-readable-by-rule --scope root --scope horos --format json` exited 0, outcome green, 10 of 10 checks passed, including `root-suite` (375.164 s) and `demonstrations-suite` on the runner's snapshot, snapshot removed; Phylax and Ephoros over the four changed paths and Hypomnema over the two copies each printed `clean` and exited 0, as at the step head. `plugins/horos/tests/run_tests.py` matches the runbook's command-interfaces digest `4aeff448d2c0963e15399de939f4dc29e3dab5d44b50af3cbf7b3c6b6ebbb9e4`; `.hexaemeron/study.md`, `.hexaemeron/runbook.md` and `.hexaemeron/design-evidence.json` (SHA-256 `297d62d1d270f02ec7fac097c2878225e2cbdd8becb841e3a8e7a552a56002b1`, selected `attribute-then-family`) match the brief and were not edited. Confidentiality: the only target-specific value in either copy is the rule digest `df4e446de062cd20d1575feb46951163c96fcfd7b0e378804b48da8996ab76fb` that the 2026-09-16 comment on issue 1483 states; `wildcat-finance/v2-protocol` at `c7be4039f8f383a9dda4e45f63331c17d63f9ed9` and `wildcat-finance/wildcat-app-v2` at `9b8b6d5d6db06428c5b539f267623277b65315cd` already appear in base-tree files such as `plugins/horos/docs/evidence/three-repository-marking.md`; neither copy holds a local path. The runbook copy names no concrete Horos label; at `90637afdd8a0b67d6ec643cc7b86bda53bec9200` no tracked file names either copy's SHA-256. `No reading boundary applies during security review.` is present in `plugins/horos/skills/horos/SKILL.md`, and that file, `EVOLUTION.md`, `horos.py` (with `ADOPTION_STANZA`) and `plugins/horos/tests/test_boundary.py` are unchanged. `90637afdd8a0b67d6ec643cc7b86bda53bec9200` verifies `G` with key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day. The bounded Sapheneia pass kept the heading, the five fields, all 14 risk ids and dispositions, both finding ids with severity, file and status, and every digest, commit, count, exit code and quoted string.
## Step 1, round 2 -- 2026-09-17T03:35:31Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: maintainer-authority=not-applicable; git-resolution-parity=not-applicable; directory-split=not-applicable; near-miss-names=not-applicable; committed-boundary-compatibility=reviewed; nested-attribute-reads=not-applicable; readable-cost=not-applicable; scoped-check=not-applicable; boundary-regeneration=reviewed; pinned-files=reviewed; generation-row=reviewed; security-review-exception=reviewed; guard-capacity=not-applicable; private-target-confidentiality=reviewed<br><br>Not checked: X-Ray, solidity-auditor and fizz were neither read nor run: the `security_suite` receipt is a waiver because this run ships no Solidity. CI was not run; only local suites ran. The eight not-applicable ids concern classifier, test and fixture behaviour, and `plugins/horos/skills/`, `plugins/horos/tests/`, `plugins/horos/examples/`, `plugins/horos/docs/evidence/` and `tests/` are unchanged between `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9` and `471be80adaabeebc3267a1210878ea83e7526d6d`. For `pinned-files`, `generation-row` and `security-review-exception` only the clauses step 1 can affect were reviewed; the `SKILL.md` re-pin and PM104, the generation row, `done resolve-versions` and the three conformance reports belong to later steps and integration. Phylax and Ephoros read no `.md` or `.json` file, so their exit 0 over this round's five changed paths checked no rule. Suite runs carried `NO_COLOR=1` with `FORCE_COLOR` empty or unset, not the study's `GIT_CONFIG_*` variables that set `commit.gpgsign=false`. Confidentiality was judged against the public text of issues 1356 and 1483 as fetched for round 1, not fetched again; the private handoff was not opened. This record commit's own gate run and lints postdate this record.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: none new. The Phylax and Ephoros suffix pass-through recorded in round 1 was not probed again. S1-R1-02 stays accepted: the runbook copy is unchanged at SHA-256 `b8684c4e8a3211b26184a5a2a0a89f086cc9e00deb0a7d9b2782f0c416260351`. Round 1's evidence label `hexaemeron/1.6.49` is exact: the repository's plugin is version 1.6.49, and its `phylax.py`, `ephoros.py`, `hypomnema.py`, `imprimatur.py`, `protasis.py` and `elenchus.py` are byte-identical to the plugin-root copies. Round 1's receipt matches the committed bytes: entry SHA-256 `d6f4b0dd4aef55d14e7bbcb309af0a0cc3ecb668762f79d282d28272b60f09c5`, log end offset 7652, synopsis SHA-256 `b20094f30cc0074fde2cbe8b9f9096e2a4a670a05c27beb25792e77fb604a118`, fixes commit `90637afdd8a0b67d6ec643cc7b86bda53bec9200`. Evidence at `471be80adaabeebc3267a1210878ea83e7526d6d` (tree `381ff69cb9cc798be110d1951002133a012a23be`), the audit-record commit above fix commit `90637afdd8a0b67d6ec643cc7b86bda53bec9200`: `cmp` exits 0 for both copies against `.hexaemeron/study.md` (SHA-256 `10e87ac04aabccb01817f9ccb57b71343d64d23b486af92849a1d9427afd9cb4`) and `.hexaemeron/runbook.md`; `scan . --write` and `scan . --census --write` changed nothing; `horos.py check .` printed `boundary matches the tree`, exit 0; imprimatur on both copies (score 100.0/100, defects 0 each), hypomnema on `plugins/horos/docs/readable-families` and protasis on the runbook copy each exited 0; `audit_synopsis.py --check .` exited 0 with this log at `committed=match`. Phylax and Ephoros over the five changed paths (`.horos/boundary.json`, `.horos/census.json`, this log, its synopsis and the study copy) and Hypomnema over the three `.md` paths each printed `clean` and exited 0. Against `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9`, the root boundary keeps all 134 entries and every non-count field, changing only `counts.files_walked` (3836 to 3840), and `.horos/candidates.json` is equal. Against `5bd4bbe118e34fa505912dc76ced6edc26de2ee0`, the census `.md` row gains 2 files and 16,558 bytes, which equals the study copy's 1,000 added bytes plus this log's 7,652 and its synopsis's 7,906 as committed in round 1; `total_bytes` moves by the same amount. `python3 scripts/run_checks.py --base fiat/1383-horos-33-evidence-files-readable-by-rule --scope root --scope horos --format json` exited 0, outcome green, 10 of 10 checks passed, including `root-suite` (286.879 s) and `demonstrations-suite` on the runner's snapshot, snapshot removed. The root suite inside the run worktree printed `Ran 2017 tests in 437.002s`, `OK`, exit 0; the audit-record commit's gate printed `Ran 2017 tests in 290.210s`, `OK`, recording tree `381ff69cb9cc798be110d1951002133a012a23be`. `90637afdd8a0b67d6ec643cc7b86bda53bec9200` and `471be80adaabeebc3267a1210878ea83e7526d6d` each pass `git verify-commit` and show `G` with key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day. Confidentiality: the committed log, its synopsis and both commit messages hold no local path and name no `wildcat-finance/` repository other than `wildcat-finance/v2-protocol` and `wildcat-finance/wildcat-app-v2`, which base-tree evidence files already name; the only target-related value is the rule digest `df4e446de062cd20d1575feb46951163c96fcfd7b0e378804b48da8996ab76fb` that the 2026-09-16 comment on issue 1483 states. The bounded Sapheneia pass kept the heading, the five fields, all 14 risk ids and dispositions, the exact zero-finding row, the null verdict, and every digest, commit, count, exit code and quoted string.
## Step 2, round 1 -- 2026-09-17T18:39:16Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: maintainer-authority=reviewed; git-resolution-parity=reviewed; directory-split=reviewed; near-miss-names=reviewed; committed-boundary-compatibility=reviewed; nested-attribute-reads=reviewed; readable-cost=reviewed; scoped-check=reviewed; boundary-regeneration=reviewed; pinned-files=reviewed; generation-row=reviewed; security-review-exception=reviewed; guard-capacity=reviewed; private-target-confidentiality=reviewed<br><br>Not checked: X-Ray, solidity-auditor and fizz were neither read nor run: the `security_suite` receipt is a waiver because this run ships no Solidity. CI was not run; only local suites ran. Phylax reads only `.py`, `requirements*.txt`, `.ts` and `.tsx` and Ephoros only `.py`, `.ts`, `.tsx`, `.yaml` and `.yml`, so their exit 0 checked no rule over this step's six `.json` paths, and Hypomnema's ordinary walk resolved record pointers in the three `.py` paths with no study-mode bridge run. For `pinned-files`, `generation-row` and `security-review-exception` only the clauses step 2 can affect were reviewed; the `SKILL.md` re-pin and PM104, the generation row, `done resolve-versions` and the three conformance reports belong to later steps and integration. The three conformance cells of `.hexaemeron/design-evidence.json` stay `pending` and were not resolved: they block integration, not this step. Attribute patterns outside the seven `ATTRIBUTE_CASES` were not compared with `git check-attr`; `_attribute_pattern_matches` keeps today's `fnmatch` over the scope-relative path or its basename, and `fnmatch` crosses `/` where Git's wildmatch does not. Neither classifier reads `.git/info/attributes` or `core.attributesFile`, which Git does, and `GitParityTests` drops every `GIT_*` variable but keeps `HOME`, so a global attributes file would move both sides of that comparison. The split path is exercised only by the new tests and one probe: the committed boundary and candidates carry no `split around readable` evidence. Suite runs carried `NO_COLOR=1`. Confidentiality was judged over the nine changed files; no issue was fetched. This record commit's own gate run and lints postdate this record.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | `plugins/horos/docs/readable-families/runbook.md:124` | Step 2's Exit requires that the regenerated root boundary `changes no hard entry and no boundary field`, and `counts.files_walked` moves 3840 to 3841. `counts` is one of the five names in `BOUNDARY_FIELDS`, and `files_walked` counts the tracked files the walk classified, so the step's own Files list, which the amendment of 2026-09-17 extended and which adds tracked `plugins/horos/tests/test_readable_families.py`, forces the change: no implementation of step 2 can satisfy both clauses. The classifier contributes none of it. At `93e410b9f2eebd92df74bc97633778f6cb3cda95` with that module removed from the index and the tree, `horos.py scan . --json` equals the boundary committed at `b102c6e9`, `counts.files_walked` at 3840 and all 134 entries included, so the +1 is attributable solely to the new module. The `committed-boundary-compatibility` risk row carries the same wording, and step 1 rounds 1 and 2 recorded the same field moving (3836 to 3838, then to 3840) for the copies step 1 added while marking that row reviewed. Fix: an amendment narrowing the clause to the boundary a classifier-only regeneration produces, or exempting counts attributable to files the step itself adds. Not written here: runbook amendments are the controller's to receipt. | open: runbook amendment owed, not written by this round |<br><br>Leads not pursued: A corroborated directory holding no readable file is enumerated twice, by `split_directory_entries` and again by `binding_directory_entry`; `_census_add` runs only after the readable test, so nothing is double-counted, and `benchmark_scope.py --root . --scope plugins/horos --runs 5` reports `full_tree_median_ms` 183.36 before the change, 165.864 recorded after and 168.924 on a rerun in this round, against the 1,000 budget. Split evidence embeds the corroboration sample count, so adding or removing one file under a split directory changes the evidence string of every remaining per-file entry where the aggregate changed one; the drift probe below printed that churn beside the two drift lines. For a directory a set pattern matches, the delivered behaviour is to decline the aggregate and walk it, so each file the maintainer set keeps a hard entry citing its own deciding line rather than the directory evidence plus `; split around readable <path>`; that follows the `maintainer-authority` row, and the Exit's split-evidence sentence is exercised on the corroborated case by `test_the_split_evidence_names_the_readable_file`. Neither is pursued: no clause names them and the cost is measured. Evidence, plugin root `hexaemeron/1.6.49`, at `93e410b9f2eebd92df74bc97633778f6cb3cda95` (parent `b102c6e9`, nine files, author Shoggoth, committer Dr Laurence E. Day, `git verify-commit` good with RSA key `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`): `phylax.py`, `ephoros.py` and `hypomnema.py` over `plugins/horos/skills/horos/scripts/horos.py`, `plugins/horos/tests/test_readable_families.py` and `tests/test_version_propagation.py` each printed `clean` and exited 0. `horos.py check .` printed `boundary matches the tree`. `python3 plugins/horos/tests/run_tests.py --elenchus-report <fresh path>` printed `Ran 274 tests`, `OK`, and wrote `elenchus.unittest.v1` with `testsRun` 274, `failures` 0, `errors` 0, `skipped` 0, `complete` true. The recorded `run_checks.py --base fiat/1383-horos-33-evidence-files-readable-by-rule --scope root --scope horos --format json` report is outcome green, 10 of 10 checks exit 0, base `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9`, including `root-suite` (253.9 s), `horos-suite` (7.9 s) and `lint-phylax`, `lint-ephoros` and `lint-hypomnema`. Counterfactual: the new module beside the starting classifier, blob `4f16f013034368a5f3a350cdd3e262c99a8e3f98` restored from `b102c6e9`, printed `Ran 12 tests`, `FAILED (failures=24)`, 0 errors, every failure an `AssertionError`, across 8 methods -- `test_classify_file_leaves_each_family_name_unclassified`, `test_each_family_stays_readable_beside_an_excluded_archive_part`, `test_each_family_survives_a_corroborated_served_directory`, `test_each_family_follows_an_explicit_readable_line`, `test_every_attribute_case_matches_git`, `test_the_split_directory_is_per_file_entries`, `test_the_split_evidence_names_the_readable_file` and `test_a_split_keeps_an_attribute_set_file_on_its_own_evidence` -- at or above the 4 the `guard-capacity` row and the `family-guards-failing-on-base` cell require. `.horos/candidates.json` moves 163 to 161 entries, removing exactly `plugins/alexandria/examples/compound-v3-phase0-v0/release/manifest.json` and `plugins/tabularium/examples/aave-v4-v0/events.jsonl` and adding none, with `counts.bytes_blob` 25,406,055 to 24,097,218. `.horos/census.json` moves its `.py` row 684 to 685 files and +31,308 bytes, which equals the new module's 17,278 plus `horos.py` going 49,053 to 63,083, and `total_files` 3848 to 3849. All 28 tracked paths whose basename is `manifest.json`, `statement.json`, `SHA256SUMS` or `events.jsonl` carry no hard entry and no candidate in either committed document. A scoped-drift probe over a split `out/` named drift in both directions and exited 1: `out/bundle-d.js: evidenced by the tree but missing from the boundary`, and after its removal `out/bundle-a.js: in the boundary but no longer evidenced by the tree`. `parse_attribute_states` mirrors `parse_attribute_file`'s `os.path.islink` refusal, its `ATTRIBUTES_CAP` 65536 read and its `errors="replace"` decode, and returns `[]` on `OSError`; the signatures of `parse_attribute_file`, `match_attribute_scopes`, `match_gitattributes`, `gitattributes_rules` and `classify_file` and the value of `BOUNDARY_SCHEMA` are unchanged, and the attribute evidence string keeps the format `"{attribute} for {pattern!r} in {where}"`. Against `9599fa46e8e2c5436a0dae4327c8e5b72f5159a9`, `plugins/horos/tests/test_boundary.py`, `plugins/horos/skills/horos/SKILL.md`, `plugins/horos/skills/horos/EVOLUTION.md`, `plugins/horos/examples/` and `plugins/horos/docs/evidence/`, which holds the five frozen copies, are unchanged; `ADOPTION_STANZA` is untouched and `No reading boundary applies during security review.` stands at `plugins/horos/skills/horos/SKILL.md:193`. The version rise is confined to `0.1.3` to `0.1.4` in `plugins/horos/.claude-plugin/plugin.json`, `plugins/horos/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json` and `tests/test_version_propagation.py`, one patch above the base, and no generation row was added. Confidentiality: the added lines of the nine changed files hold no URL, no local path and no `wildcat-finance/` repository name. `.hexaemeron/runbook.md` (SHA-256 `c75b2b82b8cd570fcc7f5da723c2c1d3c6428245dfa9e7bf5824482116da5e96`, byte-identical to `plugins/horos/docs/readable-families/runbook.md`), `.hexaemeron/study.md` (`173c61a93c1c88c1fe2fe8c2f0110620ad6948ccf4a2123a61427393d6fe978d`) and `.hexaemeron/design-evidence.json` (`297d62d1d270f02ec7fac097c2878225e2cbdd8becb841e3a8e7a552a56002b1`, selected `attribute-then-family`) match the brief and were not edited. The bounded Sapheneia pass kept the heading, the five fields, all 14 risk ids and dispositions, the finding id with its severity, file and status, the null verdict, and every digest, commit, count, exit code and quoted string.
