Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-329-janus-resolve-the-manifest-s-permitted-effec.md | source_sha256=bf598b65e51efd898514369a0215c709a51f79b2073fe5352930b5b266b0166e | h2_count=2
## Step 1, round 1 -- 2026-08-28T09:15:19Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-symbol-fail-closed=not-applicable; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=not-applicable; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed<br><br>Not checked: fizz is not-applicable, because the step ships no Solidity in its diff (two documentation files and one Python launcher) and the pre-existing harness Solidity is unchanged, so no invariant fuzz suite is built or refreshed this round; also not exercised are hosted CI, the controller receipt, push and publication, the run's later-step behaviour (the manifest reader, manifest-to-gate resolution, the fixtures and the ledger row, none of which exist at this commit), and the internal comparison logic of gate 2 and gate 5 beyond confirming that the shipped tests' own assertions pass. The five not-applicable register items name exactly those unbuilt later-step mechanisms; vm-signature-drift is reviewed as declaration-only with keyExistsJson deliberately still absent and existing signatures matching the canonical Foundry Vm shapes.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the step's own new code carries no finding -- the two committed spec copies are byte-identical to the receipted study and runbook at SHA-256 124b39c3e3aa09247a555321bc725c9f9eae939916957f7dbd0f6b78b09dedac and 4efdb552775803b80a985c688c5e9ff5890774239053292c0faa722f461815c7, and run_forge_tests.py spawns forge test --junit with a pinned argv, no shell, a fixed cwd resolved from the file, writes only its single report-path argument, and fails closed (returns forge's code, or 1 with no file written) when forge's stdout does not parse as XML; the exit battery is green (launcher exit 0 writing JUnit XML that names 24 passing tests with 0 failures, forge test 24 passed, python3 -m unittest discover -s tests 14 passed OK, janus.py validate exit 0). The Pashov X-Ray and solidity-auditor pair was applied over the full harness the launcher drives (X-Ray over the ten-source logical scope, twelve attacker agents over all in-scope sources); they converged on latent trails in the pre-existing, unchanged gate engine (plugins/janus/harness/src/JanusHarness.sol), none of which fires a false pass at this commit and each of which belongs to the run's later manifest-resolution steps rather than this scaffold step's file scope, recorded here so they carry forward. First and highest-signal (eight of twelve agents, concretely traced): _gate1_hookStorageWithinScopes cannot attribute a hook's writes to its own storage, because _hookAttributed excludes the host-to-hook entry frame and a hook writes its own slots with no self-call, so a write with account == hookAddr matches no attributed call and a manifest that forbids hook-scope writes would be unenforceable; it is latent today because every shipped test passes [hook] in allowedWriteAccounts and no manifest-to-gate resolution exists yet, but it is the false-pass direction the study's own over-permit-by-category register line flags for the hardest look and it wants a guard test the moment step 4 builds the write set from a manifest that omits the hook. Second, gate 1 storage compares only the written account and never StorageWriteObs.slot, so the manifest's slot-level permittedStorageWrites are enforced only to account granularity; this is a documented non-goal (study section 3 defers slot-level matching, resolution to account granularity stated openly), so it is design not defect but the second-adapter author should read it as the stated boundary. Third, _findingsJson interpolates host and manifest into the interchange JSON raw while every finding field is escaped; both callers pass constant literals today so no injection path is reachable, but the serializer should escape all five strings before any host or manifest name derived from an author-supplied manifest reaches it. Fourth, _drive passes _endRecording(0) so Delta.gasUsed is permanently zero; gate 5 is sound only because it reads the Wildcat model's lastHookGasUsed(), so the recorder's own gas field is dead and a future adapter without that accessor would get no gas gate. Fifth, _hookValueMoved sums only native-ETH call value, which is zero for an ERC20 transferFrom, so the exported value primitive reads zero for the ValueRedirectHook and gate 2 catches the theft only through the per-test balance-delta assertions layered on top; the primitive should be documented ETH-only or driven off valueSnapshot deltas before a second adapter reuses it. Sixth and minor, unverified: _deltaHasEffects counts a StaticCall as an effect so the anti-vacuous guard could be met by a read-only threshold; _endRecording drops reverted accesses so a hook that swallows a forbidden call's revert leaves no trace; and cross-action re-entry (gate 6) is caught only by the host's own reentrancy guard, not by any harness-level gate. An initial "the adapter drops caller" trail was retracted after verification, because borrower is deliberately set to the adapter in setUp so discarding caller is sound for the current single-host modeled actions, though the caller seam matters for the future second adapter. No fix is applied this round; all trails concern pre-existing harness Solidity this scaffold step does not touch and carry forward to the steps that wire the manifest into the gate sets.
## Step 2, round 1 -- 2026-08-29T08:26:15Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed<br><br>Not checked: gate-side enforcement of anything the reader resolves -- no non-test contract implements AccountResolver at this commit, and neither WildcatConformance.t.sol nor HostileHooks.t.sol imports ManifestReader, so no conformance verdict depends on the manifest and the resolved sets reach no gate. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which the schema requires and step 6 owns; slot-level write enforcement, which needs a slot dimension on the gate side; transient storage as an effect class, which the recorder does not observe; hosted CI, the controller receipt, push and publication. Coverage percentages are not reported: the forge coverage run completed, but ran while transient audit probe files were present under test/, so its per-file figures do not measure the committed tree. The bundled analyze_git_security.py ran with --src-dir src from the harness directory while git's toplevel is the monorepo root, so its path filter matched nothing and reported squashed_import with 0 source-touching commits; that is a scoping artefact and nothing is concluded from it. enumerate.sh produced no nSLOC because its counter uses GNU-only grep -P; the 1028 nSLOC figure was recomputed portably.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | high | plugins/janus/harness/src/ManifestReader.sol | _resolveCalls admitted kind "call" and kind "delegatecall" into one flat allowedCallTargets array and ResolvedThreshold carried no kind dimension, so a manifest granting a plain call to a target also admitted a delegatecall to it; a delegatecall runs the target's code in the hook's own storage context, so the shipped manifest's roleProvider.validateCredential call permit would hand the provider the hook's entire state, including the monotone known-lender bit the exit path depends on | fixed in 2b5c6dc3 |<br>| S2-R1-02 | medium | plugins/janus/harness/src/ManifestReader.sol | _thresholdByAction returned the first threshold whose action matched and stopped scanning, and neither the schema nor janus.py validate rejects a manifest naming one action twice, so array position decided which permission set was in force -- the positional dependence selection by name exists to remove; a prepended permissive duplicate silently shadowed a restrictive one and janus.py validate reported the manifest valid | fixed in 2b5c6dc3 |<br>| S2-R1-03 | low | plugins/janus/harness/src/ManifestReader.sol | _symbolOf returns the bytes before the first 0x2E, so a target or slot whose first character is "." yielded the empty account symbol, which _resolveSymbol then passed to the adapter as though it were a name; whether that failed closed was the adapter's decision rather than the reader's, and an adapter answering to the empty name admitted its address | fixed in 2b5c6dc3 |<br>| S2-R1-04 | medium | plugins/janus/harness/src/ManifestReader.sol | resolution is account-granular: _resolveStorageWrites reads entry.slot only under scope external and only for its symbol prefix, so a manifest naming lenderStatus[lender] resolves to the whole hook account, while StorageWriteObs does carry slot and the header claimed the reader "never returns a default, shrunken, or widened set" without qualification | accepted: account granularity is the non-goal step 1 round 1 already stated as its second carried trail, and enforcing slots needs a slot dimension on the gate side that belongs to the wiring step; the header comment now states the granularity boundary explicitly rather than reading stricter than the code is |<br><br>Leads not pursued: each of the six trails step 1 round 1 carried forward was judged against this diff. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, now bears directly on this step and is why S2-R1-01 is rated high: the reader builds a write set from a manifest for the first time, so a delegatecall admitted under a call permit lands its writes at the hook's own account, which gate 1's storage half can never condemn -- the two gaps compose, and 8 of the 12 attacker agents reached the composed path independently. Not fixed here: the guard belongs in JanusHarness.sol, which this step does not touch. Trail 2, account-only storage comparison, is the same boundary as S2-R1-04 and is recorded there. Trail 3, _findingsJson interpolating host and manifest raw while finding fields are escaped, still bears: 5 agents executed it, one producing the literal file {"host":"a","sequences":0,"x":"","manifest":"m","sequences":5,"findings":[...]} with two injected keys and a duplicate sequences, another showing scripts/janus.py render_markdown carries the same unescaped host into the report header so an unterminated <!-- hides the violations table; the reader does not touch that serializer and no manifest-derived string reaches it yet, so it carries forward unchanged. Trail 4, _drive passing _endRecording(0) so Delta.gasUsed is permanently 0, and trail 5, _hookValueMoved summing native value only so an ERC20 drain reads 0, both still bear and both remain in JanusHarness.sol; agents added two facts to trail 4 -- lastHookGasUsed survives an action that calls no hook (24215, then 1100, then 1100 across deposit, queueWithdrawal, executeWithdrawal) and rolls back when a hook burns 8393189 gas against a 2000000 budget then reverts, so gate 5's figure can belong to a different action. Trail 6's minor items are unchanged, and one is now contradicted: Delta.taken is written and never read, so the flag meant to stop an unrecorded threshold reading as clean is inert. Outside this diff, agents reported and this record does not pursue: WildcatHostModel.setHook has no access control and no nonReentrant, so a hook can repoint the binding every gate attributes through from inside its own callback, contradicting the shipped manifest's own liveness.uninstall claim; executeWithdrawal never decrements scaledTotalSupply, so scaledTotalSupply == sum(scaledBalanceOf) + scaledPendingWithdrawals holds through deposit, transfer and queueWithdrawal and breaks on the exit; WildcatHostAdapter.driveAction discards its caller argument, so every driven sequence runs as the adapter, which both suites set as the borrower; categoryOf classifies address(0) as RoleProvider because roleProvider is unvalidated and is address(0) in both suites; the value-returning hook callback lets a hook rewrite the host's APR and reserve ratio to (10000, 10000) with no call, no write and no value movement, passing every gate; and the shipped manifest permits a state-changing roleProvider call while enumerating no external write scope for it, so a hook using its own permitted call would fail gate 1's storage half. None of these is in ManifestReader.sol, Vm.sol or ManifestReader.t.sol. Negative results: 12 hand-written adversarial probes against the reader all confirmed fail-closed behaviour before any fix -- a staticcall entry admits nothing even under an adapter resolving every name to a live address, absent effect arrays yield empty sets rather than widened ones, an unknown call kind and an unknown storage scope both revert rather than being skipped, action matching is exact with no prefix fallback, a malformed earlier threshold denies rather than widens, a unicode symbol is not mis-split because no UTF-8 continuation byte is 0x2E, and a caller catching the revert observes no partially built set. The vm-signature-drift line is clean: the added keyExistsJson(string calldata json, string calldata key) external view returns (bool) matches forge-std 1.7.1 Vm.sol:1104 byte for byte, checked against a freshly initialised forge project rather than from memory. The Elenchus verdict is inconclusive rather than guarded for a mechanical reason: the runner contract reverts the whole source file, and because S2-R1-01's fix widens ResolvedThreshold with allowedDelegateTargets, the new guard tests no longer compile against the parent's struct, so forge exits non-zero, run_forge_tests.py writes no report by design, and the guard check cannot distinguish a compile failure from a fired guard. Each of the 6 guards was instead verified by reverting its own behavioural hunk alone; each failed without its fix -- test_delegatecall_permit_does_not_admit_a_plain_call with "the delegatecall entry is admitted as one", test_duplicate_action_name_reverts and both empty-symbol tests with "next call did not revert as expected" -- and passed with it. The fizz invariant suite was built and campaigned but is not committed, because the step's Files list is closed to ManifestReader.sol, Vm.sol and ManifestReader.t.sol; persisting it needs a runbook amendment. Its campaigns ran twice against real engines: before the fix, Echidna 2.3.3 at 100040 calls with 6 properties passing and Medusa 1.5.1 at 132622 calls with 16 of 16 passing; after the fix, with 2 properties added for the call-kind and empty-symbol guards, Echidna at 100037 calls with 7 of 7 passing and Medusa at 184966 calls with 18 of 18 passing. The pre-fix campaign did not catch S2-R1-01, which is a coverage limitation rather than a clean run: the property asserted that admitted targets equalled the call plus delegatecall count, which is exactly what the unfixed code did, so the property encoded the defect instead of falsifying it. X-Ray ran as a named full recomputation (plan mode full, reason cache-missing, no prior cache existed) over the exact eleven-source current inventory at candidate digest 3cfee210c00fb8ddb1334ff4bf2705d615419daa0a667038ec7bba736581ab8c, with all 4 outputs regenerated, bound and promoted; its working material is operator-owned and stays out of this record and the tree. The solidity-auditor pass ran all 12 attacker roles to completion over the 11 in-scope sources; deduplication covered 31 unique (contract, function) pairs from the raw results, every one of which survives into this record or into the trails above. Suites at exit: forge test 42 passed 0 failed (36 at entry, the 12 ManifestReader tests plus 6 new guards), python3 -m unittest discover -s tests under plugins/janus 14 passed OK, the repository invariant suite 460 passed OK after regenerating the portable Promise Machine runtime and the Horos boundary, which mirror the two changed files by digest, and janus.py validate exit 0 on the shipped manifest.
