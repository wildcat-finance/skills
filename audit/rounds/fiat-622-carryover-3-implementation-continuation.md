## Step 1, round 1 -- 2026-08-27T11:32:38Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=not-applicable; evidence-overclaim=reviewed

Not checked: GitHub-side signature verification, hosted CI, remotes, issues, pull requests and controller mutation because none was authorised or performed; live non-macOS, cgroup v1 and cgroup v2 execution, although bounded synthetic fixtures exercise those capacity paths; forced live PID reuse and cleanup of arbitrary descendants detached from the original worker process group; Steps 2-3 impact selection, shared global execution and ordered command groups because Step 1 does not implement or claim them; a serial parity leg and an automatic-capacity leg because this reconstruction left scheduler partitioning, fixture domains, worker execution, aggregate accounting and capacity selection byte-identical to the audited round-8 postimage, so neither amendment trigger occurred; and the identities of the five fixture-blocked tests, which the runner's exact accounting proves are covered by one declared fixture skip holder. The inherited `plugins/hexaemeron/tests/test_issue_429_release.py` `test_root_suite_jobs_need_no_repository_history` defect fails in exactly three subTests (`.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue) on the exact unmodified parent and on the branch alike; it sits outside the 27-path union and outside #622 scope, is reported upstream rather than fixed, hidden or waived here, and is not a finding of this round. The Solidity security suite was waived because this tree changes no Solidity.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: This fresh independent Warden round cold-read the complete accumulated diff from recorded base `b245d68e7e8c9d07b0dbbaa67e57b05cd00b18ef` (tree `2a213533fa33dda1f84dfbbb6c0d23abf78ff81d`) through the single signed step commit `a5017d52dd210d8f3433db5e5cbb892f0d0ea1c7` (tree `8d68c166b641bc51116d29b6979706632f0ecf64`) and every cumulative trust boundary named by the effective Step-1 packet; it did not rely on any prior Warden's conclusions. The step commit verifies with a good signature from Shoggoth signing subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A` of primary fingerprint `636EC19DE45DF10F3CE6206F57742DA1ABED6F46`, and each required provenance trailer occurs exactly once. The downloaded attachments retain the published hashes: packet SHA-256 `615cbef52cd6436db8ddbf445ffa68d12e20beb3fffa4de18b17fcfe13724d62` and patch SHA-256 `c5d8074f06121a56f441329b228c51f21add9e530bf02f13ecd51a7c2b4fc5f8`. The independent postimage reference worktree write-tree equals the signed evidence tree `8ed805f1546e525469a923217dd85798294e48a6` of commit `a60e688d7d4dacccf6eb5be8c88a8444e620b7f8`, and all 27 packet inventory digests match it exactly. Twenty of the 27 reconstructed paths are byte-identical to that postimage, including all three immutable audit histories, their deterministic synopses, the imported study/runbook copies, the runner, all guard surfaces and the signing isolation files; the seven divergent paths are exactly the declared transforms: the live scheduling decision physically renamed from the packet's ADR-037 path to `docs/decisions/ADR-038-select-and-schedule-repository-checks-from-one-graph.md` with only its title line changed, ADR-037-to-ADR-038 reference substitutions inside the replacement study and runbook copies and nothing else, the bootstrap identity rebind of `scripts/verify_issue_622_inoculation.py` and `tests/fixtures/issue-622-inoculation-v1.json`, the matching 7-to-9 additional-path count in `plugins/hexaemeron/tests/test_issue_622_inoculation.py`, and a single reviewed reporter-digest refresh in `tests/promise_machine_coverage.json` binding `plugins/hexaemeron/tests/run_tests.py` at `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f`, which is the only current-main path overlap and the only line changed against current main. In the rebound machine record the findings and families blocks are byte-identical to the round-8-verified postimage record, all 60 cause identities and guard sets are unchanged with exactly one owner path renamed by the declared transform, all 18 logical paths persist with only the ADR target and coverage digests updated, and the sole transform is `adr-035-selector-to-adr-038`; the rebound verifier adapts its packet parser to the Carryover-3 format, compiles the carried 23-finding map in the checked trust root because the packet delegates row-level evidence to the digest-pinned immutable audit sources, cross-checks every packet-declared count, extends the union by the Carryover-2 continuation audit log and synopsis, and weakens no check. The first product command reported exactly: `issue-622 inoculation verified: base b245d68e7e8c9d07b0dbbaa67e57b05cd00b18ef; 18 logical paths; 18 current targets; 1 transform; 23 inherited findings; 60 current cause guards; 101 unique runnable guards; 13 families; 27 cumulative paths`. The fresh comprehensive `--jobs 12` run discovered 1,506 unique tests from current bytes with manifest digest `390bf53887d80ab4462c232b52adf86a3907189f9bd0249005f0aa1fd1a9473c`, assigned all 1,506 identities as an exact disjoint union over 1,218 atomic fixture domains, ran 1,501 cases once with one ordinary skip and five proven fixture-blocked identities, and reported exact accounting with exactly three failures, zero errors, zero unexpected successes and zero scheduler errors; queue high water equalled the explicit budget of twelve, and the timing cache contributed 1,501 exact-ID hints plus five neutral entries with zero corrupt entries and balance-only authority. The recorded parent's own runner predates `--jobs`, so parent evidence was taken in the parent's own form: the serial controller suite ran 1,373 tests with 1,370 passing, one skip and exactly the same three subTest failures and zero errors, while the parent root (402 tests), Horos (228 tests) and Hermes suites all passed, as did the same three suites on the branch, so the branch comprehensive failure set equals the exact-parent failure set and both equal the three inherited subTests and nothing else. Cold review confirmed the fixture-repository signing changes disable signing only inside disposable test repositories via local config and command-scope `-c commit.gpgsign=false`, touching no production signing policy, while the hostile signing matrix ran green; spot review of `write_report` (exclusive `O_EXCL|O_NOFOLLOW` creation through a bound worktree identity), `frame_test_output` (reserved structured-event prefix escaping) and `capacity_plan` (minimum positive usable signal, conservative reserve, cap by runnable work, reported inputs) matched the register's mitigations, and the 101 runnable guards all executed green in the fresh run. Source-bound Phylax and Ephoros over all 29 changed paths and changed-document Hypomnema over the eleven changed documents each exited zero; Imprimatur scored 100 with zero defects on the replacement study, replacement runbook and ADR-038; the committed Horos boundary and candidates match a fresh deterministic classification and `horos check` exited zero; `git diff --check` exited zero. The whole-tree diff names only the 27-path union and the two regenerated `.horos` records, touching no workflow, plugin version, held frontier, unrelated current-main feature or issue #643 implementation. No fixes commit exists, so this round carries no Elenchus verdict. The exact audit declaration is `--audit-filter sapheneia:sapheneia`. Controller state identity is `19274a1bd4932a35f2ddc0ba3a82ab9ecfc2667b01bf2a4f65b22bb587a82f77`; the effective Step-1 identity is `d0752e266ed7d2376afe9e43f4a09d44719f15f89d0dfb6059a82370ae9bdc7d` over runbook SHA-256 `c4162410be5fb2f22f5915ad4ba15d81a7de85e382b03cb7facfc3c5921c0997` and study SHA-256 `158640ee7cac9fcc1d16fc8f2ae330b0d76db9a071fc5acf908364dfadbd3ad0`. No remote, GitHub object or controller state was mutated. This zero-finding round asserts the declared suite executed and returned nothing.

## Step 2, round 1 -- 2026-08-27T12:59:48Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the 27-path scope ships no Solidity; hosted CI, remotes, issues, pull requests and controller mutation, none of which was authorised or performed; live non-macOS, cgroup v1 and cgroup v2 capacity paths, exercised only through `_cgroup_quota` file fixtures on a Darwin 25.5.0 host with 18 CPUs; forced live PID reuse; cleanup of a descendant that leaves its process group and creates a new session, which S2-R1-01 now classifies as `scheduler-error` without claiming to have terminated it; the `pandects-forge-build`/`pandects-forge-test` ordered group against a real `forge`, absent from PATH here, so only the `map-ordered-group.json` and `map-failing-group.json` fixtures exercise ordering; and the sixteen omitted suites, which this step's own diff does not select. The inherited `plugins/hexaemeron/tests/test_issue_429_release.py` `test_root_suite_jobs_need_no_repository_history` defect (`1f2298b2`) fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue -- and I reproduced it independently on the unmodified parent `dfb4b8db99997160539f4f2bd27be22254ad0056` in a separate worktree. It is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | scripts/run_checks.py | `run_check` tested its deadline only after a blocking `proc.stdout.read(65_536)`, which returns at 64 KiB or at EOF of every writer, so `timeout_seconds` bounded nothing: a quiet check with a 2 s timeout ran 25.0 s, and one whose descendant retained the write end ran 60.0 s and was recorded `status: passed`, `duration_seconds: 60.02`. `proc.kill()` also reached only the leader despite `start_new_session=True`. ADR-038 requires a retained descriptor to be a bounded red result with a detachment diagnostic, outside the green state. Fixed by `_capture_output` polling the descriptor so the deadline holds independently of child output, `_terminate_group` signalling the session, and a `scheduler-error` classification after the bounded drain. Now 2.0 s and 5.3 s respectively. | fixed |
| S2-R1-02 | high | scripts/run_checks.py | A worker raising anything but `OSError` died with its thread, dropping the check from `results`, and `execute` never compared completion against selection, so a selected check vanished while the run aggregated `green` and exited 0. Reproduced through the CLI: selected `['poisoned','real']`, recorded `['real']`, `outcome=green`, exit 0. `scheduler-error` was declared in `FAILURE_CLASSES` and emitted nowhere. This is the register's `assignment-loss` risk with its stated mitigation absent. Fixed by guarding each worker, classifying a fault as `scheduler-error`, and proving the disjoint union of selection and completion before any outcome is read. | fixed |
| S2-R1-03 | medium | scripts/run_checks.py | `jobs_flag` and `requires_executable` were read with no type validation and `order`/`timeout_seconds` coerced by bare `int()`. `"jobs_flag": 7` reached `subprocess.Popen`, raising `TypeError` into S2-R1-02 and returning green at exit 0; `"timeout_seconds": "soon"` escaped `main()` as a `ValueError` traceback with empty stdout at exit 1 instead of the named `invalid-plan` refusal at exit 2 that every other malformed-map case produces. Fixed by `_optional_string` and `_bounded_int`; all six malformed bodies now refuse as `map-invalid`. | fixed |
| S2-R1-04 | medium | scripts/run_checks.py | `_safe_relpath` is lexical and could not see a symlinked path component, so `write_report` created the parents and `os.replace`d through it, landing the report outside the repository; `O_NOFOLLOW` guarded only the final temporary, and a symlinked final component was already safe because rename replaces the link. The step exit requires a confined repository-relative report path, and the committed test covered only `../escape.json`, `/etc/passwd`, `.git/config` and `a/../../b.json`. Fixed by `confine_report_path` refusing a symlinked component before execution and `write_report` re-checking that the resolved parent is inside the resolved root. | fixed |
| S2-R1-05 | medium | .gitignore | The step's own Elenchus runner contract writes `.elenchus/step-2.json`, a path neither ignored nor owned by `tests/check-map-v1.json`. It entered `changed_paths` as a relevant untracked path with no owner, so `python3 scripts/run_checks.py --scope hexaemeron --plan --format json` -- a command this step's exit requires to exit zero -- refused `unknown-ownership` at exit 2 once Elenchus had run once. `test_every_tracked_path_has_exactly_one_owner` enumerates `git ls-files` only, so tracked-path totality could not see it. Fixed by ignoring `/.elenchus/` as generated evidence, with a companion test proving ownership over the relevant untracked paths the planner actually reads. | fixed |

Leads not pursued: This round cold-read the whole of `253227c37393f266eb9cc2e59c800df2bc61c8b2` (tree `19852df2e3fe78f188b53d18f290487ce8e5ce58`) against parent `dfb4b8db99997160539f4f2bd27be22254ad0056` without relying on the implementer's reasoning; the packet's effective step digest read `eadc46df27d35a930ab6cef8e2a981f44d0355d808b9afa33c352dd638c4f2e2` over study `158640ee7cac9fcc1d16fc8f2ae330b0d76db9a071fc5acf908364dfadbd3ad0`, and controller state read `37dacd6dcfb142496fb47ae9a4f5dbef2d2b6a84a92c78aa2c65a5e8e70c2e41`. Every finding retains a regression proved red on the unmodified step commit: reverting only `scripts/run_checks.py` and `.gitignore` left 10 failures and 4 errors across the 11 new methods, and all pass at fixes commit `517f0e9cc79ced71f67ddd9a433ab53b4fce0d81` (tree `3cd4423eae608aacbb365f3c54a71784fb75f16b`), signed by subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A` with each provenance trailer once. Suite parity was measured independently rather than accepted: parent 1501 tests, 3 failures, 1 skip, 0 errors; step commit 1559, 3, 1, 0; fixes commit 1570, 3, 1, 0 at exit 1, the same three inherited subTests throughout and no new failure identity. `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each exit 0; `root-suite` 402 passed; `imprimatur-suite` 112/112; `test_check_runner` 69 passed with `ResourceWarning` promoted to error. Re-deriving the plan from this round's own diff selects exactly `hexaemeron-suite`, `imprimatur-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax`, `root-suite` and omits sixteen at budget 18, matching the step's claim, and all six ran. Negative results worth recording: `make_snapshot` runs `git checkout --detach` with `check=False`, so a failure is swallowed, but I could not construct a divergent snapshot because `git clone --local` copies the whole object directory including unreachable commits, and my orphaned-HEAD probe still matched -- unproven, not disproven, and left alone. `is_runner_owned` cannot hide a real changed path today: `/tmp/` is genuinely gitignored, no tracked path lives under `tmp/`, and `test_every_tracked_path_has_exactly_one_owner` would fail if one appeared. Longest-prefix ownership is total and unambiguous over tracked paths: duplicate owner paths refuse `ambiguous-ownership`, matching is on `/` boundaries so `docs` cannot claim `docsx`, and `scripts/promise_machine.py` correctly beats `scripts`. The global slot bound holds by construction rather than by `_allocation_for`, since `Scheduler.acquire` waits on `_in_use + slots <= budget` under one `Condition` and clamps an over-budget request instead of deadlocking, so `budget // 2` is a heuristic that cannot breach the high-water mark; the bound on live children remains conditional on a nested runner honouring `--jobs`, which only `hexaemeron-suite` declares. `unstable-source` is reachable and distinguished from a test failure: the committed tests drive the real CLI in a disposable repository to exit 3 with `failure_classes == ["unstable-source"]` and no `test-failure`. Three leads were seen and not pursued. First, `wildcat.check-run.v1` binds source, reasons, capacity, queue, supersession and distinct failure classes but not shards or cache, two of the eight the step's ephoros discipline names; both are interior to the nested runner, which emits its own `HEXAEMERON-RUN` record, and re-exporting them is a design change beyond an audit fix. Second, `refuse_stale_commands` proves only that a `script` path exists, so a `unittest discover` check over a directory emptied of tests would exit 0 vacuously while the map still validated; no declared check is vacuous today, all twelve discover directories holding between 11 and 689 tests. Third, `resolve_base` interpolates the operator's `--base` into `base + "^{commit}"` without a `--` separator, which I could not turn into argument injection because only the resolved SHA reaches `git diff`. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence that the pass was applied and does not make the controller proof of this record's semantics. Because this round carries findings, the register's `audit-recursion` mitigation applies: each is fixed at cause with a new guard, and another independent full round is required. No remote, GitHub object or controller state was mutated.

## Step 2, round 2 -- 2026-08-27T13:26:53Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the 27-path scope ships no Solidity; hosted CI, remotes, issues, pull requests and controller mutation, none of which was authorised or performed; live non-macOS, cgroup v1 and cgroup v2 capacity paths, exercised only through `_cgroup_quota` file fixtures on a Darwin 25.5.0 host with 18 CPUs; forced live PID reuse, which S2-R2-01 models by substituting `os.getpgid` rather than by racing the kernel allocator, so the fix is proved by the absence of the signal rather than by an observed collision; the `pandects-forge-build`/`pandects-forge-test` ordered group against a real `forge`, absent from PATH here, so ordering is exercised only through fixture maps; the residual window between `write_report`'s resolved-parent re-check and its `os.open`, which no test closes; and the sixteen omitted suites, which this step's own diff does not select. The inherited `plugins/hexaemeron/tests/test_issue_429_release.py` `test_root_suite_jobs_need_no_repository_history` defect (`1f2298b2`) fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue -- and I reproduced it independently on the unmodified parent `dfb4b8db99997160539f4f2bd27be22254ad0056` in a separate worktree before reading any claim about it. It is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | high | scripts/run_checks.py | The descendant-lifetime guard added by S2-R1-01 signals a process group derived from a pid the runner has already reaped. `_capture_output` calls `proc.poll()`, and `_terminate_group` calls `proc.wait()`, both of which reap the leader and return its pid to the kernel for reuse; `run_check` then waits out the full `DRAIN_SECONDS = 5.0` drain before calling `_terminate_group(proc)` again on the retained-descriptor path, where the unconditional `os.killpg(os.getpgid(proc.pid), sig)` can resolve an unrelated live process and deliver SIGTERM then SIGKILL to a stranger's process group. Measured on a real `setsid` descendant holding the write end: `_terminate_group` entered with `returncode=0` -- already reaped -- and called `os.getpgid(83280)` 5.3 s later; that pid was not recycled on the run, so the call raised ESRCH and the harm did not land, which is exactly why round 1's own regression passes. Substituting a `getpgid` that resolves, as a recycled pid would, produced `killpg(999999, SIGTERM)`. Fixed by returning a disposition string, refusing to signal at all when `proc.returncode is not None`, and resolving the group once from a leader the runner still owns; the retained-descriptor verdict stays a bounded `scheduler-error` and the record now carries `termination`. | fixed |
| S2-R2-02 | medium | scripts/run_checks.py | `execute` sequences ordered-group members by the optional `order` integer and never reads `check_map.groups`, so the mandatory `ordered` list that `load_map` validates is discarded after validation. `order` defaults to `0` through `_bounded_int(body, "order", 0)`, so a group that declares its sequence only in `ordered` -- the documented declaration -- runs in check-id order instead, because `selected_checks` sorts by id and the stable sort preserves that. Reproduced through the real pipeline: a group declaring `ordered: ["zeta-setup", "alpha-apply"]` executed `alpha-apply` first, inverting the declaration with nothing refusing. `load_map` also accepted an `order` field contradicting `ordered`, and accepted a check declaring `group: chain` while absent from that group's `ordered` list, which joined the group at execution with the default position. The repository map is unaffected today because `pandects-forge-build` and `pandects-forge-test` carry `order` 1 and 2 agreeing with both `ordered` and their id order, and the committed `map-ordered-group.json` fixture has the same triple coincidence, so `test_ordered_group_runs_in_declared_order` passed without discriminating. This is the register's `ordered-breakage` risk with its explicit representation present in the map and absent from execution. Fixed by making `ordered` authoritative: `load_map` refuses `ordered-conflict` when a restated `order` contradicts the declared sequence, refuses `map-invalid` for a duplicated or omitted member, and rewrites each member's `order` to its declared index. | fixed |
| S2-R2-03 | medium | scripts/run_checks.py | Git capture raises `PlanError`, and two call sites sit outside every handler for it. `MAX_GIT_OUTPUT_BYTES = 8_388_608` bounds `git()`, so a working diff over 8 MiB raises `git-oversized` from `source_identity`, which `main` calls at the top of the attempt loop and again after `execute` -- both outside the `try` that ends at the `confine_report_path` call. The exception escapes `main` as a traceback and `sys.exit(main())` yields exit code 1, the same code a red run uses, so a caller cannot distinguish a failed check from a runner that never started one, and no `wildcat.check-run.v1` refusal record is emitted at all. Inside `make_snapshot` the same `PlanError` -- from `git(root, "diff", "HEAD")` -- is not converted, and the caller catches only `SnapshotError`, so `remove_snapshot` never runs: verified that the owned parent directory and its `.check-runner-owned` sentinel both remained on disk, leaking a full `--no-hardlinks` clone per invocation and breaking the step's stated owned-sentinel cleanup. Fixed by converting `PlanError` to `SnapshotError` inside `make_snapshot` and by moving the attempt loop into `_run_attempts` under a `PlanError` handler that refuses at exit 2 by name. | fixed |
| S2-R2-04 | low | scripts/run_checks.py | `wildcat.check-run.v1` binds no cache disposition, one of the eight bindings the step's ephoros discipline names, and the absence is not self-evidently vacuous: the composed run invokes `hexaemeron-suite`, whose own record reports a `wildcat.test-timings.v1` cache with `status`, `hits` and `corrupt_entries`, and that record survives only as captured stdout inside a `CaptureBuffer` that truncates past 64 KiB of head and 64 KiB of tail. A reader of the run record therefore cannot tell whether this runner consulted a cache. The module docstring's claim that "timing history may balance work" also describes no behaviour in this file, which reads no cache of any kind. Round 1 saw this and judged re-exporting the nested runner's fields a design change; that reasoning holds for shards, which are already bound per check as `slots` and `nested_allocation`, but stating this runner's own disposition is one factual field, not a design change. Fixed by binding `cache` with `result_cache: none`, `selection_input: false` and a detail naming the nested runner's record as the authority for its own cache. | fixed |

Leads not pursued: This round cold-read `253227c37393f266eb9cc2e59c800df2bc61c8b2` (tree `19852df2e3fe78f188b53d18f290487ce8e5ce58`) and `517f0e9cc79ced71f67ddd9a433ab53b4fce0d81` (tree `3cd4423eae608aacbb365f3c54a71784fb75f16b`) against parent `dfb4b8db99997160539f4f2bd27be22254ad0056` (tree `feff3a8034af4f8ad2eb22ab54db36bfa59def11`) without accepting round 1's reasoning, its findings or its claims about its own fixes; the packet's effective step digest read `eadc46df27d35a930ab6cef8e2a981f44d0355d808b9afa33c352dd638c4f2e2` over study `158640ee7cac9fcc1d16fc8f2ae330b0d76db9a071fc5acf908364dfadbd3ad0` and runbook `caa9c3c612ed594224d0e3a226d7d5d708dc7595d62d4876129c760f2b57899d`, matching the delegated brief, and controller state read `fdb4d344a93bb1b51dd82ffb8c8f1237adaa9c7d9654d7138539fe0014071d02`. Suite parity was measured independently rather than accepted: parent 1501 tests, 3 failures, 0 errors; round 1 head 1570, 3, 0; this round's fixes 1580, 3, 0, all at exit 1 with the same three inherited subTest identities and no new failure. `test_check_runner` moved 69 to 79. `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each exit 0 both before and after the fixes, and all four commands the step's exit requires exit 0, with `--full` selecting 22 checks and omitting 0 and `--scope hexaemeron` selecting 6 and omitting 16. Every finding retains a regression proved red on round 1's head `7c0ca2c` (tree `830e2c24c9bca03e56712606577d1fee14c3717f`) with only the test file carried across: 6 failures and 2 errors across the 8 new discriminating methods, all green here. Two of the new methods do not discriminate and are recorded as consistency guards, not as parent-red evidence: `test_the_runner_reads_no_cache_at_all` and `test_the_repository_map_group_still_loads_and_agrees` pass on both trees. One draft regression passed on the parent for the wrong reason before it was retargeted -- shrinking `MAX_GIT_OUTPUT_BYTES` refuses during selection, inside `main`'s covered region, and never reaches the attempt loop -- and one bypassed `selected_checks`, so the stable sort preserved dict order and hid the inversion; both are noted because a guard that passes its own test proves nothing. Round 1's other three fixes were re-verified and stand: a symlinked report component refuses `unsafe-path` at both `confine_report_path` and `write_report`, nothing lands outside the tree, and a symlinked final component is replaced rather than followed because rename does not follow the last link; `/.elenchus/` is ignored at `.gitignore:48` and `--scope hexaemeron --plan` still exits 0 after Elenchus has written its report; the malformed-map refusals hold. Four negative results are worth recording. First, S2-R1-02's guard proves coverage, not the disjoint union it claims: `recorded = {r["check"] for r in results}` is a set, so a duplicate or foreign record would not be detected, and I could not construct one because each check reaches exactly one of `run_single` or `run_group`. Second, `make_snapshot` still runs `git checkout --detach` with `check=False`, and I could not construct a divergent snapshot either: a local clone from a detached-HEAD source lands on the same commit, and so does a clone of a linked worktree whose HEAD commit is reachable from no branch, which is this run's own shape -- `.git` here is a file pointing at `/Users/c0rtexzer0/622/.git/worktrees/`. Unproven, not disproven, and left alone. Third, `resolve_base` interpolates `--base` into `base + "^{commit}"` with no `--` separator, and I could not turn it into argument injection: every option-shaped base I tried, including `--git-dir=`, `--output=`, `--prefix=` and `--parseopt`, is neutralised because the concatenated suffix makes the token an unrecognised option that git then treats as a revision, which fails to resolve under `--verify --quiet`. The guard is incidental rather than intentional, and `--base` is operator-supplied. Fourth, one nested check exceeds its slot accounting by the coordinator process itself, since the parent holds `granted` slots while the nested runner spawns `granted` children plus its own process. Two of round 1's three recorded leads are decided here: the cache half of the ephoros lead became S2-R2-04, while the shards half is not a finding because `slots` and `nested_allocation` already bind it per check; and the `refuse_stale_commands` vacuity lead is not a finding, because the step's exit binds refusal to stale commands, unsafe paths, cycles, duplicate keys and ambiguous ownership rather than to a minimum test count, and a check whose script exists and runs correctly is not a stale command -- 13 of the 22 declared checks are `unittest discover`, none vacuous today, and a minimum-count assertion would be a new requirement rather than an audit fix. Two leads are left open for the next round: whether a descendant that detaches and also closes stdout should be bounded at all, since it is invisible to the retained-descriptor disposition and can outlive `remove_snapshot` while writing into the tree being deleted; and whether `--full` should still tolerate an unowned path, which it does today without refusing. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence that the pass was applied and does not make the controller proof of this record's semantics. Because this round carries findings, the register's `audit-recursion` mitigation applies: each is fixed at cause with a new guard, and another independent full round is required. No remote, GitHub object or controller state was mutated.

## Step 2, round 3 -- 2026-08-27T14:07:22Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; hosted CI, remotes, issues, pull requests and controller mutation, none of which was authorised or performed; a portable containment backend on Linux or Windows for a descendant that leaves its session and closes every inherited descriptor, because the available standard-library process-group handle does not survive that transition and the live Darwin result is S2-R3-01; live cgroup v1 and cgroup v2 hosts, whose membership, mountinfo and ancestor-minimum paths are exercised by bounded synthetic fixtures; the real Pandects Forge group, because `forge` is absent from PATH; the residual race between `write_report`'s resolved-parent check and its final open; a kernel PID-reuse race beyond round 2's substitution guard; and hosted observation of a nested coordinator plus its granted children. The inherited `plugins/hexaemeron/tests/test_issue_429_release.py` `test_root_suite_jobs_need_no_repository_history` defect (`1f2298b2`) fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue -- and I independently reproduced the exact set on unmodified parent `dfb4b8db99997160539f4f2bd27be22254ad0056`. It is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | high | scripts/run_checks.py | A descendant can fork, call `setsid`, open a different descriptor into the attempt snapshot, close stdin/stdout/stderr and outlive both its leader and `remove_snapshot`. On Darwin 25.5.0 with Python 3.14.6, the leader returned and `run_check` reported `passed` in 0.017 s with `descriptor_retained=false` and `termination=not-required`; `remove_snapshot` returned `removed`, but the detached child remained alive and its already-open `survivor.log` grew from 24,576 to 147,456 bytes during the next 250 ms. The specimen was then killed and proved absent. Retained-output detection cannot see this process, and a process-group signal cannot reach it after `setsid`; no portable containment handle available to this implementation establishes the Step-2 promise that all started work drains. | open -- blocks Step 2 exit; do not narrow the runbook or claim a descriptor heuristic as containment |
| S2-R3-02 | medium | scripts/run_checks.py | `build_selection` refused unknown ownership only when `full` was false, so `--full` silently waived the map's total-ownership contract. A relevant path with no owner could therefore enter a full plan without the pre-execution refusal required by the step. Fixed by applying `unknown-ownership` independently of selection breadth; `test_full_refuses_an_unowned_path` is parent-red. | fixed |
| S2-R3-03 | low | scripts/run_checks.py | `make_snapshot` discarded the return code from `git checkout --detach`. Prior Wardens and this round could not construct a divergent snapshot because the local clone already lands at source HEAD -- exploitation on the preimage remains unproven, not disproven -- but a failed command was nevertheless accepted without evidence that the immutable snapshot held the requested commit. Fixed by refusing a nonzero checkout and comparing the snapshot's resolved HEAD with the source commit; `test_failed_detached_checkout_refuses_the_snapshot` forces the command failure and is parent-red. | fixed |
| S2-R3-04 | medium | scripts/run_checks.py | `_copy_untracked` silently skipped every symlink, FIFO, socket and other special entry, while `source_identity` hashed only the pathname of an untracked symlink and not its target. The attempt could therefore omit relevant working-tree state or miss a target change while claiming a complete immutable snapshot. Fixed by bounded no-follow regular-file reads and exclusive writes, faithful copying of relative in-repository symlinks, target/type/content identity binding, explicit refusal of escaping links and non-ignored special files, and a bounded filesystem pass for special objects Git omits; ignored special objects remain outside the snapshot. Safe-link copy, target movement, escaping-link, special-refusal and ignored-special guards cover the boundary, with the first four parent-red where they discriminate. | fixed |
| S2-R3-05 | medium | scripts/run_checks.py | Automatic capacity chose affinity or `os.cpu_count`, sampled only fixed cgroup root files, reserved no headroom and did not cap ordinary work by runnable units. On this 18-CPU host it reported 18 slots despite the study's conservative-headroom contract, and a process in a nested cgroup could miss a stricter ancestor quota. Fixed by bounded `/proc/self/cgroup` and mountinfo parsing for v1/v2 membership, minimum positive quota across the mounted ancestry, minimum positive host signal, a one-third conservative reserve, the safety ceiling and a runnable-work cap; the current plan reports usable 18, reserve 6 and effective budget 12. The ancestor and headroom guards are parent-red. | fixed |

Leads not pursued: This independent Warden cold-read the complete stacked Step-2 tree from parent `dfb4b8db99997160539f4f2bd27be22254ad0056` through pre-round head `a1222f9c7831cf0ae6b0031497121f4a4cd6e9b1` (tree `fb08161e87660373db6065f7ba5abf575dbdc509`) and did not accept either prior round's reasoning as proof. Controller `next` was re-read from inside the location-bound worktree and matched step 2, round 3, Warden and state SHA-256 `06c7ee206386b5de25b9ac4d089219fe65b911b710975738aef41ce5a58f3318`; no controller receipt was called. The fresh source-bound runner target was deleted and proved absent before execution, then `plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/step-2.json` wrote a complete `elenchus.unittest.v1` report with 1,589 tests run, three failures, one skip, zero errors, zero expected failures and zero unexpected successes at exit 1. The same suite on the exact parent, in a separate detached worktree created and removed inside the run's ignored area, ran 1,501 tests with the same three failure events and zero errors at exit 1; there is no additional failure identity. The focused runner has 88 green tests. Both required JSON plan commands exit 0: `--full` selects all 22 checks and omits none, while `--scope hexaemeron` selects six and omits sixteen; both report automatic usable 18, reserve 6 and effective budget 12. Phylax and Ephoros over `scripts/run_checks.py` and `plugins/hexaemeron/tests/test_check_runner.py` each exit 0; changed-document Hypomnema, final audit-document Hypomnema and `git diff --check` exit 0. The four actionable causes retain causal guards, and the checkout lead's old exploit status remains explicitly unproven rather than rewritten as disproved. Negative space remains visible. `execute` still proves only missing records with a set and does not explicitly reject a foreign or duplicate terminal record; no construction reaches one. `resolve_base` still relies on the `^{commit}` suffix rather than an explicit option boundary; prior option-shaped probes did not inject an argument. A nested runner's coordinator process is still additional to its granted child slots. `remove_snapshot` reports `not-owned` or `retained` but that disposition does not itself make an otherwise green run red; the study requires preservation and visibility, which the record supplies, while the stricter outcome policy remains unsettled. A bounded malformed-map probe also showed that `refuse_stale_commands` skips a non-dot `cwd` when a check has no `script`; the two current command-only Pandects entries point to an existing directory, and whether a missing cwd must be promoted from execution-time `command-failure` to pre-execution `stale-command` remains for a later independent round. The report-path replacement window remains open. None of these observations is used to soften S2-R3-01: without real containment the effective Step-2 exit is not met. The durable record was shaped item by item under Sapheneia without dropping the exact reproduction, severities, statuses, inherited-failure boundary, uncertainty or leads. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. Because this round carries findings and one remains open, `audit-recursion` requires another independent disposition before closure. No remote, GitHub object or controller state was mutated.

## Step 2, round 4 -- 2026-08-27T14:54:34Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; hosted CI, remotes, issues, pull requests and controller mutation, none of which was authorised or performed; live Linux cgroup-v2 containment, because this Warden ran on Darwin 25.5.0 and the bounded cgroup candidate used synthetic file and call seams only; Windows containment; an adversarial same-credential process deliberately migrating itself through a writable cgroup ancestor; the real Pandects Forge group, because `forge` is absent from PATH; a kernel PID-reuse race beyond round 2's substitution guard; the report-path component-swap window between validation and replacement; forced thread-start failure; a live oversized Git-output specimen beyond the configured post-buffer check; and an independent round-4 rerun of the exact-parent full suite after the bounded candidate failed its focused stop gate. Before that gate, the clean current head ran 1,589 tests with exactly the three inherited `test_issue_429_release.test_root_suite_jobs_need_no_repository_history` subTest failures (`.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue), one ordinary skip, zero errors and five fixture-blocked identities; round 3's exact-parent result is retained only as evidence input, not restated as a fresh round-4 parity claim.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | high | scripts/run_checks.py | The executor has no hereditary membership boundary. A child forked, called `setsid`, opened `survivor.log` in the snapshot, closed file descriptors 0/1/2 and outlived its leader; `run_check` returned `passed` in 0.020 s with `descriptor_retained=false` and `termination=not-required`, while the surviving child grew the file from 4,096 to 77,824 bytes during the next 200 ms after snapshot removal. A same-process-group child that ignored SIGTERM also survived a timeout. Both specimens were killed and proved absent. Process groups and retained-output heuristics cannot establish the effective Step-2 exit, `All started work drains`; it needs a validated inherited boundary, whole-container termination and wait-until-empty, with refusal before target execution when unavailable. | open -- blocks Step 2 exit |
| S2-R4-02 | high | scripts/run_checks.py | `git()` decodes Git patch bytes with UTF-8 `replace`, and `make_snapshot` re-encodes that substituted text. A tracked source file containing `6e657720ff206c696e650a` reached the snapshot as `6e657720efbfbd206c696e650a`; the source-bound check exited 1 while the runner accepted the changed snapshot and returned green. The executed tree is therefore not the recorded source identity. | open -- blocks Step 2 exit |
| S2-R4-03 | medium | scripts/run_checks.py | Snapshot capture uses `git diff HEAD` without `--binary --full-index`. A valid owned tracked-binary change produced only Git's binary notice, and `git apply` refused `cannot apply binary patch ... without full index`; the repository currently owns twelve PNG, PDF, WOFF or BIN paths, so accepted source can be unexecutable even when every byte is in scope. | open |
| S2-R4-04 | medium | scripts/run_checks.py | Git capture does not disable textconv or external diff and has no subprocess deadline. A repository-configured textconv helper wrote a marker during source capture, before any declared check entered the scheduler, output capture or check timeout. A blocking helper can therefore start undeclared work and hold the runner without a bound. | open |
| S2-R4-05 | high | scripts/run_checks.py | A superseded retry reuses the original `CheckMap` and capacity plan. In a disposable repository, attempt 1 rewrote the live map to add a selected `must-fail` check; attempt 2 rebuilt selection against the stale object, ran only the mutator and emitted `outcome=green` although both the live source and retry snapshot declared `must-fail`. Map, ownership, selection, checks and capacity must be reloaded and identity-bound on every attempt before work starts. | open -- blocks Step 2 exit |
| S2-R4-06 | high | scripts/run_checks.py | A check with `script: null` bypasses cwd validation, and `_safe_relpath` is only lexical. A tracked `outside` symlink used as `cwd` resolved to an external temporary directory; the target command ran there and the composed runner returned green. Every declared execution path needs component-by-component no-follow confinement to the attempt snapshot, including command-only checks. | open -- blocks Step 2 exit |
| S2-R4-07 | medium | scripts/run_checks.py | `_capture_output` treats every `OSError` from `os.read` as EOF. Injected `EIO` after a child emitted evidence yielded exit code 0, zero captured bytes, no failure class and `status=passed`; output loss can therefore impersonate successful completion. A non-retryable capture fault must drain the started boundary and remain a `scheduler-error`. | open |
| S2-R4-08 | medium | scripts/run_checks.py | `remove_snapshot` can report `retained` or `not-owned`, but `_run_attempts` stores that disposition without adding a failure class, so an otherwise passing attempt still aggregates green after cleanup failed or ownership was lost. The runner cannot claim its owned snapshot lifecycle completed when the recorded cleanup says it did not. | open |

Leads not pursued: This completely fresh Warden re-read controller `next` inside the location-bound worktree and matched `audit-round`, step 2, round 4, agent `warden` and state SHA-256 `9e7d15cf01e2795f4b03d14e1a2d9f111783faf5236d00e4796e217416418814`; no controller mutation followed. The cold audit covered the complete stacked tree at signed head `274f9fca7277c047b2e23150fbc838131aa7ea41` (tree `7f7340bd08cbb1804439b9267b2ab711dcaf1eac`) against exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056`, re-verified the good Shoggoth signature and exactly one required co-author and origin trailer, and treated prior records as inputs rather than conclusions. The focused clean-head suite passed 88/88. Both JSON plans exited 0: `--full` selected 22 checks and omitted none, while `--scope hexaemeron` selected six and omitted sixteen; automatic capacity reported usable 18, reserve 6 and effective budget 12. The clean-head full suite produced 1,589 tests with only the three inherited #429 failures and no errors. Two independent read-only probes reproduced S2-R4-01, S2-R4-02 and S2-R4-07 and separately established the stale-map and symlink-cwd false-green paths; every spawned specimen was killed and every temporary tree removed. A bounded candidate then added parent-red guards and a fail-closed Linux cgroup-v2 direction with a bootstrap that wrote inherited membership before acknowledging and executing, `cgroup.kill` whole-scope termination, and `cgroup.events populated=0` drain. Its expanded focused suite passed 97 of 99 tests; the two failures were material, not cosmetic: capture `EIO` still became `command-failure`, and a superseded retry still omitted the newly declared `must-fail` check and returned green. Per the bounded stop rule, the entire candidate and all draft guards were reverted; no product or test byte from it remains, it is negative evidence only, and `fixes_commit=none`. No source-bound Elenchus replay was run because no causal fix survived, so the verdict is `none`. Further negative space stays visible: Git output is capped only after complete buffering; `confine_report_path` retains a check/use symlink-swap window; `execute` does not explicitly reject foreign or duplicate records and does not guard `thread.start()` failure; a nested coordinator is additional to its granted children; a raw mutable `--base` name is reported after resolution; malformed unhashable map values can still escape named refusal; and the timeout path discards some termination disposition. Synthetic cgroup evidence must not be described as live Linux coverage. S2-R4-01 is not narrowed to descriptor retention and remains the controlling blocker. The durable record was shaped item by item under Sapheneia without dropping reproductions, severities, uncertainty, the failed candidate, inherited-failure scope or negative-space leads. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. This audit-only record intentionally creates no fixes receipt. No remote, GitHub object or controller state was mutated.

## Step 2, round 5 -- 2026-08-27T15:51:28Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; live Linux cgroup-v2 containment or Windows containment, because this Warden ran on Darwin and every cgroup path was synthetic; a production containment fallback, because the reviewed candidate failed its lifecycle boundary and was wholly reverted; the real Pandects Forge group, because `forge` is absent from PATH; hosted CI, remotes, issues, pull requests, push and controller mutation, none of which was authorised or performed; a kernel PID-reuse race; the report-path replacement window; forced thread-start failure; and a complete design for the remaining Git, retry, path-confinement, containment and cleanup findings. The inherited `plugins/hexaemeron/tests/test_issue_429_release.py` `test_root_suite_jobs_need_no_repository_history` defect (`1f2298b2`) fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue -- on both the fixed tree and exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056`; it is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R5-01 | high | scripts/run_checks.py | The executor still has no hereditary process-membership boundary. A detached descendant can close the observed descriptors, outlive its leader and keep work alive after snapshot cleanup. The round-5 cgroup-v2 candidate did not establish the promised boundary: control descriptors reached the target, target credentials could migrate to a writable ancestor, partial preflight and thread faults leaked or raced containers, stalled bootstraps survived, and stdout EOF was mistaken for leader completion. | open -- blocks Step 2 exit; the candidate was fully reverted |
| S2-R5-02 | high | scripts/run_checks.py | Git patch capture still decodes arbitrary bytes with UTF-8 replacement and re-encodes substituted text, so a tracked non-UTF-8 source byte can differ in the executed snapshot while the run stays green. The exact-byte replay candidate was coupled to other invalid Git assumptions and was dropped rather than shipped. | open -- blocks Step 2 exit |
| S2-R5-03 | medium | scripts/run_checks.py | Snapshot capture still omits `--binary --full-index`, so a changed tracked binary yields a notice rather than replayable bytes and the snapshot cannot represent accepted source. The round-5 Git candidate was reverted after frozen review. | open |
| S2-R5-04 | medium | scripts/run_checks.py | Git capture still permits textconv and external diff helpers and has no deadline or output bound at the subprocess boundary. A configured helper can execute before the declared scheduler, write side effects and block the runner. The candidate disabled helpers but did not survive the complete Git review. | open |
| S2-R5-05 | high | scripts/run_checks.py | Retry authority is still stale. The retry reuses the original map and plan; the attempted reload remained unbound to one source identity, admitted a change between selection and identity, and could refresh to an empty check set that aggregated green. | open -- blocks Step 2 exit; the retry candidate was fully reverted |
| S2-R5-06 | high | scripts/run_checks.py | Command-only cwd validation remains lexical and symlink-following. The no-follow candidate validated a descriptor, closed it, then later resolved the pathname again, leaving a swap window that could execute the target outside the snapshot. | open -- blocks Step 2 exit; the path candidate was fully reverted |
| S2-R5-07 | medium | scripts/run_checks.py | `_capture_output` treated `EIO` as EOF, so missing output could impersonate successful completion. `_capture_output` now propagates the read fault; `run_check` drains through the existing process-group contract and records `status=failed`, `failure_class=scheduler-error` and a `capture failed` reason instead of command success or `command-failure`. | fixed and guarded in `1a78be97cc63e4af63d8c71d8ab9db49f8756a27` |
| S2-R5-08 | medium | scripts/run_checks.py | A `retained` or `not-owned` cleanup disposition can still leave an otherwise passing attempt green. The attempted non-green promotion hid cleanup failure behind a propagating exception and could erase existing `test-failure` classes, so it was removed instead of being grown around. | open |
| S2-R5-09 | high | scripts/run_checks.py | A repository clean filter can make a changed tracked file disappear from `git diff`: the snapshot keeps the old bytes while the live source changes, and the existing source identity falsely agrees. Disabling text conversion alone does not disable clean filters. | open -- blocks Step 2 exit |
| S2-R5-10 | high | scripts/run_checks.py | A detached producer started by Git capture can survive the Git leader. `_kill_command_group` returns once `proc.poll()` observes leader exit, without establishing that the producer's process group is empty, so adding only a Git deadline does not bound all work Git started. | open -- blocks Step 2 exit |
| S2-R5-11 | high | scripts/run_checks.py | Staged-source reconstruction is internally inconsistent: source identity hashes the cached diff, while snapshot construction applies the combined patch without `--index`. A reconstruction identity check would therefore reject an ordinary staged worktree even when replay succeeded. | open -- blocks Step 2 exit |
| S2-R5-12 | high | scripts/run_checks.py | Path validation and execution are not one operation. After a candidate validates cwd or script through a descriptor and closes it, an attacker can replace the path before `Popen` resolves it, allowing execution outside the checked snapshot. | open -- blocks Step 2 exit |
| S2-R5-13 | high | scripts/run_checks.py | Snapshot cleanup ownership is forgeable. The target learns the nonce from its cwd and can replace a sibling with a directory carrying the expected sentinel, causing cleanup to remove the substituted victim while the actual snapshot survives. A nonce and sentinel text are not an unforgeable ownership handle. | open -- blocks Step 2 exit |
| S2-R5-14 | medium | scripts/run_checks.py | Untracked regular-file permission changes were replayed but omitted from `source_identity`, allowing executable-bit drift under one identity. Identity now binds exactly the replayed `stat.S_IMODE(mode) & 0o777` bits for regular files, deliberately excludes unreplayed set-id bits and symlink modes, and preserves source/snapshot parity for a `04755` source and for Darwin symlinks. | fixed and guarded in `1a78be97cc63e4af63d8c71d8ab9db49f8756a27` |

Leads not pursued: This completely fresh Warden re-ran controller `next` inside the location-bound worktree and matched `audit-round`, step 2, round 5, agent `warden` and state SHA-256 `9bd1f38bff0449258a6b6fc274a64c493c411ef9d81f2e443b2b014a547c37c2`; no controller mutation followed. The cold audit began at signed head `274f9fca7277c047b2e23150fbc838131aa7ea41` and used two independent read-only probes while keeping every edit, test, record and commit with Warden. Test-first reproductions established the listed mechanisms on that exact preimage as assertion failures rather than missing-new-API errors. A broad bounded candidate attempted Git replay, retry reload, no-follow paths, cleanup classification and Linux containment. Frozen independent review invalidated its Git identity, staged-index, path lifecycle, cleanup ownership and cgroup lifecycle claims; all corresponding production and passing-test claims were removed, and its code is negative evidence only. The retained diff is limited to capture-fault classification and regular-file replayed-mode identity, with five focused round-5 guards; two final independent reviews each passed all five and confirmed that special bits and symlink mode lie outside replayed identity. The focused module passes 93/93. The fixes commit `1a78be97cc63e4af63d8c71d8ab9db49f8756a27` (tree `2f64fc055225c8246821e24e3f0bc5f35998b60e`) is signed by Shoggoth subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A` and carries exactly one required co-author trailer and one origin trailer. The official Elenchus overlay copied the changed test module onto immediate parent `274f9fca7277c047b2e23150fbc838131aa7ea41` and returned `guarded`: 1,593 tests executed, six assertion failures, zero errors and one skip. The fresh fixed-tree source report was deleted and proved absent before execution, then `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/step-2.json` wrote complete report SHA-256 `c7bf1314134455fa78c3b27f66f39456e5933105dc394207da3522260cfcf2b4`; automatic capacity used twelve workers, discovered 1,599 identities, ran 1,594, fixture-blocked five, skipped one and returned exactly the three inherited #429 failures with zero errors or unexpected successes. A disposable in-worktree worktree at exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056` was independently created and removed; its own fresh source-bound suite ran 1,501 tests with the identical three failures, zero errors and one skip. There is no additional failure identity. Source-bound Phylax and Ephoros exit zero; Hypomnema exits zero when the source, guards and referenced ADR-038 are supplied together. `git diff --check` exits zero. The failed cleanup candidate also exposed two narrower leads left within S2-R5-08: a cleanup exception can eclipse the recorded disposition, and naively promoting cleanup failure can replace rather than join prior failure classes. The failed retry candidate exposed the source-change window and empty-selection false green retained within S2-R5-05. These are not presented as fixed. The round-4 audit log and generated synopsis entered dirty because that round's controller receipt intentionally bound append-only evidence rather than a commit. This round preserves those bytes, appends only this record and regenerates the synopsis; under the same audit-round contract both audit files remain intentionally uncommitted for the controller receipt, while the causal fixes alone are committed. The durable record was shaped item by item under Sapheneia without dropping mechanisms, severities, dispositions, failed candidates, negative space, inherited-failure scope or uncertainty. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. Another independent round is required because twelve findings remain open. No remote, GitHub object, push or controller state was mutated.

## Step 2, round 6 -- 2026-08-27T16:17:21Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; live Linux or Windows hereditary containment, because this Warden ran on Darwin and retained the absence of a proved backend as an open blocker; a fresh live detached-descendant process specimen, because the independent process probe remained static and prior live records were evidence inputs rather than repeated observations; a deliberate same-identity ABA source race; a live Git helper producing more than the configured byte ceiling; the real Pandects Forge group, because `forge` is absent from PATH; hosted CI, remotes, GitHub signatures, issues, pull requests, push and controller mutation, none of which was authorised or performed. The inherited defect `1f2298b2` in `test_issue_429_release.test_root_suite_jobs_need_no_repository_history` fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests and `.github/workflows/pandects.yml` catalogue -- on both the fixed tree and exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056`; it is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | high | scripts/run_checks.py | The executor still has no hereditary membership boundary. A descendant can detach, close the captured descriptors, outlive its leader and continue work while the runner reports green and removes the snapshot. Static cold review independently confirmed that the mechanism from rounds 3-5 is unchanged. | open -- blocks Step 2 exit |
| S2-R6-02 | high | scripts/run_checks.py | A same-group descendant retaining stdout is not terminated after its leader exits. `_capture_output` reaps the leader with `poll()`, then `_terminate_group` correctly refuses to signal through the reusable pid, leaving the descendant alive when the descriptor-drain interval expires. | open -- blocks Step 2 exit |
| S2-R6-03 | high | scripts/run_checks.py | Git patch capture decodes arbitrary bytes with UTF-8 replacement and re-encodes them, so a tracked non-UTF-8 byte can differ in the executed snapshot while source identity and the final run remain accepted. | open -- blocks Step 2 exit |
| S2-R6-04 | medium | scripts/run_checks.py | Snapshot capture still omits `--binary --full-index`, so a changed tracked binary yields a notice rather than replayable bytes and the snapshot cannot represent accepted source. | open |
| S2-R6-05 | high | scripts/run_checks.py | A repository clean filter can make a changed tracked file disappear from `git diff`; the snapshot keeps the old bytes and the current source identity can falsely agree. | open -- blocks Step 2 exit |
| S2-R6-06 | medium | scripts/run_checks.py | Git capture permits textconv and external-diff helpers, has no deadline, and applies its output ceiling only after complete buffering. A helper can start undeclared work, hang, or exhaust memory before the configured bound is checked. | open |
| S2-R6-07 | high | scripts/run_checks.py | A detached producer started by Git capture can survive its Git leader. The current subprocess helpers do not establish hereditary drain for work Git starts. | open -- blocks Step 2 exit |
| S2-R6-08 | high | scripts/run_checks.py | Source identity binds a cached diff, but snapshot construction applies only the combined working-tree patch without index reconstruction. A staged tree is therefore bound and replayed under inconsistent models. | open -- blocks Step 2 exit |
| S2-R6-09 | high | scripts/run_checks.py | The runner takes identities before snapshot construction and after execution but never proves that the built snapshot equals the first identity. Separate HEAD, diff and untracked reads permit an accepted mixed or ABA snapshot. | open -- blocks Step 2 exit |
| S2-R6-10 | high | scripts/run_checks.py | A superseded retry still reuses the original `CheckMap` and capacity plan. A source movement that changes authority can therefore leave the retry executing and reporting the stale graph. | open -- blocks Step 2 exit |
| S2-R6-11 | high | scripts/run_checks.py | `tmp/check-runner` is exempted lexically and the nonce directory is created without proving each ancestor. A symlinked snapshot parent can redirect clone creation and later cleanup outside the repository. | open -- blocks Step 2 exit |
| S2-R6-12 | high | scripts/run_checks.py | Snapshot cleanup ownership remains forgeable: the target learns the nonce and can substitute a sibling carrying the copied sentinel, causing pathname-based cleanup to delete the substitute. | open -- blocks Step 2 exit |
| S2-R6-13 | medium | scripts/run_checks.py | A `retained` or `not-owned` cleanup disposition could leave an otherwise passing attempt green. Every non-`removed` disposition now joins `snapshot-error` without erasing an existing failure class, and source movement plus failed cleanup returns red instead of retrying. This does not repair S2-R6-12. | fixed and guarded in `495b33d99dd64e2b728d39a68008141caa0de20a` |
| S2-R6-14 | high | scripts/run_checks.py | Check cwd confinement remains lexical and symlink-following, and validation is separate from `Popen` path resolution. A path can resolve outside the snapshot or be swapped after a no-follow check. | open -- blocks Step 2 exit |
| S2-R6-15 | medium | scripts/run_checks.py | Report confinement resolves parent components, then reopens them by pathname for temporary creation and replacement. Rebinding a parent in that interval can redirect the write outside the repository. | open |
| S2-R6-16 | high | scripts/run_checks.py | `--base` resolves a commit for one diff call but stores and later reuses only the mutable ref name, while source identity excludes refs. Moving the ref during a run produced green under one selection and a different immediate plan under the same reported base. | open -- blocks Step 2 exit |
| S2-R6-17 | high | tests/check-map-v1.json | Changing the authority map itself selects only the map's owner closure, not every check whose authority changed. A map-only change changed `plugin-check` from pass to fail while the runner omitted it and returned green. | open -- blocks Step 2 exit |
| S2-R6-18 | high | scripts/run_checks.py | A declared check reachable from no scope is omitted even by `--full`. The parser now refuses every orphan check before planning, so full selection cannot silently omit a declared failing check. | fixed and guarded in `495b33d99dd64e2b728d39a68008141caa0de20a` |
| S2-R6-19 | high | scripts/run_checks.py | A scope could select only a later member of an ordered group and run it without its predecessor. The parser now conservatively requires every scope touching a group to include the complete declared group. | fixed and guarded in `495b33d99dd64e2b728d39a68008141caa0de20a` |
| S2-R6-20 | high | scripts/run_checks.py | Unknown top-level map fields are accepted while absent `dependencies` defaults to empty. A misspelled `dependecies` field silently erased consumer closure. The attempted strict-schema cluster expanded under frozen review and was dropped. | open |
| S2-R6-21 | high | scripts/run_checks.py | Unknown per-check fields are accepted. A misspelled `jobs_falg` silently removed nested quota propagation and charged the check as one plain slot. | open |
| S2-R6-22 | medium | scripts/run_checks.py | Unhashable group, group-member, scope-member, dependency and owner values could escape as raw `TypeError`, while a non-string owner path was stringified into the wrong refusal class. The parser now refuses each reviewed value as `map-invalid`. | fixed and guarded in `495b33d99dd64e2b728d39a68008141caa0de20a` |
| S2-R6-23 | medium | scripts/run_checks.py | `kind` is coerced with `str()` and not enum-checked. A misspelled suite kind was accepted and its failing result was classified `command-failure` instead of `test-failure`. | open |
| S2-R6-24 | medium | scripts/run_checks.py | Nested allocation counts only child slots. A nested check granted four slots launched one coordinator plus four children while the run reported a high-water mark of four. | open |
| S2-R6-25 | high | scripts/run_checks.py | A later `Thread.start()` fault can leave earlier work unjoined, and a `join()` fault escapes before remaining workers drain. The round-6 candidate covered only a pre-launch start failure; frozen review reproduced post-launch and join failures, so the entire cluster was dropped. | open -- blocks Step 2 exit |
| S2-R6-26 | medium | scripts/run_checks.py | `--report` is ignored when no check is selected: the command returns `nothing-selected` and exit 0 without writing the requested report. | open |
| S2-R6-27 | medium | scripts/run_checks.py | A successful report path need not be ignored or map-owned. Writing `report.json` succeeded, then the next plan refused that runner-created file as unknown ownership. | open |
| S2-R6-28 | low | scripts/run_checks.py | The persisted and emitted run records disagree because `report_path` is assigned only after `write_report` serializes the payload. | open |
| S2-R6-29 | medium | scripts/run_checks.py | Timeout termination evidence is false: `_capture_output` invokes `_terminate_group` but discards its disposition, so a process killed by SIGTERM can still report `termination: not-required`. | open |
| S2-R6-30 | high | scripts/run_checks.py | Pipe EOF previously ended capture even while the leader was still running, replacing the declared check timeout with the five-second drain interval. EOF now unregisters only the exhausted descriptor and waits for leader completion or the original deadline. | fixed and guarded in `495b33d99dd64e2b728d39a68008141caa0de20a` |

Leads not pursued: This independent Warden re-read controller `next` in the location-bound worktree and matched step 2, round 6, agent `warden` and state SHA-256 `1b659038e6670ef98a7e2f83be1e945ef2a1c7a1845c98f2992dd5f3a5f86489`; no controller mutation followed. The cold audit covered exact signed preimage `1a78be97cc63e4af63d8c71d8ab9db49f8756a27` against parent `dfb4b8db99997160539f4f2bd27be22254ad0056`, with two independent read-only probes. Parent-red guards were written before the retained fixes. Frozen review rejected and removed the top-level schema cluster when it expanded into unchecked per-check fields, and rejected and removed the thread-start cluster after post-launch and join faults left work alive. The retained six guard methods pass 99/99 on the fixed tree; the immediate-parent overlay runs 99 tests with eleven assertion failures, zero errors, and no missing-API failure, so the Elenchus verdict is `guarded`. Fixes commit `495b33d99dd64e2b728d39a68008141caa0de20a` (tree `0813c8e27e018013743f35dc9f14a61ae8d19e9b`) has a good Shoggoth signature from subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A` and exactly one required co-author trailer and one origin trailer. The official source report target was removed and proved absent before `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/step-2.json`; report SHA-256 `35c6bb530deb33f2c6d80fe747db9f7e00f0ba26a842d9efd01d39ebbd225278` records 1,600 executed tests, exactly the three inherited failures, zero errors, one skip and no unexpected successes from 1,605 discovered identities with five fixture-blocked. Exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056` independently ran 1,501 tests with the identical three failures, zero errors and one skip from 1,506 discovered identities with five fixture-blocked. Both JSON plans exit zero; `--full` selects all 22 checks and `--scope hexaemeron` selects six, with automatic effective budget twelve. Existing round-4 and round-5 log bytes were preserved as the exact prefix, this record alone was appended, and the deterministic synopsis was regenerated. The record was checked item by item under Sapheneia against all thirty findings, severities, statuses, hashes, test counts, open blockers, dropped candidates and negative evidence. The exact declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. No remote, GitHub object, push or controller state was mutated.

## Step 2, round 7 -- 2026-08-27T17:32:49Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; live Linux or Windows hereditary containment, because this Warden ran on Darwin and no production containment backend exists in the reviewed tree; a deliberate same-identity ABA race during snapshot construction; a live clean-filter, textconv or external-diff helper attack; a kernel PID-reuse race; a forced post-launch `Thread.start()` or `Thread.join()` failure with real long-lived work; a live report-parent symlink swap; the real Pandects Forge group, because `forge` is absent from PATH; and hosted CI, remotes, GitHub signatures, issues, pull requests, push and controller mutation, none of which was authorised or performed. The inherited defect `1f2298b2` in `test_issue_429_release.test_root_suite_jobs_need_no_repository_history` fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests and `.github/workflows/pandects.yml` catalogue -- on both fixes commit `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` and exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056`; it is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R7-01 | high | scripts/run_checks.py | The executor still has no hereditary membership boundary. A descendant can detach, close captured descriptors, outlive its leader and continue work while the runner reports green and removes the snapshot. | open -- blocks Step 2 exit |
| S2-R7-02 | high | scripts/run_checks.py | A same-group descendant retaining stdout can survive after its leader is reaped because signalling through the stale leader pid is correctly refused; descriptor-drain expiry does not terminate the descendant. | open -- blocks Step 2 exit |
| S2-R7-03 | high | scripts/run_checks.py | The canonical nested Hexaemeron shard runner starts children in independent sessions. An outer timeout reaches only the coordinator's group, so shard work can survive the red outer result and write after snapshot removal. | open -- blocks Step 2 exit |
| S2-R7-04 | high | scripts/run_checks.py | Git patch capture decodes arbitrary bytes with UTF-8 replacement and re-encodes them. A tracked non-UTF-8 byte can differ in the executed snapshot while the source identity and final run are accepted. | open -- blocks Step 2 exit |
| S2-R7-05 | medium | scripts/run_checks.py | Snapshot capture omits `--binary --full-index`, so a changed tracked binary yields a notice rather than replayable bytes and the snapshot cannot represent the accepted source. | open |
| S2-R7-06 | high | scripts/run_checks.py | A repository clean filter can hide a changed tracked file from `git diff`; the snapshot keeps old bytes and current source identity can falsely agree. | open -- blocks Step 2 exit |
| S2-R7-07 | medium | scripts/run_checks.py | Git capture permits textconv and external-diff helpers, has no deadline, and applies its byte ceiling only after complete buffering. A helper can start undeclared work, hang or exhaust memory before the bound is checked. | open |
| S2-R7-08 | high | scripts/run_checks.py | A detached producer started by Git capture can survive its Git leader because subprocess helpers do not establish hereditary drain for work Git starts. | open -- blocks Step 2 exit |
| S2-R7-09 | high | scripts/run_checks.py | Source identity binds a cached diff, but snapshot construction applies one combined working-tree patch without reconstructing the index. A staged tree is bound and replayed under inconsistent models. | open -- blocks Step 2 exit |
| S2-R7-10 | high | scripts/run_checks.py | The runner takes identities before snapshot construction and after execution but never proves that the built snapshot equals the first identity. Separate HEAD, diff and untracked reads permit an accepted mixed or ABA snapshot. | open -- blocks Step 2 exit |
| S2-R7-11 | high | scripts/run_checks.py | A superseded retry still reuses the original `CheckMap` and capacity plan. A source movement that changes authority can leave the retry executing and reporting the stale graph. | open -- blocks Step 2 exit |
| S2-R7-12 | high | scripts/run_checks.py | `tmp/check-runner` is exempted lexically and its nonce directory is created without proving each ancestor. A symlinked snapshot parent can redirect clone creation and later cleanup outside the repository. | open -- blocks Step 2 exit |
| S2-R7-13 | high | scripts/run_checks.py | Snapshot cleanup ownership remains forgeable: the target learns the nonce and can substitute a sibling carrying the copied sentinel, causing pathname-based cleanup to delete the substitute. | open -- blocks Step 2 exit |
| S2-R7-14 | high | scripts/run_checks.py | Check cwd confinement remains lexical and symlink-following, and validation is separate from `Popen` path resolution. A path can resolve outside the snapshot or be swapped after validation. | open -- blocks Step 2 exit |
| S2-R7-15 | medium | scripts/run_checks.py | Report confinement resolves parent components, then reopens them by pathname for temporary creation and replacement. Rebinding a parent in that interval can redirect the write outside the repository. | open |
| S2-R7-16 | high | scripts/run_checks.py | A mutable `--base` name could govern selection, diff and the report at different commits. The runner now resolves it once and carries that immutable commit through plan, retry, diff and report evidence. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-17 | high | tests/check-map-v1.json | Changing the authority map or runner selects only their owner closure, not every consumer whose authority changed. A map-only authority change can omit a newly failing governed check. | open -- blocks Step 2 exit |
| S2-R7-18 | high | scripts/run_checks.py | Undeclared keys at the map top level and in group, scope and owner entries could silently replace intended authority. Those four object surfaces now refuse unknown fields; this is not a claim that every value-level schema condition is closed. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-19 | high | scripts/run_checks.py | Undeclared per-check keys were accepted, so `jobs_falg` silently removed nested quota propagation. Check objects now refuse unknown fields. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-20 | medium | scripts/run_checks.py | `kind` was coerced without an enum check, so a misspelt suite kind was accepted and a failing test was classified as a command failure. Only the declared kinds are now accepted. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-21 | high | scripts/run_checks.py | Nested allocation counts only child slots. A check granted four slots can run one coordinator plus four children while the report claims a high-water mark of four; even a budget of one permits two live processes. | open -- blocks Step 2 exit |
| S2-R7-22 | medium | scripts/run_checks.py | The report records logical slot high-water but not the maximum actual live-child count required by the runbook, so coordinator and descendant oversubscription is not visible in the released evidence. | open |
| S2-R7-23 | high | scripts/run_checks.py | A later `Thread.start()` fault can leave earlier work unjoined, and a `join()` fault can escape before remaining workers drain. `_run_attempts` may then remove a snapshot that live work still uses. | open -- blocks Step 2 exit |
| S2-R7-24 | medium | scripts/run_checks.py | `--report` was ignored when no check was selected. The `nothing-selected` path now writes the requested complete report and keeps disk JSON equal to emitted JSON. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-25 | high | scripts/run_checks.py | A report path need not be runner-owned or ignored. A valid run can overwrite an existing tracked path or create an unowned path that poisons the next plan. | open -- blocks Step 2 exit |
| S2-R7-26 | low | scripts/run_checks.py | Normal persisted and emitted records disagreed because `report_path` was assigned after serialization. Normal reports now bind the same path before both writes and the serialized bytes agree. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-27 | medium | scripts/run_checks.py | Ordinary timeout evidence previously discarded the signal disposition. It now records `leader-exited-after-group-sigterm; process-group-exit-unproved`, retaining the proved leader result without claiming hereditary group drain. | fixed and guarded in `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` |
| S2-R7-28 | high | scripts/run_checks.py | `--map` accepts an outside or symlinked authority file, and `read_bytes()` buffers it before applying the size ceiling. Authority can therefore come from outside the repository and exhaust memory before refusal. | open -- blocks Step 2 exit |
| S2-R7-29 | medium | scripts/run_checks.py | `refuse_stale_commands` skips cwd validation when a check has no `script`, so a command-only entry can defer an outside, missing or swapped cwd to execution instead of refusing the stale plan. | open |
| S2-R7-30 | medium | scripts/run_checks.py | Report lifecycle is not terminal: a refusal or unstable-source result can leave a prior green report in place, and an `os.replace` failure can leak a partial file. The normal and nothing-selected parity fixes do not close these paths. | open |

Leads not pursued: This fresh independent Warden re-read controller `next` inside the location-bound worktree and matched step 2, round 7, agent `warden` and state SHA-256 `0d56ebbcef96d7c272a40e87987dff0baf8ea38fbb141e6d12267d51c397092a`; no controller mutation followed. The cold audit covered the complete stacked tree at signed preimage `495b33d99dd64e2b728d39a68008141caa0de20a` (tree `0813c8e27e018013743f35dc9f14a61ae8d19e9b`) against exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056` without treating round 4, 5 or 6 conclusions as proof. Two fresh read-only probes independently partitioned the schema, report, mutable-base and scheduler/lifecycle surfaces. Six new guard methods produced ten assertion failures and zero errors on the exact preimage before any fix; all 105 focused tests pass at the fixed tree. Four narrow clusters were frozen independently and both probes reviewed the exact bytes. The accepted claims are limited to unknown-key closure at top, check, group, scope and owner objects plus the declared `kind` enum; one immutable resolved base carried through plan, retry, diff and report; byte-identical persisted and emitted normal and nothing-selected reports; and honest leader timeout disposition that explicitly leaves process-group exit unproved. Value-level schema closure, terminal report lifecycle and report ownership remain open. Any candidate reaching into retry-map identity, snapshot or cleanup ownership, hereditary containment, Git byte/filter/index replay, cwd confinement, nested coordinator accounting or thread lifecycle was excluded rather than expanded. Frozen SHA-256 identities are `a828cc1833999c8c6a655dc192bc1d045d80734e3b44322510683e34e3acc49e` for `scripts/run_checks.py`, `591e95e8fda00a55c140f3870cdb2fc6855a33351a1d310fa2ffd39af8a4aaf7` for `test_check_runner.py`, and `8121b92368cbd440adfcba547a060b62e42b4b6dcd0c2ffceaeda0dc13ac8741` for the staged fixes diff. Fixes commit `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` (tree `5b069d3321ae79e8781d68dc81a0f01a091477ed`) has a good Shoggoth signature from subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A` and exactly one required co-author trailer and one origin trailer. The old official target was moved aside and absence proved before `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/step-2.json`; complete report SHA-256 `cbd9e480db41ee64aec57686b32931b183a9410d4b5302cdbf10500a03ca52fd` records 1,606 executed tests, exactly the three inherited failures, zero errors, one skip and no unexpected successes from 1,611 discovered identities with five fixture-blocked. Exact parent report SHA-256 `34eee916859dce5fa1ec77a3a6b447bf7930dd0e266cf88b2e2d86fd0331aee4` independently records 1,501 executed tests with the identical three failures, zero errors and one skip from 1,506 discovered identities with five fixture-blocked. The disposable detached parent worktree was removed after its evidence digest was preserved. Both JSON plans exit zero: `--full` selects all 22 checks and `--scope hexaemeron` selects six and omits sixteen, at usable 18, reserve six and effective budget twelve. Final Phylax and Ephoros scans over their repository trees plus the changed runner exit zero, and Hypomnema over its repository tree plus the runner, map, round record and generated synopsis exits zero; `git diff --check` also exits zero. Existing round-4, round-5 and round-6 audit bytes remained the exact prefix with SHA-256 `4351b743a90d963fa133526f6e615f6426afe8d5126b62ec1c3ef9cdaa812195` before this record. The record was shaped item by item under Sapheneia without dropping mechanisms, severities, narrow fix boundaries, test counts, open blockers or uncertainty. The exact declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. No remote, GitHub object, push or controller state was mutated.

## Step 2, round 8 -- 2026-08-27T18:19:23Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet because the complete Step-2 tree ships no Solidity; live Linux or Windows containment and no-follow behaviour, because this Warden ran on Darwin 25.5.0; a deliberate same-identity ABA race during snapshot construction; live clean-filter, textconv or external-diff helper execution; a live same-credential snapshot-parent, cwd, cleanup-sentinel or report-final-entry substitution race; a kernel PID-reuse race; the real Pandects Forge group, because `forge` is absent from PATH; and hosted CI, remotes, GitHub signatures, issues, pull requests, push and controller mutation, none of which was authorised or performed. The inherited defect `1f2298b2` in `test_issue_429_release.test_root_suite_jobs_need_no_repository_history` fails in exactly three subTests -- `.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests and `.github/workflows/pandects.yml` catalogue -- on both fixes commit `180983bd02dae1c82e6578a5e26ff6c7da315aa7` and exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056`; it is outside this step's files and is not a finding of this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R8-01 | high | scripts/run_checks.py | The executor still has no hereditary membership boundary. A descendant can create a new session, close every captured descriptor, outlive its leader and continue work while the runner reports green and removes the snapshot. | open -- blocks Step 2 exit |
| S2-R8-02 | high | scripts/run_checks.py | A same-process-group descendant that closes stdout can survive a reaped leader without triggering retained-descriptor handling. No group-drain proof runs on the otherwise-green path. | open -- blocks Step 2 exit |
| S2-R8-03 | high | scripts/run_checks.py | The canonical nested Hexaemeron runner starts shard workers in independent sessions. An outer timeout reaches the coordinator group, not those worker groups, so they can survive snapshot cleanup. | open -- blocks Step 2 exit |
| S2-R8-04 | high | scripts/run_checks.py | A later `Thread.start()` fault or a wait interruption could unwind while earlier workers still used the snapshot. Completion events now retain every started unit, cancellation stops later launches, the drain loop survives interruptions, and the first cancellation is re-raised only after all started threads stop. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-05 | high | scripts/run_checks.py | Nested allocation counted child slots but omitted the coordinator. A grant now reserves one coordinator slot, passes only `granted - 1` to the nested runner, and fails before launch when fewer than two slots are available. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-06 | medium | scripts/run_checks.py | Released scheduler evidence still reports logical slot high water but not the maximum observed live child-process count required by the runbook. | open |
| S2-R8-07 | medium | scripts/run_checks.py | Timeout, retained-descriptor and follow-up wait paths can overwrite or omit one signal disposition, so the final record does not compose every termination attempt into one complete account. | open |
| S2-R8-08 | high | scripts/run_checks.py | Duplicate records for a selected id and records carrying a foreign id could survive set-based accounting. Execution now emits exactly one terminal record per selected id and converts missing, duplicate or foreign membership into `scheduler-error`. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-09 | high | scripts/run_checks.py | A Git clean filter can hide changed tracked bytes from the diff captured in source identity. The snapshot can execute old bytes while the enclosing identity appears stable. | open -- blocks Step 2 exit |
| S2-R8-10 | high | scripts/run_checks.py | Git capture permits textconv and external-diff helpers, buffers their full output without a deadline, and does not hereditarily contain work they start. A helper can substitute bytes, hang, exhaust memory or detach work. | open -- blocks Step 2 exit |
| S2-R8-11 | high | scripts/run_checks.py | Git patch bytes are decoded with UTF-8 replacement and re-encoded. A tracked non-UTF-8 byte can change in the executed snapshot while the recorded identity still describes the lossy patch. | open -- blocks Step 2 exit |
| S2-R8-12 | medium | scripts/run_checks.py | Snapshot capture omits `--binary --full-index`; a changed tracked binary can yield a notice rather than replayable bytes. | open |
| S2-R8-13 | high | scripts/run_checks.py | Source identity hashes index state, but snapshot construction applies one combined working-tree patch without reconstructing the staged index. Bound and executed source models therefore diverge for staged changes. | open -- blocks Step 2 exit |
| S2-R8-14 | high | scripts/run_checks.py | HEAD, diff and untracked content are read separately, and the built snapshot is never compared with the first identity. A mixed or same-identity ABA snapshot can be accepted. | open -- blocks Step 2 exit |
| S2-R8-15 | high | scripts/run_checks.py | A superseded retry reused its original map, commands, selection and capacity. Every attempt now reloads those authorities inside before-and-after source-identity reads, and execution maps must resolve to one exact tracked Git-visible index entry; findings S2-R8-09 through S2-R8-14 remain outside this narrow closure. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-16 | medium | scripts/run_checks.py | Command-only checks skipped cwd existence validation when no `script` field was present. Every declared non-root cwd is now validated before execution. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-17 | high | scripts/run_checks.py | Cwd validation still follows symlinks and is separate from `Popen` path resolution. A declared cwd can resolve outside the snapshot or be rebound after validation. | open -- blocks Step 2 exit |
| S2-R8-18 | high | scripts/run_checks.py | `tmp/check-runner` is exempted lexically, and snapshot creation does not prove each parent component. A symlinked runner parent can redirect creation and cleanup outside the repository. | open -- blocks Step 2 exit |
| S2-R8-19 | high | scripts/run_checks.py | Parallel checks share one writable snapshot. One selected check can alter source another selected check observes while the aggregate still reports green. | open -- blocks Step 2 exit |
| S2-R8-20 | high | scripts/run_checks.py | Snapshot cleanup trusts a pathname and copyable sentinel after the target learns the nonce. A same-credential check can substitute a sibling and redirect recursive removal. | open -- blocks Step 2 exit |
| S2-R8-21 | high | scripts/run_checks.py | `--report` could overwrite a tracked path or create an unignored, unowned path that poisoned the next plan. The target must now be exactly untracked and ignored, and Git probe failures refuse. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-22 | medium | scripts/run_checks.py | Report confinement checked a parent by pathname and reopened it later. Parent creation, temporary creation and replacement now use one descriptor-relative no-follow directory walk, so parent rebinding cannot redirect the write. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-23 | high | scripts/run_checks.py | The final report temporary name remains discoverable and replaceable by the same credential between close and `os.replace`. The bound parent descriptor does not establish final-entry identity. | open -- blocks Step 2 exit |
| S2-R8-24 | medium | scripts/run_checks.py | Refusal, repeated source movement or report replacement failure could leave a prior green report or a partial file. After ownership validation, the runner now writes an initial incomplete record, replaces it on every terminal path, persists unstable-source and removes an ordinary failed partial; argparse-time refusal remains outside this claim. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-25 | high | scripts/run_checks.py | A runner or map change selected only its owner closure rather than every authority consumer, and an ignored custom map could escape both selection and identity. Runner changes and changes to the exact tracked map now select every declared check; ignored, wildcard-decoy and directory-prefix-decoy maps refuse. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-26 | high | scripts/run_checks.py | `--map` could read outside the repository, follow a symlink or allocate the whole file before its ceiling. Map reads now use a descriptor-relative no-follow walk, require a regular file, bound bytes while reading and compare file identity before and after. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-27 | medium | scripts/run_checks.py | Map and untracked-file read identities omitted ctime, so a same-size change restoring mtime could evade the pre/post comparison. Both identities now include ctime. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-28 | medium | scripts/run_checks.py | A non-zero Git ownership probe other than the exact absent result could be treated as proof that a report or map was untracked. Git errors now refuse, and tracked-map acceptance requires one literal NUL-delimited path equal to the requested bytes. | fixed and guarded in `180983bd02dae1c82e6578a5e26ff6c7da315aa7` |
| S2-R8-29 | medium | scripts/run_checks.py | `--plan` returns after one unbracketed map, command, diff and capacity read. Authority or source can move during plan construction without an unstable-source result. | open |
| S2-R8-30 | medium | scripts/run_checks.py | The no-follow walks silently use flag value zero when `O_NOFOLLOW` is unavailable. The current Darwin evidence does not establish the same confinement claim on such a platform. | open |

Leads not pursued: This fresh independent Warden re-read controller `next` inside the location-bound worktree and matched step 2, round 8, agent `warden` and state SHA-256 `e90c2ec5a257e61983083e49e4bd67536abe16f5d54f0f53e105a42c72e00755`; no controller mutation followed. The cold audit covered complete signed preimage `b1ad9ab0dfa462fb72338781fb10f214ddf2cd56` against exact parent `dfb4b8db99997160539f4f2bd27be22254ad0056` without accepting earlier conclusions as proof. Two fresh read-only probes partitioned process lifecycle and source, snapshot and report authority. Parent-red work was causal and bounded: the first eight guards produced seven failures and two errors on the signed preimage; isolated duplicate/foreign, map-ctime, report, Git-probe and cancellation guards each failed before their fix; each ignored-custom-map, wildcard-decoy and directory-prefix-decoy guard was observed red on its immediate preimage; and a final signed-preimage overlay left the initial-incomplete and persisted-unstable-source guards red with two missing-report errors. Frozen static review accepted exact source hashes `b6de42d743b4a06c34d4410207d28c5c7877ad242023acb3687921f90c5d15fa` and `cbbecb3ce2cc480be76a2c378adef20dab79c5ab76b48eb157b8c51f9b08ee68`, while retaining the hereditary, Git/filter/index, snapshot/cwd/cleanup and report-final-entry limitations recorded above. The complete focused module passes 127/127, its source/snapshot/report subset passes 15/15, and its process/budget/accounting subset passes 8/8. Fixes commit `180983bd02dae1c82e6578a5e26ff6c7da315aa7` (tree `ac3dbbb50d2e4cf2299f222bdee7e3aca99c563f`) has a good Shoggoth signature from subkey `A0CC410C7BA3DEFC032B3FAA2C9B298EB3D5E57A`, exactly one required co-author trailer and one origin trailer. The official source report target was removed and proved absent before the fixes suite ran; report SHA-256 `b476ba0f1f574e43e5ce48026949a0776db9d36c757aed7e49ed2367a51ea6e7` records 1,628 executed tests, exactly the three inherited failures, zero errors and one skip from 1,633 discovered identities with five fixture-blocked. Exact parent report SHA-256 `34eee916859dce5fa1ec77a3a6b447bf7930dd0e266cf88b2e2d86fd0331aee4` independently records 1,501 executed tests with the identical three failures, zero errors and one skip from 1,506 discovered identities with five fixture-blocked; the disposable parent and preimage worktrees were removed after their evidence was recorded. Both current JSON plans exit zero: `--full` selects all 22 checks and `--scope hexaemeron` selects six and omits sixteen, at usable 18, reserve six and effective budget twelve. Existing audit bytes remained the exact 87,879-byte prefix with SHA-256 `7298520544b33048496c0a237dd8eae6a6ceee9684f1bf72c1383abcdf271c6c` before this candidate. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence, not controller proof. No remote, GitHub object, push or controller state was mutated.

## Step 3, round 1 -- 2026-08-28T01:42:50Z

Audit schema: fiat-audit-round/v2

Covered: artifact-substitution=reviewed; partial-inoculation=reviewed; current-base-conflict=reviewed; signing-scope=reviewed; manifest-drift=reviewed; assignment-loss=reviewed; global-oversubscription=reviewed; quota-misdetection=reviewed; cache-authority=reviewed; output-loss=reviewed; report-forgery=reviewed; source-movement=reviewed; impact-omission=reviewed; ordered-breakage=reviewed; audit-recursion=reviewed; evidence-overclaim=reviewed

Not checked: the Solidity security suite (`x-ray`, `solidity-auditor`, `fizz`), waived by the receipted packet and verified mechanically -- zero `.sol` paths in the 13-file step range, the 52-path plan scope from replacement base `b245d68e7e8c9d07b0dbbaa67e57b05cd00b18ef` and the whole-run diff against `main`. The six benchmark samples were not rerun; they are frozen recorded evidence, and this round instead proved their internal consistency from the raw JSONs: one manifest digest `a4edf1abaa369a5146114a5e143a5e9cc4de7f5ef969e14129edb16d408b9c9f` and one byte-identical 1,653-identity membership across all six, identical verdict sets (exactly the three inherited issue-429 subTest failures, zero errors, one skip, five fixture-blocked), full-precision medians 665.204 s serial and 81.267 s automatic, deviations 2.154 s and 2.868 s, gain 583.937 s against deviation sum 5.022 s, queue and live-child high-water marks 12 of budget 12 on usable 18, peak child RSS 159.875 MiB, and the frozen source corroborated by commit `cb3c147d` (tree `55bf25644e0ec58d17e88a23bd21f70008049ead`, committed 23:02:54 +0100, samples driven 23:03-23:40) with runner SHA-256 `3c83bfaa7f067f00f304eeac64867f4710846caa913d6ce14e6eca7024b5d63f` identical at that commit, in each sample record and at HEAD; the host facts in the benchmark boundary (Darwin 25.5.0, 18 logical CPUs, 128 GiB, Python 3.14) were each verified on this machine, but the "otherwise idle machine" claim and the exact interpreter state of those sample runs are not reconstructible -- under the current interpreter all five optional Lazarus imports resolve, so this round's fresh run executed all 1,654 identities with zero fixture-blocked and zero skips where the samples blocked five, and both records honestly describe their own runs. `scripts/run_checks.py --full` was not rerun; the recorded `full-run.json` (byte-identical to `full-run-stdout.json`, SHA-256 `31f6dddcb44242130664b19b6d4564b0632694f49db5901c5813de546b8f423d`, written 02:07 after final commit 02:04) is the evidence: schema `wildcat.check-run.v1`, full plan of 22 checks with zero omitted, snapshot_sources 1,766 entries intact, scheduler budget 12 with slot high water 12, outcome red on failure class `test-failure` from exactly `hexaemeron-suite` (the three inherited subTests) and `lazarus-suite`, whose captured output shows the documented Darwin environmental cause ("statement path contains a symlink or non-directory: /var/folders/..."); the lazarus suite was not rerun and zero range commits touch `plugins/lazarus`, and the recorded `pandects-forge-build`/`pandects-forge-test` passes were not independently rerun. Live Linux and Windows behaviour, hosted CI, remotes, GitHub signatures, issues, pull requests, push and controller mutation were neither authorised nor performed. The inherited defect in `test_issue_429_release.test_root_suite_jobs_need_no_repository_history` fails in exactly three subTests (`.github/workflows/janus.yml` contracts, `.github/workflows/lazarus.yml` tests, `.github/workflows/pandects.yml` catalogue) on HEAD in this round's own run; it is outside this step's files and is not a finding of this round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | tests/test_run_observation.py | Two reporter tests (lines 614 and 639) still anchor scratch `TemporaryDirectory(dir=ROOT / "tests")` under the tracked tree, and `git status --porcelain` sampled at the first test's cleanup shows `?? tests/tmp*/`, so the shared `--full` snapshot stays transiently non-quiescent and the disposable-signing guard's outer-stability assertion can still race exactly the spurious untracked-path failure class ff92fdd repaired at the other fifty-eight sites, although `report_target` accepts a `tmp/`-anchored parent. | open |

Leads not pursued: This fresh independent Warden bound itself read-only to the controller inside the location-bound worktree -- `verify` reported 49 ledger entries with chain intact and state consistent, `status` showed step 3/3 in phase audit with 0 rounds, and `next` matched directive audit-round round 1, agent `warden`, state SHA-256 `af0f04fc125a8556e2db387b6c5d5674c81bb4b8d2d4e96d90730a5504057765` -- and mutated nothing: no commit, no tracked byte, no controller state, no audit-log write (this record is returned to the orchestrator for receipting), with the tracked tree porcelain-clean at start and end and HEAD fixed at `41f074ba69268970471e387cba72745ad24296fa`. The audited range is `7bd9cee4524771642cf5d47d7cce56a8869551d3..41f074ba` (merge-base of both step-2 branches confirmed), seven commits, each with a good GPG signature and exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth` trailer; every changed file was read in full. Executed cold this round: the three lints (phylax 0, ephoros 0, hypomnema 0, all clean); imprimatur over the five Exit documents (exit 0, score 100, zero defects); `horos check .` (exit 0, boundary matches the tree, one non-failing candidate-drift note for this run's audit synopsis); `git diff --check` (exit 0); the exact Elenchus runner contract `run_tests.py --elenchus-report` against HEAD, yielding a complete `elenchus.unittest.v1` report counter-identical to the recorded `.elenchus/step-3.json` (testsRun 1654, failures 3, errors 0, skipped 0) with the three failures being exactly the inherited subTests; `test_check_runner.py` in full (104 tests OK); `test_disposable_git_signing.py` (OK); the four run-observation modules (110 tests OK; inoculation 1,258 cases, zero crashes, zero unexpected-clean); the alexandria suite (296 tests OK) with `plugins/tabularium/examples/goldfinch-v0/source.json` proven byte- and mode-stable (0644, SHA-256 `644b7068...`) before and after; `test_decision_records` (5), `test_promise_machine_contract` (73), `test_unique_identifiers` (9), `promise_machine.py check` (0) and `coverage --check` (0); the cumulative verifier CLI on the Carryover-3 packet and patch (exit 0: base `b245d68e...`, 18 logical paths, 18 targets, 1 transform, 23 findings, 60 cause guards, 101 runnable guards, 13 families, 27 cumulative paths -- the waiver's 27-path scope); and a live plan from the replacement base on final bytes (exit 0, 52 changed paths, all 22 checks selected, zero omitted, map digest unchanged), which refutes as a defect the recorded `plan-from-base.json` predating the last four commits, since the runbook requires retaining the sample summaries and full-run report, not the plan. Every rebound digest was hashed independently and matches its pin (coverage file `3891d1be...`, record `0174e7cd...`, replacement study `78d8c991...`, replacement runbook `a4b4c4f5...`, `test_run_observation.py` `7717137f...`, `test_run_observation_capture.py` `431d2114...`). Parent-red was established in memory without touching bytes: the linked-worktree guard fails with "the snapshot lost the source's default-branch baseline" when `_mirror_baseline_ref` is no-opped and passes with it; the symlink-teardown guard fails with "fixture cleanup escaped its sandbox" under the old dereferencing chmod and passes with the fix; hypomnema H006 exists and fires on the parent verifier bytes' record-citation form for ADR-038/039/040, which are files only on `origin/main` (`2e2608aa`), not this branch, while the reworded comment keeps every fact and the current tree lints clean. Leads seen and not pursued: `scratch_directory`'s `mkdir(exist_ok=True)` would accept a pre-planted `tmp` symlink and redirect scratch outside the root, but the validators resolve and refuse escaped paths loudly, the planter is the same credential, and the analogous runner-parent concern is already recorded open as S2-R8-18; the benchmark table's rounded deviations (2.2, 2.9) sum to 5.1 as displayed while the stated 5.0 is the correct full-precision sum, an attempted refutation of the arithmetic that failed against the raw JSONs and leaves the keep verdict unaffected either way; "one shared scratch helper" is textually two identical module-local helpers plus one alias, functionally one policy; the Elenchus format name `unittest-json-v1` beside report schema `elenchus.unittest.v1` is the pairing this run has used since step 2; the 127-versus-104 check-runner test-count difference against the round-8 record dissolves into inner fixture classes inflating method greps, with this range adding exactly one method; and step 2's closure after its round 8 by receipted halt/resume is controller history outside this step-3 round, preserved unaltered in this log's earlier bytes. This round is read-only by delegation, so it applies no fix and creates no fixes commit; per this register's own precedent (Step 1, round 1) a round with no fixes commit carries the verdict `null`, and finding S3-R1-01 awaits its cause fix, guard and map join in the next independent round. The exact audit declaration is `--audit-filter sapheneia:sapheneia`; it is operator evidence that the pass was applied and does not make the controller proof of this record's semantics. No remote, GitHub object, push or controller state was mutated. A findings round asserts the declared suite executed and returned exactly what is recorded here.
