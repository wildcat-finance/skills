Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-restore-the-shoggoth-public-front-door-and-demo.md | source_sha256=8ebfa65d1ac7a876987886b3daaae9763b0ddac2d213394eb1254c815b70c6b2 | h2_count=4
## Step 1, round 1 -- 2026-08-31T20:46:41Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-drift=reviewed; generated-copy-drift=reviewed; external-data-egress=not-applicable; subprocess-execution=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; pending-member-overclaim=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable<br><br>Not checked: Pashov X-Ray and Solidity Auditor were waived because Step 1 changes repository documentation, one Python topology reader, JSON fixtures, tests, and the check map; no Solidity or contract security surface is in scope. `claim-without-demo`, `demo-class-inflation`, `source-drift`, `partial-demo-output`, `demo-skip-as-pass`, and `real-data-nonclaim-loss` are not applicable because this step creates no demonstration record, executes no demonstration, emits no demonstration output, and publishes no capability card, source class, status, or non-claim. `frontier-lane-collision` and `queue-duplication` are not applicable because no `EVOLUTION.md`, demonstration ledger, held job, issue, or queue state changes. `external-data-egress` and `subprocess-execution` are not applicable because the topology module opens no socket, network path, credential, model, shell, or subprocess. `front-door-regression`, `pending-member-overclaim`, `portrait-inconsistency`, and `visual-surface-drift` are not applicable because this step changes no root README, plugin landing page, image, PDF, public catalogue, or Dokimasia claim. Atomic coherence against a hostile process rewriting several different repository files between individually stable reads was not established; Step 1 reads a clean signed repository and promises descriptor-confined bounded reads, not a filesystem transaction. Hosted CI, controller receipt, push, pull request, GitHub publication, and integration were not checked.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | high | `scripts/shoggoth_topology.py:75`; `scripts/shoggoth_topology.py:233`; `scripts/shoggoth_topology.py:305`; `tests/test_shoggoth_topology.py:330` | The implementation checked only the plugin and skills leaf paths with `lstat`, then passed path strings to `scandir`. A repository whose top-level `plugins` entry was a symlink to an outside tree was accepted and returned `ACCEPTED plugins=17 skills=26`, so the no-follow and outside-`plugins/` promises could be satisfied by bytes beyond the repository boundary. | fixed and guarded in this round; traversal now requires `O_NOFOLLOW` and `O_DIRECTORY`, opens `plugins`, each plugin, `skills`, and every descendant relative to already-open descriptors, verifies types with `fstat`, and the signed-parent guard is red while the fixed tree is green |<br>| S1-R1-02 | medium | `scripts/shoggoth_topology.py:233`; `tests/test_shoggoth_topology.py:279` | The 4,096-entry ceiling ran only after `sorted(scandir(...))` had consumed and retained the complete directory. The bounded reproducer therefore raised `AssertionError: scanner consumed past the declared entry ceiling` instead of `T010`, so a wide untrusted skill tree could spend unbounded memory and scan time before refusal. | fixed and guarded in this round; the cumulative limit now runs while the iterator is consumed, retains at most 4,097 names before refusing, sorts only after the bounded collection completes, and the signed-parent guard is red while the fixed tree is green |<br>| S1-R1-03 | medium | `scripts/shoggoth_topology.py:132`; `tests/test_shoggoth_topology.py:249` | The manifest reader held a no-follow descriptor but never compared the opened file with the closed descriptor and still-named leaf. Rewriting the Claude manifest in place after its first `os.read` produced `ACCEPTED unstable=True plugins=17`, so discovery returned bytes that no longer matched the named manifest. | fixed and guarded in this round; opened, closed, and named `(device, inode, size, mtime_ns, ctime_ns)` identities must now agree or `T019` refuses, and the signed-parent guard is red while the fixed tree is green |<br><br>Leads not pursued: Facts -- The complete diff from signed base `a2b634d8e039af988bf30c8316defccf70071d8d` through signed implementation `1a5cce9b84921969a654cab985f656a30688415a` and the three cause-level fixes was reviewed without the Horos exclusion boundary. The live tree and independent fixed fixture return 17 plugins, 26 governed skills, 17 canonical or domain ids, and nine phase ids; duplicate plugin ids, duplicate JSON keys, host-manifest disagreement, paths outside the closed mapping, missing canonical `SKILL.md`, unexpected non-Hexaemeron phases, oversized manifests, static manifest and skill symlinks, a symlinked top-level `plugins` directory, an over-wide tree, and a manifest rewrite each refuse by name. The stable manifest reader caps each file at 1,048,576 bytes; traversal caps each plugin at 4,096 entries and depth eight. No socket or subprocess appears in the module. The source-bound Elenchus runner returned `guarded` after overlaying `tests/test_shoggoth_topology.py` onto the signed parent. The fixed-tree runner passed 1,122 of 1,122 tests and wrote a mode-0600 complete `unittest-json-v1` report with zero failures, errors, skips, expected failures, or unexpected successes at SHA-256 `db177afcd52d67d0ef5f276f5a8411607ae1015bc2a67d059fc1ce50a4f2b61a`. The complete checked runner used source digest `2474232a61a2b107c65eeaea17ab8385ab4939ddbaabc7d756694d70deddf6dc` and passed all 26 selected checks; Phylax, Ephoros, and Hypomnema each exited zero. The committed study is byte-identical to its receipted source at SHA-256 `d96c4b1d669e4fcda9e5b339a6b1be210c079c63e597cd5021fcf24e1f7cace7`; the committed runbook is byte-identical to the effective controller copy at SHA-256 `8ef82fa950c0db604eeb50d291c91176d41024fe24941fb03054056a5d9a48a3`; immutable design evidence remains SHA-256 `252bb05b83662a9b88c04f5f54c0e9f65dbd8b1a7cd902d8c33127c719c73d25`. The diff adds new specifications and synthetic fixtures but changes no pre-existing audit, ADR, specimen, release, `EVOLUTION.md`, portable runtime, `.python-version`, `pyproject.toml`, `LICENSE`, or workflow; the root `AGENTS.md` change preserves every word and only joins the permitted `Do not publish` line. The generated Horos boundary changed only its deterministic walked-file count from 2,279 to 2,287, and its suite passed. Imprimatur scored 100.0 with zero defects on the exact record and synopsis. Brevitas report mode returned `B001` and `B010` on the required one-record shape, plus `B010` and `B027` on the deterministic one-line synopsis; adding headings or rewriting generated synopsis bytes would violate the owning audit and synopsis formats, so structural conformance is not claimed. Inference -- Descriptor-relative opens prevent a symlink substitution from escaping the already-open repository tree; post-read identity comparison detects the exercised in-place and named-leaf manifest races. A writer that changes different files only between completed stable reads can still present a confined mixed repository state, but that requires concurrent mutation outside the clean signed-tree execution contract and supplies no path escape or external authority. Unknowns -- No host lacking `O_NOFOLLOW` or `O_DIRECTORY` was available; such a host refuses with `T018` rather than weakening the traversal. No additional candidate survived the full diff, test, fixture, count, generated-copy, or protected-history review. The mandated audit filter is exactly `sapheneia:sapheneia`; the frozen heading, schema, seventeen risk ids and dispositions, waiver, three findings, qualifications, negative evidence, identifiers, paths, locations, counts, hashes, severities, verdict, statuses, facts, inference, unknowns, and leads matched this shaped candidate item by item. Earlier durable history remains untouched, and this round does not claim controller receipt, publication, or integration. A finding-bearing round requires a fresh audit round after these fixes.
## Step 1, round 2 -- 2026-09-01T03:21:48Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-drift=reviewed; generated-copy-drift=reviewed; external-data-egress=not-applicable; subprocess-execution=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; pending-member-overclaim=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable<br><br>Not checked: Pashov X-Ray and Solidity Auditor remain waived because Step 1 changes repository documentation, one Python topology reader, JSON fixtures, tests, and the check map; no Solidity or contract security surface is in scope. The fourteen not-applicable ids keep the dispositions round 1 recorded: this step creates, executes and publishes no demonstration record, output, capability card, source class, status or non-claim; it changes no `EVOLUTION.md`, demonstration ledger, held job, issue or queue state; the topology module opens no socket, network path, credential, model, shell or subprocess; and it changes no root README, plugin landing page, image, PDF, public catalogue or Dokimasia claim. Round 1's declared boundary on atomic coherence against a hostile process rewriting several different repository files between individually stable reads stands unchanged and was not re-established here. The complete check graph on the fixed working tree returned twenty-six of twenty-seven green; `dead-code-suppressions-check` refused with `the checkout has 1 modified tracked file(s): .horos/boundary.json; commit or stash before analysing`, which is that command's clean-checkout precondition rather than a finding, so the graph is re-run on the signed commit carrying this record and its result is not claimed here. Hosted CI, controller receipt, push, pull request, GitHub publication and integration were not checked.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | medium | `.horos/boundary.json:9`; `tests/test_boundary_currency.py:164`; `tests/test_agent_instruction.py` | Round 1's fix commit `505715967f076ee218c67b153ed73230e91ac201` added two tracked files, `audit/rounds/fiat-restore-the-shoggoth-public-front-door-and-demo.md` and its `.synopsis.md` sibling, without regenerating the deterministic Horos boundary committed beside them. Tracked files went 2,295 at the base, 2,303 at signed implementation `1a5cce9b84921969a654cab985f656a30688415a`, and 2,305 at the round 1 head, while the committed `counts.files_walked` stayed at 2,287 against a fresh scan of 2,289. `python3 -m unittest discover -s tests` at that head ran 1,122 tests and failed 2: `test_the_committed_boundary_matches_a_fresh_scan` with `AssertionError: Lists differ: ['.horos/boundary.json#counts'] != []` and `test_horos_boundary_is_current_for_the_scaffold`. `root-suite` was therefore red at the audit head and Step 1 could not meet its stated exit. | fixed and guarded in this round; the boundary was regenerated with `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write`, its only changed field is `counts.files_walked` 2,287 to 2,289 with all 128 entries and the `tracked` universe byte-identical, and the two pre-existing currency tests are red on the round 1 parent and green on this tree |<br><br>Leads not pursued: Facts -- The round 1 failure was reproduced at signed head `505715967f076ee218c67b153ed73230e91ac201` before anything was changed, and the two failing tests were the only failures in 1,122. A fresh `horos scan` of that same tree differed from the committed boundary in `counts.files_walked` alone: 128 entries, the `tracked` universe, and every byte total for binary, content-addressed, generated, lockfile and vendored classes matched exactly. After regeneration, `tests.test_boundary_currency` and `tests.test_agent_instruction` ran 315 tests green, and `root-suite` passed 1,122 of 1,122 in 109.6 seconds inside the complete graph. The three bundled lints were re-run on this round's changed paths and each exited zero: Phylax over the topology module, its tests, the check map and the four fixtures; Ephoros over the module, its tests and the check map; Hypomnema over `AGENTS.md` and the two committed specifications. `cmp` still reports the committed study and runbook byte-identical to `.hexaemeron/study.md` at SHA-256 `d96c4b1d669e4fcda9e5b339a6b1be210c079c63e597cd5021fcf24e1f7cace7` and `.hexaemeron/runbook.md` at SHA-256 `8ef82fa950c0db604eeb50d291c91176d41024fe24941fb03054056a5d9a48a3`; `python3 -m unittest tests.test_shoggoth_topology` passes 13 of 13; and `git diff --check a2b634d8e039af988bf30c8316defccf70071d8d HEAD` is clean. The tracked diff still adds only new specifications, the topology module, its tests and fixtures, the check-map rows, the audit record pair and the regenerated boundary, and changes no pre-existing audit, ADR, specimen, release, `EVOLUTION.md`, portable runtime, `.python-version`, `pyproject.toml`, `LICENSE` or workflow. Inference -- The drift is arithmetic rather than behavioural: walked counts track the tracked-file total, the two audit files landed after the boundary was last written, and no entry, class or exclusion changed, so regenerating restores currency without widening or narrowing what the boundary hides. Unknowns -- Whether a later round or step adds tracked files after its own boundary regeneration is not established by this fix; only the two currency tests stand between that and a red suite. Leads not pursued -- `discover_topology` reopens the repository root by path for each of the seventeen per-plugin walks rather than holding one root descriptor, so a concurrent swap between walks could present a mixed but confined tree; round 1 already recorded that exact non-atomicity as a declared boundary and this round does not reopen it as a finding. The permitted `AGENTS.md` reflow leaves one line longer than the file's wrap convention; rewrapping the two lines after it would change line breaks the receipted runbook amendment did not permit, so it stays. This round claims no controller receipt, push, publication or integration.
## Step 1, round 3 -- 2026-09-01T03:29:58Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-drift=reviewed; generated-copy-drift=reviewed; external-data-egress=not-applicable; subprocess-execution=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; pending-member-overclaim=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable<br><br>Not checked: Pashov X-Ray and Solidity Auditor remain waived for the reason recorded in rounds 1 and 2: Step 1 carries no Solidity or contract security surface. The fourteen not-applicable ids keep their earlier dispositions unchanged. Round 1's declared boundary stands: atomic coherence against a hostile process rewriting several different repository files between individually stable reads is still not established, and this round did not attempt it. Hosted CI, controller receipt, push, pull request, GitHub publication and integration were not checked, and this record makes no claim about any of them.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: Facts -- This round audited signed head `47d574d18ef3391fdb57003d09dcc91b9d205426`, whose only change over the round 2 parent is the regenerated boundary count, the appended round 2 record and its deterministic synopsis. The complete check graph ran on that clean committed tree under source digest `bff3b0e9c1bcea8fe926044ddcdbe288c14667b8f1a8461c26cb17a3a1ee325b` and returned green on all twenty-seven selected checks, including `root-suite` at 108.5 seconds and `dead-code-suppressions-check` at 37.9 seconds, the command that had refused the uncommitted working tree in round 2. The audit-fix runner reported 1,122 of 1,122 tests passed with a complete `elenchus.unittest.v1` report carrying zero failures, errors, skips, expected failures and unexpected successes. The three bundled lints were re-run over this round's changed paths, the regenerated boundary and the audit record included, and Phylax, Ephoros and Hypomnema each exited zero. Imprimatur scored 100.0 with zero defects on the record. Inference -- The round 2 fix is confined to one generated count field, so no behaviour, boundary entry, exclusion class or test expectation moved with it, and the green graph on the committed tree is the same evidence the round 2 record declined to claim. Unknowns -- Whether a later step adds tracked files after its own boundary regeneration is still guarded only by the two currency tests. Leads not pursued -- `discover_topology` still reopens the repository root by path for each per-plugin walk rather than holding one root descriptor; round 1 recorded that non-atomicity as a declared boundary, no round has produced a path escape or authority gain from it, and closing it would widen Step 1 past its stated scope. The permitted `AGENTS.md` reflow leaves one line longer than the file's wrap convention, and rewrapping the lines after it would exceed what the receipted runbook amendment permitted. No new candidate surfaced from the diff, tests, fixtures, counts, generated copies or protected history.
## Step 2, round 1 -- 2026-09-01T04:19:27Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=reviewed; demo-class-inflation=reviewed; source-drift=reviewed; frontier-lane-collision=reviewed; count-drift=reviewed; generated-copy-drift=reviewed; external-data-egress=reviewed; subprocess-execution=reviewed; partial-demo-output=not-applicable; front-door-regression=not-applicable; pending-member-overclaim=reviewed; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; queue-duplication=reviewed; visual-surface-drift=not-applicable<br><br>Not checked: Pashov X-Ray and Solidity Auditor remain waived because Step 2 ships Markdown ledgers, one Python checker, its tests and fixtures, a Kronos lane and two instruction files; no Solidity or contract security surface is in scope. `partial-demo-output` is not applicable because this step builds and publishes no demonstration output: the checker starts no command and writes no file. `front-door-regression`, `portrait-inconsistency` and `visual-surface-drift` are not applicable because no root README, landing page, image, PDF or public catalogue changes here. The checker does not establish that any registered command still passes, that any observation is true, or that a `constructed` record could not have been `real-data` with more effort; running the commands belongs to Step 3 and the contract says so in its own words. Every declared source digest was recomputed from repository bytes, but no source was traced back to the chain, protocol or audit it claims to have come from. Hosted CI, controller receipt, push, pull request, GitHub publication and integration were not checked.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | `scripts/demonstrations.py:38`; `scripts/demonstrations.py:243`; `tests/test_demonstrations.py:243` | Each declared source was capped at 33,554,432 bytes on its own, and nothing bounded a whole run. With 26 records and the record cap of 32 sources each, one `check` could be made to read up to 26 gigabytes before any refusal fired, and the cap that did exist ran after `_read_regular_file` had already been asked for the bytes. The three bundled lints cannot see an aggregate, so both passed over it. | fixed and guarded in this round; a run-scoped `_Budget` of 268,435,456 bytes is spent from the declared size before each read is attempted, `D028` refuses the source that crosses it, and the budget test is red on the signed parent and green on this tree |<br>| S2-R1-02 | low | `scripts/demonstrations.py:433`; `plugins/hexaemeron/skills/DEMONSTRATIONS.md:96`; `tests/test_demonstrations.py:214` | The status set and the frontier were checked separately, so a record could carry `status: absent` beside `frontier.status: mature`. That pair says no demonstration exists and that nothing worth doing remains about it, which retires a demonstration that was never built and hides the exact gap the demo lane exists to close. `not-applicable` with a mature frontier is coherent and had to stay accepted. | fixed and guarded in this round; `D045` refuses `absent` beside a mature demo frontier, a companion test holds `not-applicable` open, and both are red on the signed parent and green here |<br><br>Leads not pursued: Facts -- The complete diff from signed Step 1 head `34f6b8abdeddfae803a19ef21da83bb3bfb53c91` through signed implementation `414c9628f1679673d1bc39a9ba4670cc41b75ffd` was reviewed. The checker discovers the same 26 directories as `scripts/shoggoth_topology.py`, and the live tree records 3 `real-data`, 2 `mixed`, 20 `constructed` and 1 `absent`. Every one of the 25 executable records' commands was run before the record was written and each exited zero offline; the longest was Fiat's checkpoint suite at 138.6 seconds. Reads are the Step 1 no-follow descriptor readers, duplicate JSON keys refuse, and a test asserts the module's own source holds no `subprocess`, `os.system`, `os.exec`, `socket` or `urllib`. `D017` refuses a synthetic source in a `real-data` record, `D018` requires both kinds in a `mixed` one, `D019` refuses a preserved source in a `constructed` one, and `D026` refused the Kronos record during this step when its declared test-module source drifted under an edit made after generation. Kronos binds one ledger name per lane: `K023` refuses a `full` or `phase-only` pass naming a `DEMONSTRATION.md` and a demo pass naming an `EVOLUTION.md`, `K024` refuses a demo pass that is not rank-only or that names a run, and `K022` refuses a demo frontier whose digest does not recompute. `git diff` against `a2b634d8e039af988bf30c8316defccf70071d8d` proves no `EVOLUTION.md` changed. `promise_machine.py check` and `coverage --check` are clean at 87 promises after the Kronos runtime digest was rebound. The complete check graph is green on 27 of 27 at the implementation head, the root suite passes 1,160 of 1,160 after this round's fixes, and Phylax, Ephoros and Hypomnema each exit zero. Inference -- The two findings are boundary gaps rather than behaviour faults: neither let a false status through, and both widen what an untrusted or careless ledger can cost. Unknowns -- Whether every `constructed` record could become `real-data` with a preserved capture is not established here, and this round did not attempt to trace any declared source back to the chain, protocol or audit it names. Leads not pursued -- A record may name an argv whose program or input does not exist, and `check` will not notice, because it starts no command; that is declared negative space in the contract and Step 3 is where a registered command is run. Kronos resolves a candidate ledger with `Path.resolve` and `is_file`, both of which follow symlinks, which is the behaviour that was already there before this lane was added and is not reopened here. This round claims no controller receipt, push, publication or integration.
