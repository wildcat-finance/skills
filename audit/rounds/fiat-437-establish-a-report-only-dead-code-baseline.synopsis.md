Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-437-establish-a-report-only-dead-code-baseline.md | source_sha256=e4173847ab5b3e22967af33cfc536b2f6983790fa68de6ef0f4d07b8d94b272a | h2_count=11
## Step 1, round 1 -- 2026-08-29T21:09:51Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=not-applicable; coverage-failure-as-dead=not-applicable; monitoring-leak=not-applicable; untrusted-parse=not-applicable; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the Pashov suite is waived by the controller because this repository delivery adds no Solidity; the bundled Solidity security suite does not apply, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The Python, coverage, monitoring, Solidity, repository-edge, baseline and suppression subjects arrive in steps 2 through 4, so their seven register ids are not applicable to this Step 1 round. The complete 24-check closure was not repeated; the implementation receipt records 20 green checks and four entry-reproduced macOS or dependency failures. Network, GitHub, push, pull-request, merge, controller-receipt and checkpoint actions were outside this local round and did not occur.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | scripts/dead_code.py:715 | `--output` accepted any repository-relative regular-file target. On the unfixed parent, the guard invoking `report --output a.py` exited 0 and replaced tracked `a.py` with report bytes, contrary to ADR-051's rule that the command must not edit analysed source. | fixed in `d2bf4c6a4ac8ac47847c7a934bc9ccf533a5d470`; output now requires a file below the owned `.dead-code/` sink, the CLI guard leaves tracked `a.py` byte-identical and clean, and the focused suite passes 63 of 63 |<br>| S1-R1-02 | medium | scripts/dead_code.py:734 | The writer checked ancestors by pathname, created a pathname-addressed temporary, swept other prefixed files and replaced by absolute path. An ancestor substitution after the checks therefore detached the commit from the directory that had been inspected; the parent substitution fixture lost the temporary path and returned a write refusal instead of remaining bound to the opened directory. | fixed in `d2bf4c6a4ac8ac47847c7a934bc9ccf533a5d470`; no-follow directory descriptors now anchor creation, target inspection, temporary publication and same-directory replacement, unrelated temporaries are not swept, and the substitution fixture leaves the outside directory empty while publishing through the original opened descriptor |<br><br>Leads not pursued: no further defect was found in the two-file fix range `16c9c1b4a9c896958a4082c1cc2579b170f4915b..d2bf4c6a4ac8ac47847c7a934bc9ccf533a5d470`. The exact Elenchus runner from the Step 1 contract returned `guarded` with 63 tests executed on the parent, four assertion failures, zero infrastructure errors and zero skips. The fixed dead-code suite passes 63 of 63; the dead-code plus Python-contract run passes 73 of 73. Phylax, Ephoros and Hypomnema each print `clean` and exit 0 under pinned CPython 3.14.6; Imprimatur reports 100.0 with zero defects over the changed source prose; `git diff --check` exits 0. Both commits verify as good SSH signatures for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`; the fix commit has author `Dave Coleman <dave@wildcat.finance>`, exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` trailer and exactly one `Wildcat-Origin: shoggoth` trailer. Brevitas report mode exits 1 with B010 for the mandatory single round heading and B011 for the exact two-row findings table; no rewrite was made because adding headings or a false third finding would violate the owning `fiat-audit-round/v2` host structure. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all sixteen risk ids and dispositions, waiver, negative evidence, guarded verdict, both finding ids, severities, paths, mechanisms, impacts, remedies, commit identities, signature identity, test and lint counts, Brevitas refusal and unpursued scope without changing a claim.
## Step 1, round 2 -- 2026-08-29T21:29:18Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=not-applicable; coverage-failure-as-dead=not-applicable; monitoring-leak=not-applicable; untrusted-parse=not-applicable; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: security suite receipt: `waived: this repository delivery adds no Solidity; the bundled Solidity security suite does not apply`; x-ray, solidity-auditor and fizz were not run, and nothing is claimed from them. The Python reachability, coverage, monitoring, tracked-source parse, Solidity attribution, repository-edge, baseline and suppression subjects arrive in steps 2 through 4, so their eight register ids are not applicable to this Step 1 round. The complete 24-check closure was not repeated; the implementation receipt retains its 20 green checks and four entry-reproduced macOS or dependency failures. Network, GitHub, push, pull-request, merge, controller-receipt and checkpoint actions were outside this local round and did not occur.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | medium | scripts/dead_code.py:849 | `atomic_write` derived output parts before opening the repository root. A rename-and-symlink substitution in that gap made directory traversal resolve the replacement path and publish `.dead-code/report.json` outside the intended repository tree. | fixed in `fd822174eb91981862a10f8e619348da4e0f412a`; the root descriptor is acquired before path derivation, descendant traversal starts from `os.dup(root_fd)`, and the guard leaves the substituted directory empty while publishing through the original descriptor |<br><br>Leads not pursued: no additional defect was found after fixing `S1-R2-01`. Defensive fixtures kept owned-sink rejection, post-open root and ancestor anchoring, target-swap isolation, symlink and FIFO target refusal, payload-fsync cleanup, unrelated-temporary preservation and findings-non-gating semantics green. The exact Step 1 Elenchus runner returned `guarded` with 64 tests executed on the parent, one assertion failure, zero infrastructure errors and zero skips. The fixed dead-code suite passes 64 of 64; the dead-code plus Python-contract run passes 74 of 74. Phylax, Ephoros and Hypomnema each print `clean` and exit 0 under pinned CPython 3.14.6; Imprimatur reports 100.0 with zero defects over the shipped prose and source comments; `git diff --check` exits 0. Fix commit `fd822174eb91981862a10f8e619348da4e0f412a` verifies as a good SSH signature for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, has author `Dave Coleman <dave@wildcat.finance>`, exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` trailer and exactly one `Wildcat-Origin: shoggoth` trailer. Brevitas report mode exits 1 with B010 because the append-only log has two round headings rather than three sections, and with B011 because the mandatory round-1 and round-2 tables have two and one finding rows rather than three; no heading or false finding was added because that would violate the owning `fiat-audit-round/v2` host structure. The `sapheneia-durable-record-shape` pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all sixteen risk ids and dispositions, waiver, negative evidence, guarded verdict, finding id, severity, path, mechanism, impact, remedy, commit identity, signature identity, test and lint counts, Brevitas refusal and unpursued scope without changing a claim.
## Step 1, round 3 -- 2026-08-29T21:39:26Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=not-applicable; coverage-failure-as-dead=not-applicable; monitoring-leak=not-applicable; untrusted-parse=not-applicable; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: security suite receipt: `waived: this repository delivery adds no Solidity; the bundled Solidity security suite does not apply`; x-ray, solidity-auditor and fizz were not run, and nothing is claimed from them. The Python reachability, coverage, monitoring, tracked-source parse, Solidity attribution, repository-edge, baseline and suppression subjects arrive in steps 2 through 4, so their eight register ids are not applicable to this Step 1 round. The complete 24-check closure was not repeated; the implementation receipt retains its 20 green checks and four entry-reproduced macOS or dependency failures. Network, GitHub, push, pull-request, merge, controller-receipt and checkpoint actions were outside this local round and did not occur.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R3-01 | medium | scripts/dead_code.py:899 | `command_report` built and rendered the report before it confined the output and opened the repository root. Replacing the repository pathname after rendering made both operations follow the replacement and publish `.dead-code/report.json` outside the analysed tree with exit 0. | fixed in `07eb72e1aa0e606413ab1aa6f9b0ad2e863f2e02`; the command opens the no-follow root descriptor before output derivation or report construction, confinement stays lexical to the canonical root, and publication uses that same descriptor |<br><br>Leads not pursued: no additional defect was found after fixing `S1-R3-01`. Defensive fixtures kept owned-sink rejection, root and ancestor descriptor anchoring, direct root-symlink refusal, target-swap isolation, symlink and FIFO target refusal, payload-fsync cleanup, unrelated-temporary preservation and findings-non-gating semantics green. The exact Step 1 Elenchus runner returned `guarded` with 65 tests executed on the parent, one assertion failure, zero infrastructure errors and zero skips. The fixed dead-code suite passes 65 of 65; the dead-code plus Python-contract run passes 75 of 75. Phylax, Ephoros and Hypomnema each print `clean` and exit 0 under pinned CPython 3.14.6; Imprimatur reports 100.0 with zero defects over the shipped prose and source comments; `git diff --check` exits 0. Fix commit `07eb72e1aa0e606413ab1aa6f9b0ad2e863f2e02` verifies as a good SSH signature for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, has author `Dave Coleman <dave@wildcat.finance>`, exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` trailer and exactly one `Wildcat-Origin: shoggoth` trailer. Brevitas report mode exits 1 with B011 at lines 11, 28 and 44 because the mandatory round-1, round-2 and round-3 findings tables have two, one and one data rows; no false finding was added because that would violate the owning `fiat-audit-round/v2` host structure. The `sapheneia-durable-record-shape` pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all sixteen risk ids and dispositions, waiver, negative evidence, guarded verdict, finding id, severity, path, mechanism, impact, remedy, commit identity, signature identity, test and lint counts, Brevitas refusal and unpursued scope without changing a claim.
## Step 1, round 4 -- 2026-08-29T22:06:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=not-applicable; coverage-failure-as-dead=not-applicable; monitoring-leak=not-applicable; untrusted-parse=not-applicable; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: security suite receipt: `waived: this repository delivery adds no Solidity; the bundled Solidity security suite does not apply`; x-ray, solidity-auditor and fizz were not run, and nothing is claimed from them. The Python reachability, coverage, monitoring, tracked-source parse, Solidity attribution, repository-edge, baseline and suppression subjects arrive in steps 2 through 4, so their eight register ids are not applicable to this Step 1 round. Network, GitHub, push, pull-request, merge, controller-receipt and checkpoint actions were outside this local round and did not occur.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R4-01 | medium | scripts/dead_code.py:319 | Report discovery still followed the mutable repository pathname after `command_report` opened the output root. A transient edit after the clean check changed the Horos partition while the report kept the committed tree identity; a root rename and symlink before discovery made the command analyse the replacement repository and publish that report through the original root descriptor. | fixed in `65534a9a7027993ed18df1075d08d6f9d1e74975`; Git commands now enter the already-open repository descriptor, the Horos boundary is read as a bounded regular blob from the recorded commit, and both transient-boundary and pre-discovery root-substitution guards retain the opened tree |<br><br>Leads not pursued: no additional defect was found after fixing `S1-R4-01`. The exact Step 1 Elenchus runner returned `guarded`: the unfixed parent executed 67 tests with two assertion failures, zero infrastructure errors and zero skips; both guards failed on the misbound universe. The fixed dead-code suite passes 67 of 67, and the dead-code plus Python-contract run passes 77 of 77. The requested `dead-code` affected scope passes all four selected checks. The complete `main...HEAD` closure passes 20 of 24 checks; only `hexaemeron-suite`, `lazarus-suite`, `root-suite` and `synkrisis-suite` remain red, matching the implementation receipt's four entry-reproduced macOS or dependency failures. Phylax, Ephoros and Hypomnema each print `clean` and exit 0 under pinned CPython 3.14.6; Imprimatur reports 100.0 with zero defects over the shipped prose and source comments; Horos reports that the boundary matches the tree; `git diff --check` exits 0. Fix commit `65534a9a7027993ed18df1075d08d6f9d1e74975` verifies as a good SSH signature for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, has author `Dave Coleman <dave@wildcat.finance>`, exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` trailer and exactly one `Wildcat-Origin: shoggoth` trailer. Brevitas report mode exits 1 with B010 for the mandatory single round heading, B011 for the exact one-row findings table and B001 for the owning record's nine lines before that table; no heading, false finding or reordered host field was added because each would violate `fiat-audit-round/v2`. The `sapheneia-durable-record-shape` pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all sixteen risk ids and dispositions, waiver, negative evidence, guarded verdict, finding id, severity, path, mechanism, impact, remedy, commit identity, signature identity, test and lint counts, Brevitas refusal, inherited closure failures and unpursued scope without changing a claim.
## Step 1, round 5 -- 2026-08-29T22:11:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=not-applicable; coverage-failure-as-dead=not-applicable; monitoring-leak=not-applicable; untrusted-parse=not-applicable; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: security suite receipt: `waived: this repository delivery adds no Solidity; the bundled Solidity security suite does not apply`; x-ray, solidity-auditor and fizz were not run, and nothing is claimed from them. The Python reachability, coverage, monitoring, tracked-source parse, Solidity attribution, repository-edge, baseline and suppression subjects arrive in steps 2 through 4, so their eight register ids are not applicable to this Step 1 round. The complete 24-check closure was not repeated after round 4's fix verification; round 4 records 20 green checks and the same four entry-reproduced macOS or dependency failures. Network, GitHub, push, pull-request, merge, controller-receipt and checkpoint actions were outside this local round and did not occur.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: none. This round reviewed the final accumulated Step 1 tree at signed head `fc9b4d86e62d4d350ed096641811221540bd3bf8` and found no further defect. The dead-code suite passes 67 of 67, and the dead-code plus Python-contract run passes 77 of 77. The first combined invocation ran that focused suite concurrently with the affected-scope runner and failed one Python prose-contract test because it observed the frozen round-4 candidate and the runner's transient disposable snapshot; the candidate was removed, the runner had already removed its snapshot, no tracked byte changed, and the exact focused command then passed 77 of 77 in isolation. The exact Step 1 report emitter produced a complete `elenchus.unittest.v1` record with 67 tests, zero failures, zero errors and zero skips. The requested `dead-code` affected scope passes all four selected checks. Phylax, Ephoros and Hypomnema each print `clean` and exit 0 under pinned CPython 3.14.6; Imprimatur reports 100.0 with zero defects over the shipped prose and source comments; the synopsis checker matches every committed view; Horos reports that the boundary matches the tree; `git diff --check` exits 0. Head commit `fc9b4d86e62d4d350ed096641811221540bd3bf8` verifies as a good SSH signature for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, has author `Dave Coleman <dave@wildcat.finance>`, exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` trailer and exactly one `Wildcat-Origin: shoggoth` trailer. Brevitas report mode exits 1 with B010 for the mandatory single round heading, B011 for the canonical one-row zero-finding table and B001 for the owning record's nine lines before that table; no heading, false finding or reordered host field was added because each would violate `fiat-audit-round/v2`. No fix commit and no Elenchus comparison exist for this zero-finding round, so the exact verdict is `null`. The `sapheneia-durable-record-shape` pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all sixteen risk ids and dispositions, waiver, negative evidence, null verdict, canonical zero-finding row, branch head, signature identity, test and lint counts, Brevitas refusal, initial state-contamination failure, clean rerun and unpursued scope without changing a claim.
## Step 2, round 1 -- 2026-08-29T23:19:09Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the controller waived the Pashov suite because this step ships no Solidity, so x-ray, solidity-auditor and fizz did not run and neither Solidity attribution nor repository-edge evidence is claimed. Step 3 owns repository and Solidity signals; step 4 owns baselines and suppressions, so those three register subjects do not exist in this step. Process recovery was driven on macOS 15 with BSD `ps`; Windows has no equivalent host path and was not checked. The monitor and checked commands share the contributor's credentials, so this report is not an attestation against a check that deliberately forges process records. Push, pull-request, merge, issue closure, controller receipt and integration remained outside this local round.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | scripts/dead_code.py | A coverage document could claim `status.state=ran` and one process while carrying `processes=[]`; `analyse_coverage` then emitted never-executed candidates from no observations. It also silently discarded malformed line and branch events. Either route converted incomplete evidence into absence, violating the step's report-only failure rule. | fixed in this commit; `analyse_coverage` now validates check identities and counts, reconciles per-check process and byte aggregates, validates canonical events against the analysed universe, and returns `degraded` with no findings for incomplete evidence. `test_claimed_complete_without_process_aggregate_degrades` and `test_malformed_line_event_refuses_instead_of_implying_absence` were red before the fix and are green after it. |<br>| S2-R1-02 | medium | scripts/dead_code.py | `aggregate_coverage` trusted a checked-run result whose map digest or requested scope differed from the preflight plan, so a report could bind observations to a plan that the runner did not execute. | fixed in this commit; aggregation now requires exact equality for `map_digest`, `requested_scopes` and `selected_checks`. `test_run_map_digest_must_match_the_preflight_plan` was red before the fix and is green after it. |<br>| S2-R1-03 | medium | scripts/dead_code.py | On timeout or interruption, `run_process` killed only its direct runner. A monitored descendant that started a new session survived reparented to pid 1, continuing checks after the wrapper had refused and after its private record directory could be removed. | fixed in this commit; every monitored process carries a random 128-bit run identity, recovery scans bounded process metadata for that exact identity, sends TERM then KILL, waits at most five seconds per signal and refuses if the run cannot drain. Normal completion also refuses if a monitored process survives. `test_recovery_terminates_a_process_carrying_the_run_identity` reproduces the detached shape and is green. |<br>| S2-R1-04 | low | scripts/dead_code.py | `_unused_bindings` treated stores declared by `global` or `nonlocal` as function-local assignments, so `global setting; setting = 1` produced the high-confidence candidate `configure.setting@3`. | fixed in this commit; externally declared names are excluded from the local-binding signal. `test_global_assignment_is_not_reported_as_an_unused_local` was red before the fix and is green after it. |<br><br>Leads not pursued: the audit reviewed the complete eight-file step diff at signed product tip `2425a4aa7cc291c706c11a7cee79cf289fc896fd`; its SSH signature verifies for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, and its machine-readable trailers contain `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`. The five permanent guards were run against the vulnerable tree first and produced four failures plus one error, then all five passed after the fixes; the complete `tests.test_dead_code` suite passes 114 of 114 in 7.606 seconds. With the pinned Python 3.14 directory first on `PATH`, the affected closure passes `dead-code-suite` in 8.2 seconds and all three mandatory lints; only `root-suite` remains red. The root suite passes 607 of 608 in 45.617 seconds, with its sole failure `test_selected_directory_works_as_an_isolated_copy` refusing `receipt.path` as `MP407`; that exact test fails with the same code at the run base `0698092d27871031b6d5521d77f6e8d8dc5dc937`, so the macOS `/var` to `/private/var` alias is inherited and not repaired here. The initial live monitored run exposed a performance defect because its event callback resolved filesystem paths per event; cached locations and `sys.monitoring.DISABLE` reduced the same real check path to about nine seconds, and the guard forbids hot-path resolution. A fork followed by `os._exit` can omit its final process record, which biases the report toward false-positive absence candidates; aggregate reconciliation catches a missing declared record but cannot prove that an undeclared child existed, so all dynamic candidates remain low-confidence and report-only. AST analysis is deliberately conservative and scope-insensitive beyond the guarded global and nonlocal declarations, leaving false negatives possible. A same-credential check can forge a syntactically valid process record or copy the run identity; the tool bounds and labels local evidence rather than claiming hostile-code isolation. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, all sixteen risk ids and dispositions, waived and deferred scope, four finding ids, severities, mechanisms, guards, exact commits, key, identifiers, paths, counts, timings and remaining limitations without changing a claim.
## Step 2, round 2 -- 2026-08-29T23:35:52Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the controller waiver still applies because this step ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Step 3 still owns repository and Solidity signals; step 4 still owns baselines and suppressions. Windows process recovery, deliberate same-credential process-record forgery, push, pull-request, merge, issue closure, controller completion and integration were not checked. The full 24-check closure and its four inherited host or dependency failures were not repeated; round 1 holds that negative evidence.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R2-01 | low | scripts/dead_code.py | The local-binding walk descended into child functions and comprehensions. An assignment owned by `inner` therefore also appeared as the high-confidence outer candidate `outer.nested@3`, and a comprehension target appeared as `values.item@2` even though Python gives it a separate scope. | fixed in this commit; store and declaration collection stops at child lexical scopes while the deliberately conservative load scan still retains possible closure use. `test_nested_assignment_is_not_attributed_to_the_enclosing_function` and `test_comprehension_target_is_not_reported_as_a_function_local` were red before the fix and are green after it. |<br>| S2-R2-02 | medium | scripts/dead_code.py | Coverage event integers used Python's broad `isinstance(value, int)` test, so JSON `true` was accepted as line 1 and could suppress a never-executed candidate. A list-valued branch direction reached set membership and raised `TypeError` instead of a named refusal. | fixed in this commit; line and branch offsets reject booleans, direction requires a string before membership, and both malformed shapes refuse. `test_boolean_line_identity_refuses_instead_of_aliasing_line_one` and `test_non_string_branch_direction_refuses_instead_of_crashing` guard both routes. |<br>| S2-R2-03 | medium | scripts/dead_code.py | A process status could say `state=ran` while also saying `truncated=true` or carrying errors; aggregation treated that contradictory record as complete and allowed absent events to support findings. A non-string state crashed the validator at set membership. | fixed in this commit; one total status validator requires the closed state, boolean truncation and string-error shapes, and completion requires `ran`, no truncation and no errors in both aggregation and report analysis. `test_contradictory_complete_process_status_degrades` and `test_non_string_process_state_refuses_instead_of_crashing` were red before the fix and are green after it. |<br>| S2-R2-04 | medium | scripts/dead_code.py | Terminal check rows were folded into a dictionary without rejecting duplicate ids, and `command_coverage` ignored the checked runner's process exit. A duplicate could overwrite its sibling, while a report claiming `outcome=green` paired with exit 1 still produced a coverage file and returned 0. | fixed in this commit; plan and terminal identities are non-empty and unique, and the process result must be exactly green/0 or red/1 before aggregation. `test_duplicate_terminal_check_refuses_instead_of_overwriting` and `test_green_report_cannot_disagree_with_the_runner_exit` were red before the fix and are green after it. |<br><br>Leads not pursued: this round reviewed the complete round-1 fix tree at signed head `ea3b68980ae584248e2e500741ec0db812e242e3`. The first six guards produced five assertion failures; the runner-exit harness initially mocked repository discovery too broadly and errored before reaching its predicate, then failed as intended after the harness was narrowed. Two later totality guards each reproduced an unhandled `TypeError`. All eight guards now pass. The first live monitored rerun then degraded exactly as designed because the new command-level guard inherited the outer coverage marker and its deliberate nested coverage call hit the recursion refusal before the intended predicate; clearing only that marker inside the guard preserves the product refusal and leaves the complete focused suite green at 122 of 122 in 7.149 seconds. The exact monitored scope was not rerun before this record was frozen. The exact prior 20,067-byte Step 1 audit prefix remains byte-identical at SHA-256 `4327f9ffd20edd32d6fd6a2e685cbe4e85fd4f18f55a10861c88aca6cdd5e29b`; round 1 follows its canonical separator and this round appends after the controller's 26,342-byte receipt. The scope-aware store walk remains deliberately conservative: nested loads can retain an outer candidate even when the nested name is shadowed, and named expressions inside comprehensions can escape this signal as false negatives. A same-credential check can still forge a coherent process record, and a child that exits without `atexit` can still omit one; those limits keep dynamic findings low-confidence and report-only. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, sixteen risk dispositions, waived and deferred scope, four finding ids and severities, mechanisms, exact guards, commit, digests, byte offsets, counts, timings and remaining limits without changing a claim.
## Step 2, round 3 -- 2026-08-29T23:46:18Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the controller waiver still applies because this step ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Step 3 still owns repository and Solidity signals; step 4 still owns baselines and suppressions. Windows process recovery, deliberate same-credential evidence forgery, descendants that exit without `atexit`, the complete 24-check closure, network, GitHub, push, pull-request, merge, issue closure, controller completion and integration were not checked. The exact monitored scope will be rerun against the signed fix commit; this record does not claim that later result.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R3-01 | low | scripts/dead_code.py:1806 | `_function_targets` used the first AST body statement as the execution target. Python 3.14 removes a function docstring from executable line events, so a called function whose observed first executable line was line 3 still produced the candidate `documented@2`. | fixed in this commit; the target skips a leading string docstring and emits nothing for a docstring-only body. `test_observed_function_skips_its_optimised_away_docstring` was red before the fix and is green after it. |<br>| S2-R3-02 | medium | scripts/dead_code_monitoring/sitecustomize.py:170 | `_restore` relied on its stale `owns_tool` flag. If another component freed the probe's monitoring id and claimed it before shutdown, the probe cleared that component's callbacks and freed its id. | fixed in this commit; cleanup compares the current owner with the probe's exact tool name, leaves a replacement owner untouched and records an ownership-change error so the process evidence degrades. `test_probe_does_not_clobber_a_reassigned_tool_id` was red before the fix and is green after it. |<br>| S2-R3-03 | medium | scripts/dead_code.py:1754 | Coverage analysis checked the outer coverage schema and tree but accepted a foreign tool id or unknown checked-runner plan schema. Such a document could supply absence evidence while claiming provenance the analyser never verified. | fixed in this commit; analysis requires the Python 3.14 `sys.monitoring` identity plus the checked-plan schema, SHA-256 map digest and non-empty unique scopes. `test_coverage_tool_identity_is_required` and `test_coverage_plan_schema_is_required` were red before the fix and are green after it. |<br><br>Leads not pursued: this round reviewed the complete round-2 fix tree at signed head `8d860e6decf0684fdcbe7a015db1daf95a5441bd`. The four permanent guards failed against that tree and now pass. The focused suite passes 126 of 126 in 7.308 seconds. With the pinned CPython 3.14.6 directory first on `PATH`, the affected closure passes `dead-code-suite` and the three mandatory lints; Phylax, Ephoros and Hypomnema also pass directly. An earlier affected run inherited the ambient path and returned only `dead-code-suite` red; the pinned replay passed all four checks without a tracked-byte change. After the audit and Horos records entered the diff, the six-path closure selected five checks: the dead-code suite and three lints passed, while `root-suite` passed 619 of 620 and reproduced the inherited `test_selected_directory_works_as_an_isolated_copy` refusal `{"code":"MP407","field":"receipt.path","outcome":"refused","schema":"model-proxy-diagnostic/v1"}`. Imprimatur reports 100.0 with zero defects over the audit source and synopsis, and the deterministic synopsis check matches every committed view. Brevitas report mode exits 1 only on five pre-existing Step 1 tables with fewer than three findings and the pre-existing synopsis qualifier stacks for Step 2 rounds 1 and 2; no round-3 row is flagged. The exact controller-receipted prefix was 32,143 bytes at SHA-256 `f94e6541ce28e3645c420b02b82e00d52d3e68c5c714de5af507d205c9cc04ca` before this canonical separator. Scope-insensitive static analysis can still retain shadowed bindings, named expressions in comprehensions can escape the local-binding signal, a same-credential check can forge coherent evidence, and a process that bypasses `atexit` can omit its record. Those limits leave every dynamic candidate low-confidence and report-only. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, sixteen risk dispositions, waiver, deferred and unchecked scope, three finding ids and severities, mechanisms, exact guards, signed parent, prefix digest, counts, runtime, path-sensitive replay, inherited failure, prose-check results and remaining limits without changing a claim.
## Step 2, round 4 -- 2026-08-29T23:55:05Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the controller waiver still applies because this step ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Step 3 still owns repository and Solidity signals; step 4 still owns baselines and suppressions. Windows process recovery, deliberate same-credential evidence forgery, descendants that exit without `atexit`, the complete 24-check closure, network, GitHub, push, pull-request, merge, issue closure, controller completion and integration were not checked. The exact monitored scope will be rerun against the signed fix commit; this record does not claim that later result.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R4-01 | medium | scripts/dead_code.py:1553 | Coverage aggregation copied every monitored process's raw `argv` into the persistent aggregate and included it in the public process-id digest. A nested Python command carrying a credential in an argument therefore left that credential in `.dead-code/coverage.json` after the raw per-process directory was removed. | fixed in this commit; argv is used only transiently to attribute a containment group to its declared check, then omitted from both the aggregate and process-id digest. `test_public_coverage_does_not_retain_process_argv` was red before the fix and is green after it. |<br><br>Leads not pursued: this round reviewed the complete round-3 fix tree at signed head `0ad87c8bf08bf1cb763fba21f6d72a608db95c26`. The permanent guard failed against that tree and now passes; it also proves that changing only a child's argv cannot change its public process id. The focused suite passes 127 of 127 in 7.939 seconds. With pinned CPython 3.14.6 first on `PATH`, the affected closure passes `dead-code-suite` and all three mandatory lints, while Phylax, Ephoros and Hypomnema also pass directly. The parent commit's live proof completed 126 tests, four named checks, ten process records, 136 line events and 29 branch events; both analyzers ran and emitted 6,685 report-only candidates. The exact controller-receipted prefix is 37,323 bytes at SHA-256 `ce9463261f6a72e36ddef0782823f53117de9f8838e89b81a29f7dec15d5edf8` before this canonical separator. The aggregate still carries process ids, parent ids, event locations, byte counts and bounded status errors because they answer attribution and completeness questions; it no longer carries command arguments. Scope-insensitive static analysis, same-credential forgery and processes that bypass `atexit` retain the prior limits. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, sixteen risk dispositions, waiver, deferred and unchecked scope, finding id and severity, exposure mechanism, guard, signed parent, prefix digest, counts, runtime, retained metadata and remaining limits without changing a claim.
## Step 2, round 5 -- 2026-08-29T23:58:49Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=not-applicable; repository-edge-overclaim=not-applicable; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: the controller waiver still applies because this step ships no Solidity, so x-ray, solidity-auditor and fizz did not run. Step 3 still owns repository and Solidity signals; step 4 still owns baselines and suppressions. Windows process recovery, deliberate same-credential evidence forgery, descendants that exit without `atexit`, the complete 24-check closure, network, GitHub, push, pull-request, merge, issue closure, controller completion and integration were not checked. Round 3 retains the current inherited `root-suite` failure evidence; that suite was not repeated in this clean round.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: none. This round reviewed the final accumulated Step 2 tree at signed head `1658148608d15e8a314bb71ea71fec6597c608d2` and found no further defect. Its SSH signature verifies for `dave@wildcat.finance` under key `SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk`, with the required Shoggoth co-author and origin trailers. The exact Elenchus emitter produced a complete `elenchus.unittest.v1` record with 127 tests, zero failures, zero errors and zero skips. The signed live coverage record binds the same commit and reports `ran` across four named checks and ten processes with 136 line events, 29 branch events and zero public argv fields; both analyzers ran and emitted 6,685 report-only candidates. No raw coverage-process directory remained, and the aggregate contains neither an `argv` key nor either guard secret. The exact `dead-code` scope and its three lints all pass, direct Phylax, Ephoros and Hypomnema each print `clean`, and `git diff --check` exits 0. The exact controller-receipted prefix is 40,789 bytes at SHA-256 `812d031f0a41c10b06c3701af63f2a7130942887de78f5ec451751552128fabe` before this canonical separator. No fix commit or Elenchus comparison exists for this zero-finding round, so the verdict is `null`. Scope-insensitive static analysis, same-credential forgery, processes that bypass `atexit`, Windows recovery and the inherited root-suite refusal remain explicit limits rather than clean claims. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, sixteen risk dispositions, waiver, deferred and unchecked scope, canonical zero row, null verdict, signed head, key, trailers, exact proof counts, absence checks, prefix digest and remaining limits without changing a claim.
## Step 3, round 1 -- 2026-08-30T00:38:44Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: discovery-collapse=reviewed; tree-prefix-classification=reviewed; analyser-absence-as-clean=reviewed; dynamic-reference-blindness=reviewed; coverage-failure-as-dead=reviewed; monitoring-leak=reviewed; untrusted-parse=reviewed; subprocess-argv=reviewed; solidity-project-attribution=reviewed; repository-edge-overclaim=reviewed; report-parity=reviewed; baseline-staleness=not-applicable; suppression-abuse=not-applicable; path-confinement=reviewed; partial-write=reviewed; finding-overclaim=reviewed<br><br>Not checked: Step 4 still owns the pinned baseline, suppression schema, operator demonstration and warm-run measurement. The complete 24-check closure, hosted CI, network publication, GitHub, push, pull-request, merge, issue closure, controller completion and integration were outside this local round and did not occur. External Slither and Forge behaviour outside their fixed version and analysis argv remains third-party behaviour; this round confines their project execution and records their output, exit, duration and version rather than treating either tool as trusted.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | medium | scripts/dead_code.py:2018 | Slither and Forge analysis ran with output and cache paths redirected but with the live Foundry project as cwd. Project compilation can execute repository-controlled configuration or tooling and could therefore mutate tracked source while a report still named the recorded Git tree. | fixed in this commit; each analysis now runs against a bounded, manually materialised `git archive` snapshot of the recorded commit in a private temporary directory, while tool outputs remain outside that snapshot. `test_tool_execution_cannot_mutate_the_live_project` was red against the Step 3 product head and proves the live source remains byte-identical. |<br>| S3-R1-02 | medium | scripts/dead_code.py:1635 | An oversized or non-UTF-8 declaration source that was only a referrer, not itself a repository object family, lost its outgoing edges without degrading any family. The incomplete graph still reported `ran` and could emit false orphan candidates. | fixed in this commit; any unreadable declaration source degrades every repository family, records the bounded reason and suppresses repository findings. `test_unreadable_referrer_degrades_every_family_and_suppresses_candidates` was red against the Step 3 product head. |<br>| S3-R1-03 | medium | scripts/dead_code.py:1956 | Forge coverage parsing skipped a malformed table row when another row was valid. The adapter could therefore label the project evidence `passed`, retain only the parseable subset and derive findings from an incomplete coverage table. | fixed in this commit; every source row must have exactly five cells and four valid bounded metrics, and duplicate or impossible rows refuse the whole payload. `test_partially_malformed_forge_table_refuses_all_rows` was red against the Step 3 product head. |<br>| S3-R1-04 | low | scripts/dead_code.py:3309 | Tool-controlled version, detail or refusal text was interpolated into the text report without escaping line separators. A diagnostic containing a newline could inject a forged status-looking line even though the JSON report remained structurally correct. | fixed in this commit; every dynamic text-report field is JSON-string escaped without quotes and U+2028/U+2029 are rendered explicitly. `test_text_rendering_escapes_untrusted_record_newlines` was red against the Step 3 product head. |<br><br>Leads not pursued: the four permanent guards each failed against signed Step 3 product head `3505958e9368de09d66f8759afd804b19d9f606e` and now pass. The complete focused emitter produced a complete `elenchus.unittest.v1` record with 171 tests, zero failures, zero errors and zero skips. The exact prior Step 1 and Step 2 audit record is byte-identical to its published Step 2 audit head and forms a 43,899-byte prefix at SHA-256 `74f2bd9ea0488e7e81ffe1743451fa809ecedfa656449aad4ad90b1c35c7e16f` before this canonical separator. The snapshot adapter accepts only analysed regular files from the exact commit, caps file count, individual bytes and aggregate bytes, refuses missing or repeated paths, never extracts archive paths directly and runs analysis with fixed argv and no shell. Version queries remain fixed `--version` commands in the live project cwd, because they do not execute the project analysis path; their identity output is bounded and rejects controls. Literal repository parsers still cannot prove runtime-computed or external references, so complete families retain medium confidence and a named false-positive boundary, while any computed boundary lowers confidence. Slither and Forge remain advisory and can miss execution outside the invoked tests. The bounded `sapheneia-durable-record-shape` pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema, sixteen risk dispositions, deferred and unchecked scope, four finding ids and severities, mechanisms, impacts, exact guards, signed product head, prefix digest, proof counts, snapshot bounds and remaining limits without changing a claim.
