## Step 1, round 1 -- 2026-08-30T01:19:18Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the waived Pashov Solidity suite, because this step changes no Solidity; decoder, formatter, manifest, tokenizer, model-family, mutation, or measurement behaviour reserved for steps 2 through 5; external repositories, network services, credentials, native Windows, and CI; plugin suites outside the checked runner's selected root, documentation, schema, and repository-lint scopes; and a structured Elenchus report adapter, because the source-bound runner declares UTF-8 unittest text and no other runner or report format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | high | schemas/agent-instruction-v1.schema.json | The version-1 Promise object required its id, evidence, evidence classes, boundary, authorisations, consequence, refusals, recovery, and exceptions but omitted the governed Promise claim itself; compact opcode `M` likewise carried only the id. A model could therefore retain every authority field while losing the claim those fields qualify, so the required Promise Machine fixture could not round-trip a complete promise. The added schema guard reproduced this as one assertion failure on the unfixed step tree. | fixed in this commit; `claim` is now a required exact literal, `M` carries it, and the parent-tree guard records the omission |

Leads not pursued: the physical-line and decoded-literal caps overlap, so step 2 must exercise each limit with every other limit still satisfied and refuse an encoded record over 65,536 bytes; this is not a second step-1 finding because the limits are conjunctive and the contract already requires all other rules to pass. The exact source-bound parent report is plain UTF-8 unittest text rather than one of the current Elenchus script's structured adapters, so the declared report bytes were inspected directly and no nearby runner, output, or format was substituted. No further lead was found in the full diff or the eighteen-item risk review.

## Step 1, round 2 -- 2026-08-30T01:24:40Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the waived Pashov Solidity suite, because the fixed Step 1 tree changes no Solidity; decoder, formatter, manifest, tokenizer, model-family, mutation, or measurement behaviour reserved for steps 2 through 5; external repositories, network services, credentials, native Windows, and CI; plugin suites outside the checked runner's selected root, documentation, schema, and repository-lint scopes; and a structured Elenchus report adapter, because the source-bound runner declares UTF-8 unittest text and no other runner or report format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | medium | .horos/boundary.json | Committing round 1 added the audit record and its generated synopsis as two tracked files after the Step 1 boundary count had been fixed at 2,073. A fresh scan walked 2,075 files, and `BoundaryCurrencyTests.test_the_committed_boundary_matches_a_fresh_scan` failed with `.horos/boundary.json#counts`; the signed fixed tree therefore did not retain the root-suite result observed before those paths became committed history. | fixed in this commit; a complete Horos write records 2,075 and the focused Step 1 runner now carries the same fresh-scan guard |

Leads not pursued: round 1's Promise-claim fix still holds in the schema, compact contract, and focused guard. The physical-line and decoded-literal caps remain a step-2 test-design constraint rather than a second step-1 finding for the reason round 1 records. The parent and fixed source-bound reports remain plain UTF-8 unittest text and were inspected directly without substituting a structured adapter. No further lead was found in the complete fixed diff or the eighteen-item risk review.

## Step 1, round 3 -- 2026-08-30T01:33:26Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the waived Pashov Solidity suite, because the fixed Step 1 tree changes no Solidity; decoder, formatter, manifest, tokenizer, model-family, mutation, or measurement behaviour reserved for steps 2 through 5; external repositories, network services, credentials, native Windows, and CI; plugin suites outside the checked runner's selected root, documentation, schema, and repository-lint scopes; and an Elenchus replay, because this zero-finding round makes no product fix

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: round 1's Promise-claim fix and round 2's Horos currency fix remain present and covered by the focused runner. The physical-line and decoded-literal cap overlap remains a step-2 test-design constraint, and the source-bound runner remains plain UTF-8 unittest text without a structured Elenchus adapter. The complete fixed diff contains only runbook-authorised product paths plus the append-only audit record and synopsis; no further lead emerged from the eighteen-item review.

## Step 2, round 1 -- 2026-08-30T02:59:12Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; source-to-model question fixtures and mutation scoring reserved for Step 3; manifest, compression, tokenizer, bootstrap-cost and cross-family evaluation work reserved for Steps 4 and 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | `scripts/agent_instruction.py:157`; `scripts/agent_instruction.py:453`; `scripts/agent_instruction.py:579`; `tests/test_agent_instruction.py:410`; `tests/test_agent_instruction.py:634`; `tests/test_agent_instruction.py:880`; `tests/test_agent_instruction.py:951` | Canonical decimals have no digit cap, but binding order, span comparison and compact literal length used Python `int()`. Under Python 3.14, 4,301 decimal digits escaped as `ValueError` instead of one `WAI-*` refusal; a valid model and the required valid 65,536-byte physical-line boundary could not round-trip, while the original at-limit tests parsed invalid records without completing model validation. | fixed and regression-tested in this commit; canonical decimal strings now compare by length and lexical value, literal lengths refuse above 65,000 before safe conversion, and complete models cover exact 65,536-byte line, 16,384-line and 1,048,576-byte file limits plus valid line limit-plus-one refusal |
| S2-R1-02 | medium | `scripts/agent_instruction.py:1050`; `tests/test_agent_instruction.py:1127` | Atomic output derived the sibling temporary name as `.{leaf}.wai-<token>`. An existing filesystem-valid 255-byte target component therefore required a 293-byte temporary component and refused `WAI-E-IO.WRITE`; the old target remained intact, but a valid selected output could not be replaced. | fixed and regression-tested in this commit; the exclusive sibling name is a target-independent 37-byte `.wai-<token>`, and the maximum filesystem component now replaces atomically without leaving a temporary file |

Leads not pursued: The complete Step 2 diff from signed base `ddc5334dc8204e685a7612216d766ad770eda3d4` through signed implementation `b4807a8785ec6e773c3635dcee7ee110f79e1045` and this repaired tree was reviewed for all 18 risks. Duplicate keys and unknown versions, opcodes, keys, enums and escapes refuse; reference coverage, relation cycles, scope ancestry, exception targets and binding overlap remain closed; decode exposes no partial model; symlink, FIFO, directory, traversal and failed-replace paths remain confined; and result records remain one bounded JSON line. `CompactParser` materialises at most the already bounded 1,048,576-byte, 16,384-line input before semantic count validation; this was not promoted because the outer caps precede allocation and no model returns before every semantic cap passes. The exact fixed-tree runner passed 134 of 134 tests; `.hexaemeron/audit-fix-step-2.txt` has SHA-256 `ccd1cc2a474aa25231b86801ca878a8405e3c7f75988c11d8d621461f6cf5050`. The exact parent overlay records assertion failures for both mechanisms and no infrastructure error, so the source-bound plain-text Elenchus verdict is `guarded`; no structured adapter was substituted. The root suite passed 641 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases; `self-test`, `git diff --check`, and the changed-tree Phylax, Ephoros and Hypomnema commands exited zero. Imprimatur scored this record 100.0 with zero defects. Brevitas reported B010 and B011 because Fiat's required one-record host has one heading and the exact two-finding table has two rows; adding headings or an invented row would change protected structure or evidence, so no Brevitas-clean claim is made. An additional repository-wide Hypomnema invocation including the pre-existing generated `.agents/skills/promise-machine/runtime` mirror exited 1 on 71 unresolved mirror links; Step 2 changes none of those paths, and the required changed-document review is clean. No third product defect survived review.

## Step 2, round 2 -- 2026-08-30T03:19:33Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; source-to-model question fixtures and mutation scoring reserved for Step 3; manifest, compression, tokenizer, bootstrap-cost and cross-family evaluation work reserved for Steps 4 and 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | `scripts/agent_instruction.py:193`; `scripts/agent_instruction.py:806`; `tests/test_agent_instruction.py:452`; `tests/test_agent_instruction.py:823`; `tests/test_agent_instruction.py:1089` | The fixed 786,432-byte cap promises to cover all decoded literals, but model validation counted only literal objects and compact decoding reset its counter for each field. Document, source, section, directive, scope, promise and exception ids; source paths and digests; exception targets; relation references; and binding references and decimal spans were omitted. A canonical 926,939-byte model and 837,643-byte compact document carrying exactly 786,433 decoded literal bytes therefore passed both validation and decode. | fixed and regression-tested in this commit; model validation counts every compact literal occurrence, the parser refuses the aggregate before model validation, and exact-limit plus limit-plus-one guards cover both paths |

Leads not pursued: Round 1's decimal/line-bound and target-independent temporary-name fixes remain present. Their implementation-parent overlay produced five assertion failures and no infrastructure error; the repaired tree passed adjacent 65,000-byte decimal, 65,536-byte line, 512-byte path, maximum-component, pre-replace sync, post-replace directory-sync, collision-cleanup and no-partial probes. For this round, the exact repaired runner passed 136 of 136 tests and `.hexaemeron/audit-fix-step-2.txt` has SHA-256 `c46915ad62a25f1386c65dfba5400e4eed996dd5ecef3911c675898bf9f54b67`; overlaying its tests on parent `3bbdb194af6c7b05cfb1abc8c5c7a4b44a17ec97` produced three assertion failures and no infrastructure error, so the source-bound plain-text Elenchus verdict is `guarded` without substituting a structured adapter. The full root suite passed 643 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases. Directory sync can fail after an atomic replace and leave the complete new file visible but not yet durably named; this was not promoted because atomicity still holds and the stable I/O refusal preserves the durability uncertainty. No further product defect survived the complete fixed diff or eighteen-risk review.

## Step 2, round 3 -- 2026-08-30T03:39:09Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; source-to-model question fixtures and mutation scoring reserved for Step 3; manifest, compression, tokenizer, bootstrap-cost and cross-family evaluation work reserved for Steps 4 and 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | `scripts/agent_instruction.py:193`; `schemas/agent-instruction-v1.schema.json:65`; `tests/test_agent_instruction.py:537`; `tests/test_agent_instruction.py:744` | Version 1 caps every decoded literal at 65,000 UTF-8 bytes, but canonical binding decimals reached `_decimal` and the aggregate counter without the per-value check applied to literal objects. A canonical model with a 65,001-byte `end` therefore validated and formatted into 130,324 compact bytes with a 65,047-byte longest line, then compact decode refused `WAI-E-BOUNDS.LITERAL`; the schema's decimal shape also omitted the matching early `maxLength`. | fixed and regression-tested in this commit; the shared compact-literal path now enforces the per-value cap before aggregation, the decimal schema carries `maxLength: 65000`, and exact-limit, limit-plus-one and schema-agreement guards cover the model and round trip |

Leads not pursued: Round 1's five parent-red decimal, line and temporary-name assertions and Round 2's three parent-red aggregate-counter assertions remain green on this tree. The exact Step 2 runner passed 139 of 139 tests and `.hexaemeron/audit-fix-step-2.txt` has SHA-256 `c6b1652f7bb4e04be4b86701942d4915651b74b414886a71cd64f8a3a487453e`; overlaying the tests on parent `7f73b5ae9d2bda5441fc0128d4fe7e623c057884` produced two assertion failures, no infrastructure error, and report SHA-256 `bbade7069429117075f11ffdea0df2d611c7309e4ec0385d709974b3a6af380d`, so the source-bound plain-text Elenchus verdict is `guarded` without substituting a structured adapter. Independent model/compact accounting matched across minimal, complete, exact 786,432-byte aggregate and exact 65,000-byte decimal models; each limit-plus-one specimen refused with its declared stable code. The full root suite passed 646 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases. Decimal syntax is checked before the per-value count, but the captured input is already limited to 1,048,576 bytes, the canonical regex is linear, and no model returns after a cap failure, so this was not promoted. No further product defect survived the complete fixed diff or eighteen-risk review.

## Step 2, round 4 -- 2026-08-30T03:54:39Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; source-to-model question fixtures and mutation scoring reserved for Step 3; manifest, compression, tokenizer, bootstrap-cost and cross-family evaluation work reserved for Steps 4 and 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | medium | `scripts/agent_instruction.py:381`; `docs/agent-instruction-language-v1.md:65`; `tests/test_agent_instruction.py:657` | Source records were unique only by id while span-overlap validation grouped bindings by source id. Two ids could repeat `source.md` and the same SHA-256, letting sibling directives claim overlapping `10..70` and `20..50` spans of the same physical bytes; validation and format/decode round-trip accepted the model. | fixed and regression-tested in this commit; source paths are unique within a model, validation and compact decoding refuse an alias with `WAI-E-REFERENCE.DUPLICATE_SOURCE_PATH`, and the guard preserves the exact sibling-span bypass |

Leads not pursued: All four defects guarded in rounds 1 through 3 remain green. The exact Step 2 runner passed 140 of 140 tests and `.hexaemeron/audit-fix-step-2.txt` has SHA-256 `61dc22fc33829c1ac003aa3f011f477cebaacb7c1f379f2f09a9b20270af8db0`; the same runner with the new test on parent `ef1d96a98049c77e45a4be5597c5b5a92bcc225b` produced one assertion failure, no infrastructure error, and report SHA-256 `2f179e39fb616c83232a3f2ae2ea0c21cfcfffbc682108a37485f9db62063889`, so the source-bound plain-text Elenchus verdict is `guarded` without substituting a structured adapter. Independent accounting matched minimal, complete, exact 786,432-byte aggregate and exact 65,000-byte decimal models; all limit-plus-one specimens refused with their declared stable code. Pre-replace sync failure preserved the old file, post-replace directory-sync failure left the complete new file visible with a refusal, and collision, zero-write, FIFO, maximum-component and symlink-root probes stayed confined and left no unintended temporary file. The full root suite passed 647 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases. No further product defect survived the complete fixed Step 2 diff or eighteen-risk review.

## Step 2, round 5 -- 2026-08-30T04:06:00Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=not-applicable; closed-answer-omission=not-applicable; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; source-to-model question fixtures and mutation scoring reserved for Step 3; manifest, compression, tokenizer, bootstrap-cost and cross-family evaluation work reserved for Steps 4 and 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and an Elenchus replay, because this zero-finding round makes no product fix

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Direct validation and compact decoding refuse a repeated source path with `WAI-E-REFERENCE.DUPLICATE_SOURCE_PATH` whether its digest matches or differs, while distinct paths with one digest still round-trip; unrelated bindings on one source retain overlap enforcement. JSON Schema carries each source path's shape while cross-source uniqueness remains a documented semantic-validator rule, like id and reference closure, so the schema, contract and runtime do not assign conflicting meanings. All five defects guarded in rounds 1 through 4 remain green under the exact 140-test Step 2 runner. Exact and limit-plus-one literal, aggregate, decimal, line, file, count and path specimens retain their declared outcomes; direct, decoder, no-partial, symlink, special-file and atomic-output checks remain green. The full root suite passed 647 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases. No further product defect survived the complete fixed Step 2 diff or eighteen-risk review.

## Step 3, round 1 -- 2026-08-30T06:03:45Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=reviewed; closed-answer-omission=reviewed; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; tokenizer profiles, bootstrap-cost measurement, recorded model-family identity and cross-family execution reserved for Step 4; report packaging and generated Promise Machine copy work reserved for Step 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py` | Manifest JSON permits bounded integers for schema validation, but a 4,301-digit integer reached Python `int()` before any digit cap. Python 3.14 raised a raw `ValueError`, so the CLI escaped without a stable refusal or bounded result record. | fixed and regression-tested in this commit; integer digit length is checked before conversion and the specimen refuses `WAI-E-BOUNDS.NUMBER` |
| S3-R1-02 | high | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py`; fixture `source-spans.json` records | Source-span validation checked each claimed slice digest but did not keep bindings inside the manifest's reviewed source envelope or require the binding reviewer to equal the manifest reviewer. Jointly rebound model and span bytes could therefore move governed meaning outside the reviewed envelope or attribute it to another reviewer. | fixed and regression-tested in this commit; every span stays inside its digest-bound envelope and reviewer provenance is exact |
| S3-R1-03 | high | `scripts/agent_instruction.py`; `tests/fixtures/agent-instruction-v1/manifest.json`; `tests/fixtures/agent-instruction-v1/manifest.schema.json`; `tests/test_agent_instruction.py` | Manifest totals were self-consistent sums rather than fixed corpus commitments. A coherent edit could swap a source identity or remove a question, governed binding or mutation, update sibling digests and counts, and still pass. | fixed and regression-tested in this commit; the schema and validator freeze all three source identities plus per-fixture and global totals of 15 bindings, 9 questions and 14 mutations |
| S3-R1-04 | medium | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py` | Confined fixture-directory checking called `os.listdir()` before enforcing the five-entry contract, materialising every name in an attacker-controlled directory and defeating the intended pre-allocation fan-out bound. | fixed and regression-tested in this commit; bounded `os.scandir()` iteration stops at the declared limit plus one |
| S3-R1-05 | high | `scripts/agent_instruction.py`; `docs/agent-instruction-language-v1.md`; fixture `mutations.json` records; `tests/test_agent_instruction.py` | Only the Promise Machine fixture carried a negation mutation, and it required only a model-digest change. Fiat and Horos had no dropped-negation specimen and no fixture proved that negation loss changed a declared answer. | fixed and regression-tested in this commit; each fixture has exactly one question-linked negation mutation with an `answer-change` expectation |
| S3-R1-06 | medium | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py` | A structural mutation expected to refuse with `WAI-E-` passed when the observed code merely started with that prefix. Any unrelated refusal could satisfy the case and hide the wrong failure mechanism. | fixed and regression-tested in this commit; structural refusal codes compare for exact equality |
| S3-R1-07 | medium | `scripts/agent_instruction.py`; `docs/agent-instruction-language-v1.md`; fixture `mutations.json` records; `tests/test_agent_instruction.py` | Exact-literal mutation coverage exercised only a command while the corpus uses identifier, path, SHA-256, command, number and text literals. The other five byte-sensitive classes could drift without a declared specimen. | fixed and regression-tested in this commit; literal-class metadata, target checks and corpus closure enforce exactly one checked mutation for every used class |
| S3-R1-08 | high | `scripts/agent_instruction.py`; `tests/fixtures/agent-instruction-v1/manifest.json`; `tests/fixtures/agent-instruction-v1/manifest.schema.json`; `tests/test_agent_instruction.py` | Runtime accepted a coherently replaced schema after its digest was rebound in the manifest; broad shape checks did not detect the replacement. Fixture row identities and corpus cardinalities were also open, allowing the replacement to authorise a smaller corpus. | fixed and regression-tested in this commit; runtime pins the canonical schema digest and the schema freezes row order, identities, sources and counts with closed `prefixItems` |
| S3-R1-09 | medium | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py` | A readable manifest that later refused emitted an empty `manifest_sha256` because the CLI did not retain the digest until after the check succeeded. Failure records could not be correlated to the exact supplied manifest bytes. | fixed and regression-tested in this commit; the CLI reads once, records the digest before validation and reuses those same bounded bytes |
| S3-R1-10 | high | `tests/fixtures/agent-instruction-v1/fiat-study-runbook-phase/model.json`; sibling compact, question, mutation and source-span records; `tests/test_agent_instruction.py` | The Fiat source requires a conditional marketplace reassessment after the study and before the runbook when a `labs_marketplace` receipt exists, but the reviewed model omitted that directive and the closed questions did not expose the loss. | fixed and regression-tested in this commit; a source-bound conditional directive, condition and order relations round-trip, and a closed question now requires the post-study reassessment |

Leads not pursued: The complete Step 3 diff from signed parent `7305e750f06c5180f380d0e54af71e825e32587e` through signed implementation `d61c1335970feab712229da03bee18f3b94da49b` and this repaired tree was reviewed against all 18 risks. The exact fixed-tree runner passed 70 of 70 tests and `.hexaemeron/audit-fix-step-3.txt` has SHA-256 `0bccf8a246af2875d954214a6d96002f55e72c5d6bb85d220c4ee8e86e3e96cd`. Five parent overlays preserved assertion-only failures with SHA-256 `b4d5ffb5e90d396bce6de2fc89651988938fb7074cbb190a43a3a6e6e1d8fc66`, `50806342235fca8a76e98519b1536e018ac66e70ed1b22f831ad2ab19a1a4c7b`, `94385294de36392d85bd4b2feb6b17ac453450076f5d9e0bb076868e390f43e2`, `e2284cff9db2c2308cc6f6c75f93831fc4756ed2de955bf0ff9cab9ece49f20d` and `a19d8e99ec2b0adc740b688a9fbbaa8c845416097d02c78bd0dbd5f2b3b81c26`, so the source-bound plain-text Elenchus verdict is `guarded` without substituting another adapter. The manifest check passed three fixtures, 15 bindings, 9 questions, 14 mutations and three round trips with zero failures, refusals or unknowns; its output SHA-256 is `87b0b26a296425f2fec8b561f87253810aa0ef95ba10c07d88ff5d389e311fb6`. The root suite passed 717 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases; its output SHA-256 is `db1d18f724db9740eb58311cc08588c4be2ff1971be837f30143e14afab2208f`. The exact changed-path Phylax, Ephoros and Hypomnema lints each exited zero with `clean`; Imprimatur scored the contract and record 100.0 with no defects. Brevitas left B010 for Fiat's required one-heading record host and B011 for the contract's normative two-column tables. Adding headings or columns would change protected structure, so no Brevitas-clean claim is made. Draft 2020-12 schema validation, synopsis currency and `git diff --check` exited zero. The three closed questions per fixture can falsify declared answer loss but do not prove arbitrary English completeness, and Step 4 must still test the pinned compact views in fresh model contexts; no further concrete source-model contradiction survived the full review. Sapheneia shaping retained all ten finding ids, severities, path loci, mechanisms, guard statuses, hashes, counts, exclusions and authority boundaries from the frozen inventory.

## Step 3, round 2 -- 2026-08-30T06:27:10Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=reviewed; closed-answer-omission=reviewed; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; tokenizer profiles, bootstrap-cost measurement, recorded model-family identity and cross-family execution reserved for Step 4; report packaging and generated Promise Machine copy work reserved for Step 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and a structured Elenchus adapter, because the exact source-bound runner declares plain UTF-8 unittest text and no runner or format may be substituted

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | high | `scripts/agent_instruction.py`; all three fixture `questions.json` records; `tests/fixtures/agent-instruction-v1/manifest.json`; `tests/test_agent_instruction.py`; `docs/agent-instruction-language-v1.md` | An `answer-change` expectation passed whenever its value differed from the required answer; membership in the linked question's accepted and refusal sets was never checked. All three shipped negation mutations therefore reported `declared-answer-changed` for `phase-done`, `boundary-enabled`, and `score-gates` even though none was representable in its question's closed vocabulary. | fixed and regression-tested in this commit; changed answers must be declared and non-required, the three reviewed counterfactuals now belong to their accepted sets, and their artifact digests are rebound in the manifest |
| S3-R2-02 | high | `scripts/agent_instruction.py`; `tests/test_agent_instruction.py`; `docs/agent-instruction-language-v1.md` | Mutation coverage trusted each `risk` label independently of the JSON Pointer target. A mutation labelled `evidence-class` that changed only the document title passed as an evidence-class guard, and the same laundering could satisfy negation, precedence, scope, authorisation, or recovery coverage with an unrelated valid digest change. | fixed and regression-tested in this commit; each non-literal risk now has a closed target family, negation also requires an answer-change expectation, scope resolves to an existing scope or exception expression, and exact-literal target matching remains enforced |

Leads not pursued: The complete fixed Step 3 tree and all ten round-1 guards were re-audited against the same 18 risks. The exact fixed-tree runner passed 72 of 72 tests and `.hexaemeron/audit-fix-step-3.txt` has SHA-256 `7b035208d41f190d53e8fc11d7f51ef1410e7c76e7a8dcaa05c5da1ca6d77792`. Parent-red reports preserve one assertion failure for each mechanism and no infrastructure error: `.hexaemeron/elenchus-parent-step-3-round-2.txt` has SHA-256 `f1bc11225c47a1a929e89fa76c9e19b77412579b7a37059781d37bc69abe61c5`, and `.hexaemeron/elenchus-parent-step-3-round-2-risk-target.txt` has SHA-256 `89a8966b2ba79cd2a515ae7e064b8cc34b5bb2e94d5936c2b448225964d42624`, so the source-bound plain-text Elenchus verdict is `guarded`. The repaired manifest has SHA-256 `be145dd1e2d26daa24307d219d9a9125b4bc26ac463468ce3c040d30fcb7e7b3`; its check passed three fixtures, 15 bindings, 9 questions, 14 mutations and three round trips with zero failures, refusals or unknowns, and output SHA-256 `9dfe5beb6887a0e451b1a23b8b512de85ef20f50bd6286f2848c40ced2aff200`. An independent probe confirmed all three counterfactual answers are declared and differ from their required answers, all 15 bindings retain one digest-bound source, and the largest of 21 check records is 610 bytes; its output SHA-256 is `52ead72fcf1d76a5fdd682562deb32264f9d43652f060c4e556fc0d38bdbaa44`. The fixed product root passed 719 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases; output SHA-256 is `aa8053c4d4f14f253aa5c5ca313e986176ea5c5191174a6980ecff50fa54f6d2`. The exact full-Step-3 changed-path Phylax, Ephoros and Hypomnema lints each exited zero with `clean`, and Imprimatur scored the contract and record 100.0 with no defects. Brevitas left B010 for Fiat's required one-heading record host and B011 for the record's fixed two-row finding table and the contract's normative two-column tables. Adding headings or columns would change protected structure, so no Brevitas-clean claim is made. Draft 2020-12 schema validation, synopsis currency and `git diff --check` exited zero. Risk-target checks cannot infer whether arbitrary prose is a linguistic negation, so the remaining negation evidence stays bound to reviewed directive bytes, one declared counterfactual, and the fresh-context execution required in Step 4; no further concrete source-model contradiction or product defect survived the complete review. Sapheneia shaping retained both finding ids, severities, path loci, mechanisms, guard statuses, hashes, counts, exclusions, prior-guard status and authority boundaries from the frozen inventory.

## Step 3, round 3 -- 2026-08-30T06:39:36Z

Audit schema: fiat-audit-round/v2

Covered: semantic-domain-gap=reviewed; source-model-mismatch=reviewed; precedence-collapse=reviewed; negation-drop=reviewed; exception-scope-drift=reviewed; evidence-authority-drift=reviewed; exact-literal-change=reviewed; unknown-opcode-acceptance=reviewed; duplicate-key-shadowing=reviewed; resource-exhaustion=reviewed; path-or-shell-escape=reviewed; bootstrap-understatement=not-applicable; tokenizer-mismatch=not-applicable; evaluation-contamination=reviewed; closed-answer-omission=reviewed; agent-transfer-overclaim=not-applicable; derived-view-drift=reviewed; ownership-expansion=reviewed

Not checked: the Pashov suite under `waived: this run produces no Solidity, so hexaemeron:x-ray, hexaemeron:solidity-auditor, and hexaemeron:fizz do not apply`; tokenizer profiles, bootstrap-cost measurement, recorded model-family identity and cross-family execution reserved for Step 4; report packaging and generated Promise Machine copy work reserved for Step 5; external repositories, networks, credentials, native Windows and CI; controller receipt, push, pull request, merge, publication and issue closure; and an Elenchus replay, because this zero-finding round makes no product fix

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: The complete twice-fixed Step 3 tree and all twelve prior guards were independently re-audited against the same 18 risks with zero new findings. The exact focused runner passed 72 of 72 tests and its output has SHA-256 `a07986ed50afc424e9b39f5facb23335fb1988bc49d3ee90cf1b9f2de336c853`. The repaired manifest remains SHA-256 `be145dd1e2d26daa24307d219d9a9125b4bc26ac463468ce3c040d30fcb7e7b3`; its check passed three fixtures, 15 bindings, 9 questions, 14 mutations and three round trips with zero failures, refusals or unknowns, and output SHA-256 `9dfe5beb6887a0e451b1a23b8b512de85ef20f50bd6286f2848c40ced2aff200`. An independent closed-family probe matched all 14 mutations to their target families, refused all eight non-literal mutations after moving them to the document-title family, and refused both an undeclared and required-answer replacement for each of the three negation cases; its output SHA-256 is `73827ab0b47b89c1b0624cb62007ca0d32a440818cbb21d73e2604a4ee5a7b3d`. A fresh source/model/question reread retained the three source envelopes, 15 reviewed bindings, conditional Fiat marketplace order, Horos security-review exception, and Promise Machine evidence boundaries; its review transcript has SHA-256 `d2497bb7be56b424c2470fbd79a73361665f38bb4482fab9bcd55a3506f13bca`. The pre-record root suite passed 719 tests with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases; its output SHA-256 is `f30dffa2d5e7927181428adc849b3a08c21b0ae1b876f6bd91a7c7e3df8c0438`. The exact full-Step-3 changed-path Phylax, Ephoros and Hypomnema lints each exited zero with `clean`, and Imprimatur scored the contract and record 100.0 with no defects. Brevitas left B010 for Fiat's required one-heading record host and B011 for the fixed one-row finding table and the contract's normative two-column tables. Adding headings, rows or columns would change protected structure, so no Brevitas-clean claim is made. Draft 2020-12 schema validation, synopsis currency and `git diff --check` exited zero. Risk-target checks still cannot infer whether arbitrary prose is a linguistic negation, so the remaining negation evidence stays bound to reviewed directive bytes, a declared non-required counterfactual, and the fresh-context execution required in Step 4; this known boundary did not expose a further concrete source-model contradiction. Sapheneia shaping retained the exact zero-finding row, all 18 risk dispositions, hashes, counts, exclusions, prior-guard status, known semantic boundary and authority boundaries from the frozen inventory.
