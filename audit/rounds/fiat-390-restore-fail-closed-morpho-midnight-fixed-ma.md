## Step 1, round 1 -- 2026-08-29T09:14:51Z

Audit schema: fiat-audit-round/v2

Covered: api-schema-drift=not-applicable; cursor-incompleteness=not-applicable; unknown-event=not-applicable; subject-misattribution=not-applicable; maturity-boundary=reviewed; unit-accounting=reviewed; settlement-language=reviewed; derived-citation=reviewed; source-completeness=reviewed; rest-origin=not-applicable; response-bounds=not-applicable; fixture-fidelity=not-applicable; partial-output=reviewed; marketplace-drift=not-applicable

Not checked: the Pashov X-Ray, Solidity Auditor and Fizz pipelines under the recorded non-Solidity waiver; Steps 2 through 4 adapter, API, dossier, demonstration and marketplace behavior; live Morpho responses or archive-chain completeness; power-loss durability; a hostile same-user writer able to enumerate and replace the unpredictable private stage or rewrite the published report; hosted CI, controller receipt, push, pull request, merge and issue closure

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: No product defect survived the full diff review from immutable run base `66da3817761415f31bd467140ac1510f77b91b62` through signed Step 1 head `7bac2c291275e3c4d0c13659a1907cb5c45860e1`. The study, historical product runbook, runner, runner guards and prior audit records are byte-identical to historical source tip `88e745947270fd27891b5a99f7a33c8583d63ca2`; the decision record is byte-identical after the authorised `ADR-043` to `ADR-048` allocation, and only current generated closure differs. The study matches receipt SHA-256 `83cce2c5b5d0584080ab25627b72c755fb444fd794874ea72cd7627ca50d5ae9`; the current effective Step 1 runbook SHA-256 is `873c7056d2e8b763850d7ea2a484c4681f39c0752e15945bce894b1d4de031e2`. The exact source-bound runner produced a complete `elenchus.unittest.v1` report with 290 of 290 tests, zero failures, errors, skips, expected failures or unexpected successes; all 14 focused report-runner guards passed. Portable runtime check and `git diff --check` passed. Phylax, Ephoros and Hypomnema each exited zero on all 14 final changed paths, including this record and its generated synopsis. The selected current check graph passed the Probitas suite and all three lints; its root suite reproduced only inherited immutable-base conditions outside this diff. With the default macOS temporary root, the isolated-package model-proxy check refused symlinked lexical parent `/var` as `MP407`; `TMPDIR=/private/tmp` removed that failure. The remaining primer setup error says its shipped study records Hexaemeron 1.6.5 while the checker requires 1.6.8; merged PR #759 at `19933673bf572964efcb61e72e2f309034317df1` repairs that aggregate, and the runbook assigns proof after signed base composition. Neither inherited path changes a Step 1 product claim. The audit-filter declaration is exactly `--audit-filter sapheneia:sapheneia`. No further leads remain for this round.

## Step 2, round 1 -- 2026-08-29T10:37:03Z

Audit schema: fiat-audit-round/v2

Covered: api-schema-drift=reviewed; cursor-incompleteness=reviewed; unknown-event=reviewed; subject-misattribution=reviewed; maturity-boundary=reviewed; unit-accounting=reviewed; settlement-language=reviewed; derived-citation=reviewed; source-completeness=reviewed; rest-origin=reviewed; response-bounds=reviewed; fixture-fidelity=reviewed; partial-output=reviewed; marketplace-drift=not-applicable

Not checked: the Pashov X-Ray, Solidity Auditor and Fizz pipelines under the recorded non-Solidity waiver; Steps 3 and 4 dossier, renderer, demonstration and marketplace behavior; live Morpho v0 responses, the unpublished API history lower bound, or archive-chain completeness; the account-attributed debt-unit mapping for `exit_borrow_secondary`, which stays unproved and fails closed; hosted CI, controller receipt, push, pull request, merge and issue closure

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: No product defect survived the full diff review from signed Step 1 head `f5c573a4bf6178f595e7d7b1f1e33543e9c16375` through signed Step 2 head `a9a385181e5b776f7fc32fc7b8455c2e576354a0`. Every frozen non-generated Step 2 path is byte-identical to historical source tip `1fc939ef7752838a7d7147d1661f04f2c5ec074a`; the authorised exceptions are the regenerated `.horos/boundary.json`, `.agents/skills/promise-machine/runtime/.horos/boundary.json` and `.agents/skills/promise-machine/runtime/MANIFEST.json` closures, and the `ADR-043` to `ADR-048` allocation in `plugins/probitas/docs/morpho-midnight-fixed-maturity-runbook.md` and its runtime mirror. The adapter raises on any event type outside the closed vocabulary and never drops one silently; `exit_borrow_secondary` fails closed because its account-attributed debt-unit mapping is unproved. The locked HTTPS origin is re-checked after URL construction, redirects raise, and status, content type, `Content-Length`, request, byte, page, cursor, JSON depth, JSON node and deadline ceilings all refuse visibly. Duplicate JSON field names, non-finite numbers, and float or boolean integers are refused; signed and unsigned values are held to `int256` and `uint256` with a 78-digit text ceiling. Equal-second deltas are grouped, the maturity second is included, a negative balance raises, and the reconstruction is reconciled against the current position at a `last_indexed_block` no older than the newest event block. Obligation state, observation state and settlement mode are emitted separately, so a later zero cannot rewrite an overdue maturity verdict and a post-maturity liquidation cannot render as voluntary repayment. Every emitted record carries its transaction hash and the maturity outcome names its determining transaction and event id. Refusal text carries stage, exhausted walk counts, observation time and returned index boundary without a subject address, and records are assembled in memory so no partial set reaches the shared writer. The exact source-bound runner produced a complete `elenchus.unittest.v1` report with 367 of 367 tests and zero failures, errors, skips, expected failures or unexpected successes. Phylax, Ephoros and Hypomnema each exited zero on all 21 final changed paths, including this record and its generated synopsis; the Horos boundary check, the portable runtime check and `git diff --check` also exited zero. The selected current check graph against base `f5c573a4bf6178f595e7d7b1f1e33543e9c16375` passed the Probitas suite and all three lints; its root suite reproduced only inherited conditions outside this diff. The primer setup error says its shipped study records Hexaemeron 1.6.5 while the checker requires 1.6.8; merged PR #759 at `19933673bf572964efcb61e72e2f309034317df1` repairs that aggregate, and the runbook assigns proof after signed base composition. With the default macOS temporary root the isolated-package model-proxy check refused symlinked lexical parent `/var` as `MP407`; pinning `TMPDIR` to a real path removed that failure. One maintenance lead is accepted rather than pursued: `SHIPPED_FIXTURE_FILES` in `plugins/probitas/tests/test_docs.py` is a hand-maintained allowlist, so a future adapter that registers a new fixture filename must be added there or the README fixture count silently under-counts. That is reversible test policy beside the shipped-adapter classification it replaces, not a product defect. The audit-filter declaration is exactly `--audit-filter sapheneia:sapheneia`. No further leads remain for this round.

## Step 3, round 1 -- 2026-08-29T11:03:37Z

Audit schema: fiat-audit-round/v2

Covered: api-schema-drift=reviewed; cursor-incompleteness=reviewed; unknown-event=reviewed; subject-misattribution=reviewed; maturity-boundary=reviewed; unit-accounting=reviewed; settlement-language=reviewed; derived-citation=reviewed; source-completeness=reviewed; rest-origin=reviewed; response-bounds=reviewed; fixture-fidelity=reviewed; partial-output=reviewed; marketplace-drift=not-applicable

Not checked: the Pashov X-Ray, Solidity Auditor and Fizz pipelines under the recorded non-Solidity waiver; Step 4 demonstration, frontier advance and marketplace closure; live Morpho v0 responses, the unpublished API history lower bound, or archive-chain completeness; the account-attributed debt-unit mapping for `exit_borrow_secondary`, which stays unproved and fails closed; the issue-622 published packet and patch inputs the rebound guard names, which this run does not hold; hosted CI, controller receipt, push, pull request, merge and issue closure

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | plugins/probitas/scripts/probitas.py | `cmd_render` calls `render.render` outside the `try` that catches `OSError` and `ValueError`, so a render-time `RenderError` escapes as an uncaught traceback with exit 1 and printed absolute paths instead of the bounded `probitas:` diagnostic and exit 2 that a `render.load` refusal produces. Reproduced with an evidence file whose `subject.entity` is empty. The refusal is fail-closed: no dossier is written and no false evidence is emitted, so the cost is the diagnostic contract rather than a wrong number. `cmd_verify` calls `gates.check` outside its own `try` in the same shape. | accepted |

Leads not pursued: `S3-R1-01` is accepted rather than fixed. `plugins/probitas/scripts/probitas.py` is a frozen Step 3 path, and the step's exit requires it byte-for-byte against historical tip `c2862e88bc32918826eb779e395880c55b3a83b5`; changing the caller here would break the source-equivalence the step is built to prove. The behaviour is fail-closed and its repair is a separate change against current `main`, not a restoration edit. No other product defect survived the full diff review from signed Step 2 head `7361c5813f3f55af66cf43851e3dd22353077d26` through signed Step 3 head `101efa6515623f0eeb28cb5c88dff536357700bb`. Of the 33 changed paths, 27 are byte-identical to the historical Step 3 tip and 6 are authorised reconciliation: the regenerated `.horos/boundary.json` and `.horos/candidates.json`, the regenerated `.agents/skills/promise-machine/runtime/MANIFEST.json`, the mechanically refreshed `tests/promise_machine_coverage.json` aggregate, and the current-tree-only issue-622 guard pins in `scripts/verify_issue_622_inoculation.py` and `tests/fixtures/issue-622-inoculation-v1.json`, neither of which exists at the historical tip. That guard byte-pins the coverage aggregate, so the refreshed Promise Machine digest was carried into both pin tables and the checked-in record together; live `origin/main` at `742b38756dc114832c477a53d34a9baee85deade` carries the same three-place shape, and its `RECORD_PATH` pin likewise does not equal the current record digest because that pin names the archive preimage. The renderer substitutes only slots found in the trusted template bytes and refuses a template whose slots disagree with its sections, so a slot-shaped string inside evidence cannot become document structure; every evidence-derived cell passes through bounded `sanitise.clean`, subject addresses are validated, and a citation whose source kind does not match its record is refused. `_midnight_outcome` refuses a settlement mode outside the closed vocabulary, a cleared outcome with non-zero or unsettled debt, an outstanding-at-maturity outcome with no maturity debt, a late settlement with a residual balance, an observation balance above the maturity balance, and a not-due outcome that invents a maturity balance; liquidation renders as liquidation and says so, and a later zero cannot rewrite an overdue maturity. The gaps table now carries the coverage status beside each named gap. All 15 Midnight specimens carry a source date, the locked origin and an observation time. The exact source-bound runner produced a complete `elenchus.unittest.v1` report with 387 of 387 tests and zero failures, errors, skips, expected failures or unexpected successes. Phylax, Ephoros and Hypomnema each exited zero on all 35 final changed paths, including this record and its generated synopsis; the Horos boundary check, the portable runtime check, the Promise Machine coverage and law checks and `git diff --check` also exited zero. The selected current check graph against base `7361c5813f3f55af66cf43851e3dd22353077d26` passed the Hexaemeron, Imprimatur and Probitas suites and all three lints; its root suite reproduced only the inherited primer aggregate error, which says its shipped study records Hexaemeron 1.6.5 while the checker requires 1.6.8. Merged PR #759 at `19933673bf572964efcb61e72e2f309034317df1` repairs that aggregate, and the amended runbook assigns proof after signed base composition. With the default macOS temporary root the isolated-package model-proxy check refuses symlinked lexical parent `/var` as `MP407`; pinning `TMPDIR` to a real path removes that failure. Neither inherited path changes a Step 3 product claim. The audit-filter declaration is exactly `--audit-filter sapheneia:sapheneia`. The remaining lead is recorded above and is not worth another round under this run's restoration scope.

## Step 3, round 2 -- 2026-08-29T14:42:55Z

Audit schema: fiat-audit-round/v2

Covered: api-schema-drift=reviewed; cursor-incompleteness=reviewed; unknown-event=reviewed; subject-misattribution=reviewed; maturity-boundary=reviewed; unit-accounting=reviewed; settlement-language=reviewed; derived-citation=reviewed; source-completeness=reviewed; rest-origin=reviewed; response-bounds=reviewed; fixture-fidelity=reviewed; partial-output=reviewed; marketplace-drift=not-applicable

Not checked: the Pashov X-Ray, Solidity Auditor and Fizz pipelines under the recorded non-Solidity waiver; Step 4 demonstration, frontier advance and marketplace closure; live Morpho v0 responses, the unpublished API history lower bound, or archive-chain completeness; the account-attributed debt-unit mapping for `exit_borrow_secondary`, which stays unproved and fails closed; the issue-622 published packet and patch inputs the rebound guard names, which this run does not hold; a committed regression guard for the round 1 repair, which this run does not add; hosted CI, controller receipt, push, pull request, merge and issue closure

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Round 1's `S3-R1-01` is fixed in `bc61cbdf7eac6cf830c43efc3c90fde47900a37e`, not accepted. Round 1 recorded it as `accepted` because the step's exit required `plugins/probitas/scripts/probitas.py` byte-identical to historical tip `c2862e88bc32918826eb779e395880c55b3a83b5`; the delivering contributor then chose the repair over a halt, and the dated runbook amendment recorded at runbook SHA-256 `dff23d68cf3722a52d658f3a39806c55d5ab87c8590fc223ae369e4ed4bbaae5` authorises that one deviation. `cmd_render` now evaluates `render.render` inside the try that catches `OSError` and `ValueError`, and `cmd_verify` does the same for `gates.check`. The reproducer, an evidence file whose `subject.entity` is empty, moved from an uncaught `RenderError` traceback at exit 1 to `probitas: subject entity is empty` at exit 2. The output write stays outside the try, so a failed write is still reported as itself rather than as an evidence refusal. The Elenchus verdict for this fix is `unguarded`: no committed test fails without it, because `plugins/probitas/tests/test_cli.py` is a frozen Step 3 restoration path and adding a guard there would widen the authorised deviation a second time. Guarding this caller belongs with the rest of its diagnostic surface on current `main`. The repair changes no rendered byte, no gate result and no evidence value, so the Promise Machine field map for `probitas-dossier-verification` is unchanged and only its source digest was refreshed; that digest is byte-pinned by the current issue-622 inoculation guard, so both pin tables and the checked-in record were carried with it, matching the three-place shape live `origin/main` uses at `742b38756dc114832c477a53d34a9baee85deade`. No other product defect survived the review of the fixed tree from signed Step 2 head `7361c5813f3f55af66cf43851e3dd22353077d26` through signed Step 3 head `bc61cbdf7eac6cf830c43efc3c90fde47900a37e`. Of the 35 changed paths, 25 are byte-identical to the historical Step 3 tip and 10 are authorised: four regenerated generated closures, the refreshed `tests/promise_machine_coverage.json` aggregate, the two current-tree-only issue-622 guard paths, this run's own audit record and its generated synopsis, and the authorised `probitas.py` repair. Nothing is unclassified. The exact source-bound runner produced a complete `elenchus.unittest.v1` report with 387 of 387 tests and zero failures, errors, skips, expected failures or unexpected successes. Phylax, Ephoros and Hypomnema each exited zero on all 35 final changed paths, including this record and its generated synopsis; the Horos boundary check, the portable runtime check, the Promise Machine coverage and law checks and `git diff --check` also exited zero. The selected current check graph passed the Hexaemeron, Imprimatur and Probitas suites and all three lints; its root suite reproduced only the inherited primer aggregate error, which merged PR #759 at `19933673bf572964efcb61e72e2f309034317df1` repairs on current `main` and which the amended runbook assigns to signed base composition. The audit-filter declaration is exactly `--audit-filter sapheneia:sapheneia`. No further leads remain for this round.
