Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1524-integration-blocked-design-cell.md | source_sha256=f6d7d3d9e15901c948eda73f24d2bb71f495d66831a7ba0fd5b705f4df31d85e | h2_count=1
## Step 1, round 1 -- 2026-09-25T12:09:40Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: record-digest-drift=reviewed; disclosure-report-binding=not-applicable; disclosure-of-passing-cell=not-applicable; step-cell-out-of-scope=reviewed; pr-body-parity=not-applicable; ledger-state-parity=not-applicable; partial-write=reviewed; reason-bounds=reviewed; legacy-state=not-applicable; halted-run=not-applicable; wrapper-out-collision=reviewed; wrapper-subprocess=reviewed; receipt-shape-compatibility=not-applicable; governed-prose=reviewed; version-surfaces=reviewed<br><br>Not checked: The Solidity security suite is waived because the step ships no Solidity, so neither X-Ray, the Solidity auditor nor Fizz ran. Step 1 builds no disclosure object, `--disclosure` flag, D009, `hexctl disclose design`, ledger event, receipt field or run pull request body gate, and leaves `design_evidence.py` and `hexctl.py` byte-identical to the run base `32a70f9cc3c8b14ed2001dcf9e2e7821b8c112a9`, so `disclosure-report-binding`, `disclosure-of-passing-cell`, `pr-body-parity`, `ledger-state-parity`, `legacy-state`, `halted-run` and `receipt-shape-compatibility` are not applicable; `partial-write` and `reason-bounds` were reviewed only for the wrapper's report write and text bounds, and the disclosure file, `--reason` and state writes they name arrive in Steps 2 and 3. The four pending `refusal-guards-green` resolvers were not run against `.hexaemeron/design-evidence.json`; they were run verbatim only on a scratch copy of that record and its 28 reports. Remote refs were not fetched. Linux, where `dir_fd` and process-group behaviour may differ, was not run; this host is macOS with Python 3.14.6. A resolver descendant that starts its own session leaves the process group and is outside the S1-R1-02 fix. The Step 1 `run_checks` Exit went red once on `test_checkpoint_signing_formats.CheckpointOpenPgpSigningTests.test_inspect_and_restore_reject_invalid_public_key_material` (GPG exit 128), then green on a re-run (13 of 13); this is the base-inherited skills#1781 flake, fixed on main by PR #1841 after base `32a70f9c`, it was not reproduced or investigated here, and no Step 1 cause was found.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | `plugins/hexaemeron/docs/fiat-design-cell-disclosure/design-evidence.json` | The committed record copy names every resolved report `design/reports/<file>`, but Step 1 placed the 28 report copies under `reports/` and the probe at the delivery root, as runbook Step 1 Files lists; `design_evidence.py` on the copy at `--transition design-lock` exited 1 with 28 D005 findings, report unavailable or outside the record directory. Study section 7 and the Surveyor's candidate decision named the `design/` layout. Of the 23 committed delivery records outside test fixtures, 21 replayed at design-lock in place; the other two were this copy and `docs/fiat-carried-step-commits/design-evidence.json`. | fixed in `ed7fe81d7ef8da61a9943eb54d1041eead17957c`: the 28 reports moved to `design/reports/` and the probe to `design/probe.py`, all 29 still cmp-equal to their receipted originals; the draft decision names the new paths; guard `CommittedRecordCase` replays the copy at design-lock with 28 reports consumed; runbook Step 1 Files still names `reports/` and `probe.py` and now trails the tree |<br>| S1-R1-02 | low | `plugins/hexaemeron/skills/protasis/scripts/design_report.py` | `run_child` started the resolver in the wrapper's own process group and on the timeout killed only the direct child. With the timeout at 2 s, a grandchild with its own output survived the wrapper's return; a grandchild inheriting the output after the child exited 0 held the wrapper for the full timeout, after which the report recorded exit 0 and stderr said the child exceeded the timeout and was killed. The repository's other command runners, `criteria_execution.py`, `metron.py`, `run_checks.py` and `elenchus.py`, start a new session and kill the group. | fixed in `f5c04a8e206c04162bb1aa5f11c749d90a2a7858`: the child starts in a new session; its group is killed on the timeout, on an interrupt, and when the child has exited while a descendant still holds its output, which now ends the wait after 1 s of quiet; the report still records the child's own exit |<br>| S1-R1-03 | low | `plugins/hexaemeron/skills/protasis/scripts/design_report.py` | The `--out` boundary was checked only before the child ran, and `O_NOFOLLOW` covered only the final component. A child that renamed `design/reports` and put a symlink to an outside directory in its place made the wrapper create the report outside the record directory and exit 0. | fixed in `f5c04a8e206c04162bb1aa5f11c749d90a2a7858`: the report is created from the record directory held since the check, matched by `st_dev` and `st_ino`, each directory below it opened with `O_DIRECTORY` and `O_NOFOLLOW` through `dir_fd` and the report with `O_EXCL` and `O_NOFOLLOW`; the swap now refuses with exit 2 and writes nothing outside |<br>| S1-R1-04 | low | `plugins/hexaemeron/skills/protasis/scripts/design_report.py` | The documented exits are 0 for a written report and 2 for a refusal that writes nothing, but five paths ended in a traceback: `Popen` raising `FileNotFoundError` for an absent argv[0] and `PermissionError` for a non-executable one; `_bounded_text` raising `UnicodeEncodeError` for an argv element carrying a surrogate escape and for a `--value-json` string value `\ud800`, where the checker's `_bounded_text` returns false; and an `OSError` from `fsync` after `O_CREAT`, which left a complete report at `--out` so that the rerun refused because the path exists. | fixed in `f5c04a8e206c04162bb1aa5f11c749d90a2a7858`: a start failure refuses as could not be started; `_bounded_text` returns false on `UnicodeEncodeError` as the checker does; a read failure refuses; a failed write removes the file it created and refuses, and the guard's rerun then writes a report the checker admits |<br><br>Leads not pursued: `_kill_group` signals the child's group id even after the child has been reaped, so a reused group id can be signalled; the window is one call and was not pursued. Evidence: the verdict comes from a by-hand counterfactual, not the Elenchus runner. The six S1-R1-02 to S1-R1-04 guards in `plugins/hexaemeron/tests/test_protasis_design_report.py` fail on parent `ed7fe81d7` (21 run, 6 failures, 0 errors) and pass on `f5c04a8e206c04162bb1aa5f11c749d90a2a7858` (21 of 21); guard `CommittedRecordCase` fails on parent `0fa90bf747e4cced8e6506fe90a4982b97a46f84` (1 run, 1 failure) and passes on `ed7fe81d7`. Phylax, Ephoros and Hypomnema over the tree each exited 0 at `f5c04a8e206c04162bb1aa5f11c749d90a2a7858`; the Hexaemeron suite passed 3,827 of 3,827 with 5 skipped (exit 0); the root suite passed 2,083 of 2,083 under greenlight (exit 0). The Warden stopped at 2026-09-22T22:02Z with the S1-R1-02 to S1-R1-04 fix uncommitted and this record in draft; the operator re-read that diff, re-ran the guards, lints and suites, and committed it unchanged apart from the regenerated `.horos/census.json`.
