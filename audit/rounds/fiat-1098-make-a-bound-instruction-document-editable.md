## Step 1, round 1 -- 2026-09-03T15:42:16Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=not-applicable; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; only the local root suite was executed. Adapter portability: the loopback adapter, its executable resolution and the `refusal-detail-coverage` message surface are untouched by this diff and were not examined. `agent_instruction.py measure` and `parity`: forbidden this round, unread beyond `measure_manifest`'s `_corpus_sha256` call site at `scripts/agent_instruction.py:3302`. The other two bound fixtures, `horos-boundary-check` and `promise-machine-router-selection`: the prover pins `SUBJECT` to `fiat-study-runbook-phase`, and no case checks that an edit to either of the other two behaves the same way. The `before-span` placement's reconciliation outcome: `edited_source` implements it at `scripts/prove_agent_instruction_reconciliation.py:263-264` and nothing at this step calls it; the runbook schedules it at step 4. Multi-pass omission subsets: only single-pass omissions were exercised. `.horos/boundary.json` was not regenerated with horos; only the `files_walked` delta 2478 to 2482 against the four added files and the absence of any changed `entries` row were checked. Study prose beyond the Assumptions, section 3 boundaries, section 12 and the 2026-09-03 amendment was not verified number by number. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run: this step ships Python, JSON and Markdown and no Solidity.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | scripts/prove_agent_instruction_reconciliation.py:135-156,641-650 | `--report` is the only write outside the throwaway copy. A relative target is confined under `--root` and an absolute one under its own parent, so `--report PROMISE_MACHINE.md --root .` replaces a bound document with a JSON report. `write_confined_atomic` stops symlink, `..`, backslash, control-character and empty-name escape, not target selection, and no refusal exists for a report path the manifest binds. The docstring concedes the boundary; the study's "Never" list forbids the outcome. Remedy: refuse a report target inside the manifest's bound set. Not fixed here -- the runbook pins the new module at six tests, so a refusal test cannot be added, and a guard with no hostile-input test would be an asserted rather than verified control. | open |
| S1-R1-02 | low | scripts/prove_agent_instruction_reconciliation.py:595 | `--candidate` is accepted unvalidated and copied into the `candidate` field of the `protasis-design-report/v1` object the tool writes. The sibling its docstring names, `.hexaemeron/scripts/design_probe.py:450`, closes the same flag with `choices=sorted(CANDIDATES)`. One typo yields a design report naming a candidate no design record contains; `.hexaemeron/design-evidence.json` holds exactly four. Same test-count reason for deferral as S1-R1-01. | open |
| S1-R1-03 | informational | tests/test_agent_instruction_corpus.py:197 | The comment's claim that each omission "refuses under its own code and node path" overstates by one word. Observed signatures: `manifest-source` `WAI-E-DIGEST.SOURCE` at `$.fixtures.fiat-study-runbook-phase.source.sha256`; `model` `WAI-E-MANIFEST.SOURCE` at `$.fixtures.fiat-study-runbook-phase.model.sources`; `source-spans` `WAI-E-MANIFEST.SOURCE` at `$.source_spans.source.sha256`; `compact` `WAI-E-CANONICAL.ROUNDTRIP` at `$.fixtures.fiat-study-runbook-phase`; `manifest-artifacts` `WAI-E-DIGEST.ARTIFACT` at `$.fixtures.fiat-study-runbook-phase.artifacts.compact`. `model` and `source-spans` share a code and differ only in node path. `PASS_SIGNATURES` and the distinctness assertion at line 220-224 compare the pair, so the test is correct and the prose is not. | open |
| S1-R1-04 | informational | tests/test_agent_instruction_corpus.py:210-220; scripts/prove_agent_instruction_reconciliation.py:530 | Only single-pass omissions are exercised, `skip=(name,)`. That `WAI-E-DIGEST.CORPUS` at `$.evidence.measurement_record` stays unreachable when two or more passes are omitted follows from `refuse()` raising `CodecError` and terminating on the first failure, which was read rather than tested. | accepted |
| S1-R1-05 | informational | scripts/prove_agent_instruction_reconciliation.py:399-419 | `capture_output=True` buffers the whole of the checker's stdout before `MAX_OUTPUT_BYTES` is consulted, so the 1 MiB cap bounds parsing, not memory; the 600-second timeout is the only bound on a runaway checker. The executed bytes are digest-checked against the repository copy at line 305-306, so this is a defence-in-depth gap, not an open input. | accepted |
| S1-R1-06 | informational | scripts/agent_instruction.py:1217-1262, used through `_write` and `write_report` | A `SIGKILL` or `SIGTERM` between `O_CREAT` and `os.replace` leaves a `.wai-<32 hex>` temporary in the target's parent directory; the `finally` unlink covers exceptions and `SIGINT` only. No half-written report and no mutated fixture is reachable: the replace is atomic and every edit write lands in the temporary copy. A `SIGKILL` also leaves the `prove-agent-instruction-*` tree under `TMPDIR`, outside the repository. Inherited from the checker's writer, not introduced by this diff. | accepted |
| S1-R1-07 | informational | tests/test_agent_instruction_corpus.py:257 | `assertNotIn(ROOT, tree.parents)` compares a resolved `ROOT` against an unresolved `tempfile` path, so under a `TMPDIR` inside the repository the isolation assertion could pass while the copy sat inside the tree. Writes stay inside the copy either way, so no bound document is exposed. | accepted |
| S1-R1-08 | informational | tests/test_agent_instruction_corpus.py:327-328 | The `span-shift` report's `value` is not asserted, only its `criterion` and `unit`, so a regression in the placement count would pass this selector. `prove_span_shift` returns 1 today, with `before-span` reported uncovered and the criterion unmet. | open |

Leads not pursued: whether an edit to `horos-boundary-check` or `promise-machine-router-selection` reconciles as `fiat-study-runbook-phase` does; whether `format` regeneration is byte-identical for those two fixtures, proved here for the subject only; whether the `before-span` placement reconciles as the study predicts, which step 4 owns; whether `.horos/boundary.json` fully recomputes; and whether `_corpus_sha256`'s six-key subject is the right subject, which is step 3's design question rather than a step-1 defect. Evidence for this round: the full `fiat/1098-make-a-bound-instruction-document-editable...af660b1c0fd6c55f7a0b35afd465670e4908a567` diff was read, five files and 1,796 insertions against one deletion. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0 over `scripts/prove_agent_instruction_reconciliation.py`, `tests/test_agent_instruction_corpus.py` and `docs/agent-instruction-machine-binding`; the Pashov suite is waived and did not run. `tests/test_agent_instruction_corpus.py` passes 6 of 6, `python3 tests/run_tests.py` exits 0 at 1,122 of 1,122, and `prove_agent_instruction_reconciliation.py selftest` exits 0 with value 6. Mason's first claim holds as qualified by S1-R1-03 and S1-R1-04: the five omissions produce five distinct code-and-node-path pairs and the corpus refusal is reached only with all five applied. Mason's second claim holds and was proved directly as well as through the accepted no-op: `format` over `model.json` in a scratch copy reproduced the committed `compact.wai` byte for byte at SHA-256 beaf79abd92c011d7ecaed9a901e2a6c17c5b627e91b2551dcdefa45ee102b1b. `_corpus_sha256` at `scripts/agent_instruction.py:3276-3285` still digests a six-key subject carrying `fixtures`, so the whole-file and reviewed-span digests both remain bound and checked; the prover calls it rather than reimplementing it. `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` remain at `tests/test_agent_instruction.py:3357` and `:3364`, and `git diff` shows that file unchanged by this step. The live tree was byte-identical before and after every run, `git status` clean throughout, and `coverage_bound_artifacts` reports six bound paths with the three rewritten artefacts and the manifest asserted present. No `measure` or `parity` invocation, socket, adapter call or contact with 127.0.0.1:11434 appears anywhere in the diff; that absence rests on reading the source and the subprocess's fixed `env` and `check`/`format`-only argument lists, not on sandboxing the run. No bound document was edited and no fix was committed, so no Elenchus verdict is due. Appending this record and its synopsis moved `.horos/boundary.json` `files_walked` from 2,482 to 2,484 and failed `tests/test_boundary_currency.py` and `tests/test_agent_instruction.py::test_horos_boundary_is_current_for_the_scaffold` at 1,120 of 1,122; the boundary was regenerated with `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` in the same commit and the root suite returned to 1,122 of 1,122 with exit 0. That is the mechanical consequence of a tracked audit record in a whole-tree boundary, not a defect in the step's diff, and it confirms the same arithmetic behind the step's own 2,478 to 2,482 delta. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI and adapter portability, all eight finding ids with severities, file and line locations, statuses and remedies, the five refusal signatures, the three lint exit codes, the test counts, the compact digest, the head commit, the two claim verdicts, the null Elenchus verdict and the unpursued leads without changing any claim.

## Step 1, round 2 -- 2026-09-03T16:06:45Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=not-applicable; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were again neither read nor run, so nothing here says this branch is green in CI. Adapter portability: unchanged from round 1 and still unexamined. `agent_instruction.py measure` and `parity`: forbidden, unread. The `before-span` placement: still unexercised, step 4 owns it. Multi-pass omission subsets: still only single omissions. The Elenchus report bytes: the runner writes `{report}` inside the detached parent worktree, so no file persists at `.hexaemeron/elenchus/step-1.json` in this tree and this round attests the classifier's declared verdict rather than any report bytes. The reconciliation behaviour of `horos-boundary-check` and `promise-machine-router-selection` remains unexercised -- round 2 extended the write guard to cover them but proved nothing about how an edit to either reconciles. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | scripts/prove_agent_instruction_reconciliation.py:176-223,710-724 | `--report` could land on a path the manifest binds; `--report PROMISE_MACHINE.md --root .` would have replaced a bound document with a JSON report. `bound_targets` now derives the bound set from the manifest's own `fixtures[*].source.path` and `fixtures[*].artifacts[*].path` plus the manifest itself, 19 resolved paths, and `write_report` refuses such a target before opening anything. Both sides are resolved, so a symlinked route to a bound path is the same path. Verified end to end: the guarded run exits 2 and leaves `PROMISE_MACHINE.md` at SHA-256 f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15, unchanged. The round-1 docstring conceding the gap was corrected in the same commit. | fixed and guarded in 88ef0e6d6a4faf609899530d086dfc82c5b68ba7 |
| S1-R1-02 | low | scripts/prove_agent_instruction_reconciliation.py:117-128,153-173,662-664 | `--candidate` was copied unvalidated into the design report's `candidate` field. It is now closed with `choices` over the ids the run's design record declares, read from `.hexaemeron/design-evidence.json` beside the script and capped at 1 MiB, with a closed four-id fallback for a tree carrying no record. `not-a-candidate` is now an argparse usage error at exit 2, before any proof runs and before any report is written. | fixed and guarded in 88ef0e6d6a4faf609899530d086dfc82c5b68ba7 |
| S1-R2-01 | low | scripts/prove_agent_instruction_reconciliation.py:248-276 | Enumerating the bound set for S1-R1-01 exposed a narrower check than the constructor claims. `Reconciliation.__init__` verifies three artefacts, `model`, `compact` and `source_spans`, and only for the subject fixture, against a manifest that binds five artefacts for each of three fixtures: 3 of 15, plus the subject source. Its docstring says construction "verifies it agrees with itself before anything is copied, so a proof never runs against a tree that was already inconsistent". Pre-existing drift in any of the twelve unverified bound artefacts would therefore surface later as a `check` refusal attributed to the edit, which is the confusion the constructor exists to prevent. All fifteen artefacts and three sources match their manifest digests today, so the gap is latent rather than live. Not fixed: the round's mandate was S1-R1-01 and S1-R1-02, and widening the constructor is a behaviour change that belongs in its own commit. | open |
| S1-R1-03 | informational | tests/test_agent_instruction_corpus.py:197 | Carried from round 1, unchanged: `model` and `source-spans` share the code `WAI-E-MANIFEST.SOURCE` and differ only in node path, so the comment's "its own code and node path" overstates. The test compares the pair and is correct. | open |
| S1-R1-04 | informational | tests/test_agent_instruction_corpus.py:210-220 | Carried from round 1, unchanged: only single-pass omissions are exercised. | accepted |
| S1-R1-05 | informational | scripts/prove_agent_instruction_reconciliation.py:463-486 | Carried from round 1, unchanged: `capture_output=True` buffers the checker's stdout before the 1 MiB cap is consulted. | accepted |
| S1-R1-06 | informational | scripts/agent_instruction.py:1217-1262 | Carried from round 1, unchanged: a `SIGKILL` or `SIGTERM` between `O_CREAT` and `os.replace` leaves a `.wai-<32 hex>` temporary. No half-written report and no mutated fixture is reachable. | accepted |
| S1-R1-07 | informational | tests/test_agent_instruction_corpus.py:257 | Carried from round 1, unchanged: the isolation assertion compares a resolved `ROOT` against an unresolved `tempfile` path. | accepted |
| S1-R1-08 | informational | tests/test_agent_instruction_corpus.py:327-328 | Carried from round 1, unchanged: the `span-shift` report's `value` is not asserted. | open |

Leads not pursued: whether the constructor should verify all fifteen bound artefacts across all three fixtures rather than three, which is S1-R2-01 and is a candidate for round 3; whether `mutations.json` and `questions.json` need mechanical passes of their own, answered no for a source edit and left open for other edit shapes; whether the `before-span` placement reconciles as the study predicts, which step 4 owns; and whether `_corpus_sha256`'s six-key subject is the right subject, which is step 3's design question. Evidence for this round: both round-1 low findings are closed by signed commit 88ef0e6d6a4faf609899530d086dfc82c5b68ba7, author Shoggoth, committer Dr Laurence E. Day, with both required trailers, two files changed, 191 insertions against 7 deletions. Each guard carries a hostile-input test observed to fail without it: with the bound-target refusal and the `choices` argument removed in a scratch copy, `test_a_report_path_the_manifest_binds_is_refused` and `test_a_candidate_outside_the_design_record_is_refused` both failed, 2 of 2, and both pass on the fixed tree. The report case runs against a throwaway copy, so that mutation run damaged only a copy under `TMPDIR`; `PROMISE_MACHINE.md` was byte-identical before and after at SHA-256 f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15. Both guards return exit 2 end to end. The new cases assert the control's presence before using it, so a tree without the guard fails them as assertions rather than erroring on a missing attribute. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0 over the same three paths; the Pashov suite is waived and did not run. `tests/test_agent_instruction_corpus.py` passes 8 of 8 and `python3 tests/run_tests.py` exits 0 at 1,124 of 1,124, up from 1,122 by exactly the two tests added. The exact source-bound classifier, `python3 tests/run_tests.py --elenchus-report {report}` at format `unittest-json-v1`, returns `guarded` on `88ef0e6d6a4f` from a parent assertion failure in `tests/test_agent_instruction_corpus.py`; the runner writes its report inside the detached parent worktree, so this round declares that verdict without attesting report bytes. `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` remain at `tests/test_agent_instruction.py:3357` and `:3364`, untouched. No bound document was edited, no `measure` or `parity` ran, and no `hexctl` command was issued. The guard covers all three bound documents, so it now reaches the two fixtures round 1 listed as unchecked, without establishing anything about how an edit to either reconciles. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI, adapter portability and the absent report bytes, all nine finding ids with severities, file and line locations, statuses and closures, the fix commit identity, the exact `guarded` verdict, the mutation-run counts, the test and suite counts, the bound-set size, the latent-not-live qualification on S1-R2-01, the digest of the unchanged bound document and the unpursued leads without changing any claim.

## Step 1, round 3 -- 2026-09-03T16:33:07Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=not-applicable; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were again neither read nor run, so nothing here says this branch is green in CI. Adapter portability: unchanged since round 1 and still unexamined. `agent_instruction.py measure` and `parity`: forbidden, unread. The `before-span` placement: still unexercised, step 4 owns it, and the span-shift value assertion added this round pins the count at 1 until it lands. Multi-pass omission subsets: still only single omissions. The reconciliation behaviour of `horos-boundary-check` and `promise-machine-router-selection`: their bound paths are now verified at construction and protected by the report guard, but no case exercises how an edit to either reconciles. Whether the twelve newly verified bound paths have ever actually drifted in this repository's history: not investigated, only their current agreement checked. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R3-01 | low | audit/rounds/fiat-1098-make-a-bound-instruction-document-editable.md, round 2 `Not checked` | Round 2 recorded that "the runner writes `{report}` inside the detached parent worktree, so no file persists at `.hexaemeron/elenchus/step-1.json` in this tree", and named the classifier's format as `unittest-json-v1`. Both statements mislead. The runner persists its report wherever it is told: `python3 tests/run_tests.py --elenchus-report .hexaemeron/elenchus/probe.json` exits 0 and leaves 162 bytes at that path, schema `elenchus.unittest.v1`, digest 1891c859b0655278d15ce35f4c645fc5d26be409aad006d1a79fa8933e966cb2. What is unobservable is the harness's report, not the runner's: `elenchus.py:296-306` creates a `mkdtemp` workdir, adds a detached parent worktree and resolves `--report-file` against that worktree, `prepare_report_path` at `:250-254` refuses any path that is not a relative descendant of it, and `:361` removes the worktree afterwards. So the round-2 sentence attributed to the runner what the harness does, and stated as impossible what was merely unobserved. `unittest-json-v1` is the `--report-format` flag value; `elenchus.unittest.v1` is the schema the report declares. Both are true of different fields, and naming only the flag read as a substituted contract. The command was not substituted. Corrected here rather than by editing round 2. | corrected in this record |
| S1-R2-01 | low | scripts/prove_agent_instruction_reconciliation.py:262-292 | `Reconciliation.__init__` verified three artefacts for the subject fixture against a manifest binding a source and five artefacts for each of three fixtures. `bound_digests` now enumerates all eighteen bound paths with their manifest digests, and the constructor checks every one; `bound_targets` reads the same enumeration, so the constructor and the report guard cannot drift apart. The docstring's self-consistency claim is now true and names the paths it covers. | fixed and guarded in 549030a81d7ab37af6aa5633c71ea46218193846 |
| S1-R1-03 | informational | tests/test_agent_instruction_corpus.py:195-201 | The comment now says each omission refuses under its own pairing of code and node path, states that `model` and `source-spans` share `WAI-E-MANIFEST.SOURCE` and differ only in node path, and points at the assertion that compares the pair. | fixed in 549030a81d7ab37af6aa5633c71ea46218193846 |
| S1-R1-08 | informational | tests/test_agent_instruction_corpus.py:330-336 | The `span-shift` report's `value` is now asserted at 1, beside its criterion and unit. | fixed in 549030a81d7ab37af6aa5633c71ea46218193846 |
| S1-R1-01 | low | scripts/prove_agent_instruction_reconciliation.py:192-236,726-742 | Closed in round 2: `--report` refuses a target the manifest binds, over the nineteen resolved paths `bound_targets` reports. Re-checked this round against the widened enumeration; still refusing, and `PROMISE_MACHINE.md` still at SHA-256 f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15. | fixed and guarded in 88ef0e6d6a4faf609899530d086dfc82c5b68ba7 |
| S1-R1-02 | low | scripts/prove_agent_instruction_reconciliation.py:117-128,153-174,678-680 | Closed in round 2: `--candidate` is closed over the design record's four ids. Unchanged this round. | fixed and guarded in 88ef0e6d6a4faf609899530d086dfc82c5b68ba7 |
| S1-R1-04 | informational | tests/test_agent_instruction_corpus.py:212-227 | Carried unchanged: only single-pass omissions are exercised; multi-pass unreachability rests on the checker refusing on the first failure. | accepted |
| S1-R1-05 | informational | scripts/prove_agent_instruction_reconciliation.py:479-502 | Carried unchanged: `capture_output=True` buffers the checker's stdout before the 1 MiB cap is consulted. | accepted |
| S1-R1-06 | informational | scripts/agent_instruction.py:1217-1262 | Carried unchanged: a `SIGKILL` or `SIGTERM` between `O_CREAT` and `os.replace` leaves a `.wai-<32 hex>` temporary. No half-written report and no mutated fixture is reachable. | accepted |
| S1-R1-07 | informational | tests/test_agent_instruction_corpus.py:260 | Carried unchanged: the isolation assertion compares a resolved `ROOT` against an unresolved `tempfile` path. | accepted |

Leads not pursued: whether the twelve newly verified bound paths have drifted at any point in this repository's history, as against agreeing today; whether `mutations.json` and `questions.json` need mechanical passes of their own, answered no for a source edit and still open for other edit shapes; whether the `before-span` placement reconciles as the study predicts, which step 4 owns; and whether `_corpus_sha256`'s six-key subject is the right subject, which is step 3's design question. Evidence for this round: signed commit 549030a81d7ab37af6aa5633c71ea46218193846, author Shoggoth, committer Dr Laurence E. Day, both required trailers, two files changed, 81 insertions against 13 deletions. The drift guard was observed to fail without the widening: with the constructor reverted to its three-artefact loop in a scratch copy, `test_construction_refuses_a_tree_already_off_a_bound_digest` failed 1 of 1 with `ProverError not raised`, and passes on the fixed tree. It plants drift in a throwaway copy, in `questions.json`, an artefact no mechanical pass rewrites, so the refusal can only come from the constructor's own check. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0; the Pashov suite is waived and did not run. `tests/test_agent_instruction_corpus.py` passes 9 of 9, `prove_agent_instruction_reconciliation.py selftest` exits 0, and `python3 tests/run_tests.py` exits 0 at 1,125 of 1,125, up from 1,124 by the one test added. The exact source-bound classifier returns `guarded` on `549030a81d7a` from a parent assertion failure in `tests/test_agent_instruction_corpus.py`. Answering S1-R3-01, this round also reconstructed that comparison independently so the verdict rests on visible bytes: a detached worktree at parent 20324ad1 with the fix's changed test file overlaid, run under the step's exact contract, exits 1 and writes `{"complete": true, "errors": 0, "expectedFailures": 0, "failures": 1, "schema": "elenchus.unittest.v1", "skipped": 0, "testsRun": 1125, "unexpectedSuccesses": 0}` at SHA-256 895c7dd84d3388ed7788f089582b429c588d7ff27cc769044f55d3024841d39c, the single failure being the new drift guard's assertion. One assertion failure and zero infrastructure errors is what Elenchus classifies as `guarded`, so the verdict rests on those bytes rather than on the classifier's word. The fixed tree writes zero failures and zero errors over 1,125 tests at SHA-256 e7fa2759111cda24fd9acdba5071d3bd32ab864191296a5ab055ba06bad16f6f. The same reconstruction for round 2's commit 88ef0e6d records two assertion failures and zero errors over 1,124 tests at SHA-256 30ea11d23a51dc133fbc76ff399ca5958aeb121a2e5530289bf7d1a9aee1428d, so round 2's `guarded` also holds on bytes; its verdict is confirmed, not revised. `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` remain at `tests/test_agent_instruction.py:3357` and `:3364`, untouched. No bound document was edited, no `measure` or `parity` ran, and no `hexctl` command was issued. Rounds 1 and 2 were not edited. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI and adapter portability, all ten finding ids with severities, file and line locations, statuses and closures, both fix commit identities, the exact `guarded` verdict, the three report digests and their exact counts, the classifier source locations, the mutation-run result, the suite counts, the eighteen-path enumeration, the digest of the unchanged bound document and the unpursued leads without changing any claim.

## Step 2, round 1 -- 2026-09-03T19:05:12Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; only the local root suite was executed. That gap is unchanged from step 1's three rounds. Adapter portability: the loopback adapter, its executable resolution and the `refusal-detail-coverage` message surface are untouched by this diff and were not examined; unchanged since step 1 round 1. `agent_instruction.py measure` and `parity`: forbidden this round, unread beyond `measure_manifest`'s `_corpus_sha256` call site; unchanged. The other two bound fixtures as reconciliation subjects, `horos-boundary-check` and `promise-machine-router-selection`: step 1 round 3 brought their bound paths under the prover's construction check, and no case still exercises how an edit to either reconciles; that gap stands. The `before-span` placement: still unexercised, step 4 owns it; unchanged. 127.0.0.1:11434 was never contacted and its state was never probed, so this round records only that no command it ran opens a socket, not the port's condition. `_corpus_sha256`'s other five subject keys -- `schema`, `risk_classes`, `binding_count`, `question_count` and `mutation_count` -- were read but not exercised against an edit; only `fixtures` was. The three reconciliation-tool findings step 1 closed were not re-verified on their own branch, only observed absent from this one, which is S2-R1-04. `tests/promise_machine_coverage.json` was verified row by row against the tree it names, and no claim is made that its selector list is the right selector list. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run: this step ships Python, JSON and Markdown and no Solidity.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | scripts/agent_instruction.py:3291-3346 | The projection does not reach the manifest's `artifacts.*.sha256` entries, and `_corpus_sha256` carries `fixtures` whole, so the corpus subject still moves on an out-of-span edit. Demonstrated: with `fiat-study-runbook-phase` edited past its span and the three derived artefacts and their manifest digests recomputed, `digest_neutral_projection` over `canonical_record_bytes` of the six-key subject differs from the committed projection at offset 151 across 179 bytes, the compact entry moving from `beaf79abd92c011d7ecaed9a901e2a6c17c5b627e91b2551dcdefa45ee102b1b` to `99e1881e8df0c430ab6056eb8df7dd527fc73ed7b94b9ccab66df11942b85a82`. Those entries are digests of the artefacts, they move when the source digest inside them moves, and they are not bound source digests, so the substitution passes over them. The shipped docstring claimed "two trees that differ only in which revision of a bound document they carry project to identical bytes"; that holds for `model.json`, `source-spans.json` and `compact.wai` and not for the manifest, which is the byte string the design candidate `digest-neutral-corpus` names when it says "and from the corpus subject". `test_projection_is_unchanged_by_an_out_of_span_edit` builds `rewritten` from those three artefacts only and never compares two manifests, so no case in the step could see it. Fixed at 0602b95e as far as this step's exit allows: the docstring states the gap and `test_the_projection_does_not_yet_neutralise_the_bound_artefact_digests` pins the counterexample for all three fixtures, so a step 3 that switches the subject without widening the projection fails rather than shipping a corpus digest that still moves. Widening the substitution here would exceed "replaces every bound whole-file source digest and changes nothing else", so the remedy is step 3's and the finding stays open against it. | open |
| S2-R1-02 | medium | scripts/agent_instruction.py:3321-3328 | "The reviewed span digest is untouched here and stays the review boundary" was a property of the three committed fixtures, not of the code. The substitution matches a digest value and cannot tell `source.sha256` from any other field carrying the same bytes, so a fixture whose reviewed span ran from 0 to the file's length would make `span_sha256` equal `source.sha256` and the projection would neutralise the review boundary along with the binding it is aimed at. Today none does: the three spans are 18445-22773 of 72713, 11183-12201 of 14030 and 18285-24109 of 27732, and the three `span_sha256` values `4a8a421960fb`, `0f2fb54b8f73` and `8dac2250f011` differ from their `source.sha256` values `809f4111662e`, `8de17c0b3bf6` and `f996fb645c81`. Fixed at 0602b95e by `test_the_reviewed_span_digest_is_distinct_from_the_projected_digest`, which refuses a whole-file span and asserts each span digest survives the projection, so the docstring's claim is checked rather than left to the fixtures' good behaviour. | fixed |
| S2-R1-03 | low | scripts/agent_instruction.py:3324-3327 | The docstring's multiplicity rule -- one bound digest neutralised "wherever it appears" -- had no case that could see it. Each bound source digest occurs exactly once in the manifest and once in each of `model.json`, `source-spans.json` and `compact.wai`, so a regression to `bytes.replace(digest, marker, 1)` passes all four of the step's cases without changing a byte of their evidence; it was run as a mutation and all four stayed green. Fixed at 0602b95e by `test_every_occurrence_of_a_bound_digest_is_substituted`, which builds a two-occurrence buffer in memory, writes no file and requires no committed artefact to carry a repeated binding. | fixed |
| S2-R1-04 | high | (branch topology) | The branch under audit does not carry step 1's audit fixes. `fiat/...-step-1-...--audit` is not an ancestor of this step's branch, which was cut from step 1's step branch at `af660b1c`. Five findings step 1 closed are therefore absent from the tree step 2 builds on: S1-R1-01 and S1-R1-02 by `88ef0e6d` and S1-R2-01, S1-R1-03 and S1-R1-08 by `549030a8`, together 348 lines across `scripts/prove_agent_instruction_reconciliation.py` and `tests/test_agent_instruction_corpus.py`, plus the comment rewrite `ec7d95b5`. The run's own audit record and synopsis were absent for the same reason, so the path this directive names held no file. Fiat's audit loop names this exact cost: "the next step branches from this step's branch, and fixes parked [on the stacked branch cost more] ... Commit the fixes onto the step branch itself before the prose phase." This round restored `audit/rounds/fiat-1098-make-a-bound-instruction-document-editable.md` and its synopsis from `fiat/...-step-1-...--audit` so this record appends to the complete run log rather than starting a second one; the record is byte-identical to step 1's up to this heading. The reconciliation-tool and corpus-test fixes were not carried across, because a Warden folding another step's commits into this stacked branch would put six foreign commits inside the fixes range the controller verifies. Remedy for the controller: fold `fiat/...-step-1-...--audit` onto step 1's branch, or rebase this step onto it, before step 3 branches from step 2. | open |
| S2-R1-05 | low | scripts/agent_instruction.py:3310-3315 | The stated premise for matching by byte -- "A 64-character lowercase-hexadecimal run in a bound artefact is a digest binding wherever it appears" -- is false, and the code never implemented it. `span_sha256` is such a run and is a different binding; so are the three `artifacts.*.sha256` entries of S2-R1-01 and any digest quoted in prose. What the code does is match each bound `source.sha256` value's own 64-byte literal, which is the narrower and safer rule and reaches every embedding regardless of encoding. A reader taking the comment at its word could "correct" the code toward the premise and neutralise the review boundary in the process. Fixed at 0602b95e: the comment now says what the substitution matches and names what it leaves alone. | fixed |
| S2-R1-06 | low | tests/promise_machine_coverage.json:7,113 | Both digests match the committed bytes -- `2066234ba7165b19c2b479e96ff7667e3671cbfcdb7bdf967e67a214ce5c128b` and `27813c953cf63018250903237c1ce166e5b63ceaf4641980792039722840c1b6` as receipted, rebound to `00d0591ff07c457a959ce5393849cd72fbcbb96cee1b2c3286d97405ada77cc3` and `08da630799d4e766429b171a3f59e01beab84323817bfc2e283ba0ddefdb721d` by this round -- and all 28 rows of the `agent_instruction` entry were recomputed and agree. The failure mode the step already hit once, rebinding and then editing the file, has no structural guard: nothing generates this file, no `--write` mode exists anywhere in `scripts/` or `tests/`, and `assert_bound_paths` at `tests/test_agent_instruction.py:3205` fails with a bare digest comparison naming no remedy. The repository already has the pattern to copy in the same suite: `tests/test_boundary_currency.py:164` fails with "regenerate with: python3 plugins/horos/skills/horos/scripts/horos.py scan . --write". Remedy: a rebind mode for this file, or failing that the command in the assertion message. Not fixed here -- the file is bound by eight capability rows and several suites, so a generator is step-sized work outside this round. | open |
| S2-R1-07 | medium | (round evidence) | The `guarded` verdict this round reports carries no information about this commit's guard. Elenchus overlays the fix's changed test paths onto the detached parent and classifies the result; the changed paths it selected were `tests/test_agent_instruction.py` and `tests/promise_machine_coverage.json`, and the two parent assertion failures it recorded are `test_specialised_coverage_binds_runtime_documentation_and_manifest` and `test_every_bound_capability_digest_matches_the_file_it_names`, both comparing the rebound checker digest `00d0591ff07c` against the parent's `2066234ba716`. Neither is one of this round's three added cases, and none of the three can fail on the parent: the executable body of `digest_neutral_projection` is identical across `5b02364a` and `0602b95e`, proved by comparing the function's AST with docstrings removed at `2085fc90a900a343` on both. This commit is documentation and tests with no behaviour change, so nothing here is guarded in substance. The general form matters beyond this round: any audit fix in this repository that rebinds `tests/promise_machine_coverage.json` in the same commit reads as `guarded` whatever it does, because the new digest lands on old bytes. Reported, not relabelled -- the classifier's exact value stands as this round's verdict. | open |

Leads not pursued: whether `mutations.json` and `questions.json` should be in `_corpus_sha256`'s subject at all, given they carry no source digest and came through the projection untouched; whether an all-`f` digest colliding with an existing repository convention matters, since `plugins/ariadne/tests/fixtures/conformance/fail-check-components-grounded-agent-component-not-a-subject.json:93` already uses `f` sixty-four times as a deliberately non-verifying sentinel, a different meaning from this marker's "not measured here" and with no code path joining the two; whether the root suite's one-off 1127 of 1129 immediately after `horos.py scan . --write`, which returned to 1129 of 1129 on the next three consecutive runs with no intervening edit, is a race between the suite's own `.dead-code` and `.horos` writes and its currency assertions; and whether `_corpus_sha256`'s six-key subject is the right subject, which is step 3's design question and stands unanswered from step 1. Evidence for this round: the full `5b02364a` against `af660b1c` diff was read, three files, 55 insertions in `scripts/agent_instruction.py`, 231 in `tests/test_agent_instruction.py` and one changed digest pair in `tests/promise_machine_coverage.json`. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0 over `scripts/agent_instruction.py`, `tests/test_agent_instruction.py`, `tests/promise_machine_coverage.json` and `audit`, before and after the fix; the Pashov suite is waived and did not run. Press point one was answered by construction rather than by reading: the projection is pure by byte and narrower than its own docstring, and its one real reach is the corpus subject it fails to cover, which is S2-R1-01. Press point two holds as argued -- `f` sixty-four times satisfies `SHA256_RE` at `scripts/agent_instruction.py:142`, which is `[0-9a-f]{64}` under `fullmatch`, so no shape check needs relaxing, and no code path in `scripts/agent_instruction.py`, `tests/test_agent_instruction.py` or `tests/fixtures/agent-instruction-v1/` treats an all-`f` or all-zero digest specially. The preimage argument is sound in substance and stronger than it needs to be: a bound source digest that did equal the marker would make its own substitution a no-op and change nothing, and `check` verifies every binding against real bytes regardless. Press point three: each of the four cases was run against eight mutations of the projection and each fails under a mutation of the property its name states -- a no-op projection fails idempotence, the changed-positions case and the out-of-span case; a marker the projection rewrites fails idempotence; a shortened marker fails the changed-positions length assertion; substituting one fixture's digest only fails three of the four; and switching `_corpus_sha256` onto the projection fails the corpus case alone. One property was unguarded, replacing only the first occurrence, which is S2-R1-03 and is closed. The three cases added this round were mutation-checked the same way, each caught by exactly the mutation of its own property and blind to the other two. Press point four: all 28 bound rows of the `agent_instruction` coverage entry were recomputed against the tree and none was stale, the selector list resolves against the file at both digests, and no selector was removed. `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` pass, 2 of 2, and `git diff` against the step branch shows zero deleted lines in `tests/test_agent_instruction.py`. `python3 scripts/agent_instruction.py check` exits 0 on the untouched fixture and `git status` shows no file under `tests/fixtures/agent-instruction-v1/` changed at any point. `python3 tests/run_tests.py` exits 0 at 1,129 of 1,129, up from the step's receipted 1,126 by exactly the three cases added. The fix commit is `0602b95e`, signed, author Shoggoth, committer Dr Laurence E. Day, both required trailers, three files, 113 insertions against 10 deletions. The source-bound classifier, `python3 tests/run_tests.py --elenchus-report {report}` at CLI format `unittest-json-v1` writing schema `elenchus.unittest.v1`, returns `guarded` on `0602b95e` with 1,129 executed, 2 assertion failures, 0 errors, 0 skipped and exit code 1; the runner writes its report inside the detached parent worktree, so nothing persists at `.hexaemeron/elenchus/step-2.json` in this tree and this round declares that verdict without attesting report bytes, exactly as step 1 round 3 established. What that verdict rests on is S2-R1-07. No `measure` or `parity` invocation, no socket, no adapter call and no contact with 127.0.0.1:11434 occurred, and no `hexctl` command was issued. `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/horos/skills/horos/SKILL.md` and `PROMISE_MACHINE.md` were read and not edited; all three are byte-identical to the step branch. Appending this record and its synopsis moved `.horos/boundary.json` `files_walked`, and the boundary was regenerated with `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` in the record's own commit. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI, adapter portability and the unprobed port, all seven finding ids with severities, file and line locations, statuses and remedies, the six commit identities, the five digest pairs, the AST comparison, the mutation counts, the suite counts, the exact `guarded` verdict with its qualification intact and the unpursued leads without changing any claim.

## Step 2, round 2 -- 2026-09-03T20:25:41Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were again neither read nor run, so nothing here says this branch is green in CI; unchanged across all five rounds of this run. Adapter portability: unchanged since step 1 round 1 and still unexamined; this round exercised the fake adapter under load but not a real one, and says nothing about a differently-sited client executable. `agent_instruction.py measure` and `parity`: forbidden, unread, and not run. The `before-span` placement: still unexercised, step 4 owns it. Whether step 1's three closed findings are correct on their merits: this round verified that they arrived byte-exact and compose cleanly, not that S1-R1-01, S1-R1-02, S1-R2-01, S1-R1-03 and S1-R1-08 were well fixed; step 1's rounds own that and were not re-derived. The GitHub merge `0e094109` reports `%G?` as `E`, signature unverifiable locally because GitHub's web-flow key is not in this keyring; no claim is made about it either way, and the controller verifies pushed identities GitHub-side. skills#1127 was not read, only ruled out as the cause of the error this round reproduced. The widened-projection behaviour proved below is a simulation run in memory against a copy of the subject bytes; no step 3 code exists and none was written. `.hexaemeron/elenchus/step-2.json` again does not persist in this tree, so the verdict is declared and its report bytes are not attested. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | tests/test_agent_instruction_corpus.py:492-553 | The composition had no case holding its two halves together. Step 1's widened `Reconciliation.__init__` verifies every path `bound_digests` reports, which is 18 entries: per fixture, one whole-file `source.sha256` and five `artifacts.*.sha256`. Step 2's projection neutralises the first of those six and none of the other five. Three of the five -- `model`, `source_spans` and `compact` -- embed the source digest and move with it, which is why `manifest-artifacts` is a mechanical pass in its own right; `mutations` and `questions` embed none and are the control. Fixed at `330b7cea` by `test_the_projection_covers_only_the_source_quarter_of_the_bound_set`, which states the disagreement against `bound_digests` itself rather than a list rewritten in the test, so the two halves cannot drift into disagreeing about what "bound" means. Observed to fail under a widened projection, with its own message, and under a no-op projection; the mutation is applied through the module loader, because `setUpClass` reloads the checker from disk and a patch on the cached class attribute is discarded. | fixed |
| S2-R2-02 | medium | tests/test_agent_instruction_corpus.py:101,138,162 | Step 3 inverts four cases, and only two of them were known. `test_the_corpus_subject_still_carries_the_whole_file_source_digest` and `test_the_projection_does_not_yet_neutralise_the_bound_artefact_digests` were written to be inverted. The two step 1 hardened are not: `test_out_of_span_edit_refuses_the_corpus_digest_at_the_measurement_record` and `test_out_of_span_edit_also_stales_the_parity_record` both assert an out-of-span edit moves the corpus digest, the second with the message "the edit did not move the corpus digest", which is exactly the behaviour step 3 removes. With them goes `rebind_measurement_corpus` at `scripts/prove_agent_instruction_reconciliation.py:460-469` and the `WAI-E-DIGEST.CORPUS` refusal at `$.evidence.parity_record` it exists to reach: with the corpus digest no longer moving on an out-of-span edit, the rebind writes the value already recorded and the second refusal is unreachable by that route. The five-pass proof is not at risk and was checked rather than assumed: every entry in `PASS_SIGNATURES` is a `WAI-E-DIGEST.SOURCE` or `WAI-E-MANIFEST.SOURCE` refusal against real bytes, none is the corpus digest, so each omission still refuses under step 3. Remedy: step 3's exit names all four cases and states what becomes of the parity-record refusal. Not fixed here -- inverting a case before the behaviour changes would leave the suite asserting something untrue of the tree it runs on. | open |
| S2-R2-03 | medium | tests/test_agent_instruction.py:2252 | The `1128/1129` error is parallel-runner load, not the port, and the port reading should not be carried forward. `ParityAdapterTests` spawns `fake-runtime.py`, whose source at `tests/test_agent_instruction.py:2093-2160` imports `json`, `os`, `sys` and `time`, reads stdin and fabricates a payload; it opens no socket, and the `http://127.0.0.1:11434/api/chat` string at `:2237` is inert argv the fake never reads. `_ollama_generate` reaches it through `_run_bounded`, which is `subprocess.Popen` at `scripts/agent_instruction.py:1360`, and whose only timeout path refuses `WAI-E-ADAPTER.TIMEOUT` at `:1391` -- an escaping exception, so a test expecting a successful generate reports ERROR rather than FAIL, which is the traceback shape reported. Reproduced: 16 concurrent `ParityAdapterTests` processes, `test_fake_model_refusal_is_preserved` errored in 16 of 16, all 77 escaping exceptions were `WAI-E-ADAPTER.TIMEOUT` and no other, each process took 18.4 to 20.2 seconds against the fake profile's `timeout_seconds` of 2, and the same class sequentially is 34 of 34 in 3.4 seconds. The negative evidence is decisive: `ollama serve` held `TCP *:11434 (LISTEN)` as PID 19372 throughout the reproduction and throughout every green run this round, so a constant cannot explain a variable outcome. Remedy is skills#1127's, which the study defers and this round leaves alone; a `timeout_seconds` above interpreter-spawn latency under load would also close it. Not fixed here, out of scope by direction. | open |
| S2-R2-04 | low | (coverage register) | Nothing binds the guard. `scripts/prove_agent_instruction_reconciliation.py` and `tests/test_agent_instruction_corpus.py` appear in no row of `tests/promise_machine_coverage.json`, checked by searching the whole file, and `.hexaemeron/design-evidence.json` binds the reports the prover writes rather than the prover. So step 1's audit hardened the component that verifies every bound artefact, and that component is itself unbound: an edit to either file moves no digest any register would notice. Whether it should be bound is a judgement this round does not make -- the register binds promise checkers and their focused tests, and the prover is a design-evidence resolver. Remedy: bind it, or record why a design-evidence resolver stays outside the register. | open |
| S2-R1-01 | high | scripts/agent_instruction.py:3291-3346 | Carried, and now decided rather than open on judgement. Simulating step 3 across all three fixtures: with today's subject the corpus digest moves on an out-of-span edit, which is the reported refusal; with the subject switched onto the projection as it stands it still moves, so the design fails; with the projection widened to the `artifacts.*.sha256` entries it does not move. The widening is also safe, which was the open question: under it an in-span edit still moves the corpus digest for all three fixtures, because `span_sha256` is never substituted, so `in-span-edit-refusal` holds. The recommendation is therefore that step 3's exit require the widening rather than leave this open against it, and S2-R2-01 now ties the requirement to the prover's own enumeration. | open |
| S2-R1-04 | high | (branch topology) | Closed. `0e094109` put step 1's audit fixes on step 1's branch and `a1d163be` forward-merged step 1 into step 2, parents `5b02364a` and `0e094109`, signed, both trailers. Verified rather than accepted: `88ef0e6d` and `549030a8` are both ancestors of the step branch; the merge adds exactly step 1's 357 lines over `5b02364a` and nothing else; and in the composed tree every file is byte-identical to the side that owns it -- `prove_agent_instruction_reconciliation.py` at `6b6326e5176e` and `test_agent_instruction_corpus.py` at `5a375c0a6ab8` match `ec7d95b5`, and `agent_instruction.py` at `00d0591ff07c`, `test_agent_instruction.py` at `08da630799d4` and `promise_machine_coverage.json` at `7d0d4ab8611e` match `0602b95e`. No reverted hunk and no dropped fix. The stacked branch carried round 1's fixes and the step branch step 1's, so neither held the composition until `35339df5` built it here; step 1's 71-line record is a byte-exact prefix of the resolved log at `b805a502603b8e9a`, so the superset resolution dropped no record byte. | fixed |
| S2-R1-07 | medium | (round evidence) | Carried, and the mechanism is now confirmed by a controlled contrast rather than argued. Round 1's commit rebound two digests in `tests/promise_machine_coverage.json` and read `guarded` on two parent failures that were the rebind, not its added cases. This round's commit touches `tests/test_agent_instruction_corpus.py`, which no coverage row binds, so nothing was rebound, and the same classifier on the same runner returns `passed` with 1,133 executed, 0 assertion failures and 0 errors. The two commits are the same kind of change -- a pin, no behaviour change -- and the verdict differs only by whether a digest rebind rode along. `passed` is the honest signal for both. Remedy remains S2-R1-06's, or committing a rebind separately from the change it records. | open |
| S2-R1-06 | low | tests/promise_machine_coverage.json:7,113 | Carried unchanged: no generator, no `--write` mode, and `assert_bound_paths` still fails with a bare digest comparison naming no remedy, against `tests/test_boundary_currency.py:164` in the same suite which names its regeneration command. All rows were recomputed again on the composed tree and none is stale; no rebind was needed this round. | open |

Leads not pursued: whether `mutations.json` and `questions.json` belong in `_corpus_sha256`'s subject at all, unchanged from round 1 and now sharper, since they are two of the five bound artefact digests per fixture that carry no source digest and would come through a widened projection untouched; whether the `manifest-artifacts` pass keeps its `PASS_SIGNATURES` entry unchanged once step 3 lands, checked as far as the refusal codes go and not exercised against a changed corpus; whether GitHub's web-flow key should be in this keyring so a merge like `0e094109` verifies locally rather than reporting `E`; and whether the fake adapter's `timeout_seconds` of 2 is the right bound for a parallel suite, which is skills#1127's question. Evidence for this round: the composition is 357 lines over `5b02364a` and was read in full. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0 over `scripts/agent_instruction.py`, `scripts/prove_agent_instruction_reconciliation.py`, `tests/test_agent_instruction.py`, `tests/test_agent_instruction_corpus.py`, `tests/promise_machine_coverage.json`, `audit` and `docs`, run before and after the fix; the Pashov suite is waived and did not run. `python3 tests/run_tests.py` exits 0 at 1,133 of 1,133, up from the composed tree's 1,132 by the one case added, and 1,132 is 1,129 plus step 1's three corpus cases. `tests/test_agent_instruction_corpus.py` passes 10 of 10, `DigestNeutralProjectionTests` 7 of 7, the prover selftest exits 0 at value 6, `agent_instruction.py check` exits 0, and both stale-report guards pass, 2 of 2, untouched. **The port's state, recorded as the register requires: `ollama serve` was listening on `TCP *:11434` as PID 19372 for the whole round, observed with `lsof -nP -iTCP:11434 -sTCP:LISTEN` and never contacted.** No socket was opened to it, no `measure` or `parity` ran, no model was consulted and no `hexctl` command was issued. That the suite is green with the port listening is therefore not evidence that it would be green with the port free, and this round makes no CI claim. `test_the_projection_does_not_yet_neutralise_the_bound_artefact_digests` was re-checked on the composed tree and still fails for the reason it was written for: it passes unmutated, and under a widened projection it fails with its own "widening has landed" message rather than on anything step 1 introduced. The fix commit is `330b7cea`, signed, author Shoggoth, committer Dr Laurence E. Day, both trailers, one file, 62 insertions and no deletions; the merge resolution is `35339df5`, signed, same identities, both trailers. `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/horos/skills/horos/SKILL.md` and `PROMISE_MACHINE.md` are byte-identical to the step branch, and no file under `tests/fixtures/agent-instruction-v1/` changed at any point. Appending this record and regenerating its synopsis moved `.horos/boundary.json`, regenerated in the record's own commit. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI, the unverifiable GitHub signature and the simulated widening, all eight finding ids with severities, file and line locations, statuses and remedies, the four carried findings with their status changes, every commit identity and digest, the reproduction counts, the port observation, the suite counts, the exact `passed` verdict and the unpursued leads without changing any claim.

## Step 2, round 3 -- 2026-09-03T22:26:08Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=not-applicable; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were again neither read nor run, so nothing here says this branch is green in CI; unchanged across all six rounds of this run. Adapter portability: unchanged since step 1 round 1 and still unexamined. `agent_instruction.py measure` and `parity`: forbidden, unread, not run. The `before-span` placement: still unexercised, step 4 owns it. Whether step 1's five closed findings are correct on their merits: still not re-derived, only their arrival verified. skills#1127 was again not read, only ruled out in round 2 as the cause of the adapter error, and this round did not re-run that reproduction. The step 3 simulation behind S2-R1-01 remains an in-memory experiment against copies of the subject bytes; no step 3 code exists and none was written. `.hexaemeron/elenchus/step-2.json` again does not persist in this tree, so the verdict is declared and its report bytes are not attested. The rebuilt commits were compared by tree object and by signature; their messages were read but not diffed against the superseded ones word by word, so a message-only difference between a superseded commit and its replacement would not have been caught here. The remote was read with `git ls-remote` and not written; whether GitHub's own view of step 1's pull requests is consistent with what that listing reports was not checked, and no `gh` command was issued. `backup/old-main-f72b20ae` was seen in the branch listing and not examined; it is unrelated to this run as far as its name goes and that was not verified. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite does not apply and did not run.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | audit/rounds/fiat-1098-make-a-bound-instruction-document-editable.md | Rounds 1 and 2 name commits that no longer exist, and the schema admits no note between rounds, so the mapping had nowhere to go. It does have somewhere: a round's own prose is ordinary text inside the schema, and this row is it. Superseded to rebuilt, verified by tree object rather than taken from the rebuild's own account: `a1d163be` to `d1dccbff` at tree `b0713e20cafd9f6e`, `35339df5` to `311acd87` at `b016c4c53544c13a`, `330b7cea` to `aa7323b5` at `b65bacf04da90d6f`, and `80aefffe` to `9cd16c98` at `1749fe26407974bb`. The count is four, not three: `0e094109` is also named in round 2 and is also off the live range, though it still resolves because step 1's branch and the remote both still point at it, which is S2-R3-02. Rounds 1 and 2 were not edited, and no byte of either changed. Fixed by recording the mapping here rather than by amending them, which is what the byte-pinning of audit files requires. The general remedy is not this round's to make: a schema that carries a per-round `Superseded identifiers` field, or a rebuild that records its own mapping before the next round, would remove the need to notice. | fixed |
| S2-R3-02 | high | (branch topology) | The cleanup reached step 2's two branches and not step 1's. `fiat/1098-...-step-1-characterise-the-refusal-and-com` still points at `0e094109`, `%G?` of `E`, signed with the web-flow key `B5690EEEBB952194` -- the exact commit and the exact condition the controller refused round 2 for -- and `git ls-remote origin` shows the pushed branch at the same commit, so it is not only a local leftover. That tip is no longer an ancestor of step 2's audit head, because `d1dccbff` merges `5b02364a` with `ec7d95b5` directly and bypasses it. No content is lost: `0e094109`'s tree is `fa67636c83c5a724` and `ec7d95b5`'s is the same object, so step 2's chain carries step 1's exit state byte for byte by a locally signed route. What is left is a branch whose recorded receipt and whose current tip disagree with the chain its own content now ships through, one merge away from the range being receipted. Remedy is the controller's, not a Warden's: reset step 1's branch to a locally signed merge of the same two sides, its tree already being `ec7d95b5`'s, or record that step 1 is superseded and ships through `d1dccbff`. Not fixed here -- branch topology and anything touching a pushed ref is outside this round, and no `hexctl` or `gh` command was issued. | open |
| S2-R2-04 | low | tests/promise_machine_coverage.json:9-16 | Closed. `scripts/prove_agent_instruction_reconciliation.py` and `tests/test_agent_instruction_corpus.py` are bound as `prover` and `prover_tests` on the `agent_instruction` entry. The entry's key set is not closed anywhere -- the only closed set in that file is the `tests` record's `{path, sha256, selectors}` at `tests/test_agent_instruction.py:3265`, and `promise_machine.py`'s closed sets cover handoffs and runtime bindings, not this -- so the two keys are admitted without a schema change. `test_every_bound_capability_digest_matches_the_file_it_names` recomputes any `path`/`sha256` pair under a promise entry and its docstring says a capability added later is bound by construction, so the walker picked both up as soon as they were named; `test_specialised_coverage_binds_the_reconciliation_prover` makes the naming deliberate, so removing a row fails visibly rather than quietly unbinding a file. Observed refusing on a planted stale digest in a throwaway worktree. | fixed |
| S2-R1-06 | low | tests/test_agent_instruction.py:3200-3216 | Closed against the second branch of its own remedy, which was the message. There is no generator to name: `promise_machine.py sync` writes fixed plugin copies, `coverage` checks classifications, and neither rebinds these digests, so nothing regenerates the file and the first branch of the remedy remains unavailable. `assert_bound_paths` now names the file, the exact `shasum -a 256` recompute for the path that failed, and the ordering that causes the failure -- rebind as the last edit before committing rather than the first, which is precisely how this step lost a commit to a stale digest before round 1. Observed firing with that message on a planted stale digest; the live tree was untouched and `git status` was clean before and after. A generator would still be better than a message, and that is now a stated absence rather than a silent one. | fixed |
| S2-R1-07 | medium | (round evidence) | Carried, and the condition is now isolated rather than described. Three rounds, same classifier, same runner, same kind of change -- a pin or a message with no behaviour change: round 1 rebound a digest naming `scripts/agent_instruction.py` and read `guarded` on two parent failures that were the rebind; round 2 rebound nothing and read `passed`; round 3 rebound four digests and still read `passed`, with zero parent failures. The difference is not whether a rebind rode along but whether a rebound digest names a file Elenchus overlays. It overlays the fix's changed test paths, so a rebind naming `tests/test_agent_instruction.py` lands beside the bytes it describes and agrees, while one naming `scripts/agent_instruction.py` puts a new digest on the parent's old non-test bytes and manufactures a failure. Round 2's proposed remedy -- commit the rebind separately -- is withdrawn: a bound file and its digest cannot be split across two commits without a red one in between, so that was never available. The remedy is Elenchus's: overlay the non-test files a coverage rebind names, or keep a pure-digest register out of the changed-tests set. Not fixed here; it is a change to a sibling skill's classifier, outside this round and this step. | open |
| S2-R1-01 | high | scripts/agent_instruction.py:3291-3346 | Carried unchanged and still step 3's, as directed. The recommendation from round 2 stands on the evidence recorded there: step 3's exit should require widening the projection to the `artifacts.*.sha256` entries, because the subject switched onto the projection as it stands still moves on an out-of-span edit, and the widening is safe since an in-span edit still moves `span_sha256`. Nothing this round changed the projection, and `test_the_projection_does_not_yet_neutralise_the_bound_artefact_digests` and `test_the_projection_covers_only_the_source_quarter_of_the_bound_set` both still pass on the rebuilt branch. | open |
| S2-R2-02 | medium | tests/test_agent_instruction_corpus.py:101,138,162 | Carried unchanged and still step 3's, as directed. The four cases step 3 inverts and the fate of `rebind_measurement_corpus` and the `$.evidence.parity_record` refusal are as recorded in round 2. All ten cases in that module pass on the rebuilt branch. | open |
| S2-R2-03 | medium | tests/test_agent_instruction.py:2252 | Carried unchanged; skills#1127 owns it and the study defers it. The reproduction was not repeated this round and the ruling is not revisited: the fake adapter opens no socket, and the escaping `WAI-E-ADAPTER.TIMEOUT` is parallel-runner load against a two-second cap. The port's state is recorded below for this round as the register requires. | open |

Leads not pursued: whether the audit-record schema should carry a per-round `Superseded identifiers` field, which would have given S2-R3-01 a structural home instead of prose in the round that noticed; whether `promise_machine.py` should grow a rebind mode for the coverage digests, which is the first branch of S2-R1-06's remedy and remains unbuilt; whether Elenchus should overlay the non-test files a coverage rebind names, which is S2-R1-07's remedy and belongs to that skill; whether `backup/old-main-f72b20ae` is related to this run at all; and whether `mutations.json` and `questions.json` belong in `_corpus_sha256`'s subject, unchanged since round 1 and still step 3's. Evidence for this round: the rebuild was verified rather than accepted, on all three questions asked. `0e094109` is absent from both step 2 branches, and every one of the fourteen commits in `af660b1c..stacked` reports `%G?` of `G` under the single key `B83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day; the web-flow key appears nowhere in the live range. Every rebuilt commit carries the same tree object as the one it supersedes, listed in S2-R3-01, and the one that matters most does: `311acd87` carries `35339df5`'s tree `b016c4c53544c13a`, so the add/add resolution audited in round 2 is the one in place and no second resolution was substituted. The final trees are identical on both branches -- `1749fe26407974bb` for the stacked head against `backup/step2-audit-contaminated`, and `b0713e20cafd9f6e` for the step head against `backup/step2-contaminated`. The rebuilt merges' parents are `5b02364a` and `ec7d95b5` for `d1dccbff`, replacing `0e094109` with the side it merged, and `ed45f0d1` and `d1dccbff` for `311acd87`; `88ef0e6d`, `549030a8`, `ec7d95b5`, `0602b95e` and `ed45f0d1` all remain ancestors. Step 2's branches are on no remote, so the rebuild is uncontested and needs no force-push; step 1's branch is pushed at `0e094109`, which is S2-R3-02. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0 over `scripts/agent_instruction.py`, `scripts/prove_agent_instruction_reconciliation.py`, `tests/test_agent_instruction.py`, `tests/test_agent_instruction_corpus.py`, `tests/promise_machine_coverage.json`, `audit` and `docs`, run before and after the fix; the Pashov suite is waived and did not run. On the rebuilt branch before this round's fix, `python3 tests/run_tests.py` exits 0 at 1,133 of 1,133 and `agent_instruction.py check` exits 0, both as the controller reported; after the fix it is 1,134 of 1,134, up by the one case added. Every row of the coverage register was recomputed and none is stale, including the two added. The fix commit is `62efcc93`, signed, author Shoggoth, committer Dr Laurence E. Day, both trailers, two files, 52 insertions against 2 deletions. The source-bound classifier returns `passed` on `62efcc93` with 1,134 executed, 0 assertion failures, 0 errors and 0 skipped, and no parent failure at all, which is the third point of the contrast recorded at S2-R1-07. **The port's state, recorded as the register requires: `ollama serve` was listening on `TCP *:11434` as PID 19372 for the whole round, observed with `lsof -nP -iTCP:11434 -sTCP:LISTEN` and never contacted.** No socket was opened to it, no `measure` or `parity` ran, no model was consulted, and no `hexctl` command was issued. A green suite here is therefore not evidence of a green suite with the port free, and this round makes no CI claim. `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/horos/skills/horos/SKILL.md` and `PROMISE_MACHINE.md` are byte-identical to the step branch, no file under `tests/fixtures/agent-instruction-v1/` changed, and `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` pass, 2 of 2, untouched. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI, the unread skills#1127 and the unexamined remote pull requests, all eight finding ids with severities, file and line locations, statuses and remedies, the four superseded-to-rebuilt mappings with their tree objects, every commit identity, signature state and key, the suite counts, the port observation, the exact `passed` verdict, the withdrawal of round 2's own remedy suggestion, and the unpursued leads without changing any claim.

## Step 3, round 1 -- 2026-09-04T01:12:18Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=reviewed; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; unchanged across all seven rounds of this run, and still applies. Adapter portability: unchanged since step 1 round 1 and still unexamined, so the `refusal-detail-coverage` message surface is unread. The other two bound fixtures' reconciliation through the prover: `SUBJECT` is still pinned to `fiat-study-runbook-phase`, and the prover exercises no edit to `horos-boundary-check` or `promise-machine-router-selection`; step 2's naming of this still applies, narrowed by the fact that the three unit-level corpus cases now cover all three fixtures at the digest. The `before-span` placement: still uncovered, `span-shift` returns `value: 1` against a threshold of 2 and exits 1 with `uncovered: ['before-span']`, and the runbook schedules it at step 4. `measure` and `parity` were not run and 127.0.0.1:11434 was not contacted; the budget is one each and both were spent before this round. The recorded token counts were therefore not recomputed: `_measurement_material` carries `tokens` over from the supplied record rather than recomputing it, so `check` verifies each stream's `sha256` and `bytes` against the exact projected bytes and takes the count itself on trust. The full 16.8 GB and 65.4 GB model blobs were not rehashed; the Ollama store's own content-addressed filenames and the manifest layer digests were read instead. `_write`'s atomicity under interruption was not exercised; only its confinement and the fact that the prover writes into a `TemporaryDirectory` copy were read. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite did not run and this round claims nothing from it. `.horos/boundary.json` was regenerated by `horos.py scan --write` rather than reviewed row by row. Port state at exit, as the register requires: `lsof -nP -iTCP:11434 -sTCP:LISTEN` shows PID 19372 `/Applications/Ollama.app/Contents/Resources/ollama serve` listening, started 2026-09-03 14:55:26; the local suite is green with that listener present, which is not evidence about a CI run where it is absent.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | scripts/agent_instruction.py:2820,3105 | `WAI-E-MEASURE.PROJECTION` and `WAI-E-PARITY.PROJECTION` had no behavioural test. Observed by deleting both membership checks and running the full suite: 1139 tests, one failure, `test_specialised_coverage_binds_runtime_documentation_and_manifest` at `path='scripts/agent_instruction.py'`, which is the checker's own whole-file digest binding and notices any edit to that file whatever it does. `tests/promise_machine_coverage.json` was the only thing that saw the refusals go. `docs/agent-instruction-language-v1.md:454` and `docs/decisions/ADR-074-digest-neutral-measured-corpus.md:181-182` both state that an undefined `projection` name refuses with those two codes, so the contract was documented and unproved. Fixed at 2fdeeeb1: `test_undefined_measurement_projection_refuses` and `test_undefined_parity_projection_refuses` rename one recorded stream to `digest-neutral-bound-sha256/v2` in a copied tree, rebind the report's manifest binding so the projection check is the refusal reached rather than the stale-record digest, and require the exact code. Both fail without their check, reaching `WAI-E-MEASURE.RECORD` and `WAI-E-PARITY.RECORD` instead: the record byte comparison still catches the edit, under a code naming the wrong cause. Both are bound as selectors on the `agent_instruction` coverage entry. | fixed |
| S3-R1-02 | medium | tests/fixtures/agent-instruction-v1/evidence/family-profiles.json, scripts/agent_instruction.py:1495-1544 | The re-pin at 4a7d541f is materially right and its stated cause is not checkable from the fields the checker verifies. Verified independently from the machine, with no request made to 127.0.0.1:11434: the weights did not move -- `~/.ollama/models/manifests/registry.ollama.ai/library/qwen3.8-27b-aeon/q4_k_m` lists exactly `5caf67d19e598ac71ba02c3df7785f86247ec77ca4d54cba2e2d342453c87f27` as the model layer and `d466586099626aa3572522d8b6138a3c5b7793e699959da200a69169428d7b5a` as the projector, in that order, both blobs present, matching `model_blobs_sha256` and `vocabulary_sha256`; gpt-oss lists `6be6d66a3f546d8c19b130dc41dc24b2fc159f84ffbc76a0ee0676205083cf5a`, matching its own. The message attributes the drift to `a newer runtime`, and the two pins whose job is to catch that did not move and are correct today: `/Applications/Ollama.app/Contents/Resources/ollama` still hashes to `eee609f0a6da58b978d453e0385fd0e3496e6cf319c639875669b51cb4277d2d`, and every file in the bundle is dated 2026-08-22, earlier than the profile's own `observed_on` of 2026-08-30. The qwen config blob, unchanged since 2026-08-16, already carries `renderer: qwen3.5` and `parser: qwen3.5`; the gpt-oss config blob carries neither, which is why only one model's digest moved. What did change is the process: `lsof` shows PID 19372, `/Applications/Ollama.app/Contents/Resources/ollama serve`, started 2026-09-03 14:55:26, so the server that rendered the identity bytes on 2026-08-30 was a different, older build from the one that rendered them for this step. That process is what `_verify_profile_identity` reaches through the client at `scripts/agent_instruction.py:1528`, and no field in the profile identifies it: `runtime_executable_sha256` and `version_sha256` pin the client file and its `--version` output, both of which survived the change, so `WAI-E-ADAPTER.EXECUTABLE_CHANGED` and `WAI-E-ADAPTER.VERSION_CHANGED` cannot see a server swap. `acquisition_sha256` was the one field that saw it, and it was re-pinned by hand. Closing this needs a new pinned field, which is a design decision and not a round-1 patch. | open |
| S3-R1-03 | low | (commit 8d58b500) | The subject says `rebind the six coverage digests the reissue moved`, the body names seven items, and the commit moves eight: `checker`, `manifest`, `prover`, `prover_tests`, `documentation[0]` for `docs/agent-instruction-language-v1.md`, and the `family-profiles.json`, `measurement.json` and `parity.json` fixture rows. `family-profiles.json` is the one the body omits. Every one of the eight recomputes correctly against its file as committed, checked by walking every `path`/`sha256` pair in `tests/promise_machine_coverage.json`: no mismatches. Only the count in the message is wrong. Not fixed: the commit is on the step branch and rewriting its message would rewrite that branch's history, which this branch stacks on. | open |
| S3-R1-04 | low | docs/agent-instruction-language-v1.md:481-486 | The version-1 contract said the recorded figures were `taken before the corpus subject and the measured streams moved onto the projection described above, and refreshed from the reissued report once that run existed`, dated by `observed_on` `2026-08-30`. `evidence/measurement.json` holds one run, the step-3 reissue made after the switch, and `observed_on` is the pinned tokenizer profile's own observation date, copied in at `scripts/agent_instruction.py:3048` and compared back at `scripts/agent_instruction.py:3368`, so it dates the profile and not the run. The sentence reads as though the figures predate the projection, in the document a reader consults to learn what a count is a count of. Fixed at 2fdeeeb1, prose only. Every figure in that table was checked against the record first and none changed: source 11,170/2,528; canonical-model 8,569/1,989; compact 6,069/2,086; bootstrap 932/277; compact plus bootstrap 7,001/2,363; delta -4,169/-165; per-fixture -122, +705, -194; two-document prefix +306. | fixed |
| S2-R1-01 | high | scripts/agent_instruction.py:3379-3512 | Closed by this step, as step 2 round 3 directed. `_bound_digest_values` now enumerates each fixture's `source.sha256` and all five `artifacts.*.sha256`, eighteen across three fixtures, and `_corpus_sha256` digests its subject through `digest_neutral_projection`. Verified rather than read: narrowing the enumeration back to the source digests turns 27 cases red in `tests/test_agent_instruction.py` and 20 in `tests/test_agent_instruction_corpus.py`, and removing the projection from `_corpus_sha256` turns 8 and 3 red, with no errors in either mutation. The prover's end-to-end proof agrees: `offline` returns `value: true` with `exit: 0`, `accepted: true`, no refusals, and `corpus_sha256` `09160c0a1d2044c4d7786ff633281ba6acc527e2437a9cdc60d27f27730efb14`, identical to the committed digest, on a scratch tree carrying an out-of-span edit reconciled by the four mechanical passes with no evidence record rebound and no model consulted. | closed |
| S2-R2-02 | medium | tests/test_agent_instruction.py:3455-3872, tests/test_agent_instruction_corpus.py:340-720 | Closed. Six inversions landed, the four the amendment names and two more: the prover's `complete-reconciliation-refusal` check became `complete-reconciliation-accepted` with a `ProverError` behind it, and `test_prover_selftest_exits_zero_and_writes_a_closed_report` moved `('offline', 1)` to `('offline', 0)` and `assertFalse(reports['offline']['value'])` to `assertTrue`. Each was checked for vacuity by removing its property rather than by reading it. Narrowing the projection fails `test_the_projection_neutralises_the_bound_artefact_digests`, `test_out_of_span_edit_leaves_the_corpus_digest_where_it_was`, `test_out_of_span_edit_no_longer_stales_the_parity_record`, `test_the_corpus_subject_no_longer_carries_the_whole_file_source_digest`, `test_the_projection_covers_every_path_the_prover_binds` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`. Removing the projection from `_corpus_sha256` fails the last four of those. Widening the projection onto `span_sha256` fails `test_the_corpus_subject_still_moves_on_an_in_span_edit` for all three fixtures, `test_the_reviewed_span_digest_is_distinct_from_the_projected_digest` for all three, and `test_no_reviewed_span_carries_a_bound_digest`, so `in-span-edit-refusal` is defended from both sides. Every one of these is an assertion failure, not an error, so none of them could return `inconclusive` in place of a verdict. | closed |

Leads not pursued: whether `_bound_digest_values` should import the prover's `bound_digests` instead of restating the walk -- it cannot, because the prover imports the checker and the dependency runs one way, and `test_the_projection_covers_every_path_the_prover_binds` closes it by probing the checker with the prover's own list; whether `check` should refuse a fixture whose reviewed span covers its whole file, which would make `span_sha256` equal `source.sha256` and let the projection neutralise the review boundary -- `test_the_reviewed_span_digest_is_distinct_from_the_projected_digest` asserts the span is not `(0, len(raw))` for all three fixtures, so the suite catches it and the checker does not; whether the three integration cases that error rather than fail under a projection regression -- `test_bound_manifest_runs_complete_clean_demonstration`, `test_check_emits_bounded_binding_roundtrip_mutation_and_summary_records` and `test_checker_emits_one_record_per_mutation` -- should be made to fail cleanly, observed only under a synthetic mutation and alongside fourteen clean failures, so the signal was never ambiguous; whether the profile should pin the model config blob as well as the weight layers, since `renderer` and `parser` live there and decide how a prompt is rendered, which is S3-R1-02 seen from the model's side; the Elenchus verdict's own shape this round -- the parent assertion Elenchus read was `test_specialised_coverage_binds_runtime_documentation_and_manifest` at `path='docs/agent-instruction-language-v1.md'`, the coverage rebind, and not either new projection case, which pass on the parent because the refusals they cover were already there and only the tests were missing; this is the fourth round in this run where a rebind supplied the guard, which S2-R1-07 already carries.

## Step 3, round 2 -- 2026-09-04T01:26:37Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=reviewed; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=not-applicable; reconcile-tool-writes=not-applicable; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run in this round either, so nothing here says this branch is green in CI; unchanged across all eight rounds of this run. Adapter portability and the `refusal-detail-coverage` message surface: unchanged since step 1 round 1 and still unexamined; this round's diff adds two test cases and one ADR paragraph and reaches neither. `reconcile-tool-writes` and `span-offset-shift` are marked not-applicable rather than reviewed because this round's diff touches no tool write path and no offset handling; round 1 reviewed both and the `before-span` placement is still uncovered, `span-shift` still returning `value: 1` against a threshold of 2, and still step 4's. `measure` and `parity` did not run and no socket was opened to 127.0.0.1:11434; the machine facts in S3-R2-01 come from `shasum`, `stat`, `lsof`, `ps` and reads under `~/.ollama/models`, all filesystem and process-table reads. The recorded token counts were again not recomputed, for the reason S3-R2-05 records, and the 16.8 GB and 65.4 GB model blobs were again not rehashed; the store's content-addressed filenames and the manifest layer digests were read instead. The prover was not re-run this round: `offline`, `span-shift` and `selftest` were executed in round 1 at `value: true` exit 0, `value: 1` exit 1 and `value: 6` exit 0, and nothing in this round's diff reaches the prover or the checker. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite did not run and this round claims nothing from it. Port state at exit: `lsof -nP -iTCP:11434 -sTCP:LISTEN` shows PID 19372 `/Applications/Ollama.app/Contents/Resources/ollama serve` listening, started 2026-09-03 14:55:26 and unchanged since round 1; the local suite is green with that listener present, which is not evidence about a CI run where it is absent.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | medium | (commit 4a7d541f) | Correction of a durable record, made inside the round that establishes it, because the schema takes one and the commit cannot be rewritten. `4a7d541f` states: `acquisition_sha256` digests the whole output of `ollama show --modelfile qwen3.8-27b-aeon:q4_k_m`, and that output gained `RENDERER qwen3.5` and `PARSER qwen3.5` lines from a newer runtime. The clause `from a newer runtime` is false as written, and it is now the durable record of why a `TRUSTED_` constant moved. Observed from the machine on 2026-09-04, with no request made to 127.0.0.1:11434. There is no newer runtime at the pinned path: `/Applications/Ollama.app/Contents/Resources/ollama` still hashes to `eee609f0a6da58b978d453e0385fd0e3496e6cf319c639875669b51cb4277d2d`, matching `runtime_executable_sha256`, and the newest file anywhere in the bundle is dated 2026-08-22 20:34:50, earlier than the profile's own `observed_on` of 2026-08-30. The two lines are not new either: the qwen model config blob `f6f3f3e89ed32d9ed38663f385e97b4346553640307216958c3c8ad89ebbfe20`, unchanged since 2026-08-16 07:47:16, already declares `renderer: qwen3.5`, `parser: qwen3.5` and `requires: 0.32.5`. The true cause is the serving process, which is neither the file nor the config: `lsof -nP -iTCP:11434 -sTCP:LISTEN` shows PID 19372, `/Applications/Ollama.app/Contents/Resources/ollama serve`, started 2026-09-03 14:55:26. The server that rendered the August identity bytes was an older build than the 2026-08-22 bundle and did not render those two config keys; the restart on 2026-09-03 put the 2026-08-22 build in front of the same unchanged config, which does. So the runtime that changed is the running server and not the file the profile pins, and the message's own next sentence, `this is one model's modelfile text, not the machine`, is right for the wrong reason: gpt-oss's config blob `0b3eaefc220f88ee3875909c1306654b2fbda9e31dae0133d6cdf488b37f9402` carries no `renderer` or `parser` key at all, which is why only one model's digest moved. Everything else `4a7d541f` asserts stands and was re-verified: the weight blobs `5caf67d19e598ac71ba02c3df7785f86247ec77ca4d54cba2e2d342453c87f27` and `d466586099626aa3572522d8b6138a3c5b7793e699959da200a69169428d7b5a` are the qwen manifest's model and projector layers in that order, `6be6d66a3f546d8c19b130dc41dc24b2fc159f84ffbc76a0ee0676205083cf5a` is gpt-oss's, and the parity answers therefore came from the same weights. | closed |
| S3-R1-02 | medium | tests/fixtures/agent-instruction-v1/evidence/family-profiles.json, scripts/agent_instruction.py:1495-1544 | Carried and filed sharply, because it is the shape of the fault this delivery exists to fix. `acquisition_sha256` digests rendered text, so it moves on a change that alters nothing that matters and would move identically on a change that alters everything: the same bytes-changed signal covers a cosmetic rendering difference and a genuine renderer or parser swap, and the operator cannot tell them apart from the digest. The only available response to either is a hand re-pin, which is what happened, and S3-R2-01 is what a re-pin under an indistinguishable signal costs. It was also the only field that could see the change at all: `_verify_profile_identity` checks `runtime_executable_sha256` at `scripts/agent_instruction.py:1506` and `version_sha256` at `scripts/agent_instruction.py:1527` before it reaches `acquisition_sha256` at `scripts/agent_instruction.py:1538`, and both survived the server swap, so `WAI-E-ADAPTER.EXECUTABLE_CHANGED` and `WAI-E-ADAPTER.VERSION_CHANGED` cannot fire on it. What a new pinned field would have to cover, stated so a carryover row can point at something real: the identity of the process answering on 127.0.0.1:11434, established before any measured or answered call, as a fact about the process rather than about text it renders; and the model's config blob digest, because `renderer`, `parser`, `requires` and the layer `diff_ids` all live there, it is content-addressed and stable, and pinning it would have rendered the August-to-September change as what it was -- config identical, difference in the renderer -- instead of as an opaque digest move. What it costs. The profile is a closed twenty-seven-field object at `scripts/agent_instruction.py:1946-1972`, so a new field is a version change to `wildcat-agent-instruction-family-profile/v1` and re-pins the same three bindings this re-pin moved: the profile, the manifest's `evidence.family_profiles` pointer and `TRUSTED_PROFILE_SHA256`. The config blob lives under `~/.ollama/models`, outside the repository root, and `read_confined` takes a root-relative path by construction, so the checker either gains a second read path with its own allowlist, which is a phylax boundary widening, or obtains the digest through the server and is back to trusting rendered text. And the field would do its job by refusing rather than being re-pinned, which means a genuine renderer change would block the delivery until a fresh `measure` and `parity` pair could be run -- a budget this run does not have. That trade is the finding: pinning the thing that matters makes an unrepeatable run refusable. Not step 3's: it changes a schema and needs a model run, so it belongs in the run's carryover as its own issue. | open |
| S3-R1-03 | low | (commit 8d58b500) | Closed by correction here, not by editing the commit, and no later step or issue owns a rewrite. The true inventory, recomputed against the files as committed: the subject says six, the body names seven -- checker, prover, corpus test module, manifest, both evidence records, version-1 contract -- and the commit moves eight `sha256` values in `tests/promise_machine_coverage.json`. The eighth, omitted from the body, is `tests/fixtures/agent-instruction-v1/evidence/family-profiles.json`, moved by the re-pin at `4a7d541f` earlier in the same step. All eight are correct against their files, checked by walking every `path` and `sha256` pair in that file: no mismatches, in round 1 and again this round. The remedy is the record and not the message. `8d58b500` is the step branch tip and this audit branch stacks on it, so rewriting the message would orphan round 1's two commits and the receipt naming that sha, which push discipline refuses rather than defers. `git notes` would annotate without rewriting and was considered and rejected: notes are not pushed by default, are not this repository's practice, and inventing a mechanism mid-run puts the correction somewhere no later reader looks. Step 2 round 3 met the same wall with its superseded-to-rebuilt map and answered it the same way, in the round's own prose. | closed |
| S3-R2-04 | medium | docs/decisions/ADR-074-digest-neutral-measured-corpus.md:176-190 | The whole-file-span hazard is latent rather than present, and until this round no record named it as a trap. The projection substitutes a digest value and cannot tell a `source.sha256` from any other field carrying the same bytes. No fixture's reviewed span covers its whole file today, so no `span_sha256` holds those bytes and the review boundary survives; a fourth fixture whose span ran from 0 to the file's length would make the two digests identical, the projection would neutralise the span digest along with the binding it aims at, and `in-span-edit-refusal` would fail while every other property still held. `test_the_reviewed_span_digest_is_distinct_from_the_projected_digest` at `tests/test_agent_instruction.py:3825` asserts `(start, end)` is not `(0, len(source))` for every fixture and that no `span_sha256` is in the bound set, so the suite catches it and `check` does not: a fourth-fixture author meets a test assertion rather than a refusal naming the cause. `scripts/agent_instruction.py` says it in a docstring and ADR-074 did not, which is the record a reader consults for the projection's boundary. Fixed at the record, not at the checker, and the row says which: ADR-074's Consequences now carries the trap, names the case that catches it, and states that closing the gap properly means a new refusal in `check` and therefore a change to a version-1 contract, which this round did not take. | fixed |
| S3-R2-05 | high | scripts/agent_instruction.py:2797-2836 | The recorded token counts are carried, not recomputed, so no gate can see a hand-edited one. `_measurement_material` reads `tokens` from the supplied record and puts that same value into the expected object it compares against, which makes the comparison tautological for that one field; the docstring added by this step says so plainly, and the reason is sound -- recomputing a count means consulting the model, which `check` must not do. What that leaves is the delivery's acceptance number resting on the run having been honest. Demonstrated rather than argued, in a throwaway copy: moving `documents[0].compact.tokens` by -5 and nothing else refuses `WAI-E-MEASURE.RECORD` at `$.evidence.measurement_record.documents[0]`, because `check` recomputes `one_document`, `totals`, `amortised` and every `measurement.result` event from `documents[*]`; moving the same count and every field derived from it, with the record's manifest binding rebound, exits 0 with no refusal and `totals.delta_tokens` reading `-170` in place of `-165`. `WAI-E-MEASURE.NON_NEGATIVE_DELTA` does not fire on either direction of that edit, so the one refusal the runbook names as able to end the delivery can be walked past by arithmetic alone. This is not a claim that the committed counts are wrong: round 1 recomputed every digest, byte count, correlation id and derived total in the record and all of them hold, and `-165` is honest arithmetic over the counts the run produced. It is a claim about what the exit clause `no count, correlation id or date is hand-written` is worth mechanically -- the date is checked against the tokenizer profile at `scripts/agent_instruction.py:3368` and the correlation id recomputes from the corpus, profile and bootstrap digests, and the counts are checked by nothing. What still constrains a tamper is a reviewer reading a diff: the edit must be carried through every derived field, the record's manifest binding must be rebound, and `tests/promise_machine_coverage.json` binds the record as well. Pinned rather than closed, because closing it means binding the counts to the run that produced them and that is a design change needing a model budget this run has spent: `test_an_edited_token_count_refuses_on_its_own` records the half of the guard that works and `test_a_consistent_token_count_edit_is_not_detected` fixes the exposure in place, expected to go red the day the counts are bound and to be deleted deliberately when it does. | open |

Leads not pursued: whether `check` should refuse a whole-file reviewed span outright rather than leaving the suite to catch it, which is S3-R2-04's proper closure and costs a new refusal code in a version-1 contract; whether the two evidence records should carry an adapter-side signature over the counts, which is the only shape that would close S3-R2-05 without a second model run and which nobody has costed; whether `acquisition_sha256` should be split into the part that identifies the model and the part that is rendering, so a re-pin stops being the only response to either, noted here because S3-R1-02's remedy may be a narrower field rather than an extra one; whether the audit-record schema should carry a `Corrections` field, raised again because S3-R2-01 and S3-R1-03 both had to correct a durable record from inside a round's prose and step 2 round 3 already hit the same wall, so this is the third instance and the first time the corrected record is a commit message rather than an audit round; the Elenchus verdict's shape this round, reported as returned and qualified here because the verdict field takes one word and nothing else -- the parent assertion Elenchus read was `test_specialised_coverage_binds_runtime_documentation_and_manifest` at `path='docs/decisions/ADR-074-digest-neutral-measured-corpus.md'`, this round's coverage rebind, and not either new token-count case; both of those pass on the parent, because the exposure they pin exists there too and only the cases were missing, so `guarded` is true of the commit and says nothing about the two cases that are the round's actual work. Fifth round in this run where a rebind supplied the guard, which is the condition S2-R1-07 carries and round 1 recorded for the fourth time.

## Step 4, round 1 -- 2026-09-04T03:47:48Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=reviewed; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=not-applicable; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; unchanged across all nine rounds of this run. Adapter portability and the `refusal-detail-coverage` message surface: unchanged since step 1 round 1 and still unexamined; step 4's diff reaches neither. `measure` and `parity` did not run, no socket was opened to 127.0.0.1:11434, no model was consulted and no `hexctl` command was issued; the budget is one each and both were spent before this round. The recorded token counts were therefore not recomputed, for the reason S3-R2-05 carries, and `totals.delta_tokens` reads `-165` on a fixture tree `git diff f91bbc6c..HEAD -- tests/fixtures/` shows to be byte-unchanged by step 4 and by this round's fixes. The other two bound fixtures are still unexercised through the prover: `SUBJECT` is pinned to `fiat-study-runbook-phase` and no edit to `horos-boundary-check` or `promise-machine-router-selection` was reconciled, so the before-span behaviour proved below is proved for one fixture. The re-derivation was not exercised against a non-uniform delta, because no such edit exists for this fixture: an edit between two node spans is inside the reviewed span, where `rederive_offsets` returns `None` by contract. Whether `_corpus_sha256` should carry `source.start` and `source.end` at all is the design question behind S4-R1-01 and was not decided here; the record was read, not reopened. The design record itself was read and not amended: no `hexctl` ran, so criterion `span-shift-regression` still declares `concern: correctness` over a coverage count, which is S4-R1-03. Elenchus wrote its report inside a detached parent worktree; `.hexaemeron/elenchus/step-4.json` in this tree is from the local runner rather than from the classifier, so the verdict below is declared and its report bytes are not attested. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite did not run and this round claims nothing from it. Port state at exit, as the register requires: `lsof -nP -iTCP:11434 -sTCP:LISTEN` shows PID 19372 `ollama serve` listening on `TCP *:11434`, started 2026-09-03 14:55:26 and unchanged since step 3; the local suite is green with that listener present, which is not evidence about a CI run where it is absent.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | high | scripts/agent_instruction.py:3503-3510, .hexaemeron/design-evidence.json, docs/agent-instruction-machine-binding/runbook.md:150-160 | The delivery closes #1098's first acceptance check for one class of edit and not the other, and nothing a reader reaches says so. That check is "a contributor on Linux with no Ollama can edit `plugins/hexaemeron/skills/fiat/SKILL.md` and bring `tests/test_agent_instruction.py` green"; it names no placement. Verified end to end at `36b6f99a`, by hand and without the prover's own `reconcile`: a `git archive` copy, a 47-byte comment prepended to the bound source, the whole-file digest rebound, `source.start`/`source.end` re-derived by locating the reviewed span's bytes (18445 to 18492, 22773 to 22820, delta 47, digest checked at the new position), the seven `source-spans.json` entries and the seven `model.json` bindings moved to their located offsets, `compact.wai` regenerated by `format`, the three artefact digests rebound, then the coverage register's four moved rows rebound and `.horos` regenerated. `agent_instruction.py check` refuses at exit 2 with one refusal, `{"code":"WAI-E-DIGEST.CORPUS","node_path":"$.evidence.measurement_record"}`, and `python3 -m unittest tests.test_agent_instruction` reports 11 failures and 4 errors, 15 of which name `WAI-E-DIGEST.CORPUS`. The identical procedure with the same comment appended leaves `check` at exit 0 with no refusal and the module green but for `test_portable_promise_machine_package_is_current`, which fails identically on an unedited copy and is an artefact of the scratch directory. The cause is `_corpus_sha256`'s subject carrying `fixtures` whole at `scripts/agent_instruction.py:3503-3510`: `digest_neutral_projection` substitutes only values in `_bound_digest_values`, and an offset is not one, so `start` and `end` stay in the digested subject. Mason's account is correct in every particular. The consequence is not: study assumption 5 says "an edit that leaves those bytes identical has not changed what the recorded token counts are counts of", and that assumption "chooses the selected design", yet a before-span edit leaves the reviewed bytes identical and is still charged a `measure` run. So acceptance check one is **met for an edit after the reviewed span end and not met for an edit before the reviewed span start**, and the run's carryover and its integration pull request must say it in those words rather than reporting the check closed. Open because closing it is a design decision on `_corpus_sha256`'s subject that no audit round may take: it would either drop the offsets from the subject, which weakens the corpus digest again, or accept the cost and say so. | open |
| S4-R1-02 | medium | .hexaemeron/reports/digest-neutral-corpus-span-shift-regression.json | The gate's committed evidence carries no trace of the refusal it covers. The report is the seven-key `protasis-design-report/v1` shape and its whole content is `criterion: span-shift-regression`, `unit: count`, `value: 2`, `exit: 0`. `prove_span_shift` returns `len(placements)` and `not uncovered`, so neither number moves when a placement refuses; the outcomes live in `detail`, and `--detail` writes to stderr and is absent from the resolver command the design record names. A reader of the committed report cannot tell that `before-span` is `{"accepted": false, "exit": 2, "refusals": [{"code": "WAI-E-DIGEST.CORPUS", "node_path": "$.evidence.measurement_record"}]}` while `after-span` is accepted. Partially answered at `32b7d8e3`: the prover's module docstring now states that the count is placements covered rather than placements green, names both outcomes, and says the report carries the count and the exit while the outcomes are on `--detail`. Open at the report itself, because the seven-key shape is `design_probe.py`'s and widening it is Protasis's decision, not this round's. | open |
| S4-R1-03 | medium | .hexaemeron/design-evidence.json | The criterion's own wording does not fix the reading its resolver takes. `span-shift-regression` declares `kind: gate`, `owner: elenchus`, `stage: conformance`, `blocks: integration`, `comparator: at-least`, `threshold: 2`, `unit: count` and `concern: correctness`, and carries no prose field: the criteria array holds exactly those eight keys. Nothing there says what is counted. `prove_span_shift` counts placements covered, which is 2; a green-count reading, placements whose reconciliation is accepted, gives 1 and leaves the gate unmet against the same threshold. Mason's reading is defensible and is the one the prover implements, but `concern: correctness` points at the other one, and a `count` unit with no named subject cannot settle it. Open, and not fixable here: the design record is controller-pinned and amending it needs `hexctl`, which this round is forbidden. | open |
| S4-R1-04 | medium | scripts/prove_agent_instruction_reconciliation.py:472-545, tests/test_agent_instruction_corpus.py:348-375 | The node loop's central claim was not pinned by anything that would fail if it stopped being true. `rederive_offsets`'s docstring, the module header and the test's own docstring all said the offsets are re-derived rather than shifted, the last of them as "Three assertions separate the two, and a shift passes none of them". Mutation-tested at `36b6f99a`: replacing the loop's `_locate` and its two checks with `new_start = start + delta` left the full suite behaviourally green at 1118 of 1130 in a `git archive` copy against a 1120 of 1130 baseline, and the only two extra failures were `test_every_bound_capability_digest_matches_the_file_it_names` and `test_specialised_coverage_binds_the_reconciliation_prover`, both whole-file digest bindings that any byte change to the file trips. `tests/test_agent_instruction_corpus.py` passed 14 of 14 unchanged. The reason is structural rather than an oversight: a prepend moves every span by one uniform delta, so a located node and a shifted node land on the same integers and the per-node digest check at those integers passes either way. Fixed at `32b7d8e3` by pinning the refusal a shift has no opinion about: `assertPlantedSpanRefuses` plants a node whose recorded bytes occur twice inside the reviewed span, where locating refuses and adding does not, and the same arithmetic mutation now fails that assertion by `ProverError not raised`. The two docstrings were corrected to say which assertions do the work and that no assertion on the integers can separate the two. | fixed |
| S4-R1-05 | medium | docs/decisions/ADR-075-digest-neutral-measured-corpus.md:144-150 | The Consequences opening was false for one of the two placements, and contradicted the same section. It read "An out-of-span edit to a bound instruction document, with the five mechanical passes applied, no longer moves the corpus digest, and neither evidence record is staled by it." A before-span edit is out of span -- the reviewed bytes are untouched and `WAI-E-DIGEST.SOURCE_SPAN` does not fire -- and it does move the corpus digest, as S4-R1-01 shows by hand; it also needs a sixth pass the sentence does not name. Three paragraphs below, the same section says the corpus digest "still distinguishes a change to ... any recorded offset", which is the true statement the opening denies. Mason judged the sentence literally true and left it alone as byte-bound and outside step 4's Files; on the reading a contributor will take, that "out-of-span edit" means both placements, it is not true, and the record is the thing a reader consults for the projection's boundary. Fixed at the record, at `d0e411db`: the opening now names the after-span placement, a second paragraph names the before-span placement, the sixth pass and the refusal, and the in-span paragraph is unchanged. The coverage register's ADR row was rebound in the same commit. | fixed |
| S4-R1-06 | low | .hexaemeron/reports/digest-neutral-corpus-offline-reconciliation-green.json | The sibling gate over-claims in the same shape as S4-R1-02, one level quieter. `prove_offline` exercises `AFTER_SPAN_PLACEMENT` alone and writes `value: true` under the criterion `offline-reconciliation-green`; the runbook's exit reads "The offline report proves a bound-source edit reconciles with no model consulted", with no placement named. Verified: the before-span placement does not reconcile offline, and the report says nothing about it. Lower than S4-R1-02 because `span-shift` is where both placements are meant to be visible and `offline` was never scoped to both. Partially answered at `32b7d8e3`, where the module docstring now says `offline` answers for the after-span placement alone. Open at the report and at the runbook's exit wording. | open |
| S4-R1-07 | low | scripts/prove_agent_instruction_reconciliation.py:183-201, 349-357 | The hardening this step introduced skipped the one pair it most needed to cover. `recorded_offset` was added with a docstring stating why `int()` is wrong -- it takes `"+1"`, `" 1"`, `"1_0"` and a Unicode digit, and raises `ValueError` rather than a `ProverError` on the rest, so a drifted record reaches the reader as a crash instead of a refusal naming the field -- and every `source-spans.json` and `model.json` offset goes through it. `Reconciliation.__init__` then read the manifest's own `source.start` and `source.end`, the anchor the whole re-derivation hangs off and the pair `delta` is computed from, with bare `int()`. Reachable rather than theoretical: a manifest carrying `"018445"` passes the constructor's whole-file digest check, its `span_sha256` check and every `bound_digests` check, because `int("018445")` is `18445`. Fixed at `32b7d8e3`: both go through `recorded_offset` with `limit = len(self.source)`, the `end <= start` check the node loop already had was added, and `test_the_manifests_own_offsets_are_read_as_recorded_decimals` plants a padded decimal in a throwaway copy and fails on `36b6f99a` by assertion, `ProverError not raised`. | fixed |
| S3-R1-02 | medium | tests/fixtures/agent-instruction-v1/evidence/family-profiles.json, scripts/agent_instruction.py:1495-1544 | Carried from step 3 round 1 and still open at step 4's audit. `acquisition_sha256` digests rendered text, so a cosmetic rendering difference and a genuine renderer or parser swap produce the same bytes-changed signal and the operator cannot tell them apart; the only response to either is a hand re-pin. Step 4's diff reaches no adapter or profile path, so nothing this round did changes it either way, and no round has yet costed the narrower fields the row names: the identity of the process answering on 127.0.0.1:11434, and the model's config blob digest. | open |
| S3-R2-05 | high | scripts/agent_instruction.py:2797-2836 | Carried from step 3 round 2 and still open at step 4's audit, and it is the reason S4-R1-01 costs what it does. The recorded token counts are carried rather than recomputed, so `_measurement_material`'s comparison is tautological for `tokens` and no gate can see a hand-edited count; `WAI-E-MEASURE.NON_NEGATIVE_DELTA` does not fire on an edit that moves the count and every field derived from it. Step 4's diff touches no measurement path and this round ran no `measure`, so the exposure is unchanged and `totals.delta_tokens` still reads `-165` on a byte-unchanged fixture tree. | open |

Leads not pursued: whether `_corpus_sha256` should project `source.start` and `source.end` the way it projects bound digests, which would make both out-of-span placements corpus-neutral and is the only route that closes S4-R1-01 without accepting the cost -- not pursued because it widens what the corpus digest stops distinguishing for a second time in one delivery and belongs to Protasis and a design amendment, not to a round; whether `prove_offline` should take a placement argument so `offline` can be asked about both, which would answer S4-R1-06 at the tool rather than in a docstring and costs a second resolver row in the design record; whether the `protasis-design-report/v1` shape should carry a bounded outcome field, which is the structural home S4-R1-02 has none of and is the third time in this run that a finding has had to live in prose because the schema takes one word; whether `S3-R2-04`'s row in this file, which names `docs/decisions/ADR-074-digest-neutral-measured-corpus.md`, should be reconciled with the renumbering to ADR-075 that `aa5035cc` performed one commit later -- deliberately not touched, because audit rounds are byte-pinned and a sweep is exactly what that pinning refuses, so it is named here instead; and whether the three-and-a-half-minute cost of `prove_span_shift`, now four `reconcile` calls each spawning a full `check` subprocess, is worth a shared scratch tree. Evidence for this round: the step diff `f91bbc6c..36b6f99a` is three files, 545 insertions against 46 deletions, and was read in full. Phylax exit 0, Ephoros exit 0 and Hypomnema exit 0, before and after the fixes, over `scripts/prove_agent_instruction_reconciliation.py` and `tests/test_agent_instruction_corpus.py` for the first two and `docs`, `scripts` and `tests` for the third; the Pashov suite is waived and did not run. `python3 tests/run_tests.py` exits 0 at 1,143 of 1,143 on `36b6f99a` and at 1,144 of 1,144 after the fixes, up by the one case added. Both resolvers exit 0 before and after, and re-running them with `--report` leaves both report files byte-identical; `selftest` exits 0. The two headline mutations were confirmed independently: removing `rederive_offsets` fails `test_a_before_span_edit_has_its_offsets_re_derived_rather_than_shifted` by assertion, "the prover re-derives no offsets, so a before-span edit cannot reconcile", not by `AttributeError`, because the case opens with a `hasattr` guard; and always rebinding the measurement corpus digest fails `test_a_before_span_edit_still_stales_the_measurement_record` by assertion on the exact claim, `$.evidence.measurement_record` against `$.evidence.parity_record`. Six other cases error rather than fail under the first mutation, which is the pre-existing surface and not the two the runbook names. `test_stale_measurement_report_refuses` and `test_stale_parity_report_refuses` pass, 2 of 2, and `git diff f91bbc6c..HEAD -- tests/test_agent_instruction.py` is empty, so neither guard was touched by the step or by this round. No file under `tests/fixtures/agent-instruction-v1/` changed at any point, and `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/horos/skills/horos/SKILL.md` and `PROMISE_MACHINE.md` are byte-identical to the step branch; the hand reconciliation that produced S4-R1-01 ran entirely inside `git archive` copies under a scratch directory. Every row of the coverage register was recomputed against the tree and none is stale, including the three this round rebound. The fix commits are `d0e411db` and `32b7d8e3`, both signed, author Shoggoth, committer Dr Laurence E. Day, both required trailers; two files and 18 insertions against 4 deletions, then three files and 152 insertions against 45 deletions. The source-bound classifier, `python3 tests/run_tests.py --elenchus-report {report}` at CLI format `unittest-json-v1` writing schema `elenchus.unittest.v1`, returns `guarded` on `32b7d8e3` with 1,144 executed, 3 assertion failures, 0 errors, 0 skipped and exit code 1. Two of those three are the coverage rebind reaching the parent, the sixth round in this run where that has happened and the condition S2-R1-07 carries; the third is `test_the_manifests_own_offsets_are_read_as_recorded_decimals`, which is this round's own guard and fails on the parent for the reason it was written. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI, the unexercised fixtures and the unattested Elenchus report bytes, all nine finding ids with severities, file and line locations, statuses and remedies, the two carried findings with their status unchanged, both refusal codes with their node paths, every offset, delta, digest, count and commit identity, the exact `guarded` verdict with its qualification intact, the direct answer on acceptance check one, and the unpursued leads without changing any claim.

## Step 4, round 2 -- 2026-09-04T07:44:02Z

Audit schema: fiat-audit-round/v2

Covered: measure-sign-flip=reviewed; projection-honesty=reviewed; whole-file-binding-loss=reviewed; corpus-subject-closure=reviewed; stale-guard-retention=reviewed; in-span-edit-refusal=reviewed; span-offset-shift=reviewed; reconcile-tool-writes=reviewed; refusal-detail-coverage=reviewed; coverage-row-currency=reviewed; port-dependent-suite=reviewed; warm-model-precondition=not-applicable

Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; unchanged across all ten rounds of this run. `measure` and `parity` did not run, no socket was opened to 127.0.0.1:11434, no model was consulted and no `hexctl` command was issued; the second pair the 2026-09-04 study amendment buys belongs to step 5 and spending it here would end the delivery. The recorded token counts were therefore not recomputed, for the reason S3-R2-05 carries. Elenchus did not run and the verdict below is `null`: this round made no fixes commit, so the classifier had no subject, and the record commit is not one. The ADR renumber S4-R2-01 names was not applied, for the reason that finding gives, so the branch is left red rather than half-repaired. The other two bound fixtures are still unexercised through the prover: `SUBJECT` is pinned to `fiat-study-runbook-phase` and no edit to `horos-boundary-check` or `promise-machine-router-selection` was reconciled. Both amendments were read as design record and audited as prose; neither was amended and no hand edit was made to `.hexaemeron/study.md` or `.hexaemeron/runbook.md`, both of which are untracked under `.hexaemeron/.gitignore` `*`. Step 5's own implementation was not reviewed, because it does not exist. Imprimatur, Vulgate and Brevitas belong to the prose phase and were not run. The waived Pashov Solidity suite did not run and this round claims nothing from it. Port state at exit, as the register requires: `lsof -nP -iTCP:11434 -sTCP:LISTEN` shows PID 19372 `ollama serve` listening on `TCP *:11434`, unchanged since step 3; the local suite result below was taken with that listener present, which is not evidence about a CI run where it is absent.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R2-01 | high | tests/test_decision_records.py:110-153, docs/decisions/ADR-075-digest-neutral-measured-corpus.md | The root suite is red on this branch, and step 4's exit requires it green. `python3 tests/run_tests.py` reports 1143 of 1144 and exits 1; the one failure is `tests.test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`, asserting "ADR-075: this branch adds ['ADR-075-digest-neutral-measured-corpus.md'] while the default branch already has ['ADR-075-keep-suite-redundancy-discovery-report-only.md']". Not a regression in the audited diff: the branch is byte-unchanged since `36b6f99a` and the test compares against the default branch, which moved. `origin/main` gained the colliding record through merge `0a0995e2`, pull request #1187, at 2026-09-04 07:55:01 +0100, and the local remote ref was fast-forwarded to it at 07:56:03 +0100. At round 1's `2026-09-04T03:47:48Z` the local `origin/main` stood at `a46f3b50`, whose tree carries no such file, so round 1's 1,144 of 1,144 at exit 0 was true when it was written and this is a new condition rather than an over-claim. Second collision of this run: `aa5035cc` already renumbered ADR-074 to ADR-075 for the same reason one step earlier. The remedy is the test's own -- renumber to ADR-076, unused on `origin/main` today, and move every live reference with it: the filename, its `# ADR-075:` heading, its line-11 prose, `tests/promise_machine_coverage.json:31`, `tests/test_agent_instruction.py:3283-3301`, `tests/test_agent_instruction_corpus.py:30,576` and `scripts/agent_instruction.py:3490`, then rebind the coverage rows the edits move. Not applied in this round: the receipted runbook amendment `a8c2b308` names `docs/decisions/ADR-075-digest-neutral-measured-corpus.md` in its Files list and "ADR-075's Consequences" in its Exit, so the rename and one `hexctl amend runbook` have to land together and only the controller may issue the second; renaming alone would leave a receipted design record pointing at a file that is not there, which is worse than the red the finding reports. | open |
| S4-R2-02 | medium | .hexaemeron/runbook.md amendment 2026-09-04, docs/decisions/ADR-075-digest-neutral-measured-corpus.md:150-161 | The amended step 5 exit instructs retiring a paragraph that is only half false. It reads "ADR-075's Consequences names both out-of-span placements as corpus-neutral and retires the sixth-pass paragraph that step 4's audit added for the before-span case." That paragraph, written at `d0e411db` to close S4-R1-05, carries two separable claims. Step 5 falsifies one: with the pass applied the edit "gets past `WAI-E-DIGEST.SOURCE_SPAN` and then refuses `WAI-E-DIGEST.CORPUS` at `$.evidence.measurement_record`. Only a `measure` run clears it." Step 5 leaves the other true: reconciling a before-span edit "needs a sixth pass, `span-offsets`, which re-derives those offsets from the reviewed span's own bytes". Checked at the code rather than argued: `_check_fixture` at `scripts/agent_instruction.py:2628-2631` refuses `WAI-E-DIGEST.SOURCE_SPAN` whenever `_digest(source_bytes[start:end])` misses the recorded `span_sha256`, and taking the offsets out of `_corpus_sha256`'s subject does not reach that comparison; the amended exit concedes it in its own words, "with the five mechanical passes and the re-derivation applied". Retiring the paragraph whole leaves ADR-075 implying the two out-of-span placements cost the same reconciliation when one still needs a pass the other does not, which is S4-R1-05's defect class inverted, in the record S4-R1-05 was raised against. Remedy: retire the two falsified sentences and keep the sixth-pass sentence. | open |
| S4-R2-03 | medium | docs/decisions/ADR-075-digest-neutral-measured-corpus.md:175-181 | A sentence step 5 falsifies that the amendment does not schedule. Consequences reads "It still distinguishes a change to any reviewed span, to any recorded offset, to the set or the paths of the bound artefacts, or to the counts and risk classes." True on this branch, because `source.start` and `source.end` sit in `_corpus_sha256`'s subject at `scripts/agent_instruction.py:3503-3510`. False the moment step 5 projects them out, which is the amendment's entire content. The amended exit names two ADR edits, the placement sentence and the sixth-pass paragraph, and this clause is three paragraphs below both and outside either. Left standing it contradicts the same section, which is the shape S4-R1-05 named and `d0e411db` repaired one paragraph earlier. Remedy: strike "to any recorded offset" in the same commit, or replace it with the residual the amendment's own Why already states. | open |
| S4-R2-04 | low | .hexaemeron/runbook.md amendment 2026-09-04, scripts/agent_instruction.py:1475-1512, 1706-1712 | A site count in the amended step 5 exit that the tree does not bear out. It reads "PR #1100 attached that guidance to the two `EXECUTABLE_CHANGED` sites only". `_adapter_identity_detail` at `scripts/agent_instruction.py:1475` is passed as the third `refuse` argument at three sites, not two: the client executable at 1501 and the runtime executable at 1508 inside `_verify_profile_identity`, and the client executable again in the adapter call path at 1708. The substantive half of the claim is correct and was checked: every `WAI-E-ADAPTER.EXECUTABLE` refusal, at 1318, 1320, 1329, 1343, 1837, 1839, 1841 and 1843, and both `WAI-E-ADAPTER.UNAVAILABLE` refusals, at 1402 and 1462, take two arguments and carry no detail, so `refusal-detail-coverage` names a real hole and step 5's exit closes a real one. Only the count is wrong, and step 5's audit will count them. | open |
| S4-R1-01 | high | scripts/agent_instruction.py:3503-3510, .hexaemeron/study.md, .hexaemeron/runbook.md | Re-disposed against the two amendments and still open. The amendments schedule this finding; they do not close it. The mechanism half is untouched on the branch: the subject still carries `fixtures` whole, and `prove_span_shift --detail`, re-run this round, still reports `before-span` as `{"accepted": false, "exit": 2, "refusals": [{"code": "WAI-E-DIGEST.CORPUS", "node_path": "$.evidence.measurement_record"}]}` against `after-span` accepted at exit 0. The disclosure half is closed, at `d0e411db`, where ADR-075 names the before-span placement, its sixth pass and its refusal. The amended step 5 exit does entail the fix, in those words: "An edit before a reviewed span start, with the five mechanical passes and the re-derivation applied, leaves `agent_instruction.py check` at exit 0 with no refusal, exactly as the after-span placement already does." Its mechanism was checked rather than accepted, by enumerating the fixture record's eight fields against a before-span prepend: only `source.sha256`, the five `artifacts.*.sha256` and `source.start`/`source.end` move, the first six are already substituted by `digest_neutral_projection` through `_bound_digest_values`, and `id`, `root`, `binding_count`, `mutation_count`, `question_count`, `review`, `source.id`, `source.path` and `source.span_sha256` are all fixed under it; the subject's five other keys are the schema, the three counts and `risk_classes`, none of which moves. Three offset pairs, one `source` record per fixture, is correct. So projecting them out does make the corpus digest neutral to both out-of-span placements, and `in-span-edit-refusal` survives because `span_sha256` is never substituted and stays in the subject. One half of round 1's evidence is absent from the exit: #1098's first acceptance check says "bring `tests/test_agent_instruction.py` green", round 1 pinned both `check` at exit 2 and the module at 11 failures and 4 errors, and the amended exit names `check` alone. "exactly as the after-span placement already does" imports the module-green half by reference on one reading and not on another, so step 5's audit should establish it directly rather than infer it from the corpus digest. | open |
| S4-R1-02 | medium | .hexaemeron/reports/digest-neutral-corpus-span-shift-regression.json | Survives step 5 in a narrower form than the amendment allows. The amendment says S4-R1-02 and S4-R1-06 "stop describing a report that hides a refusal, because there is no refusal left to hide", which is true of the instance and not of the defect. Re-run this round: the report is byte-identical at `{"criterion":"span-shift-regression","unit":"count","value":2,"exit":0}` while `--detail` shows `before-span` refusing, so neither number moves when a placement refuses. After step 5 there is nothing to hide today, and the seven-key shape still cannot express a refusal, so the identical report would be written on a branch where a placement had regressed. Narrower than round 1 recorded in one further respect: `.hexaemeron/.gitignore` is `*`, so neither resolver report is tracked in git and no repository reader reaches either; they are run artefacts the design record names, which lowers the blast radius and does not change the shape. Open at the report shape, which is `design_probe.py`'s and Protasis's to widen. | open |
| S4-R1-03 | medium | .hexaemeron/design-evidence.json | The gate's verdict stops turning on the ambiguity; the ambiguity does not close. Checked rather than accepted: `prove_span_shift --detail` reports exactly two placements, `uncovered: []`, `value: 2`, `exit: 0`, so the coverage reading is 2 today and the green-count reading is 1. With `before-span` accepted at step 5 both readings give 2 against `comparator: at-least` and `threshold: 2`, so neither leaves the gate unmet and the harm that made this medium is gone. The criterion still holds exactly eight keys with no prose field, and `unit: count` still names no subject, so what is counted stays unfixed by anything a reader reaches and the next criterion of this shape inherits it. Open on the branch, where the green-count reading still gives 1. Not fixable here: the design record is controller-pinned and amending it needs `hexctl`, which this round is forbidden. | open |
| S4-R1-06 | low | .hexaemeron/reports/digest-neutral-corpus-offline-reconciliation-green.json | Survives in the same narrower form as S4-R1-02, one level quieter. `prove_offline` still exercises `AFTER_SPAN_PLACEMENT` alone and still writes `value: true` under `offline-reconciliation-green`; re-run this round at exit 0 with the report byte-identical. After step 5 the runbook's step 4 exit wording, "The offline report proves a bound-source edit reconciles with no model consulted", becomes true of both placements in fact while `offline` has still tested one, so the over-claim narrows from false-for-before-span to untested-for-before-span rather than closing. Neither amendment gives `prove_offline` a placement argument. | open |
| S3-R1-02 | medium | tests/fixtures/agent-instruction-v1/evidence/family-profiles.json, scripts/agent_instruction.py:1495-1544 | Carried from step 3 round 1 and unchanged at step 4 round 2, as expected. `acquisition_sha256` digests rendered text, so a cosmetic rendering difference and a genuine renderer or parser swap produce the same bytes-changed signal and the only response to either is a hand re-pin. Neither step 4's diff nor either amendment reaches an adapter or profile path, and the narrower fields the row names -- the identity of the process answering on 127.0.0.1:11434, and the model's config blob digest -- are still uncosted by any round. | open |
| S3-R2-05 | high | scripts/agent_instruction.py:2797-2836 | Carried from step 3 round 2, unchanged, and now the exposure the second model pair runs into. The recorded token counts are carried rather than recomputed, so `_measurement_material`'s comparison is tautological for `tokens` and no gate can see a hand-edited count; `WAI-E-MEASURE.NON_NEGATIVE_DELTA` does not fire on an edit that moves the count and every field derived from it. The 2026-09-04 study amendment retires assumption 3 and buys a second `measure` and a second `parity` run, and neither amendment recomputes a count, so the reissued records inherit the same tautology at twice the model cost. The amendment says so itself: "S3-R2-05 is untouched by this and stays open". `totals.delta_tokens` still reads `-165` on a fixture tree `git diff f91bbc6c..HEAD -- tests/fixtures/` shows byte-unchanged. | open |

Leads not pursued: whether step 5 should project the offsets out of `_corpus_sha256`'s subject by removing the two keys before canonicalising rather than by the byte substitution `digest_neutral_projection` uses -- the amended exit's "projects each fixture's `source.start` and `source.end` out of its digested subject" reads as removal and is the safe mechanism, while the neighbouring docstring argues at length for substitution by byte, and a byte substitution keyed on a decimal is unsound where a 64-hex literal is safe, because `18445` is a substring of `184450`; not pursued because it is Mason's implementation choice at step 5 and the exit already names the safe reading. Whether `WAI-E-REFERENCE.SPAN`, `WAI-E-DIGEST.SPAN` and `WAI-E-MANIFEST.BINDINGS` in `_validate_source_spans` at `scripts/agent_instruction.py:2259-2282` belong beside `WAI-E-DIGEST.SOURCE_SPAN` and `WAI-E-DIGEST.ARTIFACT` in the amendment's cost sentence -- the sentence is true as written and under-credits itself, because `governed_start <= start < end <= governed_end` and the per-binding digest and `model["bindings"]` equality are what make a moved offset onto a byte-identical window unreachable, which `WAI-E-DIGEST.SOURCE_SPAN` alone does not; named rather than raised because no claim in the amendment is false. Whether `prove_span_shift`'s own before-span placement, a 46-byte prepend re-deriving 18445 to 18491 and 22773 to 22819, should be reconciled with the 47-byte hand edit round 1 used, 18445 to 18492 and 22773 to 22820, which both amendments carry -- two different edits, both correct, and a reader meeting the two pairs may take one for a contradiction. Whether `S3-R2-04`'s row in this file, which names `docs/decisions/ADR-074-digest-neutral-measured-corpus.md`, should be reconciled with the renumbering `aa5035cc` performed and with the further renumbering S4-R2-01 now requires -- deliberately not touched for the second round running, because audit rounds are byte-pinned and a sweep is what that pinning refuses. Evidence for this round: the step diff `f91bbc6c..HEAD` is seven files, 707 insertions against 62 deletions, and the branch is byte-unchanged since round 1's fixes at `32b7d8e3` and the record commit `f8bac16f`. Both amendments were read in full: study `b55e90c5134860c6104bb0c36306b7f84e07c4955583b01652bec8a19eed2508` retiring assumption 3, runbook `a8c2b308a4462d62771987e2da73a241e5e2a916b32ce365f977e650acfa728e` replacing step 5's Goal, Exit, Files and Tests. Phylax exit 0 and Ephoros exit 0 over `scripts/prove_agent_instruction_reconciliation.py` and `tests/test_agent_instruction_corpus.py`, Hypomnema exit 0 over `docs`, `scripts` and `tests`, all three reporting `clean`; the Pashov suite is waived and did not run. `python3 tests/run_tests.py` reports 1143 of 1144 and exits 1 for the single reason S4-R2-01 gives, confirmed by name through a separate `unittest.defaultTestLoader.discover` run over the same tree at 1144 run, 1 failure, 0 errors. `agent_instruction.py check` exits 0 at 20 passed, 0 failed, 0 refused, 0 unknown over 3 fixtures. Both resolvers exit 0, `selftest` exits 0 at `value: 7`, and re-running all three with `--report` left `git status --porcelain` empty, so all three report files are byte-stable. Every row of `tests/promise_machine_coverage.json` was recomputed against the tree: 81 path-and-digest rows, 0 stale and 0 missing. No file under `tests/fixtures/agent-instruction-v1/` changed. `plugins/hexaemeron/skills/fiat/SKILL.md` at `809f4111662e3168c29e3fb65868fd88e086e5cd10762f95f5c0be6df314199c`, `plugins/horos/skills/horos/SKILL.md` at `8de17c0b3bf6c62d3161c1140b86114ea6e7551867acf7319fdba1121075e784` and `PROMISE_MACHINE.md` at `f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15` are byte-identical to the step branch. This round made no fixes commit and no source edit; only this record and its synopsis change. The bounded Sapheneia pass preserved the heading, the four schema fields, all twelve risk ids and dispositions, every named unchecked item including CI and the unspent model budget, all ten finding ids with severities, file and line locations, statuses and remedies, the four new findings and the six carried ones with their status unchanged, every refusal code with its node path, every offset, delta, digest, count, commit identity and timestamp, the exact `null` verdict with the reason it is null, the direct answer on whether the amendments close or schedule S4-R1-01, and the unpursued leads without changing any claim.
