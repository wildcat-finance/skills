## Step 1, round 1 -- 2026-09-18T05:43:01Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=not-applicable; ownership-source=not-applicable; adopted-step-exclusion=not-applicable; current-step-at-next=not-applicable; receipt-range-disjointness=not-applicable; gained-range-bound=not-applicable; unknown-relation=not-applicable; honest-extension-regression=not-applicable; state-mutation-on-refusal=not-applicable; diagnostic-overclaim=not-applicable; github-read-scope=not-applicable; run-branch-guard-regression=not-applicable; fake-git-rev-list=not-applicable; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=reviewed

Not checked: the security suite (waived: no Solidity in the step); Elenchus (no fixes commit); the contents of the 32 report copies beyond byte-identity with `.hexaemeron/design/reports/`; the version claim of any remote head whose commit is absent locally (none: all 369 heads from `git ls-remote --heads origin` resolve to local objects).

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | docs/fiat-carried-step-commits/study.md | The receipted study names copy homes that do not exist: section 3 item 7 and the section 9 Resolvers row say the four resolvers are copied under `docs/design/`, and section 12 names `docs/fiat-1480-carried-step-commits-study.md`, `docs/fiat-1480-carried-step-commits-runbook.md` and `docs/design/`. Runbook Step 1, its Files list and the section 12 amendment's `design-bridge` record path put every copy under `docs/fiat-carried-step-commits/`, which is where the step committed them, so the runbook Disciplines claim that the copies are "the homes study section 12 names" holds only for the draft record. The docs copy must stay byte-identical to the controller-pinned `.hexaemeron/study.md` (SHA-256 `c9ced5a303422ae19471e00d38b8d33b9b13a20561cdc72dd75070b30ca97250`), so this cannot be fixed on the stacked branch. | open: study amendment owed |

Leads not pursued: Step diff `f0fa0c6632bd5fbef7f35e731646c32741ef282a..5da736c405421b59fe539f42ce9516f31d23a034`, 49 files; `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and `plugins/hexaemeron/skills/fiat/SKILL.md` unchanged, so no digest cascade is owed and the governed range 18784-29736 is untouched. Phylax, Ephoros (all 49 changed paths) and Hypomnema (the 41 changed `docs/` paths) each exited 0; `hypomnema.py --study docs/fiat-carried-step-commits/study.md --design-evidence docs/fiat-carried-step-commits/design-evidence.json --repo-root .` exited 0; the step's seven Exit commands exited 0 (`run_checks.py --scope root --scope hexaemeron --format json` outcome `green`, 15 checks passed including `root-suite`, `hexaemeron-suite`, `hexaemeron-forge-test`; Imprimatur 100.0/100 on all three documents; both Protasis modes clean; the Hypomnema walk and the Phylax design walk clean). The directive's `repo_suite` `python3 -m unittest discover -s tests` ran 2051 tests in 241.816 s, OK. `scripts/plugin_release.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --head HEAD` exited 0 (`hexaemeron: 1.6.51 -> 1.6.52`); the six surfaces read `1.6.52`; every local ref, every remote head and every worktree other than this run claims at most `1.6.51`. `.hexaemeron/study.md` and `.hexaemeron/design-evidence.json` match the brief's digests (`c9ced5a3…`, `06f801ea…`), selection `gained-range-ownership` by `unique-frontier`, eight selection cells `pass`, the conformance cell `product-refuses-specimen-stack` pending and blocking `step:4`, none due at this boundary. `cmp` shows `study.md`, `runbook.md`, `design-evidence.json`, the 4 resolver copies and all 32 report copies byte-identical to their `.hexaemeron/` sources. Re-running `docs/fiat-carried-step-commits/design/demonstrate_admission.py --hexctl plugins/hexaemeron/skills/fiat/scripts/hexctl.py` reproduced `admission-at-starting-ref.txt` byte for byte, verdict line `admitted`. The four resolver copies open only a caller-named `--out` with mode `xb` after refusing an existing path or a controller path, use `subprocess.run` with argument lists and no shell; `demonstrate_admission.py` writes only inside its temporary fixture. The draft record has heading `# Decision: Refuse receipted commits carried into a lower step branch`, sections Status, Context, Decision, Alternatives, Consequences in order, status `Accepted, 2026-09-18`, no ADR number, the three decisions of study section 12 and the three rejected constructions of study section 4 as alternatives; the names it cites (`refuse_rewritten_stack` at hexctl.py:18380, `done_merge_step` at 18466, `verify_local_range` at 21724, `_native_relation_git` at 3976, `expected_head_sha=None` at 18539, `ADR-021`) resolve; no inherited record in `docs/decisions/` changed. `horos.py check .` reports the boundary matches the tree and a fresh `scan . --census` equals `.horos/census.json` row for row (35 rows, 4,217 files, 123,724,906 bytes). Step commit `5da736c4` carries a valid signature from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`. The draft Status line says publication in the draft home "is due in Step 1" while the file already sits there; read as a statement of this step's deliverable, not pursued. No further Step 1 lead warrants another round.

## Step 1, round 2 -- 2026-09-18T06:27:51Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=not-applicable; ownership-source=not-applicable; adopted-step-exclusion=not-applicable; current-step-at-next=not-applicable; receipt-range-disjointness=not-applicable; gained-range-bound=not-applicable; unknown-relation=not-applicable; honest-extension-regression=not-applicable; state-mutation-on-refusal=not-applicable; diagnostic-overclaim=not-applicable; github-read-scope=not-applicable; run-branch-guard-regression=not-applicable; fake-git-rev-list=not-applicable; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=reviewed

Not checked: the security suite (waived: no Solidity in the step); the contents of the 32 report copies beyond byte-identity with `.hexaemeron/design/reports/`; remote heads were not re-listed this round (round 1 listed all 369).

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: S1-R1-01 settled: the receipted study gained a second dated amendment (2026-09-18, digest `03ddb733b661d1c447eade961ca58553902035818711881f3f5180d7896ae5f7`) superseding the copy homes in section 3 item 7, the section 9 Resolvers row and section 12 with `docs/fiat-carried-step-commits/design/`, `docs/fiat-carried-step-commits/study.md` and `docs/fiat-carried-step-commits/runbook.md`; fixes commit `3b5a386e55999faf2bf0c3c9b581bcf5add434af` refreshes `docs/fiat-carried-step-commits/study.md` to the receipted bytes (SHA-256 `b4c12a9c4629fe531d7ed3e3868c6031e4b74788597f818a7987a607b12163c9`) and `.horos/census.json`; `cmp` shows the study, runbook, `design-evidence.json`, 4 resolver copies and 32 report copies byte-identical to their `.hexaemeron/` sources; the runbook (`dfa6a8b2…`) and design record (`06f801ea…`) are unchanged. Elenchus classified `3b5a386e` `unguarded` with detail "the commit changed no test files" (`elenchus.py --ref 3b5a386e --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .hexaemeron/elenchus-step-1.json`); a prose-only fix has no failing test to guard, so the value is expected and stands as recorded. The same runner executed directly on `3b5a386e` wrote `.hexaemeron/elenchus-step-1.json` (`elenchus.unittest.v1`, complete, 3456 run, 0 failures, 0 errors, 5 skipped, 1 expected failure). On the fixed tree (diff `f0fa0c66..3b5a386e`, 51 paths, 41 under `docs/`): Phylax and Ephoros over the 51 paths and Hypomnema over the 41 docs paths each exited 0; `hypomnema.py --study docs/fiat-carried-step-commits/study.md --design-evidence docs/fiat-carried-step-commits/design-evidence.json --repo-root .` exited 0; `protasis.py --study` and `protasis.py` on the runbook clean with both amendments parsed; Imprimatur 100.0/100, 0 defects on the study copy, runbook copy and draft record; the Hypomnema walk over `docs/fiat-carried-step-commits docs/decisions/drafts` and the Phylax walk over `docs/fiat-carried-step-commits/design` clean; `run_checks.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --scope root --scope hexaemeron --format json` outcome `green`, 15 of 15 checks passed; `python3 -m unittest discover -s tests` ran 2051 tests in 242.189 s, OK; `plugin_release.py` exited 0 (`hexaemeron: 1.6.51 -> 1.6.52`); `horos.py check .` reports the boundary matches the tree; `hexctl.py` and `SKILL.md` are still unchanged against `f0fa0c66`, so no digest cascade is owed and the governed range 18784-29736 is untouched. Risk-register ids keep their round 1 dispositions: no controller code changed in this step. `3b5a386e` carries a valid signature from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`. No further Step 1 lead warrants another round.

## Step 2, round 1 -- 2026-09-18T09:18:21Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=reviewed; ownership-source=reviewed; adopted-step-exclusion=reviewed; current-step-at-next=reviewed; receipt-range-disjointness=not-applicable; gained-range-bound=reviewed; unknown-relation=reviewed; honest-extension-regression=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-read-scope=reviewed; run-branch-guard-regression=reviewed; fake-git-rev-list=reviewed; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=not-applicable

Not checked: the security suite (waived: no Solidity in the step); Elenchus (no fixes commit); `done merge-step` and `status` (unchanged this step; Step 3 owns `receipt-range-disjointness`); `plugins/hexaemeron/skills/fiat/SKILL.md` (not in the step diff, so the governed range was not re-measured); the resolver report `.hexaemeron/design/reports/gained-range-ownership-added-native-processes-per-next.json` was not re-run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | `refuse_carried_step_commits` reads every unmerged step's tip through `remote_branch_tip` (`git ls-remote`), re-reading the waiting tips `refuse_rewritten_stack` read a moment earlier: on the module's healthy three-step graph fixture at current step 1 it made 3 `remote_branch_tip` calls (`run-step-1`, `run-step-2`, `run-step-3`) and 0 `rev-list` children, against 2 calls for `refuse_rewritten_stack`. The added cost of a healthy `next` is one `ls-remote` per unmerged step, where study section 10, the runbook Step 2 disciplines and the draft record's Consequences record 1 added process (the current-step tip read), a figure measured on the resolver's reference procedure. Refusal behaviour is unaffected. | accepted: the runbook Step 2 deliverable prescribes reading each unmerged tip and forbids changing `refuse_rewritten_stack`, so sharing the reads needs a runbook amendment or a Step 3 decision |
| S2-R1-02 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | `_native_gained_range` catches the `SystemExit` that `_native_relation_git` raises after `bounded_tool` (or `bounded_run` on start, timeout and output-cap) has already printed its own `hexctl: error:` line, so an unknown-range refusal writes two error lines to stderr: observed on the missing-object graph case, `hexctl: error: gained range <R>..<T> could not be enumerated` then the authoritative `a step branch has an unknown gained range: ...` naming the pair. Neither line names a cause; `_native_relation_commit` in `refuse_rewritten_stack` shares the pattern. | accepted: the runbook binds the read to `_native_relation_git`; a single-line refusal needs `bounded_probe` directly, a design change outside this step |

Leads not pursued: (1) a push receipt with no `verified_commits` and an abbreviated `head_commit` (`verified_commits` arrived in `cc7e81f7`, 2026-08-21; `head_commit` resolution in `6aae31c3`, 2026-08-29) refuses in `receipt_owned_commits` as "not a full SHA" where `refuse_rewritten_stack` resolves it; study section 9 requires full SHAs for ownership and no live run with such a receipt is known, so not pursued. (2) An adopted step's commits are outside the ownership set in every tip's gained range, not only inside its `reachable_from` branch as the register row is worded; the draft record decision 1 and the runbook deliverable specify exclusion from the set, and the carrier step's own commits still refuse (`test_an_adopted_early_merge_is_admitted`); not pursued. (3) Neither guard ancestry-checks the current step's tip at `next`: a rewritten current-step tip whose gained range holds no owned commit and at most 500 commits is admitted there, and `done merge-step`'s `verify_local_range` over `pr_base..remote_head` owns that range (Step 3 intersects it). (4) A cherry-picked copy under a new SHA passes, as the draft record's Not covered states. Evidence for this round, step diff `11a9c3b8df33ec1f34d871f09521455c1c8d1199..9ddd57f8e225d37936827e4abba1baeeea427263` (15 files, +679/-18): Phylax and Ephoros over the 15 paths and Hypomnema over `docs/promise-machine/obligation-gates/integration-projection.md` each exited 0; `run_checks.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --scope root --scope hexaemeron --format json` exit 0, outcome `green`, 15 of 15 passed; `python3 -m unittest discover -s tests` ran 2051 tests in 247.242 s, OK, exit 0; `plugins.hexaemeron.tests.test_carried_step_commits` 18 tests OK, exit 0 (11 graph, 5 CLI, 2 harness); `test_hexctl.RewrittenStackRefusal` 6 OK including `test_the_step_being_merged_is_never_queried`; `test_stack_topology` and `test_early_step_merge` 55 OK; the module copied alone onto a detached worktree at `11a9c3b8` fails 16 of 18; `plugin_release.py` exit 0 (`hexaemeron: 1.6.52 -> 1.6.53`); `promise_machine.py check` exit 0; `horos.py check .` exit 0, boundary matches the tree; `hexctl.py` SHA-256 `b88c1d5198a90e739328d4151d89693febbc1ca2b2a5839baf491226ef0c6fb7` is the pin in `tests/promise_machine_coverage.json` and `evaluation-run.json` changed only `tree_sha256` (`a4a5099c…`); `git ls-remote --heads origin` listed 371 heads, every one at its local tracking SHA, `main` at `f0fa0c66` holding 1.6.51, and no ref other than the step branch claims 1.6.53; `9ddd57f8` and `11a9c3b8` carry good signatures from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`; `design-evidence.json` (`06f801ea…`), study (`b4c12a9c…`) and runbook (`dfa6a8b2…`) match the brief; the added `hexctl.py` lines contain no `gh`, GitHub or `pulls` token; `FAKE_GIT_REV_LIST` is read by the harness and the new module only; the 5 CLI cases assert `state.json` and `ledger.jsonl` byte-identical across each refusal.

## Step 2, round 2 -- 2026-09-18T09:55:22Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=reviewed; ownership-source=reviewed; adopted-step-exclusion=reviewed; current-step-at-next=reviewed; receipt-range-disjointness=not-applicable; gained-range-bound=reviewed; unknown-relation=reviewed; honest-extension-regression=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-read-scope=reviewed; run-branch-guard-regression=reviewed; fake-git-rev-list=reviewed; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=not-applicable

Not checked: the security suite (waived: no Solidity in the step); `done merge-step` and `status` (unchanged; Step 3 owns `receipt-range-disjointness`); `plugins/hexaemeron/skills/fiat/SKILL.md` (not in the step diff); the resolver report `.hexaemeron/design/reports/gained-range-ownership-added-native-processes-per-next.json` was not re-run; the runner in any form other than the runbook's declared command.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: S2-R1-02 fixed in `f66fd857f6568cf703c59c43d107ae2746c7c14b`: `_native_gained_range` now reads through `bounded_probe` with `_native_git_executable()`, `--no-replace-objects`, `_native_relation_environment()`, `output_max=GIT_OUTPUT_MAX` and `timeout=GIT_TIMEOUT`, returning `None` on a start failure, timeout, output cap or non-zero status with nothing printed, so an unknown range yields exactly one `hexctl: error:` line naming the pair; `test_a_missing_object_refuses_as_unknown_naming_the_pair` now asserts `message.count("hexctl: error:") == 1` and fails on `9ddd57f8` (`AssertionError: 2 != 1`) with only the module copied onto a detached worktree; the docstring names `bounded_probe`, `_native_git_executable`, `_native_relation_environment`, `_native_relation_git` and `die`, all of which exist. S2-R1-01 stays `accepted` as recorded in round 1, pending Fiat's Step 3 runbook amendment under which the carry guard shares `refuse_rewritten_stack`'s tip reads. Elenchus on `f66fd857` with the step's exact contract (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-2.json`) returned `inconclusive` twice, once beside `run_checks` and once alone: the runner exited 3 with status `scheduler-error`, all 12 worker launches failed `[Errno 1] Operation not permitted` under Elenchus's hard runner boundary, which permits no child processes, and the report was incomplete; one `digest_rebinds` row records `tests/promise_machine_coverage.json` re-pinning `hexctl.py` from `b88c1d51…` to `b0d1e24e…` with `target_overlaid=false`, the expected shape for a fix that edits the pinned file. The verdict is recorded as returned; the guard evidence is the hand counterfactual above, and the runner contract is a matter for the runbook, not this round. Evidence on the fixed tree, diff `11a9c3b8df33ec1f34d871f09521455c1c8d1199..f66fd857` (17 paths): Phylax and Ephoros over the 17 paths and Hypomnema over the changed Markdown each exited 0; `run_checks.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --scope root --scope hexaemeron --format json` exit 0, outcome `green`, 15 of 15 passed; `python3 -m unittest discover -s tests` ran 2051 tests in 234.842 s, OK, exit 0; `test_carried_step_commits` 18 tests OK, exit 0; `test_hexctl.RewrittenStackRefusal` 6 OK; `plugin_release.py` exit 0 (`hexaemeron: 1.6.52 -> 1.6.53`, no further bump); `promise_machine.py check` exit 0; `tests.test_obligation_gate_demonstration tests.test_promise_machine_contract tests.test_unique_identifiers` 284 OK; `horos.py check .` exit 0 at commit time; cascade: `hexctl.py` SHA-256 `b0d1e24e9deaff03e296eadd7e9e8037da4ad27fee26b74c6b9834a42d9713d2` pinned in `tests/promise_machine_coverage.json`, `evaluation-run.json` re-tallied with the recorded model and date `2026-08-31` changing only `tree_sha256` (`a4a5099c…` to `8d43b51a…`), `integration-projection.json` and `.md` re-pinned (`ff8d2c59…`, `38dd78f8…`, byte counts unchanged); `f66fd857` carries a good signature from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Laurence, greenlight exit 0. The round-1 leads (abbreviated head-only legacy receipt, adopted-step exclusion scope, current-step tip ancestry at `next`, cherry-picked copies) stand unchanged.

## Step 3, round 1 -- 2026-09-18T19:10:34Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=reviewed; ownership-source=reviewed; adopted-step-exclusion=reviewed; current-step-at-next=reviewed; receipt-range-disjointness=reviewed; gained-range-bound=reviewed; unknown-relation=reviewed; honest-extension-regression=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-read-scope=reviewed; run-branch-guard-regression=reviewed; fake-git-rev-list=reviewed; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=not-applicable

Not checked: the security suite (waived: no Solidity in the step); `plugins/hexaemeron/skills/fiat/SKILL.md` (not in the step diff, so the governed range was not re-measured); the version claim of 2 of 372 remote heads (`codex/1135-signature-proof`, `kronos/state`) whose commits are absent locally and whose `plugins/hexaemeron/.claude-plugin/plugin.json` the GitHub contents API did not return; the resolver report `.hexaemeron/design/reports/gained-range-ownership-added-native-processes-per-next.json` was not re-run; the runner in any form other than the runbook's declared command.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | `carried_step_observation` set `entry["tip"]` only after an abbreviated recorded head had resolved, so in report mode a legacy `head_commit` that resolves to no native commit joined `unknown` with `tip` `None`, and `status` printed `recorded head 0000000; its tip could not be read` for a tip it had read (observed through the module fixture with step 2's `head_commit` set to `0000000`; stderr carried the true cause, `step 2 recorded push head does not resolve to a native commit`). The refuse path was unaffected: without `report` the `SystemExit` re-raises. | fixed in `8eddde452b361ff5c6e8e7a23ed718b6d455ab2f`: the tip is set as soon as it is in hand, the line reads `recorded head 0000000 and observed tip <tip>; the range 0000000..<tip> is unknown`; `test_status_prints_an_unresolvable_recorded_head_as_an_unknown_range` fails on `d3228f8b` with the module alone and passes on the fix |
| S3-R1-02 | info | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | `done_merge_step` calls `refuse_carried_step_commits` with `waiting_only=True`, so the guard it runs before `inspect_pull_request` covers the waiting steps only; a carry into the step being merged is refused by the `pr_base..remote_head` intersection, which runs after `inspect_pull_request` and `remote_branch_tip` have read GitHub and origin, after `verify_local_range` has verified the range's local signatures, and before `effective_push`, `verify_github_commits`, any `state.json` field or ledger event. The amended Exit's sentence "a carry refuses before any GitHub evidence is recorded" therefore holds for the waiting steps by construction and for the step being merged in the sense that nothing is recorded, not in the sense that nothing is read. | accepted: study section 10 and the amended Disciplines bind `done merge-step` to no added process, and a native gained-range read of the current step would add one; it would also refuse `TestMergedState.test_a_merge_time_repair_refreshes_the_attribution`, `TestPublicationBindings.test_merge_repairs_signed_post_push_head` and `test_merge_time_repair_refuses_invalid_github_verification` as unknown, because their synthetic tips (`c`*40, `7`*40) name no native object and `test_hexctl.py` is not edited; the fall-through alternative (an unknown current-step range admitted to the repaired-range check) contradicts study section 11's unknown rule; register rows `receipt-range-disjointness` (intersected before evidence is recorded and before any state write) and `github-read-scope` (`done merge-step` keeps exactly the reads it has, in the same order) hold as worded; `test_done_merge_step_adds_no_tip_read` pins the process count and the four merge-step CLI cases pin state and ledger bytes |

Leads not pursued: (1) In `status` report mode an unreadable tip still lets `remote_branch_tip`'s own `hexctl: error:` line reach stderr while stdout prints the unknown `CARRY:` line and the exit is 0 (observed with `FAKE_GIT_MODE=remote-absent`: three error lines, three `CARRY:` lines, exit 0); the `STACK:` path has the same shape (`unreceipted_run_branch_movement` catches the `SystemExit` `die` raised after printing), study section 8 question 2 is answered on stdout, and a silent report-mode read needs a reader that does not `die`, outside this step. (2) The conformance report `.hexaemeron/design/reports/gained-range-ownership-product-refuses-specimen-stack.json` (SHA-256 `54cd9c320e78c114b19695f96798fb867e76fc1c0d6c2697d279b8c3a6a7fbda`, `value` `true`) was written once by `conform_carry.py` when the module held 30 cases and 3 were added afterwards; the resolver binds no commit and records only that the fixed module ran green with at least one test, `run_checks`' `hexaemeron-suite` check ran the 33-case module green on `d3228f8b` and the module is 34 OK on `8eddde45`, so the boolean holds for both trees and the once-written report is not rewritten. (3) At `done merge-step` a carry into the step being merged whose carried commits fail `verify_local_commit` refuses with the signature message before the ownership intersection is reached; both refuse before mutation, and the study binds the intersection to the range `verify_local_range` already enumerated. (4) The Step 2 round-1 leads stand: a legacy receipt with no `verified_commits` and an abbreviated `head_commit` refuses in `receipt_owned_commits` as "not a full SHA"; an adopted step's commits are outside the ownership set in every gained range; the current step's tip is not ancestry-checked at `next`; a cherry-picked copy under a new SHA passes. (5) Elenchus on `8eddde45` with the step's exact contract (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-3.json`) returned `inconclusive`: the runner exited 3 with status `scheduler-error`, all 12 worker launches failed under Elenchus's hard runner boundary and the report was incomplete; one `digest_rebinds` row records `tests/promise_machine_coverage.json` re-pinning `hexctl.py` from `71ded999…` to `f6df43f1…` with `target_overlaid=false`, the expected shape for a fix that edits the pinned file; no `.hexaemeron/elenchus-step-3.json` was written. The verdict is recorded as returned; the guard evidence is the hand counterfactual in S3-R1-01. Evidence for this round, step diff `b51e0e2dc32f067b74fe21425e9254f3889bf1ff..d3228f8b964dd1e946169d2053d6ccdc9c233bfc` (16 files, +564/-78) and fixed tree `b51e0e2d..8eddde45` (16 files, +589/-78): Phylax and Ephoros over the 16 changed paths and Hypomnema over `docs/fiat-carried-step-commits/runbook.md` and `docs/promise-machine/obligation-gates/integration-projection.md` each exited 0 on both trees; `protasis.py docs/fiat-carried-step-commits/runbook.md` clean with the amendment parsed, `protasis.py --study` clean, `design_evidence.py .hexaemeron/design-evidence.json` clean with `product-refuses-specimen-stack` pending for all four candidates; `run_checks.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --scope root --scope hexaemeron --format json` exit 0, outcome `green`, 15 of 15 passed on `d3228f8b` and exit 0, outcome `green`, 15 of 15 passed on `8eddde45` (a run against the uncommitted fix was red on `dead-code-suppressions-check` alone, which refuses a dirty checkout); `python3 -m unittest discover -s tests` on `8eddde45` ran 2051 tests in 298.265 s, OK, exit 0; `test_carried_step_commits` 34 OK on `8eddde45` (11 graph, 6 `next`, 7 `done merge-step`, 5 `status`, 3 shared-tip-map, 2 harness); the module copied alone onto a detached worktree at `b51e0e2d` fails 12 of 33 (10 failures, 2 errors), the 3 passing by nature being the equal-tip receipt, the healthy `status` and the merge-step tip-read count; `test_hexctl.RewrittenStackRefusal`, `TestMergedState` and `TestPublicationBindings` 52 OK including `test_the_step_being_merged_is_never_queried` and the three merge-time repair cases; `test_stack_topology`, `test_early_step_merge`, `test_step_branch_extensions` and `test_push_receipt_identity` 74 OK; `tests.test_obligation_gate_demonstration tests.test_promise_machine_contract tests.test_unique_identifiers` 284 OK; `plugin_release.py --base <step 2 branch> --head HEAD` exit 0 (`hexaemeron: 1.6.53 -> 1.6.54`, no further bump); `promise_machine.py check` exit 0; `horos.py check .` exit 0 on both trees; `dead_code.py suppressions --check` exit 0 on `8eddde45`; cascade: `hexctl.py` SHA-256 `f6df43f134951cd7b506a8263e2800d8b7911cb5791dbe0e68b6ca6036918033` pinned in `tests/promise_machine_coverage.json`, `evaluation-run.json` re-tallied with the recorded model and date `2026-08-31` changing only `tree_sha256` (`82b1b765…` to `9ca6d77e…`, 55 passed, 0 failed, verified), `integration-projection.json` and `.md` re-pinned (`6ee79689…`, `d10d3c9f…`, byte counts unchanged), `.horos/census.json` refreshed; `git ls-remote --heads origin` listed 372 heads, 370 resolve locally, the highest claim among them 1.6.53 on the step 2 branch, `main` at `f0fa0c66` holding 1.6.51, and no ref other than the step 3 branch and its `--audit` branch claims 1.6.54; `d3228f8b`, `b51e0e2d` and `8eddde45` carry good signatures from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, `8eddde45` authored by Shoggoth with greenlight exit 0; `design-evidence.json` (`06f801ea…`), study (`b4c12a9c…`) and runbook (`77b8639c…`, one amendment) match the brief, and `docs/fiat-carried-step-commits/runbook.md` and the docs report copy are byte-identical to their `.hexaemeron/` sources; the added `hexctl.py` lines contain no `gh`, REST or GraphQL call (the one `GitHub` token is a comment); `hexctl_harness.py` and `test_hexctl.py` are unchanged; the docstrings name `refuse_carried_step_commits`, `refuse_rewritten_stack`, `remote_branch_tip`, `_native_gained_range`, `_native_relation_commit`, `die`, `verify_local_range`, `effective_push`, `inspect_pull_request` and `docs/decisions/drafts/refuse-receipted-commits-carried-into-a-lower-step-branch.md`, all of which exist; the seven merge-step and `next` CLI cases assert `state.json` and `ledger.jsonl` byte-identical across each refusal and `test_next_reads_each_unmerged_tip_once_across_both_guards` and `test_done_merge_step_adds_no_tip_read` assert one `ls-remote` per unmerged branch.

## Step 4, round 1 -- 2026-09-19T19:28:10Z

Audit schema: fiat-audit-round/v2

Covered: carry-direction=reviewed; ownership-source=reviewed; adopted-step-exclusion=reviewed; current-step-at-next=reviewed; receipt-range-disjointness=reviewed; gained-range-bound=reviewed; unknown-relation=reviewed; honest-extension-regression=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-read-scope=reviewed; run-branch-guard-regression=reviewed; fake-git-rev-list=reviewed; digest-cascade=reviewed; version-collision=reviewed; governed-skill-range=reviewed

Not checked: the security suite (waived: the step ships no Solidity); the version claim of 1 of 399 remote heads (`codex/issue-1489-store-inputs`, whose commit is absent locally) and of the 2 heads carrying no `plugins/hexaemeron/.claude-plugin/plugin.json`; the design resolver and conformance scripts, which were not re-run, their committed copies compared byte for byte instead; `tests/promise_machine_id_history.json`, unchanged at `83fe042237463fa7582c0eaee75af385990498f268ebf410c229995db81e4d36` because no promise id moved; any model, tokenizer, parity or measurement process, none of which ran; the runner in any form other than the runbook's declared command; the tree of this record's own commit, appended after every command below ran.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | low | docs/fiat-carried-step-commits/runbook.md | The 2026-09-19 amendment moved the receipted `.hexaemeron/runbook.md` to SHA-256 `5765ffb2ec5420c5e3c804c2fbe159126ca023dfbc5f177b37fb6217499cf528` and left the committed copy at `77b8639c20e45f51e4a7cbba5bb0a3c435927cd37e939be43bdb17446f15952b`. At `4e916c94` `cmp docs/fiat-carried-step-commits/runbook.md .hexaemeron/runbook.md` exits 1 (`EOF on docs/fiat-carried-step-commits/runbook.md`), the whole difference being the amendment's 80 appended lines. The amendment's replacement Files list names that copy. | fixed in `155250b784617eba8a8c50e24bbb58b7225349b0`: the copy is now `5765ffb2ec5420c5e3c804c2fbe159126ca023dfbc5f177b37fb6217499cf528`, `cmp` exits 0, and `.horos/census.json` is re-tallied for the 5415 added Markdown bytes. `docs/fiat-carried-step-commits/study.md` is untouched. No repository test names `fiat-carried-step-commits`, so the guard evidence is the hand counterfactual: `cmp` exits 1 at `4e916c94` and 0 at `155250b7`. |
| S4-R1-02 | low | plugins/hexaemeron/skills/fiat/EVOLUTION.md | `hypomnema.py` over the changed Markdown of `24124655..HEAD` exits 1 on three H009 rows at lines 84, 85 and 86, on the stable decision slugs `accept-any-validly-signed-authorship`, `refuse-location-dependent-pointers-before-a-receipt-pins-a-digest` and `build-the-outer-checkpoint-archive-natively`, none written by this step. The slugs are written here without their stable-reference prefix because that exact form is itself a live reference the record lint resolves, and this record must not add a dangling one. The whole-directory walk reports the same three inside a set of 7 at both `24124655` and `4e916c94`: 6 H009, adding `DEMONSTRATION.md:19`, `references/checkpoint-archive.md:8` repeating the checkpoint-archive slug, and `scripts/hexctl.py` at `:6044` then `:6045`, plus 1 H006 at `scripts/hexctl.py:128` citing `ADR-014`. The sets are identical in files and slugs; the only difference is that one line shift, caused by the compatibility-set line this step adds. | accepted: clearing them means authoring or editing inherited decision records, which the study's boundaries forbid. The 2026-09-19 amendment scopes this step's record lint to the two prose files it writes, where `hypomnema.py` exits 0, and the repository's own `lint-hypomnema` check inside `run_checks` is green. |
| S4-R1-03 | info | plugins/hexaemeron/skills/fiat/SKILL.md | The added sentence reads "A waiting branch that gained a commit another step's push receipt owns refuses before any write", and the paragraph it joins uses "waiting" in contrast with "the current step". At `next`, `refuse_carried_step_commits` is called without `waiting_only`, so the current step's branch is carry-checked as well and the merge-step directive is withheld for a carried current step, which is register row `current-step-at-next`. The sentence states nothing false and asserts no exclusivity, and `references/push-discipline.md` carries the complete rule as "For each unmerged step whose observed tip has left its recorded head". | accepted: the operator home states the full scope, the Exit allows one or two sentences here, and a wording repair moves the SKILL.md SHA-256 and with it the agent-instruction manifest, `compact.wai`, `model.json`, `source-spans.json`, the `promise_machine_coverage.json` rows, `evaluation-run.json`'s tree digest and the integration projection, for a completeness gap with no behaviour behind it. |

Leads not pursued: (1) The SKILL.md clause "settles that without a GitHub read" describes the gained-range read; at `next` the guard still costs one `ls-remote` for the current step's tip when `refuse_rewritten_stack` has not already read it. Register row `github-read-scope` is "no GitHub REST or GraphQL call is added", which holds: `hexctl.py` gains one line in this step, `"fiat-v6.66.1"` in `CHECKPOINT_COMPATIBLE_CONTROLLER_VERSIONS`, and no call of either kind. (2) `_native_gained_range` also returns `None` on a `UnicodeDecodeError` and on an output line that is not a full SHA; the push-discipline paragraph lists the five unknown causes the register row `gained-range-bound` names and not these two, so prose and register agree with each other rather than with the function's full set. (3) The generation row is deliberately `fiat-v6.66.1` although `origin/main` already publishes `fiat-v6.66.1` and `fiat-v6.67.1`: the close gate compares against the previous row in the same file, `fiat-v6.65.1`, and the integration sync renumbers this run's row through the `version-relations` block, so it is not raised here and main's ledger is not read into this branch. (4) Step 3's leads stand: the report-mode stderr line, the once-written conformance report, the signature refusal ahead of the ownership intersection at `done merge-step`, the legacy abbreviated head-only receipt, the adopted-step exclusion scope, and a cherry-picked copy under a new SHA. Evidence for this round, step diff `2412465574726fbc84dbd3303366038d7ec936ea..4e916c94e6e2568ce60d562108cc520092eee81e` (22 files, +81/-41) and fixed tree `24124655..155250b7` (23 files, +161/-41): governed range, SKILL.md bytes 18784 to 29736 hash `73b8c169309b3ec0881642dbf57dd435cce8fe324faf7562ca22f6b79b29d45e` at both commits, equal to the agent-instruction manifest's `span_sha256`; the frontmatter `version` line is 19 bytes at offset 368 on both (`6.65.1` to `6.66.1`); the added paragraph starts at byte 54513 and the file grows 112610 to 112965. Lints on the fixed tree over its 23 changed paths: Phylax 0, Ephoros 0, Hypomnema 1 over the changed Markdown (S4-R1-02) and 0 over the amendment's two prose files; Imprimatur 0 on those two files, score 100.0/100 and 0 defects each. `run_checks.py --base fiat/1480-refuse-a-later-step-s-commits-carried-into --scope root --scope hexaemeron --format json` exit 0, outcome `green`, 15 of 15 passed at `4e916c94` and again at `155250b7`. `python3 -m unittest discover -s tests` ran 2051 tests in 327.572 s, OK, exit 0; `tests.test_evolution_contract tests.test_agent_instruction tests.test_version_propagation` 369 OK; `plugins.hexaemeron.tests.test_carried_step_commits` 34 tests in 155.855 s, OK; `plugin_release.py --base <step 3 branch> --head HEAD` exit 0, `hexaemeron: 1.6.54 -> 1.6.60`; `promise_machine.py check` exit 0, `clean: 18 plugin(s), 18 copy/copies`; `horos.py check .` exit 0, boundary matches the tree. Cascade re-derived by hand, each equal to its pin: SKILL.md `8f14af30c284040911987e37e08deb118aded5f845807ac71c4f1679a4d5b4fc`, EVOLUTION.md `0c889d6a2add5e3556738d2604fd82c5f936855b3e98d554bd25749ff3489a93`, `hexctl.py` `dc35d35fd3d69f4eebaf66ad3baa4495bed6d7b545138917f89111efd965ed5a`, `compact.wai` `0cbfdb1ad15001f3b504fe8cb4517d8a6d53bc6782536ba87957c4ace86b31b2`, `model.json` `b263215028a19e08347624b339ebc35295d043db551350cdc2083757a4ad6234`, `source-spans.json` `6be46219f4885493a9ecdae86b8f62840d888cb189081ba580e9a6186bf98d77`, manifest `0aa1160be3e77af8404106cc68e0f5936ce1f16b797339dccb3f711022622319`, `promise_machine_coverage.json` `70abcef9a6034dd000223025f6415c04c1278fe5d986216bf5cf02b0468f0dc9`, `evaluation-run.json` `dc3658c5b0b4005e4f18cefba84f80937f5c4a0d875029f2e95406e5fb45b6b3` carrying `tree_sha256` `f42976a9129d0012b82a0e05a38e14a7833263b320ab67e4a653d09df26d5aae` with its recorded model, date `2026-08-31` and 55 passed, 0 failed unchanged. Version scan: `git ls-remote --heads origin` listed 399 heads, 398 resolve locally, the highest claim among them 1.6.59 on `main`, `fiat/1755-repair-checkpoint-key-marker-false-positive` and `repair/gate-adapter-replay`; across 561 local refs 1.6.60 is claimed by this step branch alone. EVOLUTION.md gains exactly 2 changed lines; its one new row sits on the `generation` axis, keeps frontier revision `delegated-task-identity` and `a54452aef0e415d7d17a548751178de0804d22af4829255b3c5d8bfe289581f1`, cites skills#1480, `docs/decisions/drafts/refuse-receipted-commits-carried-into-a-lower-step-branch.md` (present in the tree) and the committed study and runbook, and states the held target unchanged. Prose against implementation: `GIT_PATHS_MAX` is 500 and `_native_gained_range` asks `rev-list --max-count=501`; `receipt_owned_commits` skips a receipt recording `early_merge` and falls back to `head_commit` when `verified_commits` is absent; `done_merge_step` calls `refuse_carried_step_commits(..., waiting_only=True)` before `inspect_pull_request` and intersects the repaired `pr_base..remote_head` before `effective_push`, `verify_github_commits` and the `integrate` write; `status` calls `carried_step_observation(..., report=True)` and prints one `CARRY:` line per carried and per unknown entry. `docs/fiat-carried-step-commits/study.md` equals `.hexaemeron/study.md` at `b4c12a9c4629fe531d7ed3e3868c6031e4b74788597f818a7987a607b12163c9`; `design-evidence.json` `06f801ea6d139d7554d01170d523eaf6fd40311d9d256906e9b2debd72aaefb1` matches the brief with selected candidate `gained-range-ownership`; the committed `design` and `reports` directories are byte-identical to `.hexaemeron/design` and `.hexaemeron/design/reports`, and `gained-range-ownership-product-refuses-specimen-stack.json` records `value` `true` at exit 0. Elenchus with the step's exact contract (`python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-4.json`, no stale report present) returned `unguarded`, detail `the commit changed no test files`, `tests` empty; no report was written and no runner started. `155250b7` carries a good signature from `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Laurence, greenlight exit 0.
