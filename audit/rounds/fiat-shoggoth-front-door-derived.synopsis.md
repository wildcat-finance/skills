Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-shoggoth-front-door-derived.md | source_sha256=4a45295962d2e0f48da333edcfc03a51737af6f5e20d2fe79f7d1367fd36bc49 | h2_count=16
## Step 1, round 1 -- 2026-09-04T10:45:17Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; dokimasia-source-class=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=reviewed; empty-selection-as-pass=not-applicable; schema-declared-not-checked=not-applicable; contract-refusal-drift=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. The selected Step 1 candidate creates no public card, public checker, member-status copy, portrait, or PDF, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `real-data-nonclaim-loss`, and `visual-surface-drift` are not applicable. It creates no demonstration ledger, status, source capture, runner, schema, refusal clause, report destination, or queue, and changes no `EVOLUTION.md`, so `demo-class-inflation`, `dokimasia-source-class`, `source-drift`, `frontier-lane-collision`, `external-data-egress`, `subprocess-execution`, `empty-selection-as-pass`, `schema-declared-not-checked`, `contract-refusal-drift`, `partial-demo-output`, `demo-skip-as-pass`, `stale-record-provenance`, and `queue-duplication` are not applicable. It publishes no generated runtime, so `generated-copy-drift` is not applicable.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | `docs/design/build_shoggoth_front_door_design_evidence.py` | `Path.exists()` treated a dangling `design-evidence.json` symlink as absent, then `Path.write_text()` followed it. The generator exited zero and created `escaped.json` outside `--out`; the check-then-write gap also admitted a target swap before truncation. | fixed in `7b16284f95c63e723718a0708008db1243fa7480`: keep the output roots open, inspect entries without following links, and create every file descriptor-relative with `O_EXCL` and `O_NOFOLLOW`; the dangling-symlink regression now refuses without creating the outside file or reports directory |<br>| S1-R1-02 | low | `scripts/shoggoth_topology.py` | `_open_flags()` silently folded an absent or zero `O_NONBLOCK` into the regular-leaf flags. A fixed manifest leaf that is a FIFO could therefore block before the regular-file check, despite the bounded-read contract. | fixed in `7b16284f95c63e723718a0708008db1243fa7480`: an unavailable non-blocking flag now returns `unsupported-platform`, and the regression proves the former silent path refuses |<br><br>Leads not pursued: No further in-scope product defect remained after the complete 38-path diff review. A platform that exposes the named open flags but not descriptor-relative operations, and concurrent replacement of an already-open output or plugin-tree entry, were not driven; this step claims fail-closed no-follow confinement and current-tree agreement, not an atomic snapshot of a live-mutating repository. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, hash, timeout qualification, verdict, status, and negative-space boundary; no Horos reading exclusion was applied. The implementation diff from run tip `ff47f3070c8dce05c767b6c0dad65234c56870de` to `b4c50a1f219ddd4cebd4333ee4cb9cd89be37edf` is 38 files, 4,702 insertions, and 7 deletions at patch SHA-256 `5989acfc5b995424750d1d554d368fc816b7b6d9f4b5d4796765924e383927e8`; all changed paths had no Git attributes, so the configured LFS filter applied to none. The fixed-tree root suite passed 1,148 of 1,148 tests in 531.242 seconds; the topology module passed 32 of 32. Phylax, Ephoros, ordinary Hypomnema, and study-bridge Hypomnema each exited 0. The first full 28-check wrapper passed 27 checks but terminated the still-active Hexaemeron shard at its 1,800.468-second limit under system load, recorded at SHA-256 `089732faf8de10cdeae7a4a553fb249e732941b84cc0f676dd265110cf001a8f`; the reduced direct rerun passed all 2,204 Hexaemeron tests in 609.371 seconds with zero failures, errors, or skips and exact once-only accounting. Elenchus over the signed fix commit executed 1,148 parent tests and returned `guarded` from exactly two assertion failures, zero errors, and zero skips. The generator reproduced the record and all 24 reports byte for byte; Protasis design-lock and Horos both returned clean. The fix has a good Laurence signature, Shoggoth author, Laurence committer, and exactly one parsed co-author and origin trailer.
## Step 1, round 2 -- 2026-09-04T10:58:19Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; dokimasia-source-class=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=reviewed; empty-selection-as-pass=not-applicable; schema-declared-not-checked=not-applicable; contract-refusal-drift=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. The selected Step 1 candidate creates no public card, public checker, member-status copy, portrait, or PDF, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `real-data-nonclaim-loss`, and `visual-surface-drift` are not applicable. It creates no demonstration ledger, status, source capture, runner, schema, refusal clause, report destination, or queue, and changes no `EVOLUTION.md`, so `demo-class-inflation`, `dokimasia-source-class`, `source-drift`, `frontier-lane-collision`, `external-data-egress`, `subprocess-execution`, `empty-selection-as-pass`, `schema-declared-not-checked`, `contract-refusal-drift`, `partial-demo-output`, `demo-skip-as-pass`, `stale-record-provenance`, and `queue-duplication` are not applicable. It publishes no generated runtime, so `generated-copy-drift` is not applicable.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R2-01 | low | `.horos/boundary.json` | Round 1 added the audit record and synopsis after its Horos check, leaving `files_walked` at 2,500 for a 2,502-file tree. `horos.py check .` and the two existing root currency guards failed on `.horos/boundary.json#counts`. | fixed in `73de3d6ae5de7f64e2490934a12471bb80f1aaff`: the deterministic scan changed only `files_walked` to 2,502; both guards, Horos, and all 1,148 root tests then passed |<br><br>Leads not pursued: No further in-scope defect remained after the complete fixed-tree reread. The round-1 boundary fixes still pass their two hostile cases, the topology module passes 32 of 32, and the generator reproduces the record and 24 reports byte for byte with 36 results over four candidates and nine criteria. The pre-fix root suite ran 1,148 tests in 100.290 seconds and failed only the two existing Horos currency guards; the fixed suite passed 1,148 of 1,148 in 85.478 seconds. Elenchus returned `unguarded` because the one-field data fix changes no test file; separately, those two pre-existing guards were observed red before the fix and green after it. Phylax, Ephoros, ordinary Hypomnema, and study-bridge Hypomnema each exited 0. No Horos reading exclusion was applied. The reviewed tree from run tip `ff47f3070c8dce05c767b6c0dad65234c56870de` through the signed fix is 40 files, 4,845 insertions, and 7 deletions at patch SHA-256 `fa3b3b9726ee4b9e753a949300e8f9c4e8851f9336fdf958f19ab8870cba05d3`; all reviewed paths have no Git attributes, so no configured filter applies. A platform exposing the required flags without descriptor-relative operations and concurrent replacement after an entry is open remain unexercised; the implementation claims fail-closed confinement and current-tree agreement, not an atomic live-tree snapshot. Audit filter `sapheneia:sapheneia` retained the finding, severity, risk dispositions, counts, durations, hashes, verdict, status, negative evidence, and residual limits. Brevitas reported B010 for the required two round headings and B011 for the two-row and one-row finding tables; adding a heading or finding would alter protected host structure or evidence, so no Brevitas-clean claim is made.
## Step 1, round 3 -- 2026-09-04T11:04:26Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=not-applicable; dokimasia-source-class=not-applicable; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=reviewed; empty-selection-as-pass=not-applicable; schema-declared-not-checked=not-applicable; contract-refusal-drift=not-applicable; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=reviewed; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. The selected Step 1 candidate creates no public card, public checker, member-status copy, portrait, or PDF, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `real-data-nonclaim-loss`, and `visual-surface-drift` are not applicable. It creates no demonstration ledger, status, source capture, runner, schema, refusal clause, report destination, or queue, and changes no `EVOLUTION.md`, so `demo-class-inflation`, `dokimasia-source-class`, `source-drift`, `frontier-lane-collision`, `external-data-egress`, `subprocess-execution`, `empty-selection-as-pass`, `schema-declared-not-checked`, `contract-refusal-drift`, `partial-demo-output`, `demo-skip-as-pass`, `stale-record-provenance`, and `queue-duplication` are not applicable. It publishes no generated runtime, so `generated-copy-drift` is not applicable.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: No further in-scope defect remained after the final complete fixed-tree and prior-round review. The root suite passed 1,148 of 1,148 tests in 81.415 seconds, the topology module passed 32 of 32, and the generator reproduced the record and all 24 reports byte for byte with 36 results over four candidates and nine criteria. Phylax, Ephoros, ordinary Hypomnema, study-bridge Hypomnema, Protasis design-lock, Horos, and `git diff --check` each exited 0. No Horos reading exclusion was applied. The reviewed tree from run tip `ff47f3070c8dce05c767b6c0dad65234c56870de` through the signed fix `73de3d6ae5de7f64e2490934a12471bb80f1aaff` is 40 files, 4,845 insertions, and 7 deletions at patch SHA-256 `fa3b3b9726ee4b9e753a949300e8f9c4e8851f9336fdf958f19ab8870cba05d3`; all reviewed paths have no Git attributes, so no configured filter applies. A platform exposing the required flags without descriptor-relative operations and concurrent replacement after an entry is open remain unexercised; the implementation claims fail-closed confinement and current-tree agreement, not an atomic live-tree snapshot. Audit filter `sapheneia:sapheneia` preserved every risk disposition, count, duration, hash, null verdict, negative result, and residual limit. Brevitas reports B011 because the required round tables carry two, one, and zero genuine findings; inventing rows would alter protected evidence, so no Brevitas-clean claim is made.
## Step 2, round 1 -- 2026-09-05T01:56:37Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=reviewed; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=not-applicable; empty-selection-as-pass=not-applicable; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=reviewed; queue-duplication=reviewed; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. This step executes no demonstration command and writes no public claim, so `subprocess-execution`, `external-data-egress`, `report-path-escape`, `empty-selection-as-pass`, `partial-demo-output`, `demo-skip-as-pass`, `claim-without-demo`, `real-data-nonclaim-loss`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, and `visual-surface-drift` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. The 28-check wrapper `python3 scripts/run_checks.py --full --jobs 6` named by the step exit was not run in this round; its report path lies under `.hexaemeron/`, and the root suite it wraps ran directly instead. Whether any declared source's bytes still match its digest is established by `check` only for the 27 committed records at this tree, not for a future tree.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | `plugins/hexaemeron/skills/DEMONSTRATIONS.md` | All 28 new documents (27 `DEMONSTRATION.md` History rows and the policy's closing sentence) cited the decision record by the numbered path `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md`. The number must change at the run's sync-run (S2-R1-03), and `decision_assignments.py` renames the record and rewrites only its heading, so all 28 references would dangle and fail Hypomnema H001 on the next walk. | fixed in `c939a2b80127bf0a134a8c35c65d35e9a9a19946`: every document cites the stable identity `adr/govern-real-data-demonstrations-separately`, which H009 resolves now and after assignment; a guard asserts the identity in all 28 documents and refuses any `ADR-NNN` reference; the ordinary Hypomnema walk over the 28 documents and `docs/decisions` exited 0 |<br>| S2-R1-02 | medium | `tests/test_demonstrations.py` | `test_every_evolution_ledger_is_byte_identical_to_the_entry_tree` compared every live `EVOLUTION.md` with commit `7b16284f95c63e723718a0708008db1243fa7480` through `git show`, so once merged the root suite fails on the next legitimate behaviour-ledger advance anywhere in the repository. | fixed in `c939a2b80127bf0a134a8c35c65d35e9a9a19946`: the test digests every ledger, runs `check` and `frontier --root . --lane demo --dry-run`, and asserts byte identity across both; a guard refuses any 40-hex commit id in the module; the step exit's `git diff --exit-code -- 'plugins/*/skills/**/EVOLUTION.md'` still proves entry-tree identity and exited 0 |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` (2026-09-04T09:09:08Z) while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06` (2026-09-04T07:58:52Z). | accepted: step 2's Files clause does not permit `docs/decisions`, the study pins that path in a `hypomnema-design-bridge/v1` block, and the merge-time decision-record assignment (Hypomnema `decision_assignments.py`, on main since #888) is the allocation authority at the run's sync-run, so the record is not renumbered in this round; the record is a numbered final rather than a `docs/decisions/drafts/` draft, and the planner refuses a new numbered record in the product, so the sync-run must move or renumber it explicitly |<br><br>Leads not pursued: The step diff from `fiat/shoggoth-front-door-derived-step-1-commit-the-design-boundary-and-d` to `b2f0b3da7c89c0a542ab7e8e08cb5cf360c02b4b` is 43 files, 4,281 insertions, and 89 deletions at patch SHA-256 `6d37786aa961ce112617df703c1ae8cf9dc0ea7c5e4230bca7648fb4bff239e4`; through the fix it is 43 files, 4,312 insertions, and 89 deletions at `bdcc890ea3ab7238d71900ae0277f774579cfe4500369a3baf1cc523426aa5d7`. Phylax, Ephoros, and Hypomnema each exited 0 over all 43 changed paths before the fix and over the 29 fixed paths after it. `demonstrations.py check` reported 27 records (4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent`, 0 `not-applicable`) and exited 0; `frontier --lane demo --dry-run` selected `hexaemeron:vulgate` at status `absent` and exited 0; `tests.test_demonstrations` passed 52 of 52 before the fix and 54 of 54 after it; `tests.test_shoggoth_topology` passed 32 of 32; Horos and `git diff --check` exited 0. The pre-fix root suite passed 1,199 of 1,200 in 104.307 seconds and the fixed-tree suite 1,201 of 1,202 in 172.010 seconds, each failing only the S2-R1-03 test. Elenchus over the signed fix under `python3 tests/run_tests.py --elenchus-report {report}` returned `guarded` from a parent assertion failure in `tests/test_demonstrations.py`. The refusal-count test compares the policy catalogue with the `REFUSALS` declaration; that all 40 declared codes are raised by the checker was confirmed by grep in this round, not by a test. The record grammar carries no field naming the producing skill's behaviour version, so `stale-record-provenance` has nothing mechanical to check here. Records nested 3,000, 20,000, and 100,000 containers deep were refused `D004` on Python 3.14.6; older interpreters were not driven. The Anamnesis `real-data` record declares `plugins/anamnesis/specimens/pilot/policy.json` as its one `audit` source while the three preserved audit-round sources under `specimens/pilot/sources/` that the policy admits are undeclared; the status is not inflated, and step 3's source verification is where the declaration should widen. Structure-only checking (`root` of `None`) skips `_safe_relpath`'s `:` and `.git` refusals that the live check applies. Dokimasia records `real-data` over three preserved production-run records with the absent primary inputs named in its non-claim, as the runbook requires. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, hash, commit, timestamp, verdict, status, negative result, and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 2 -- 2026-09-05T02:20:19Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=reviewed; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=not-applicable; empty-selection-as-pass=not-applicable; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=reviewed; queue-duplication=reviewed; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. This step executes no demonstration command and writes no public claim, so `subprocess-execution`, `external-data-egress`, `report-path-escape`, `empty-selection-as-pass`, `partial-demo-output`, `demo-skip-as-pass`, `claim-without-demo`, `real-data-nonclaim-loss`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, and `visual-surface-drift` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. Whether any declared source's bytes still match its digest is established by `check` only for the 27 committed records at this tree, not for a future tree. Interpreters other than Python 3.14.6 were not driven.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R2-01 | low | `.horos/census.json` | Round 1's fixes commit `c939a2b80127bf0a134a8c35c65d35e9a9a19946` grew `tests/test_demonstrations.py` and its record commit `109940386b107981f60930b462ebe0e8b85a34ca` added the audit log and synopsis, but each refreshed only `.horos/boundary.json` and `.horos/candidates.json`, so the committed census described the tree at `b2f0b3da7c89c0a542ab7e8e08cb5cf360c02b4b`: 2,548 files and 93,775,034 bytes against a fresh scan's 2,550 files and 93,817,521 bytes, with the `.md` row at 885 files instead of 887. `horos.py check .` and the root guard in `tests/test_boundary_currency.py` re-derive the boundary alone, so every gate stayed green. | fixed in `70d16f37ba30e2a027bae5c2e1cb52bc8366b879`: `scan . --census --write` regenerated the census, and `HorosCensusCurrencyTests` in `tests/test_demonstrations.py` compares the committed census with a fresh scan; the guard was observed red on the stale census and green after the refresh, and `horos.py check .`, both boundary guards, and `git diff --check` exited 0 |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from round 1 for the same reason; step 2's Files clause does not permit `docs/decisions`, the study pins that path in a `hypomnema-design-bridge/v1` block, and the run's sync-run must move or renumber the record explicitly, so it is not renumbered in this round |<br><br>Leads not pursued: The reviewed diff from `fiat/shoggoth-front-door-derived-step-1-commit-the-design-boundary-and-d` (`7b16284f95c63e723718a0708008db1243fa7480`) to `109940386b107981f60930b462ebe0e8b85a34ca` is 46 files, 4,391 insertions, and 90 deletions at patch SHA-256 `b7e8d40241f6b1eb22b4487960a4f39cc76023d55b9b5fe4932419ab9f1b1c7f`; through the fix it is 46 files, 4,424 insertions, and 90 deletions at `2a75d8f2ab575dcdae1932adbb5f67d72a5f113b6481a94299733936829761cf`. The round-1 fixes were re-read for regression and none was found: all 28 documents cite `adr/govern-real-data-demonstrations-separately` and the guard refuses any `ADR-NNN` reference, the byte-identity test digests every `EVOLUTION.md` before and after `check` and `frontier --root . --lane demo --dry-run`, its guard refuses any 40-hex commit id, and the step exit's `git diff --exit-code -- 'plugins/*/skills/**/EVOLUTION.md'` and `git diff --stat` against the entry tree both report no `EVOLUTION.md` change. Phylax and Ephoros exited 0 over all 46 changed paths, Hypomnema exited 0 over the 39 changed Markdown paths plus `docs/decisions`, and all three exited 0 over the two fixed paths. `demonstrations.py check` reported 27 records (4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent`, 0 `not-applicable`) and exited 0; `frontier --lane demo --dry-run` selected `hexaemeron:vulgate` at status `absent` and exited 0; `tests.test_demonstrations` passed 54 of 54 before the fix and 55 of 55 after it; `tests.test_shoggoth_topology` passed 32 of 32; Horos and `git diff --check` exited 0 before and after the fix. The pre-fix root suite passed 1,201 of 1,202 in 99.314 seconds and the fixed-tree suite 1,202 of 1,203 in 118.034 seconds, each failing only the S2-R1-03 test. `python3 scripts/run_checks.py --full --jobs 6` over the pre-fix tree ran 29 checks and returned red because `root-suite` failed on that same test; the other 28 passed, including `hexaemeron-suite` in 683.4 seconds with no adapter timeout, and the report at `.hexaemeron/run-checks-step-2-round-2.json` has SHA-256 `2691bbbd25fedc6b107aeddd255a91b0bf4c021151bf208aeb43e4630e6a90d8`; the wrapper was not rerun over the fixed tree, whose root suite, lints, Horos, and demonstration suite ran directly instead. Elenchus over the signed fix under `python3 tests/run_tests.py --elenchus-report {report}` executed 1,203 parent tests with 2 assertion failures, 0 errors, and 0 skips and returned `guarded`. The census guard's natural long-term home is `tests/test_boundary_currency.py`, which lies outside this step's Files clause; moving it there is a lead for the sync-run or a later step. Records nested 3,000 and 100,000 containers deep were refused `D004`; a `True` timeout, block, or expected exit was refused `D012`, `D022`, or `D032`; an uppercase digest was refused `D023`. An observation string carrying an embedded newline is accepted by `D014`, although the policy asks for one statement per line; an argv word beginning with a dash is accepted, which is step 3's execution boundary to judge; structure-only checking (`root` of `None`) still skips `_safe_relpath`'s `:` and `.git` refusals that the live check applies. The Anamnesis `real-data` record still declares `plugins/anamnesis/specimens/pilot/policy.json` as its one `audit` source while the preserved audit-round sources under `specimens/pilot/sources/` stay undeclared; step 3's source verification is where the declaration should widen. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, hash, commit, timestamp, verdict, status, negative result, and unpursued lead; no Horos reading exclusion was applied.
## Step 2, round 3 -- 2026-09-05T02:38:45Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=reviewed; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=not-applicable; subprocess-execution=not-applicable; report-path-escape=not-applicable; empty-selection-as-pass=not-applicable; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=not-applicable; stale-record-provenance=reviewed; queue-duplication=reviewed; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step contains no Solidity. This step executes no demonstration command and writes no public claim, so `subprocess-execution`, `external-data-egress`, `report-path-escape`, `empty-selection-as-pass`, `partial-demo-output`, `demo-skip-as-pass`, `claim-without-demo`, `real-data-nonclaim-loss`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, and `visual-surface-drift` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. Whether any declared source's bytes still match its digest is established by `check` only for the 27 committed records at this tree, not for a future tree. Interpreters other than Python 3.14.6 were not driven. The Anamnesis demo command was read, not run.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from rounds 1 and 2 for the same reason; step 2's Files clause does not permit `docs/decisions`, the study pins that path in a `hypomnema-design-bridge/v1` block, and the run's sync-run must move or renumber the record explicitly, so it is not renumbered in this round |<br><br>Leads not pursued: The reviewed diff from `fiat/shoggoth-front-door-derived-step-1-commit-the-design-boundary-and-d` (`7b16284f95c63e723718a0708008db1243fa7480`) to `4e74737f8491c05e5615632e031d2a84ec4ca63c` is 46 files, 4,442 insertions, and 90 deletions at patch SHA-256 `35fa6943f395745d86610b168c1560fddbd908231743fc310201ed2a7547e657`; no changed path has a Git attribute, so no configured filter applies. The round-2 fix `70d16f37ba30e2a027bae5c2e1cb52bc8366b879` was re-read for regression and none was found: the regenerated census matches a fresh scan under the boundary's `tracked` universe, so the wrapper report written under the ignored `.hexaemeron/` cannot move it; `HorosCensusCurrencyTests` passes at head and puts only `plugins/horos/skills/horos/scripts`, which holds `horos.py` alone, on `sys.path`; and the record commit `4e74737f8491c05e5615632e031d2a84ec4ca63c` refreshed candidates and census together with `files_walked` unchanged at 2,542. Phylax and Ephoros exited 0 over all 46 changed paths, and Hypomnema exited 0 over the 39 changed Markdown paths plus `docs/decisions`. `demonstrations.py check` reported 27 records (4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent`, 0 `not-applicable`) and exited 0; `frontier --lane demo --dry-run` selected `hexaemeron:vulgate` at status `absent` and exited 0; `tests.test_demonstrations` passed 55 of 55; `tests.test_shoggoth_topology` passed 32 of 32; `git diff --exit-code -- 'plugins/*/skills/**/EVOLUTION.md'` exited 0 and `git diff --stat` against the entry tree reports no `EVOLUTION.md` change; Horos and `git diff --check` exited 0. The root suite passed 1,202 of 1,203 in 118.091 seconds, failing only the S2-R1-03 test. `python3 scripts/run_checks.py --full --jobs 6` over this tree ran 29 checks and returned red because `root-suite` failed on that same test in 122.236 seconds; the other 28 passed, including `hexaemeron-suite` in 756.6 seconds with no adapter timeout, and the report at `.hexaemeron/run-checks-step-2-round-3.json` has SHA-256 `34517fca7da8e73fcd8c2d3314acaa511b3f78f4ac316e3e96ea22d78996f981`. No fix was made, so this round has no Elenchus verdict. The four leads round 2 left open were each decided on evidence. An observation string carrying an embedded newline or an escape byte is accepted by `D014`, confirmed by probe; no live record carries one, `D014`'s catalogue clause and code agree, and the surface where such text could mislead is the step 3 report renderer, so the refusal belongs there or in a joint `D014` clause-and-code change on step 3's terms. An argv word beginning with a dash is accepted; the live records use option words such as `--specimen`, no live `argv[0]` begins with a dash, and which executables step 3 admits is its execution boundary. Structure-only checking (`root` of `None`, reached only through `check_record(verify_bytes=False)` from tests) accepts `.git/config` and `a:b/x.json` where the live check refuses both with `D025`, confirmed by probe; no shipped command reaches that path, so it is not a defect of this step, and a step 3 caller must not treat that mode as path-safe. The Anamnesis `real-data` record declares `plugins/anamnesis/specimens/pilot/policy.json` as its one `audit` source; that policy pins the SHA-256 and byte count of the three preserved audit-round sources under `specimens/pilot/sources/`, `admit` and `_admitted_texts` refuse a drifted source (`A120`), and `cmd_demo` refuses a build that disagrees with the committed release (`A153`), so drift in any undeclared material input, including the undeclared `curation-policy.json`, fails the recorded `expect_exit` of 0 rather than passing silently; widening the declaration remains step 3's source-verification work, where the report would name the drifted leaf. The census guard's natural long-term home is still `tests/test_boundary_currency.py`, outside this step's Files clause. In this Warden's judgement no open lead is worth another round: each is a step 3 execution or reporting boundary, lies outside this step's Files clause, or is a no-defect consistency note, and the only open row is the accepted S2-R1-03 item the sync-run owns. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, hash, commit, timestamp, null verdict, status, negative result, and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 1 -- 2026-09-05T09:00:31Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. The socket denial is a Python site hook, so a child that reaches the kernel's socket call through `ctypes` or another extension is outside anything the runner can observe: a probe obtained a live descriptor with `CTYPES_SOCKET_FD 3` while `network_attempt` stayed false, and that gap is now stated in `DEMONSTRATIONS.md` rather than refused. No run establishes that a child made no network call; it establishes only that no denied Python socket call went unrecorded. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration was executed once per tree rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation taken under concurrent load on one machine, never a speed claim. Whether a declared source's bytes still match its digest is established for the 27 records at these two trees only. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here; the round establishes only that each command produced the observations its own ledger declares. The report path was probed against traversal, an existing target, symlinked final and parent components and a dangling symlink, but not against a writer racing `os.link` itself.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | high | `scripts/demonstrations.py` | The step's Boundaries clause says argv arrays run "without a shell under the pinned interpreter", but nothing constrained `argv[0]`. Only `python3` was rewritten to `sys.executable`; any other word went to `execvp` through `PATH`, and `preflight_record`'s program check ran only when `argv[0] == "python3"`. A record declaring `["sh", "-c", "echo SHELL_RAN"]` ran a real shell, printed `SHELL_RAN`, and the record verified. A non-Python child never loads the site hook, so `external-data-egress` and `subprocess-execution` were both open through a declared argv. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S3-R1-02 | high | `scripts/demonstrations.py` | `python3 -S`, `-E` and `-I` leave the child outside the socket denial: `-S` and `-I` skip `site`, so `sitecustomize` never runs, and `-E` and `-I` ignore `PYTHONPATH`, so the hook directory is never on the path. Each of the three built a real `socket.socket` (`SOCKET_OK socket`, not `_DeniedSocket`) with `network_attempt` false, and `getaddrinfo` resolved a hostname under `-S`. `preflight_record`'s `not argv[1].startswith("-")` clause was what skipped the program check for exactly this form, and no clause refused the options. The report still recorded `"enforcement": "python-site-hook"`. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S3-R1-03 | medium | `scripts/demonstrations.py` | The marker recording a denied socket sat in a directory the child can write, at a path derivable from the `PYTHONPATH` the child is given. A probe child tripped the hook, unlinked the marker and was recorded with `network_attempt` false, so the refusal the hook had already earned was erased by the process it was meant to catch. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S3-R1-04 | medium | `scripts/demonstrations.py` | `resolve_report_target` proved containment against the path as it stood, then `publish_report` travelled that path again by name after the run, which takes seconds to minutes. Replacing a parent directory with a symlink in that window published the report outside the declared output root: a probe resolved a target under the root, swapped the parent, and `publish_report` returned a digest with the object standing in the outside directory. `mkdir`, `os.open` and `os.link` all re-resolved the pathname; only the final component was protected, by `O_NOFOLLOW` and `O_EXCL`. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S3-R1-05 | medium | `scripts/demonstrations.py` | The process group was torn down only on a timeout or an output overflow. A command that exited 0 after forking left the grandchild running: a probe child printed and exited at once, `execute_command` returned after 6,025 ms because the reader threads were still blocked on the pipe the grandchild held, and the grandchild was alive after the run ended and the work root was removed. Two consequences: the step's process-group-teardown boundary did not hold on its ordinary path, and the duration the report records for that command was the grandchild's lifetime rather than the command's, which is the number `metron` reads. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S3-R1-06 | medium | `plugins/anamnesis/skills/anamnesis/DEMONSTRATION.md` | The Exit clause's "verifies every declared source digest before execution" is true and does not reach the code that runs. No public-set record declares its command program as a source: `plugins/anamnesis/skills/anamnesis/scripts/anamnesis.py`, `plugins/lazarus/examples/goldfinch-v1/demo.py`, `plugins/alexandria/examples/credit-history-v0/demo.py` and `plugins/dokimasia/scripts/dokimasia.py` appear in no `sources` array, and `preflight_record` proves the program exists without digesting it. A report therefore records `evidence: verified` for every input while the program that produced the result is unverified, which is `source-drift` over the executable half of the record. Step 2 round 3 left this open as "widening the declaration remains step 3's source-verification work". | open: not fixed in this round because closing it changes what all four ledgers declare and therefore what the published report asserts, which is a contract change that should be audited on its own rather than ride with six execution-boundary fixes; carried to round 2 |<br>| S3-R1-07 | low | `scripts/demonstrations.py` | The step's ephoros discipline requires the runner to emit `demonstration.selected`, `started`, `verified` and `refused` under one correlation id. A ledger, schema or topology refusal reached during a `run` raised `DemonstrationError` or `TopologyError`, which `command_run` did not catch, so the correlation id was minted and no event was emitted at all: a probe run against a malformed root exited 2 with an empty event stream and one line of stderr prose. | fixed in `d2cbca5b393860b72853f04896872d383e20566d` |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 for the same reason; `docs/decisions` lies outside step 3's Files clause, the study pins that path in a `hypomnema-design-bridge/v1` block, and the run's sync-run must move or renumber the record explicitly, so it is not renumbered in this round |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `7295cf3e1b342426de1217c736b46a7d7120c130` is 16 files, 1,945 insertions and 31 deletions across three signed commits. Phylax, Ephoros and Hypomnema each exited 0 over the changed paths on both the audited tree and the fixed tree, Hypomnema with `docs/decisions` in scope. On the audited tree `demonstrations.py check --root .` reported 27 records (4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent`, 0 `not-applicable`) and exited 0; `tests.test_demonstrations` passed 83 of 83; Anamnesis passed 194, Lazarus 625, Alexandria 471 and Dokimasia 264; `run --public-set` verified 4 of 4 at 5,325 ms aggregate against the 600,000 ms ceiling, slowest 92 ms, 4,194 ms, 916 ms and 123 ms, with the Lazarus chain anchor recorded as `recorded` and every file source as `verified`; Horos, `git diff --check` and `git diff --exit-code -- 'plugins/*/skills/**/EVOLUTION.md'` exited 0. The root suite passed 1,230 of 1,231 in 142.887 seconds, failing only the S2-R1-03 test, and `run_checks.py --full --jobs 6` ran 29 checks and returned red on `root-suite` alone in 156.7 seconds with no adapter timeout, its report at `.hexaemeron/run-checks-step-3-round-1.json` carrying SHA-256 `e3a7ff88bd730b9034cc37ab46723d86137124c99e18084aa5fc0bcf99d3a372`. The same `run_checks.py --full --jobs 6` over the fixed tree at `d2cbca5b393860b72853f04896872d383e20566d` again ran 29 checks and returned red on `root-suite` alone in 180.1 seconds, its report at `.hexaemeron/run-checks-step-3-round-1-fixed.json` carrying SHA-256 `2cc8129d2c6b893709231d76b22a8a81f7c7b666520966555fc3e006f93d4acf`; `hexaemeron-suite` passed in both runs, at 1,063.9 and 1,264.9 seconds, with no adapter timeout. On the fixed tree the root suite passes 1,238 of 1,239 in 157.103 seconds, failing only the S2-R1-03 test, `tests.test_demonstrations` passes 91 of 91, and the public set verifies 4 of 4 at 5,762 ms aggregate, slowest 103 ms, 4,540 ms, 977 ms and 142 ms. The Elenchus comparison used the source-bound runner `python3 tests/run_tests.py --elenchus-report {report}` in `unittest-json-v1` to the fresh report `.elenchus/front-door-derived-counts-step-3.json`; its first run returned `inconclusive` because three guards raised on the parent rather than asserting, the guards were rewritten to assert, and the rerun returned `guarded` on a parent assertion failure. The refusal catalogue still holds 54 conditions in both the policy and the checker; `D072`, `D074` and `D081` gained their new conditions in the same commit as the code, and no new code was introduced. Six controls were probed and held: shell metacharacters, `$(id)`, backticks, a pipe and a glob were passed literally with no file created; argv words beginning with a dash reached the program as data; the child environment was exactly the allowlist plus `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, `PYTHONIOENCODING` and the macOS runtime's `__CF_USER_TEXT_ENCODING`, with `GITHUB_TOKEN`, `GIT_DIR`, `AWS_SECRET_ACCESS_KEY` and `LAZARUS_RPC_URL` absent; a child writing 2,621,440 bytes was truncated at exactly 1,048,576 and refused; a child sleeping past a 2,000 ms budget was killed at 2,016 ms with its forked grandchild, which did not survive; and a child that swallowed the socket exception and exited 0 was still refused. Six report-path probes were refused with `D080`: a `..` traversal, an existing target, the output root itself, a symlinked final component pointing outside, a symlinked parent directory, and a dangling symlink. Seven leads were seen and left. Two of them are properties of this round's own fixes, recorded so the next round reads them as known rather than new. The `-S`, `-E` and `-I` refusal scans the whole of `argv[1:]`, so a program argument that happens to equal one of those three words is refused as an interpreter option; the over-refusal fails closed and no live record carries such an argument. The unconditional `os.killpg(proc.pid, SIGKILL)` now runs after `proc.wait()` has reaped the direct child, so the pid is free at that moment; a recycled pid would have to make itself a process-group leader before the signal to be reachable at all, and the call already swallows `ProcessLookupError`. A relative `--report` value is joined to the process's working directory while containment is proved against `--output-root`, so a caller invoking the runner from outside the output root gets a `D080` refusal rather than a surprise write; the behaviour fails closed and no live invocation depends on it. `{{work}}` expands to `{` plus the work path plus `}` because the token is matched as an unanchored substring; the expansion still lands inside the private `0700` work root, so nothing escapes and the policy's "only substitution" clause holds. `HOME` is passed to the child, so a credential in `~/.netrc`, `~/.gitconfig` or `~/.aws` is reachable through the filesystem even though no credential environment key is copied; the child has ordinary filesystem access in any case, and confining that needs the kernel sandbox this step declares it is not. `children_max_rss_bytes` reads `RUSAGE_CHILDREN`, which is cumulative over every child of the run, so the per-command field is a running maximum rather than that command's own peak; it is an observation and no threshold reads it. A `-c` body is not digest-bound either, but no public-set record uses `-c` and the four use file programs, so that exposure is the one S3-R1-06 already names. In this Warden's judgement another round is worth running: six fixes landed on the widest-surface step in the run, S3-R1-06 is open by decision rather than by judgement, and the next round audits the fixed tree, where a regression introduced by a fix would show. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 2 -- 2026-09-05T10:03:53Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. The socket denial remains a Python site hook, so a child reaching the kernel's socket call through `ctypes` or another extension stays outside anything the runner observes: the round-1 probe was repeated on the fixed tree and again obtained a live descriptor with `CTYPES_SOCKET_FD 3` while `network_attempt` stayed false. That gap is stated in `DEMONSTRATIONS.md` rather than refused, and no run establishes that a child made no network call; a run establishes only that no denied Python socket call went unrecorded. Declaring the four programs as sources binds their bytes at the two trees this round read; it does not establish that the programs are correct, only that the bytes executed are the bytes the digest names. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration ran once per tree rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation taken under concurrent load on one machine, never a speed claim. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here. The report path was probed against traversal, an existing target, symlinked final and parent components, a dangling symlink and a parent swapped after resolution, but not against a writer racing `os.link` itself. The first `run_checks.py --full` invocation of this round returned `unstable-source` with zero checks because this Warden edited the tree while it ran; that invocation establishes nothing and its successor at the fixed tree is the one reported below.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R2-01 | high | `scripts/demonstrations.py` | The round-1 fix for S3-R1-02 refused `-S`, `-E` and `-I` by whole-word equality (`word in HOOK_DISABLING_OPTIONS`), and CPython bundles short options, so the same interpreter state was reachable by another spelling. `-Sc`, `-Ic`, `-Ec`, `-EsSc` and `-sSc` were each admitted by `preflight_record`; each child skipped `site`, never loaded the socket hook, built a real `socket.socket` (`SOCKET_OK socket`, not `_DeniedSocket`) and under `-Sc` resolved a name to `('::1', 80, 0, 0)`, while `network_attempt` stayed false and the report still recorded `"enforcement": "python-site-hook"`. This is the defect S3-R1-02 named, reintroduced by an incomplete fix rather than a new mechanism. | fixed in `63b7122b35b978d9360d3201e78142852f3e42da` |<br>| S3-R1-06 | medium | `plugins/anamnesis/skills/anamnesis/DEMONSTRATION.md` | Carried from round 1, where it was left open by decision. No public-set record declared its command program as a source, so `plugins/anamnesis/skills/anamnesis/scripts/anamnesis.py`, `plugins/lazarus/examples/goldfinch-v1/demo.py`, `plugins/alexandria/examples/credit-history-v0/demo.py` and `plugins/dokimasia/scripts/dokimasia.py` were proved to exist and never digested, while the report recorded `evidence: verified` for every input: `source-drift` over the executable half of the record. Each of the four ledgers now declares its program as a `repository` source carrying its digest; `D084` refuses a registered public demonstration whose program no source declares, and refuses a program whose bytes differ from the declared digest, re-read immediately before execution rather than trusted from load time. | fixed in `63b7122b35b978d9360d3201e78142852f3e42da` |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 and step 3 round 1 for the same reason; `docs/decisions` lies outside step 3's Files clause, the study pins that path in a `hypomnema-design-bridge/v1` block, and the run's sync-run must move or renumber the record explicitly, so it is not renumbered in this round |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `63b7122b35b978d9360d3201e78142852f3e42da` is 19 files, 2,557 insertions and 34 deletions across four signed commits. Phylax, Ephoros and Hypomnema each exited 0 over the changed paths on both the entry tree and the fixed tree, Hypomnema with `docs/decisions` in scope. Twelve boundary controls were re-probed against the fixed code rather than read from the round-1 fix descriptions, and eleven held: a symlinked report target and a target resolving outside through a symlinked parent were both refused `D080`; a parent directory swapped for a symlink after `resolve_report_target` returned was refused `D081` by the no-follow walk, where round 1 had published outside the root; shell metacharacters, `$(...)`, backticks, a pipe, a glob and a leading semicolon reached the program as five literal argv words with no file created; argv words beginning with a dash reached the program as data; a child writing 3,145,728 bytes was truncated at exactly 1,048,576 and refused `D077`; a child sleeping past a 2,000 ms budget was killed at 2,014 ms; a child that forked and exited 0 returned in 27 ms against round 1's 6,025 ms and its grandchild was dead seven seconds later, so the teardown now holds on the ordinary path and the recorded duration is the command's own; the child environment was exactly `HOME`, `LC_CTYPE`, `PATH`, `TMPDIR`, `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, `PYTHONIOENCODING` and the macOS runtime's `__CF_USER_TEXT_ENCODING`, with `GITHUB_TOKEN`, `GIT_DIR`, `AWS_SECRET_ACCESS_KEY` and `LAZARUS_RPC_URL` set in the parent and absent from the child; `sh`, `bash`, `/bin/sh` and `env` were each refused `D072`; and a child that swallowed the socket exception and exited 0 was still refused, as was one that tripped the hook and then unlinked the marker. The twelfth probe is S3-R2-01 above. Round 1 left `{{work}}` as an unsettled lead and this round settles it as not a defect: the token is matched as an unanchored substring, so `{{work}}` expands to `{` plus the work path plus `}` and `x{work}y` to `x` plus the work path plus `y`, but every expansion lands inside the private `0700` work root, `{WORK}`, `{work` and `work}` stay literal, and the only cost is that a record cannot express a literal `{work}` string, which no record needs; the docstring's "every other brace is literal" overstates by one case and the behaviour is contained. Four leads carried from round 1 were re-read and stand unchanged: the `-S`, `-E` and `-I` scan still covers the whole of `argv[1:]`, so a program argument equal to one of those words is refused as an interpreter option, and widening it to bundles widens that same over-refusal to any single-dash word containing `S`, `E` or `I`, which fails closed and which none of the four live records trips because each passes only long options; the unconditional `os.killpg` after `proc.wait()` still runs against a reaped pid; a relative `--report` value is still joined to the process working directory while containment is proved against `--output-root`, so an invocation from outside the root gets `D080` rather than a surprise write; and `HOME` is still passed to the child, so a credential in `~/.netrc`, `~/.gitconfig` or `~/.aws` stays reachable through the filesystem. `children_max_rss_bytes` still reads `RUSAGE_CHILDREN` and is still a running maximum rather than a per-command peak. Closing S3-R1-06 leaves one exposure it does not reach: a `-c` body and a `-m` module name are not committed files, so neither carries a digest, and the new `programs` array records no entry for them rather than implying one; no public-set record uses either form. The declaration changed no record's status, because `repository` is a preserved class: `check --root .` still reports 27 records as 4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent` and 0 `not-applicable` and exits 0. The refusal catalogue now holds 55 conditions in both the policy and the checker, `D084` having joined in the same commit as the code it names, and `D074`'s entry was widened to name the bundle. On the fixed tree `tests.test_demonstrations` passes 101 of 101 against round 1's 91, the four plugin suites pass 194, 625, 471 and 264, and the public set verifies 4 of 4 at 5,611 ms aggregate against the 600,000 ms ceiling, slowest 94 ms, 4,431 ms, 947 ms and 139 ms, with all five program entries across the four distinct programs reported `verified` beside their digests, the Lazarus chain anchor still `recorded` and every file source still `verified`. The root suite passes 1,248 of 1,249 in 143.172 seconds, failing only the S2-R1-03 test, and `run_checks.py --full --jobs 6` ran 29 checks and returned red on `root-suite` alone, that check failing on the same one test in 146.019 seconds with no adapter timeout, its report at `.hexaemeron/run-checks-step-3-round-2-fixed.json` carrying SHA-256 `a22ccbbdc3df5dea98a6c9417bb02c0082d1c4cd1069f7a2d171861e511be3d5`. Horos, `git diff --check` and `git diff --exit-code -- 'plugins/*/skills/**/EVOLUTION.md'` exited 0. In this Warden's judgement another round is worth running: a high-severity escape was found in a round-1 fix on this step's widest surface, which is direct evidence that a fix on this boundary can be incomplete, and the two fixes this round landed have not themselves been audited against a fresh tree. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 3 -- 2026-09-05T11:25:56Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. This round ran in the Fiat main session rather than a delegated Warden: the round-3 agent reached the host's session limit after committing `d27dd15752e585ab32c18249fef85cadb2877cf7` and before writing this record, leaving a clean tree and an unchanged log, and the same packet was completed in the main session. The two findings below are that agent's, established from its commit and its fix, and every figure in this record was re-measured here rather than carried from its report. The socket denial remains a Python site hook, so a child reaching the kernel's socket call through `ctypes` or another extension stays outside anything the runner observes, and `DEMONSTRATIONS.md` now also states that a child which starts a further process without the hook's `PYTHONPATH` gets an unhooked interpreter; neither is refused, and no run establishes that a child made no network call. Declaring the four programs as sources binds their bytes at the tree this round read; it does not establish that those programs are correct, only that the bytes executed are the bytes the digest names. A `-c` body and a `-m` module name are not committed files, carry no digest and take no `programs` entry. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration ran once rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation taken under concurrent load on one machine, never a speed claim. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R3-01 | high | `scripts/demonstrations.py` | Round 2 closed `S3-R1-06` by digesting the program each public demonstration runs, and read that program as `argv[1]`. CPython does not: `python3 -u prog.py` runs the same committed file as `python3 prog.py`. `-u`, `-B`, `-O`, `-x`, `-q`, `-v`, `-s`, `-P`, `-X <opt>`, `-W <opt>` and `--` each put the file somewhere neither the `D084` declaration gate nor the pre-execution digest re-read looked, so a registered public demonstration could run an undeclared, undigested program while its report carried an empty `programs` array. This is the third consecutive round in which a fix on this boundary held against the exact probe that motivated it and failed against the shape of the attack: `S3-R1-02` was whole-word flag matching, `S3-R2-01` was its bundled spelling, and this is the same binding read at the wrong argv position. | fixed in `d27dd15752e585ab32c18249fef85cadb2877cf7`: `classify_program` walks a closed interpreter grammar, stepping past flag bundles, the argument-taking `-W` and `-X` and a closing `--` to the program they precede, returning `-c`, `-m`, stdin and a `{work}` path as carrying no committed file, and refusing an option word the grammar cannot place with `D084` rather than guessing. Probed here against 38 argv spellings: every bundled form holding a letter outside `bBdhOPqRstuvVx` is refused, including `-uc`, `-Ic`, `-Ec`, `-EsSc`, `-sSc`, `-uSc`, `-bSc` and `-vES`; bare `-S`, `-I` and `-E` are refused; the attached-argument forms `-Xdev` and `-Wignore` are refused; long options other than `--check-hash-based-pycs` are refused; `python3 -- -c` classifies `-c` as a file, matching CPython. The grammar is fail-closed by exclusion |<br>| S3-R3-02 | medium | `scripts/demonstrations.py` | A grandchild calling `setsid` leaves the process group `_kill_group` tears down. It survived the run and held the command's pipes for the whole 10-second teardown wait, which round 1 recorded as the command's own duration: 10,032 ms for a 24 ms command. A recorded duration therefore described the runner's teardown rather than the work, and a process the runner started outlived the run unrecorded. | fixed in `d27dd15752e585ab32c18249fef85cadb2877cf7`: the duration ends when the command's own process is reaped, and a reader still blocked after the teardown join is evidence that something outside the group holds the pipes, refused with the new `D085`. `DEMONSTRATIONS.md` states the teardown boundary the group cannot reach |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 and step 3 rounds 1 and 2 for the same reason; `docs/decisions` lies outside step 3's Files clause and the run renumbers or moves the record at `sync-run` under the repository's merge-time decision assignment |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `d27dd15752e585ab32c18249fef85cadb2877cf7` is 19 files, 2,735 insertions and 34 deletions across five signed commits, at patch SHA-256 `d61e73bf4cc164ca92797266a9f58c37b6ae3bff905a6e97a0a6a35129fd3fb2`. Phylax, Ephoros and Hypomnema each exited 0 over the changed paths, Hypomnema with `docs/decisions` in scope. The refusal catalogue holds at 56 conditions stated in `DEMONSTRATIONS.md` and 56 enforced in the checker, with `D084` and `D085` in both. The root suite passed 1,252 of 1,253 tests in 161.640 seconds, failing only the accepted `S2-R1-03` collision; `run_checks.py --full` passed 28 of 29 checks at report SHA-256 `2fe5c37e6e93d042674a12d82a35d52c2f9dc82ad319e2998f4b7362178c1e75`, `root-suite` failing for that same cause with no adapter timeout. `tests.test_demonstrations` passed 105 of 105, up from 83 at the implementation head; the Anamnesis, Lazarus, Alexandria and Dokimasia suites passed 194, 625, 471 and 264. `demonstrations.py check --root .` reported 27 records as 4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent` and 0 `not-applicable`, unchanged by the program declarations because `repository` is a preserved class. The public set verified 4 of 4 at 4,499 ms aggregate against the 600,000 millisecond ceiling, slowest 97, 3,543, 743 and 116 ms, and every entry in every `programs` array now reads `verified` rather than found. Horos, `git diff --check` and the two `EVOLUTION.md` identity checks each exited 0. The Elenchus verdict is `guarded` on direct evidence rather than on the runner's exit alone: each of the four new guards was run against the parent tree `f3e81ccc7f90c0a1241a15e84e416d02b821d454` carrying the fixed test file and each failed there, with 1, 1, 4 and 1 failures, then passed on the fix; the runner's own report at `elenchus.unittest.v1` records 1,252 of 1,253 with one failure and zero errors and skips. Not pursued: a writer racing `os.link` against the report publication window, a platform other than the one driven, and the two stated non-claims above, none of which this step's Files clause reaches. On judgement this closes the loop: three rounds found ten defects on this boundary and fixed nine, the tenth being the accepted collision that belongs to `sync-run`, and this round's own probes of the interpreter grammar, the digest binding, the report path and the process teardown found nothing further to fix.
## Step 3, round 4 -- 2026-09-05T12:28:14Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. This round is an independent pass over round 3's own fixes, run by a new agent that read every suite document from nothing because the host cannot keep an agent alive between rounds. The two stated non-claims were re-probed on this tree and both hold unchanged: a child reaching the kernel socket through `ctypes` obtained a live descriptor, printing `CTYPES_SOCKET_FD 3` while `network_attempt` stayed false, and a child that started a further interpreter with `PYTHONPATH` removed got a real `socket` class in the grandchild while `network_attempt` again stayed false; the control child, hooked, was denied with `PermissionError` and recorded `network_attempt` true. A third route now joins them in `DEMONSTRATIONS.md` and is stated rather than refused: a grandchild that leaves the process group and also drops its inherited descriptors is not detected and survives the run. No run establishes that a child made no network call, and no run establishes that nothing the runner started outlived it. `-c` and `-m` are now refused for a registered public demonstration, so the exposure S3-R4-01 names is closed for the public set; for every other record `-c`, `-m`, stdin and a `{work}` path still carry no digest and no `programs` entry, and each can still reach a committed file. Declaring the four programs as sources binds their bytes at the trees this round read; it does not establish that those programs are correct. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration ran once per tree rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation taken on one machine, never a speed claim; the entry-tree measurements were taken while an unrelated `run_checks.py --full` from another worktree was running on the same machine. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here. A writer racing `os.link` inside the report publication window was not probed, and no platform other than the one driven was exercised.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R4-01 | high | `scripts/demonstrations.py` | The `D084` declaration gate refuses a registered public demonstration whose committed-file program no source declares, and `-c` and `-m` walked straight through it. `classify_program` returns `uncommitted` for both, so the gate bound nothing, `preflight_record` produced an empty `programs` map, and the report would carry an empty `programs` array while a committed file ran. The runner's working directory is the repository root and both forms put it on `sys.path`, so `python3 -m repo_contract` and `python3 -c "import repo_contract"` each executed the tracked file `repo_contract.py`; the `-c` probe printed that file's absolute path from inside the repository. The same Anamnesis record is refused `D084` when its program is written as the path `plugins/anamnesis/skills/anamnesis/scripts/anamnesis.py` with its source removed, and accepted when written as `-m` or `-c`. This is the fourth consecutive round in which a fix on this boundary held against the exact probe that motivated it and failed against the shape of the attack: `S3-R1-02` was whole-word flag matching, `S3-R2-01` its bundled spelling, `S3-R3-01` the binding read at the wrong argv position, and this is the binding declining to read a program position it classified as carrying no file. | fixed in `9afb87f3c5994e4d5295a3a2ad48d4e1861c0432`: a registered public demonstration's program must classify as `file` and be declared by a source; `-c`, `-m`, stdin and a `{work}` program are refused `D084` at the declaration gate and again in `preflight_record`. Records outside the public set keep all four forms |<br>| S3-R4-02 | medium | `plugins/hexaemeron/skills/DEMONSTRATIONS.md` | The teardown clause said a grandchild leaving the process group "is detected by the grip it keeps on the command's pipes and refused with `D085`, not silently allowed to survive", and that "a command that exits 0 after forking leaves nothing behind it". `D085` reads only that grip. A grandchild that calls `setsid`, reopens `/dev/null` over its inherited descriptors and then sleeps releases the pipes at once: the readers unblocked, `escaped_group` was false, the command recorded exit 0 in 28 ms with no refusal, and the process was alive after the run returned and the work root was removed. The document claimed a boundary the mechanism does not hold. | fixed in `9afb87f3c5994e4d5295a3a2ad48d4e1861c0432`: the clause now states that only the pipe-holding subset is detected and that a grandchild which also drops its descriptors is not detected and survives the run, beside the two routes the network paragraph already names |<br>| S3-R4-03 | low | `scripts/demonstrations.py` | `preflight_record` reads every command's program digest once, before the record's first command executes, while the code comment claims the re-read is what makes "verified before execution" name this execution rather than the earlier check. For a record carrying more than one command that is not so: `alexandria-credit-history-v0` carries `build` and `verify` over the same `plugins/alexandria/examples/credit-history-v0/demo.py`, so `verify`'s program was verified before `build` ran, leaving a window the width of the first command's execution rather than the width of a read. | fixed in `9afb87f3c5994e4d5295a3a2ad48d4e1861c0432`: `verify_command_program` re-reads and re-compares each command's declared program in the moment before that command runs, refusing `D084` on drift or an unreadable file |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 and step 3 rounds 1, 2 and 3 for the same reason; `docs/decisions` lies outside step 3's Files clause and the run renumbers or moves the record at `sync-run` under the repository's merge-time decision assignment |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `9afb87f3c5994e4d5295a3a2ad48d4e1861c0432` is 19 files, 2,907 insertions and 34 deletions across ten signed commits, at patch SHA-256 `7190e166b2f2ed1a92d3b460494f0779701ed62654474ef97bac685a8788400f`; every commit reads `Shoggoth|Dr Laurence E. Day|G`. Phylax, Ephoros and Hypomnema each exited 0 over the 19 changed paths, Hypomnema with `docs/decisions` in scope. The refusal catalogue holds at 56 conditions in the policy and 56 in the checker; `D084` gained two conditions in the same commit as the code and no new code was introduced, so the parity test is unchanged. On the entry tree the root suite passed 1,252 of 1,253 in 119.736 seconds and `run_checks.py --full --jobs 6` passed 28 of 29 at report SHA-256 `b56d7cfa5ed5a7089f42aa5852153c4e65fecac02493076e3c20c27289cd893a`; on the fixed tree the root suite passes 1,256 of 1,257 in 112.902 seconds and `run_checks.py --full --jobs 6` passes 28 of 29 at report SHA-256 `91e159e3877eebb87fb3b5f333bda4aba8ae4b916e596a0c565b0fe7b4e3f507`. Both runs failed `root-suite` alone, for the accepted `S2-R1-03` collision, with no adapter timeout. `tests.test_demonstrations` passes 109 of 109 against round 3's 105; the Anamnesis, Lazarus, Alexandria and Dokimasia suites pass 194, 625, 471 and 264. `demonstrations.py check --root .` reports 27 records as 4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent` and 0 `not-applicable` and exits 0 on both trees. The public set verified 4 of 4 at 4,480 ms aggregate on the entry tree, slowest 74, 3,506, 783 and 117 ms, and 4 of 4 at 3,990 ms on the fixed tree, slowest 89, 3,144, 659 and 98 ms, both against the 600,000 millisecond ceiling, with every `programs` entry reading `verified`. Horos, `git diff --check` and the `EVOLUTION.md` identity check each exited 0; the Horos boundary and census were refreshed with `scan . --write` and `scan . --write --census` and `check .` then reported that the boundary matches the tree. The Elenchus verdict is `guarded` on direct evidence as well as the runner's: each of the four new guards was run against the parent `6b01647853b92b55ac340f1a9f30b65dd82a4e01` in a detached worktree carrying the fixed test file, and each failed there as an assertion rather than an error -- "DemonstrationError not raised" for the two program-form guards, "False is not true" for the re-verification guard, and the removed clause still present for the teardown guard -- then all four passed on the fix; the source-bound runner `python3 tests/run_tests.py --elenchus-report {report}` in `unittest-json-v1` returned `guarded` on a parent assertion failure. The interpreter grammar was probed by differential rather than by re-reading round 3's 38 spellings: every single-dash single letter from `-a` to `-Z`, the argument-taking `-W`, `-X` and `--check-hash-based-pycs` in separated, attached and `=` spellings, `--help`, `--version`, `--`, `-`, the flag-only bundles `-uB`, `-BOq`, `-buB` and `-Ptu`, and `-m` and `-c` were each run under CPython 3.14.6 against a marker program and compared with `classify_program`. Six spellings diverge in the fail-closed direction and are refused `D084` or `D074` while CPython would have run the file -- `-i`, `-E`, `-I`, `-S`, `-W=always` and `-X=always` -- and `-h`, `-V` and `-x` classify a file the interpreter never reaches, which over-claims a digest for a program that does not run and refuses nothing. `-m` and `-c` were the two that diverged the other way and are S3-R4-01. The `index += 2` skip was probed against `-W`, `-X` and `--check-hash-based-pycs` at the end of an argv, before a program, and before another option, and in every case the grammar and CPython agreed on whether a file ran. A symlinked program is refused by the no-follow read with `ELOOP`; a hardlinked program is the same inode and digests identically; the argv[0] gate refuses `sh`, `bash`, `/bin/sh` and `env` with `D072`, so a non-CPython interpreter, a venv shim and `/usr/bin/env python3` never reach `execvp`, and `PYTHONSTARTUP` is not in the child's allowlist and applies only to an interactive interpreter. Five teardown constructions were driven: a `setsid` grandchild holding the pipes and a `setpgid` grandchild holding the pipes were both refused `D085` with the command's own duration recorded at 31 ms against a 10-second teardown; a double-forked grandchild that re-parents but stays in the group was killed by the group teardown and was dead after the run; an ordinary command whose helper exits inside the group produced no `D085`; and the descriptor-dropping escape is S3-R4-02. Settled controls were spot-checked rather than re-enumerated and all held: `D080` refused a `..` traversal, an existing target, the output root itself and a symlinked final component; `D081` refused a parent swapped after resolution and nothing landed outside; a published report left no `.partial-` artefact; `D077` truncated a 3,000,000-byte write at exactly 1,048,576 and refused; `D076` killed a sleeper at 2,008 ms against a 2,000 ms budget; the child environment was exactly `HOME`, `LC_CTYPE`, `PATH`, `TMPDIR`, `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, `PYTHONIOENCODING` and the macOS runtime's `__CF_USER_TEXT_ENCODING`; and `-Sc` and `-EsSc` were still refused `D074`. The empty-selection and partial-report specimens were exercised through their committed tests rather than re-probed by hand. Five leads were seen and left. `-h`, `-V` and `-x` name a program the interpreter will not run, so a record using them would digest a file that never executes; the observations would then fail and the record is refused, and no live record passes them. A `{work}` program is still admissible outside the public set and can be written by an earlier command in the same record, so undigested bytes can execute there, produced by bytes that were digested. The per-command re-read narrows the digest window to one read but does not close it, so a writer racing between the read and `execvp` remains unprobed, as does the `os.link` race. A daemon reader thread left alive by a `D085` escape is closed out from under it while the run continues to the next record, which could in principle return bytes from a later command's pipe into a dead sink; the thread's `os.read` raises and returns on a closed descriptor in every observation taken here, and the condition needs an escape that is already a refusal. `children_max_rss_bytes` still reads `RUSAGE_CHILDREN` and is still a running maximum rather than a per-command peak, and `HOME` is still passed to the child, so a credential in `~/.netrc`, `~/.gitconfig` or `~/.aws` stays reachable through the filesystem. On judgement the loop can now close: the probes of the interpreter grammar and the process teardown found one further instance of the same pattern and one overstated clause, both fixed here, and the grammar's remaining divergences from CPython are all in the fail-closed direction, which is a different shape from the three escapes rounds 1 to 3 found. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 5 -- 2026-09-05T15:41:00Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. This round is an independent pass over round 4's fixes, run by a new agent that read every suite document from nothing because the host cannot keep an agent alive between rounds. Round 4's three stated non-claims were re-driven: the unhooked-grandchild route holds unchanged, the descriptor-dropping route holds unchanged and survived three ways, and the `ctypes` route holds as stated but no longer covers what it did, because the pure-Python route into the same C type it was standing in for is `S3-R5-01` and is now refused. A genuine `ctypes` call raises no CPython audit event and stays outside the hook. No run establishes that a child made no network call, and none establishes that nothing the runner started outlived it. A fourth route is stated rather than refused from this round: the program digest is re-read immediately before each command and the child then resolves it by pathname, so a writer inside that window runs undigested bytes. `-c` and `-m` remain refused for a registered public demonstration and admissible elsewhere, where they carry no digest and no `programs` entry. Declaring the four programs as sources binds their bytes at the trees this round read; it does not establish that those programs are correct. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration ran once rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation on one machine, never a speed claim; the audit hook this round installs fires on every audited event in the child, and its cost was not isolated. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here. A writer racing `os.link` inside the report publication window was not probed, and no platform other than the one driven was exercised.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R5-01 | high | `scripts/demonstrations.py` | The socket denial replaced `socket.socket`, `socket.SocketType`, `_socket.socket` and nine resolver names, which binds names rather than the type. The type those names displaced stays reachable from ordinary Python: `socket.socket.__mro__[2]` is `_socket.socket`, and `[c for c in object.__subclasses__() if c.__module__ == '_socket' and c.__name__ == 'socket'][0]` finds it without reading the MRO at all. A probe child constructed it, bound and listened on `('127.0.0.1', 54909)`, connected a second raw socket from `('127.0.0.1', 54910)`, sent and received `b'LIVE-TCP-BYTES'`, and sent a UDP datagram, then exited 0 with `network_attempt` false, no refusal, and the report still recording `"enforcement": "python-site-hook"`; an earlier form of the same probe took `('127.0.0.1', 54856)`. No `ctypes`, no extension of its own and no second process, so this sits inside the hooked interpreter and outside both routes `DEMONSTRATIONS.md` named. This is the fifth consecutive round in which a control held against the exact probe that motivated it and failed against the shape of the attack: `S3-R1-02` was whole-word flag matching, `S3-R2-01` its bundled spelling, `S3-R3-01` the binding read at the wrong argv position, `S3-R4-01` the binding declining to read a program position, and this is a denial bound to a name rather than to the constructor. | fixed in `baab1622fed11d9fe155a875dfd5bf8c0349c9b5`: the site hook registers a CPython audit hook that records the attempt and raises on every `socket.` audit event, which fires inside the socket constructor whichever handle reached it, and no interpreter API removes a registered hook. The same probe is now refused `D074` with `network_attempt` true, as are nine further in-process routes |<br>| S3-R5-02 | medium | `scripts/demonstrations.py` | `_deny` looked its recorder up as the module global `_record`, and the child can import the hook module because its directory is the `PYTHONPATH` the runner gives it. A probe child that assigned `sitecustomize._record = lambda: None` and then called `socket.socket()` was denied with `PermissionError`, swallowed it, printed and exited 0, and the command recorded `network_attempt` false with no refusal -- against the contract's unconditional "a child that opens or resolves a socket is refused even when it swallows the exception and exits 0". Round 1's `S3-R1-03` closed marker removal, and removal is not the only way to lose the record: unlinking, truncating in place and `chmod 0` are all caught by the pinned identity, and suppressing the write is not. | fixed in `baab1622fed11d9fe155a875dfd5bf8c0349c9b5`: the recorder, the denial function and the denied socket class are closure locals returned from one installer, the resolvers hold that closure by value, and `_install`, `_deny` and `_name` are deleted from the module, so no name the denial uses is left standing for a child to reassign |<br>| S3-R5-03 | low | `scripts/demonstrations.py` | `preflight_record`'s comment says it applies "the same closed rule the declaration gate applies" on the path that actually executes, and it applied the form half alone. A public-set command whose program is a committed file no source declares passed preflight with its entry reading `found`, and `verify_command_program` returns without comparing anything on a non-`verified` entry, which is the exposure `S3-R1-06` and `S3-R2-01` named. `check_record` refuses that record when it loads, so no path reaches this today and it is defence in depth rather than an escape; the second layer was not the rule its comment named. | fixed in `baab1622fed11d9fe155a875dfd5bf8c0349c9b5`: `preflight_record` applies the declaration half too, refusing `D084` for a registered public demonstration whose program no source declares |<br>| S3-R5-04 | low | `plugins/hexaemeron/skills/DEMONSTRATIONS.md` | The contract said the per-command re-read "binds each program to the moment that command executes". It binds it to the read that precedes the command. A probe that replaced the program's bytes between `verify_command_program`'s read and the child's `execvp` ran the substituted program -- the child printed `EVIL` where the digest named bytes that print `GOOD` -- while the report entry still read `verified`; the run was refused by `D079` for the observation that then failed, not by any digest check, so a substitution reproducing the declared observations would have verified. Round 4 recorded this window as unprobed. It is real, and the contract named three routes it does not hold while leaving this one unstated. | fixed in `baab1622fed11d9fe155a875dfd5bf8c0349c9b5` as a stated boundary rather than a closed one: closing it needs the child to execute the descriptor the read holds, which changes what the program sees as its own path, so the contract now states the residual window as a fourth route beside the two the network paragraph names and the one the teardown paragraph names |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 and step 3 rounds 1, 2, 3 and 4 for the same reason; `docs/decisions` lies outside step 3's Files clause and the run renumbers or moves the record at `sync-run` under the repository's merge-time decision assignment |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `baab1622fed11d9fe155a875dfd5bf8c0349c9b5` is 19 files, 3,097 insertions and 34 deletions across twelve signed commits, at patch SHA-256 `73368d360cd7b485c605a0587e833403bcf56c0db20dbf75037b859d9cb0b102`; every commit reads `Shoggoth|Dr Laurence E. Day|G`. Phylax, Ephoros and Hypomnema each exited 0 over the 19 changed paths on both the entry tree and the fixed tree, Hypomnema with `docs/decisions` in scope. The refusal catalogue holds at 56 conditions in the policy and 56 in the checker, with no code added: `D074` and `D084` gained their new conditions in the same commit as the code they name. On the fixed tree the root suite passes 1,260 of 1,261 in 108.634 seconds, failing only the accepted `S2-R1-03` collision, and `run_checks.py --full --jobs 6` passes 28 of 29 at report SHA-256 `4557ce37ac323dee979587a7bd535732d7fade6cd9e1b24370e2c4c6fd138627`, `root-suite` failing for that same cause with no adapter timeout. `tests.test_demonstrations` passes 113 of 113 against round 4's 109; the Anamnesis, Lazarus, Alexandria and Dokimasia suites pass 194, 625, 471 and 264. `demonstrations.py check --root .` reports 27 records as 4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent` and 0 `not-applicable` and exits 0. The public set verifies 4 of 4 at 3,784 ms aggregate against the 600,000 millisecond ceiling, slowest 82, 2,973, 646 and 83 ms, every `programs` entry reading `verified` and the Lazarus chain anchor still `recorded`. The Horos boundary and census were refreshed with `scan . --write` and `scan . --write --census`, after which `check .` reported that the boundary matches the tree; `git diff --check` and the `EVOLUTION.md` identity check each exited 0. The Elenchus verdict is `guarded` on direct evidence as well as the runner's: each of the four new guards was run against the parent `ee22d20d6a2902abde3e16a5d9ff7a7fa4e6c3eb` in a detached worktree carrying the fixed test file, and each failed there as an assertion rather than an error -- `0 != 2` for the two socket guards, "DemonstrationError not raised" for the declaration guard, and the removed clause still present for the contract guard -- then all four passed on the fix; the source-bound runner `python3 tests/run_tests.py --elenchus-report {report}` in `unittest-json-v1` returned `guarded` on a parent assertion failure, and its report is written inside the detached parent worktree the check tears down, so its counts were not read here. Round 4's three fixes were attacked directly and all three hold. The `-m` and `-c` binding was driven with twenty argv forms against a registered public demonstration: `-m` naming a package, a package.module, a namespace package and a `__main__` module, `-m` behind a benign flag and behind `-X dev`, `-c` bodies that `exec`, `compile`, `__import__` and `runpy` a committed file, stdin, a `{work}` path, the bundles `-uc` and `-um`, the attached `-mscripts.demonstrations`, a `.pyc` path, a directory, an undeclared committed file, `-m` after `--`, and the declared program behind a benign `-u` as a control. Every form but the control was refused `D084` at the declaration gate; `preflight_record` refused each of them as well, `D084` for the option and `{work}` forms and `D072` for the three whose named file is absent or not a regular file, except the undeclared committed file, which is `S3-R5-03`. A module name is refused for its form before anything resolves it, so how the module would have been reached never arises. A symlinked program stays refused: `_read_regular_file` opens every intermediate component with `O_NOFOLLOW | O_DIRECTORY` and the leaf with `O_NOFOLLOW`, and the platform without `O_NOFOLLOW` is refused outright, while the child's `execvp` follows, so the gate refuses exactly what the child would have followed; driven here, a symlinked leaf and a symlinked intermediate directory were each refused `D072`, and a hardlinked program was admitted, being the same inode and digesting identically. The descriptor-dropping detection was driven with ten constructions. A `setsid` grandchild holding both pipes, one dropping stdout alone, one dropping stderr alone and one that held the pipes and then `SIGSTOP`ped itself were each detected, `escaped_group` true; one that reopened `/dev/null` over both, one that reopened a private pipe over both, and one that dropped both and left a great-grandchild behind were not detected and survived, which is the route round 4 stated; an in-group helper that exits and is reaped, a quiet command and a command that writes 200 lines then sleeps a second produced no `D085`, so no false positive was found on a legitimate command. In every case the recorded duration was the command's own, 26 to 52 milliseconds against a 5 to 10 second teardown wall clock. The per-command digest re-read covers every command in a multi-command record -- three commands produced three re-reads, all `verified` -- and bytes changed between command 1 and command 2 were refused `D084` at command 2, so the window is per-command as claimed; the residual read-to-exec race is `S3-R5-04`. Settled controls were spot-checked and all held: `D080` refused a `..` traversal, an existing target inside the root, the output root itself, an absolute path outside it, a symlinked final component and a dangling symlink, and admitted a fresh contained path; `D081` refused a parent directory swapped for a symlink after `resolve_report_target` returned, and nothing landed outside; `D072` refused `sh`, `bash`, `/bin/sh` and `env`; `D074` refused `-S`, `-E`, `-I`, `-Sc` and `-EsSc`; `D077` truncated a 3,000,000-byte write at exactly 1,048,576 and refused; `D076` killed a sleeper at 2,014 milliseconds against a 2,000 millisecond budget; the child environment was exactly `HOME`, `LC_CTYPE`, `PATH`, `TMPDIR`, `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, `PYTHONIOENCODING` and the macOS runtime's `__CF_USER_TEXT_ENCODING`, with `GITHUB_TOKEN`, `GIT_DIR`, `AWS_SECRET_ACCESS_KEY` and `LAZARUS_RPC_URL` set in the parent and absent from the child; and `$(id)`, a backtick `id`, `a|b`, `*` and `;id` reached the program as five literal argv words. The empty-selection and partial-report specimens were exercised through their committed tests rather than re-probed by hand. Nine further in-process routes were driven against the fixed hook and every one was denied and recorded: the raw type through the MRO and through `object.__subclasses__()`, `_socket.socketpair`, `ssl`, `http.client`, `urllib`, `multiprocessing.connection.Listener`, an `asyncio` event loop, and a socket built from an inherited descriptor with `fileno=`; a tenth confirmed that `sys` exposes `addaudithook` and `audit` and no removal. Five leads were seen and left. The audit hook fires on every audited event in the child rather than only socket events, and its cost was not isolated: the public set's 3,784 millisecond aggregate against round 4's 3,990 is one run each on one machine under different load and attributes nothing. `-h`, `-V` and `-x` still name a program the interpreter may not reach, so a record using them would digest a file that never executes; the observations would then fail and the record is refused, and no live record passes them. A `{work}` program is still admissible outside the public set, so undigested bytes can execute there, produced by bytes that were digested. The `os.link` race inside the report publication window is still unprobed, as is any platform other than Python 3.14.6 on Darwin 25.5.0 arm64. `children_max_rss_bytes` still reads `RUSAGE_CHILDREN` and is still a running maximum rather than a per-command peak, and `HOME` is still passed to the child, so a credential in `~/.netrc`, `~/.gitconfig` or `~/.aws` stays reachable through the filesystem. In this Warden's judgement another round is worth running, on this round's own evidence rather than on the pattern of earlier ones: round 4's three fixes hold under direct attack, but a high-severity escape was found in the oldest control on this surface, one that four rounds had probed only through the names it replaces, which is evidence that a round finding nothing new in the places it looked is not a round establishing that nothing is left; and the audit hook that closes it is a new mechanism on the widest-surface step, probed only by the agent that wrote it, which is the position round 4 was in. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 3, round 6 -- 2026-09-05T17:02:33Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=not-applicable; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=reviewed; contract-refusal-drift=reviewed; partial-demo-output=reviewed; front-door-regression=not-applicable; absent-document-as-clean=not-applicable; stale-member-status=not-applicable; portrait-inconsistency=not-applicable; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=reviewed; real-data-nonclaim-loss=reviewed; stale-record-provenance=reviewed; queue-duplication=not-applicable; visual-surface-drift=not-applicable; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity security suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. This step writes no public claim and advances no ledger frontier, so `claim-without-demo`, `front-door-regression`, `absent-document-as-clean`, `stale-member-status`, `portrait-inconsistency`, `visual-surface-drift`, `queue-duplication` and `frontier-lane-collision` are not applicable; it publishes no generated runtime and touches no design generator, so `generated-copy-drift` and `design-command-unreachable` are not applicable. This round is an independent pass over round 5's audit hook, run by a new agent that read every suite document from nothing because the host cannot keep an agent alive between rounds. Round 5's four stated non-claims were re-driven and hold: a genuine `ctypes` `socket(2)` call obtained a live descriptor with `CTYPES_SOCKET_FD 3` and `network_attempt` false; a child that spawned a further interpreter with the hook's `PYTHONPATH` removed bound a real socket in the grandchild with `network_attempt` false; a non-Python `bash`/`/dev/tcp` subprocess reached the kernel with `network_attempt` false; and the read-to-`execvp` digest window remains open. The audit hook itself was attacked as a mechanism and holds against every in-process Python route, including a fresh subinterpreter, which loads its own `sitecustomize` and installs its own hook because `PYTHONPATH` is inherited. The one escape found defeats not the denial but the record: a child that exhausts its file-descriptor table before it trips the hook made the marker write fail, so a denied socket recorded `network_attempt` false with no refusal; that is `S3-R6-01`, and both its deterministic single-threaded form and a multi-threaded descriptor-slot race are now refused. A fifth residual is stated rather than refused: the marker write can still be lost by exhausting disk space in the private work root so `os.write` fails rather than descriptor allocation, which the held descriptor does not cover; it is a coarser, machine-wide resource attack and denies no socket. No run establishes that a child made no network call, and none establishes that nothing the runner started outlived it. Declaring the four programs as sources binds their bytes at the trees this round read; it does not establish that those programs are correct. Only Python 3.14.6 on Darwin 25.5.0 arm64 was driven. Each demonstration ran once rather than three times, no per-skill duration threshold is declared, and every recorded millisecond is an observation on one machine, never a speed claim. That the four demonstrations are true of the world -- Anamnesis's component digest, Lazarus's receipt-trie relation, Alexandria's release id, Dokimasia's scrutiny -- is not established here. A writer racing `os.link` inside the report publication window was not probed, and no platform other than the one driven was exercised.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R6-01 | medium | `scripts/demonstrations.py` | Round 5's `S3-R5-02` made the recorder a closure so a child could not reassign it, closing one way to lose the record of a denied socket. Suppressing the record was still reachable by a second route the closure fix did not consider: `record` wrote the marker with `os.open`, which needs a free file descriptor. A child that lowers `RLIMIT_NOFILE` and exhausts its descriptor table before it trips the hook makes that `os.open` fail with `EMFILE`; the `except OSError: pass` swallows it, and the audit hook still raises, so the socket is denied but the marker is never written. The armed marker keeps its pinned identity at size zero, `network_attempted` returns false, and the run recorded `network_attempt` false with no refusal and exit 0 -- against the contract's "a child that opens or resolves a socket is refused even when it swallows the exception and exits 0" and its stronger "no denied Python socket call went unrecorded". The child obtains no live socket; the defect is in the audit record, the same class as `S3-R5-02`. | fixed in `bf13f5f3f23ea221a90e16bd0511e5a1034c17d4`: `_install` opens the marker descriptor once, before the child runs any of its own code, and `record` reuses it, allocating no descriptor at denial time, with an `os.open` fallback only for a child that closes the held one. The single-threaded exhaustion probe and a six-thread descriptor-slot race that beat a spare-and-retry variant 1 run in 10 are now both refused `D074` with `network_attempt` true across 20 race runs; ordinary, swallowed and raw-type denials still hold |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | The root suite still fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch`: this branch carries the record step 1 added in `9af57aa4180a4538a00d2987e2a298ddc55a4a51` while `origin/main` carries `ADR-076-digest-neutral-measured-corpus.md` from `330851883755c301b2850375e53fc6c26d2a0b06`. | accepted: carried forward from step 2 rounds 1, 2 and 3 and step 3 rounds 1, 2, 3, 4 and 5 for the same reason; `docs/decisions` lies outside step 3's Files clause and the run renumbers or moves the record at `sync-run` under the repository's merge-time decision assignment |<br><br>Leads not pursued: The reviewed diff from `d0eb8712c7d076fc9857faad653144df1260e8ba` to `bf13f5f3f23ea221a90e16bd0511e5a1034c17d4` is 19 files, 3,174 insertions and 34 deletions across fourteen signed commits, at patch SHA-256 `9ded6a0f55b683670f9a5477eafea23b7acc9973768b781afb2d690a111e77c3`; every commit reads `Shoggoth|Dr Laurence E. Day|G`. Phylax, Ephoros and Hypomnema each exited 0 over the changed paths `scripts/demonstrations.py` and `tests/test_demonstrations.py`, Hypomnema over `DEMONSTRATIONS.md` with `docs/decisions` in scope. The refusal catalogue holds at 56 conditions in the policy and 56 in the checker with no code added, `S3-R6-01` reusing `D074`. The audit hook was attacked as a mechanism, not through the names it blocks, and every in-process Python route to a socket was refused `D074` with `network_attempt` true: `_socket` imported directly, `socket.socket.__mro__[2]`, `object.__subclasses__()`, an `importlib` fresh `_socket` spec, `importlib.reload(_socket)`, popping `socket` and `_socket` from `sys.modules` and reimporting, `socketpair` and `socket.fromfd`; `gc.get_objects()` found no live socket object to reuse. A fresh subinterpreter through `concurrent.interpreters` and through low-level `_interpreters` under both the `isolated` and `legacy` configs is closed rather than open: each subinterpreter runs `site`, loads the runner's `sitecustomize` because `PYTHONPATH` is inherited, and installs its own audit hook, so a raw `_socket.socket` built inside a subinterpreter was refused `D074`. An in-process attempt to clear the registered hook and then open a socket was refused; the interpreter exposes no removal API. Four marker attacks failed closed and each was refused `D074`: recreating the marker as a fresh empty file, replacing it with a directory, replacing it with a symlink to a child-owned file, and the chmod, unlink and truncate identity cases the pin already covered. The two network non-claims the policy names were re-driven and both still hold as stated: a `ctypes` `socket(2)` syscall returned live `FD 3` with `network_attempt` false, and a grandchild interpreter spawned with `PYTHONPATH` stripped bound a real socket with `network_attempt` false; a non-Python `bash`/`/dev/tcp` subprocess reached the kernel with `network_attempt` false, which is the same "starts a further process" boundary. The regression spot-checks held through the committed suite and by hand: the interpreter grammar still refuses `-Sc`, `-EsSc`, `-S`, `-E` and `-I` with `D074`/`D084` and reads `-u`, `-m` and `-c` per its closed grammar; `-c` and `-m` are refused for a registered public demonstration; the per-command digest re-read fires per command; `D085` detects a pipe-holding grandchild while a descriptor-dropping one survives as stated; `D080` and `D081` guard the report path against traversal, an existing target, the output root, a symlinked final or parent component and a dangling symlink; `D077` truncates and refuses an oversized write; `D076` kills a sleeper with its group; `D072` refuses `sh`, `bash`, `/bin/sh` and `env`; the child environment is the allowlist plus `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, `PYTHONIOENCODING` and the macOS runtime's `__CF_USER_TEXT_ENCODING`; and the empty-selection and partial-report specimens are exercised by their committed tests. `tests.test_demonstrations` passes 114 of 114 against round 5's 113, the new descriptor-exhaustion guard included; the Anamnesis, Lazarus, Alexandria and Dokimasia suites pass 194, 625, 471 and 264. `demonstrations.py check --root .` reports 27 records as 4 `real-data`, 2 `mixed`, 20 `constructed`, 1 `absent` and 0 `not-applicable` and exits 0. The public set verified 4 of 4 at 6,164 ms aggregate against the 600,000 millisecond ceiling, slowest 132, 4,813, 1,067 and 152 ms, every `programs` entry reading `verified` and the Lazarus chain anchor `recorded`; the aggregate against round 5's 3,784 is one run each on one machine under different load and attributes nothing, and the held-descriptor fix adds nothing to the demonstration path, which attempts no socket. The root suite passes 1,261 of 1,262 in 136.502 seconds, failing only the accepted `S2-R1-03` collision, and `run_checks.py --full --jobs 6` passes 28 of 29 at report SHA-256 `d54ea846f82fc7cef35f05bf0da0be92b5936bfadb5ab55e3b433c75f5bc2cdb`, `root-suite` failing for that same cause with no adapter timeout. The Horos boundary and census were refreshed with `scan . --write` and `scan . --write --census`, after which `check .` reported that the boundary matches the tree; `git diff --check` and the `EVOLUTION.md` identity check each exited 0. The Elenchus verdict is `guarded` on direct evidence as well as the runner's: the new descriptor-exhaustion guard was run against the parent `e4c52d5c90745a4f5d2f0cc2cd381f6647c4e03a` in a detached worktree carrying the fixed test file and failed there as an assertion, "0 != 2", the fd-exhausted child having been recorded with no refusal, then passed on the fix; the source-bound runner `python3 tests/run_tests.py --elenchus-report {report}` in `unittest-json-v1` returned `guarded` on that parent assertion failure, and its report is written inside the detached parent worktree the check tears down, so its counts were not read here. Five leads were seen and left. The marker write is still lost if the child exhausts disk space in the private work root so `os.write` fails rather than descriptor allocation, which the held descriptor does not cover; it is a coarser resource attack, machine-wide rather than child-local, and denies no socket, so it is stated rather than fixed. The `ctypes`, unhooked-grandchild and non-Python-subprocess routes reach the kernel below the Python hook and cannot be denied by it; the read-to-`execvp` digest window and the descriptor-dropping teardown route stand as round 5 left them. A `{work}` program is still admissible outside the public set, so undigested bytes can execute there. The `os.link` race inside the report publication window is still unprobed, as is any platform other than Python 3.14.6 on Darwin 25.5.0 arm64. `children_max_rss_bytes` still reads `RUSAGE_CHILDREN` and is a running maximum, and `HOME` is still passed to the child, so a credential in `~/.netrc`, `~/.gitconfig` or `~/.aws` stays reachable through the filesystem. In this Warden's judgement the loop can close on this round's own evidence rather than on the pattern of earlier ones. Round 5's audit hook is the control this round set out to test, and it holds against every route that goes through a Python socket type, including the subinterpreter that four earlier rounds never reached; the one escape found is not a live socket but a lost record, closed here against both its single-threaded and its raced forms, and the remaining exposure is the shape the policy already states rather than claims away -- a socket obtained below Python through `ctypes`, a non-Python child, or the disk-full record loss, none of which a Python-level hook can deny. That residual belongs in a stated non-claim, which `DEMONSTRATIONS.md` already carries for the two kernel routes, not in another round on this step; a kernel sandbox is a design change outside this step's Files clause and outside what another audit round can add. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, duration, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 4, round 1 -- 2026-09-05T20:30:42Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=reviewed; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=not-applicable; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=reviewed; absent-document-as-clean=reviewed; stale-member-status=reviewed; portrait-inconsistency=reviewed; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=reviewed; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=reviewed; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. `subprocess-execution`, `external-data-egress` and `report-path-escape` are marked reviewed rather than the runbook's not-applicable because this round tested the boundary the runbook asserts: a `sys.addaudithook` census over a full `check_public_front_door.main(["--root", "."])` recorded no `subprocess.Popen`, `os.exec`, `os.fork`, `os.posix_spawn`, `socket.*`, write-mode `open`, `os.remove`, `os.rename`, `os.mkdir`, `os.symlink` or `shutil` event; the only `exec` and `compile` events came from stdlib `dataclasses`, `argparse` and `_colorize` module initialisation. That census covers Python-level operations on this platform only, not a call made below Python through `ctypes` or a non-Python child, and the module's own `CheckerBoundaryTests` assertion still reads one module's AST while the file reads happen in `demonstrations`. The four Atlas URLs, `https://github.com/wildcat-finance/skills-runtime` and every other external link were checked for presence and shape and were not fetched. `FUTUREPROOFING.md`'s per-member status prose was not re-read against each skill's own `EVOLUTION.md`, so `stale-member-status` is reviewed at the front-door boundary only. Neither PDF was regenerated or inspected, so `visual-surface-drift` is reviewed over the rendered Markdown only. The rendered inspection used a local `marked` 12.0.2 render at 375, 768, 1100 and 1400 CSS pixels, not GitHub's own renderer. Word-number recognition stops at ninety-nine. The reference-style reader resolves `[text][label]`, `![alt][label]` and the collapsed `[]` form and leaves the shortcut form `[label]` unread. Only Python 3.14.6 on Darwin 25.5.0 arm64 was exercised. `run_checks --full` was run twice, against the audited tree and against the fixes commit, and not against the tree this record itself creates.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S4-R1-01 | medium | `scripts/check_public_front_door.py` | Headings were read from the fenced view, so a fenced line was structure. With `## SO, YOU WANT TO BUILD GOD?` present only inside a ` ```text ` fence the checker exited 0: FD04, FD05, the chirp and aside boundaries and the card-section boundary were all satisfied by text that renders as code, and no contribution heading existed on the rendered page. The same blindness refused legitimate content in the other direction: adding `# install the thing` to the `npx` fence reported `FD11 heading 'install the thing' is not all caps`. Fixed by `unfenced()`, which blanks fence bodies to spaces while keeping every offset and line break, and which `check_structure`, `check_asides` and `check_cards` use for heading discovery only, because the command a card must display lives in a fence. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-02 | medium | `scripts/check_public_front_door.py` | `check_cards` collected bindings into a set, so `expected - bound` and `bound - expected` were both empty when two cards bound one record. A fifth card repeating card 1's marker, with its own command, preserved source, observed result and non-claim, exited 0 against a tree holding four `real-data` records, while the exit contract says the section carries exactly the four. Fixed by reporting FD21 on the second card that binds a directory already bound. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-03 | medium | `scripts/check_public_front_door.py` | A count marker bound its number to a key and never to the noun the reader sees. `<!-- front-door:count key="plugins" -->18 governed skills` and `<!-- front-door:count key="governed" -->27 plugins here` both exited 0, because 18 is the derived plugin count and 27 the derived governed count, while the page told a reader there are 18 governed skills and 27 plugins. Fixed by new refusal FD29 and a `COUNT_NOUNS` table requiring the key's own word inside the matched claim. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-04 | medium | `scripts/check_public_front_door.py` | `COUNT_CLAIM_RE` was lower case with no `re.IGNORECASE` while FD11 requires every ATX heading to be all caps, so neither FD26 nor FD28 could ever read a count inside a heading on this page. `## THE 99 GOVERNED SKILLS` exited 0. Two of the step's own rules combined to exempt a whole class of prose. Fixed by compiling the grammar case-insensitively. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-05 | medium | `scripts/check_public_front_door.py` | The FD11 exclusion for the generated contributors region was author-widenable without bound. `generated_spans` returned `(start, len(text))` when the closing marker was absent, so moving `<!-- contributors:start -->` above the title and deleting `<!-- contributors:end -->` exempted every heading in the document; `## Installation, in sentence case` then exited 0. Keeping both markers and moving the opening one above the title did the same thing. Fixed by new refusal FD30: an unclosed region, or one reaching the level-one title or either governed heading, is refused and no span is applied. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-06 | medium | `scripts/check_public_front_door.py` | Reference-style Markdown was invisible. `![Phylax][px]` with `[px]: ./assets/characters/phylax.png` put a second image on the root and exited 0 with no FD12, defeating `portrait-inconsistency` on the exact route it names; `[the catalogue][fp]` with `[fp]: ./FUTUREPROOFING.md` linked one target twice and exited 0, defeating FD07. `HTML_IMAGE_RE` and `HTML_HREF_RE` also required double quotes, so `<img src='./assets/characters/phylax.png'>` exited 0. Fixed by `reference_definitions`, `reference_uses` and a back-referenced quote group. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-07 | low | `README.md` | Count claims spelled out in words carried no marker and were never read, and `domains` and `phases` were qualifiers with no bare noun form, so FD28 never saw `thirty governed skills in twenty plugins` or `9 phases`, and both exited 0. `README.md` carried the live instance: `Four members rebuild something real` asserted the card count in words over a set FD21 already governs, and would have become false and silent the moment a fifth record turned `real-data`. The sixteen specimens carried the same sentence as `Two members`. Fixed by a `NUMBER_WORDS` table to ninety-nine, `claim_number()`, the two bare nouns, and rewording both sentences to `The members below` so each states the guarantee without asserting a number. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-08 | low | `scripts/check_public_front_door.py` | FD11 tested `heading != heading.upper()`, which is false for a heading holding no letters, so `## 2026` and `## ---` exited 0 while holding none of the house style. Fixed by also requiring at least one alphabetic character. | fixed in 9bf70483b2c25e408883cee721ae4476d74de00a |<br>| S4-R1-09 | low | `README.md` | Three routes the old root carried are now unreachable from the front door or any document it links: `./docs/agent-instruction-language-v1.md`, `./docs/pdf/how-to-help-shoggoth.pdf` and `https://github.com/laurenceday/shoggoth-interceptor`. None appears in `README.md`, `FUTUREPROOFING.md`, `AGENTS.md`, `INSTALL.md`, `PROMISE_MACHINE.md`, `SHOGGOTH.md`, `CONTRIBUTORS.md` or `docs/how-to-help-shoggoth.md`. The printable guide is the sharpest of the three, because `docs/how-to-help-shoggoth.md` is the page the front door sends contributors to and no longer offers its own PDF. Not fixed here: the natural homes are `docs/how-to-help-shoggoth.md`, which is outside this step's Files clause, and restoring all three to `README.md` spends the 1,400-word budget on the catalogue role the step exists to remove. The three shields.io badge routes the step deleted are not affected; `/go/chatgpt`, `/go/claude` and `/api/job` are all linked from `README.md` and all three badge images now live in `docs/how-to-help-shoggoth.md`. | accepted |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | Carried from step 2 round 1. The root suite fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` with `ADR-076: this branch adds ['ADR-076-gove...']`, because step 1's ADR-076 collides with the record already on `origin/main` at `33085188`. This is the single failure in every suite figure below and the single failing check in both `run_checks --full` runs. `docs/decisions` is outside this step's Files clause and the run disposes of it at `sync-run`. | accepted |<br><br>Leads not pursued: The reviewed range is `ae8045cb..3d419d51`, five signed commits and 25 files, every one inside the amended Files clause; the 2026-09-05 amendment naming `plugins/tabularium/tests/test_release.py` was read and that file's change is in clause. Mechanical exits over the 25 changed paths, on both the audited and the fixed tree: phylax 0, ephoros 0, hypomnema 0 with `docs/decisions` in scope, imprimatur 0 on `README.md` and 0 on `FUTUREPROOFING.md`. `scripts/check_public_front_door.py --root .` exits 0 with 4 checked cards on both trees. `python3 scripts/demonstrations.py check --root .` exits 0 and `git diff --check` exits 0. Root suite on the audited tree: 1,276 of 1,277, the one failure being S2-R1-03. Root suite on the fixed tree: 1,289 of 1,290, the same one failure, the thirteen extra tests being this round's guards. `tests.test_public_front_door` 15 of 15 before and 28 of 28 after, `tests.test_marketplace_prose` 23 of 23, `tests.test_shoggoth_topology` 32 of 32. `run_checks --full --jobs 6` on the audited tree: red, 29 of 30 checks, `root-suite` the only failure with class `test-failure`, report SHA-256 `f521f87a6863b4016ebbd37da844cc99ebe3cb3645074decf7c92c88f7acf438`. `run_checks --full --jobs 6` on the fixes commit `9bf70483`: red, 29 of 30 checks, `root-suite` again the only failure with class `test-failure`, `front-door-check` passed, report SHA-256 `cf689c2568b1e93e31920bfd43431b56441e740b3bee25adfd2cff5d77b5aa17`. Horos `check .` read `boundary matches the tree` before and after; `.horos/boundary.json` needed no change and `.horos/census.json` was refreshed by `scan . --write` and `scan . --write --census`. Elenchus was established directly before it was run: the thirteen new cases, overlaid alone onto a detached worktree at `3d419d51`, produced thirteen assertion failures and zero errors, and the declared runner `python3 tests/run_tests.py --elenchus-report {report}` under schema `elenchus.unittest.v1` then returned `guarded`. Four leads were seen and left. First, a `front-door:demo` marker nested inside an outer HTML comment stays a live binding, because `MARKER_RE` reads the raw text while `COMMENT_RE` blanks the outer comment from the rendered view; commenting a card out therefore does not remove it. The refusal direction is safe, so it was left rather than made to swallow a card silently. Second, eleven of the thirty declared refusals are named by no case: FD02, FD05, FD09, FD10, FD15, FD16, FD18, FD22, FD23, FD24 and FD27. FD05 fires inside `late-contribution-heading.md` without being named there, and each of the eleven was exercised by hand this round and fired correctly. `test_every_declared_refusal_is_reachable_from_the_checker` counts string occurrences rather than reachability, so a regression disabling one of the eleven would still pass. Third, `tests/test_public_front_door.py` reads specimens with `Path.read_text()`, which follows symlinks where the checker's no-follow descriptor walk does not; a symlink to `/etc/hosts` planted in `tests/fixtures/public-front-door/` was read and then produced one failure and one `AttributeError`, which is loud but unnamed. Fourth, `test_the_front_door_derives_every_count_it_claims` parses its number with `int()` while the checker now uses `claim_number()`, so the guard and the rule it guards disagree on a thousands separator and, since S4-R1-07, on a marked count written in words: the first marked word-count on the front door would raise `ValueError` in that case rather than fail it. Unreachable at the current counts, and left to the next round because repairing it needs a module attribute the Elenchus parent does not have, which would turn this round's thirteen assertion failures into a mixed report. Boundary probes that held: `README.md` as a symlink, absent, a directory, over 262,144 bytes, exactly 262,144 bytes, and not UTF-8 all refuse through FD01 or `unsafe-path`; `--root` above the tree and `--root` through a symlink both refuse; a `../../../../../../etc/passwd` link target in the README is inert because no link target is ever opened; and `skill="../../../etc"` reports FD17 with no path built from it. The three word budgets are exact: 150, 220 and 1,400 pass and 151, 221 and 1,401 report FD03, FD05 and FD06. The bare-environment claim was checked without reading the module's own assertion: the checker exits 0 under `python3 -I -S`, the suite runs 15 of 15 under `python3 -S` with `PYTHONPATH` cleared and 15 of 15 again with every `site-packages` and `dist-packages` entry stripped from `sys.path` at runtime, and an import census after a full run finds only `check_public_front_door`, `demonstrations` and `shoggoth_topology` outside the standard library, all three first-party. The rendered README was inspected at 375, 768, 1100 and 1400 CSS pixels: hierarchy runs h1, h2, h3 with no skipped level, all 22 link targets are distinct and every one of the 17 relative targets resolves on disk, the single image is the collective portrait and is not broken, no marker or comment text reaches the rendered body, and nothing overflows horizontally once GitHub's own `word-wrap: break-word` is applied, leaving only the five code fences scrolling inside themselves. Two rendered notes were left alone: the portrait carries `width="1200"` while `assets/characters/shoggoth.png` is 960 pixels wide, so a full-width column upscales it to 1012, and `## Thanks` is the one heading not in caps because `scripts/contributors.py` owns those bytes. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 4, round 2 -- 2026-09-05T21:46:49Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=reviewed; demo-class-inflation=reviewed; dokimasia-source-class=reviewed; source-drift=reviewed; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=not-applicable; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=reviewed; empty-selection-as-pass=reviewed; schema-declared-not-checked=not-applicable; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=reviewed; absent-document-as-clean=reviewed; stale-member-status=reviewed; portrait-inconsistency=reviewed; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=reviewed; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=reviewed; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. Round 1's `sys.addaudithook` census was not re-run, so `subprocess-execution`, `external-data-egress` and `report-path-escape` are reviewed over this round's diff instead, which starts no subprocess, opens no socket, builds no path from external data and writes nothing, and which moves one existing read from `Path.read_text()` to the no-follow reader. `README.md` bytes did not change this round, so `visual-surface-drift` is reviewed over the unchanged rendered Markdown; neither PDF was regenerated or inspected and no fresh four-width render of `README.md` was taken. `FUTUREPROOFING.md`'s per-member status prose was not re-read against each skill's own `EVOLUTION.md`. No external link was fetched. The rendered comparisons used a local `marked` 17.0.6 GFM render of the constructed probes, not GitHub's own renderer; round 1 used `marked` 12.0.2. `rendered()` preserves byte offsets and does not preserve line numbers: over 3,000 random documents its length held in every case and 402 lost line breaks, because a multi-line HTML comment is blanked to spaces including its newlines. Nothing was changed for it -- no rule reads a line number, and no line start outside a comment can be destroyed -- but round 1's record states that both are preserved and the line-number half does not reproduce. `unfenced()` preserves both. Hypomnema exits 1 with 82 H001 findings whenever the 16 front-door specimens are among its paths, under a file list, a `tests` walk, a `tests/fixtures` walk and a `tests/fixtures/public-front-door` walk alike; the exit recorded below is over the changed documents outside `tests/fixtures/`, which is the scope the repository's own `lint-hypomnema` check declares, and those links must not resolve because `specimen-live-coupling` requires a specimen to share nothing with the live tree. Word recognition still stops at ninety-nine, so `one hundred plugins` is refused as unmarked rather than read as 100. A heading holding only caseless letters, such as `## 一二三`, satisfies FD11. Only Python 3.14.6 on Darwin 25.5.0 arm64 was exercised. `run_checks --full` was run twice, against the audited tree and against the fixes commit, and not against the tree this record itself creates.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S4-R2-01 | medium | `scripts/check_public_front_door.py` | `FENCE_RE` anchored both delimiters at column zero while the renderer allows three leading spaces on each, so an indented closing row left the fence open, the next opening row was consumed as its closer, and the prose between them was blanked. That is the inverted mask the round-1 comment says it fixed, reached by another spelling. ` ```text `, `example`, `  ``` `, `## Sentence case that renders as a heading` exited 0 with no FD11 while `marked` 17.0.6 rendered that line as `<h2>`, and one indented closer with no re-open exempted every heading below it. The other direction refused legitimate content: `   ```text ` around `## Sentence case at column zero` reported FD11 on a line the renderer sets as code. Fixed by `[ ]{0,3}` on the opening and closing delimiter. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-02 | medium | `scripts/check_public_front_door.py` | `COUNT_CLAIM_RE` allowed only a closed list of seven qualifiers between the number and the noun, and read `domains` and `phases` in the plural alone, so one unlisted adjective hid a whole claim. `42 assorted plugins`, `42 total plugins`, `42 separate plugins`, `42 distinct plugins`, `42 shipped plugins`, `42 governed and first-party plugins`, `42 governed domain phase upstream plugins` and `1 domain` each exited 0 with no marker and no FD28, which is the stale literal `count-literal-reintroduction` names and the half of S4-R1-07 its `NUMBER_WORDS` table did not close. Fixed by up to three free-text words in place of the list, and singular bare nouns. The widened grammar finds the same four claims on `README.md` as the old one -- `27 governed skills`, `18 plugins`, `18 domain agents`, `9 phase agents` -- and all four carry markers. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-03 | medium | `scripts/check_public_front_door.py` | S4-R1-06 closed two of the three reference forms and one of the three HTML attribute spellings. `![lantern-art]` with `[lantern-art]: ./plugins/lantern/art.png` put a second image on the root and exited 0 with no FD12, and so did `<img src=./plugins/lantern/art.png>` and `<IMG SRC="./plugins/lantern/art.png">`; the matching `[fp]`, `<a href=./FUTUREPROOFING.md>` and `<A HREF="./FUTUREPROOFING.md">` forms each linked one target twice and exited 0 with no FD07. `marked` 17.0.6 renders all six as the image or the link, so `portrait-inconsistency` was defeated on three further routes. Fixed by `REFERENCE_SHORTCUT_RE` and by attribute patterns that accept a bare value and are case-insensitive. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-04 | medium | `scripts/check_public_front_door.py` | FD30 refused an unclosed region and one reaching the title or either governed heading, which left every other heading exemptible by an author. Moving `<!-- contributors:start -->` above `## The rest of the collective` and `<!-- contributors:end -->` below it exited 0 with that sentence-case heading unchecked; a repeated opening marker widened the first region the same way; and a `contributors:start` written inside a fence, where it renders as an example rather than a boundary, opened a live region. Fixed by naming `## Thanks`, the one heading `scripts/contributors.py` writes, refusing a region that covers any other heading or carries a repeated boundary marker, and locating the markers in the fence-blanked view. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-05 | low | `tests/test_public_front_door.py` | Round 1's second lead, settled. `test_every_declared_refusal_is_reachable_from_the_checker` asserted that each code appeared at least twice in the checker's source, which a regression that stops a rule reaching its `_finding` call leaves untouched, and eleven codes -- FD02, FD05, FD09, FD10, FD15, FD16, FD18, FD22, FD23, FD24 and FD27 -- were named by no case. Fixed by a `PROVOCATIONS` table carrying one document per refusal, a case asserting that the table and `REFUSALS` name the same set, and a case asserting that each document reports its own code. All 30 fire, on this tree and on the parent. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-06 | low | `tests/test_public_front_door.py` | Round 1's fourth lead, settled. `test_the_front_door_derives_every_count_it_claims` parsed with `int()` while the checker uses `claim_number()`: `int("thirty")` and `int("1,024")` both raise `ValueError`, so the first marked word count that S4-R1-07 made legal would have errored the guard rather than failed it. Round 1 left it because its own Elenchus parent had no `claim_number`; this round's parent `02034481` has one, so the repair costs nothing. Fixed by calling `front_door.claim_number()`. Unreachable at the current counts, and the guard for it lives in the same file as the fix, so it does not go red on the parent. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R2-07 | low | `tests/test_public_front_door.py` | Round 1's third lead, settled. The suite read specimens with `Path.read_text()`, which follows a symlink where the checker's `demonstrations._read_regular_file` refuses one with `unreadable-file`, so a link planted among the specimens would have been read by the suite and refused by the checker, and the suite was the more trusting of the two. Fixed by `read_no_follow()` behind every specimen read, with a case that plants a link in a temporary tree and asserts the refusal. Like S4-R2-06 the fix and its guard share a file, so it does not go red on the parent. | fixed in 6b885a159472f34e18801f3f1a9d4e2083665f94 |<br>| S4-R1-09 | low | `README.md` | Carried from round 1 and re-checked. `./docs/agent-instruction-language-v1.md`, `./docs/pdf/how-to-help-shoggoth.pdf` and `https://github.com/laurenceday/shoggoth-interceptor` still appear in no document reachable from the front door; both files are present on disk and the only remaining mentions are runbooks, studies and this audit file. Round 1's reason holds for this step, so the disposition stands, and it now names an owner rather than nobody: step 5's Files clause already lists `docs/how-to-help-shoggoth.md` and `docs/pdf/how-to-help-shoggoth.pdf`, so rehoming the three routes is in clause there. If step 5 does not take it, it carries to the run pull request as a carryover item, because otherwise the run ships a public root with three reader-facing routes deleted and rehomed nowhere. | accepted |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | Carried from step 2 round 1. The root suite fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` because step 1's ADR-076 collides with the record already on `origin/main` at `33085188`. This is the single failure in every suite figure below and the single failing check in both `run_checks --full` runs. `docs/decisions` is outside this step's Files clause and the run disposes of it at `sync-run`. | accepted |<br><br>Leads not pursued: The reviewed range is `ae8045cb..6b885a159472f34e18801f3f1a9d4e2083665f94`, eight signed commits and 28 files, every one inside the amended Files clause. Mechanical exits over the 28 changed paths, on both the audited and the fixed tree: phylax 0, ephoros 0, hypomnema 0 over the changed documents outside `tests/fixtures/` with `docs/decisions` in scope, imprimatur 0 on `README.md` and 0 on `FUTUREPROOFING.md`. `scripts/check_public_front_door.py --root .` exits 0 with 4 checked cards on both trees, `python3 scripts/demonstrations.py check --root .` exits 0, `tests.test_marketplace_prose` runs 23 of 23, `tests.test_shoggoth_topology` 32 of 32, `tests.test_public_front_door` 28 of 28 before and 41 of 41 after, `python3 plugins/horos/skills/horos/scripts/horos.py check .` exits 0 printing `boundary matches the tree` with no candidate drift, and `git diff --check` exits 0. Root suite on the audited tree: 1,289 of 1,290, the one failure being S2-R1-03. Root suite on the fixed tree: 1,302 of 1,303, the same one failure. Between them it read 1,301 of 1,303, the extra failure being `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan` against the changed checker and suite; `scan . --write` and `scan . --write --census` were both run, `.horos/boundary.json` needed no change, `.horos/census.json` moved by two lines, and the suite came back to one failure. `run_checks --full --jobs 6` on the audited tree `02034481`: red, 29 of 30 checks, `root-suite` the only failure with class `test-failure`, report SHA-256 `0205cafbfaadf1f1ae8ff4dfd1952d3c16a29d0605d6338d1a5da969a59f7691`. `run_checks --full --jobs 6` on the fixes commit `6b885a15`: red, 29 of 30 checks, `root-suite` again the only failure with class `test-failure`, `front-door-check` passed, report SHA-256 `87320ec2828b518c4001100f1c1b08b245932f1e980008514ba4f1d996f30cf9`. Elenchus was established directly before it was run: the changed test file alone, overlaid onto a detached worktree at `02034481`, produced 11 assertion failures and zero errors over 41 tests, each of the 11 also failing when run on its own, and the four remaining new cases pass there because their fix and their guard share that file; the declared runner `python3 tests/run_tests.py --elenchus-report {report}` under format `unittest-json-v1` and schema `elenchus.unittest.v1` then returned `guarded` for `6b885a159472`. Probes that held against round 1's eight fixes: nested four-in-three and tilde-over-backtick fences, a tilde fence and a fence opened and never closed all keep their headings out of the outline; a fence opened inside an HTML comment correctly leaves the heading below it checked and reports FD11; a count marker inside a fence stays live and reports FD27; two cards on one record, a card on the `mixed` record, a card carrying another record's digest, a card carrying 64 zeros, a fifth and sixth card, a missing card, a duplicate with its marker attributes reordered and extra whitespace, and a duplicate outside the section report FD21, FD20, FD19, FD19, two FD21, FD21, FD21 and FD16 respectively; a key over the wrong noun and a noun under the wrong key both report FD29; worded numbers agree in every declared key and a worded number against the derived 0 reports FD26; a number in an all-caps heading, in a link label, in a card body and inside a fence each report FD28; a bare topology noun with no number reports nothing; `## 2026`, `## ---`, `### 42` and `## ??!` report FD11 and `## ÉCOLE` does not; an image in an HTML comment is invisible as it is to a reader; and an unclosed region, an inverted marker order and a region reaching the title all report FD30. Four leads were seen and left. First, a `front-door:demo` marker nested inside an outer HTML comment is still a live binding, re-verified this round: `<!-- parked: <!-- front-door:demo ... --> -->` reported FD18, FD19, FD20, FD22, FD23, FD24, FD25 and FD21, so commenting a card out refuses rather than passing, which is the safe direction. Second, FD29 still tests its expected word as a substring of the whole matched claim rather than against the claim's own noun, so with free-text filler a key's word appearing among the filler satisfies it; a stricter test was written and reverted because it reported FD29 on the legitimate `3 domain agents`, where `domain` qualifies `agents`, and FD26 still compares the number in every such case. Third, a count claim inside a Markdown link label can never be marked, because `COUNT_CLAIM_RE.match` starts at the `[`, so `<!-- front-door:count key="plugins" -->[18 plugins](./x.md)` reports FD27 and FD28 together; the refusal direction is safe and the front door writes no such claim. Fourth, a count claim or an image written inside a fenced block is still read as prose and reports FD28 or FD12, because only heading discovery uses the fence-blanked view; that refuses a code example rather than passing one, and moving the other rules onto the outline would hide the command a card must display. Boundary probes from round 1 were not repeated. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 5, round 1 -- 2026-09-06T01:22:25Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=reviewed; demo-class-inflation=not-applicable; dokimasia-source-class=reviewed; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=reviewed; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=not-applicable; empty-selection-as-pass=not-applicable; schema-declared-not-checked=not-applicable; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=reviewed; absent-document-as-clean=reviewed; stale-member-status=reviewed; portrait-inconsistency=reviewed; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=reviewed; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=reviewed; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. `README.md` bytes did not change this step -- the two-line change at `a7d91531` belongs to step 4 -- so no fresh multi-width render of the front door was taken, and `visual-surface-drift` is reviewed over the two PDFs and the unchanged rendered Markdown instead. The Anamnesis portrait removal and the Dokimasia repair both landed before `a7d91531`; both were verified against `66b6cfd6` and `a7d91531` rather than found in this step's diff. `FUTUREPROOFING.md`'s per-member prose was not re-read against each skill's own `EVOLUTION.md`; only the sentences the status grammar recognises were. No external link was fetched, and no rendered comparison used GitHub's own renderer. Eight of the fourteen probed member-status phrasings remain silent and are listed under S5-R1-04; the maintained set was searched for all fourteen and carries none of them, but no attempt was made to enumerate the phrasings nobody has written yet. The historical marker still pins the numeral and not the noun beside it, which S5-R1-01's fix does not change. Word recognition still stops at ninety-nine. Only Python 3.14.6 on Darwin 25.5.0 arm64 was exercised. The second full run shared the machine with another Fiat delivery's own suite, so its durations are not comparable with the first run's; no check timed out in either. Round 1's boundary probes from steps 3 and 4 -- oversize documents, `--root` above the tree, traversal in a link target or a marker attribute -- were not repeated. The ten remaining pages of `docs/pdf/the-promise-machine-explained-properly.pdf` were read as extracted text rather than as rendered images; pages 1, 2, 7 and 12 were inspected as images.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S5-R1-01 | high | `scripts/check_public_front_door.py` | The new `front-door:historical` marker was accepted on every page carrying the count rule and added its claim's offset to `marked`, so the number under it was compared with neither the tree nor anything else. `captured` is free text and `figure` need only equal the prose numeral, so any current claim could be republished as a dated capture. Replacing the front door's `<!-- front-door:count key="domain" -->` with `<!-- front-door:historical captured="2026-01-01" figure="99" -->99 domain agents` exited 0 with no finding against a derived 3; the same marker over `99 plugins are installed.` on `README.md`, on a plugin landing page, separated from its figure by two blank lines, and inside a fence each exited 0. This is `count-literal-reintroduction` reached through the rule written to prevent it, and the exemption is permanent: FD31 then refuses any correction. Fixed by a history rule that only `INSTALL.md` carries; elsewhere FD34 reports and the number stays an ordinary count claim, which all five probes now show as FD34 with FD28. | fixed in ba1ba42640e140834288586f6aa4ad8c0a3bfdac |<br>| S5-R1-02 | high | `scripts/check_public_front_door.py` | The `marketplace-context` entry in `GENERATED_REGIONS` exempted every count and status claim between its markers, on any swept page, with no test behind the exemption. Its comment gave the reason as "its bytes are written from the skill's own ledger and checked against it by `tests/test_marketplace_prose.py`". Neither half holds: no script under `scripts/` writes those bytes, and that module asserts one block per landing README, one `**Current frontier.**` line in each, and agreement of that line across a plugin's copies -- presence and agreement, never derivation. Appending `<!-- marketplace-context:start -->`, `The tree holds 99 plugins and this version has not shipped.`, `<!-- marketplace-context:end -->` to `SHOGGOTH.md` exited 0 with no FD28 and no FD32, so two comment markers around any prose on any page bought the exclusion FD30 exists to refuse. Fixed by naming where each region is written -- `contributors` on the front door, `marketplace-context` on a plugin landing page -- and refusing one declared elsewhere under FD30 without applying it. The comment now states what the module checks. | fixed in ba1ba42640e140834288586f6aa4ad8c0a3bfdac |<br>| S5-R1-03 | medium | `scripts/check_public_front_door.py` | FD32 and FD33 read the marker's `skill` and `version` and never the subject of the sentence, so a claim about one member passed under a marker naming another whose ledger was current. `Quarry has not shipped its compile path, and this version of quarry does not ship one either.` under `<!-- front-door:status skill="lantern" version="lantern-v1.0.0" -->` on quarry's own landing page exited 0, as did one marker covering claims about two other members. The rule's stated promise is that no page describes a member against its own `EVOLUTION.md`; what it established was that some governed ledger stood at the declared version. Fixed for the pages where status claims live: FD35 refuses a landing-page marker naming a skill that page does not ship. Partial, and the docstring now says which two gaps remain: a named page belongs to no plugin, and a landing page can still carry a sentence about another member under its own marker. Neither is closable by a name test -- `plugins/probitas/README.md`'s marked region does not contain `probitas`, and `plugins/anamnesis/README.md`'s contains `Elenchus` and `Synkrisis` -- without rewriting prose this step's Files clause holds read-only. | fixed in ba1ba42640e140834288586f6aa4ad8c0a3bfdac |<br>| S5-R1-04 | medium | `scripts/check_public_front_door.py` | `STATUS_CLAIM_RE` carried `(?:yet )?` on its `shipped` branch and on no other, and read no contraction, so `is not implemented` reported and `is not yet implemented` did not. Twelve of fourteen probed phrasings were silent: `is not yet implemented`, `hasn't shipped`, `does not yet ship`, `is unimplemented`, `remains unbuilt`, `has not been built`, `No compile path has shipped`, `is still to come`, `was never built`, `is planned, not present`, `is not available`, `is not yet built`. This repository's own voice mask calls contractions normal, so the contracted form is the one the house style produces. No maintained page carries any of the twelve today. Fixed for the four that are the rule's own inconsistencies -- `yet` in every branch, and `n't` beside ` not` -- leaving eight silent: `is unimplemented`, `remains unbuilt`, `has not been built`, `No compile path has shipped`, `is still to come`, `was never built`, `is planned, not present`, `is not available`. Those eight are open vocabulary rather than a spelling of a branch that already exists, and widening to them would fire on ordinary prose across 27 documents. | fixed in ba1ba42640e140834288586f6aa4ad8c0a3bfdac |<br>| S5-R1-05 | low | `scripts/check_public_front_door.py` | A maintained document that is present and zero bytes passed every rule and was counted among the 27 that hold the contract, because FD01 answered presence and readability alone. Emptying `plugins/quarry/README.md` in the planted tree exited 0 with no finding, which is `absent-document-as-clean` one step short of absence. Fixed: an empty page is an FD01 refusal, on the same terms as a missing, symlinked, non-regular or non-UTF-8 one, each of which was re-probed and still refuses. | fixed in ba1ba42640e140834288586f6aa4ad8c0a3bfdac |<br>| S5-R1-06 | low | `scripts/check_public_front_door.py` | `check()` reads `ledger_version` for all 27 governed skills before it opens any document, so an `EVOLUTION.md` that reads cleanly but carries no `- Current version:` row raises `FrontDoorError` out of the whole sweep and `main` returns 2 with no refusal code and no findings list, even on a tree where no page carries a status claim. Removing that row from one planted ledger reproduced it. Accepted rather than fixed: the failure is closed and names the file, so nothing passes through it, and every other declared condition has an `FDnn` and a case, so giving this one both belongs with the refusal inventory rather than inside an audit fix. The cost is diagnosability -- one malformed ledger hides the state of the other 26 documents. | accepted |<br>| S5-R1-07 | medium | `scripts/build_contributor_guide.py` | The builder's docstring says "The public Markdown source is docs/how-to-help-shoggoth.md", and the builder reads no Markdown at all: every string on its five pages is a literal in the module. The step changed 38 heading lines and surrounding prose in that source; `python3 scripts/build_contributor_guide.py` then rewrote the PDF to the same bytes, `b74a5bde75038b67692786b3806e31b06aa1990f0a97751d1d1d8c184f2b6c47` before and after, and `git diff --exit-code -- docs/pdf/how-to-help-shoggoth.pdf` exited 0 with no output. The Exit's tolerant comparison is therefore byte-identical by construction and can report no prose drift; only the page-by-page human inspection reaches it. All five pages were inspected and are sound: hierarchy intact, nothing overflowing, no broken or anomalous image, both `INSTALL.md` deep links still resolve because a GitHub anchor is derived from lower-cased heading text and `### CODEX` and `### CLAUDE CODE` still yield `#codex` and `#claude-code`. The footer reads `31 August 2026`. Accepted for the integrate carryover: `scripts/build_contributor_guide.py` is outside this step's Files clause. | accepted |<br>| S5-R1-08 | low | `docs/pdf/the-promise-machine-explained-properly.pdf` | Recorded because the step's Exit requires it. Fifteen ATX headings and one count sentence changed in `docs/the-promise-machine-explained-properly.md` this step, the binary has no generator in this repository, and it did not move. It is a designed twelve-page field guide rather than a rendering: page 7 reads `Six phase skills travel between jobs` and names six, while discovery derives 9 phase skills, and the same shape of claim was rewritten out of `plugins/hexaemeron/README.md` in this step because the checker refuses it. The gap is declared on the artefact itself -- every page footer reads `source snapshot 21 August 2026` and page 12 reads `Source boundary: local Wildcat Labs snapshot, 21 August 2026, Promise Machine v1; Hexaemeron package 1.5.3` -- so nothing in it claims to be current. Accepted and carried to the integrate carryover rather than left as a stale binary nobody named. | accepted |<br>| S5-R1-09 | low | `plugins/hexaemeron/README.md` | Two true counts were deleted rather than derived, because `COUNT_KEYS` derives `plugins`, `governed`, `canonical` and `phase` and has no key for the discipline population. `six phase disciplines; all six ship an executable check` became `the phase disciplines, of which six ship an executable check`, and `Six skills carry the standards each phase is held to` became `The standards each phase is held to are carried by their own skills`. Both originals were accurate; the grammar reads `six phase` as a claim about the derived `phase` count of 9 and refuses it. The surviving `six ship an executable check` escapes FD28 only because no topology noun falls inside the two-word window, and it is checked instead by `plugins/hexaemeron/tests/test_fiat_skill.py`, which derives it from the phase skills that ship a script. `two agents share one run's tree` became `a second agent enters one run's tree` for the same reason. Accepted: the study requires every mutable count claim to be derived, no third option existed inside this step's clause, and the numbers are recoverable from the lists beside them. | accepted |<br>| S5-R1-10 | low | `scripts/check_public_front_door.py` | The step's Exit says "Every ATX heading on that set is all caps" and names both `PROMISE_MACHINE.md` and `.agents/skills/promise-machine/SKILL.md` in that set; the checker gives both `CONTRACT_RULES` and exempts them. Read against the study, half of that holds. Assumption 3 says the rule "applies to every ATX heading on that maintained human entry surface. It does not rewrite headings in normative agent instructions, audit evidence, ADR history, or specimens", and assumption 2 defines that surface by enumeration: `README.md`, `INSTALL.md`, `FUTUREPROOFING.md`, `SHOGGOTH.md`, `PROMISE_MACHINE.md`, the three `docs/` pages, "all 18 first-party plugin `README.md` files, and the two generated PDFs under `docs/pdf/`", adding that "Agent contracts, canonical `SKILL.md` files, ADRs, audits, historical studies, runbooks, and specimens are factual sources, not surfaces to restyle." The router is a canonical `SKILL.md` and is absent from that enumeration, so its exemption is supported outright. `PROMISE_MACHINE.md` is named in the enumeration, so assumptions 2 and 3 do not support its exemption; the boundary does -- "The new public-heading style is checked only over the maintained surface in assumption 2. It must not churn operational contracts and history to satisfy an aesthetic rule" -- as does the `historical-record-rewrite` risk, which requires checks to "preserve digest-bound, append-only, or explicitly historical text". Both reasons were verified rather than taken. `scripts/promise_machine.py` holds fourteen exact sentence-case headings of that file in `REQUIRED_HEADINGS` and refuses each unless it occurs once, and its `PROMISE_ID` pattern `[a-z][a-z0-9]*(?:-[a-z0-9]+)*` refuses a promise id that is not lowercase, which covers five of the remaining ten `###` headings; upper-casing any of them reddens `python3 scripts/promise_machine.py check`, one of this step's own Exit commands. The file is also pinned whole: `tests/fixtures/agent-instruction-v1/promise-machine-router-selection/model.json` names it at SHA-256 `f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15`, which is what its 27,732 bytes hash to now, and carries byte spans `18285`-`24109` and `18352`-`24107` into it, with `manifest.json` and `source-spans.json` beside it. Any heading change moves that digest and those offsets. So the reading holds, on firmer ground than the module comment gives; what does not hold is the Exit clause, which still reads "Every ATX heading on that set is all caps" over a set naming both files, and which no amendment narrowed. The exemption is visible -- `CONTRACT_RULES` carries its reason and the module docstring names both files -- but no case pins the set, and `test_the_maintained_set_covers_the_named_pages_and_every_plugin` asserts only that each swept page carries some rule, so a third exemption would be silent. Accepted, not fixed: a case pinning the exemption set passes on `a9e0fe60` as well as here, so adding it to this round's fixes commit would replace a guard that went red with one that never did. It belongs in the next round or the integrate carryover, together with the Exit clause that still reads the other way. | accepted |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | Carried from step 2 round 1 and re-checked. The root suite fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` because step 1's ADR-076 collides with the record already on `origin/main` at `33085188`. This is the single failure in every root-suite figure below and the single failing check in both `run_checks --full` runs. `docs/decisions` is outside this step's Files clause and the run disposes of it at `sync-run`. | accepted |<br><br>Leads not pursued: The reviewed range is `a7d91531..a9e0fe60`, seven signed commits and 38 files, 33 of them named by the amended Files clause; the 2026-09-06 amendment naming `INSTALL.md`, `FUTUREPROOFING.md`, `tests/test_marketplace_prose.py`, `tests/test_shoggoth_identity.py`, `plugins/tabularium/tests/test_release.py` and `plugins/hexaemeron/tests/test_fiat_skill.py` was read and all six of those changes are in clause. `README.md` is not in the range: its two-line change sits at `a7d91531`, which belongs to step 4. The five files Files does not name are the new front-door specimens under `tests/fixtures/public-front-door/`, which the Tests clause requires by describing each of them; Boundaries' "Change no ... specimen" reads on the preserved demonstration specimens under `plugins/*/specimens/`, and none of those changed. The gap is between two clauses of the same runbook rather than a change outside it, and it is named here rather than logged as a finding. Mechanical exits over the 38 changed paths on the audited tree: phylax 0, ephoros 0, hypomnema 0 with `docs/decisions` in scope, imprimatur 0 over all 30 changed Markdown files, each of the 30 printing its own header. Round 2 of step 4 recorded hypomnema over the changed documents outside `tests/fixtures/`, because the front-door specimens then produced 82 H001 findings; this round's five new specimens were inside the hypomnema paths and it still exits 0, and they exit 0 on their own, because none of the five carries a relative link. The same four exit 0 over the three paths this round changed. `scripts/check_public_front_door.py --root .` exits 0 on both trees, reporting 27 documents and 4 checked cards. The step's other Exit commands on the audited tree: `tests.test_marketplace_prose` 23 of 23, `tests.test_public_front_door` 45 of 45, `scripts/promise_machine.py check` 0 printing `clean: 18 plugin(s), 18 copy/copies`, `scripts/build_contributor_guide.py` 0, `scripts/portable_promise_machine.py package` 0 into a temporary directory, `tests.test_skills_sh_package` 13 of 13, `horos.py check .` 0 printing `boundary matches the tree` with no candidate drift, and `git diff --check` 0. `tests.test_public_front_door` is 53 of 53 on the fixed tree. Root suite on the audited tree: 1,306 of 1,307, the one failure being S2-R1-03. Root suite on the fixed tree: 1,314 of 1,315, the same one failure. Between them it read 1,313 of 1,315, the extra failure being `test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan` against the changed checker and suite; `scan . --write` and `scan . --write --census` were both run, `.horos/boundary.json` needed no change, `.horos/census.json` moved, `check .` printed `boundary matches the tree`, and the suite came back to one failure. `run_checks.py --full --jobs 6` on the audited tree `a9e0fe60`: red, 29 of 30 checks, `root-suite` the only failure with class `test-failure` and exit code 1, its 1,307 tests reporting the one S2-R1-03 failure, report SHA-256 `ed857af2ce2fc64d3a7001f12724ef3153116c90c55ce5c6c618602d4828fd7d`. `run_checks.py --full --jobs 6` on the fixes commit `ba1ba426`: red, 29 of 30 checks, `root-suite` again the only failure with the same class and exit code, its 1,315 tests reporting the same one failure, `front-door-check` passed, report SHA-256 `aa0142d2a13dec73ae3780f06ad8e3e9eccf69907878f71f50b1537491a5ee71`. The two earlier full runs in this step found defects in `tabularium-suite` and `hexaemeron-suite` that the root suite does not reach; both passed in both runs here, at 7.9s and 897.7s on the audited tree and 7.2s and 1,143.0s on the fixed tree. Elenchus was established directly before it was run: the changed test file alone, overlaid onto a detached worktree at `a9e0fe60`, produced 10 assertion failures and zero errors over 53 tests, and the positive case asserting the exemption still holds on `INSTALL.md` passed there as it does here; the declared runner `python3 tests/run_tests.py --elenchus-report {report}` under format `unittest-json-v1` and schema `elenchus.unittest.v1` then returned `guarded` for `ba1ba42640e1`. The audit file was 149,325 bytes at SHA-256 `ff5c37d1b9238a884bbb0745663d4cae82b640d92b9a744ba9f7c3a7d8d8103c` before this record, carrying 14 records ending at `## Step 4, round 2 -- 2026-09-05T21:46:49Z`. Six leads were seen and left. First, the historical marker pins the numeral and not the noun beside it: changing `INSTALL.md`'s `two plugins pinned at their old commit` to `two domains` exits 0, because FD31 compares `claim.group("number")` with the `figure` attribute alone. Left, because the pin's job is to refuse a figure brought up to date, and pinning the whole claim would fire on any rewording of a sentence the Boundaries clause already holds read-only. Second, a landing page can still carry a sentence about another member under its own marker, which is the half of S5-R1-03 FD35 does not reach. Third, a count or status claim inside a landing page's `marketplace-context` region is still exempt after the S5-R1-02 fix; `plugins/anamnesis/README.md` already carries one, `41 findings from three skills chosen by hand`, and it is the anamnesis ledger's own Next Fiat job text quoted verbatim. Closing it needs either an `EVOLUTION.md` edit or a rewrite of ledger-derived prose, and this step's Files clause forbids both. Fourth, `count`, `historical`, `status` and `demo` markers written inside a fenced block are live bindings, because `markers()` reads the raw text, while a region boundary inside a fence is correctly a quoted example, because `generated_spans()` reads the fence-blanked view; a page documenting the marker syntax in a fence would therefore grant a real exemption. After the S5-R1-01 fix the historical case refuses rather than passes, and the others already refused, so the direction is safe and it was left. Fifth, a `plugins/<id>/` directory holding a `SKILL.md` but no `EVOLUTION.md` and no manifest entry is invisible to discovery and owes no landing page, while a `plugins/<id>/` with no `skills/` directory raises `unsafe-path` and exits 2; both shapes are stray directories and they are handled differently. Left because neither passes anything off as checked. Sixth, `by_skill` keys governed skills by their last path segment, so two skills of the same name in different plugins would collide, but the topology reader refuses `duplicate-skill-id` before the checker builds that map, so the collision is unreachable. Two regression checks that held: widening did not weaken the earlier rules -- a sentence-case heading and an unmarked count on a plugin landing page report FD11 and FD28, a count marker quoted in a fence reports FD27 with FD28 on the real claim, and every step-4 case still passes -- and no construction on one document changes another, checked by opening an unclosed region on `SHOGGOTH.md` while breaking a heading on `plugins/quarry/README.md` and getting one finding for each page. One premise in the packet is not what the code does: FD12 and the card rules did not widen to 27 documents. `check_structure`, `check_asides`, `check_roster` and `check_cards` still run only under `FRONT_DOOR_RULE`, so FD02 through FD10 and FD12 through FD25 remain front-door rules; what widened is the heading, count and status machinery with the comment mask, the fence mask and the generated-region decision underneath it. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
## Step 5, round 2 -- 2026-09-06T02:48:25Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: claim-without-demo=reviewed; demo-class-inflation=not-applicable; dokimasia-source-class=reviewed; source-drift=not-applicable; frontier-lane-collision=not-applicable; count-literal-reintroduction=reviewed; specimen-live-coupling=reviewed; fixture-tree-miscount=reviewed; generated-copy-drift=reviewed; external-data-egress=reviewed; subprocess-execution=reviewed; report-path-escape=not-applicable; empty-selection-as-pass=not-applicable; schema-declared-not-checked=not-applicable; contract-refusal-drift=reviewed; partial-demo-output=not-applicable; front-door-regression=reviewed; absent-document-as-clean=reviewed; stale-member-status=reviewed; portrait-inconsistency=reviewed; historical-record-rewrite=reviewed; design-command-unreachable=not-applicable; demo-skip-as-pass=not-applicable; real-data-nonclaim-loss=reviewed; stale-record-provenance=not-applicable; queue-duplication=not-applicable; visual-surface-drift=reviewed; horos-boundary-staleness=reviewed<br><br>Not checked: The Solidity suite is waived because this step ships no Solidity, and the vendored suite documents were the copies read. This round audited the named targets round 1 left partial and the two rules it wrote; it was not a fresh sweep of the whole step diff, which round 1 covered over `a7d91531..a9e0fe60`. No Markdown document changed, so no page was re-rendered and `visual-surface-drift` is reviewed over the two unchanged PDFs alone; `docs/pdf/how-to-help-shoggoth.pdf` regenerated to `b74a5bde75038b67692786b3806e31b06aa1990f0a97751d1d1d8c184f2b6c47` again and was not re-inspected page by page, because round 1 inspected all five pages of these same bytes. The ten remaining pages of `docs/pdf/the-promise-machine-explained-properly.pdf` were not read at all this round. Subject-join shapes that need a parse rather than a paragraph were measured and left: a pronoun subject, an alias, and a sentence naming two members are recorded under S5-R2-03 rather than closed. Word recognition still stops at ninety-nine, and the historical marker still pins the numeral and not the noun. Only Python 3.14.6 on Darwin 25.5.0 arm64 was exercised. The boundary probes from steps 3 and 4 -- oversize documents, `--root` above the tree, traversal in a link target or a marker attribute -- were not repeated. `FUTUREPROOFING.md`'s per-member prose was again not re-read against each skill's own ledger; only the sentences the widened status grammar recognises were. No external link was fetched and no rendered comparison used GitHub's own renderer.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S5-R2-01 | high | `scripts/check_public_front_door.py` | S5-R1-02 closed the host and not the exemption. On the page it belongs to, a `marketplace-context` region went on exempting every count and status claim between its markers, across all 18 landing pages, and nothing in this repository writes those bytes: the block is hand-maintained prose held only to one `**Current frontier.**` line per copy by `tests/test_marketplace_prose.py`, which is presence and agreement. Two constructions of my own exited 0 with nothing reported -- `**Current frontier.** Quarry has not shipped its compile path and this version of thicket has not landed either.` left three member-status claims unread, and `**Current frontier.** The marketplace ships 99 governed skills and 42 plugins.` left two count claims unread. That is `count-literal-reintroduction` and `stale-member-status` reached by typing two comment markers, which is the author-granted exclusion FD30 exists to refuse. Fixed: each region now declares what it may exempt. `contributors` covers all three rules because `scripts/contributors.py` writes those bytes and would delete a marker placed in them; `marketplace-context` covers its heading, which is the marketplace's, and a claim inside it is exempt under the new FD36 only when the skill's own `EVOLUTION.md` carries the sentence making it. That is the derivation the old comment asserted and did not have. Measured over the live tree first: exactly two claims sit inside a generated region, `three skills` on `plugins/anamnesis/README.md` and `remain unimplemented` on `plugins/tabularium/README.md`, and both are quoted verbatim from their own ledger rows, so the fix closes the hole without reddening the tree or touching a read-only `EVOLUTION.md`. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-02 | medium | `scripts/check_public_front_door.py` | FD34 narrowed the page and not the claim, so `count-literal-reintroduction` is still reachable through the rule S5-R1-01 wrote to prevent it -- on one page instead of 27. On `INSTALL.md`, `<!-- front-door:historical captured="2026-06-05" figure="99" -->99 governed skills right now, and that number is current.` exited 0 with no finding, because `history=True` is a property of the page rather than of the figure. `captured` was also free text, so the refusal named "historical marker only where a dated capture is recorded" checked that no capture was recorded anywhere: `captured=""`, `captured="last summer"` and `captured="2026"` each exited 0. Fixed: `captured` must match `YYYY-MM-DD` and that exact date must appear in the page's own rendered prose, so the exemption rests on evidence a reader can see rather than on an attribute the renderer hides. Both live markers satisfy it -- `captured="2026-08-22"` over the figures `thirteen` and `fourteen`, with `2026-08-22` written once in `INSTALL.md`'s visible prose. This bounds the exemption; it does not make it unreachable, because an author who writes the date into prose can still exempt a current number, and that residue is stated rather than closed. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-03 | medium | `scripts/check_public_front_door.py` | FD35 asked only that the marker name a member the page ships, and one plugin ships many. `plugins/hexaemeron/README.md` ships 10 governed skills -- elenchus, ephoros, fiat, hypomnema, imprimatur, kronos, metron, phylax, protasis, vulgate -- so all 10 ledgers vouched for any claim on that page. A marker naming `vulgate` over `Fiat has not shipped its controller path, and this version of fiat does not ship one either.` exited 0. The consequence is worse than the mismatch: `fiat` stands at `fiat-v5.49.1` and `metron` at `metron-v1.1.0`, so binding a fast-moving member's status to the slowest-moving sibling's ledger keeps a stale sentence current indefinitely and FD33 never fires. The specimen tree could not reach this shape at all, because the topology reader refuses a phase skill outside the phase host and every planted plugin shipped exactly one skill, so round 1's narrowing was never exercised against the arrangement it has to survive. Fixed: a landing-page marker must name that page's entry skill, or a member it ships that the marked prose itself names, and a new specimen tree plants the phase host to prove it. Three shapes remain open and are not closable by a name test, which was measured rather than assumed: over 27 documents the tree holds three status markers, `plugins/probitas/README.md`'s two regions name no governed skill at all ("This version emits none"), and `plugins/anamnesis/README.md`'s names `elenchus` and `synkrisis` beside its own. So a pronoun subject, an alias, and a sentence naming two members still pass, and requiring the region to name its own skill would refuse two correct claims. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-04 | medium | `scripts/check_public_front_door.py` | Eight phrasings were left silent in S5-R1-04 as open vocabulary. Six of them are not: they are conjugations of predicates the rule already names. The grammar read `has not shipped` and not `has not been shipped`, `is not built` and not `was never built`, and its adverb slot held exactly one literal, `yet`, so `is not currently implemented` and `has not so far shipped` escaped a rule that reads `is not yet implemented`. This is not hypothetical: `plugins/tabularium/README.md` carries `the Phase 1 canonical adapter and Ethereum USDC specimen remain unimplemented`, a live member-status claim on a maintained landing page that the rule could not see. Fixed along four axes with the four predicates unchanged -- the passive auxiliary `been`, `never` as a negation, the past tense `was`, `were` and `had` beside `remains`, and a closed adverb list replacing the single literal -- plus the `un-` adjective forms over the same participles. Widening was measured against all 27 documents before it was written and produced exactly one new hit, the tabularium sentence, which is inside that page's generated region and exempt under FD36 because its ledger carries it. Two families stay out and are recorded rather than closed: different predicates (`is not available`, `is still to come`, `is planned, not present`), because guessing the predicate set is how a rule starts firing on ordinary prose; and subject negation (`No compile path has shipped`, `nothing has shipped`), which needs a noun phrase read backwards. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-05 | low | `scripts/check_public_front_door.py` | FD33 compares the marker with the ledger and nothing compared the marker with its own prose, so a sentence naming a superseded release passed under a marker standing at the current one. `<!-- front-door:status skill="probitas" version="probitas-v1.3.0" -->probitas-v0.1.0 has not shipped its scoring path.` exited 0. That is the stale-status failure the whole rule exists for wearing a version number. Fixed as FD37: a release token in the marked region that names the marker's own skill must equal the declared version. Scoped so it cannot fire on unrelated prose -- a token naming another skill is left alone -- and no marked region in the live tree carries a version token at all, so the rule is a guard rather than a repair. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-06 | low | `tests/test_public_front_door.py` | S5-R1-10 established that the heading exemption is sound for both files and pinned by no case, so a third could be added silently: `test_the_maintained_set_covers_the_named_pages_and_every_plugin` asserts only that each swept page carries some rule. Two cases now pin it. The first names the exempt set exactly -- `.agents/skills/promise-machine/SKILL.md` and `PROMISE_MACHINE.md` -- and asserts every swept page still carries the count rule. The second joins the exemption to its reason: `scripts/promise_machine.py` pins 14 exact sentence-case headings, and the case asserts at least one of them occurs in `PROMISE_MACHINE.md`, so upper-casing them reddens `python3 scripts/promise_machine.py check`, one of this step's own exit commands. Round 1's objection stands and is recorded rather than dodged: both cases pass on the parent `a7d8a492b4afa237266a229b2c975c5ced7af527` as well as on the fix, so they are characterisation guards that never went red. They are worth their place because the condition they pin is a silent widening rather than a behaviour change, and the round's verdict does not rest on them: 24 assertion failures on the parent come from the other cases. | fixed in 8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da |<br>| S5-R2-07 | low | `.hexaemeron/runbook.md` | The step's Contracts clause requires applying `plugins/hexaemeron/skills/brevitas/SKILL.md`, and no such file exists: brevitas ships as its own plugin at `plugins/brevitas/skills/brevitas/SKILL.md`, and `plugins/hexaemeron/skills/` holds elenchus, ephoros, fiat, fizz, hypomnema, imprimatur, kronos, metron, phylax, protasis, solidity-auditor, vulgate and x-ray. The other four paths the clause names resolve. The contract could not have been applied at the path written, and the runbook is controller-pinned, so this is a documentation gap for the record rather than something a fix commit can repair. It joins the Exit clause S5-R1-10 named, which still reads "Every ATX heading on that set is all caps" over a set naming both exempted files. Accepted for the integrate carryover. | accepted |<br>| S5-R1-06 | low | `scripts/check_public_front_door.py` | Re-decided against the refusal inventory this round changed, and it still stands, on different reasoning from round 1's. Round 1 deferred it because giving `ledger_version`'s failure a code "belongs with the refusal inventory"; I added FD36 and FD37 to that inventory and did not add a third here. The reason is that this is not an inventory gap. `FDnn` codes are rule refusals reported at exit 1, and the module docstring declares exit 2 for "an input cannot be read at all", which is what a ledger with no `- Current version:` row is. Giving it an `FDnn` would move a cannot-read condition into the reported-rule namespace. The asymmetry with FD01 is real and is the actual cost: an unreadable maintained document becomes a finding and the sweep continues, while an unreadable ledger ends the run, and a ledger only prevents the status rules for its own skill from being decided. Closing that means deferring the ledger reads to their point of use and deciding what a marker naming an unreadable ledger reports -- a change to when evidence is read rather than a hole through which something passes. Nothing passes through it. Carried to the integrate carryover, not fixed here. | accepted |<br>| S5-R1-07 | medium | `scripts/build_contributor_guide.py` | Carried from round 1 and re-checked on this tree. The builder still reads no repository Markdown despite its docstring naming `docs/how-to-help-shoggoth.md` as its source, and `python3 scripts/build_contributor_guide.py` rewrote the PDF to `b74a5bde75038b67692786b3806e31b06aa1990f0a97751d1d1d8c184f2b6c47` before and after, with `git diff --exit-code -- docs/pdf/how-to-help-shoggoth.pdf` exiting 0 and printing nothing. The step's own Exit therefore cannot detect a prose change in that source, and the byte-identical result is a property of the generator rather than evidence the PDF is current. `scripts/build_contributor_guide.py` is outside this step's Files clause. Needs an owner before the run integrates. | accepted |<br>| S5-R1-08 | low | `docs/pdf/the-promise-machine-explained-properly.pdf` | Carried from round 1 unchanged, because no Markdown moved this round. Fifteen ATX headings and one count sentence changed in `docs/the-promise-machine-explained-properly.md` during this step, the binary has no generator in this repository, and it did not move. Its page 7 reads `Six phase skills travel between jobs` while discovery derives 9 phase skills, and its specialist page names 13 domain skills against 17 specialist plugins. The gap is declared on the artefact -- every page footer reads `source snapshot 21 August 2026` and page 12 reads `Source boundary: local Wildcat Labs snapshot, 21 August 2026, Promise Machine v1; Hexaemeron package 1.5.3` -- so nothing in it claims to be current. Needs an owner before the run integrates. | accepted |<br>| S5-R1-09 | low | `plugins/hexaemeron/README.md` | Confirmed: the deletion was right, and the mechanism is now recorded rather than the conclusion. Round 1 said no third option existed inside this step's clause; there is no third option anywhere, because the quantity is not derivable. `shoggoth_topology` derives four keys and no more -- plugins 18, governed 27, canonical 18, phase 9 -- and the six phase disciplines are a subset of those 9 that the tree does not distinguish from the two prose masks and the loop. A `disciplines` count key would need a hard-coded list of six names, which is the literal this contract exists to remove. The surviving `of which six ship an executable check` is genuinely derived and stays checked: `plugins/hexaemeron/tests/test_fiat_skill.py` counts which of its six named phase skills ship `skills/<name>/scripts/<name>.py` and asserts the README's word against that count, which is a question about the tree rather than about the size of its own list. Both reworded sentences are accurate as they stand. | accepted |<br>| S2-R1-03 | medium | `docs/decisions/ADR-076-govern-real-data-demonstrations-separately.md` | Carried from step 2 round 1 and re-checked. The root suite fails `test_decision_records.DecisionRecordNumbering.test_no_number_collides_with_one_already_on_the_default_branch` because step 1's ADR-076 collides with the record already on `origin/main` at `33085188`. This is the single failure in every root-suite figure below and the single failing check in `run_checks --full` on the fixes commit. `docs/decisions` is outside this step's Files clause and the run disposes of it at `sync-run`. | accepted |<br><br>Leads not pursued: The reviewed range is `a7d8a492b4afa237266a229b2c975c5ced7af527..8abd87edc9e80a94044d1c6024f7a9eb1d2ff4da`, one signed commit over three files. Every construction below was built on the round-1 tree and passed there before it was fixed, so each names a claim the sweep read and reported nothing about. Mechanical exits over the changed paths: phylax 0, ephoros 0, hypomnema 0 with `docs/decisions` in scope. Imprimatur has no changed Markdown to read until this record lands, and it is run over this file and its synopsis in the same commit. The step's Exit commands on the fixed tree: `scripts/check_public_front_door.py --root .` 0, reporting 27 documents and 4 checked cards; `tests.test_marketplace_prose` 23 of 23; `tests.test_public_front_door` 65 of 65, up from 53; `tests.test_shoggoth_identity` 6 of 6; `scripts/promise_machine.py check` 0 printing `clean: 18 plugin(s), 18 copy/copies`; `scripts/build_contributor_guide.py` 0; `git diff --exit-code -- docs/pdf/how-to-help-shoggoth.pdf` 0; `scripts/portable_promise_machine.py package` 0 into a temporary directory; `tests.test_skills_sh_package` 13 of 13; `horos.py check .` 0 printing `boundary matches the tree` with no candidate drift; and `git diff --check` 0. Horos was refreshed with `scan . --write` and `scan . --write --census`: `.horos/boundary.json` needed no change, `.horos/census.json` moved and is committed with the fixes, and the census reports 2580 files over 94459481 bytes. Root suite on the fixed tree: 1,326 of 1,327, the one failure being S2-R1-03. `run_checks.py --full --jobs 6` on the audited tree `a7d8a492`: red, 28 of 30, report SHA-256 `75bb9bc93056f51549c5777197087cff6a8abe229feaf6e3094f5f0fb4ab0986`, with `root-suite` failing on S2-R1-03 and `hexaemeron-suite` failing at 1800.334s with exit code -15 and reason `timeout`. That second failure is load and not the diff: the same suite run alone on the same tree returned 2204 of 2204 in 892.1s at exit 0, and it passed inside the next full run at 1173.7s. `run_checks.py --full --jobs 6` on the fixes commit `8abd87ed`: red, 29 of 30, `root-suite` the only failure with class `test-failure` and exit code 1, its 1,327 tests reporting the same single S2-R1-03 failure, `front-door-check` passed, report SHA-256 `e821fe00fb8df8a397107bc1b5be4d14044788fc8249dc33b39fbd2ce4b9a4d2`. Elenchus was established directly before it was run: the changed test file alone, overlaid onto a detached worktree at `a7d8a492`, produced 24 assertion failures and zero errors over 65 tests, spread across every new guard -- 9 status conjugations, 4 adverb forms, 3 malformed capture attributes, 2 unreachable refusals with the inventory mismatch beside them, and one each for the unrecorded capture date, the two region claims, the sibling voucher and the version mismatch; the two negative controls and the two heading-exemption cases pass on both trees. The declared runner `python3 tests/run_tests.py --elenchus-report {report}` under format `unittest-json-v1` and schema `elenchus.unittest.v1`, writing `.elenchus/front-door-derived-counts-step-5.json`, then returned `guarded` for `8abd87edc9e8`. The audit file was 173,443 bytes at SHA-256 `82edf9c4049e97aae0fa8e85db2a7cc1ebb5e0d31bc45a8962669b094cb92d98` before this record, carrying 15 records ending at `## Step 5, round 1 -- 2026-09-06T01:22:25Z`. Step 4's establishment was re-probed and holds: a second image on the front door still reports FD12, a 400-word comment still costs no words against the introduction budget, a `#` line inside a fence is still not a heading, a count marker quoted in a fence is still live and reports FD26, and all three region refusals -- unclosed, over-reaching a governed heading, and declared on the wrong page -- still report FD30 with the refused region not applied. FD12 and the card rules are still front-door rules only, as round 1 recorded. Five leads were seen and left. First, `check_regions` runs before every rule and its spans are now rule-scoped, so a page could in principle open a `contributors` region it is entitled to and exempt a count under it; the front door is the only page entitled to one, `scripts/contributors.py` owns those bytes, and no count claim sits inside the live region. Second, the FD36 escape compares a flattened sentence against a flattened ledger, so an author who copies a claim out of `EVOLUTION.md` and leaves it there after the ledger moves keeps the exemption until the ledger row is rewritten; the ledger is the source of truth for its own frontier and this checker does not police it. Third, `claim_sentence` splits on `.` and `;` followed by whitespace, so a claim inside a sentence carrying an abbreviation would take a truncated sentence into the comparison and fail closed into FD36 rather than open. Fourth, FD37 reads a version token only when it begins with the marker's own skill id, so a claim naming a superseded release by date or by revision name rather than by version is not read. Fifth, the historical exemption is now bounded by a date the page records, and an author who writes that date into prose can still exempt a number about now; closing that needs the checker to read tense, which it cannot. Audit filter `sapheneia:sapheneia` preserved every finding, severity, risk disposition, count, exit code, digest, commit, timestamp, verdict, status, negative result and unpursued lead; no Horos reading exclusion was applied.
