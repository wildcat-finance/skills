## Step 1, round 1 -- 2026-09-01T12:09:58Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=not-applicable; unreachable-merge-admitted=not-applicable; ancestry-unanswered=not-applicable; second-merge-suppressed=not-applicable; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=not-applicable; diagnostic-overclaim=reviewed; github-verification-gap=not-applicable; integration-gates-intact=not-applicable; verify-replay-drift=not-applicable; legacy-run-compatibility=not-applicable; regression-uses-disposable-repo=reviewed

Not checked: every controller behaviour steps 2 through 5 introduce. This step commits three documents and touches no code, so the nine not-applicable concerns have no surface here yet. Also not checked: live GitHub merge and integration operations, and whether the selected design behaves as declared once written; the design record measures declared change sets against the real tree and `measure_design.py` states that boundary itself. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run. Also not checked: the cause of the pre-existing plugin-suite failure named below beyond the assertion it raises and the commit that introduced the unowned path.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the Hexaemeron plugin suite is not green at this run's base. `test_check_runner.CheckMapContractTests.test_every_tracked_path_has_exactly_one_owner` fails at `plugins/hexaemeron/tests/test_check_runner.py:72` with `AssertionError: Lists differ: ['VENUES.json'] != []`, because commit `6b91ad96` added a tracked top-level `VENUES.json` and linked it from every governed ledger without registering the path in `tests/check-map-v1.json`, where `grep -c 'VENUES.json'` returns 0. It reproduces at the unmodified run branch tip with none of this run's commits applied, at 2157 tests, 1 failure and 0 error events, identical to the count on the step branch, so this step neither introduces nor worsens it. It is outside this run's scope, no open issue covered it on 2026-09-01, and it has been flagged for separate work rather than repaired here. It is the same canary that issue 923's record left unpursued, then for 14 Homologia paths. Step 1's Exit and Tests were amended to name it and to require its count not to rise, and to name the repository-wide suite explicitly, which issue 1067 records as the gap behind a green receipt on a red branch. Evidence for the covered concerns: the three lints exit 0 over all four paths of the step diff and report clean having read real paths rather than an empty argument list; the imprimatur lint scores 100.0 with 0 defects on each of the three documents; every relative link in the three documents resolves; the repository-wide suite reports 1110 of 1110 OK at exit 0; the whole-set audit synopsis currency check exits 0 across 54 sources, which is the number the study states as observed. `early-merge-indistinguishable` is answered by ADR-068 fixing the receipt field name `early_merge` and stating why an inferred early merge does not satisfy issue 1021. `diagnostic-overclaim` was re-checked against the tree at `79072bef`: all twelve cited source line numbers still match, including 11317 for the head refusal, 11330 for the early-merge refusal and 14480 for the `--merge-commit` registration, and `sp = sub.add_parser("done", ...)` still holds that flag with `phase` as a positional choice. The two accuracy defects a previous attempt at this step found, the prior-art citation and the audit-source count, are folded into the source text rather than carried as amendments. `regression-uses-disposable-repo` is answered by confirming `plugins/hexaemeron/tests/hexctl_harness.py` is present. Every commit in this step's range is signed with key `B83B60AE16F5DD1A` and GitHub reports `verified: true` with `reason: valid` for the pushed head, with author resolving to `shoggoth-wildcat` and committer to `laurenceday`.

## Step 2, round 1 -- 2026-09-01T13:01:51Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=not-applicable; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=not-applicable; verify-replay-drift=not-applicable; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=not-applicable

Not checked: `done merge-step` behaviour and `hexctl verify` replay over an adopted receipt, which are step 3's exit and cannot be stood in for here; the disposable-repository end-to-end sequence, which is step 5's. Also not checked: the end-to-end unanswered-graph-query path. The fixture's only way to silence `merge-base` is a mode that also silences the range check running before it, so the run would refuse for the wrong reason; the unit case proves the answer is `None` rather than `False` and a source assertion proves the consuming branch refuses about reachability, but no run has been driven through that refusal. Also not checked: whether one bounded fetch is enough in every repository configuration, since no test exercises the fetch branch; under the fixture the first ancestry call answers, so the fetch is never taken. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | The push receipt records `github_merge_verified: []` beside an adopted merge that had been GitHub-verified, with the verification recorded inside `early_merge` instead. The field is written once and read nowhere, so no behaviour was wrong, but an empty list reads as "no merge was verified", which is the opposite of what the receipt establishes. The two legacy fields are scoped to what the arguments carried, and for a stacked step that is nothing because `done push` refuses `--merge-commit` there, while an adopted merge is an observation and stays with its own verification in its own block. That scope is now stated where the receipt is written rather than left to be inferred. The controller digest moved with the comment, so the coverage inventory's nine fiat runtime bindings, its run-observation controller pin and `INTEGRATED_CONTROLLER_SHA256` were re-pinned with it. | fixed in c7805e5c367903de92677bb4e9f3706dca1a8115 |

Leads not pursued: the pre-existing `test_check_runner.CheckMapContractTests.test_every_tracked_path_has_exactly_one_owner` failure stands, unchanged and unworsened: its unowned-path list is still exactly `['VENUES.json']`, and this step's new tracked module is covered by an existing prefix rule. Evidence for the covered concerns: the three lints exit 0 over all eight paths of the step diff, reading real paths rather than an empty argument list; the focused module reports 14 of 14 OK through its own `--elenchus-report` mode; against the unpatched controller 12 of those 14 fail, and the two that pass are the regression guards for behaviour this step preserves, the existing head refusal and the inertness of a refusal; the root suite reports 1110 of 1110 OK at exit 0; the plugin suite reports 2171 tests, 1 failure, 0 errors; the promise coverage check reports clean at 88 of 88; `portable_promise_machine.py check` and `horos check` both exit 0. `head-mismatch-admitted` is answered by the check at line 11317 running before the adoption branch and by a test that moves the pull request head and asserts the refusal still names the head. `unreachable-merge-admitted` is answered by `adopted_merge_base_tip` and a test asserting the refusal names both "not reachable from the recorded base" and "rewritten ref". `ancestry-unanswered` is answered by `_ancestry_answer` returning `None` for a genuinely absent object against real Git objects, and by the two answers being distinguishable for the same pair. `early-merge-indistinguishable` is answered by the recorded block and a test asserting its merge commit, its base ref and its GitHub verification. `state-mutation-on-refusal` is answered by a test comparing state and ledger digests across a refused adoption. `github-verification-gap` is answered by a test driving the fixture's `verified-false` mode and asserting the refusal. `legacy-run-compatibility` is answered by `adopt_early_merge` defaulting to false and by a source assertion that the original refusal string is unchanged behind it. `diagnostic-overclaim` was this round's finding and also the round in which the study's claim that `done push` is not merge-blind was withdrawn, after `done_push` was read rather than only `inspect_pull_request`: a stacked step refuses `--merge-commit` at line 6722, before the call at 6773, so the merge-aware branch cannot be entered and issue 1021 was correct.

## Step 2, round 2 -- 2026-09-01T13:12:31Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=not-applicable; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=not-applicable; verify-replay-drift=not-applicable; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=not-applicable

Not checked: the same three the previous round named and for the same reasons, `done merge-step`, `hexctl verify` replay over an adopted receipt and the disposable-repository sequence, all of which belong to later steps; the end-to-end unanswered-graph-query path, which the fixture cannot isolate; and the fetch branch, which the fixture never takes because its first ancestry call answers. The recorded non-Solidity waiver excludes the Pashov suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the pre-existing `test_every_tracked_path_has_exactly_one_owner` failure stands, with its unowned-path list still exactly `['VENUES.json']`. Evidence for the covered concerns: the three lints exit 0 over all ten paths of the step diff; the imprimatur lint scores 100.0 with 0 defects on each of the five changed documents, including this record and its synopsis; the root suite reports 1110 of 1110 OK at exit 0; the plugin suite reports 2171 tests, 1 failure and 0 errors, unchanged from the base count; the promise coverage check reports clean at 88 of 88 after the digest re-pin that round 1's fix required; `horos check` reports the boundary matches the tree; the worktree is clean. The finding from round 1 remains fixed at `c7805e5c`, and re-reading the adoption path, the receipt fields and the two refusal messages against the tree surfaced nothing further.

## Step 3, round 1 -- 2026-09-01T20:58:56Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=reviewed; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=reviewed; verify-replay-drift=reviewed; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=not-applicable

Not checked: the disposable-repository end-to-end sequence, which is step 5's exit; the end-to-end unanswered-graph-query path, which the fixture cannot isolate because its only way to silence `merge-base` also silences the range check running first; the fetch branch inside `adopted_merge_base_tip`, which the fixture never takes because its first ancestry call answers. Also not checked: whether a replacement pull request actually recovers the case the new refusal names. The refusal states what is required and stops; no run has been driven through that recovery, and this run does not implement it. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | high | plugins/hexaemeron/skills/fiat/scripts/hexctl.py | `done merge-step` satisfied an adopted step from its record whether or not the run branch carried that step's work. An early merge lands in the base the pull request targeted: the run branch for the bottom of the stack, but the step below for anything above it. Whether the run branch ends up holding the work then depends on which of the two merges happened first, which is an order nobody chose. Issue 1021's own run is the unfavourable case, with 1014 landing on the run branch at 2026-08-31T05:03:41Z and 1017 landing on step 1's branch at 05:42:09Z, so the run branch never received step 2. A complete adoption record does not make the work present. Left as written, the receipt would have claimed a landing that did not happen, and the run would have dead-ended later at integration on a remote tip not matching the final recorded step merge, with the cause pointing at the wrong thing. Fixed by gating satisfaction on the adopted merge being reachable from the run branch, using the same bounded native ancestry primitive, and refusing otherwise with a message naming where the merge did land, that the closed pull request cannot supply the missing merge, and that a replacement is required. The guard test fails against the ungated controller, which was confirmed by removing the gate and observing the receipt succeed at exit 0 where the test expects 2. | fixed in 9a0ab8979610bf0045ee59b3c377f817f1bae6b9 |

Leads not pursued: recovering a step whose work is not on the run branch. The new refusal names what is needed, a replacement pull request bringing the step branch onto the run branch, but nothing here performs it, so issue 1021's acceptance condition is met for a step whose early merge landed on the run branch and not for one that landed below it. That gap is carried forward rather than closed. The pre-existing `test_check_runner.CheckMapContractTests.test_every_tracked_path_has_exactly_one_owner` failure also stands, its unowned-path list still exactly `['VENUES.json']`; a separate session was started for it and did not complete, so it remains. Evidence for the covered concerns: the three lints exit 0 over all four paths of the step diff; the focused module reports 23 of 23 OK; the root suite reports 1110 of 1110 OK at exit 0; the plugin suite reports 2180 tests, 1 failure, 0 errors; the promise coverage check is clean and `horos check` reports the boundary matches the tree. `second-merge-suppressed` is answered by `recorded_adoption` refusing a partial record, by the new reachability gate, and by a test asserting that a verification naming a different commit refuses, because verified-something is not verified-this. `verify-replay-drift` is answered by a test that drives an adopted merge through `done merge-step` and then runs `hexctl verify` over the resulting ledger. `legacy-run-compatibility` is answered by `recorded_adoption` returning `None` for a receipt with no `early_merge` and for one holding an explicit null, so a run from before this change infers nothing and still owes an ordinary merge. `integration-gates-intact` is answered by the step-order, run-branch-movement and rewritten-stack checks being untouched, by GitHub still being read with the base the pull request actually merged into, and by issue 923's own module still reporting 10 of 10. `diagnostic-overclaim` is answered by the new refusal naming only what was observed, the ref the merge landed in and the tip it was compared against.

## Step 3, round 2 -- 2026-09-01T21:05:48Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=reviewed; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=reviewed; verify-replay-drift=reviewed; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=not-applicable

Not checked: the same four the previous round named and for the same reasons, the disposable-repository sequence belonging to step 5, the end-to-end unanswered-graph-query path the fixture cannot isolate, the fetch branch the fixture never takes, and whether a replacement pull request recovers the case the new refusal names. The recorded non-Solidity waiver excludes the Pashov suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the two this step carries forward stand unchanged, recovering a step whose work is not on the run branch, and the pre-existing unowned `VENUES.json` path. Evidence for the covered concerns: the three lints exit 0 over all six paths of the step diff; the imprimatur lint scores 100.0 with 0 defects on both changed documents; the root suite reports 1110 of 1110 OK at exit 0; the plugin suite reports 2180 tests, 1 failure and 0 errors, unchanged from the base count; the promise coverage check is clean after the digest re-pin the round 1 fix required; `horos check` reports the boundary matches the tree; the worktree is clean. Round 1's finding remains fixed at `9a0ab897`, and re-reading the adoption reader, the reachability gate, the merge record and the directive against the tree surfaced nothing further.

## Step 4, round 1 -- 2026-09-01T21:46:26Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=reviewed; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=not-applicable; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=reviewed; verify-replay-drift=not-applicable; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=not-applicable

Not checked: the disposable-repository end-to-end sequence, which is step 5's exit. This step changes documents, one ledger row, the checkpoint compatible-version set and digest-bound derivatives; it adds no controller behaviour, so the concerns above were reviewed as descriptions against the code rather than as new surface. Also not checked: whether the carried token counts would be identical under a fresh measurement. The measurement and parity records describe artefacts that are byte-identical in length and differ only in an embedded 64-character digest, and the checker carries those counts rather than deriving them, but no tokenizer adapter was available to re-measure and `measure` refuses with `WAI-E-ADAPTER.UNAVAILABLE`. The recorded non-Solidity waiver excludes the Pashov suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the two this run carries forward stand, recovering a step whose work is not on the run branch, and the pre-existing unowned `VENUES.json` path. A third is added: the receipt table in `SKILL.md` still describes the push and merge-step directives without the adopted-merge case, because those two rows sit inside the byte range the agent-instruction corpus measures and editing them changes a recorded token count nothing here can regenerate. The phase notes and `push-discipline.md` carry the transition instead, and the table stays byte-identical deliberately. Evidence for the covered concerns: the three lints exit 0 over all sixteen paths of the step diff; the imprimatur lint scores 100.0 with 0 defects on each of the five changed documents; the root suite reports 1110 of 1110 OK at exit 0 post-commit, having gone through 23 and then 8 failures as each digest-bound derivative was re-pinned; the plugin suite reports 2180 tests, 1 failure, 0 errors; `agent_instruction check` exits 0 with 21 OK records and no faults; the promise coverage check, `portable_promise_machine check` and `horos check` all exit 0. `diagnostic-overclaim` was the substance of this round and every load-bearing claim in the new prose was checked against the tree rather than assumed: the head check does precede the adoption branch by source position, the unanswered path does refuse as undetermined rather than as a denial, a stacked step does still refuse `--merge-commit` so there is no flag for an operator to know about, and the merge record does carry the mechanism that satisfied the step. The ledger row was checked the same way: the frontier line recomputes to `e413d6041edb34b3807a54019489605814a591f60547755f8f66f01830f643aa`, which is byte-identical to the digest the previous row records, the axis is `generation`, the revision is unchanged, the header and the newest row name the same version, and the held issue 363 job is untouched. The evolution-contract test passes over the new row and its newest-row assertions, and the previous row keeps its own pins under a by-version block.

## Step 5, round 1 -- 2026-09-01T21:59:50Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=reviewed; unreachable-merge-admitted=reviewed; ancestry-unanswered=reviewed; second-merge-suppressed=reviewed; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=reviewed; diagnostic-overclaim=reviewed; github-verification-gap=reviewed; integration-gates-intact=reviewed; verify-replay-drift=reviewed; legacy-run-compatibility=reviewed; regression-uses-disposable-repo=reviewed

Not checked: the sequence against a live GitHub. The repository the regression drives is real and disposable, created per test in a temporary directory, and the harness states plainly that its fake git "covers signatures, refs and pull requests; it cannot stand in for repository structure". So repository structure, the worktree, branches and the controller's own transitions are exercised for real, while the pull-request payloads, remote refs, signature verdicts and merge answers are recorded fixtures. No test drives a live service, and none could. Also not checked: whether one bounded fetch suffices in every repository configuration, since the fixture answers the first ancestry query and never takes the fetch branch; and whether a replacement pull request recovers the case step 3's refusal names. The recorded non-Solidity waiver excludes the Pashov suite.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | low | .hexaemeron/design-evidence.json | The pending conformance cell declared its resolver as `python3 plugins/hexaemeron/tests/run_tests.py --report {report}`, which is wrong twice over: the suite runner takes `--elenchus-report` rather than `--report`, and its positional argument is the report path rather than a test selector, so that command cannot scope to this module at all. The evidence was produced by `python3 plugins/hexaemeron/tests/test_early_step_merge.py --elenchus-report .hexaemeron/test-reports/step-5.json`, which the report records in its own `command` field, and the checker validates that field as bounded text rather than against the declared resolver, so the record passes at the integration transition either way. The declaration cannot be corrected: the design record is immutable after the study receipt, and an ordinary amendment may not change its criteria. Accepted, with the exact command that ran recorded here and in the report. | accepted |

Leads not pursued: the three this run carries forward stand, recovering a step whose work is not on the run branch, the receipt-table gap inside the measured byte range, and the pre-existing unowned `VENUES.json` path. Evidence for the covered concerns: the three lints exit 0 over the step diff's single path; the focused module reports 25 of 25 OK through its own reporter with zero failures and zero errors; the root suite reports 1110 of 1110 OK at exit 0 post-commit; the plugin suite reports 2182 tests, 1 failure, 0 errors; `design_evidence.py --transition integration` exits 0, so the record's last pending cell is resolved. `regression-uses-disposable-repo` is this step's substance: the end-to-end case merges step 1's pull request before its push receipt, receipts the push anyway, completes step 1 from its adopted record, merges step 2 ordinarily, and reaches the integrate directive, then asserts that the head the push receipt named is unchanged, that the step branch tip is where it was, and that each merge record names the mechanism that satisfied it. A second case asserts the part the 972 recovery needed a person for: no replacement pull request is opened and the recorded merge is the one the original pull request carried. Every other concern is reviewed through that sequence rather than only in isolation, which is the point of driving it end to end, because the original failure was not any single refusal but that there was nowhere to go after one.
