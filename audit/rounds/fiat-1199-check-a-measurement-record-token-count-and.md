## Step 1, round 1 -- 2026-09-18T02:45:07Z

Audit schema: fiat-audit-round/v2

Covered: count-anchor-reachability=not-applicable; disabled-path-unchanged=reviewed; synthetic-fixture-fidelity=not-applicable; identity-cause-separation=reviewed; honest-remeasure-not-trapped=not-applicable; coverage-register-currency=reviewed; issue-1192-surface-untouched=reviewed

Not checked: the Pashov pair, waived at init for a run producing no Solidity; the interval between `report_destination`'s `resolve()` and `write_report`'s later `os.replace`, so a same-UID attacker swapping a path component mid-run is uncovered; Steps 2-6, none of which is built; hosted CI, push and controller receipt.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | tests/prove_measurement_guards.py | `scratch_directory`:267-282 justifies diverging from the repository's fixed `tmp/` anchor on the premise that the destination is `.hexaemeron/design/` and therefore gitignored. `report_destination`:244-264 does not enforce that premise: it constrains containment in ROOT, the `--identity-cause-attribution.json` suffix and a `CANDIDATES` prefix, never the directory. `--report <tracked-dir>/source-pinned-count-commitment--identity-cause-attribution.json` is accepted, and `write_report`:296-301 then creates `.report-XXXXXX` plus any missing parent chain in a status-visible directory: the transient `??` class #622 S3-R1-01 recorded. `tests/test_scratch_quiescence.py:54` matches the enclosing function name, never the `dir=` target, so it cannot see this. The docstring's other reason misstates the mechanism: `os.replace` across directories on one filesystem is an atomic rename, not a cross-directory copy; a same-directory anchor avoids cross-device `EXDEV`, which raises rather than copying. Demonstrated by direct call; unreachable on the intended path, where the caller supplies `.hexaemeron/design/reports/`. | accepted: the file is digest-pinned by the runbook's `command-interfaces` fence, so any change is a further runbook amendment; no reachable impact and no boundary crossed |

Leads not pursued: hypomnema study mode on the shipped `docs/fiat-1199-measurement-guards-study.md` returns H008 "study has no design bridge block", exit 1, and Hypomnema places the standing record before the step that ships a study is receipted; the step defers it to Step 5, whose Exit creates `docs/decisions/drafts/pin-measurement-counts-in-checker-source.md` and adds the fence to both study copies, and the step's stated reason, that writing it here would name a design Steps 3 and 4 have not built, holds, so it is left as Step 5's obligation. The stand-in runtime's shebang is `sys.executable` as an absolute path, 46 bytes here, so a host with a longer interpreter path would fail the specimens on the kernel's shebang limit. The report lands at mode 0600 because `mkstemp` creates it so and `os.replace` preserves it, identical in the baseline bytes, so not a change this step made. Round evidence, which this schema has no field of its own for: implement commit 837543aaa20e9aacf14ad07ce2da425d7534987a, signed, `%G?`=G, signer laurence.e.day (github-signer), key B83B60AE16F5DD1A; root suite `python3 -m unittest discover -s tests` ran 2049 tests, OK, exit 0; phylax 0, ephoros 0, hypomnema 0 over the changed paths. The runbook amendment was verified rather than accepted from prose: removing `scratch_directory` and restoring `tempfile.mkstemp(dir=str(target.parent), prefix=".report-")` at its one call site reproduces exactly 6aef210ebe7c6a76cb228feb0ac94673a92676f088b0dfe6db1099d5f57168d2, the runbook's baseline digest, so no second edit rode along in the committed ecc983e8b85627a2f962144edadbf5a61fa11025baacd60d7c4fbf17336a30ef; the baseline bytes exist nowhere in git, and that digest match is what establishes the claim. Both versions produce byte-identical stdout 385abd40, stderr e6f893ca and exit 1, and `write_report` called directly on each yields the same report bytes at digest 3f7c9783, mode 0600, no residue and the same failure type, so the one changed function is behaviourally identical; the end-to-end run alone would not have shown this, because exit 1 never reaches `write_report`. `anchored_scratch_violations` reports `mkstemp(dir=...) outside scratch_directory` on the baseline and clean on the committed bytes, and `tests.test_scratch_quiescence` passes at 4 tests. Path probes on `report_destination` found no bypass: `..` traversal, absolute-outside, a symlinked directory leaving ROOT, case-folded candidate, case-folded criterion, suffix-as-substring and prefixed candidate all refuse, and `resolve()` precedes `relative_to(ROOT)`, which is the sound order. No receipted selection report is reachable: all 20 under `.hexaemeron/design/reports/` are named for the five selection criteria and `identity-cause-attribution` is not among them. An audit hook over a full specimen run recorded 2 reads and 0 writes under `tests/fixtures/agent-instruction-v1/`, its tree digest 89ef5ae1 unchanged, and 8 child spawns, each the disposable stub at mode 0700 inside a 0700 system-temp parent, with argv carrying only plain 64-hex blob digests through `_argv`, which caps length and rejects NUL, under `shell=False`. `_verify_profile_identity` was read at `scripts/agent_instruction.py:1589-1668`: the whole-text `acquisition_sha256` comparison at :1650 precedes the `MODEL_BLOB_RE` projection at :1656, so both specimens reach `WAI-E-ADAPTER.IDENTITY_CHANGED at $.profile.acquisition_sha256` and the resolver exits 1 today, which is skills#1098's S3-R1-02 and the state Step 4 addresses. The commit touches no path under `scripts/` or `tests/fixtures/`, so `disabled-path-unchanged` and `issue-1192-surface-untouched` hold by absence. `tests/promise_machine_coverage.json` is keyed by contract domain, no row's file changed, and `tests.test_promise_machine_contract` passes at 243 tests. The tracked docs match the receipted artefacts byte for byte at study 590121a8c8d4f71e3c9bd771e2f7e652dab9e6781b39f12d427c0a5c5cf0c3f1 and runbook 5751a7f79267857dfcaa4b3cfca675c16e533ddbb5a6d5281a33d11a113923d1, and the Horos deltas account for the three added files and nothing else: `.md` +2 files +54777 bytes, `.py` +1 file +15726 bytes, total +3 files +70503 bytes, `files_walked` +3.
