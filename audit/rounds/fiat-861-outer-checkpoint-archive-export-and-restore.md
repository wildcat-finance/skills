## Step 1, round 1 -- 2026-09-07T21:18:19Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity security suite is waived in the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 ships no controller code, so no register id was exercised against executing behaviour: every disposition above is a review of the contract text this step ships against the study's register, and `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not yet exist to run. The 35 hostile fixtures, the seven exporter refusal tests and the restore tests belong to Steps 2 to 4. `subprocess-argv` and `concurrent-mutation` are reviewed against reference text that names `bounded_tool` with fixed argv and the `verify_run` run lock without restating the register's no-shell, dash-prefixed-ref, stable-stat-and-re-read and same-account clauses; that gap is a lead below, not a claim of coverage. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5, so the six declared limits are checked as values and derivations only. Nothing under `.hexaemeron/` was edited and no receipted artefact was amended.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `docs/fiat-checkpoint-archive-study.md` | The study copy this step ships carries no `design-bridge` fence, so `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exits 1 with `H008 study has no design bridge block`, leaving the join from selected candidate `native-subcommands` to its decision home unchecked by machine while the ordinary walk exits 0. A probe copy of the same bytes carrying the three fence rows `schema` `hypomnema-design-bridge/v1`, `decision` `native-subcommands` and `record` `docs/decisions/drafts/build-the-outer-checkpoint-archive-natively.md` exits 0 against the same design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, so the fence resolves from this step's head onward; the probe was removed and the worktree left clean. | open, not fixed in this round: the tracked copy must stay byte-identical to `.hexaemeron/study.md` (`173fe658f240f55fb27faa44554f36ea23d77ab5dde38ffa9079bb35b163bc12`) and only Fiat may amend a receipted artefact, so the remedy is a dated study amendment adding those three rows, then re-copying both tracked artefacts |

Leads not pursued: The reference carries five register controls by pointer rather than by statement: no-shell, path-validated-before-argument and dash-prefixed-ref-impossible for `subprocess-argv`, and stable-stat-and-re-read plus the same-account boundary for `concurrent-mutation`; it also omits the `partial-publish` clause that the sidecar is written before the rename, which the runbook's Step 2 and Step 3 exits both state. The runbook's Step 1 exit does not require any of the five, so they are recorded rather than raised, and the repair is additive study-sourced prose in `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md`. Two budget derivations are roundings rather than exact products: `checkpoint.archive.bytes` 201581002 doubles the study's 100,790,501-byte stored pack of the bundle alone, which excludes the 3,212,143-byte capsule, and `checkpoint.archive.expanded_bytes` 209715200 is 200 MiB where 2 x (100,790,361 + 3,212,143) is 208,005,008; both stay above the roughly 104.0 MB archive the layout implies, and the reference reproduces the study's derivation strings verbatim, so neither ceiling is tighter than it reads. The study copy names the producer's absolute origin path four times, matching nine other tracked studies under `docs/`, so it is convention here and not a new class of leak. Verified positives, none of which extends past this step's documents: `cmp` exits 0 and the digests match for both tracked artefacts (study `173fe658f240f55fb27faa44554f36ea23d77ab5dde38ffa9079bb35b163bc12`, runbook `5fc8abcf11c3900ed37d09bac44b9158ceb9b42b93f2144c02fad01ee76aab25`); all three of the reference's fenced blocks are byte-identical to the study's commands, store-path and nine-entry layout blocks; the 13 manifest fields and every closed subfield agree with study section 1; the 24 refusal classes and 35 fixture ids equal the study's sets with no repeat; the refusal table's stage column matches the runbook step by step at 11 archive, 19 inspect and 7 restore classes; the ceilings and the six secret patterns agree item for item; all six budget rows are byte-identical to study section 10 and to `checkpoint-archive-budgets.json`, which declares exactly six `lower_is_better` limits at variance 0.25; the `## Open items` list names only values the study genuinely leaves open; the ADR-028 amendment is 18 insertions and 0 deletions after the 2026-09-02 amendment, cites the record once by stable slug, and leaves the status Accepted; the draft record carries the `# Decision:` heading, `Proposed, 2026-09-07.`, all five sections, the three commands verbatim and the B, C and D readings; and `fiat-controller-checkpoint-restore/v1` in the reference equals the controller constant at `hexctl.py:435`. The five tests are not vacuous: 13 mutations on a detached copy at `/tmp/fiat-861-mut-w1`, since removed, each failed its intended test, covering a dropped refusal-class row, a dropped fixture id, study-side class drift, a changed budget limit, a renamed budget in the reference table, a removed 'no operative clause' sentence, a relative link replacing the stable slug, an ADR-028 status flip, a dropped command, a changed measured reading, a lost dated status, both not-the-decision phrases and an injected dead relative link. The Hexaemeron manifest discovers 2,436 tests including the two new ones, and `-k hostile` and `-k restore_from_archive` still select 0, so Steps 3 and 4 keep their pinned counts. Phylax, Ephoros and the ordinary Hypomnema walk each printed `clean` and exited 0; imprimatur scored 100.0/100 with 0 defects and brevitas exited 0 on all three changed prose files; `horos.py check .` reported `boundary matches the tree` and `git diff --check` exited 0; the five step tests ran 5 and passed; the step commit `edd0ddf6` is `G` with author Shoggoth, committer Laurence and exactly one of each trailer. The reviewed diff is 10 files, 2,750 insertions and 9 deletions at patch SHA-256 `4aaea6dd7deb521d5587f23db405cef426caf687a24f326bfc56ea54cec61c72`, with no Git attribute on any changed path. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, and the study's own measurements from 2026-09-06 were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 2 -- 2026-09-07T22:53:03Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: This round raised no new finding; the table's one row carries round 1's S1-R1-01 as fixed, so the row count is one and the new-finding count is zero. The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code, so no register id was exercised against executing behaviour: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above is a re-review of contract text that did not change: `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` is byte-identical to round 1 at `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, and the amended study's `risk-register` fence is byte-identical to the packet's `risk_register.markdown` and carries the same 25 unique ids. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5. Nothing under `.hexaemeron/` was edited and no receipted artefact was amended here; both tracked copies were re-copied, never hand-edited. No `hexctl` command was run, so controller state was not read: which five of the runbook's ten amendment blocks bind was established from the packet's digests and from block order in the append-only file, not from `steps.json`. The Elenchus verdict below is the declared result of one bounded check whose declared runner never started, because the check short-circuited on a commit that changed no test file, so no report bytes exist and none are attested.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `docs/fiat-checkpoint-archive-study.md` | Carried from round 1: the tracked study copy carried no `design-bridge` fence, so `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exited 1 with `H008 study has no design bridge block`, leaving the join from selected candidate `native-subcommands` to its decision home unchecked by machine while the ordinary walk exited 0. | fixed in 95d5e6984e74faee8c4ff62ba440f78de6f4624e: Fiat's dated study amendment appended the fence, and both tracked artefacts were re-copied from `.hexaemeron/` with no hand edit. `cmp` exits 0 for each pair; the tracked study is now `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the tracked runbook `607335ddbc1b2765803f66e6adefd5d932c0e02c49e4ca09cd968a9feaf1e008`, the two receipted digests, where round 1 recorded `173fe658f240f55fb27faa44554f36ea23d77ab5dde38ffa9079bb35b163bc12` and `5fc8abcf11c3900ed37d09bac44b9158ceb9b42b93f2144c02fad01ee76aab25`. The same study-mode command now exits 0, and the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` exits 0. |

Leads not pursued: Round 1's two leads stay leads, and I agree with Fiat's position on both. The reference still carries five register controls by pointer rather than by statement -- no-shell, path-validated-before-argument and dash-prefixed-ref-impossible for `subprocess-argv`, stable-stat-and-re-read and the same-account boundary for `concurrent-mutation` -- and still omits the `partial-publish` clause that the sidecar is written before the rename; the file is byte-unchanged since round 1 and the step's current replacement Exit requires none of the six, so the repair stays additive study-sourced prose. The two budget derivations are still roundings rather than exact products: `checkpoint.archive.bytes` 201581002 doubles the study's 100,790,501-byte stored pack of the bundle alone and excludes the 3,212,143-byte capsule, and `checkpoint.archive.expanded_bytes` 209715200 is 200 MiB where 2 x (100,790,361 + 3,212,143) is 208,005,008; both stay above the roughly 104.0 MB the layout implies, the reference reproduces the derivation strings verbatim, and the current Exit fixes the six limits as values, which `test_archive_budgets_declare_the_six_measured_limits` holds. Four leads are new, none of them a criterion defect. First, the study's second amendment says `No step's entry, exit, files, tests or disciplines change` while each of the five re-issued runbook blocks says that same amendment made `each step's effective field revert to its baseline`; the two reconcile under the `Steps touched` field's content grammar, which all ten blocks use for content rather than for binding, and a correcting study amendment would itself un-bind the five runbook amendments again and cost a second five-block re-issue for no criterion change, so that cost is Fiat's to weigh rather than mine to force. Second, the `Still holding` field changes reading between the two sets: the stale five record a moment-of-writing progression, declaring 4, 3, 2, 1 and 0 later exits broken, while the current five declare all five exits holding in every block, which is true of the finished tree but not of the moment each block was appended. The end state is checkable, and holds: no current replacement Exit names `python3 scripts/portable_promise_machine.py check`, the only command in the five baseline exits that cannot pass in this checkout. Third, both tracked bodies still name the pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md`, at study lines 450 and 838 and runbook lines 72 and 127, and no file exists there; the study's first amendment names both study locations explicitly, the runbook amendment replaces both baseline mentions, every occurrence is an inline code span rather than a link so no H001 fires, and none of these bytes is new in this round. Fourth, the tracked runbook carries no marker separating the five superseded blocks from the five current ones -- same date, same heading level, and the binding lives only in gitignored controller state -- so a reader of the tracked copy alone orders them by file position; the harm is bounded because each current replacement body is word-identical to the block it re-binds. The Elenchus line is carried here as its own contract directs: the fix is `unguarded` because it changed no test file, which is the expected shape for a byte re-copy of two documents rather than a missing guard. Verified positives, none extending past this step's documents. Both re-copies: `cmp` exit 0 for each pair, and both digests equal the receipted values in the status cell above. Both Hypomnema modes: study mode exit 0 against design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, whose `protasis-design-evidence/v1` envelope selects `native-subcommands` from four candidates, and the ordinary walk exit 0; the study carries exactly one `design-bridge` fence, at lines 917 to 921, with the three ordered rows `schema | hypomnema-design-bridge/v1`, `decision | native-subcommands` and `record | docs/decisions/drafts/build-the-outer-checkpoint-archive-natively.md`. Both artefact changes are purely additive against round 1's bytes: the study is 0 deletions and 29 insertions, one appended amendment, which proves its own `Nothing else changes` claim, and the runbook is 0 deletions and 372 insertions, taking its amendment blocks from five to ten. All five stale-current pairs have word-identical replacement Exit and Files bodies, differing only in `Why` and `Still holding`, so the current blocks' claim that `no criterion, command or path differs from the repair it re-binds` holds token by token. The packet recomputes: `baseline_sha256` `6a232687a37224c58ee740927c0d2faedc4b034f5faaf00ed11087fea4c03b2b`, amendment `9ec2d1c114b855aa76a52c1fdcaf49f6361aff896d4e7408cbab382a9f221ced`, `effective_sha256` `28991eac5a547131e67a88d42f1ebf24da830f953503fb8ca8184aa4a580926f`, and the effective field equals the baseline followed by that one amendment. Against the baseline, the current step-1 Exit makes five changes and Files one: the draft path gains its `drafts/` directory, the `# Decision: Build the outer checkpoint archive natively` heading and the four remaining sections become required, the allocator clause gains `which indexes drafts only below docs/decisions/drafts/`, the ADR-028 citation gains `by its stable slug reference`, and `python3 scripts/portable_promise_machine.py check` is dropped, the only criterion removed. The tree supports that removal: the command exits 1 here with `portable runtime is absent or not a directory`, `493d7c72` removed 996 paths under `.agents/skills/promise-machine/runtime`, and no runner names it in `tests/check-map-v1.json`, `.github/workflows/` or `scripts/run_checks.py`, while `AGENTS.md:290` still does, which is the stale list the amendment calls stale; `DRAFTS = b"docs/decisions/drafts/"` sits at `decision_assignments.py:49`. The tree meets the added criteria: the draft opens with that exact heading, carries `Proposed, 2026-09-07.`, and carries `## Status`, `## Context`, `## Decision`, `## Alternatives` and `## Consequences` at lines 3, 19, 44, 87 and 120; ADR-028 carries one `## Amendment: Native outer archive (2026-09-07)` at line 295, directly after the 2026-09-02 amendment at line 278, cites `adr/build-the-outer-checkpoint-archive-natively`, and keeps its status Accepted. The reference still states what the Exit demands: six schemas, the zip metadata rule, seven ceiling values, 24 refusal-class rows, 35 fixture ids with no repeat, six secret patterns, `## Restore transaction` at line 249 and the budgets pointer. Every other step file is byte-unchanged since round 1, so round 1's positives over them stand on identical bytes. The five step tests re-derive both sets from the amended study bytes and pass: `Ran 5 tests`, exit 0. Phylax, Ephoros and the ordinary Hypomnema walk each printed `clean` and exited 0; imprimatur exited 0 on all three changed prose files, scoring 100.0/100 with 0 defects on the draft record and on the reference and reporting cadence signals only on ADR-028, and brevitas exited 0 on all three; `horos.py scan . --census --write` refreshed the census, `horos.py check .` reported `boundary matches the tree`, and `git diff --check` exited 0. On the detached snapshot `a70a6e2b09904e83526e4f6c979778644c5db1b7` of tree `c8660e0b95b26397109664aa49fdc51bf720bea7`, since removed, `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` came back green over its 13 selected checks with the Hexaemeron shard alone taking 1,777.3 s, `python3 plugins/hexaemeron/tests/run_tests.py` passed 2,436 of 2,436, and `python3 -m unittest discover -s tests` ran 1,626 and printed OK, each exit 0, across 2,420 s of wall time under a load average of 19. The fix commit `95d5e6984e74faee8c4ff62ba440f78de6f4624e` is `G` under key `B83B60AE16F5DD1A`, with author Shoggoth, committer Laurence and exactly one of each trailer. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, no `hexctl` command was run, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 3 -- 2026-09-08T02:43:36Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code, so no register id was exercised against executing behaviour: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above is a re-review of contract text that did not change: the reference is byte-identical to rounds 1 and 2 at `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, the study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0`, the runbook `607335ddbc1b2765803f66e6adefd5d932c0e02c49e4ca09cd968a9feaf1e008` and the design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, all four equal to the packet's digests, and the study's `risk-register` fence is byte-identical to the packet's `risk_register.markdown` with the same 25 unique ids. The design record's conformance criteria block `step:3`, `step:4`, `step:5` and `integration`; none blocks step 1, so no conformance cell was due at this boundary, and the six selection-stage cells recording `pass` for `native-subcommands` were read as recorded evidence rather than re-measured. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5. Nothing under `.hexaemeron/` was edited and no receipted artefact was amended. No `hexctl` command was run, so controller state was not read. imprimatur and brevitas had no target: this round changes one Python test file, two generated Horos files, the audit log and its synopsis, and no step Markdown, so rounds 1 and 2 readings stand on identical bytes and audit prose is not swept. The root suite was held against the clean detached snapshot of the repaired tree and not against the tree this round's record commit produces; the census and boundary currency guards were run separately over the final tree. The Elenchus verdict below is the declared result of the runbook's bounded check; no report bytes are attested.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `docs/fiat-checkpoint-archive-study.md` | Carried from rounds 1 and 2: the tracked study copy carried no `design-bridge` fence, so `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exited 1 with `H008 study has no design bridge block`, leaving the join from selected candidate `native-subcommands` to its decision home unchecked by machine while the ordinary walk exited 0. | fixed in 95d5e6984e74faee8c4ff62ba440f78de6f4624e and re-verified this round: `cmp` exits 0 for both tracked pairs, the tracked study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the tracked runbook `607335ddbc1b2765803f66e6adefd5d932c0e02c49e4ca09cd968a9feaf1e008`, the study carries exactly one `design-bridge` fence at lines 917 to 921, and the same study-mode command exits 0 against design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`. |
| S1-R3-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | The module docstring says the second test holds the budgets file to the six limits study section 10 derived, and the test read neither prose table: it compared `checkpoint-archive-budgets.json` against six constants declared inside the test and asserted only that each budget name appeared in the reference's `## Budgets` section. Five executed mutations on the detached copy at `.claude/worktrees/fiat-861-fdb3a2/tmp/fiat/fiat-861-mut-r3`, since removed, each left all five tests passing at exit 0 before the repair: the reference's `checkpoint.archive.export_wall_ms` limit changed from 15000 to 25000 (M1); that row's derivation alone changed from `5.4 x 2,766 ms` to `9.0 x 2,766 ms` (M1b); `checkpoint.archive.inspect_wall_ms`'s unit changed from `ms` to `s` (M1c); the `checkpoint.archive.export_peak_rss_bytes` row deleted outright (M1d); and study section 10's own `export_wall_ms` limit changed from 15000 to 25000 (M5). Only a changed limit in the JSON was caught (M6, exit 1). The reference is the contract Steps 2 to 4 are built against, so a drifted limit there is the ceiling an implementer reads while every gate stays green. | fixed in 62f6b116495bb3a8da793279c208a20dc4c04e8c, 32 insertions and 2 deletions: the test now parses both tables through one `BUDGET_ROW` pattern, requires six reference rows, requires the study's section-10 rows to equal them exactly, and asserts each row's unit and integer limit against the loaded JSON entry. M1, M1b, M1c, M1d and M5 each now exit 1 and M6 still exits 1. No test was added or renamed: `Ran 5 tests` and exit 0, `run_tests.py` still discovers 2,436, and `-k hostile` and `-k restore_from_archive` still match nothing in this file. |
| S1-R3-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Five further values the step's effective Exit requires the reference to state are pinned by nothing. Executed mutations on the same detached copy, each leaving all five tests at exit 0 and Phylax, Ephoros and Hypomnema `clean`: `- 4,200 entries.` to `- 9,000 entries.` (M2); `- 1,300 MiB expanded.` to `- 9,300 MiB expanded.` (M3); the container's Unix mode `0100644` to `0100755` (M4); `git -c pack.threads=1 bundle create` to `git bundle create` (M7); and the secret pattern `AKIA[0-9A-Z]{16}` to `AKIB[0-9A-Z]{16}` (M8). M8 is the sharpest of the five: `secret-shaped-member` is the register's Phylax control and this list is the specification Step 2's exporter is built from, so a silent drift there ships an exporter that does not catch an AWS key. | open, not fixed in this round: the remedy needs a criterion change from Fiat. The step's Tests field fixes `Five new tests` and names both existing tests with their scope, binding `test_archive_reference_names_every_refusal_class_and_fixture_id` to the 24 classes and 35 ids of study sections 4 and 5, so a sixth test would break that count and the 2,436 discovery figure while widening a named test past its parenthetical would exceed the criterion. Checked by hand instead at this head: the seven ceiling values, the container metadata rule and the six secret patterns each agree with the study item for item. |

Leads not pursued: Round 2's two carried leads are resolved against the controller's code rather than against the step's Exit scope, and both stay declined. `subprocess-argv`: the reference names `bounded_tool` with fixed argv, output cap and timeout and does not restate the register's no-shell, path-validated-before-argument and dash-prefixed-refs-impossible clauses; no shell is structure rather than prose, because `bounded_tool` calls `bounded_run` calls `bounded_probe`, which runs `subprocess.Popen([program, *argv], cwd=os.path.realpath(base_dir), stdout=PIPE, stderr=STDOUT, shell=False, env=env)` at `hexctl.py:11790-11796`, and both wrappers' docstrings state "no shell, a hard timeout, a hard output cap"; dash-prefixed refs are structurally impossible in this design, because the restore transaction interpolates no ref name and uses the literal refspec `+refs/heads/*:refs/heads/*` while `remote.origin.url` is derived from a validated `run.repository` and fetches nothing; only path validation before argument formation is genuinely unstated in prose, and the step's effective Exit requires none of the three. `concurrent-mutation`: the reference says `archive` takes the run lock through the controller's ordinary verification exactly as `checkpoint export` does, and `cmd_checkpoint_export` reads `state_before` and `ledger_before` through `_checkpoint_read_staged` at `hexctl.py:15354-15357`, then immediately before publication re-reads both and `_checkpoint_refs` and refuses with `checkpoint source changed before publication` at `hexctl.py:15421-15432`, having already refused when `_checkpoint_snapshot(state_root(base_dir), None)` differs from the captured inventory, so the register's stable-stat-and-re-read clause is carried by naming executable code that performs it; the same-account boundary is stated in the register as a boundary rather than a claim, so nothing is owed. `partial-publish`: the reference states the hidden sibling stage, the no-replace rename and that an existing boundary directory is never replaced, and omits only that the sidecar is written before the rename, which the runbook's Step 2 and Step 3 exits state at lines 742 and 1110 and again at 816 and 1185. The budget-derivation lead needs one correction: round 2 recorded both as roundings and only one is. `checkpoint.archive.bytes` 201581002 is the exact product 2 x 100,790,501, and 100,790,501 is candidate A's measured carrier rather than the bundle, since the draft record's alternatives table gives A's carrier as 100,790,501 while the study's named fixture gives the bundle as 100,790,361, and the 140-byte difference is exactly one stored entry's framing around `git/repository.bundle`: a 30-byte local header plus its 21-byte name, a 46-byte central directory record plus the same name, and a 22-byte end-of-central-directory record. `checkpoint.archive.expanded_bytes` 209715200 is the only rounding: it is 200 MiB where 2 x (100,790,361 + 3,212,143) is 208,005,008, so it sits 1,710,192 bytes looser than its own derivation and never tighter, and it is a Metron budget rather than the inspector's 1,300 MiB expanded refusal ceiling, which the reference separates by calling the ceilings safety limits and not performance targets. Round 2's four newer leads stand where round 2 left them, with two refinements. The stale pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md` occurs five times rather than four: study lines 450 and 838 and runbook lines 72 and 127 are stale mentions with no file at that path, while study line 889 is the study's own first amendment stating the move from that path to `docs/decisions/drafts/build-the-outer-checkpoint-archive-natively.md`, a legitimate quotation of a superseded path rather than a pointer; every occurrence is an inline code span so no H001 fires, and the remedy is still a dated study amendment that would un-bind the five current runbook amendments and cost a second five-block re-issue. The tracked runbook still carries no marker separating its five superseded 2026-09-07 amendment blocks from its five current ones, and the file holds exactly ten blocks under that date. Two leads are new. First, `.horos/candidates.json` is a tracked generated file that no gate holds current, and this run has moved it: round 2's appended record took the audit synopsis to 20,412 bytes, above Horos's `BLOB_MIN_BYTES` of 16,384, so a fresh scan classifies it `blob` on mean line length above 400 in the first 4,096 bytes and `horos.py check .` prints `candidate drift: audit/rounds/fiat-861-outer-checkpoint-archive-export-and-restore.synopsis.md: evidenced by the tree but missing from the boundary` while still exiting 0, where the same file at the step head is 8,054 bytes and draws no drift. The convention is settled, since that file already lists `audit/AUDIT_SYNOPSIS.md` and other runs' synopses among its 100 entries, so this round regenerates it; but the step's Exit names only `scan . --census --write`, which writes the census alone, and `tests/test_boundary_currency.py` compares `boundary_document` only, so the missing check is this repository's own open issue #1130, recorded in `plugins/horos/docs/markdown-outline/study.md` and `plugins/horos/docs/content-addressed-objects/study.md`, and not a new class. Second, `study_refusal_classes` in the shipped inventory test locates its input by two unpragma'd prose anchors, `### Details the runbook binds` and `- Refusal classes,`, so a reworded study would raise `IndexError` rather than fail a named assertion; the study is amended by appended dated blocks that do not rewrite section 4, so the coupling is recorded rather than raised. One checked negative: the study's `identity-unavailable` entry adds "any other identity refusal stops export" where the reference's condition cell keeps only "export continues with `status: unavailable` only for a legacy symbolic base", and the word "only" carries the same constraint, so the paraphrase loses no rule; in the other direction the reference is stricter than the study, adding `gpg` output and JSON value to the study's "no entry content" diagnostic rule. Verified positives, all re-derived this round and none extending past this step's documents. The packet's four digests recompute against the tree and `cmp` exits 0 for both tracked pairs. Hypomnema exits 0 in both modes: the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs`, and study mode against design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, whose `protasis-design-evidence/v1` envelope selects `native-subcommands` from four candidates by rule `unique-frontier`; the study carries exactly one `design-bridge` fence, at lines 917 to 921, with the three ordered rows `schema` `hypomnema-design-bridge/v1`, `decision` `native-subcommands` and `record` `docs/decisions/drafts/build-the-outer-checkpoint-archive-natively.md`. The design record's 48 result cells are 21 pass, 3 fail and 24 pending; all 24 existing report files under `.hexaemeron/reports/` match their recorded SHA-256 with no mismatch, the 24 pending cells are exactly those whose reports do not yet exist, and every `fail` cell belongs to a rejected candidate, `inspector-only` on `byte-identical-rebuild` and on `restore-without-source-clone` and `external-archiver` on `byte-identical-rebuild`, which is what the draft record's alternatives table states for C and D. That table's readings equal the Exit's enumerated values in order: pack 26, 26, 1,977 and 335 ms; carrier 100,790,501, 100,790,501, 100,470,196 and 100,790,483 bytes; 2, 3, 4 and 3 spawned programs. The reference states what the Exit demands: all six schemas, present in the study as well; 13 closed manifest fields, `schema`, `archive`, `boundary`, `run`, `refs`, `bundle`, `controller_capsule`, `identity`, `signer`, `proof`, `acceptance`, `controller` and `limits`; the nine fixed entry paths; 24 unique refusal classes and 35 unique fixture ids, each set equal to the study's; stage tallies of 11 archive, 19 inspect and 7 restore; the seven ceiling values 4,200 entries, 1,300 MiB expanded, 1 GiB bundle, 64 MiB per other entry, 1,024 UTF-8 bytes and 255 bytes per name component, and 64 prior acceptances, all present item for item in the study's ceiling sentence at lines 123 to 127; the six secret patterns, whose five code spans are identical in both documents; `## Restore transaction`; and the budgets pointer. All three of the reference's fenced blocks, commands, store path and layout, appear verbatim in the study. The reference's `## Open items` list is coherent with its own body and with the study: `README.txt`'s fixed text is described but not given, `identity.status` `unavailable` is closed to a closed reason without enumeration, and #862 and #863 are the study's own named deferrals. ADR-028 keeps `Accepted, 2026-08-27`, its single `## Amendment: Native outer archive (2026-09-07)` is the file's last heading at line 295 directly after the 2026-09-02 amendment at line 278, the heading order before it equals the shipped test's tuple, and it cites `adr/build-the-outer-checkpoint-archive-natively` once by stable slug with no relative link. `audit_synopsis.py --check .` exits 0 over the whole repository and validates the inherited round-2 record at source_lines=31, synopsis_lines=3, budget=pass, source_sha256 `628a32e1f39bb30494de7bc96650363b083b4b62df016db3089e492c87d884f7`, with the committed synopsis equal to a fresh render. The reviewed range `0bc39f27..95d5e698` is three commits, all `G`, each authored by Shoggoth, committed by Laurence and carrying exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`; the cumulative diff is 12 files, 3,168 insertions and 9 deletions at patch SHA-256 `aa64fbdeba15ff2014715bb68e080e646bf0ee9610e911195632fc223af123df`, and no changed path carries a Git attribute. Phylax, Ephoros and the ordinary Hypomnema walk each printed `clean` and exited 0 on the repaired tree, `git diff --check` exited 0, and `horos.py check .` exited 0. The five step tests ran 5 and passed both before and after the repair. On the clean detached snapshot `9b356b0581f109836851a75accae96ec49de6cf8` of tree `35425e1ab56d1690a1aeceae054be36bef2763a3`, parented on `95d5e698` and equal to the fix commit's staged tree, since removed: `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` printed `outcome green` and exited 0 over its 13 selected checks at source digest `52035ef18c45bde611c6c254e5d3f35f8ff42540e870121602c73cbae626c914` and map `8d3a80b027f3`, with `hexaemeron-suite` at 1,106.2 s and `root-suite` at 259.3 s and no `WAI-E-ADAPTER.TIMEOUT` line; `python3 plugins/hexaemeron/tests/run_tests.py` passed 2,436 of 2,436 in 447.477 s across 12 shards of 203, with 0 failures, 0 errors and 0 skips, manifest digest `af01121d6fc718a2d9d1c2357a7f2a6969413e2caaf6403ada2084ef14a2dec7` over 2,436 discovered and 2,436 unique ids and peak child RSS 234,553,344 bytes; and `python3 -m unittest discover -s tests` ran 1,626 and printed OK. All three exited 0, across 1,881 s of wall time from 2026-09-08T01:38:58Z to 2026-09-08T02:10:19Z. The Elenchus line is the declared result of the runbook's exact runner: `elenchus.py --repo . --ref 62f6b116495bb3a8da793279c208a20dc4c04e8c --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-861-step-1.json --timeout 5400` exited 0 in 717 s from 02:21:07Z to 02:33:04Z and returned `passed` with detail `the runner report records that the guard passed on the parent`, over a parent report of 2,436 executed, 0 assertion failures, 0 errors and 0 skips, naming `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` as the changed test file. `passed` rather than `guarded` is the expected shape: the repair closes a latent gap, so the new assertions hold on the unfixed parent, whose three statements already agree. The default 900-second timeout was raised to 5,400 because the declared runner's own wall time is about 450 s; the command, report format and report file are the runbook's, unsubstituted. The fix commit `62f6b116495bb3a8da793279c208a20dc4c04e8c` is `G` under key `B83B60AE16F5DD1A`, with author Shoggoth, committer Laurence and exactly one of each trailer. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, no `hexctl` command was run, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 4 -- 2026-09-08T04:55:23Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code, so no register id was exercised against executing behaviour: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above is a re-review of contract text that did not change this round: the reference is byte-identical to rounds 1, 2 and 3 at `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020` and the study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0`, both equal to the packet's digests; the design record is `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`. This round's diff changes one Python test file and appends two runbook amendments; it changes no specification byte, so the reference digest above is the round-3 digest unchanged. The packet at `.hexaemeron/briefs/step-1-audit-4.json` was emitted before the second amendment of 2026-09-08 and names runbook `009153594d12b84b5de5e214b686b346f9eca36bac84abc676ce840aa7d9a04a` with effective step `6ad3a9a50a83f25e73c15fe026b7e8ff9d568ed0eaff606d3742b2a28cae81eb`; the effective step was re-read from `.hexaemeron/runbook.md` at `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275` instead, and the packet's Tests field was not used. Ledger holds 25 entries; entry 24 is amendment `db45944703662e1e03ab0c8724fecec280946bb1a9538c213a566e5a588ff53e` and entry 25 is amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04`. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5. Nothing under `.hexaemeron/` was edited except the round logs this round wrote, and no receipted artefact was amended: no `hexctl` command was run, so controller state was not read or written. imprimatur and brevitas had no target, because the only changed Markdown is the tracked runbook, which the Exit excludes as one of the two byte-identical run artefacts and which Fiat lints at its `docs/` path before receipt; audit prose is not swept. Round 3's separate suite hold applies again: the three root commands were held against the clean detached snapshot of the fix commit's staged tree and not against the tree this round's record commit produces, and the census and boundary currency guards were run separately over the final tree. The Elenchus verdict below is the declared result of the runbook's bounded check; no report bytes are attested and no claim is made that the command ran. A prior Warden session working this same round was lost; its uncommitted six-test tree and its findings S1-R4-01 and S1-R4-02 are carried below from the controller's brief rather than from its own context, and its 20-mutation list for S1-R3-02 was re-derived here rather than inherited, so the labels below are this session's.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `docs/fiat-checkpoint-archive-study.md` | Carried from rounds 1, 2 and 3: the tracked study copy carried no `design-bridge` fence, so `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exited 1 with `H008 study has no design bridge block`, leaving the join from selected candidate `native-subcommands` to its decision home unchecked by machine while the ordinary walk exited 0. | fixed in 95d5e6984e74faee8c4ff62ba440f78de6f4624e and re-verified this round: `cmp .hexaemeron/study.md docs/fiat-checkpoint-archive-study.md` and `cmp .hexaemeron/runbook.md docs/fiat-checkpoint-archive-runbook.md` each exit 0, the tracked study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the tracked runbook `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275`, and the same study-mode command exits 0 against design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`. |
| S1-R3-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Carried from round 3: the budgets test compared `checkpoint-archive-budgets.json` against six constants declared inside the test and read neither prose table, so five executed mutations left all five tests at exit 0 -- the reference's `checkpoint.archive.export_wall_ms` limit 15000 to 25000 (M1), that row's derivation `5.4 x 2,766 ms` to `9.0 x 2,766 ms` (M1b), `checkpoint.archive.inspect_wall_ms` unit `ms` to `s` (M1c), the `checkpoint.archive.export_peak_rss_bytes` row deleted (M1d), and study section 10's own `export_wall_ms` limit 15000 to 25000 (M5). | fixed in 62f6b116495bb3a8da793279c208a20dc4c04e8c and re-verified this round: `test_archive_budgets_declare_the_six_measured_limits` is unchanged by this round's fold, still parses both tables through one `BUDGET_ROW` pattern, requires six reference rows equal to the study's section-10 rows, and asserts each row's unit and integer limit against the loaded JSON entry. It passes at `95b5ec62e258cd878324d7c504381f5b456cc2b7` with budgets file `61d6d638bf69e96500d9dc6cd2e9567a04f6f3aecd6c37a9f8eff89854ba6f64`. |
| S1-R3-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Carried from round 3 and closed here: values the step's effective Exit requires the reference to state were pinned by nothing, so each survived mutation with all five tests and Phylax, Ephoros and Hypomnema green. Round 3 executed five: `- 4,200 entries.` to `- 9,000 entries.` (M2), `- 1,300 MiB expanded.` to `- 9,300 MiB expanded.` (M3), the container's Unix mode `0100644` to `0100755` (M4), `git -c pack.threads=1 bundle create` to `git bundle create` (M7), and the secret pattern `AKIA[0-9A-Z]{16}` to `AKIB[0-9A-Z]{16}` (M8). M8 was the sharpest: `secret-shaped-member` is the register's Phylax control and this list is the specification Step 2's exporter is built from, so a silent drift there ships an exporter that does not catch an AWS key. | closed in `95b5ec62e258cd878324d7c504381f5b456cc2b7`. The class was re-derived this round as 20 anchored mutations over the whole family the Exit names, not the five round 3 happened to execute: the seven ceilings (M2 4,200 to 9,000; M3 1,300 MiB to 9,300 MiB; M9 bundle 1 GiB to 2 GiB; M10 64 MiB per other entry to 640 MiB; M11 1,024 UTF-8 bytes to 2,048; M12 255 bytes per component to 511; M13 64 prior acceptance receipts to 96), the nine container clauses (M4 mode `0100644` to `0100755`; M14 `stored, method 0` to `deflated, method 8`; M15 DOS time 1980-01-01 to 2020-01-01; M16 `create_system` 3 to 0; M17 no directory entries to directory entries; M18 no ZIP64 records to ZIP64 records; M19 no comment to a comment; M20 no extra fields to extra fields; M21 sorted by UTF-8 bytes to sorted by code point), the bundle determinism command (M7), and three secret patterns (M8 `AKIA[0-9A-Z]{16}` to `AKIB[0-9A-Z]{16}`; M22 `ghp_[A-Za-z0-9]{36}` to `{40}`; M23 `xox[baprs]-` to `xox[bapr]-`). All 20 exit 1 against the folded test and all 20 exit 0 against the test file at `e5a6010e7b388dc172d3a756167e19cbc5950039`, digest `89dbc0bd647633bc654551b0b1d701961bad6d2ad65954c3cd3e900589ce5399`. Deletion of each was executed separately and is recorded under S1-R4-02. The class is closed because the structural cause is removed: the test now compares the value rather than asserting a name is present, so a value added to these families is compared by construction. |
| S1-R4-01 | medium | `docs/fiat-checkpoint-archive-runbook.md` | Opened by the lost prior session of this round and re-derived here from the runbook bytes. The amendment of 2026-09-08 that repaired S1-R3-02 added a sixth test and stated that no Exit criterion changed. That claim was wrong: the effective Exit of every step pins an unfiltered test count computed from five tests in two modules, so a sixth test broke six criteria across five steps to close one finding, and the tree as it stood ran six. At runbook `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275` the six are Step 1 `Six documents and five tests exist` at line 1006 and `Ran 5 tests` at line 1049 (amendment at line 1004), Step 2 `Ran 17 tests` at line 1118 (amendment at 1084), Step 3 `Ran 55 tests` in the amendment at 1149, Step 4 `Ran 61 tests` in the amendment at 1219, and Step 5 `Ran 68 tests` at line 1350 (amendment at 1277). The Step 3 and Step 4 unfiltered counts are over `plugins.hexaemeron.tests.test_hexctl_checkpoint_archive` alone and wrap across lines, so a line-oriented grep for `Ran 55` or `Ran 61` under-counts the cascade to three steps; the correct reading is five. The filtered counts Step 3 `Ran 35 tests` and Step 4 `Ran 6 tests` were unaffected either way. | fixed by amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04`, which replaced Step 1 Tests again, taking the runbook from `009153594d12b84b5de5e214b686b346f9eca36bac84abc676ce840aa7d9a04a` to `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275`, keeping five tests and widening the first test's scope instead of adding a sixth. Fiat owns that amendment; this round did not run `hexctl`. The step fix `95b5ec62e258cd878324d7c504381f5b456cc2b7` folded the sixth test's assertions into `test_archive_reference_names_every_refusal_class_and_fixture_id` and removed `test_archive_reference_pins_the_ceilings_metadata_bundle_command_and_secrets`. Verified at that commit: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive tests.test_fiat_checkpoint_archive_record -v` exits 0 with `Ran 5 tests`, the module alone runs 2, `plugins/hexaemeron/tests/run_tests.py` discovers and passes 2,436 of 2,436, and `python3 -m unittest discover -s tests` runs 1,626 and prints OK, so the four downstream counts are untouched. `-k hostile` and `-k restore_from_archive` still match nothing in this file. |
| S1-R4-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Opened by the lost prior session of this round. Eight further reference values the step's effective Exit requires are pinned by nothing: a schema version, a schema name, an entry path, a dropped `README.txt` layout line, a renamed manifest field row, the `## Restore transaction` heading, the sidecar two-space rule, and the `acceptance/current` rule inverted. Executed here as eight anchored edits, each exiting 0 against the test file at `e5a6010e7b388dc172d3a756167e19cbc5950039`: the manifest row's `fiat-checkpoint-archive/v1` to `/v2` (R1); `fiat-checkpoint-inspect/v1` renamed to `fiat-checkpoint-inspection/v1` (R2); the layout entry `git/repository.bundle` renamed to `git/repo.bundle` (R3); the `README.txt` layout line deleted outright (R4); the manifest row `controller_capsule` renamed to `capsule` (R5); `## Restore transaction` renamed to `## Restore steps` (R6); the sidecar span's two spaces reduced to one (R7); and `No acceptance/current entry is ever written.` inverted to `An acceptance/current entry is written when the boundary is current.` (R8). All eight agreed with the study before the edit, so they were unpinned rather than drifted. | fixed in `95b5ec62e258cd878324d7c504381f5b456cc2b7` under amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04`. All eight now exit 1. The folded test compares the reference's closed set of six schema names against the study, compares the nine entry paths as one fence equal to the study's and as an ordered first column, compares the thirteen closed manifest field rows in order and requires the study to name each in the same order, and asserts the `## Restore transaction` heading with a non-empty section, the sidecar two-space span with the one-space form rejected, and the `acceptance/current` rule three ways in the reference and two in the study. Deletion was executed as a separate family of 17 anchored deletions, all exiting 1 against the folded test: four ceiling bullets (D1 to D4), four container clauses (D5 mode, D6 no ZIP64 records, D7 no comment and no extra fields, D8 sorted by UTF-8 bytes), the `-c pack.threads=1` fragment (D9), the `AKIA[0-9A-Z]{16}` bullet (D10), the `## Restore transaction` heading (D11), the sidecar sentence (D12), the `acceptance/current` sentence (D13), the `limits` manifest row (D14), the `zip64-present` refusal row (D15), the `nfc-mismatch-name` fixture bullet (D16), and the `fiat-checkpoint-restore-transcript/v1` name (D17). Four study-side edits were executed too, all exiting 1: the study's own ceiling 4,200 to 9,000 (M5s), its `AKIA` pattern (M6s), its `README.txt` layout line deleted (M7s), and its `controller_capsule` field renamed (M8s). Across all 49 cases the folded test left 0 survivors, and against the test file at `e5a6010e` 47 of 49 survived; the two already caught were D15 and D16, the refusal-class and fixture-id sets the pre-existing first test already held. The harness mirrored only the four paths the test reads into a disposable scaffold under `/var/folders`, since removed, so no mutation touched the run worktree. |
| S1-R4-03 | low | `docs/fiat-checkpoint-archive-runbook.md` | New this round, and the one particular in which the widened scope is wrong. The effective Tests field at runbook `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275` requires ten value families to equal `what the study states ... against sections 1, 3 and 4`, and two of the ten are not in those sections. First, the `## Restore transaction` heading has no counterpart anywhere in the study: `grep -n "^#* *Restore transaction" .hexaemeron/study.md` finds nothing, and the study's only statement of the subject is section 12 at line 847, `ceilings, refusal classes, restore transaction) lives in a new`, which names it among the things the reference must contain rather than stating it. That item is therefore unsatisfiable as written, and the folded test satisfies it instead as a reference-side structural assertion, heading present with a non-empty section. Second, the `acceptance/current` rule is split: `current` is the literal `outside` is in section 1 at line 104, but the refusal half `acceptance/current refuses` is in section 5's risk register at line 642, so the folded test reads a section the field does not name. No pinned value or count is affected and every value agrees between the two documents; the cost is that a reader satisfying the field literally finds one item impossible and one out of the cited range. This is the same class as S1-R4-01, an amendment whose scope claim does not match the bytes, one round smaller. | open, not fixed in this round: the remedy is a criterion change only Fiat can make, one clause widening the cited sections to include 5 and stating that the `## Restore transaction` heading is a reference structure whose study counterpart is section 12's list. The step fix does not depend on it, because the ten families are pinned either way; nothing in the tree needs to change. Not folded into the existing amendment, because amending a receipted field is the controller's operation and this round ran no `hexctl` command. |

Leads not pursued: One operational trap is new and cost this round a 30-minute suite run, so it is recorded rather than filed. `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6`, the form rounds 3 and 4 both used, returned `outcome red`, `failures command-failure` and exit 1 on the clean detached snapshot `49bd36de69faf23bd0a01f12f7265e2edac13f47` of tree `97ddc4734c51a518d8bc0349203eee2bf47bafa5` at 03:35:08Z to 04:05:46Z, with `hexaemeron-suite failed 1800.4s` the only non-passing row among 13 selected checks and zero `WAI-E-ADAPTER.TIMEOUT` lines. That is the per-check wall cap, not a test failure: `scripts/run_checks.py` line 51 declares `DEFAULT_TIMEOUT_SECONDS = 1_800` and line 2292 records a timeout as `status="failed", failure_class="command-failure", reason="timeout"`, the map's `hexaemeron-suite` argv is `python3 plugins/hexaemeron/tests/run_tests.py` with `--jobs` appended, and `--jobs 6` gave that one check 2 workers against 3,029.5 s of child CPU. The same command re-run at the step's Exit wording, `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a` with no `--jobs`, printed `outcome green` and exited 0 over the same 13 checks on the same snapshot at 04:21:23Z to 04:34:25Z under an automatic budget of 12 slots, with `hexaemeron-suite` at 766.1 s, `root-suite` at 425.9 s, `dead-code-suite` at 166.0 s and `dead-code-suppressions-check` at 136.1 s. The step's Exit carries no `--jobs` flag, so the throttle was the operator's and not the criterion's; the trap is that adding it can force a false red on this repository's largest check, distinct from the known `WAI-E-ADAPTER.TIMEOUT` family. Round 3's five carried leads stand where round 3 left them and none is re-argued here: the `subprocess-argv` path-validation clause unstated in prose, `concurrent-mutation` carried by naming executable code, `partial-publish` omitting only the sidecar-before-rename order, the `checkpoint.archive.expanded_bytes` 200 MiB rounding that sits 1,710,192 bytes looser than its own derivation and never tighter, and the five stale occurrences of the pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md`. Two of round 3's leads are now measurably worse and are re-stated with this round's numbers rather than closed. The tracked runbook still carries no marker separating its five superseded 2026-09-07 amendment blocks from its five current ones, and it now holds twelve blocks in total, ten under 2026-09-07 and two under 2026-09-08, the second of which supersedes the first; nothing in the file says so, and `grep -n "^### Amendment"` returns the twelve at lines 636, 716, 780, 849, 906, 1004, 1084, 1149, 1219, 1277, 1376 and 1417. And `.horos/candidates.json` remains a tracked generated file that no gate holds current: the audit source is 40,412 bytes and its synopsis 40,753 bytes before this round's append, both far above Horos's `BLOB_MIN_BYTES` of 16,384, so the drift round 3 recorded persists and this round regenerates the file again; the missing check is this repository's own open issue #1130 and not a new class. Two leads are new and both are declined as out of this step's Exit scope. First, `study_refusal_classes` and the new `study_manifest` slice in the folded test locate their input by unpragma'd prose anchors, `### Details the runbook binds`, `- Refusal classes,` and `` `checkpoint.json` is closed to: ``; the first two were round 3's recorded coupling and the third is this round's, and all three now fail by name through `anchored` or a named assertion rather than raising `IndexError`, which is the coupling reduced rather than removed. Second, the folded test's layout check asserts the reference's and the study's fenced layout blocks byte-equal, which pins the nine entry paths the Exit names and also their description column, one column wider than the criterion requires; it is kept because it is what makes R4 and M7s, the deleted `README.txt` line, fail in both documents, and a later legitimate edit to a description in one document alone will fail this test and force a study amendment, which is the intended direction. One checked negative carries forward unchanged and was re-derived: the study's `identity-unavailable` entry adds `any other identity refusal stops export` where the reference's condition cell keeps only `export continues with status: unavailable only for a legacy symbolic base`, and the word `only` carries the same constraint, so the paraphrase loses no rule. Verified positives, all re-derived this round. The reference is byte-identical to round 3 at `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, so no specification byte moved and the round-3 readings of its contents stand: six schemas, thirteen closed manifest fields, nine entry paths, 24 unique refusal classes and 35 unique fixture ids each equal to the study's, the seven ceilings, the six secret patterns, `## Restore transaction` and the budgets pointer. The folded test now holds every one of those against the study mechanically rather than by hand. `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` moved from `89dbc0bd647633bc654551b0b1d701961bad6d2ad65954c3cd3e900589ce5399` to `c0e0826b60c5e9517c970b4732eaef6115b898a34924b887cdf6640ef018d9ce`, 347 insertions and 11 deletions, and `tests/test_fiat_checkpoint_archive_record.py` is unchanged at `40727d4c94ad84b61a3c96d77d9f3a028936337bd95d082cceb0ea1143dd94ea`. Phylax exited 0 printing `clean` over `plugins tests`, Ephoros exited 0 printing `clean` over `plugins tests`, Hypomnema exited 0 printing `clean` in both modes, the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` and study mode against the design record, `horos.py scan . --census --write` exited 0 after `git add`, `horos.py check .` exited 0 printing `boundary matches the tree`, and `git diff --check` exited 0. The constant the prior session renamed to `EXPECTED_PEM_PROSE` to answer Phylax `P004` without a pragma is kept under that name. On the snapshot above, the three root commands each exited 0: `run_checks.py` green as recorded in the trap paragraph, `plugins/hexaemeron/tests/run_tests.py` passing 2,436 of 2,436 in 492.0 s across 12 shards of 203 with 0 failures, 0 errors and 0 skips, manifest digest `af01121d6fc718a2d9d1c2357a7f2a6969413e2caaf6403ada2084ef14a2dec7` over 2,436 discovered and 2,436 unique ids, peak child RSS 224,641,024 bytes and 3,029.5 s of child CPU, and `python3 -m unittest discover -s tests` running 1,626 in 330.2 s and printing OK. `.githooks/greenlight` then ran root discover in the run worktree, 1,626 tests in 547.9 s and OK, and recorded tree `97ddc4734c51a518d8bc0349203eee2bf47bafa5` in `LAST_GREEN`, which is the fix commit's staged tree. The Elenchus line is the declared result of the runbook's exact runner: `elenchus.py --repo . --ref 95b5ec62e258cd878324d7c504381f5b456cc2b7 --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-861-step-1.json --timeout 5400 --format json` exited 0 in 505 s from 04:45:43Z to 04:54:08Z and returned `passed` with detail `the runner report records that the guard passed on the parent`, over a parent report of complete true, 2,436 executed, 0 assertion failures, 0 errors and 0 skips, naming `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` as the changed test file. `passed` rather than `guarded` is the expected shape, as in round 3: the fold closes a latent gap and changes no specification byte, so the new assertions hold on the unfixed parent, whose reference and study are the same bytes. The default 900-second timeout was raised to 5,400 because the declared runner's own wall time is about 500 s; the command, report format and report file are the runbook's, unsubstituted, and the report was written inside the detached parent worktree, so `.elenchus/` in the run worktree stays absent. The fix commit `95b5ec62e258cd878324d7c504381f5b456cc2b7` is `G` under key `B83B60AE16F5DD1A`, author Shoggoth, committer Laurence, with exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`, over 3 files, 427 insertions and 11 deletions. One housekeeping fact belongs in the record because it changes what a later reader can trust. Two suite runs on the discarded six-test tree were live when this round began, both on snapshot worktree `fiat-861-snap-r4` at `e2482d3e`: the brief's pid 71010 from 03:14Z, and a second launched at 03:23:45Z from `.hexaemeron/round4-suite.sh` reparented to init as pid 5629, which had truncated the first one's log. Both were stopped with `SIGTERM` before any of this round's own runs started, the launcher was moved aside to `.hexaemeron/round4-suite.sh.stopped-by-warden-r4` so nothing could relaunch it, `.hexaemeron/round4-suite.log` holds only their two header lines and no result, and neither run's output is exit evidence for this round. The `fiat-861-snap-r4` worktree was removed and replaced by `fiat-861-snap-r4b` at the fix commit's staged tree, from which every suite result above was taken. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, no `hexctl` command was run, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 5 -- 2026-09-08T13:57:13Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code, so no register id was exercised against executing behaviour: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above is a re-review of contract text that did not change this round: the reference is byte-identical to rounds 1 to 4 at `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, the study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, each equal to the packet's digest, and the selected candidate stays `native-subcommands`. This round's diff changes two Python test modules, appends one runbook amendment to the tracked copy and regenerates `.horos/census.json`, so no specification byte moved and the reference digest above is the round-3 digest unchanged. The packet at `.hexaemeron/briefs/step-1-audit-r5.json` names runbook `e824252acc7feb83ceec2b8eb095639a2c5e47c6b99ce9cfdf4fc9762ecc28e7`, baseline step `6a232687a37224c58ee740927c0d2faedc4b034f5faaf00ed11087fea4c03b2b` and effective step `e303fb1c3e1f854bb764eeeb5a87a2428c8df168089edc6a1bb9826897898e8e`, and the tracked runbook matches its runbook digest, so unlike round 4 the packet's Tests field was used as delivered. Ledger holds 27 entries; entry 25 is amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04`, entry 26 is round 4 and entry 27 is amendment `17be5e2662306d200026538fd122b580b2570a1e66375c7023c3213c99e8c8bb`. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5, so the six declared limits are checked as values and derivations only. Nothing under `.hexaemeron/` was written: the brief, study, runbook and ledger were read there and no `hexctl` command was run, so controller state was neither read nor written. imprimatur and brevitas had no target, because the only changed Markdown is the tracked runbook, which the Exit excludes as one of the two byte-identical run artefacts and which Fiat lints at its `docs/` path before receipt; audit prose is not swept. The three root commands were held against a clean detached snapshot of this round's staged tree, which is the tree `LAST_GREEN` names and the tree this record is committed in; a record cannot restate the result of a run over its own bytes, so those exits and counts are carried in the round's hand-back and in `LAST_GREEN` rather than here. The Elenchus verdict below is the declared result of the runbook's bounded check; no report bytes are attested. The prior Warden session working this same round was lost after it committed its fixes as `7313f54ce6216bcb4f3647a825e7928efa3ffe7a` and before it wrote any record, so nothing about it was receipted; its two findings are named from that commit's message and every mutation class below was re-derived here against the tracked bytes rather than inherited, so the counts and labels are this session's.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `docs/fiat-checkpoint-archive-study.md` | Carried from rounds 1 to 4: the tracked study copy carried no `design-bridge` fence, so `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exited 1 with `H008 study has no design bridge block`, leaving the join from selected candidate `native-subcommands` to its decision home unchecked by machine while the ordinary walk exited 0. | fixed in `95d5e6984e74faee8c4ff62ba440f78de6f4624e` and re-verified this round: `cmp .hexaemeron/study.md docs/fiat-checkpoint-archive-study.md` and `cmp .hexaemeron/runbook.md docs/fiat-checkpoint-archive-runbook.md` each exit 0, the tracked study is `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the tracked runbook `e824252acc7feb83ceec2b8eb095639a2c5e47c6b99ce9cfdf4fc9762ecc28e7`, and the same study-mode command exits 0 printing `clean` against design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`. |
| S1-R3-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Carried from rounds 3 and 4: the budgets test compared `checkpoint-archive-budgets.json` against six constants declared inside the test and read neither prose table, so five executed mutations left all five tests at exit 0. | fixed in `62f6b116495bb3a8da793279c208a20dc4c04e8c` and re-verified this round with three anchored mutations, all exiting 1 against the current modules: the reference row `checkpoint.archive.export_wall_ms` limit 15000 to 25000, the same row in study section 10, and the `"limit": 15000` entry in the budgets JSON itself. `test_archive_budgets_declare_the_six_measured_limits` is untouched by this round's diff, and the budgets file is unchanged at `61d6d638bf69e96500d9dc6cd2e9567a04f6f3aecd6c37a9f8eff89854ba6f64`. |
| S1-R3-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Carried from rounds 3 and 4 and closed in round 4: the ceilings, container clauses, bundle determinism command and secret patterns the step's Exit requires the reference to state were pinned by nothing, so each survived mutation with all five tests and Phylax, Ephoros and Hypomnema green. | closed in `95b5ec62e258cd878324d7c504381f5b456cc2b7` and re-verified this round: the five mutations round 3 executed all exit 1 against the current modules -- `- 4,200 entries.` to `- 9,000 entries.`, `- 1,300 MiB expanded.` to `- 9,300 MiB expanded.`, mode `0100644` to `0100755` at both of its occurrences, `git -c pack.threads=1 bundle create` to `git bundle create`, and `AKIA[0-9A-Z]{16}` to `AKIB[0-9A-Z]{16}`. The class stays closed: this round's fold widened the same value-comparison machinery rather than replacing it. |
| S1-R4-01 | medium | `docs/fiat-checkpoint-archive-runbook.md` | Carried from round 4: the amendment that repaired S1-R3-02 added a sixth test and claimed no Exit criterion changed, but the effective Exit of every step pins an unfiltered test count computed from five tests in two modules, so a sixth test broke criteria across five steps to close one finding. | fixed by amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04` and re-verified this round: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive tests.test_fiat_checkpoint_archive_record -v` exits 0 with `Ran 5 tests`, the hexctl module alone runs 2 and the record module alone 3, and `-k hostile` and `-k restore_from_archive` each run 0 tests over both modules. The tracked runbook still carries `Ran 5 tests`, `Ran 17 tests`, `Ran 55 tests`, `Ran 61 tests` and `Ran 68 tests` with the filtered `Ran 35 tests` and `Ran 6 tests` beside them, `five tests exist` four times and `six tests exist` never. Its two remaining `Six new tests` occurrences are accounted for: line 1393 is inside the superseded 2026-09-08 amendment block and line 460 is Step 4's own baseline Tests field, which legitimately declares six tests of its own. |
| S1-R4-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Carried from round 4: eight further reference values the step's effective Exit requires -- a schema version, a schema name, an entry path, a dropped `README.txt` layout line, a renamed manifest field row, the `## Restore transaction` heading, the sidecar two-space rule and the `acceptance/current` rule inverted -- were pinned by nothing. | fixed in `95b5ec62e258cd878324d7c504381f5b456cc2b7` under amendment `f21c763ae4744a6db589f0597706cd74250cb6344b19411f939b2e96ffc78a04` and re-verified this round with four anchored mutations, all exiting 1: `fiat-checkpoint-inspect/v1` to `fiat-checkpoint-inspection/v1`, `## Restore transaction` to `## Restore steps`, the acceptance sentence inverted, and the `acceptance-self-reference` refusal row's condition changed from an `acceptance/current` entry to an `acceptance/prior` entry. One correction to round 4's own record: it quoted the acceptance sentence as `No acceptance/current entry is ever written.`, and the reference writes `acceptance/current` inside a code span at line 76, so that anchor matches nothing as quoted; the mutation was re-run against the true bytes and exits 1. |
| S1-R4-03 | low | `docs/fiat-checkpoint-archive-runbook.md` | Carried from round 4 and closed here. Round 4 left it open because the remedy was a criterion change only Fiat can make: the effective Tests field required ten value families to equal what the study states against sections 1, 3 and 4, and two of the ten were misattributed. `## Restore transaction` has no study counterpart, section 12 naming the restore transaction among the archive contract's contents without stating any of it, so value parity was unsatisfiable as written; and the `acceptance/current` refusal half is section 5, not 1, 3 or 4. | fixed by amendment `17be5e2662306d200026538fd122b580b2570a1e66375c7023c3213c99e8c8bb`, ledger entry 27, which replaced Step 1 Tests a third time and took the tracked runbook from `53a22dd9caadb5aa0cddede1a004dfa945b41ea04beabbb79fb03f8297640275` to `e824252acc7feb83ceec2b8eb095639a2c5e47c6b99ce9cfdf4fc9762ecc28e7`. Both clauses the finding asked for are present in the effective field: the cited sections now read 1, 3, 4 and 5, and the field states that the reference's `## Restore transaction` heading is pinned as a structural assertion over the reference alone, because section 12 names the restore transaction among that contract's contents without stating its content. No pinned value, command, path or count changes and the folded test already implements that shape. Fiat owns the amendment; this round ran no `hexctl` command, and `7313f54ce6216bcb4f3647a825e7928efa3ffe7a` copied the amended bytes into the tracked copy as a 47-line append with no deletion, so `cmp` exits 0. |
| S1-R5-01 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Opened by the lost prior session of this round and re-derived here. The content manifest's thirteen rows were compared on their field-name column alone, so every `closed to` cell and every `joined against` path was pinned by nothing while the step's Exit requires the reference to state that contract. Twelve anchored mutations were executed here against the tracked reference, eleven of them exiting 0 against the parent modules `c0e0826b60c5e9517c970b4732eaef6115b898a34924b887cdf6640ef018d9ce` and `40727d4c94ad84b61a3c96d77d9f3a028936337bd95d082cceb0ea1143dd94ea`: `compression` `stored` to `deflated`, `sha256` dropped from the archive entries triple, the bundle join `git/repository.bundle` renamed to `git/repo.bundle` in the join alone, `working_commit_sha` to `working_commit`, `ledger_entries` to `ledger_count`, `unavailable` to `unknown`, `key_path` to `keyfile`, `initial_base_sha` to `initial_base`, the controller_capsule join `controller-capsule/MANIFEST.json` to `MANIFEST.txt`, the signer join `proof/pubkey.asc` to `proof/pubkey.pem`, and the proof row `sha256` to `digest`. The twelfth, `current` is the literal `outside` inverted to `inside`, already exits 1 under the parent as round 4's R8 family. All eleven agreed with the study before the edit, so they were unpinned rather than drifted. A drift here is the specification Step 2's exporter is built from, and the bundle case is the sharpest: the reference would name one path in its layout and another in its manifest, a contradiction the exporter would have to resolve by guessing. | fixed in `7313f54ce6216bcb4f3647a825e7928efa3ffe7a`. Each row's `closed to` cell is compared to study section 1's parenthetical for the same field through `STUDY_MANIFEST_FIELD` and `normalise_closed`, which strips code-span backticks because the study writes format `zip` where the reference writes `format` `zip`; the `schema` row carries no study parenthetical and is asserted against the schema name `EXPECTED_SCHEMAS` already holds; and every span in the `joined against` column containing a slash must appear in the layout fence, which the same test holds equal to the study's, so the join is bound to section 1 through it. All twelve mutations exit 1 against the fix. |
| S1-R5-02 | low | `docs/decisions/drafts/build-the-outer-checkpoint-archive-natively.md` | Opened by the lost prior session of this round and re-derived here. The draft decision record's twelve measured cells were asserted as substrings of its whole Alternatives section, so six of the twelve were satisfied by a neighbouring row: A and B both read 26 pack ms and 100,790,501 carrier bytes, and B and D both read 3 spawned programs. Ten anchored mutations were executed here, eight of them exiting 0 against the parent modules: A pack 26 to 27, A carrier 100,790,501 to 100,790,502, B spawned 3 to 5, the A and C pack readings swapped, B pack 26 to 30, B carrier 100,790,501 to 100,790,600, D spawned 3 to 6, and the table head `pack ms` to `pack s`. The swap is the sharpest: it makes the selected candidate `native-subcommands` read 1,977 ms against losing candidate `inspector-only` at 26 ms, inverting the measurement the selection rests on, with all five tests and all three lints green. The two unique-value cells, D pack 335 to 336 and A spawned 2 to 9, already exit 1 under the parent. | fixed in `7313f54ce6216bcb4f3647a825e7928efa3ffe7a`. Each candidate row is compared to its own column of study section 4's result matrix through `RECORD_MATRIX_ROW` and `STUDY_MATRIX_ROW` over the three measured criteria `pack-wall-milliseconds`, `carrier-bytes` and `subprocess-boundaries-per-export`, the head naming candidate, pack ms, carrier bytes, spawned programs and gates is required verbatim through `RECORD_MATRIX_HEADER`, and the row set must be exactly A to D in `CANDIDATE_ORDER`. All ten mutations exit 1 against the fix. |

Leads not pursued: One residue of this round's own fix is established rather than argued. The `joined against` column is pinned only where a cell carries a code span containing a slash, which is 6 of the 13 rows; the other 7 carry prose or a dotted token and are compared to nothing. Three anchored mutations exit 0 against the fix and establish it: the `refs` row's `boundary.refs` renamed to `boundary.references`, the `schema` row's `the inspector's supported set` inverted to `the inspector's unsupported set`, and the `limits` row's `the inspector` changed to `the exporter`. This is not a finding, because the Exit's list of what the reference must state names the nine entry paths and never a join column, so those cells are outside the criterion and their path-shaped members are pinned as extra rigour rather than as a requirement; the family is recorded so a later step that starts reading the join column knows what holds it. A second observation belongs beside it: the fix commit's message states twelve anchored mutations passed unnoticed, and 22 anchored cases were executed here of which 19 exit 0 against the parent, so the executed twelve understate the class rather than misdescribe it, and the fix leaves 0 survivors across all 22. Round 4's operational trap is now confirmed structurally and acted on rather than re-measured. Round 4 recorded that `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` returned `outcome red` with `hexaemeron-suite failed 1800.4s` as its only non-passing row and zero `WAI-E-ADAPTER.TIMEOUT` lines, while the same command without `--jobs` printed `outcome green`. `scripts/run_checks.py` line 51 declares `DEFAULT_TIMEOUT_SECONDS = 1_800`, `--jobs` is documented as `override the automatic budget` and there is no per-check timeout flag, so throttling the budget starves the repository's largest check into its own wall cap. This round therefore ran the three root commands in the Exit's exact form with no `--jobs` flag, which is a deliberate departure from the operator's load hint and is recorded as such. Round 3's five carried leads stand where round 3 left them and none is re-argued: the `subprocess-argv` path-validation clause unstated in prose, `concurrent-mutation` carried by naming executable code, `partial-publish` omitting only the sidecar-before-rename order, the `checkpoint.archive.expanded_bytes` 200 MiB rounding that sits 1,710,192 bytes looser than its own derivation and never tighter, and the five stale occurrences of the pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md`. Round 4's two worsening leads are re-stated with this round's numbers rather than closed. The tracked runbook still carries no marker separating superseded amendment blocks from current ones, and it now holds thirteen, ten under 2026-09-07 at lines 636, 716, 780, 849, 906, 1004, 1084, 1149, 1219 and 1277 and three under 2026-09-08 at lines 1376, 1417 and 1461, of which the first two are superseded by the third; nothing in the file says so, and the `Six new tests;` at line 1393 is the visible cost of that silence. And `.horos/candidates.json` remains a tracked generated file that no gate holds current, which is this repository's own open issue #1130 and not a new class. The prose-anchor coupling lead grows by one: `STUDY_MANIFEST_FIELD` locates study section 1's manifest sentence through the unpragma'd anchor `` `checkpoint.json` is closed to: ``, joining the anchors round 3 and round 4 recorded, and all of them now fail by name through `anchored` or a named assertion rather than raising `IndexError`, which is the coupling reduced rather than removed. Verified positives, all re-derived this round. Phylax exited 0 printing `clean` over `plugins tests`, Ephoros exited 0 printing `clean` over `plugins tests`, and Hypomnema exited 0 printing `clean` in both modes, the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` and study mode against the design record. `git diff --check` exited 0. The two test modules moved from `c0e0826b60c5e9517c970b4732eaef6115b898a34924b887cdf6640ef018d9ce` to `ef447b695d819d6961db270bc633ccc754fc24853ca3dcf0c2651612b662fde6` and from `40727d4c94ad84b61a3c96d77d9f3a028936337bd95d082cceb0ea1143dd94ea` to `3036d6939649dd8f397acf191bdb28b6d20b6c6e4028ac741775be1af0d1cbdb`, 76 and 60 insertions against 11 and 6 deletions, and the fix commit's only other changes are the runbook append and three lines of `.horos/census.json`. Every mutation in this record was executed against a disposable scaffold under the session scratchpad holding copies of the four documents the tests read plus the link targets their dead-link check resolves; no tracked file was mutated, the run worktree stayed clean throughout, and the scaffold was removed. The Elenchus line is the declared result of the runbook's exact runner: `elenchus.py --repo . --ref 7313f54ce6216bcb4f3647a825e7928efa3ffe7a --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-861-step-1.json --timeout 5400 --format json` exited 0 between 13:44:09Z and 13:51:13Z and returned `passed` with detail `the runner report records that the guard passed on the parent`, over a parent report of complete true, 2,436 executed, 0 assertion failures, 0 errors and 0 skips, naming both changed test files. `passed` rather than `guarded` is the expected shape, as in rounds 3 and 4: the fix closes a latent gap and changes no specification byte, so the new assertions hold on the unfixed parent, whose reference, study and draft record are the same bytes. The command, report format and report file are the runbook's, unsubstituted; the default 900-second timeout was raised to 5,400 because the declared runner's own wall time is about 500 s under this machine's load; and the report was written inside the detached parent worktree, so `.elenchus/` in the run worktree stays absent. The fix commit `7313f54ce6216bcb4f3647a825e7928efa3ffe7a` is `G` under key `B83B60AE16F5DD1A`, author Shoggoth, committer Laurence, with exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`, over 4 files, 186 insertions and 20 deletions. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, no `hexctl` command was run, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 6 -- 2026-09-08T15:18:23Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, so no register id was exercised against executing behaviour, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above is a re-review of contract text no round has changed since round 3: the reference is `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, the study `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0`, the tracked runbook `e824252acc7feb83ceec2b8eb095639a2c5e47c6b99ce9cfdf4fc9762ecc28e7`, the design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, the budgets file `61d6d638bf69e96500d9dc6cd2e9567a04f6f3aecd6c37a9f8eff89854ba6f64` and the draft decision record `9564f43e03f5a64a1f866cd0b3ceaab89a61174d2e341cfb3b66c29184e51087`; study, runbook and design record each equal the packet's digest and the selected candidate stays `native-subcommands`. `cmp .hexaemeron/study.md docs/fiat-checkpoint-archive-study.md` and `cmp .hexaemeron/runbook.md docs/fiat-checkpoint-archive-runbook.md` each exit 0, and `shasum -a 256` gives each pair the digest above. This round's diff changes one Python test module and regenerates `.horos/census.json`, so no specification byte moved. The packet at `.hexaemeron/briefs/step-1-audit-r6.json` names baseline step `6a232687a37224c58ee740927c0d2faedc4b034f5faaf00ed11087fea4c03b2b`, effective step `e303fb1c3e1f854bb764eeeb5a87a2428c8df168089edc6a1bb9826897898e8e` and controller state `3b84d7cfc76f3d0e94a89bf8639ffc6662f258eb072d94f108857f025b26ab12`, and the tracked runbook matches its runbook digest, so its effective Tests field was used as delivered: it cites study sections 1, 3, 4 and 5 and pins the reference's `## Restore transaction` heading reference-side, which is the round-4 repair as Fiat amended it. Ledger holds 28 entries; entry 28 is round 5. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5, so the six declared limits are checked as values and derivations only. One `hexctl` command ran, `next --brief-out .hexaemeron/briefs/step-1-audit-r6.json`, which reads controller state and writes only that packet; no controller state was written, nothing under `.hexaemeron/` was edited and no receipted artefact was amended. imprimatur and brevitas had no target, because this round changes no Markdown but this record, and audit prose is not swept. The three root commands were held against a clean detached snapshot of this round's staged tree, which is the tree `LAST_GREEN` names and the tree this record is committed in; a record cannot restate the result of a run over its own bytes, so those exits are carried in the round's hand-back and in `LAST_GREEN` rather than here. The Elenchus line below is the declared result of the runbook's bounded check against a commit object whose tree is this round's fix applied to `cadc51ac645e36200bd3970fd2b8797a5e1bd9f9`; no report bytes are attested.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R6-01 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | The step's Exit requires the reference to state six schemas with their closed fields from study sections 1 and 4. Only the schema names were held: `## Results and proof` states the closed fields of five result objects and no test compared them to anything. Twelve anchored mutations were executed against the parent modules `ef447b695d819d6961db270bc633ccc754fc24853ca3dcf0c2651612b662fde6` and `3036d6939649dd8f397acf191bdb28b6d20b6c6e4028ac741775be1af0d1cbdb`, and all twelve exit 0 with the five tests, Phylax, Ephoros and Hypomnema green: inspect `outer_sha256` to `outer_digest`; `manifest_sha256` dropped from the export result; the `inspect` stage dropped from the export result's `timing_ms`; signature-proof `status` (`G` required) to (`E` required); transcript `destination_was_empty` to `destination_empty`, and again dropped outright; inspect `signatures` per-commit `status` to `state`; restore `verify` to `check`; restore `snapshot_id` to `snapshot`; restore `outer_sha256` to `outer_digest`; the native `fiat-controller-checkpoint-restore/v1` object renamed; and `the native` inverted to `the foreign`. These five objects are the printed contract Steps 2 to 4 are built against. | fixed in this round's commit, which takes `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` from `ef447b695d819d6961db270bc633ccc754fc24853ca3dcf0c2651612b662fde6` to `4e41362430b62db0d587275aa1769380a2f51037c7b2db7fba1b48b7b8636692`. The export, inspect and signature-proof field lists are compared to study section 4's three bullets and the transcript's to section 1's sentence, as ordered code spans through `RESULT_OBJECTS`, the signature proof slice bounded where both documents write No raw `gpg` output., because the reference alone goes on to name `push.verified_commits`. The restore result is bounded rather than compared, because section 1 states four of its six members: those four must be among the reference's. Nine of the twelve mutations now exit 1; the three that survive are S1-R6-03. |
| S1-R6-02 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | The same Exit requires the reference to state the store path and both boundary names. Nothing in `## Store path and boundaries` was pinned but its sidecar two-space rule. Six anchored mutations were executed against the same parent modules and all six exit 0 with the five tests and the three lints green: the store path `<origin>/.hexaemeron/checkpoints/<run-worktree-name>/` to `<origin>/.hexaemeron/checkpoint/<run-worktree-name>/`; the fence's `checkpoint.zip.sha256` to `checkpoint.zip.sha512`; `audit-verdict-step-<n>-loop-<l>-<full-head-sha>` to `audit-verdict-step-<n>-<full-head-sha>`; `step-<n>-<full-head-sha>` to `step-<n>-<head-sha>`; `done push` to `done pull`; and `ADR-028's` to `ADR-027's`. The loop case is the sharpest: without `-loop-<l>-` the reference names one directory for every audit loop of a step, while the same section states that an existing boundary directory is never replaced, so a Step 2 exporter built from it would refuse the second loop as occupied rather than publish it. | fixed in this round's commit for the store path: the reference's fence is held equal to study section 1's, and the two are byte-identical today, so the first two mutations now exit 1. Four survive. The two boundary directory names are S1-R6-03. `done push` and the `ADR-028's` attribution are adjacent prose in the same sentence rather than boundary names, so they are outside the criterion and are recorded as a lead below. |
| S1-R6-03 | low | `docs/fiat-checkpoint-archive-runbook.md` | Four values the Exit requires the reference to state have no counterpart in study sections 1, 3, 4 or 5, which is the section list the effective Tests field binds value parity to, so no test this step may add can pin them and the residues of S1-R6-01 and S1-R6-02 cannot be closed under the field as written. The two boundary directory names `step-<n>-<sha>` and `audit-verdict-step-<n>-loop-<l>-<sha>` are stated in the study's assumption 5, which sits before section 1, and again in section 6's glossary. The restore result's native `fiat-controller-checkpoint-restore/v1` object name is in the study nowhere: `grep -c fiat-controller-checkpoint-restore docs/fiat-checkpoint-archive-study.md` returns 0. Its `outer_sha256` member is absent from section 1's restore sentence and from section 8's answer, which between them name `snapshot_id`, `verify`, `status` and `next` and no more. This is the shape of round 4's S1-R4-03: value parity unsatisfiable as written, remediable only by an amendment Fiat owns. | open. The repair is a Tests-field amendment, either widening the cited sections to reach the study's assumption list and section 6 for the two boundary names and section 8 for the restore result, or pinning those four reference-side as `## Restore transaction` already is. This round pinned nothing beyond what sections 1, 3, 4 and 5 state, so no criterion moved and no count changed: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive tests.test_fiat_checkpoint_archive_record -v` exits 0 with `Ran 5 tests`, the tracked runbook still carries `Ran 5 tests` four times, `five tests exist` four times and `six tests exist` never. |

Leads not pursued: The two survivors S1-R6-02 excludes are established rather than argued. `done push` to `done pull` and `ADR-028's` to `ADR-027's` each exit 0 against the fix as well as the parent; both are in the boundary-names sentence, neither is a boundary name, and the Exit's list of what the reference must state names neither the condition attached to a boundary nor the ADR the boundaries come from. Round 5's own residue is unchanged: the manifest's `joined against` column is pinned only where a cell carries a code span containing a slash, which is 6 of the 13 rows, and the other 7 are compared to nothing. The prose-anchor coupling lead grows by twelve: this round's fix adds `STORE_PATH_ANCHOR`, `STUDY_STORE_PATH_ANCHOR`, `NO_RAW_GPG`, four reference and four study anchors in `RESULT_OBJECTS`, `RESTORE_RESULT_ANCHOR`, `STUDY_RESTORE_RESULT_ANCHOR` and `STUDY_RESTORE_RESULT_END`, and every one fails by name through `anchored` or `fenced_block` rather than raising `IndexError`, which is the coupling bounded rather than removed. Round 4's two worsening leads stand where round 5 left them: the tracked runbook still carries no marker separating superseded amendment blocks from current ones, and still holds thirteen, ten under 2026-09-07 and three under 2026-09-08 of which the first two are superseded by the third; and `.horos/candidates.json` remains a tracked generated file that no gate holds current, which is this repository's open issue #1130, visible here as `horos.py check .` exiting 0 with `boundary matches the tree` while printing `candidate drift` for this audit file and its synopsis. Round 3's five carried leads stand and none is re-argued: the `subprocess-argv` path-validation clause unstated in prose, `concurrent-mutation` carried by naming executable code, `partial-publish` omitting only the sidecar-before-rename order, the `checkpoint.archive.expanded_bytes` 200 MiB rounding that sits 1,710,192 bytes looser than its own derivation and never tighter, and the five stale occurrences of the pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md`, still two in the tracked runbook and three in the tracked study. Verified positives, all re-derived this round. The eight findings of rounds 1 to 5 are closed and stay closed: 35 anchored mutations covering the S1-R3-01, S1-R3-02, S1-R4-02, S1-R5-01 and S1-R5-02 families were re-executed against the current modules and all 35 exit 1, and S1-R1-01, S1-R4-01 and S1-R4-03 were re-verified by command rather than by mutation -- both `cmp` calls exit 0, `hypomnema.py --study docs/fiat-checkpoint-archive-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` exits 0 printing `clean`, the five tests print `Ran 5 tests`, and the effective Tests field carries both clauses S1-R4-03 asked for. `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .` exits 0. Phylax exits 0 printing `clean` over `plugins tests`, Ephoros exits 0 printing `clean` over `plugins tests`, and Hypomnema exits 0 printing `clean` in both modes, the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` and study mode against the design record. `git diff --check` exits 0. All 53 mutations in this record were executed against a disposable detached worktree of `cadc51ac645e36200bd3970fd2b8797a5e1bd9f9` under `/private/tmp`, restored file by file after each run and removed at the end; no tracked file was mutated and the run worktree stayed clean throughout. The Elenchus line is the declared result of the runbook's exact runner: `elenchus.py --repo . --ref 768ac4080a7fb96f82e9743e318d53fd363d7ba1 --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-861-step-1.json --timeout 5400 --format json` exited 0 between 15:11:51Z and 15:17:31Z and returned `passed` with detail `the runner report records that the guard passed on the parent`, over a parent report of complete true, 2,436 executed, 0 assertion failures, 0 errors and 0 skips, naming the one changed test file. The command, report format and report file are the runbook's, unsubstituted; the pinned report path was free because Elenchus writes inside the detached parent worktree, where `.elenchus/` does not exist, so no fresh name was needed and `.elenchus/` in the run worktree stays absent. The default 900-second timeout was raised to 5,400 because the declared runner's own wall time is about 300 s per shard under this machine's load, another run's `run_tests.py --jobs 8` having held the machine throughout. `passed` rather than `guarded` is the expected shape, as in rounds 3, 4 and 5: the fix closes a latent gap and changes no specification byte, so the new assertions hold on the unfixed parent, whose reference and study are the same bytes. `768ac408` is a commit object built for that check alone, its tree this round's fix applied to `cadc51ac645e36200bd3970fd2b8797a5e1bd9f9`; the round's single commit carries the identical test-module change, and the controller verifies its range. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 1, round 7 -- 2026-09-08T16:56:23Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=reviewed; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=reviewed; destination-emptiness=reviewed; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=reviewed

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Step 1 still ships no controller code: `checkpoint archive`, `checkpoint inspect` and `checkpoint restore --archive` do not exist to run, so no register id was exercised against executing behaviour, and the 35 hostile fixtures, the exporter refusal tests and the restore tests belong to Steps 2 to 4. Every disposition above re-reviews contract text no round has changed since round 3: the reference `ec24f69154079d33aee04f9c685319ce72cfc75e2829e7d81f0305f189db6020`, the study `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0`, the design record `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91`, the budgets file `61d6d638bf69e96500d9dc6cd2e9567a04f6f3aecd6c37a9f8eff89854ba6f64` and the draft decision record `9564f43e03f5a64a1f866cd0b3ceaab89a61174d2e341cfb3b66c29184e51087`, each equal to round 6's, and the selected candidate stays `native-subcommands`. The tracked runbook is the one specification byte that moved: Fiat's amendment `fb35ef45e78790aa7eb35ead610169ea26459ee4f3a162969e17b7cba5739d21` of this date took `.hexaemeron/runbook.md` from `e824252acc7feb83ceec2b8eb095639a2c5e47c6b99ce9cfdf4fc9762ecc28e7` to `1fb4ec7d32a18c61715011bc7b7e6c5bef2a170badbde327d7b1354fdf947563` and this round re-copied it, so `cmp .hexaemeron/study.md docs/fiat-checkpoint-archive-study.md` and `cmp .hexaemeron/runbook.md docs/fiat-checkpoint-archive-runbook.md` each exit 0 with `shasum -a 256` giving the study pair `607b3790de1030b6a87f2c0980dc60064660c2169d790175a2ba5a43490a47d0` and the runbook pair `1fb4ec7d32a18c61715011bc7b7e6c5bef2a170badbde327d7b1354fdf947563`. The packet at `.hexaemeron/briefs/step-1-audit-r7.json` names baseline step `6a232687a37224c58ee740927c0d2faedc4b034f5faaf00ed11087fea4c03b2b`, effective step `44018387b6e20bbdc043d42c11a7570202950a46f0edc9ee3f5936868a705aff` and runbook `1fb4ec7d32a18c61715011bc7b7e6c5bef2a170badbde327d7b1354fdf947563`, which the tracked copy matches, so its effective Tests field was used as delivered: value parity now cites the study's assumption list and sections 1, 3, 4, 5 and 6, and the native restore object name and the restore result's `outer_sha256` member join `## Restore transaction` as reference-side structural assertions. Controller state is `3f61ee56770c00c138f47b0e7e8450fceac50003a708c57b5f4e0195cba25cd5`; the ledger holds 30 entries, entry 29 round 6 and entry 30 that amendment. This round's diff changes one Python test module, the tracked runbook copy and `.horos/census.json`, and appends this record and its synopsis; no reference, study, budget or decision byte moved. S1-R6-03 closes in this round's single commit: the amendment removed the obstacle the finding named, and the commit does the pinning it made possible, taking `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` from `4e41362430b62db0d587275aa1769380a2f51037c7b2db7fba1b48b7b8636692` to `50a7c53ad0e8de5b133afbb260c82aaa40cf95b949de89395aeee9b8d7f4eabe`. A record committed inside its own commit cannot state that commit's SHA, so the fix bytes are identified here by that digest transition and by the commit object `e76375c20648e64500bb06d40a6be9521c9a848c`, whose tree is exactly this fix applied to `e6e28962a741d6da5aada92274108cf107fc05c0`; the SHA is in the round's hand-back, which the controller receipts. No `hexctl` command ran: Fiat wrote the packet, nothing under `.hexaemeron/` was edited and no receipted artefact was amended. `metron.py check` was not run because neither the baseline nor the run file exists before Step 5, so the six declared limits are checked as values and derivations only. imprimatur and brevitas had no target: the only Markdown this round changes is the tracked runbook copy, which the Exit excludes as one of the two byte-identical run artefacts, and this record, and audit prose is not swept. The three root commands were held against a clean detached snapshot of this round's staged tree, which is the tree `LAST_GREEN` names and the tree this record is committed in; a record cannot restate the result of a run over its own bytes, so those exits are carried in the round's hand-back and in `LAST_GREEN` rather than here. The Elenchus line below is the declared result of the runbook's bounded check against `e76375c20648e64500bb06d40a6be9521c9a848c`; no report bytes are attested.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Nine anchored mutations establish that the fix's new assertions are not vacuous: the `step-<n>-<full-head-sha>` placeholder shortened to `<head-sha>`; `audit-verdict-step-<n>-loop-<l>-<full-head-sha>` stripped of its `loop-<l>-` segment; each of those two boundary names deleted from the sentence outright; the native restore object renamed to `fiat-controller-checkpoint-relocate/v1`; its qualifier inverted from `the native` to `the foreign`; that member deleted outright; the restore result's `outer_sha256` renamed to `outer_digest`; and that member deleted outright. All nine exit 0 against the parent module `4e41362430b62db0d587275aa1769380a2f51037c7b2db7fba1b48b7b8636692` with the five tests green, and all nine exit 1 against `50a7c53ad0e8de5b133afbb260c82aaa40cf95b949de89395aeee9b8d7f4eabe`. They are the five survivors S1-R6-01 and S1-R6-02 left open, plus one deletion for each of the four values, so those two residues close with S1-R6-03. The two survivors S1-R6-02 excluded stay excluded and were re-executed against the extended module: `done push` to `done pull` and `ADR-028's` to `ADR-027's` each still exit 0. Both sit in the boundary-names sentence, neither is a boundary name, and the Exit's list of what the reference must state names neither the condition attached to a boundary nor the ADR the boundaries come from. One new lead, raised as a lead rather than a finding: the study writes the boundary head placeholder three ways, `<full-head-sha>` in section 1's fence at lines 72 and 73, `<sha>` in assumption 5 at lines 26 and 27 and in section 6's glossary at line 660, and `<wt>` for `<run-worktree-name>` in section 1's command example at line 133. The fence is the normative statement and the rest are prose abbreviations of the same shape, so no implementer builds a different directory from them; the fix compares the stem before the placeholder across the two documents and holds the reference's own placeholder against that fence, which is the parity the two can carry. Round 5's residue is unchanged: the manifest's `joined against` column is pinned only where a cell carries a code span containing a slash, 6 of the 13 rows, and the other 7 are compared to nothing. The prose-anchor coupling lead grows by seven: this round adds `BOUNDARY_SENTENCE_ANCHOR`, `BOUNDARY_SENTENCE_END`, `STUDY_BOUNDARY_ASSUMPTION_ANCHOR`, `STUDY_BOUNDARY_ASSUMPTION_END`, `STUDY_GLOSSARY_HEADING`, `STUDY_GLOSSARY_BOUNDARY_ANCHOR` and `STUDY_GLOSSARY_BOUNDARY_END`, and every one fails by name through `anchored` or `section` rather than raising `IndexError`, which is the coupling bounded rather than removed. Round 4's two worsening leads stand: the tracked runbook still carries no marker separating superseded amendment blocks from current ones, and now holds fourteen, ten under 2026-09-07 and four under 2026-09-08 of which the first three are superseded by the fourth; and `.horos/candidates.json` remains a tracked generated file no gate holds current, this repository's open issue #1130, visible here as `horos.py check .` exiting 0 with `boundary matches the tree` while printing `candidate drift` for this audit file and its synopsis. Round 3's five carried leads stand and none is re-argued: the `subprocess-argv` path-validation clause unstated in prose, `concurrent-mutation` carried by naming executable code, `partial-publish` omitting only the sidecar-before-rename order, the `checkpoint.archive.expanded_bytes` 200 MiB rounding that sits 1,710,192 bytes looser than its own derivation and never tighter, and the five stale occurrences of the pre-amendment draft path `docs/decisions/draft-build-the-outer-checkpoint-archive-natively.md`, still two in the tracked runbook and three in the tracked study. Verified positives. The closures of rounds 1 to 6 stand and were not re-executed: this round only adds assertions, and no existing assertion was weakened, the store-path fence comparison and the study-bounded restore-result comparison being byte-identical to round 6's, so every mutation an unchanged assertion already caught is still caught. `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive tests.test_fiat_checkpoint_archive_record -v` exits 0 with `Ran 5 tests`, and the tracked runbook carries `Ran 5 tests` four times, `five tests exist` four times and `six tests exist` never. Phylax exits 0 printing `clean` over `plugins tests`, Ephoros exits 0 printing `clean` over `plugins tests`, and Hypomnema exits 0 printing `clean` in both modes, the ordinary walk over `README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` and study mode against the design record. `git diff --check` and `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .` each exit 0. All eleven mutations in this record ran in a disposable detached worktree of `e6e28962a741d6da5aada92274108cf107fc05c0` under `/private/tmp`, the reference restored from a pristine copy after each run and the worktree removed with `git worktree remove --force`; no tracked file was mutated and the run worktree stayed clean throughout. The Elenchus line is the declared result of the runbook's exact runner: `elenchus.py --repo . --ref e76375c20648e64500bb06d40a6be9521c9a848c --test-command "python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}" --report-format unittest-json-v1 --report-file .elenchus/fiat-861-step-1.json --timeout 5400 --format json` exited 0 between 16:50:16Z and 16:55:32Z and returned `passed` with detail `the runner report records that the guard passed on the parent`, over a parent report of complete true, 2,436 executed, 0 assertion failures, 0 errors and 0 skips, naming the one changed test file. The command, report format and report file are the runbook's, unsubstituted: the pinned path was free because `.elenchus/` does not exist in this run worktree and Elenchus writes inside its own detached parent worktree, so no fresh name was needed and `.elenchus/` here stays absent. The default 900-second timeout was raised to 5,400 because the declared runner's shards each took 221 to 313 seconds under this machine's load. `passed` rather than `guarded` is the expected shape, as in rounds 3 to 6: the fix closes a latent gap and changes no specification byte, so the new assertions hold on the unfixed parent, whose reference and study are the same bytes. Not driven: no archive was built, inspected or restored, no signature or keyring path was exercised, no container ran, and the study's measurements were read as recorded evidence rather than re-measured. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 1 -- 2026-09-09T03:37:43Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite is waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. Four register ids are `not-applicable` because their subject is not in this step: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls that Step 4 introduces, and `hostile-fixture-set` names the inspector test module that Step 3 introduces; `checkpoint inspect` and `checkpoint restore --archive` do not exist to run. Seven of the twenty-one `reviewed` ids are reviewed against the export half only, because the register clause they carry is enforced by the inspector rather than by the exporter: `zip-slip`, `name-collision`, `zip-bomb` and `trailing-data` name central-directory and extraction checks Step 3 owns, `manifest-join` is checked here only by the producer's own self-check, `identity-join` is checked here only at the minting end, and `version-compatibility` is recorded rather than enforced. Of the seven declared ceilings the export enforces two, the 4,200-entry count and the 1 GiB bundle; the rest are the inspector's, and they compose rather than needing an export check, since a 256 MiB capsule plus a 1 GiB bundle is 1,280 MiB against the 1,300 MiB expanded ceiling and 4,096 capsule files plus seven fixed members is 4,103 against 4,200. No archive was restored and no inspector was run. The step's own budgets were not measured; Step 5 owns that. Nothing under `.hexaemeron/` was hand-edited: the only write there was the named resolver, and it rewrote its report to the same bytes. No `hexctl` command was run.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Both acceptance guards in `_checkpoint_archive_proof` are untested, so the archive's central security claim has no regression cover. Two anchored mutations on a detached copy of `b5e556cf` left all 17 tests green: relaxing `status != "G"` at `hexctl.py:17330` to `status not in ("G", "U")`, which would accept a good signature by a key the keyring does not trust, ran 17 tests in 115.130s and exited 0; deleting the `fingerprint not in fingerprints` clause at `hexctl.py:17331` ran 17 tests in 106.574s and exited 0. The effective Exit names status `G` verbatim, and the fingerprint clause is what makes the manifest's `signer` block and the reference's phrase `G under the pinned fingerprints` mean anything. The fixture cannot reach either branch: `_checkpoint_archive_seed_keyring` writes ultimate ownertrust for the discovered fingerprints, so the happy path is always `G` and always in-set. By contrast the trailer guard is covered, and relaxing `!= 1` to `< 1` failed `test_archive_signature_proof_requires_good_status_and_exactly_one_trailer_each`. | open, not fixed in this round: the repair is a test, and the effective Exit pins `Ran 17 tests` for this module, so adding one breaks the criterion. S2-R1-02 records that collision. Fiat's choice is to widen the count clause, or to name a second test module the Files field permits. |
| S2-R1-02 | medium | `.hexaemeron/runbook.md` | The step's Exit and its elenchus discipline are jointly unsatisfiable for an audit repair. The Exit requires `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive -v` to exit 0 with `Ran 17 tests`; the Disciplines field requires that `an audit repair lands with a test named `test_<class>_refuses_before_<boundary>``; and the Files field authorises amending `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` and no other test module. An eighteenth test in that module fails the count clause, and a repair with no new test fails the discipline. Observed directly: the module at `b5e556cf` reports `Ran 17 tests in 107.897s` and `OK`. This blocks every code repair the audit loop can propose for this step, including S2-R1-01. | open, not fixed in this round: the remedy is a criterion change only Fiat can make, one clause of one runbook amendment, either relaxing the count to a floor or naming a repair test module in the Files field. |
| S2-R1-03 | low | `.hexaemeron/study.md` | The Exit, study line 744 and reference lines 77 to 78 each require the bundle to be built by `git -c pack.threads=1 bundle create` from the bounded ref set `with --no-tags`. `git bundle create` does not accept that argument. Measured on git 2.50.1 (Apple Git-155): `git -c pack.threads=1 bundle create <file> refs/heads/main --no-tags` and the same command with the flag before the ref both answer `error: unrecognized argument: --no-tags`. `hexctl.py:17056` therefore omits it and is correct to. The behaviour the flag names holds by construction and is tested: the argv is the explicit list `["-c", "pack.threads=1", "bundle", "create", path, *revisions, *anchored]` with each revision spelled `refs/heads/<name>` from `_checkpoint_refs`; `_checkpoint_archive_bundle` refuses `ref-disagreement` unless the bundle header's heads equal exactly that map; and `test_archive_bundle_is_built_single_threaded_from_exactly_the_checkpoint_refs` creates `fixture-tag` and asserts no `refs/tags/` head appears. An independent control bundle built here from one annotated-tagged commit carried only `refs/heads/main` in its header. | open, not fixed in this round: the criterion is unsatisfiable as written, so the repair is a dated study amendment removing or restating the clause, then a re-copy of the reference, neither of which an audit round may author. |
| S2-R1-04 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | The six secret patterns at `hexctl.py:612-619` do not match the OpenPGP armored secret-key header, which is the one private-key armor a command that exports OpenPGP material is most likely to meet. Measured against the shipped tuple through `_checkpoint_archive_secret_shaped`: `-----BEGIN PRIVATE KEY-----`, `-----BEGIN RSA PRIVATE KEY-----`, `-----BEGIN EC PRIVATE KEY-----`, `-----BEGIN ENCRYPTED PRIVATE KEY-----` and `-----BEGIN OPENSSH PRIVATE KEY-----` all refuse, and `-----BEGIN PGP PRIVATE KEY BLOCK-----` passes, because pattern one requires `PRIVATE KEY-----` immediately after its optional label while the armor spells `PRIVATE KEY BLOCK-----`. Pattern two is fully subsumed by pattern one, which matches the OpenSSH header on its own, so the six declared patterns are five distinct ones. `test_archive_export_refuses_secret_shaped_member` plants `AKIA` and the OpenSSH header only. The contract is short rather than the code: reference lines 193 to 198 and study lines 589 to 593 name a PEM block and the OpenSSH header, and OpenPGP armor is neither. | open, not fixed in this round: the six patterns are pinned by the reference and the study, so the repair is a dated study amendment first and then the code. |
| S2-R1-05 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | The secret scan's carry window is not derived from its patterns and the invariant stated at `hexctl.py:621` is false. `_checkpoint_archive_scan` at `hexctl.py:16862-16879` reads 65,536-byte chunks and carries `CHECKPOINT_ARCHIVE_SECRET_WINDOW = 64` bytes between them, and the docstring says that window is `longer than the longest pattern prefix`; pattern one's label `(?:[A-Z0-9]+(?: [A-Z0-9]+)* )?` is unbounded, so a match can exceed it. Measured: the 77-byte header `-----BEGIN AAAA BBBB CCCC DDDD EEEE FFFF GGGG HHHH IIII JJJJ PRIVATE KEY-----` matches in memory, is refused when it straddles a chunk boundary starting 20 bytes before the edge, and passes the scan when it starts 70 bytes before the edge; the 35-byte OpenSSH header is refused at both offsets, and the 77-byte header wholly inside one chunk is refused. Every real PEM label is short enough to be caught, so the exposure is to crafted rather than accidental content. | open, not fixed in this round: the repair is code only, bounding pattern one's label and deriving the window from the patterns' maximum match length, but it rewrites the same tuple S2-R1-04 needs a study amendment to change, and any window fixed to today's patterns is stale the moment that amendment lands. |

Leads not pursued: Four leads, none a criterion defect. First, the export learns the signer fingerprints from the commits it is about to verify rather than from an independent pin: the first pass in `_checkpoint_archive_proof` reads `%GF` for each receipted commit against the operator's default `GNUPGHOME` with no environment override, exports exactly those fingerprints, and grants them ultimate ownertrust in the disposable keyring, so `G` means signed by a key already in the operator's public keyring. That is honest for a transcript the receiver recomputes, which is what reference line 659 calls it, and the manifest publishes the fingerprints for the receiver to judge; it also means export refuses `signature-unverified` on a machine whose keyring lacks the signer, which no test covers. Second, the disposable `GNUPGHOME` is created by `tempfile.mkdtemp` in the system temporary root, not under a scratch root, where register entry `keyring-handling` says `created 0700 under the scratch root`; `hexctl.py:17133-17142` gives the reason, that an agent socket path is capped near 104 bytes and the derived store path is too long, and export has no `--scratch` option to place it under. The reference pins that clause only for restore, at line 276, and the other four clauses hold: mode 0700, seeded only from the archive's key member, removed in a `finally` that every refusal path reaches through `SystemExit`, and never the operator's keyring. The `agent never started` clause also holds in fact even though `--no-autostart` cannot reach the `gpg` that `git` spawns: a disposable home seeded the same way, then driven through `git -c gpg.program=gpg log --format=%G?` against `b5e556cf`, returned `G` and left no `S.gpg-agent` socket and no agent process. Third, `_checkpoint_archive_guarded` catches every `Exception` and every non-zero `SystemExit` from the readers it wraps and reports one bounded class, so a genuine defect inside `_checkpoint_snapshot` or identity minting surfaces as `manifest-mismatch` or `identity-unavailable` rather than a traceback; that is the single-refusal-class shape the Disciplines field asks for and it fails closed, at the cost of telling an operator which of the two happened. Fourth, a killed export leaves an orphaned hidden `.<boundary>.stage-` directory in the store with no reaper; the boundary directory is unaffected, because it appears only by no-replace rename, so this is litter rather than a partial publish. Two mutations survived that are not defects: deleting the `os.lexists` boundary reservation at `hexctl.py:16973` left all 17 tests green because the no-replace rename still refuses and `_checkpoint_archive_guarded` still reports `boundary-occupied`, and deleting the separate `state.json` and `ledger.jsonl` scan left all 17 tests green because both files are also capsule members and the per-member scan catches them; in both cases a deeper control holds, and the observation is that the tests cannot say which control fired. Verified positives. Six of ten anchored mutations on a detached copy at `/private/tmp/fiat-861-mut-s2r1`, restored to zero modifications and since removed, were caught: dropping `pack.threads=1`, relaxing the trailer count from exactly one to at least one, replacing the fixed `(1980, 1, 1, 0, 0, 0)` entry timestamp with the build time, changing the entry mode from 0644 to 0755, skipping the per-member secret scan, and taking the outer digest from the manifest instead of the packed bytes. The diff is additive: 1,872 insertions across four files with one deleted line, the `checkpoint` subparser help string, so `cmd_checkpoint_export`, `cmd_checkpoint_restore`, `fiat-controller-checkpoint/v1`, the compatibility set and the `snapshot_id` derivation are byte-unchanged, as the Files field requires. All eleven export refusal classes are reachable, with 1, 2, 2, 1, 8, 1, 15, 2, 1, 2 and 12 sites respectively in Exit order; `identity-unavailable` is reached only through `_checkpoint_archive_guarded`, and `bundle-incomplete` and `identity-unavailable` are the two with no test that provokes them. `subprocess-argv` holds at the reader: `bounded_probe` uses `shell=False`, a list argv, `stderr=subprocess.STDOUT` into a capped captured pipe so no child stream reaches a diagnosis, and a hard timeout; every ref reaching `git bundle create` is prefixed `refs/heads/`, every commit is `COMMIT_RE`-validated, and `tempfile.mkdtemp` returns an absolute path so the stage and bundle paths cannot begin with a dash or resolve against the child's differing cwd. `concurrent-mutation` holds through the existing `_checkpoint_read_staged`, which checks `S_ISREG`, `st_nlink == 1`, the ceiling and the stat identity before, after and by name, with `O_NOFOLLOW`; the register itself states the same-account boundary that the three separate reads of a staged member leave open. Members are written by descriptor with `O_EXCL` and `O_NOFOLLOW` at 0600, the archive and sidecar are chmodded 0600, the sidecar is written before the rename, and the stage is removed under `dir_fd` on every unpublished path. `outer-digest` is recomputed from the packed file by `_checkpoint_archive_digest`, never read from inside the archive. `acceptance-self-reference` holds and is correctly scoped: no `acceptance/` entry is written, `acceptance.current` is the literal `outside`, and study line 316 and reference line 309 both record `acceptance/prior/` as reserved with its schema an open item, so the unused `CHECKPOINT_ARCHIVE_ACCEPTANCE_DIR` constant is a reservation rather than a gap. The manifest's closed fields match the reference table row for row, including `identity` as `status` `bound` with `snapshot_id` or `unavailable` with a closed reason, and the seven ceiling constants equal the reference's seven values. Commands and exits, all observed here. On the clean detached snapshot `/private/tmp/fiat-861-verify-s2r1` of `b5e556cf`, tree `4055316fc98eecb0c358648bcd899e521d072b12`, which equals the recorded `LAST_GREEN`, and since removed: `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` exited 0 with `outcome green` over 13 selected checks and 22 omitted, the Hexaemeron shard taking 1,411.0 s, the root shard 209.7 s and `demonstrations-suite` passing first time at 28.1 s; `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6` exited 0 with 2,451 of 2,451 passed, 0 skipped, 0 errors, 424.1 s wall and 255,229,952 bytes peak child RSS; `python3 -m unittest discover -s tests` exited 0 with `Ran 1626 tests in 169.509s` and `OK`. The three ran from 02:35:49Z to 03:09:50Z and no `WAI-E-ADAPTER.TIMEOUT` appears in any log, so issue #1175's family did not recur. In the run worktree `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, the same for `ephoros.py`, and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md `.agents/skills/promise-machine/SKILL.md` `.agents/skills/promise-machine/PORTABLE.md` plugins docs` each printed `clean` and exited 0; `horos.py check .` reported `boundary matches the tree` and exited 0; `git diff --check` exited 0. `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` was re-run from 03:10:51Z to 03:16:13Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report the implementing session wrote at 01:21, so the `step:3` transition's evidence agrees. The tracked digest binding is current: `hexctl.py` hashes to `b8bf465b7b3b769b417514a368edc88df9382064eff50f790e803956ccc41fb5` and `tests/promise_machine_coverage.json` carries that value in all 11 places, with the previous digest absent. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 2 -- 2026-09-09T05:41:10Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. The four `not-applicable` ids keep round 1's reason: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls Step 4 introduces, `hostile-fixture-set` names the inspector test module Step 3 introduces, and neither `checkpoint inspect` nor `checkpoint restore --archive` exists to run. This round is a re-check rather than a fresh survey: round 1 covered the diff once, and the seven ids it reviewed against the export half only -- `zip-slip`, `name-collision`, `zip-bomb`, `trailing-data`, `manifest-join`, `identity-join` and `version-compatibility` -- are re-reviewed on the same terms, because nothing in this round's diff moved them. The two paths the repairs touch were re-verified end to end: the secret scan from `CHECKPOINT_ARCHIVE_SECRET_PATTERNS` at `hexctl.py:620` through `_checkpoint_archive_scan` at `hexctl.py:16899` to its three call sites and the capsule and inventory walks that feed them, and the proof from `_checkpoint_archive_proof` through the disposable keyring, the material export, the commit read and the trailer count. No `hexctl` command was run, as the brief requires, so no archive was exported, inspected or restored and S2-R2-02 is read from code and a direct call to the shipped scan rather than from a refusal of the command. The step's own budgets were not measured; Step 5 owns that. Nothing under `.hexaemeron/` was hand-edited: the only write there was the named resolver, which rewrote its report to the same bytes. `.horos/boundary.json` was not regenerated -- only the census was, as the packet's per-commit recipe names -- so `horos.py check .` reports candidate drift on this file and its synopsis while still exiting 0 on `boundary matches the tree`.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Round 1: both acceptance guards in `_checkpoint_archive_proof` were untested, so the archive's central security claim had no regression cover. Two anchored mutations on a detached copy of `b5e556cf` left all 17 tests green: relaxing `status != "G"` at `hexctl.py:17330` to `status not in ("G", "U")` ran 17 tests in 115.130s and exited 0; deleting the `fingerprint not in fingerprints` clause at `hexctl.py:17331` ran 17 tests in 106.574s and exited 0. The fixture cannot reach either branch, because `_checkpoint_archive_seed_keyring` writes ultimate ownertrust for the discovered fingerprints, so the happy path is always `G` and always in-set. | fixed in `cbb6be78`. The two tests the amended Exit names by behaviour now exist. `test_signature_unverified_refuses_before_publish_on_a_status_other_than_good` seeds the disposable keyring with the key and no ownertrust, which is what an operator meets when the material travels and the trust does not; measured first on a throwaway key in a scratch repository, since removed, where `%G?` answered `U` and `git verify-commit` still exited 0, so the status comparison is the only check that can refuse. `test_signature_unverified_refuses_before_publish_on_an_unpinned_fingerprint` returns an unpinned fingerprint from the verification pass alone, leaving status `G` and both trailers correct. Both are parent-red against the greened tree `2153634d0f14f6023d9e7f26ab4aa0c1b710d82a`: the same two mutations, now at `hexctl.py:17366` and `hexctl.py:17367`, each fail exactly one test, 1 of 21. |
| S2-R1-02 | medium | `.hexaemeron/runbook.md` | Round 1: the Exit's `Ran 17 tests` and the Disciplines field's requirement that an audit repair land with a new test were jointly unsatisfiable, and the Files field authorised no second test module, so every code repair this step's audit loop could propose was blocked, S2-R1-01 included. Observed then: the module reported `Ran 17 tests in 107.897s` and `OK` at `b5e556cf`. | closed by the runbook amendment of 2026-09-09, `d68d063425777a6349473fdb4a7dda6e3e358956c2e15f1b9664c2c26d2ae364`, rather than by a commit; the runbook is now `6ada57cd96bfca73e3affaf39d52b734b9a98b11b9bec42e74b2ac544e0e5078` and step 2's effective text `dce16841970bc2e03ec0c11f800ef719e4305e3e5c5088910decbe58bbbbd6e9`. The Exit reads "at least 17 tests", naming the two guards by behaviour. Checked against what this round built: the module runs 21 tests and exits 0, and both named guards exist. The Tests field still reads "Fifteen new tests" and lists fifteen names; the amendment replaced Exit only, and that field states the scope step 2 opened rather than a ceiling, so it does not conflict with the twenty-one. |
| S2-R1-03 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Round 1, recorded then against `.hexaemeron/study.md`: the Exit, study line 744 and reference lines 77 to 78 each required the bundle to be built by `git -c pack.threads=1 bundle create` from the bounded ref set `with --no-tags`, and that argument does not exist. Measured on git 2.50.1 (Apple Git-155): the flag before or after the ref both answer `error: unrecognized argument: --no-tags`. `hexctl.py` omits it and is correct to. | fixed in `cbb6be78`. The study amendment `b9ce9abbea8284b1b73bacace8f27444c959bdd469edeac871528c9cd45e0c9f` and the runbook amendment withdrew the clause; reference line 78 now states that no `--no-tags` argument is passed, bundle creation having no such option and the explicit ref list already excluding tags. Line 263, the restore `git fetch +refs/heads/*:refs/heads/*`, is unchanged, because `git fetch` does take the flag. Study section 9 line 744 still carries `--no-tags` in its subprocess-control list: those bytes are controller-pinned and the amendment withdrew the statement in prose rather than editing the section. |
| S2-R1-04 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Round 1: the six patterns at `hexctl.py:612-619` were five distinct ones, pattern two being fully subsumed by pattern one, which matches the OpenSSH header on its own; and none matched `-----BEGIN PGP PRIVATE KEY BLOCK-----`, whose header ends `PRIVATE KEY BLOCK-----` rather than `PRIVATE KEY-----`, which is the one private-key armour a command exporting OpenPGP material is most likely to meet. | fixed in `cbb6be78`. The subsumed OpenSSH pattern is dropped and the OpenPGP block added, keeping six, as the study's 2026-09-09 amendment requires; the study is now `1707277ff638740d6a36b48c0b04cd08329366efb1c62c0dd3bf23d780f686e5` and the reference's bullet list follows it. Measured through the shipped `_checkpoint_archive_secret_shaped`: `-----BEGIN PRIVATE KEY-----` and the `RSA`, `EC`, `ENCRYPTED` and `OPENSSH` labels all refuse, so nothing the old set caught is released, and `-----BEGIN PGP PRIVATE KEY BLOCK-----` now refuses; `-----BEGIN PGP PUBLIC KEY BLOCK-----` and `-----BEGIN CERTIFICATE-----` pass, so the exported `proof/pubkey.asc` member is not caught by its own scan. Removing the OpenPGP pattern fails four cases across three of the 21 tests. |
| S2-R1-05 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Restated, and round 1's number withdrawn. Its 77-byte specimen `-----BEGIN AAAA BBBB CCCC DDDD EEEE FFFF GGGG HHHH IIII JJJJ PRIVATE KEY-----` does not exist under the amended pattern set: the longest header the six can match is 37 bytes and the former 64-byte window covered it, as the study amendment records. What stands is narrower. `CHECKPOINT_ARCHIVE_SECRET_WINDOW = 64` was a bare literal derived from nothing, and pattern one's label `(?:[A-Z0-9]+(?: [A-Z0-9]+)* )?` was unbounded, so no length existed to derive it from and the docstring's claim that the window is "longer than the longest pattern prefix" was unprovable as written. | fixed in `cbb6be78`. `CHECKPOINT_ARCHIVE_SECRET_LABEL` at `hexctl.py:612` bounds the label to four words of sixteen uppercase-alphanumeric characters, `CHECKPOINT_ARCHIVE_SECRET_HEADERS` at `hexctl.py:636` declares the longest run each of the six patterns needs in view, and `hexctl.py:651` is `max(map(len, CHECKPOINT_ARCHIVE_SECRET_HEADERS))`, measured at 95 bytes. `test_secret_shaped_member_refuses_across_a_chunk_boundary` asserts one-to-one pattern-to-header matching, asserts the window equals that maximum, and plants each header across the 65,536-byte chunk boundary at three offsets, ending at the worst case of `len - 1` bytes before it. Reverting the window to a bare 64 fails that test, 1 of 2. The bound is a judgement rather than a measurement: a label longer than four sixteen-character words would be missed rather than refused. |
| S2-R2-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Step 1's reference-to-study binding read study section 4's bullet only, so a dated study amendment replacing a pinned value left the equality green while the reference and the effective study disagreed. Observed here: after `cp .hexaemeron/study.md docs/fiat-checkpoint-archive-study.md` brought the 2026-09-09 amendment into the tracked copy at `1707277ff638740d6a36b48c0b04cd08329366efb1c62c0dd3bf23d780f686e5`, all five Step 1 binding tests passed unchanged -- `CheckpointArchiveScaffoldTests` 2 tests in 0.002s `OK`, and `tests/test_fiat_checkpoint_archive_record.py` 3 tests in 0.002s `OK` -- although that amendment had replaced one of the six patterns those tests pin. The packet expected them to fail; they did not, and the gap is why. | fixed in `cbb6be78`. `EXPECTED_AMENDED_SECRET_SPANS` holds the reference while `EXPECTED_SECRET_SPANS` still holds section 4, the test reads the 2026-09-09 amendment and requires it to name both the dropped and the added span, and the compiled pattern tuple is held equal to the reference's five spans with the PEM pattern checked by what it must and must not match. Reverting the reference bullet to the OpenSSH header fails that test, 1 of 2. The binding still reaches only an amendment a test names by date, so a later amendment needs the same edit. |
| S2-R2-02 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | `checkpoint archive` cannot archive a run whose study documents the secret patterns, which includes this one. `_checkpoint_archive_capsule` calls `_checkpoint_snapshot(state_root(base_dir), controller_stage)`, which copies every file under `.hexaemeron/` except `lock` into `controller-capsule/controller/`; `_checkpoint_archive_inventory` at `hexctl.py:17472` is an `os.walk` of the whole stage; and the export runs `_checkpoint_archive_scan` on every member it returns. `.hexaemeron/study.md` carries `-----BEGIN OPENSSH PRIVATE KEY-----` at lines 591 and 948, which is its own statement of the pattern list rather than a secret. Measured by calling the shipped `_checkpoint_archive_scan` on that path: exit 1, stderr exactly `secret-shaped-member\n`. Three further controller files match the same string: `.hexaemeron/candidates/study-amend-1.md`, `-2.md` and `-3.md`. `.hexaemeron/runbook.md`, `state.json` and `ledger.jsonl` each exit 0. Both the pre-amendment and the amended pattern sets match it, through pattern one in each case, so S2-R1-04's repair neither introduced nor removed it. Not observed as a refusal of the command itself: the packet forbids running `hexctl`, so the join from the scan to `cmd_checkpoint_archive` is read from those three code paths rather than executed. | open, not fixed in this round. The repair is a criterion change only Fiat can author: either scope the scan away from the run's own specification text, or restate the study's "every opaque controller file" clause so a quoted pattern is not a member. The study's bytes and the scan's declared scope are both controller-pinned, so an audit round can change neither. It fails closed -- nothing is published and no secret escapes -- so the cost is that the step's own command cannot run on the run it was built for, not a disclosure. |
| S2-R2-03 | low | `docs/fiat-checkpoint-archive-study.md` | The Exit requires `python3 plugins/brevitas/skills/brevitas/scripts/brevitas.py <path>` to exit 0 for every changed Markdown file, and two of this round's changed Markdown files are byte-copies of `.hexaemeron/` sources the run may not edit. Measured over all four of this round's changed Markdown files: `docs/fiat-checkpoint-archive-study.md` exits 1 with 12 findings, `docs/fiat-checkpoint-archive-runbook.md` exits 1 with 1, `1543: B027 claim stacks 2 qualifiers; maximum is 1`, this audit record exits 1 with 5, three `B011` and two `B027`, and its generated synopsis exits 1 with 2. Each of the four exits 1 at the step head `8f9f10e8`; the first three carry the same counts there, and the synopsis carried 1, gaining one because the generator writes a summary line per round and each such line stacks five qualifiers. `.hexaemeron/study.md` and `.hexaemeron/runbook.md` exit 1 as well, so neither the re-copy nor this round's prose introduced any of it: the one `B027` this round's own text did add, at the leads paragraph, was removed before the record was committed. The record's five are round 1's and Step 1's, and audit prose is byte-pinned once written. `imprimatur.py` exits 0 on all four, and both lints exit 0 on the reference. | open, not fixed in this round. The bytes are fixed by the controller and the copies must equal them, so the repair is a criterion change only Fiat can author, scoping the clause to Markdown the step itself writes. |

Leads not pursued: Round 1's four leads stand unchanged and were not re-worked: the export learns the signer fingerprints from the commits it verifies rather than from an independent pin, the disposable `GNUPGHOME` sits in the system temporary root rather than under a scratch root for the socket-length reason `hexctl.py` gives, `_checkpoint_archive_guarded` collapses a genuine defect in a wrapped reader into one bounded class, and a killed export leaves an orphaned hidden stage directory with no reaper. Four leads are new to this round, none a criterion defect. First, the git bundle is itself a scanned member, so repository text carrying one of the six patterns passes through the scan; pack data is deflate-compressed, which is why it does not in practice, and no bundle of this repository was built to confirm it, so the claim is reasoned rather than measured. Second, `github_pat_[A-Za-z0-9_]{22,}` keeps an open tail, and its declared header is the 33-byte minimum; that is sound because the tail repeats one character class, so a minimal match exists wherever the pattern matches at all, but the reasoning lives in a docstring and no test enforces it. Third, the PEM label bound of four sixteen-character words is a judgement: every armour label in use is at most nine characters, and the bound only has to exist for the window to be derivable, but a longer label would now be missed rather than refused. Fourth, three module-level constants had to be renamed from `SECRET_SPAN_*` and `SECRET_AMENDMENT_HEADING` to `SUBSUMED_PATTERN_SPAN`, `ADDED_PATTERN_SPAN` and `PATTERN_AMENDMENT_HEADING` because Phylax P004 reads a `secret`-named string literal as a credential; the older `EXPECTED_SECRET_SPANS` escapes only because it is a tuple, so the rule is name-shaped rather than value-shaped here. Verified positives. Five anchored mutations on a detached copy at `/private/tmp/fiat-861-mut-s2r2`, cut from the greened snapshot `cdeedf4a` and since removed after restoring to tree `2153634d0f14f6023d9e7f26ab4aa0c1b710d82a` with zero modifications, were all caught: relaxing the status guard to admit `U` failed 1 of 21; deleting the fingerprint comparison failed 1 of 21; reverting the window to a bare 64 failed 1 of 2; removing the OpenPGP pattern failed 4 cases across 3 names of 21, `test_archive_export_refuses_secret_shaped_member`, `test_archive_reference_names_every_refusal_class_and_fixture_id` and `test_secret_shaped_member_refuses_across_a_chunk_boundary`; and reverting the reference bullet failed 1 of 2. `keyring-handling` still holds under the new tests: the stub in the status test reuses the same disposable home and bounded environment and skips only the ownertrust import, so mode 0700, `--no-autostart`, seeding from the archive's own key member and removal in the `finally` are untouched, and the operator's keyring is never opened. `diagnostic-leak` holds: the two new refusal assertions read stderr as exactly `signature-unverified\n`, and the scan test as exactly `secret-shaped-member\n`, so no path, member name or `gpg` output travels with either. `subprocess-argv` holds: this round adds no production subprocess site, and the one call a test makes goes through `bounded_run` with the same fixed list argv. The diff is confined: `hexctl.py` changes only the constants block at lines 612 to 656, so `_checkpoint_archive_proof`, `cmd_checkpoint_export`, `cmd_checkpoint_restore`, `fiat-controller-checkpoint/v1`, the compatibility set and the `snapshot_id` derivation are byte-unchanged. Commands and exits, all observed here. Artefacts re-copied and compared: `cmp` exits 0 for both pairs, `.hexaemeron/study.md` and `docs/fiat-checkpoint-archive-study.md` both `1707277ff638740d6a36b48c0b04cd08329366efb1c62c0dd3bf23d780f686e5`, `.hexaemeron/runbook.md` and `docs/fiat-checkpoint-archive-runbook.md` both `6ada57cd96bfca73e3affaf39d52b734b9a98b11b9bec42e74b2ac544e0e5078`, each equal to the packet's digest. `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive -v` ran 21 tests in 93.344s and exited 0, at or above the Exit's floor of 17. On the clean detached snapshot `/private/tmp/fiat-861-snap-s2r2` of tree `2153634d0f14f6023d9e7f26ab4aa0c1b710d82a`, which is the tree `LAST_GREEN` names and the tree `cbb6be78` carries, from 05:00:43Z to 05:27:05Z: `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` exited 0 with `outcome green` over 13 selected checks and 22 omitted, the Hexaemeron shard taking 899.0s and the root shard 178.8s; `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6` exited 0 with 2,455 of 2,455 passed, 0 skipped, 0 errors, 442.2s wall and 258,932,736 bytes peak child RSS, four more tests than round 1's 2,451; `python3 -m unittest discover -s tests` exited 0 with `Ran 1626 tests in 207.532s` and `OK`. No `WAI-E-ADAPTER.TIMEOUT` appears in any log, so issue #1175's family did not recur, and no suite needed a re-run. In the run worktree `phylax.py plugins tests`, `ephoros.py plugins tests` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md `.agents/skills/promise-machine/PORTABLE.md` plugins docs` each printed `clean` and exited 0; `horos.py check .` reported `boundary matches the tree` and exited 0; `git diff --check` exited 0; `imprimatur.py` exited 0 on all three changed Markdown files and `brevitas.py` exited 0 on the reference, with the two artefact copies recorded as S2-R2-03. `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` was re-run from 04:48:37Z to 04:52:14Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report round 1 read, so the `step:3` transition's evidence is unchanged. Elenchus ran `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/fiat-861-step-2.json` against `cbb6be78` in format `unittest-json-v1` for schema `elenchus.unittest.v1`, and returned `guarded` with the detail `the runner report records a parent assertion failure`; the parent report is complete with 2,455 executed, 5 assertion failures, 0 errors and 0 skipped, and the changed files it compared were `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` and `tests/promise_machine_coverage.json`. The tracked digest binding is current: `hexctl.py` hashes to `2041b77815de35fa1e4277948ea5385822e2efc52d29f344eca6f6c13afedd73` and `tests/promise_machine_coverage.json` carries that value in all 11 places, with the previous `b8bf465b7b3b769b417514a368edc88df9382064eff50f790e803956ccc41fb5` absent. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 3 -- 2026-09-09T16:12:52Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite stays waived by the brief because this run ships no Solidity, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. The four `not-applicable` ids keep round 1's reason: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls Step 4 introduces, `hostile-fixture-set` names the inspector test module Step 3 introduces, and neither `checkpoint inspect` nor `checkpoint restore --archive` exists to run. This round re-checks two repairs rather than re-surveying the step. The diff is confined and that was measured rather than read: across `b5e556cf`, `cbb6be78` and `5780d852` the extracted bodies of `_checkpoint_archive_scan`, `_checkpoint_archive_proof`, `_checkpoint_archive_capsule`, `_checkpoint_archive_inventory`, `cmd_checkpoint_archive`, `cmd_checkpoint_export`, `cmd_checkpoint_restore` and `_checkpoint_snapshot` and the `CHECKPOINT_SCHEMA` block hash to the same value at all three, and `fiat-controller-checkpoint/v1` occurs once at each, so only `_checkpoint_archive_secret_shaped` and the secret constants at `hexctl.py:612-710` moved. The seven ids round 1 reviewed against the export half only -- `zip-slip`, `name-collision`, `zip-bomb`, `trailing-data`, `manifest-join`, `identity-join` and `version-compatibility` -- are re-reviewed on the same terms, because nothing in this round's diff moves them. No `hexctl` command was run, as the brief requires, so no archive was exported, inspected or restored: the scan is exercised by calling the shipped `_checkpoint_archive_secret_shaped` and `_checkpoint_archive_scan` in process under `PYTHONDONTWRITEBYTECODE=1`, and the join to `cmd_checkpoint_archive` is read from the three call sites at `hexctl.py:17913`, `hexctl.py:17919` and `hexctl.py:17952`. The step's own budgets were not measured; Step 5 owns that, and the lookahead this round examines is a term S2-R3-01's second repair route would have to be measured against. Nothing under `.hexaemeron/` was written. `.horos/boundary.json` is still not regenerated, as round 2 recorded, so `horos.py check .` reports candidate drift on the audit record, 66,285 bytes recorded against 153,986 on disk, and on its synopsis, 66,682 against 154,651, while still exiting 0 on `boundary matches the tree`.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Rounds 1 and 2: both acceptance guards in `_checkpoint_archive_proof` were untested, so the archive's central security claim had no regression cover; two anchored mutations on a detached copy of `b5e556cf`, relaxing `status != "G"` to admit `U` and deleting the `fingerprint not in fingerprints` clause, each left all 17 tests green. | closed in `cbb6be78` and unchanged here. `test_signature_unverified_refuses_before_publish_on_a_status_other_than_good` at line 1592 and `test_signature_unverified_refuses_before_publish_on_an_unpinned_fingerprint` at line 1625 both still exist, which the amended Exit names by behaviour. Re-checked rather than re-mutated: the extracted body of `_checkpoint_archive_proof` hashes the same at `b5e556cf`, `cbb6be78` and `5780d852`, so neither guard's subject moved, and round 2's measurement of 1 of 21 failing per mutation stands. |
| S2-R1-02 | medium | `.hexaemeron/runbook.md` | Rounds 1 and 2: the Exit's `Ran 17 tests` and the Disciplines field's requirement that an audit repair land with a new test were jointly unsatisfiable, and the Files field authorised no second test module. | closed by the runbook amendment of 2026-09-09 rather than by a commit, and the current amendment `37dff5a2881d58774b2e8067639a277c58fa5e7a8a215d6b88643cdbd5c683e5` carries the same floor. Measured here on the fix tree: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive` ran 23 tests in 247.039s and exited 0, six above the floor of 17, and this round's repair added the eighteenth to twenty-third without a count clause to break. |
| S2-R1-03 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Rounds 1 and 2: the Exit, study line 744 and reference lines 77 to 78 each required the bundle to be built `with --no-tags`, and `git bundle create` has no such argument; measured on git 2.50.1 (Apple Git-155) it answers `error: unrecognized argument: --no-tags` with the flag before or after the ref. | closed in `cbb6be78` and unchanged here. The current Exit states that no `--no-tags` is passed because the command has no such option and the explicit ref list already excludes tags. Study section 9 still carries the phrase in its subprocess-control list, as round 2 recorded: those bytes are controller-pinned and the amendment withdrew the statement in prose rather than editing the section. |
| S2-R1-04 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Rounds 1 and 2: the six patterns were five distinct ones, the OpenSSH header being subsumed by the PEM pattern, and none matched `-----BEGIN PGP PRIVATE KEY BLOCK-----`. | closed in `cbb6be78` and re-measured here through the shipped `_checkpoint_archive_secret_shaped`, now as blocks: `-----BEGIN PRIVATE KEY-----`, `-----BEGIN OPENSSH PRIVATE KEY-----` and `-----BEGIN PGP PRIVATE KEY BLOCK-----` each with one base64 body line all refuse, and `-----BEGIN PGP PUBLIC KEY BLOCK-----` with the same body line passes, so the exported `proof/pubkey.asc` member is still not caught by its own scan. The six compiled patterns are `CHECKPOINT_ARCHIVE_SECRET_BLOCK_PATTERNS + CHECKPOINT_ARCHIVE_SECRET_TOKEN_PATTERNS` and remain six. |
| S2-R1-05 | low | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Rounds 1 and 2: `CHECKPOINT_ARCHIVE_SECRET_WINDOW` was a bare literal 64 derived from nothing, and pattern one's label was unbounded, so no length existed to derive it from. | closed in `cbb6be78` and widened here. Measured on the fix tree: the longest declared header is 95 bytes, `CHECKPOINT_ARCHIVE_SECRET_BLOCK_LOOKAHEAD` is 1,792, and `CHECKPOINT_ARCHIVE_SECRET_WINDOW` is 1,887, the sum. The carry is exactly sufficient and not more: a header whose end lies more than 1,792 bytes before a chunk edge has its whole decision region inside that chunk, and one closer than that starts at or after `len - 1,887`. The four-word sixteen-character label bound is still the judgement round 2 recorded. |
| S2-R2-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | Round 2: Step 1's reference-to-study binding read study section 4's bullet only, so a dated study amendment replacing a pinned value left the equality green while the reference and the effective study disagreed. | closed in `cbb6be78`, with the residual round 2 named now visible. The binding reads the study slice anchored at `### Amendment -- 2026-09-09`, and `anchored` splits on the first occurrence; the study now carries two headings with that exact text, at lines 938 and 972. The first is the pattern amendment the test means, so the binding still holds, but it selects it by append order rather than by anything the test states. The second amendment, which is the one this round's code implements, is bound by no test. |
| S2-R2-02 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Round 2: `checkpoint archive` could not archive a run whose study documents the secret patterns, which includes this one. `.hexaemeron/study.md` carried `-----BEGIN OPENSSH PRIVATE KEY-----` at lines 591 and 948 as its own statement of the pattern list, the shipped `_checkpoint_archive_scan` exited 1 on it with stderr exactly `secret-shaped-member\n`, and the three `.hexaemeron/candidates/study-amend-*.md` matched the same string. | fixed in `5780d852` under the study's second 2026-09-09 amendment, and re-measured here across the whole capsule source rather than the one file the finding named. `_checkpoint_archive_scan` exits 0 on all 104 paths under `.hexaemeron/`, `study.md` included, whose armour headers now number four at lines 591, 948, 976 and 985, and on the three candidates, `state.json`, `ledger.jsonl`, the two tracked copies, the reference, this record and its synopsis. The repair is guarded through the command and not only at the predicate: `test_secret_shaped_member_refuses_before_publish_on_a_truncated_key_block` runs the real `checkpoint archive` against a fixture run, refuses on a body-only witness and on a footer-only witness, asserts nothing reached the store on either, then plants the header in prose and requires the export to complete. Six anchored mutations of the new logic left no survivors. What the repair also released is S2-R3-01. |
| S2-R2-03 | low | `.hexaemeron/runbook.md` | Round 2, recorded then against `docs/fiat-checkpoint-archive-study.md`: the Exit required `python3 plugins/brevitas/skills/brevitas/scripts/brevitas.py <path>` to exit 0 for every changed Markdown file, and four of the step's changed Markdown files are bytes the run may not edit -- the append-only audit record, its generated synopsis and the two digest-bound copies. | closed by the runbook amendment of 2026-09-09, `37dff5a2881d58774b2e8067639a277c58fa5e7a8a215d6b88643cdbd5c683e5`, rather than by a commit; the clause now reads "for every changed Markdown file this run may edit, which excludes the append-only audit record and its synopsis and the two digest-bound copies of the study and runbook". Measured on the step diff `4887f2a3..5780d852`: exactly five Markdown files changed, the four excluded ones and the reference. `brevitas.py` exits 1 on the four with 5, 2, 1 and 12 findings, the same counts round 2 measured, and exits 0 on the reference; `imprimatur.py` exits 0 on all five. The scope is exact rather than approximately right: no changed Markdown file this run owns is excluded, and no excluded file is one it owns. |
| S2-R3-01 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | The block rule releases a private key carried on one line, which is the shape the members the scan singles out give it. The body witness `CHECKPOINT_ARCHIVE_SECRET_BODY` at `hexctl.py:668` is `^[A-Za-z0-9+/=]{16,}[ \t]*\r?$` under `re.MULTILINE`, so it needs a whole physical line, and the footer witness has to fall within `CHECKPOINT_ARCHIVE_SECRET_BLOCK_LOOKAHEAD`, 1,792 bytes of the header's end. A PEM key held as a JSON string value has no physical line break at all, and past 1,792 bytes it has no footer in view either. Measured through the shipped `_checkpoint_archive_scan` on ephemeral RSA keys generated in `/private/tmp` for this round and deleted after it. As raw members the 2,048-, 3,072- and 4,096-bit keys are 1,704, 2,484 and 3,268 bytes, carry 26, 38 and 50 body lines and all exit 1 with stderr exactly `secret-shaped-member\n`. As `json.dumps({"deploy_key": <pem>})` the same three are 1,750, 2,542 and 3,338 bytes, carry no body line at all, and put their footer 1,678, 2,470 and 3,266 bytes past the header's end: the 2,048-bit member still exits 1 because 1,678 is inside the lookahead, and the 3,072- and 4,096-bit members exit 0 with no refusal. The same three carried as one `ledger.jsonl` line behave identically. The matcher at `cbb6be78` refuses all three one-line members, so this round's repair released them rather than inheriting the hole. The scan's declared targets are `state.json`, `ledger.jsonl` and every opaque controller file; the first two are JSON, in which a PEM value is one line, and a JSON member is what a controller file carrying a credential would be; the risk register's clause `the named secret patterns refuse export` and amended section 4 now disagree over such a member, and the disagreement is a published key rather than only prose. It fails open, which is the opposite direction from S2-R2-02. | open, not fixed in this round. Both repairs are criterion changes only Fiat can author, and the reference restates the same rule, so a dated study amendment comes first either way. Admitting an escaped newline as a body-line delimiter closes it completely; widening the lookahead to the longest armour body the ceilings admit closes the footer-bearing case only, leaves a truncated one-line key open, and changes a carry that is re-read for every 65,536-byte chunk of a bundle up to 1 GiB, which is Step 5's measured budget rather than this step's. |
| S2-R3-02 | low | `.hexaemeron/runbook.md` | Step 2's effective Exit states the sidecar as `<64 lowercase hex> checkpoint.zip\n` with one space, and every other pinned statement of it uses two. The step-2 runbook amendment receipted at ledger row 43, whose bytes at offsets 121,688 to 126,910 hash to `37dff5a2881d58774b2e8067639a277c58fa5e7a8a215d6b88643cdbd5c683e5` exactly as that receipt records, carries the one-space form at offset 122,048, runbook line 1,871. The step's baseline Exit at line 178 carries two spaces and that is the only intra-line double space in it; the amended Exit has none, and a word-level diff of the two Exits shows the six intended edits and nothing else, so the space is the one byte of evidence the re-transcription lost. Three pinned statements disagree with it: study line 75 reads "(two spaces, relative name; `shasum -a 256 -c` reads it)", `hexctl.py:17980` writes `f"{outer}  {CHECKPOINT_ARCHIVE_FILE}\n"`, and `test_hexctl_checkpoint_archive.py:233-234` holds `SIDECAR_SPAN` with two spaces and rejects `SIDECAR_ONE_SPACE` by name in both the study and the reference. Taken literally the command cannot satisfy its own Exit. The three superseded step-2 amendments at runbook lines 723, 1,091 and 1,564 carry the same one space, so this stands from the first re-issue rather than from this round, and no round has recorded it. | open, not fixed in this round: the repair is one clause of one runbook amendment restoring the two spaces, which only Fiat can author. No behaviour is affected, because the code and the study agree and only the Exit's transcription is wrong. |
| S2-R3-03 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | The reference's new block paragraph is pinned by nothing, so the contract home the later steps read can drift from the study and from the code with every gate green. A grep for the path over `plugins`, `tests` and `scripts` finds it named only in `test_hexctl_checkpoint_archive.py`, bound at line 104 and read at three sites: lines 481 and 889 in `CheckpointArchiveScaffoldTests`, and line 1792, which only scans the file. Four anchored edits to the paragraph, each run against all three sites -- `CheckpointArchiveScaffoldTests` with `tests/test_fiat_checkpoint_archive_record.py`, 5 tests, and `CheckpointArchiveSecretScanTests`, 3 tests -- all exit 0 with `OK`: widening "one whole line of base64 body" to "two whole lines" (R1), deleting the paragraph outright (R2), dropping "plus that lookahead" from the carry sentence (R3), and inverting "A key whose footer was truncated still carries body lines and still refuses" to say it publishes (R4). The baseline before them exits 0 on both. This is the class Step 1 recorded as S1-R4-02 and S1-R6-02 and closed by folding pins into the inventory test, and the paragraph is new in `5780d852`. | open, not fixed in this round: the repair is a test, which an audit round may author and the Exit's floor of 17 now permits, but the paragraph restates the study rule S2-R3-01 asks Fiat to amend, so a pin written now binds text the next amendment moves. It belongs with the round that applies that amendment. |

Leads not pursued: Round 1's four leads and round 2's four stand unchanged and were not re-worked: the export learns the signer fingerprints from the commits it verifies rather than from an independent pin, the disposable `GNUPGHOME` sits in the system temporary root for the socket-length reason `hexctl.py` gives, `_checkpoint_archive_guarded` collapses a genuine defect in a wrapped reader into one bounded class, a killed export leaves an orphaned hidden stage directory with no reaper, the git bundle is itself a scanned member whose pack data is compressed, `github_pat_` keeps an open tail enforced only by a docstring, the four-word sixteen-character PEM label bound is a judgement, and Phylax P004 reads a `secret`-named string literal as a credential. Seven leads are new to this round, none a criterion defect. First, `hexctl.py` refuses its own scan: the literal `xoxb-` inside `CHECKPOINT_ARCHIVE_SECRET_HEADERS` at line 662 matches token pattern six, so calling `_checkpoint_archive_scan` on the controller script exits 1. It is not a member, the capsule being `.hexaemeron/` and the outer members the seven fixed ones, so this is an observation rather than a refusal path; the two armour headers the file also carries, at lines 622 and 16952, now pass because prose follows them. Second, the study binding reads the slice anchored at `### Amendment -- 2026-09-09` and `anchored` splits on the first occurrence, while the study now carries two headings with that exact text, at lines 938 and 972; the first is the pattern amendment the test means, so the binding holds by append order rather than by anything the test states. Third, an armour block indented as a Markdown code block, carrying body lines but no footer, passes, because the body witness reads a line only from column one; measured, and narrow, since the footer witness still catches an indented block that keeps its footer. Fourth, the margin protecting this run's own documents is wide and self-guarding: no whole line of base64 follows any armour header in `.hexaemeron/study.md` at lines 591, 948, 976 and 985, in the reference at line 207 or in `.hexaemeron/candidates/study-amend-1.md` at line 591, there being no such line anywhere after them in those files, and no matching footer exists at all; `test_secret_scan_passes_the_run_s_own_specification_documents` reads the live documents, so a later amendment that puts a digest on its own line under a quoted header goes red at test time rather than at export. Fifth, the amended Exit says the patterns are "scanned with a chunk overlap derived from the longest header they can match" and the shipped overlap is that maximum, 95, plus the 1,792-byte block lookahead; "derived from" is satisfied and the reference states the two-term rule exactly, so this is under-description rather than the contradiction S2-R3-02 records in the same Exit. Sixth, the block rule costs more on a member packed with armour headers, because each header match runs a 1,792-byte `find` and a body walk where the old code ran one `search` per pattern: measured over five passes on a 65,536-byte buffer, plain filler takes 0.22 ms and a buffer packed with `-----BEGIN PGP PRIVATE KEY BLOCK-----` takes 3.90 ms, which extrapolates to 3.6 s and 63.9 s per GiB against a 1 GiB bundle ceiling and a 256 MiB capsule ceiling. Bundle bytes are deflate-compressed pack data and cannot carry that density, the amplification is bounded at about eighteen times, and the command is local, so this is a cost to know rather than a denial; Step 5 owns the budget it would show up in. Seventh, `5780d852` is authored by `Dr Laurence E. Day <laurence@wildcat.finance>` where every other commit on the branch is authored by `Shoggoth <shoggoth@wildcat.finance>`; it is signed `G` under `B83B60AE16F5DD1A` and carries exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`, as `cbb6be78` and `5f939dd5` do, and no gate this step names reads the author field. Verified positives. The four block cases the packet names each land on the correct side, measured through the shipped predicate and through `_checkpoint_archive_scan`: a bare header with prose after it publishes; a header whose body line begins 1,790 bytes past its end publishes while one at 1,760 refuses, so the boundary is the declared 1,792; a truncated block of header plus body lines refuses; a real PEM key refuses. Every refusal writes exactly `secret-shaped-member\n` to stderr and nothing else, so `diagnostic-leak` holds on the new path. The token half is untouched and still refuses on the match alone, measured on `AKIA` plus sixteen and `ghp_` plus thirty-six, and `-----BEGIN PGP PUBLIC KEY BLOCK-----` with a body line publishes, so the exported `proof/pubkey.asc` member is still not caught by its own scan. The footer witness is derived rather than generic: a PGP header closed by a PEM footer publishes. Six anchored mutations of the new logic, on a detached copy at `/private/tmp/fiat-861-mut-s2r3` cut from `5780d852` at tree `bab844172d2697810f1546c54134cbe7e2506b9d` and restored to zero modifications after each, left no survivors, against a baseline of 23 tests in 247.039s exiting 0: reverting `_checkpoint_archive_secret_shaped` to the header-only `any(pattern.search(...))` failed 6 cases across `test_secret_scan_passes_a_member_that_carries_no_header`, `test_secret_scan_passes_the_run_s_own_specification_documents` and `test_secret_shaped_member_refuses_before_publish_on_a_truncated_key_block`; deleting the body witness failed 14 across `test_archive_export_refuses_secret_shaped_member`, `test_secret_shaped_member_refuses_across_a_chunk_boundary` and the truncated-block test; deleting the footer witness failed 2 in the truncated-block test; replacing the derived footer with a generic `-----END ` failed 1 in `test_secret_scan_passes_a_member_that_carries_no_header`; dropping the lookahead term from `CHECKPOINT_ARCHIVE_SECRET_WINDOW` failed 1 in `test_secret_shaped_member_refuses_across_a_chunk_boundary`; and unanchoring the body regex to a bare base64 run failed 1 in `test_secret_scan_passes_the_run_s_own_specification_documents`. `_checkpoint_archive_scan` is byte-identical at `b5e556cf`, `cbb6be78` and `5780d852`, so the carry loop did not change and only the constant it reads did, and the carry is exactly sufficient: a header ending more than 1,792 bytes before a chunk edge has its whole decision region inside that chunk, and one nearer than that starts at or after `len - 1,887`. The tracked digest binding is current: `hexctl.py` hashes to `bc831c0143669ec71143c99e5689f07fae80ce1d11b3fcfd8094d58bf58eaa48` and `tests/promise_machine_coverage.json` carries that value in all 11 places, with round 2's `2041b77815de35fa1e4277948ea5385822e2efc52d29f344eca6f6c13afedd73` absent. The controller artefacts are the packet's: `.hexaemeron/study.md` and `docs/fiat-checkpoint-archive-study.md` both hash to `6c6162e5ad3a49118920641c6bbdf6d8d27788b9a0b3e30624fd846556080856`, `.hexaemeron/runbook.md` and `docs/fiat-checkpoint-archive-runbook.md` both to `5c49bb074b43e2f0ebe5c7ef4409a0d85a39a37738d40b0332b7387afafa38e0`, and `.hexaemeron/design-evidence.json` to `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91` with `native-subcommands` selected. No step's amended field is left un-bound by a later study amendment: ledger row 42 is the study amendment at 06:13:10Z and rows 43 to 46 are the four runbook amendments at 06:13:48Z to 06:13:49Z that follow it, row 43 being step 2's, whose byte range 121,688 to 126,910 in `.hexaemeron/runbook.md` hashes to the `37dff5a2881d58774b2e8067639a277c58fa5e7a8a215d6b88643cdbd5c683e5` that receipt records. The dated study amendment preceded the code the Files field requires it to precede, 06:13:10Z against the fix commit's 13:53:38Z. The Exit's dropped clause was dropped for a true reason: `python3 scripts/portable_promise_machine.py check` exits 1 in the run worktree because `.agents/skills/promise-machine/runtime` is absent, and that directory is neither tracked nor ignored, so no checkout carries it. `.hexaemeron/measure_design.py --conformance` writes only its report: `main` returns after `resolve_conformance`, which calls `write_report` alone, while `write_record`, the one function that would rewrite `design-evidence.json`, is reached only when `--conformance` is absent, and the report carries no timestamp. Commands and exits, all observed here. On the clean detached snapshot `/private/tmp/fiat-861-snap-s2r3` of tree `bab844172d2697810f1546c54134cbe7e2506b9d`, which is the tree `5780d852` carries and the tree this worktree's `LAST_GREEN` names, the three Exit suites were run verbatim with no `--jobs` flag: from 15:03:51Z to 15:32:59Z, `python3 plugins/hexaemeron/tests/run_tests.py` exited 0 with 2,457 of 2,457 passed, 0 failures, 0 errors, 0 skipped, 483.8 s wall and 229,523,456 bytes peak child RSS on an automatic budget of 12, two tests above round 2's 2,455; `python3 -m unittest discover -s tests` exited 0 with `Ran 1626 tests in 299.516s` and `OK`. `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a` was run twice on that snapshot and that tree, verbatim both times. The first, 15:03:51Z to 15:19:55Z, exited 1 with `outcome red` and `failures test-failure` over 13 selected checks and 22 omitted on an automatic 12-slot budget: `demonstrations-suite` alone failed at 37.1 s, while `hexaemeron-suite` passed at 952.5 s and `root-suite` at 419.6 s. The second, 15:47:44Z to 16:11:58Z with nothing else of this round running, exited 0 with `outcome green`, `demonstrations-suite` passing at 50.5 s, `hexaemeron-suite` at 1,435.5 s and `root-suite` at 351.5 s. The red is the runner's load rather than the diff: `python3 -m unittest tests.test_demonstrations` exits 0 on the same tree twice, 114 tests in 36.634 s with the machine idle and 30.809 s under load, and that module holds wall-clock deadlines -- a five-second poll loop, a child timeout capped at 1,000 ms, `time.sleep(6)` and `time.sleep(8)`. No `WAI-E-ADAPTER.TIMEOUT` appears in any of the four logs, so issue #1175's family did not recur. In the run worktree `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, the same for `ephoros.py`, and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each printed `clean` and exited 0; `horos.py check .` reported `boundary matches the tree` and exited 0; `git diff --check` exited 0; `imprimatur.py` exited 0 on all five changed Markdown files and `brevitas.py` exited 0 on the reference. `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive` ran 23 tests in 247.039s and exited 0, six above the Exit's floor of 17. `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` ran from 15:35:06Z to 15:45:41Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report rounds 1 and 2 read, so the `step:3` transition's evidence is unchanged. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 4 -- 2026-09-09T22:32:14Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite stays waived by the brief, whose `security_suite` field records that this run ships Python, tests and Markdown only, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. The four `not-applicable` ids keep round 1's reason: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls Step 4 introduces, `hostile-fixture-set` names the inspector test module Step 3 introduces, and neither `checkpoint inspect` nor `checkpoint restore --archive` exists to run. The seven ids reviewed against the export half only -- `zip-slip`, `name-collision`, `zip-bomb`, `trailing-data`, `manifest-join`, `identity-join` and `version-compatibility` -- are re-reviewed on the same terms, because nothing in this round's diff moves them. The diff is confined, and that was measured rather than read: with docstrings stripped, the bodies of `_checkpoint_archive_scan`, `_checkpoint_archive_secret_shaped`, `_checkpoint_archive_proof`, `_checkpoint_archive_capsule`, `_checkpoint_archive_inventory`, `cmd_checkpoint_archive`, `cmd_checkpoint_export`, `cmd_checkpoint_restore` and `_checkpoint_snapshot` hash identically at `5780d852`, `8f568888`, `0aeff97b` and `9d3af4c6`, so the whole behaviour change in `0aeff97b` is two module-level constants: `CHECKPOINT_ARCHIVE_SECRET_LINE_BREAK = rb"(?:\x0a|\\n)"` is new, and `CHECKPOINT_ARCHIVE_SECRET_BODY` is rewritten from `^[A-Za-z0-9+/=]{16,}[ \t]*\r?$` under `re.MULTILINE` to the same run between two zero-width delimiters. `9d3af4c6` touches the test module and the census only. No `hexctl` command was run: the scan is exercised by calling the shipped `_checkpoint_archive_scan` and `_checkpoint_archive_secret_shaped` in process under `PYTHONDONTWRITEBYTECODE=1`, both as they ship at `9d3af4c6` and as they stood at `8f568888`, so every before-and-after number below is one measurement against two matchers. The step's budgets were not measured; Step 5 owns them, and round 3's sixth lead, the cost of the block rule on a header-dense member, was not re-measured after the body witness widened. Nothing under `.hexaemeron/` was edited by hand; `measure_design.py --conformance` wrote only its own report. `.horos/boundary.json` is still not regenerated, as rounds 2 and 3 recorded, so `horos.py check .` reports candidate drift on the audit record, 66,285 bytes recorded against 181,890 on disk, and on its synopsis, 66,682 against 182,627, while still exiting 0 on `boundary matches the tree`. `.elenchus/fiat-861-step-2-r4.json` was already on disk from an earlier session when this round began and was not read as evidence; this round's verdict comes from the fresh report at `.elenchus/fiat-861-step-2.json`, which the runbook names.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Round 3: the block rule published a private key carried as a JSON string value, because the body witness needed a physical line break and the footer witness had to fall inside the 1,792-byte lookahead, and a PEM key inside `json.dumps` supplies neither. | closed in `0aeff97b` and re-measured here on ephemeral RSA keys generated under `/private/tmp` for this round and deleted after it. As `json.dumps({"deploy_key": <pem>})` the 2,048-, 3,072- and 4,096-bit members are 1,750, 2,542 and 3,342 bytes and put their footer 1,678, 2,470 and 3,270 bytes past the header's end. Against the matcher at `8f568888` the 2,048-bit refuses on that footer, inside the lookahead, and the other two exit 0; against the matcher at `9d3af4c6` all three refuse with stderr exactly `secret-shaped-member\n`, carrying 26, 38 and 50 body matches where they carried none. The same three carried on one `ledger.jsonl` line behave identically, and all three raw PEM members refuse under both. The reach of the repair is therefore the 3,072- and 4,096-bit sizes: a 26-body-line key in `json.dumps` already refused before it, on its footer. What the repair does not reach is S2-R4-02. |
| S2-R3-02 | low | `.hexaemeron/runbook.md` | Round 3: step 2's effective Exit stated the sidecar as `<64 lowercase hex> checkpoint.zip\n` with one space, where `hexctl.py:18006`, study line 76 and `test_hexctl_checkpoint_archive.py:233-234` all require two and the test rejects the one-space form by name, so the command could not satisfy its own Exit as written. | closed by the runbook amendment Fiat receipted as `600ce0bf71dee7ef0960db87f21e74be97adabab484cb9fd6933809243748282`, not by a commit. Verified byte for byte rather than by reading the receipt: those 6,948 bytes sit at offset 165,493 of `.hexaemeron/runbook.md`, runbook line 2,531, and hash to exactly that value, and the live file hashes to `97c78ad1dec7e23cb4558a8b83aa98b730c6b7d487fcc0fc0b7b4abe59472668`, the digest the packet names. Its Exit carries the two-space form at line 2,539, as do the step-2 amendment before it at line 2,187 and the step's baseline Exit at line 178; the one-space form survives only in the four superseded amendments at lines 723, 1,091, 1,564 and 1,871. No behaviour changed, as round 3 said. |
| S2-R3-03 | low | `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md` | Round 3: the reference's private-key block paragraph was pinned by nothing, so the contract home the later steps read could drift from the study and from the code with every gate green; four anchored edits, one deleting it outright, left every test that read the file green. | closed in `9d3af4c6`. The pin is `pin_the_reference_block_paragraph`, called from `test_archive_reference_names_every_refusal_class_and_fixture_id` at line 562 of `CheckpointArchiveScaffoldTests`: an equality against `EXPECTED_BLOCK_PARAGRAPH`, six `BLOCK_RULE_PARITY` rows requiring each rule in the paragraph to be a rule study section 4 states, and three assertions on the shipped tuple lengths, 2 block patterns, 4 token patterns and 6 in total. Round 3 deferred this to the round applying the amendment the paragraph restates; that amendment is the third of 2026-09-09 and landed in `0aeff97b`. Measured rather than read: see S2-R4-01. |
| S2-R4-01 | low | `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` | `0aeff97b` put the pin inside `CheckpointArchiveExportTests`, whose `setUp` calls `skipTest("gpg is unavailable, so no fixture key can be generated")` at line 1109 when the class could not mint a fixture key, so on a machine without `gpg` the pin skipped with its class and S2-R3-03 stayed open there. Nothing the pin reads needs `gpg`. Step 5's exit is a clean-machine demonstration, which is where such a machine appears. | closed in `9d3af4c6`, which moves the pin to `CheckpointArchiveScaffoldTests`, the only one of the module's three classes that never skips, and calls it from the test above it rather than as a test of its own, because step 5's Exit pins an exact count over this module. Measured on two detached worktrees, one cut from `0aeff97b` at tree `6fa4c486eea3d55c4e568d6d3eb0a66ce3b0c083` and one from `9d3af4c6` at tree `455eabe731443a9866c2842db35f8d505c3cf001`, each run with `gpg` off `PATH`, where the module baseline is `Ran 23 tests`, `OK (skipped=18)`, exit 0. Eight anchored edits to the paragraph: widening one body line to two, deleting the paragraph outright, dropping `plus that lookahead`, inverting the truncated-key rule to say it publishes, dropping the escaped-newline delimiter sentence, renaming `state.json` to `config.json` inside the paragraph, miscounting the four token patterns as five, and miscounting the six headers as five. All eight survive at `0aeff97b` and all eight are killed at `9d3af4c6`. Each edit was reverted before the next, both worktrees ended at zero modifications, and both were removed. |
| S2-R4-02 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | The escaped-newline delimiter does not close the class S2-R3-01 opened, where the study's third 2026-09-09 amendment says of it "Reading the escape as a delimiter closes the whole hole". A PEM key whose lines end CRLF and which is held as a JSON string value still publishes. `json.dumps` writes CRLF as the four bytes backslash, `r`, backslash, `n`; the body witness ends a line with `[ \t]*\r?` followed by a zero-width lookahead for end-of-buffer, the newline byte or the two-character escape, and its `\r?` matches the byte `0x0d`, not the two-character `\r` escape, so the lookahead never reaches the escaped newline behind it. Measured on the same ephemeral keys through the shipped `_checkpoint_archive_scan`: as `json.dumps` of a CRLF PEM the 2,048-, 3,072- and 4,096-bit members are 1,806, 2,622 and 3,446 bytes, carry zero body matches each, and put their footers 1,732, 2,548 and 3,372 bytes past the header's end, so the 2,048-bit still refuses inside the 1,792-byte lookahead and the 3,072- and 4,096-bit exit 0 with no refusal and no stderr. The minimum case agrees: a 32-character base64 run delimited by the escaped CRLF yields no body match, where the escaped LF and both real line endings each yield one. A `\u000a` escape, valid JSON for the same character, also yields none. `state.json` and `ledger.jsonl` are the scan's own declared targets and both are JSON. This is the S2-R3-01 shape one escape further out, and it fails open. | open, not fixed in this round. The repair is a criterion change only Fiat can author: study section 4 states the delimiter rule, `BLOCK_RULE_PARITY` pins the reference's restatement of it word for word, and the Files field requires a dated study amendment before the code. Admitting an optional escaped CR before the escaped LF closes the CRLF case; the `\u000a` case needs the rule stated over JSON string escapes rather than over one enumerated pair. |
| S2-R4-03 | low | `docs/fiat-checkpoint-archive-runbook.md` | At `9d3af4c6` the tracked runbook copy is 165,493 bytes where `.hexaemeron/runbook.md` is 172,441, and it is a strict prefix of it: what it lacks is exactly the 6,948 bytes of the `600ce0bf` amendment that closed S2-R3-02, so it hashes to `5417ad178054d631d8903f4bf5aebe219b61f1df3761dee971ea4511ddc37ece` against the controller's `97c78ad1...`. `0aeff97b` refreshed the copy and Fiat receipted the amendment after it. Nothing pins the two together: outside itself the path is named once across `plugins`, `tests` and `scripts`, at `tests/test_fiat_checkpoint_archive_record.py:29`, which reads it only for the record slug, the ADR-028 filename, a case-insensitive `run artefact`, the phrase `the decision homes` and dead relative links. The tracked study copy sits in the same position and happens to agree at `191a6101...`. | fixed in this round's commit by re-copying `.hexaemeron/runbook.md` over it, after which `cmp` exits 0 and both hash to `97c78ad1dec7e23cb4558a8b83aa98b730c6b7d487fcc0fc0b7b4abe59472668`. The missing pin is not fixed: a test comparing the two would have to read the run-scoped `.hexaemeron/` directory, which no clean checkout carries, so that repair belongs to the controller rather than to a test in this repository. |
| S2-R4-04 | low | `.hexaemeron/runbook.md` | Step 2's Exit is satisfiable by a run in which almost nothing ran. It requires the module to exit 0 "with at least 17 tests", and `unittest` counts a skipped test in `Ran N`. Measured with `gpg` off `PATH` on a detached copy of `9d3af4c6`: `Ran 23 tests in 3.773s`, `OK (skipped=18)`, exit 0, so the floor of 17 is cleared by the five tests that actually executed. The eighteen that skip include both signature-proof guards the same Exit names by behaviour, `test_signature_unverified_refuses_before_publish_on_a_status_other_than_good` at line 1736 and `test_signature_unverified_refuses_before_publish_on_an_unpinned_fingerprint` at line 1769, and the whole of `test_archive_export_refuses_secret_shaped_member`. Step 5's exit is a clean-machine demonstration, which is exactly a machine that may carry no `gpg`. | open, not fixed in this round. The repair is a criterion clause only Fiat can author, counting tests that ran rather than tests collected. Making the module fail instead of skip would break a machine that legitimately has no `gpg`, and it is the criterion rather than the module that is wrong. Nothing this round records was weakened by it: the Exit run on this machine has `gpg` and reports `Ran 23 tests in 98.977s`, `OK`, with no skips. |

Leads not pursued: Round 1's four leads, round 2's four and round 3's seven stand unchanged and were not re-worked. Four are new, none a criterion defect. First, this record and its synopsis now refuse the scan themselves: round 3's Leads text quotes the literal `xoxb-`, which token pattern four matches, at offset 171,215 in the record and 171,952 in the synopsis, and this section quotes it again. Round 3 measured both files at exit 0 before its own section was appended, so the change is the record's own words. Neither file is a member, the capsule being `.hexaemeron/` and the outer members the seven fixed ones, so this is an observation on the same terms as round 3's first lead about `hexctl.py`. Second, the paragraph pin is reachable only through `test_archive_reference_names_every_refusal_class_and_fixture_id`, whose name says nothing about it, which is the price step 5's exact count over this module charges for the pin; a reader looking for what holds the reference finds it in the body of another test. Third, `EXPECTED_BLOCK_PARAGRAPH` is a literal constant of the paragraph's collapsed text, so every future study amendment that moves a rule in section 4 must edit this test in the same commit; that is what the parity rows are for, and it is a cost rather than a defect. Fourth, round 3's sixth lead measured the block rule's cost on a header-dense member before the body witness widened, and the widened witness carries three lookbehind branches where the old one carried a `^`; the cost was not re-measured here and Step 5 owns the budget it would show in. Verified positives. The widening adds no false-refusal pressure on a binary member: over 1 MiB of seeded random bytes and the first 64 MiB of the repository's own 101 MiB deflate-compressed pack, both the old and the new body witness find zero matches, that slice carries zero armour headers, and `_checkpoint_archive_secret_shaped` is False under both. It also breaks nothing this run must archive: `_checkpoint_archive_scan` exits 0 on all 112 paths scanned under both matchers -- every file under `.hexaemeron/` except `__pycache__`, both tracked copies and the reference -- so `0aeff97b` introduced no new refusal on anything the capsule carries. The tracked digest binding is current: `hexctl.py` hashes to `869bf750426558b309755c871fecd6baa23672c0156fb2a97301a70a3e027671` and `tests/promise_machine_coverage.json` carries that value in all 11 places, round 3's `bc831c01...` absent. The controller artefacts are the packet's: `.hexaemeron/study.md` and `docs/fiat-checkpoint-archive-study.md` both hash to `191a6101565c849f2f23f266f0770b8738f02863194764194cc198237aa65365` and `cmp` exits 0, `.hexaemeron/runbook.md` hashes to `97c78ad1dec7e23cb4558a8b83aa98b730c6b7d487fcc0fc0b7b4abe59472668` and the tracked copy joins it in this round's commit, and `.hexaemeron/design-evidence.json` hashes to `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91` with `native-subcommands` selected. Both repair commits are signed `G` under `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed by `Dr Laurence E. Day <laurence@wildcat.finance>`, each carrying exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`; round 3's seventh lead about `5780d852`'s author does not recur. Commands and exits, all observed here. In the run worktree at `9d3af4c6`, clean: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive` exited 0 with `Ran 23 tests in 98.977s` and `OK`, six above the Exit's floor of 17; `phylax.py plugins tests`, `ephoros.py plugins tests` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each printed `clean` and exited 0; `horos.py check .` exited 0; `git diff --check` exited 0. `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` ran from 22:05:57Z to 22:10:53Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report rounds 1, 2 and 3 read, so the `step:3` transition's evidence is unchanged. As this round's baseline, `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6 --elenchus-report .elenchus/fiat-861-step-2.json` ran once in the run worktree at `9d3af4c6` from 22:10:53Z to 22:20:03Z and exited 0 with 2,457 of 2,457 passed, 0 failures, 0 errors, 0 skipped, 548.6 s wall and 231,112,704 bytes peak child RSS on an explicit budget of 6. The verdict itself is `elenchus.py --ref 9d3af4c6` given the runbook's three declared inputs with no substitution -- test command `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report file `.elenchus/fiat-861-step-2.json` -- run from 22:20:26Z to 22:30:25Z, exit 0: status `passed` over `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`, report complete at 2,457 executed, 0 assertion failures, 0 errors and 0 skipped, detail `the runner report records that the guard passed on the parent`. `passed` rather than `guarded` is what S2-R4-01 actually is: the defect it repaired is a skip that appears only where `gpg` is absent, and the Elenchus runner has `gpg`, so the moved pin passes on the parent too. Its parent-red evidence is the mutation pair in that row instead, not this verdict. On the clean detached snapshot `/private/tmp/fiat-861-snap-s2r4b` of tree `b6ab7fee8ae3b1fe7167a417e13a2049f2542a6f`, which is this round's staged tree as it stood before these three results were written into this section, the three Exit suites ran and each exited 0. `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --jobs 6` ran from 22:33:11Z to 23:03:04Z with `outcome green` over 9 selected checks and 22 omitted, every one passed: dead-code-suite 108.0 s, dead-code-suppressions-check 98.7 s, demonstrations-suite 36.9 s, front-door-check 0.6 s, harness-roster-check 0.4 s, hexaemeron-suite 1,743.8 s, imprimatur-suite 1.9 s, joined-front-door-suite 22.0 s and root-suite 408.1 s, so round 3's load-induced red on `demonstrations-suite` did not recur. `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6` ran from 23:03:04Z to 23:11:19Z with 2,457 of 2,457 passed, 0 failures, 0 errors, 0 skipped, 494.7 s wall and 223,019,008 bytes peak child RSS on an explicit budget of 6; two earlier `0/0 tests passed` objects in its log at 0.3 s each are child invocations a test drives, not the audited run. `python3 -m unittest discover -s tests` ran from 23:11:19Z to 23:14:39Z with `Ran 1626 tests in 199.097s` and `OK`. No `WAI-E-ADAPTER.TIMEOUT` appears in any of the three logs, so issue #1175's family did not recur on a machine where it would now be a defect rather than load. The snapshot worktree was removed after every log was read. Between that measured tree and the tree this round commits the only bytes that differ are this paragraph, the synopsis regenerated from it, and the two `.horos/census.json` entries for those two files. `imprimatur.py` and `brevitas.py` were not re-run: the only Markdown this round writes is the append-only record, its generated synopsis and the digest-bound runbook copy, which the amended Exit excludes by name. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 5 -- 2026-09-10T03:29:07Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite stays waived by the brief, whose `security_suite` field records that this run ships Python, tests and Markdown only, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. The four `not-applicable` ids keep round 1's reason: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls Step 4 introduces, `hostile-fixture-set` names the inspector test module Step 3 introduces, and neither `checkpoint inspect` nor `checkpoint restore --archive` exists to run. The seven ids reviewed against the export half only -- `zip-slip`, `name-collision`, `zip-bomb`, `trailing-data`, `manifest-join`, `identity-join` and `version-compatibility` -- are re-reviewed on the same terms, because nothing in this round's diff moves them. This round audits `adf2dec8`, the repair of round 4's findings, which landed after round 4's receipt. Its diff against `6e4bc08c` is seven files: `hexctl.py` changes only docstrings and two constants at lines 671 to 711, `CHECKPOINT_ARCHIVE_SECRET_LINE_BREAK` from `(?:\x0a|\\n)` to `(?:\x0d|\\r)?(?:\x0a|\\n)` and `CHECKPOINT_ARCHIVE_SECRET_BODY` dropping the `\r?` its tail consumed; `_checkpoint_archive_secret_shaped` at line 16978 and `_checkpoint_archive_scan` at line 17026 change only in docstring; the test module gains `rsa_private_key_pem` at line 555, `test_secret_shaped_member_refuses_before_publish_on_a_crlf_key_in_json` at line 2028 and two `BLOCK_RULE_PARITY` rows; the reference paragraph, the two tracked artefact copies, the census and the eleven digest bindings follow. No `hexctl` command was run: the scan is exercised by calling the shipped `_checkpoint_archive_secret_shaped` and `_checkpoint_archive_scan` in process under `PYTHONDONTWRITEBYTECODE=1`, and the round-4 matcher is reproduced in process as the body pattern `6e4bc08c` carried rather than by checking that commit out. The step's budgets were not measured; Step 5 owns them. Nothing under `.hexaemeron/` was hand-edited except this round's own working files under `.hexaemeron/briefs/step-2-r5-*`; `measure_design.py --conformance` wrote only its report. `.horos/boundary.json` is still not regenerated, as rounds 2 to 4 recorded, so `horos.py check .` reports candidate drift on this record, 66,285 bytes recorded against 202,798 on disk before this section, and on its synopsis, 66,682 against 203,594, while exiting 0 on `boundary matches the tree`. The previous session's interrupted module log at `.hexaemeron/briefs/step-2-r5-module-run-interrupted.log` was not read as evidence; every number below is from this round's own runs.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-02 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | Round 4: a PEM key whose lines end CRLF, held as a JSON string value, published once its footer passed the 1,792-byte lookahead, because the body witness's `\r?` matched the carriage-return byte and not the two-character escape `json.dumps` writes, so the lookahead never reached the escaped line feed behind it; the 3,072- and 4,096-bit members exited 0 with no refusal, and this failed open. | closed in `adf2dec8` under the study amendment of 2026-09-10, ledger row 56, `7a808ae5cba472d033f0c5678a7420a70c588f99009bb8d01c28b9e07d0f86ff` over the 2,198 bytes from offset 65,604 of `.hexaemeron/study.md`, receipted 2026-09-09T23:28:34Z, which precedes the repair's 2026-09-10T02:27:12Z commit as the Files field requires. Re-measured here through the shipped predicate on ephemeral RSA keys generated in process by the test module's own `rsa_private_key_pem`, never written to disk: as `json.dumps({"deploy_key": <CRLF pem>})` the 2,048-, 3,072- and 4,096-bit members are 1,774, 2,590 and 3,414 bytes with footers 1,692, 2,508 and 3,332 bytes past the header's end, and all three refuse; the same three on one `ledger.jsonl` line refuse; raw LF, raw CRLF and `json.dumps` LF members at all three sizes refuse; on the 3,072-bit CRLF member the round-4 body pattern finds 0 body lines and the shipped one finds 37, and the predicate with the round-4 pattern substituted in process returns False where the shipped one returns True. The minimum case agrees: a 32-character base64 run delimited by raw LF, raw CRLF, escaped LF or escaped CRLF is one body line, and by raw CR, escaped CR or `\u000a` is none. The residue the amendment states was measured and is as stated: with the footer past the lookahead, `\u000a`, `\u000d\u000a`, raw CR-only and escaped CR-only keys publish at 3,072 and 4,096 bits, and the 2,048-bit `\u000d\u000a` member, whose footer at 1,900 bytes is the only 2,048-bit case past the lookahead, publishes too; every one of them refuses on its footer inside the lookahead. The guard is real: `test_secret_shaped_member_refuses_before_publish_on_a_crlf_key_in_json` generates a 3,072-bit key whose keypair it proves by `pow`, asserts the footer is past the lookahead and no raw newline is inside the body, requires refusal on both JSON carriages, keeps the three forms that refused before the amendment, and pins the `\u000a` residue as a positive so that closing it must be a dated amendment. Elenchus ran `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `adf2dec8` in format `unittest-json-v1` with report file `.elenchus/fiat-861-step-2.json`, the runbook's three declared inputs with no substitution, from 03:22:10Z to 03:28:00Z, exit 0, and returned `guarded` with the detail `the runner report records a parent assertion failure`; the parent report is complete at 2,458 executed, 4 assertion failures, 0 errors and 0 skipped, and the changed files it compared were `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` and `tests/promise_machine_coverage.json`. The report Elenchus reads is written inside its detached parent worktree, so `.elenchus/fiat-861-step-2.json` in the run worktree is still round 4's file of 2026-09-09T23:20 and was not read as evidence. |
| S2-R4-03 | low | `docs/fiat-checkpoint-archive-runbook.md` | Round 4: the tracked runbook copy was a strict 165,493-byte prefix of the 172,441-byte controller runbook, lacking the `600ce0bf` amendment, and nothing pins the two together. | the copy was refreshed in round 4's commit and again in `adf2dec8`, which carries the four 2026-09-10 runbook amendments at ledger rows 57 to 60: `cmp` exits 0 and both files hash to `0f99b29bfcc7e1f0738568175b037db7ff295be7217adf9ca95be0f45daf1c46`, the packet's digest; the study copy likewise, both at `7424d87f8af382933ba95e5ac20d98f81f9f950e9dc7243598b4ece4dcbfc8a0`. The missing pin stands as round 4 left it: a test comparing the copies to `.hexaemeron/` would read a run-scoped directory no clean checkout carries, so that repair belongs to the controller, and every amendment since has needed a manual re-copy that the digests above show was done. |
| S2-R4-04 | low | `.hexaemeron/runbook.md` | Round 4: the Exit's floor of "at least 17 tests" counted tests collected, `unittest` counts a skip in `Ran N`, and with `gpg` off `PATH` the module reported `Ran 23 tests`, `OK (skipped=18)`, exit 0, clearing the floor on five tests that ran while both signature guards skipped. | closed by the runbook amendment of 2026-09-10 Fiat receipted at ledger row 57, `49d6adc2e689a703e11877c1752fc93582ec62e40bf1eaa5a691bcb1446322a7` over bytes 172,441 to 178,043 of `.hexaemeron/runbook.md`, runbook line 2634, verified byte for byte here; the effective Exit counts tests that ran, excludes skips and expected failures, requires `gpg` on `PATH`, and names the two signature guards by behaviour. Measured on `adf2dec8` with `gpg` at `/opt/homebrew/bin/gpg`: `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive -v` exited 0 with `Ran 24 tests in 140.030s`, `OK`, 24 `ok` lines and 0 skips, so the floor of 17 is cleared by tests that ran, and `test_signature_unverified_refuses_before_publish_on_a_status_other_than_good` and `test_signature_unverified_refuses_before_publish_on_an_unpinned_fingerprint` are among the 24. |

Leads not pursued: Round 1's four leads, round 2's four, round 3's seven and round 4's four stand unchanged and were not re-worked. Five are new, none a criterion defect. First, the delimiter rule reads "in the matching form" and the shipped `(?:\x0d|\\r)?(?:\x0a|\\n)` also admits the two mixed forms, a raw carriage return before an escaped line feed and an escaped one before a raw line feed; measured, both refuse at all three sizes, so the code is wider than the rule in the fail-closed direction, and a raw control byte inside a JSON string is not valid JSON in any case. Second, a key whose lines end in a carriage return alone, raw or escaped, publishes past the lookahead exactly as the `\u000a` case does; it is a member of the residue the amendment states in general terms, "no delimiter the witness can see", and is named here so the class has a second measured member beside the one the test pins. Third, `.hexaemeron/briefs/` is capsule content, since `_checkpoint_snapshot` copies everything under `.hexaemeron/` except `lock`, and this round's first measurement script spelled a PEM header and its footer on one line, so `_checkpoint_archive_scan` refused it, 1 of 128 paths; the script was removed and rewritten to build the header from fragments, after which all 129 paths under `.hexaemeron/`, the two tracked copies and the reference exit 0. That is the S2-R2-02 class met from the operator's side: a Warden's working file under the capsule root can stop this run's own export, and it fails closed. Fourth, the residue assertion in the new test holds a fail-open behaviour as an expectation, so a later repair that makes the scan refuse `\u000a` goes red there until it edits the test; the comment says that is the point, and it is a cost rather than a defect. Fifth, `adf2dec8` is authored by `Dr Laurence E. Day <laurence@wildcat.finance>` where every repair commit but `5780d852` is authored by `Shoggoth <shoggoth@wildcat.finance>`; it is signed `G` under `B83B60AE16F5DD1A` and carries exactly one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`, and no gate this step names reads the author field, as round 3's seventh lead recorded. Verified positives. The widened witness adds no false-refusal pressure: over 1 MiB of seeded random bytes it finds 0 body lines and the predicate is False, and over a 65,536-byte buffer packed with the OpenPGP header the shipped witness takes 3.46 ms against the round-4 witness's 3.58 ms and 0.15 ms each on plain filler, best of five, so round 4's fourth lead about re-measuring the block rule's cost after the widening is answered with no increase. `rsa_private_key_pem()` takes 1.19 s wall, so the module's 140.030 s against round 4's 98.977 s is not the key generation; the machine carried other sessions' load and the time was not re-measured idle. The reference paragraph is pinned: `pin_the_reference_block_paragraph` at line 1086 of `CheckpointArchiveScaffoldTests` holds `EXPECTED_BLOCK_PARAGRAPH` equal to the paragraph as `adf2dec8` rewrote it and its eight `BLOCK_RULE_PARITY` rows, two new, each name a rule the amended study states, and that class ran among the 24. The tracked digest binding is current: `hexctl.py` hashes to `43dbfe51391319d63677d35b01611564194134f3b65b1d053e664c918d720f46` and `tests/promise_machine_coverage.json` carries that value in all 11 places, round 4's `869bf750...` absent. `.hexaemeron/design-evidence.json` hashes to `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91` with `native-subcommands` selected. The commit message's claims were checked against the tree: 24 ran, none skipped, and its recorded greenlight tree `53fbb3b119dd4f52248447837134cb748b547308` is the tree `adf2dec8` carries. Commands and exits, all observed here, in the run worktree at `adf2dec8`, clean: `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests` and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each printed `clean` and exited 0; `imprimatur.py` scored 100.0 with 0 defects and `brevitas.py` printed `clean`, both exit 0, on the reference, the only changed Markdown this run may edit; `horos.py check .` exited 0; `git diff --check adf2dec8^ adf2dec8` exited 0; `audit_synopsis.py --check .` reported `committed=match` for every source before this section was appended. On the clean detached snapshot of `adf2dec8`, tree `53fbb3b119dd4f52248447837134cb748b547308`, cut under this session's scratch root and removed after every log was read, the three Exit suites ran in sequence from 02:42:29Z and each exited 0. `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --format json --jobs 6` ran to 03:03:42Z with `outcome green` over 13 selected checks and 22 omitted, `failure_classes` empty, every one passed: dead-code-suite 62.7 s, dead-code-suppressions-check 58.7 s, demonstrations-suite 30.1 s, front-door-check 0.4 s, harness-roster-check 0.2 s, harness-roster-freshness 0.2 s, hexaemeron-suite 1,234.3 s, imprimatur-suite 1.0 s, joined-front-door-suite 13.0 s, lint-ephoros 3.0 s, lint-hypomnema 1.4 s, lint-phylax 5.3 s and root-suite 252.0 s, so round 3's load-induced red on `demonstrations-suite` did not recur; `--format json` was used because the human format hides the failure reason. `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6` ran to 03:14:06Z with status `passed`, 2,458 of 2,458 started, completed and passed, 0 failures, 0 errors, 0 skipped, 624.0 s wall and 192,282,624 bytes peak child RSS on an explicit budget of 6, one test above round 4's 2,457, which is the CRLF guard; the raw shard output in its log carries child invocations a test drives, not the audited run. `python3 -m unittest discover -s tests` ran to 03:17:04Z with `Ran 1626 tests in 176.988s` and `OK`. No `WAI-E-ADAPTER.TIMEOUT` appears in any of the three logs. In the run worktree `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` ran from 03:17:04Z to 03:22:04Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report rounds 1 to 4 read, so the `step:3` transition's evidence is unchanged. Between that measured tree and the tree this round commits the only bytes that differ are this section, the synopsis regenerated from it, and the `.horos/census.json` entries for those two files. `imprimatur.py` and `brevitas.py` were not re-run on this record or its synopsis, which the amended Exit excludes by name. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.

## Step 2, round 6 -- 2026-09-10T07:49:27Z

Audit schema: fiat-audit-round/v2

Covered: zip-slip=reviewed; name-collision=reviewed; special-entries=reviewed; zip-bomb=reviewed; trailing-data=reviewed; manifest-join=reviewed; outer-digest=reviewed; bundle-completeness=reviewed; bundle-nondeterminism=reviewed; signature-scope=reviewed; keyring-handling=reviewed; identity-join=reviewed; remote-url-derivation=not-applicable; source-path-leak=reviewed; secret-shaped-member=reviewed; diagnostic-leak=reviewed; subprocess-argv=reviewed; partial-publish=reviewed; interrupted-restore=not-applicable; destination-emptiness=not-applicable; read-only-bytecode=reviewed; concurrent-mutation=reviewed; version-compatibility=reviewed; acceptance-self-reference=reviewed; hostile-fixture-set=not-applicable

Not checked: The Solidity suite stays waived by the brief, whose `security_suite` field records that this run ships Python, tests and Markdown only, so x-ray, solidity-auditor and fizz were neither read nor run and no Solidity claim is made. The four `not-applicable` ids keep round 1's reason: `remote-url-derivation`, `interrupted-restore` and `destination-emptiness` are restore controls Step 4 introduces, `hostile-fixture-set` names the inspector test module Step 3 introduces, and neither `checkpoint inspect` nor `checkpoint restore --archive` exists to run. The seven ids reviewed against the export half only -- `zip-slip`, `name-collision`, `zip-bomb`, `trailing-data`, `manifest-join`, `identity-join` and `version-compatibility` -- are re-reviewed on the same terms, because nothing in this round's diff moves them. This round audits `415c91be` as round 5 left it and repairs S2-R6-01 in `c4bca2c0`, whose diff against `415c91be` is seven files: `hexctl.py` changes `CHECKPOINT_ARCHIVE_SECRET_LINE_BREAK` at line 671 from `(?:\x0d|\\r)?(?:\x0a|\\n)` to `(?:\x0d|\\r|\\u000[dD])?(?:\x0a|\\n|\\u000[aA])`, adds `(?<=\\u000[aA])` to the lookbehind of `CHECKPOINT_ARCHIVE_SECRET_BODY` at line 701, and otherwise changes only docstrings there and at `_checkpoint_archive_secret_shaped`, line 16989; the reference's block paragraph at line 195 restates the amended delimiter set and the narrower residue; the test module moves `EXPECTED_BLOCK_PARAGRAPH` at line 258 to match, replaces two `BLOCK_RULE_PARITY` rows with three at line 288 so the table holds nine, gains `test_secret_shaped_member_refuses_before_publish_on_a_numeric_escaped_key_in_json` at line 2090 and the expected failure `test_secret_shaped_member_refuses_before_publish_on_a_stripped_key_past_the_lookahead` at line 2151, and names the first 2026-09-10 amendment in the CRLF guard's docstring; the two tracked artefact copies, the eleven digest bindings in `tests/promise_machine_coverage.json` and the census follow. No `hexctl` command was run: the scan is exercised by calling the shipped `_checkpoint_archive_secret_shaped` and `_checkpoint_archive_scan` in process under `PYTHONDONTWRITEBYTECODE=1`, and the round-5 witness is reproduced in process as the two patterns `415c91be` carries rather than by checking that commit out. The step's budgets were not measured; Step 5 owns them. Nothing under `.hexaemeron/` was hand-edited: this round's working files live under the session scratch root, `measure_design.py --conformance` wrote only its report, and the previous session's logs and scripts at `.hexaemeron/briefs/step-2-r6-*` were read only to learn why its greenlight exited 1 and supply no number below. That session's staged tree `a246bdc3`, two files, was read with `git diff --cached` and superseded: its expected-failure test asserting the `\u000a` refusal is now the ordinary refusal test, its reasoning for holding a genuine residue as an expected failure is kept in the stripped-key test, and its census entry was regenerated. `.horos/boundary.json` is still not regenerated, as rounds 2 to 5 recorded, so `horos.py check .` reports candidate drift on this record, 66,285 bytes recorded against 218,844 on disk before this section, and on its synopsis, 66,682 against 219,687, while exiting 0 on `boundary matches the tree`. The witness timings below were taken at a load average of 119 while a neighbouring session's `run_checks.py` ran, so they bound nothing. imprimatur and brevitas were not re-run on this record or its synopsis, which the amended Exit excludes by name.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | medium | `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` | A PEM key whose line feeds are written as JSON's numeric escape `\u000a`, held as a JSON string value, published once its footer passed the 1,792-byte lookahead, because the body witness read the newline byte and the two-character escape `\n` and not the six-character escape that `json.loads` decodes to the same character, and this failed open. The previous session opened it against the residue the first 2026-09-10 study amendment stated; Fiat measured it and moved the numeric escapes into the delimiter set by the second 2026-09-10 amendment, `2dc091d156ef087ffab18effad0aa981a250e27301f2fc018cf151ebce9489c1`, receipted before this round's brief. Measured here through the shipped predicate at `415c91be` on ephemeral RSA keys generated in process by the test module's `rsa_private_key_pem`, never written to disk: as `{"deploy_key": "<pem>"}` with every line feed written `\u000a`, the 3,072-bit key, 37 body lines, is 2,668 bytes with its footer 2,584 bytes past the header's end and publishes, as does the 4,096-bit key, 49 lines, at 3,516 and 3,432; `\u000A`, `\u000d\u000a` and `\u000D\u000A` publish at both sizes, 2,902 and 3,822 bytes for the CRLF forms; the 2,048-bit key, 25 lines, refuses in the line-feed forms on its footer at 1,744 bytes inside the lookahead and publishes in the CRLF numeric forms at 1,900 and in the mixed `\r\u000a` form at 1,796; the same keys written `\n`, raw or raw CRLF refuse at all three sizes. | closed in `c4bca2c0`. The line break and the body lookbehind now read `\u000a` and `\u000d` in either letter case; re-measured through the repaired predicate, every numeric form refuses at 2,048, 3,072 and 4,096 bits, the mixed `\r\u000a` form included, and the raw, `\n` and CRLF forms still refuse. The guard `test_secret_shaped_member_refuses_before_publish_on_a_numeric_escaped_key_in_json` generates a 3,072-bit key whose keypair it proves by `pow`, and for each of `\u000a`, `\u000A`, `\u000d\u000a` and `\u000D\u000A` asserts that `json.loads` returns the key with that line ending, that the body carries no newline byte and no `\n`, that the footer is more than the lookahead past the header, and that the scan refuses. Five anchored mutations of `hexctl.py` on a detached copy of this round's tree at `bf3ca1f1`, cut under the session scratch root, restored to pristine bytes after each and removed after every log was read: reverting both regexes to the round-5 witness fails the guard on 4 subtests, dropping only the lookbehind's numeric escape fails 4, lower-case hex digits only fails 2, dropping the carriage return's numeric escape fails 2, and widening the lookahead fourfold to 7,168 bytes fails the guard's geometry on 5, the CRLF guard's on 2, and turns the stripped-key expected failure into `FAILED (unexpected successes=1)`; the CRLF guard stays green under the first four, so what the guard reads is the numeric escape and nothing the CRLF guard already read. The residue the amendment now states is measured as stated: with its line breaks stripped the 3,072-bit key is 2,434 bytes with its footer 2,356 past the header and publishes, the 4,096-bit key 3,210 and 3,132 and publishes, and the 2,048-bit key, 1,666 and 1,588, refuses on its footer inside the lookahead; a key whose lines end in `\u000d` alone publishes at 3,072 and 4,096 bits on the same terms. `test_secret_shaped_member_refuses_before_publish_on_a_stripped_key_past_the_lookahead` asserts the refusal of the stripped 3,072-bit member under `@unittest.expectedFailure`, with its geometry proved in the ordinary guard because the decorator swallows a failed precondition, so closing the residue later is a dated amendment that edits that test. Elenchus ran `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}` against `c4bca2c0` in format `unittest-json-v1` with report file `.elenchus/fiat-861-step-2.json`, the runbook's three declared inputs with no substitution, from 07:39:28Z to 07:48:14Z, exit 0, and returned `guarded` with the detail `the runner report records a parent assertion failure`; the parent report is complete at 2,459 executed, 6 assertion failures, 0 errors and 1 skipped, the skip being the expected failure the adapter folds into that count, and the changed files it compared were `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` and `tests/promise_machine_coverage.json`; its diagnostic output shows the numeric-escape guard failing on the parent at `member='line feed, lower case'`. The default 900-second timeout was raised to 5,400 because the declared runner took 1,212.8 s on the snapshot below under this machine's load; the timeout is Elenchus's own argument and none of the three declared inputs. The report Elenchus reads is written inside its detached parent worktree, so `.elenchus/fiat-861-step-2.json` in the run worktree is still round 4's file of 2026-09-09T23:20 and was not read as evidence. |

Leads not pursued: Round 1's four leads, round 2's four, round 3's seven, round 4's four and round 5's five stand unchanged and were not re-worked. Six are new, none a criterion defect. First, round 5's second lead narrows to the residue the second 2026-09-10 amendment states: with the numeric escapes admitted, the measured members are a body with its line breaks stripped and a body whose lines end in a carriage return alone, raw, `\r` or `\u000d`, and both escape only once the base64 body is longer than the 1,792-byte lookahead, which the 2,048-bit key's 1,588-byte body is not and the 3,072-bit key's 2,356-byte body is. Second, the shipped set is wider than the rule in the fail-closed direction, as round 5's first lead recorded for the two-character forms: `\r\u000a`, `\u000d\n` and the other mixed spellings match, and the first was measured refusing at all three sizes. Third, the S2-R4-03 mechanism recurred: on entry to this round the tracked study copy was `7424d87f8af382933ba95e5ac20d98f81f9f950e9dc7243598b4ece4dcbfc8a0`, 67,802 bytes, against the controller's `5c26efccc6aa68cf78d5d9a1ea7562ba6346f7d4e6a2a2ac9085702f1f1be17e`, 69,771, and the runbook copy `0f99b29bfcc7e1f0738568175b037db7ff295be7217adf9ca95be0f45daf1c46`, 193,265 bytes, against `609cce3e8d0b2f1412c5741017d417f4ddfbbbca03f1218b72c88088e7556623`, 213,819, after the second study amendment and four runbook re-binds; both were re-copied here and `cmp` exits 0 on each pair. The pin round 4 asked for now exists in part: `pin_the_reference_block_paragraph` run against the stale study copy failed its three new parity rows, `Ran 8 tests`, `FAILED (failures=3, expected failures=1)` over the two scan and scaffold classes, and passed after the re-copy, so a study copy lacking an amendment's rule text fails the module, but only for the rules the rows name and never for the runbook copy; the controller-side pin stands as round 5 left it. Fourth, the reference's sentence that a line ends at a newline character or at `\n` is no longer complete on its own; it stands because the study's third 2026-09-09 amendment still states it, the second parity row still reads it, and the delimiter-set sentence after it completes it, so this is a preference and not a defect. Fifth, the widened witness costs more per chunk: on a 65,536-byte buffer packed with the OpenPGP header the body witness takes 3.89 ms against the round-5 witness's 2.72 ms, and 3.54 against 2.40 ms on plain filler, best of five under the load stated above; over 1 MiB of seeded random bytes both find 0 body lines and the predicate is False, so the widening adds no false-refusal pressure, and the cost was not re-measured idle. Sixth, the previous session's greenlight log records `FAILED (failures=1)` on `test_a_grandchild_that_leaves_the_process_group_is_refused` in `test_demonstrations`, `0 != 2`, the load signature round 3 recorded; nothing about that tree is proven or claimed, and every suite result in this record is from this round's own snapshot. Verified positives. All 155 paths this run's export would scan, 152 under `.hexaemeron/` and the two tracked copies and the reference, pass `_checkpoint_archive_scan` under the widened witness. `python3 -m unittest plugins.hexaemeron.tests.test_hexctl_checkpoint_archive -v` with `gpg` at `/opt/homebrew/bin/gpg`, GnuPG 2.5.21, ran from 06:09:41Z to 06:12:52Z, exit 0, `Ran 26 tests in 190.358s`, `OK (expected failures=1)`: 25 `ok`, 0 skipped, 1 expected failure, so the floor of 17 is cleared by tests that ran, and `test_signature_unverified_refuses_before_publish_on_a_status_other_than_good` and `test_signature_unverified_refuses_before_publish_on_an_unpinned_fingerprint` are among the 25. The reference paragraph is pinned: `EXPECTED_BLOCK_PARAGRAPH` equals the paragraph as `c4bca2c0` rewrote it and each of the nine `BLOCK_RULE_PARITY` rows names a rule the amended study states, checked against `flat(read(STUDY))` on the refreshed copy. The tracked digest binding is current: `hexctl.py` hashes to `38cb6dc8a9f7379fc852882950b575420711164fe3a54d329a022f796b9ac5ca` and `tests/promise_machine_coverage.json` carries that value in all 11 places, round 5's `43dbfe51...` absent; the reference hashes to `4d763318ec3cc8ad12c652f6d69db5970c77925d821c237e42bcfd8fa5fb0a0e`. `.hexaemeron/design-evidence.json` hashes to `101172ad264e56b0910cc64e8f11662da8b7f874ca030c3da76ba7cfacf6db91` with `native-subcommands` selected. Commands and exits, all observed here, in the run worktree on this round's staged tree `e1634c1c8e537192f29decd40f480011afd8f698`: `python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests`, `python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests` and `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each printed `clean` and exited 0; `imprimatur.py` scored 100.0 with 0 defects and `brevitas.py` printed `clean`, both exit 0, on the reference, the only changed Markdown this run may edit; `horos.py check .` exited 0; `git diff --check` exited 0; `audit_synopsis.py --check .` reported `committed=match` for every source before this section was appended. On the clean detached snapshot of this round's staged tree, commit `bf3ca1f1` carrying tree `e1634c1c8e537192f29decd40f480011afd8f698`, cut under the session scratch root with `git status --short` empty and removed after every log was read, the three Exit suites ran in sequence from 06:14:16Z. `python3 scripts/run_checks.py --base 0bc39f278e24d8cdd79abed5da16bd5ce81e4c5a --format json --jobs 6` first ran to 06:45:24Z with `outcome red` and `failure_classes` `["command-failure"]` on one of 13 selected checks: `hexaemeron-suite`, the nested `run_tests.py --jobs 2`, reached its 1,800-second cap at 1,801.0 s, `reason` `timeout`, exit -15, with 0 bytes of output, while the other 12 passed at two to three times their round-5 durations and the load average stood between 90 and 119 with a neighbouring session's `run_checks.py` and its nested `run_tests.py --jobs 5` running throughout; that is the load signature the brief names, so the identical tree was re-run once, from 07:16:41Z to 07:37:54Z, `outcome green`, `failure_classes` empty, every one of the 13 passed: dead-code-suite 57.0 s, dead-code-suppressions-check 54.3 s, demonstrations-suite 28.8 s, front-door-check 0.3 s, harness-roster-check 0.2 s, harness-roster-freshness 0.2 s, hexaemeron-suite 1,236.6 s, imprimatur-suite 0.8 s, joined-front-door-suite 11.8 s, lint-ephoros 2.8 s, lint-hypomnema 1.1 s, lint-phylax 5.0 s and root-suite 238.9 s, 22 omitted; `--format json` was used because the human format hides the failure reason. `python3 plugins/hexaemeron/tests/run_tests.py --jobs 6` ran from 06:45:24Z to 07:05:37Z with status `passed`, 2,460 of 2,460 started, completed and passed, 0 failures, 0 errors, 0 skipped, 1 expected failure, 0 unexpected successes, 1,212.8 s wall and 257,736,704 bytes peak child RSS on an explicit budget of 6, two tests above round 5's 2,458, which are the numeric-escape guard and the stripped-key expected failure. `python3 -m unittest discover -s tests` ran to 07:15:55Z with `Ran 1626 tests in 616.204s` and `OK`. No `WAI-E-ADAPTER.TIMEOUT` appears in any of the four logs. In the run worktree `python3 .hexaemeron/measure_design.py --conformance existing-checkpoint-suites-green --candidate native-subcommands` ran from 07:17:03Z to 07:22:11Z, exited 0, and left `.hexaemeron/reports/native-subcommands-existing-checkpoint-suites-green.json` carrying `"value": true` at `44ae2e605222dee4a628711336b96cef6e43b2aafeac4e7348a1f00d5de382b9`, byte-identical to the report rounds 1 to 5 read, so the `step:3` transition's evidence is unchanged. Between that measured tree and the tree this round commits the only bytes that differ are this section, the synopsis regenerated from it, and the `.horos/census.json` entries for those two files. The commit message's claims were checked against the tree: 26 ran, 25 ok, 1 expected failure, none skipped, and its named snapshot `bf3ca1f1` carries the tree `e1634c1c8e537192f29decd40f480011afd8f698` that `c4bca2c0` commits. Audit filter `sapheneia:sapheneia` was applied to this record and retained every finding, severity, status, path, line, digest, count, command, exit code, negative result and unpursued lead; no Horos reading exclusion was applied.
