Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1318-elenchus-binds-guard-test-and-repair-files.md | source_sha256=ec4a6019c605432b41ca676b74a71a57ea983ab5bdc5644fe605b76fd894b6c7 | h2_count=2
## Step 1, round 1 -- 2026-09-06T08:26:23Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: guard-test-unbound=reviewed; repair-files-short=reviewed; check-path-drift=reviewed; blob-read-bounded=reviewed; git-argv-injection=reviewed; partial-record-write=reviewed; refusal-order=reviewed; frontier-drift=not-applicable; package-repin-collision=not-applicable; checker-interface-drift=reviewed; docs-copy-drift=reviewed<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). `python3 scripts/run_checks.py --base origin/main` not re-run this round; the implement receipt owns that exit. `python3 -m unittest plugins.hexaemeron.tests.test_elenchus_checker` not run under bare unittest (it routes into the parallel runner); the runner report `.elenchus/fiat-1318-step-1.json` was read, not regenerated: `elenchus.unittest.v1`, `testsRun` 2420, `failures` 0, `errors` 0, `complete` true. X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. `frontier-drift` and `package-repin-collision` belong to step 3 and the integration; the diff `944277d7..1d766bee` touches no `EVOLUTION.md` and no version file.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: (1) Exit order: the Exit says `F019` is refused directly after `F007`; `emit()` decides `F019` there from the same two inputs and holds it, so a draft failing `F019` and `F008`, `F010`, `F018` or a composed-record rule names both; the held refusal still exits 1 before `prepare_output_path` and writes nothing, the register's `refusal-order` wording holds, and the ordering itself is not read as a finding. (2) `hypomnema.py --study docs/elenchus-emit-path-bindings/study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` reports `H008 study has no design bridge block`, exit 1; 3 of the 49 committed `docs/*/study.md` carry a bridge, the study's hypomnema answer says no new decision record is earned and step 3 owns the ledger row, and the Exit pins the copy byte-identical to the controller artefact, so no round fix is available. (3) Whole-word occurrence accepts a `guard.test` segment that appears only in a comment, the boundary `unbound_segment`'s docstring states. (4) Issue 1228: the root suite inside this run worktree was green this round rather than red on the two `test_agent_instruction_corpus` tests the brief predicted. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.26`, each printed `clean` and exited 0 over the changed paths; emitter suite `Ran 63 tests`, `OK`; the same test file against the entry ref `944277d7` emitter: `Ran 63 tests`, `FAILED (failures=7)`, the seven new refusal cases; root suite `python3 -m unittest discover -s tests` inside the run worktree `Ran 1389 tests`, `OK`, exit 0, and in a detached snapshot of `1d766beed3c7dc1b239d9b7220d16fdecae8e53d` `Ran 1389 tests`, `OK`, exit 0; `horos.py check .` prints `boundary matches the tree`, exit 0; `docs/elenchus-emit-path-bindings/study.md` and `runbook.md` `cmp` equal to `.hexaemeron/study.md` (`b05fde2e0d1fb09a4125977185fd9011ecab8d5b8699b19dd7f885d0fd81d1ea`) and `.hexaemeron/runbook.md` (`b92fdbd24119b2759e8350af77122abb2b9e52cf328ad642121141100919bb65`); design record `e950257f3d6c1dc607aaab6b6367603a933d78e56004ab3decdf2040fb552716`, selected `emit-path-rules`, `.hexaemeron/reports/emit-path-rules-emitter-suite-green.json` and `emit-path-rules-existing-cases-unedited.json` each `value` true; `git diff --numstat` on the test file 152 added, 0 deleted; `elenchus.py` and `test_elenchus_checker.py` diff empty; `guard_blob` runs `git cat-file -s` then `git cat-file blob` as fixed argv with no shell on `<40-or-64-hex commit>:<path passed _relative_path>`, refuses a size over `MAX_INPUT_BYTES` (1048576) before the blob read and never decodes the bytes; `TEST_NAME` keeps every `guard.test` segment a non-empty ASCII identifier, so no empty pattern reaches `unbound_segment`; each refusal names its code, field, segment or paths and commit on stderr; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all eleven risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, the null verdict, and the four unpursued leads without changing any claim.
## Step 2, round 1 -- 2026-09-06T09:27:44Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: guard-test-unbound=not-applicable; repair-files-short=not-applicable; check-path-drift=not-applicable; blob-read-bounded=not-applicable; git-argv-injection=not-applicable; partial-record-write=not-applicable; refusal-order=not-applicable; frontier-drift=reviewed; package-repin-collision=reviewed; checker-interface-drift=reviewed; docs-copy-drift=not-applicable<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). `python3 scripts/run_checks.py --base origin/main` not re-run this round; the implement receipt owns that exit (32 checks, `outcome green`). The root suite was run on a detached snapshot of `a3018f2ab5d7ae25047eaf6baf3c157f703fdeb7` only, not inside the run worktree. X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. The seven emitter-side risk ids and `docs-copy-drift` are not-applicable: the diff `32bf0429..a3018f2a` touches no `.py` under `skills/elenchus/scripts`, no emitter test and no `docs/elenchus-emit-path-bindings/` file.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: (1) The Exit's literal `1.6.27` became `1.6.28` under the Exit's own collision clause: `origin/main` claims `1.6.27` through the merged issue 1276, the enumeration over every local and remote ref after `git fetch origin` reads `1.6.26 / 1.6.27 / 1.6.28` with `1.6.28` held only by this branch, and the register's `package-repin-collision` wording names the property rather than the number; it is re-checked before the integration push. (2) Inside `## Emit the result as a record` the `F007` bullet now names a file rather than a test and the count line reads `Twenty codes ship, F000 to F019` with `Five more`, beyond the Exit's list; both keep the arithmetic 11 + 5 + 4 = 20 true and the old wording contradicted the new `F018` sentence, so read as conformant. (3) `docs/elenchus-emit-path-bindings/runbook.md:138` and `study.md:20` still say `1.6.27` and `1.6.26`; they are byte-pinned copies of the receipted artefacts and the collision clause in the same runbook governs, so not a finding. Evidence for this round: `phylax.py` and `ephoros.py` over the six changed `.py` and `.json` files and `hypomnema.py` over the three changed `.md` files, from plugin root `hexaemeron/1.6.26`, each printed `clean` and exited 0; `imprimatur.py` on `SKILL.md`, `EVOLUTION.md` and `demonstration.md` each exited 0 with 0 defects; `test_evolution` `Ran 11 tests` `OK`, `tests.test_version_propagation` `Ran 8 tests` `OK`, `tests.test_evolution_contract` `Ran 10 tests` `OK`, emitter suite `Ran 63 tests` `OK`; root suite `python3 -m unittest discover -s tests` on the detached snapshot `Ran 1389 tests`, `OK`, exit 0; Hexaemeron runner `2420/2420 tests passed`, report `.elenchus/fiat-1318-step-2.json` `elenchus.unittest.v1`, `testsRun` 2420, `failures` 0, `errors` 0, `complete` true; `.hexaemeron/reports/resolve.py refused-at-emit` and `frontier-retained-byte-for-byte` each `True` for `emit-path-rules`, so every sentence the resolver binds in `SKILL.md`, `demonstration.md` and `EVOLUTION.md` survives; the `elenchus-v1.5.0` row is `generation`, its digest `08e77bae576b3351d6f38e60ce9da88327014bcaa7459e319b8e51d79caeda8b` equals the SHA-256 recomputed over the exact current frontier line, and the header, the row and the `SKILL.md` frontmatter all read `1.5.0`; `1.6.28` appears exactly once in each of the six re-pin files; `elenchus.py`, `test_elenchus_checker.py` and `fixed_and_guarded.py` are untouched by this diff; design record `e950257f3d6c1dc607aaab6b6367603a933d78e56004ab3decdf2040fb552716`, selected `emit-path-rules`. The bounded Sapheneia pass preserved the heading, the four schema fields, all eleven risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, the null verdict, and the three unpursued leads without changing any claim.
