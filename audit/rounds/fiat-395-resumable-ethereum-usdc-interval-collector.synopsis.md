Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-395-resumable-ethereum-usdc-interval-collector.md | source_sha256=183b83b3f233dcb8abe893a3dbc33f215aaae45280edd7740557a1c2bb7854a1 | h2_count=2
## Step 1, round 1 -- 2026-08-31T03:56:27Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; coverage-inflation=not-applicable; reorg-rewind=not-applicable; epoch-gap=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable<br><br>Not checked: x-ray, solidity-auditor and fizz did not run; the `security_suite` receipt waives them because the run ships no Solidity and no Foundry or Hardhat project. Six of the eleven register concerns sit at boundaries steps 2 to 5 open and no code implementing them exists at this commit, so they were read as not-applicable rather than reviewed; `unbounded-response` is one of them, because its declared boundary is the transport and this step has none, but its staging-side sibling was found and is S1-R1-03 below. The three bundled lints each exited 0 over the step's changed Python and Markdown. The committed study and runbook were not re-audited for content: they are the receipted artefacts, with two surface deviations forced by repository law and recorded in the step commit message. One correction to the implement receipt: its test summary reported the root suite as exit 0, which was the exit code of a `tail` at the end of a pipeline rather than of `unittest`. Measured directly, that suite reports 776 tests with 2 failures, both of them this step's and both listed below.<br><br>Elenchus verdict: inconclusive<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | `resume()` treated a symlinked checkpoint as an absent one and truncated every journal to zero. Reproduced: one committed shard staged 72 bytes of `logs.jsonl`; replacing `checkpoint.json` with a symlink to an identical file outside the root made `resume()` return `next_shard: 0` and left the journal at 0 bytes. Bytes a completed shard had staged were discarded where a hostile or careless tree should have been refused. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-02 | low | plugins/alexandria/scripts/alexandria_lib/interval.py | A `journals` entry that is a symlink to a regular file raised a bare `FileExistsError` out of `Path.mkdir(exist_ok=True)` instead of a controlled `AlexandriaError`. Reproduced against a constructed staging root. Every other refusal in the module is controlled. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-03 | medium | plugins/alexandria/scripts/alexandria_lib/interval.py | `record()` bounded nothing while `entries()` refuses a journal above `MAX_JOURNAL_BYTES`. With the ceiling patched to 64 bytes, one record wrote 273 bytes and the reader then refused the journal it had just written. A long collection could stage evidence no reader will accept, and the writer would not say so. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-04 | low | plugins/alexandria/scripts/alexandria_lib/interval.py | `commit()` before `resume()` wrote a checkpoint whose `records` count understated the journals: measured `records=1` against two staged records. The count is working state a reader uses to judge a resumed run. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-05 | info | plugins/alexandria/scripts/alexandria_lib/interval.py | `DIGEST_RE` was unused, and `validate_checkpoint`'s `plan_digest` parameter shadowed the module function of the same name. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-06 | medium | .agents/skills/promise-machine/runtime | The step commit bdba6f1a20a79c908798eeb0d4109af69515b4ee shipped an incomplete portable mirror. `portable_promise_machine.py sync` builds its universe from `git ls-files`, the six new files were still untracked when it ran, and `check` then agreed with a universe that did not contain them. At bdba6f1a, `plugins/alexandria/scripts/alexandria_lib/interval.py` is tracked and its mirror copy is absent, so `check` fails at that commit. Found by re-running the whole battery rather than the suite the step touched. | fixed in 13147dde938797873dcde124321dd8b298978df0 |<br>| S1-R1-07 | high | .agents/skills/promise-machine/runtime/MANIFEST.json | The repaired mirror manifests 1001 files. `tests/test_skills_sh_package.py` asserts `file_count` is below `MAX_FILES = 1000`, a figure held because it is the skills CLI's `SKILLS_EXTRACT_MAX_FILES` default, and ADR-054 records the payload decision behind it. `main` at 1c1137898bce9086c34310bd29b5cf8a889f800c and bdba6f1a both manifest 995, so the repository stands four files below that ceiling. Step 1 needs six. The canonical `SKILL.md` links `docs/study.md` and `docs/runbook.md`, so omitting committed design records from the mirror would break the router's dependency closure, and the remaining five steps add roughly seven more mirrored files. The run cannot produce a tree that passes this suite without a repository-level decision about the portable payload or its ceiling. | open |<br><br>Leads not pursued: the ceiling in S1-R1-07 is the reason this round does not close. Three routes were measured and none fits inside this run's authority: raising `MAX_FILES` would misreport installability through a route the CLI actually gates; omitting `plugins/*/docs/**` would drop two documents the canonical `SKILL.md` links; and merging the three interval schemas into one file, plus omitting the step 6 example's fixture subtree the way `plugins/alexandria/examples/compound-v3-phase0-v0/input/**` already is, recovers four files against a shortfall of about seven. Issue #940 asked whether the payload should live outside this tree and was closed by ADR-054 in favour of keeping it, which is the decision this finding sits against. Not pursued: whether `portable_promise_machine.py` should refuse a sync whose universe is missing tracked files, which would have turned S1-R1-06 into a loud failure at the moment it happened; that belongs to the script's own owner rather than to an Alexandria step. Not pursued: whether `tests/test_skills_sh_package.py` should run in a workflow, which is the CI gap issue #882 already carries for the Alexandria and Probitas suites. The macOS `/var/folders` alias that finding S1-R1-03 of the release-statement run repaired in a sibling runner was checked here and holds: `resolve_root` resolves before comparison and `test_an_unresolved_alias_still_contains_its_own_children` covers it.
## Step 1, round 2 -- 2026-08-31T04:11:48Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: torn-shard=reviewed; staging-path-escape=reviewed; skip-as-pass=reviewed; whole-battery-regression=reviewed; silent-truncation=not-applicable; coverage-inflation=not-applicable; reorg-rewind=not-applicable; epoch-gap=not-applicable; reconciliation-bias=not-applicable; endpoint-leak=not-applicable; unbounded-response=not-applicable<br><br>Not checked: nothing new. The same six concerns still belong to steps 2 to 5 and the Pashov pair remains waived by the `security_suite` receipt. The round re-read the fixed module for faults the repair itself could have introduced: the size ledger `record()` now keeps is reset by `close()` and re-read from `fstat` when a journal is reopened, so a resumed run cannot inherit a stale figure across the truncation `resume()` performs; the symlink refusal added to `resume()` sits in front of an `exists()` call that would otherwise follow a link, and the read below it keeps `O_NOFOLLOW` so a swap between the check and the open is refused at open rather than raced.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the whole battery ran against the fixed tree. The three bundled lints exited 0, the Alexandria suite reported 352 of 352 through the runner step 1's contract names with zero skips, `portable_promise_machine.py check`, `horos.py check .`, `audit_synopsis.py --check .` and `git diff --check` each exited 0, and the working tree is clean. The root suite reports 776 tests with one failure, `test_manifest_binds_every_runtime_file_to_source_bytes`, which is S1-R1-07 and nothing else; the scratch-quiescence failure round 1 recorded alongside it is gone. S1-R1-07 stays open and is accepted rather than repaired here: framework-63, issue #949, moves the generated skills.sh payload into its own repository and lands after framework-65, issue #972, so the ceiling this run stands against is owned upstream and the contributor directed the run to continue locally against it. One thing is recorded rather than fixed: a refused oversized record still leaves an empty journal file behind, because `record()` opens the journal before it weighs the entry. The file is inside the staging root, carries no bytes and is the same file a successful record would have created, so nothing is disclosed and nothing is lost; moving the weight in front of the open would cost a stat per record for no evidence gained.
