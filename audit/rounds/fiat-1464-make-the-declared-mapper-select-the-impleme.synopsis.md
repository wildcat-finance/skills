Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1464-make-the-declared-mapper-select-the-impleme.md | source_sha256=4b19f9cc4acd2e25de1f14960e56d35a3ca8990c1424f3de9abefb7ff171a0c5 | h2_count=5
## Step 1, round 1 -- 2026-09-08T13:30:23Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=not-applicable; synopsis-cell-splitting=not-applicable; third-corpus-rights-basis=not-applicable; scope-bounds-for-the-third-corpus=not-applicable; program-digest-repin=reviewed; registry-mutability=not-applicable<br><br>Not checked: the security suite is waived for this run and the waiver is on the ledger; this step changes Python, Markdown and JSON and no Solidity, so x-ray, solidity-auditor and fizz have no target. Four register ids name artefacts step 2 owes, the synopsis mapper and the third corpus, and one names the immutability step 3 owes; each is recorded not-applicable here rather than reviewed early. Also unchecked: hosted CI, the controller receipt, push and publication; whether the registry is the right shape for a mapper that needs configuration, which the study argues and this step does not establish; and whether any second format is readable, which step 2 owns.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | plugins/anamnesis/tests/test_s11_registry.py | The step's exit requires the committed study, runbook and design record to be byte-identical to the receipted artefacts, and the guard for it read `.hexaemeron/`, which exists only inside this run's own worktree. It skipped everywhere else, including on a clean checkout, which is what CI runs: on a detached snapshot of this step's head the anamnesis suite reported four skips, two of them this test's own subtests, so the byte-identity claim was checked by nobody once the worktree is archived. This repeats a shape shipped twice before, in `test_s2_curation.ByteCap` and `test_s7_records.CommittedCopiesAreTheReceiptedArtefacts`, which skip on the same condition. A second guard now pins the three receipted digests and checks them unconditionally, and the controller comparison stays as the evidence that the pins track the live receipt. On the clean snapshot the suite runs 268 tests rather than 267, and appending one byte to the committed study fails the new guard | fixed in this round |<br><br>Leads not pursued: three observations carry forward. The two older skipping guards, `test_s2_curation.ByteCap.test_the_cap_is_the_number_the_design_record_declares` and `test_s7_records.CommittedCopiesAreTheReceiptedArtefacts`, still skip on a clean checkout; both belong to delivered runs whose worktrees are archived, so repairing them here would edit another delivery's guards outside this step's files, and the shape is now recorded twice in this record. The new pins couple three literals to the documents, so step 4's re-sync after any amendment moves them together or the guard reddens; the step 4 exit already re-syncs both documents and the fix's own docstring says so. The registry is a `MappingProxyType` over a module constant and nothing in a policy or a source can add an entry, which the study claims and the step's comment repeats; proving that a later import cannot write to it is `registry-mutability`, which step 3 owns and this round did not attempt. Evidence for the covered concerns: the unknown-mapper fixture refuses `A078` from `curate` and from `_admitted_within_scope` before any assertion, quarantine entry or release directory exists, with the release directory confirmed absent after the refusal, and the refusal emits `anamnesis.source.refused` carrying rule `A078`, policy version `estate-2026-09-06` and a 16-character correlation id; resolution is an exact `(name, version)` lookup with no fallback, default or partial match, and a non-string declaration refuses on the same rule; `_assertion` writes `mapper_identity(resolved)` rather than the policy string; the unreferenced `MAPPER` constant is gone, with zero remaining references; `cmd_ingest` resolves the same way; both shipped specimens rebuild to `41d640fb168049d5061e12c9d7282dafad2266343eeb0be2a078db8797c0bfbf` and `509239765f9fa2db782d3bc70fadea3b05411fc0638402fe5e0a43882f0063e3`, and both `curation-policy.json` files are byte-unchanged; the demonstration ledger moved to `anamnesis-demo-v0.4.0` as a generation with its frontier revision and digest unchanged, and the root card digest follows the new program digest; the runner admits steps 1 through 14 and step 11 passes 19 of 19; and the three lints, `git diff --check`, the Horos boundary check and the whole-set synopsis check each exit 0, with the root suite green at 1,628 tests.
## Step 1, round 2 -- 2026-09-08T13:35:16Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=not-applicable; synopsis-cell-splitting=not-applicable; third-corpus-rights-basis=not-applicable; scope-bounds-for-the-third-corpus=not-applicable; program-digest-repin=reviewed; registry-mutability=reviewed<br><br>Not checked: unchanged from round 1, except that this round did reach registry mutability far enough to record what it observed, which does not discharge the guard step 3 owes. Still unchecked: hosted CI, the controller receipt, push and publication, and whether any second format is readable, which step 2 owns.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: round 1's three stand as recorded, and one observation joins them. `resolve_mapper` reads only `name` and `version` from the declaration it is handed, so called directly with `{"name": "warden-audit-round-markdown", "version": "1", "parse": "x"}` it resolves and ignores the extra key. That is not reachable from a policy file: `load_curation_policy` closes the mapper object to exactly those two keys and the same input refuses `A011` at load, before resolution is reached, and the function's own docstring states that layering. It is recorded because the guard for it lives in the layer above rather than in the function a reader of the registry would look at. Round 2 asked whether a resolved mapper can still misreport what read a source and found it cannot: the registry holds one key, its entry's `parse` is `parse_source` itself, a resolved entry's identity is byte-equal to the declaration that selected it, the mapping proxy raises `TypeError` on assignment, a missing version, a list-valued name and an integer version each refuse `A078`, and every assertion in the shipped estate release carries the one resolved identity that its manifest policy declares.
## Step 2, round 1 -- 2026-09-08T14:31:43Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=not-applicable; synopsis-cell-splitting=reviewed; third-corpus-rights-basis=reviewed; scope-bounds-for-the-third-corpus=reviewed; program-digest-repin=reviewed; registry-mutability=not-applicable<br><br>Not checked: the security suite is waived for this run and the waiver is on the ledger; this step changes Python, Markdown and JSON and no Solidity. `mapper-backtracking` and `registry-mutability` are step 3's, which owns the probe set and the immutability guard, and this round did not attempt either. Also unchecked: hosted CI, the controller receipt, push and publication; whether the synopsis rendering is faithful to the audit records it renders, which `audit_synopsis.py` owns and this corpus preserves rather than judges; and whether a consumer that reads two projections will act on the scope now carried in them.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | plugins/anamnesis/skills/anamnesis/schemas/synkrisis-observation-v1.json | The synopsis corpus preserves the pilot's own findings: both releases carry 41 findings over a denominator of 41, and their assertions carry the identical set of 36 native finding ids. What tells them apart is the declared scope, whose `preserves` sentence states that this corpus renders the same three audit records rather than adding new ones. That sentence sat in the release and was hashed into its id, and the Synkrisis view carried `release_id`, `cohort`, `denominators`, `policy`, `exclusions`, `unknowns` and `not_established` but not the scope. A consumer reading two projections saw two cohorts of the same size over the same denominator with nothing to distinguish a second rendering from a second corpus, which is the reading the whole denominator discipline exists to prevent. The observation schema now requires `scope` and `observations` carries it from the manifest; a projection without it refuses `A145`, and all three committed projections were regenerated. No release id moves, because a projection is not a release component | fixed in this round |<br><br>Leads not pursued: three observations carry forward. The Elenchus analogue view still carries no scope; it returns per-finding analogues rather than counts, so the double-count reading this finding is about does not arise there, and widening a second closed schema was not done on a risk nobody has. Step 2 changed `plugins/anamnesis/tests/test_s11_registry.py`, which its Files field does not name: step 1's guards assert that the registry holds one entry and that the selected candidate's conformance report does not exist, and step 2 must falsify both by design, so the step cannot be green at both ends without touching them; both edits tighten rather than relax, the second now also asserting that the three rejected candidates' reports stay absent, and the Files field is a description this round records as incomplete rather than amending. The synopsis sources' guard permits a repository-relative `origin_path` where the estate corpus's identical-looking guard forbids every path, and the difference is deliberate: the estate's originals are held by the maintainer and its sources may name no location, while these three are tracked files in this repository whose provenance is exactly their path. Evidence for the covered concerns: the header check refuses `A079` on a header-less file, on one declaring another schema and on empty bytes, before any cell is read, and emits the existing refusal event; across all three sources the Warden mapper returns 31 rounds and 0 findings against the synopsis mapper's 31 rounds and 41 findings, so the fail-open control is exact; a policy-level read of the synopsis sources under the pilot curation policy refuses `A074` at the scope check and emits its event, and that refusal predates this step rather than being new to it; the three sources are byte-identical to the tracked `AUDIT_SYNOPSIS.md` files at the declared digests and 36,455 bytes, each admitted under `licence`, Apache-2.0, holder Wildcat Labs; the pilot and estate rebuild unchanged to `41d640fb` and `50923976` and both shipped curation policies are byte-unchanged; the 32 reports committed in step 1 are byte-unchanged and the thirty-third parses as one closed report object; and the three lints, `git diff --check`, the Horos boundary check, the whole-set synopsis check and the design checker at `step:3`, `step:4` and `integration` each exit 0, with the root suite green at 1,628 tests.
## Step 2, round 2 -- 2026-09-08T15:23:45Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=not-applicable; synopsis-cell-splitting=reviewed; third-corpus-rights-basis=reviewed; scope-bounds-for-the-third-corpus=reviewed; program-digest-repin=reviewed; registry-mutability=not-applicable<br><br>Not checked: unchanged from round 1. `mapper-backtracking` and `registry-mutability` remain step 3's. Also unchecked: hosted CI, the controller receipt, push and publication.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: round 1's three stand, and the check selection this step's exit requires produced one red that is not this step's and is recorded here rather than fixed. `python3 scripts/run_checks.py --base origin/main` from a clean snapshot of the step head reported 14 checks green and `hexaemeron-suite` red at 1,104.9 seconds, failure class `test-failure`, one failing case: `test_kronos_scoreboard.ScoreboardTest.test_no_governed_ledger_declares_anything_today` on `ledger='plugins/anamnesis/skills/anamnesis/EVOLUTION.md'`, asserting that no governed ledger declares an input while that ledger carries the `declared-inputs` block naming `foreign-format-corpus`. The block was added by the delivery this run's held job came from, issue 1351, and merged as `12cbe2e6`; the assertion was renamed and generalised on `main` the following morning in `aa18376a`, which admits a ledger that carries a declaration. This run's base `f0ef9266` predates that repair, so the red is the age of the base and not the diff: on a scratch composition of this step's head with `origin/main` at `41a21f8e`, resolving only the generated `.horos/census.json` by regenerating it, the same suite reports 2,480 of 2,480 passing, and `origin/main` alone reports the same. The integration sync performs that composition, so the remedy is the one the loop already owes rather than a change to this step. Two things follow that this round records rather than acts on. A change merged through the integration gate broke a suite no gate ran, which is the condition issue 1260 already describes, and this is a second instance of it with a named test and a named repair. And the repair arrived from outside this run, so a reader comparing this round's red against a later green needs both commits to understand it, which is why both are named here. Evidence for the covered concerns: after round 1's fix all three projections carry their declared scope, a scope-less projection refuses `A145`, the pilot and synopsis views are distinguishable by scope id while carrying the same 41 findings over the same denominator, and the estate view is unaffected at 17; all three specimens rebuild unchanged to `41d640fb`, `509239765f9f` and `74c591e1f010`, so carrying the scope into a view moved no release id; the anamnesis suite is 284 tests with 2 skips, the step 12 runner passes 16 of 16, and the three lints, `git diff --check`, the Horos boundary check, the whole-set synopsis check and the design checker at `step:3`, `step:4` and `integration` each exit 0.
## Step 3, round 1 -- 2026-09-08T16:36:05Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=reviewed; synopsis-cell-splitting=reviewed; third-corpus-rights-basis=reviewed; scope-bounds-for-the-third-corpus=reviewed; program-digest-repin=reviewed; registry-mutability=reviewed<br><br>Not checked: the security suite is waived for this run and the waiver is on the ledger; this step changes Python, Markdown and one test file and no Solidity. Also unchecked: hosted CI, the controller receipt, push and publication; whether the possessive rewrite is optimal, as against sufficient, which the measurements below bound rather than settle; and whether a corpus admitting bytes this repository did not write raises a different parsing concern, which the successor job owns.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S3-R1-01 | high | plugins/anamnesis/skills/anamnesis/scripts/anamnesis.py | `FINDING_ROW` put a greedy `\s*` beside a lazy group at six places, so the engine could try every way of splitting one span of whitespace across both. Measured on a cell shaped `\| S1-R1-01 \| medium \| ` followed by spaces: 11.9 ms at 269 bytes, 101.6 at 519, 776.8 at 1019, 5,869.9 at 2019 and 45,255.6 at 4019, about eightfold per doubling, with or without a closing pipe. A cell is bounded only by the physical line the source byte cap bounds, and `max_source_bytes` is 1,000,000 in all three shipped policies under a ceiling of 8,000,000, so one crafted line inside an admitted source would have held curation indefinitely. The step's exit permitted recording the cap as the only bound claimed, and the implementation took that option; a measured super-linear growth is not a bound, and the corpus exists to admit bytes this repository did not write. Every whitespace run in the pattern is now possessive, which removes the ambiguity without changing what the pattern accepts: the same crafted cell answers in 0.009 ms at 1,000 bytes, 0.055 at 10,000, 0.661 at 100,000 and 4.612 at 1,000,000, linear across three orders of magnitude, and all three specimens rebuild to the release ids they shipped with. The decision record's consequences now carry the measured bound in place of the restated cap, and a guard holds the structural property rather than a duration, because a wall clock would be the budget study section 10 declines to declare | fixed in this round |<br><br>Leads not pursued: three observations carry forward. Eight of the eleven guards this step ships pass at the parent commit `c668469e`, because the step adds no program bytes by design and its guards exercise controls steps 1 and 2 built, whose own guards went red at their own parents; the two that fail there are the backtracking outcome and the decision record's shape, and the possessive guard added by this round's fix is a third. Coupling the other eight to this step's prose to make them red would be worse test design, and the implementation said so before I asked. The `%b` split still loses a producer cell that carries the separator rather than shortening it: neither fragment matches the finding grammar, so no partial row reaches a record, but the row is absent from the release and nothing counts it, which is fail-closed on the byte and silent at the corpus, and no admitted source carries the separator inside a cell today. The step's own `run_checks` selection is red on `hexaemeron-suite` for the base-age reason step 2 round 2 records in full, and this round did not re-measure it. Evidence for the covered concerns: assigning into the registry, replacing it and deleting an entry each fail after import, and a source declaring an implementation, a policy field naming a module path and an environment variable naming one all fail to reach it; the synopsis mapper refuses `A079` and returns no rounds on Warden Markdown and on each of the pilot's three source files; every pattern returns on the widest admitted cell, 668 bytes at line 7 of the pandects synopsis, and on all 434 admitted cells; the decision record carries the five sections, a dated status, all three decisions from study section 12 and the measured value 2; the pilot, estate and synopsis rebuild to `41d640fb`, `509239765f9f` and `74c591e1f010`; the demonstration ledger re-pins the program digest and carries one new generation row at `anamnesis-demo-v0.6.0` with its frontier revision and digest unchanged, the root card follows, and both gates exit 0; and the three lints, `git diff --check`, the Horos boundary check and the design checker at `step:4` each exit 0.
