Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1464-make-the-declared-mapper-select-the-impleme.md | source_sha256=ac0a02d16cd49c2a144a72708a74d8cfa86d329a15954602b5e8011505a6af0c | h2_count=1
## Step 1, round 1 -- 2026-09-08T13:30:23Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: unresolved-mapper-writes-records=reviewed; fail-open-mapper=reviewed; declared-not-resolved-in-assertion=reviewed; release-identity-drift=reviewed; mapper-backtracking=not-applicable; synopsis-cell-splitting=not-applicable; third-corpus-rights-basis=not-applicable; scope-bounds-for-the-third-corpus=not-applicable; program-digest-repin=reviewed; registry-mutability=not-applicable<br><br>Not checked: the security suite is waived for this run and the waiver is on the ledger; this step changes Python, Markdown and JSON and no Solidity, so x-ray, solidity-auditor and fizz have no target. Four register ids name artefacts step 2 owes, the synopsis mapper and the third corpus, and one names the immutability step 3 owes; each is recorded not-applicable here rather than reviewed early. Also unchecked: hosted CI, the controller receipt, push and publication; whether the registry is the right shape for a mapper that needs configuration, which the study argues and this step does not establish; and whether any second format is readable, which step 2 owns.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | plugins/anamnesis/tests/test_s11_registry.py | The step's exit requires the committed study, runbook and design record to be byte-identical to the receipted artefacts, and the guard for it read `.hexaemeron/`, which exists only inside this run's own worktree. It skipped everywhere else, including on a clean checkout, which is what CI runs: on a detached snapshot of this step's head the anamnesis suite reported four skips, two of them this test's own subtests, so the byte-identity claim was checked by nobody once the worktree is archived. This repeats a shape shipped twice before, in `test_s2_curation.ByteCap` and `test_s7_records.CommittedCopiesAreTheReceiptedArtefacts`, which skip on the same condition. A second guard now pins the three receipted digests and checks them unconditionally, and the controller comparison stays as the evidence that the pins track the live receipt. On the clean snapshot the suite runs 268 tests rather than 267, and appending one byte to the committed study fails the new guard | fixed in this round |<br><br>Leads not pursued: three observations carry forward. The two older skipping guards, `test_s2_curation.ByteCap.test_the_cap_is_the_number_the_design_record_declares` and `test_s7_records.CommittedCopiesAreTheReceiptedArtefacts`, still skip on a clean checkout; both belong to delivered runs whose worktrees are archived, so repairing them here would edit another delivery's guards outside this step's files, and the shape is now recorded twice in this record. The new pins couple three literals to the documents, so step 4's re-sync after any amendment moves them together or the guard reddens; the step 4 exit already re-syncs both documents and the fix's own docstring says so. The registry is a `MappingProxyType` over a module constant and nothing in a policy or a source can add an entry, which the study claims and the step's comment repeats; proving that a later import cannot write to it is `registry-mutability`, which step 3 owns and this round did not attempt. Evidence for the covered concerns: the unknown-mapper fixture refuses `A078` from `curate` and from `_admitted_within_scope` before any assertion, quarantine entry or release directory exists, with the release directory confirmed absent after the refusal, and the refusal emits `anamnesis.source.refused` carrying rule `A078`, policy version `estate-2026-09-06` and a 16-character correlation id; resolution is an exact `(name, version)` lookup with no fallback, default or partial match, and a non-string declaration refuses on the same rule; `_assertion` writes `mapper_identity(resolved)` rather than the policy string; the unreferenced `MAPPER` constant is gone, with zero remaining references; `cmd_ingest` resolves the same way; both shipped specimens rebuild to `41d640fb168049d5061e12c9d7282dafad2266343eeb0be2a078db8797c0bfbf` and `509239765f9fa2db782d3bc70fadea3b05411fc0638402fe5e0a43882f0063e3`, and both `curation-policy.json` files are byte-unchanged; the demonstration ledger moved to `anamnesis-demo-v0.4.0` as a generation with its frontier revision and digest unchanged, and the root card digest follows the new program digest; the runner admits steps 1 through 14 and step 11 passes 19 of 19; and the three lints, `git diff --check`, the Horos boundary check and the whole-set synopsis check each exit 0, with the root suite green at 1,628 tests.
