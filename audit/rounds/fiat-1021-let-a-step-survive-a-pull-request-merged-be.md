## Step 1, round 1 -- 2026-09-01T10:46:11Z

Audit schema: fiat-audit-round/v2

Covered: head-mismatch-admitted=not-applicable; unreachable-merge-admitted=not-applicable; ancestry-unanswered=not-applicable; second-merge-suppressed=not-applicable; early-merge-indistinguishable=reviewed; state-mutation-on-refusal=not-applicable; diagnostic-overclaim=reviewed; github-verification-gap=not-applicable; integration-gates-intact=not-applicable; verify-replay-drift=not-applicable; legacy-run-compatibility=not-applicable; regression-uses-disposable-repo=reviewed

Not checked: every controller behaviour steps 2 through 5 introduce. This step commits three documents and touches no code, so the nine not-applicable concerns above have no surface here yet. Also not checked: live GitHub branch, pull-request, merge, signature and integration operations; the recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run. Also not checked: whether the selected design behaves as declared once written. The design record measures declared change sets against the real tree, and `measure_design.py` states that boundary itself.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | docs/fiat-early-step-merge-study.md | Item 2 cited `028dcdc8`, `8cc85686` and `0949a2e5` as the last pull requests to change the Fiat skill. All three are base-sync merge commits, not pull-request merges: this repository mostly squash-merges, so a pull request that changed the skill arrives as a direct commit and never appears in `git log --merges`. The citation therefore answered a different question from the one Protasis's checklist asks, and it omitted the more relevant of the two reads, because 1017 is the pull request whose early merge into step 1's branch produced issue 1021. The correct reads are 1040, landed as `c148ab9a` and merged 2026-08-31T13:02:49Z, and 1017, landed as `08a1024c` and merged 2026-08-31T05:42:09Z. Corrected by amending the receipted study, prior digest `1fd554b05e12b80d942ae15cf42d73392255a584170d93ddb90016104986ff82`, new digest `0be35b98e49971e71ff205dd6b1f3887de1cc68c4b5e215a61a9a9921a56251d`, amendment digest `7b3b71987ec5ac3f12dc26e7d0c2a9f891efd0d7a991f360534dd5d6648f5a1c`, then regenerating the committed copy from the amended bytes. | fixed in b6430371d93f0d389e289262a4000a64c9c06431 |

Leads not pursued: the runbook's per-step Exit says "the suite is green" rather than naming the repository-wide suite explicitly, which is a known way for a step to earn a green receipt and still be red on hosted CI, recorded as issue 1067. It is not repaired by a runbook amendment here, because both suites are being run at every step exit and their counts recorded in this file: this round reports the root suite at 1110 of 1110 OK and the Hexaemeron plugin suite at 2131 of 2131 passed with zero failures and zero errors, both against the fixed tree at `b6430371`. Evidence for the covered concerns: the three lints exit 0 over the four paths of `510cb51d` and report clean having read real paths rather than an empty argument list; `early-merge-indistinguishable` is answered by ADR-068 fixing the receipt field name `early_merge` and stating why an inferred early merge does not satisfy issue 1021; `diagnostic-overclaim` was the finding above and every remaining claim in the study and the ADR was checked against the tree at `53fba06a`, including the shared-parser registration at line 14480, the head check at line 11317, the refusal at line 11330 and the 62-commit base advance; `regression-uses-disposable-repo` is answered by confirming `plugins/hexaemeron/tests/hexctl_harness.py` exists and that the focused-module runner contract works, verified by running issue 923's module to 10 tests OK. Two runner traps were established rather than assumed and cost no findings: the positional argument to `run_tests.py` is the report path and not a test selector, which is the shape of issue 923's own S2-R1-01, and `--elenchus-report` refuses a target that already exists, so each round needs a fresh report path.
