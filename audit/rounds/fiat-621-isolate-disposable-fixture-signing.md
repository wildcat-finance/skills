## Step 1, round 1 -- 2026-08-28T18:06:32Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=not-applicable; vacuous-regression=not-applicable; config-scope-escape=not-applicable; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=not-applicable; missed-construction-site=not-applicable; clone-and-worktree-paths=not-applicable; skip-instead-of-fail=reviewed; sentinel-leak=not-applicable; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite is waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. Seven register ids are marked not-applicable because their subjects are the hostile-configuration harness, the guard, the fixture construction rule and the sentinel, none of which exist yet: injection-precedence, vacuous-regression, config-scope-escape, format-coverage, missed-construction-site, clone-and-worktree-paths and sentinel-leak subjects arrive in steps 2 through 5, and this round reviews only what step 1 committed. The Elenchus classifier did not run because this round made no fix, so there is no verdict to report. The runbook's step 2 to step 5 exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: no defect was found in the complete five-file diff at signed step tip `a3e50cb46f793f410693d8d65b4bafa4268a04bc`, which carries a good SSH signature for dave@wildcat.finance under key SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk, exactly one `Co-authored-by: Shoggoth` and one `Wildcat-Origin: shoggoth` trailer, and no runtime-host byline. `diff -u plugins/hexaemeron/tests/run_tests.py tests/run_tests.py` produces exactly two hunks covering the three divergences the 2026-08-28 amendment permits: the module docstring at line 2, and `repository = os.path.dirname(here)` with `top_level_dir=repository` in `main`, the discovery root following from the file's placement under `tests/`. Every other byte, including all 20 lines of containment, is identical, and both files are mode 100644. The two spec copies are byte-identical to the receipted artefacts at SHA-256 61fb70b62e01f38caa500b28917a7d3b69f64313d591a06710ac0bc21d04e090 and a73d74a87192c13cea4324de6f0f251999ddf28d5befcfb5c3ba546cfb7c6556, and both `cmp` calls exit 0. All fifteen amended Exit commands exit 0 under pinned CPython 3.14.6 at `/Users/kethcode/.local/bin/python3.14`; phylax, ephoros and hypomnema each print `clean` and exit 0 from both the repository scripts and plugin cache 1.6.7; `git diff --check` over `f5d94a5f27168e6c0faecd43eee426f6ee892cdc..a3e50cb4` exits 0; horos reports `boundary matches the tree` with no drift, so `.horos/boundary.json` is correctly unchanged. The root suite runs 470 tests with zero failures, and a clean detached worktree at parent `f5d94a5f27168e6c0faecd43eee426f6ee892cdc` runs 462, confirming the commit message's 462-to-470 claim and the eight expected new tests with no existing case lost. `.elenchus/fiat-621-step-1.json` is 161 bytes declaring `elenchus.unittest.v1`, `complete: true`, 470 tests, zero failures, zero errors and zero skips; its directory is created 0o700 and the file 0o600; it was removed afterwards and the tree is clean. Beyond the eight shipped cases, thirteen direct probes of the ported boundary all held: a parent directory symlinked outside the worktree, an empty path, a NUL byte, `..` mid-path, the worktree root itself, an existing directory target, an existing symlink to `/etc/passwd`, and a positional path combined with the flag are each refused with the diagnostic naming the failed precondition; an absolute path inside the worktree, a parent symlink resolving inside it and nested new directories are accepted; and both races fail closed, with a symlink planted at the target after the check raising FileExistsError and a parent replaced by an outside symlink raising NotADirectoryError, neither leaking a file outside the worktree. Nested directories are created 0o700 and the report 0o600. The two most security-relevant of those behaviours, the symlinked parent resolving outside and the two races, are not covered by `tests/test_root_elenchus_runner.py`; the step's own Tests field does not require them, so this is recorded as a coverage observation rather than a defect. The added `tests/__init__.py` introduces a top-level `tests` package, and no sibling suite regressed: Hexaemeron passes 1,394 of 1,394 with one skip under node 26.6.0, berean 162 with one skip, alexandria 295, ariadne 689 with seven skips, brevitas 70, horos 228, pandects 117, probitas 276, sapheneia 11 and tabularium 134 all pass, the imprimatur runner passes 112 of 112 and `scripts/portable_promise_machine.py check` exits 0. Synkrisis reports one `test_bench` RSS-budget failure and lazarus sixteen missing-dependency loader errors; both reproduce byte-for-byte at the parent commit and are pre-existing host conditions, not this step. On the register: contributor-config-mutation holds because every new test writes only inside its own `tempfile.TemporaryDirectory`, restores the working directory in a `finally`, and leaves `git status --porcelain` empty before and after; subprocess-argv holds because the added files contain no `subprocess`, `os.system`, shell or git-config call at all, so no phylax pragma is needed; skip-instead-of-fail holds because none of the eight new cases uses `skipTest` or `skipIf` and the report records zero skips, and `refusal()` raises AssertionError when a hostile path is accepted, so the refusal cases cannot pass vacuously; signature-test-weakening holds because `plugins/hexaemeron/tests/test_issue_429_recovery.py` and `plugins/hexaemeron/tests/test_hexctl.py` are untouched by the five-file diff, leaving their assertions and case counts unchanged by construction; measurement-honesty holds because the committed study's section 10 names this host and its SSH signing configuration, explicitly declines to re-claim the issue's 188.38-against-6.91-second figures recorded at revision e4d1f5677fa1 on a machine this run does not have, records M1 at 25.6, 25.5 and 25.5 ms per signed commit against 20.3, 20.5 and 20.1 unsigned with spread at most 0.4 ms and improvement 5.2 ms, and states that M2's expected suite-level delta is inside variance and will be reported as inside variance rather than as a speedup. Three qualifications are recorded without raising a finding. The step commit message says "Two things diverge from the reference" and then names only the discovery root and the top-level directory, while the amendment permits and the code makes three; the omitted docstring change is permitted and correct, and repairing the sentence would require rewriting the receipted step commit the controller verified, so it was left alone. Neither committed document contains a relative Markdown link, so the amended Exit's claim that the hypomnema command proves every relative link resolves from `docs/disposable-fixture-signing/` holds only vacuously. Imprimatur scores the study 100.0 with zero defects and the runbook 98.8 with one high `structural_metaphor: 'load-bearing'` at 459:25, which falls inside the receipted amendment text and cannot be edited without breaking the required byte identity; imprimatur still exits 0. The amendment's own justification was verified rather than assumed: brevitas invoked on both documents in one call exits 2 with `unrecognized arguments`, and the two single-document calls each exit 0. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and deferred scope, the zero-finding row, the null verdict, every commit, key, hash, path, count, byte size, permission mode and millisecond figure, all three qualifications, and the coverage observation, without changing any claim.

## Step 2, round 1 -- 2026-08-28T21:16:53Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite is waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The guard constructs disposable repositories with `git init` only; no `git clone` or `git worktree add` arm was executed, and no linked worktree was shown to share the config it inherits, so clone-and-worktree-paths is reviewed against what step 2 commits rather than demonstrated. The covered-suite enumeration asserts nothing this round: `COVERED_SUITES` is the empty tuple by the step's own design, so `CoveredSuites` iterates zero suites and no construction site is yet caught by it. Both new cases were exercised only on a host where `ssh-keygen` and `git` are present; the fail-rather-than-skip branches for a missing `ssh-keygen`, a missing `git` and an unwritable hostile configuration were read but not driven by removing the binaries. Sibling plugin suites were not re-run this round; only the root suite was executed, and step 1's round holds the cross-suite evidence. The runbook's step 3 to step 5 exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | tests/test_disposable_fixture_signing.py | The positive case could not detect a signed fixture commit, which is the defect the delivery exists to prevent on an ssh-signing host. `LocalDeclaration` reads `git log -1 --format=%G?` under `harness.child_environment(files.config)`, and that hostile configuration names no `gpg.ssh.allowedSignersFile`, so git cannot verify an ssh signature under it and answers `N` -- the same letter a genuinely unsigned commit gets. Measured: a commit carrying a `gpgsig -----BEGIN SSH SIGNATURE-----` header reads `G` under the contributor's own configuration, `N` under the hostile openpgp arm and `N` under the hostile ssh arm, while the unsigned control also reads `N`; git prints `error: gpg.ssh.allowedSignersFile needs to be configured and exist for ssh signature verification` to stderr and still exits 0. Mutating the fixture builder so the declared repository signs with a throwaway ed25519 key instead of declining left the pre-fix `LocalDeclaration` reporting `OK`, so the assertion the step's Tests field calls the discriminator carried no information. | fixed in `0788c09ad93ac1aa9eed5ac73fa38750b17453f1`; `signature_headers` reads the commit object with `git cat-file commit HEAD`, which needs no verifier and no key material and cannot be fooled by the configuration the read happens under; the `%G?` assertion is kept unchanged because the step's Tests field requires it and it is correct as far as it goes; under the same signing mutation the positive case now fails with `AssertionError: Lists differ: ['gpgsig'] != []`; `SignedFixture` pins the reason by building a genuinely signed commit from a throwaway ed25519 key that never leaves the test's own temporary directory and recording both halves, `%G?` saying `N` and the object saying otherwise, and it fails rather than skips when `ssh-keygen` is absent; root suite 480 of 480 green with zero failures, errors and skips |
| S2-R1-02 | medium | tests/hostile_signing_harness.py | `child_environment` dropped `GIT_CONFIG_COUNT` so that no value could reach a fixture carrying `git -c` precedence, which outranks the repository-local declaration the guard exists to test, but left `GIT_CONFIG_PARAMETERS` in place. That variable carries the same precedence and arrives by the route the dropped list's own comment names: git converts its own `-c` into it and hands it to every process it spawns. Measured against git 2.54.0: a fixture that declares `commit.gpgsign false` correctly still reached the signer under an inherited `GIT_CONFIG_PARAMETERS`, commit exiting 128 with 61 bytes in the sentinel, identical to the `GIT_CONFIG_COUNT` triple the harness already refuses; a pre-commit hook run under `git -c some.key=somevalue` saw `GIT_CONFIG_PARAMETERS` set while `GIT_CONFIG_COUNT` was unset. So the tuple dropped the variable a caller types by hand and kept the one git itself propagates. The worse direction is vacuity rather than a false red: in steps 3 and 4 a covered suite runs under the hostile configuration and is asserted to leave the sentinel empty, and an outer `GIT_CONFIG_PARAMETERS` carrying `commit.gpgsign=false` would force every fixture unsigned however it was built, passing the guard with no construction site having declared anything. | fixed in `4bc05e56d81a36cd99c5d09a6965af76c4b1bb55`; `GIT_CONFIG_PARAMETERS` joins `REPOINTING_VARIABLES`, and `GIT_CONFIG_KEY_n` and `GIT_CONFIG_VALUE_n` stay because they are inert once the count that introduces them is gone; the environment test asserted the drop against `os.environ`, which holds neither variable and so passed without the drop, and now asserts against a base carrying both channels through `child_environment`'s previously unused `base` parameter; `InheritedPrecedence` proves it end to end with a correctly declared fixture committing clean under either inherited channel, and reverting the one-line drop fails it with `AssertionError: 128 != 0` and the sentinel holding `hostile signer reached: --status-fd=2 -bsau 0123456789ABCDEF` |

Leads not pursued: the round reviewed the complete three-file step diff at signed step tip `3037787a19045ee07dc0ac636ca74944e9628d66`, which adds `tests/hostile_signing_harness.py` at 182 lines and `tests/test_disposable_fixture_signing.py` at 329 lines and refreshes `docs/disposable-fixture-signing/runbook.md` by 73 lines, and both fix commits carry a good SSH signature for dave@wildcat.finance under key SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk, exactly one `Co-authored-by: Shoggoth` and one `Wildcat-Origin: shoggoth` trailer each, and no runtime-host byline; `git diff --check` over `3037787a..0788c09a` exits 0. The Elenchus verdict of `passed` was determined with the step's own runner contract, `/Users/kethcode/.local/bin/python3.14 tests/run_tests.py --elenchus-report {report}` at format `unittest-json-v1` and report file `.elenchus/fiat-621-step-2.json`, against `HEAD` and again against `4bc05e56`, both returning `passed` with the detail `the runner report records that the guard passed on the parent` and reports of 480 and 479 executed tests with zero assertion failures, errors and skips. The mechanism explains it and no relabelling is claimed: the classifier builds a detached worktree at the parent, copies the commit's changed test files into it and runs the command, so where a change is entirely under `tests/` the fix travels with the guard and the parent cannot fail. `unguarded` was not the mechanical answer here, because the classifier reserves that status for a commit that changed no test files at `elenchus.py:281`; the substantive guard evidence sits in the two mutations recorded above, where reverting only the harness drop fails `InheritedPrecedence` with `128 != 0` and re-signing the declared fixture fails the positive case with `['gpgsig'] != []`. All six amended Exit commands, the three generator `test` checks and the emit call exit 0 at the audit tip under pinned `/Users/kethcode/.local/bin/python3.14`; phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7 and from the repository copies; `cmp` still reports both spec copies byte-identical to the receipted artefacts; `.elenchus/fiat-621-step-2.json` declares `elenchus.unittest.v1`, `complete: true`, 480 tests and zero failures, errors and skips, and was removed afterwards leaving the tree clean. The four scrutiny points the packet named were each driven rather than read. The `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` triple appears nowhere in either file as a mechanism, only in prose and in one dropped tuple entry and one assertion, and the surrounding channels were tested individually: an inherited `XDG_CONFIG_HOME` holding `commit.gpgsign = true` and an inherited legacy `GIT_CONFIG` holding the same both leave a declared fixture committing at 0 with an empty sentinel, because `GIT_CONFIG_GLOBAL` being set suppresses them, so `GIT_CONFIG_PARAMETERS` was the single remaining open channel and is now closed. Sentinel isolation holds under nesting: a hostile directory emitted inside another records 61 bytes into its own `sentinel.log` while the outer sentinel stays at 0 bytes, and it stays at 0 even with `HOSTILE_SENTINEL` exported at the outer path, because the emitted signer resolves its sentinel from `dirname "$0"` and ignores the variable entirely. No leakage was found in either direction: `~/.gitconfig` at SHA-256 15cb85c8b86f7528cded24b504615ac33a9890b62c58021177f15e2d06b443a0 and the shared local config at `/Users/kethcode/wildcat/skills/.git/config` at SHA-256 ef69159f5c509474baeab95738614af97b9cf99d5feaf63c60ca0759180fb470 are byte-identical before and after the guard runs, the `~/.ssh` listing is unchanged, `git status --porcelain` holds only the two untracked audit files, the process environment carries no `GIT_CONFIG` variable at all, and the TMPDIR listing is byte-identical across a guard run, so the harness leaves no directory behind. Fail-rather-than-skip holds by construction: neither file calls `skipTest`, `skipIf` or `SkipTest`, every precondition path routes through `self.fail` with a message that names what was missing, `load_harness` runs at import so an unreadable harness raises rather than skips, and the report records zero skips. Five mutations were run against the step tip and reverted byte-identically, with both files hashing back to 7a0574f22d94785f6a961f9816e03eace4954b324914c8f91d69cc46d3e7845a and 584913f84a0ffd77cc6e3749a038267771b8afbc6303264901fa9720448dcbdb: removing the signer's recording line and turning the openpgp arm's `gpgsign` to `false` each fail `NegativeControl` with the message naming the harness rather than the fixtures, and removing the local declaration fails `LocalDeclaration` with `128 != 0`, so the amendment's mutation claim is confirmed rather than assumed. One mutation did not kill the guard and is recorded as a qualification rather than a finding: changing the signer's `exit 1` to `exit 0` leaves `NegativeControl` green, because a signing program that returns success while emitting no signature is still rejected by git itself, so `exit 1` is belt-and-braces and the arm's `gpgsign = true` plus the recording line are what carry hostility. Four further observations are recorded without raising a finding. The amended Exit block exports `HOSTILE_SENTINEL` at the emitted sentinel's path, which the same amendment then makes inert by having the signer resolve its own directory; the two paths agree by construction for any caller using the emitted layout, and the block is receipted text whose bytes must match the shipped copy, so it was left alone. That block also creates its hostile directory with `mktemp -d` and never removes it, so each verification run leaves a mode 0700 signer and a sentinel in TMPDIR; two such directories from earlier step-2 verification, `tmp.wBmzJA4Sxq` and `tmp.FSyUOsHeVX`, were observed and left in place, and this residue comes from the shell block rather than from the Python guard, which the identical TMPDIR listing clears. `CoveredSuites` currently asserts nothing because `COVERED_SUITES` is empty, which the step's Tests field states explicitly, so missed-construction-site is reviewed as correct-for-now rather than demonstrated. On the rest of the register: config-scope-escape holds because every git call in both files routes through `self.git`, which builds `[GIT, "-C", str(root), *arguments]` with the fixture root always under the case's own `tempfile.TemporaryDirectory`, and no call can fall through to the outer checkout; format-coverage holds because all three arms are present and exercised, the openpgp arm through `gpg.program`, the ssh arm through the `[gpg "ssh"]` section's `program`, and the unsigned control committing at 0 with an empty sentinel; subprocess-argv holds because every call uses fixed argv with no `shell=`, no `os.system` and no `os.popen`, resolves the interpreter through `sys.executable` and the binaries through `shutil.which`, and bounds itself at 60 or 300 seconds, and phylax asks for no pragma and prints `clean`; signature-test-weakening holds for the two named suites because `plugins/hexaemeron/tests/test_issue_429_recovery.py` and `plugins/hexaemeron/tests/test_hexctl.py` are untouched across the whole run diff from `f5d94a5f` to `0788c09a` and still run 9 and 378 cases green, and the weakening found this round was in the new signature assertion rather than in theirs; measurement-honesty holds because step 2 changes no byte of either study copy, so section 10's recorded figures and its inside-variance statement stand exactly as step 1's round reviewed them. The audit added two cases, taking the root suite from 478 to 480, and no existing case was lost or renamed. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and deferred scope, both findings with their severities and fixed status, the `passed` verdict and how it was determined, every commit, key, hash, path, count, byte size, exit code, permission mode and assertion string, all five qualifications and the negative results for the channels that did not defeat the declaration, without changing any claim.

## Step 2, round 2 -- 2026-08-28T21:33:17Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The `extensions.worktreeConfig` variant was not exercised: a linked worktree was shown to share the config it was added from under the default layout only, and a repository that enables per-worktree config could behave differently. Bare and mirror clones were not exercised, only a plain `git clone` of a working repository. The covered-suite enumeration still asserts nothing because `COVERED_SUITES` is the empty tuple, so no suite was run under the hostile configuration this round and the enumeration path was read rather than driven with an entry in place. Sibling plugin suites were not re-run beyond `tests.test_issue_429_recovery`; only the root suite was executed in full. The runbook's step 3 to step 5 exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | medium | tests/test_disposable_fixture_signing.py | The guard built every disposable repository with `git init`, so the register's clone-and-worktree-paths risk rested on assumption while the two other construction verbs behave in opposite ways. Measured against git 2.54.0 under the hostile configuration: a clone of a repository that declares `commit.gpgsign false` carries no local `commit.gpgsign` at all, reads `true` from the hostile global, and its commit fails at 128 with 61 bytes in the sentinel, so a clone inherits nothing and is its own construction site; a linked worktree added from the same repository reads `false`, commits at 0 and leaves the sentinel empty, so it inherits and needs no second declaration. Both later steps already depend on that asymmetry with nothing testing it. Step 4's Files field names the clone site at `plugins/hexaemeron/tests/test_kronos_scoreboard.py` lines 664 and 759, which is right because a clone must declare for itself, and step 3's Files field names `plugins/horos/tests/test_universe.py` at lines 56, 134 and 195 while omitting the `git worktree add` at line 247, which is also right because that worktree is added from the repository `UniverseTests.setUp` creates at line 56 and inherits its declaration. Neither judgement was checkable from the suite. | fixed in `57898486e8d2a3a7e6b575f44d3655dc3f4a9742`; `CreationVerb` asserts both behaviours, the clone leg proving that a clone reaches the signer undeclared and then commits clean once it declares for itself with no signature header on the object, and the worktree leg proving that a linked worktree of a declared repository reads `false`, commits at 0, leaves the sentinel empty and carries no signature header; non-vacuity by mutation, where declaring the rule in the clone at creation fires the must-fail leg with `0 == 0` and building the worktree's source undeclared fires the precondition with `128 != 0`; the module docstring's existing clause "whichever verb created it" is now proven rather than asserted |
| S2-R2-02 | low | tests/test_disposable_fixture_signing.py | `hostile` created the arm's directory with `directory.mkdir()` outside its own `try`, so an unwritable or full TMPDIR raised a bare `PermissionError` from `mkdir` rather than the diagnostic the module promises, its docstring saying that a missing git or an unwritable configuration "fails and names the precondition that was missing". Driving all six precondition branches rather than reading them, five failed with the diagnostic and named what was missing -- `write_arm` raising `OSError`, `write_arm` raising `ValueError`, a git subcommand exiting non-zero, `ssh-keygen` absent, and `ssh-keygen` exiting non-zero -- while the sixth, an unwritable workspace, produced `PermissionError: [Errno 13] Permission denied` with no diagnostic. No branch ever skipped or passed, so the register's skip-instead-of-fail property held throughout and this is a contract and diagnostic gap rather than a correctness hole, which is why it is low. | fixed in `8343783f43e71b6c0d6c847283347058d9837fdb`; creating the directory is part of writing the configuration, so `directory.mkdir()` moved inside the `try` that already catches `OSError` and `ValueError`; re-driving the same branch now reports FAIL with `this guard fails rather than skips: the hostile openpgp configuration could not be written ([Errno 13] Permission denied: ...)`, so all six branches now name their precondition |

Leads not pursued: the round reviewed the tree as round 1's fixes left it, at signed tip `f806aae5595d0fb8c003fe97b5500ed9b18a4a89`, and both new commits carry a good SSH signature for dave@wildcat.finance under key SHA256:VJg6PMUeK3adligE1q1AnhR3D3sbOMFeF5VZbmOTCNk, exactly one `Co-authored-by: Shoggoth` and one `Wildcat-Origin: shoggoth` trailer each, and no runtime-host byline. The Elenchus verdict of `passed` was determined with the step's own runner contract, `/Users/kethcode/.local/bin/python3.14 tests/run_tests.py --elenchus-report {report}` at format `unittest-json-v1` and report file `.elenchus/fiat-621-step-2.json`, run against both fix commits and returning `passed` each time with the detail `the runner report records that the guard passed on the parent` and reports of 482 and 480 executed tests at zero assertion failures, errors and skips. The mechanism is the same one round 1 recorded and no relabelling is claimed: the classifier copies a commit's changed test files onto a detached parent worktree, so where a change lies entirely under `tests/` the fix travels with the guard and the parent cannot fail; the guard evidence is the mutations recorded above instead. All six amended Exit commands, the emit call and the three generator `test` checks exit 0 at the round-2 tip; phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7 and from the repository copies; both spec copies remain byte-identical to the receipted artefacts under `cmp`; the report declares `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips, and `.elenchus/` was removed leaving the tree clean. The empty covered-suite enumeration was judged rather than assumed and is recorded as acceptable for step 2 without a finding. `CoveredSuites` passes today by iterating an empty tuple, which is the vacuity family, but a guard that failed on an empty `COVERED_SUITES` would fail step 2's own exit, which the runbook requires green and whose Tests field states that this step registers no suite and that steps 3 and 4 add their entries; both later steps bind that in their Files fields, each naming `tests/test_disposable_fixture_signing.py` for enumeration entries, and the runbook's chosen mitigation, a module-level constant a reader can see without reading test bodies, is present and correct. The check belongs in the rounds for steps 3 and 4, which should assert the tuple is no longer empty and that each entry's representative test is one that actually commits; this round makes no change for it. One design limitation is recorded with it: the enumeration runs one representative test per covered suite, so it catches an unguarded construction site only when the chosen representative exercises that site, which makes the register's missed-construction-site phrase "caught by the suite rather than by a contributor" narrower than it reads, and the narrowing is the runbook's design rather than a defect in the code. Every clone and worktree construction site in the repository was surveyed and one candidate gap dissolved on inspection: `plugins/hexaemeron/tests/test_issue_429_recovery.py` clones at line 460 and is named by neither step 3's nor step 4's Files field, but it holds exactly one `git commit`, at line 492, which already carries `-c commit.gpgsign=false`, so the site is covered, in the per-invocation `git -c` style rather than the repository-local style this delivery standardises; that style works because `git -c` outranks the hostile global, which is the same precedence channel round 1's S2-R1-02 was about, and it is per-call, so a second commit added to that file without the flag would sign, which is an observation for step 4 rather than a defect now. The two apparent clone sites at `plugins/hexaemeron/tests/test_hexctl_currency.py` lines 456 and 518 are not construction sites at all: `clone` there is a directory name built with `os.path.join`, not a `git clone`. On the rest of the register at this tip: injection-precedence holds because round 1's drop stands and `InheritedPrecedence` is green in the 482-test run; vacuous-regression holds because both new legs were mutated and both fired; config-scope-escape holds because all 22 git calls in the module route through `self.git`, which builds `[GIT, "-C", str(root), *arguments]` against a root inside the case's own `tempfile.TemporaryDirectory`, and the new `clone` call names its source and destination explicitly as absolute paths so nothing falls through to the outer checkout; contributor-config-mutation holds because `~/.gitconfig` at SHA-256 15cb85c8b86f7528cded24b504615ac33a9890b62c58021177f15e2d06b443a0 and the shared local config at SHA-256 ef69159f5c509474baeab95738614af97b9cf99d5feaf63c60ca0759180fb470 are unchanged and `git status --porcelain` is empty; signature-test-weakening holds because the whole run diff from `f5d94a5f` to this tip touches neither named suite and `tests.test_issue_429_recovery` still runs 9 cases green; format-coverage holds unchanged with all three arms present, the new arm using openpgp; sentinel-leak holds because the TMPDIR listing is byte-identical across a guard run and the clone, worktree and signer artefacts all live inside the case's own temporary directory; subprocess-argv holds because the module has no `shell=`, `os.system`, `os.popen` or `check_output` at all, its four `subprocess.run` sites use fixed argv bounded at 60 or 300 seconds, and phylax asks for no pragma; measurement-honesty holds because step 2 still changes no byte of either study copy. The guard now runs 12 cases and the root suite 482, up from 480, with no existing case lost or renamed. Two conditions were not driven and are named above rather than claimed: `extensions.worktreeConfig` and bare or mirror clones. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and undriven scope, both findings with their severities and fixed status, the `passed` verdict and how it was determined, every commit, key, hash, path, line number, count, exit code and assertion string, the judgement recorded without a finding, the dissolved lead and the design limitation, without changing any claim.

## Step 2, round 3 -- 2026-08-28T21:43:26Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The covered-suite enumeration still asserts nothing because `COVERED_SUITES` is the empty tuple, so no suite ran through `CoveredSuites` itself; the two suites exercised under the hostile configuration this round were driven directly as subprocesses, which is the same mechanism but not the same code path. Only two of the repository's suites were run under the hostile configuration, `tests.test_issue_429_recovery` and `tests.test_kronos_scoreboard`, chosen because they hold the construction sites this round had to settle; the remaining plugin suites were not surveyed that way and no claim is made about them. Sibling plugin suites were not re-run for regression beyond those two; only the root suite was executed in full. Signing behaviour was measured on this ssh-signing host under git 2.54.0 only, and no second host or git version was exercised. The runbook's step 3 to step 5 exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: no defect was found at signed tip `db5bff2d4ede78921cc6fec153a3e9c3a3c43ebd`, and this round made no fix, so there is no Elenchus verdict to report. All six amended Exit commands, the emit call and the three generator `test` checks exit 0; phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7 and from the repository copies; both spec copies remain byte-identical to the receipted artefacts under `cmp`; the report declares `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips, and `.elenchus/` was removed leaving the tree clean. The two conditions round 2 named as undriven were driven and neither can reach a fixture site in this repository. A bare repository cannot be a commit site at all: `git commit` in one built by `init --bare`, by `clone --bare` or by `clone --mirror` fails at 128 with `fatal: this operation must be run in a work tree` and a sentinel of 0 bytes, refusing before any signer is consulted, and all three report `core.bare=true`. The one path that turns a bare repository into a commit site is adding a worktree to it, and that worktree inherits the bare repository's declaration, reading `false` and committing at 0 with an empty sentinel, so it is already covered by the inheritance `CreationVerb` pins. The repository contains no bare or mirror clone in any case: the four bare repositories are all `init --bare`, at `plugins/hexaemeron/skills/kronos/scripts/kronos.py` line 499 and at `plugins/hexaemeron/tests/test_kronos_scoreboard.py` lines 642, 834 and 910, and the kronos one is fetched into and then removed with `shutil.rmtree`, never committed to. `extensions.worktreeConfig` cannot reach a fixture either. Enabling it alone does not defeat the declaration: a worktree of a repository that declares the rule still reads `false` and commits at 0 with an empty sentinel, because the extension only adds a per-worktree file and leaves `commit.gpgsign` in the shared config. The declaration is defeated only when something explicitly writes the key into worktree scope, which was confirmed by running `git config --worktree commit.gpgsign true` and watching the same worktree read `true` and fail at 128 with 61 bytes recorded. Nothing in this repository does that: `git grep` over every tracked file outside `audit/` finds zero occurrences of `worktreeConfig`, of `config --worktree`, of `"--worktree"`, of `sparse-checkout`, of `sparseCheckout` and of `core.bare`, so the extension is never enabled, no worktree-scoped key is ever written, and the `sparse-checkout` path that would enable it implicitly is never taken. Both conditions are therefore closed as unreachable rather than as untested. All four fixes from rounds 1 and 2 were re-verified by mutation against this tip rather than against the tree as it stood when they were written, with both files restored byte-identically afterwards and `git status --porcelain` empty. Reverting the `GIT_CONFIG_PARAMETERS` entry from `REPOINTING_VARIABLES` fails `InheritedPrecedence` with `128 != 0`; making the declared fixture sign with a throwaway ed25519 key fails `LocalDeclaration` with `Lists differ: ['gpgsig'] != []`, which is the object read rather than `%G?` and confirms the discriminating half is still the one that fires; declaring the rule in the clone at creation fails the clone leg with `0 == 0`; building the worktree leg's source undeclared fails its precondition with `128 != 0`; and driving an unwritable workspace fails with the precondition named. All six precondition branches were driven again at this tip and all six now report FAIL with the diagnostic, where before round 2's fix one of them errored with a bare `PermissionError`. The `test_issue_429_recovery.py` observation carried forward from round 2 was settled empirically rather than by reading, and it belongs to step 4 rather than here, because step 2 changes no fixture under test. Running that suite as a subprocess under the hostile configuration, which is what the enumeration will do, exits 1 with 139 bytes in the sentinel, and classifying those bytes shows exactly one invocation and it is `--keyid-format=long --status-fd=1 --verify`, with zero `-bsau` signing invocations. So the clone at line 460 signs nothing: its single `git commit` at line 492 already carries `-c commit.gpgsign=false`, and the controller subprocesses that `bootstrap_run` drives through `hexctl init`, `done study` and `done runbook` make no signing attempt either, which round 2 asserted from reading the test's own `git commit` calls and which is now measured across everything the suite spawns. Round 2's conclusion therefore stands, on stronger evidence than round 2 had, and the reasoning it recorded was narrower than the site. One new caveat came out of the same measurement and is left as a lead for steps 3 and 4 rather than as a finding here. The suite reaches the hostile signer by *verifying* a signature, not by making one: `test_composition_has_exact_parent_order_and_signed_header` runs `git verify-commit 0fb3bcfba14a36c623f380105504d41d1eb66c86` at line 154, the hostile `gpg.program` intercepts the verification, and the call exits 1. The enumeration's contract, which the step's Tests field states as asserting exit 0 and an empty sentinel, therefore conflates signing a fixture with verifying a signature, and a suite that legitimately verifies can never satisfy it however correctly its fixtures are declared. This is not a defect in what step 2 ships, because `COVERED_SUITES` is empty and the assertion is the runbook's stated design faithfully implemented; it becomes a live problem only if step 3 or step 4 registers a verifying suite or picks a verifying test as a representative. Two things soften it: the failure would be loud rather than silent, and the guard's message already prints the sentinel contents, so a reader would see `--verify` and diagnose it immediately. The recommendation for those steps is to choose a representative that commits rather than verifies, or to distinguish signing invocations from verification ones if a verifying suite has to be covered. The contrast is instructive and was measured the same way: `tests.test_kronos_scoreboard` under the hostile configuration exits 1 with 915 bytes recorded and its invocations are `--status-fd=2 -bsau 0123456789ABCDEF`, which is real fixture signing, so step 4's Files field naming lines 664 and 759 of that file is both correct and necessary. A related mechanism was measured and is recorded because it bears on which sites need the rule at all: `git commit-tree` does not honour `commit.gpgsign`, producing an unsigned object at exit 0 with an empty sentinel and no `gpgsig` header, while porcelain `git commit` in the same repository under the same configuration fails at 128 with 61 bytes recorded, so tool code that builds commits with plumbing is not a signing site. On the register at this tip: injection-precedence and vacuous-regression hold because every fix mutation above fired; config-scope-escape holds because the clone and worktree calls stay inside the case's own temporary directory and the outer checkout's worktree count is 5 before and after a guard run, so no worktree registration leaks into it; contributor-config-mutation holds because `~/.gitconfig` at SHA-256 15cb85c8b86f7528cded24b504615ac33a9890b62c58021177f15e2d06b443a0 and the shared local config at SHA-256 ef69159f5c509474baeab95738614af97b9cf99d5feaf63c60ca0759180fb470 are unchanged across a guard run and `git status --porcelain` is empty; signature-test-weakening holds because the two named suites are untouched by the whole run diff and `test_issue_429_recovery` was additionally shown to attempt no signing at all; format-coverage holds unchanged with all three arms present; missed-construction-site holds for what step 2 ships, with the enumeration caveat above recorded against steps 3 and 4; clone-and-worktree-paths holds with both verbs asserted and both remaining variants shown unreachable; skip-instead-of-fail holds with all six branches driven and all six naming their precondition; sentinel-leak holds because the TMPDIR listing is byte-identical across a guard run and no `sentinel.log`, `hostile-signer` or `hostile.gitconfig` exists anywhere inside the repository; subprocess-argv holds because phylax prints `clean` and the module still has no `shell=`, `os.system`, `os.popen` or `check_output`; measurement-honesty holds because step 2 changes no byte of either study copy. The guard runs 12 cases and the root suite 482, unchanged from round 2 since this round adds no test. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unsurveyed scope, the zero-finding row, the null verdict, every commit, hash, path, line number, count, exit code, byte size and assertion string, the two conditions closed as unreachable, the correction to round 2's reasoning, and the enumeration caveat left as a lead, without changing any claim.

## Step 3, round 1 -- 2026-08-28T22:42:22Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. Only the two registered suites were run under the hostile configuration; the remaining plugin suites were not surveyed that way and no claim is made about whether their fixtures sign, which is step 4's subject. The enumeration was exercised on this host only, where `git` and `ssh-keygen` are both present, so the interaction between a covered suite's own `@unittest.skipIf(GIT is None)` guard and the enumeration was reasoned about rather than driven by removing git. Signing behaviour was measured under git 2.54.0 on this ssh-signing host only. The corrected artefacts were checked for the two errors this round raised and re-run through every document gate, but their remaining prose was not re-audited line by line against the tree. The `.horos/boundary.json` candidate note recorded below was observed and left alone, and no boundary file was regenerated. Step 4's and step 5's exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | docs/disposable-fixture-signing/study.md | The shipped study misattributed the linked worktree's source repository, and the runbook's fourth amendment inherited the error. The study's section 2 note said `plugins/horos/tests/test_universe.py:247` "adds a linked worktree, which shares the fixture repository's config file, so site 8's entry covers it", and the amendment repeated it as "a `worktree add` from the repository line 56 creates". Site 8 is `test_universe.py:56`, `UniverseTests`. Parsed on the parent tree, `UniverseTests` ends well above the worktree and line 247 falls inside `CandidateBindingTests`, lines 180 to 259, whose `git init` is at line 195 and is site 10. So the worktree is governed by site 10's declaration, not site 8's. The conclusion both documents drew was still correct, because `CandidateBindingTests` declares too and no seventh declaration is needed, but the recorded reason named a site that does not govern it, and site 10 is load-bearing rather than redundant: its `setUp` commits at `git commit -q -m "seed"`, so a reader who trusted the study and removed site 10's declaration would reintroduce signing at a site the study states is covered. The step's own commit message had it right, saying "which is the one at :195". | fixed in `ef150a7595d6ef0d578fc577fe2173e2cd85dd55`; the controller amended the canonical study to name site 10 at `:195` with the measured class spans, `UniverseTests` 51 to 120, `BindingDirectoryTests` 121 to 179 and `CandidateBindingTests` 180 to 259, and to record that site 10 is load-bearing, and amended the runbook so its worktree sentence names `CandidateBindingTests` and site 10 and says the fourth amendment inherited the error from the study; this round refreshed both shipped copies onto those artefacts, which was the remedy this audit named and which could not be applied from the step branch without breaking a byte-identity exit |
| S3-R1-02 | low | docs/disposable-fixture-signing/runbook.md | The runbook's fourth amendment miscounted the Horos suite's construction sites, saying the comment sits at "the first of its three sites" where the suite has five, recorded by the study's own section 2b table as rows 6 to 10: `test_scoped_entry.py:64`, `test_demonstration.py:53`, and `test_universe.py` at 56, 134 and 195, spread across three modules. Read as three modules the sentence was satisfiable and Mason satisfied it; read as the suite's sites it was not, and the two readings point at different modules, because the comment sits in `test_universe.py`, the last of the three modules in discovery order rather than the first. This is the same defect class the fourth amendment was itself written to fix, its stated reason being that "a shipped runbook that miscounts its own work is a defect in the document". | fixed in `ef150a7595d6ef0d578fc577fe2173e2cd85dd55`; the controller replaced step 3's `Files` so the sentence no longer counts three sites, saying instead that the comment sits in `test_universe.py`, the module carrying three of the suite's five sites, and naming the two paths a reader takes from a bare declaration to the reason, `git blame` on the line and the guard module's opening docstring, which are the two this round measured; this round refreshed the shipped copy onto that artefact |

Leads not pursued: the code this step delivered is clean and no finding was raised against it; both findings were in the receipted documents and both are now fixed by refreshing the shipped copies onto the corrected artefacts. Both copies are byte-identical to the canonical files, `cmp` exiting 0 for each, at SHA-256 20a2bc0b386ffdfaeea45d5b936fa8f57f8f4542a633ff70560d4f6ab663c955 for the study and 2f4f5c4b651e6338a8c1b1161d8de06be2b684be33f7d5ef10d0c1d501b23277 for the runbook. Every document gate passes on the refreshed copies: protasis prints `clean` for the study under `--study` and for the runbook, imprimatur exits 0 scoring both at 99.2 out of 100, brevitas exits 0 in one call per document, and hypomnema prints `clean`. The Elenchus verdict of `unguarded` was measured rather than assumed, with step 3's own runner contract at format `unittest-json-v1` and report file `.elenchus/fiat-621-step-3.json`, and it is the mechanically correct answer for this fix rather than a defect: the classifier returned the detail `the commit changed no test files` with an empty changed-test list and no report, because the fix commit touches only the two documents under `docs/disposable-fixture-signing/`. That is the branch at `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:281` which step 2's audit identified, and it is the same branch the third amendment cites to rule `unguarded` out for step 3's own code change; the two are consistent, because that change touches five test files and this one touches none. Run against the step commit `48949fa8ddc1045ead14c3bbeb138a8cd5992b52` for contrast, the classifier returns exactly `passed` with the detail `the runner report records that the guard passed on the parent`, which is the value the third amendment predicted and asked to have recorded, and it is recorded here. The step's code was audited before the documents were corrected and every check held. All six declarations are `git config --local commit.gpgsign false` placed immediately after the `git init` that created the repository and before any other configuration, at `tests/test_boundary_currency.py:169`, `plugins/horos/tests/test_demonstration.py:54`, `plugins/horos/tests/test_scoped_entry.py:65` and `plugins/horos/tests/test_universe.py` at 59, 138 and 200, and a repository-wide grep finds exactly six fixture declarations and no seventh. `config-scope-escape`, which the step's disciplines field names as the live register entry, holds for all six: each module's `git(root, *args)` helper builds `["git", "-C", root, *args]`, every call passes `self.root` from a `tempfile.TemporaryDirectory`, and no call can fall through to the outer checkout; the root module additionally routes through `git_env()`, whose `GIT_ENV_TO_DROP` tuple removes the nine repointing variables and notably does not drop `GIT_CONFIG_GLOBAL`, so the hostile configuration does reach the fixture and that entry is not vacuous on that account. The check deferred from step 2's round 2 was made and both halves pass. `COVERED_SUITES` now holds two entries and each names a representative that reaches a real `git commit` twice, the root suite's `GuardMutationTests.test_a_new_generated_file_without_a_refresh_is_named` committing "tracked tree" in `setUp` and "generated file" in the body, and the Horos suite's `UniverseTests.test_an_aggregated_directory_counts_only_universe_files` committing "seed" in `setUp` and "vendor" in the body. Neither is a verifying test: `verify-commit`, `verify_commit`, `%G?` and `--verify` appear nowhere in `tests/test_boundary_currency.py` or in any module under `plugins/horos/tests/`, so the caveat step 2's third round raised against a verifying suite does not apply to either registered suite and the third amendment's representative-selection sentence is satisfied. The enumeration genuinely fails when a site regresses, measured by deleting one declaration at a time and reverting byte-identically: removing `tests/test_boundary_currency.py:169` fails `CoveredSuites` with `1 != 0 : root suite does not survive inherited signing`, and removing `plugins/horos/tests/test_universe.py:59` fails it with `1 != 0 : Horos suite does not survive inherited signing`; run alone under the hostile configuration each regressed representative records 61 bytes whose invocation is `--status-fd=2 -bsau 0123456789ABCDEF`, which is signing rather than verification. The two forbidden sites are untouched and neither commits: `tests/test_boundary_currency.py:140` runs `git init -q`, `write` and `git add .` inside a `GIT_INDEX_FILE` isolation block, and `plugins/horos/tests/test_scoped_entry.py`, now line 141 after the edit above it, runs `git init -q` against a symlink-refusal fixture; neither received a declaration. The `worktree add`, now line 252, carries no declaration and the test containing it makes no commit of its own. The acceptance evidence the amended exit names was reproduced after the fix: both suites exit 0 under the hostile configuration with the sentinel at 0 bytes, all five commands of the first exit block exit 0, the root suite runs 482 tests and the Horos suite 228, the counts they reported at step 2's exit, so the enumeration added no test because it iterates as subTests inside the existing `CoveredSuites` method, and the report declares `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips before `.elenchus/` was removed. `horos check .` exits 0 reporting `boundary matches the tree`, and phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7. Three things are recorded without raising a finding. The step's commit message reports the pre-step root suite at "3 errors and 2 failures"; measured in a detached worktree at the parent `00b1de176596a0d1b0759dfa900212334be1c272` under the hostile configuration the root suite gives `FAILED (errors=3)` with 183 bytes and 3 signing invocations and the Horos suite `FAILED (errors=42)` with 2562 bytes and 42, so the Horos figure matches exactly and the root figure is 3 errors and zero failures. The two failures were located rather than guessed: this step's tree with the enumeration registered but all six declarations removed reproduces `FAILED (failures=2, errors=3)` exactly, because `CoveredSuites` contributes one failing subTest per registered suite and contributes none at the parent, where the tuple is empty. The number is real and reproducible and the baseline it names is the enumeration-registered-but-undeclared intermediate rather than the pre-step tree; repairing the sentence would mean rewriting a receipted step commit the controller verified, so it was left alone. Second, `horos check` now prints `candidate drift: audit/rounds/fiat-621-isolate-disposable-fixture-signing.synopsis.md: evidenced by the tree but missing from the boundary` before its `boundary matches the tree` verdict; the note is absent at step 2's step tip `3037787a` and appears first after this auditor's own round-1 record commit, it is a candidate rather than drift so `horos check` still exits 0 and step 1's condition that `.horos/boundary.json` change only if the check reports drift is not triggered, and the boundary lists no `audit/` entry at all while 22 synopsis files exist on disk, so the note reflects this file's growth rather than a repository-wide gap. Integration should settle whether generated audit synopses belong in the boundary, and this is carried forward for the run-level pull request rather than acted on here. Third, imprimatur reports one high `structural_metaphor: 'load-bearing'` in each corrected document, at 690:36 in the study and 459:25 in the runbook, both inside receipted amendment text that cannot be edited without breaking the byte identity this round exists to restore, and imprimatur still exits 0 at 99.2 for both. One procedural note, for a reader comparing this record against the round's history: an earlier draft of this record was committed locally as `100b736c`, stating both findings unfixed and the verdict `null`, which is what was true before the controller amended the artefacts; it was never pushed and never receipted, and because this round then fixed both findings it would have recorded something false, so the audit branch was reset to the step tip and the record rewritten from the same measurements. The 44955 bytes of step 1 and step 2 records were an exact prefix throughout. On the rest of the register: injection-precedence holds because the enumeration supplies its hostile configuration through `GIT_CONFIG_GLOBAL` from `harness.child_environment` and the `GIT_CONFIG_COUNT` triple appears nowhere in the added code; vacuous-regression holds by the two site-removal mutations above; contributor-config-mutation holds because `~/.gitconfig` and the shared local config at `/Users/kethcode/wildcat/skills/.git/config` are byte-identical before and after both suites run and `git status --porcelain` is empty; signature-test-weakening holds because the whole step diff touches neither `test_issue_429_recovery.py` nor `test_hexctl.py`; format-coverage holds unchanged with all three arms present in the harness; missed-construction-site is materially improved, since the enumeration that asserted nothing at step 2 now covers two suites and was shown to fail when either regresses; skip-instead-of-fail holds, and the one interaction worth naming is benign, that the Horos and boundary modules carry `@unittest.skipIf(GIT is None)` decorators which cannot make the enumeration pass vacuously, because `CoveredSuites` inherits `HostileCase.setUp`, which fails rather than skips when `git` is absent and would fail before any representative subprocess ran; sentinel-leak holds because the TMPDIR listing is byte-identical across both suite runs, the outer checkout's worktree count is 5 before and after, and no `sentinel.log`, `hostile-signer` or `hostile.gitconfig` exists anywhere inside the repository; subprocess-argv holds because every added call goes through a helper with fixed argv, no shell, and the existing `phylax: allow subprocess` pragma with its reason; measurement-honesty holds because the step changes no byte of either study copy beyond this round's refresh onto the corrected artefact, and the one measurement discrepancy above is recorded rather than smoothed. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and undriven scope, both findings with their severities and fixed status, the measured `unguarded` verdict together with the contrasting `passed` on the step commit, every commit, digest, path, line number, count, exit code, byte size and assertion string, the three qualifications, the procedural note and the carried-forward boundary question, without changing any claim.

## Step 3, round 2 -- 2026-08-28T22:53:41Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The two corrected passages and the claims they make were re-derived from the tree, but the rest of the study's and runbook's prose was not re-audited line by line, so no claim is made about statements this audit has not raised. Only the two registered suites were run under the hostile configuration; the remaining plugin suites are step 4's subject. Signing behaviour was measured under git 2.54.0 on this ssh-signing host only. The `.horos/boundary.json` question carried below was observed and left alone, and no boundary file was regenerated. Step 4's and step 5's exits, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: no defect was found at signed tip `14bd49b4f05da964ae053457f737d2231135bc99`, and this round made no fix, so there is no Elenchus verdict to report. The corrected statements were re-derived from the tree rather than read, and the load-bearing one is exactly right. Parsing `plugins/horos/tests/test_universe.py` on the parent tree `00b1de17` gives `UniverseTests` a `class` keyword at 51 and a last statement at 114, `BindingDirectoryTests` 121 and 176, and `CandidateBindingTests` 180 and 259, with the `worktree add` at 247; so `CandidateBindingTests` spans 180 to 259 exactly as both amendments state, and 247 sits inside it, which is the claim the correction exists to make and it holds under every reading. Two supporting spans are recorded as a qualification rather than a finding, because the amendments mix two conventions and neither one makes all three simultaneously exact. Measured from the class keyword to the last statement the spans are 51 to 114, 121 to 176 and 180 to 259, which matches the third figure and not the first two; measured from the class keyword to the line before the next class keyword they are 51 to 120, 121 to 179 and 180 to 263, which matches the first two and not the third. The amendments state 51 to 120, 121 to 179 and 180 to 259, taking the first two from the second convention and the third from the first. Under the wider reading the extra lines are not the class's own: at the parent, lines 115 and 116 are blank, 117 is the module-level constant `MINIFIED = "var a=1;" * 400 + "\n"`, 118 and 119 are blank, and 120 is `@unittest.skipIf(GIT is None, "git unavailable")`, the decorator belonging to `BindingDirectoryTests`, with 177 and 178 blank and 179 the same decorator for `CandidateBindingTests`. The imprecision is therefore at most six lines at one boundary and three at another, it never touches the line the argument turns on, and the runbook's separate sentence that `UniverseTests` "ends at line 120" is the same convention. No finding is raised on it, because the conclusion is correct under both conventions, the decisive span is exact, and the harm this audit named in round 1, a reader treating site 10 as redundant with site 8 and removing it, is closed either way. Every number in both amendments is in parent-tree coordinates, which is consistent with the whole document family: sites are cited as `:56`, `:134`, `:195`, `:140`, `:166` and the worktree as `:247` throughout, all pre-step. Step 3's own edits shifted the file, so at this tip `UniverseTests` runs 51 to 117 declaring at 59, `BindingDirectoryTests` 124 to 180 declaring at 138, `CandidateBindingTests` 184 to 264 declaring at 200, and the `worktree add` is at 252; a reader checking any cited line against the current tree finds it three to five lines low. That is systemic to documents written before the step they direct rather than introduced by this correction, and the study's amendment scopes its measurement honestly by saying the spans were measured on the parent tree, so it is recorded here as a qualification for a later reader rather than as a defect. One structural observation is recorded without raising a finding. The study's section 2 coverage note still reads, at line 272 of the shipped copy, that the worktree at `:247` "shares the fixture repository's config file, so site 8's entry covers it", which is the sentence round 1 found false; the correction lives in an amendment block at the end of the document and names the passage it supersedes, but nothing at section 2 marks it. The runbook has the identical property, its fourth amendment at line 793 still carrying "the repository line 56 creates" and the superseded "first of its three sites" phrasing, both replaced by the fifth amendment's `Complete replacement Files`. Append-only amendment blocks that supersede earlier text are this run's established convention, which the step 3 packet itself states by binding "the amended fields", and the runbook body is superseded the same way, so this is a property of the convention rather than a defect in the correction; it is noted because a reader who stops before the amendments reads the superseded claim in both documents. The other claims the fifth amendment makes were checked and hold: `test_universe.py` carries three declarations and the Horos suite carries five in total, so "the module carrying three of that suite's five sites" is exact, and the two reader paths it names from a bare declaration to the reason, `git blame` on the line and the opening docstring of `tests/test_disposable_fixture_signing.py`, are the two this audit measured in round 1. Both shipped copies remain byte-identical to the canonical artefacts at this tip rather than only at the moment they were copied, `cmp` exiting 0 for each, at SHA-256 20a2bc0b386ffdfaeea45d5b936fa8f57f8f4542a633ff70560d4f6ab663c955 for the study and 2f4f5c4b651e6338a8c1b1161d8de06be2b684be33f7d5ef10d0c1d501b23277 for the runbook, each matching its canonical file. Nothing regressed. All five commands of step 3's first exit block exit 0, the root suite runs 482 tests and the Horos suite 228, the counts they reported at step 2's exit; both suites exit 0 under the hostile configuration with the sentinel at 0 bytes; `horos check .` exits 0; phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7; and the report declares `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips before `.elenchus/` was removed. The enumeration still bites at this tip, re-measured by deleting one declaration at a time and reverting byte-identically: removing the root site fails `CoveredSuites` with `1 != 0 : root suite does not survive inherited signing`, and removing the Horos representative's site fails it with `1 != 0 : Horos suite does not survive inherited signing`. Six fixture declarations remain and no seventh. On the register at this tip: injection-precedence and vacuous-regression hold by those two mutations; config-scope-escape holds because all six calls route through a helper building `["git", "-C", root, *args]` against a `tempfile.TemporaryDirectory`; contributor-config-mutation holds because `~/.gitconfig` and the shared local config at `/Users/kethcode/wildcat/skills/.git/config` are byte-identical across both suite runs and `git status --porcelain` is empty; signature-test-weakening holds because the step diff touches neither `test_issue_429_recovery.py` nor `test_hexctl.py`; format-coverage holds unchanged; missed-construction-site holds with the enumeration covering both suites and failing when either regresses; skip-instead-of-fail holds, with the `@unittest.skipIf(GIT is None)` decorators in the covered modules unable to make the enumeration pass vacuously because `CoveredSuites` inherits `HostileCase.setUp`, which fails rather than skips when git is absent; sentinel-leak holds because the TMPDIR listing is byte-identical across both runs, the outer worktree count is 5 before and after, and no `sentinel.log`, `hostile-signer` or `hostile.gitconfig` exists inside the repository; subprocess-argv holds because phylax prints `clean` and every added call keeps fixed argv, no shell and its `phylax: allow subprocess` pragma; measurement-honesty holds because the two documents now say what the tree does on the point this audit raised. Three things stay open and are carried forward rather than closed here. `horos check` prints `candidate drift: audit/rounds/fiat-621-isolate-disposable-fixture-signing.synopsis.md: evidenced by the tree but missing from the boundary` before its `boundary matches the tree` verdict; it is a candidate rather than drift so the check still exits 0 and step 1's condition is untriggered, the boundary lists no `audit/` entry while 22 synopsis files exist on disk, and integration should settle whether generated audit synopses belong in the boundary. The step commit's "3 errors and 2 failures" names the enumeration-registered-but-undeclared intermediate rather than the parent, which gives 3 errors and zero failures, and repairing it would mean rewriting a receipted step commit, so it stays as recorded in round 1. And step 2's third round left a caveat for step 4, that `plugins/hexaemeron/tests/test_issue_429_recovery.py` reaches the hostile signer through `git verify-commit` rather than by signing, which the enumeration's contract of exit 0 and an empty sentinel cannot tell apart from a fixture signing itself; neither suite registered by this step has that property, so it remains step 4's to handle. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unaudited scope, the zero-finding row, the null verdict, every commit, digest, path, line number, span, count, exit code and assertion string, all three qualifications and the three carried-forward items, without changing any claim.

## Step 4, round 1 -- 2026-08-29T01:03:22Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The `-bsau` discriminator was measured against the emitted openpgp arm and against the ssh arm, but not against `gpg.x509.program`, which no arm of the harness writes. The exit's four assertions were driven against the delivered tree and against one regressed site, `test_kronos_scoreboard.py`; other regression shapes were not enumerated, and the error-count band was probed with literal summary strings rather than by manufacturing a run for each count. Suites outside the four covered ones were not re-run for regression beyond what the exit block executes. Signing behaviour was measured under git 2.54.0 on this ssh-signing host and node 26.6.0 only. Step 5's exit, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | low | docs/disposable-fixture-signing/runbook.md | The seventh amendment's error-count assertion did not mean what its prose said. The command was `grep -qE 'FAILED \(errors=1'` and the prose read "The one error is exactly that test and no other", but the pattern had no closing parenthesis, so it matched any count beginning with 1. Measured against literal unittest summary lines it accepted `FAILED (errors=1)`, `(errors=10)`, `(errors=13)`, `(errors=19)` and `(errors=100)`, and rejected `(errors=2)` and `(errors=9)`. The band was reachable rather than theoretical: removing both declarations from `plugins/hexaemeron/tests/test_kronos_scoreboard.py` makes that module alone report `FAILED (errors=15)` with 915 sentinel bytes and 15 signing invocations, which in the whole suite is 16 errors and was accepted. Runs carrying failures as well as errors were correctly rejected, because unittest prints `failures=` first. The signing claim itself was never weakened: assertion four, `! grep -q -- '-bsau'`, fails on exactly that regression, so nothing false could have been recorded about the rule. What went unchecked was the other half of the criterion, that the suite is otherwise green. | fixed by amendment rather than by a commit, because the remedy lives in the runbook and not in the tree; the controller closed the group as `FAILED \(errors=1\)` and recorded the kronos measurement as the reason. That closure overcorrected, which this round records separately as S4-R1-04 |
| S4-R1-02 | medium | docs/disposable-fixture-signing/runbook.md | The shipped runbook was left behind by this step's own amendments, twice, and a receipted exit criterion stopped passing as a result. Step 4 recorded three amendments before this round opened, the sixth replacing `Goal` and `Tests`, the seventh `Exit` and the eighth `Files`, and two more while it was open; the eighth's own `Files` list names six hand-edited paths and four regenerated ones and no shipped copy, and the step's diff touches nothing under `docs/`. At the step commit the shipped runbook held five amendment blocks against the canonical artefact's eight, was a strict prefix of it and 9288 bytes shorter, and `cmp .hexaemeron/runbook.md docs/disposable-fixture-signing/runbook.md`, which step 1's amended exit carries, reported `EOF on docs/disposable-fixture-signing/runbook.md` rather than exiting 0. Step 3's fifth amendment had set the pattern by naming both shipped copies in its `Files` field for exactly this reason, and step 4's omission is what let the copy drift. | fixed in `f8b3da37f66bc6ceec8c79d36f55e29fecb16738` and again in `d3369ff3e8cd252eceed919d777865edff5b8ddf` after two further amendments landed mid-round; the shipped runbook now carries nine amendment blocks and hashes to 6eace23593d902aea46a0401ade73afe6b24ad1c50872e7a4d3c7f385c7dedce, the study copy to 20a2bc0b386ffdfaeea45d5b936fa8f57f8f4542a633ff70560d4f6ab663c955, both `cmp` calls exit 0, protasis prints `clean` for each document, imprimatur, brevitas per document and hypomnema all exit 0, `horos check` reports `boundary matches the tree` and both currency guards pass; step 4's `Files` field still wants an amendment naming the shipped copies the way step 3's does, or the copy will drift a third time |
| S4-R1-03 | low | docs/disposable-fixture-signing/runbook.md | The sixth amendment's predicted Elenchus result did not hold, and the reason it gave is the reason it failed. It declared "Expected exact Elenchus result: `passed`" on the ground that "a test-only change carries its own fix and the parent cannot fail". Measured against the step commit `c1d299a0614cd43788ddca0de4e6919d2fecbc84` with step 4's own runner contract, the classifier returns `guarded`, with the detail `the runner report records a parent assertion failure` and four assertion failures. This step is not a test-only change: the eighth amendment records that it must also regenerate `.agents/skills/promise-machine/runtime/MANIFEST.json`, `.horos/boundary.json` and `.horos/candidates.json`, and the classifier copies only changed test files onto the parent. | fixed by amendment rather than by a commit, because the remedy lives in the runbook and not in the tree; the controller restated the expected value as `guarded` with the generated-file mechanism as its reason, and recorded that the guards which fire are currency guards rather than the signing guard |
| S4-R1-04 | medium | docs/disposable-fixture-signing/runbook.md | The closure of S4-R1-01 overcorrected, and made the exit criterion unsatisfiable on a correct tree. Adding the closing parenthesis gave `grep -qE 'FAILED \(errors=1\)'`, which requires the digit to be followed immediately by `)`. The hostile Hexaemeron run does not print that: the suite carries one skip, so unittest writes `FAILED (errors=1, skipped=1)` with the count followed by a comma. Measured against the captured log, the amended pattern does not match, so assertion two would have exited 1 against exactly the tree the step delivers, turning a criterion that was too weak into one that could never pass. The first pattern was too loose, accepting `errors=10`, `13`, `19` and `100`; the second was too strict, rejecting the real string; only a pattern that anchors the digit boundary without demanding the parenthesis satisfies both, and `FAILED \(errors=1[,)]` was measured matching the real line and rejecting `FAILED (errors=13, skipped=1)`. | fixed by amendment rather than by a commit, because the remedy lives in the runbook and not in the tree; the controller adopted `grep -qE 'FAILED \(errors=1[,)]'`, checked it against the real string before amending, and recorded in the artefact that this was the third revision of one assertion, first too loose, then too strict, and only the third verified against the string it has to match |

Leads not pursued: the code this step delivered is clean and no finding was raised against it; all four findings were in the receipted runbook, three of them remedied by amendment with no code diff behind them and one by the three refresh commits on this branch. The exit's own acceptance evidence was re-measured at the audit tip with the amended pattern and all four assertions now pass: assertion one names `Issue429RecoveryTests.test_composition_has_exact_parent_order_and_signed_header` at rc 0, assertion two at `grep -qE 'FAILED \(errors=1[,)]'` exits 0 against the live summary line `FAILED (errors=1, skipped=1)` from `Ran 1394 tests in 468.557s`, assertion three finds `--verify` in the sentinel at rc 0, and assertion four finds no `-bsau` at rc 0, with the sentinel holding 139 bytes, one verification and zero signing invocations. Both halves of that measurement were reproduced independently: the controller ran the same block against a fresh hostile directory at `b5c243fca2bbd84cbfc42b340763dc7ee28fa307` and recorded the same summary line, the same four exit codes at 0, hostile Hermes at 0, and a sentinel holding one line and that line a verification, with both `cmp` calls identical; the two runs differ only in the per-run git vtag temporary filename, `tmp0yxRDP` against `tmpl5wttv`, and in no measured value, so no discrepancy between them is recorded. Assertion two was measured passing rather than assumed fixed, which matters because the second revision of that pattern did not pass: run against the same live log, `FAILED \(errors=1` matches but accepts `errors=13`, `FAILED \(errors=1\)` does not match at all, and `FAILED \(errors=1[,)]` matches while rejecting `errors=13`. Hermes under the hostile configuration exits 0 with 0 bytes of its own in the sentinel. The `-bsau` argument the seventh amendment rests on was tested rather than accepted, and it holds for the arm these commands run, with two bounds now written into the exit. Under the emitted openpgp arm every signing form carries it: a commit with the signing key set records `--status-fd=2 -bsau 0123456789ABCDEF`, and so do `git tag -s`, `git merge -S` and a commit with an empty `user.signingkey`, which records `--status-fd=2 -bsau ` rather than dropping to `-bsa`; verification records `--keyid-format=long --status-fd=1 --verify`, and verifying an unsigned commit does not reach the signer at all. Nothing reaches it in a third form. Under the ssh arm signing is `-Y sign -n git -f ... -U ...` and carries no `-bsau`, so the discriminator does not hold there; it does not bite here because `--emit` writes the openpgp arm and a fixture that forces `gpg.format=ssh` locally reaches the real `ssh-keygen` rather than the sentinel, failing at 128 with the sentinel empty, and the emptiness criterion this replaced had the identical bound, so nothing was weakened by the change. The four sites the step declares were each confirmed at creation and correctly scoped: `plugins/hexaemeron/tests/test_elenchus_checker.py:115` immediately after `init --quiet -b main`, through a helper building `["git", "-C", str(self.path), *args]`; `plugins/hexaemeron/tests/test_kronos_scoreboard.py:667` immediately after `init`, and `:763` immediately after the `clone --branch kronos/state`, both through a helper passing `cwd`; and `plugins/hermes/skills/hermes/scripts/test_hermes.py:176` immediately after `init -q` with `cwd=self.repo`. The clone site is the one step 2's audit measured as inheriting nothing, and it is the site the Hexaemeron representative exercises. A repository-wide count finds ten declarations across the two steps and no eleventh. The two hardening edits are uniformity rather than repair: `plugins/hexaemeron/tests/test_hexctl.py:53` and `plugins/hexaemeron/tests/test_fiat_skill.py:1456` each move the existing call ahead of `user.email` and `user.name` and add the explicit `--local`, both remain before the commit that follows, the numstat for each file is one line added and one removed, and no test asserts on configuration order or on the absence of the scope flag. The exclusions hold, verified independently rather than taken on report: `git diff` over `plugins/hexaemeron/tests/test_issue_429_recovery.py` from the merge-base with main at `f5d94a5f27168e6c0faecd43eee426f6ee892cdc` to this tip is 0 bytes, and `plugins/hexaemeron/tests/test_hexctl.py` carries exactly one hunk across the whole branch, at line 50, so `:1366` and `:1388` still read `git("init", "-b", "main")` with no declaration added and the signature classes at 1809, 5850 and 5938 are untouched. `signature-test-weakening`, which this step's disciplines field names as the live entry, holds in both halves. The counts are unchanged: `test_hexctl` runs 378 tests, `TestCommitVerification`, `GitHubSignerDiagnosis` and `RewrittenStackRefusal` six each, the Hexaemeron suite 1394 as step 1's audit recorded, and the whole step diff adds or removes no `def test_` anywhere. The legs still fail when the code under test stops refusing: mutating `verify_local_commit` in `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` so its `bounded_tool_status(... ["verify-commit", commit_sha]) != 0` guard can never fire turns `TestPublicationBindings.test_sync_run_refuses_unsigned_commit`, `TestPublicationBindings.test_merge_time_repair_refuses_invalid_local_signature`, `GitHubSignerDiagnosis.test_a_github_signed_commit_is_refused_and_the_cause_is_named` and `TestCommitVerification.test_local_fake_git_negative_matrix_is_fail_closed_and_secret_safe` from four passes into four failures, and the file was restored byte-identically. Both representatives commit rather than verify, measured by removing each declaration and reverting byte-identically: the Hexaemeron representative `test_kronos_scoreboard.DurableHomeTest.test_extra_blobs_in_the_state_ref_are_ignored`, whose site is the clone at `:763`, goes from rc 0 with an empty sentinel to rc 1 with 61 bytes recording `--status-fd=2 -bsau 0123456789ABCDEF` and zero verifications, and the Hermes representative `test_hermes.HermesHarnessTests.test_rejects_any_gas_regression_at_gate_three` does the same from its site at `:176`. The four generated files are genuinely regenerated rather than hand-edited: the portable runtime mirror is byte-identical to its source under `cmp`, the MANIFEST entry for `plugins/hermes/skills/hermes/scripts/test_hermes.py` moves from 50788 to 51030 bytes with its sha256 becoming ecf885c2e27a346231f0ac0d0f44c84d47845ce13e1a7db535717bdc77b26a81, which matches the mirrored file exactly, `total_bytes` and the boundary's `bytes_generated` both move by the same 242, and re-running `scripts/portable_promise_machine.py sync` and then `plugins/horos/skills/horos/scripts/horos.py scan . --write` leaves the tree clean, which is what shows the committed outputs are what the generators produce and that the stated order held. `.horos/candidates.json` also gains the two `audit/rounds/fiat-621-*` entries, which closes the candidate-drift observation this auditor carried forward from step 3 by recording the log and its synopsis as candidates. All five commands of the clean exit block exit 0, with the Hexaemeron suite at 1394 of 1394 under node 26.6.0, Hermes ok, the root suite at 482, and the report declaring `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips before `.elenchus/` was removed. The Elenchus verdict of `guarded` was measured with step 4's own runner contract against the step commit `c1d299a0614cd43788ddca0de4e6919d2fecbc84`, returning the detail `the runner report records a parent assertion failure` with four assertion failures and seven changed test files copied onto the parent. The four that fire are `tests.test_boundary_currency.BoundaryCurrencyTests.test_the_committed_boundary_matches_a_fresh_scan`, `tests.test_skills_sh_package.SkillsShPackageTests.test_generated_runtime_is_current`, `tests.test_skills_sh_package.SkillsShPackageTests.test_manifest_binds_every_runtime_file_to_source_bytes` at `path='plugins/hermes/skills/hermes/scripts/test_hermes.py'`, and `tests.test_skills_sh_package.SkillsShPackageTests.test_selected_directory_works_as_an_isolated_copy`, reproduced by hand in a detached parent worktree with the seven files copied in. They are generated-file currency guards noticing that the copied source and its runtime mirror no longer match the un-copied MANIFEST and boundary, not the signing guard failing for want of the fix, so `guarded` here is a real red-to-green result but says nothing about the fixture rule, and no part of this record treats it as proof of that rule. The three refresh commits on this branch are documents only and each returns `unguarded` for the reason step 2's audit found at `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:281`, the detail `the commit changed no test files`. On the rest of the register: injection-precedence holds because the enumeration supplies its hostile configuration through `GIT_CONFIG_GLOBAL` and the `GIT_CONFIG_COUNT` triple appears nowhere in the added code; vacuous-regression holds by the two representative mutations and the kronos site removal; config-scope-escape holds because all four new calls and both hardened ones name their own fixture repository through `-C` or `cwd` against a temporary directory; contributor-config-mutation holds because `~/.gitconfig` and the shared local config at `/Users/kethcode/wildcat/skills/.git/config` are byte-identical across a full root-suite run and `git status --porcelain` is empty; format-coverage holds unchanged with all three arms present; missed-construction-site is now complete for the study's section 2b table, all ten sites declaring; clone-and-worktree-paths holds with the clone site declared at creation and exercised by the representative; skip-instead-of-fail holds with the one skip in the hostile Hexaemeron run being a pre-existing suite skip rather than a guard skipping; sentinel-leak holds for the Python guard, whose `tempfile` directories leave nothing behind, with the TMPDIR delta across a root-suite run being one unrelated `tmp` directory replaced by another and zero hostile artefacts under any `tmp`-prefixed path; subprocess-argv holds because phylax prints `clean` and every added call keeps fixed argv, no shell, and its `phylax: allow subprocess` pragma; measurement-honesty holds because this step changes no byte of either study copy beyond the refreshes onto the corrected artefacts. One observation is carried forward rather than closed, and it has grown since step 2's third round recorded it: every step's hostile exit block creates its directory with `mktemp -d` and never removes it, so each invocation leaves a mode 0700 signer and its configuration in TMPDIR permanently; fifteen such directories are present on this host now, none of them from the Python guard, and integration may want the blocks to clean up after themselves. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unenumerated scope, all four findings with their severities and their commit-or-amendment remedies, the `guarded` verdict with its four named currency guards and the statement that they are not the signing guard, every commit, digest, path, line number, count, exit code, byte size, argv form and assertion string, and the carried-forward TMPDIR observation, without changing any claim.

## Step 4, round 2 -- 2026-08-29T01:18:39Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The eight-minute hostile Hexaemeron run was not repeated: round 1 measured it twice independently and in agreement, and `git diff` from the step commit to this tip shows zero changes outside `docs/`, `audit/` and `.horos/`, so no code the suite exercises has moved. The clean Hexaemeron run under node 26.6.0 was likewise measured in round 1 at the same code state and not repeated. The three amendments were checked against the claims they make about this tree and against the measurements they cite; their remaining prose was not re-audited line by line. The TMPDIR residue was characterised and counted but no directory was removed, because removing another agent's temporary files is not this round's to do and the count is itself the evidence. Step 5's exit, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R2-01 | low | docs/disposable-fixture-signing/runbook.md | The hostile configuration the exit blocks generate is never removed, and the register entry that says otherwise is measurably wrong for that path. `sentinel-leak` reads "the sentinel file and signer script the hostile harness writes ... both live inside the test's own temporary directory and are removed with it". That holds for the guard, whose `tempfile.TemporaryDirectory` leaves nothing: measured again at this tip, the TMPDIR listing is byte-identical across a full run of `tests.test_disposable_fixture_signing`. It does not hold for the runbook's shell blocks, which write the same two files through the same `--emit` entry point. The runbook contains nine `HOSTILE_DIR="$(mktemp -d)"` sites, in the host-conditions preamble and in the exit of every step from 2 to 5 including four replacement exits carried by amendments, and it contains no `rm -rf "$HOSTILE_DIR"` and no `trap` at all, so every invocation of any of them leaks one directory permanently. Fifteen such directories are present on this host, none from the Python guard. What each holds is not clutter but a complete working hostile configuration: a mode 0700 `hostile-signer` beside a `hostile.gitconfig` naming it by absolute path, so the pair remains functional for as long as it sits there. Severity is low rather than higher because the signer is inert on its own, appending one line and exiting 1, is owner-only, and lives in the contributor's private TMPDIR, so it does nothing unless something is deliberately pointed at it. This round escalates rather than discovers: step 2's third round and step 4's first round each recorded the residue as an observation, and the growth from two directories to fifteen, together with step 5's exit not having run yet, is what makes it worth a finding now. | not fixed in this round; the remedy is an amendment adding cleanup to the exit blocks, either `rm -rf "$HOSTILE_DIR"` at the end of each or a `trap` beside each `mktemp -d`, which is the controller's to record, and no code change is called for because the guard already removes what it writes; fixing it before step 5 runs keeps the count from growing further, and this round's own `sentinel-leak` disposition is recorded as reviewed against the finding rather than against the guard alone |

Leads not pursued: the code this step delivered remains clean and the round's one finding is in the runbook rather than the tree, so no fix commit was made and there is no Elenchus verdict to report. The three amendments this step carries were checked against the tree rather than assumed correct, in the way step 3's corrections were checked, and none of them overstates what this audit measured. The `-bsau` bounds paragraph is exact on every clause. `--emit` writes the openpgp arm because `EMITTED_ARM = OPENPGP` and `main` calls `write_arm(directory, EMITTED_ARM)`; that is "the only one these commands run" in the strongest sense, because `--emit` accepts no arm argument at all, so no invocation of it can produce another. The emitted configuration contains zero occurrences of `ssh`, so no `gpg.ssh.program` is ever wired in. Every signing form named carries `-bsau`, re-measured: a commit records `--status-fd=2 -bsau 0123456789ABCDEF`, and so do `git tag -s` and `git merge -S`, while a commit with an empty `user.signingkey` records `--status-fd=2 -bsau ` rather than dropping to `-bsa`; verification records `--keyid-format=long --status-fd=1 --verify` and never `-bsau`, and verifying an unsigned commit does not reach the signer at all. The claim the controller asked to have scrutinised, that the ssh arm "is not reachable from these commands", is true as written and not overstated: the hostile ssh signer cannot be wired in because `--emit` cannot emit that arm, and no fixture in any of the four covered suites sets `gpg.format`, `gpg.ssh.program` or `gpg.program`, so nothing flips the format from inside. It is worth a reader knowing what the sentence does not say, which the paragraph's closing clause already supplies: ssh signing can still occur under the openpgp arm if a fixture declares `gpg.format=ssh` for itself, and it then reaches the real `ssh-keygen` rather than the sentinel, failing at 128 with the sentinel empty, which is why the paragraph records that the emptiness criterion it replaced had the identical bound. That is a bound on the criterion, not a hole the amendment opened. The four fixes from round 1 all hold at this tip. The operative error-count assertion is `grep -qE 'FAILED \(errors=1[,)]'`, and against round 1's live log it matches the real summary line, rejects `FAILED (errors=13, skipped=1)` and `FAILED (errors=10)`, and still matches a bare `FAILED (errors=1)`, so it is correct in all four directions where the first revision was too loose and the second too strict. The operative expected Elenchus result is `guarded`, with the mechanism written out correctly and with the instruction that the record should say the guards are not the signing guard, which round 1's record does. Both shipped copies are byte-identical to the canonical artefacts, `cmp` exiting 0 for each at 891910cef48702012ecc1c36704fa8693401b423bf0e0195d64647cff69813b9 for the runbook and 20a2bc0b386ffdfaeea45d5b936fa8f57f8f4542a633ff70560d4f6ab663c955 for the study, and step 4's `Files` field now names both copies, which is the change that stops the drift recurring a fourth time. The enumeration was exercised across all four registered suites rather than the two this step added, and it bites for every one: removing the declaration at `tests/test_boundary_currency.py:169` fails `CoveredSuites` with `1 != 0 : root suite does not survive inherited signing`, at `plugins/horos/tests/test_universe.py:59` with `Horos suite does not survive inherited signing`, at `plugins/hexaemeron/tests/test_kronos_scoreboard.py:763` with `Hexaemeron suite does not survive inherited signing`, and at `plugins/hermes/skills/hermes/scripts/test_hermes.py:176` with `Hermes suite does not survive inherited signing`, each reverted byte-identically, and the enumeration is green as shipped. Ten declarations remain across the two steps and no eleventh. One correction to this audit's own earlier work is recorded rather than left implicit. Every round from step 2 onward marked `sentinel-leak` as reviewed on the strength of the guard's `TemporaryDirectory` being removed, but the register entry's subject is "the sentinel file and signer script the hostile harness writes", which the harness writes by two paths and not one; those earlier dispositions were therefore scoped to the guard rather than to the entry as written, and this round marks the same id reviewed against both paths, with the shell path raised as S4-R2-01. No earlier record is altered; the correction lives here. On the rest of the register at this tip: injection-precedence holds because the enumeration supplies its configuration through `GIT_CONFIG_GLOBAL` and the `GIT_CONFIG_COUNT` triple appears nowhere in the added code; vacuous-regression holds by the four enumeration mutations above; config-scope-escape holds because all ten declarations and both hardened calls name their own fixture repository through `-C` or `cwd` against a temporary directory; contributor-config-mutation holds because `~/.gitconfig` and the shared local config at `/Users/kethcode/wildcat/skills/.git/config` are byte-identical across a full root-suite run and `git status --porcelain` is empty; signature-test-weakening holds because nothing outside `docs/`, `audit/` and `.horos/` has changed since round 1 verified the counts at 378 and 1394 and drove all four signature legs red under a mutated `verify_local_commit`; format-coverage holds unchanged with all three arms present in the harness; missed-construction-site is complete for the study's section 2b table with all ten sites declaring and all four suites enumerated; clone-and-worktree-paths holds with the clone site declared at creation and exercised by the Hexaemeron representative; skip-instead-of-fail holds, the one skip in the hostile Hexaemeron run being a pre-existing suite skip rather than a guard skipping; subprocess-argv holds because phylax prints `clean` and every added call keeps fixed argv, no shell and its pragma; measurement-honesty holds because this round changes no byte of either study copy and records the scoping correction above rather than smoothing it. All the fast exit commands pass at this tip, Hermes at 0, the root suite at 482, `horos check` reporting `boundary matches the tree`, phylax, ephoros and hypomnema each printing `clean` and exiting 0 from plugin cache 1.6.7, and the report declaring `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips before `.elenchus/` was removed. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unrepeated scope, the single finding with its severity and its amendment remedy, the null verdict, every commit, digest, path, line number, count, exit code, argv form and assertion string, the verification of all three amendments including the clause the controller asked to have scrutinised, and the correction to this audit's own earlier scoping, without changing any claim.

## Step 4, round 3 -- 2026-08-29T01:28:48Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The eight-minute hostile Hexaemeron run and the clean run under node 26.6.0 were not repeated: `git diff` from the step commit to this tip shows zero changes outside `docs/`, `audit/` and `.horos/`, so no code either exercises has moved since round 1 measured them, the hostile one twice and independently. The trap was measured in this shell and in three exit paths, but not under a signal such as SIGINT or SIGTERM, which `trap ... EXIT` does not cover on every shell. The leak sites that cannot be repaired were confirmed unrepairable by reading the amendment grammar and the step states rather than by attempting an amendment. Step 5's exit, the demo path and ADR-045 were read as context only and were not executed. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: no defect was found at signed tip `7af86284c3c16439dba4e6bc604aa21ece180240`, and the round raises nothing, so the phase closes from this side. The trap was measured rather than taken on report. Step 4's operative exit sets `trap 'rm -rf "$HOSTILE_DIR"' EXIT` at line 1277 of the canonical runbook, immediately after the `mktemp -d` at 1276 and before the `--emit` that writes into it, and replaying that shape in three exit paths gives the same answer each time: on a normal exit, on a failing assertion returning 1, and on an early `exit 3`, the signer is executable while the shell runs and the directory is gone once it exits. The limitation the amendment states is accurate in both halves. A trap set in one shell removes its own directory when that shell exits, and a directory created without one survives, which is exactly the case of a reader who runs the blocks in separate sessions and is what the amendment tells them to handle themselves. The claim that only step 4's exit and step 5's can be repaired also holds, and reading the amendment grammar sharpens it rather than contradicting it. Every amendment in this runbook replaces a named step field, declaring `Complete replacement Goal`, `Tests`, `Exit` or `Files` and naming the steps it touches, so there is no grammar by which the preamble at line 41 could be replaced; it is under `## Host conditions every command below assumes`, which is not a step. Steps 2 and 3 are complete and their exits have already been run and receipted, so amending them now would move a criterion after the fact. Counting only the fields that bind, rather than every `mktemp -d` in the file, four leak sites remain live: the preamble at 41, step 2's replacement exit at 665, step 3's base exit at 289, which no amendment ever replaced, and step 5's base exit at 450. Step 4's base exit at 374 and its three earlier replacement exits at 936, 1050 and 1156 are superseded and do not bind. Of the four that remain, three cannot be repaired for the reasons the amendment gives, and one can: step 5's exit has not been amended and step 5 has not run, so amending it before it does is the remaining opportunity to keep the count from growing, and that refinement is offered for the carried-forward list rather than raised as a finding, because the amendment claims only what it repaired and its `Steps touched` field says `Step 4's exit`. The cleanup was verified independently and is complete for what the finding named. No directory holding a `hostile-signer` remains at the depth any exit block writes; the count there was already zero before this round touched anything. Three directories one level deeper did survive the controller's sweep, at `tmp.B8z0Z9XBYn/h`, `tmp.B8z0Z9XBYn/hs` and `tmp.eBhFJ0xfe9/h`, and they are this auditor's own probe residue rather than the runbook's leak: the exit blocks write their three files directly into `$HOSTILE_DIR`, while step 2's argv probe and the clone-and-worktree probe used a nested `$W/h` and `$W/hs` layout of the audit's own devising. They have been removed, and no `hostile-signer`, `hostile.gitconfig` or `sentinel.log` remains anywhere in TMPDIR. The register entry `sentinel-leak` was re-checked against both paths its subject covers, which is the scoping this audit corrected last round. The Python guard removes what it writes: measured again here, the TMPDIR listing is unchanged across a full run of `tests.test_disposable_fixture_signing` and the count of hostile artefacts anywhere is zero before and zero after, with one earlier reading that showed a changed listing traced to an unrelated concurrent temporary directory and no hostile artefact in it. The shell path is now half repaired and half documented: step 4's exit removes its directory, and the four sites that do not are named above rather than papered over. Nothing regressed. Both shipped copies are byte-identical to the canonical artefacts, `cmp` exiting 0 for each at 85169e30d213da4d0b46ec366c6f6fbf79a286409cae506174b58654b1703e78 for the runbook and 20a2bc0b386ffdfaeea45d5b936fa8f57f8f4542a633ff70560d4f6ab663c955 for the study. The enumeration was exercised across all four registered suites and bites for every one, removing the declaration at `tests/test_boundary_currency.py:169`, `plugins/horos/tests/test_universe.py:59`, `plugins/hexaemeron/tests/test_kronos_scoreboard.py:763` and `plugins/hermes/skills/hermes/scripts/test_hermes.py:176` failing `CoveredSuites` by the suite's own name each time, each reverted byte-identically, and green as shipped. The fast exit commands all exit 0, Hermes at 0, the root suite at 482, `horos check` reporting `boundary matches the tree`, phylax, ephoros and hypomnema each printing `clean` and exiting 0 from plugin cache 1.6.7, and the report declaring `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips before `.elenchus/` was removed. One commit was made this round and it is maintenance rather than a fix: the eleventh amendment moved the canonical runbook, and step 4's `Files` field now names both shipped copies, so refreshing them is part of the step's contract; the commit changes documents only and the classifier returns `unguarded` on it with the detail `the commit changed no test files`, which is why the round's verdict field is `null` rather than that value. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unrepeated scope, the zero-finding row, the null verdict, every commit, digest, path, line number, count and exit code, the operative-versus-superseded analysis of the remaining leak sites, the attribution of the three surviving directories to this audit's own probes, and the carried-forward opportunity at step 5's exit, without changing any claim.

## Step 5, round 1 -- 2026-08-29T03:48:37Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. M1 was reproduced on this host at this moment, not on the host or at the moment the recorded samples were taken, so the recorded figures were checked for internal consistency and for arm integrity rather than for exact reproduction. M2's twenty-four suite runs were not repeated; the root-suite row was audited by measuring the two added modules directly, and the other three rows by recomputing their arithmetic. The `<sign-off>` retirement was checked by searching the tree for what still mandates the workaround, not by running the superseded runbook. The two disclosures were weighed rather than re-derived: the two seconds of document linting during one Hexaemeron sample could not be reproduced after the fact, and the broader configuration drift Mason reported was not traced to its originating run. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | low | docs/decisions/ADR-046-declare-signing-policy-on-disposable-git-fixtures.md | The decision record miscounts the work it records, in two places, and the study it inherited the count from carries the same error. ADR-046 says at line 23 that "at `f5d94a5f`, ten such sites in six files declared no signing policy", and at line 151 that "ten sites became six edit points, one per chokepoint". The study says at line 234, immediately above the table that contradicts it, "Ten sites in six files." Parsing the study's own section 2b table gives ten sites across seven distinct files: `tests/test_boundary_currency.py`, `plugins/hermes/skills/hermes/scripts/test_hermes.py`, `plugins/hexaemeron/tests/test_elenchus_checker.py`, `plugins/hexaemeron/tests/test_kronos_scoreboard.py` twice, `plugins/horos/tests/test_scoped_entry.py`, `plugins/horos/tests/test_demonstration.py`, and `plugins/horos/tests/test_universe.py` three times. The second statement is wrong on a different axis as well: each of the ten sites is its own chokepoint, a distinct `setUp`, constructor or clone, and steps 3 and 4 added exactly ten declarations, one per site, so "one per chokepoint" gives ten edit points rather than six. Counting the declarations in the tree confirms it, one each in five files, two in `test_kronos_scoreboard.py` and three in `test_universe.py`. Nothing behaves differently for the error: the rule, the scope, the exclusions and the injection-precedence table are all correct, and the number six appears to have been carried in from step 3's amended goal, which correctly counts the six sites in the root and Horos suites alone. The reason it is worth raising is that this is the durable record, the artefact a later reader consults first, and the run's own standard, set when the fourth amendment corrected the same class of slip, is that a shipped document which miscounts its own work is a defect in the document. | fixed in two halves, one of which is not a code diff. The study's section 2b sentence is under the byte-identity criterion `cmp .hexaemeron/study.md docs/disposable-fixture-signing/study.md` that step 1's exit carries, so it could not be edited here and was corrected by amendment instead; that amendment also records where the six most likely came from, reading the three `test_universe.py` rows as one edit and the two kronos rows as one, which is what "one per chokepoint" would mean if a module were a chokepoint, and it is not, because the helpers and the constructions are separate and removing any single declaration fails the guard on its own. The code diff is `9d10910f8f941ed0db5c4977a9d13e294c5ab185`, which corrects ADR-046's two sentences to seven files and ten edit points and refreshes the shipped study copy onto the amended artefact at af6813639a250aa01e747d4a2b956877002e1d92849abec1b125c64107b7eda7, the shipped runbook already matching its own at ce39daa77b4461427015d390fc986b7b3a8844af4e8723e5afdb8b21e614530e. Both `cmp` calls exit 0, imprimatur and brevitas on the ADR exit 0, `tests.test_decision_records` exits 0, protasis prints clean for each document and hypomnema exits 0. Neither number changed what was built: steps 3 and 4 worked from the table rather than the sentence above it |

Leads not pursued: the code and the documents this step delivered are otherwise clean, and the one finding is fixed. The demo path, which is the acceptance proof for items 1 and 5, was run in full at the audit tip rather than inferred from step 4's measurements, because step 5's form is new: all four suites write into one sentinel and the assertion is on what that combined sentinel holds. Three suites exit 0 and leave it completely empty, the root suite, Hermes and Horos together giving 0 bytes. The Hexaemeron suite then runs `Ran 1394 tests in 475.454s` and `FAILED (errors=1, skipped=1)`, its two greps exit 0, and the combined sentinel ends at 139 bytes holding exactly one line, `hostile signer reached: --keyid-format=long --status-fd=1 --verify`, so `grep -q -- '--verify'` exits 0 and `! grep -q -- '-bsau'` exits 0. Every remaining exit command exits 0: the report declares `elenchus.unittest.v1`, `complete: true`, 482 tests and zero failures, errors and skips, `tests.test_decision_records`, imprimatur and brevitas on the ADR, hypomnema and `horos check` all pass. Acceptance item 2 is proved rather than asserted: both configuration diffs print nothing, 0 bytes each, so no suite in the demo path touched the contributor's global configuration or the checkout's local one. That also settles Mason's disclosure about configuration drift: the broader diff it ran found +306 bytes attributed to a different Fiat run in this shared repository, but the two diffs the exit actually specifies are empty here, so the criterion passes on its own terms and nothing is being excused. M1 was audited as evidence rather than read as prose, and it holds. Every figure recomputes: signed 21.3, 21.6 and 21.3 give a mean of 21.40 and a range of 0.3, unsigned 16.6, 16.9 and 16.8 give 16.77 and 0.3, the difference row 4.7, 4.7 and 4.5 gives 4.63 and 0.2, the difference mean equals the gap between the arm means to two decimals, and 4.6 against a 0.3 spread is the factor of 15 claimed. The arm-integrity claim was reproduced rather than taken on report: running the same loop here, every signed sample's last commit carries a `gpgsig` header and reads `G` under `%G?`, and every unsigned sample's carries none and reads `N`, so the arms are what they claim and the gap is signing rather than something else. This host's own timings differ, signed 21.3, 27.8 and 20.9 against unsigned 15.8, 15.6 and 15.7, because one signed sample caught other load; the direction and the magnitude class reproduce and the improvement still exceeds the unsigned arm's 0.2 ms range by more than an order of magnitude, but the recorded 0.3 ms spread belongs to the recorded samples and not to this reproduction. M2's root-suite row is the one that most needed a second opinion, and the reasoning holds, with better evidence available than the record uses. All four rows recompute, including that the spread column is the larger of the two arms' own ranges as the stated criterion requires, and that the deltas are taken from unrounded means, which is why horos reads -0.11 rather than -0.10. The record explains the +1.84 s by attributing twenty extra tests at the before arm's average rate of 70.8 ms, giving 1.42 s and a residual of 0.42 s inside the before arm's own 1.63 s range. Measuring the two added modules directly is stronger: `tests.test_disposable_fixture_signing` runs its 12 tests in 1.85 s and `tests.test_root_elenchus_runner` its 8 in 0.03 s, about 1.88 s against an observed delta of 1.84 s, so the added work accounts for essentially the whole difference and leaves no residual to explain. The average-rate proxy understates rather than flatters, because `CoveredSuites` spawns four entire suites as subprocesses and is far slower than an average root-suite test, and the record still lands its residual inside the spread using the weaker figure. That is what turns "not explaining away a real number" from an assertion into a measurement, and it is recorded here rather than only as a verdict. On overstatement, `measurement.md` claims nothing it has not earned. Three suites are recorded as inside variance in those words, no suite-level speedup is claimed on this host, and item 4 is explicitly rested on M1 alone. The host section names the machine and the signing configuration beside the numbers, and all of it verifies: `commit.gpgsign` is `true`, `gpg.format` is `ssh`, `user.signingkey` is `/Users/kethcode/.ssh/id_ed25519_github.pub`, git is 2.54.0, and `f5d94a5f27168e6c0faecd43eee426f6ee892cdc` is the actual merge-base of this delivery with `origin/main` as the before-arm claim requires. The issue's 188.38 and 6.91 second figures are attributed to a GPG contributor at revision `e4d1f5677fa1` and explicitly not reproduced, with the record stating that no claim rests on them. ADR-046 was checked on the part a later reader leans on hardest, and its injection-precedence table re-derives exactly, all four rows: the `GIT_CONFIG_COUNT` triple against a declared fixture gives rc 128 and one signer invocation, `GIT_CONFIG_PARAMETERS` the same, a `GIT_CONFIG_GLOBAL` file against an undeclared fixture the same, and a `GIT_CONFIG_GLOBAL` file against a declared one rc 0 and no invocation. The surrounding claims hold too: `plugins/hexaemeron/skills/kronos/scripts/kronos.py:574` does pass `-c commit.gpgsign=false`, both hardening sites did already carry the rule at `f5d94a5f`, the three signature classes in `test_hexctl.py` are named correctly, and the `%G?` paragraph states the verifier caveat this audit measured at step 2. The numbering is right and the collision is handled: `ADR-045-select-and-schedule-repository-checks-from-one-graph.md` is on `origin/main` and is issue 622's, `ADR-046` is free there, the filename and the first heading both say 046, and the record explains the renumber so a reader who finds the study naming 045 knows why. Four things go to the run's carried-forward list, this being the last round that can add to it. First, the TMPDIR leak that step 4's audit raised: the runbook has twelve `mktemp -d` sites against five traps, and the operative blocks for the preamble, step 2 and step 3 cannot be amended because the preamble is not a step field and those steps are complete, so those keep the leak. Second, the `<sign-off>` supersession runs one way only: `plugins/horos/docs/marker-self-exclusion/runbook.md` still says expanding the prefix is mandatory and holds zero references to ADR-046, and while the ADR honestly records that the document is not edited, a reader arriving from the Horos side never learns the workaround is retired. Third, the enumeration covers one representative test per suite across its four entries, so it catches a forgotten declaration only when the chosen representative exercises that site, which is narrower than the register entry's phrase about a new site being caught by the suite rather than by a contributor. Fourth, the study's section 2b still reads "Ten sites in six files" in its body with the correction living in an amendment several hundred lines later, which is this run's append-only convention and the same shape this audit recorded at step 3, and it is named once here for whoever reads the study top to bottom. The Elenchus verdict of `unguarded` was measured with step 5's own runner contract against the fix commit and returned the detail `the commit changed no test files` with an empty changed-test list, which is the branch at `plugins/hexaemeron/skills/elenchus/scripts/elenchus.py:281` that step 2's audit identified; the commit touches only `docs/`, so that is the mechanically correct answer rather than a defect. On the rest of the register: injection-precedence is now recorded durably by the ADR's table and re-derived here; vacuous-regression holds because the demo path's empty sentinel across three suites and its single verification line across four are positive evidence rather than an absence; config-scope-escape holds because no site changed this step and all ten declarations still name their own repository; contributor-config-mutation holds by acceptance item 2's two empty diffs; signature-test-weakening holds because this step changes no test file at all; format-coverage, missed-construction-site, clone-and-worktree-paths and skip-instead-of-fail hold unchanged from step 4's rounds, with the clone and worktree facts now written into the ADR; sentinel-leak holds for the guard and is carried forward for the shell blocks; subprocess-argv holds because phylax prints `clean`; measurement-honesty is the live entry for this step and is the subject of most of the checks above. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unreproduced scope, the single finding with its severity and its two-part remedy, the measured `unguarded` verdict, every commit, digest, path, line number, sample, mean, range, delta, count, exit code and byte size, the direct measurement of the two added modules, the two disclosures and how each was weighed, and all four carried-forward items, without changing any claim.

## Step 5, round 2 -- 2026-08-29T03:57:29Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The eight-minute hostile Hexaemeron leg and the rest of the demo path were not repeated; round 1 ran the whole path at this tip and the controller ran it independently, the two agreeing, and nothing found this round bears on either. M1 and M2 were audited in round 1 and their figures were not re-measured here. The whole-repository survey below matched on files that both create a repository and commit into it; a construction site that creates a repository in one file and commits into it from another would not appear in it. The five sites the enumeration does not guard were established by removing each declaration in turn, not by tracing every representative's call graph. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R2-01 | low | docs/decisions/ADR-046-declare-signing-policy-on-disposable-git-fixtures.md | The decision record described the guard as wider cover than it gives, in the section a future contributor reads to learn what protects them from forgetting the rule. Its Consequences said the cost of choosing ten edit points over one is "paid by `tests/test_disposable_fixture_signing.py`, which enumerates all four suites and fails when a disposable repository reaches the signer", which a reader takes as any disposable repository in those suites. Measured by removing each of the ten declarations in turn and running `CoveredSuites`, it is half of them: five removals fail the guard and five do not. The five it catches are the four registered representatives' own construction sites, `tests/test_boundary_currency.py:169`, `plugins/hermes/skills/hermes/scripts/test_hermes.py:176`, `plugins/hexaemeron/tests/test_kronos_scoreboard.py:763` and `plugins/horos/tests/test_universe.py:59`, plus `plugins/hexaemeron/tests/test_kronos_scoreboard.py:667`, which the Hexaemeron representative reaches through `make_scope`. The five it misses are `plugins/hexaemeron/tests/test_elenchus_checker.py:115`, `plugins/horos/tests/test_scoped_entry.py:65`, `plugins/horos/tests/test_demonstration.py:54` and `plugins/horos/tests/test_universe.py` at 138 and 200, because no registered representative exercises them. The partial cover is the runbook's stated design rather than a defect in the guard: step 2's tests field chose one representative test from each covered suite, and this audit has carried the limitation forward since step 2's third round. What was wrong was the durable record describing that design without its limit, on the one claim a contributor would rely on when adding an eleventh site. | fixed in `c7d1fc2e26263cd55395539b3f93196ca9bd1b84`; the sentence now says the guard runs one representative test from each of the four suites and fails when that representative's fixture reaches the signer, gives the measured five-of-ten split with the reason the other five are missed, and says a new site is caught when a representative's path reaches it and by review otherwise; imprimatur and brevitas on the ADR exit 0, `tests.test_decision_records` exits 0 and hypomnema exits 0. The design limit itself is not fixed and stays on the run's carried-forward list |

Leads not pursued: this closes the audit of all five steps, and the run's own work holds together when looked at whole rather than step by step. Round 1's fix is correct against the tree rather than merely different: counting the declarations gives ten across seven files, one each in `tests/test_boundary_currency.py`, `plugins/horos/tests/test_scoped_entry.py`, `plugins/horos/tests/test_demonstration.py`, `plugins/hexaemeron/tests/test_elenchus_checker.py` and `plugins/hermes/skills/hermes/scripts/test_hermes.py`, two in `plugins/hexaemeron/tests/test_kronos_scoreboard.py` and three in `plugins/horos/tests/test_universe.py`, which is what ADR-046 now says at lines 23 and 151. Both shipped copies are byte-identical to the canonical artefacts at this tip, `cmp` exiting 0 for each, the study at af6813639a250aa01e747d4a2b956877002e1d92849abec1b125c64107b7eda7 after the amendment moved it and the runbook at ce39daa77b4461427015d390fc986b7b3a8844af4e8723e5afdb8b21e614530e. The aggregate question, whether anything that looked right in isolation looks wrong across the five steps, was answered with a fresh whole-repository survey rather than by re-reading the study's inventory. Fourteen files both create a git repository and commit into it; twelve carry a declaration and two do not, and both of those resolve correctly. `plugins/hexaemeron/tests/test_hexctl_frontier_receipt.py` creates no repository of its own: its `FrontierReceiptCase` extends `HexctlCase`, whose `setUp` calls `make_origin_checkout`, and that helper declares the rule at `plugins/hexaemeron/tests/test_hexctl.py:53`, which is exactly the principle ADR-046 records about a chokepoint that is a helper. `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` is product code building real run worktrees whose commits must carry a signature, so it is correctly outside a rule about disposable fixtures, and declaring there would break the run-signing this delivery's own commits depend on. No construction site is missed, and every fixture-creating test file in the repository falls inside one of the four enumerated suites, so no suite is uncovered. The exclusions still hold at the end of the run: `git diff` over `plugins/hexaemeron/tests/test_issue_429_recovery.py` from the merge-base at `f5d94a5f27168e6c0faecd43eee426f6ee892cdc` remains 0 bytes, and the signature classes in `test_hexctl.py` are untouched. The enumeration is green and still bites at this tip. Four items go to the run's carried-forward list and their numbers are stated here as the pull request should carry them. First, the hostile blocks that leak a temporary directory. The runbook holds twelve `HOSTILE_DIR="$(mktemp -d)"` sites and three now carry a `trap`, and counting only the fields that bind rather than every occurrence, three operative sites still leak and none of the three can be repaired: the preamble at line 41, which is not a step field and so no amendment grammar reaches it, step 2's replacement exit at line 665 and step 3's base exit at line 289, both belonging to completed steps. This is a correction to what the previous round carried: step 5's exit was then unamended and counted among the leaking sites, and its operative exit at line 1520 now traps both the hostile directory and the capture directory, as step 4's at line 1276 does, so the live count is three rather than four. Second, the `<sign-off>` supersession runs one way only. `plugins/horos/docs/marker-self-exclusion/runbook.md` still states that expanding the prefix is mandatory and holds no reference to ADR-046 or to being superseded, while ADR-046 states plainly that the document is not edited; the record is honest about it, but a reader arriving from the Horos side learns nothing of the retirement. Third, the enumeration's cover is partial, five of the ten sites as measured above, which ADR-046 now describes accurately but which remains a property of the design rather than something this delivery fixes; whoever extends the guard should know that adding a representative per construction site, rather than per suite, is what would close it. Fourth, the study's section 2b still reads "Ten sites in six files" in its body at line 234, with the correction living in an amendment at line 740, five hundred lines later; that is this run's append-only amendment convention and the same shape this audit recorded against the runbook at step 3, and it is named once so a reader going through the study top to bottom is not misled by the sentence above the table that contradicts it. On the register at the close: injection-precedence is recorded durably by ADR-046's table, re-derived in round 1; vacuous-regression holds because the guard's own non-vacuity was mutation-tested at step 2 and the enumeration's was re-tested at every step since; config-scope-escape holds across all ten declarations, each naming its own repository through `-C` or `cwd`; contributor-config-mutation was proved rather than asserted by round 1's two empty configuration diffs; signature-test-weakening holds, the protected suites untouched across the whole run and their refusal legs measured red under a mutated `verify_local_commit` at step 4; format-coverage holds with all three arms present; missed-construction-site is the entry this round exercised hardest and it holds for what exists while its guard's reach is now stated honestly; clone-and-worktree-paths is settled and written into the ADR; skip-instead-of-fail holds with all six of the guard's precondition branches driven at step 4; sentinel-leak holds for the Python guard and is carried forward for the three shell blocks that cannot be amended; subprocess-argv holds with phylax clean; measurement-honesty was the live entry for step 5 and its audit is in round 1. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unrepeated scope, the single finding with its severity and its fixing commit, the measured `unguarded` verdict, every commit, digest, path, line number, count and exit code, the whole-repository survey and both resolutions it produced, the correction to the previous round's leak count, and all four carried-forward items, without changing any claim.

## Step 5, round 3 -- 2026-08-29T04:03:43Z

Audit schema: fiat-audit-round/v2

Covered: injection-precedence=reviewed; vacuous-regression=reviewed; config-scope-escape=reviewed; contributor-config-mutation=reviewed; signature-test-weakening=reviewed; format-coverage=reviewed; missed-construction-site=reviewed; clone-and-worktree-paths=reviewed; skip-instead-of-fail=reviewed; sentinel-leak=reviewed; subprocess-argv=reviewed; measurement-honesty=reviewed

Not checked: the Pashov suite remains waived by the controller because this run ships no Solidity and alters no contract, so x-ray, solidity-auditor and fizz were not run and nothing is claimed from them. The demo path, both measurements and the eight-minute hostile Hexaemeron leg were not repeated; round 1 ran the whole path at this tip, the controller ran it independently and the two agreed, and nothing found since bears on either. The five sites the enumeration misses were established by removing each declaration in turn and by reading which class or module each belongs to, not by tracing every representative's full call graph. The three leaking blocks were confirmed unrepairable from the amendment grammar and the step states rather than by attempting an amendment. Push, pull-request, issue-comment, merge, controller-receipt and checkpoint actions were outside this local round and did not occur; live GitHub state was not refetched.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: no defect was found at signed tip `1559306452b189cccc064bff83b5f44fd10608df`, this round makes no fix and there is no Elenchus verdict to report, and the audit of all five steps closes here. Round 2's correction was re-verified against the tree rather than against its own arithmetic, because a corrected sentence being wrong in a new way is a failure mode this run produced twice. The three claims it makes each hold. The guard registers four suites and each entry carries exactly one representative test. Removing each of the ten declarations in turn and running `CoveredSuites` gives five failures and five passes, the same five in each group as the measurement the sentence was written from: it catches `tests/test_boundary_currency.py:169`, `plugins/hermes/skills/hermes/scripts/test_hermes.py:176`, `plugins/hexaemeron/tests/test_kronos_scoreboard.py:667` and `:763`, and `plugins/horos/tests/test_universe.py:59`, and it misses `plugins/hexaemeron/tests/test_elenchus_checker.py:115`, `plugins/horos/tests/test_scoped_entry.py:65`, `plugins/horos/tests/test_demonstration.py:54` and `plugins/horos/tests/test_universe.py` at 138 and 200. The stated reason holds for every one of the five misses: `:138` is `BindingDirectoryTests.setUp` and `:200` is `CandidateBindingTests.setUp`, neither being the class the Horos representative lives in, which is `UniverseTests`, while `test_scoped_entry.py`, `test_demonstration.py` and `test_elenchus_checker.py` are modules no registered representative imports. The one caught site that is not a representative's own is `test_kronos_scoreboard.py:667`, and the mechanism this audit recorded for it is confirmed: `test_extra_blobs_in_the_state_ref_are_ignored` calls `self.make_scope("a")` on its first line and `make_scope` at line 655 carries that declaration. Both shipped copies are byte-identical to the canonical artefacts at this tip, `cmp` exiting 0 for each, the study at af6813639a250aa01e747d4a2b956877002e1d92849abec1b125c64107b7eda7 and the runbook at ce39daa77b4461427015d390fc986b7b3a8844af4e8723e5afdb8b21e614530e. Every gate is green at the close: the root suite runs 482 tests, `tests.test_decision_records`, imprimatur and brevitas on the ADR, `horos check` and `audit_synopsis.py --check` all exit 0, phylax, ephoros and hypomnema each print `clean` and exit 0 from plugin cache 1.6.7, and `.elenchus/` was removed leaving the tree clean. What this run leaves undone is four things, and their numbers are stated here in the form the pull request should carry them. First, three hostile blocks leak a temporary directory and none of the three can be repaired. The runbook holds twelve `HOSTILE_DIR="$(mktemp -d)"` sites of which three now carry a `trap`; counting only the fields that bind, the operative ones are the preamble at line 41, step 2's replacement exit at line 665, step 3's base exit at line 289, step 4's at line 1276 and step 5's at line 1520, and the last two trap while the first three do not. The preamble is unreachable by amendment because every one of the runbook's twenty-one `Complete replacement` clauses names a step field and the preamble's heading, `## Host conditions every command below assumes`, is not a step; step 3's exit was never replaced by any amendment, so its base block binds; and steps 2 and 3 are complete, so amending either would move a criterion after it was receipted. Each leaked directory holds a mode-0700 signer beside the configuration naming it by absolute path, which is inert alone and owner-only, and the Python guard leaks nothing. Second, the `<sign-off>` retirement is legible from one side only: `plugins/horos/docs/marker-self-exclusion/runbook.md` still states that expanding the prefix is mandatory and holds no reference to ADR-046 or to being superseded, while ADR-046 states plainly that the document is not edited, so the record is honest but a reader arriving from the Horos side learns nothing of the retirement. Third, the guard's cover is five of the ten sites, which ADR-046 now describes accurately after this round's predecessor corrected it, and closing it means registering a representative per construction site rather than per suite. Fourth, the study's section 2b still reads "Ten sites in six files" in its body at line 234, with the amendment that corrects it beginning at line 740 and stating the corrected count at line 747; that is this run's append-only convention rather than an error, and it is named so a reader going through the study top to bottom is not misled by the sentence immediately above the table that contradicts it. On the register at the close, every entry is reviewed against evidence gathered across the eleven rounds rather than asserted: injection-precedence rests on ADR-046's table, re-derived twice; vacuous-regression on the guard's own mutation tests and on the ten-removal measurement above; config-scope-escape on all ten declarations naming their own repository; contributor-config-mutation on round 1's two empty configuration diffs; signature-test-weakening on the protected suites being untouched across the whole run and their refusal legs measured red under a mutated `verify_local_commit`; format-coverage on all three arms being present and exercised; missed-construction-site on the fourteen-file survey that found no missed site and on the guard's reach now being stated honestly; clone-and-worktree-paths on the measured inheritance facts, now written into the decision record; skip-instead-of-fail on all six precondition branches being driven; sentinel-leak on the guard leaking nothing and the three shell blocks carried forward above; subprocess-argv on phylax printing `clean` throughout; and measurement-honesty on M1's arms being verified by header and `%G?`, on M2's arithmetic recomputing, and on the two added modules being measured directly at 1.85 and 0.03 seconds against an observed 1.84-second delta. The bounded Sapheneia pass under `--audit-filter sapheneia:sapheneia` preserved the heading, schema fields, all twelve risk ids and dispositions, the waived and unrepeated scope, the zero-finding row, the null verdict, every commit, digest, path, line number, count and exit code, the re-verification of round 2's correction site by site, and all four carried-forward items in their final form, without changing any claim.
