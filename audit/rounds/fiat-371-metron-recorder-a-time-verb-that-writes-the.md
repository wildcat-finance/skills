## Step 1, round 1 -- 2026-09-06T10:24:33Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=not-applicable; process-group-teardown=not-applicable; group-escape=not-applicable; output-cap=not-applicable; timeout-bound=not-applicable; partial-run-file=not-applicable; failed-repetition-hidden=not-applicable; stray-top-level-number=not-applicable; aggregation-declared=not-applicable; spread-from-samples=not-applicable; unit-mismatch=not-applicable; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: the twelve recorder concerns, from subprocess-argv to repeat-bounds, name behaviour of `metron.py record`, which this step does not ship; the diff is `docs/metron-recorder-study.md`, `docs/metron-recorder-runbook.md`, `.horos/boundary.json` and `.horos/census.json`, 864 insertions and 5 deletions, no Python. version-surfaces is not exercised because no version bump is in this step. The Pashov suite is waived: no Solidity in the run. No measurement was taken.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the tracked runbook's step 1 Files clause, `docs/metron-recorder-runbook.md:84`, names the audit log as `audit/rounds/fiat-371-metron-recorder-time-a-command-into-the-run.md`, the earlier run's path, while the controller directive names `audit/rounds/fiat-371-metron-recorder-a-time-verb-that-writes-the.md`; the directive path is used here, and the clause is not edited because the tracked copy must stay byte-identical to the receipted `.hexaemeron/runbook.md` (sha256 c6788f913a35487e4d7cceb38d519630913d9b9219e1df2448ab25e0ea27332a), so a correction belongs to a controller amendment rather than an audit fix. Evidence for this round: `cmp` on both doc pairs exits 0 against study sha256 7a193554dfcfc949686e2541a423335cf3ca30b81fbb6d1fd7b796288d98c436 and that runbook digest; design evidence sha256 578d33bbf14a32dd663ee4df52c60c6350b5d1adde0956851f91e75518b16843 selects `subcommand-wall-clock` by rule `unique-frontier`, and the tracked study is the receipted study that made that selection; `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each exit 0 clean; `protasis.py --study` and `protasis.py` on the two docs exit 0; `imprimatur.py` on both scores 100.0 with 0 defects; `horos.py check .` reports the boundary matches the tree; `git diff --check` exits 0; `python3 -m unittest discover -s tests` on a clean detached snapshot of 0c31b0873d9138ca1e00e29895817426afb33205 ran 1623 tests in 355.514s, OK; `plugins/hexaemeron/tests/fixtures/metron/metron-budgets.json` is untouched by the diff, so DEMONSTRATION.md's pinned digest still holds.

## Step 1, round 2 -- 2026-09-06T10:51:16Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=not-applicable; process-group-teardown=not-applicable; group-escape=not-applicable; output-cap=not-applicable; timeout-bound=not-applicable; partial-run-file=not-applicable; failed-repetition-hidden=not-applicable; stray-top-level-number=not-applicable; aggregation-declared=not-applicable; spread-from-samples=not-applicable; unit-mismatch=not-applicable; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: the twelve recorder concerns, from subprocess-argv to repeat-bounds, name behaviour of `metron.py record`, which this step does not ship; the diff against `fiat/371-metron-recorder-a-time-verb-that-writes-the` is `.horos/boundary.json`, `.horos/census.json`, this log, its synopsis, `docs/metron-recorder-runbook.md` and `docs/metron-recorder-study.md`, 888 insertions and 5 deletions, no Python. version-surfaces is not exercised because no version bump is in this step. The Pashov suite is waived: no Solidity in the run. No measurement was taken. The brief's `runbook_step` digest c6788f913a35487e4d7cceb38d519630913d9b9219e1df2448ab25e0ea27332a is the pre-amendment runbook; the amended `.hexaemeron/runbook.md` is sha256 28fc0aab6d4fc7eefc7e1ae3660e42936a8a2c40dadc88bf88bb21bdf02d00d1. The amendment's `Still holding` claims for steps 2 to 4 were not exercised, since those steps are unbuilt.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | low | docs/metron-recorder-runbook.md | step 1 Files clause named the predecessor run's audit log and credited the census scan with the boundary rewrite | fixed in 748aca2d5cbff676e2243bd6c5b7db495a301548 |

Leads not pursued: `hypomnema.py --study docs/metron-recorder-study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` reports H008, no design bridge block, and exits 1; the step's exit names only the ordinary walk, 9 of the 52 tracked studies carry the fence, and study section 12 names the draft record `docs/decisions/drafts/record-timed-runs-at-the-median-with-spread.md` that step 3 creates, so a bridge is a controller study amendment, not an audit fix, because the tracked study must stay byte-identical to the receipted `.hexaemeron/study.md` (sha256 7a193554dfcfc949686e2541a423335cf3ca30b81fbb6d1fd7b796288d98c436). Evidence for this round: `cmp` on both doc pairs exits 0 against that study digest and runbook sha256 28fc0aab6d4fc7eefc7e1ae3660e42936a8a2c40dadc88bf88bb21bdf02d00d1; 748aca2d5cbff676e2243bd6c5b7db495a301548 changes `docs/metron-recorder-runbook.md` (7 added lines, the `Amendment -- 2026-09-06` section) and `.horos/census.json` (2 lines) only, signature G, author Shoggoth; the amended Files clause names this run's log and assigns `scan . --write` to `.horos/boundary.json` and `scan . --census --write` to `.horos/census.json`, and `horos.py check .` reports the boundary matches the tree; `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each exit 0 clean; `protasis.py --study` and `protasis.py` on the two docs exit 0; `imprimatur.py` on both scores 100.0 with 0 defects; `git diff --check` exits 0 on the tree and on the range; `python3 -m unittest discover -s tests` on a clean detached snapshot of 748aca2d5cbff676e2243bd6c5b7db495a301548 ran 1623 tests in 230.584s, OK; `elenchus.py --ref 748aca2d5cbff676e2243bd6c5b7db495a301548 --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .hexaemeron/test-reports/step-1.json` exits 0 with status `unguarded`, detail `the commit changed no test files`, and writes no report because the check returns before the command runs; `unittest-json-v1` is the elenchus.py adapter for the `elenchus.unittest.v1` schema `run_tests.py` writes, the runbook's declared format; design evidence sha256 578d33bbf14a32dd663ee4df52c60c6350b5d1adde0956851f91e75518b16843 selects `subcommand-wall-clock` by rule `unique-frontier`, its nine selection-stage criteria pass and its two pending criteria block step 2 and step 3, not this boundary; `plugins/hexaemeron/tests/fixtures/metron/metron-budgets.json` is untouched by the diff, so DEMONSTRATION.md's pinned digest still holds.

## Step 1, round 3 -- 2026-09-06T11:01:49Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=not-applicable; process-group-teardown=not-applicable; group-escape=not-applicable; output-cap=not-applicable; timeout-bound=not-applicable; partial-run-file=not-applicable; failed-repetition-hidden=not-applicable; stray-top-level-number=not-applicable; aggregation-declared=not-applicable; spread-from-samples=not-applicable; unit-mismatch=not-applicable; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: the twelve recorder concerns name behaviour this step does not ship, and version-surfaces has no bump in this step; the Pashov suite is waived because the run has no Solidity. The diff since round 2's fixes commit 748aca2d5cbff676e2243bd6c5b7db495a301548 is the round 2 record, its synopsis and `.horos/census.json` only, 20 insertions and 3 deletions, no Python and no tracked docs.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round 2 lead stands, `hypomnema.py --study docs/metron-recorder-study.md` reports H008 for the absent design-bridge fence and a fence is a study amendment rather than an audit fix; it is carried in the run pull request. Evidence for this round, taken by the controller session inline because the diff since round 2 is the audit record itself: `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each exit 0 clean on 70de69fc728bcdfe04f40bbd640d32349be8d095; `horos.py check .` reports the boundary matches the tree; `audit_synopsis.py --check .` exits 0; `python3 -m unittest discover -s tests` on a clean detached snapshot of 70de69fc728bcdfe04f40bbd640d32349be8d095 reports `Ran 1623 tests in 538.507s`, OK; `python3 plugins/hexaemeron/tests/run_tests.py .hexaemeron/test-reports/step-1-r3.json` passed 2431 of 2431; `plugins/hexaemeron/tests/fixtures/metron/metron-budgets.json` is untouched, so DEMONSTRATION.md's pinned digest still holds.

## Step 2, round 1 -- 2026-09-07T00:56:10Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=reviewed; process-group-teardown=reviewed; group-escape=reviewed; output-cap=reviewed; timeout-bound=reviewed; partial-run-file=reviewed; failed-repetition-hidden=reviewed; stray-top-level-number=reviewed; aggregation-declared=reviewed; spread-from-samples=not-applicable; unit-mismatch=reviewed; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: spread-from-samples and repeat-bounds name `--repeat`, `--warmup` and the `spread` object, which step 3 ships and this head does not; version-surfaces has no version bump in the diff, which is `plugins/hexaemeron/skills/metron/scripts/metron.py`, `plugins/hexaemeron/tests/test_metron_check.py` and `.horos/census.json`, 631 insertions and 8 deletions. The Pashov suite is waived: the run has no Solidity. No measurement was taken; the step's metron discipline is none. Every subprocess case was exercised on macOS 26.5.1 arm64 under CPython 3.14.6 only, so the `setsid` escape and the group teardown are unverified on Linux. The file-descriptor reuse shape in the escape path was reasoned from the source, not reproduced.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | docs/metron-recorder-runbook.md | step 2's Exit clause 3 and Tests paragraph require the forked-grandchild case to exit 1 and leave no file; the study's `process-group-teardown` risk and the prior art the study names require that case to succeed, which is what the head does | reported |

Leads not pursued: `time_once` closes `proc.stdout` and `proc.stderr` at `plugins/hexaemeron/skills/metron/scripts/metron.py:541` while an escaped reader thread is still blocked in `os.read` on the same descriptor, freeing the descriptor number under a live reader; the escape path returns 1 and the interpreter exits before anything reopens it, so I could not make it misbehave and left it alone, but step 3's `--repeat` keeps the process alive past a repetition and would put a later `Popen` pipe or `NamedTemporaryFile` in that window. Two smaller shapes were left for the same reason: the `kill_group` in the `finally` at metron.py:529 signals a process group id whose leader `proc.wait` has already reaped, which is safe while the group still has members and raises `ProcessLookupError` when it does not; and `time`'s total wall time can exceed `--timeout-seconds` by up to two teardown budgets, measured at 5.08 s of real time against a declared 1 s deadline when a `setsid` grandchild held the pipes. A `--cwd` that is not a directory is reported as `command 'python3' could not start`, naming the cwd only inside the errno text rather than as the cause. Evidence for this round, taken on aff3bfe4cb6ed6d7aad80fc0e1f2444f88668daa: `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each print `clean` and exit 0; `python3 -m unittest plugins.hexaemeron.tests.test_metron_check` ran 104 tests in 8.583s, OK, taking the module from 84 into the runbook's 98 to 104 band; `python3 -m unittest discover -s tests` on a clean detached snapshot of that head ran 1623 tests in 159.099s, OK; `python3 plugins/hexaemeron/tests/run_tests.py .hexaemeron/test-reports/step-2-r1-exit.json` passed 2451 of 2451 with 0 failures and 0 errors under schema `elenchus.unittest.v1`; `horos.py check .` reports the boundary matches the tree; `git diff --check` and `audit_synopsis.py --check .` exit 0; `.hexaemeron/reports/subcommand-wall-clock--recorder-suite-green.json` carries value true under schema `protasis-design-report/v1`, resolving Exit 5's design gate; `cmp` makes `docs/metron-recorder-study.md` and `docs/metron-recorder-runbook.md` byte-identical to the pinned copies at sha256 7a193554dfcfc949686e2541a423335cf3ca30b81fbb6d1fd7b796288d98c436 and 28fc0aab6d4fc7eefc7e1ae3660e42936a8a2c40dadc88bf88bb21bdf02d00d1, and design evidence 578d33bbf14a32dd663ee4df52c60c6350b5d1adde0956851f91e75518b16843 selects `subcommand-wall-clock`; `plugins/hexaemeron/tests/fixtures/metron/` is untouched by the diff, so DEMONSTRATION.md's pinned digest still holds. Exit 1 wrote 23.173 ms with the top level exactly `measurements` and `recorder`, and Exit 2's `check` against a one-budget ms file printed `neutral` and exited 0. subprocess-argv: `--out /etc/passwd`, `--name X`, a second `--`, `;rm -rf /`, `$FOO`, `$(id)` and `` `id` `` all reached the command as those exact words with `FOO=SECRETVAL` set in the caller's environment, none read as a recorder option and none expanded, because `split_command` takes the first `--` only and `Popen` gets the list with no `shell`. timeout-bound: `--timeout-seconds` 0, -1, 3601, 1.5 and ten each exit 2 and the command's sentinel file was never created; a 1 s deadline against `time.sleep(30)` failed as `timeout` in 1.07 s of real time. output-cap: an unbounded `sys.stdout.write` loop failed as `output-cap` in 0.07 s, and so did one written by an in-group grandchild, so an overflowing child cannot block the runner. process-group-teardown: a command that forks a 60 s grandchild and exits 0 recorded 26.292 ms, its grandchild was gone within a second, and a grandchild that wrote 500 bytes 300 ms after its parent exited contributed none of them, so `stdout_bytes` read 7 rather than 507. group-escape: the same command with `os.setsid` in the grandchild was refused as `escaped` in 5.08 s with no run file. partial-run-file and failed-repetition-hidden: each of the four causes printed exactly one stderr line naming `repetition 1` and the cause, wrote no run file, left a pre-existing `--out` with its previous bytes and left no `.metron-*` temporary behind. stray-top-level-number: `--note 12345` is stored as the JSON string `"12345"`, so `load_measurements` read the file unedited and returned one key. aggregation-declared: the run file names `median` and the single measurement equals its one sample. S2-R1-01 rests on `docs/metron-recorder-runbook.md:131` and `:164` against study risk `process-group-teardown` at `docs/metron-recorder-study.md:320` and the prior art the study names at `docs/metron-recorder-study.md:158`, namely `tests/test_demonstrations.py:1740`, which asserts exit code 0 for the forked case, and `:1762`, which asserts code 2 and refusal D085 for the `setsid` case; the tracked runbook must stay byte-identical to the receipted `.hexaemeron/runbook.md`, so the correction is a controller amendment rather than an audit fix, as step 1 round 2's finding was.

## Step 2, round 2 -- 2026-09-07T01:06:49Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=reviewed; process-group-teardown=reviewed; group-escape=reviewed; output-cap=not-applicable; timeout-bound=not-applicable; partial-run-file=not-applicable; failed-repetition-hidden=not-applicable; stray-top-level-number=not-applicable; aggregation-declared=not-applicable; spread-from-samples=not-applicable; unit-mismatch=reviewed; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: the round re-checks the fix for S2-R1-01 and nothing else; the diff since 2fdb788b3eeb326a14a3466c55bd482e5033e256 is `docs/metron-recorder-runbook.md` and `.horos/census.json` only, with no Python, so the nine concerns the corrected wording does not touch are carried from round 1's review of the same product bytes rather than re-exercised. This round was run by the controller session inline, as the audit loop permits for a re-check whose diff is one document. No measurement was taken. The Pashov suite stays waived: the run ships no Solidity.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | low | docs/metron-recorder-runbook.md | step 2's Exit and Tests fields listed a grandchild that forked and stayed in the group among the cases that exit 1 and leave no run file, while the shipped code exits 0 and writes the file for that case | fixed in 25d140b1a3b159028f80d5c2a28c996bdd715196 |

Leads not pursued: round 1's lead on `plugins/hexaemeron/skills/metron/scripts/metron.py:541` stands and was confirmed by reading the teardown block: after an escaped reader is detected, both pipe objects are closed while that reader may still be blocked in `os.read` on the same descriptor, so a later open in the same process could be handed the recycled number. It is unreachable today because the escape path returns 1 and the interpreter exits, and step 3 is where a repeat loop could open that window, so the correction belongs to step 3's build rather than to a step 2 fix; it is named in step 2's pull request and carried to step 3. Round 1's three smaller leads stand unchanged: `kill_group` signals a process group whose leader `proc.wait` already reaped; the command's total wall time can exceed `--timeout-seconds` by up to two teardown budgets, measured at 5.08 s against a declared 1 s; and a `--cwd` that is not a directory is reported as a command that could not start. Evidence for this round: the controller amended the receipted runbook on 2026-09-07 through `hexctl amend runbook`, taking it from sha256 28fc0aab6d4fc7eefc7e1ae3660e42936a8a2c40dadc88bf88bb21bdf02d00d1 to 452704cfe4518b8c51f5ce5c71b99d57b5f2ed8eab95f8905463c90c01c0f06e, and the tracked copy follows it byte for byte, proved by `cmp -s`; `protasis.py` on the tracked runbook exits 0; `imprimatur.py` scores 100.0 with 0 defects; `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each exit 0 clean; `horos.py check .` reports the boundary matches the tree; `git diff --check` exits 0; `python3 -m unittest discover -s tests` on a clean detached snapshot of the fix tree ran 1623 tests in 281.309s, OK; `python3 plugins/hexaemeron/tests/run_tests.py` passed 2451 of 2451; `elenchus.py --ref 25d140b1a3b159028f80d5c2a28c996bdd715196 --test-command "python3 plugins/hexaemeron/tests/run_tests.py {report}" --report-format unittest-json-v1 --report-file .hexaemeron/test-reports/step-2.json` exits 0 with status `unguarded` and detail `the commit changed no test files`, writing no report because the check returns before the command runs; `plugins/hexaemeron/tests/fixtures/metron/metron-budgets.json` is untouched, so DEMONSTRATION.md's pinned digest still holds.

## Step 2, round 3 -- 2026-09-07T01:11:26Z

Audit schema: fiat-audit-round/v2

Covered: subprocess-argv=not-applicable; process-group-teardown=not-applicable; group-escape=not-applicable; output-cap=not-applicable; timeout-bound=not-applicable; partial-run-file=not-applicable; failed-repetition-hidden=not-applicable; stray-top-level-number=not-applicable; aggregation-declared=not-applicable; spread-from-samples=not-applicable; unit-mismatch=not-applicable; repeat-bounds=not-applicable; demo-source-digest=reviewed; version-surfaces=not-applicable

Not checked: the diff since round 2's record commit 20f500c4172b5673712301d6cc1bad2a3d2b4f56 is that round's own record, its synopsis and the two Horos generated files, with no Python and no tracked document, so every product concern is carried from rounds 1 and 2 over the same product bytes rather than re-exercised. This round was run by the controller session inline. No measurement was taken, and the Pashov suite stays waived because the run ships no Solidity.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the four leads from rounds 1 and 2 stand unchanged, and the first of them is the one that matters. `plugins/hexaemeron/skills/metron/scripts/metron.py:541` closes both pipe objects after an escaped reader is detected, while that reader may still be blocked in `os.read` on the same descriptor, so a later open in the same process could be handed the recycled number; it is unreachable today because the escape path returns 1 and the interpreter exits, and step 3's repeat loop is where the window could open, so the correction is carried into step 3's build and named in step 2's pull request. The other three: `kill_group` signals a process group whose leader `proc.wait` already reaped; total wall time can exceed `--timeout-seconds` by up to two teardown budgets, measured at 5.08 s against a declared 1 s; and a `--cwd` that is not a directory is reported as a command that could not start. Evidence for this round: `phylax.py plugins tests scripts docs`, `ephoros.py plugins tests scripts docs` and `hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs` each exit 0 clean on 20f500c4172b5673712301d6cc1bad2a3d2b4f56; `horos.py check .` reports the boundary matches the tree; `audit_synopsis.py --check .` exits 0; `git diff --check` exits 0; `cmp -s` holds the tracked study and runbook against the receipted `.hexaemeron` copies, sha256 7a193554dfcfc949686e2541a423335cf3ca30b81fbb6d1fd7b796288d98c436 and 452704cfe4518b8c51f5ce5c71b99d57b5f2ed8eab95f8905463c90c01c0f06e; the root suite and the plugin suite results are recorded below.
