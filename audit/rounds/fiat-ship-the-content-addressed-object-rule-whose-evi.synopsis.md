Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-ship-the-content-addressed-object-rule-whose-evi.md | source_sha256=4c021fc33669cdb9f9dc99be76a333faff197e0092d8d4967c2d242a594dfcf2 | h2_count=5
## Step 1, round 1 -- 2026-09-04T23:14:31Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: whole-file-read=not-applicable; shape-gate-first=not-applicable; symlink-in-store=not-applicable; unreadable-object=not-applicable; tamper-as-drift=not-applicable; boundary-regeneration=reviewed; evidence-wording=reviewed; prose-reconciliation=reviewed; ledger-row=not-applicable; self-witness=not-applicable<br><br>Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run, so nothing here says this branch is green in CI; only the local suites were executed. X-Ray, solidity-auditor and fizz: not run, because the `security_suite` receipt is a waiver (this run ships no Solidity) and the step's diff is two Markdown files and one regenerated JSON file. The five store-rule ids marked not-applicable above: `plugins/horos/skills/horos/scripts/horos.py` is not in this step's diff, so `digest_matches_name`, `content_addressed_algorithm` and `classify_file` were read only far enough to confirm the diff does not touch them. The receipted `.hexaemeron/study.md` (SHA-256 `d6bd736548495d9deab7e006bcb9ce85d7f03978908aa8bbd993d192fd49a141`) and `.hexaemeron/runbook.md` (`b0070ffb6eb47f0d4a2800e4fd946fc0496d0aca20bf834dd2fb2a006d90956c`) were compared byte-for-byte to the committed copies (`cmp` identical at `658c2c3c`) and left unedited; S1-R1-01 corrects the committed copy only, so the receipted study still carries the sentence it corrects. The runbook copy's content beyond its identity with the receipted runbook and its five discipline links resolving from `plugins/horos/docs/content-addressed-objects/` (all five exist) was not reviewed for accuracy except where S1-R1-03 names it. `.horos/census.json` and `.horos/candidates.json`: unchanged in the diff (0 lines), so their currency was not separately regenerated.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | plugins/horos/docs/content-addressed-objects/study.md:167-172, 224-231 | The committed study copy said "The currency guard compares entries, not counts, so this run's regenerations are held to entries" (section 2, issue 842 paragraph) and carried "Widening the boundary currency guard to compare counts as well as entries" as an open lead. Both are wrong against the tree: `diff_boundary_documents` at `plugins/horos/skills/horos/scripts/horos.py:983` iterates `("schema", "tool", "universe", "counts")` before the entries, `tests/test_boundary_currency.py:203-211` (`test_new_ordinary_records_without_a_refresh_are_count_drift`) pins `.horos/boundary.json#counts` as the drift named when two tracked files are added without a refresh, and step 1 met exactly that drift when `980be4de` added the two spec copies, which is why `658c2c3c` regenerated the boundary (`files_walked` 2519 to 2521, no other byte moved). The runbook already requires regeneration in the same commit, so the code path is right and the prose is not. Fixed at `31f59c94`: both passages now say the guard compares counts alongside entries and name the function and the test; issue 842's reservation that `counts.files_walked` follows the worktree is kept as recorded. The receipted `.hexaemeron/study.md` is digest-pinned by the controller and keeps the old sentences; amending it is `hexctl amend study`, the controller's call. After the fix: imprimatur on the study copy `score 100.0/100 defects 0`, exit 0; `horos.py check .` prints `boundary matches the tree`, exit 0; `tests.test_boundary_currency` and `tests.test_marketplace_prose` 31 of 31 OK; horos suite 235 of 235 OK. | fixed |<br>| S1-R1-02 | low | tests/test_agent_instruction_corpus.py (`test_a_candidate_outside_the_design_record_is_refused`, `test_prover_selftest_exits_zero_and_writes_a_closed_report`); scripts/prove_agent_instruction_reconciliation.py:161 | Inside this run worktree the root suite is red on exactly those two tests: `Ran 1207 tests`, `FAILED (failures=2)`, exit 1, and the module alone `Ran 20 tests`, `failures=2`. Cause, reproduced here rather than taken from the controller's word: the prover reads `DESIGN_EVIDENCE = ".hexaemeron/design-evidence.json"` at the repository root (commit `88ef0e6d`, the #1098 run) to close its `--candidate` flag, and the two tests assume that record is absent or is the #1098 record; this run's record (SHA-256 `abf112dbcff8508966d2dbdc49ca8bcf5180aa1cabfcefb14841363b84682392`, selected `harden-record`) is untracked (`git ls-files .hexaemeron` returns 0 paths) and sits at that path. In a detached clean snapshot of `658c2c3c` (`git worktree add --detach`, no `.hexaemeron/`) the module is `Ran 20 tests / OK` and the root suite is `Ran 1207 tests / OK`, exit 0, which is the condition `scripts/run_checks.py` measures against. Not this step's defect: neither commit in the diff touches either file, and the failure is a property of running the suite inside any Fiat worktree whose design record is not the #1098 one. Not fixed on the stacked branch; it belongs to a carryover issue at integration, where the prover should read the record it is bound to rather than whichever run's record occupies the path. | accepted |<br>| S1-R1-03 | low | .hexaemeron/runbook.md:49, 91, 140, 173; plugins/horos/docs/content-addressed-objects/runbook.md (same lines) | Every step's Elenchus runner declares `report format unittest text output` with the report taken from stderr (`2> {report}`). `elenchus.py` accepts only `unittest-json-v1`, `forge-junit-v1` and `node-test-json-v1` for `--report-format`, and its SKILL.md says a legacy invocation without a valid report flag is `inconclusive`; the repository already ships the emitter the accepted format needs (`tests/run_tests.py --elenchus-report {report}`, which the #1098 runbook used and which returned `guarded` there). This round did not reach the report: `31f59c94` changes no test file, so `check` returned `unguarded` with detail `the commit changed no test files` before reading the flags, and that verdict is exact. A later step whose fix adds a guard test would classify `inconclusive` under the declared format however good the guard, so the verdict column of this run would stop carrying evidence. Not fixed here: the receipted runbook is digest-pinned, the committed copy is required verbatim by step 1's exit, and the repair is `hexctl amend runbook` for each step, the controller's call. | open |<br><br>Leads not pursued: whether `counts.files_walked` should be excluded from the currency comparison so a worktree does not fail on a field that is not evidence, which issue 842 records and this run's study refuses by name; whether the #1098 prover should locate its design record by digest rather than by path, which is the shape of the S1-R1-02 carryover and was not designed here; the imprimatur note on the runbook copy (`repeated openers: tests nonex3, elenchus runnerx4, phylax nonex3, ephoros nonex3, metron nonex3, elenchus nonex3, entry stepx3`) under an exit 0, which is the runbook's fixed field grammar and not a defect; and a sibling README under `plugins/horos/docs/<job>/`, refused by the study for the third run running. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.20`, each given the three changed paths, each printed `clean` and exited 0. Root suite inside the worktree: `Ran 1207 tests in 146.820s`, `FAILED (failures=2)`, exit 1, both failures S1-R1-02. Root suite in a detached snapshot of `658c2c3c`: `Ran 1207 tests in 163.654s`, `OK`, exit 0. Root suite in a detached snapshot of the fix commit `31f59c94`: `Ran 1207 tests in 100.637s`, `OK`, exit 0. Horos suite at `31f59c94`: `Ran 235 tests`, `OK`. Both unittest commands carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. Elenchus was invoked with the runbook's exact command `python3 -m unittest discover -s plugins/horos/tests -t plugins/horos 2> {report}` and report file `.hexaemeron/elenchus-step-1.txt`, with no `--report-format` because the declared value is not one the tool accepts; the returned status is `unguarded`. Committing this record and its synopsis adds two tracked files, so the same commit regenerates `.horos/boundary.json` (`files_walked` 2521 to 2523, `#counts` only), the drift `horos.py check .` named before the regeneration and the same shape S1-R1-01 describes. The fix commit verifies `G` under `B83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, carrying both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all ten risk ids and dispositions, every named unchecked item, the three finding ids with severities, file locations and statuses, every quoted sentence, digest, count, exit code and line number, the exact verdict word, and the unpursued leads without changing any claim.
## Step 1, round 2 -- 2026-09-04T23:23:39Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: whole-file-read=not-applicable; shape-gate-first=not-applicable; symlink-in-store=not-applicable; unreadable-object=not-applicable; tamper-as-drift=not-applicable; boundary-regeneration=reviewed; evidence-wording=reviewed; prose-reconciliation=reviewed; ledger-row=not-applicable; self-witness=not-applicable<br><br>Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run; only the local suites were executed. X-Ray, solidity-auditor and fizz: not run, because the `security_suite` receipt is a waiver (this run ships no Solidity) and this round's diff since `4d1a2dc4` is one Markdown file. The five store-rule ids marked not-applicable above: `plugins/horos/skills/horos/scripts/horos.py` is not in the diff. The receipted `.hexaemeron/runbook.md` (SHA-256 `196a48b0cdf9c6dfa6853debf6765a32fb76fa002eb655991304135ad6258d97`, the brief's `runbook_step.sha256`), `.hexaemeron/study.md` (`d6bd736548495d9deab7e006bcb9ce85d7f03978908aa8bbd993d192fd49a141`) and `.hexaemeron/design-evidence.json` (`abf112dbcff8508966d2dbdc49ca8bcf5180aa1cabfcefb14841363b84682392`, selected `harden-record`) were digested and matched the brief, and were left unedited. The three appended amendment blocks were reviewed only for whether the paths and formats they name exist in the tree: `plugins/brevitas/tests/run_tests.py` and `tests/run_tests.py` (with `--elenchus-report`) exist, `plugins/horos/tests/run_tests.py` does not yet exist and is step 2's to write, and `unittest-json-v1` is one of the three values in `elenchus.py:29` `REPORT_FORMATS`; whether the amended step 2 count (235 to 239) and file list are right is step 2's audit, not this one. `.horos/census.json` and `.horos/candidates.json`: unchanged, not regenerated.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-02 | low | tests/test_agent_instruction_corpus.py (`test_a_candidate_outside_the_design_record_is_refused`, `test_prover_selftest_exits_zero_and_writes_a_closed_report`); scripts/prove_agent_instruction_reconciliation.py:161 | Reproduced unchanged this round: inside the run worktree the root suite is `Ran 1207 tests in 92.396s`, `FAILED (failures=2)`, exit 1, exactly those two tests; in a detached clean snapshot of `72f0106f` (no `.hexaemeron/`) it is `Ran 1207 tests in 101.267s`, `OK`, exit 0. Same cause as round 1: the #1098 prover reads `.hexaemeron/design-evidence.json` at the repository root and this run's record occupies that path. Not this step's defect and not fixed on the stacked branch; carried to integration as a carryover issue. | accepted |<br>| S1-R1-03 | low | plugins/horos/docs/content-addressed-objects/runbook.md:180-236 | The controller answered the finding with three receipted amendments (`### Amendment -- 2026-09-05`) on the canonical `.hexaemeron/runbook.md`: step 2's Tests and Files replaced (runner `python3 plugins/horos/tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report file `.hexaemeron/elenchus-step-2.json`, Files gains `plugins/horos/tests/run_tests.py`); steps 3 and 4 get `python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, report files `.hexaemeron/elenchus-step-3.json` and `.hexaemeron/elenchus-step-4.json`. Step 1 keeps `report format unittest text output` because its rounds run under the original contract. The committed copy required verbatim by step 1's exit was 57 lines behind the canonical file (`cmp: EOF` at line 180), so each amendment's line "Step 1: entry holds; exit holds" was not true of the tree until this fix. Fixed at `72f0106f`: the canonical bytes copied over the committed copy, `cmp` exit 0, imprimatur on the copy `score 100.0/100 defects 0` exit 0, `horos.py check .` prints `boundary matches the tree` exit 0 with no `.horos` change (the file was already tracked). Elenchus on `72f0106f` under step 1's original runner (`python3 -m unittest discover -s plugins/horos/tests -t plugins/horos 2> {report}`, report file `.hexaemeron/elenchus-step-1.txt`, no `--report-format` because `unittest text output` is not accepted): `unguarded -- the commit changed no test files`. | fixed in 72f0106feeb2f2e6aa0b1ed8d2ef9a0c6d2d253d |<br><br>Leads not pursued: no new finding this round; the two rows above are prior items at their closing state. Whether `counts.files_walked` should leave the currency comparison, and whether the #1098 prover should find its design record by digest rather than path: both still as round 1 recorded, not designed here. The imprimatur cadence note on the runbook copy after the fix (`elenchus runnerx7`, `step 2x4`, `amendment --x3`, `why auditx3`, `steps touchedx3`, `still holdingx3`) under exit 0 is the amendment grammar, not a defect. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.20`, each given the one changed path, each printed `clean` and exited 0. Horos suite at `72f0106f`: `Ran 235 tests`, `OK`. `tests.test_boundary_currency` and `tests.test_marketplace_prose`: 31 of 31 OK; `.horos/boundary.json` still carries "digest of the file's own bytes" (78 occurrences). Every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all ten risk ids and dispositions, every named unchecked item, both finding ids with severities, file locations and statuses, every digest, count, exit code, line number and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 2, round 1 -- 2026-09-05T00:01:35Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: whole-file-read=not-applicable; shape-gate-first=reviewed; symlink-in-store=not-applicable; unreadable-object=reviewed; tamper-as-drift=reviewed; boundary-regeneration=reviewed; evidence-wording=reviewed; prose-reconciliation=reviewed; ledger-row=not-applicable; self-witness=not-applicable<br><br>Not checked: CI, named plainly -- `.github/workflows/repo.yml` and `tests/check-map-v1.json` were neither read nor run; only the local suites were executed. X-Ray, solidity-auditor and fizz: not run, because the `security_suite` receipt is a waiver (this run ships no Solidity). The step's diff `475e6c30..4cb8aa3d` is five paths (`.horos/boundary.json`, `plugins/horos/examples/README.md`, `plugins/horos/tests/run_tests.py`, `plugins/horos/tests/test_boundary.py`, `plugins/horos/tests/test_classify.py`) and touches nothing under `plugins/horos/skills/` (0 lines), as the selected design `harden-record` requires; `whole-file-read` and `symlink-in-store` are not-applicable because neither `digest_matches_name` nor `classify_file` changed and the step adds no test for either, `ledger-row` and `self-witness` because the version, the ledger and the evidence bundle are step 3's. The receipted `.hexaemeron/runbook.md` (SHA-256 `23c101bd34e18395a7d8d44b56d4eb99c9dd884ee951c66569a83a716c60226b`, the brief's `runbook_step.sha256`), `.hexaemeron/study.md` (`d6bd736548495d9deab7e006bcb9ce85d7f03978908aa8bbd993d192fd49a141`) and `.hexaemeron/design-evidence.json` (`abf112dbcff8508966d2dbdc49ca8bcf5180aa1cabfcefb14841363b84682392`, selected `harden-record`) were digested, matched the brief and were left unedited. The runner was reviewed against `plugins/brevitas/tests/run_tests.py` by `diff`: the only differences are the two docstrings, the temporary-file prefix (`.horos-report-`), and the argument parser (an optional positional plus `--elenchus-report`, exactly one report path accepted); the path confinement, `O_NOFOLLOW` directory walk, mode-0600 private temporary, hard-link publish and `MAX_REPORT_BYTES` (65536) are byte-identical to the original. Probed by hand: an absolute path outside the worktree, a symlinked parent directory and a symlinked target are each refused (`REPORT must stay inside the current worktree`, `REPORT parent is not a directory`, `REPORT target must not already exist`), an existing target is refused without touching it (`.hexaemeron/design-evidence.json` digest unchanged), and a fresh relative target is written at mode 0600 with `testsRun` 239. The `.horos/census.json` and `.horos/candidates.json` files are unchanged in the diff and were not regenerated separately. The runner's behaviour under a report directory the process cannot create (mode-0500 parent) was not exercised.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | low | plugins/horos/docs/content-addressed-objects/runbook.md:237-313 | The committed runbook copy carried three `### Amendment -- 2026-09-05` blocks while the receipted `.hexaemeron/runbook.md` carries five: during step 2 the controller receipted two more (amendment SHA-256 `a503d354798b7e57179c896088b7435ff83947700d2c3b515144b238e0e8de62` on Tests and Files, and `fd49000c51457e7e065431fb113284b0527f8fa7dc6485c816206067def42fd4` on Goal and Files, moving every `plugins/horos/skills/horos/SKILL.md` edit to step 3 because that file is byte-bound by `tests/fixtures/agent-instruction-v1/manifest.json`, and adding `.horos/boundary.json` to step 2's Files). `cmp` reported `EOF` on the copy, so step 1's exit (the copy verbatim) did not hold against the receipted runbook, the same shape as S1-R1-03. Fixed at `69724108`: the canonical bytes copied over the committed copy (77 lines added), `cmp` exit 0, imprimatur on the copy `score 100.0/100 defects 0` exit 0, `horos.py check .` prints `boundary matches the tree` exit 0 with no `.horos` change (the file was already tracked). The receipted `.hexaemeron/runbook.md` and `.hexaemeron/study.md` were not edited. Elenchus on `69724108` under the amended step 2 contract (`python3 plugins/horos/tests/run_tests.py --elenchus-report {report}`, `--report-format unittest-json-v1`, `--report-file .hexaemeron/elenchus-step-2.json`): `unguarded -- the commit changed no test files`, the honest value for a prose-only fix. | fixed in 69724108a69404e981f8d5d91a548e42fee51bf2 |<br><br>Leads not pursued: S1-R1-02 is carried unchanged, not re-found: inside the run worktree the root suite is `Ran 1207 tests in 95.256s`, `FAILED (failures=2)`, exit 1, exactly `test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`, and in a detached clean snapshot of `4cb8aa3d` (`git worktree add --detach`, no `.hexaemeron/`, removed and pruned afterwards) it is `Ran 1207 tests in 97.766s`, `OK`, exit 0; the cause is the #1098 prover reading this run's `.hexaemeron/design-evidence.json`, as round 1 recorded, and it stays a carryover for integration. The example README's tamper section says `check` "exits 1 and names the object"; run by hand it also prints a `.horos/boundary.json#counts` drift line (`bytes_content_addressed` 94 to 49) before `drift: store/objects/sha256/7d/7d2aa7ee1155c6102a2dbb74ff9efa27115cec234f2ea4555a0d3a92663d7e82: in the boundary but no longer evidenced by the tree`, `2 path(s) drifted`, exit 1, and `git checkout -- plugins/horos/examples/fixture/store` restores it; the README's sentence is true and was left alone. `test_boundary.py` binds `GIT = shutil.which("git")` for its `skipIf` but the helper runs the literal `"git"`; the two agree wherever the skip does not fire, so nothing was changed. Whether `counts.files_walked` should leave the currency comparison: still as round 1 recorded, not designed here. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.20`, each given the five changed paths, each printed `clean` and exited 0. Horos suite at `4cb8aa3d`: `python3 -m unittest discover -s plugins/horos/tests -t plugins/horos` `Ran 239 tests`, `OK` (235 to 239 as the amended Tests field expects), and `plugins/horos/tests/run_tests.py` `239/239 tests passed` writing `{"complete":true,"errors":0,"expectedFailures":0,"failures":0,"schema":"elenchus.unittest.v1","skipped":0,"testsRun":239,"unexpectedSuccesses":0}`. The four guard tests were each re-observed red against a rule broken in a scratch copy of `plugins/horos`: `digest_matches_name` returning `True` fails `test_a_tampered_store_object_is_named_as_drift`; dropping the `len(shards) != 1` clause fails `test_a_deeper_shard_path_stays_readable`; case-folding the algorithm segment fails `test_an_uppercase_algorithm_segment_stays_readable`; dropping `skipped_unreadable += 1` fails `test_an_unreadable_store_object_is_skipped_never_classified`; each mutant `Ran 1 test`, `FAILED (failures=1)`, and the unmutated four are `Ran 4 tests`, `OK`. `.horos/boundary.json`: `files_walked` 2523 to 2524 in `5b44ff00` for the new runner, `#counts` only; `horos.py check .` exit 0 and `horos.py scan . --write` moved no tracked byte under `.horos/`; the boundary still carries "digest of the file's own bytes" (78 occurrences); `tests.test_boundary_currency` and `tests.test_marketplace_prose`: 31 of 31 OK. Every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all ten risk ids and dispositions, every named unchecked item, the finding id with severity, file location and status, every digest, count, exit code, line number and quoted string, the exact verdict word, and the unpursued leads without changing any claim.
## Step 2, round 2 -- 2026-09-05T00:06:43Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: whole-file-read=not-applicable; shape-gate-first=reviewed; symlink-in-store=not-applicable; unreadable-object=reviewed; tamper-as-drift=reviewed; boundary-regeneration=reviewed; evidence-wording=reviewed; prose-reconciliation=reviewed; ledger-row=not-applicable; self-witness=not-applicable<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. The runner under a report directory the process cannot create, as round 1 left it. This round re-checked the tree with round 1's fix applied (`475e6c30..daac31bc`, eight paths) and ran inline by the controller, not by a delegated Warden, because the only change since round 1 is the prose fix `69724108` and the round 1 record.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: S1-R1-02 carried unchanged: in the run worktree the root suite is `Ran 1207 tests`, `FAILED (failures=2)`, the two `test_agent_instruction_corpus` tests; in a detached clean snapshot of `daac31bc` it is `Ran 1207 tests`, `OK`. `horos.py check .` at `daac31bc` prints `boundary matches the tree`, exit 0, with one advisory line `candidate drift: audit/rounds/fiat-ship-the-content-addressed-object-rule-whose-evi.synopsis.md: evidenced by the tree but missing from the boundary`; candidates never gate the exit code, as the step 1 rounds of the previous run recorded, and the synopsis stays readable by design. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.20`, each given the changed paths, each printed `clean` and exited 0; horos suite `Ran 239 tests`, `OK`; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all ten risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, and the unpursued leads without changing any claim.
## Step 3, round 1 -- 2026-09-05T00:24:12Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: whole-file-read=not-applicable; shape-gate-first=not-applicable; symlink-in-store=not-applicable; unreadable-object=not-applicable; tamper-as-drift=reviewed; boundary-regeneration=reviewed; evidence-wording=reviewed; prose-reconciliation=reviewed; ledger-row=reviewed; self-witness=reviewed<br><br>Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run; only the local suites were executed). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver (this run ships no Solidity). The step's diff `904937e2..5dd37120` is 15 paths and touches nothing under `plugins/horos/skills/horos/scripts/` (0 paths), as the selected design `harden-record` requires, so `whole-file-read`, `shape-gate-first`, `symlink-in-store` and `unreadable-object` are not-applicable: neither `content_addressed_algorithm`, `digest_matches_name` nor `classify_file` changed and the step adds no test; `tamper-as-drift` is reviewed only through the bundle's drift demonstration, re-run by hand and matching its three quoted lines and exit 1. The receipted `.hexaemeron/runbook.md` (SHA-256 `23c101bd34e18395a7d8d44b56d4eb99c9dd884ee951c66569a83a716c60226b`, the brief's second amendment `runbook_sha256`), `.hexaemeron/study.md` (`d6bd736548495d9deab7e006bcb9ce85d7f03978908aa8bbd993d192fd49a141`) and `.hexaemeron/design-evidence.json` (`abf112dbcff8508966d2dbdc49ca8bcf5180aa1cabfcefb14841363b84682392`, selected `harden-record`) were digested, matched the brief and were left unedited; the committed copy `plugins/horos/docs/content-addressed-objects/runbook.md` is `cmp` equal to the canonical file. The bundle's `2,550 paths` figure for the `widen-layouts` refusal and its six-gap enumeration for `record-only` were read against `resolve.py` (the cacache pattern at line 57, the two external boundaries at line 132) but the two resolvers were not re-run, so those two numbers are Mason's observation, not this round's. The `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json` descriptors were covered only by the stale-wording grep and `tests.test_marketplace_prose`, not read whole.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: S1-R1-02 carried unchanged, not re-found: in the run worktree the root suite is `Ran 1207 tests in 90.173s`, `FAILED (failures=2)`, exit 1, exactly `test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`; in a detached clean snapshot of `5dd37120` (`git worktree add --detach`, no `.hexaemeron/`, removed with `--force` and pruned afterwards) it is `Ran 1207 tests in 92.828s`, `OK`, exit 0; the cause is the #1098 prover reading this run's `.hexaemeron/design-evidence.json`, as step 1 round 1 recorded, and it stays a carryover for integration. The fixture-chain re-pin, which the amended Exit requires this record to speak to: the reviewed span (bytes 11183..12201 of `git show 904937e2:plugins/horos/skills/horos/SKILL.md`, the `### horos-boundary-check` promise, SHA-256 `0f2fb54b8f737de4b8ec42b88bdc6d9238f65de8525847c81381cbb83d0e91a2`) occurs exactly once in the edited file, at bytes 11633..12651 (+450, the file grew from 14,030 to 14,480 bytes), and the manifest's `span_sha256` is unchanged at that value while its whole-file `sha256` moved to `24f121a3f8929bd6ca2e39fe5e0caac5e289d955687232a1fc0bdff217925eaf`, the digest of the file at head; `model.json` changed only the source digest and the four bindings' `start`/`end` (11183/12201 to 11633/12651 and 11209/12199 to 11659/12649), `compact.wai` the same source digest and four `B` lines, `source-spans.json` the source digest and the span offsets. The token counts and parity responses in `tests/fixtures/agent-instruction-v1/evidence/measurement.json` and `evidence/parity.json` are a recorded observation carried across the re-pin; they were not re-measured at this step. A leaf-by-leaf comparison of `git show 904937e2:<path>` against the file at head: `measurement.json` 163 leaves, 12 changed, 0 added, 0 removed, all 19 `tokens`/`prompt_tokens`/`response` leaves identical, the 12 changes being `corpus_sha256`, the `correlation_id` on the record, its seven events and its summary, and the `canonical_model`/`compact` digests of the horos document; `parity.json` 522 leaves, 69 changed, 0 added, 0 removed, all 72 protected leaves identical, the 69 changes being `corpus_sha256`, the record's and each of the 18 results' `correlation_id`, each result's `source` and `compact` `job_id`, and the `compact` `input_sha256`/`prompt_sha256` on the six results that feed the horos document. The carry is honest because the bytes the counts describe did not move: the corpus digest changed only because the span's recorded offsets moved by +450 while the span's own bytes, and every other source, are unchanged, so a re-measurement would tokenise the same reviewed bytes; this is the procedure the repository used on 2026-09-04 and the bundle's closing section says the same. `python3 scripts/agent_instruction.py check --manifest tests/fixtures/agent-instruction-v1/manifest.json`: `run.summary` `"outcome":"accepted"`, `passed` 20, `refused` 0, `unknown` 0, exit 0; `python3 scripts/promise_machine.py coverage`: `clean: promises=95 coverage_rows=95 coverage_selected=95`, exit 0; the coverage register's seven digests match the seven re-pinned files. `ledger-row`: `plugins/horos/skills/horos/EVOLUTION.md` carries exactly one `horos-v11.3.3` row, axis `evolution`, revision `markdown-outline-extractor`, and its digest `f0da2b17da1d83eca42aace7a81216542e2f51bd17c189b48dfb9712a07ec925` equals sha256 of `{status}|{revision}|{current frontier}|{next Fiat job}\n` recomputed from the header fields with backticks stripped; the next job is `Ship the Markdown outline extractor the census names at 1.83 MB across 261 files with no boundary bytes and no map support.`, the job the `horos-v9.2.3` epoch row names, not `mature`; `plugins/horos/skills/horos/SKILL.md` line 5 reads `version: "11.3.3"`; `python3 -m unittest tests.test_evolution_contract` `Ran 10 tests`, `OK`. `prose-reconciliation`: `python3 -m unittest tests.test_marketplace_prose` `Ran 23 tests`, `OK`; a tree-wide grep for `still owes its`, `7,844,971`, `drafted content-addressed`, `Two held jobs` and `first job is done` outside `audit/`, `.hexaemeron/`, `plugins/horos/docs/marker-self-exclusion/` and `plugins/horos/docs/content-addressed-objects/` finds only the historical `horos-v10.3.3` ledger row; the `Current frontier` sentence is byte-identical in `SKILL.md`, `README.md`, `AGENTS.md` and the `EVOLUTION.md` header; `plugins/horos/.codex-plugin/plugin.json` and `FUTUREPROOFING.md` now say the store is bound by the digest of each file's own bytes and only the Markdown outline extractor remains. `evidence-wording`: `.horos/boundary.json` carries `digest of the file's own bytes` 78 times, one per `content_addressed` entry. `self-witness`: the bundle's `What witnesses the two layouts` section says the flat layout is witnessed by the OCI image layout specification and the shipped fixture only, that no home tree holds a flat store, and its inventory says the fixture's 94 bytes are not evidence a real flat store exists. `boundary-regeneration`: `horos.py check .` prints `boundary matches the tree`, exit 0; `scan . --write` moved nothing under `.horos/`; the diff's only boundary change is `files_walked` 2524 to 2525 for the new bundle. After this record was appended, `scan . --write` moved `.horos/candidates.json` alone (`bytes_blob` 17,505,515 to 17,515,728; the sibling synopsis's single-line-blob candidate entry 25,259 to 35,472 bytes), committed alongside the record, with `tests.test_boundary_currency` `Ran 8 tests`, `OK` and `horos.py check .` `boundary matches the tree`, exit 0. The bundle's figures against `.horos/boundary.json`: 78 entries, 7,850,052 bytes; `compound-v3-phase0-v0/release` 70 files 7,844,877 bytes, `proof-backed-state-v0/release` 6 files 5,081 bytes, `horos/examples/fixture/store` 2 files 94 bytes, all recomputed and equal; `store-hash-ms` 5 ms against the 250 ms gate, the resolver re-run this round printed `value` 5 with samples `5.0, 4.9, 4.5, 4.4, 4.4` ms (the bundle records Mason's samples `12.1, 4.7, 4.7, 4.7, 4.7`; the median rounds to the same 5) and rewrote `harden-record-store-hash-ms.json` to the same bytes, SHA-256 `07ba5e3e65b4fec56d54e1df202884ae991e509e539aaabf088f7d21aaf6c220`, the digest the design record pins; the other refused-candidate values (`added-layouts-unwitnessed` 1, `exact-match-misses` 78, `open-hardening-gaps` 4, `boundary-document-bytes` 12,587 against 37,014) match their pinned reports; commit `5d5aba71` (`horos: draft a content-addressed object rule`) exists. The SKILL.md rule paragraph's `refuses git's own object store` is true by construction rather than by the shape gate: `.git` is in `SKIPPED_DIR_NAMES` (`horos.py:127`) and is never walked, and the comment above `CONTENT_ADDRESSED_PARENTS` explains why a git store would fail the shape anyway; left as written. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.20`, each given the 15 changed paths, each printed `clean` and exited 0; horos suite `python3 -m unittest discover -s plugins/horos/tests -t plugins/horos` `Ran 239 tests`, `OK`; imprimatur exit 0 on `skills-content-addressed.md`, `SKILL.md`, `README.md`, `AGENTS.md`, `EVOLUTION.md` and `FUTUREPROOFING.md` (cadence notes only, no defect); the three step commits verify `G`, author Shoggoth, committer Dr Laurence E. Day. Every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all ten risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, offset, count, exit code, line number and quoted string, and the unpursued leads without changing any claim.
