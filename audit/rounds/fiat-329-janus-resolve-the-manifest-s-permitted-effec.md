## Step 1, round 1 -- 2026-08-28T09:15:19Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=not-applicable; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=not-applicable; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: fizz is not-applicable, because the step ships no Solidity in its diff (two documentation files and one Python launcher) and the pre-existing harness Solidity is unchanged, so no invariant fuzz suite is built or refreshed this round; also not exercised are hosted CI, the controller receipt, push and publication, the run's later-step behaviour (the manifest reader, manifest-to-gate resolution, the fixtures and the ledger row, none of which exist at this commit), and the internal comparison logic of gate 2 and gate 5 beyond confirming that the shipped tests' own assertions pass. The five not-applicable register items name exactly those unbuilt later-step mechanisms; vm-signature-drift is reviewed as declaration-only with keyExistsJson deliberately still absent and existing signatures matching the canonical Foundry Vm shapes.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the step's own new code carries no finding -- the two committed spec copies are byte-identical to the receipted study and runbook at SHA-256 124b39c3e3aa09247a555321bc725c9f9eae939916957f7dbd0f6b78b09dedac and 4efdb552775803b80a985c688c5e9ff5890774239053292c0faa722f461815c7, and run_forge_tests.py spawns forge test --junit with a pinned argv, no shell, a fixed cwd resolved from the file, writes only its single report-path argument, and fails closed (returns forge's code, or 1 with no file written) when forge's stdout does not parse as XML; the exit battery is green (launcher exit 0 writing JUnit XML that names 24 passing tests with 0 failures, forge test 24 passed, python3 -m unittest discover -s tests 14 passed OK, janus.py validate exit 0). The Pashov X-Ray and solidity-auditor pair was applied over the full harness the launcher drives (X-Ray over the ten-source logical scope, twelve attacker agents over all in-scope sources); they converged on latent trails in the pre-existing, unchanged gate engine (plugins/janus/harness/src/JanusHarness.sol), none of which fires a false pass at this commit and each of which belongs to the run's later manifest-resolution steps rather than this scaffold step's file scope, recorded here so they carry forward. First and highest-signal (eight of twelve agents, concretely traced): _gate1_hookStorageWithinScopes cannot attribute a hook's writes to its own storage, because _hookAttributed excludes the host-to-hook entry frame and a hook writes its own slots with no self-call, so a write with account == hookAddr matches no attributed call and a manifest that forbids hook-scope writes would be unenforceable; it is latent today because every shipped test passes [hook] in allowedWriteAccounts and no manifest-to-gate resolution exists yet, but it is the false-pass direction the study's own over-permit-by-category register line flags for the hardest look and it wants a guard test the moment step 4 builds the write set from a manifest that omits the hook. Second, gate 1 storage compares only the written account and never StorageWriteObs.slot, so the manifest's slot-level permittedStorageWrites are enforced only to account granularity; this is a documented non-goal (study section 3 defers slot-level matching, resolution to account granularity stated openly), so it is design not defect but the second-adapter author should read it as the stated boundary. Third, _findingsJson interpolates host and manifest into the interchange JSON raw while every finding field is escaped; both callers pass constant literals today so no injection path is reachable, but the serializer should escape all five strings before any host or manifest name derived from an author-supplied manifest reaches it. Fourth, _drive passes _endRecording(0) so Delta.gasUsed is permanently zero; gate 5 is sound only because it reads the Wildcat model's lastHookGasUsed(), so the recorder's own gas field is dead and a future adapter without that accessor would get no gas gate. Fifth, _hookValueMoved sums only native-ETH call value, which is zero for an ERC20 transferFrom, so the exported value primitive reads zero for the ValueRedirectHook and gate 2 catches the theft only through the per-test balance-delta assertions layered on top; the primitive should be documented ETH-only or driven off valueSnapshot deltas before a second adapter reuses it. Sixth and minor, unverified: _deltaHasEffects counts a StaticCall as an effect so the anti-vacuous guard could be met by a read-only threshold; _endRecording drops reverted accesses so a hook that swallows a forbidden call's revert leaves no trace; and cross-action re-entry (gate 6) is caught only by the host's own reentrancy guard, not by any harness-level gate. An initial "the adapter drops caller" trail was retracted after verification, because borrower is deliberately set to the adapter in setUp so discarding caller is sound for the current single-host modeled actions, though the caller seam matters for the future second adapter. No fix is applied this round; all trails concern pre-existing harness Solidity this scaffold step does not touch and carry forward to the steps that wire the manifest into the gate sets.

## Step 2, round 1 -- 2026-08-29T08:26:15Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves -- no non-test contract implements AccountResolver at this commit, and neither WildcatConformance.t.sol nor HostileHooks.t.sol imports ManifestReader, so no conformance verdict depends on the manifest and the resolved sets reach no gate. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which the schema requires and step 6 owns; slot-level write enforcement, which needs a slot dimension on the gate side; transient storage as an effect class, which the recorder does not observe; hosted CI, the controller receipt, push and publication. Coverage percentages are not reported: the forge coverage run completed, but ran while transient audit probe files were present under test/, so its per-file figures do not measure the committed tree. The bundled analyze_git_security.py ran with --src-dir src from the harness directory while git's toplevel is the monorepo root, so its path filter matched nothing and reported squashed_import with 0 source-touching commits; that is a scoping artefact and nothing is concluded from it. enumerate.sh produced no nSLOC because its counter uses GNU-only grep -P; the 1028 nSLOC figure was recomputed portably.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | plugins/janus/harness/src/ManifestReader.sol | _resolveCalls admitted kind "call" and kind "delegatecall" into one flat allowedCallTargets array and ResolvedThreshold carried no kind dimension, so a manifest granting a plain call to a target also admitted a delegatecall to it; a delegatecall runs the target's code in the hook's own storage context, so the shipped manifest's roleProvider.validateCredential call permit would hand the provider the hook's entire state, including the monotone known-lender bit the exit path depends on | fixed in 2b5c6dc3 |
| S2-R1-02 | medium | plugins/janus/harness/src/ManifestReader.sol | _thresholdByAction returned the first threshold whose action matched and stopped scanning, and neither the schema nor janus.py validate rejects a manifest naming one action twice, so array position decided which permission set was in force -- the positional dependence selection by name exists to remove; a prepended permissive duplicate silently shadowed a restrictive one and janus.py validate reported the manifest valid | fixed in 2b5c6dc3 |
| S2-R1-03 | low | plugins/janus/harness/src/ManifestReader.sol | _symbolOf returns the bytes before the first 0x2E, so a target or slot whose first character is "." yielded the empty account symbol, which _resolveSymbol then passed to the adapter as though it were a name; whether that failed closed was the adapter's decision rather than the reader's, and an adapter answering to the empty name admitted its address | fixed in 2b5c6dc3 |
| S2-R1-04 | medium | plugins/janus/harness/src/ManifestReader.sol | resolution is account-granular: _resolveStorageWrites reads entry.slot only under scope external and only for its symbol prefix, so a manifest naming lenderStatus[lender] resolves to the whole hook account, while StorageWriteObs does carry slot and the header claimed the reader "never returns a default, shrunken, or widened set" without qualification | accepted: account granularity is the non-goal step 1 round 1 already stated as its second carried trail, and enforcing slots needs a slot dimension on the gate side that belongs to the wiring step; the header comment now states the granularity boundary explicitly rather than reading stricter than the code is |

Leads not pursued: each of the six trails step 1 round 1 carried forward was judged against this diff. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, now bears directly on this step and is why S2-R1-01 is rated high: the reader builds a write set from a manifest for the first time, so a delegatecall admitted under a call permit lands its writes at the hook's own account, which gate 1's storage half can never condemn -- the two gaps compose, and 8 of the 12 attacker agents reached the composed path independently. Not fixed here: the guard belongs in JanusHarness.sol, which this step does not touch. Trail 2, account-only storage comparison, is the same boundary as S2-R1-04 and is recorded there. Trail 3, _findingsJson interpolating host and manifest raw while finding fields are escaped, still bears: 5 agents executed it, one producing the literal file {"host":"a","sequences":0,"x":"","manifest":"m","sequences":5,"findings":[...]} with two injected keys and a duplicate sequences, another showing scripts/janus.py render_markdown carries the same unescaped host into the report header so an unterminated <!-- hides the violations table; the reader does not touch that serializer and no manifest-derived string reaches it yet, so it carries forward unchanged. Trail 4, _drive passing _endRecording(0) so Delta.gasUsed is permanently 0, and trail 5, _hookValueMoved summing native value only so an ERC20 drain reads 0, both still bear and both remain in JanusHarness.sol; agents added two facts to trail 4 -- lastHookGasUsed survives an action that calls no hook (24215, then 1100, then 1100 across deposit, queueWithdrawal, executeWithdrawal) and rolls back when a hook burns 8393189 gas against a 2000000 budget then reverts, so gate 5's figure can belong to a different action. Trail 6's minor items are unchanged, and one is now contradicted: Delta.taken is written and never read, so the flag meant to stop an unrecorded threshold reading as clean is inert. Outside this diff, agents reported and this record does not pursue: WildcatHostModel.setHook has no access control and no nonReentrant, so a hook can repoint the binding every gate attributes through from inside its own callback, contradicting the shipped manifest's own liveness.uninstall claim; executeWithdrawal never decrements scaledTotalSupply, so scaledTotalSupply == sum(scaledBalanceOf) + scaledPendingWithdrawals holds through deposit, transfer and queueWithdrawal and breaks on the exit; WildcatHostAdapter.driveAction discards its caller argument, so every driven sequence runs as the adapter, which both suites set as the borrower; categoryOf classifies address(0) as RoleProvider because roleProvider is unvalidated and is address(0) in both suites; the value-returning hook callback lets a hook rewrite the host's APR and reserve ratio to (10000, 10000) with no call, no write and no value movement, passing every gate; and the shipped manifest permits a state-changing roleProvider call while enumerating no external write scope for it, so a hook using its own permitted call would fail gate 1's storage half. None of these is in ManifestReader.sol, Vm.sol or ManifestReader.t.sol. Negative results: 12 hand-written adversarial probes against the reader all confirmed fail-closed behaviour before any fix -- a staticcall entry admits nothing even under an adapter resolving every name to a live address, absent effect arrays yield empty sets rather than widened ones, an unknown call kind and an unknown storage scope both revert rather than being skipped, action matching is exact with no prefix fallback, a malformed earlier threshold denies rather than widens, a unicode symbol is not mis-split because no UTF-8 continuation byte is 0x2E, and a caller catching the revert observes no partially built set. The vm-signature-drift line is clean: the added keyExistsJson(string calldata json, string calldata key) external view returns (bool) matches forge-std 1.7.1 Vm.sol:1104 byte for byte, checked against a freshly initialised forge project rather than from memory. The Elenchus verdict is inconclusive rather than guarded for a mechanical reason: the runner contract reverts the whole source file, and because S2-R1-01's fix widens ResolvedThreshold with allowedDelegateTargets, the new guard tests no longer compile against the parent's struct, so forge exits non-zero, run_forge_tests.py writes no report by design, and the guard check cannot distinguish a compile failure from a fired guard. Each of the 6 guards was instead verified by reverting its own behavioural hunk alone; each failed without its fix -- test_delegatecall_permit_does_not_admit_a_plain_call with "the delegatecall entry is admitted as one", test_duplicate_action_name_reverts and both empty-symbol tests with "next call did not revert as expected" -- and passed with it. The fizz invariant suite was built and campaigned but is not committed, because the step's Files list is closed to ManifestReader.sol, Vm.sol and ManifestReader.t.sol; persisting it needs a runbook amendment. Its campaigns ran twice against real engines: before the fix, Echidna 2.3.3 at 100040 calls with 6 properties passing and Medusa 1.5.1 at 132622 calls with 16 of 16 passing; after the fix, with 2 properties added for the call-kind and empty-symbol guards, Echidna at 100037 calls with 7 of 7 passing and Medusa at 184966 calls with 18 of 18 passing. The pre-fix campaign did not catch S2-R1-01, which is a coverage limitation rather than a clean run: the property asserted that admitted targets equalled the call plus delegatecall count, which is exactly what the unfixed code did, so the property encoded the defect instead of falsifying it. X-Ray ran as a named full recomputation (plan mode full, reason cache-missing, no prior cache existed) over the exact eleven-source current inventory at candidate digest 3cfee210c00fb8ddb1334ff4bf2705d615419daa0a667038ec7bba736581ab8c, with all 4 outputs regenerated, bound and promoted; its working material is operator-owned and stays out of this record and the tree. The solidity-auditor pass ran all 12 attacker roles to completion over the 11 in-scope sources; deduplication covered 31 unique (contract, function) pairs from the raw results, every one of which survives into this record or into the trails above. Suites at exit: forge test 42 passed 0 failed (36 at entry, the 12 ManifestReader tests plus 6 new guards), python3 -m unittest discover -s tests under plugins/janus 14 passed OK, the repository invariant suite 460 passed OK after regenerating the portable Promise Machine runtime and the Horos boundary, which mirror the two changed files by digest, and janus.py validate exit 0 on the shipped manifest.

## Step 2, round 2 -- 2026-08-29T09:45:23Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves, unchanged from round 1 -- no non-test contract implements AccountResolver and no gate consumes ResolvedThreshold, so allowedDelegateTargets is populated and unread exactly as every other field is, and the widening is not yet a gate. Gate 1 also takes one address[] and tests only kind != StaticCall, so it carries no dimension for the call/delegatecall split and the wiring step must add one or reinstate the fold; that is recorded, not fixed, because JanusHarness.sol is outside this step. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement, which needs a slot dimension on the gate side; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Coverage percentages are not reported: forge coverage was not run against this tree, so no per-file figure would measure it. Multi-byte whitespace is deliberately outside the blank-symbol guard, so a symbol of U+00A0 alone still reaches the adapter as a name; that is the stated grammar boundary, not an unknown. Whether Echidna or Medusa will ever implement the JSON cheatcodes is not established, only that neither does at 2.3.3 and 1.5.1.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | high | plugins/janus/harness/adapters/ManifestFuzz.sol | the suite 168ec6d1 committed proved nothing under either engine: neither Echidna 2.3.3 nor Medusa 1.5.1 implements keyExistsJson, parseJsonUint or parseJsonString, so every generated manifest reverted with empty return data before the reader resolved anything, resolveSuccesses stayed 0, _check was never entered, and GL01 to GL07 -- each a negated ghost flag only _check sets -- held untested; reintroducing S2-R1-01's exact fold left Echidna at 7 of 7 passing, exit 0, over 100056 calls and Medusa at 22 passing, so GL04 did not falsify the defect its commit message named; GL07 had a second hole surviving the engine question, because FuzzResolver did not answer to the empty name, so the adapter refused before the reader's own guard was reached and deleting that guard under Foundry still left GL07 passing | fixed in 8264206e |
| S2-R2-07 | high | plugins/janus/harness/adapters/ManifestFuzz.sol | every property was a cardinality test or a != address(0) test, so nothing compared a resolved address against the name behind it: a reader writing address(0xDEAD) into every admitted call slot left all nine properties green over a full invariant run; GL02 could not fail either, because FuzzResolver derived ok as a != address(0) so UnresolvableSymbol always fired before SymbolResolvesToZero and deleting the reader's zero-address guard left everything green; and _check read t.valueRecipients[i] under a bound taken from t.valueAssets.length ten lines before comparing the two lengths, so a reader returning them at different lengths panicked 0x32 inside the try and rolled the draw back, leaving GL03 permanently green | fixed in be6987b7 |
| S2-R2-02 | medium | .agents/skills/promise-machine/runtime/MANIFEST.json | 168ec6d1 added the fuzz suite, its two engine configs and the campaign launcher without regenerating the portable Promise Machine runtime or the Horos boundary that mirror them by digest, so the repository invariant suite was red at that commit -- 460 tests, 1 failure, test_generated_runtime_is_current, missing=[adapters/ManifestFuzz.sol, adapters/echidna/echidna.yaml, adapters/medusa/medusa.json, scripts/run_fuzz_campaigns.py] changed=[.horos/boundary.json, MANIFEST.json] -- and the tree round 1 recorded as green had stopped being so before this round opened | fixed in 796c5ed4 |
| S2-R2-06 | medium | plugins/janus/harness/src/ManifestReader.sol | the dot grammar was applied to a value movement's asset and recipient, which the schema gives no suffix to strip, unlike a call target's function name and an external slot's expression, both of which it documents; against an adapter holding both names "USDC.e" resolved to canonical USDC at 0xC0 rather than the bridged token at 0xCE, a value permit bound to an asset the manifest did not name and chosen by the reader | fixed in be6987b7 |
| S2-R2-03 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R1-03's guard tested bytes(symbol).length == 0 only, so a target of " " or " .getCredential", and an external slot of a tab followed by ".counter[market]", still reached the adapter as a name -- the class the guard exists to close, since neither is a name the manifest author wrote and an adapter that trims before looking a name up admits it; verified against an adapter answering to every name, which admitted 0xBEEF for all three | fixed in 8264206e |
| S2-R2-05 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R2-03's replacement enumerated four whitespace bytes, which is three short: 0x0B, 0x0C and 0x00 are all writable into a manifest as JSON escapes, none is a name, and each still reached the adapter as one, admitted as 0xBEEF; janus.py validate accepts all three, so the reader is the only defence | fixed in be6987b7 |
| S2-R2-04 | low | docs/janus-manifest-resolution/runbook.md | the committed runbook carries two amendments, both dated 2026-08-28, and neither names adapters/ManifestFuzz.sol, adapters/echidna/echidna.yaml, adapters/medusa/medusa.json or scripts/run_fuzz_campaigns.py; step 2's committed Files clause is still closed to ManifestReader.sol, Vm.sol and ManifestReader.t.sol, so as committed the spec does not name the four files 168ec6d1 added, which is what the 2026-08-28 amendment requiring the committed copy to match the receipted runbook at each step's final push exists to prevent | reported, not fixed: a live run's runbook is controller-pinned and amended through hexctl, never by hand, so this is Fiat's to reconcile against its own receipted copy, which this round cannot read |
| S2-R2-08 | low | plugins/janus/scripts/janus.py | J009 tests a raw string against the six literals {"*", "any", "all", "ANY", "ALL", "*.*"}, while the reader resolves only the prefix, so "*.transfer", "any.foo", "all.x" and "ALL.y" all validate and yield the symbols "*", "any", "all" and "ALL", each admitted against a catch-all adapter; "*.*" being in the set shows one dotted spelling was anticipated and the rest left open | reported, not fixed: the wildcard rule is the schema's and the validator's, both of which step 6 owns, and duplicating the literal list in Solidity would put the rule in two places |
| S2-R2-09 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R1-02's fix removed the early return, so .action is now parsed on every threshold and a later malformed threshold aborts resolution of an earlier well-formed action, with a raw 164-byte cheatcode revert rather than a named error; a genuine regression from that fix set, in the fail-closed direction | accepted: detecting that an action is stated twice requires reading every threshold, so the coupling is the cost of selecting by name rather than by position, and refusing is the safe direction |

Leads not pursued: the five fix-focused probes were executed, not read. Consumers: ResolvedThreshold is read in exactly two places, ManifestReader.t.sol and ManifestFuzz.sol, both reading allowedCallTargets and allowedDelegateTargets; no reader of the old flat set survives anywhere. Duplicate refusal fails closed in every arrangement and by name -- adjacent [deposit,deposit], the duplicate last of four [withdraw,deposit,withdraw,deposit], first and last [deposit,withdraw,deposit], and triple -- all with selector 0xe8099334 DuplicateActionInManifest("deposit"), while [withdraw] alone and [] refuse with the distinct 0x2ebd6b65 ActionNotInManifest("deposit"). Refusal order and partial sets: an unknown kind on a resolvable symbol gives 0xc1a04a5d UnknownCallKind("weird"); the same entry with an empty symbol gives 0x7b62c6b4 EmptyAccountSymbol(""), the grammar check preceding the kind check; a duplicated action whose first copy also carries an empty symbol gives DuplicateActionInManifest, the manifest-level scan completing before any symbol work; and a well-formed entry ahead of a malformed one still reverts whole, both entry points being external view, so a caller catching the revert observes no partially built set. No fix reordered a stricter check behind a looser one: _isBlank sits where the length == 0 test sat. Constructions still admitted are the documented grammar, not gaps -- "roleProvider." resolves to "roleProvider", and U+FF0E does not split because no UTF-8 continuation byte is 0x2E, so a fullwidth-stop name stays one symbol the adapter may refuse. Both properties the round was told to test by reintroducing their defects failed to falsify as committed, which is S2-R2-01. With the fold reintroduced into _resolveCalls, Echidna 2.3.3 finished 100056 calls at 7 of 7 passing and exit 0, and Medusa 1.5.1 reported 22 passing, its only failure this audit's own instrumentation probe. Three probes on that tree showed why: echidna_PROBE_never_attempts failed, so fuzzResolve was reached; echidna_PROBE_never_succeeds passed over 100043 calls, so resolveSuccesses never left zero; echidna_PROBE_revert_data_empty passed, so every revert carried zero bytes rather than one of the reader's seven named errors. A standalone CheatProbe pinned the cause under both engines: the cheatcode address carries code, and raw calls to keyExistsJson(string,string), parseJsonUint and parseJsonString all fail. Under Foundry the generator resolves 353 of 2048 draws, first at draw 1, before the fix, and 254 of 4096, first at draw 20, after it. Nine mutants were run against the finished tree, each reintroducing exactly the defect its guard names, and each was caught: the call/delegatecall fold fails GL04 and test_delegatecall_permit_does_not_admit_a_plain_call; restoring bytes(symbol).length == 0 fails GL07 and three blank-symbol tests; deleting the blank guard fails GL07 and five tests; narrowing it back to the four-byte test fails test_control_byte_only_symbols_revert; returning the first action match fails GL08 and test_duplicate_action_name_reverts; deleting the zero-address guard fails GL02 and test_zero_address_resolution_reverts; rewriting every admitted call target to address(0xDEAD) fails GL09 and four tests; reapplying the dot grammar to the value path fails GL09 and both dotted-asset tests; returning value recipients one short fails GL00 and two tests that panic on the reader's own out-of-bounds return. Control: 52 tests, 0 failures. Three qualifications. Three of the ten properties got no mutant of their own this round -- GL01 on set widening, GL05 on the gas budget, and GL06 on an unresolvable symbol or unknown kind or scope -- so they are exercised by the campaign and by the unit tests but are not shown here to falsify anything. GL03 is still the weakest of the ten: the length comparison now precedes every paired read, so it can fire, but the reader allocates both value arrays from one length, so the mutant that shortens one is caught by GL00 and the unit tests rather than by GL03 itself. And GL08 is new because S2-R1-02 had no property at all: the generator emitted one threshold, so neither DuplicateActionInManifest nor ActionNotInManifest was ever generated. Current fact: the fuzz suite is committed, at plugins/janus/harness/adapters/ManifestFuzz.sol with adapters/echidna/echidna.yaml, adapters/medusa/medusa.json and plugins/janus/scripts/run_fuzz_campaigns.py. Round 1's record says it was uncommitted, which was true when that round was receipted and is left untouched; S2-R2-04 records that the committed runbook does not yet name those paths. The campaigns at this commit exit non-zero by design, which is the report and not a regression: Echidna exits 1 with GL00 failed and GL01 to GL09 passing over 100042 calls, 4648 unique instructions, corpus 17, seed 3527166591070649310; Medusa exits 7 with 22 passing and GL00 the single failure. Both outcomes are stated in run_fuzz_campaigns.py and echidna.yaml. At 168ec6d1, before any change, the same campaigns were green and meaningless: Echidna 100027 calls, 7 of 7 passing, exit 0, 3621 unique instructions, corpus 12, seed 3079658920421933649; Medusa 18 of 18 passing, exit 0, 160364 calls. All six trails step 1 round 1 carried forward are unchanged and this round adds no fact to any of them. No commit in this round touches JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol or WildcatHostAdapter.sol, and all four were re-read in full against the current tree. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, still composes with anything that widens a write set, and it is also what stops gate 1's storage half from compensating for the missing call-kind dimension recorded above. Trail 2, account-only storage comparison, is unchanged in code, and the reader header's granularity paragraph from round 1 was not edited. Trails 3, 4, 5 and 6 are unchanged, Delta.taken still written and never read. The solidity-auditor pass ran all 12 attacker roles to completion over the 12 in-scope sources at 56056d3f, with 17 unique (contract, function) tuples deduplicated and all 17 carried into this record or the trails; its model-selection turn was skipped because AskUserQuestion is unavailable in this runtime, so no model was passed. It supplied S2-R2-05 through S2-R2-09, each of which this round reproduced by execution before fixing or accepting it. Its own negative results, confirmed here: the three round 1 fixes are intact and the rebuilt suite holds them; a staticcall widened into the state-changing set fails GL04; positional .thresholds[0] selection is caught, by GL08 rather than GL05; no JSON injection is reachable through the generator, because _sym, _kind and _scope emit no quote or backslash; _resolveCalls array sizing and _shrink leak no stale tail. Its leads this round does not pursue: gasBudget is unbounded above at schema, validator and reader, so 2**256-1 validates; janus.py validate accepts blank symbols, dotted wildcards, duplicate action names and NUL bytes, every case the reader refuses; both gate-5 sites in the test suites read .thresholds[0].gasBudget positionally, the same positional dependence the reader was written to remove; and the shipped deposit threshold permits kind:"call" to roleProvider with no external write scope, so a hook using its own call permit would fail gate 1's storage half, masked today because HonestAccessHook makes no external call. X-Ray ran twice over the exact twelve-source current inventory, round 1's eleven plus adapters/ManifestFuzz.sol: first as a named full recomputation, plan mode full, reason cache-missing, no prior cache, at candidate digest 20ad4225dbda2cd02abf064e69b2b33ba79eb951f51d6444c41f38f4d9db3800; then, after the round's second fix changed two sources, plan mode incremental, reason source-drift, dirty adapters/ManifestFuzz.sol and src/ManifestReader.sol, ten reusable, none removed and none reverse-invalidated, at candidate digest 58ee123d93d54609f07056dd9ad1e13c08152010cbc23a93127f81ebcd983e21. All four outputs were regenerated, bound and promoted in both runs; no global synthesis or finding was reused. The working material is operator-owned and stays out of this record and the tree. The Elenchus verdict is inconclusive for the same mechanical reason as round 1, not a relabelling: the runner contract reverts the whole source file, and each of this round's two fix commits changes ManifestFuzz.sol's fuzzResolve signature and ghost names alongside ManifestReader.sol, so ManifestReader.t.sol does not compile against either parent, forge exits non-zero, run_forge_tests.py writes no report by design, and tmp/elenchus/fiat-329-step-2-r2.xml was never created. Run against 8264206e and again against be6987b7, the runner returned inconclusive both times. One reading is recorded so it is not mistaken later: run with --ref HEAD while HEAD was 1f4c3aeb, a resync commit whose only content is generated mirrors, the runner returned passed, naming .agents/skills/promise-machine/runtime/plugins/janus/harness/test/ManifestReader.t.sol -- that verdict is about the mirror's copy of the test file and its parent, and carries no evidence about either fix. The nine guards were verified individually by the mutation battery above instead. Suites at exit, at 1f4c3aeb: forge test 52 passed 0 failed across 7 suites -- 25 ManifestReaderTest, 3 ManifestFuzzInvariantTest, 9 WildcatConformanceTest, 7 HostileHooksTest, 1 ReentryInvariantTest, 5 StateDeltaRecorderTest, 2 ScaffoldTest -- against the 42 this round expected, the 10 new being 7 reader guard tests and 3 in the invariant contract; python3 -m unittest discover -s tests under plugins/janus 14 passed OK; the repository invariant suite 460 passed OK, which needed the Horos scan and the portable-runtime sync alternated to a fixpoint rather than run once each, because the boundary entry counts the runtime mirror the sync writes -- .agents/skills/promise-machine/runtime/ settled at 931 files and 20502420 bytes, up from 927 and 20479946; janus.py validate exit 0 on the shipped manifest; git status clean, with no corpus or ignored artefact staged and both engine corpus directories matching .gitignore's **/corpus/.
