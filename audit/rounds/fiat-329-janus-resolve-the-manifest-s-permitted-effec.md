## Step 1, round 1 -- 2026-08-28T09:15:19Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=not-applicable; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=not-applicable; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: fizz is not-applicable, because the step ships no Solidity in its diff (two documentation files and one Python launcher) and the pre-existing harness Solidity is unchanged, so no invariant fuzz suite is built or refreshed this round; also not exercised are hosted CI, the controller receipt, push and publication, the run's later-step behaviour (the manifest reader, manifest-to-gate resolution, the fixtures and the ledger row, none of which exist at this commit), and the internal comparison logic of gate 2 and gate 5 beyond confirming that the shipped tests' own assertions pass. The five not-applicable register items name exactly those unbuilt later-step mechanisms; vm-signature-drift is reviewed as declaration-only with keyExistsJson deliberately still absent and existing signatures matching the canonical Foundry Vm shapes.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the step's own new code carries no finding -- the two committed spec copies are byte-identical to the receipted study and runbook at SHA-256 124b39c3e3aa09247a555321bc725c9f9eae939916957f7dbd0f6b78b09dedac and 4efdb552775803b80a985c688c5e9ff5890774239053292c0faa722f461815c7, and run_forge_tests.py spawns forge test --junit with a pinned argv, no shell, a fixed cwd resolved from the file, writes only its single report-path argument, and fails closed (returns forge's code, or 1 with no file written) when forge's stdout does not parse as XML; the exit battery is green (launcher exit 0 writing JUnit XML that names 24 passing tests with 0 failures, forge test 24 passed, python3 -m unittest discover -s tests 14 passed OK, janus.py validate exit 0). The Pashov X-Ray and solidity-auditor pair was applied over the full harness the launcher drives (X-Ray over the ten-source logical scope, twelve attacker agents over all in-scope sources); they converged on latent trails in the pre-existing, unchanged gate engine (plugins/janus/harness/src/JanusHarness.sol), none of which fires a false pass at this commit and each of which belongs to the run's later manifest-resolution steps rather than this scaffold step's file scope, recorded here so they carry forward. First and highest-signal (eight of twelve agents, concretely traced): _gate1_hookStorageWithinScopes cannot attribute a hook's writes to its own storage, because _hookAttributed excludes the host-to-hook entry frame and a hook writes its own slots with no self-call, so a write with account == hookAddr matches no attributed call and a manifest that forbids hook-scope writes would be unenforceable; it is latent today because every shipped test passes [hook] in allowedWriteAccounts and no manifest-to-gate resolution exists yet, but it is the false-pass direction the study's own over-permit-by-category register line flags for the hardest look and it wants a guard test the moment step 4 builds the write set from a manifest that omits the hook. Second, gate 1 storage compares only the written account and never StorageWriteObs.slot, so the manifest's slot-level permittedStorageWrites are enforced only to account granularity; this is a documented non-goal (study section 3 defers slot-level matching, resolution to account granularity stated openly), so it is design not defect but the second-adapter author should read it as the stated boundary. Third, _findingsJson interpolates host and manifest into the interchange JSON raw while every finding field is escaped; both callers pass constant literals today so no injection path is reachable, but the serializer should escape all five strings before any host or manifest name derived from an author-supplied manifest reaches it. Fourth, _drive passes _endRecording(0) so Delta.gasUsed is permanently zero; gate 5 is sound only because it reads the Wildcat model's lastHookGasUsed(), so the recorder's own gas field is dead and a future adapter without that accessor would get no gas gate. Fifth, _hookValueMoved sums only native-ETH call value, which is zero for an ERC20 transferFrom, so the exported value primitive reads zero for the ValueRedirectHook and gate 2 catches the theft only through the per-test balance-delta assertions layered on top; the primitive should be documented ETH-only or driven off valueSnapshot deltas before a second adapter reuses it. Sixth and minor, unverified: _deltaHasEffects counts a StaticCall as an effect so the anti-vacuous guard could be met by a read-only threshold; _endRecording drops reverted accesses so a hook that swallows a forbidden call's revert leaves no trace; and cross-action re-entry (gate 6) is caught only by the host's own reentrancy guard, not by any harness-level gate. An initial "the adapter drops caller" trail was retracted after verification, because borrower is deliberately set to the adapter in setUp so discarding caller is sound for the current single-host modeled actions, though the caller seam matters for the future second adapter. No fix is applied this round; all trails concern pre-existing harness Solidity this scaffold step does not touch and carry forward to the steps that wire the manifest into the gate sets.

## Step 2, round 1 -- 2026-08-29T08:26:15Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves -- no non-test contract implements AccountResolver at this commit, and neither WildcatConformance.t.sol nor HostileHooks.t.sol imports ManifestReader, so no conformance verdict depends on the manifest and the resolved sets reach no gate. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which the schema requires and step 6 owns; slot-level write enforcement, which needs a slot dimension on the gate side; transient storage as an effect class, which the recorder does not observe; hosted CI, the controller receipt, push and publication. Coverage percentages are not reported: the forge coverage run completed, but ran while transient audit probe files were present under test/, so its per-file figures do not measure the committed tree. The bundled analyze_git_security.py ran with --src-dir src from the harness directory while git's toplevel is the monorepo root, so its path filter matched nothing and reported squashed_import with 0 source-touching commits; that is a scoping artefact and nothing is concluded from it. enumerate.sh produced no nSLOC because its counter uses GNU-only grep -P; the 1028 nSLOC figure was recomputed portably.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | high | plugins/janus/harness/src/ManifestReader.sol | _resolveCalls admitted kind "call" and kind "delegatecall" into one flat allowedCallTargets array and ResolvedThreshold carried no kind dimension, so a manifest granting a plain call to a target also admitted a delegatecall to it; a delegatecall runs the target's code in the hook's own storage context, so the shipped manifest's roleProvider.validateCredential call permit would hand the provider the hook's entire state, including the monotone known-lender bit the exit path depends on | fixed in 2b5c6dc3 |
| S2-R1-02 | medium | plugins/janus/harness/src/ManifestReader.sol | _thresholdByAction returned the first threshold whose action matched and stopped scanning, and neither the schema nor janus.py validate rejects a manifest naming one action twice, so array position decided which permission set was in force -- the positional dependence selection by name exists to remove; a prepended permissive duplicate silently shadowed a restrictive one and janus.py validate reported the manifest valid | fixed in 2b5c6dc3 |
| S2-R1-03 | low | plugins/janus/harness/src/ManifestReader.sol | _symbolOf returns the bytes before the first 0x2E, so a target or slot whose first character is "." yielded the empty account symbol, which _resolveSymbol then passed to the adapter as though it were a name; whether that failed closed was the adapter's decision rather than the reader's, and an adapter answering to the empty name admitted its address | fixed in 2b5c6dc3 |
| S2-R1-04 | medium | plugins/janus/harness/src/ManifestReader.sol | resolution is account-granular: _resolveStorageWrites reads entry.slot only under scope external and only for its symbol prefix, so a manifest naming lenderStatus[lender] resolves to the whole hook account, while StorageWriteObs does carry slot and the header claimed the reader "never returns a default, shrunken, or widened set" without qualification | accepted: account granularity is the non-goal step 1 round 1 already stated as its second carried trail, and enforcing slots needs a slot dimension on the gate side that belongs to the wiring step; the header comment now states the granularity boundary explicitly rather than reading stricter than the code is |

Leads not pursued: each of the six trails step 1 round 1 carried forward was judged against this diff. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, now bears directly on this step and is why S2-R1-01 is rated high: the reader builds a write set from a manifest for the first time, so a delegatecall admitted under a call permit lands its writes at the hook's own account, which gate 1's storage half can never condemn -- the two gaps compose, and 8 of the 12 attacker agents reached the composed path independently. Not fixed here: the guard belongs in JanusHarness.sol, which this step does not touch. Trail 2, account-only storage comparison, is the same boundary as S2-R1-04 and is recorded there. Trail 3, _findingsJson interpolating host and manifest raw while finding fields are escaped, still bears: 5 agents executed it, one producing the literal file {"host":"a","sequences":0,"x":"","manifest":"m","sequences":5,"findings":[...]} with two injected keys and a duplicate sequences, another showing scripts/janus.py render_markdown carries the same unescaped host into the report header so an unterminated <!-- hides the violations table; the reader does not touch that serializer and no manifest-derived string reaches it yet, so it carries forward unchanged. Trail 4, _drive passing _endRecording(0) so Delta.gasUsed is permanently 0, and trail 5, _hookValueMoved summing native value only so an ERC20 drain reads 0, both still bear and both remain in JanusHarness.sol; agents added two facts to trail 4 -- lastHookGasUsed survives an action that calls no hook (24215, then 1100, then 1100 across deposit, queueWithdrawal, executeWithdrawal) and rolls back when a hook burns 8393189 gas against a 2000000 budget then reverts, so gate 5's figure can belong to a different action. Trail 6's minor items are unchanged, and one is now contradicted: Delta.taken is written and never read, so the flag meant to stop an unrecorded threshold reading as clean is inert. Outside this diff, agents reported and this record does not pursue: WildcatHostModel.setHook has no access control and no nonReentrant, so a hook can repoint the binding every gate attributes through from inside its own callback, contradicting the shipped manifest's own liveness.uninstall claim; executeWithdrawal never decrements scaledTotalSupply, so scaledTotalSupply == sum(scaledBalanceOf) + scaledPendingWithdrawals holds through deposit, transfer and queueWithdrawal and breaks on the exit; WildcatHostAdapter.driveAction discards its caller argument, so every driven sequence runs as the adapter, which both suites set as the borrower; categoryOf classifies address(0) as RoleProvider because roleProvider is unvalidated and is address(0) in both suites; the value-returning hook callback lets a hook rewrite the host's APR and reserve ratio to (10000, 10000) with no call, no write and no value movement, passing every gate; and the shipped manifest permits a state-changing roleProvider call while enumerating no external write scope for it, so a hook using its own permitted call would fail gate 1's storage half. None of these is in ManifestReader.sol, Vm.sol or ManifestReader.t.sol. Negative results: 12 hand-written adversarial probes against the reader all confirmed fail-closed behaviour before any fix -- a staticcall entry admits nothing even under an adapter resolving every name to a live address, absent effect arrays yield empty sets rather than widened ones, an unknown call kind and an unknown storage scope both revert rather than being skipped, action matching is exact with no prefix fallback, a malformed earlier threshold denies rather than widens, a unicode symbol is not mis-split because no UTF-8 continuation byte is 0x2E, and a caller catching the revert observes no partially built set. The vm-signature-drift line is clean: the added keyExistsJson(string calldata json, string calldata key) external view returns (bool) matches forge-std 1.7.1 Vm.sol:1104 byte for byte, checked against a freshly initialised forge project rather than from memory. The Elenchus verdict is inconclusive rather than guarded for a mechanical reason: the runner contract reverts the whole source file, and because S2-R1-01's fix widens ResolvedThreshold with allowedDelegateTargets, the new guard tests no longer compile against the parent's struct, so forge exits non-zero, run_forge_tests.py writes no report by design, and the guard check cannot distinguish a compile failure from a fired guard. Each of the 6 guards was instead verified by reverting its own behavioural hunk alone; each failed without its fix -- test_delegatecall_permit_does_not_admit_a_plain_call with "the delegatecall entry is admitted as one", test_duplicate_action_name_reverts and both empty-symbol tests with "next call did not revert as expected" -- and passed with it. The fizz invariant suite was built and campaigned but is not committed, because the step's Files list is closed to ManifestReader.sol, Vm.sol and ManifestReader.t.sol; persisting it needs a runbook amendment. Its campaigns ran twice against real engines: before the fix, Echidna 2.3.3 at 100040 calls with 6 properties passing and Medusa 1.5.1 at 132622 calls with 16 of 16 passing; after the fix, with 2 properties added for the call-kind and empty-symbol guards, Echidna at 100037 calls with 7 of 7 passing and Medusa at 184966 calls with 18 of 18 passing. The pre-fix campaign did not catch S2-R1-01, which is a coverage limitation rather than a clean run: the property asserted that admitted targets equalled the call plus delegatecall count, which is exactly what the unfixed code did, so the property encoded the defect instead of falsifying it. X-Ray ran as a named full recomputation (plan mode full, reason cache-missing, no prior cache existed) over the exact eleven-source current inventory at candidate digest 3cfee210c00fb8ddb1334ff4bf2705d615419daa0a667038ec7bba736581ab8c, with all 4 outputs regenerated, bound and promoted; its working material is operator-owned and stays out of this record and the tree. The solidity-auditor pass ran all 12 attacker roles to completion over the 11 in-scope sources; deduplication covered 31 unique (contract, function) pairs from the raw results, every one of which survives into this record or into the trails above. Suites at exit: forge test 42 passed 0 failed (36 at entry, the 12 ManifestReader tests plus 6 new guards), python3 -m unittest discover -s tests under plugins/janus 14 passed OK, the repository invariant suite 460 passed OK after regenerating the portable Promise Machine runtime and the Horos boundary, which mirror the two changed files by digest, and janus.py validate exit 0 on the shipped manifest.

## Step 2, round 2 -- 2026-08-29T09:45:23Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves, unchanged from round 1 -- no non-test contract implements AccountResolver and no gate consumes ResolvedThreshold, so allowedDelegateTargets is populated and unread exactly as every other field is, and the widening is not yet a gate. Gate 1 also takes one address[] and tests only kind != StaticCall, so it carries no dimension for the call/delegatecall split and the wiring step must add one or reinstate the fold; that is recorded, not fixed, because JanusHarness.sol is outside this step. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement, which needs a slot dimension on the gate side; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Coverage percentages are not reported: forge coverage was not run against this tree, so no per-file figure would measure it. Multi-byte whitespace is deliberately outside the blank-symbol guard, so a symbol of U+00A0 alone still reaches the adapter as a name; that is the stated grammar boundary, not an unknown. Whether Echidna or Medusa will ever implement the JSON cheatcodes is not established, only that neither does at 2.3.3 and 1.5.1.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R2-01 | high | plugins/janus/harness/adapters/ManifestFuzz.sol | the suite 168ec6d1 committed proved nothing under either engine: neither Echidna 2.3.3 nor Medusa 1.5.1 implements keyExistsJson, parseJsonUint or parseJsonString, so every generated manifest reverted with empty return data before the reader resolved anything, resolveSuccesses stayed 0, _check was never entered, and GL01 to GL07 -- each a negated ghost flag only _check sets -- held untested; reintroducing S2-R1-01's exact fold left Echidna at 7 of 7 passing, exit 0, over 100056 calls and Medusa at 22 passing, so GL04 did not falsify the defect its commit message named; GL07 had a second hole surviving the engine question, because FuzzResolver did not answer to the empty name, so the adapter refused before the reader's own guard was reached and deleting that guard under Foundry still left GL07 passing | fixed in 8264206e |
| S2-R2-07 | high | plugins/janus/harness/adapters/ManifestFuzz.sol | every property was a cardinality test or a != address(0) test, so nothing compared a resolved address against the name behind it: a reader writing address(0xDEAD) into every admitted call slot left all nine properties green over a full invariant run; GL02 could not fail either, because FuzzResolver derived ok as a != address(0) so UnresolvableSymbol always fired before SymbolResolvesToZero and deleting the reader's zero-address guard left everything green; and _check read t.valueRecipients[i] under a bound taken from t.valueAssets.length ten lines before comparing the two lengths, so a reader returning them at different lengths panicked 0x32 inside the try and rolled the draw back, leaving GL03 permanently green | fixed in be6987b7 |
| S2-R2-02 | medium | .agents/skills/promise-machine/runtime/MANIFEST.json | 168ec6d1 added the fuzz suite, its two engine configs and the campaign launcher without regenerating the portable Promise Machine runtime or the Horos boundary that mirror them by digest, so the repository invariant suite was red at that commit -- 460 tests, 1 failure, test_generated_runtime_is_current, missing=[adapters/ManifestFuzz.sol, adapters/echidna/echidna.yaml, adapters/medusa/medusa.json, scripts/run_fuzz_campaigns.py] changed=[.horos/boundary.json, MANIFEST.json] -- and the tree round 1 recorded as green had stopped being so before this round opened | fixed in 796c5ed4 |
| S2-R2-06 | medium | plugins/janus/harness/src/ManifestReader.sol | the dot grammar was applied to a value movement's asset and recipient, which the schema gives no suffix to strip, unlike a call target's function name and an external slot's expression, both of which it documents; against an adapter holding both names "USDC.e" resolved to canonical USDC at 0xC0 rather than the bridged token at 0xCE, a value permit bound to an asset the manifest did not name and chosen by the reader | fixed in be6987b7 |
| S2-R2-03 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R1-03's guard tested bytes(symbol).length == 0 only, so a target of " " or " .getCredential", and an external slot of a tab followed by ".counter[market]", still reached the adapter as a name -- the class the guard exists to close, since neither is a name the manifest author wrote and an adapter that trims before looking a name up admits it; verified against an adapter answering to every name, which admitted 0xBEEF for all three | fixed in 8264206e |
| S2-R2-05 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R2-03's replacement enumerated four whitespace bytes, which is three short: 0x0B, 0x0C and 0x00 are all writable into a manifest as JSON escapes, none is a name, and each still reached the adapter as one, admitted as 0xBEEF; janus.py validate accepts all three, so the reader is the only defence | fixed in be6987b7 |
| S2-R2-04 | low | docs/janus-manifest-resolution/runbook.md | the committed runbook carries two amendments, both dated 2026-08-28, and neither names adapters/ManifestFuzz.sol, adapters/echidna/echidna.yaml, adapters/medusa/medusa.json or scripts/run_fuzz_campaigns.py; step 2's committed Files clause is still closed to ManifestReader.sol, Vm.sol and ManifestReader.t.sol, so as committed the spec does not name the four files 168ec6d1 added, which is what the 2026-08-28 amendment requiring the committed copy to match the receipted runbook at each step's final push exists to prevent | reported, not fixed: a live run's runbook is controller-pinned and amended through hexctl, never by hand, so this is Fiat's to reconcile against its own receipted copy, which this round cannot read |
| S2-R2-08 | low | plugins/janus/scripts/janus.py | J009 tests a raw string against the six literals {"*", "any", "all", "ANY", "ALL", "*.*"}, while the reader resolves only the prefix, so "*.transfer", "any.foo", "all.x" and "ALL.y" all validate and yield the symbols "*", "any", "all" and "ALL", each admitted against a catch-all adapter; "*.*" being in the set shows one dotted spelling was anticipated and the rest left open | reported, not fixed: the wildcard rule is the schema's and the validator's, both of which step 6 owns, and duplicating the literal list in Solidity would put the rule in two places |
| S2-R2-09 | low | plugins/janus/harness/src/ManifestReader.sol | S2-R1-02's fix removed the early return, so .action is now parsed on every threshold and a later malformed threshold aborts resolution of an earlier well-formed action, with a raw 164-byte cheatcode revert rather than a named error; a genuine regression from that fix set, in the fail-closed direction | accepted: detecting that an action is stated twice requires reading every threshold, so the coupling is the cost of selecting by name rather than by position, and refusing is the safe direction |

Leads not pursued: the five fix-focused probes were executed, not read. Consumers: ResolvedThreshold is read in exactly two places, ManifestReader.t.sol and ManifestFuzz.sol, both reading allowedCallTargets and allowedDelegateTargets; no reader of the old flat set survives anywhere. Duplicate refusal fails closed in every arrangement and by name -- adjacent [deposit,deposit], the duplicate last of four [withdraw,deposit,withdraw,deposit], first and last [deposit,withdraw,deposit], and triple -- all with selector 0xe8099334 DuplicateActionInManifest("deposit"), while [withdraw] alone and [] refuse with the distinct 0x2ebd6b65 ActionNotInManifest("deposit"). Refusal order and partial sets: an unknown kind on a resolvable symbol gives 0xc1a04a5d UnknownCallKind("weird"); the same entry with an empty symbol gives 0x7b62c6b4 EmptyAccountSymbol(""), the grammar check preceding the kind check; a duplicated action whose first copy also carries an empty symbol gives DuplicateActionInManifest, the manifest-level scan completing before any symbol work; and a well-formed entry ahead of a malformed one still reverts whole, both entry points being external view, so a caller catching the revert observes no partially built set. No fix reordered a stricter check behind a looser one: _isBlank sits where the length == 0 test sat. Constructions still admitted are the documented grammar, not gaps -- "roleProvider." resolves to "roleProvider", and U+FF0E does not split because no UTF-8 continuation byte is 0x2E, so a fullwidth-stop name stays one symbol the adapter may refuse. Both properties the round was told to test by reintroducing their defects failed to falsify as committed, which is S2-R2-01. With the fold reintroduced into _resolveCalls, Echidna 2.3.3 finished 100056 calls at 7 of 7 passing and exit 0, and Medusa 1.5.1 reported 22 passing, its only failure this audit's own instrumentation probe. Three probes on that tree showed why: echidna_PROBE_never_attempts failed, so fuzzResolve was reached; echidna_PROBE_never_succeeds passed over 100043 calls, so resolveSuccesses never left zero; echidna_PROBE_revert_data_empty passed, so every revert carried zero bytes rather than one of the reader's seven named errors. A standalone CheatProbe pinned the cause under both engines: the cheatcode address carries code, and raw calls to keyExistsJson(string,string), parseJsonUint and parseJsonString all fail. Under Foundry the generator resolves 353 of 2048 draws, first at draw 1, before the fix, and 254 of 4096, first at draw 20, after it. Nine mutants were run against the finished tree, each reintroducing exactly the defect its guard names, and each was caught: the call/delegatecall fold fails GL04 and test_delegatecall_permit_does_not_admit_a_plain_call; restoring bytes(symbol).length == 0 fails GL07 and three blank-symbol tests; deleting the blank guard fails GL07 and five tests; narrowing it back to the four-byte test fails test_control_byte_only_symbols_revert; returning the first action match fails GL08 and test_duplicate_action_name_reverts; deleting the zero-address guard fails GL02 and test_zero_address_resolution_reverts; rewriting every admitted call target to address(0xDEAD) fails GL09 and four tests; reapplying the dot grammar to the value path fails GL09 and both dotted-asset tests; returning value recipients one short fails GL00 and two tests that panic on the reader's own out-of-bounds return. Control: 52 tests, 0 failures. Three qualifications. Three of the ten properties got no mutant of their own this round -- GL01 on set widening, GL05 on the gas budget, and GL06 on an unresolvable symbol or unknown kind or scope -- so they are exercised by the campaign and by the unit tests but are not shown here to falsify anything. GL03 is still the weakest of the ten: the length comparison now precedes every paired read, so it can fire, but the reader allocates both value arrays from one length, so the mutant that shortens one is caught by GL00 and the unit tests rather than by GL03 itself. And GL08 is new because S2-R1-02 had no property at all: the generator emitted one threshold, so neither DuplicateActionInManifest nor ActionNotInManifest was ever generated. Current fact: the fuzz suite is committed, at plugins/janus/harness/adapters/ManifestFuzz.sol with adapters/echidna/echidna.yaml, adapters/medusa/medusa.json and plugins/janus/scripts/run_fuzz_campaigns.py. Round 1's record says it was uncommitted, which was true when that round was receipted and is left untouched; S2-R2-04 records that the committed runbook does not yet name those paths. The campaigns at this commit exit non-zero by design, which is the report and not a regression: Echidna exits 1 with GL00 failed and GL01 to GL09 passing over 100042 calls, 4648 unique instructions, corpus 17, seed 3527166591070649310; Medusa exits 7 with 22 passing and GL00 the single failure. Both outcomes are stated in run_fuzz_campaigns.py and echidna.yaml. At 168ec6d1, before any change, the same campaigns were green and meaningless: Echidna 100027 calls, 7 of 7 passing, exit 0, 3621 unique instructions, corpus 12, seed 3079658920421933649; Medusa 18 of 18 passing, exit 0, 160364 calls. All six trails step 1 round 1 carried forward are unchanged and this round adds no fact to any of them. No commit in this round touches JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol or WildcatHostAdapter.sol, and all four were re-read in full against the current tree. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, still composes with anything that widens a write set, and it is also what stops gate 1's storage half from compensating for the missing call-kind dimension recorded above. Trail 2, account-only storage comparison, is unchanged in code, and the reader header's granularity paragraph from round 1 was not edited. Trails 3, 4, 5 and 6 are unchanged, Delta.taken still written and never read. The solidity-auditor pass ran all 12 attacker roles to completion over the 12 in-scope sources at 56056d3f, with 17 unique (contract, function) tuples deduplicated and all 17 carried into this record or the trails; its model-selection turn was skipped because AskUserQuestion is unavailable in this runtime, so no model was passed. It supplied S2-R2-05 through S2-R2-09, each of which this round reproduced by execution before fixing or accepting it. Its own negative results, confirmed here: the three round 1 fixes are intact and the rebuilt suite holds them; a staticcall widened into the state-changing set fails GL04; positional .thresholds[0] selection is caught, by GL08 rather than GL05; no JSON injection is reachable through the generator, because _sym, _kind and _scope emit no quote or backslash; _resolveCalls array sizing and _shrink leak no stale tail. Its leads this round does not pursue: gasBudget is unbounded above at schema, validator and reader, so 2**256-1 validates; janus.py validate accepts blank symbols, dotted wildcards, duplicate action names and NUL bytes, every case the reader refuses; both gate-5 sites in the test suites read .thresholds[0].gasBudget positionally, the same positional dependence the reader was written to remove; and the shipped deposit threshold permits kind:"call" to roleProvider with no external write scope, so a hook using its own call permit would fail gate 1's storage half, masked today because HonestAccessHook makes no external call. X-Ray ran twice over the exact twelve-source current inventory, round 1's eleven plus adapters/ManifestFuzz.sol: first as a named full recomputation, plan mode full, reason cache-missing, no prior cache, at candidate digest 20ad4225dbda2cd02abf064e69b2b33ba79eb951f51d6444c41f38f4d9db3800; then, after the round's second fix changed two sources, plan mode incremental, reason source-drift, dirty adapters/ManifestFuzz.sol and src/ManifestReader.sol, ten reusable, none removed and none reverse-invalidated, at candidate digest 58ee123d93d54609f07056dd9ad1e13c08152010cbc23a93127f81ebcd983e21. All four outputs were regenerated, bound and promoted in both runs; no global synthesis or finding was reused. The working material is operator-owned and stays out of this record and the tree. The Elenchus verdict is inconclusive for the same mechanical reason as round 1, not a relabelling: the runner contract reverts the whole source file, and each of this round's two fix commits changes ManifestFuzz.sol's fuzzResolve signature and ghost names alongside ManifestReader.sol, so ManifestReader.t.sol does not compile against either parent, forge exits non-zero, run_forge_tests.py writes no report by design, and tmp/elenchus/fiat-329-step-2-r2.xml was never created. Run against 8264206e and again against be6987b7, the runner returned inconclusive both times. One reading is recorded so it is not mistaken later: run with --ref HEAD while HEAD was 1f4c3aeb, a resync commit whose only content is generated mirrors, the runner returned passed, naming .agents/skills/promise-machine/runtime/plugins/janus/harness/test/ManifestReader.t.sol -- that verdict is about the mirror's copy of the test file and its parent, and carries no evidence about either fix. The nine guards were verified individually by the mutation battery above instead. Suites at exit, at 1f4c3aeb: forge test 52 passed 0 failed across 7 suites -- 25 ManifestReaderTest, 3 ManifestFuzzInvariantTest, 9 WildcatConformanceTest, 7 HostileHooksTest, 1 ReentryInvariantTest, 5 StateDeltaRecorderTest, 2 ScaffoldTest -- against the 42 this round expected, the 10 new being 7 reader guard tests and 3 in the invariant contract; python3 -m unittest discover -s tests under plugins/janus 14 passed OK; the repository invariant suite 460 passed OK, which needed the Horos scan and the portable-runtime sync alternated to a fixpoint rather than run once each, because the boundary entry counts the runtime mirror the sync writes -- .agents/skills/promise-machine/runtime/ settled at 931 files and 20502420 bytes, up from 927 and 20479946; janus.py validate exit 0 on the shipped manifest; git status clean, with no corpus or ignored artefact staged and both engine corpus directories matching .gitignore's **/corpus/.

## Step 2, round 3 -- 2026-08-29T15:25:16Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves, unchanged from rounds 1 and 2 -- no non-test contract implements AccountResolver, no gate consumes ResolvedThreshold, and both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, so the reader and the gates remain two halves that do not meet and step 4 owns joining them. Gate 1 still takes one address[] and tests only kind != StaticCall, so it carries no call/delegatecall dimension; recorded again, not fixed, because JanusHarness.sol is outside this step. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Multi-byte whitespace remains outside the blank-symbol guard by design, and this round widened the recorded boundary from U+00A0 alone to a class of at least four: U+00A0, U+2003, U+3000 and U+FEFF each reach the adapter as a name when written alone. Whether Echidna or Medusa will ever implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, which this round verified independently rather than inheriting.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R3-01 | medium | plugins/janus/harness/src/ManifestReader.sol | be6987b7 fixed the dotted-name defect on one of the three paths that carry account names: a call target's .target and an external slot's .slot still split at the first dot, so with an adapter holding USDC at 0xC0 and USDC.e at 0xCE the same string resolved to 0xCE through the value path and 0xC0 through the other two -- a permit bound to an account the manifest did not name, chosen by the reader, and the same class S2-R2-06 names; it does not refuse, and the refusal that does occur when the prefix is unknown depends on the adapter not holding the prefix rather than on the reader noticing; the campaign could not have found it, because _sym returns no dotted name so _buildCalls and _buildWrites only ever emit a dot-free account plus a suffix and asset.e is wired only into the value path, which does not split | fixed in 460dedf8 |
| S2-R3-02 | medium | plugins/janus/harness/adapters/ManifestFuzz.sol | three of the four resolved sets carry an exact cardinality check in _check and the value-movement arm carried only the one-sided > nm, so a reader that resolved every entry, leaving every refusal intact, and then returned the value arrays one pair short satisfied all ten properties -- > nm is false because the set shrank rather than grew, both arrays stay equal length so GL03 is silent, and the address loops are min-bounded so they compare the surviving prefix and agree; that is the shrunken set ManifestReader's own header says it never returns, and the mutant was caught only by two unit tests, which do not run under either engine | fixed in 8cfe5fd0 |
| S2-R3-03 | medium | plugins/janus/harness/adapters/ManifestFuzz.sol | the nine echidna_GL01 to echidna_GL09 functions were executed by nothing that could fail: invariant_manifest_resolution_holds asserted the raw ghost getters rather than the properties, so forge coverage reported 17 of 26 functions in the suite with the nine missing being exactly these, and under Echidna and Medusa they are called but hold vacuously, which is S2-R2-01 one level up; inverting GL01, making GL09 return true unconditionally, and pointing GL06 at sawBudgetDrift each left the tree at 52 of 52 | fixed in 460dedf8 |
| S2-R3-04 | low | plugins/janus/harness/adapters/ManifestFuzz.sol | the early return in _check read the sticky campaign flags sawPairMismatch and sawKindConfusion where its comment described a per-draw condition; the flags are never reset, so the first GL03 or GL04 failure switched the GL02 and GL09 address comparisons off for every later draw and a reader that both confused kinds and returned wrong addresses reported only the first; it cannot produce a false green, since the property that set the flag has already failed, so the cost is counterexample quality | fixed in 8cfe5fd0 |
| S2-R3-05 | low | plugins/janus/harness/adapters/ManifestFuzz.sol | every manifest the generator emitted carried exactly one action name, so .thresholds[0] and the by-name prefix were the same string and no property could tell selection-by-name from selection-by-position: reading the budget positionally from .thresholds[0], and replacing the whole .action comparison with if (true) so the reader ignores its own action argument, each left all ten properties green and were caught only by unit tests, leaving the register's threshold-lookup-no-fallback line resting entirely on three unit tests | fixed in 8cfe5fd0 |
| S2-R3-06 | low | plugins/janus/harness/adapters/ManifestFuzz.sol | FuzzResolver derived ok as a != address(0) and be6987b7 added ghost for the (true, zero) case, fixing one direction; the table still could not produce (false, non-zero), so deleting UnresolvableSymbol from the reader fell through to SymbolResolvesToZero -- the same revert, _check still not entered, GL06 green -- and within the campaign GL06 tested the zero-address guard twice and the unresolvable-name guard never, which is the register's unresolved-symbol-fail-closed line | fixed in 8cfe5fd0 |
| S2-R3-07 | low | plugins/janus/harness/test/ManifestReader.t.sol | neither UnknownCallKind nor UnknownStorageScope had a unit test, so admitting an unknown kind silently, treating it as a plain call, or admitting an unknown scope each failed only the fuzz invariant -- the one artefact this round proved can go vacuous -- leaving the register's over-permit-by-category line without a deterministic guard | fixed in 460dedf8 |
| S2-R3-08 | low | plugins/janus/harness/test/ManifestReader.t.sol | a regression this round introduced and then caught by re-running the battery against its own fix: S2-R3-01's ambiguity guard made test_control_byte_only_symbols_revert stop killing its own mutant, because the test used a bare vm.expectRevert() over dotted targets against an OmniResolver and the new refusal satisfies it, so narrowing _isBlank back to four whitespace bytes left the tree at 61 of 61 where it had previously failed that test; it was the only bare expectRevert in the suite | fixed in 460dedf8 |

Leads not pursued: the five fix-focused probes were executed, not read, and one round 2 claim did not reproduce. Probe 1, the cheatcode claim, holds, verified independently rather than inherited: a standalone CheatProbe making raw calls reports, under Echidna 2.3.3, CP0 and CP1 falsified and CP2, CP3, CP4 and CP5 passing over 3008 calls, 643 unique instructions, seed 1202156850909097486, exit 0 -- that is, the probe ran, the cheatcode address does carry code, and keyExistsJson(string,string), parseJsonUint and parseJsonString each failed on every call and keyExistsJson never answered true. Medusa 1.5.1 reports the same shape, 12 passed and 2 failed, CP0 and CP1 the two falsified. Probe 1's replacement bites under the engine it now runs on: twenty-three mutants were run against the finished tree under Foundry, every one caught. Round 2's nine re-derive, with two corrections. The call/delegatecall fold fails GL04 and test_delegatecall_permit_does_not_admit_a_plain_call only when it is S2-R1-01's exact fold, one flat set; a fold that pushes into both sets fails GL01 first, because the assertion order in invariant_manifest_resolution_holds reports only the first failing property, so a GL attribution read off forge output is the first in order and not the complete set. And deleting the blank guard now fails GL07 and six unit tests rather than five, because be6987b7 added test_control_byte_only_symbols_revert after 8264206e counted them. The remaining seven reproduce exactly: restoring bytes(symbol).length == 0 fails GL07 and three; narrowing to the four-byte test fails test_control_byte_only_symbols_revert alone with no GL property; returning the first action match fails GL08 and test_duplicate_action_name_reverts; deleting the zero-address guard fails GL02 and test_zero_address_resolution_reverts; rewriting every admitted call target to a constant fails GL09 and six tests; reapplying the dot grammar to the value path fails GL09 and both dotted-asset tests; returning value recipients one short fails GL00 and two tests. GL00, GL08 and GL09 fail for the reasons their commits gave, checked one at a time rather than in aggregate. Probe 3 is answered: GL01, GL03, GL05 and GL06 all have mutants now, and all four bite. GL01 fails on a write set widened by one entry and on an unknown storage scope admitted; GL03 fails on a value recipients array returned one long, the lengthening direction round 2 did not try -- round 2's observation that the shortening direction is caught by GL00 instead is correct and stands; GL05 fails on budget drift and, after S2-R3-05's decoy threshold, on a positional .thresholds[0] read as well; GL06 fails on an unknown call kind admitted, on an unresolvable symbol admitted, and, after S2-R3-06, on UnresolvableSymbol deleted. None of the four had to be written off. Probe 4 holds: the campaigns still exit non-zero by design with GL00 the only failure, and no genuine property failure hides behind it -- both engines enumerate GL01 to GL09 individually as passing. At the pre-fix tree Echidna exited 1 over 100029 calls, 4648 unique instructions, 3 unique codehashes, corpus 14, seed 3413087962100531943, and Medusa exited 7 with 22 passed and 1 failed; at the fixed tree Echidna exited 1 over 100035 calls, 4736 unique instructions, corpus 14, seed 7750054011664335377, and Medusa again 7 with 22 and 1. The state is documented in run_fuzz_campaigns.py's module docstring, in echidna.yaml, and in ManifestFuzz.sol's GL00 comment; a later reader meets it in all three. Probe 5 holds: 796c5ed4 and 1f4c3aeb touch only .agents/skills/promise-machine/runtime, .horos/boundary.json and .horos/candidates.json, and every mirrored source file is byte-identical to its in-tree original at HEAD, checked with cmp over all seven. No hand edit is carried in either. Probe 2 is answered in both directions and the false-fail direction is clean: every printable ASCII byte from 0x21 to 0x7E is still admitted as a name, so is 0x7F, so are Cyrillic, CJK and Latin-1-supplement names, and so is any run containing one byte above 0x20 -- the rejected set is exactly the 33 bytes 0x00 to 0x20 and none of them is a name. In the other direction the guard admits what its comment says it admits and no more, but the recorded boundary was narrower than the fact: U+00A0 is not the only multi-byte whitespace that reaches the adapter as a name, and U+2003, U+3000 and U+FEFF do too. One sub-fact refines S2-R2-05 rather than contradicting it: a raw control byte never reaches the reader at all, because vm.keyExistsJson refuses the manifest first with control character (\u0000-\u001F) found; only the JSON-escaped form reaches the guard, which is the form round 2 said was writable. Every escaped byte from \u0001 to \u0020 and \u0000 refuses with EmptyAccountSymbol. The record's U+FF0E claim holds and is structural rather than sampled: no byte of a multi-byte UTF-8 sequence can be 0x2E, since continuation bytes are 0x80 to 0xBF and lead bytes 0xC2 to 0xF4. What did not reproduce: a claim raised this round, not by round 2 -- the invariant role argued that refusing every staticcall entry outright would leave all ten properties green, because a valid draw with no call entries still resolves; executed, that mutant fails GL00 and five unit tests, so the fail-open direction is covered, weakly and probabilistically, by the anti-vacuity guard rather than being invisible. Recorded because the reasoning was sound and the outcome was not. Coverage percentages, which round 2 could not report: forge coverage gives src/ManifestReader.sol 100.00% lines (104/104), 100.00% statements (137/137), 100.00% branches (20/20) and 100.00% functions (15/15); adapters/ManifestFuzz.sol 93.25% lines (152/163), 85.41% statements, 68.06% branches (49/72) and 65.38% functions (17/26), the nine uncovered functions being exactly GL01 to GL09, which is how S2-R3-03 was found; src/StateDeltaRecorder.sol 96.49% lines (55/57); src/JanusHarness.sol 89.77% lines (79/88) and 68.42% branches (13/19); src/wildcat/WildcatHostModel.sol 81.01% lines (64/79); src/wildcat/HonestAccessHook.sol 73.68% lines (14/19) and 0.00% branches (0/3); src/wildcat/WildcatHostAdapter.sol 64.10% lines (25/39) and 28.57% branches (4/14); src/hostile/HostileHooks.sol 55.32% lines (26/47); repository total 84.95% lines (570/671). The solidity-auditor pass ran all 12 attacker roles to completion over the same 12 in-scope sources, at local VERSION 3 matching the upstream VERSION, with its model-selection turn skipped because AskUserQuestion is unavailable in this runtime, so no model was passed; its bundles were built outside the worktree and no .audit-* or x-ray directory was created inside it. Ten of the twelve converged on the pre-existing gate engine rather than this step's files; that material is routed to the carried trails below rather than this round's findings, because JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol and WildcatHostAdapter.sol are unchanged across the whole of step 2 and belong to later steps. Its in-scope contributions are S2-R3-01, raised by the boundary role and reproduced here by execution before it was fixed, and S2-R3-02, raised independently by the invariant, boundary and numerical-gap roles and confirmed by a mutant that isolates cardinality from refusal. All six trails step 1 round 1 carried forward remain open against the unchanged gate engine; this round adds evidence to four and changes none. Trail 1, the hook's own storage being unattributable because _hookAttributed excludes the host-to-hook entry frame, was independently re-derived by eight of the twelve roles with executed proofs, several against an empty allowedWriteAccounts, and one role recorded its false-fail twin: a write to an account the hook merely called is charged to the hook even when the host made it after the hook returned, which is the same missing frame index seen from the other side. Trail 2, account-only storage comparison, is unchanged in code and unchanged in the reader header. Trail 3, _findingsJson interpolating host and manifest raw while every finding field is escaped, was re-derived with a concrete payload and one honest bound: Python's json.load keeps the last duplicate key, so injected findings cannot suppress real ones and the reachable damage is key injection or an unparseable report. Trail 4, _drive passing _endRecording(0) so Delta.gasUsed is permanently zero, was re-derived with a measured counterexample. Trail 5, _hookValueMoved summing native value only so an ERC20 drain reads zero, was re-derived, and one role added that the same sum double-counts a multi-hop forward. Trail 6 is unchanged. Four gate-engine facts are new this round, recorded as trail material rather than findings because they lie outside this step's files: WildcatHostModel.setHook carries neither nonReentrant nor an authorisation check, so a hook can rebind the host's hook binding from inside its own callback while the shipped manifest's liveness clause asserts the binding is immutable; WildcatHostModel.executeWithdrawal never decrements scaledTotalSupply, so the model's own supply conservation breaks after a full deposit, queue and execute cycle and the figure is handed to every hook as MarketState.scaledTotalSupply; WildcatHostAdapter.valueSnapshot reads only the model's own balance, so value moved to an address in roles() nets to zero; and WildcatHostAdapter.categoryOf classifies address(0) as RoleProvider, which is what both suites construct it with. Round 2's note that the reader does not read mayReturnValues, recorded there as step 6's, now has an executed demonstration: with the manifest declaring mayReturnValues false, a hook returning (10000, 10000) moves the host's annualInterestBips from 1000 to 10000 through the return channel, makes no call, and passes gate 1 storage. Delta.taken is still written and never read. S2-R2-04 is reconciled and not carried: the controller amended the runbook on 2026-08-29 so step 6 owns refreshing docs/janus-manifest-resolution/runbook.md and study.md to the receipted artefacts, at canonical runbook SHA-256 f7211fbd4ceb81990f7e7ba0f5d1b3822df3bacf7ab9f3a96ab22bfe4973087f; no runbook was edited by hand in this round. S2-R2-08, the janus.py J009 dotted-wildcard bypass, is carried forward unchanged and unfixed, because the study's constraints keep the Python validator untouched by this run and the schema and validator belong to step 6. S2-R2-09 remains accepted on the same reasoning. X-Ray ran twice over the exact twelve-source inventory, both instruction digests reconciled against the hexaemeron-x-ray-preaudit overlay first: skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10, both matching. The first run planned mode full, reason scope-mismatch, all twelve dirty and none reusable, because the only prior cache carried eleven sources and a scope mismatch is a named full recomputation rather than a narrowing, at candidate digest 766725293533784f28e004318527b94fd762256de06e4123075b7ca73d8a7b83; the second, after this round's fixes changed two sources, planned mode incremental, reason source-drift, dirty adapters/ManifestFuzz.sol and src/ManifestReader.sol, ten reusable, none removed and none reverse-invalidated, at candidate digest d56f72b4f2e824140fcd2be523f2bdbc776230270e1958461372a70dc6cd3880. Every current source was read and digested in full before either plan. All four outputs were regenerated, bound and promoted in both runs and no global synthesis or finding was reused. The working material is operator-owned and stays out of this record and the tree. The Elenchus verdict is unguarded, for a reason new to this run rather than round 1 and round 2's: run with --ref HEAD while HEAD was 8cfe5fd0, the round's last fixes commit, the runner reports that the commit changed no test files, because that commit changes only adapters/ManifestFuzz.sol and the suite lives outside src and test -- the same placement 168ec6d1 and 8264206e both had to work around, now surfacing in the runner contract. No report was written and none was expected on that path. Run against 460dedf8, the other fixes commit, the runner returns inconclusive for the mechanical reason rounds 1 and 2 both hit: that commit changes ManifestReader.sol's private helpers alongside ManifestReader.t.sol, so the test file does not compile against the parent, forge exits non-zero, run_forge_tests.py writes no report by design, and tmp/elenchus/fiat-329-step-2-r3-a.xml was never created. Neither verdict was run against a resync commit, which round 2 correctly identified as evidence about the mirror rather than about a fix; 84081f4d was committed after both runs. The twenty-three-mutant battery is what stands behind the guards instead. Suites at exit: forge test 71 passed 0 failed across 8 suites -- 33 ManifestReaderTest, 10 ManifestFuzzPropertyTest, 9 WildcatConformanceTest, 7 HostileHooksTest, 5 StateDeltaRecorderTest, 4 ManifestFuzzInvariantTest, 2 ScaffoldTest, 1 ReentryInvariantTest -- against the 52 this round opened with, the 19 new being 8 reader guard tests, 10 in the new property contract and 1 in the invariant contract; python3 -m unittest discover -s tests under plugins/janus 14 passed OK; the repository invariant suite 460 passed OK, which again needed the Horos scan and the portable-runtime sync alternated to a fixpoint rather than run once each, settling on the second pass with .agents/skills/promise-machine/runtime/ at 931 files and 20527229 bytes, the same file count as round 2 and 24809 bytes more; janus.py validate exit 0 on the shipped manifest; git status clean, with no corpus or ignored artefact staged and both engine corpus directories matching .gitignore's **/corpus/.

## Step 2, round 4 -- 2026-08-29T16:06:24Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: gate-side enforcement of anything the reader resolves, unchanged from rounds 1, 2 and 3 -- no non-test contract implements AccountResolver, no gate consumes ResolvedThreshold, and both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, so the reader and the gates remain two halves that do not meet and step 4 owns joining them. Gate 1 still takes one address[] and tests only kind != StaticCall, so it carries no call/delegatecall dimension; recorded again, not fixed, because JanusHarness.sol is outside this step. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Multi-byte whitespace remains outside the blank-symbol guard by design, and this round widened the recorded boundary again rather than confirming round 3's count: U+2028 and U+200B reach the adapter as names alongside round 3's U+00A0, U+2003, U+3000 and U+FEFF, so the set is not four but open-ended, and the honest statement is that it is every codepoint above U+007F that renders as blank, a set Unicode revisions extend. Enumerating a subset would rebuild exactly the S2-R2-05 defect the byte rule replaced, a list that looks complete and is not, so this is recorded as a boundary and not raised as a finding this round; the reasoning is given in full below. Whether Echidna or Medusa will ever implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, verified independently again this round.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R4-01 | medium | plugins/janus/harness/src/ManifestReader.sol | 460dedf8's ambiguity guard asked whole && wholeAddr != address(0), so an adapter answering (true, address(0)) for the whole written name was read as an absence and the reader split the string anyway; a known name resolving to zero is a refusal this reader raises everywhere else, and both ZeroResolver in the unit tests and ghost in the campaign exist to model exactly that answer, so the whole of S2-R3-01 stayed alive in that corner -- with an adapter holding USDC at 0xC0 and answering (true, address(0)) for USDC.e, the value path reverted SymbolResolvesToZero("USDC.e") while the call-target and external-slot paths both returned 0xC0, one string through one resolver binding a permit to an account the manifest does not name; executed against the tree at defdda80 before the fix, and the fix also had to reorder, because asking the ambiguity question before the prefix is resolved makes AmbiguousAccountSymbol stand in for SymbolResolvesToZero and breaks test_zero_address_resolution_reverts | fixed in 4163a5bb |
| S2-R4-02 | low | plugins/janus/harness/adapters/ManifestFuzz.sol | the AmbiguousAccountSymbol refusal 460dedf8 added had no property behind it: _sym returns no dotted name, so every target and slot the generator emits is a dot-free account plus .someFunction or .field[key], which no adapter holds -- the fact round 3 recorded about the pre-fix generator and did not close when it added the guard -- so deleting the guard left all ten properties green and was caught only by two unit tests, leaving the register's over-permit-by-category line resting on deterministic tests alone under the one artefact this step has twice proved can go vacuous; GL09 cannot stand in for it, because a reader that splits asset.e resolves asset, which is the address the generator's own expectation already holds for that symbol choice, so the two agree | fixed in 4163a5bb |
| S2-R4-03 | low | plugins/janus/harness/src/ManifestReader.sol | a regression this round introduced and then caught by re-running the battery against its own fix, the same way round 3 caught S2-R3-08: resolving the prefix before asking the ambiguity question moves the blank-symbol guarantee into _resolveSymbol, whose first act is the same check, so _resolveDotted's own copy of the blank guard became a line no mutant can kill and deleting it changed nothing anywhere in the suite; it is the step's recurring shape one level down, a guard that reads as protection and is not the thing protecting, and it is removed with the ordering that carries the guarantee named in its place | fixed in 4163a5bb |

Leads not pursued: the five priority probes were executed rather than read, and two round 3 claims did not reproduce as stated. Probe 1, round 3's own fixes as the subject, in both directions. The false-fail direction of 460dedf8 is clean and stays clean under this round's fix: the shipped manifest's roleProvider.getCredential and roleProvider.validateCredential resolve unchanged against an adapter that does not hold the whole name, a dotted name the adapter does not hold whole keeps its single reading, and the only behaviour this round changes is the (true, address(0)) answer, which is S2-R4-01 and was a silent substitution rather than a refusal. The one boundary worth naming is that an adapter which answers ok for every name it does not know, in violation of AccountResolver's own comment, now refuses every dotted target rather than silently splitting it; refusing is the fail-closed direction and the reader owns that choice. The four checks 8cfe5fd0 added were each paired with the defect it names and shown to be what catches it, not merely present: the value-arm cardinality check is what makes GL01 fail on a reader returning the value arrays one pair short, and with the check removed GL01 is silent and only the unit tests speak; the write-arm check is what makes GL01 fail on a short write array; phantom is what makes GL06 fail on a deleted UnresolvableSymbol, and with phantom removed GL06 is silent; the decoy threshold is what makes GL05 fail on a positional .thresholds[0].gasBudget read, and with the decoy removed GL05 is silent. S2-R3-04's per-draw local is load-bearing in the way its own record claimed: with a reader that both folds the call kinds and writes a constant into every admitted target, the fixed tree reports GL09 and the sticky-flag version reports GL04 with GL09 suppressed. S2-R3-08's lesson holds and extends: no bare vm.expectRevert() call site remains anywhere in the harness tests, every one of the twenty-three expectRevert sites in ManifestReader.t.sol and the three in HostileHooks.t.sol and StateDeltaRecorder.t.sol names a selector, and the only textual matches for the bare form are the Vm.sol declaration, which is a real Foundry cheatcode correctly declared and merely unused, and gitignored engine corpus artefacts. Probe 2, coverage, is answered in both directions and one round 3 figure did not reproduce. forge coverage gives src/ManifestReader.sol 100.00% lines, statements, branches and functions at every tree this round touched, so the percentage claim holds; the absolute figures do not. Round 3 recorded 104/104 lines, 137/137 statements, 20/20 branches and 15/15 functions, and the tree it shipped has 108/108, 145/145, 23/23 and 16/16, the difference being _resolveDotted itself, the function 460dedf8 added -- so round 3's coverage run predates its own fixes and its record presents pre-fix figures as the shipped tree's. The same staleness runs through its mutation counts: deleting the blank guard fails GL07 and seven unit tests here, not six, and restoring bytes(symbol).length == 0 fails GL07 and four, not three, the extra one in each case being test_a_leading_dot_is_still_blank_not_ambiguous, which 460dedf8 added after the count was taken. Nothing in round 3's substance depends on those numbers and every attribution it made reproduces; the figures alone are one tree behind. Treated with suspicion rather than comfort, 100 per cent coverage did hide an unasserted path, and it is S2-R4-01: line 278's compound condition counts as one branch and is exercised in both directions, but only ever with whole false, so the whole && wholeAddr == address(0) case was covered without being asserted anywhere. At this round's tree the figures are 107/107, 140/140, 22/22 and 16/16, the reduction from 108 being S2-R4-03's removed line. Probe 3, the mutation battery, was re-derived independently rather than trusted and then extended. Thirty-five mutants ran against this round's tree: thirty caught, five survived, none broken. The five survivors are all removals of a check from _check or FuzzResolver against a correct reader, where nothing is wrong for the check to find, and each was then paired with the defect it names, which is the composite result above. The battery covers the paths round 3's twenty-three did not, including the _check early-return path fixed in S2-R3-04 and the value-arm cardinality fixed in S2-R3-02, both re-derived here from the mutant rather than the record, plus the ambiguity guard deleted, made unreachable, restored to round 3's narrow form, and restored to round 3's narrow form and original ordering together -- the last three all fail GL10, which is what S2-R4-02's fix is for. GL10 needs two ambiguous names rather than one, because the guard has two ways to be wrong and no single name shows both: asset.e, which FuzzResolver answers for at 0xA8, dies when the guard is deleted, and host.shadow, which it answers for at address(0), dies when the guard also demands a non-zero address, which is S2-R4-01 itself; both split to a live prefix, 0xA3 and 0xA2, which is the address the generator's own expectation holds, so GL09 stays green on each and only GL10 sees them. GL10 is itself protected from the other side: returning true unconditionally, and pointing it at sawBudgetDrift, each fail ManifestFuzzPropertyTest. Probe 4 holds in one direction and corrects the record in another. Round 3's fold correction reproduces exactly: S2-R1-01's exact fold into one flat set fails GL04 and test_delegatecall_permit_does_not_admit_a_plain_call, while a fold that pushes into both sets fails GL01 first, so a GL attribution read off forge output is the first assertion in order and not the complete set. Round 3's blank-guard count does not reproduce, for the stale-tree reason given above. The multi-byte whitespace question is decided here as a boundary and not a finding, and the reasoning is not that the risk is small. _isBlank is total over the domain it can decide without a decoder: the rejected set is exactly the thirty-three bytes 0x00 to 0x20, none of which is a name, and every byte of a multi-byte UTF-8 sequence is above 0x20, so no single-byte rule can reach a codepoint. Admitting one requires an adapter that holds an invisible name at a live address, which is the adapter's decision about its own namespace and is where AccountResolver puts name existence. Any partial fix -- folding in U+00A0, U+2003, U+3000, U+FEFF and now U+2028 and U+200B -- would reproduce the S2-R2-05 defect the byte rule was written to replace, an enumeration that looks complete and is not, and the set grows with Unicode revisions rather than being fixed at six. The only total fix is a UTF-8 whitespace decoder in the reader, which would also have to keep admitting the Cyrillic and CJK names round 3 verified are admitted; that is a design change for a later step and not a defect in this one. Executed this round: U+0020, U+0009, U+0000 and U+001F are refused, U+00A0, U+2003, U+3000, U+FEFF, U+2028 and U+200B all reach the adapter, and an ordinary name and a Cyrillic name are both admitted. Probe 5, the four gate-engine facts round 3 recorded as trail material, all four confirmed and none of them a defect in this step's own surface. WildcatHostModel.setHook carries neither nonReentrant nor an authorisation check, one function below a setBorrower that does carry require(borrower == address(0)), so the model contradicts the shipped manifest's own liveness.uninstall clause that the binding is immutable. WildcatHostModel.executeWithdrawal decrements queuedOf, batchScaledAmount and scaledPendingWithdrawals and transfers the asset without touching scaledTotalSupply, which deposit increments, so the model's supply only grows while its asset balance falls. WildcatHostAdapter.valueSnapshot returns asset.balanceOf(address(model)) alone, and that is the fact with the tightest coupling to this step: step 2 now resolves value movements into (asset, recipient) pairs that the recorder structurally cannot observe unless the model is one end of the movement, so a permit this reader resolves correctly is one the gate engine cannot yet check -- a step 4 obligation, recorded so the gate step meets it rather than a reader defect. WildcatHostAdapter.categoryOf does classify address(0) as RoleProvider when roleProvider is constructed as address(0) and hook is not, and the brief's reason for attention resolves in the reader's favour rather than against it: the reader never emits address(0) into a resolved set, categoryOf is not fed by the reader, and Category and categoryOf have no caller anywhere in src, test, adapters, scripts or docs, so the misclassification is unreachable today and is trail material for whichever step first calls it. The six trails carried from step 1 round 1 are all still open against an unchanged gate engine and none changed this round; JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol and WildcatHostAdapter.sol are byte-identical across the whole of step 2. Re-confirmed by reading rather than inherited: trail 1's _hookAttributed skips the host-to-hook entry frame itself, so a write to the hook's own address is never charged to it; trail 2's gate 1 compares StorageWriteObs.account and never .slot; trail 3's _findingsJson interpolates host and manifest raw while every finding field goes through _escape; trail 4's _drive calls _endRecording(0) so Delta.gasUsed is permanently zero; trail 5's _hookValueMoved sums CallObs.value only, so an ERC20 movement reads as zero; trail 6 is unchanged. Delta.taken is still written and never read. The solidity-auditor and X-Ray passes are both incomplete this round and neither is claimed as a completed pass. The twelve prescribed attacker roles did not run: there was no role fan-out, no wait barrier, no deduplication record, no completeness pass and no judgement record, so nothing here rests on a solidity-auditor report and none is cited. What did happen is a direct review of the whole Solidity scope -- all twelve current sources in the X-Ray inventory read and digested this round, with the reader, the fuzz adapter and the test file read line by line rather than sampled -- and the findings above come from that reading, from execution against the tree, and from the mutation battery, each of which is reproducible from what is recorded here. The X-Ray digest preconditions were reconciled first and both match the hexaemeron-x-ray-preaudit overlay -- skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10 -- so reuse was permitted. It was not used: this round did not run the adapter's plan, assemble, bind-outputs or promote transitions and did not regenerate architecture.json, x-ray.md, entry-points.md or invariants.md, so no completed X-Ray run is claimed for round 4 and the cache is left exactly as round 3 promoted it. That is a gap in this round's coverage of the pre-audit views, stated rather than papered over; the audit substance above rests on direct reading, execution and mutation, not on those four artefacts. No x-ray/ or .audit-* directory was created inside the worktree. Fizz refreshed the committed suite and re-ran both campaigns sequentially through plugins/janus/scripts/run_fuzz_campaigns.py, one engine per invocation, and the exit codes are the engines' own rather than a pipeline's: at this round's tree Echidna exits 1 over 100033 total calls, 4964 unique instructions, 3 unique codehashes, corpus 15, seed 4938936107165604621, and Medusa exits 7 with 24 passed and 1 failed. GL00 is the only failure under either engine and no genuine failure hides behind the non-zero exit: Echidna enumerates GL01 to GL10 individually as passing and Medusa lists each of the eleven property tests, the ten passing ones named. The cause is unchanged and was re-checked, not inherited -- neither engine implements keyExistsJson, parseJsonUint or parseJsonString, so every generated manifest reverts before the reader resolves anything, which is exactly what GL00 exists to report. At the tree this round opened with, Echidna exited 1 over 100032 calls with 4736 unique instructions, corpus 14, seed 7855521739122573900, and Medusa exited 7 with 22 passed and 1 failed, so round 3's Echidna 1, Medusa 7 and its 4736 figure all reproduce. The rise from 4736 to 4964 and from corpus 14 to 15 is GL10 and the two ambiguous names the generator now emits. The Elenchus verdict is inconclusive, run with --ref HEAD while HEAD was 4163a5bb, this round's only fixes commit, and never against the resync commit 0b0ed9ab. The reason is mechanical and is the one rounds 1, 2 and 3 all hit for 460dedf8, not round 3's unguarded: the commit changes ManifestReader.sol and adapters/ManifestFuzz.sol alongside test/ManifestReader.t.sol, and the new test file references echidna_GL10_ambiguous_name_fails_closed and sawAmbiguousAccepted, neither of which exists at the parent defdda80, so the test file cannot compile against the parent, forge exits non-zero, run_forge_tests.py writes no report by design, and tmp/elenchus/fiat-329-step-2-r4.xml was never created. That absence was confirmed rather than assumed, and the parent was checked for GL10 directly. The thirty-five-mutant battery is what stands behind the guards instead. S2-R2-04 remains reconciled and not carried, with step 6 owning the spec refresh under the 2026-08-29 runbook amendment at canonical runbook SHA-256 f7211fbd4ceb81990f7e7ba0f5d1b3822df3bacf7ab9f3a96ab22bfe4973087f; no runbook was edited by hand in this round. S2-R2-08, the janus.py J009 dotted-wildcard bypass, is carried forward unchanged and unfixed because the study keeps the Python validator untouched by this run. S2-R2-09 remains accepted on the same reasoning. Suites at exit: forge test 76 passed 0 failed across 8 suites against the 71 this round opened with, the 5 new being 4 reader guard tests and 1 property test for GL10; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK, which again needed the Horos scan and the portable-runtime sync alternated to a fixpoint rather than run once each, settling on the second pass and confirmed by a third that changed nothing; portable_promise_machine.py check exit 0 with every mirrored janus source byte-identical to its in-tree original, checked with cmp; janus.py validate exit 0 on the shipped manifest; git status clean, with no corpus or ignored artefact staged and both engine corpus directories still matching .gitignore's **/corpus/.

## Step 2, round 5 -- 2026-08-29T17:04:15Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run -- no role fan-out, no wait barrier, no deduplication record, no completeness pass and no judgement record -- so nothing here rests on a solidity-auditor report and none is cited. The X-Ray digest preconditions were reconciled first and both match the hexaemeron-x-ray-preaudit overlay, skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10, so reuse was permitted; it was not used, the adapter's plan, assemble, bind-outputs and promote transitions did not run, and architecture.json, x-ray.md, entry-points.md and invariants.md were not regenerated, so no completed X-Ray run is claimed for round 5 and the cache is left as round 3 promoted it. That is a gap in this round's coverage of the pre-audit views, stated rather than papered over. The findings below rest on direct reading of the three changed sources, on execution against the tree, and on targeted mutation, each reproducible from what is recorded here. Gate-side enforcement remains unchecked and unchanged from rounds 1 to 4: no non-test contract implements AccountResolver, no gate consumes ResolvedThreshold, both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, and JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol and WildcatHostAdapter.sol are byte-identical across the whole of step 2, so step 4 still owns joining the two halves and the three register lines that depend on that join are marked not-applicable here rather than reviewed. The six trails carried from step 1 round 1 are unchanged and were not re-derived this round. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Multi-byte whitespace remains outside the blank-symbol guard on round 4's recorded reasoning, unchanged and not re-executed this round. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, verified again this round. The ambiguity walk is O(dots) adapter calls with O(n^2) memory copying in the written name's length; the manifest is repo-local trusted input to a test harness and no bound was added, which is recorded as a boundary rather than raised as a finding.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R5-01 | high | plugins/janus/harness/src/ManifestReader.sol | 4163a5bb's ambiguity guard asked the adapter about exactly one competing reading, the whole written string, which is only the last of them; a written name has one reading per dot plus the whole string, and the one the guard skipped is the shape this manifest actually writes, since every dotted target here is account.function and so a dotted account carrying its function suffix is account.part.function whose live reading is the intermediate one. With an adapter holding USDC at 0xC0 and USDC.e at 0xCE, the guard fired on USDC.e and was silent on USDC.e.transfer, binding the permit to canonical 0xC0 -- the bridged token the manifest named left unpermitted and one it never named permitted instead, on both the call-target and external-slot paths. That is the whole of S2-R3-01 surviving one dot further out. Executed against the committed tree at 33cf3ae0 before the fix: three guards fail there and pass after it | fixed in db25bacc |
| S2-R5-02 | informational | plugins/janus/harness/src/ManifestReader.sol | a round 4 lead decided rather than left open, recorded because the refusal it produces names a symbol the manifest author did not write: where the adapter knows the whole dotted name and not the grammar's prefix, the reader reports UnresolvableSymbol(prefix) and does not fall back to the reading the adapter does hold, because a fallback would make which string the grammar reads as the account depend on what the adapter happens to contain, the delegation of the reader's own grammar that the blank guard exists to prevent; the value path resolves the same name because there the whole string is the name, so the two paths differ in outcome kind by design while never binding one name to two addresses | accepted |
| S2-R5-03 | medium | plugins/janus/harness/adapters/ManifestFuzz.sol | a regression in this round's own fix for S2-R5-01, caught by re-reading the suite it shipped with: fuzzPathAgreement, the path-agreement generator added alongside that fix and the one property here that needs no oracle, set sawPathDisagreement and nothing ever read it -- no echidna_GL11_ property function, no assertion in invariant_manifest_resolution_holds, no pairing in invariant_every_ghost_flag_agrees_with_its_property, and no ghost probe test -- so neither engine could report it and Foundry never called it. Deleting the entire body of fuzzPathAgreement left all 83 tests green, which is the exact structural blindness this suite's own comments were written about and the same shape S2-R4-02 made a finding one round earlier | fixed in db25bacc |
| S2-R5-04 | low | plugins/janus/harness/src/ManifestReader.sol | the contract header still described the pre-widening guard, saying the reader refuses where the adapter knows the whole written string, which is precisely what S2-R5-01 changed, so the header understated the reader it documents in a step whose stated purpose is that the written grammar is the enforced one | fixed in db25bacc |

Leads not pursued: the round opened on an uncommitted tree carrying the S2-R5-01 fix and its guards, written but never logged, committed or receipted, so the finding was established rather than accepted. It reproduces exactly: restoring only ManifestReader.sol to its committed state at 33cf3ae0 while keeping the new tests fails test_a_multi_dot_call_target_known_at_an_inner_prefix_is_ambiguous, test_a_multi_dot_external_slot_known_at_an_inner_prefix_is_ambiguous and test_a_multi_dot_target_known_at_an_inner_prefix_at_zero_is_ambiguous, and all three pass with the fix restored. The two S2-R5-02 tests pass against the committed reader as well, which is what identifies them as a recorded decision rather than a fix. The Elenchus verdict is guarded, with the method stated because the runner contract could not establish it alone: run_forge_tests.py at the fixes commit db25bacc writes tmp/elenchus/fiat-329-step-2.xml with 85 tests, 0 failures and 0 errors across 8 suites, but the whole-commit-against-parent form does not compile, for the mechanical reason rounds 1, 2 and 4 all hit -- the new test file references echidna_GL11_paths_agree_on_one_name and sawPathDisagreement, and neither exists at 33cf3ae0, confirmed by reading that blob directly rather than inferring it. The guard claim rests instead on the two targeted restorations above, each executed in both directions. S2-R5-03 was found by asking what reads each ghost the suite sets, not by a campaign: the Echidna run at the round's opening tree enumerated GL00 to GL10 and no GL11, which is what exposed it, and the same question against the test file found no assertion and no probe. Confirmed by mutation rather than reading alone: gutting fuzzPathAgreement to its signature left 83 of 83 green on the pre-fix suite, and the identical mutation against the fixed suite fails test_gl11_draws_actually_bind_on_more_than_one_path; GL11 returning true unconditionally fails test_gl11_reports_a_path_disagreement_and_nothing_else. GL11's coverage claim is deliberately not a counter threshold on the property itself: the vacuity it is exposed to is GL00's, an engine that reaches no reader binds nothing and agrees trivially, and GL00 already reports that state for the whole suite, so a second copy would fail beside it under both engines and say nothing GL00 did not. It is made deterministically instead by driving all eight names in _pathName once and requiring at least two to bind on more than one path; the table was traced by hand against FuzzResolver and three names bind -- asset, dot-free and identical on all three paths, roleProvider.getCredential, whose suffix the call and slot paths both discard, and asset.e, which binds on the value path alone and is the S2-R5-01 shape GL10 rather than GL11 is responsible for. GL11 is therefore stated as not standing in for GL10 and the reverse, since GL11 sees only the case where two paths both bind. Not pursued: whether the O(n^2) candidate construction in _refuseASecondReading deserves a length bound, left because the input is a repo-local manifest in a test harness and a bound would be an unevidenced change to a fail-closed path; and whether _pathName should be generated rather than a fixed table, left because the property is about the paths disagreeing rather than about the alphabet, which is the reason its own comment gives. Suites at exit: forge test 85 passed 0 failed across 8 suites, against the 83 this round opened with, the 2 new being the GL11 probe test and its coverage test; forge coverage on src/ManifestReader.sol 100.00% at 115/115 lines, 157/157 statements, 23/23 branches and 17/17 functions, against round 4's 107/140/22/16, the additions being _refuseASecondReading and the guards for it; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK, which first reported 4 failures that were the portable-runtime mirror being one tree behind the fixes and not a defect, cleared by the resync in b32aefc4; portable_promise_machine.py check exit 0 and horos check exit 0, with the scan and sync alternated to a fixpoint compared by content digest rather than by changed-path list, settling on the first pass and confirmed by a second and third that changed nothing, every mirrored janus source byte-identical to its in-tree original checked with cmp; janus.py validate exit 0 on the shipped manifest; both campaigns re-run sequentially through run_fuzz_campaigns.py, one engine per invocation, Echidna exiting 1 over 100015 total calls with 6068 unique instructions, corpus 21 and seed 4956836687935943191, and Medusa exiting 7 with 29 passed and 1 failed against round 4's 24 and 1, the rise being GL11 and its ghost getter; GL00 is the only failure under either engine, both now enumerate echidna_GL11_paths_agree_on_one_name by name and Medusa lists sawPathDisagreement among its assertion tests, and the cause of GL00 is unchanged and was re-checked rather than inherited; git status clean, with no corpus or ignored artefact staged and both engine corpus directories still matching .gitignore's **/corpus/.

## Step 2, round 6 -- 2026-08-29T17:13:19Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run -- no role fan-out, no wait barrier, no deduplication record, no completeness pass and no judgement record -- so nothing here rests on a solidity-auditor report and none is cited. The X-Ray digest preconditions were reconciled again and both still match the hexaemeron-x-ray-preaudit overlay, skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10; reuse was permitted and again not used, so the adapter's plan, assemble, bind-outputs and promote transitions did not run, architecture.json, x-ray.md, entry-points.md and invariants.md were not regenerated, and no completed X-Ray run is claimed for round 6. That gap is unchanged from rounds 4 and 5 and is stated rather than papered over. This round's reading was scoped to what round 5 changed -- src/ManifestReader.sol, adapters/ManifestFuzz.sol and test/ManifestReader.t.sol -- plus a ten-mutant battery over the new walk; the rest of the harness was not re-read this round, and the four gate-engine facts and six step 1 trails were not re-derived. Gate-side enforcement remains unchecked and unchanged from rounds 1 to 5: no non-test contract implements AccountResolver, no gate consumes ResolvedThreshold, both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, and JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol and WildcatHostAdapter.sol remain byte-identical across the whole of step 2, so the three register lines that depend on that join stay not-applicable. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Multi-byte whitespace remains outside the blank-symbol guard on round 4's recorded reasoning, not re-executed this round. The O(dots) adapter calls and O(n^2) candidate construction in the walk remain unbounded, unchanged from round 5 and still recorded as a boundary rather than a finding, the input being a repo-local manifest in a test harness. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, verified again this round.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R6-01 | low | plugins/janus/harness/src/ManifestReader.sol | _refuseASecondReading opened with if (head == b.length) return; and its comment gave that line the guarantee that hook, host and a bracketed slot expression never reach the adapter twice, but the loop bound already carries it: a dot-free name has head == b.length, so i starts at b.length + 1, the condition i <= b.length is false, and the body never runs. Deleting the line changed nothing anywhere in the suite, 85 of 85 still green, which is the same dead guard as S2-R4-03 one round further on and the third instance in this step of a line that reads as the protection and is not it. The guarantee itself is real rather than merely relocated, checked from the other side: starting the walk at 0, so the grammar's own prefix is asked a second time, fails 32 tests | fixed in ef0aed34 |
| S2-R6-02 | informational | plugins/janus/harness/adapters/ManifestFuzz.sol | one of fuzzPathAgreement's three comparisons cannot fire at this tree: the call and slot arms resolve the same written name through the same _resolveDotted grammar and differ only in which manifest field carries it, so slotBound always equals callBound and the call/value comparison reaches any single-arm defect first; deleting the slot/value line changes no result, confirmed by mutation rather than assumed. Recorded and kept rather than removed, because steps 3 and 4 give the two arms different resolution work and the line becomes reachable when they stop being the same path, with the comment now saying so instead of implying cover it does not currently give | accepted |

Leads not pursued: the round audited the tree round 5 shipped, and its two findings are the two survivors of a ten-mutant battery over the new walk and the GL11 property. The eight caught are recorded here so the survivors can be read against them. Killed: asking only about the whole written string, which is S2-R5-01 restored, 18 failures against the 4 it produced before GL11 went live; skipping the whole-string reading and asking only about inner dots, 12; demanding a non-zero address alongside ok, which is S2-R4-01 restored one layer out, 8, GL10 among them; passing head as 0 so the grammar prefix is asked twice, 32; an off-by-one in the candidate copy, 18, GL11 among them; starting the walk at 0, 32; GL11 returning true unconditionally, 2; and GL11 reading sawWidenedSet instead of its own ghost, 4, failing both its probe test and GL01's. GL11 appearing in the failure set of the reader mutants rather than only the ghost-probe ones is the round's evidence that S2-R5-03's fix did more than restore a name: the property now kills reader defects, which it could not do in any form before ef0aed34's parent. The two survivors are S2-R6-01 and S2-R6-02 above, and neither is a removal of a check against a correct reader in round 4's sense -- S2-R6-01 is a line no reader defect can reach and S2-R6-02 is a comparison no defect can reach first, so neither is left as an unexplained survivor. The Elenchus verdict is unguarded, meant literally rather than as a criticism: this round's fix is the deletion of a line no test can distinguish the presence of, which is the finding itself, so no test can fail without it by construction. run_forge_tests.py at ef0aed34 writes tmp/elenchus/fiat-329-step-2.xml with 86 tests, 0 failures and 0 errors across 8 suites, and the guarantee the deleted line claimed is separately checked by the walk-from-0 mutant above. One test was added for the only shape of the widened walk that had none, a slot whose bracket expression contains a dot: the walk does not parse brackets, so it asks about roleProvider.lenderStatus[a as well as the whole string, and that candidate is not a name any adapter holds, so it cannot make the reader refuse a slot it should resolve. Pinned rather than argued, because the false-fail direction of a widened guard is what breaks working manifests. Not pursued: whether the bracket-unaware walk should parse brackets rather than ask about meaningless candidates, left because the extra candidate is harmless and parsing would add grammar the schema does not define; and whether _pathName should be generated rather than a fixed table, carried unchanged from round 5 on the reason its own comment gives. Suites at exit: forge test 86 passed 0 failed across 8 suites, against the 85 this round opened with, the 1 new being the bracket test; forge coverage on src/ManifestReader.sol 100.00% at 114/114 lines, 155/155 statements, 22/22 branches and 17/17 functions, against round 5's 115/157/23/17, the reduction being exactly the removed line and its branch; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK after the resync in a6d9a0d3; portable_promise_machine.py check exit 0 and horos check exit 0, with the scan and sync alternated to a fixpoint compared by content digest, settling on the first pass and confirmed by a second and third that changed nothing, every mirrored janus source byte-identical to its in-tree original checked with cmp; janus.py validate exit 0 on the shipped manifest; both campaigns re-run sequentially through run_fuzz_campaigns.py, one engine per invocation, Echidna exiting 1 over 100029 total calls with 6068 unique instructions, corpus 21 and seed 170787654568877309, and Medusa exiting 7 with 29 passed and 1 failed, both figures unchanged from round 5; GL00 is the only failure under either engine, all eleven of GL01 to GL11 are enumerated as passing by both, and the cause of GL00 is unchanged and was re-checked rather than inherited; git status clean, with no corpus or ignored artefact staged and both engine corpus directories still matching .gitignore's **/corpus/.

## Step 2, round 7 -- 2026-08-29T17:19:03Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run -- no role fan-out, no wait barrier, no deduplication record, no completeness pass and no judgement record -- so nothing here rests on a solidity-auditor report and none is cited, unchanged from rounds 4, 5 and 6. The X-Ray digest preconditions were reconciled again and both still match the hexaemeron-x-ray-preaudit overlay, skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10; reuse was permitted and again not used, so architecture.json, x-ray.md, entry-points.md and invariants.md were not regenerated and no completed X-Ray run is claimed for round 7. That gap runs through rounds 4 to 7 and is the largest single thing this step's audit has not done; it is stated here rather than left for the close to imply otherwise. Gate-side enforcement remains unchecked and unchanged from rounds 1 to 6: no non-test contract implements AccountResolver, no gate consumes ResolvedThreshold, both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, and JanusHarness.sol, StateDeltaRecorder.sol, WildcatHostModel.sol and WildcatHostAdapter.sol remain byte-identical across the whole of step 2, so the three register lines depending on that join stay not-applicable and step 4 still owns it. The six trails carried from step 1 round 1 are unchanged and were not re-derived this round. Also unchecked: the manifest fields the reader does not read (rollbackRule, entryPoints, extraDataAllowed, failureMode, mayReturnValues), which step 6 owns; slot-level write enforcement; transient storage as an effect class; hosted CI, the controller receipt, push and publication. Multi-byte whitespace remains outside the blank-symbol guard on round 4's recorded reasoning, not re-executed this round. The O(dots) adapter calls and O(n^2) candidate construction in the second-reading walk remain unbounded and recorded as a boundary rather than a finding. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, verified again this round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round found nothing, and the reading was widened rather than narrowed to reach that, round 6 having scoped itself to the three files round 5 changed. The whole of src/ManifestReader.sol was re-read at 911c98b11444f88d172576ead88c3977a5746a402ff0f0855829366bd02c7df6, including the helpers no recent round had examined -- _thresholdByAction, _resolveCalls, _resolveStorageWrites, _resolveValueMovements, _shrink, _arrayLength, _indexed, _utoa, _eq, _isBlank and _symbolOf -- and every private helper was checked for a caller, since an unused one would be the same dead-code shape as S2-R4-03 and S2-R6-01; all eleven have live callers. _resolveStorageWrites and _resolveValueMovements need no _shrink because every branch either assigns its slot or reverts, so filled always equals length, unlike _resolveCalls where a staticcall entry resolves and admits nothing; that asymmetry is deliberate and is what GL01's cardinality arms check. Twelve mutants were run against this tree and all twelve caught, which is the round's substance rather than the re-read alone: the duplicate-action refusal removed, 4 failures; the threshold scan returning at its first match, 4; a staticcall entry admitted into the state-changing call set, 18; an unknown call kind silently ignored rather than reverting, 6; the call arm shrunk to length instead of admitted, 16, GL01 first; the delegate arm shrunk one short, 4; the blank-symbol guard disabled, 16; the gas budget read positionally from .thresholds[0], 6; a value movement split at the dot, which is S2-R3-01 restored, 10; the external scope resolving its whole slot string instead of its prefix, 14; _symbolOf returning the whole name, 60; and storage scope host resolving hook, 4, GL09 first. Two kills name guards this run added that had not previously caught a defect elsewhere in the reader: the bracket test added in round 6 is the first failure reported for the external-scope mutant, and GL11 from round 5 fires on the value-movement and cardinality mutants. No survivor was produced, so unlike rounds 4 and 6 there is no survivor to pair with a defect it names. Not pursued and carried unchanged: whether the bracket-unaware walk should parse brackets rather than ask about meaningless candidates, left because the extra candidate is harmless and parsing would add grammar the schema does not define; whether _pathName should be generated rather than a fixed table, left on the reason its own comment gives; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed because the study keeps the Python validator untouched by this run, with S2-R2-09 still accepted on the same reasoning. Suites at exit: forge test 86 passed 0 failed across 8 suites, unchanged from the 86 this round opened with, no file altered by the round; run_forge_tests.py writes tmp/elenchus/fiat-329-step-2.xml with 86 tests, 0 failures and 0 errors across 8 suites, and there is no fixes commit this round so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK with no resync needed, the tree being unchanged since a6d9a0d3; portable_promise_machine.py check exit 0 and horos check exit 0 without rerunning either generator; janus.py validate exit 0 on the shipped manifest; both campaigns re-run sequentially through run_fuzz_campaigns.py against the same contract digests this round read, adapters/ManifestFuzz.sol at 4eee2e001052a27348e8d74662bdf03f68619ddc02430ed373c265c51cef8620 and test/ManifestReader.t.sol at b7b0b287de1c035a89f857d63ec7050f9d7bb53637b82c167689795f0995b418, Echidna exiting 1 over 100057 total calls with 6068 unique instructions, corpus 21 and seed 2577866560745691159, and Medusa exiting 7 with 29 passed and 1 failed; GL00 is the only failure under either engine, all eleven of GL01 to GL11 are enumerated as passing by both, and the cause of GL00 is unchanged and was re-checked rather than inherited; git status clean, with no corpus or ignored artefact staged and both engine corpus directories still matching .gitignore's **/corpus/.

## Step 3, round 1 -- 2026-08-29T18:03:22Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run -- no role fan-out, no wait barrier, no deduplication record, no completeness pass and no judgement record -- so nothing here rests on a solidity-auditor report and none is cited, unchanged from step 2 rounds 4 to 7. The X-Ray digest preconditions were reconciled again and both still match the hexaemeron-x-ray-preaudit overlay, skills/x-ray/SKILL.md at b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41 and lib/xray_reuse.py at 23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10; reuse was permitted and not used, so architecture.json, x-ray.md, entry-points.md and invariants.md were not regenerated and no completed X-Ray run is claimed for this round. The reading was scoped to what step 3 added -- HostAdapter.sol, WildcatHostAdapter.sol, MockRoleProvider.sol and WildcatAdapter.t.sol -- plus the fuzz suite it changed; the reader itself was not re-read, having closed clean at step 2 round 7. Gate-side enforcement remains unchecked: the adapter now answers names, but no gate consumes a resolved threshold, both conformance suites still hand-build the arrays they enforce and still read .thresholds[0].gasBudget positionally, and JanusHarness.sol, StateDeltaRecorder.sol and WildcatHostModel.sol are unchanged by this step, so step 4 still owns joining the halves. WildcatHostAdapter.categoryOf still has no caller in src, adapters or scripts and is exercised only by tests; that is unchanged from step 2 round 4, and resolveAccount deliberately does not call it. The manifest fields the reader does not read, slot-level write enforcement, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, confirmed again by GL00 failing under both. Two things about MockRoleProvider are recorded rather than raised: grant and validateCredential are unauthenticated, and it models no expiry policy or provider registry, because it exists to be a call target of the right shape rather than the access-control model under test, which HonestAccessHook is.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | medium | plugins/janus/harness/adapters/ManifestFuzz.sol | this step's entire product is a resolver implementation and the invariant suite resolved through a different one: ManifestFuzz deployed FuzzResolver and carried no reference to WildcatHostAdapter at all, so properties GL01 to GL11 were statements about the stub's table and no campaign executed a line of the shipped one, leaving any change to resolveAccount invisible to every campaign. The register's over-permit-by-category line names WildcatHostAdapter explicitly and until now rested on deterministic tests alone, which is the same shape S2-R4-02 recorded one step earlier for the reader's ambiguity guard. GL12 and GL13 now drive a real adapter over a real model, hook, asset and provider, across ten drawn names -- four held and six near-misses a prefix, case-folding or category-keyed table would answer for | fixed in fc9f044a |
| S3-R1-02 | low | plugins/janus/harness/src/wildcat/WildcatHostAdapter.sol | the adapter's header claims the name table mirrors categoryOf and nothing checked it; the two encode the same four deployment facts in opposite directions, name to address and address to kind, so either can drift from the other silently, which is the documentation-against-behaviour gap S2-R5-04 recorded for the reader's own header one step earlier | fixed in fc9f044a |

Leads not pursued: the round opened with a thirteen-mutant battery over the step's new code, and eleven of the thirteen were caught by the deterministic tests alone before any fix. Killed: hook resolving to the host, 4; an unknown name answering ok, 6; a zero roleProvider collapsed into the unknown answer, 2; asset resolving to the provider, 4; validateCredential made view, 4; getCredential returning a constant, 2; the validations counter never incrementing, 4; host resolving to the asset, 4; categoryOf dropping its asset arm, 2; and categoryOf classifying the borrower as a role provider, 2. Those last two are S3-R1-02's guards and were run after the fix rather than before it. That the battery found nothing turned the round toward the artefact rather than the code: the tests caught every defect put to them, so the question left was what nothing was putting to them at all. The answer was the campaign, and it is S3-R1-01. Both directions of the fix were checked rather than assumed. Forward: the unknown-name mutant now fails GL12 and the asset-to-provider mutant now fails GL13, each having been caught only by a unit test before. Backward: GL12 returning true unconditionally fails its probe test, GL13 pointed at sawWidenedSet fails both its own probe and GL01's, and fuzzAdapterTable gutted to its signature fails the deterministic coverage test that requires ten draws and exactly four resolutions. GL12 and GL13 are also the first properties in this suite that are not vacuous under the external engines, and that is a property of where they sit rather than a claim about them: every earlier property is a negated ghost set only after resolveJson returns, and neither engine implements the JSON cheatcodes the reader is built on, so GL01 to GL11 hold there without testing anything, which is what GL00 exists to report. fuzzAdapterTable calls the adapter directly and touches no cheatcode. Echidna's unique instruction count rises from 6068 at step 2 round 7 to 8134 here, and Medusa's passing count from 29 to 36, which is the adapter code being reached rather than a change in the reader. One piece of evidence was discarded rather than reported: the first campaign pair of this round overlapped the mutation battery, so their compile may have caught a mutated tree, and both were re-run to completion against a tree confirmed clean by git status and by digest before the figures above were taken. Not pursued: whether resolveAccount should be built from categoryOf, decided against and recorded in the adapter's header, because answering by category is the widening this step exists to avoid; whether the table should be data rather than four keccak comparisons, left because four names do not earn a mapping and the comparisons are what make the near-miss tests meaningful; and whether MockRoleProvider should model expiry or a provider registry, left because HonestAccessHook already holds the credential behaviour the gates measure. Suites at exit: forge test 99 passed 0 failed across 9 suites, against the 94 this round opened with, the 5 new being 2 GL probe tests, 1 GL coverage test and the 2 categoryOf correspondence tests; run_forge_tests.py writes tmp/elenchus/fiat-329-step-3.xml with 99 tests, 0 failures and 0 errors; forge coverage gives src/wildcat/MockRoleProvider.sol 100.00% and src/wildcat/WildcatHostAdapter.sol 69.57% lines, with resolveAccount itself fully covered and every uncovered line -- rollbackRule, roles, categoryOf and two driveAction arms -- predating this step; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0, alternated to a fixpoint compared by content digest, settling on the first pass; janus.py validate exit 0 on the shipped manifest; Echidna exits 1 over 100072 total calls with 8134 unique instructions, corpus 21 and seed 338263617812877225, and Medusa exits 7 with 36 passed and 1 failed, GL00 the only failure under either engine and GL01 to GL13 all enumerated as passing by both; git status clean, with no corpus or ignored artefact staged.

## Step 3, round 2 -- 2026-08-29T18:08:19Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged from round 1 and from step 2 rounds 4 to 7; the two overlay digests were reconciled again and both still match. The reading was scoped to round 1's own fix -- the GL12 and GL13 additions to ManifestFuzz.sol and their wiring in ManifestReader.t.sol -- because that is the only thing that changed since the previous round. The adapter, the mock provider and the reader were not re-read this round. Gate-side enforcement remains unchecked and unchanged: no gate consumes a resolved threshold, both conformance suites still hand-build their arrays and read .thresholds[0].gasBudget positionally, and step 4 owns joining the halves. categoryOf still has no caller outside tests. The unauthenticated grant and validateCredential on MockRoleProvider remain recorded rather than raised, on round 1's reasoning. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, confirmed again by GL00 failing under both.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R2-01 | low | plugins/janus/harness/adapters/ManifestFuzz.sol | GL13 folded two opposite defects into one ghost: a held name resolving to the wrong address points a permit at an account the manifest never named, and a held name that stops answering makes a permission the manifest granted silently disappear, but both set sawAdapterCrossBound, so a reader of a GL13 failure could not tell which had happened and the two want different fixes. Executed before the split, making roleProvider answer ok false and making it answer with the asset's address each failed GL13 and nothing that distinguished them; after it, the first fails GL14 and the second GL13. This is the standard round 1 set for itself one round earlier and then missed, since S2-R4-02 needed two ambiguous names in GL10 for the same reason | fixed in 93f0812c |

Leads not pursued: the round audited round 1's own fix and nothing else, on the reasoning that every fix in this run so far has carried a regression -- S2-R4-03, S2-R5-03 and S2-R6-01 were each found in the round that produced them or the one after. That expectation was correct again here. The finding came from asking what a failure of each new property would tell a reader, not from mutating for a wrong answer: GL12 and GL13 both survive every mutation that makes them report falsely, and the defect was in what a true report meant. The rest of round 1's fix was checked and stands. The early return in the unheld-name arm of fuzzAdapterTable is load-bearing rather than the dead-guard shape this run has now found three times, because without it an unheld name falls through into the held-name expectation and reports a cross-binding that did not happen; that was confirmed by deleting it rather than by reading. _isHeldName duplicates the adapter's own four names inside the generator, which is deliberate and is the same independence GL09 and GL13 rely on: an oracle that asked the adapter what it holds would agree with the adapter by construction and could not falsify anything. adapterResolved is read by the deterministic coverage test and is not vacuous. Not pursued: whether GL12 should likewise be split by near-miss class -- case, whitespace, category label, unnamed role -- left because all four are the same defect, a name outside the table answering, and no fix distinguishes them, which is the test GL13 failed and GL12 passes. Suites at exit: forge test 100 passed 0 failed across 9 suites, against the 99 this round opened with, the 1 new being GL14's probe test; run_forge_tests.py writes tmp/elenchus/fiat-329-step-3.xml with 100 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0, alternated to a fixpoint compared by content digest, settling on the first pass; janus.py validate exit 0 on the shipped manifest; Echidna exits 1 over 100046 total calls with 8168 unique instructions, corpus 22 and seed 6450648613154958304, against round 1's 8134 and corpus 21, and Medusa exits 7 with 38 passed and 1 failed against round 1's 36; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean, with no corpus or ignored artefact staged.

## Step 3, round 3 -- 2026-08-29T18:12:34Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged from rounds 1 and 2 and from step 2 rounds 4 to 7; both overlay digests were reconciled again and still match. This round widened back out from round 2's narrow scope and read the seam rather than the table: HostAdapter.sol, the AccountResolver declaration in ManifestReader.sol, and every contract in the tree that implements it. The reader's own body was not re-read, having closed clean at step 2 round 7, and the name table was not re-mutated, rounds 1 and 2 having exhausted it. Gate-side enforcement remains unchecked and unchanged: no gate consumes a resolved threshold, both conformance suites still hand-build their arrays and read .thresholds[0].gasBudget positionally, and step 4 owns joining the halves. categoryOf still has no caller outside tests. MockRoleProvider's unauthenticated grant and validateCredential remain recorded rather than raised. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, confirmed again by GL00 failing under both. One thing this round establishes only for the drift it executed: adding a parameter is one way a signature can move, and a return-type or mutability change was not separately run, so the finding is stated as an absent compile-time check rather than as an enumeration of every drift it would catch.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R3-01 | low | plugins/janus/harness/src/wildcat/WildcatHostAdapter.sol | the shipped adapter satisfied AccountResolver by matching its signature without declaring it, so the compiler checked nothing, and the protection ran exactly the wrong way round: all five stub resolvers in the tests -- FuzzResolver, StubResolver, ZeroResolver, OmniResolver and KnowsTheWholeNameAsZero -- declare the interface and are checked against it, while the one resolver that ships was not. Executed rather than argued: adding a parameter to AccountResolver.resolveAccount and rebuilding failed five contracts, every one of them a test double, and named WildcatHostAdapter in none, so the shipped adapter compiled through a drift that would have reverted every reader call at run time; with the declaration in place the same drift names it and the build fails | fixed in 23f400b9 |

Leads not pursued: the round asked what in this step is asserted by convention rather than checked by something. Rounds 1 and 2 answered that for the campaign and for ghost attribution; the seam was what remained. The declaration was put on the concrete adapter rather than on HostAdapter deliberately: HostAdapter keeps the virtual, which is what obliges any future host adapter to answer names at all, and pushing the interface up to it would make every adapter depend on the reader for a seam only a resolving adapter uses. That the virtual and the interface now both declare the same function is not duplication to remove -- the override names both parents, and deleting either loses a different guarantee. Round 2's fix was re-checked and stands: the C1 and C2 mutants still fail GL14 and GL13 respectively and still fail nothing else that would confuse them. Not pursued: whether AccountResolver should move out of ManifestReader.sol into a file of its own, which would let the adapter depend on the seam without depending on the reader and is the cleaner shape, left because it touches ManifestReader.sol and every importer and so falls outside this step's receipted Files list; it is recorded here as the better fix rather than done quietly. Also not pursued: a return-type or state-mutability drift, not separately executed, so this round's evidence covers the parameter-count case it ran. Suites at exit: forge test 100 passed 0 failed across 9 suites, unchanged by the fix, which is the expected shape for a change that alters no behaviour; run_forge_tests.py writes tmp/elenchus/fiat-329-step-3.xml with 100 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 on the shipped manifest; Echidna exits 1 over 100032 total calls with 8168 unique instructions, corpus 21 and seed 1081411489503577436, and Medusa exits 7 with 38 passed and 1 failed, both unchanged from round 2 as expected for a declaration-only change; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean.

## Step 3, round 4 -- 2026-08-29T18:16:35Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=reviewed; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which now runs through step 2 rounds 4 to 7 and step 3 rounds 1 to 4 without exception and is the largest thing this step's audit has not done; both overlay digests were reconciled again and still match. Gate-side enforcement remains unchecked and unchanged from every round of both steps: no non-test contract consumes a ResolvedThreshold, both conformance suites still hand-build the address arrays they enforce and still read .thresholds[0].gasBudget positionally, and JanusHarness.sol, StateDeltaRecorder.sol and WildcatHostModel.sol are unchanged by this step. The reader and the adapter now meet, but neither meets a gate, and step 4 owns that. categoryOf still has no caller outside tests, so the address(0)-classified-as-RoleProvider trail from step 2 round 4 remains unreachable and uncarried. The six trails from step 1 round 1 are unchanged and were not re-derived. MockRoleProvider's unauthenticated grant and validateCredential, and its absence of an expiry policy or provider registry, remain recorded rather than raised. Also unchecked: the manifest fields the reader does not read, slot-level write enforcement, transient storage as an effect class, hosted CI, the controller receipt, push and publication. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, confirmed again by GL00 failing under both. The AccountResolver drift executed in round 3 covered parameter count only; a return-type or mutability drift is still unrun.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round found nothing. It looked at the audit artefact and not only at the code, because rounds 1 and 2 both found the defect there rather than in the adapter. Six mutants ran against the generator and the contracts and all six were caught: _isHeldName returning true for every name, so GL12's oracle can no longer disagree with the table, 4 failures; _adapterName replacing a near-miss with a held name, which is the coverage shape S3-R1-01 was about, 2; adapterResolved never incrementing, 2; adapterDraws never incrementing, 2; the provider's grant made a no-op, 2; and the adapter's hook arm frozen to the zero address rather than reading model.hook(), 10. That last one is worth naming: the hook is the one entry in the table read live rather than captured at construction, because WildcatHostModel.setHook can move it, and the mutant confirms the live read is load-bearing rather than incidental. The related question -- what happens to a threshold resolved before a hook swap -- is a step 4 concern and is recorded there rather than answered here, since nothing in this step holds a resolved set across a host action. Round 3's fix was re-checked and stands, and being a declaration it changed no test count, no campaign figure and no coverage number, which is the expected shape. Round 2's split still attributes correctly. Not pursued and carried: moving AccountResolver out of ManifestReader.sol into its own file, recorded in round 3 as the cleaner fix and left because it falls outside this step's receipted Files list; a return-type or mutability drift of that interface; whether GL12 should be split by near-miss class, left on round 2's reasoning that all four are one defect; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed because the study keeps the Python validator untouched, with S2-R2-09 still accepted. Suites at exit: forge test 100 passed 0 failed across 9 suites, unchanged from the 100 this round opened with, no file altered by the round; run_forge_tests.py writes tmp/elenchus/fiat-329-step-3.xml with 100 tests, 0 failures and 0 errors, and there is no fixes commit so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK with no resync needed; portable_promise_machine.py check exit 0 and horos check exit 0 without rerunning either generator; janus.py validate exit 0 on the shipped manifest; both campaigns re-run against the exact contract digests this round read, WildcatHostAdapter.sol at 5072ca4fe77a5026fcf4a052be2093e95594439a20e29fd84bfd4b22dec653a4, ManifestFuzz.sol at c359bf1adeea66abb4b76653ce0b77ccb6f9a4e93b2d1c3ca1f6564985cddae7 and MockRoleProvider.sol at 3d90d22a41254cdc230c5f36f71ab68a25237b1872605c183baca2c2968292a0, Echidna exiting 1 over 100039 total calls with 8168 unique instructions, corpus 21 and seed 1455559175158520600, and Medusa exiting 7 with 38 passed and 1 failed; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean, with no corpus or ignored artefact staged.

## Step 4, round 1 -- 2026-08-29T18:36:16Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged through step 2 rounds 4 to 7 and step 3 rounds 1 to 4; both overlay digests were reconciled again and still match. Gate 1 is the only gate with an executable helper; gates 2, 3, 5, 6 and 7 are asserted by direct observation in individual tests rather than through named gate functions, which is pre-existing structure this step did not change and did not attempt to. This round reviewed the rewiring and the honest path; the reader, the adapter and the name table were not re-read, having closed clean at step 2 round 7 and step 3 round 4. Slot-level write enforcement remains outside the gates: resolution is account-granular, two hook-scope entries resolve to the same address twice, and `StorageWriteObs.slot` is recorded and never compared. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established. One thing this round establishes only for the shipped manifest and this harness: the disagreement below is a property of `wildcat-open-term.json` read through this gate implementation, and it is not a claim about what Wildcat's production hooks or a production role provider actually do.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | medium | plugins/janus/harness/manifests/wildcat-open-term.json | the shipped manifest's deposit threshold permits a state-changing call to the role provider and permits no write to it: roleProvider.validateCredential is declared kind call, the kind reserved for state-changing calls, while permittedStorageWrites lists two entries both scoped hook and nothing scoped to the provider. Gate 1 attributes a write to the hook when the written account is one the hook's subtree made a state-changing call into, which is the attribution that catches a laundered call, so a provider that records anything when validated produces a write the manifest did not enumerate and the storage half rejects the honest hook -- and recording is the only reason to declare the call state-changing rather than staticcall. Worked to cause: the honest deposit fails the storage half against the resolved write set and passes it when the provider address alone is added, so the rejection is the manifest's missing clause and not a gate defect; removing the provider's write makes the disagreement disappear and fails ten tests. The manifest is the artefact under test and was not edited to make a verdict green; the schema already carries the clause that would close the gap, a permittedStorageWrites entry of scope external whose slot names roleProvider, and adding it changes the Wildcat spec | pinned in 717c3b32; the manifest change is not made here and is carried to whoever owns that file |
| S4-R1-02 | low | plugins/janus/harness/test/WildcatConformance.t.sol | gate 1's storage half was asserted exactly once in the whole suite and only negated, by the hostile storage-mutation test, so it was never shown to accept anything; a resolved write set wrong in the permissive direction, or a storage check that rejected everything, would have been invisible from both sides. Asserting it on the honest deposit is what surfaced S4-R1-01 | fixed in 717c3b32 |
| S4-R1-03 | low | scripts/portable_promise_machine.py | the portable-runtime sync builds its file set from git-tracked files and its check does not verify import closure, so a new untracked source produces a mirror that omits it while the check still exits 0. Reached by executing rather than reading: adding IRoleProvider.sol left the mirrored HonestAccessHook.sol importing ./IRoleProvider.sol with no such file beside it, a runtime that cannot compile, and portable_promise_machine.py check reported success. Staging the file and re-syncing repaired the mirror. The tooling gap is outside this run's study, which keeps the repository scripts untouched, so it is recorded and carried rather than fixed | accepted |

Leads not pursued: the round set out to check the rewiring and found the manifest instead. The route there is worth recording, because it was not a mutation. The rewiring itself survives every mutation put to it, so the question became what the rewired tests still do not assert, and gate 1's storage half was the answer: two halves, one exercised on both the honest and hostile sides and one exercised only hostile. Asserting the missing half is what failed, and it failed for a reason in the manifest rather than in the assertion. The elenchus route from there was three steps: the honest deposit fails the storage half against its resolved write set; a diagnostic that widened the set by the provider address alone makes it pass, which locates the cause at one account; and removing the provider's write makes the whole disagreement disappear, which confirms the write is what the gate sees. The diagnostic widening was then removed from the shipped test and rebuilt inside the pinning test as evidence, so the test states the finding rather than working around it. Both directions are asserted so the pin cannot rot into a green tick. Not pursued: editing the manifest to add the external write scope, which would close the gap and is deliberately not done, since the manifest is what this run reads rather than what it writes and a spec change belongs to its owner; whether gate 1 should distinguish a callee's own bookkeeping from a hook writing through a proxy, which is a gate design question this step's Files do not reach and which the laundering attribution exists to prevent answering casually; and whether the remaining six gates deserve executable helpers like gate 1's, left as pre-existing structure. S2-R2-08, the janus.py J009 dotted-wildcard bypass, is still carried and unfixed, with S2-R2-09 still accepted. Suites at exit: forge test 102 passed 0 failed across 9 suites, against the 101 this round opened with; run_forge_tests.py writes tmp/elenchus/fiat-329-step-4.xml with 102 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0 after the mirror repair described in S4-R1-03; janus.py validate exit 0 on the shipped manifest, which is worth stating beside S4-R1-01: the manifest is valid against its schema, and the disagreement is between two of its clauses rather than a schema violation; Echidna exits 1 over 100027 total calls with 8168 unique instructions, corpus 21 and seed 4663104327572565219, and Medusa exits 7 with 38 passed and 1 failed, both unchanged from step 3 round 4 since this step changes no contract the campaign compiles; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean.

## Step 4, round 2 -- 2026-08-29T18:39:55Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=not-applicable; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged through every round of steps 2, 3 and 4; both overlay digests were reconciled again and still match. This round mutated the rewiring rather than re-reading it, and did not revisit the reader, the adapter, the name table or the manifest disagreement recorded as S4-R1-01, which stands as pinned. Gate 1 remains the only gate with an executable helper. Slot-level write enforcement remains outside the gates. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established. The campaigns were not re-run this round: no contract the campaign compiles changed, only a test file, and the step 4 round 1 figures stand.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R2-01 | low | plugins/janus/harness/test/HostileHooks.t.sol | the file's _threshold helper takes an action name and every call in it asked for deposit, so a helper that ignored its argument and always resolved deposit changed no result; the rewiring was correct and untested in the one dimension the register names, since threshold-lookup-no-fallback says selection is by name and nothing falls back to the first entry, and on the hostile side nothing could tell the two apart. The manifest supplies the comparison at 2000000 on deposit against 1000000 on setAnnualInterestAndReserveRatioBips, and asserting the two differ kills the mutant that pins the action | fixed in bb977d76 |

Leads not pursued: four mutants ran against round 1's tree and three were caught. Killed: the conformance _threshold pinned to deposit, 2 failures, caught by the round 1 test that resolves the rate action; gate 1's storage helper made to accept unconditionally, 4, which is the half round 1 brought under assertion and would have been silent before it; and the honest hook's provider call removed, 6. The survivor is S4-R2-01. Its fix surfaced a second fact, recorded rather than routed around: the new test installs a hook before resolving, because resolution is all or nothing and the deposit threshold's permittedStorageWrites are scope hook, so reading a budget still resolves the hook symbol and a host with no hook installed refuses with SymbolResolvesToZero. The hostile setUp installs no hook because each test installs its own. That refusal is the fail-closed posture working as specified, and it is stated in the test rather than worked around by reading the budget some other way. Not pursued: whether ResolvedThreshold should offer a budget-only read that skips symbol resolution, which would have made this test simpler and is deliberately not added, since a partial resolve is exactly the silent shrinking the reader refuses to do; whether the remaining six gates deserve executable helpers, carried from round 1; editing the manifest to close S4-R1-01, carried and still not done; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 103 passed 0 failed across 9 suites, against the 102 this round opened with; run_forge_tests.py writes tmp/elenchus/fiat-329-step-4.xml with 103 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 on the shipped manifest; git status clean.

## Step 4, round 3 -- 2026-08-29T18:44:11Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=not-applicable; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=not-applicable; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged through every round of steps 2, 3 and 4; both overlay digests were reconciled again and still match. The manifest disagreement recorded as S4-R1-01 stands as pinned and was not revisited. Gate 1 remains the only gate with an executable helper, and the other six are still asserted by direct observation in individual tests. Slot-level write enforcement remains outside the gates. The campaigns were not re-run this round: no contract the campaign compiles changed except MockRoleProvider, which the campaign deploys but whose counter the properties do not read, and the step 4 round 1 figures stand. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R3-01 | low | plugins/janus/harness/src/wildcat/HonestAccessHook.sol | setRoleProvider carries a set-once guard and nothing tried to set the provider twice, so deleting the guard changed no result anywhere in the suite; that is the dead-guard shape S2-R4-03 and S2-R6-01 both recorded, and the answer here is the opposite one, since a swapped provider would silently move the account a resolved permit names, so the guard is kept and exercised rather than removed | fixed in b618573b |
| S4-R3-02 | low | plugins/janus/harness/src/wildcat/MockRoleProvider.sol | validations is a counter and no test called validateCredential twice, so replacing validations++ with validations = 1 satisfied every assertion in the suite; a latch cannot report a second call, which is the only thing a counter is for, and the step 3 non-vacuity argument rested on it | fixed in b618573b |
| S4-R3-03 | informational | plugins/janus/harness/test/HostileHooks.t.sol | the hostile storage assertion cannot tell the resolved write set from the resolved call set, because the only hook-attributed write in that delta is the registry's and the registry is absent from both, so either set rejects and the verdict is the same. The two assertions now read one local so a swapped set reaches both, which is the most this file can do; distinguishing them needs a hostile hook that writes its own storage as well as the registry's, and src/hostile/HostileHooks.sol is outside this step's Files. The property is covered on the honest side, where swapping the write set for the call set fails two tests because the honest hook's one attributed write is the provider's and the call set contains it | accepted |

Leads not pursued: five mutants ran and two were caught outright: gate 1's call helper made to accept unconditionally, 6 failures, and the honest conformance write set swapped for the call set, 2. The three survivors are the three findings above, and the third stayed a survivor after a fix attempt rather than before one, which is why it is recorded as accepted with its reason rather than left unexplained. The attempt is recorded because it failed structurally rather than carelessly: the first version built the widened set from a second reference to the resolved threshold, so a mutant swapping the set in the assertion left the widened set alone and the pair still passed. Rewriting both to read one local closed that hole and the mutant still survived, which is what established that the two sets are genuinely indistinguishable from this delta rather than merely unasserted. Not pursued: adding a self-write to StorageMutationHook, which would distinguish them and is outside this step's Files, recorded here as the fix a later step or a runbook amendment could make; whether the remaining six gates deserve executable helpers like gate 1's, carried from rounds 1 and 2; editing the manifest to close S4-R1-01, carried and still not done, since the manifest is what this run reads rather than what it writes; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 104 passed 0 failed across 9 suites, against the 103 this round opened with; run_forge_tests.py writes tmp/elenchus/fiat-329-step-4.xml with 104 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 on the shipped manifest; git status clean.

## Step 4, round 4 -- 2026-08-29T18:47:17Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=not-applicable; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=not-applicable; honest-liveness-regression=reviewed

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged through every round of steps 2, 3 and 4; both overlay digests were reconciled again and still match. S4-R1-01 stands as pinned and S4-R3-03 as accepted; neither was revisited. Gate 1 remains the only gate with an executable helper. Slot-level write enforcement remains outside the gates. The campaigns were not re-run: no contract the campaign compiles changed this round. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round. Two of this round's five mutants were ill-formed and are reported as such rather than as survivors: replacing an assertion's condition with a constant, and deleting an assertion, both leave a correct tree green by construction and say nothing about the code under it. They are named below so the round's method is checkable rather than only its result.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R4-01 | low | plugins/janus/harness/src/wildcat/HonestAccessHook.sol | the guard making the role provider optional was a line no mutant could kill: every HonestAccessHook in the suite either had a provider set or was never driven, so calling the provider unconditionally -- which on an unset provider means calling address(0) and reverting -- changed no result anywhere, and the comment claiming the hook behaves exactly as before when left unset had nothing behind it. The hostile suite and gate 3 liveness path that comment names both run with a provider now, so the configuration it described was one no test used | fixed in c9c7040d |
| S4-R4-02 | informational | plugins/janus/harness/src/wildcat/IRoleProvider.sol | loosening getCredential from view to non-view in the interface changes no result, because MockRoleProvider declares its override view and Solidity permits an override to be more restrictive than the function it overrides. The view marker there is documentation of the manifest's staticcall kind rather than an enforced constraint, and the enforcement lives where it can be executed instead, in the step 3 test that drives a real staticcall at each entry point | accepted |

Leads not pursued: five mutants ran, one was caught, one is S4-R4-01, one is S4-R4-02 and two were ill-formed. Killed: the honest hook calling getCredential in place of validateCredential, 6 failures, which is the assertion that the permitted call is the state-changing one rather than the read. The two ill-formed mutants were an inverted budget assertion and a deleted widened-set assertion, both of which mutate the test rather than the subject and therefore survive against any correct tree; the useful version of each would perturb the manifest or the gate, and the manifest is not edited by this run. That distinction is recorded because three earlier rounds in this step reached their finding by mutating the artefact rather than the code. It would be easy to read these two the same way and count them as survivors. Not pursued and carried: adding a self-write to StorageMutationHook so the hostile storage assertion can distinguish the two resolved sets, S4-R3-03's stated fix, outside this step's Files; executable helpers for the six gates that lack them, carried from rounds 1 to 3; editing the manifest to close S4-R1-01, still not done, since the manifest is what this run reads rather than what it writes; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 105 passed 0 failed across 9 suites, against the 104 this round opened with; run_forge_tests.py writes tmp/elenchus/fiat-329-step-4.xml with 105 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 on the shipped manifest; git status clean.

## Step 4, round 5 -- 2026-08-29T18:52:00Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=not-applicable; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which now holds without exception through step 2 rounds 4 to 7, step 3 rounds 1 to 4 and step 4 rounds 1 to 5, and is the largest single thing this run's audit has not done; both overlay digests were reconciled again and still match. Gate 1 remains the only gate with an executable helper, and gates 2, 3, 5, 6 and 7 are still asserted by direct observation in individual tests rather than through named functions; that is pre-existing structure no step of this run changed. Slot-level write enforcement remains outside the gates: resolution is account-granular and StorageWriteObs.slot is recorded and never compared. S4-R1-01 stands as pinned rather than fixed, and the manifest is unedited. S4-R3-03 stands as accepted. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this step. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established, only that neither does at 2.3.3 and 1.5.1, confirmed again by GL00 failing under both.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round found nothing, and the battery was deliberately built only from mutants that perturb the subject rather than the assertions, after round 4 produced two ill-formed mutants that survive against any correct tree. Five ran and all five were caught: the honest hook dropping its monotone known-lender bit, 4 failures; the provider call moved ahead of the credential check, so an unapproved lender reaches the provider before being refused, 6; gate 1's storage check returning true whenever it is given a non-empty set, which is the shape that would have hidden S4-R1-01, 4; the provider's getCredential ignoring its account argument and returning a constant, 2; and the adapter resolving the `hook` name to the borrower rather than the installed hook, 12. The second is worth naming as a behaviour question rather than a bookkeeping one: the honest hook checks the credential first and calls the provider only after it has decided, so a lender it is about to refuse never reaches the provider at all, and moving the call earlier is caught rather than merely being a different ordering. The third is the direct regression guard for this step's own finding. Not pursued and carried into step 5 or beyond: adding a self-write to StorageMutationHook so the hostile storage assertion can distinguish the resolved write set from the call set, which is S4-R3-03's stated fix and sits outside this step's Files; executable helpers for the six gates that lack them; editing the manifest to add the external write scope that would close S4-R1-01, which is a change to the Wildcat spec and belongs to whoever owns that file; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed because the study keeps the Python validator untouched, with S2-R2-09 still accepted on the same reasoning. Suites at exit: forge test 105 passed 0 failed across 9 suites, unchanged from the 105 this round opened with, no file altered by the round; run_forge_tests.py writes tmp/elenchus/fiat-329-step-4.xml with 105 tests, 0 failures and 0 errors, and there is no fixes commit so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK with no resync needed; portable_promise_machine.py check exit 0 and horos check exit 0 without rerunning either generator; janus.py validate exit 0 on the shipped manifest, which is worth restating beside S4-R1-01: the manifest is valid against its schema and the disagreement is between two of its own clauses; both campaigns re-run against the exact contract digests this round read, MockRoleProvider.sol at cbb58d37a060a763dd0ea395da71b81e6f5f3665d310f066f81b0c7adc644313, HonestAccessHook.sol at 5c1f30a616c4d8c6a6dee40af5040c74850fbbc00b04c453ba7d89016c6e7ff3 and ManifestFuzz.sol at c359bf1adeea66abb4b76653ce0b77ccb6f9a4e93b2d1c3ca1f6564985cddae7, Echidna exiting 1 over 100044 total calls with 8168 unique instructions, corpus 21 and seed 1551177606380429248, and Medusa exiting 7 with 38 passed and 1 failed; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean, with no corpus or ignored artefact staged.

## Step 5, round 1 -- 2026-08-29T19:03:43Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=not-applicable; vm-signature-drift=not-applicable; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which now holds without exception through every round of steps 2, 3, 4 and 5; both overlay digests were reconciled again and still match. This round read the two fixtures and the new test file; the reader, the adapter, the gates and the conformance suites were not re-read, all having closed clean in their own steps. S4-R1-01 stands as pinned and the manifest remains unedited; the refusal proved here is about an omitted call entry and is a different question from that disagreement, which concerns a permitted call's storage effect. Gate 1 remains the only gate with an executable helper. Slot-level write enforcement remains outside the gates. The manifest fields the reader does not read, transient storage as an effect class, hosted CI, the controller receipt, push and publication all remain outside this round. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | medium | plugins/janus/harness/test/ManifestDisagreement.t.sol | the two fixtures are the shipped manifest with one edit each and the whole disagreement rests on that being true, and nothing checked it: changing the omitted-call fixture's deposit gas budget to 999 left every test green, so a fixture that drifted because the shipped manifest changed and the copy did not would leave the refusal comparing against a stale baseline while still passing, proving something weaker than it claims and looking identical from the outside. Both fixtures are now walked action by action against the shipped manifest and required to agree on everything the reader exposes, with exactly one exception each, and the comparison goes through the reader rather than over file bytes because what the disagreement depends on is the resolved threshold and two files can differ in whitespace or key order without differing in any way a gate could see | fixed in d21912f1 |

Leads not pursued: five mutants ran against the step's tree and four were caught, three of them decisively. The refusal's fixture restored to include the omitted call entry fails 4 tests, so the refusal depends on the fixture's content rather than on the hook failing everything. The unknown-symbol fixture pointed at a symbol the adapter does hold fails 2, so the abort depends on the symbol being unknown. The reader made to return a placeholder address instead of reverting on an unresolvable symbol fails 16, which is the largest kill this run has recorded and establishes that the fail-closed path is the reader's rather than any gate's. The survivor is S5-R1-01. One mutant is reported inconclusive rather than as a kill: deleting a hook-scope storage write from the omitted fixture failed 4 tests, but the edit left the file malformed, so what failed was parsing rather than the comparison, and it says nothing about drift detection; the well-formed version of that probe is the non-deposit permitted-calls edit run after the fix, which fails 2. Not pursued: comparing the fixtures to the shipped manifest by file bytes as well as through the reader, left because byte equality would fail on whitespace and key order without any resolved threshold differing, and the resolved threshold is what every verdict reads; a python-side fixture check in plugins/janus/tests, which would catch drift without a forge run and is outside this step's Files; editing the manifest to close S4-R1-01, still carried and not done; adding a self-write to StorageMutationHook, S4-R3-03's stated fix, still carried; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 112 passed 0 failed across 10 suites, against the 110 this round opened with; run_forge_tests.py writes tmp/elenchus/fiat-329-step-5.xml with 112 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 over the shipped manifest and both fixtures, which is the register's fixture-manifest-validity line and is what makes the refusal the gate's rather than the parser's; Echidna exits 1 over 100046 total calls with 8168 unique instructions, corpus 21 and seed 8559825852364392603, and Medusa exits 7 with 38 passed and 1 failed, both unchanged from step 4 since this step changes no contract the campaign compiles; GL00 is the only failure under either engine and GL01 to GL14 are all enumerated as passing by both; git status clean.

## Step 5, round 2 -- 2026-08-29T19:06:46Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=not-applicable; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=not-applicable; vm-signature-drift=not-applicable; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, unchanged through every round of steps 2 to 5; both overlay digests were reconciled again and still match. This round mutated rather than re-read, and did not revisit the fixtures' content, the adapter or the conformance suites. S4-R1-01 stands as pinned and the manifest remains unedited; S4-R3-03 and S4-R4-02 stand as accepted. Gate 1 remains the only gate with an executable helper. Slot-level write enforcement remains outside the gates. The campaigns were not re-run: no contract the campaign compiles changed this round, only a test file and a fixture that was restored, and the round 1 figures stand. Hosted CI, the controller receipt, push and publication remain outside this round.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round found nothing. Four mutants ran, three well-formed and all three caught, and one ill-formed. Killed: gate 1's call check made to ignore the allowed set entirely, 10 failures, which is the guard the whole refusal rests on; the reader made to admit a staticcall entry into the state-changing call set, 26, the largest kill of the step and the register's staticcall-kind-not-permitting-writes line, which matters here because the omitted-call fixture keeps its staticcall entry, so a reader that admitted it would make the refusal disappear and the fixture pair meaningless; and the honest hook calling getCredential in place of validateCredential, 12. The ill-formed one is recorded by name, this being the second step in which one appeared: limiting the fixture-drift walk to the first action changed no result, but a mutant that reduces a test's coverage is invisible on a correct tree by construction, because the actions it stops visiting are the ones that agree. The question it appears to ask was answered directly instead, by introducing real drift in a later action: changing the omitted fixture's queueWithdrawal budget to 12345 fails 2 tests, so the walk does cover the actions after the first. That probe was run and reverted, and the fixture is byte-identical to its committed form. Not pursued and carried: a python-side fixture check that would catch drift without a forge run, outside this step's Files; editing the manifest to close S4-R1-01, still not done since the manifest is what this run reads rather than what it writes; adding a self-write to StorageMutationHook, S4-R3-03's stated fix; executable helpers for the six gates that lack them; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 112 passed 0 failed across 10 suites, unchanged from the 112 this round opened with, no file altered by the round; run_forge_tests.py writes tmp/elenchus/fiat-329-step-5.xml with 112 tests, 0 failures and 0 errors, and there is no fixes commit so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; portable_promise_machine.py check exit 0 and horos check exit 0; janus.py validate exit 0 over the shipped manifest and both fixtures; git status clean.

## Step 6, round 1 -- 2026-08-29T19:17:42Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which holds without exception through every round of steps 2 to 6; both overlay digests were reconciled again and still match. This step ships no Solidity, so the register lines above were reviewed as prose claims against the code that already implements them rather than re-executed: each is stated in `SKILL.md`, the plugin `README.md` or a schema `description`, and each has a test behind it from the step that built it. The gates, reader, adapter and fixtures were not re-read. S4-R1-01 stands as pinned and the manifest remains unedited; S4-R3-03, S4-R4-02 and S4-R1-03 stand as accepted. Whether Echidna or Medusa will implement the JSON cheatcodes is still not established. The campaigns were not re-run: this step changes no contract the campaign compiles, and the step 5 figures stand. The demonstration renders a findings file the harness emitted in the same run; it is not evidence that the harness would find those five violations against a different hook set, only that the reporter renders what the harness wrote.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S6-R1-01 | low | plugins/janus/scripts/janus.py | the plugin carries three version surfaces that all say janus and now disagree: the skill label is janus-v0.2.0 in EVOLUTION.md and SKILL.md frontmatter, the claude and codex plugin manifests read 0.1.1, and janus.py holds VERSION = "0.1.0" so `janus --version` prints janus 0.1.0. The skill bump made in this step created the third disagreement, since the CLI constant had matched the skill's baseline. Nothing governs it: tests/test_evolution_contract.py binds the skill label to the ledger and tests/test_version_propagation.py binds the two plugin manifests to each other, and neither reaches a script constant, so no contract says which surface the CLI should track. It is recorded rather than bumped because it sits outside this step's Files, because the plugin manifests at 0.1.1 already show these axes were never locked together, and because deciding which of the three a CLI version means belongs to the skill's owner rather than to a run that happened to move one of them | accepted |

Leads not pursued: the round's subject was the ledger row. The strongest evidence about it is not this round's reading but the repository's own suite, run against deliberate corruptions rather than merely observed passing. A header naming janus-v0.3.0 while the row names 0.2.0 fails 2 tests in tests/test_evolution_contract.py, and changing the held Next Fiat job under a generation axis fails 1. Both were reverted and the ledger is byte-identical to its committed form. The row was also checked by hand against the versioning contract: axis generation, 0.1.0 to 0.2.0, the frontier revision second-host-adapter and its digest c244247ec1071dda04c29206e52efe3eab264e8c323eaf15468f03e3a9688764 retained byte for byte, the digest recomputed from the live status, revision, frontier and next-job line and matching, header and row naming the same version, and the Next Fiat job byte-identical to its form before this run, compared by digest rather than by eye. The axis is generation and not evolution because the held job is a second host adapter and this run did not ship one; the gates reading the manifest is a behaviour change under the contract's own definition, not a frontier advance, and claiming otherwise would have replaced a held target the contract forbids replacing. Two prose surfaces named in this step's Files needed no edit and that is recorded rather than left ambiguous: `.agents/skills/janus/SKILL.md` does not exist, the only mirror under `.agents/skills/` being promise-machine; and `docs/janus-manifest-resolution/study.md` refreshed to byte-identical output, because no study amendment was ever made and only the runbook was amended, four times. Two schema descriptions were used to carry S4-R1-01 into the format contract rather than leaving it in an audit log, which is the one place a later manifest author would read it. Not pursued and carried: editing the shipped manifest to add the external write scope that would close S4-R1-01, which remains a change to the Wildcat spec and belongs to its owner; adding a self-write to StorageMutationHook, S4-R3-03's stated fix; executable helpers for the six gates that lack them; a python-side fixture-drift check; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 112 passed 0 failed across 10 suites, unchanged by this step, which ships no Solidity; run_forge_tests.py writes tmp/elenchus/fiat-329-step-6.xml with 112 tests, 0 failures and 0 errors, and there is no fixes commit so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK, including test_evolution_contract 9 passed, test_version_propagation 7 passed, test_marketplace_prose, test_unique_identifiers and test_promise_machine_contract all OK; the imprimatur lint exits 0 at 100.0 over EVOLUTION.md, SKILL.md, the plugin README.md and AGENTS.md; janus.py validate exits 0 over the shipped manifest and both fixtures after the schema description edits, so the required shape is unchanged; the demonstration path runs, the harness emitting out/findings.test.json and janus.py report rendering it to Markdown and to SARIF 2.1.0 with five results, one per hostile hook; portable_promise_machine.py check exit 0 and horos check exit 0, alternated to a fixpoint compared by content digest and settling on the first pass; git status clean.

## Step 6, round 2 -- 2026-08-29T19:20:42Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=reviewed; hostile-set-regression=not-applicable; honest-liveness-regression=not-applicable

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which holds without exception through every round of steps 2 to 6; both overlay digests were reconciled again and still match. This step ships no Solidity and this round read prose and one JSON file. S4-R1-01 stands as pinned and the manifest remains unedited; S4-R3-03, S4-R4-02, S4-R1-03 and S6-R1-01 stand as accepted. The campaigns were not re-run: no contract the campaign compiles changed. The demonstration was not re-run this round; its round 1 result stands and the reporter was untouched.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S6-R2-01 | low | plugins/janus/harness/schemas/hook-manifest.schema.json | the description edits went through a JSON round-trip, which re-serialised the whole schema: the result was semantically identical, shown by structural comparison with descriptions stripped, but the diff ran to 91 insertions and 25 deletions of which only 10 lines carried meaning, and this step's Exit says description fields only. Reformatting required from one line to six is exactly the change a reviewer of a schema diff looks for and exactly the change that was not intended. The committed file is restored and the five descriptions applied as targeted text edits, leaving a 5-line diff with no non-description line moved | fixed in 57cc0072 |

Leads not pursued: the round checked the new prose claim by claim against the code that implements each one rather than reading it for sense, and every claim held: selection by action name and never by position, the account symbol as the text before the first dot, a staticcall entry admitting nothing state-changing, account-granular enforcement, and a value movement's names carrying no suffix are each present in the reader and each carry a test from the step that built them. The five named errors the prose promises are among the reader's eight, the other three being UnknownCallKind, UnknownStorageScope and AmbiguousAccountSymbol, and the prose does not claim those are the whole set. The finding came from checking the diff rather than the file: the schema read correctly and validated correctly, and only the shape of the change was wrong, which is a class this run had not looked at before and which no test in the repository covers, since a semantically identical reformat passes everything. That is a limit rather than a finding: no check here distinguishes a minimal diff from a noisy one, and the Exit's description-fields-only clause is enforced by reading. Not pursued and carried: bumping janus.py's VERSION constant, recorded as S6-R1-01 in round 1 and left to the skill's owner; editing the shipped manifest to close S4-R1-01; adding a self-write to StorageMutationHook, S4-R3-03's stated fix; executable helpers for the six gates that lack them; a python-side fixture-drift check; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed with S2-R2-09 still accepted. Suites at exit: forge test 112 passed 0 failed across 10 suites; run_forge_tests.py writes tmp/elenchus/fiat-329-step-6.xml with 112 tests, 0 failures and 0 errors; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; janus.py validate exits 0 over the shipped manifest and both fixtures after the restored schema, so the required shape is unchanged in fact as well as in claim; the schema parses; portable_promise_machine.py check exit 0 and horos check exit 0; git status clean.

## Step 6, round 3 -- 2026-08-29T19:23:34Z

Audit schema: fiat-audit-round/v2

Covered: unresolved-symbol-fail-closed=reviewed; threshold-lookup-no-fallback=reviewed; staticcall-kind-not-permitting-writes=reviewed; over-permit-by-category=reviewed; vm-signature-drift=not-applicable; fixture-manifest-validity=reviewed; hostile-set-regression=reviewed; honest-liveness-regression=reviewed

Not checked: the twelve-role solidity-auditor protocol did not run and no completed X-Ray run is claimed, which holds without exception through every round of steps 2 to 6 and is the largest single thing this run's audit has not done. Both overlay digests were reconciled again and still match. S4-R1-01 stands as pinned and the shipped manifest is unedited; S4-R1-03, S4-R3-03, S4-R4-02 and S6-R1-01 stand as accepted. The campaigns were not re-run: this step changes no contract the campaign compiles, and the step 5 figures stand. Gate 1 remains the only gate with an executable helper and slot-level write enforcement remains outside the gates, both unchanged by this run. The demonstration establishes that the reporter renders a findings file the harness emitted in the same run; it is not evidence about a different hook set.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the round found nothing and checked the three things this step can get wrong that a passing suite would not show. First, whether the committed spec is actually current: both copies were diffed against the receipted artefacts under the inverse of step 1's link rewrite and are identical, and the committed runbook names every file the run built, including ManifestReader.sol, WildcatHostAdapter.sol, MockRoleProvider.sol, IRoleProvider.sol, ManifestFuzz.sol and ManifestDisagreement.t.sol, which is what S2-R2-04 recorded as missing and this step reconciles. Second, whether the demonstration is reproducible rather than a one-off: out/findings.test.json was deleted, the emitting test re-run, and the reporter re-rendered it to Markdown and to SARIF 2.1.0 with five results. Third, whether the step's own artefacts are guarded: reverting the SKILL.md frontmatter to 0.1.0 fails 4 repository tests, and a single trailing space added to one schema description fails 2, so the ledger-to-frontmatter binding and the schema's description prose are both checked by the repository rather than by this run's reading alone. That second result revises round 2's stated limit: the repository does check schema description prose, so what it does not check is the shape of a diff, not the descriptions themselves. Not pursued and carried out of this run: bumping janus.py's VERSION constant, S6-R1-01, left to the skill's owner because no contract says which of three version surfaces a CLI should track; editing the shipped manifest to add the external write scope that would close S4-R1-01, which is a change to the Wildcat spec; adding a self-write to StorageMutationHook, S4-R3-03's stated fix; executable helpers for the six gates that lack them; a python-side fixture-drift check; the portable-runtime sync's blindness to untracked sources, S4-R1-03; and S2-R2-08, the janus.py J009 dotted-wildcard bypass, still carried and unfixed because the study keeps the Python validator untouched, with S2-R2-09 still accepted on the same reasoning. Suites at exit: forge test 112 passed 0 failed across 10 suites, unchanged by the round; run_forge_tests.py writes tmp/elenchus/fiat-329-step-6.xml with 112 tests, 0 failures and 0 errors, and there is no fixes commit so the Elenchus verdict is null; python3 -m unittest discover -s plugins/janus/tests 14 passed OK; the repository suite 460 passed OK; janus.py validate exits 0 over the shipped manifest and both fixtures; the imprimatur lint exits 0 at 100.0 over every document this step changed; portable_promise_machine.py check exit 0 and horos check exit 0; git status clean, with the regenerated findings file under the gitignored out/ directory and nothing staged from it.
