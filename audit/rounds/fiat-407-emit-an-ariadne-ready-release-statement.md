## Step 1, round 1 -- 2026-08-26T15:51:05Z

Audit schema: fiat-audit-round/v2

Covered: subject-binding=reviewed; predicate-fidelity=reviewed; untrusted-release=reviewed; output-confinement=reviewed; partial-write=reviewed; claim-inflation=reviewed; schema-drift=reviewed; determinism=reviewed

Not checked: the waived Pashov X-Ray and Solidity Auditor pipelines because the step ships no Solidity, Foundry or Hardhat surface; DSSE signing, cosign, publisher identity, provider completeness, consensus finality, canonical-chain proof, network or RPC behaviour; hostile concurrent writers with permission on the output directory; hosted CI, controller receipt, push, pull request, issue publication and final integration

Elenchus verdict: passed

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | `plugins/alexandria/scripts/alexandria_lib/statement.py:290`; `plugins/alexandria/tests/test_statement.py:442` | `_temporary()` created a fresh sibling and then called `os.fstat()` before returning its name and descriptor to `_write_statement()`. If that inspection raised, the caller still held `None` for both cleanup locals, so the open descriptor and temporary directory entry survived; direct `_temporary()` callers received the raw `OSError`, while `_write_statement()` converted it to `AlexandriaError` without recovering those resources. | fixed in `eb8dc5b32cfefd6d30a9c911b8052eb9bb3f1657`; the direct parent-red test raised `OSError: inspection failed`, while the fixed helper closes the descriptor, removes the fresh entry and raises a controlled `AlexandriaError` |
| S1-R1-02 | low | `plugins/alexandria/tests/run_tests.py:188`; `plugins/alexandria/tests/test_runner.py:223` | The new Elenchus report writer closed its descriptor after the same post-open `os.fstat()` failure but deleted only when an inode identity existed. The fresh report therefore remained and made the next exclusive report run refuse an existing target. | fixed in `eb8dc5b32cfefd6d30a9c911b8052eb9bb3f1657`; the parent-red assertion observed the report still present, while the fixed path removes it and proves the descriptor closed |
| S1-R1-03 | medium | `plugins/alexandria/tests/run_tests.py:18`; `plugins/alexandria/tests/test_runner.py:60` | `report_target()` compared an absolute report spelling directly with the resolved worktree root. On macOS the exact Elenchus runner supplied a `/var/folders/...` path for a checkout resolved as `/private/var/folders/...`, so the source-bound audit contract refused its own contained report as outside the worktree. | fixed in `bac7e2ff6e0109ebbff2fede1a24dcaee07d3e09`; the lexical suffix below the outer matching root alias is retained, `..` and a self-symlink below the root remain refused, and the exact Elenchus invocation returned `passed` |

Leads not pursued: No fourth candidate survived full-diff review of subject binding, predicate projection, untrusted-release verification, output confinement, partial writes, claim language, schema drift and determinism. A hostile process already able to write the selected output directory can race the temporary name between identity check and replacement; the step promises path confinement and atomic replacement, not integrity against a concurrent co-writer with that authority. A final directory-descriptor `close()` failure after successful replacement can change the CLI result only under an operating-system close failure and cannot safely roll back an installed output, so no synthetic close mock was promoted to a finding. The schema is a closed structural contract, while release-digest, capture-component and count equalities remain emitter and verifier checks rather than standalone JSON Schema proofs. The exact Elenchus classifier returned `passed`, not `guarded`: for `bac7e2ff6e0109ebbff2fede1a24dcaee07d3e09` it overlaid changed `run_tests.py` and `test_runner.py` onto parent `eb8dc5b32cfefd6d30a9c911b8052eb9bb3f1657`, so the report-path fix travelled with its guard; the earlier exact run was `inconclusive` because S1-R1-03 blocked report creation. The fixed tree passed focused statement and runner tests 38 of 38, Alexandria 293 of 293, Ariadne 632 tests run with 6 skipped, zero failures and zero errors, and root 399 of 399 with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases under Python 3.9.6. The fresh 0600 regular `.elenchus/alexandria-unittest.json` report has schema `elenchus.unittest.v1`, `complete=true`, 293 tests, zero failures, zero errors, zero skips, zero expected failures, zero unexpected successes and SHA-256 `6fbe0a229fe9b7d9aca3391d91096abcc0f8ffcb2b0e2754d7588ad870358be2`. The deterministic credit-history demonstration emitted release `sha256:d57f0b009d40a804e5f760e8cde4a6b1eb1ada1cc9dbf858e0494c1e750e840c` and byte-identical statement SHA-256 `fbbbf122f7e343d29c1ca9e8fac2f5b039a2d786cae1361b98c96f85d5284c88`; Ariadne reported five core-gate passes, the predicate unregistered, the statement unsigned, and gates 2 and 5 unchecked. The first two demo probes deliberately retained unresolved `/tmp` and `/var` aliases and were refused without output before the documented `pwd -P` normalisation succeeded. Phylax, Ephoros and Hypomnema each exited zero over the exact changed paths or documents. Imprimatur reported 100.0 with zero defects on the record and synopsis; Brevitas report mode returned `B010` at line 1 because the mandatory `fiat-audit-round/v2` host has one H2 heading, and adding two headings would violate the Warden/controller grammar, so structural clean is not claimed. The risk register remains bound to study SHA-256 `4a554d02d2296dac4a33b6d9ddf064407287265818d0cf81d202c1f8b1665eaf`; the effective runbook SHA-256 is `2e76ada3da241372fa18b1df230b1326f05bb045c3e8627253e46134b335b313`, its artefact SHA-256 is `56d81dae5b66e237954a68208beeb5f8da5af90bbc23a3d3fde23d6bf8cd16c9`, and the read-only controller state SHA-256 was `176036e4e8d14bab437f6cd3b7a416c1955b7c6c055c1b578945a129b1fa968e`.

## Step 1, round 2 -- 2026-08-26T16:14:16Z

Audit schema: fiat-audit-round/v2

Covered: subject-binding=reviewed; predicate-fidelity=reviewed; untrusted-release=reviewed; output-confinement=reviewed; partial-write=reviewed; claim-inflation=reviewed; schema-drift=reviewed; determinism=reviewed

Not checked: the waived Pashov X-Ray and Solidity Auditor pipelines because the step ships no Solidity, Foundry or Hardhat surface; DSSE signing, cosign, publisher identity, provider completeness, consensus finality, canonical-chain proof, network or RPC behaviour; hostile concurrent writers with permission on the output directory; hosted CI, controller receipt, push, pull request, issue publication and final integration

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | medium | `plugins/alexandria/scripts/alexandria_lib/statement.py:142`; `plugins/alexandria/tests/test_statement.py:350` | A verified 8,384,969-byte manifest below Alexandria's 8,388,608-byte control limit produced an 8,390,343-byte statement. `statement` installed it and returned success, but Ariadne `inspect` exited 2 because the bytes exceeded its 8,388,608-byte default input cap, so the successful output did not satisfy the advertised direct Ariadne-readability boundary. | fixed in `775b151c5b04e57366170312eef5f237ecd5a050`; canonical bytes are bounded before `_prepare_output()`, an existing target stays unchanged on refusal, and a test binds the copied limit to Ariadne's `DEFAULT_MAX_BYTES` |

Leads not pursued: No additional candidate survived the full fixed-tree and step-diff review. The post-open parent, statement-temporary and report-file cleanup guards passed; the outer worktree-alias mapping retained only its lexical suffix below the physical root; nested symlinks remained refused without creating an outside directory; and the exact source-bound Elenchus command returned `guarded` from a parent assertion failure for `775b151c5b04e57366170312eef5f237ecd5a050`. Focused statement and runner tests passed 40 of 40, Alexandria passed 295 of 295, Ariadne ran 632 with 6 skipped and no failures or errors, and root passed 399 of 399 with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases under Python 3.9.6. The fresh 0600 regular `.elenchus/alexandria-unittest.json` report has schema `elenchus.unittest.v1`, `complete=true`, 295 tests, zero failures, zero errors, zero skips, zero expected failures, zero unexpected successes and SHA-256 `094a0c6d4960e4e60e4476e298a74da50fec5b78794a65fb80d66b5ea9151ea1`. The deterministic credit-history demonstration emitted release `sha256:d57f0b009d40a804e5f760e8cde4a6b1eb1ada1cc9dbf858e0494c1e750e840c` and byte-identical statement SHA-256 `fbbbf122f7e343d29c1ca9e8fac2f5b039a2d786cae1361b98c96f85d5284c88`; Ariadne reported five core-gate passes, the predicate unregistered, the statement unsigned, and gates 2 and 5 unchecked. Phylax, Ephoros and Hypomnema each exited zero, while Imprimatur and Brevitas exited zero on the two public contract files changed by the fix. Imprimatur also exited zero on the exact record and synopsis; Brevitas report mode returned `B010` for their two headings and `B011` for the record's one finding row, both fixed by the mandatory `fiat-audit-round/v2` host, so structural clean is not claimed. The schema remains a closed structural contract; release-digest, capture-component and count equalities remain verified emitter relations rather than standalone JSON Schema proofs. The accepted study's statement that `EVOLUTION.md` would not be edited is historical planning text superseded by the source-bound runbook's explicit generation advance, so the receipted study bytes were not rewritten. The risk register remains bound to study SHA-256 `4a554d02d2296dac4a33b6d9ddf064407287265818d0cf81d202c1f8b1665eaf`; the effective runbook SHA-256 is `2e76ada3da241372fa18b1df230b1326f05bb045c3e8627253e46134b335b313`, its artefact SHA-256 is `56d81dae5b66e237954a68208beeb5f8da5af90bbc23a3d3fde23d6bf8cd16c9`, and the read-only controller state SHA-256 was `1448e4ada485276506903996fcf8fa26ce1525dfb471b7ddd26d0fc991b7206f`.

## Step 1, round 3 -- 2026-08-26T16:33:34Z

Audit schema: fiat-audit-round/v2

Covered: subject-binding=reviewed; predicate-fidelity=reviewed; untrusted-release=reviewed; output-confinement=reviewed; partial-write=reviewed; claim-inflation=reviewed; schema-drift=reviewed; determinism=reviewed

Not checked: the waived Pashov X-Ray and Solidity Auditor pipelines because the step ships no Solidity, Foundry or Hardhat surface; DSSE signing, cosign, publisher identity, provider completeness, consensus finality, canonical-chain proof, network or RPC behaviour; hostile concurrent writers with permission on the output directory; hosted CI, controller receipt, push, pull request, issue publication and final integration

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: No candidate survived the fresh review of the complete fixed diff from `58b7dcd1004bf8e6b0cf517bbcc778789e2c43ff` through `c071929272545d29f640d73a2397a6ac778aef43`. A possible failure after installing an output whose filename cannot be represented by the receipt serializer was not reproducible on macOS: the filesystem refused the surrogate spelling before installation, no portable reproducer established a contract violation, and arbitrary non-UTF-8 path names are not promised. Component access and redistribution classes remain outside this source-bound statement contract; the command emits the release identifier, component digests and verified capture facts requested by issue 407, while publication and signing remain downstream. Hostile directory co-writers and final directory-close failures remain outside the stated integrity boundary, and schema cross-field equalities remain emitter and verifier checks rather than standalone JSON Schema proofs. Focused statement and runner tests passed 40 of 40, Alexandria passed 295 of 295, Ariadne ran 632 with 6 skipped and no failures or errors, root passed 399 of 399 with 1,258 inoculation cases, zero crashes and zero unexpected-clean cases, and the portable entry test passed 1 of 1. Promise Machine consistency, both Protasis checks, JSON parsing, Python compilation and `git diff --check` exited zero. The fresh 0600 regular `.elenchus/alexandria-unittest.json` report has schema `elenchus.unittest.v1`, `complete=true`, 295 tests, zero failures, zero errors, zero skips, zero expected failures, zero unexpected successes and SHA-256 `094a0c6d4960e4e60e4476e298a74da50fec5b78794a65fb80d66b5ea9151ea1`. The deterministic credit-history demonstration emitted release `sha256:d57f0b009d40a804e5f760e8cde4a6b1eb1ada1cc9dbf858e0494c1e750e840c` and byte-identical statement SHA-256 `fbbbf122f7e343d29c1ca9e8fac2f5b039a2d786cae1361b98c96f85d5284c88`; Ariadne reported five core-gate passes, the predicate unregistered, the statement unsigned, gates 2 and 5 unchecked, and verify exit zero. Phylax, Ephoros and Hypomnema each exited zero. Imprimatur exited zero on all eleven changed prose files and, after the append, on the exact record and synopsis. Brevitas exited zero on the six public contract files and the synopsis; report mode returned `B011` on the mandatory audit record's historical round 2 and zero-finding round 3 one-row tables, which are excluded completeness evidence, so audit-host structural clean is not claimed. With no fix commit, Elenchus has no verdict. The risk register remains bound to study SHA-256 `4a554d02d2296dac4a33b6d9ddf064407287265818d0cf81d202c1f8b1665eaf`; the effective runbook SHA-256 is `2e76ada3da241372fa18b1df230b1326f05bb045c3e8627253e46134b335b313`, its artefact SHA-256 is `56d81dae5b66e237954a68208beeb5f8da5af90bbc23a3d3fde23d6bf8cd16c9`, and the read-only controller state SHA-256 was `e78f0f23874a4ee2c8d1dbb806aed12687555fa9af8c056b08a8904dd9982701`.
