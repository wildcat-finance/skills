## Step 1, round 1 -- 2026-09-02T07:51:26Z

Audit schema: fiat-audit-round/v2

Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable

Not checked: the recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity and no Foundry or Hardhat project. Three of the nine register concerns have no surface at this commit: ADR-070 through ADR-073 do not exist, so `retired-body-drift` has nothing to diff; no issue body was edited and `docs/wave-delta-issue-estate-2026-09-02.md` does not exist, so `issue-body-publication` has nothing to read back; and neither fresh programme document exists, so `banner-inversion` has no pair to invert. Also not checked: whether the four successors, the estate rewrite, the fresh programme pair and `docs/wave-delta-reinstatement-demo.py` do what the runbook says, all of which are steps 2 through 7 exits and cannot be stood in for here. Also not checked: whether GitHub hides pull request #569 rather than lacking it; `gh api repos/wildcat-finance/skills/pulls/569` answers HTTP 404, which does not separate a deleted item from a hidden one. Also not checked: the ADR-028 amendment's forward tense. It states that ADR-029 through ADR-032 "stay Retired" and each "gains one standing successor" while no successor exists at this commit; that is the register a decision record is written in and ADR-069 carries the timing ("They land in the two steps after this one"), so it is recorded as looked-at rather than as a finding.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | docs/wave-delta-reinstatement-study.md | Section 7 Sources cites `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `cmd_checkpoint_export` line 12629, `cmd_checkpoint_restore` line 13754, subparsers at line 14801. The first two are exact. The third is not: `sp.add_subparsers(dest="checkpoint_action", required=True)` is at line 14803, the `sub.add_parser("checkpoint", ...)` call opens at line 14799, and line 14801 is that call's closing parenthesis. A reader who jumps to 14801 lands on punctuation. The file is outside this delivery's write set and unchanged between `ff47f3070c8dce05c767b6c0dad65234c56870de` and `c76c2b722fcf1f4a754ca06f1fe8bbb8d9bf90be`, so the citation was wrong when written rather than overtaken. | open; repair route named below |
| S1-R1-02 | low | docs/wave-delta-reinstatement-study.md | Section 2 states "Each also records that PR #569 first published it under a number between ADR-024 and ADR-027". `gh issue view 569` and `gh api repos/wildcat-finance/skills/pulls/569` both answer that 569 does not resolve. The study's own `dead-issue-links` control requires that "one that does not resolve is named as unresolved in the text rather than linked". Half the control holds: #569 is plain prose, not a link. The other half does not: it is not named as unresolved, while #547 is named that way in the same document's Sources section ("#547 does not resolve"), so the convention was applied unevenly. The underlying claim is accurate and attributed -- `docs/decisions/ADR-029-separate-the-checkpoint-protocol-from-its-authority-service.md:10` reads "PR #569 published this record as ADR-024" -- so this is a missing qualification, not a false statement. | open; repair route named below |

Leads not pursued: both findings land in `docs/wave-delta-reinstatement-study.md`, which is byte-identical to the receipted controller artefact `.hexaemeron/study.md` at SHA-256 `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec`, the digest the round's own packet pins. Hand-editing the docs copy would break that identity and create the drift this round was asked to check for, and the amend path that repairs the controller artefact is a mutating controller command reserved to the orchestrator. Both are therefore left open with the route named rather than fixed on the stacked branch: amend `.hexaemeron/study.md` through the controller, then re-copy to `docs/`. Neither is worth a second round on its own. Second lead, examined and not filed as a finding against this delivery: `plugins/hexaemeron/tests/run_tests.py` answered a concurrent invocation with `status: scheduler-error` and `testsRun: 0`. That path is fail-closed -- `coordinator_main` returns 3 whenever `scheduler_errors` is non-empty, at `plugins/hexaemeron/tests/run_tests.py:3843`, before the pass/fail return at 3845 -- so a contention refusal cannot be read as a green suite, which is the failure mode that would have made it serious. What remains is that a step exit running the runner beside another invocation can take a spurious exit 3; that is a usability property of a file under `plugins/`, which the runbook's scope guard excludes from every step of this delivery, so it belongs to the Fiat frontier rather than here. Third lead, out of scope: `tests/test_decision_records.py:139` reads `if added and True:`, a no-op conjunct in the default-branch collision check; the test is not in this diff and its behaviour is correct, so it is noted rather than touched. Evidence for the covered concerns: the three lints exit 0 over all five changed paths and report `clean` having read real paths; the repository's declared root suite, `python3 -m unittest discover -s tests` from `tests/check-map-v1.json` `root-suite`, reports `Ran 1116 tests`, `OK`, exit 0, with no skips, so `test_no_number_collides_with_one_already_on_the_default_branch` compared rather than skipping. `adr-number-collision`: `git ls-tree -r --name-only origin/main -- docs/decisions` tops out at ADR-068, leaving 069 through 073 free, and `origin/main` is at the base commit `ff47f3070c8dce05c767b6c0dad65234c56870de`. `adr-028-clause-drift`: the slice `test_adr_supersedes_remote_transport_with_mandatory_local_handoff` reads, from the mandatory-local heading to the next `\n## `, is byte-identical across the base and the audited commit at 1089 characters and SHA-256 prefix `981913c56fc926a3`; the whole ADR-028 change is a pure 952-byte append after `## Consequences`, with the new `## Amendment: distributed layer reinstated (2026-09-02)` heading at line 241 and the mandatory-local slice still bounded by `## Alternatives` at line 189. `pinned-artefact-digest`: `EXPECTED_DIGESTS` covers only `docs/fiat-controller-checkpoint-study.md` and `docs/fiat-controller-checkpoint-runbook.md`, neither is in the diff, and `python3 -m unittest tests.test_decision_records tests.test_fiat_checkpoint_decision_record` runs 10 tests OK. `capsule-overclaim`: every capability ADR-069 and study section 2 assign to the reopened layer is disclaimed verbatim by `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md` -- "These commands do not create or verify the Git bundle, package an archive, handle keys or mint a semantic checkpoint identity" at its `## Outer recovery boundary`, and "not the semantic checkpoint identity, service acceptance or outer archive identity owned by the remaining Wave Delta work" at its manifest table -- and every capability the study says already ships is present there as "ref-to-commit map", "ledger prefix", and the `## Relocation transaction` section. `scope-creep-to-code`: `! git diff --name-only ff47f3070c8dce05c767b6c0dad65234c56870de..HEAD | grep -q '^plugins/'` exits 0, the diff is five files, and the sole deletion is `.horos/boundary.json` `files_walked` 2465 becoming 2468, which matches the three files added exactly. Design conformance against the locked candidate `additive-successors` at `.hexaemeron/design-evidence.json` SHA-256 `6a171b6f72d1a651ebd9b7dd32af331ba6a9aaff600f02100c328907d1b9d755`: ADR-069 states the reinstatement route, ADR-028 keeps Status Accepted with a pure append, and ADR-029 through ADR-032 are absent from the diff and retain "Retired, 2026-08-27". Artefact drift: `docs/wave-delta-reinstatement-study.md` equals `.hexaemeron/study.md` at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` and `docs/wave-delta-reinstatement-runbook.md` equals `.hexaemeron/runbook.md` at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`, both matching the packet pins, with `diff` reporting no difference on either pair. Shipped-state claims all hold: `hexctl.py` declares exactly `export` and `restore` under `checkpoint`; `cmd_checkpoint_export` is at line 12629 and `cmd_checkpoint_restore` at line 13754 as cited; `fiat-v5.49.1` is the current version in `plugins/hexaemeron/skills/fiat/EVOLUTION.md`; rows `fiat-v5.35.1`, `fiat-v5.43.1` and `fiat-v5.44.1` say what section 2 says they say, and `fiat-v5.44.1` ends "until a distributed checkpoint framework is accepted", which is the hook ADR-069 uses; `tests/test_evolution_contract.py:222` is `self.assertIn("ADR-028", capsule["evidence"])` as cited; `tests/test_shipped_prose_lints.py:66` excludes `docs`; `docs/decisions/` holds ADR numbers 1 through 68 missing only 026 and 027, 66 files before this delivery's ADR-069. Estate: #859 through #867, #508, #899 and #901 all resolve OPEN under milestone 64, "Wave Δ — distributed Fiat checkpoints"; #508's title is "bind exhausted-audit carryover, delegated writes, and runbook gates", matching assumption 9's description of its lane; #899 and #901 are both `hexctl` controller defects by title, matching assumption 7; #547 does not resolve, exactly as Sources states; PR #1072 merged 2026-08-31 and PR #997 merged 2026-08-30 as cited. Links: both relative Markdown links in the diff resolve (ADR-069 to ADR-028, ADR-028 to ADR-069), and every absent path the two documents name is a step 2 through 7 output the runbook creates. Gates re-run independently and green: `protasis.py --study` clean, `protasis.py` on the runbook clean, `hypomnema.py --study --design-evidence` clean, `imprimatur.py` 100.0/100 with 0 defects on all four changed prose files, `promise_machine.py check` and `coverage --check` both exit 0.

## Step 1, round 2 -- 2026-09-03T15:14:20Z

Audit schema: fiat-audit-round/v2

Covered: adr-number-collision=reviewed; adr-028-clause-drift=reviewed; pinned-artefact-digest=reviewed; retired-body-drift=not-applicable; capsule-overclaim=reviewed; issue-body-publication=not-applicable; dead-issue-links=reviewed; scope-creep-to-code=reviewed; banner-inversion=not-applicable

Not checked: The recorded suite waiver excludes the Pashov pair, so `x-ray`, `solidity-auditor` and `fizz` did not run; this step ships no Solidity and no Foundry or Hardhat project. Whether GitHub reports `verified: true` and `reason: valid` for the five stacked commits cannot be observed in this round, because nothing is pushed; that is the push receipt's own gate. What is observed locally is that all five carry a good signature from key `B83B60AE16F5DD1A`, author `Shoggoth <shoggoth@wildcat.finance>` and committer `Dr Laurence E. Day <laurence@wildcat.finance>`, which is the shape GitHub reported `verified: true`, `reason: valid` for `db31d809` on `main`. Three register concerns still have no surface at this commit: ADR-070 through ADR-073 do not exist, `docs/wave-delta-issue-estate-2026-09-02.md` does not exist and no issue body was edited, and neither fresh programme document exists, so `retired-body-drift`, `issue-body-publication` and `banner-inversion` have nothing to read. Whether the four successors, the estate rewrite, the fresh programme pair and `docs/wave-delta-reinstatement-demo.py` do what the runbook says remains a steps 2 through 7 exit and is not stood in for here. Five study claims were re-read against `origin/main` at `01a17bed45058a1fc20875bb19765fdf91cb293a`: the current Fiat version, the `checkpoint` subcommand surface, the `docs/decisions/` numbering, the ADR-028 mandatory-local slice, and `tests/test_evolution_contract.py` line 222. The twelve issue numbers and the two merged pull requests the study cites were read at the base on 2026-09-02 and were not re-read against the moved branch. Whether the run branch merges cleanly into the advanced base was not tested; the base moved 58 commits and that composition belongs to the integrate phase. #569 remains unresolvable rather than proved absent: `gh api repos/wildcat-finance/skills/pulls/569` answers HTTP 404, which does not separate a deleted item from a hidden one.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R2-01 | medium | docs/decisions/ADR-069-reinstate-the-distributed-checkpoint-layer-above-the-local-store.md | ADR-069 listed a semantic checkpoint identity distinct from archive bytes among the things the reopened layer adds, and the study's section 2 said `hexctl.py` defines exactly two checkpoint subcommands with no intake, publication, acceptance, revocation or lineage-resolution command. Both hold at the base `ff47f3070c8dce05c767b6c0dad65234c56870de` and neither holds on `origin/main`, which is 58 commits ahead at `01a17bed45058a1fc20875bb19765fdf91cb293a`. Upstream commit `482172e7` adds `cmd_checkpoint_identity` at line 13661 and the `identity` subparser at line 16206, help text `print one verified semantic checkpoint identity`, and amends ADR-028 with `## Amendment: Immutable run anchors and checkpoint identity (2026-08-29)`, which derives that identity from the verified run anchor and separates it from exact capsule and archive bytes. A governing record that merges into that branch would claim as still-owed a capability already accepted there, which is the `capsule-overclaim` concern reached through a moved base rather than through prose. | fixed in 5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f |
| S1-R2-02 | low | docs/wave-delta-reinstatement-study.md | Sections 2 and 7 cited `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` by line: `cmd_checkpoint_export` 12629, `cmd_checkpoint_restore` 13754, subparsers 14803. All three are exact at the base and none survives the move: on `origin/main` the same file has changed by 1444 insertions and 36 deletions, and those three lines hold a `die` call, an `os.stat` call and a `for` statement, while the cited definitions sit at 13971, 15096 and 16205. This is the same class as S1-R1-01, caused by the base advancing rather than by a mis-read. | fixed in 5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f |
| S1-R2-03 | high | commit metadata on the stacked branch | The three stacked commits written after the receipted implement commit carried committer `Shoggoth <shoggoth@wildcat.finance>` while signed with Laurence's key `B83B60AE16F5DD1A`: `9ab3fd5d`, `aeba6bac` and `693d2b9c`. The receipted implement commit `c76c2b722fcf1f4a754ca06f1fe8bbb8d9bf90be` carries committer `Dr Laurence E. Day <laurence@wildcat.finance>`, so the step held two identity shapes at once. Fiat refuses a push receipt unless GitHub reports `verified: true` and `reason: valid` for every commit in the pushed range, and GitHub ties a GPG signature to an email verified on the account holding the key; no commit with committer `shoggoth@wildcat.finance` appears in the last 300 commits of `origin/main`. Discovered at `done push` this costs the branch, because re-signing rewrites receipted SHAs. Nothing was pushed and none of the three was receipted, so the repair was free here. | fixed by rewriting 9ab3fd5d, aeba6bac and 693d2b9c as 71f0df1d, 69112d0b and 0bac93c2, tree identical |

Leads not pursued: The docs copy of the study now differs from the receipted controller artefact in four hunks rather than two. `.hexaemeron/study.md` stays at `e6a5662b6f53562398a940deb1065450312fd42a9c2d82296c263ca6d89c4fec` and `docs/wave-delta-reinstatement-study.md` is at `0a435548a7c529ad9c3cba7e962ff047f921389264d75e675b41ead368ea07a4`. The controller amend path appends a dated block and cannot revise a sentence in place, so the receipted artefact remains the record of what was specified at receipt time and the docs copy is the governing document; steps 4 and 6 take their account of the shipped half from the docs copy, and this record is where that instruction lives. The runbook pair stays byte-identical at `cd76c2e530d395d623c33e5a2dd901496aeff048c36e21dc4b7d1d0e8b2b4872`. Second lead: the upstream ADR-028 amendment inserts before `## Amendment: Mandatory local checkpoint hand-off (2026-08-30)` while this step's amendment appends at end of file, so the two edits occupy different regions and a three-way merge has no overlap to resolve; the resulting order of amendment sections was reasoned about rather than proved by a trial merge, which belongs to the integrate phase. Third lead, unchanged from round 1 and outside this diff: `tests/test_decision_records.py:139` reads `if added and True:`. Fourth lead, also unchanged: `plugins/hexaemeron/tests/run_tests.py` emits `status: scheduler-error` with `testsRun: 0` for two of its own partial-write self-test fixtures; the real invocation in this round finished `status: passed` with 2204 tests, 0 failures, 0 errors and 0 skipped, and the runner is fail-closed at `plugins/hexaemeron/tests/run_tests.py:3843`, so a contention refusal cannot read as a green suite. Evidence for the covered concerns: the three lints exit 0 over all seven changed paths and report `clean` having read real paths; the declared root suite `python3 -m unittest discover -s tests` from `tests/check-map-v1.json` `root-suite` reports `Ran 1116 tests`, `OK`, exit 0, with no skips, so `test_no_number_collides_with_one_already_on_the_default_branch` compared rather than skipped. `adr-number-collision`: `git ls-tree -r --name-only origin/main -- docs/decisions` tops out at ADR-068 and holds no ADR-069, so 069 through 073 remain free on the moved branch. `adr-028-clause-drift`: this step's ADR-028 change is still a pure 17-line append after the last section, the upstream change to the same file lands 68 lines earlier, and `python3 -m unittest tests.test_decision_records tests.test_fiat_checkpoint_decision_record` runs 10 tests OK. `pinned-artefact-digest`: `docs/fiat-controller-checkpoint-study.md` and `docs/fiat-controller-checkpoint-runbook.md` are absent from the seven-path diff. `capsule-overclaim`: S1-R2-01 is its finding and the repair leaves every remaining item in ADR-069's list absent from both `controller-checkpoint.md` and upstream `hexctl`. `dead-issue-links`: the two relative links in the diff resolve, and the qualification convention now covers #569 and #547 alike. `scope-creep-to-code`: `! git diff --name-only ff47f307..HEAD | grep -q '^plugins/'` exits 0. Prose and record gates re-run green on the fixed tree: `imprimatur` scores 100.0/100 with 0 defects on all four changed prose files, `protasis --study` and `protasis` on the runbook are clean, `hypomnema --study --design-evidence` is clean, `promise_machine check` reports 18 plugins and `coverage --check` 94 of 94, and `tests.test_boundary_currency` with `tests.test_audit_synopsis_currency` run 46 tests OK. Identity evidence: `scripts/check_commit_identity.py` against a bare replica of the rewritten branch reports `status: passed`, `commit_count: 5`, `shoggoth_author_count: 5`, `human_author_count: 0`. Elenchus: `5a5490a7a8c75e6df8545e48f3f142eb4d8fff6f` is `unguarded`, detail `the commit changed no test files`, which is correct for three prose repairs and one metadata rewrite; no test can guard a citation's accuracy, and the boundary counts that can be guarded are covered by the 46 currency tests above.
