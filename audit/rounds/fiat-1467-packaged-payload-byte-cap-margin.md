## Step 1, round 1 -- 2026-09-08T05:46:53Z

Audit schema: fiat-audit-round/v2

Covered: omission-pattern-breadth=not-applicable; root-files-desync=not-applicable; manifest-omission-honesty=not-applicable; silent-payload-shrink=not-applicable; threshold-without-measurement=not-applicable; dangling-image-references=not-applicable; transform-idempotence=not-applicable; transform-determinism=not-applicable; transform-reaches-only-departed-targets=not-applicable; transform-changes-nothing-else=not-applicable; manifest-source-binding-restated=not-applicable; source-tree-untouched=reviewed; publication-lag-masking=not-applicable

Not checked: the security suite is waived and the waiver is on the ledger; this step adds Markdown, JSON and Python and changes no Solidity, so x-ray, solidity-auditor and fizz have no target. Twelve register ids name artefacts absent at this commit: step 2 owes the omission class, the `ROOT_FILES` removal, the manifest omissions array, the packaged-set shrink, the reference transform with its four properties and the restated manifest source binding; step 3 owes the byte threshold; `publication-lag-masking` concerns the published `wildcat-finance/skills-runtime` copy, which this step neither builds nor reads. Also unchecked: `python3 scripts/run_checks.py --full`, which the step Exit names and this round's mechanical part does not include, because `dead-code-suppressions-check` refuses a dirty tree; hosted CI, the controller receipt, push and publication; whether the study's design selection is correct, which step 1 commits rather than establishes; and the `IMAGE` and `LINK` regexes as HTML and Markdown parsers, which the four transform register entries schedule for step 2.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | docs/packaged-payload-byte-cap-margin/study.md, docs/packaged-payload-byte-cap-margin/runbook.md | The `source-tree-untouched` register entry at study.md line 599, and step 2 exit 2 at runbook.md lines 75 and 162, both state that `git ls-files` reports the same 32 portrait paths tracked before and after. It reports 34, at `12cbe2e6` and at `61e6de7c` alike. 32 is the packaged count, measured from `MANIFEST.json`; the two tracked portraits that never enter the package are `assets/characters/promise-machine-binding.png` and `assets/characters/shoggoth.png`, which `ROOT_FILES` does not carry. Step 2 exit 2 is unsatisfiable on its second clause as written. The substantive claim holds: this step deleted, renamed and edited no tracked file. Not fixed here because both files are receipted controller-pinned bytes that step 1 was required to commit verbatim, and a hand edit would desynchronise them from `.hexaemeron/`; the repair is an amendment to step 2 Exit | open, controller amend owed |
| S1-R1-02 | low | docs/packaged-payload-byte-cap-margin/resolve_design.py, docs/packaged-payload-byte-cap-margin/measure_built_margin.py | Both resolvers hard-code the emitted report's own `command` provenance field to `python3 .hexaemeron/` plus the script name. Running the committed `resolve_design.py --candidate omission-class-with-reference-repair --criterion tracked-files-deleted` printed a report whose `command` reads `python3 .hexaemeron/resolve_design.py --candidate omission-class-with-reference-repair --criterion tracked-files-deleted`, naming a path that did not run and that does not survive the run, `.hexaemeron/` being untracked controller state. A report written with `--out` from the committed location misdeclares its source. Not fixed here because the emitted string would then disagree with the byte-pinned `protasis-design-evidence/v1` record at SHA-256 `53718d5865f6f7b4afd5a462fef2e9b387b25780f4f33e52710f855c19dc3fbb`, whose four `resolver` fields name that same `.hexaemeron/measure_built_margin.py` path. Step 2 and step 3 exits invoke both resolvers without `--out`, so this run writes no report from the committed location | open, pinned-record conflict |

Leads not pursued: the `IMAGE` regex parses an HTML tag with a regular expression and `_link_escapes` reproduces the shipped link test's resolution rule; both produced the byte-pinned selection reports, and neither was re-derived here, because the 35 reports are receipted study evidence and the four transform register entries schedule that check at step 2 where the transform ships. `ROOT = next(p for p in HERE.parents if ...)` carries no default, so a copy placed outside any tree holding `scripts/portable_promise_machine.py` raises a bare `StopIteration` at import with no message; it fails closed and every location the runbook names has the generator above it. `measure_built_margin.py` ignores `--candidate` when computing its value and measures only the tree as built, which its docstring states. `--out` is an operator-supplied write path used with `mkdir(parents=True, exist_ok=True)` and no containment check; both resolvers are operator-run local tools and that path carries the operator's own authority. Evidence for the covered concerns: phylax exited 0 and ephoros exited 0 over the two added Python files, and hypomnema exited 0 three ways, over the three added documents, over the whole `docs` tree, and in study mode binding `study.md` to `design-evidence.json` at the repository root. `python3 -m unittest discover -s tests` ran 1628 tests in 217.101 s, OK, exit 0; neither known-red class appeared, `test_kronos_scoreboard.ScoreboardTest.test_no_governed_ledger_declares_anything_today` of https://github.com/wildcat-finance/skills/issues/1472 sitting in the hexaemeron suite rather than `tests/`, and the commit-gate activation test and the issue #1228 corpus tests passing. The commit carries 43 paths, 41 added and 2 modified, and no path under `.hexaemeron/`. `design-evidence.json` is byte-identical to the receipted record at SHA-256 `53718d5865f6f7b4afd5a462fef2e9b387b25780f4f33e52710f855c19dc3fbb`, all 35 reports are byte-identical, and `runbook.md` is byte-identical at SHA-256 `e1af02c5e8f8c4fc2e92180546ccb5383b120ccfafea83f3777ed70f110b7e07`. `study.md` differs from the receipted `.hexaemeron/study.md` in exactly five lines, one discipline link each for ephoros, phylax, metron, elenchus and hypomnema, every one repointed to `../../plugins/hexaemeron/skills/` plus the name and `SKILL.md`, and all five targets exist; there is no sixth difference. The resolver delta beyond the ROOT computation is one docstring sentence in `resolve_design.py`, replacing the claim that the root is the parent of this file's directory; `measure_built_margin.py` carries the ROOT change alone. Both `subprocess.run` calls pass an argument list with no shell, `cwd=ROOT`, and an argv of `sys.executable`, the resolved generator path, `package`, `--out` and a `TemporaryDirectory` path, so no operator or external data reaches argv, and each `# phylax: allow` pragma states a reason. Neither resolver reads or writes anything under `.hexaemeron/`, and neither writes outside its temporary directory unless `--out` is given. Both committed copies resolve ROOT to the run worktree root despite the nested worktree ancestry and both execute: `tracked-files-deleted` reports 0 and `built-payload-margin` reports 1,257,757 bytes against the 5,242,880 step 2 requires, that cell being the pending conformance criterion blocking step 3. The baseline package holds 1323 files at `total_bytes` 24,956,643 with 32 packaged portraits. The `.horos/census.json` and `.horos/boundary.json` deltas match the addition exactly, 3 `.md`, 36 `.json` and 2 `.py` files totalling 114,885 bytes, `total_files` 2970 to 3011 and `files_walked` 2962 to 3003. The step commit is signed `G` by key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed `Dr Laurence E. Day <laurence@wildcat.finance>`, and closed by one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`.

## Step 1, round 2 -- 2026-09-09T01:18:18Z

Audit schema: fiat-audit-round/v2

Covered: omission-pattern-breadth=not-applicable; root-files-desync=not-applicable; manifest-omission-honesty=not-applicable; silent-payload-shrink=not-applicable; threshold-without-measurement=not-applicable; dangling-image-references=not-applicable; transform-idempotence=not-applicable; transform-determinism=not-applicable; transform-reaches-only-departed-targets=not-applicable; transform-changes-nothing-else=not-applicable; manifest-source-binding-restated=not-applicable; source-tree-untouched=reviewed; publication-lag-masking=not-applicable

Not checked: the security suite is waived and the waiver is on the ledger; the step and its fixes change Markdown, JSON and Python and no Solidity, so x-ray, solidity-auditor and fizz have no target and did not run. Twelve register ids name artefacts absent at this tree: step 2 owes the omission class, the `ROOT_FILES` removal, the manifest omissions array, the packaged-set shrink, the reference transform with its four properties and the restated manifest source binding; step 3 owes the byte threshold; `publication-lag-masking` concerns the published `wildcat-finance/skills-runtime` copy, which this step neither builds nor reads. The `built-payload-margin` conformance cell is `pending` for all four candidates in the design record; it is due at step 2's exit and blocks step 3, not this boundary; the committed resolver reports 1,257,757 bytes against the 5,242,880 threshold at this tree, the expected pre-step-2 value. Also unchecked: hosted CI, the controller receipt, push and publication; whether the study's design selection is correct, which step 1 commits rather than establishes; the `IMAGE` and `LINK` regexes as HTML and Markdown parsers, which the four transform register entries schedule for step 2; byte reproduction of the pinned reports from a `.hexaemeron/` path for 33 of the 35 cells, two being measured and the rest resting on the fix changing only the path component of `command`; and the test count of the greenlight run that admitted `e886707c`, not captured separately, its evidence being the recorded tree.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | docs/packaged-payload-byte-cap-margin/study.md, docs/packaged-payload-byte-cap-margin/runbook.md | Carried from round 1. The `source-tree-untouched` register entry at study.md line 599 and step 2 exit 2 at runbook.md lines 75 and 162 count 32 tracked portrait paths where `git ls-files` reports 34, 33 `.png` and one `.webp`, at `12cbe2e6`, `61e6de7c` and `e886707c` alike; 32 is the packaged count, and `assets/characters/promise-machine-binding.png` and `assets/characters/shoggoth.png` are tracked and were never packaged. Exit repaired: the controller receipted a runbook amendment dated 2026-09-09, amendment SHA-256 `be7a5c69d87fc2e159443dee6a8cfc0af9f00fc7d5a7f09cc4e90b7e69200ba8`, runbook SHA-256 `e1af02c5e8f8c4fc2e92180546ccb5383b120ccfafea83f3777ed70f110b7e07` to `62ff562ed935ad786521a859d7896234827254cfa828a3d9dfcbbf79059c0289`, replacing step 2 Exit with the 34 count at runbook.md line 186 while lines 75 and 162 keep the superseded wording as append-only history; `909237e3` brought the committed runbook.md to those bytes, verified this round at SHA-256 `62ff562ed935ad786521a859d7896234827254cfa828a3d9dfcbbf79059c0289`, equal to `.hexaemeron/runbook.md` and to the brief. Residual: study.md line 599 still reads 32. Left as written because a study amendment rebinds every receipted runbook amendment to the new study digest and would orphan all four, and the protected claim holds: `git diff --diff-filter=D --name-only 12cbe2e6..e886707c` names no path, so nothing was deleted or edited to buy margin. | fixed in 909237e3; study.md line 599 wording accepted as residual, a study amendment would orphan the four receipted runbook amendments |
| S1-R1-02 | low | docs/packaged-payload-byte-cap-margin/resolve_design.py, docs/packaged-payload-byte-cap-margin/measure_built_margin.py | Carried from round 1. Both committed resolvers hard-coded the emitted report's `command` field to `python3 .hexaemeron/` plus the script name, so a report written with `--out` from the committed location named a path that did not run. Fixed in `e886707c`: each derives the path from the file it occupies, `SELF = Path(__file__).resolve().relative_to(ROOT).as_posix()`, five added lines and one changed line per file, 682 bytes in total. From `docs/packaged-payload-byte-cap-margin/` the field reads `python3 docs/packaged-payload-byte-cap-margin/resolve_design.py --candidate omission-class-with-reference-repair --criterion tracked-files-deleted`, and that report differs from the pinned one on that line alone. The same source at `.hexaemeron/` in a detached worktree at `909237e3` reproduced `omission-class-with-reference-repair-tracked-files-deleted.json` and `omission-class-with-reference-repair-extract-margin.json` byte for byte, and `measure_built_margin.py` there emitted `python3 .hexaemeron/measure_built_margin.py --candidate omission-class-with-reference-repair`, so the 35 receipted reports and the design record at SHA-256 `53718d5865f6f7b4afd5a462fef2e9b387b25780f4f33e52710f855c19dc3fbb` are undisturbed; the controller copies under `.hexaemeron/` were not touched. Every `value` is unchanged: `tracked-files-deleted` 0, `built-payload-margin` 1,257,757. Round 1's objection, that the string would disagree with the byte-pinned record, no longer applies: the record's four `resolver` fields name the controller copies, which still emit `.hexaemeron/`, and Protasis `design_evidence.py` line 379 reads `command` only as bounded text. phylax and ephoros exit 0 over both files after the change. | fixed in e886707c |
| S1-R2-01 | low | docs/packaged-payload-byte-cap-margin/runbook.md | The preamble at lines 20 to 24 says of the two resolvers that "The bytes are the same; only the path a reviewer types differs." The committed copies have differed from `.hexaemeron/resolve_design.py` and `.hexaemeron/measure_built_margin.py` since `61e6de7c`, in the depth-independent `ROOT` computation and one docstring sentence, as step 1's amended Files field of 2026-09-08 states, and `e886707c` adds the `SELF` derivation, so the sentence is false at `61e6de7c`, `909237e3` and `e886707c`. Not fixed here: the runbook is receipted controller-pinned bytes, the amendment grammar replaces one step's Files or Exit field and cannot reach the preamble, and a study amendment would orphan the four receipted runbook amendments. The amended step 1 Files field is the governing statement of the delta. | accepted: no amendment field reaches the preamble; the amended step 1 Files field states the delta and governs |

Leads not pursued: `Guard check on `e886707c3de8`: unguarded -- the commit changed no test files`; the step's Tests contract names no new test, and its declared report format `json` is not one of `unittest-json-v1`, `forge-junit-v1` or `node-test-json-v1`, so the report flags were omitted and the classification came from the changed-test set before those flags are read; `909237e3` classifies the same way, `unguarded -- the commit changed no test files`; both are recorded evidence for this round and neither blocks it. The `IMAGE` regex parses an HTML tag with a regular expression and `_link_escapes` reproduces the shipped link test's resolution rule; neither was re-derived here, because the 35 reports are receipted study evidence and the four transform register entries schedule that check at step 2. `ROOT = next(p for p in HERE.parents if ...)` carries no default and raises a bare `StopIteration` at import outside a tree holding `scripts/portable_promise_machine.py`, failing closed; `SELF` cannot raise, because `ROOT` is an ancestor of the resolved file by construction. `--out` is an operator-supplied write path used with `mkdir(parents=True, exist_ok=True)` and no containment check, unchanged, carrying the operator's own authority. `measure_built_margin.py` ignores `--candidate` when measuring and reports the tree as built, unchanged and stated in its docstring. `route-restriction` sits in the study's matrix with its seven reports committed but is absent from the design record's four candidates; the study's section 4 explains it, `MAX_CANDIDATES = 4` at `design_evidence.py` line 44 and a profile identical to `warning-gate`, so not pursued. The study's `S1-R1-07` is the finding of `audit/rounds/fiat-395-resumable-ethereum-usdc-interval-collector.md`, qualified as such at its first use, not an id of this run. Evidence for the covered concerns: phylax exited 0 and ephoros exited 0 over the two resolvers at `909237e3` and again at `e886707c`; hypomnema exited 0 three ways at both trees, over the three added documents, over the whole `docs` tree, and in study mode binding `study.md` to `design-evidence.json` at the repository root. `python3 -m unittest discover -s tests` at `909237e3` ran 1628 tests in 292.783 s, OK, exit 0. `.githooks/greenlight` on the staged fix ran the suite and recorded tree `7c98561654df5ec3bd4a5dfdfd3d630cd50f6e7b`, which admitted `e886707c`. `python3 scripts/run_checks.py --full --format json --report tmp/checks/step-1.json` at `e886707c`, the command the step Exit names and round 1 could not run against a dirty tree: 35 checks, 34 passed, 1 failed, the failure being `hexaemeron-suite` on `test_kronos_scoreboard.ScoreboardTest.test_no_governed_ledger_declares_anything_today` of https://github.com/wildcat-finance/skills/issues/1472, red at the base `12cbe2e6` and named as the one permitted red by the amended step 1 Exit; outcome `red`, exit 1, 635 s wall clock from 01:06:05Z to 01:16:40Z with `hexaemeron-suite` at 570.074 s, that suite running 2478 tests in five shards with `failures=1` and the failure line naming ledger `plugins/anamnesis/skills/anamnesis/EVOLUTION.md`; `git status --porcelain` was empty afterwards, the report path being ignored at `.gitignore` line 38. The fixes range `61e6de7c..e886707c` is three commits and 7 paths, 2 added and 5 modified, none deleted, none under `assets/` or `.hexaemeron/`; `git ls-files` reports 34 `assets/characters/` paths at `e886707c`, 33 `.png` and one `.webp`. `113cd8e5`, `909237e3` and `e886707c` are each signed `G` by key `B83B60AE16F5DD1A`, authored `Shoggoth <shoggoth@wildcat.finance>`, committed `Dr Laurence E. Day <laurence@wildcat.finance>`, and closed by one `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and one `Wildcat-Origin: shoggoth`. Artefact parity at `e886707c`: `design-evidence.json` is byte-identical to the controller copy at SHA-256 `53718d5865f6f7b4afd5a462fef2e9b387b25780f4f33e52710f855c19dc3fbb`; all 35 committed reports are byte-identical to `.hexaemeron/reports/`, which also holds one controller artefact, `probe-manifest-killed-probe-recovery.json`, not a step file; `study.md` differs from `.hexaemeron/study.md` in the five discipline-link lines only; `.horos/census.json` moved by the 682 changed `.py` bytes, `.py` 12,429,999 to 12,430,681 and `total_bytes` 102,308,109 to 102,308,791, `.horos/boundary.json` is unchanged at `files_walked` 3005, and a second scan of each left it unchanged.
