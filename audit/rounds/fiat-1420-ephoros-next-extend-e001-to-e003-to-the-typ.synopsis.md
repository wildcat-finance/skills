Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1420-ephoros-next-extend-e001-to-e003-to-the-typ.md | source_sha256=7a2b8c8c7fe7844675dbb3716e419adf53a81525bc279fe9addd80c2aeedd099 | h2_count=6
## Step 1, round 1 -- 2026-09-07T10:39:10Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=not-applicable; clone-verdict-honesty=reviewed; constant-template-boundary=not-applicable; e002-e005-overlap=not-applicable; e003-duration-shape=not-applicable; fixture-repin-scope=not-applicable; marketplace-clean-honesty=reviewed; pragma-surface-parity=not-applicable; self-lint-recursion=reviewed<br><br>Not checked: every recogniser behaviour steps 2 onward introduce. This step ships documents and probes and changes no source, so the six not-applicable concerns have no surface here: no recogniser is added, no lexing path moves, no fixture is re-pinned and no suppression surface changes. The diff introduces no `ephoros: allow`, `phylax: allow` or `hypomnema: allow` pragma; the two pragma strings inside the shipped study and runbook are grammar quoted in prose, and the lints read Python, TypeScript and block-YAML rather than Markdown. Also not checked: whether the fifteen reports' values are reproducible, which no artefact in this tree can establish, because the probes read a gitignored 19 MB extraction at `.hexaemeron/validation/wildcat-app-v2` and a `corpus/decoy.ts` that is not shipped; the reports are taken as recorded values at stated digests, not as re-measured ones. Also not checked: the three `clone-verdict-e001` cells resolve, which by construction cannot happen before the recognisers exist. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | low | docs/ephoros-typescript-rule-parity/design-probes/ | Six of the eight shipped probe modules fail at import from the shipped depth, and nothing in the tree said so. `clone_verdict.py`, `recall.py` and `strategies.py` compute `ROOT = Path(__file__).resolve().parents[2]`, which is the repository root from `.hexaemeron/design-probes/` and `docs/` from here, so the checker import resolves to `docs/plugins/hexaemeron/skills/ephoros/scripts/ephoros.py`, which does not exist; `failclosed.py`, `runtime.py` and `space.py` import `strategies` and fail with it. All eight import cleanly at the original depth, so the break is siting alone. Keeping the bytes exact is right: each report's `command` field names `.hexaemeron/design-probes/...`, repointing `ROOT` would falsify that, and it would not reproduce anything either, because the clone is a gitignored 19 MB extraction and the decoy is absent. The defect is the silence, not the choice. A reader who ran one got a bare FileNotFoundError. Fixed by adding a README that states the modules are an archived record rather than runnable tooling, names which six fail and why, and names the inputs they require. No archived byte changed and no digest moved. | fixed in 6b31c63958e11309b311bc5d25385ee4dd7b9d41 |<br>| S1-R1-02 | low | .hexaemeron/runbook.md | The step's Exit requires `design-probes/` to hold "the seven probe modules and the two support modules", nine. Eight exist: six probes, `clone_verdict.py`, `deps.py`, `failclosed.py`, `recall.py`, `runtime.py` and `space.py`, plus two support modules, `specimen.py` and `strategies.py`. The ninth is absent from the working directory too, not merely from the tree, so the Exit as written cannot be satisfied by any tree. The step ships all eight that exist and is complete against reality. This is a defect in the document rather than in the delivery, and the runbook is controller-pinned and cannot be amended from an audit round. | carryover |<br>| S1-R1-03 | low | docs/ephoros-typescript-rule-parity/design-probes/ | `corpus/decoy.ts`, 225 bytes, is not shipped, so the record of method is incomplete in the one place it is checkable. It is the entire discriminating input for `interpolated-message-recall`: it holds two occurrences inside a block comment and a string literal, a masked reader reports 14 and an unmasked reader 16, and that gap is what eliminates `unmasked-regex`. The clone is 19 MB and unshippable, but the decoy is not, and the criterion it settles decides the selection. The step conformed to its Files field, which names `design-probes/*.py` and so excludes it. It is left as carryover rather than fixed here because placing it is a decision this step never made: `docs/` is walked by `lint-ephoros` and `lint-phylax`, and `marketplace-clean-honesty` requires the tree lint to exit 0 without a pragma and a fixture-directory specimen to be proved unreachable by the walk. Shipping a deliberate adversarial specimen into a walked directory needs that unreachability argument, which belongs to the step that owns the recognisers. | carryover |<br><br>Leads not pursued: the fixes commit changes no test, so its Elenchus verdict is `unguarded` by the runner's own rule rather than by a missing guard; a README stating what code is for has no assertion to carry, and the five committed cases deliberately assert nothing about the probe modules. The conformance report at the Tests field's declared path, `.hexaemeron/reports/conformance/fiat-1420-step-1.json`, recorded `"failures": 1` over 2,478 tests when this round opened; the runner refuses an existing target, so it was preserved outside the run and re-run at the audit head, where it records 2,478 tests with 0 failures and 0 errors at exit 0. That path is gitignored and never reaches the tree, so no reader of the repository could have been misled by the stale value, and the cause of that one recorded failure was not chased. The design record marks `reader-peak-space` `pass` on all three candidates although its comparator is `minimise` and the selected candidate is the worst of the three, 831,792 bytes against 132,667; the study's own table declines to score that row and states the trade with both numbers, so the prose is honest and only the machine-readable cell is coarse. That semantics belongs to Protasis and the record is a byte-identical copy of a controller-pinned artefact, so it was not touched. Evidence for the covered concerns: `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each report clean at exit 0 over their full declared path vectors, before and after the fix; the root suite reports 1,628 of 1,628 at exit 0 on the step head and the same green through `.githooks/greenlight` on the staged audit tree, recorded as tree `41380d5c9b349eae96b547e677ced5f33b66ecfb`; `design_evidence.py --transition design-lock` exits 0 against the committed record at `docs/ephoros-typescript-rule-parity/design-evidence.json`. All fifteen cited reports resolve at the exact SHA-256 the record states, with no stray report on disk and none cited but missing, and every cited path is record-relative. The three `pending` cells are exactly the `clone-verdict-e001` rows, one per candidate, each naming its resolver and `blocks: integration`, so they are explicitly deferred rather than silently unmet. Every settled cell agrees with its criterion's comparator and threshold. The shipped study, runbook, design record and all eight probe modules are byte-identical to their `.hexaemeron/` originals by `cmp`, and the study, runbook and record match the digests the directive pins. All six relative study links resolve from `docs/`. The five test cases were mutation-tested independently of the Mason's claim: corrupting a report's bytes, making a cited path absolute, changing the selected candidate, emptying the cited-report list, breaking a study link and emptying the study each turn the suite red, and the repaired vacuous case now fails on an empty list, so the count guard bites. The 29 committed files match the Files field exactly with no strays, `SOURCES.md` and `DEMONSTRATION.md` are untouched, no committed blob carries an absolute path outside the repository or a username string, and `.hexaemeron/` remains entirely untracked at both the step head and the audit head.
## Step 2, round 1 -- 2026-09-07T16:17:29Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=reviewed; clone-verdict-honesty=reviewed; constant-template-boundary=reviewed; e002-e005-overlap=not-applicable; e003-duration-shape=not-applicable; fixture-repin-scope=reviewed; marketplace-clean-honesty=reviewed; pragma-surface-parity=reviewed; self-lint-recursion=reviewed<br><br>Not checked: the E002 and E003 TypeScript surfaces, which steps 3 and 4 introduce, so `e002-e005-overlap` and `e003-duration-shape` have no surface at this head: `check_typescript` emits E000, E001 and E005 alone, `TS_METRIC_WORDS` and the mean-over-duration recogniser stay Python-only, and no label-key or duration path moved in the diff. Also not checked: whether the Mason's recorded metron medians of 73.3 ms and 1,018.6 ms reproduce, because both were taken on that run; the recogniser's cost was re-measured here instead, and the re-measurement rather than a confirmation of those two figures is what this record carries. Also not checked: whether the fifteen selection reports' values are reproducible, unchanged from round 1's reading, since the probes still read a gitignored extraction and an unshipped decoy. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-01 | medium | plugins/hexaemeron/tests/test_ephoros_checker.py | Half of E001 shipped with no committed guard. `formatted_message` answers two questions -- an interpolated template literal, or a `+` in the mask beside a quote in the text -- and all eight cases of `TypeScriptInterpolatedMessageTests` exercise only the first. The branch itself is correct: `logger.info("got lender " + lender)` reports one E001 and `logger.info(a + b)` reports nothing. Nothing in the suite said so. Two mutations prove the gap rather than argue it: deleting the concatenation branch, and widening it by dropping the quote requirement, each leave the committed 123 cases green. The pinned clone cannot close the gap either, because all 21 of its logger call sites pass a template literal and none passes a concatenation, so both the 14 that fire and the 7 that stay clean come from the first branch alone. Fixed by committing those two observations as cases; each mutation now turns the suite red at 125. The recogniser is untouched, so the clone verdict is still 14. The Tests field enumerates eight cases and says "expect eight cases"; that bounds what the implementation step owed, and an audit round's guard is additive to it, so this is read as permitted rather than as an amendment question. | fixed in f0481e39319d685a365608e471f06e01a140a0a1 |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | The concatenation branch takes its `+` from the mask, where comments and strings are blanked, but its quote from the raw text, where they are not. A quote inside a comment inside the first argument therefore counts as a string literal: `logger.info(a + b /* it's fine */)` reports E001, and so does `` logger.debug(`cost is \${x}`) ``, whose escaped dollar-brace is a constant template that `_interpolated` reads as interpolation. Both diverge from the rule `SKILL.md` states, and both fail toward reporting rather than toward silence. Neither occurs in the pinned clone or in the walked marketplace, and the mask cannot supply the quote because it blanks the delimiters too, so the repair needs the index to tell a comment span from a string span -- a change to the surface E002 and E003 land on in steps 3 and 4. Left for the step that owns that surface rather than repaired from an audit round against a controller-pinned clone count. | carryover |<br><br>Leads not pursued: the acceptance clause was settled by reading the clone rather than by agreeing with the probe. All 21 logger call sites in `wildcat-app-v2@564a189b` were enumerated and their first arguments read: 14 are interpolated template literals and 7 are constant templates -- `Getting authorised lenders batches...`, `Getting withdrawal batches...`, `Getting batch updates...`, `Getting all markets...`, `Getting lender updates...`, `Getting lender withdrawals...` and `Updating withdrawals...` -- with no `${` in any of the seven and no concatenation anywhere. The 14 land exactly where the study said, 7 in `useGetWithdrawals.ts`, 5 in `useGetLenderWithdrawals.ts`, 1 in `updateMarkets.ts` and 1 in `useGetLenders.ts` at line 29, so the two counts agree site for site and not merely in total. A separate text sweep for every non-`console` `.debug|.info|.warn|.error|.log(` call in the clone returns 24 hits: those 21 and three `toast.error` calls, which are user-facing notifications rather than telemetry and which no sink gate claims. `design_evidence.py --transition integration` is clean and is not vacuous: rewriting the report's `value` to 99 turns it red with `D008 span-index/clone-verdict-e001 report fails the gate at integration`, and the file was restored byte for byte afterwards. The report itself is one closed `protasis-design-report/v1` object carrying exactly the six field values the Exit names. The criterion it resolves was pinned `equals 14` at design lock, before the recogniser existed. The metron figure was re-measured rather than accepted. `runtime.py span-index` exercises the archived `strategies.span_index` prototype, not the shipped recogniser, so its move from 27 ms at design lock to 73.3 ms cannot be the recogniser's cost; on this host the same unchanged prototype now reads 65.2, 56.8 and 58.0 ms. The shipped checker was timed directly over the same 65,473-byte specimen at the step head and at its parent: median 57.5 ms against 53.8 ms over five runs each, so the recogniser costs about 4 ms of 55 against a 2,000 ms gate, and the 27-to-73 gap is host variance measurable on code this step did not touch. `formatted_message` does read from the index: the first argument comes from `ranges`, which reads the per-file comma table, and both answers come from offset tables built once per file and bisected. The commit message's red-first quotation was reproduced rather than trusted: the step's tests were run against the parent checker in an isolated tree and gave `Ran 123 tests` and `FAILED (failures=3)` on exactly the three cases the message names, each with the same `Lists differ: ['E001'] != []` assertion. Six of the eight new cases were green against the parent by construction, because they assert that nothing fires and a checker with no recogniser satisfies that trivially; only an assertion that something fires can go red first. `tests/promise_machine_coverage.json` re-pins correctly: `11191d67ca3abb2bbbc2268701cff6155ee5e8254baa42ee96aa9e55dae31753` is the SHA-256 of the committed `SKILL.md`, it occurs exactly once in the file, the superseded `adc2a269...` occurs nowhere, the two other `skill_path` entries naming that document carry no digest, and the diff touches no `Promise`, `Evidence`, `Boundary`, `Authorises`, `Consequence`, `Refuses`, `Recovery` or `Exceptions` line, so the binding's field map correctly did not move. That out-of-Files edit is a defect in the runbook rather than in the tree: the Exit requires `SKILL.md` to change, the digest binds it whole-file, `tests/test_unique_identifiers.py` recomputes it, and no command regenerates it, so the step had no conforming alternative; the runbook is controller-pinned and an audit round cannot amend it, so it is recorded here rather than raised as a finding against the commit. The two new fixtures are unreachable by the tree lint by construction, because `walk` drops any path with `fixtures` in its parts, so `marketplace-clean-honesty` holds with no pragma. `logger-message.ts` is byte-identical to its base and now reports one E001 and no E005, so the original claim that an address inside a message is not an E005 key survives beside the new one. The Python side keeps its divergence: a placeholder-free f-string still reports E001 there while a `${}`-free template stays clean here, and `SKILL.md` states that in the same paragraph. Behaviour beyond the eight cases was probed and left alone where the rule as written is honest about it: a tagged template such as `String.raw` and a ternary carrying an interpolated template both stay clean, and a `+` inside a nested call or beside a string-keyed index both fire, which is imprecision of the same family as `S2-R1-02` and was folded into it rather than given rows of its own. Optional chaining, multi-line calls and `this.log.error` all fire correctly, and `logger.debug()` with no arguments does not crash, because `ranges` always returns at least one span. Gates: `lint-ephoros`, `lint-phylax` and `lint-hypomnema` each report clean at exit 0 over their full `tests/check-map-v1.json` vectors, before and after the fix; `python3 -m unittest discover -s tests` reports `Ran 1628 tests` and `OK` at exit 0 through `.githooks/greenlight` on the staged fix tree, recorded as tree `7ac10425cfe3fc69cf3199138d5ab1e557c02e72`; `test_ephoros_checker` moves 123 to 125 at exit 0; `horos.py check .` prints `boundary matches the tree`; `audit_synopsis.py --check .` and `git diff --check` both exit 0. No committed blob in this step or this round carries an absolute path outside the repository or a username string. One gap is left to step 5 rather than repaired here: the tracked `docs/ephoros-typescript-rule-parity/design-evidence.json` still refuses `--transition integration` with `D008 ... report is unavailable`, because `reports/span-index-clone-verdict-e001.json` was not shipped beside it; step 2's Files field excludes `docs/` and step 5's Files field re-ships both the record and its reports, so the shipped archive resolves when that step lands. The declared runner contract was applied unchanged; at `elenchus.py`'s default 900-second ceiling the first run returned `inconclusive` with `the run did not finish inside 900s`, so it was repeated at `--timeout 3600`, where the runner finished and reported 2,488 executed tests with 0 assertion failures and 0 errors on the parent. The verdict is `passed` rather than `guarded` by the runner's own rule: the guard's subject is the parent's own recogniser, so a case that asserts it works cannot go red there, and no translation of that verdict was attempted.
## Step 2, round 2 -- 2026-09-11T07:44:58Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=reviewed; clone-verdict-honesty=reviewed; constant-template-boundary=reviewed; e002-e005-overlap=not-applicable; e003-duration-shape=not-applicable; fixture-repin-scope=reviewed; marketplace-clean-honesty=reviewed; pragma-surface-parity=reviewed; self-lint-recursion=reviewed<br><br>Not checked: the E002 and E003 TypeScript surfaces, which steps 3 and 4 introduce, so `e002-e005-overlap` and `e003-duration-shape` still have no surface at this head. Also not checked: the metron medians, because the recogniser is byte-identical to `41fad525` and round 1 re-measured it; the fifteen selection reports' reproducibility, unchanged from rounds 1 and 2; and who authored the four staged, uncommitted paths this round inherited, dated 7 September with no commit or receipt behind them, which no artefact on this machine can establish. The recorded non-Solidity waiver excludes the Pashov suite, so `x-ray`, `solidity-auditor` and `fizz` did not run.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | Disposition decided: documented rather than narrowed. Measured over `wildcat-app-v2@564a189b` with the shipped span index, 24 bracket sites sit behind a log-named receiver, 21 name a log method and 3 are `.find` or `.some` on `log` and `logs`, which the method gate excludes. None of the 21 first arguments carries a `+` in the mask or the text, none carries `\${`, all 14 that fire do so from an unescaped `${` in a template literal, and the 7 that stay clean are constant templates; the concatenation half fires 0 times over the corpus, so the imprecision has no exposure there. The three shapes still fail toward reporting and a repair needs the span kinds threaded into the index, which is the surface steps 3 and 4 build on. The inherited staged paragraph documenting this was corrected in three places before landing: `${` is read anywhere in the first argument once it opens with a backtick, so ``logger.info(`done` /* ${x} */)`` fires and "inside the literal" understated it; the `+` must sit outside any string or comment, so ``logger.info(a /* it's + fine */)`` stays clean where "outside any string" predicted a finding; and "escapes no `${` anywhere" was false as written, because `storybook-static/71.4a477e29.iframe.bundle.js`, its `.js.map` and `sb-manager/formatter-SWP5E3XI-7BGIK6BL.js` carry `\${`, so the claim is scoped to the `.ts` and `.tsx` files the walk reads. | carryover |<br>| S2-R2-01 | low | plugins/hexaemeron/tests/test_ephoros_checker.py | The round-1 guard for the concatenation half is shape-bound. Its two cases name one shape, a double-quoted literal left of a `+`, and three recogniser mutations survive them together with the five cases staged since: requiring the quote to precede the `+` (a literal on the right goes clean), dropping the backtick from the quote set (`` `got ` + lender `` goes clean), and letting `_has` ignore the argument's end (a later `"a" + b` anywhere in the file fires at the call). Fixed with three cases, one per mutation. The five staged cases were adopted after each was shown to kill exactly one mutation the committed 125 survive: quote set narrowed to `"`, `+` read from the text, method gate dropped, whitespace strip dropped, last argument read instead of first. | fixed in 18b34cad43f0475fda253221f723db7c970585ac |<br>| S2-R2-02 | low | plugins/hexaemeron/tests/test_ephoros_checker.py | The E001 line is unguarded on a multi-line call. Reporting at the closing line instead of the opening line survives every case in the class, because both fixtures are single-line calls; 11 of the clone's 14 findings are multi-line `logger.debug(` calls counted at their opening line, including `useGetLenders.ts:29`, so the line the Exit and the study name is the one no test held. Fixed with one case: a three-line call reports `("E001", 1)`. | fixed in 18b34cad43f0475fda253221f723db7c970585ac |<br><br>Leads not pursued: the inherited staged work was verified as if written here and adopted in part. The five test cases are true and non-vacuous; the paragraph's two clone claims were checked against the extraction itself, one true and one over-broad as recorded in `S2-R1-02`; the staged `SKILL.md` did hash to `188be096064f0fbdbbcb9e7aa70056054c3a32b70f9f1f9f49130f11a4c1291a`, that digest occurred once in `tests/promise_machine_coverage.json`, and the corrected file re-pins to `01beed78dd0769c141090f04bafa2aef70a6ea9d19414909ab27c827d8be1305`, once, with the superseded `11191d67...` and `188be096...` occurring nowhere and the binding's field map untouched, so the whole-file diff of that JSON is the one `sha256` line. Seventeen recogniser mutations were run against the extended class: sixteen turn it red and one survives, removing the `console` skip, because `console.log` is already excluded by the sink-word gate (`console` is not in `TS_LOG_WORDS`), so the console case proves its claim through that gate rather than that line, and the line still guards the `[` E005 path. One method artefact is recorded because it changed a result: a same-size mutation written within the same second as the previous one reused stale bytecode under `importlib`, so two mutations first read as survivors; every run was repeated with `-B` and cleared `__pycache__`, and the `${`-from-mask mutation then turned six cases red while the closing-line mutation stayed a survivor until `S2-R2-02`. The six example claims the corrected paragraph makes were each confirmed by probe and deliberately not pinned as cases, because `S2-R1-02`'s repair would flip them; the step that repairs it owns the paragraph and its digest. Probed and left alone as honest under the rule as written: `logger.info(msg += "x")` fires and `logger.info(count += 1)` does not, a chain of three operands fires, a regex literal carrying `+` and a quote stays clean, `logger.info(+count, 'x')` stays clean while `logger.info(f(+count, 'x'))` fires, `logger.info(n + 1 > 0 ? 'a' : 'b')` fires, `logger.info(x as 'a' | 'b')` stays clean, a tagged template and a ternary carrying an interpolated template stay clean, and `log.warn`, `logging.info`, `logs.info` and `this.log.error` all fire. `constant-template-boundary` holds on both sides: `` logger.info(`hello`) `` reports nothing and `log.info(f"hello")` reports E001. `pragma-surface-parity` holds for E001: a reasoned `//` pragma on the line and the line above suppresses, a bare pragma, a block-comment pragma on either line, pragma text inside the template or a string argument and a `#` pragma each leave E001 standing, and a reasoned pragma does not suppress E000. `fixture-repin-scope`: `logger-message.ts` is byte-identical to the step head and reports one E001 at line 2 and no E005. `e000-containment`: ignoring lexer errors turns the E000 case red, and the 1 MiB cap case stands. `recogniser-scan-shape`: the recogniser is untouched, `formatted_message` still reads the first argument from the comma table and both answers from file-wide tables built once and bisected. Gates: `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each report `clean` at exit 0 over their full `tests/check-map-v1.json` vectors on the fix tree, so `self-lint-recursion` needed no triage; `python3 .hexaemeron/design-probes/clone_verdict.py` prints `"e001": 14` at exit 0 before and after the fix; `python3 -m unittest discover -s tests` reports `Ran 1628 tests` and `OK` at exit 0 through `.githooks/greenlight` on the staged fix tree, recorded as tree `997e1daabefc555af6c4059ca2ee66d97c39a73a`, and again at exit 0 on the committed fix; `test_ephoros_checker` moves 125 to 134 at exit 0; `horos.py check .` prints `boundary matches the tree`, beside a `candidate drift` line naming this run's `.synopsis.md` as evidenced by the tree but missing from the boundary, which is a candidate rather than a drift and exits 0; `git diff --check` exits 0. The declared runner contract was applied unchanged at `--timeout 3600`: the runner names the `elenchus.unittest.v1` report `unittest-json-v1` on its `--report-format` flag, the report landed fresh at `.hexaemeron/reports/conformance/fiat-1420-step-2-round-2.json` inside the detached parent, and it recorded 2,497 executed tests with 0 assertion failures and 0 errors. The verdict is `passed` rather than `guarded` by the runner's own rule: every new case asserts behaviour the parent's recogniser already has, so none can go red there, and the runner lists `tests/promise_machine_coverage.json` beside the test module among the changed test files, which is its reading of the `tests/` prefix rather than a claim about the file. `marketplace-clean-honesty`: no pragma was added anywhere in this round and the two E001 fixtures stay unreachable by the walk. No committed blob in this round carries an absolute path outside the repository or a username string.
## Step 3, round 1 -- 2026-09-11T13:14:06Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=reviewed; clone-verdict-honesty=reviewed; constant-template-boundary=reviewed; e002-e005-overlap=reviewed; e003-duration-shape=not-applicable; fixture-repin-scope=reviewed; marketplace-clean-honesty=reviewed; pragma-surface-parity=reviewed; self-lint-recursion=reviewed<br><br>Not checked: the E003 surface, which step 4 introduces, so `e003-duration-shape` has no surface at this head. A predecessor Warden produced the five staged, uncommitted paths this round inherited (`ephoros.py`, `test_ephoros_checker.py`, `SKILL.md`, `tests/promise_machine_coverage.json`, `.horos/census.json`) and was ended by a session limit before recording or committing anything; nothing on this machine establishes more about that work than the verification recorded here, which reproduced every claim before adopting it and replaced its `_item_offset` draft. The Pashov suite and fizz, waived by the directive because the run ships no Solidity. The pinned clone gives no E002 evidence: `wildcat-app-v2@564a189b` holds 0 label containers, so its E002 count of 0 is the absence of a surface, not a verdict on the recogniser; the two fixtures and the nine-case class are the whole guard.<br><br>Elenchus verdict: guarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | Still open, and this step widened the family without repairing it. The span kinds are still not threaded into the index, so `_string_bounds` trims whitespace in the text, where a comment is not blank: a string label element or string key preceded by a comment inside the same item, `tags: [` newline `// note` newline `"requestId" ]` or `{ /* c */ "walletAddress": w }`, is unrecognised by both E002 and E005, while the same shape with an identifier key is read from the mask and reported correctly. This half fails toward silence, unlike the E001 half. 0 occurrences in the pinned clone, which holds no label container, and 0 in the three walked `.ts` files. Left for the step that threads span kinds through the index, as step 2 round 2 recorded. | carryover |<br>| S3-R1-01 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | `_object_keys`, the array branch of `_label_container` and the string branch of `labels_call` reported each finding at the item's first character, the one after the separator, so a key on its own line in a multi-line container was reported one line early and the documented `// ephoros: allow <why>` beside the key did not suppress it. Reproduced at `98a29871` over a four-line `labels: {` literal: `walletAddress` on line 3 reports at line 2, `requestId` on line 4 at line 3, and a reasoned pragma beside `requestId` leaves E002 standing; the E005 half reproduces at `9e397521`, so it predates this step. The inherited draft walked the text past whitespace, which left a comment on its own line before the key taking the finding (line 3 for a key on line 4) and the pragma beside the key inert; it was replaced by reporting at the key's own offset, the chain start read from the mask or the opening quote. Guarded by `test_a_key_on_its_own_line_reports_that_line_and_takes_its_pragma`, red at `98a29871`, red on the inherited draft, green on the fix. | fixed in daea70fd8cc0d263f72f101363c174b2a3b77a8d |<br>| S3-R1-02 | low | plugins/hexaemeron/skills/ephoros/SKILL.md | The camel-case paragraph said Python's `_`-anchored rule reports only `request_id` and `chain_id`. Running the Python checker: its `request_?id`, `run_?id` and `trace_?id` fragments already report `requestId`, `runId` and `traceId`, and its `id` fragment reaches `chain_id`, `market_id` and `token_id` while `chainId`, `marketId` and `tokenId` pass. The paragraph now states that reach exactly, names `chainId`, `marketId` and `tokenId` as keys the `id` word claims here, and scopes E005's `-es` limit to the Python and block-YAML surfaces, which share `ADDRESS_KEY`. The inherited wording was adopted after each of its claims was reproduced. `tests/promise_machine_coverage.json` re-pins the whole-file digest to `37b8462ecd8d7183ac52acdc832ecac0cd2e44fc126df540e5c03ec2bfe3dfb5`, once; `a0086f8d...` occurs nowhere and the binding's field map is unchanged. | fixed in daea70fd8cc0d263f72f101363c174b2a3b77a8d |<br><br>Leads not pursued: `chainId` reporting E002 through `id` is the documented behaviour of this surface, not a finding against it. Python's `id` fragment has reported `chain_id` since E002 existed, the Exit binds the TypeScript vocabulary to the same fragments, and narrowing `id` on this side alone breaks parity and drops `requestId` (mutation m11, removing `id`, turns four cases red). The false positive is the vocabulary's, contradicting the skill's own bounded-label example `a chain id` on both surfaces, and is left for whoever owns the E002 fragment list. Measured surface, from the identifiers the mask exposes: across the 11 telemetry fixtures one camel-case identifier fires, `requestId`, through `id`; across the three walked `.ts` files, `deploy.ts`, `relations.ts` and `market.ts`, one identifier would fire as a key, `lineaMessageService` through `message`, none sits in a label container, and `lint-ephoros` is clean at exit 0 with no pragma; across the 882 lexable `.ts`/`.tsx` files of `wildcat-app-v2@564a189b`, 341 distinct camel-case identifiers would fire if used as label keys, 58 only through `id`, 25 of those in the chain-id family (`chainId`, `targetChainId`, `selectedChainId` among them), and a grep for `labels|labelNames|label_names|tags|attributes` before `[` or `{` and for `.labels(` finds 0 containers. The Promise Machine `ephoros-mechanical-gate` Boundary still says `E005 alone on the TypeScript surface`, stale since step 2; step 5's Files field names `plugins/hexaemeron/skills/ephoros/SKILL.md`, so the repair is covered, but step 5's Exit names only the version and the constant-template sentence, so the Boundary edit is owed by the Files field alone and step 5's round should hold it. `.horos/candidates.json` in the step commit is scan output: `horos.py scan . --write` writes `boundary.json` and `candidates.json` together while `--census --write` writes only the census, the registered entry's 27,763 bytes equal this run's synopsis blob at both `9e397521` and `98a29871`, so the step registered a file already in the tree rather than editing anything by hand, and the same scan over this round's tree re-registers it at 38,204 bytes; this round commits that refresh beside the census. `tests/promise_machine_coverage.json` sits outside the Files field for the reason step 2 recorded: the digest binds `SKILL.md` whole-file, `tests/test_unique_identifiers.py` recomputes it, and no command regenerates it. The metron clause was re-recorded rather than assumed, median of three in-process runs on this host: the shipped checker over the 65,473-byte nested-bracket specimen from `design-probes/specimen.py` reads 45.1 ms at `9e397521`, 37.4 ms at `98a29871` and 50.2 ms on the fix, with single runs from 34.3 to 56.9 ms, so every move is inside the run-to-run spread; over the 885 walked clone files 0.60 s, 0.55 s and 0.57 s against the 2,000 ms `cap-scale-runtime` gate, with E001 at 14 and E002 at 0 on all three, so `clone-verdict-honesty` holds. `recogniser-scan-shape`: keys come from the comma and colon tables through `ranges` and `_first_colon`, word starts from two file-wide tables bisected by `_word_between`, and no pass is added. The seven original cases were mutation-tested beside the two inherited cases: of 17 recogniser mutations 16 turn the class red and one survives it, making E000 suppressible by a pragma, because the class carries no pragma-on-E000 case; that rule is held against this recogniser by `TypeScriptBoundaries.test_a_pragma_cannot_suppress_a_lexer_failure`, which the same mutation turns red, so the Mason's placement of it in case 4 was wrong but the assertion exists. The 1 MiB cap and the E000 early return are each held by case 4 alone (mutations m05 and m06 fail only there). The address-first order in `_ts_label_code` and `_string_label_code` was held by none of the seven; the inherited `test_a_key_carrying_both_vocabularies_is_e005_alone` is the only case the two swap mutations fail, so it was adopted as load-bearing. `e002-e005-overlap` holds in both directions in code and by probe: `walletAddress`, `userAddress`, `walletId`, `wallet_id` and `address_hash` report E005 alone; `hashes` reports E002 and `addresses` E005 here; on Python `addresses` and `hashes` both pass while `address` reports E005 and `hash` E002, as the paragraph states. `constant-template-boundary` is untouched by this step. `pragma-surface-parity` for E002: a reasoned `//` pragma on the line or the line above suppresses, a bare one does not, and pragma text inside a block comment or a string is not a line-comment span. `fixture-repin-scope`: `logger-message.ts` is unchanged and still reports one E001 and no E005. `e000-containment`: the unterminated template and the over-cap file each report E000 alone with no E002. Gates: `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each report `clean` at exit 0 over their `tests/check-map-v1.json` vectors on the fix tree, so `self-lint-recursion` needed no triage and the checker was not edited for its own lint; `test_ephoros_checker` moves 141 to 143 at exit 0; `python3 -m unittest discover -s tests` reports `Ran 1628 tests` and `OK` at exit 0 on the committed fix, and ran to exit 0 through `.githooks/greenlight` on the staged fix tree, recorded as tree `3f30387d761db7469ed298ecfd8f20d8cb012faf`; `git diff --check` exits 0. The declared runner contract was applied unchanged at `--timeout 3600`, format flag `unittest-json-v1` for `elenchus.unittest.v1`; the report landed fresh at `.hexaemeron/reports/conformance/fiat-1420-step-3.json` inside the detached parent at `98a29871` and recorded 2,506 executed tests, 1 assertion failure and 0 errors, so the verdict is `guarded`; the runner's bounded output truncates the failing name, and the harness run of the class at `98a29871` failed exactly `test_a_key_on_its_own_line_reports_that_line_and_takes_its_pragma`. No committed blob in this round carries an absolute path outside the repository or a username string.
## Step 4, round 1 -- 2026-09-11T17:50:59Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=reviewed; clone-verdict-honesty=reviewed; constant-template-boundary=reviewed; e002-e005-overlap=reviewed; e003-duration-shape=reviewed; fixture-repin-scope=reviewed; marketplace-clean-honesty=reviewed; pragma-surface-parity=reviewed; self-lint-recursion=reviewed<br><br>Not checked: The Pashov suite and fizz, waived by the directive because the run ships no Solidity. The pinned clone gives no E003 evidence: over its 882 `.ts`/`.tsx` files a grep finds 0 `mean`, `fmean`, `average` or `avg` calls and 0 `reduce(...)` divided by a `.length` chain, so the shipped checker's E003 count of 0 there is the absence of a surface, not a verdict on the recogniser; the two fixtures and the sixteen cases of `TypeScriptMeanDurationTests` and `TypeScriptMeanDurationBoundaries` are the whole guard. The clone is an extracted tree here, so its commit identity was not re-verified; its file count matches step 3's. `runtime.py span-index` drives the archived prototype in `design-probes/strategies.py`, which builds `_TsSpanIndex` without spans and walks only the E001 path, so its 36.0 ms over the 65,473-byte specimen shows that the probe still runs and nothing about this pass. A predecessor Warden dispatched for this round was ended by a session limit before touching the tree; nothing was inherited.<br><br>Elenchus verdict: passed<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | Still open at `3cc5e63a`, reproduced: `m.labels({ /* c */ "walletAddress": w })` and `tags: [` newline `// note` newline `"requestId" ]` report nothing while the same shapes without the comment report E005 and E002. This step consulted the lexer's spans once, to build the duration word table, and did not thread them into the index: `self.spans` has one reader, `_duration_between`, and the `spans=None` default falls back to one code span over the whole text, so a caller that omits it, the archived probe among them, sees no change in any other method. Left for the step that threads span kinds through the index, as step 2 round 2 recorded. | carryover |<br>| S4-R1-01 | low | plugins/hexaemeron/tests/test_ephoros_checker.py | The pragma-on-E000 assertion in `TypeScriptMeanDurationTests.test_the_same_shape_outside_lexed_code_reports_no_e003` placed the reasoned pragma on line 1 while the E000 is reported at line 3, outside the line-or-line-above window, so the case never exercised suppression: applying the reasoned-pragma filter to E000 left it green and reddened only `TypeScriptBoundaries.test_a_pragma_cannot_suppress_a_lexer_failure`, the misattribution class step 3 recorded. The E000 early return and the 1 MiB cap are held by the case, beside `test_a_lexer_failure_reports_e000` and `test_an_oversized_typescript_file_fails_visibly`. The pragma now sits on line 2, directly above the E000; the `["E000"]` assertion is unchanged and the same mutation reddens both cases. | fixed in f0f3f803f9c6adef910ad008eb015894cdfa1d9f |<br>| S4-R1-02 | low | plugins/hexaemeron/tests/test_ephoros_checker.py | Of 21 recogniser mutations applied to the 152-test module at `3cc5e63a`, 12 survived, 10 of them behaviours `SKILL.md` documents: the target-name path (`const avgLatency = mean(samples)` is the rule's first shape and no case fired on the target alone), the right-hand-side terminator (`mean(latencies) * 1000` outside), the plain `=` (`{ meanLatency: mean(latencies) }` outside), the `.length` tail and its two-segment minimum after `reduce`, the annotation step, comment spans out of and string and template spans in the word table, and the `latencies` plural. The other two are equivalent: the compound-assignment check is redundant with `_ts_chain_ending`, which already refuses a non-identifier before `=`, and the target-start offset differs from the chain start only for a target on the line above. All fifteen documented shapes behave as written at `3cc5e63a`; only the guards were missing. `TypeScriptMeanDurationBoundaries` pins each of the ten with the shape `SKILL.md` names, eight cases; the module moves 152 to 160 and 20 of 21 applied mutations turn it red. The eight enumerated cases and their assertions are unchanged. | fixed in f0f3f803f9c6adef910ad008eb015894cdfa1d9f |<br><br>Leads not pursued: `recogniser-scan-shape`, measured rather than taken from the step: 18 specimen shapes at 128 KiB, 256 KiB, 512 KiB and 1 MiB, median of three in-process runs each, scale by 1.77 to 2.15 per doubling, all linear; the worst is 569 ms at 1,152 KiB for an `s?.mean(` nest, the plain `mean(` nest 523 ms at 1 MiB, an assigned-at-every-level nest reporting 65,536 findings 420 ms, a `reduce(` nest with a `/ waits.length` tail at every level 278 ms, 26,214 saturated assignment lines 234 ms. Shapes the step did not try: whitespace-padded nests, the assigned nest, the tailed reduce nest, a 512 KiB dotted target before one `=`, chained `= mean(a) =` assignments, optional-chain nests, dotted annotations, a word table of nothing but `ms`, a 1 MiB comment inside the argument, and long space or comment runs after each closer. The mechanism: an admitted bracket pays one backward whitespace run and one `=` test, at most one target-chain parse per `=`, one forward run to its own terminator or tail, and one bisection, and every run scanned is disjoint from every other, so the pass is O(n log n) in the file plus the findings. The ninth case, `TypeScriptBoundaries.test_a_mean_named_nest_keeps_its_single_exact_finding`, is permitted: it guards the register's own `recogniser-scan-shape` clause in the class that holds the sibling nest guards, sits outside the eight-case class the Tests field enumerates, which stays at eight, is red on `c72852fe` (the parent checker with the step's 152 cases gives 3 red: the two the commit message names, observed before this case existed, and this one), and is the only case the no-`=`-gate mutation reddens. False-positive surface, from the `\n` in `TS_RHS_TERMINATORS`: `const x = mean(latencies)` newline `  .toFixed(2)` and newline `  + 1` each fire while the same operand on one line does not; TypeScript-only, since Python's grammar ends the statement; 0 occurrences over the clone, which holds no mean call, and 0 over the three walked `.ts` files; a repair needs a lookahead across the newline and changes the shape the Exit binds, so it is left for the owner of that shape. `avgLatency ??= mean(latencies)` fires, `??` not being in the reject set, and it does assign. `AVG_TIMEOUT_MS`, `avgRetryTimes` and `avgSecondsOfArc` fire through `ms`, `times` and `seconds`; the first fires in Python too, the other two pass there under the `_`-anchored fragments, the camel-case asymmetry step 3 recorded for E002. False-negative surface: `await mean(x)`, `mean<number>(x)`, `mean(x) as number`, `mean(x)!`, a union or generic annotation, `obj["avgLatency"] =`, a destructuring target, `/ (xs.length)`, `() => mean(x)` and `useMemo(() => mean(x))` each pass; Python misses `await` as well, because `Await` is not a `Call`, and the rest are TypeScript syntax with no Python analogue; the rule is documented as lexical and none is a finding. `e003-duration-shape`: sentence lengths, layout positions and prices stay clean by vocabulary alone, and `reduce` fires only under a duration word in the target or the expression, `xs.reduce(f, 0) / latencies.length` included. `_ts_chain_before` keeps its signature and returns `chain[1]` of `_ts_chain_span`, the segments or `None` it returned before; its callers in the tree are the wrapper and `strategies.py:53`, whose `.hexaemeron/design-probes` and `docs/ephoros-typescript-rule-parity/design-probes` copies are byte-identical; `check_typescript` now reads `_ts_chain_span` and its `chain is None or not chain[1]` gate equals the old `not segments`. `tests/promise_machine_coverage.json`: `5e104e4331cae43a0d7301810b432621f614826a483146847592126b3198ff9c` is `shasum -a 256` of the committed `SKILL.md`, occurs once in the tree, at line 4267, `37b8462e...` remains only in the step 3 record and synopsis, and the `ephoros-observability-review` bindings are the same eight fields; the `ephoros-mechanical-gate` Boundary still says `E005 alone on the TypeScript surface`, owed by step 5 as step 3 recorded. `clone-verdict-honesty`: the shipped checker over the extracted clone reads 0.58, 0.52 and 0.59 s against the 2,000 ms gate with E001 at 14, E002 at 0 and E003 at 0. `fixture-repin-scope`: `logger-message.ts` is outside the diff and reports one E001 and no E005. `e000-containment`: the unterminated template and the over-cap file report E000 alone with no E003, and a reasoned pragma directly above the E000 leaves it standing. `pragma-surface-parity` for E003: a reasoned `//` pragma on the line or the line above suppresses, a bare one does not, and pragma text inside a block comment or a string suppresses nothing. `constant-template-boundary` and `e002-e005-overlap` are untouched by the diff and hold: the template literal holding `${mean(latenciesMs)} ms mean latency` is masked and reports nothing, and `_ts_label_code` is unchanged. `marketplace-clean-honesty`: `lint-ephoros` exits 0 with no pragma over `plugins tests scripts docs`. Disciplines: the E003 message names the mean and the histogram remedy in one sentence; the vocabulary and the dataflow non-goal are in `SKILL.md`; the guard's red observation reproduces on `c72852fe`. Gates: `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each exit 0 over their `tests/check-map-v1.json` vectors at `3cc5e63a` and on the fix, so `self-lint-recursion` needed no triage and the checker was not edited; `python3 -m unittest discover -s tests` reports `Ran 1628 tests` and `OK` at exit 0 at `3cc5e63a` and through `.githooks/greenlight` on the staged fix tree `a2442488406a731f433878b73cb45b78e3db88ac`; `git diff --check` exits 0 over the range; 0 added lines carry an absolute path or a username; the Horos census and boundary rescanned over `3cc5e63a` produce no diff, so the step's files were current, and the census is refreshed again in this round's commits. The declared runner contract was applied unchanged at `--timeout 3600`, format flag `unittest-json-v1` for `elenchus.unittest.v1`; the report landed fresh at `.hexaemeron/reports/conformance/fiat-1420-step-4.json` inside the detached parent at `3cc5e63a` and recorded 2,523 executed tests, 0 assertion failures and 0 errors, so the verdict is `passed`: the fix adds guards for behaviour the parent already has, and its red observation is the mutation set above, not the parent. No committed blob in this round carries an absolute path outside the repository or a username string.
## Step 5, round 1 -- 2026-09-11T19:10:13Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: recogniser-scan-shape=reviewed; e000-containment=reviewed; clone-verdict-honesty=reviewed; constant-template-boundary=reviewed; e002-e005-overlap=reviewed; e003-duration-shape=reviewed; fixture-repin-scope=reviewed; marketplace-clean-honesty=reviewed; pragma-surface-parity=reviewed; self-lint-recursion=reviewed<br><br>Not checked: The Pashov suite and fizz, waived by the directive because the run ships no Solidity. Whether the extraction at `.hexaemeron/validation/wildcat-app-v2` is the tree of commit `564a189b`, which no artefact in this repository can establish; it is untracked, was not modified or re-extracted here, and its 882 `.ts`/`.tsx` files match the study's assumption 2 and steps 3 and 4. Who authored the four staged paths dated 7 September that step 2 round 2 inherited, unchanged from that round. Imprimatur and Vulgate over the step's prose, which the prose phase owns. Whether the eight archived probe modules reproduce their sixteen reports, unchanged from round 1. A predecessor Warden was dispatched for this round and ended by a session limit before touching the tree; nothing was inherited.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S2-R1-02 | low | plugins/hexaemeron/skills/ephoros/scripts/ephoros.py | Still open at `159a8541`, reproduced on the shipped checker: `logger.info(a + b /* it's fine */)` and `` logger.debug(`cost is \${x}`) `` each report E001; `m.labels({ /* c */ "walletAddress": w })` reports nothing where `m.labels({ "walletAddress": w })` reports E005; `tags: [` newline `// note` newline `"requestId" ]` inside `new Counter({ ... })` reports nothing where the same array on one line reports E002. The same `tags:` array in a plain object literal reports nothing with or without the comment, because a label container is one inside a metric-named call or a `.labels(...)` argument, so the ledger's example holds only under that definition. This step changes no recogniser. The lead is now the ephoros ledger's held `Next Fiat job` under revision `span-kinded-index`, with the four shapes above as its red-first fixtures, so its home for the run is that row rather than a new issue. | carryover |<br><br>Leads not pursued: the version label was ruled on first. `ephoros-v2.2.0` is the label the contract requires: `VERSIONING.md` increments evolution and says the other counters do not reset, and `tests/test_evolution_contract.py`'s `test_history_axes_enforce_independent_counters_and_frontier_hold` asserts an evolution row is `(prev[0] + 1, prev[1], prev[2])` from `ephoros-v1.2.0` with a changed digest, while a `1.3.0` row would be a generation row required to keep revision `typescript-rule-parity` and digest `02ce0a73...`. The runbook's Exit and Tests tokens `ephoros-v1.3.0` and `1.3.0` are a defect in a controller-pinned document that study assumption 6 anticipated, so the token is a carryover for Fiat and not a finding against the tree. The row was verified as evidence: SHA-256 over the exact line `open|span-kinded-index|<Current frontier>|<Next Fiat job>` with its final newline recomputes to `d1d922633d2121067b6e74c5b667f820fea74f7f4d454ee935ebc34af959294e`; the history moves from three rows to four, generation `2` and epoch `0` are retained, the header's `Current version`, the row and the `SKILL.md` frontmatter all read `2.2.0`, and every link in the ledger resolves under the hypomnema walk. The clone-verdict clause was checked in both halves. The shipped checker over the extraction reports exactly 14 findings, all E001, at 7 sites in `useGetWithdrawals.ts`, 5 in `useGetLenderWithdrawals.ts`, `updateMarkets.ts:83` and `useGetLenders.ts:29`; a grep for `logger|log|logging|logs` before `.debug|.info|.warn|.error|.log(` finds 21 sites in 5 files (9, 7, 2, 2, 1), so 7 stay clean; the clone carries 0 `ephoros: allow` pragmas, so none is suppressed; `clone_verdict.py` prints `"e001": 14` at exit 0. The absence half: 0 matches for `labels|labelNames|label_names|tags|attributes` before `[` or `{` and for `.labels(` across the 882 files; 0 `mean|fmean|average|avg(` calls; the only `/ <name>.length` matches are two commented-out `=== 0` comparisons at `LendersSection/index.tsx:94` and `:100`; the nearest shape is `avgAprWeighted: aprDenom > 0 ? aprWeightedSum / aprDenom : 0` at `src/lib/protocol-stats/aggregate.ts:58`, a weighted mean of an APR with no duration word and none of the recognised shapes, so "no label container and no mean over a duration" is true and "absence of surface" is the right reading of the two zeros. The metron clause was measured: the checker over the extraction takes 0.82, 0.64 and 0.68 s wall-clock, median 0.68 s, against the 2,000 ms budget. The next job was judged on evidence: the four shapes it names are real on this tree as the table row records, its acceptance is mechanical (red-first fixtures, two clean and two reporting at the key's own line with a reasoned pragma suppressing, `logger.info("took " + ms)` still firing, the clone count held at 14, a cap-scale measurement against 2,000 ms, a clean marketplace lint, both suites), and nothing more serious is left unstated: the false positives and false negatives step 4 listed are documented lexical limits that fail toward reporting or have no Python analogue, and the only shapes found that fail toward silence are the family the job names. One same-cause shape is outside the acceptance list: `` logger.info(`done` /* ${x} */) `` reports E001 because `${` is read anywhere in the first argument, so a fixture for it belongs beside the four and is additive. The decision record is well-formed: `# Decision:` heading, `Accepted, 2026-09-11`, the five sections, a 47-byte lowercase kebab slug; `SKILL.md:199` cites `adr/keep-a-constant-template-literal-outside-e001` and the hypomnema walk is clean, so H009 found the draft; `git diff 96f50824..159a8541 -- docs/decisions/` touches only the draft, the two inherited `docs/decisions/draft-*.md` files are unchanged, `origin/main` at `93b9d45d` holds ADR-088 as its greatest number and no `drafts/` directory, so the composer will assign ADR-089, and `decision_assignments.py plan` against that base refuses `object-ancestry` as the Mason reported, because the product does not contain the moved base. The draft's own arithmetic, 21 sites, 14 interpolated and 7 constant, matches the grep. The shipped copies were checked against the committed files: study, runbook and `design-evidence.json` are byte-identical to their `.hexaemeron/` sources by `cmp`, at the digests the directive pins (`f01e8a30...`, `50f8f0e8...`, `bc5f6eb2...`), each carries 0 `### Amendment --` headings, 16 reports are shipped, and `design_evidence.py --transition integration` is clean at exit 0 on the committed record and on the controller's; the record's three `clone-verdict-e001` cells stay `pending` in the bytes and the span-index one resolves through `reports/span-index-clone-verdict-e001.json`, value 14, exit 0, which is what step 2 left to this step. The fifth version pin is forced by the Exit rather than scope creep: `test_phylax_model_proxy.py:5679` asserts the set of the four manifest versions, and leaving it at `1.6.30` reddens `run_tests.py`, which the Exit requires at exit zero; `1.6.33` because `origin/main` holds `1.6.32`; integration will conflict on those six lines, four manifests and two test pins, and resolves by keeping `1.6.33`. Promise and digest movement: `7a993ef04f74ff490a6dc77daecd103c45198eb7680e069f425189ed600ea786` is `shasum -a 256` of the committed `SKILL.md`, occurs once in `tests/promise_machine_coverage.json`, `5e104e43...` occurs nowhere outside audit records, the two promise headings `ephoros-mechanical-gate` and `ephoros-observability-review` are unchanged so no identifier-history entry is owed, and `E005 alone on the TypeScript surface` occurs nowhere in the tree outside audit records; the register's `E005 alone on the TypeScript side` is a different sentence about overlap and is true. Files field: 16 files committed; outside the field are `test_phylax_model_proxy.py`, `tests/promise_machine_coverage.json` (recorded since step 2), `docs/ephoros-typescript-rule-parity/design-probes/README.md` (fifteen to sixteen reports and the pending-cell paragraph, true once the sixteenth report landed) and `docs/decisions/drafts/` in place of `docs/decisions/ADR-<n>-<slug>.md`, which the hypomnema convention and the composer require; each is forced by the Exit or by the frontier's cold-read obligation, so the Files field is the defective document. The plugin `AGENTS.md` and `README.md` are unchanged after a cold read, and neither describes the rule scope beyond a one-line table entry; a sweep for `read Python only`, `E001 to E003 read`, `TypeScript parity for` and `E005 alone on` finds no stale statement outside the controller-pinned study and runbook, the `ephoros-v1.2.0` history row and audit records. `VERSIONING.md`'s obligations: the cold read is met; the `SOURCES.md` refresh is not owed because ephoros is not one of the four named skills; the status stays `open` with a job on reproduced shapes, which is a concrete, evidenced chance of improvement; no `declared-inputs` block is present and none is owed, though the job depends on the pinned clone as a corpus and could declare it. The hypomnema study mode, `--study docs/ephoros-typescript-rule-parity-study.md --design-evidence docs/ephoros-typescript-rule-parity/design-evidence.json --repo-root .`, reports `H008 study has no design bridge block` at exit 1; no step's Tests field named that mode, the ordinary walk is clean, the study is controller-pinned, and the decision homes exist, so it is recorded here for the carryover rather than as a finding. Register concerns held by probe: an unterminated template reports E000 alone; `` logger.info(`hello`) `` reports nothing and `log.info(f"hello")` reports E001; `walletAddress` reports E005 alone; `avgSentenceLength = mean(lengths)` and `/ prices.length` stay clean while `avgLatencyMs = mean(latenciesMs)` fires; `logger-message.ts` is outside the diff and reports one E001 at line 2 and no E005; 0 pragmas are added in the diff and `lint-ephoros` is clean over `plugins tests scripts docs` without one; `ephoros.py` is untouched by this step, so `self-lint-recursion` needed no triage. Gates: `lint-phylax`, `lint-ephoros` and `lint-hypomnema` each report `clean` at exit 0 over their `tests/check-map-v1.json` vectors; `test_ephoros_checker` reports 160 at exit 0, from 115 at the run base `e7d0fdea`; `tests.test_ephoros_typescript_parity` reports 11, the 5 inherited and the 6 the Tests field expects; `python3 -m unittest discover -s tests` reports `Ran 1634 tests` and `OK` at exit 0 at `159a8541`, 191 s, and again through `.githooks/greenlight` on the staged audit tree; `python3 scripts/run_checks.py --full --format json` reports outcome `green` at exit 0 on its first run, all 35 selected checks `passed`, `hexaemeron-suite`, `root-suite`, `demonstrations-suite` and the three lints among them; `horos.py check .` prints `boundary matches the tree`; `git diff --check` exits 0 over the range; 0 added lines carry an absolute path or a username. No fixes commit was made, so no Elenchus run was owed and the verdict is null.
