## Step 1, round 1 -- 2026-09-05T01:36:44Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=not-applicable; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=reviewed; ledger-row=not-applicable; no-execution=not-applicable

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. The step's diff is the two verbatim spec copies under `plugins/horos/docs/markdown-outline/` and `.horos/boundary.json`; no source, test or skill file changed, so the eight outliner ids, `census-regeneration`, `fixture-repin`, `ledger-row` and `no-execution` are not-applicable. This round ran inline by the controller. The receipted `.hexaemeron/study.md`, `.hexaemeron/runbook.md` and `.hexaemeron/design-evidence.json` (`c8879b32c9405b0d91ae6b5a0789059457a54df2b81e217b081c30357b451256`) were left unedited; both copies `cmp` equal to them.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | medium | .horos/boundary.json | The step's boundary regeneration ran before the two copies were `git add`ed, so the scan's tracked universe did not include them and the committed boundary stayed at `files_walked` 2542 against a fresh scan's 2544. In a clean detached snapshot of the implement head `a899e4ff` the root suite was `Ran 1233 tests`, `FAILED (failures=2)`: `test_boundary_currency.test_the_committed_boundary_matches_a_fresh_scan` and `test_agent_instruction.test_horos_boundary_is_current_for_the_scaffold`, both on `.horos/boundary.json#counts`. The implement receipt's test summary claimed that snapshot green in error; this record corrects it. Fixed at `4319ba9432a24d6456c489eaf37b56397ade6853`: `scan . --write` over the tree with the copies tracked, `#counts` only, `horos.py check .` prints `boundary matches the tree` exit 0. Elenchus on `4319ba9432a24d6456c489eaf37b56397ade6853` under the step's runner (`python3 plugins/horos/tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-1.json`): `unguarded -- the commit changed no test files`, the honest value for a regenerated artefact. | fixed in 4319ba9432a24d6456c489eaf37b56397ade6853 |

Leads not pursued: issue 1228 carried: inside the run worktree the root suite stays red on the two `test_agent_instruction_corpus` tests; in a detached clean snapshot of `4319ba9432a24d6456c489eaf37b56397ade6853` it is `Ran 1233 tests`, `OK`. The study's five discipline links resolve from the committed location. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22`, each printed `clean` and exited 0 over the changed paths; horos suite `Ran 239 tests`, `OK`; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the finding id with severity, file and status, every digest, count and quoted string, the exact verdict word, and the unpursued leads without changing any claim.

## Step 1, round 2 -- 2026-09-05T01:38:51Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=not-applicable; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=reviewed; ledger-row=not-applicable; no-execution=not-applicable

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. This round re-checked the tree with round 1's fix applied and ran inline by the controller; the only change since round 1 is the regeneration `4319ba9432a24d6456c489eaf37b56397ade6853` and the round 1 record.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: issue 1228 carried unchanged: in a detached clean snapshot of `40013792c2c551f68644bb4843a91b93daea362d` the root suite is `Ran 1233 tests`, `OK`; inside the run worktree it stays red on the two `test_agent_instruction_corpus` tests. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22`, each printed `clean` and exited 0 over the changed paths; horos suite `Ran 239 tests`, `OK`; `horos.py check .` prints `boundary matches the tree`, exit 0; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, and the unpursued leads without changing any claim.

## Step 2, round 1 -- 2026-09-05T02:39:19Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=reviewed; html-hidden-heading=reviewed; setext-under-lazy-line=reviewed; container-prefix=reviewed; front-matter-phantom=reviewed; unterminated-fence=reviewed; hostile-input=reviewed; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=not-applicable; ledger-row=not-applicable; no-execution=reviewed

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. CommonMark conformance was held against the study's oracle (markdown-it-py, CommonMark preset, `.hexaemeron/design-reports/oracle.json` over the 895 files of `corpus-list.txt`) and five hand cases under the same parser, not against the CommonMark spec suite. `census-regeneration`, `fixture-repin`, `prose-reconciliation` and `ledger-row` belong to steps 3 and 4 and nothing in this diff touches their surfaces. The diff `78ea5a4d..e1b1f748` changes no existing extractor: `git diff --stat` names only `languages/markdown/`, one `.md` line in `languages/__init__.py`, the two `map` help strings in `horos.py`, `GUIDE.md`, `test_md_outline.py`, `.horos/boundary.json#counts` and `tests/promise_machine_coverage.json`. The brief's `design_evidence` digest `c8879b32c9405b0d91ae6b5a0789059457a54df2b81e217b081c30357b451256`, study `588d3a5685bcb80cd8f2c6dc1089d55d52f263bc029e7532c0972f4fc8247009` and runbook `96e4f9f09e2599e647a3b43a4797d4e186c0dcc6d96d0d4e9a417d0d100b7818` match the files on disk; the shipped module is the study's `line-scanner` candidate byte for byte below its docstring.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S2-R1-01 | medium | plugins/horos/skills/horos/scripts/languages/markdown/markdown.py | `hostile-input`: one crafted line drives `map` quadratic, and the study's hostile set never held that shape. On the step head, `* ` x 20,000 then `x` (40,002 bytes) took 9.1 s, `# a` + 30,000 spaces + `b` 4.4 s and the same with a closing ` #` 4.4 s; the 2 MiB `* ` line did not finish in 300 s. Four mechanisms: the ATX regex's lazy `.*?` before `[ \t]*$` and the closing-hash regex each rescanned the tail per step; the thematic-break regex backtracked over its repeated group; the container-opening loop sliced the line once per list marker and re-checked the remainder for a thematic break each time; `_strip_containers` re-counted the indent per open item. Fixed: greedy ATX with `_atx_text`, `_thematic` answered from the line's uniform tail, position scanning with unanchored `QUOTE` and `LIST_ITEM`. After the fix the 2 MiB lines outline in 525 ms (markers then `x`), 162 ms (markers), 3 ms (ATX spaces), 2 ms (closing hashes), 262 ms (2 MiB of `>`), 344 ms (`- > ` mix); the eight hostile inputs of `resolve.py` run with zero exceptions at 0, 0, 0, 0, 1, 0, 94 and 75 ms (empty, cr-only, binary, unterminated-fence, one-long-line, deep-quotes, deep-list, many-blocks; deep-list was 4,753 ms on the step head, Mason recorded 5,186 ms). Guard: `test_a_long_line_of_markers_and_spaces_is_outlined_in_linear_time`, red on the parent at 9.1 s, 4.4 s and 4.4 s against a 2 s bound. | fixed in f6b11b058d0eb692283aa29ce104c821cccff821 |
| S2-R1-02 | low | plugins/horos/skills/horos/scripts/languages/markdown/markdown.py | `container-prefix`, `html-hidden-heading`, `setext-under-lazy-line`: three container-close rules diverged from the oracle, each losing a heading or a fence range, never inventing one. An HTML block of start kinds 1 to 5 inside a blockquote or list item did not close with its container, so `> <!--` then `# h` hid the heading and confessed lines 1-2 where the oracle reads an HTML block at line 1 and an h1 at line 2; a blank line inside a blockquote-held fence did not close the fence, so a blockquote-marked backtick fence, a blank line, a second blockquote-marked backtick fence and `# h` gave one fence 1-3 where the oracle reads fences 1-1 and 3-3; and `para[2]` stayed true after any lazy line, so `> Foo`, `bar`, `> ===` and `- foo`, `bar`, `  ---` outlined no heading where the oracle reads an h1 and an h2 at line 1. Fixed: one `closes` test (a container the line did not continue, where a blank line keeps an item and closes a blockquote) shared by the HTML and fence branches, and the lazy flag dropped; a lazy underline is still refused by `starts_block`. Guards: `test_a_container_closing_closes_the_html_block_or_fence_it_holds` and `test_an_underline_at_the_paragraph_depth_after_a_lazy_line_is_a_heading`, both red on the parent. The corpus differential is 0 missed, 0 extra, 0 fence_missed, 0 fence_extra, 0 crashes over 895 files before and after the fix. | fixed in f6b11b058d0eb692283aa29ce104c821cccff821 |

Leads not pursued: (1) Correction to the step 1 round 2 record above: it states that a clean detached snapshot of `40013792c2c551f68644bb4843a91b93daea362d` ran 1233 tests OK. It did not. That commit's boundary regeneration ran before its own audit log and synopsis were tracked; in a clean snapshot of it this round `horos.py check .` exits 1 with `drift: .horos/boundary.json#counts` (`files_walked` 2544 committed against 2546 fresh, `1 path(s) drifted`) and `test_boundary_currency.test_the_committed_boundary_matches_a_fresh_scan` and `test_agent_instruction.test_horos_boundary_is_current_for_the_scaffold` fail. The round 2 commit `78ea5a4d` regenerated with the audit files tracked; its clean snapshot, and the step head `e1b1f748`, are `Ran 1233 tests`, `OK` with `boundary matches the tree`. (2) `e1b1f748` re-pins `tests/promise_machine_coverage.json` `runtime.horos-boundary-scan.sha256` to `09edab05d87777db3457e6d9a687be0e06ad1d5e8e06e05240dc81e4b6c9ace3`, a file outside the step's Files field: accepted, the register binds `horos.py` by whole-file digest and the help-string edit the Files field names moved it; without the re-pin the root suite the exit requires is red. (3) Elenchus under the host's default `TMPDIR` (`/var/folders/...`, resolving through `/private/var`) returned `inconclusive -- the runner did not create its report` because `run_tests.py` resolves its worktree root strictly and refuses the unresolved report path with `REPORT must stay inside the current worktree`; with `TMPDIR` set to the scratchpad (already a `/private` path) the same command on the same commit returned `guarded -- the runner report records a parent assertion failure` (executed 258, assertion_failures 6, errors 0). The recorded verdict is that second run; the runner's path check is outside this step's Files and is not re-found here. (4) `_strip_containers` is still O(open containers) per line; a 4,000-deep list outlines in 343 ms, so no bound is added. (5) Issue 1228 carried: inside the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `test_agent_instruction_corpus.test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`; in clean detached snapshots of `e1b1f748` and of `f6b11b058d0eb692283aa29ce104c821cccff821` it is `Ran 1233 tests`, `OK`. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22` each printed `clean` and exited 0 over the changed paths on the step head and again on the fix; horos suite `Ran 255 tests`, `OK` on the step head and `Ran 258 tests`, `OK` on the fix, in the worktree and in the clean snapshot; `horos.py check .` prints `boundary matches the tree` on both; `scan . --write` after the fix left `git status --short .horos/` empty; `map plugins/horos/skills/horos/SKILL.md` exits 0 with `module: Horos`, `front matter: lines 1-6`, `declarations: 16` and `unparsed: 1 region(s): lines 8-10` before and after; the pinned `GUIDE.md` outline is unchanged; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, both finding ids with severity, file and status, every digest, count, timing and quoted string, the exact verdict word, and the five unpursued leads without changing any claim.

## Step 2, round 2 -- 2026-09-05T07:12:02Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=reviewed; html-hidden-heading=reviewed; setext-under-lazy-line=reviewed; container-prefix=reviewed; front-matter-phantom=reviewed; unterminated-fence=reviewed; hostile-input=reviewed; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=not-applicable; ledger-row=not-applicable; no-execution=reviewed

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. CommonMark conformance was held against the study's oracle (markdown-it-py, CommonMark preset) over the 895 corpus files and 56 hand cases under the same parser, not against the CommonMark spec suite. `census-regeneration`, `fixture-repin`, `prose-reconciliation` and `ledger-row` belong to steps 3 and 4; the diff since round 1 (`e1b1f748..3a84ee06`) touches only `languages/markdown/markdown.py`, `test_md_outline.py`, this log and its synopsis, and `git diff --stat 78ea5a4d..3a84ee06 -- plugins/horos/skills/horos/scripts` names no file outside `languages/markdown/`, the `.md` registry line and the two `map` help strings. This round audited the fixed tree at `3a84ee0623e4b010aed76d636b11afaa7a252231` (fix `f6b11b058d0eb692283aa29ce104c821cccff821`, then the round 1 record) as a new Warden.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: (1) The round 1 fixes hold. Crafted lines from S2-R1-01 against the shipped module, each under a 10 s alarm: `* ` x 20,000 then `x` 9 ms; `# a` + 30,000 spaces + `b` 0 ms; the same with a closing ` #` 0 ms; 2 MiB of `* ` 125 ms; 2 MiB of `* ` then `x` 407 ms; 2 MiB of `>` 241 ms; 2 MiB of `- > ` 271 ms. The eight `resolve.py` hostile inputs: 0, 0, 0, 0, 1, 0, 63 and 52 ms (empty, cr-only, binary, unterminated-fence, one-long-line, deep-quotes, deep-list, many-blocks), zero exceptions. `resolve.py` loads only `candidates/`, so the differential was re-run by a scratch script importing the shipped `markdown.py` with the resolver's exact comparison: 0 missed, 0 extra, 0 fence_missed, 0 fence_extra, 0 crashes over 895 files. 56 hand cases covering the rewritten rules (`_atx_text` closing hashes, `_thematic` under list markers and indents, container-held fences and HTML blocks, setext after lazy lines, empty and code-indented items) all agree with markdown-it. (2) The three round 1 guards still fail against the pre-fix module `e1b1f748` in a scratch copy: `Ran 3 tests`, `FAILED (failures=6)` (10.4 s, 4.9 s and 4.9 s against the 2 s bound; `[] != ['# h  (line 2)']`; both setext cases), and pass on the fixed tree. (3) `_strip_containers` stays O(open containers) per line, carried from round 1 lead (4); `_uniform_start` and `_thematic` are each one pass per line. (4) Issue 1228 carried: in the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `test_agent_instruction_corpus.test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`; in a clean detached snapshot of `3a84ee06` it is `Ran 1233 tests`, `OK`, horos `Ran 258 tests`, `OK`, `horos.py check .` `boundary matches the tree` exit 0. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22` each printed `clean` and exited 0 over `languages/markdown`, `test_md_outline.py`, `languages/__init__.py`, `horos.py` and `fixture-md`; horos suite `Ran 258 tests`, `OK` in the worktree; `horos.py check .` `boundary matches the tree` exit 0; `map plugins/horos/skills/horos/SKILL.md` exit 0 with `module: Horos`, `front matter: lines 1-6`, `declarations: 16`, `unparsed: 1 region(s): lines 8-10`; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. `f6b11b05` and `3a84ee06` verify `G` under key `B83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. No fix commit this round, so no Elenchus run. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count, timing and quoted string, and the four unpursued leads without changing any claim.

## Step 3, round 1 -- 2026-09-05T07:39:39Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=reviewed; html-hidden-heading=reviewed; setext-under-lazy-line=reviewed; container-prefix=reviewed; front-matter-phantom=reviewed; unterminated-fence=reviewed; hostile-input=reviewed; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=not-applicable; ledger-row=not-applicable; no-execution=reviewed

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. CommonMark conformance is held against markdown-it-py 4.2.0 in its `commonmark` preset, not against the CommonMark spec suite. The five container and fence rules of `fence-shadowed-heading`, `html-hidden-heading`, `setext-under-lazy-line` and `container-prefix` were reviewed only through this step's corpus evidence, which is a whole-corpus agreement result and not a case-by-case re-derivation; their synthetic cases belong to step 2 and this diff changes no extractor. `hostile-input` was reviewed as untouched: the `resolve.py` hostile set and its guards are outside `git diff 0696638c..987be4fa`, whose only contribution is 0 crashes over 900 real files. `census-regeneration`, `fixture-repin`, `prose-reconciliation` and `ledger-row` belong to the reconcile step and step 4; the bundle itself says the census is regenerated there and not here. The diff is the single commit `987be4fa` over five paths -- `plugins/horos/dev/md_oracle.py`, `plugins/horos/docs/evidence/skills-markdown-outline.md`, `skills-markdown-outline.results.json`, `plugins/horos/tests/test_evidence.py` and `.horos/boundary.json#counts` (`files_walked` 2550 to 2553) -- and touches no extractor, no other test module and no skill file. The brief's `design_evidence` digest `c8879b32c9405b0d91ae6b5a0789059457a54df2b81e217b081c30357b451256`, study `588d3a5685bcb80cd8f2c6dc1089d55d52f263bc029e7532c0972f4fc8247009` and runbook `96e4f9f09e2599e647a3b43a4797d4e186c0dcc6d96d0d4e9a417d0d100b7818` are the values carried in this packet.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S3-R1-01 | low | plugins/horos/docs/evidence/skills-markdown-outline.md | `declared-altitudes`: the bundle's re-measured corpus outline time does not reproduce. It quoted 216 ms as 170 ms, the median of three runs given as (167, 173, 170), against the 1,000 ms budget of study section 10. Running the bundle's own declared method -- every corpus file read and decoded once up front, three passes calling `declarations()` on each decoded source in process, median of the three `time.perf_counter` wall times, the shipped `languages/markdown/markdown.py` substituted for the candidate -- over the same 900 tracked `.md` files at `0696638c` gives 215, 216, 214 ms, median 216, and nine further invocations on a quiet machine ranged 215 to 218; the same method over the study's 895-file `corpus-list.txt` gives 208 to 215. The machine is ruled out by a control: the study's own `line-scanner` candidate under `.hexaemeron/design-reports/candidates/`, measured by the same method over the same 895-file list, gives 332, 334 and 332 ms against its committed `line-scanner-corpus-outline-ms.json` value of 319, so this host is 4 percent slower than the one that produced the study's figures, not 27 percent faster. The shipped-to-candidate ratio is 0.64 here against the bundle's implied 0.53. The budget claim is unaffected either way: 216 ms is 21.6 percent of 1,000 ms. Fixed: the figure and its three runs corrected to the re-measured values, and the candidate control recorded in the same paragraph so a later reader can repeat the comparison. `imprimatur.py` on the corrected bundle exits 0 at score 100.0 with 0 defects; the two capture-line tests are untouched because the timing is prose and not an `mdoutline` capture line. | fixed in be2e8fbdf37661eb5254686229cf158082e0b468 |

Leads not pursued: (1) The differential was reproduced rather than read. A detached worktree at `0696638c` with the committed `plugins/horos/dev/md_oracle.py` copied in, run under the study's virtualenv (Python 3.14.6, markdown-it-py 4.2.0, mdurl 0.1.2), exits 0 with `mismatching files: 0` and totals identical to the committed `skills-markdown-outline.results.json` in every key except `elapsed_ms` (2,326 mine against 2,047 committed): 900 files, 15,896,939 bytes, 0 crashes, 9,527 headings oracle, 9,527 matched, 0 missed, 0 missed_confessed, 0 extra, 1,130 fences oracle, 1,130 fence_matched, 0 fence_missed, 0 fence_missed_confessed, 0 fence_extra, 576 regions, 190 files_with_regions. All 900 per-file rows compare equal. The bundle's sixteen `mdoutline` capture lines equal those totals exactly. (2) The two declared exclusions are the exclusions implemented. `md_oracle.split_front_matter` and `_Outline.run` both take a line-1 `---` after `rstrip("\r")`, close on the next `---` or `...`, and leave an unterminated opener unstripped; the oracle shifts its line numbers back by the stripped count and the outliner starts scanning after the block, so front matter is outlined on one side and compared on neither. No exclusion widens `matched`, which is `|oracle intersect ours|` and never consults the confessed regions; regions can only move a miss from `missed` to `missed_confessed`, and both are zero here, so no exclusion did any work in this corpus. The bundle's claim that the corpus holds no unterminated fence is entailed by `fence_matched` equal to `fence_oracle` at 1,130 with `fence_extra` 0: an unterminated fence would appear in the oracle set running to end of file and in neither of the other two. (3) `md_oracle.compare` confesses a fence when `inside(f[0]) or inside(f[1])`, where `resolve.py:differential` uses `inside(f[0])` alone. The wider rule could confess a fence the narrower one counts as missed. Not pursued: both counts are zero over this corpus, so no committed number depends on the difference, and `resolve.py` is under `.hexaemeron/` and outside this step's Files. (4) `test_the_markdown_outline_bundle_matches_its_committed_results` checks the `mdoutline:commit` line only against `^[0-9a-f]{40}$`. Not pursued: the results document records no commit field, so there is nothing in the tree to compare it against. (5) The corpus commit `0696638c` is the step's entry head and the parent of `987be4fa`, so the bundle's own `.md` and `.results.json` are outside the corpus it reports on: 900 tracked `.md` files at `0696638c` against 901 at `987be4fa`. The bundle names that commit in prose and in the `mdoutline:commit` capture line and claims nothing about the step head, and `md_oracle.run` refuses to start when `git rev-parse HEAD` is not the named commit, so the reading is stated rather than hidden; that the two bundle files are self-excluded is left implicit and no correction is made. (6) `md_oracle.py` passes `--commit` into `git ls-files --with-tree`, where a value beginning with a dash would otherwise be read as an option, but `run()` refuses unless that value equals `git rev-parse HEAD` before `corpus()` is reached, so the argument is a resolved commit id by the time it becomes a Git argument. Corpus files are read with `read_bytes()` and decoded `errors="replace"`, never imported, executed or written, and `markdown_it` appears in no path under `plugins/horos/skills` or `plugins/horos/tests`; `md_oracle` is named only by documentation. (7) Issue 1228 carried: in the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `test_agent_instruction_corpus.test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`; in a clean detached snapshot of `987be4fa` it is `Ran 1233 tests`, `OK`. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22` each printed `clean` and exited 0 over the changed paths, and `hypomnema.py` and `imprimatur.py` again after the fix; horos suite `Ran 258 tests`, `OK` at `0696638c` and `Ran 260 tests`, `OK` at `987be4fa` and after the fix, the growth of exactly two the runbook required; both new tests were observed red against a mutated copy of the results document in a scratch tree (`files` 899 gives `900 != 899 : files`, `missed` 3 gives `3 != 0`), so neither is vacuous; `horos.py check .` prints `boundary matches the tree` exit 0 and `scan . --write` left `git status --short .horos` empty before and after the fix; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The fix commit `be2e8fbdf37661eb5254686229cf158082e0b468` verifies `G` under `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, both required trailers. Elenchus ran against that commit with `TMPDIR` set to the scratchpad and returned `unguarded -- the commit changed no test files`, which is the expected reading for a corrected prose figure and is recorded, not relabelled. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the one finding id with its severity, file and status, every digest, count, timing and quoted string, the exact verdict word, and the seven unpursued leads without changing any claim.

## Step 3, round 2 -- 2026-09-05T07:48:18Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=not-applicable; prose-reconciliation=not-applicable; ledger-row=not-applicable; no-execution=not-applicable

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver. The differential itself was not re-run this round: round 1 reproduced all 900 per-file rows and every total from the committed `md_oracle.py`, and the fix `be2e8fbd` changed one paragraph of the bundle and no input to it. The eight outliner ids stay not-applicable because the step changes no rule; `census-regeneration`, `fixture-repin`, `prose-reconciliation` and `ledger-row` are step 4's. This round ran inline by the controller, because the only change since round 1 is the prose fix and the round 1 record.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: issue 1228 carried unchanged: in a detached clean snapshot of `0f3185c48e07a4d3e2ab782c911b94091b5a0f61` the root suite is `Ran 1233 tests`, `OK`; inside the run worktree it stays red on the two `test_agent_instruction_corpus` tests. Round 1's timing finding is closed as fixed and the corrected figure (216 ms median, with the candidate control at 332 ms against its recorded 319 ms) was not re-measured here; a third measurement would not settle a figure two independent runs already agree on. Evidence for this round: `phylax.py`, `ephoros.py` and `hypomnema.py` from plugin root `hexaemeron/1.6.22`, each printed `clean` and exited 0 over the four changed paths; horos suite `Ran 260 tests`, `OK`; `horos.py check .` prints `boundary matches the tree`, exit 0; every unittest command carried `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, every named unchecked item, the exact zero-finding row, every digest, count and quoted string, and the unpursued leads without changing any claim.

## Step 4, round 1 -- 2026-09-05T08:27:41Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=reviewed; boundary-regeneration=reviewed; census-regeneration=reviewed; fixture-repin=reviewed; prose-reconciliation=reviewed; ledger-row=reviewed; no-execution=reviewed

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver, so the three bundled lints plus the repository suites are the mechanical part. The corpus differential was not re-run: step 3 round 1 reproduced all 900 per-file rows and every total, and step 4 changes no extractor rule, so the seven outliner and hostile-input ids stay not-applicable. `.horos/census.json` was not regenerated by this round's fix: the runbook regenerates it once, at `f11de00a`, and nothing holds the repository census to a fresh scan (`tests/test_census.py` binds only the fixture census), so the evidence bundle's quotation of the committed `.md` row stays exact across this round's commits.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | low | plugins/horos/.codex-plugin/plugin.json | `prose-reconciliation`: the Codex `interface.longDescription` carried two claims the tree had already falsified. It said `It also emits skeleton maps for Python, TypeScript, Go, C++, and Solidity`, omitting the `.md` entry the registry in `plugins/horos/skills/horos/scripts/languages/__init__.py` has held since step 2, and it ended `and Markdown has no outline extractor`, which is flatly false at this head. The register's criterion is that every mutable prose surface names Markdown among the mapped languages and none says it has no extractor; the runbook's Files contract lists this field as ask-first, and the controller answered the ask for this round. Nothing bound it: `plugins/horos/.claude-plugin/plugin.json` carries no `longDescription`, and `tests/test_marketplace_prose.py` asserts `longDescription` content for the hexaemeron and sapheneia plugins only. Fixed by adding `Markdown` to the language list and replacing the last clause with `and a Markdown map is its headings and fenced code blocks`. The cross-host-bound `description` and `interface.shortDescription`, which `assert_host_descriptions_agree` holds equal and which name no language, are unchanged; `tests.test_marketplace_prose` is `Ran 23 tests`, `OK` after the fix. | fixed in fd836f3f0b88151878363a55af6b4aa9660431d5 |
| S4-R1-02 | low | plugins/horos/skills/horos/scripts/horos.py | The module docstring's usage line read `map <file.py>  print the file's skeleton instead of the file`, naming a Python-only verb. It has been wrong for every language added since, and step 2's `.md` registration is the newest case. Only the docstring was stale: `map`'s argparse help and its `file` argument already interpolate `languages.supported()` and the refusal message names the supported list, so every surface a caller reaches at runtime was correct. Fixed to `map <file>` at the same 15-column alignment as the `scan` and `check` lines, with no other byte moved and no code path touched. The edit moves the file's whole-file digest, which `tests/promise_machine_coverage.json` binds at `runtime.horos-boundary-scan.sha256`; that pin, which occurred exactly once in the file, is re-pinned in the same commit from `09edab05d87777db3457e6d9a687be0e06ad1d5e8e06e05240dc81e4b6c9ace3` to `66f9f90d3b0791674949b2004250e19a588e4d94243ed91ab2befca79fdf4a8a`, exactly as step 2 did, and `python3 scripts/promise_machine.py coverage` prints `clean: promises=97 coverage_rows=97 coverage_selected=97` at exit 0 afterwards. | fixed in fd836f3f0b88151878363a55af6b4aa9660431d5 |

Leads not pursued: the `SKILL.md` frontmatter `description` still reads `print Python skeleton maps for oriented reading`, naming one of the six mapped languages. Left for a maintainer, for three reasons: the runbook's Files contract holds it ask-first and unchanged, `assert_host_descriptions_agree` binds it to the Codex `description` and `shortDescription` so it cannot move on one host alone, and editing `SKILL.md` before byte 12403 would move the reviewed span and force a second fixture re-pin inside an audit round. Deliberately left alone as pinned historical records quoting the held job's original wording: the `horos-v9.2.3` epoch row in `plugins/horos/skills/horos/EVOLUTION.md`, lines 104 and 113 of `plugins/horos/docs/evidence/skills-markdown-outline.md`, and the past delivery documents under `plugins/horos/docs/content-addressed-objects/`, `.../marker-self-exclusion/` and `.../scoped-entry/`. Issue 1228 carried unchanged: inside the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `test_agent_instruction_corpus` (`test_selftest_candidates_match_the_manifest` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`), both because the live `.hexaemeron/design-evidence.json` candidate set is this run's own rather than the prover's four; in a detached clean snapshot of `f11de00a` the same suite is `Ran 1233 tests`, `OK`. The carried counts, stated as the step's exit requires: `tests/fixtures/agent-instruction-v1/evidence/measurement.json` and `.../parity.json` were re-pinned, not re-measured. Compared leaf by leaf against `git show 11a7e76d:<path>`, both keep an identical key set (163 and 522 leaves). Twelve leaves moved in `measurement.json` and 69 in `parity.json`, every one a derived identity: `corpus_sha256`, `correlation_id`, `compact.job_id`, `source.job_id`, `compact.input_sha256`, `compact.prompt_sha256`, `documents[1].canonical_model.sha256` and `documents[1].compact.sha256`. All 19 `tokens` leaves in `measurement.json` and all 36 `prompt_tokens` and 36 `response` leaves in `parity.json` are byte-identical to the parent. That is honest because the measured quantity did not change: the re-pin moved the `SKILL.md` reviewed span from bytes 11633-12651 to 12403-13421 without altering one byte inside it (`span_sha256` `0f2fb54b8f737de4b8ec42b88bdc6d9238f65de8525847c81381cbb83d0e91a2`, recomputed here from the file and occurring exactly once in the manifest), so the prompt the model saw and the reply it gave are the same artefacts under new addresses. No model ran in this step, and the record states them as a carried observation, not a fresh measurement. Evidence for this round. Lints from plugin root `hexaemeron/1.6.22` over the fourteen changed paths, each printing `clean`: `phylax.py` exit 0, `ephoros.py` exit 0, `hypomnema.py` exit 0; re-run over the three fix paths, all three `clean` at exit 0. Suites, each carrying `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`: `tests.test_evolution_contract` `Ran 10 tests`, `OK`, including `test_mature_frontiers_have_no_next_job`; `tests.test_marketplace_prose` `Ran 23 tests`, `OK`; `discover -s plugins/horos/tests -t plugins/horos` `Ran 260 tests`, `OK`; `tests.test_boundary_currency` `Ran 8 tests`, `OK`. `ledger-row`: the digest recipe was validated first against the parent header, whose four fields hash to `f0da2b17da1d83eca42aace7a81216542e2f51bd17c189b48dfb9712a07ec925`, the committed `horos-v11.3.3` row; the same recipe over the new header gives `7d19b7476565d3eb54bc2adb0118f1fde2d86f751819c6c5e43ba77ced7b1556`, which is the `horos-v12.3.3` row. Axis: evolution 11 to 12, generation 3 and epoch 3 retained; `Frontier status: mature` with `Next Fiat job: None -- mature`; `SKILL.md` frontmatter `version: "12.3.3"`. The maturity evidence recomputes from the regenerated census: against 44,315,347 readable bytes (96,175,040 walked less 51,859,693 boundary bytes) the shares are `.md` 35.95%, `.json` 29.13%, `.py` 24.60%, `.jsonl` 7.85% and `.sol` 1.18%, and the next filetype is `(no suffix)` at 0.51%, then `.js` 0.41% over 13 files and `.yml` 0.15% -- so the five above 1% are exactly the three mapped and the two record formats the row names, and every figure it quotes rounds to the census. The bundle's quoted `.md` row also recomputes: 901 files, 15,931,591 bytes, 16.57% of walked bytes, 1,485 boundary bytes. The row's `900 tracked .md files` is the corpus at `0696638c`, which the bundle pins, not this head's 901. `prose-reconciliation`: the `**Current frontier.**` sentence is byte-identical across the ledger header, `SKILL.md`, `plugins/horos/README.md` and `plugins/horos/AGENTS.md` (457 characters, SHA-256 prefix `fe6a0ca6ab383ddb`). `fixture-repin`: exactly seven files moved and no others; `python3 scripts/agent_instruction.py check --manifest tests/fixtures/agent-instruction-v1/manifest.json` exits 0 with a `run.summary` recording `"outcome":"accepted"`, `"refused":0`, `"failed":0`, `"passed":20`. `boundary-regeneration` and `census-regeneration`: `horos.py check .` prints `boundary matches the tree` at exit 0 both at `f11de00a` and after this round's fix; the regeneration ran in the prescribed order -- add the changed sources, `scan . --write`, add `.horos` -- and produced no change, so `fd836f3f` carries no `.horos` path, the three fixed files sitting inside the boundary's readable set with none of them a classified entry. `no-execution`: the `horos.py` edit is inside the module docstring and moves no execution path, and `phylax.py` over the file is `clean` at exit 0. Elenchus on `fd836f3f0b88151878363a55af6b4aa9660431d5` under the step's runner (`python3 tests/run_tests.py --elenchus-report {report}`, `unittest-json-v1`, `.hexaemeron/elenchus-step-4.json`) returns `guarded`, because the re-pin of `tests/promise_machine_coverage.json` makes the parent fail the coverage assertion the fix answers. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, both findings with their severities, files and statuses, every digest, count, path, exit code and quoted string, the carried-counts statement, every named unchecked item and every unpursued lead, changing no claim.

## Step 4, round 2 -- 2026-09-05T09:12:04Z

Audit schema: fiat-audit-round/v2

Covered: fence-shadowed-heading=not-applicable; html-hidden-heading=not-applicable; setext-under-lazy-line=not-applicable; container-prefix=not-applicable; front-matter-phantom=not-applicable; unterminated-fence=not-applicable; hostile-input=not-applicable; declared-altitudes=not-applicable; boundary-regeneration=reviewed; census-regeneration=not-applicable; fixture-repin=reviewed; prose-reconciliation=reviewed; ledger-row=not-applicable; no-execution=reviewed

Not checked: CI (`.github/workflows/repo.yml` and `tests/check-map-v1.json` neither read nor run). X-Ray, solidity-auditor and fizz: not run, the `security_suite` receipt is a waiver, so the three bundled lints plus the repository suites are the mechanical part. The corpus differential was not re-run: step 3 round 1 reproduced all 900 per-file rows and every total, and no commit in `f11de00a..0287070d` touches an extractor rule, so the seven outliner ids and `hostile-input` stay not-applicable. `declared-altitudes`, `census-regeneration` and `ledger-row` are not-applicable against this round's file list, which carries no evidence bundle, no `.horos/census.json` and no `EVOLUTION.md`. The evidence records were re-pinned, not re-measured, and no model ran.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R2-01 | low | plugins/horos/skills/horos/SKILL.md | `prose-reconciliation`: the frontmatter `description`, which is the sentence every agent reads in its skill list, said `print Python skeleton maps for oriented reading` while `scripts/languages/__init__.py` maps six languages. Round 1 left it on three grounds and the second of them is wrong: `assert_host_descriptions_agree`, read here at `repo_contract.py` lines 71 to 98, compares the marketplace entry, `.claude-plugin/plugin.json` `description`, `.codex-plugin/plugin.json` `description`, the Codex `interface.shortDescription` and any `agents/openai.yaml` short description, and never reads `SKILL.md`; the runbook's 2026-09-05 amendment replaces the Files clause and puts the field in the step. Rewritten to `print skeleton maps of Python, TypeScript, Go, C++, Solidity and Markdown for oriented reading`, taking the description from 442 to 489 characters with `version` and every other frontmatter byte unmoved. The old clause is bound nowhere else: its only other live occurrences in the tree are this file's round 1 record and its synopsis, which are append-only history, and no test reads the frontmatter `description`. The edit precedes the reviewed span, so the seven-file chain is re-pinned in the same commit with `repin_fixture.py` at pre-edit ref `dfc90b38` and then `repin_evidence.py`: the span moves from bytes 12403-13421 to 12450-13468, `span_sha256` stays `0f2fb54b8f737de4b8ec42b88bdc6d9238f65de8525847c81381cbb83d0e91a2` and occurs exactly once in the file and once in the manifest, and exactly eight files move, the seven plus `SKILL.md`. `python3 scripts/agent_instruction.py check --manifest tests/fixtures/agent-instruction-v1/manifest.json` exits 0 with `"outcome":"accepted"`, `"refused":0`, `"failed":0`, `"passed":20`; `python3 scripts/promise_machine.py coverage` prints `clean: promises=97 coverage_rows=97 coverage_selected=97` at exit 0; `imprimatur.py` over the edited file scores 100.0/100 with 0 defects at exit 0. | fixed in 0287070da901c127707c373da6cf076f93606663 |

Leads not pursued: the round 1 fixes were re-checked at this head and both hold. The Codex `interface.longDescription` names `Python, TypeScript, Go, C++, Solidity, and Markdown` and ends `and a Markdown map is its headings and fenced code blocks`, with no denial of the extractor; `horos.py`'s docstring usage line reads `map <file>` and the file's SHA-256 is `66f9f90d3b0791674949b2004250e19a588e4d94243ed91ab2befca79fdf4a8a`, which is exactly the `runtime.horos-boundary-scan.sha256` pin and occurs once in the coverage register. The carried counts, as the step's exit requires: `measurement.json` and `parity.json` were re-pinned, not re-measured. Compared leaf by leaf against `git show dfc90b38:<path>`, the key sets are identical at 163 and 522 leaves; 12 leaves moved in `measurement.json` and 69 in `parity.json`, every one a derived identity (`corpus_sha256`, `correlation_id`, `sha256`, `job_id`, `input_sha256`, `prompt_sha256`). All 19 `tokens` leaves in `measurement.json` and all 36 `prompt_tokens` and 36 `response` leaves in `parity.json` are byte-identical to the parent. That is honest because the measured quantity did not change: the re-pin moved the span's address without altering one byte inside it, so the prompt the model saw and the reply it gave are the same artefacts under a new offset. The Elenchus verdict is `inconclusive` rather than round 1's `guarded`, and the mechanism was reproduced rather than assumed: Elenchus overlays only the fix's changed test files onto the parent, so the parent keeps the pre-edit `SKILL.md` while gaining the re-pinned manifest, and `tests/test_agent_instruction_corpus.py` line 108 raises `ProverError: plugins/horos/skills/horos/SKILL.md is off its manifest digest` in `setUpClass`. That is an infrastructure error beside the assertion failures, and Elenchus classifies a mixed report as inconclusive by rule. The verdict is recorded as it was returned and not translated; it is evidence about this runner contract, not about the fix. Issue 1228 carried, with one name changed: inside the run worktree the root suite is `Ran 1233 tests`, `FAILED (failures=2)` on `tests.test_agent_instruction_corpus.AgentInstructionCorpusTests`, this round in `test_a_candidate_outside_the_design_record_is_refused` and `test_prover_selftest_exits_zero_and_writes_a_closed_report`, both comparing `candidate_choices(ROOT)` against the prover's fallback set while the live `.hexaemeron/design-evidence.json` holds this run's candidates; in a detached clean snapshot of `0287070d` the same suite is `Ran 1233 tests`, `OK`. Evidence for this round. Lints from plugin root `hexaemeron/1.6.22` over the eight changed paths, each printing `clean`: `phylax.py` exit 0, `ephoros.py` exit 0, `hypomnema.py` exit 0. Suites, each carrying `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false`: `tests.test_marketplace_prose` `Ran 23 tests`, `OK`; `tests.test_evolution_contract` `Ran 10 tests`, `OK`; `discover -s plugins/horos/tests -t plugins/horos` `Ran 260 tests`, `OK`. `boundary-regeneration`: `horos.py check .` prints `boundary matches the tree` at exit 0, and the regeneration ran in the prescribed order, add the changed sources, `scan . --write`, add `.horos`, producing no movement, so `0287070d` carries no `.horos` path; the one `.horos/candidates.json` change in `f11de00a..0287070d` is `dfc90b38` regenerating for the grown round 1 synopsis. `no-execution`: the edit is one frontmatter line and moves no execution path, and `phylax.py` over the file is `clean` at exit 0. The bounded Sapheneia pass preserved the heading, the four schema fields, all fourteen risk ids and dispositions, the finding with its severity, file and status, every digest, offset, count, exit code and quoted string, the carried-counts statement, every named unchecked item and every unpursued lead, changing no claim; it cut this round's leads to the evidence itself after round 1's ran to roughly 1,400 words.
