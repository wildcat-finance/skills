## Step 1, round 1 -- 2026-08-30T07:00:29Z

Audit schema: fiat-audit-round/v2

Covered: currency-as-validity=reviewed; recompute-invariance=reviewed; publication-commit-identity=reviewed; git-argv=reviewed; path-report-rendering=reviewed; pipefail-masking=reviewed; workflow-silence=reviewed; schema-stability=reviewed; suppression-path=reviewed; coverage-binding-drift=reviewed; receipted-artefact-immutability=reviewed; self-hosting-order=reviewed

Not checked: the security suite is waived and recorded, so `x-ray`, `solidity-auditor` and `fizz` did not run and this round makes no Solidity claim; the diff is one Python function, its tests, a workflow and prose. Refusals 7 and 8 were not reached from a constructed history. A merge whose record blob disagrees with the commit `git rev-list -1` returns could not be built, because default history simplification follows a parent that is TREESAME for the path, and `read_commit_regular` has already established the blob at the checkout before the discovery runs; both were driven only through the step's own `run_git` substitution, which replaces the discovery and leaves the refusal under test in production code. The workflow was parsed by Ruby's YAML and its summary block was extracted and executed against a synthetic capture on this host; no GitHub runner rendered it, so `$RUNNER_TEMP`, `$GITHUB_STEP_SUMMARY` and the rendered markdown were not observed where they run. Whether this branch is pushed before any other is Fiat's to hold, so this round establishes that the branch carries the changed command and not that CI ran it.

Elenchus verdict: guarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | `scripts/dead_code.py:4183` | `baseline --check` reads `.dead-code/suppressions.json` only at the recorded source commit, and dropping the source-to-checkout path comparison leaves nothing reading that file at the checkout. On the synthetic fixture repository, deleting `.dead-code/suppressions.json` and committing an entry for a finding that does not exist each leave the command at exit 0 with `status matched` and the file named on the `currency` line, while the same entry at the source commit still refuses with `.dead-code/suppressions.json entry 0 is unused; finding sha256:0000... is absent`. `command_report` never calls `load_suppressions`, so no other command in the workflow reads it either. Study item 4.11 and runbook item 2.11 both scope the refusal to the source commit, so this is the written contract; what is lost is the transitive cover the removed refusal gave by accident, and the `transition` sentence in `tests/promise_machine_coverage.json`, "with only exact current suppressions accepted", now overstates what the command establishes. Closing it needs a second recompute at the checkout, which the 46-second budget in exit item 9 does not hold. | recorded, not fixed |
| S1-R1-02 | low | `.github/workflows/dead-code.yml:97` | The step summary filtered the captured output to `currency` and `status`, so the `published` line naming the commit that wrote the checked-out record reached no reader. Study item 8's second on-call question sends a reader to the summary for how far behind the record is, and study item 8, ADR-054's Consequences and the operator guide's recovery entry all describe that commit as sitting on the `currency` line, where `_baseline_summary` puts it on its own `published` line. The filter now names `published`, `currency` and `status`, and ADR-054 and `docs/promise-machine/dead-code-v1.md` name the line each value sits on. The receipted study keeps its wording, because exit item 10 proves its tracked copy byte-identical. | fixed and guarded in `8b6631be7946c4e0a93b19d51621488d0b8b8532` |
| S1-R1-03 | informational | `.github/workflows/dead-code.yml:97` | The captured lines were interpolated into a markdown list item, so a tracked path carrying a backtick, an angle bracket, a vertical bar, an underscore or an asterisk reached the GitHub step summary as markup rather than as the path the command named. Driven on the fixture repository with six such names, each of which reached the currency line unchanged. `validate_repository_path` rejects NUL, control characters, backslashes and absolute paths, so no line break, directive or shell string was reachable and the exposure is a misread path list rather than an injection; the `path-report-rendering` control nonetheless states that paths are printed without markdown interpretation. The summary now reproduces the captured lines verbatim inside a fenced block. | fixed and guarded in `8b6631be7946c4e0a93b19d51621488d0b8b8532` |
| S1-R1-04 | informational | `docs/dead-code-baseline-currency/study.md:103` | Study item 4 and runbook item 2 present the eleven refusals "in the order it evaluates" and place `compare_baseline_documents` tenth and `load_suppressions` eleventh. `command_baseline` calls `load_suppressions` at `scripts/dead_code.py:4183` and `compare_baseline_documents` at `:4190`. A forged record published over a source commit whose suppressions name an absent finding refuses on `.dead-code/suppressions.json entry 0 is unused` rather than on the drift it also carries, so the eleventh evaluates before the tenth. The order predates this diff, and refusal 11 is unreachable for any record `baseline --write` produced, because the write ran the same call at the same commit and returned. Not fixed: exit item 10 proves both tracked copies byte-identical to the receipted `.hexaemeron/` sources with `cmp -s`, so the wording cannot be corrected in place. | recorded, not fixable in this run |

Leads not pursued: all eleven refusals were driven rather than read, against the step branch at `7965e6c3829a8356abe7da0f5eaef796f63513fe`. Refusals 1, 2 as both an absent and a symlinked record, 3, 4, 5 and 11 were exercised by probes on the synthetic fixture repository and each named its own message; 6, 7, 8, 9, 10 and the reported exit-0 case are covered by the step's replacement tests, which pass. `source_commit` reaches `git diff` and `git merge-base` as data from a committed file, and `validate_baseline_document` puts it through `_require_oid` at line 3619 before `require_baseline_publication` runs, so no record can shape a git argument. `_changed_paths` refuses when either diff exceeds `MAX_GIT_OUTPUT_BYTES`, so a repository 16 MiB of paths past its publication commit turns a currency question into a hard refusal; the same ceiling applied to the single diff before the change, so it is not a regression. `_require_oid` demands 40 lowercase hexadecimal characters, so the command refuses in a SHA-256 object-format repository; that constraint is shared with `resolve_commit` and `resolve_tree` and predates this step. The summary step still prints a fixed `- changes: added=0 resolved=0 stale_suppressions=0` while `_baseline_summary` emits a `changes` line of its own; `workflow-silence` names the status line alone, so this sits outside the register and outside this round. The three mapped lints exit 0, and `phylax` and `ephoros` were additionally run over `scripts`, which no mapped lint reads, and are clean there too. `python3 scripts/dead_code.py baseline --check` exits 0 on the fixes commit in 26.6 seconds against the study's 43.6-second before-measurement and 46-second budget, printing `published 41bea02dd9ea4cfe2ba32342f4e08de62b50ce0e` and `currency  stale; 9 path(s) changed after publication`, which is exit item 8 and the acceptance demonstration. The affected-scope closure selected seven checks, of which `dead-code-suite`, `root-suite`, `imprimatur-suite`, `lint-phylax`, `lint-ephoros` and `lint-hypomnema` passed and `hexaemeron-suite` failed; that suite fails on clean `main` at `7e97b5195d5b0e43146b4200f26cd41b89003413` and is held by `#944`, and the run was made before this round's operator-guide amendment, so the closure at the round's tip goes to the controller with the round. `#944` and `#939` are held out of scope by the packet and were not chased.
