## Step 1, round 1 -- 2026-08-31T04:33:36Z

Audit schema: fiat-audit-round/v2

Covered: prose-artefact-loss=reviewed; deletion-filter-scope=reviewed; silent-truncation=reviewed; shared-constant-widening=reviewed; refusal-diagnostic=reviewed; registry-independence=reviewed; unsafe-path-grammar=reviewed; byte-ceiling-bypass=reviewed; generated-copy-drift=reviewed; boundary-currency=reviewed; version-propagation=reviewed

Not checked: whether 4,096 is the right ceiling for an authored-prose diff. The round establishes that the packet refuses above `PROSE_PATHS_MAX` and that the refusal names the surface, not that the number is correctly sized; it was taken from `INTEGRATION_PATHS_MAX` because both bound a surface that grows with the work, and the two are deliberately separate constants so they may diverge. Also not checked: Scribe's behaviour on a packet of that size. The ceiling refusal is driven over a mocked path list rather than a real repository, so no prose pass has been run against four thousand authored paths. Also not checked: the single error the root suite reports. It is the primer's deterministic rebuild, which issue 973 records against a pristine tree at this run's base `840d8dd3` with no local changes; the signature matches byte for byte and the cause was not investigated here.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: the `_checkpoint_ref_names` duplicate-or-oversized diagnostic that issue 774's record leaves open is shared with the checkpoint ref set this step does not touch, and the study places it outside this run; it stands. The two environmental failures that record names, against a host path-length limit and a deep-JSON case, did not reproduce here: the plugin runner reports 2,059 of 2,059 with status `passed` and zero failures or errors. Issue 973's primer failure is filed, reproducible on an unmodified tree at this base, and outside this step. Evidence for the covered concerns: the three lints exit 0 over the changed controller, the five test modules and the two published documents. `GIT_PATHS_MAX` keeps the value 500 and two call sites, the commit range and the checkpoint ref set, both still reading the shared constant; `scribe_files` reads neither `GENERATOR_AGGREGATE_REGISTRY` nor any entry from it, and still calls `bounded_git`, `scoped_path` and the absolute-and-dot refusals over the retained set, so the byte ceiling is taken before any count and the grammar refusals run after it. The four checked-in controller digests agree at `61ca073d6f565880ea614e1d3bf35e2544f8f8906c357ef6c9f2fd4f153fc0ee`, `portable_promise_machine.py check` and `promise_machine.py check` both exit 0, and `horos check` reports that the boundary matches the tree. The step introduces no literal version label and touches no `EVOLUTION.md`. The case that filed the issue, run against the real range that killed the directive, `fiat/949-...-step-2-...` to `fiat/949-...-step-3-...--audit`, the fixed reader returns 10 authored paths where the old one refused at 1,006, and the 995 deletions it dropped were the generated payload.
