Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-1720-portable-payload-reserve-and-standing-measu.md | source_sha256=3a016623798d224ecb7efba3a9ed5218a8aa716b350ddec4ba40b9fdadaf35d1 | h2_count=2
## Step 1, round 1 -- 2026-09-20T15:29:41Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: measure-drift=not-applicable; over-line-blind=not-applicable; refusal-text=not-applicable; class-overreach=reviewed; link-pullback=reviewed; lazarus-retained=reviewed; dangling-mentions=not-applicable; manifest-truth=not-applicable; figure-prose=reviewed; record-currency=reviewed; resolver-writes=reviewed; workflow-untouched=reviewed; other-suites=reviewed<br><br>Not checked: x-ray and solidity-auditor ran their scope check against the step's full diff (41 files, zero `.sol`, no `foundry.toml`/`hardhat.config`) and found no in-scope Solidity material, matching the recorded non-Solidity waiver; fizz does not apply for the same reason. The five not-applicable concerns (measure-drift, over-line-blind, refusal-text, dangling-mentions, manifest-truth) describe `scripts/portable_promise_machine.py`, `PORTABLE.md` and `tests/test_skills_sh_package.py` behaviour this step does not touch, so they have no surface until a later step edits the generator. Imprimatur, Protasis and the full `run_checks.py` root/docs scope are Mason's Exit gate, already green on this clean tree, and were not independently rerun. Live GitHub, merge and PR operations are out of scope for this round.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: `.hexaemeron/reports/probe-manifest-killed-probe-recovery.json` (schema `protasis-design-report/v1`, candidate `probe-manifest`, unrelated to this resolver's four `CANDIDATES`) sits beside the 32 receipted reports; it is gitignored local scratch (`.hexaemeron/.gitignore` excludes it), untracked, and correctly absent from the packaged `docs/portable-payload-reserve/reports/` (32 files, byte-identical to `.hexaemeron/reports/` apart from this one file). Not pursued: pre-existing scratch outside this step's Files list, not a step defect. Evidence for the covered concerns: `phylax.py docs/portable-payload-reserve/design`, `ephoros.py docs/portable-payload-reserve/design`, `hypomnema.py docs/portable-payload-reserve docs/decisions` and `hypomnema.py --study docs/portable-payload-reserve/study.md --design-evidence docs/portable-payload-reserve/design-evidence.json --repo-root .` all exit 0 clean. `study.md`, `runbook.md` and `design-evidence.json` under `docs/portable-payload-reserve/` are byte-identical to their `.hexaemeron/` originals at `b20a981525c9dd1b37dbc6bdb229d9ca56ab5ccae184b9888fa6445e27d43e04`; the 32 selection reports are byte-identical; `design/resolve.py`, `design/main-measurement.json` and `design/selection-summary.json` match the runbook-stated hashes `9060bf1f6fd8a444315987b6ffcb5d7bf130f0c4855304b5f1d7d8f19d232de1`, `68aa491948d78473289c8864abe70af388d254691ecb5c6aa04d51369d0b8559` and `de1338c4849f195edaddf9dd8648a94f5da1b42918b10d800f9f227c1ee9a99c`. A fresh `resolve.py selection --root . --reports <scratch>` run reproduced every candidate's `package_bytes`/`package_files`/`margin`/`reserve_held` exactly as cited in `study.md` section 4 and the decision draft (example-payload-class 17,790,545/1,404/3,180,975/5,242,880; second-distribution 15,524,133/1,114/5,447,387/5,242,880; lower-reserve 20,381,433/1,550/3,735,815/2,097,152; plugin-budgets 20,381,433/1,550/590,087/5,242,880), and `design-evidence.json`'s recorded gate states (`lower-reserve` fails `reserve-held`, `plugin-budgets` fails `delivery-room`, the other two candidates and gates pass) match those figures with every `report.sha256` verified against the actual report file. `resolve.py`'s subprocess calls are fixed local `git`/`sys.executable` argv with no shell, `write_report` refuses a symlinked path and refuses to overwrite a report with different bytes, and its link-closure walk (`close_links`/`link_targets`) is bounded by the file universe with no network access. `docs/decisions/ADR-040-package-one-dependency-closed-portable-router.md`, `docs/decisions/ADR-090-omit-portable-decorative-portraits.md` and `docs/decisions/drafts/keep-one-complete-lazarus-fixture-in-the-portable-runtime.md`, all named by the new draft decision, exist and are untouched by this step's diff. `distribution/skills-runtime/sync.yml` is untouched. `python3 plugins/horos/skills/horos/scripts/horos.py check .` reports the boundary matches the tree. `tests.test_agent_instruction`, `test_agent_instruction_corpus`, `test_agent_instruction_reconciliation`, `test_portable_duplicate_fixture` and `test_portable_skills` ran 424 tests, 0 failures; `plugins.hexaemeron.tests.test_phylax_model_proxy.ConformanceTests.test_skill_package_marketplace_coverage_and_portable_versions_are_exact` passed.
## Step 2, round 1 -- 2026-09-20T17:48:51Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: measure-drift=reviewed; over-line-blind=reviewed; refusal-text=reviewed; class-overreach=not-applicable; link-pullback=not-applicable; lazarus-retained=not-applicable; dangling-mentions=not-applicable; manifest-truth=not-applicable; figure-prose=not-applicable; record-currency=not-applicable; resolver-writes=not-applicable; workflow-untouched=reviewed; other-suites=reviewed<br><br>Not checked: x-ray and solidity-auditor ran their scope check against the step's diff (5 files, zero `.sol`, no `foundry.toml`/`hardhat.config`) and found no in-scope Solidity material, matching the recorded non-Solidity waiver; fizz does not apply for the same reason. Beyond the scope check, both tools' Python-relevant angles (command injection, path traversal, argument parsing, information disclosure) were applied by hand to the new `measure` surface; see below. The eight not-applicable concerns (class-overreach, link-pullback, lazarus-retained, dangling-mentions, manifest-truth, figure-prose, record-currency, resolver-writes) describe the omission-class predicate, `PORTABLE.md` link-kept exceptions, the Lazarus fixture guard, the package test's `OMISSIONS`/`EXPECTED_OMISSIONS` rows, figure prose in comments/`PORTABLE.md`/decision records, the committed design-time measurement record and `design/resolve.py`; this step's diff touches only `scripts/portable_promise_machine.py`, `tests/test_portable_payload_measurement.py` (new) and `tests/test_skills_sh_package.py`, none of which carries that surface, so they have no surface until a later step. Imprimatur, Protasis and the full `run_checks.py` root/docs scope are Mason's Exit gate and were not independently rerun in full; the promise-machine-adjacent test modules were rerun directly (below). Live GitHub, merge and PR operations are out of scope for this round.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: `hypomnema.py scripts/portable_promise_machine.py tests/test_portable_payload_measurement.py tests/test_skills_sh_package.py` exits 1 with 3 findings (H006 `ADR-054` citation at `scripts/portable_promise_machine.py:30`; H006 `ADR-054` citation at `tests/test_skills_sh_package.py:76`; H009 stable decision reference `adr/omit-checkpoint-authority-conformance-corpora-from-the-portable-runtime` with no record at `tests/test_skills_sh_package.py:161`), contradicting Mason's Exit-gate report of a passing hypomnema lint; `phylax.py` and `ephoros.py` are clean (exit 0) over the identical three-file scope. All 3 findings are byte-identical to the step's entry tree, confirmed by running the same `hypomnema.py` invocation against a temporary worktree at commit `7d8fccec` (step 1's exit / step 2's entry commit) before any of this step's edits; `tests/test_portable_payload_measurement.py` on its own is hypomnema-clean. Not pursued: debt carried from an earlier step's lineage (issue #1676's ADR-054 and checkpoint-authority references), outside this step's Files list, not a defect this step introduced. Also not pursued as a finding: the study's own metron command for this step, `measure --require-margin 794493`, exits 1 here (package margin 590,087) -- `study.md` section 4's `delivery-room` gate row states `at least 794,493 | ... | 590,087 fail` and section 10 frames the command as exiting 1 "on a tree whose margin is smaller"; the gate is designed to block `step:4`, not this step's `step:3` transition (which needs only `measure-agrees`), so the observed failure is the documented, expected state, not a code defect. Command-injection/path-traversal/argument-parsing/information-disclosure review of the new `measure` surface: `measure_tree` calls `_package_bytes`/`expected_files` exactly as `package`/`check` do, reusing the same fixed-argv `git -C <root> ...` and `[sys.executable, <horos>, "scan", ...]` subprocess calls with no shell and no new subprocess; `--top`/`--require-margin` are parsed by a new `_non_negative_int` argparse type. Adversarial CLI runs: `--top -5`, `--top=-5`, `--require-margin -1` (exit 2, correctly consumed as the option's value, not mis-parsed as a flag); `--top abc`, `--top 3.5`, `--top 0x10` (exit 2, "not an integer"); `--top` missing its value (exit 2, argparse's own "expected one argument"); an unknown action (exit 2); `--top` with `10**36`, with a leading space, a leading `+` and underscore grouping (all exit 0 harmlessly -- `--top` only bounds a list slice); `--require-margin 999999999999999999999999999999` (exit 1, correctly refuses and names both figures); `measure --out <path> --json` (exit 0, confirmed nothing written to `<path>`: the action writes nothing and `--out` is silently unused for `measure`). `--json`'s `largest_default_included` entries are repository-relative POSIX paths only (e.g. `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`), never absolute filesystem paths or user-supplied data; `kept_by_link` is `[]` as the runbook specifies for this step. One cosmetic, non-security observation: piping `measure --json` into a truncating reader (`| head -c N`) surfaces a `BrokenPipeError` traceback on stderr -- standard Python behaviour for any script writing to a closed stdout pipe, discloses nothing beyond the script's own source line numbers, and is not specific to this diff. `python3 plugins/horos/skills/horos/scripts/horos.py check .` reports the boundary matches the tree; `distribution/skills-runtime/sync.yml` is byte-identical to the entry tree. `tests.test_agent_instruction`, `test_agent_instruction_corpus`, `test_agent_instruction_reconciliation`, `test_portable_duplicate_fixture`, `test_portable_skills`, `test_skills_sh_package` and `test_portable_payload_measurement` ran 462 tests, 0 failures; `plugins.hexaemeron.tests.test_phylax_model_proxy.ConformanceTests.test_skill_package_marketplace_coverage_and_portable_versions_are_exact` passed.
