## Step 1, round 1 -- 2026-09-14T10:28:29Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=not-applicable; checker-narrowing=not-applicable; signature-gate=reviewed; promise-overreach=not-applicable; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=not-applicable; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). The root and Hexaemeron suites and `scripts/run_checks.py` were not re-run; this round cites the implement runs on `928884056101bdc2b739c12bdbc2441b6647e30a`, the identical committed tree, and recomputed their bad-result ids from the raw logs. Not applicable at this tree: parity-anchor-dead and checker-narrowing (Steps 2 and 3; the merge deletes the `hexctl.py` frozensets, `verify_host_set_parity` and `scripts/check_commit_identity.py`, so neither subject exists yet), promise-overreach (Step 3), ambiguity-loss (Step 5; the adopted draft has no line matching ambigu, lookalike or impersonat) and enforcement-gap (Step 6). promise-bytes and settings-file bear on this tree and were reviewed as the intermediate states Steps 2 and 4 repair. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and the Step 3 wall-time budget were not read. The pre-commit gate did not run for `92888405` (written with `git commit-tree`; per its message the superseded `f914d873` used `FIAT_SKIP_PRECOMMIT=1`) or for this record commit (`git commit-tree`), because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S1-R1-01 | low | docs/signature-only-authorship/study.md:449 | The Exit clause "The merge commit carries both trees" holds: second parent `89103fce09072913823529e5e341cb9eb383adac` has tree `74f950061e1eb2b82b84224fd6e964100e732650`, shared by `d9114b653772dd6ba09a81d7af8ccc1943f23042` and `0314fd93b750ac7567071866e0a93e68f19ca884`; each of the six rebuilt commits keeps its original's tree, author, author date and message, with both provenance trailers appended and a new committer date; each dropped web-flow merge is tree-identical to the head it merged (`627f1e77`, `adbeeb9d`, `bad762e4`, `74f95006`). The study non-goal that the prior branch's 16 commits are merged rather than replayed, keeping their trailers, does not hold: 6 keep their hashes, 6 are replayed with trailers their originals lacked, and the merges of #1430, #1455, #1459 and #1481 (author `shoggoth-wildcat-labs[bot]`, committer GitHub) are dropped, so `d9114b65` is not an ancestor of `92888405`. Adopted records cite four ids now outside the run branch's ancestry: `audit/rounds/fiat-1135-retire-mandatory-shoggoth-co-signature-and.md:64` (`9a0a1bde`), `:80` (`27a9622a`) and `:96` (`843d232d`), with its synopsis, and `docs/shoggoth-signature-only-retirement-demonstration.md:7` and `:40` (`155abea7`). Only the `92888405` message maps them to their successors. | open: record the reconstruction and its id map with the salvage decision (Step 5 draft) and amend the study non-goal |
| S1-R1-02 | low | docs/signature-only-authorship/runbook.md:42 | Step 1 Files names none of eight paths `92888405` changes: `docs/promise-machine/obligation-gates/demonstration-evidence.md`, `demonstration-run.json` and `evaluation-run.json`; `tests/fixtures/promise-machine/composition/cases.json`; runtime fixtures `fiat-final-integration.json`, `fiat-receipted-delivery.json`, `fiat-runbook-amendment.json` and `fiat-study-amendment.json`. All 60 changed lines are digest or size pins that follow the merge, mapped one by one: `plugins/hexaemeron/tests/test_hexctl.py` `b745c9a2` to `25c86464` (36 pins), `plugins/hexaemeron/tests/test_fiat_skill.py` `08d4e9e2` to `c7ca610f` (10), `plugins/hexaemeron/skills/fiat/EVOLUTION.md` `db00390b` to `39e0dab7` (4), `PROMISE_MACHINE.md`, `tests/promise_machine_coverage.json`, the composition cases and the evaluation run in the two demonstration files (8), one `bytes` value 260067 to 260109, and the evaluation run's own `tree_sha256`. With the eight files at `59239072` bytes, `python3 scripts/promise_machine.py check` refuses with 20 findings. The re-tally is honest: `emit` on both snapshots gives 11 byte-identical prompts and manifests differing only in `tree_sha256`, `tally` with model `ollama/qwen3.8-uncensored:Q6_K@sha256:79b2ee0918f76b4dd182bdd34c2ff14d1e89ec149311a9167e4d58450a472bce` and date `2026-08-31` reproduces the committed file byte for byte, and `verify` exits 0. The same field routes both marketplace manifests through `python3 scripts/promise_machine.py sync`, which writes only plugin `PROMISE_MACHINE.md` copies (`wrote 0`); each manifest differs from base by the version token alone, so the merge message's claim that they came from the promise machine's commands is inaccurate. | open: runbook amendment required (Complete replacement Files) |
| S1-R1-03 | medium | plugins/hexaemeron/skills/fiat/scripts/hexctl.py:12660 | The merge leaves the signature gate's code unchanged (an AST diff of `verify_local_commit` against `59239072` removes only the host and trailer checks and the docstring's trailer clause; `signing_key` and `verify_local_range` are identical), but its GitHub web-flow refusal runs only after `git verify-commit` fails. This host's default keyring holds GitHub keys `B5690EEEBB952194` and `4AEE18F83AFDEB23`, so `git verify-commit 77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883` exits 0 (`%G?` `U`), `verify_local_commit` accepts the specimen, and `tests/prove_signature_only_refusals.py` exits 1 with "web-flow specimen commit 77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883 was accepted by verify_local_commit". Under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, `git verify-commit` exits 1 (`%G?` `E`) and the prover exits 0, writing a report byte-identical to the recorded one (SHA-256 `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09`), which names no keyring. The runbook's `%G?` `E` statement and the comment at `hexctl.py:12236-12241` hold only without GitHub's keys, and study section 11 lists a signature refusal that stops firing as a run stop. | open: decision required outside Step 1 Files (receipt under a keyring without GitHub keys, or make the refusal keyring-independent by amendment) |
| S1-R1-04 | low | tests/prove_signature_only_refusals.py:297 | The prover cannot tell a keyring that validates GitHub's key from a removed guard: both exit 1, which its docstring reserves for a specimen accepted or refused with another message, while exit 3 is for an unestablished precondition. Two paths show it: the web-flow case in S1-R1-03, and `locally_verified_commit` (`:135`), which harvests the first of the last 25 commits the keyring verifies; run from GitHub-signed HEAD `d9114b65` under the default keyring, the prover exits 1 with "altered specimen commit 15d7b845549f288e77669e31d805bbd7714810e0 is signed with GitHub key B5690EEEBB952194", and the same run under the scratch keyring exits 0. | open: held behind S1-R1-03; if the refusal stays keyring-dependent, precheck `git verify-commit` on the web-flow specimen and skip GitHub-keyed commits when harvesting, exiting 3 with the cause; a guard test is outside Step 1 Tests |
| S1-R1-05 | low | docs/signature-only-authorship/study.md:1 | `hypomnema.py --study docs/signature-only-authorship/study.md --design-evidence .hexaemeron/design-evidence.json` exits 1 with H008 "study has no design bridge block": section 12 names `docs/decisions/drafts/accept-any-validly-signed-authorship.md` in prose only. The prior run met the same H008 and closed it with a study amendment, committed as `cfebac48673d14920d9eedb1461ac04051cfc0b1`. A scratch candidate appending a `design-bridge` fence from `retain-declaration` to that draft passes `protasis.py --study`, the controller's amendment grammar and study mode (exit 0). | open: study amendment required, then refresh the tracked copy |

Leads not pursued: Suite evidence is the implement runs on `928884056101bdc2b739c12bdbc2441b6647e30a`, the identical tree, with ids recomputed from raw logs: root runner `Ran 1818`, 1788/1818, the same 30 bad ids as `59239072` (`comm` empty both ways; `tests.test_agent_instruction` 27, `tests.test_agent_instruction_corpus` 2, `tests.test_skills_sh_package` 1); Hexaemeron runner 3 bad ids, all among `59239072`'s 4 (`test_audit_synopsis_recovery` 1, `test_metron_check.TimeCommandTests` 2); `scripts/run_checks.py --base 592390722f10df53658906623b15428dbfb88d8f --format json` red, 14 of 16 checks passed, and the failing root-suite (30) and hexaemeron-suite (1) ids are all in the base sets. Loaded root test ids fall from 1864 to 1840 only in branch-adopted modules (`test_commit_identity` -28, `test_host_settings` -3, `test_contributors` -9 +6, `test_marketplace_prose` -1 +1, `test_shoggoth_identity` -1 +2, `test_ruleset_identity_retirement` +9). Lints from plugin root 1.6.36 on a clean snapshot: phylax 0 and ephoros 0 over the 79 paths of `git diff --no-renames --name-only --diff-filter=d 59239072 92888405`, hypomnema 0 over its 40 `.md` paths. On that snapshot `promise_machine.py check` 0, `coverage --check` 0 (98 rows), `horos.py check .` 0, `audit_synopsis.py --check .` 0, `git diff --check` 0, no conflict marker in the ten paths, both horos scans and `promise_machine.py sync` leave the tree unchanged, and both `cmp` checks pass in the run worktree; `portable_promise_machine.py check` exits 1 on clean snapshots of both `59239072` and `92888405` (ignored runtime mirror absent) and 0 after its own sync, with `SHOGGOTH.md` at `8fc95b7e33d0dddb07671a641cebaadc17afe977ea3afcb1c419b602304aad4a`, `contract=shoggoth-collective/v5`, pinned at `tests/test_shoggoth_identity.py:11`. `59239072..92888405` holds 13 commits, all `G B83B60AE16F5DD1A`, none GitHub-signed; `hexctl verify` is ok. The 63 clean paths: Git's `merge-tree` of `59239072` and `d9114b65` differs from the head tree only at the 21 designated paths (10 conflicted, 3 created, the 8 of S1-R1-02); 41 are byte-identical to `d9114b65`, and 22 (`AGENTS.md`, `README.md`, `plugins/hexaemeron/AGENTS.md`, `PROMISE_MACHINE.md` and its 18 mirrors) differ from it only by the three-way merge of base changes, so an empty diff against `d9114b65` holds for 41 paths, not 63. The conflicted pins: both plugin manifests, both marketplace manifests and both tests change one token to `1.6.33` (base `1.6.31`, branch `1.6.30`), and `tests/promise_machine_coverage.json` carries 23 same-file digest re-pins plus the branch's two renamed `test_contributors` rows. `1.6.33` is not a resolved version (`version_relations` resolution null): main carried it from `704c1c5b` (2026-09-11) until `868084d1` (2026-09-13), `origin/main` is at `1.6.37`, and 36 local and remote refs claim it, so integration must re-pin all six sites; the merged Fiat row `fiat-v5.55.1`, asserted by `tests/test_evolution_contract.py`, also collides with main's `868084d1`. The gate skip follows the 2026-09-09 Exit amendment, which names `FIAT_SKIP_PRECOMMIT=1`; `92888405` used `git commit-tree`, and its message discloses both. salvage-drift: two greps over the 30 adopted paths find no line asserting a withdrawn rule. Of 42 lines matching trailer, host-guard, runtime-host or ambiguity tokens, `SHOGGOTH.md:75` and `test_fiat_skill.py:740` state the withdrawal, `docs/shoggoth-signature-only-retirement-demonstration.md` (3) describes specimens, `push-discipline.md:115` and `test_fiat_skill.py:231` carry the pull-request origin marker, `hexctl_harness.py` (13) and `host_identity_cases.py:5` are accepted legacy shapes, the two prior audit files (13) are history, and 8 are unrelated ambiguity wording. A trailer, co-author, provenance and byline grep over the 11 prose files finds withdrawal or attribution statements (`SHOGGOTH.md:72-75`, `INSTALL.md:118-126`, `AGENTS.md:166-167`, `plugins/hexaemeron/AGENTS.md:134`, `docs/how-to-help-shoggoth.md:212-220`, `mason.md:69`, `warden.md:109`, `fiat/SKILL.md:591-592`, `push-discipline.md:35-37`), ADR-assignment trailers and the integration attribution check; `README.md:195` still calls the identity contract's rules host-provenance rules, as base and branch did. `INSTALL.md:109` and `docs/how-to-help-shoggoth.md:217-218` describe `.claude/settings.json`, absent here until Step 4, whose Exit checks only `INSTALL.md`. residual-refusal: in `hexctl.py` from base to head, `is_host_identity` 7 to 0, `HOST_BYLINE_RE` 3 to 0, `HOST_PR_LOGINS` 3 to 0, `HOST_IDENTITY_NAMES` 2 to 0, `HOST_IDENTITY_EMAILS` 2 to 0, `COAUTHOR_TRAILER` 2 to 0, `ORIGIN_TRAILER` 2 to 0, `Wildcat-Origin` 2 to 0 and `runtime host` 12 to 0; `scripts/check_commit_identity.py` (42 `raise Refusal` at base) and `.github/workflows/identity.yml` are absent; non-test code naming a guard token falls from 3 files to 0, and the `NON_HUMAN_*` sets left in `scripts/contributors.py` only classify accounts for the contributor ranking. preserved-record-drift: `59239072..92888405` adds the two prior-run audit files (identical to `d9114b65`) and this run's two specification copies, and changes no audit file, ADR-016, ADR-019, ADR-052, ADR-058, design or proof record, or `samples.jsonl`; record-append-only: both prior design records enter at `d9114b65`'s bytes. promise-bytes: all 19 copies carry the branch rewrite at `89b6fac2` (numstat 5 4 each against `59239072`, within lines 344-353), consistent with this tree's `contributors.py`, for Step 2 to restore. live-configuration, read-only on 2026-09-14: ruleset 21830871 `Required CI`, enforcement `evaluate`, required context `invariants` (app 15368), no bypass actors, updated 2026-09-07, as study assumption 4 records; classic protection on `main` now exists (required `invariants`, strict, `enforce_admins` on, `required_signatures` off, force pushes and deletions off) where assumption 4 recorded `Branch not protected`: drift reported, nothing changed. Push phase: `origin`'s step branch is `9f54459570280219477f5b1f033343997fb3f305` (same tree, second parent `d9114b65`), not an ancestor of `92888405` and without a pull request, so publishing the receipted head is not a fast-forward. Elenchus: the Step 1 runner on this red parent reports 11 errors, so any Step 1 fix touching `tests/` classifies `inconclusive`. Prover Phylax notes: `git()` checks its output cap only after buffering all output (the 60 s timeout bounds time, not memory), and `locally_verified_commit` pins `gpg.program` alone rather than hexctl's four-program verifier configuration; both read this repository's own objects.

## Step 1, round 2 -- 2026-09-14T11:33:01Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=not-applicable; checker-narrowing=not-applicable; signature-gate=reviewed; promise-overreach=not-applicable; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=not-applicable; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. Not applicable at this tree: parity-anchor-dead and checker-narrowing (Steps 2 and 3; `hexctl.py` declares no `HOST_*` frozenset and `scripts/check_commit_identity.py` is absent), promise-overreach (Step 3), ambiguity-loss (Step 5) and enforcement-gap (Step 6). promise-bytes and settings-file bear on this tree and are unchanged since round 1. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and the Step 3 wall-time budget were not read. The pre-commit gate did not run for `4552203e` or for this record commit, both written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Round 1 dispositions. S1-R1-01 is answered by study amendment `15ec3f4ecaa62441ab050644e6f39a116b202a94f6347887b6f4c1e2b0ac1c58` (study `a53c28e8394d2c2ba7e6bf53f68280c4c7349d4f4373ad500f22c6d2f040e1b8`), which records the reconstruction and supersedes the non-goal; the four ids the adopted records cite are commits of #1455, #1459 and #1481 that the GitHub API still returns with `verified: true`. S1-R1-02 is answered by runbook amendment `fa0e6d74fe143ee9b1e9121b9f5a574970b07637dd59bda26f03d1a33d04ff52`. S1-R1-03 is answered by that study amendment's reading of the section 11 stop under a keyring that cannot validate GitHub's web-flow keys, with the gate and the host keyring unchanged, and by the orchestrator's decision to carry the question to integration as an issue. S1-R1-04 is fixed in `4552203e54a897229f4d20185f66e3544fa9bc06`. S1-R1-05 is answered by the study amendment's `design-bridge` fence. `4552203e` also copies `.hexaemeron/study.md` and `.hexaemeron/runbook.md` (`6033b22a0c83e8d6c9d233428d24f7c076e2216cefff7d019cf54d936491a9cb`) over the two tracked copies, and both `cmp` checks pass. The fix: the prover exits 3 and names the cause when `git verify-commit` accepts `77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883` or the harvested commit carries a key in `GITHUB_SIGNING_KEYS`; its git reader kills a child once output passes the cap; its signature checks pin hexctl's four verifier programs. Reproduction and guard were taken by hand, because Step 1's Tests field admits no test module: on this host's default keyring the parent's prover exits 1 ("web-flow specimen commit 77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883 was accepted by verify_local_commit") and the fixed prover exits 3 ("this keyring validates GitHub web-flow key B5690EEEBB952194: git verify-commit accepts 77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883"); from GitHub-signed HEAD `d9114b65` the parent exits 1 on the altered specimen and the fix exits 3 naming `d9114b653772dd6ba09a81d7af8ccc1943f23042`; under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A` each run exits 0, and the report at the receipted path keeps SHA-256 `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09`. The new reader matches `subprocess.run` on four calls, raises at a 1000-byte cap and at a 0.05 s timeout, and returns status 129 without hanging when a child exits before reading 500 KB of input. Elenchus ran the runbook's runner (`python3 tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.elenchus/fiat-1135-step-1.json`) on `4552203e`: `inconclusive`, "the runner report records an infrastructure error", with 1818 executed, 20 assertion failures and 11 errors. The parent's baseline errors make any Step 1 fix under `tests/` inconclusive, and the run's bounded output does not name its 20th failure, one more than the 19 the snapshot runs report. On a clean detached snapshot of `4552203e`, the root runner gives `Ran 1818`, 1788/1818, with the same 30 bad ids as `59239072` (`comm` empty both ways), and the Hexaemeron runner runs 2484 tests with 2 bad ids (`test_audit_synopsis_recovery` 1, `test_metron_check.TimeCommandTests` 1), both among `59239072`'s 4. In the clean run worktree at `4552203e`: `git diff --check` 0, the prover exits 3 on the default keyring and 0 under the scratch keyring, `promise_machine.py check` 0, `coverage --check` 0 (98 rows), `portable_promise_machine.py check` 0, `horos.py check .` 0, `audit_synopsis.py --check .` 0, and `hypomnema.py --study docs/signature-only-authorship/study.md --design-evidence .hexaemeron/design-evidence.json --repo-root .` 0. `59239072..4552203e` holds 15 commits, all `G B83B60AE16F5DD1A`, none signed with `B5690EEEBB952194` or `4AEE18F83AFDEB23`. Lints from plugin root 1.6.36 over the seven paths this round changes (`.horos/candidates.json`, `.horos/census.json`, this record and its synopsis, both specification copies and the prover): phylax 0 and ephoros 0; hypomnema 0 over the four Markdown paths plus `docs/decisions/drafts/accept-any-validly-signed-authorship.md`, which the study copy cites by stable slug, and 1 (H009 at `study.md:867`) without that draft, because the walk indexes only the paths it is given. Review against the register: the round changes none of the 30 adopted paths, its prover diff adds no host or trailer token, and `hexctl.py` is unchanged; the only design records it changes are this run's two specification copies, which the Files amendment takes at the step's last commit although study section 3's `docs/*/study.md` and `docs/*/runbook.md` glob reads over them; ruleset 21830871 and classic protection on `main` read as in round 1, and `origin`'s step branch now points to `92888405`. The Step 1 Exit names only `test_audit_synopsis_recovery` in the Hexaemeron base set, while measured base runs also fail up to 3 `test_metron_check.TimeCommandTests` ids under load, as the Step 2 amendment `b6422c1d876148901a062350b64386ff60c6ddf1fbe13167146de3a9a0b65e1c` records; that amendment's Entry reads "the base's own,, where", and the tracked copy keeps the doubled comma. `1.6.33` and the merged `fiat-v5.55.1` row still collide with main and remain integration's. The prover's write to a child's standard input is not timed; it relies on every input being built from capped reads and on `git hash-object` reading all of its input before it writes.

## Step 2, round 1 -- 2026-09-14T14:29:57Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=reviewed; checker-narrowing=not-applicable; signature-gate=reviewed; promise-overreach=not-applicable; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=not-applicable; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=not-applicable

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. Not applicable at this tree: checker-narrowing and promise-overreach (Step 3; `scripts/check_commit_identity.py` is absent), ambiguity-loss (Step 5) and enforcement-gap (Step 6). settings-file bears on this tree only as the intermediate state Step 4 repairs. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and the Step 3 wall-time budget were not read. `python3 scripts/contributors.py --check` exits 2 on 2026-09-14 at this head and at `592390722f10df53658906623b15428dbfb88d8f` (item 6), so no ranking was computed or compared. The pre-commit gate did not run for `aa2199c311ade3ef4293d009689d9caed1f4602f` or for this record commit, both written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Suites were re-run for this round on clean detached snapshots of `aa2199c311ade3ef4293d009689d9caed1f4602f` (tree `5cd13eeee5e571c439f47fe65488e7bcd44a7aaf`), `NO_COLOR=1`: root runner `Ran 1829`, 1799/1829, the same 30 bad ids as `59239072` (`comm` empty both ways once subtest parameter suffixes are dropped; `tests.test_agent_instruction` 27, `tests.test_agent_instruction_corpus` 2, `tests.test_skills_sh_package` 1); Hexaemeron runner 2484 run, 3 failure events, 0 errors (`test_audit_synopsis_recovery` 1, `test_metron_check.TimeCommandTests` 2 at load average 19-27), all among `59239072`'s 4, and `test_metron_check` alone runs 117 OK; `scripts/run_checks.py --base 0b9208517d0f1bb7db5493f874ff55a9b1b401f1 --format json` red, 11 of 13 selected checks passed, `dead-code-suite`, `dead-code-suppressions-check`, `lint-phylax`, `lint-ephoros` and `lint-hypomnema` included; root-suite ran 1829 (19 failures, 11 errors, 1 skip) with the same 30 ids and hexaemeron-suite ran 2484 with 1 failure, `test_audit_synopsis_recovery`. The implement receipt's `run_checks` evidence came from the run worktree, not the detached snapshot the amended Exit names; this run replaces it with the same outcome. On that snapshot: `--verify-host-set` 0 ("host set matches hexctl.py"), `--host-set` 0, `promise_machine.py check` 0 (18 copies), `coverage --check` 0 (98 rows), `portable_promise_machine.py check` 1 before its sync (runtime mirror absent) and 0 after it, the Exit's `git diff` 0 bytes, `tests.test_contributors` 116 OK, `tests.test_ruleset_identity_retirement` 9 OK, `horos.py check .` 0, `audit_synopsis.py --check .` 0 (80 pairs match), `git diff --check` 0 on the tree and over `0b920851..aa2199c3`, and both horos scans and `promise_machine.py sync` (`wrote 0`) leave the tree unchanged; `hexctl verify` ok (16 ledger entries). Lints from plugin root 1.6.36: phylax 0 and ephoros 0 over the 28 paths of `git diff --no-renames --name-only --diff-filter=d 0b9208517d0f1bb7db5493f874ff55a9b1b401f1 aa2199c311ade3ef4293d009689d9caed1f4602f`, hypomnema 0 over its 20 `.md` paths. All 28 paths fall inside the amended Files field; `.horos/boundary.json` and `.horos/candidates.json` did not need to move. Item 1: `scripts/contributors.py` differs from `59239072` only in four comment blocks rewritten at equal line counts (module docstring, `AGENT_LOGINS`, the non-frozenset skip, the `verify_host_set_parity` docstring) and one word of the set-drift stop at `:205`. Its first list is `set(PARITY_SET_NAMES) - set(declared)`, sets this module declares and `hexctl.py` lacks, so "missing there" is correct and `59239072`'s "missing here" named the wrong file. No document, fixture or promise quotes either wording, `test_stops_on_a_set_here_with_no_counterpart_in_hexctl` pins it, and the change sits inside the Goal's parity surface and the Files field; the Exit fixes no message text. The one ADR-016 comment left (`:330`) cites ADR-016:76-77 on unfamiliar future host names, which that record still says. Item 2: `scripts/contributors.py:191` is `def verify_host_set_parity(hexctl_path) -> None:`, `PROMISE_MACHINE.md:344` is the ranking promise's `- Promise:` line under the `:342` heading, and `hexctl.py:12261-12266` names both with the reason. A position-free AST comparison with `0b920851` shows `hexctl.py` gains exactly three top-level assignments, `HOST_IDENTITY_NAMES` (`:12267`, 14 members), `HOST_IDENTITY_EMAILS` (`:12285`, 2) and `HOST_PR_LOGINS` (`:12291`, 5), each equal to `59239072`'s and to `contributors.py`'s, with no load of any of them and no `HOST_` string constant; the other 675 top-level statements, `signing_key`, `verify_local_commit` and `verify_local_range` included, are unchanged. `grep -c` finds 0 of `is_host_identity`, `is_host_login`, `HOST_BYLINE_RE`, `COAUTHOR_TRAILER`, `ORIGIN_TRAILER` and `Wildcat-Origin`, and outside `hexctl.py` only `scripts/contributors.py` and the two test files name the sets in code. `dead_code.py report --json --analyser python` gives 223 findings at `0b920851` and here, differing only by one existing `hexctl.py` finding moving 40 lines (`cmd_checkpoint_restore.root@16302` to `@16342`), so the sets add none. Item 3: all 20 tracked `PROMISE_MACHINE.md` paths are blob-identical to `59239072`; the canonical file is SHA-256 `ae019700f378a250fe08a3d6cb68c0553ee60a410cbb6035d3c9c3cd2866b34f`, 32144 bytes, 19 copies carry `### promise-machine-contributor-ranking`, and the 20th is the `tests/fixtures/promise-machine/divergent-copy` fixture. Item 4: `tests.test_contributors` loads 108 ids at `59239072`, 105 at `0b920851` and 116 here. Every base id survives; the 8 added are the five `HostSetParityDirections` cases, `test_contributor_classification_does_not_reach_fiat_admission`, `test_the_contributor_promise_keeps_its_hexctl_parity_check` and the branch's `test_the_guide_does_not_make_account_matching_an_admission_rule`. With only this head's test file overlaid on `0b920851`, the five direction cases fail with 11 failure events and 0 errors (control 1, member absent here 3, member absent there 3, set absent there 3, set absent here 1); the whole module there runs 109 with 14 failures and 6 errors, because restored and renamed tests dereference names the parent lacks. From the parent, the three `ContributorClassification` ids become `HostSetParity` ids (two revert to `59239072`'s tests and the admission test is rewritten), `test_non_human_logins_are_recognised` becomes `test_host_logins_are_recognised`, and `test_the_contributor_promise_has_no_fiat_host_set_dependency` is inverted as `test_the_contributor_promise_keeps_its_hexctl_parity_check`. `tests/test_ruleset_identity_retirement.py` changes only `:102` (the name) and `:111` (`assertNotIn` to `assertIn`); `RulesetComparatorTests` and the absence assertions at `:96`, `:97`, `:100` and `:115` are untouched for Steps 3 and 4. No changed test asserts a withdrawn rule: the admission test asserts that `hexctl.py` loads none of the sets and names no withdrawn rule, and keeping runtime hosts out of the ranking is answer 1's. Item 5: the `contributor_ranking` coverage row carries 37 selectors at `59239072` and at `0b920851` and 41 here, `59239072`'s 37 plus the four direction selectors; against the parent it restores `test_stops_on_host_set_drift` and `test_stops_on_a_changed_member_of_a_known_set` and drops `test_contributor_classification_does_not_reach_fiat_admission` and `test_non_human_names_and_emails_are_classified_locally`. Its other fields equal `59239072`'s, its two digests match their files at all three commits, and the run-observation row re-pins `hexctl.py` from `599d0fbb` to `95849af6`. Both demonstration files hold 9 pins and every one matches the head bytes; this step moves `PROMISE_MACHINE.md` (`89b6fac2` to `ae019700`), `tests/promise_machine_coverage.json` (`c0cf3ae5` to `80bf493b`, 260109 to 260329 bytes) and `evaluation-run.json` (`3856acce` to `4d52a47c`, 3231 bytes), and `git grep` finds no parent digest of any of the 28 changed files in the head tree. The re-tally reuse is honest: `emit` on `0b920851` and on this head gives 11 byte-identical prompts and manifests differing only in `tree_sha256` (`ecfdaf39` to `d669dbc3`); `tally` with model `ollama/qwen3.8-uncensored:Q6_K@sha256:79b2ee0918f76b4dd182bdd34c2ff14d1e89ec149311a9167e4d58450a472bce` and date `2026-08-31` reproduces the committed file byte for byte; `verify` exits 0 (11 cases, 55 passed); `corpus_sha256`, `prompt_template_sha256` and `evaluation-answers.json` (`3ebdb3a8`) are unchanged. Item 6: with `GITHUB_TOKEN` from `gh auth token` at 2026-09-14T14:06:33Z, `python3 scripts/contributors.py --check` exits 2 at this head and at a detached `59239072` with empty stdout and byte-identical 158-byte stderr, `contributors.py: unknown identity: 'shoggoth-wildcat-labs[bot]' is a Bot that is not in the host set; extend HOST_PR_LOGINS in hexctl.py and here, then rerun`; `0b920851` exits 2 on the same identity through `NON_HUMAN_PR_LOGINS`, so the amended Exit holds. Step 6's Exit still asks for exit 0. `exclusion_reason` stops on any Bot outside `HOST_PR_LOGINS` before `AGENT_LOGINS` is read, so the Shoggoth's own bot can be excluded only by adding it to the runtime-host set in both files, against the `AGENT_LOGINS` comment, or by a code change; the second Step 2 amendment records a scratch tree with that login in both sets exiting 1 on `CONTRIBUTORS.md` and `README.md`, which Step 6's "Change no product file" excludes. Item 7: `docs/signature-only-authorship/runbook.md` (SHA-256 `6033b22a0c83e8d6c9d233428d24f7c076e2216cefff7d019cf54d936491a9cb`) is an exact byte prefix of `.hexaemeron/runbook.md` (`b986150743dec96809bc4608a7fdfd2a27ed5fbf18d7cd425b4a465e15fd85e0`), 36480 of 40509 bytes and 119 of 129 lines. It lacks only amendment `6697b9c8bbecb890441beca9eb1c10288426f3e5d327711d97c699fd7dedac36`, receipted after `0b920851`, so it keeps the doubled comma and an Exit without the same-day `--check` reading; the study copy is identical. This is not a Step 2 finding: Step 2's Files excludes the copy, the runbook preamble preserves `docs/*/runbook.md`, and Step 1's Files amendment defines the copy as taken at Step 1's last commit, which it still is. Proposed disposition before integration: amend Step 6's Files to re-copy both specification copies at its last commit, naming them as this run's own copies that the preamble glob reads over, and add a `cmp` for each to its Exit; failing that, the Step 5 draft should state that the tracked copies stop at Step 1 and that later amendments live in the controller state `hexctl reset` archives locally. Register: the step changes none of the 30 adopted paths, and a grep of them for parity, host-set, `HOST_`, `frozenset` and `NON_HUMAN` finds no line the restoration makes false. It re-derives 2 of the 10 conflicted paths (`.horos/census.json`, `tests/promise_machine_coverage.json`), both current, with no conflict marker in the 28 paths. `scripts/check_commit_identity.py` and `.github/workflows/identity.yml` are still absent, and the restored Stop sites are `59239072`'s ranking stops, one word apart (item 1). The prover at this head exits 3 under the default keyring ("this keyring validates GitHub web-flow key B5690EEEBB952194") and 0 under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, its report differing from the receipted `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09` only in the `command` field's `--out` path. `0b920851..aa2199c3` changes no preserved record; `59239072..aa2199c3` only adds records, and both prior-run design records, the prior demonstration and both prior-run audit files are blob-identical to `d9114b65` with no later commit touching them. `SHOGGOTH.md` is `8fc95b7e33d0dddb07671a641cebaadc17afe977ea3afcb1c419b602304aad4a` at `contract=shoggoth-collective/v5`, pinned at `tests/test_shoggoth_identity.py:11`. Read-only at 2026-09-14T14:14:28Z, ruleset 21830871 and classic protection on `main` read as in Step 1 round 1. `.claude/settings.json` is absent, `INSTALL.md` is unchanged and `:100` still asserts absence. The census at this head counts 3133 files and 104103435 bytes. `0b920851..aa2199c3` holds 1 commit, `G B83B60AE16F5DD1A`, author Shoggoth, committer Dr Laurence E. Day, no GitHub key. Phylax: the step's reads are `59239072`'s code; `frozensets_from_source` parses the fixed `HEXCTL_RELATIVE` path with `ast.parse` and `ast.literal_eval` and imports nothing. Later leads: no test pins the `hexctl.py` anchors `scripts/contributors.py:191` and `PROMISE_MACHINE.md:344`, so a line added above either moves it while the comment still reads as checked; Step 3's Files cites `tests/test_ruleset_identity_retirement.py:112` for the `"identity.yml"` absence, but `:112` is blank and that test is `:113` with its assertion at `:115`, as at `d9114b65`; `scripts/contributors.py:745-747` renders "ADR-016 makes the Shoggoth the author of governed agent work" into `CONTRIBUTORS.md` and `README.md`, which will cite superseded text once the Step 5 draft supersedes ADR-016's Decision paragraph, and no step's Files names either generated file.

## Step 3, round 1 -- 2026-09-14T16:19:56Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=reviewed; checker-narrowing=reviewed; signature-gate=reviewed; promise-overreach=reviewed; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=reviewed; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=reviewed

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. There is no fixes commit, so Elenchus did not run. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and `python3 scripts/contributors.py --check` (live GitHub data; Step 2 round 1 recorded its same-day exit 2) were not read. No `identity` status was read back: step pull requests target the run branch and `.github/workflows/identity.yml` filters `branches: [main]`. The pre-commit gate did not run for `d66356556f6450d13b71e642678c701d7d603f5f` or for this record commit, both written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: Suites were re-run for this round on clean detached snapshots of `d66356556f6450d13b71e642678c701d7d603f5f`, `NO_COLOR=1`: root runner `Ran 1858`, 1828/1858, the same 30 bad ids as `59239072` (`comm` empty both ways; `tests.test_agent_instruction` 27, `tests.test_agent_instruction_corpus` 2, `tests.test_skills_sh_package` 1); Hexaemeron runner 2484 run, 2 failure events, 0 errors (`test_audit_synopsis_recovery` 1, `test_metron_check.TimeCommandTests` 1 at load average 17-27), both among `59239072`'s 4; `scripts/run_checks.py --base 48dc23bda8980334f183a0a3329e5cf52d0f043b --format json` red, 9 of 10 selected checks passed, `dead-code-suite`, `dead-code-suppressions-check` (223 findings, 0 suppressions), `lint-phylax`, `lint-ephoros` and `lint-hypomnema` included; root-suite ran 1858 (19 failures, 11 errors, 1 skip) with the same 30 ids once the `tests.` prefix is dropped, and `hexaemeron-suite` was not selected, since no changed path maps to its scope. The implement receipt's `run_checks` evidence came from the run worktree, not the detached snapshot the amended Exit names; this run replaces it with the same outcome. Item 7: the skip is `tests.test_commit_gate.ActivationTests.test_this_checkout_has_the_gate_activated`, which skips whenever `WILDCAT_CHECK_CONTAINMENT` is set (`tests/test_commit_gate.py:1716`, `:1838`); `scripts/run_checks.py:2209` and `:2334` set it to a fresh `uuid4` hex for every child. Run alone, the test skips with the variable and passes without it, and the root runner ran it with 0 skips. It reads only the checkout's `core.hooksPath`, which this step does not touch, so the skip hides nothing here. Lints from plugin root 1.6.36 on the `d6635655` snapshot, over the seven paths of `git diff --no-renames --name-only --diff-filter=d 48dc23bda8980334f183a0a3329e5cf52d0f043b d66356556f6450d13b71e642678c701d7d603f5f` (`.github/workflows/identity.yml`, `.horos/boundary.json`, `.horos/census.json`, `scripts/check_commit_identity.py`, `tests/test_commit_identity.py`, `tests/test_python_contract.py`, `tests/test_ruleset_identity_retirement.py`): phylax 0, ephoros 0, hypomnema 0. On that snapshot `tests.test_commit_identity` 29 OK, `tests.test_python_contract` 19 OK, `tests.test_ruleset_identity_retirement` 9 OK, `grep -c 'raise Refusal'` 34, the `contributors` import grep 0, the Exit's `git diff` on `identity.yml` 0 bytes, `horos.py check .` 0, `audit_synopsis.py --check .` 0 (80 pairs match), `git diff --check` 0 over `48dc23bd..d6635655`, both horos scans leave the tree unchanged, `promise_machine.py check` 0 (18 copies), `coverage --check` 0 (98 rows), and `portable_promise_machine.py check` 1 before its sync (runtime mirror absent) and 0 after it; no SHA-256 or blob id of a changed path's parent bytes is pinned in the head tree. Item 1: at `59239072`, `shoggoth_author_count` and `human_author_count` appear only in the checker, its test, and the byte-pinned `audit/rounds/fiat-859-reinstate-the-wave-delta-distributed-checkpo.md` and its synopsis, which quote observed values; here only that history names them, and `wildcat-commit-identity-check` appears only in the checker and its test. `identity.yml` reads `steps.evaluate.outcome`, never the checker's stdout. The removal is within the Exit and the design: `human_author_count` would count an accepted runtime host as human, against the Exit's "claims nothing about who authored it" and `promise-overreach`, and the removal adds no identifier. Keeping `/v1` is honest here: nothing reads or documents the field set, the new record is a strict subset of the old, and a `/v2` would be a new identifier where `retain-declaration` scored `new-identifiers-introduced` 0. The Step 5 draft should state that the success record dropped both counts under the same schema name, because the fiat-859 record quotes them. Item 2, by AST: 28 base tests, 29 here; 9 removed, being the seven withdrawn-rule refusal tests (one holding `:269` and `:273`) plus `test_host_sets_are_the_existing_fiat_and_contributor_sets` and `test_provenance_trailers_match_fiat`, whose subjects (`policy.contributors` and the two trailer constants) this Exit and Step 2's remove; 10 added (`WithdrawnRuleTests` 7, the co-author and Git output/timeout ceiling tests, the import test); 4 changed; 15 unchanged. The host-set parity the removed test also asserted stays asserted by `tests.test_contributors.HostSetParity`. Tracing every `Refusal` construction per test: base tests reach 8 surviving sites and 7 withdrawn ones (`:265` untested); head tests reach the same 8 (head lines `:54`, `:63`, `:141`, `:161`, `:180`, `:195`, `:258`, `:279`) plus `:103`, `:105`, `:234`, `:239` and `:271`; the other 21 surviving sites are reached by neither file. No kept or new test asserts a withdrawn refusal; withdrawn-rule wording appears only in `WithdrawnRuleTests` names and comments. The middle-commit test keeps its guard: its middle commit now fails the surviving `:239` and the test pins that commit's id, so a checker that read only the head would fail it (reasoned, not run). Reading "the eight withdrawn cases" as the assertions of the eight rules holds. The head test file against `59239072`'s checker runs 29 tests with 32 failures and 0 errors: 30 `WithdrawnRuleTests` subtests, each refused by a withdrawn rule (`:307` 18, `:215` 4, `:213` 2, `:273` 2, `:244`, `:259`, `:265` and `:269` 1 each), plus the record-key and import tests. Item 3: by AST source segment, the six ceilings, the four regexes and `_repository_path`, `_git_environment`, `_git`, `_one_line`, `_full_sha`, `_commit_range`, `_commit_bytes`, `_parsed_commit` and `main` equal `59239072`'s bytes; `Refusal`, `_identity`, `_message_policy`, `evaluate` and `parser` changed; `import contributors`, `HOST_BYLINE_RE`, `SHOGGOTH_NAME`, `SHOGGOTH_EMAIL`, `COAUTHOR_TRAILER` and `ORIGIN_TRAILER` went, and nothing was added. `_git_environment` still sets `GIT_CONFIG_NOSYSTEM` to `1` and `GIT_CONFIG_GLOBAL` to `os.devnull`. The `raise Refusal` multiset falls from 42 to 34 by exactly `:213`, `:215`, `:244`, `:259`, `:265`, `:269`, `:273` and `:307` and gains none. All 34 survivors, each driven through the function that raises it at both commits with Git reads mocked and a ceiling lowered where a case needs one, refuse at the mapped lines with messages identical to `59239072`'s. In the checker, `contributors`, `is_host_identity`, `is_host_login`, `HOST_BYLINE_RE`, `SHOGGOTH_NAME`, `SHOGGOTH_EMAIL`, `COAUTHOR_TRAILER`, `ORIGIN_TRAILER`, `runtime host`, `runtime-host`, `ambiguous` and `Wildcat-Origin` count 4, 2, 1, 2, 6, 6, 3, 2, 2, 4, 2 and 2 at base and 0 here; outside test files, the only Python, YAML or shell file naming a guard token is `scripts/contributors.py` (6), at both `48dc23bd` and this head. Each module-docstring claim maps to executed code: "from the exact base to the exact head" to the ancestry check and the `base..head` read, the three ceilings to their refusals, and "identities parsed" to `_identity` on exactly one author and one committer line; it has no attribution clause, and it under-states the file, which also bounds co-author trailers, Git output and time, and the login grammar. The `Refusal` docstring and the argparse description drop the required-check and host-attribution claims, which matches the advisory status read below. The checker's `main` function is byte-identical, so a refusal still prints `identity: <refusal>` to stderr and exits 2. Item 4: `.github/workflows/identity.yml` is blob `437c98ebcbb2258324dd58ab56b6584032035098` at `59239072` and here, with `pull_request_target`, `branches: [main]`, `timeout-minutes: 5` at `:23`, its one checkout at `github.event.pull_request.base.sha`, and a blob-filtered bare fetch of the candidate; `tests/test_python_contract.py` is `59239072`'s blob `417f7c0831fdc5a3b4d39ee9ca0edc9e79222291`. Item 5: against `48dc23bd` only the two renamed tests changed; `RulesetComparatorTests`, `test_claude_attribution_override_is_absent` (`:99-100`), the Step 2 test and the module docstring are unchanged, and the file overlaid on `48dc23bd` fails exactly those two of its 9 tests. The module docstring (`:1`, "retiring the hosted identity status") and the class name `RetiredSurfaceTests` are Step 4's to settle, not this step's: Step 3's Files permits only the three inversions, Step 4 inverts the last absence at `:100` and its Tests field fixes the file's end state, and no test or check reads either. Item 6: the metron records carry one argv for both checkers, `python3 scripts/check_commit_identity.py --repository <candidate.git> --base 592390722f10df53658906623b15428dbfb88d8f --head d66356556f6450d13b71e642678c701d7d603f5f --pull-request-login laurenceday`, run from checkouts of `59239072` and `d6635655` whose checker SHA-256s `3d10535274ee7624887c559bfeeb4a59d35b79ef2cc1f6be4b357be4a139d426` and `4ddcade31d8e68ffe02ce665f8ed550e32126cab2df971779a8ef5ca1cb068a6` match the blobs, over one bare, non-shallow, blob-filtered candidate with 19 commits in that range: single runs 676.718 ms and 643.703 ms, exit 0; 41 interleaved pairs, p50 908.056 and 861.732 ms, p95 1067.71 and 939.365 ms, at load averages 14.3 to 20.97; both inside `timeout-minutes: 5`. The implement receipt records both single runs, as the Exit asks. `origin/main` at `485c90d3ad545b696584197f83d942c705988216` still carries `59239072`'s checker, workflow and `scripts/contributors.py` blobs, so the job judging this run's integration pull request runs the checker that accepted these 19 commits. Register: the step changes none of the 30 adopted paths, and a grep of the 28 non-audit ones for identity status, job, workflow, check, gate, script or policy, `check_commit_identity`, `identity.yml`, required status and retire finds no line the restoration makes false; the hits are Fiat's run retirement and the prior demonstration's dated Step 3 history. It re-derives 2 of the 10 conflicted paths, `.horos/boundary.json` (3125 to 3128 files walked) and `.horos/census.json` (`.py` 587 to 589, `.yml` 13 to 14, 3133 to 3136 files), both current. All 20 `PROMISE_MACHINE.md` blobs equal `59239072`'s. `hexctl.py` is unchanged; `tests.test_contributors.HostSetParity` and `dead-code-suppressions-check` pass. The prover exits 3 under the default keyring ("this keyring validates GitHub web-flow key B5690EEEBB952194") and 0 under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, its report differing from `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09` only in `command`'s `--out` path. `48dc23bd..d6635655` changes no preserved record, `59239072..d6635655` modifies or deletes none, and the prior run's study, runbook, demonstration and both audit files are blob-identical to `d9114b65`. `SHOGGOTH.md` is `8fc95b7e33d0dddb07671a641cebaadc17afe977ea3afcb1c419b602304aad4a` at `contract=shoggoth-collective/v5`, pinned at `tests/test_shoggoth_identity.py:11`. ambiguity-loss: `:213` and `:265` go deliberately, and `test_a_lookalike_shoggoth_identity_is_accepted` pins acceptance as author, committer and co-author; the draft still has no line matching ambigu, lookalike or impersonat, which Step 5's Exit owes. Read-only at 2026-09-14T16:11:42Z, ruleset 21830871 `Required CI` has enforcement `evaluate`, required context `invariants` (15368) only, no bypass actors and `updated_at` 2026-09-07T13:32:32.092+01:00, and classic protection on `main` requires `invariants` (strict) with `enforce_admins` on and `required_signatures` off, as in Step 1 round 1; no rule requires `identity`, so the restored status is advisory, and nothing was changed. `.claude/settings.json` is absent while `INSTALL.md:109` documents it and `tests/test_ruleset_identity_retirement.py:100` asserts its absence, the intermediate state Step 4 repairs. enforcement-gap: the restored checker reads no signature and publishes a status no rule requires; the #1514 statement is Step 6's. `59239072..d6635655` holds 19 commits, all `G B83B60AE16F5DD1A`; `d6635655` has author Shoggoth, committer Dr Laurence E. Day and no GitHub key. Later leads: Step 4's Exit proves `INSTALL.md` with `git diff --no-renames 592390722f10df53658906623b15428dbfb88d8f -- .claude/settings.json INSTALL.md` printing nothing, but `INSTALL.md` is adopted at `d9114b65`'s bytes, which replace `59239072`'s `INSTALL.md:125-133` with `:125-128`, so that command prints the hunk whatever Step 4 does, and reverting the paragraph would reinstate the runtime-host refusal and the ADR-016 citation. The proposed Step 4 amendment proves `.claude/settings.json` against `59239072` and `INSTALL.md` against `d9114b653772dd6ba09a81d7af8ccc1943f23042`, and carries the red-base Entry and Exit clauses and the `.horos/candidates.json` Files clause of Step 3's amendment. Renaming `RetiredSurfaceTests` would orphan the study's and runbook's references to it, so at most the module docstring is worth restating there. The Step 5 draft still says to retire the `identity` job and its checker (`docs/decisions/drafts/accept-any-validly-signed-authorship.md:53-56`), rejects retaining a structural identity job (`:64-66`) and has the workflow, checker and required status removed (`:89-92`), all false after this step; `:20-21` describes the job before the change, which is still true of `main`; Step 5's rewrite owns all four. `docs/signature-only-authorship/runbook.md` is still a 36480-byte prefix of the controller runbook carrying 3 of its 5 amendments (Step 2 round 1's lead). Study section 8 question 2 has the restoring step read the status back on its own pull request, but no step pull request can produce that status, and Step 6's Exit already treats the readback as an integration observation. The implement commit message says the host-set comparison's subject is gone from both files; `hexctl.py` and `scripts/contributors.py` still declare the sets, and only the checker's import went.

## Step 4, round 1 -- 2026-09-14T17:41:24Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=reviewed; checker-narrowing=reviewed; signature-gate=reviewed; promise-overreach=reviewed; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=reviewed; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=reviewed

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. The Hexaemeron runner was not re-run on `ce5173fc15a75b1fd4c61fe4b7749b197b43cc59`; its diff from `423f8c80129d8a8bfcffe825abf0bc52a124181b` touches `tests/test_host_settings.py` and `.horos/census.json`, and no Python file under `plugins/hexaemeron/tests` names `census.json`, `.horos/` or `test_host_settings`. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and `python3 scripts/contributors.py --check` (live GitHub data; Step 2 round 1 recorded its same-day exit 2) were not read. No `identity` status was read back: step pull requests target the run branch and `.github/workflows/identity.yml` filters `branches: [main]`. No Claude Code session was observed reading `.claude/settings.json`, so nothing here shows what the host does with the file or with a non-string value. The pre-commit gate did not run for `423f8c80129d8a8bfcffe825abf0bc52a124181b`, `ce5173fc15a75b1fd4c61fe4b7749b197b43cc59` or this record commit, each written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: inconclusive

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S4-R1-01 | low | tests/test_host_settings.py:29 | The narrowed test pins both key sets and the `sessionUrl` value but not the types of `commit` and `pr`, so it passes a settings document with `commit` set to `{"hooks": {"PreToolUse": []}}` or `0`, or with `pr` set to `null`; the `59239072` test refused all three. Its docstring says the module pins "the file's shape", and the Exit says the test "asserts the file's shape". The settings reference quoted at `docs/fiat-host-byline-readback/study.md:493-494` types `attribution` as an object with `commit` and `pr` strings and a `sessionUrl` Boolean, and that study's control on this boundary (`:1000-1005`) is one object with three scalar values and a root test that refuses any other key, which a nested value now passes. | fixed in `ce5173fc15a75b1fd4c61fe4b7749b197b43cc59`: both values are asserted to be strings and their text stays unpinned |

Leads not pursued: Suites ran on clean detached snapshots with `NO_COLOR=1`. At `423f8c80129d8a8bfcffe825abf0bc52a124181b`: root runner `Ran 1861`, 1831/1861, the same 30 bad ids as `59239072` (`comm` empty both ways once subtest parameter suffixes are dropped; `tests.test_agent_instruction` 27, `tests.test_agent_instruction_corpus` 2, `tests.test_skills_sh_package` 1); Hexaemeron runner 2484 run, 1 failure and 0 errors (`test_audit_synopsis_recovery`), among `59239072`'s 4; `scripts/run_checks.py --base 7f63db205e32e8c158ad236c2e1dbac650dc3e39 --format json` red, 8 of 9 selected checks passed (`dead-code-suite`, `dead-code-suppressions-check`, `demonstrations-suite`, `front-door-check`, `joined-front-door-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax`), root-suite ran 1861 (19 failures, 11 errors, 1 skip) with the same 30 ids, and `hexaemeron-suite` was not selected. The implement receipt's `run_checks` evidence came from the run worktree, not the detached snapshot the amended Exit names; this run replaces it with the same outcome. At `ce5173fc15a75b1fd4c61fe4b7749b197b43cc59` the same `run_checks` command gives the same result (8 of 9 passed; root-suite 1861 run, 19 failures, 11 errors, 1 skip, the same 30 ids), and its one skip is, as Step 3 round 1 traced, `tests.test_commit_gate.ActivationTests.test_this_checkout_has_the_gate_activated`, which skips under `WILDCAT_CHECK_CONTAINMENT` and passes without it; the direct root runner reports no skip. On that snapshot `tests.test_host_settings` and `tests.test_ruleset_identity_retirement` run 12 OK, `horos.py check .` 0, `audit_synopsis.py --check .` 0 (80 pairs match), `git diff --check` 0 over `7f63db20..ce5173fc`, both horos scans leave the tree unchanged, `promise_machine.py check` 0 (18 copies), `coverage --check` 0 (98 rows), `portable_promise_machine.py check` 1 before its sync (runtime mirror absent) and 0 after it, and `contributors.py --verify-host-set` 0. Lints from plugin root 1.6.36 on that snapshot over the five paths of `git diff --no-renames --name-only --diff-filter=d 7f63db205e32e8c158ad236c2e1dbac650dc3e39 ce5173fc15a75b1fd4c61fe4b7749b197b43cc59` (`.claude/settings.json`, `.horos/boundary.json`, `.horos/census.json`, `tests/test_host_settings.py`, `tests/test_ruleset_identity_retirement.py`): phylax 0, ephoros 0, hypomnema 0. Item 1: `.claude/settings.json` is blob `b7f9b0a6294ff7c4771ffbf85d8c514176cf25c3` at `59239072` and here, and `INSTALL.md` is `d9114b65`'s blob `905bf083e8a3321dbdf0f05eb48e12f3c853d471`, so both amended Exit diffs print nothing; `INSTALL.md:109` reads "The repository carries `.claude/settings.json` with one object:" and `:112` shows the object the file parses to. Item 2: the dict and key-set assertions are shape, and the kept ask-first sentence holds (`docs/fiat-host-byline-readback/study.md:630-633` puts any key other than `attribution` on the ask-first tier); the trailing-newline case is file shape and rests on no refusal. Dropping the `commit` and `pr` value pins follows the Exit's "only" and `INSTALL.md:125` ("optional presentation preferences, not Fiat policy"): a changed value now leaves the suite green while `INSTALL.md:112`, `:117-120` and `docs/how-to-help-shoggoth.md:218` go false, and a value pin would take an amendment. The narrowing also dropped the two value types (S4-R1-01). Probe, loading each test module against a temporary settings file: the head test accepts `commit` `{"hooks": {"PreToolUse": []}}`, `commit` `0` and `pr` `null`; the `59239072` test and the fix refuse all three; the fix still accepts the committed document and a non-empty `commit` string; head, base and fix each refuse an extra top-level key and `sessionUrl` `0`. Elenchus ran the Tests field's runner (`python3 tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.elenchus/fiat-1135-step-4.json`) on `ce5173fc`: `inconclusive`, "the runner report records an infrastructure error", 1861 executed, 20 assertion failures, 11 errors, 0 skipped. One of the 20 is `tests.test_demonstrations.HorosCensusCurrencyTests.test_the_committed_census_matches_a_fresh_scan`, outside the base set, because the overlaid test file is 373 bytes longer than the parent's census records (`.py` 13061112 against 13061485). A fix to a test's own assertions cannot fail on its parent, so the probe is the guard evidence. Item 3: the docstring drops the refusal claim and the ADR-016 citation, and its other claims hold: `INSTALL.md:109` and `:115` for the shared project setting, `:125` for the preferences, and `verify_local_commit` (`hexctl.py:12679-12724`) reads only the native signature while `hexctl.py` has 0 matches for `is_host_identity`, `HOST_BYLINE_RE`, `Wildcat-Origin` and `runtime host`. "Whatever its bylines say" does not reach co-author trailers: `message_coauthors` (`:12386-12408`) still refuses a malformed co-author identity or more than 32 trailers in the GitHub attribution read at `done push` and at a `done merge-step` push repair. The base's `skills#617` pointer left with the withdrawn sentence, so "the study that added the file" names no path; that study is `docs/fiat-host-byline-readback/study.md`. Item 4: against `7f63db20` the file changes only `:1` and `:99-100`, `RulesetComparatorTests` (`:63-91`) is untouched, and the file asserts the four presences at `:95-97`, `:99-100`, `:102-111` and `:113-115`. The new docstring is accurate: `assert_identity_only_delta` (`:33-60`) accepts only the removal of `{"context": "identity", "integration_id": 15368}` from ruleset `21830871`'s required checks, and the four presence tests cover surfaces that retirement kept. The class name `RetiredSurfaceTests` now labels four presences; Step 3 round 1 left renaming out. `:115`'s `assertIn` holds while either `"identity.yml"` row (`tests/test_python_contract.py:37`, `:48`) remains. Item 5: on a detached `7f63db20` with both head test files overlaid, the two modules run 12 tests: 2 failures (`test_the_file_holds_one_attribution_object`, `test_claude_attribution_override_is_present`) and 2 errors (`test_session_url_is_off`, `test_the_file_ends_with_one_newline`, each a `FileNotFoundError` on `.claude/settings.json`); the other 8 pass. Item 6: in `tests/`, `plugins/*/tests/`, `scripts/`, `INSTALL.md`, `README.md` and `AGENTS.md`, `.claude/settings.json` appears only at `INSTALL.md:109`, `tests/test_host_settings.py:3` and `:19`, and `tests/test_ruleset_identity_retirement.py:100`, and nothing asserts its absence. The ADR-016 citations left in that scope concern the ranking, not the settings file: `scripts/contributors.py:330`, which Step 2 round 1 found still true, `:745`, Step 2 round 1's lead for Step 5, and `tests/test_contributors.py:341`, which asserts that a decision record cites ADR-016. `docs/shoggoth-signature-only-retirement-demonstration.md:78` records the prior run's deletion of `tests.test_host_settings` as history of `155abea7`. Item 7: at `59239072`, `INSTALL.md:125-126` said the effect of `sessionUrl: false` "has not been observed here in a live cloud session" and `docs/how-to-help-shoggoth.md:230` said "The cloud-link behaviour has not been observed as proved"; the adopted bytes of both files drop that unknown while describing a file that is present again, and no remaining step's Files names either file, so Step 5's reading of the settings file is the nearest home. Step 5's Exit has the prior study and runbook amendments name two reversals; this step also restores what the prior run's Step 3 deleted under the condition at `docs/shoggoth-signature-only-retirement-runbook.md:47`, which study section 12 reads as unmet, and Step 5's Exit places that reading only in the draft. No record yet says why `sessionUrl` alone keeps a value pin; study section 12 item 5's third reason is the nearest. Register: the step changes none of the 30 adopted paths; `.horos/boundary.json`, `.horos/candidates.json` and `.horos/census.json` are current, since both scans leave the tree unchanged, and the five paths carry no conflict marker. `hexctl.py`, `scripts/check_commit_identity.py` (34 `raise Refusal`, 0 `contributors` imports), `scripts/contributors.py`, `.github/workflows/identity.yml`, `tests/test_python_contract.py` and the draft are blob-identical to `d66356556f6450d13b71e642678c701d7d603f5f`; the draft still has no line matching ambigu, lookalike or impersonat, and `tests/test_commit_identity.py:428` pins lookalike acceptance. All 20 `PROMISE_MACHINE.md` blobs equal `59239072`'s. The prover exits 3 under the default keyring ("this keyring validates GitHub web-flow key B5690EEEBB952194") and 0 under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, its report differing from `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09` only in `command`'s `--out` path. `7f63db20..ce5173fc` changes no preserved record, and the prior run's study, runbook, demonstration and both audit files are blob-identical to `d9114b65`. `SHOGGOTH.md` is `8fc95b7e33d0dddb07671a641cebaadc17afe977ea3afcb1c419b602304aad4a` at `contract=shoggoth-collective/v5`, pinned at `tests/test_shoggoth_identity.py:11`. Read-only at 2026-09-14T17:27:50Z, ruleset 21830871 `Required CI` has enforcement `evaluate`, required context `invariants` (15368) only, no bypass actors and `updated_at` 2026-09-07T13:32:32.092+01:00, and classic protection on `main` requires `invariants` (strict) with `enforce_admins` on, `required_signatures` off and force pushes and deletions off, as in Step 3 round 1; nothing was changed. `ce5173fc` regenerates the census after its only tracked edit (`.py` 13061112 to 13061485 bytes, 104202938 to 104203311 in total), and `demonstrations-suite`, which holds `HorosCensusCurrencyTests`, passes at both heads. enforcement-gap: the settings file only shapes host attribution text and checks no commit. Phylax: `docs/fiat-host-byline-readback/study.md:1000-1005` treats the settings file as a boundary where Step 4's Disciplines reads "phylax: none"; S4-R1-01 restores the scalar half of that study's control. `59239072..ce5173fc` holds 22 commits, all `G B83B60AE16F5DD1A`, none signed with `B5690EEEBB952194` or `4AEE18F83AFDEB23`; `423f8c80` and `ce5173fc` have author Shoggoth and committer Dr Laurence E. Day.

## Step 4, round 2 -- 2026-09-14T18:10:46Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=reviewed; checker-narrowing=reviewed; signature-gate=reviewed; promise-overreach=reviewed; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=reviewed; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=reviewed

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. Neither test runner nor `scripts/run_checks.py` was re-run for this round; the runs cited below were taken on `4253b3ae5aee5973f197e4bdd19f01661a0e0523`, the tree this round audits. There is no fixes commit, so Elenchus did not run. The cause of the one out-of-set root result below was not established. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and `python3 scripts/contributors.py --check` (live GitHub data) were not read, and no `identity` status was read back. The live settings reference was not read in full: `https://code.claude.com/docs/en/settings-reference` and its `.md` form returned only the index table, which lists `attribution`, `attribution.commit`, `attribution.pr` and `attribution.sessionUrl` with scope `Any file`, so the string types the fix's comment cites rest on the 2026-08-26 quote at `docs/fiat-host-byline-readback/study.md:493-494`. No Claude Code session was observed reading `.claude/settings.json`. The pre-commit gate did not run for this record commit, written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own root suite and `.githooks/greenlight` cannot record a green.

Elenchus verdict: null

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| -- | -- | -- | none | -- |

Leads not pursued: S4-R1-01 is fixed in `ce5173fc15a75b1fd4c61fe4b7749b197b43cc59`. At `4253b3ae`, `tests/test_host_settings.py` is still blob `49971c194344476b3deab739b73bde6cef619266`. The fix adds a reason comment and two `assertIsInstance(..., str)` checks at `:30-34`, after the attribution key-set assertion, so both run only once both key sets match; the module keeps its three test ids, no line exceeds 88 characters, and no tracked file pins the file's SHA-256 or blob. The checks pin the types of `commit` and `pr`, not their text, which fits the Exit's "the file's shape and its `sessionUrl` setting only", and they rest on the settings reference's types rather than on a withdrawn refusal, which fits the Tests field. Rerun on this blob, the probe refuses `commit` `{"hooks": {"PreToolUse": []}}`, `commit` `0` and `pr` `null`, accepts the committed document and a non-empty `commit` string, and still refuses an extra top-level key and `sessionUrl` `0`. `ce5173fc` changes only that file and `.horos/census.json`, and `ce5173fc..4253b3ae` changes only the audit log, its synopsis, `.horos/candidates.json` and `.horos/census.json`. Carried leads: at `59239072`, `INSTALL.md:125-126` and `docs/how-to-help-shoggoth.md:230` said the `sessionUrl: false` cloud-link effect had not been observed, and the adopted bytes of both files drop that unknown while describing a file that is present again; this step undoes the prior run's Step 3 deletion, made under the condition at `docs/shoggoth-signature-only-retirement-runbook.md:47`, while Step 5's Exit has the prior study and runbook amendments name two reversals; and no record says why `sessionUrl` alone keeps a value pin, study section 12 item 5's third reason being the nearest. The orchestrator is taking round 1's two Step 5 suggestions into Step 5's amendment: a draft clause stating what the test pins and that the `sessionUrl: false` effect is documented but unobserved, and an amendment clause naming this restoration. That answers the first two leads; the proposed draft clause says the test pins the `sessionUrl` value, not why that value alone is pinned. Suites for this tree ran on clean detached snapshots of `4253b3ae` with `NO_COLOR=1`: Hexaemeron runner 2484 run, 1 failure and 0 errors (`test_audit_synopsis_recovery`), a base id; `scripts/run_checks.py --base 7f63db205e32e8c158ad236c2e1dbac650dc3e39 --format json` red, 8 of 9 selected checks passed, root-suite 1861 run (19 failures, 11 errors, 1 skip) with exactly the base 30 ids. The direct root runner's first run, from about 17:50:16Z to 17:53:34Z, gave `Ran 1861`, 1830/1861 and `FAILED (failures=20, errors=11)`: the base 30 plus `tests.test_demonstrations.RunnerExecutionBoundaryTests.test_a_grandchild_that_leaves_the_process_group_is_refused`, which failed `self.assertEqual(code, 2)` at `tests/test_demonstrations.py:1778` with `0 != 2`; the one-minute load average was 12.75 just before that run and 8.36 just after it. Run alone five times on the same snapshot between 17:54Z and 17:55Z, that test passed each time (`Ran 1 test` in 10.035 to 10.044 s, one-minute load average 7.24 to 8.51). A second full root run, from about 17:55:11Z to 17:59:28Z, gave `Ran 1861`, 1831/1861 and `FAILED (failures=19, errors=11)` with exactly the base 30 ids, and the one-minute load average was 17.62 just after it. `tests/test_demonstrations.py` and `scripts/demonstrations.py`, the runner it drives, are blob-identical at `59239072`, `7f63db20` and `4253b3ae`, and the test's escaped-process-group shape matches the Hexaemeron base id `test_metron_check.TimeCommandTests.test_a_grandchild_that_leaves_the_process_group_is_refused`, whose class the runbook amendments record as failing under host load; a timing cause is inferred, not shown, since the second full run passed at a higher load. Lints from plugin root 1.6.36 on a clean snapshot of `4253b3ae` over the eight paths of `git diff --no-renames --name-only --diff-filter=d 7f63db205e32e8c158ad236c2e1dbac650dc3e39 4253b3ae5aee5973f197e4bdd19f01661a0e0523` (`.claude/settings.json`, `.horos/boundary.json`, `.horos/candidates.json`, `.horos/census.json`, `audit/rounds/fiat-1135-retire-the-shoggoth-cosignature-and-host-au.md`, its synopsis, `tests/test_host_settings.py`, `tests/test_ruleset_identity_retirement.py`): phylax 0, ephoros 0, hypomnema 0. On that snapshot `horos.py check .` 0, `audit_synopsis.py --check .` 0 (80 pairs match), `git diff --check` 0 over `7f63db20..4253b3ae`, both horos scans leave the tree unchanged, `tests.test_host_settings` and `tests.test_ruleset_identity_retirement` run 12 OK, `promise_machine.py check` 0 (18 copies), `coverage --check` 0 (98 rows), `contributors.py --verify-host-set` 0, and `portable_promise_machine.py check` 1 before its sync (runtime mirror absent) and 0 after it. The prover exits 3 under the default keyring ("this keyring validates GitHub web-flow key B5690EEEBB952194") and 0 under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A`, its report differing from `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09` only in `command`'s `--out` path. Register: outside the audit log, its synopsis and the two `.horos` files, every path the register names is blob-identical to `ce5173fc`, where round 1 checked it. Read-only at 2026-09-14T18:07:18Z, ruleset 21830871 `Required CI` has enforcement `evaluate`, required context `invariants` (15368) only, no bypass actors and `updated_at` 2026-09-07T13:32:32.092+01:00, and classic protection on `main` requires `invariants` (strict) with `enforce_admins` on, `required_signatures` off and force pushes and deletions off, as in round 1; nothing was changed. `59239072..4253b3ae` holds 23 commits, all `G B83B60AE16F5DD1A`, none signed with `B5690EEEBB952194` or `4AEE18F83AFDEB23`; `ce5173fc` and `4253b3ae` have author Shoggoth and committer Dr Laurence E. Day.

## Step 5, round 1 -- 2026-09-14T21:33:38Z

Audit schema: fiat-audit-round/v2

Covered: salvage-drift=reviewed; merge-resolution=reviewed; residual-refusal=reviewed; promise-bytes=reviewed; parity-anchor-dead=reviewed; checker-narrowing=reviewed; signature-gate=reviewed; promise-overreach=reviewed; preserved-record-drift=reviewed; record-append-only=reviewed; shoggoth-digest=reviewed; ambiguity-loss=reviewed; live-configuration=reviewed; settings-file=reviewed; census-currency=reviewed; enforcement-gap=reviewed

Not checked: x-ray, solidity-auditor and fizz did not run (security_suite waiver: no Solidity). Checks run on the commit that carries this record are outside this record. The two test runners ran on `5b1e061fedb10290dfe3dcd06f2f58d4d33334c2`; on the fix commit `d8ec620a75bd55dd404dffe2c21d8bd68049fdc5` the root and Hexaemeron suites ran only inside `scripts/run_checks.py`. Organisation rulesets (study assumption 5: no `admin:org`), the external Interceptor repository and `python3 scripts/contributors.py --check` (live GitHub data) were not read, no `identity` status was read back, and the live settings reference was not re-read. The prior run's controller state was not found, so the order of that run's Step 1 receipts and its 2026-09-06 amendments is inferred. The pre-commit gate did not run for `d8ec620a75bd55dd404dffe2c21d8bd68049fdc5` or for this record commit, both written with `git commit-tree`, because base `592390722f10df53658906623b15428dbfb88d8f` is red on its own suites and `.githooks/greenlight` cannot record a green.

Elenchus verdict: unguarded

| id | severity | file | finding | status |
| --- | --- | --- | --- | --- |
| S5-R1-01 | low | docs/decisions/drafts/accept-any-validly-signed-authorship.md:15 | The Status says ADR-052 "is not withdrawn either" and, in the same sentence, that "its refusal of a runtime host in those roles is the ban this record withdraws"; "those roles" has no antecedent in this record, and the sentence before names only ADR-016's and ADR-058's paragraphs as superseded, so it does not say clearly which parts of ADR-052 survive. Both halves match ADR-052: `docs/decisions/ADR-052-separate-governed-authorship-from-publication.md:30-34` records authorship and publication separately, names the author fields and the publisher's committer identity, signing key and repository account, and ends "A runtime host is accepted in none of those roles"; `:36-37` has Fiat reject a known runtime host as author or committer, and `:75-76` rejects a known-host committer. | fixed in `d8ec620a75bd55dd404dffe2c21d8bd68049fdc5`: ADR-052 stays in force except for its runtime-host clauses, which the Status quotes and names |
| S5-R1-02 | low | docs/decisions/drafts/accept-any-validly-signed-authorship.md:80 | "This run adopts 30 at the branch's bytes and reworks 43" is not true of the tree it ships in. `.hexaemeron/design-probe.py --candidate retain-declaration --surface` lists 73 changed, 43 reworked and 30 adopted; at `5b1e061fedb10290dfe3dcd06f2f58d4d33334c2` the 22 reverted paths equal `59239072`'s blobs and the 11 rewritten differ from both sides, but only 26 of the 30 adopted paths equal `d9114b653772dd6ba09a81d7af8ccc1943f23042`'s blobs. `AGENTS.md`, `README.md` and `plugins/hexaemeron/AGENTS.md` are the conflict-free `git merge-file` of `3cc0ad7f`, `59239072` and `d9114b65`, unchanged since `928884056101bdc2b739c12bdbc2441b6647e30a`, and `5b1e061f` changed `tests/test_evolution_contract.py`, as its amended Files field directs. | fixed in `d8ec620a75bd55dd404dffe2c21d8bd68049fdc5`: the figures 73, 30 and 43 stay, and the draft states that 26 keep the branch's bytes and why the other 4 do not |
| S5-R1-03 | low | docs/shoggoth-signature-only-retirement-runbook.md:97 | The new Still holding field says "Steps 1 to 4 keep the evidence their receipts recorded against the fields as first written". The runbook's two 2026-09-06 amendments (`:69-87`) replaced Step 1's Exit and then its Exit and Files while Step 1 was built, the first because the original Brevitas command exited 2 and the second after Step 1's commit gate failed on Horos currency; `40179157`, dated 2026-09-06T22:32:18Z, refreshed the boundary records before Step 1 round 1 at 22:39:03Z. Step 1's receipts were therefore recorded against amended fields (inferred). Steps touched ("Step 3's Disciplines only. No other field of any step changes.") is honest. | fixed in `d8ec620a75bd55dd404dffe2c21d8bd68049fdc5`: the clause reads "against the fields in force when each step was receipted" |
| S5-R1-04 | low | scripts/contributors.py:745 | `scripts/contributors.py:745-747` renders "ADR-016 makes the Shoggoth the author of governed agent work, which is not the same as being a person who helped." into `CONTRIBUTORS.md:25-27`, which the daily refresh overwrites from the generator. This step's draft supersedes ADR-016's Decision paragraph, so the generated page now cites superseded policy as current. Neither file is in Step 5's Files field, Step 6's says "Change no product file", and Step 2 round 1 raised this as a lead for this step. | open: Files amendment or integration carryover; replace the three strings at `scripts/contributors.py:745-747`, and the same three lines at `CONTRIBUTORS.md:25-27`, with "Runtime hosts, the repository owner and the Shoggoth are excluded. The", "Shoggoth may author agent work, which is not the same as being a person who" and "helped." |

Leads not pursued: Elenchus ran the Tests field's runner (`python3 tests/run_tests.py --elenchus-report {report}`, format `unittest-json-v1`, report `.elenchus/fiat-1135-step-5.json`, no stale report present) on `d8ec620a`: `unguarded`, "the commit changed no test files"; the fix is prose, and the checks below are its guard. Suites on a clean detached snapshot of `5b1e061f` with `NO_COLOR=1`: root runner `Ran 1861`, 1831/1861, `FAILED (failures=19, errors=11)`, exactly the base 30 ids (`comm` empty both ways; `tests.test_agent_instruction` 27, `tests.test_agent_instruction_corpus` 2, `tests.test_skills_sh_package` 1), 563 s at one-minute load average 27.2 to 18.6; `tests.test_demonstrations.RunnerExecutionBoundaryTests.test_a_grandchild_that_leaves_the_process_group_is_refused` passed there and alone (1 OK, 10.039 s). Hexaemeron runner: 2484 run, 2 failure events, 0 errors (`test_audit_synopsis_recovery` 1, `test_metron_check.TimeCommandTests.test_an_escaped_reader_keeps_its_pipe_descriptors` 1), both base ids, load average 25.1 at its end; `test_metron_check` alone runs 117 OK. `scripts/run_checks.py --base fdad2b661b70076e6e8fa5d96b2c31a13c125909 --format json` on a clean snapshot of `d8ec620a`, from 21:20:36Z at load 6.2: red in 401 s, 11 of 13 selected checks passed (`dead-code-suite`, `dead-code-suppressions-check`, `demonstrations-suite`, `front-door-check`, `harness-roster-check`, `harness-roster-freshness`, `imprimatur-suite`, `joined-front-door-suite`, `lint-ephoros`, `lint-hypomnema`, `lint-phylax`); `root-suite` ran 1861 (19 failures, 11 errors, 1 skip) with the base 30 ids once the `tests.` prefix and subtest suffixes are dropped, and `hexaemeron-suite` ran 2484 with 1 failure event, `test_audit_synopsis_recovery`; neither timed out. On the `5b1e061f` snapshot: `tests.test_decision_records` 5 OK, `tests.test_evolution_contract` 20 OK, `promise_machine.py check` 0 (18 copies), `coverage --check` 0 (98 rows), `horos.py check .` 0 with both scans leaving the tree unchanged, `audit_synopsis.py --check .` 0 (80 pairs match), `git diff --check` 0 over `fdad2b66..5b1e061f`, `contributors.py --verify-host-set` 0, `portable_promise_machine.py check` 1 before its sync (runtime mirror absent) and 0 after it, Imprimatur 0 on the draft, both prior records, `EVOLUTION.md` and `demonstration-evidence.md`, Hypomnema 0 on the Exit's three documents, and Protasis 0 on both prior records; study-mode Hypomnema exits 0 in the run worktree against `.hexaemeron/design-evidence.json` (`7739586e`). On the `d8ec620a` snapshot both test modules, that Hypomnema walk, Imprimatur on the draft and runbook, Protasis on both prior records, `horos.py check .`, `audit_synopsis.py --check .` and `git diff --check` over `fdad2b66..d8ec620a` exit 0 again, the ADR-016, ADR-019, ADR-052 and ADR-058 diffs against `59239072` print nothing, and the prior records still add 34 and 10 lines with 0 deletions against `fdad2b66` and `d9114b65`. Lints from plugin root 1.6.36 on that snapshot over the 12 paths of `git diff --no-renames --name-only --diff-filter=d fdad2b661b70076e6e8fa5d96b2c31a13c125909 d8ec620a75bd55dd404dffe2c21d8bd68049fdc5` (`.horos/candidates.json`, `.horos/census.json`, the draft, `demonstration-evidence.md`, `demonstration-run.json`, `evaluation-run.json`, both prior records, the Fiat `EVOLUTION.md`, `fiat-final-integration.json`, `tests/promise_machine_coverage.json`, `tests/test_evolution_contract.py`): phylax 0, ephoros 0, hypomnema 0. Item 1: the draft states the Exit's seven items in order, the supersession in its Status (`:7-18`) and the rest in its Decision at `:66-75`, `:77-95`, `:97-109`, `:111-128`, `:130-142` and `:144-156`. The probe's 73, 43 and 30 hold, and the 10, 22 and 11 hold by blob (S5-R1-02). The reconstruction holds: the first six commits (`5878bebd` to `e23cfbcc`) keep their ids and carry both trailers; `9a0a1bde`, `fd653aaf`, `27a9622a`, `155abea7`, `843d232d` and `0314fd93` carry neither, and each rebuilt commit keeps its original's tree, author, author date and message with the two trailers appended; the web-flow merges `8d3adee8`, `32caa838`, `d1f33d61` and `d9114b65` (`U B5690EEEBB952194`) are each tree-identical to the head they merged; `89103fce` has `d9114b65`'s tree `74f95006`, `d9114b65` is not an ancestor of `5b1e061f`, and the map in `92888405` names all four ids the adopted records cite. `scripts/contributors.py:191` is `def verify_host_set_parity`, `PROMISE_MACHINE.md:344` is the ranking promise's `- Promise:` line, `hexctl.py:12261-12266` gives the reason, and each set name occurs once in `hexctl.py`. The checker has 34 `raise Refusal` (42 at base), no `contributors` import (base `:15`, used at `:214`, `:258` and `:306`), neither casefold lookalike test (base `:207-213`, `:260-265`), no `shoggoth_author_count` or `human_author_count` (base `:332-333`), the same `SCHEMA`, equal ceilings and the docstring the draft restates; `tests/test_commit_identity.py:428-440` accepts a lookalike author, committer and co-author. `tests/test_host_settings.py:23-44` pins what the draft says, plus a trailing-newline case, and `INSTALL.md:121-123` and the 2026-08-26 quote at `docs/fiat-host-byline-readback/study.md:506-509` document the `sessionUrl` effect. The web-flow refusal is `verify_local_commit`'s `GITHUB_SIGNING_KEYS` branch (`hexctl.py:12705-12717`), reached only after `git verify-commit` fails, and its message says not to import GitHub's public key. The prover exits 3 under the default keyring ("this keyring validates GitHub web-flow key B5690EEEBB952194") and 0 under a `GNUPGHOME` holding only `3BCD9EFDA6670A3F65AF679EB83B60AE16F5DD1A` and its encryption subkey, its report differing from `323975dda5a3d7f046bc0d40dad45672aa2d2119d0bd75ae6fcdb0d115e6da09` only in `command`'s `--out` path. The heading, slug and unnumbered path stay, and `59239072..d8ec620a` changes no numbered record. Item 2: S5-R1-01; the fix leaves the supersession of ADR-016's and ADR-058's paragraphs as it was. Item 3: both halves hold. `plugins/hexaemeron/skills/fiat/SKILL.md:266`, `AGENTS.md:168`, `SHOGGOTH.md:77` and `plugins/hexaemeron/AGENTS.md:135` give local access and an authenticated connector equal standing for the same checked fields. In `hexctl.py` the only `gh` argv is `github_rest`'s `bounded_probe(base_dir, "gh", ["api", "--method", "GET", path])` (`:12213-12215`), with six callers and no other GitHub API client; the other `gh` strings are operator commands and messages (`:5813`, `:9675`, `:13143`), and `checked_github_object`'s docstring (`:12190-12191`) grants a connector a standing the controller never exercises. Item 4: against both `fdad2b66` and `d9114b65` the prior study gains 34 lines and the prior runbook 10, with 0 deletions; each `d9114b65` blob is an exact byte prefix of its file, and each file gains one amendment dated 2026-09-14 that names the parity-anchor reversal, the return of the identity checker and workflow, and the restoration of `.claude/settings.json` and `tests/test_host_settings.py`, and cites `adr/accept-any-validly-signed-authorship`. The parity gate went in `e23cfbcc` (the prior Step 2), the checker, its test, the workflow, the settings file and its test in `27a9622a` (the prior Step 3), and `.claude/settings.json` is blob `b7f9b0a6` both before `27a9622a` and here. The runbook's `Complete replacement Disciplines:` keeps Step 3's phylax, ephoros, metron and elenchus clauses byte-identical and replaces a hypomnema clause that pointed at a claim the revised draft dropped; Protasis passes both files, and a scratch copy with a prose `What changed` fails P005 at `:91`. That form meets the Exit, and its Steps touched is honest; its Still holding was not (S5-R1-03). Item 5: against `fdad2b66` only the Change cell of `EVOLUTION.md:79` changed; its version token, axis, revision, digest and evidence and every earlier row are unchanged, and no added line carries a new version token. Each clause of the new cell holds: local receipts verify signatures, `require_github_verified` (`hexctl.py:13201-13216`) serves `done_push`, `done_merge_step`, `done_sync_run` and `done_integrate`, the sets have no load, the web-flow branch is unchanged, and the connector clause is item 3; `tests/test_evolution_contract.py:371-373` asserts three of its phrases. Item 6: the `EVOLUTION.md` SHA-256 `aae080fd` fills the 4 pins in `fiat-final-integration.json`, whose SHA-256 `415acf4d` fills the 2 in `tests/promise_machine_coverage.json`; the coverage file's `60e05e96` and `evaluation-run.json`'s `6bc300fb` appear once each in both demonstration files, byte counts 260329 and 3231 unchanged. `evaluation-run.json` changes only `tree_sha256` (`d669dbc3` to `a841f927`): `emit` gives `a841f927`, `tally` with the recorded model and date 2026-08-31 reproduces the file byte for byte, and `verify` exits 0 (11 cases, 55 passed). No SHA-256 or blob id of any of the 12 parent files remains in the head tree, and nothing pins the draft, the prior records, the test file, the demonstration files or the `.horos` files. Item 7: S5-R1-04. The tracked `docs/signature-only-authorship/runbook.md` (`6033b22a`, 36480 bytes) is a byte prefix of `.hexaemeron/runbook.md` (`c0e1bb51`, 57695 bytes) with 3 of its 7 amendments, while the study copy equals `.hexaemeron/study.md` (`a53c28e8`), whose section 4 the draft cites; re-copying both is left to Step 6's amendment. Other leads: ADR-016's Decision has three paragraphs; its first (`docs/decisions/ADR-016-attribute-governed-agent-work-to-shoggoth.md:28-32`) holds the host authorship ban, its Status (`:8-10`) keeps "The authorship decision" in force and hands the third to ADR-052, and the draft, like the Exit, names "ADR-016's Decision paragraph" without a position while it locates ADR-058's. ADR-058's fourth Decision paragraph (`:49-52`) and its Consequences (`:79-82`) still treat `identity` as a required signal; the draft calls the status advisory but lists only ADR-058's second paragraph as superseded. The draft's `single-owner` alternative says it "satisfies 3 of 4 answers and fails the parity gate", while `.hexaemeron/design-evidence.json` fails it on both gates (3 against at-least 4). The Fiat row's evidence cell still links the prior run's study and runbook rather than `docs/signature-only-authorship/`, and the Files field limits the edit to the Change cell. The prior study says historical studies and runbooks "remain byte-identical" (`docs/shoggoth-signature-only-retirement-study.md:404-406`), which bears on study assumption 7, the reading that permits both amendments. Read-only at 2026-09-14T21:04:23Z, ruleset 21830871 `Required CI` has enforcement `evaluate`, required context `invariants` (15368) only, no bypass actors and `updated_at` 2026-09-07T13:32:32.092+01:00; classic protection on `main` requires `invariants` (strict) with `enforce_admins` on, `required_signatures` off, force pushes and deletions off and no `required_pull_request_reviews`; the repository's ruleset list holds only 21830871, and `rules/branches/main` returns none. No rule readable here requires the approving review ADR-058 sets (`:54-55`), which the draft says is not withdrawn; nothing was changed. Register: the step changes one adopted path, `tests/test_evolution_contract.py`, whose new assertions name no withdrawn rule, and the fix changes none; the step re-derives `.horos/candidates.json` and `.horos/census.json` and re-pins `tests/promise_machine_coverage.json`, all current and free of conflict markers. `hexctl.py`, `scripts/check_commit_identity.py`, `scripts/contributors.py`, `tests/test_commit_identity.py` and `.github/workflows/identity.yml` are blob-identical to `d66356556f6450d13b71e642678c701d7d603f5f`; `hexctl.py` has 0 matches for `is_host_identity`, `is_host_login`, `HOST_BYLINE_RE`, `COAUTHOR_TRAILER`, `ORIGIN_TRAILER`, `Wildcat-Origin`, `runtime host` and `runtime-host`, and the checker has no line naming a guard token. All 19 promise copies are byte-identical to `59239072`. `59239072..d8ec620a` modifies or deletes no preserved record, and the prior demonstration and both prior audit files equal `d9114b65`'s blobs. `SHOGGOTH.md` is `8fc95b7e33d0dddb07671a641cebaadc17afe977ea3afcb1c419b602304aad4a` at `contract=shoggoth-collective/v5`, pinned at `tests/test_shoggoth_identity.py:11`. `INSTALL.md:109` and `:112` agree with `.claude/settings.json`. The draft promises nothing about an organisation rule, and the #1514 statement is Step 6's. `59239072..d8ec620a` holds 26 commits, all `G B83B60AE16F5DD1A`, none signed with `B5690EEEBB952194` or `4AEE18F83AFDEB23`; `5b1e061f` and `d8ec620a` have author Shoggoth and committer Dr Laurence E. Day.
