Answer one question from one public synthetic instruction document. This request is a fresh context: use no prior message, example, repository instruction, tool, or outside fact. The candidate ids contain every permitted document-grounded conclusion and evidence refusal without marking the required id. Read the document before selecting one. Read every word of each hyphenated id literally: `refuse-unknown` means refusal because the answer is unknown, not a generic refusal. Choose the one answer id that best matches the document. Return one JSON object with exactly one `answer_id` string and no other key.

Mode: {mode}
Decoder bootstrap:
{decoder_bootstrap}
Document:
{document}
Question: {question}
Candidate answer ids: {answer_ids}
