# specimen coverage

## fiat `SKILL.md:636-675`

- 638-640 -> tape 26; `secsuite.unresolved` is typed as
  `target.solidity && security_suite.unresolved`; `halt.ledger`, `fiat.ask_user`,
  and `stop` remain three distinct effects.
- 644 -> 2. 645-647 -> 3. 648 -> 4. 649 -> 5.
- 650-652 -> 6 plus reachable `git.receipt` definition. local signatures,
  exact provenance multiplicity, pushed-object verification, and merge-SHA
  verification remain separate predicates.
- 653 -> 7-8. 654-658 -> 9-10 plus `git.stack_shape` definition.
- 659-661 -> 11-14. 662-669 -> 15-19.
- 670-672 -> 20-23: status, verify, and handoff capture each precede reset;
  verified terminal state requires reset.
- 673-675 -> 24-25; epoch replacement is guarded by reopening evidence.

no operational clause is intentionally omitted. markdown connective prose and
the heading are non-authoritative render material.

## phylax `SKILL.md:79-132`

- 81-83 -> tape 2-6. 85-87 -> 7. 89-92 -> 8-13.
- 96-103 -> 2 plus the reachable `sec.ingress` definition and tape 14.
- 105-106 -> 15.
- 110-112 -> the `ssrf` causal edge in the `sec.url` module; tape 16 applies
  the control. 114-128 is an illustrative Python realization, kept as a
  content-addressed example and included only in a URL-fetch implementation
  slice. it is not silently deleted.
- 130-132 -> tape 17; DNS rebinding risk selects resolve-once/pinned-connect.

the current demo does not carry the illustrative Python bytes in its base
slice. a comprehension test must decide whether the causal/control graph is
enough for non-implementation review and whether an implementation slice must
load the example.

## sapheneia `SKILL.md:203-222,238-248`

- 205-209 -> tape 8-12.
- 213-219 -> tape 2-6.
- 221-222 -> tape 7 plus precedence record 8.
- 240-247 -> promise record 14. its subject, claim, three evidence inputs, two
  evidence classes, boundary, grants, five refusal families, two recovery
  actions, and consequence 0 are separate fields.

the exact English claim and boundary are replaced by typed propositions. this
is semantic migration, not byte round-trip; source-span review and behavioural
vectors must approve it before authority moves to nir.
