# Runbook: Settle declared study criteria with recorded command results

The opening accepted study is `.hexaemeron/study.md`, SHA-256 `4a572737afa69d9a24c5923a828b68398c3e1bb9d85045b1488d7b8a87e13e92`. Its eight criterion descriptors and selected construction govern these five steps. The source controller remains outside this worktree; use the controller's current directive for every branch, parent and receipt. Initial product commit: `eaab110104d186bdd0b1d362dd583a2e72ecc21a`.

## Operating boundaries

- **Always:** preserve the accepted source and evidence; execute root and Hex suites before a commit; run the checked repository runner and all selected checks; pass the exact staged tree through the normal commit gate; sign commits and verify published identities; preserve append-only audit history; run Sapheneia, Imprimatur, Vulgate and final Imprimatur on new prose; archive each completed step through the native checkpoint command.
- **Ask first:** a dependency, storage/public ABI change, CI change, trust-boundary expansion or released-digest rewrite outside the accepted #1273 design requires a new decision. The declared criterion schema, execution operation, receipts and compatibility changes are already authorised by this run. Ordinary fixes, source repins, package increments and the specified integration work remain inside scope.
- **Never:** fabricate execution or a verdict; change the fixed external controller; weaken command admission; edit controller state or ledger by hand; erase a failing test, frozen evidence or prior audit record; include credentials in an artifact; force-push over another contributor; merge a step directly into main; claim this outer run itself demonstrates its successor.

Several accepted criteria share one step Exit. Execute that command once for that observation. Report files are create-only: preserve a failed report before a deliberate retry, and never overwrite evidence that a receipt already binds. The source controller does not enforce the new criteria contract; the separate proof must identify the target controller it executes.

Run the combined checked runner on the clean committed candidate, so its committed-tree suppression check has the state it requires. The normal staged commit gate and the separate root/Hex runs precede that commit. Preserve each result against the exact tree it checked.

Before any product edit, record the accepted no-known-findings assertion through the current inoculation directive. The source-bound inventory must check against this complete runbook with an independently empty expected id set. The named historical faults remain regression inputs and exclusions; no parent guard result is invented.

The selected design has five pending conformance reports. Each exact resolver comes from the immutable design record. Invoke it as an evidence operation after its owning implementation exists, and retain its real result before the named transition. A resolver is not an admitted outer-run Exit command. Step 1 establishes the stable decision bridge before its prose receipt and before Step 2.

Every step that changes a Hex package byte increments its package version above that step PR's base. Keep the four package surfaces and two live test expectations consistent. Governed skill rows are earned once at delivery: Protasis's completed held job projects `protasis-v6.12.1`, subject to the material-improvement judgement and actual base. Fiat follows the relation below. Neither projection reserves an integration version.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 89938e2d649d73c98298ea70823282ed83d397b2e21c9b79cdd01e35184944f7
candidate | controller-capture
```

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
```

## Step 1: Scaffold the declared contract and decision record

**Goal.** Publish the accepted study, this runbook, design evidence and standing decision, with a proof scaffold that refuses behavior not yet implemented.

**Entry.** The controller's Step 1 branch and parent descend from the signed initial commit `eaab110104d186bdd0b1d362dd583a2e72ecc21a`; the accepted study, design lock and no-known inventory match their receipts, and inoculation has admitted this step.

**Exit.** The committed study and runbook match their current accepted sources; selection evidence replays; the standing draft resolves through the study's stable bridge; the scaffold produces the design-home report and refuses the four future conformance operations. Run `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report .hexaemeron/reports/step-1-exit.json`. Run `python3 scripts/run_checks.py --scope hexaemeron --jobs 26 --report .hexaemeron/reports/step-1-checked.json`. The root scaffold test must execute through the checked runner, so a Hex-only pass cannot establish this step's new root behavior. The selected design-home report is due before Step 2.

**Files.** Create `docs/protasis-success-criteria/study.md`, `docs/protasis-success-criteria/runbook.md`, `docs/protasis-success-criteria/design-evidence.json`, `docs/protasis-success-criteria/selection_probe.py`, `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, the eighteen exact selection-report copies under `docs/protasis-success-criteria/reports/`, and required source/audit/prose provenance copies under `docs/protasis-success-criteria/evidence/`. Publish `docs/decisions/drafts/settle-study-criteria-from-recorded-execution.md` from the prepared decision draft. Add `tests/test_success_criteria_scaffold.py`; update `tests/check-map-v1.json`, `.horos/census.json` and `.horos/boundary.json` when their checked ownership/census requires it. Audit writes are only the controller-named per-run log and its generated synopsis. No plugin source change is needed for this scaffold.

**Tests.** Add meaningful scaffold checks for the exact design-home join, unchanged selected report digests, missing/duplicate decision homes, refusal of unavailable operations and refusal to overwrite an existing report. A conformance report follows an executed check and retains source evidence; no constant success stub is allowed. Expected new test count is determined by cases, not predicted. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-guard.json`. The registered full Hex runner may return inconclusive under Elenchus's process boundary; retain the actual verdict and separately run the owning root tests. Run the explicit Hypomnema study/design bridge after publishing its home.

**Disciplines.** phylax: bound proof input and fresh report creation without accepting invented evidence. ephoros: make every unavailable operation return a named reason. metron: preserve measured selection values and do not turn them into production latency claims. elenchus: preserve any observed failing scaffold case and earn its actual causal verdict. hypomnema: establish the one numberless standing record and stable bridge before shipping the study.

## Step 2: Parse criteria and bind effective Exit commands

**Goal.** Implement the closed criterion declaration and join each descriptor to one effective registered Exit command at its consuming step.

**Entry.** Step 1 is pushed and checkpointed; the selected design-home report has passed at `step:2`; the controller supplies the exact Step 2 parent/branch and admits its no-known inoculation.

**Exit.** The pure loader rejects malformed or ambiguous declarations and returns exact descriptors for valid effective steps, including accepted runbook amendments; target readiness uses it and the target adapter's reviewed source pin matches the changed CLI. Run `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report .hexaemeron/reports/step-2-exit.json`. Run `python3 scripts/run_checks.py --scope hexaemeron --jobs 26 --report .hexaemeron/reports/step-2-checked.json`. This settles the declared-exit-join claim only to the extent the owning tests exercise it. The selected declaration-contract report is due before Step 3.

**Files.** Add `plugins/hexaemeron/skills/protasis/scripts/success_criteria.py` and `plugins/hexaemeron/tests/test_success_criteria.py`; edit `plugins/hexaemeron/skills/protasis/scripts/protasis.py`, `plugins/hexaemeron/skills/protasis/scripts/gate_commands.py`, `plugins/hexaemeron/tests/test_protasis_checker.py`, `plugins/hexaemeron/tests/test_gate_commands.py`, `docs/protasis-success-criteria/proof.py` and `tests/test_success_criteria_scaffold.py`. Keep any new parser specimens under `plugins/hexaemeron/tests/fixtures/success-criteria/`. Update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` for the step's package increment. Regenerate `.agents/skills/promise-machine/runtime/`, `.horos/census.json`, and `.horos/boundary.json` when their owners require it; adjust `tests/check-map-v1.json` only for real ownership changes. Preserve the controller-named audit log and synopsis.

**Tests.** Cover absent versus required declaration, one valid fence, duplicate fences/keys/ids, unknown fields/schema, booleans in integer fields, unsafe/empty values, criterion count and study byte limits, wrong step, Tests-only commands, missing Exit, superseded Exit, duplicate command ambiguity, a valid amended Exit and several criteria sharing one exact Exit. Exercise actual target adapter admission without changing the outer runner or external adapter. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-2-guard.json`. Retain real parent/fixed-tree outcomes and the known full-suite Elenchus limit.

**Disciplines.** phylax: parse untrusted bounded Markdown/JSON and argv without execution or permissive fallback. ephoros: return stable declaration/join refusal reasons. metron: exercise declared count/byte edges without a throughput claim. elenchus: guard malformed and mismatched joins with assertions tied to the defect. hypomnema: document the new schema and error meanings next to the implementation while preserving the accepted decision.

## Step 3: Capture command execution at its consuming step

**Goal.** Add controller-owned execution custody, an immutable activation marker and the due-step evidence gate.

**Entry.** Step 2 is pushed and checkpointed; declaration-contract passes at `step:3`; the controller supplies Step 3's parent/branch and admits its no-known inoculation. The active external controller stays unchanged.

**Exit.** The target controller's new execution operation launches registered argv, captures bounded outcomes on a clean signed implementation commit, settles shared descriptors from one observation, and refuses implementation completion without matching successful evidence. Run `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report .hexaemeron/reports/step-3-exit.json`. Run `python3 scripts/run_checks.py --scope hexaemeron --jobs 26 --report .hexaemeron/reports/step-3-checked.json`. The observed-execution and interrupted-is-unmet claims require actual child cases, not imported reports. The selected execution-custody report is due before Step 4.

**Files.** Add `plugins/hexaemeron/skills/fiat/scripts/criteria_execution.py`, `plugins/hexaemeron/tests/test_fiat_criteria_execution.py` and reusable test fixtures under `plugins/hexaemeron/tests/fixtures/success-criteria/`. Edit `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `plugins/hexaemeron/skills/protasis/scripts/success_criteria.py`, `plugins/hexaemeron/tests/test_hexctl.py`, `docs/protasis-success-criteria/proof.py` and `tests/test_success_criteria_scaffold.py` for integration and proof. Update the four package files `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, plus `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py`. Update exact changed source expectations in `tests/fixtures/promise-machine/runtime/fiat-receipted-delivery.json` and `tests/fixtures/promise-machine/runtime/fiat-known-failure-inoculation.json` when required. Regenerate affected portable copies and Horos files; change `tests/check-map-v1.json` only where ownership changes. Preserve the per-run audit log/synopsis.

**Tests.** Use real disposable child processes to cover successful execution, startup refusal, exit seven, interruption between start/result, timeout, incremental stream overflow, clean-source enforcement and tracked drift after launch. Exercise strict run/init/descriptor/step/argv/CLI/adapter/commit bindings, state-only marker addition/removal, inert operation_ran false, caller-supplied success refusal, one shared observation, explicit retry and 128-criterion/512-attempt/4 MiB data ceilings. Check cap-minus-one, cap and cap-plus-one without waiting for a real 1,800-second test. Preserve raw outcome distinctions and supporting measurement records. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-3-guard.json`. Root and Hex compatibility suites must remain green before this step hands over.

**Disciplines.** phylax: close environment/argv/file inputs and enforce safe incremental stream custody without claiming a sandbox. ephoros: distinguish launch, failure, incomplete attempt, timeout and overflow in bounded result records. metron: observe byte/count/time bounds and preserve actual measurements. elenchus: reproduce causal execution and interruption defects before fixing them. hypomnema: keep the result schema and retry limitations beside their owner, with the existing ADR as the decision home.

## Step 4: Join completion, amendments and legacy replay

**Goal.** Require complete historical execution evidence before integration is authorised and preserve those bindings through amendments, verification and checkpoint restore.

**Entry.** Step 3 is pushed and checkpointed; execution-custody passes at `step:4`; the controller supplies Step 4's parent/branch and admits its no-known inoculation.

**Exit.** The target controller withholds integration for unmet or tampered required results and checks again at completion; plain/JSON inspection launches no command; completed descriptors remain frozen; valid unrelated amendments, legacy runs and checkpoint replay retain their exact historical meaning. Run `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report .hexaemeron/reports/step-4-exit.json`. Run `python3 scripts/run_checks.py --scope hexaemeron --jobs 26 --report .hexaemeron/reports/step-4-checked.json`. The completion-refuses-gaps, amendment-custody, legacy-preserved and replay-preserves-bindings claims owe the stated negative cases. The selected terminal-compatibility report is due before Step 5.

**Files.** Add `plugins/hexaemeron/skills/fiat/scripts/criteria_receipts.py` and `plugins/hexaemeron/tests/test_fiat_criteria_replay.py`. Edit `plugins/hexaemeron/skills/fiat/scripts/hexctl.py`, `plugins/hexaemeron/skills/fiat/scripts/criteria_execution.py`, `plugins/hexaemeron/skills/protasis/scripts/success_criteria.py`, `plugins/hexaemeron/tests/test_hexctl.py`, `plugins/hexaemeron/tests/test_hexctl_checkpoint.py`, `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`, `docs/protasis-success-criteria/proof.py` and `tests/test_success_criteria_scaffold.py`. Update required exact source/contract expectations in `tests/fixtures/promise-machine/runtime/fiat-final-integration.json`, `tests/fixtures/promise-machine/runtime/fiat-controller-checkpoint.json`, `tests/fixtures/promise-machine/runtime/fiat-checkpoint-archive.json`, `tests/fixtures/promise-machine/runtime/fiat-study-amendment.json` and `tests/fixtures/promise-machine/runtime/fiat-runbook-amendment.json`. Update the four package files and two live version expectations named in Step 3; regenerate portable copies and `.horos/census.json`/`.horos/boundary.json`; update `tests/check-map-v1.json` only for actual owner changes. Preserve the per-run audit log/synopsis.

**Tests.** Cover absent, failed, forged, wrong-run, wrong-source, wrong-step and wrong-command evidence before the integration directive and final receipt; reject marker substitution and preserve source-bound legacy states with no backfill. Prove completed claim/step/command/removal/reassignment refusal occurs before mutation; allow unrelated amendments through a checked chain and require new evidence for changed unbuilt descriptors. Exercise duplicate amendment digests, partial write recovery, checkpoint round-trip and wrong-root/result replacement without re-execution. A sentinel must remain unchanged during repeated status/verify/restore reads. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-4-guard.json`. Keep new test modules below existing source-read ceilings.

**Disciplines.** phylax: validate durable identity, strict file reads and restored relative storage before trusting results. ephoros: expose each unmet reason and immutable historical source in plain and JSON readback. metron: bound added replay data and demonstrate inspection does not add executions. elenchus: guard mutation, replay and completion bypasses with exact negative specimens. hypomnema: document amendment/retry/restore answers beside the existing recovery contract and maintain the stable decision reference.

## Step 5: Demonstrate the new controller and close the frontier

**Goal.** Demonstrate the full selected contract with the new controller, publish bounded evidence and complete Protasis's held frontier with the correct governed versions.

**Entry.** Step 4 is pushed and checkpointed; terminal-compatibility passes at `step:5`; the controller supplies Step 5's parent/branch and admits its no-known inoculation. No earlier receipt is treated as a successor-controller demonstration.

**Exit.** The separate new-controller demonstration records positive and negative outcomes, original source/command identity, a vacuous passing command and zero inspection launches; its final report states all exclusions and actual observed counts. Run `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report .hexaemeron/reports/step-5-exit.json`. Run `python3 scripts/run_checks.py --scope hexaemeron --jobs 26 --report .hexaemeron/reports/step-5-checked.json`. The selected joined-demonstration report must pass before integration. Frontier/version, source-copy and publication checks must be green on the final candidate, with any current-main composition revalidated separately.

**Files.** Complete `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, `docs/protasis-success-criteria/demonstration.md`, `docs/protasis-success-criteria/evidence/`, and source-bound conformance report copies under `docs/protasis-success-criteria/reports/`. Add `plugins/hexaemeron/tests/test_fiat_criteria_demonstration.py`; update `tests/test_success_criteria_scaffold.py` and relevant criterion tests for actual final proof. Update `plugins/hexaemeron/skills/protasis/SKILL.md`, `plugins/hexaemeron/skills/protasis/EVOLUTION.md`, `plugins/hexaemeron/skills/protasis/DEMONSTRATION.md`, `plugins/hexaemeron/skills/fiat/SKILL.md`, `plugins/hexaemeron/skills/fiat/EVOLUTION.md`, `plugins/hexaemeron/skills/fiat/DEMONSTRATION.md`, `plugins/hexaemeron/skills/DEMONSTRATIONS.md`, `plugins/hexaemeron/skills/fiat/references/controller-checkpoint.md`, `plugins/hexaemeron/skills/fiat/references/checkpoint-archive.md`, `plugins/hexaemeron/README.md`, `tests/test_evolution_contract.py`, `tests/promise_machine_coverage.json`, `tests/promise_machine_id_history.json`, applicable criterion-owned fixtures under `tests/fixtures/promise-machine/runtime/`, and their generated obligation-gate projections under `docs/promise-machine/obligation-gates/`. Update all four Hex package files and the two live version expectations; regenerate portable copies and Horos artifacts; preserve the per-run audit log/synopsis. Existing frozen measurement/model artifacts remain unchanged. Final ADR assignment and signed current-main composition belong to their existing integration owners.

**Tests.** Execute the target controller in disposable Git-backed fixtures and identify that exact controller in readback. Observe missing execution, nonzero exit, wrong criterion/step/command/source, interrupted result, timeout/overflow, stale amendment, withholding integration, marker/legacy behavior, checkpoint replay, shared descriptors and deliberately vacuous success. Use real local command execution; state which remote/GitHub surfaces are fixtures. Verify all new Promise Machine and demonstration claims against the actual emitted reports and source digests. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-5-guard.json`. Required root/Hex, selected owner suites, prose, source-census, package and signature checks run on the actual candidate. Do not relabel prior timeout or inconclusive evidence as passing.

**Disciplines.** phylax: prove declared execution/refusal boundaries without expanding them into hostile-host or semantic guarantees. ephoros: publish bounded operator readback for success, unmet criteria and recovery. metron: report observed work/resource counts with their fixture scope and no unmeasured speed claim. elenchus: preserve each causal negative specimen and actual verdict. hypomnema: publish the final demonstration, criterion contracts and one earned frontier row per changed skill, preserving the held-job/evolution distinction and the single standing decision.

### Amendment -- 2026-09-16

**What changed.** Complete replacement Files: Create `docs/protasis-success-criteria/study.md`, `docs/protasis-success-criteria/runbook.md`, `docs/protasis-success-criteria/design-evidence.json`, `docs/protasis-success-criteria/selection_probe.py`, `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, the eighteen exact selection-report copies under `docs/protasis-success-criteria/reports/`, and required source/audit/prose provenance copies under `docs/protasis-success-criteria/evidence/`. Publish `docs/decisions/drafts/settle-study-criteria-from-recorded-execution.md` from the prepared decision draft. Add `tests/test_success_criteria_scaffold.py`; update `tests/check-map-v1.json`, `.horos/census.json` and `.horos/boundary.json` when their checked ownership/census requires it. Audit writes are only the controller-named per-run log and its generated synopsis. Repair only the two observed fixture faults in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`: the archive-document guard must not assume an unrelated active study is its own specification, and temporary-file cleanup must be checked inside a directory owned by the inspection under test. Keep archive production code and the fixed source controller unchanged. Update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` for this step's package increment. Regenerate affected portable copies and the Horos outputs. Copy the amended runbook into its published home and update its proof and provenance bindings. Preserve the earlier unconsumed design-home report and companion in evidence history before executing the same selected resolver again against the amended sources.

Complete replacement Tests: Add meaningful scaffold checks for the exact design-home join, unchanged selected report digests, missing/duplicate decision homes, refusal of unavailable operations and refusal to overwrite an existing report. A conformance report follows an executed check and retains source evidence; no constant success stub is allowed. Expected new test count is determined by cases, not predicted. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-guard.json`. The registered full Hex runner may return inconclusive under Elenchus's process boundary; retain the actual verdict and separately run the owning root tests. Run the explicit Hypomnema study/design bridge after publishing its home. Reproduce both fixture faults and retain causal regression checks: an unrelated active study must not invalidate the archive-document guard, and another inspection's temporary directory must not be attributed to this inspection. The cleanup check must still catch an owned temporary directory that remains after inspection. Keep the existing archive specification scans, secret-shaped specimen coverage and refusal assertions. Rerun the disposable-signing guard on the fixed candidate without concurrent product or staging changes. Preserve the failed full-suite result and each isolated reproduction; rerun both full suites after repairs, with the candidate and staged tree unchanged for their duration.
**Why.** The Step 1 Hex Exit ran 3,345 tests and reported three assertion failures. The archive-document test also failed alone in 0.020 seconds because it treated this run's unrelated study as its own archive specification. The cleanup test reported a new global temporary directory without binding that directory to its own inspection. The disposable-signing guard observed product and staging changes during its observation; that failure requires a stable-candidate rerun. The root gate passed 2,041 tests in 1,913.548 seconds on the prior staged tree. These results require fixture repair and fresh checks; they change no selected design criterion or feature boundary.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.

### Amendment -- 2026-09-16

**What changed.** Complete replacement Files: Create `docs/protasis-success-criteria/study.md`, `docs/protasis-success-criteria/runbook.md`, `docs/protasis-success-criteria/design-evidence.json`, `docs/protasis-success-criteria/selection_probe.py`, `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, the eighteen exact selection-report copies under `docs/protasis-success-criteria/reports/`, and required source/audit/prose provenance copies under `docs/protasis-success-criteria/evidence/`. Publish `docs/decisions/drafts/settle-study-criteria-from-recorded-execution.md` from the prepared decision draft. Add `tests/test_success_criteria_scaffold.py`; update `tests/check-map-v1.json`, `.horos/census.json` and `.horos/boundary.json` when their checked ownership/census requires it. Audit writes are only the controller-named per-run log and its generated synopsis. Repair only the two observed fixture faults in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`: the archive-document guard must not assume an unrelated active study is its own specification, and temporary-file cleanup must be checked inside a directory owned by the inspection under test. Keep archive production code and the fixed source controller unchanged. Update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` for this step's package increment. Regenerate affected portable copies and the Horos outputs. Copy the amended runbook into its published home and update its proof and provenance bindings. Preserve the earlier unconsumed design-home report and companion in evidence history before executing the same selected resolver again against the amended sources. Preserve the original `docs/promise-machine/obligation-gates/evaluation-run.json` in published evidence before replacing its current projection with the exact output of `tests/promise_evaluation_driver.py`. Refresh only the source-tree binding and its derived input digests in `docs/promise-machine/obligation-gates/integration-projection.json` and `docs/promise-machine/obligation-gates/integration-projection.md`. Keep historical answers, model, date, prompts, outcomes and original demonstration bytes unchanged. Record the original and projected digests, producer identity, unchanged prompt digests and absence of a new model observation beside the preserved original.

Complete replacement Tests: Add meaningful scaffold checks for the exact design-home join, unchanged selected report digests, missing/duplicate decision homes, refusal of unavailable operations and refusal to overwrite an existing report. A conformance report follows an executed check and retains source evidence; no constant success stub is allowed. Expected new test count is determined by cases, not predicted. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-guard.json`. The registered full Hex runner may return inconclusive under Elenchus's process boundary; retain the actual verdict and separately run the owning root tests. Run the explicit Hypomnema study/design bridge after publishing its home. Reproduce both fixture faults and retain causal regression checks: an unrelated active study must not invalidate the archive-document guard, and another inspection's temporary directory must not be attributed to this inspection. The cleanup check must still catch an owned temporary directory that remains after inspection. Keep the existing archive specification scans, secret-shaped specimen coverage and refusal assertions. Rerun the disposable-signing guard on the fixed candidate without concurrent product or staging changes. Preserve the failed full-suite result and each isolated reproduction; rerun both full suites after repairs, with the candidate and staged tree unchanged for their duration. Verify that the evaluation projection differs only at `tree_sha256`, all eleven prompt files are byte-identical, the raw answers retain their digest, and all fifty-five outcomes retain their values. Execute the owning evaluation verifier, portable sync and portable check on the current candidate; a changed prompt or any other changed record field refuses this source-only procedure. Preserve the original evaluation record and each earlier projection before a later source-only refresh in this run.
**Why.** The approved fixture repairs change the coverage source digest. Portable generation checks that digest against the current evaluation projection and refuses the old binding. The prepared projection changes only `tree_sha256`: all eleven prompts, raw answers, model, date and fifty-five outcomes remain identical. The evaluation verifier, portable sync and portable check passed in a disposable copy; current-candidate full gates remain due. The user approved the prepared update and later checked source-only updates in this run with: "Approval for anything you need." This permits the recorded source-only projection while preserving frozen observations and the original record. It changes no selected design criterion, execution claim or command gate.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.

### Amendment -- 2026-09-16

**What changed.** Complete replacement Files: Create `docs/protasis-success-criteria/study.md`, `docs/protasis-success-criteria/runbook.md`, `docs/protasis-success-criteria/design-evidence.json`, `docs/protasis-success-criteria/selection_probe.py`, `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, the eighteen exact selection-report copies under `docs/protasis-success-criteria/reports/`, and required source/audit/prose provenance copies under `docs/protasis-success-criteria/evidence/`. Publish `docs/decisions/drafts/settle-study-criteria-from-recorded-execution.md` from the prepared decision draft. Add `tests/test_success_criteria_scaffold.py`; update `tests/check-map-v1.json`, `.horos/census.json` and `.horos/boundary.json` when their checked ownership/census requires it. Audit writes are only the controller-named per-run log and its generated synopsis. Retain the two earlier fixture repairs in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`: the archive-document guard must not assume an unrelated active study is its own specification, and temporary-file cleanup must be checked inside a directory owned by the inspection under test. Keep archive production code and the fixed source controller unchanged. Update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` for this step's package increment. Regenerate affected portable copies and the Horos outputs. Copy the amended runbook into its published home and update its proof and provenance bindings. Preserve the earlier unconsumed design-home report and companion in evidence history before executing the same selected resolver again against the amended sources. Preserve the original `docs/promise-machine/obligation-gates/evaluation-run.json` in published evidence before replacing its current projection with the exact output of `tests/promise_evaluation_driver.py`. Refresh only the source-tree binding and its derived input digests in `docs/promise-machine/obligation-gates/integration-projection.json` and `docs/promise-machine/obligation-gates/integration-projection.md`. Keep historical answers, model, date, prompts, outcomes and original demonstration bytes unchanged. Record the original and projected digests, producer identity, unchanged prompt digests and absence of a new model observation beside the preserved original. Repair the reproduced restricted-environment fixture assumptions in `plugins/hexaemeron/tests/test_audit_synopsis_recovery.py`, `plugins/hexaemeron/tests/test_carryover.py`, `plugins/hexaemeron/tests/test_confined_replacement_lifecycle.py`, `plugins/hexaemeron/tests/test_fiat_decision_assignments.py`, `plugins/hexaemeron/tests/test_known_failure_inventory.py`, `plugins/hexaemeron/tests/test_version_relations.py`, `tests/test_adr_assignment_workflow.py`, `tests/test_demonstrations.py` and `tests/test_joined_front_door.py`. Add `tests/fixture_python.py` and `tests/test_fixture_python.py` for a scoped launcher bound to the executing interpreter, and `plugins/hexaemeron/tests/fixture_tools.py` and `plugins/hexaemeron/tests/test_fixture_tools.py` for explicit native signing-tool selection. Use only bounded system and Homebrew installation directories to select real absolute GPG and GPGCONF executables; expose them through a fixture-owned launcher directory for nested verification. Extend `SignedRunFixture` in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` to use those real tools under the restricted path, preserving its existing policy when the tools are truly unavailable. The other signed fixtures must fail explicitly when their required tools are unavailable. Pin nested Python calls to the executing interpreter, resolve required signing tools by an explicit path, and control inherited Git replacement settings only for the ordinary-Git fixture observation. Give the ADR workflow shell fixture and the two public-set fixtures a temporary Python launcher bound to the executing interpreter, preserving the unmodified workflow shell, recorded producer argv and real demonstration. Preserve the failed admission, full diagnostic results and narrow causal probes under the published evidence home. Production code, the fixed source controller, registered commands, signing assertions and negative checks remain outside this repair.

Complete replacement Tests: Add meaningful scaffold checks for the exact design-home join, unchanged selected report digests, missing/duplicate decision homes, refusal of unavailable operations and refusal to overwrite an existing report. A conformance report follows an executed check and retains source evidence; no constant success stub is allowed. Expected new test count is determined by cases, not predicted. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-guard.json`. The registered full Hex runner may return inconclusive under Elenchus's process boundary; retain the actual verdict and separately run the owning root tests. Run the explicit Hypomnema study/design bridge after publishing its home. Reproduce both fixture faults and retain causal regression checks: an unrelated active study must not invalidate the archive-document guard, and another inspection's temporary directory must not be attributed to this inspection. The cleanup check must still catch an owned temporary directory that remains after inspection. Keep the existing archive specification scans, secret-shaped specimen coverage and refusal assertions. Rerun the disposable-signing guard on the fixed candidate without concurrent product or staging changes. Preserve the failed full-suite result and each isolated reproduction; rerun both full suites after repairs, with the candidate and staged tree unchanged for their duration. Verify that the evaluation projection differs only at `tree_sha256`, all eleven prompt files are byte-identical, the raw answers retain their digest, and all fifty-five outcomes retain their values. Execute the owning evaluation verifier, portable sync and portable check on the current candidate; a changed prompt or any other changed record field refuses this source-only procedure. Preserve the original evaluation record and each earlier projection before a later source-only refresh in this run. Preserve the restricted-environment Hex and root failures on `84859f22bf3f1d446f6c8c93310757f40b9b42f0`. Retain failing-before and passing-after evidence for child interpreter selection, required signing-tool lookup, inherited replacement-object settings and the nested public demonstration producer. Exercise the repaired fixture paths under the exact environment declared by the fixed controller and in the ordinary environment. Check that the nested interpreter reports the declared executable and version, the tool paths select real absolute executables, required-tool absence fails visibly, and launcher cleanup and environment restoration hold after success and exceptions. Retain the real unsigned and tampered-signature refusals. Keep actual signature verification, the replacement-object control, controller refusal assertions and every public demonstration observation. Do not replace those observations with skips or mocked success. After the required Exit commands and normal commit gate pass on one unchanged candidate, require the unchanged fixed-source `done implement` command to admit that signed candidate through its own declared Hex and root suites.
**Why.** The signed Step 1 candidate passed its ordinary suites and checked runner, but fixed-source `done implement` refused the Hex suite before writing an implementation receipt. The captured restricted-environment reruns produced three Hex failures and twenty-nine errors, with nine fixture-blocked tests, and twelve root failures. Narrow probes identify child Python 3.9.6 in place of the pinned 3.14.6, signing-tool lookup and an inherited Git replacement setting. The unchanged Lazarus record fails with the system Python launcher and passes with a fixture-owned launcher for the pinned interpreter. These observations require fixture repairs and fresh evidence under the existing gates. The standing user approval covers the required repair; no selected criterion or feature boundary changes.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.

### Amendment -- 2026-09-16

**What changed.** Complete replacement Files: Create `docs/protasis-success-criteria/study.md`, `docs/protasis-success-criteria/runbook.md`, `docs/protasis-success-criteria/design-evidence.json`, `docs/protasis-success-criteria/selection_probe.py`, `docs/protasis-success-criteria/proof.py`, `docs/protasis-success-criteria/README.md`, the eighteen exact selection-report copies under `docs/protasis-success-criteria/reports/`, and required source/audit/prose provenance copies under `docs/protasis-success-criteria/evidence/`. Publish `docs/decisions/drafts/settle-study-criteria-from-recorded-execution.md` from the prepared decision draft. Add `tests/test_success_criteria_scaffold.py`; update `tests/check-map-v1.json`, `.horos/census.json` and `.horos/boundary.json` when their checked ownership/census requires it. Audit writes are only the controller-named per-run log and its generated synopsis. Retain the two earlier fixture repairs in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py`: the archive-document guard must not assume an unrelated active study is its own specification, and temporary-file cleanup must be checked inside a directory owned by the inspection under test. Keep archive production code and the fixed source controller unchanged. Update `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py` for this step's package increment. Regenerate affected portable copies and the Horos outputs. Copy the amended runbook into its published home and update its proof and provenance bindings. Preserve the earlier unconsumed design-home report and companion in evidence history before executing the same selected resolver again against the amended sources. Preserve the original `docs/promise-machine/obligation-gates/evaluation-run.json` in published evidence before replacing its current projection with the exact output of `tests/promise_evaluation_driver.py`. Refresh only the source-tree binding and its derived input digests in `docs/promise-machine/obligation-gates/integration-projection.json` and `docs/promise-machine/obligation-gates/integration-projection.md`. Keep historical answers, model, date, prompts, outcomes and original demonstration bytes unchanged. Record the original and projected digests, producer identity, unchanged prompt digests and absence of a new model observation beside the preserved original. Repair the reproduced restricted-environment fixture assumptions in `plugins/hexaemeron/tests/test_audit_synopsis_recovery.py`, `plugins/hexaemeron/tests/test_carryover.py`, `plugins/hexaemeron/tests/test_confined_replacement_lifecycle.py`, `plugins/hexaemeron/tests/test_fiat_decision_assignments.py`, `plugins/hexaemeron/tests/test_known_failure_inventory.py`, `plugins/hexaemeron/tests/test_version_relations.py`, `tests/test_adr_assignment_workflow.py`, `tests/test_demonstrations.py` and `tests/test_joined_front_door.py`. Add `tests/fixture_python.py` and `tests/test_fixture_python.py` for a scoped launcher bound to the executing interpreter, and `plugins/hexaemeron/tests/fixture_tools.py` and `plugins/hexaemeron/tests/test_fixture_tools.py` for explicit native signing-tool selection. Use only bounded system and Homebrew installation directories to select real absolute GPG and GPGCONF executables; expose them through a fixture-owned launcher directory for nested verification. Extend `SignedRunFixture` in `plugins/hexaemeron/tests/test_hexctl_checkpoint_archive.py` to use those real tools under the restricted path, preserving its existing policy when the tools are truly unavailable. The other signed fixtures must fail explicitly when their required tools are unavailable. Pin nested Python calls to the executing interpreter, resolve required signing tools by an explicit path, and control inherited Git replacement settings only for the ordinary-Git fixture observation. Give the ADR workflow shell fixture and the two public-set fixtures a temporary Python launcher bound to the executing interpreter, preserving the unmodified workflow shell, recorded producer argv and real demonstration. Preserve the failed admission, full diagnostic results and narrow causal probes under the published evidence home. Production code, the fixed source controller, registered commands, signing assertions and negative checks remain outside this repair. Apply the published path-escape fixture repair from signed commit `c572009b7922b4b7b9400893c5adc6d4df641a72` to `plugins/hexaemeron/tests/test_imprimatur_family_evidence.py`. Add the external-controller publication, signature, source-pin and command-adapter readbacks to the published evidence home. Copy the admitted amended study and runbook into their published homes and update their current proof constants and provenance inventory. Preserve all earlier reports and projections before producing fresh source-bound design-home evidence. The original external source stays unchanged; the published replacement authorised by the study becomes the fixed driver for later operations.

Complete replacement Tests: Add meaningful scaffold checks for the exact design-home join, unchanged selected report digests, missing/duplicate decision homes, refusal of unavailable operations and refusal to overwrite an existing report. A conformance report follows an executed check and retains source evidence; no constant success stub is allowed. Expected new test count is determined by cases, not predicted. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 12 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-guard.json`. The registered full Hex runner may return inconclusive under Elenchus's process boundary; retain the actual verdict and separately run the owning root tests. Run the explicit Hypomnema study/design bridge after publishing its home. Reproduce both fixture faults and retain causal regression checks: an unrelated active study must not invalidate the archive-document guard, and another inspection's temporary directory must not be attributed to this inspection. The cleanup check must still catch an owned temporary directory that remains after inspection. Keep the existing archive specification scans, secret-shaped specimen coverage and refusal assertions. Rerun the disposable-signing guard on the fixed candidate without concurrent product or staging changes. Preserve the failed full-suite result and each isolated reproduction; rerun both full suites after repairs, with the candidate and staged tree unchanged for their duration. Verify that the evaluation projection differs only at `tree_sha256`, all eleven prompt files are byte-identical, the raw answers retain their digest, and all fifty-five outcomes retain their values. Execute the owning evaluation verifier, portable sync and portable check on the current candidate; a changed prompt or any other changed record field refuses this source-only procedure. Preserve the original evaluation record and each earlier projection before a later source-only refresh in this run. Preserve the restricted-environment Hex and root failures on `84859f22bf3f1d446f6c8c93310757f40b9b42f0`. Retain failing-before and passing-after evidence for child interpreter selection, required signing-tool lookup, inherited replacement-object settings and the nested public demonstration producer. Exercise the repaired fixture paths under the exact environment declared by the fixed controller and in the ordinary environment. Check that the nested interpreter reports the declared executable and version, the tool paths select real absolute executables, required-tool absence fails visibly, and launcher cleanup and environment restoration hold after success and exceptions. Retain the real unsigned and tampered-signature refusals. Keep actual signature verification, the replacement-object control, controller refusal assertions and every public demonstration observation. Do not replace those observations with skips or mocked success. After the required Exit commands and normal commit gate pass on one unchanged candidate, require the unchanged fixed-source `done implement` command to admit that signed candidate through its own declared Hex and root suites. Preserve the failing isolated reproduction in which an occupied `escaped-schemas` sibling prevents the old fixture rename, and check that the repaired case still refuses the symlink and leaves that sibling untouched. Verify the original controller remains unchanged. Verify the new external controller against the existing receipt chain after admitting its runbook adapter update; every declared command must remain identical. Recheck the copied source inventory, explicit design bridge and selected design-home report against the admitted amendments. The earlier successful implementation receipt remains historical evidence on its own signed candidate; the Warden must run fresh required gates on the amended audit candidate.
**Why.** The admitted study permits one published controller replacement to accept the committed audit pair, and the new adapter requires a source-bound runbook amendment. The selected commands and design are unchanged. The reproduced shared temporary-directory collision also requires its bounded fixture repair. Preserve all earlier results, source bindings and observations; the user's standing approval covers these necessary repairs.
**Steps touched.** Step 1.
**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.
