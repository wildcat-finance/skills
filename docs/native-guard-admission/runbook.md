# Route native integration requirements to their evidence

Assuming, unless corrected:

1. The accepted study is `.hexaemeron/study.md`, SHA-256 `45e864d1acdd4a66e1a8034dd1662235d811229d2c7ab87cc62115c0e6dada2f`, at starting commit `aededf66434ed4b4e3994bbaab5f1005fe10b453`.
2. The active controller remains the installed Hexaemeron `1.6.64` / Fiat `6.70.1`. Product commands run in this managed worktree; the successor is exercised only in separate disposable repositories.
3. Every command uses Python `3.14.6`. Put its pinned executable directory and Node `26.6.0` before ambient tools in `PATH`. The existing licence, toolchain files and CI already supply the scaffold; this run adds no dependency or CI change.
4. This is the separately authorised Skills delivery. Both service runs, their signed scaffold, checkpoints and ledgers remain preserved. Published examples contain synthetic or already-public data only.

The selected design is the study's `typed-applicability` construction. Protasis checks the source/status declaration and its joins; Fiat captures and enforces those joins. Existing known-failure and observed-criteria mechanisms retain their ownership. Elenchus and `criteria_execution.py` are outside the implementation delta.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 4f5aba14c308c63b3d1ea62aa682f380b996f68230601bd9a8fe7fb3e3c014ab
candidate | typed-applicability
```

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
protasis | plugins/hexaemeron/skills/protasis/EVOLUTION.md | next-generation-after-integration-base
```

The study's checked inventory declares no established local regression in this new interface. Its explicit claim names Step 1 and four source/view pairs. There are no known-failure assignment lines. Fiat still owes a source-bound no-known-findings inoculation record at every consuming step; an empty list alone is not admission.

Always preserve exact study/design bytes, source statuses, pending results and the full risk register. Before each step starts, require the preceding green exit and its controller receipt. Run the selected repository checks and the tracked commit gate, refresh generated copies and census, sign as `Dave Coleman <dave@wildcat.finance>`, and verify every commit locally. End each contributed commit message once with `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`.

The checked-runner command for each step is `python3 scripts/run_checks.py --scope hexaemeron --base aededf66434ed4b4e3994bbaab5f1005fe10b453 --jobs 32 --report .hexaemeron/reports/selected-checks.json`. Preserve each completed report under its step before a fresh invocation. Its selected scope is a minimum; the ownership graph includes every actual changed path. A stale or failed report is not a green result. The separate commit gate checks the staged tree.

Every declared Exit below is executed through the active controller's criteria route when that step owns success criteria. A shared Exit supplies one observation for its descriptors. Preserve failed attempts and use the supported retry path. Report creation is exclusive; never overwrite evidence already consumed by a receipt. The full Hex runner may be inconclusive under Elenchus's no-child boundary: retain that actual verdict and run the ordinary repository tests separately. It cannot stand in for an assigned parent assertion failure.

Ask before work outside the accepted scope: a dependency or privileged executor, service implementation, a changed trust guarantee, unrelated CI, or a rewritten released digest. Never manufacture red evidence, retrofit an old run's marker, change a captured classification, edit historical audits, publish private research, or replace the installed controller with the one being built.

Each step's Files includes its own `docs/native-guard-admission/study.md` and `docs/native-guard-admission/runbook.md` mirrors when a permitted source amendment must be copied, the run's append-only audit pair under `audit/rounds/`, and necessary generated `.agents/skills/promise-machine/runtime/`, `.horos/boundary.json` and `.horos/census.json` outputs. A step changing Hexaemeron package bytes also increments the package above its PR base in `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.agents/plugins/marketplace.json` and `.claude-plugin/marketplace.json`, with matching expectations in `tests/test_version_propagation.py` and `plugins/hexaemeron/tests/test_phylax_model_proxy.py`. Governed skill generations are earned once at final delivery. Generated files follow their owner; old audits and digest-bound reports remain exact. A new path requires a declared check-map owner. Public prose follows Sapheneia, Imprimatur, Vulgate and the final Imprimatur pass; Brevitas applies where its contract includes the record.

## Step 1: Scaffold the applicability contract

**Goal.** Publish the reviewed specification, decision and synthetic proof scaffold without claiming that the future applicability parser or controller already exists.

**Entry.** Start at the controller's exact Step 1 parent, initially `aededf66434ed4b4e3994bbaab5f1005fe10b453`, after its no-known-findings inoculation receipt. The parent-owned selected baseline must be green before product editing. The immutable study and design record select `typed-applicability`.

**Exit.** Commit exact study and runbook copies, the selected design and its executed selection reports, synthetic candidate models and reproducible selection source. Publish the canonical decision named in Files from the prepared decision and pass the explicit Hypomnema study/design bridge. Add a reviewed schema description, synthetic examples covering the four dispositions, a provenance manifest and the proof interface named in Files. Its unimplemented conformance operations refuse by name and produce no successful report. Register meaningful scaffold tests in the existing Hex runner. The proving Exit is `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/native-admission-step-1.json`. The selected repository checks and ordinary prose/pointer checks also pass.

**Files.** Create `docs/native-guard-admission/study.md`, `docs/native-guard-admission/runbook.md`, `docs/native-guard-admission/README.md`, `docs/native-guard-admission/contract.md`, `docs/native-guard-admission/design-evidence.json`, `docs/native-guard-admission/research/`, `docs/native-guard-admission/reports/design/`, `docs/native-guard-admission/fixtures/`, `docs/native-guard-admission/proof.py` and `docs/decisions/drafts/route-audit-obligations-to-their-evidence.md`. Add `plugins/hexaemeron/tests/test_native_guard_admission_scaffold.py`. Update `tests/check-map-v1.json` for the new owned documentation/proof subtree, plus the common generated and audit surfaces above. Selection models and reports are synthetic research evidence; local service inputs are not copied.

**Tests.** Check the exact selected-record/decision join, schema/example shape, preserved source statuses, fixture provenance and absence of operator material. Prove unavailable operations refuse and cannot create a pass report; report paths must refuse replacement. These tests establish the scaffold only. Observe the actual count when run. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/native-admission-step-1-guard.json`. The explicit Hypomnema bridge runs after its canonical decision home exists.

**Disciplines.** phylax: the proof CLI and fixture provenance define the input boundary. ephoros: unavailable proof operations return a bounded cause. metron: none, this step makes no runtime or allocation claim. elenchus: reproduce and guard any failure that appears, without fabricating a parent defect for the new interface. hypomnema: publish and check the cross-skill decision before shipping the study.

## Step 2: Check source applicability in Protasis

**Goal.** Validate one bounded applicability declaration and join its exact source witnesses to existing known-failure and success-criteria declarations.

**Entry.** The controller's exact Step 1 head is green, reviewed and pushed; Step 2 has its own no-known-findings inoculation receipt. The scaffold, source models and fixed design remain available. No successor controller receipt is claimed by this entry.

**Exit.** Implement the study's absent/refused/clean parser contract, exact source-id colon detail binding, source/status spans, complete checked source/view equality, closed dispositions and derived target routes. Every existing known-failure id appears exactly once as a local regression; integration targets resolve to checked success-criteria descriptors; historical and excluded rows grant no success. Enforce the study's document, source, row, span, string and aggregate limits with stable no-follow reads. Complete the selected applicability-parser and bounded-reader resolvers from the immutable design record and retain their exact reports before Step 3 opens. The proving Exit is `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/native-admission-step-2.json`, settling applicability-declaration and bounded-applicability-read. The selected repository checks also pass.

**Files.** Add `plugins/hexaemeron/skills/protasis/scripts/audit_applicability.py` and `plugins/hexaemeron/tests/test_audit_applicability.py`. Extend `plugins/hexaemeron/skills/protasis/scripts/protasis.py` only for the reviewed checker interface, with its command binding in `plugins/hexaemeron/skills/protasis/scripts/gate_commands.py` if that interface changes. Reuse `known_failure_inventory.py` and `success_criteria.py` through their checked interfaces; a minimal accessor needed to reuse captured bytes belongs here and must preserve existing behavior. Update `docs/native-guard-admission/proof.py`, its contract/fixtures/guide, parser tests, read-budget evidence and the common mirrors/generated/audit surfaces. Register new test coverage in `tests/check-map-v1.json` when needed. No Fiat product behavior changes in this step.

**Tests.** Cover truly absent versus every attempted malformed fence, duplicate keys/ids/fields, non-finite values, wrong schema/types, boolean offsets, UTF-8 boundaries, empty/out-of-range/mismatched spans, unknown or prefix-confusable source ids, changed status witnesses, portable aliases, links, non-regular and changing files, source/view digest drift, omitted/extra views, duplicate/missing regression joins, unknown criteria and conflicting target fields. Preserve positive examples of all four dispositions and an unknown source status. Test exact-limit and one-over bounds. Measure the maximum accepted synthetic input against 30 seconds and 64 MiB traced additional allocation, recording host, interpreter, input digest and command; this is a budget check, not a speedup. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/native-admission-step-2-guard.json`.

**Disciplines.** phylax: untrusted Markdown, JSON and filesystem reads require closed bounded validation. ephoros: refusal codes identify the failing row or binding without source disclosure. metron: measure the maximum accepted reader input against the study's fixed budget. elenchus: reproduce new failures and retain the actual guard outcome. hypomnema: document the parser's evidence boundary and keep the decision and guide current.

## Step 3: Bind applicability to Fiat receipts

**Goal.** Capture immutable applicability routes and require their owning evidence before each dependent controller transition.

**Entry.** The controller's exact Step 2 head is green, reviewed and pushed, with both due parser and reader reports accepted at `step:3`; Step 3 inoculation is complete. The active external controller still governs this live run.

**Exit.** Add the new-init contract marker, atomic runbook capture, reserved receipt ownership, immutable source/status/classification/target joins, amendment continuity and read-only replay. Preserve legacy receipt shapes without fabricated captures; attempted applicability in an unmarked run names the fresh-run recovery. Status, next, delegated packets and checkpoint replay expose the same pending or settled obligations without private source content. Local regressions still depend on existing inoculation and final-green evidence. Integration requirements depend on exact successful existing criteria execution at their consuming step and integration. Resolve the selected frozen-controller-join design cell before Step 4 opens. The proving Exit is `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/native-admission-step-3.json`, settling immutable-routing, criteria-custody and legacy-preservation. The selected repository checks also pass.

**Files.** Change `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and add focused `plugins/hexaemeron/tests/test_applicability_lifecycle.py` and `plugins/hexaemeron/tests/test_applicability_replay.py`. Extend `test_hexctl.py`, `test_inoculation_lifecycle.py`, `test_fiat_criteria_replay.py`, `test_hexctl_checkpoint.py`, `test_hexctl_checkpoint_archive.py` and `test_gate_commands.py` under that test directory only where they cover the new bindings. Update `docs/native-guard-admission/proof.py`, contract/guide/fixtures, controller conformance reports and the common mirrors/generated/audit surfaces. Refresh exact changed-source expectations in applicable `tests/fixtures/promise-machine/runtime/` records without enlarging their claims; final instruction and Promise Machine declaration changes belong to Step 4. The runtime executor `criteria_execution.py`, Elenchus code and verdict declarations remain outside the delta.

**Tests.** Exercise the separate successor in disposable signed repositories. Cover marker addition/removal/disagreement, legacy absence, reserved generic record refusal, capture interruption and supported recovery, source/view/status/span/rationale/tracking/target mutation, removed or reclassified regressions, criterion and consuming-step substitution, missing/failed/interrupted/stale/forged attempts, shared descriptors, unrelated valid amendments and completed-descriptor preservation. Every refusal before a receipt must leave state and ledger unchanged. Check repeated status/next/verify and checkpoint round-trip with a sentinel that proves they launch no test. Keep current identity and historical source resolution distinct. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/native-admission-step-3-guard.json`.

**Disciplines.** phylax: validate captured evidence and source identities before controller mutation. ephoros: expose bounded obligation and attempt states with their causes. metron: no new performance claim; retain existing execution limits and the parser's checked caps. elenchus: reproduce each observed failure and preserve the actual red/fixed outcomes. hypomnema: record receipt and recovery semantics without rewriting historical evidence.

## Step 4: Demonstrate native admission and release

**Goal.** Demonstrate the joined successor with real separate regression and native-execution evidence, then publish the bounded capability.

**Entry.** The controller's exact Step 3 head is green, reviewed and pushed, with `frozen-controller-join` accepted at `step:4`; Step 4 inoculation is complete. The implementation exists, but its joined native demonstration and release evidence remain due.

**Exit.** Run the selected native-execution, bounded-execution and legacy-and-hostile-replay commands exactly as recorded in the immutable design matrix. Their signed disposable successor runs must identify the actual controller and source commands. Observe a genuine pure-Python parent assertion failure and final green in one lane. In another, retain an absent feature as pending, implement a benign native child, independently inspect its output and settle through the existing criteria executor. Record failure, timeout, overflow, stale/misbound evidence, legacy and checkpoint replay refusals. Remote publication endpoints inside the demonstration are explicit fixtures; actual run publication stays with the orchestrator. The proving Exit is `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report .hexaemeron/reports/native-admission-step-4.json`, settling native-admission-demonstration. All selected checks, the six selected conformance reports, release consistency and signature checks pass before integration.

**Files.** Complete `docs/native-guard-admission/proof.py`, its signed disposable fixtures, report validators, guide and public conformance evidence. Add `plugins/hexaemeron/tests/test_native_guard_admission_demo.py`. Update Fiat and Protasis `SKILL.md` and `EVOLUTION.md`, applicable Fiat worker/reference text, `plugins/hexaemeron/README.md`, plugin manifests, `.agents/plugins/marketplace.json`, `.claude-plugin/marketplace.json`, affected front-door/catalogue/version tests and their generated projections. Reconcile existing Promise Machine declarations, obligation/coverage/history fixtures and governed demonstration records through their owner tools when a claim changes; do not invent claims to populate a registry. Include `tests/test_evolution_contract.py`, `tests/test_version_propagation.py` and `tests/test_promise_machine_contract.py` when those existing contracts require matching changes. Preserve held jobs, frontier tuples and Elenchus's version. Include the common mirrors/generated/audit surfaces and exact changes to the cross-skill decision status justified by completed evidence.

**Tests.** Drive the actual successor CLI through admission, pending state, genuine regression retention, implementation, observed native execution and read-only replay. Reject canned supplied success, wrong run/init/step/command/source, omitted regression routes, post-capture reclassification, missing or replaced result files and stale checkpoint evidence. Prove real timeout/overflow outcomes with reduced fixture limits; do not infer whole-job resource containment. Check demonstration report bindings, synthetic-data provenance, no private signing material, preserved historical receipts and exact unchanged Elenchus/executor source. Run version, package, portable-copy, public-prose and selected repository checks on the final tree. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --jobs 8 --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/native-admission-step-4-guard.json`.

**Disciplines.** phylax: temporary signing and native fixture processes stay within reviewed test authority. ephoros: reports distinguish pending, refusal, execution outcome and bounded success. metron: preserve measured reader and executor limits without a service performance claim. elenchus: the demonstration earns actual regression evidence and retains negative outcomes. hypomnema: publish the final guide, decision status and fresh-service-run instructions with every remaining limit.

The final run pull request names inherited open issues and remaining service implementation through its carryover block. This release does not claim a service sandbox exists, that a source judgment is true, or that every relevant finding was discovered. After publication, installation and the required host refresh, a fresh service run can review and classify its own source-bound obligations under the released controller.

### Amendment -- 2026-09-20

**What changed.** Complete replacement Files: Add `plugins/hexaemeron/skills/protasis/scripts/audit_applicability.py` and `plugins/hexaemeron/tests/test_audit_applicability.py`. Extend `plugins/hexaemeron/skills/protasis/scripts/protasis.py` only for the reviewed checker interface, with its command binding in `plugins/hexaemeron/skills/protasis/scripts/gate_commands.py` if that interface changes. Reuse `known_failure_inventory.py` and `success_criteria.py` through their checked interfaces; a minimal accessor needed to reuse captured bytes belongs here and must preserve existing behavior. Update `docs/native-guard-admission/proof.py`, its contract/fixtures/guide, parser tests, read-budget evidence and the common mirrors/generated/audit surfaces. Register new test coverage in `tests/check-map-v1.json` when needed. No Fiat product behavior changes in this step. Include the separately reviewed repair for https://github.com/wildcat-finance/skills/issues/1596 in `plugins/homologia/tests/test_check.py` by merging its original signed delivery after its pull request has landed. Preserve its bounded wait for an observed ctime transition, same inode, size and restored mtime checks, and final `HOM-CHECK-PATH` refusal. Preserve Homologia runtime bytes and both prior failed selected-check reports. Refresh the common census after composition.

**Why.** The required selected checks failed twice on the existing Homologia fixture before it exercised its runtime guard. The original fixture separately failed four of ten focused runs; the bounded fixture repair passed one hundred focused runs and the full Homologia suite. Its independent delivery removes that prerequisite failure without changing applicability routes, selected design, command coverage or success criteria. The complete selected command remains required on the composed tree.

**Steps touched.** Step 2.

**Still holding.** Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.

### Amendment -- 2026-09-22

**What changed.** Complete replacement Entry: The controller's exact Step 1 head is green, reviewed and pushed; Step 2 has its own no-known-findings inoculation receipt. The scaffold, source models and fixed design remain available. Use the released external Hexaemeron 1.6.73 / Fiat 6.74.1 maintenance controller after the completed Codex session refresh, as recorded in the study amendment. Resumption verified the 20-entry ledger and all four preserved canonical file hashes. No amendment transaction was pending. The applicability successor remains confined to its separate disposable demonstration.

Complete replacement Files: Add `plugins/hexaemeron/skills/protasis/scripts/audit_applicability.py` and `plugins/hexaemeron/tests/test_audit_applicability.py`. Extend `plugins/hexaemeron/skills/protasis/scripts/protasis.py` only for the reviewed checker interface, with its command binding in `plugins/hexaemeron/skills/protasis/scripts/gate_commands.py` if that interface changes. Reuse `known_failure_inventory.py` and `success_criteria.py` through their checked interfaces; a minimal accessor needed to reuse captured bytes belongs here and must preserve existing behavior. Update `docs/native-guard-admission/proof.py`, its contract/fixtures/guide, parser tests, read-budget evidence and the common mirrors/generated/audit surfaces. Register new test coverage in `tests/check-map-v1.json` when needed. Include the separately reviewed repair for https://github.com/wildcat-finance/skills/issues/1596 in `plugins/homologia/tests/test_check.py` by merging its original signed delivery after its pull request has landed. Preserve its bounded wait for an observed ctime transition, same inode, size and restored mtime checks, and final `HOM-CHECK-PATH` refusal. Preserve Homologia runtime bytes and both prior failed selected-check reports. Refresh the common census after composition. Include the published replay and amendment recovery repairs from https://github.com/wildcat-finance/skills/pull/1808, https://github.com/wildcat-finance/skills/pull/1830 and https://github.com/wildcat-finance/skills/pull/1837 by merging original signed implementation commit `d88167b6e98706c7a023030ef3b422d8eab7e6af`. Preserve its ancestors `da4a2c1064afeea6b3c41f6b56d47a6d702b87f1` and `a5c02d67c3bab71ec3d8317b2be2430969237320` and both signed Homologia delivery commits. Include their tests, package metadata, governed version records and owner-derived digests. These maintenance repairs are the only Fiat behavior changes admitted in Step 2. Preserve the native package version above Hexaemeron 1.6.73 and retain the accepted applicability implementation. Refresh exact public study and runbook mirrors, scaffold expectations, owner-derived provenance and the common census after amendment and composition. Preserve all earlier reports.

**Why.** The preceding study amendment committed, but its follow-on runbook amendment refused before mutation with `recovery-admission-drift`. The separately released repair permits only the reviewed command-adapter change when all other gate evidence agrees. Composing its original signed delivery preserves the earlier maintenance and Homologia repairs. All 37 selected checks and a fresh registered Step 2 Exit must pass on the composed tree before implementation is receipted.

**Steps touched.** Step 2.

**Still holding.** Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.

### Amendment -- 2026-09-22

**What changed.** Complete replacement Files: Add `plugins/hexaemeron/skills/protasis/scripts/audit_applicability.py` and `plugins/hexaemeron/tests/test_audit_applicability.py`. Extend `plugins/hexaemeron/skills/protasis/scripts/protasis.py` only for the reviewed checker interface, with its command binding in `plugins/hexaemeron/skills/protasis/scripts/gate_commands.py` if that interface changes. Reuse `known_failure_inventory.py` and `success_criteria.py` through their checked interfaces; a minimal accessor needed to reuse captured bytes belongs here and must preserve existing behavior. Update `docs/native-guard-admission/proof.py`, its contract/fixtures/guide, parser tests, read-budget evidence and the common mirrors/generated/audit surfaces. Register new test coverage in `tests/check-map-v1.json` when needed. Include the separately reviewed repair for https://github.com/wildcat-finance/skills/issues/1596 in `plugins/homologia/tests/test_check.py` by merging its original signed delivery after its pull request has landed. Preserve its bounded wait for an observed ctime transition, same inode, size and restored mtime checks, and final `HOM-CHECK-PATH` refusal. Preserve Homologia runtime bytes and both prior failed selected-check reports. Refresh the common census after composition. Include the published replay and amendment recovery repairs from https://github.com/wildcat-finance/skills/pull/1808, https://github.com/wildcat-finance/skills/pull/1830 and https://github.com/wildcat-finance/skills/pull/1837. Merge original signed commit `a5c02d67c3bab71ec3d8317b2be2430969237320` with its two signed Homologia ancestors. Apply the published deltas from `a0bf6ef1075250574771d5ad2b1e3c08b1164f77`, `da4a2c1064afeea6b3c41f6b56d47a6d702b87f1` and `d88167b6e98706c7a023030ef3b422d8eab7e6af` as reviewed source changes in a new locally signed composition. The effective-command bound from https://github.com/wildcat-finance/skills/pull/1814 preserves the later Protasis version history. Retain the original commits, patch digests, attribution and failed direct-merge candidate; their published ancestry must remain in the final base composition. Include their tests, package metadata, governed version records and owner-derived digests. These maintenance repairs are the only Fiat behavior changes admitted in Step 2. Preserve the native package version above Hexaemeron 1.6.73 and retain the accepted applicability implementation. Refresh exact public study and runbook mirrors, scaffold expectations, owner-derived provenance and the common census after amendment and composition. Preserve all earlier reports.

**Why.** Merging the latest maintenance commit directly would add GitHub-created ancestors to Step 2's local signature range. Fiat's read-only signature check refused that source ancestry. The revised composition applies the same published repairs through locally signed contributions while preserving the original source commits and evidence. The checked signature rule remains unchanged. All 37 selected checks and a fresh registered Step 2 Exit must pass before implementation is receipted.

**Steps touched.** Step 2.

**Still holding.** Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.
