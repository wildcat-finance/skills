# Runbook: Solidity event AST and ABI agreement

The user authorised the generic validator and Wildcat V1/V2 validation on 2026-09-26. The seven remaining venue corpus handoffs and rebuilds still block full issue 1366 completion. This tranche produces reviewed code and verified Wildcat outputs. It does not close the issue or receipt final integration.

The source is the receipted study at `.hexaemeron/study.md`. The initial product is `df56bfeec3f67bf689d0b083c4994fc329477905`; Python is `3.14.6`. Preserve the accepted private archive and the held return/mutability frontier. No new dependency, schema, CI workflow or deployment claim is authorised.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 8d79dece25d42cf1528280b487803020a5994518b986f35531ecec774ab0396f
candidate | compiler-membership
```

```version-relations
lemma | plugins/lemma/skills/lemma/EVOLUTION.md | next-generation-after-integration-base
```

Known-failure assignment: `kf-1366-indexed-bit` -> Step 1

The capture's reporter does not exist in the initial product. Inoculation creates only its four declared guard files and retains the real parent assertion failure. It exercises the existing production entrypoint in-process with a preserved real compiler output, so the parent guard needs no child process. Its healthy control must write a corpus before the divergent indexed-bit case is tested.

After inoculation introduces that reporter, review its complete source and parser, then append a runbook amendment registering the exact source digest and replacing the Tests field for Steps 1 through 3 with its event-specific command. This registration is required before any audit or implementation receipt. The bootstrap Elenchus command below covers controller integrity only and establishes no event regression evidence; it must not be used to classify an event fix. Source changes to a registered reporter require a fresh reviewed amendment.

Every step retains its own full source-bound audit, prose and signed delivery gates. Regenerate portable copies and both Horos artefacts after tracked changes, before the staged-tree green record. Tests use synthetic public compiler fixtures; private standard JSON and corpus bytes remain outside Git. Existing tools and workflows satisfy the toolchain, licence and CI scaffold requirements.

## Step 1: Reject divergent event metadata

**Goal.** Add the event agreement validator and close the reproduced indexed-bit failure.

**Entry.** The run branch at the initial product, followed by the controller's exact signed guard commit and complete inoculation receipt.

**Exit.** Selected contract, abstract-contract, interface and library event inventories are checked before chunk emission. The parent accepts the divergent bit; the fixed tree rejects it with the intended event diagnostic and no new output. Healthy controls remain green. Commit the study and runbook copies, record the event extension in the canonical skill, ledger and invariants, preserve the held frontier, and update the Lemma package and generated copies. The affected-scope gate is `python3 scripts/run_checks.py --scope lemma --scope root`.

**Files.** The four captured guard paths; `plugins/lemma/chunkers/solidity.py`; `plugins/lemma/tests/test_solidity.py` if existing fixtures need faithful compiler evidence; `tests/check-map-v1.json` to add the event suite to the existing Lemma scope; `plugins/lemma/INVARIANTS.md`; `plugins/lemma/skills/lemma/SKILL.md`; `plugins/lemma/skills/lemma/EVOLUTION.md`; Lemma package manifests and marketplace version entries; `docs/lemma-event-agreement/study.md`; `docs/lemma-event-agreement/runbook.md`; generated portable copies, Horos artefacts and the step's audit record.

**Tests.** Retain the capture's exact indexed-bit guard, with a successful control and an assertion failure on the starting product. Cover multiset membership, overloads, inherited and qualified events, event-only owners, empty event inventories, explicit indexed and anonymous booleans, selected-source evidence and primitive wire types. Preserve existing callable and provenance cases. The checked runner must execute both Lemma suites and the new event suite, with no skipped event case. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py {report}`; format: `unittest-json-v1`; report file: `.elenchus/issue-1366-step-1.json`. Replace this bootstrap declaration through the reviewed reporter amendment above before implementation receipt or audit.

**Disciplines.** phylax: compiler JSON is untrusted and every membership/type lookup must fail closed. ephoros: bounded diagnostics name source, owner and event; no unattended service is added. metron: no extra compiler invocation and no speed claim. elenchus: retain the indexed-bit parent failure and fixed-tree pass. hypomnema: put the selected membership design and rejected inheritance walk in the governed ledger and preserve committed study/runbook copies.

## Step 2: Cover wire types and whole-build refusals

**Goal.** Complete strict event-type handling and discriminating CLI refusal coverage.

**Entry.** The reviewed and pushed Step 1 head, including the retained guard and green validator.

**Exit.** Independent AST resolution covers arrays, struct tuples, contract addresses, enums, user-defined value types and external function types where the pinned compiler shape supplies sufficient evidence. Unsupported, missing, cyclic or malformed shapes refuse by name. The real entrypoint refuses missing/extra events, duplicate-row changes, parameter order/type changes, every indexed-bit flip and anonymous changes. A mismatch in the second compilation unit leaves no first-unit corpus; pre-existing output bytes remain unchanged. The affected-scope gate is `python3 scripts/run_checks.py --scope lemma --scope root`.

**Files.** `plugins/lemma/chunkers/solidity.py`; `plugins/lemma/tests/test_events.py`; synthetic standard JSON and compiler-output fixtures under `plugins/lemma/tests/fixtures/issue-1366/`; existing Lemma tests only for related coverage; the invariant and specification copies when their evidence changes; generated portable copies, Horos artefacts and the step's audit record. Keep the registered reporter unchanged unless a source-bound amendment rebinds it first.

**Tests.** Use preserved real outputs from solc 0.8.22, 0.8.25 and 0.8.28. Compare full event descriptor multiplicities, including equal signatures with distinct declarations. Mutate ABI wire types while retaining internalType, nested tuple components and ordered flags. Check excluded dependency declarations, unresolved IDs, non-boolean flags, missing usedEvents and excessive or cyclic type expansion. Healthy controls must succeed using valid source refs and existing parent directories. Run direct in-process guards without child processes and separate CLI controls with pinned compilers. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py {report}`; format: `unittest-json-v1`; report file: `.elenchus/issue-1366-step-2.json`. The reviewed reporter amendment must supersede this bootstrap declaration before implementation receipt or audit.

**Disciplines.** phylax: bound type traversal and diagnostics, with no silent coercion or fallback. ephoros: each refusal identifies the failed event relation and output state. metron: preserve zero extra compiler calls and record any traversal bound. elenchus: each rejection has a healthy control and fails for its intended assertion on the unfixed tree. hypomnema: document supported shapes and named refusals beside their owning code.

## Step 3: Verify the Wildcat successor corpus

**Goal.** Demonstrate complete synthetic conformance and reproducible Wildcat rebuilding under the changed validator.

**Entry.** The reviewed and pushed Step 2 head, the exact accepted archive from the study and its verified external extraction.

**Exit.** Run the immutable design record's selected conformance resolver and pass its step:3 gate. Rebuild all 10 accepted Wildcat partitions twice to fresh external destinations under an explicit successor tool binding. Compare chunk IDs, schema, event census and corpus bytes with the accepted baseline; explain provenance changes caused by the new tool version and hashes. Retain exact commands, compiler/input/tool/output digests, wall time and sizes in the external report. Commit a concise validation record containing no private source or corpus bytes, and link the unresolved venue obligations to issue 1359. The checked repository gate is `python3 scripts/run_checks.py --scope lemma --scope root`. Full issue closure and base integration remain blocked on the other venue results.

**Files.** `docs/lemma-event-agreement/validation.md`; a source-owned verifier or build adapter under `plugins/lemma/tests/` only if needed to bind successor tool bytes without editing the accepted bundle; synthetic fixtures or tests for any discovered related refusal; specification and invariant updates if required; generated portable copies, Horos artefacts and the step's audit record. External successor outputs and reports stay in the run's existing private work directory.

**Tests.** The selected conformance report must contain actual executed test results, never a predicted pass. Verify all accepted input members and original digests before and after both builds. A second build must reproduce the first. Re-run the indexed-bit rejection, named unsupported-shape rejection and late multi-unit no-output test. Record all seven missing venue-generation results as unavailable rather than passed. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py {report}`; format: `unittest-json-v1`; report file: `.elenchus/issue-1366-step-3.json`. The reviewed reporter amendment must supersede this bootstrap declaration before implementation receipt or audit.

**Disciplines.** phylax: keep private inputs outside Git and bind the successor tool explicitly. ephoros: retain exact per-partition coverage and refusals. metron: record observed duration and sizes against the preserved baseline without claiming an improvement. elenchus: preserve any failed rebuild and diagnose before rerunning. hypomnema: the validation record states exact scope, commands, results and missing venue evidence.

### Amendment -- 2026-09-26

**What changed.** Complete replacement Tests: Every step runs the event suite through the reviewed reporter. Elenchus command: `python3 plugins/lemma/tests/emit_issue_1366_report.py --case event-tests --report {report}`; format: `unittest-json-v1`; report file: `.elenchus/issue-1366-event-audit.json`. Step 1 retains the exact indexed-bit parent assertion failure and its successful control, then covers multiset membership, overloads, inherited and qualified events, event-only owners, empty inventories, explicit indexed and anonymous booleans, selected-source evidence and primitive wire types. Preserve callable and provenance cases. The checked runner executes both existing Lemma suites and the event suite without skipped event cases. Step 2 adds preserved real outputs from solc 0.8.22, 0.8.25 and 0.8.28; full descriptor multiplicities, including equal signatures with distinct declarations; wire-type mutations retaining internalType; nested tuple components and ordered flags; excluded dependency declarations; unresolved IDs; malformed flags; missing usedEvents; and excessive or cyclic expansion. Healthy controls use valid source refs and existing output parents. Direct guards run in-process without children, with separate CLI controls using pinned compilers. Step 3 requires actual executed conformance results, verifies every accepted input and original digest before and after two fresh builds, and requires the second build to reproduce the first. Re-run indexed-bit, named unsupported-shape and late multi-unit no-output cases. Record the seven missing venue-generation results as unavailable.

**Why.** The signed guard introduced the reporter after the original runbook receipt. Its complete source and literal parser have now been reviewed. It emits actual unittest counters, refuses an existing report and traverses report descendants without following symlinks. Its path repair accepts an alias of the same worktree root; five focused cases check root aliases, descendant symlinks, outside paths, traversal and existing bytes. The retained parent failure still binds the original signed guard. This registration binds the repaired reporter used by subsequent implementation and audit gates. The design lock, version relation, indexed-bit assignment, private-input boundary and seven outstanding venue obligations are unchanged.

```command-interfaces
schema | protasis-command-interfaces/v1
plugins/lemma/tests/emit_issue_1366_report.py | build_parser | 408b7b4310c2e298a6af44370773cec385e44317ef3a1e810d2fb5925b9d4985
```

**Steps touched.** Step 1, Step 2, Step 3.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds.
