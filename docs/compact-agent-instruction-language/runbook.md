# runbook: compact lossless agent instruction language

This runbook derives from `.hexaemeron/study.md` at SHA-256
`28c3319301ea86e91bc872d1b803fc1b7e00b9b5a9826c2b0e990d2f7d7f64aa`.
It delivers the bounded `wildcat-agent-instruction/v1` prototype in five
ordered pull requests. Human Markdown remains authored; compact files remain
derived. No step changes another repository, creates a plugin, changes CI, or
makes the compact form authoritative.

## Step 1: Freeze the language contract and scaffold

**Goal.** Commit the accepted study and runbook beside an ADR, public version-1 contract, machine-readable schema, and the smallest test scaffold that fixes the prototype's authority and file layout before implementation.

**Entry.** The run branch at `d1ca7ba5af741d45d1da6492632661e688157bff`, with the study receipted at SHA-256 `28c3319301ea86e91bc872d1b803fc1b7e00b9b5a9826c2b0e990d2f7d7f64aa`, this runbook receipted, and no implementation files present.

**Exit.** `docs/compact-agent-instruction-language/study.md` and `docs/compact-agent-instruction-language/runbook.md` match the receipted artifacts byte for byte; ADR-051 records the source/derived authority direction and rejected designs; the public contract fixes the semantic model, grammar, opcode table, bounds, compatibility and recovery; the JSON schema parses; the existing repository licence and Python pin are cited rather than duplicated; no CI file changes; and `python3 -m unittest tests.test_agent_instruction.AgentInstructionScaffoldTests -v`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/compact-agent-instruction-language/study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/compact-agent-instruction-language/runbook.md`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/decisions/ADR-051-encode-a-closed-agent-instruction-model.md docs/agent-instruction-language-v1.md docs/compact-agent-instruction-language/study.md docs/compact-agent-instruction-language/runbook.md`, and `git diff --check` each exit 0.

**Files.** Create `docs/compact-agent-instruction-language/study.md`, `docs/compact-agent-instruction-language/runbook.md`, `docs/decisions/ADR-051-encode-a-closed-agent-instruction-model.md`, `docs/agent-instruction-language-v1.md`, `schemas/agent-instruction-v1.schema.json`, `scripts/agent_instruction.py`, `tests/test_agent_instruction.py`, and `tests/fixtures/agent-instruction-v1/README.md`; change no CI, licence, toolchain-pin, plugin, Promise Machine, or external-repository file.

**Tests.** Add `AgentInstructionScaffoldTests` for the receipted-copy digests, schema JSON loading, version magic, CLI help link, fixture-layout sentinel, existing licence, and `.python-version`; the expected focused count is at least 8. If audit finds a defect, run `python3 -m unittest tests.test_agent_instruction.AgentInstructionScaffoldTests -v > {report} 2>&1`; the report format is UTF-8 unittest text and the report file is `.hexaemeron/audit-fix-step-1.txt`.

**Disciplines.** phylax: the public contract and schema freeze the later untrusted-input boundary but this step opens no decoder or subprocess path. ephoros: none, this step creates no unattended runtime. metron: none, this step makes no compression or performance claim. elenchus: use the declared focused documentation test if review exposes a reproducible contract or copy defect. hypomnema: ADR-051 records the expensive-to-reverse authority direction, semantic boundary, alternatives, and consequences.

## Step 2: Build the bounded canonical model and compact codec

**Goal.** Implement the standard-library validator, canonical JSON loader, deterministic formatter, strict decoder, confined file output, and stable refusal codes for the closed version-1 model.

**Entry.** Step 1's pull request is integrated into the run branch; its copied study and runbook, ADR, public contract, schema, CLI scaffold, and scaffold tests are green and unchanged in authority.

**Exit.** The CLI validates canonical models, emits one compact byte string, decodes it to the same canonical JSON bytes, reproduces the compact bytes on a second format pass, refuses every unknown or malformed version-1 construct without returning a partial model, confines regular input and atomic output paths, and `python3 -m unittest tests.test_agent_instruction.CanonicalModelTests tests.test_agent_instruction.CompactCodecTests tests.test_agent_instruction.PathBoundaryTests -v`, `python3 scripts/agent_instruction.py self-test`, `python3 -m unittest discover -s tests`, and `git diff --check` each exit 0.

**Files.** Change `scripts/agent_instruction.py`, `schemas/agent-instruction-v1.schema.json`, `docs/agent-instruction-language-v1.md`, and `tests/test_agent_instruction.py`; create bounded valid and invalid samples only under `tests/fixtures/agent-instruction-v1/codec/`.

**Tests.** Extend `tests/test_agent_instruction.py` with canonical-model, compact-codec, and path-boundary cases covering every opcode and field, exact ordering, final newline, two-space nesting, length-prefixed UTF-8 literals, escapes, duplicate keys, unsupported values, reference closure, precedence cycles, scope and exception targets, resource caps at limit and limit-plus-one, symlink and special-file refusal, atomic output, round-trip byte equality, and formatter idempotence; the expected focused count is at least 55. If audit finds a defect, run `python3 -m unittest tests.test_agent_instruction.CanonicalModelTests tests.test_agent_instruction.CompactCodecTests tests.test_agent_instruction.PathBoundaryTests -v > {report} 2>&1`; the report format is UTF-8 unittest text and the report file is `.hexaemeron/audit-fix-step-2.txt`.

**Disciplines.** phylax: this step parses attacker-controlled JSON and compact bytes and opens confined file paths, so bounded input, duplicate rejection, no shell, symlink refusal, and atomic writes are gates. ephoros: emit only one bounded validation or round-trip result with stable code, digest and node path because the CLI is operator-run rather than unattended. metron: none, correctness and bounds are claimed but no speed or token saving is measured. elenchus: every found codec defect must retain its minimal failing bytes and stable risk id as a regression before repair. hypomnema: update the public contract for externally visible grammar or refusal semantics; use comments only for local parser invariants.

## Step 3: Bind the three fixtures and hostile mutations

**Goal.** Add reviewed source-to-model bindings, deterministic compact views, closed questions, and hostile mutations for the Promise Machine router, Fiat study/runbook phase, and Horos boundary contracts.

**Entry.** Step 2's pull request is integrated; all version-1 model and codec tests pass, and the three authored Markdown sources still match the study's entry-ref bytes.

**Exit.** One manifest binds exactly three fixtures to source blob digests and byte spans; each canonical model validates, round-trips, and regenerates its checked-in compact bytes; every governed source span maps to a reviewed node; each question has a closed accepted and refusal answer set; each required negation, precedence edge, scope, evidence class, authorisation, recovery, and exact-literal class has a hostile mutation whose expected structural, digest, or answer change is declared; stale source or derived bytes refuse; and `python3 scripts/agent_instruction.py check --manifest tests/fixtures/agent-instruction-v1/manifest.json`, `python3 -m unittest tests.test_agent_instruction.FixtureBindingTests tests.test_agent_instruction.MutationTests -v`, `python3 -m unittest discover -s tests`, and `git diff --check` each exit 0.

**Files.** Change `scripts/agent_instruction.py`, `tests/test_agent_instruction.py`, and `docs/agent-instruction-language-v1.md`; create `tests/fixtures/agent-instruction-v1/manifest.json`, `tests/fixtures/agent-instruction-v1/manifest.schema.json`, and fixture-owned model, compact, question, mutation, and source-span metadata below `tests/fixtures/agent-instruction-v1/promise-machine-router-selection/`, `tests/fixtures/agent-instruction-v1/fiat-study-runbook-phase/`, and `tests/fixtures/agent-instruction-v1/horos-boundary-check/` without editing their authored source documents.

**Tests.** Add `FixtureBindingTests` and `MutationTests` for exact source blobs and spans, manifest closure, deterministic regeneration, missing or overlapping bindings, stale derivatives, closed answer vocabularies, fresh-context inventory shape, and at least one mutation for each risk class named in the exit; the expected focused count is at least 35 and the manifest must enumerate the exact mutation count. If audit finds a defect, run `python3 -m unittest tests.test_agent_instruction.FixtureBindingTests tests.test_agent_instruction.MutationTests -v > {report} 2>&1`; the report format is UTF-8 unittest text and the report file is `.hexaemeron/audit-fix-step-3.txt`.

**Disciplines.** phylax: manifest paths and fixture bytes are untrusted inputs and remain confined, regular, digest-bound, and non-executable. ephoros: check emits bounded binding, round-trip, mutation, and run-summary records keyed by manifest and fixture digests. metron: none, this step records raw material but makes no tokenizer or savings verdict. elenchus: a silently accepted hostile mutation is a finding and receives a minimal regression tied to its risk id before any fix. hypomnema: fixture review provenance and source spans live in the manifest; the public contract changes only if a supported semantic construct changes.

## Step 4: Add exact tokenizer measurement and isolated family parity

**Goal.** Implement bounded external adapters and record reproducible local evidence for bootstrap-inclusive token size and fresh-context question parity without making either adapter part of instruction authority.

**Entry.** Step 3's pull request is integrated; the exact three-fixture corpus and mutation set pass; a local non-credentialed tokenizer profile and two locally available, genuinely distinct model-family profiles can be named with executable, model or vocabulary, argv, context, and acquisition digests. If those profiles cannot be established without a new dependency, network acquisition, credential, paid endpoint, source exfiltration, or licence choice, stop at this entry and ask the operator rather than weakening the evidence contract.

**Exit.** `measure` records source, canonical-model, compact, complete decoder-bootstrap, one-document, and corpus-amortised bytes and real-model tokens under an exact local profile; the bootstrap-inclusive three-fixture compact total is strictly smaller than the source total; `parity` launches each question separately for two recorded local family identities, preserves every bounded answer or refusal, and reports equal required source-versus-compact answer ids with no unlisted value; missing identities, changed digests, alias-only families, unavailable runs, contaminated context, heuristic counts, non-integer counts, timeouts, or failed mutations refuse; and `python3 scripts/agent_instruction.py measure --manifest tests/fixtures/agent-instruction-v1/manifest.json --output tmp/agent-instruction-v1-measurement.json`, `python3 scripts/agent_instruction.py parity --manifest tests/fixtures/agent-instruction-v1/manifest.json --output tmp/agent-instruction-v1-parity.json`, `python3 -m unittest tests.test_agent_instruction.MeasurementTests tests.test_agent_instruction.ParityAdapterTests -v`, `python3 -m unittest discover -s tests`, and `git diff --check` each exit 0.

**Files.** Change `scripts/agent_instruction.py`, `tests/test_agent_instruction.py`, `tests/fixtures/agent-instruction-v1/manifest.json`, `tests/fixtures/agent-instruction-v1/manifest.schema.json`, and `docs/agent-instruction-language-v1.md`; create checked, non-secret tokenizer and family profile records plus deterministic bounded measurement and parity records below `tests/fixtures/agent-instruction-v1/evidence/`; do not add a dependency, credential, provider SDK, CI job, or model response outside the declared bounded records.

**Tests.** Add `MeasurementTests` and `ParityAdapterTests` using fake local executables for all error and boundary paths, then exercise the exact declared local profiles in the two exit commands; cover bootstrap omission, changed executable or vocabulary digest, tokenizer mismatch, negative or non-integer counts, non-negative compression delta, unavailable model, alias-only families, context reuse, unknown answer ids, output caps, timeout, secret redaction, argv lists, environment allowlists, and atomic report writes; the expected isolated unit count is at least 30 and the live evidence report states its exact case count. If audit finds a defect, run `python3 -m unittest tests.test_agent_instruction.MeasurementTests tests.test_agent_instruction.ParityAdapterTests -v > {report} 2>&1`; the report format is UTF-8 unittest text and the report file is `.hexaemeron/audit-fix-step-4.txt`.

**Disciplines.** phylax: this step launches untrusted local tokenizer and model runtimes, so explicit argv, cleared environment allowlist, timeouts, caps, no shell, no secrets, public-synthetic inputs, and digest-bound profiles are mandatory. ephoros: emit bounded measurement, parity, and run-summary records with correlation ids, counts, verdicts, unknowns and refusal codes; there is no production alert. metron: record the source baseline first, then the compact-plus-bootstrap count under the same tokenizer profile; retain per-document and amortised results and reject unlike cohorts. elenchus: adapter, count, contamination, or answer defects are reduced under the fake executable before the live profile is rerun. hypomnema: record adapter trust boundaries and evidence limits in the public contract; do not promote observations into semantic authority.

## Step 5: Register the promise and run the complete demonstration

**Goal.** Bind the prototype's structural and measurement promise to its checker and corpus, publish a plain primer with the observed limits, and run the complete issue #909 demonstration from clean repository bytes.

**Entry.** Step 4's pull request is integrated; its exact tokenizer measurement and two-family parity records satisfy their gates, all earlier fixture and mutation checks pass, and no source-authority or scope decision has changed.

**Exit.** The root Promise Machine names one bounded prototype promise whose evidence, boundary, authorisation, consequence, refusal, recovery, and exceptions match the implementation; the coverage registry binds the checker, corpus, hostile fixtures and reports; generated Promise Machine copies are current; a human primer states what the language can do, cannot do, how to reproduce the demo, and the actual byte, token, bootstrap, mutation and family results without overstating arbitrary-English losslessness or Shoggoth readiness; the demo runs from clean bound bytes; and `python3 scripts/agent_instruction.py check --manifest tests/fixtures/agent-instruction-v1/manifest.json`, `python3 scripts/agent_instruction.py measure --manifest tests/fixtures/agent-instruction-v1/manifest.json --output tmp/agent-instruction-v1-measurement.json`, `python3 scripts/agent_instruction.py parity --manifest tests/fixtures/agent-instruction-v1/manifest.json --output tmp/agent-instruction-v1-parity.json`, `python3 -m unittest tests.test_agent_instruction -v`, `python3 -m unittest discover -s tests`, `python3 scripts/promise_machine.py check`, `python3 scripts/check_portable_runtime.py`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/compact-agent-instruction-language/study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/compact-agent-instruction-language/runbook.md`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/decisions/ADR-051-encode-a-closed-agent-instruction-model.md docs/agent-instruction-language-v1.md docs/compact-agent-instruction-language/study.md docs/compact-agent-instruction-language/runbook.md README.md PROMISE_MACHINE.md`, and `git diff --check` each exit 0.

**Files.** Change `PROMISE_MACHINE.md`, generated plugin-local Promise Machine copies through the existing sync mechanism, `tests/promise_machine_coverage.json`, `README.md`, `docs/agent-instruction-language-v1.md`, `docs/decisions/ADR-051-encode-a-closed-agent-instruction-model.md`, and the deterministic evidence records under `tests/fixtures/agent-instruction-v1/evidence/`; change implementation or focused tests only if the final demo exposes a defect through Elenchus; do not change any `EVOLUTION.md`, CI file, plugin ownership, authored fixture source, or external repository.

**Tests.** Extend Promise Machine and focused integration coverage for promise registration, generated-copy currency, exact checker and evidence paths, clean-tree reproduction, and stale-report refusal; the expected focused codec count is the Step 4 count plus at least 8 integration cases, while the full suite reports its actual discovered count. If audit finds a defect, run `python3 -m unittest tests.test_agent_instruction -v > {report} 2>&1`; the report format is UTF-8 unittest text and the report file is `.hexaemeron/audit-fix-step-5.txt`.

**Disciplines.** phylax: the final demo reruns every file, subprocess, secret-redaction, confinement, and report-integrity gate before its promise is registered. ephoros: the primer and promise cite the bounded run summary rather than an uncorrelated success claim. metron: the published saving uses only the same-profile source baseline and compact-plus-complete-bootstrap measurement and preserves all non-gating counts. elenchus: any final-suite or demo failure stops publication, is preserved, reduced, fixed at cause, and guarded before the whole matrix reruns. hypomnema: the ADR, public contract, primer, Promise Machine entry, and runbook remain the durable homes for decision, interface, front-facing capability, evidence boundary, and operator recovery respectively.

### Amendment -- 2026-08-30

**What changed.** Complete replacement Files: Create `docs/compact-agent-instruction-language/study.md`, `docs/compact-agent-instruction-language/runbook.md`, `docs/decisions/ADR-051-encode-a-closed-agent-instruction-model.md`, `docs/agent-instruction-language-v1.md`, `schemas/agent-instruction-v1.schema.json`, `scripts/agent_instruction.py`, `tests/test_agent_instruction.py`, and `tests/fixtures/agent-instruction-v1/README.md`; update `.horos/boundary.json` only with the deterministic generated count required by a fresh complete Horos scan; change no CI, licence, toolchain-pin, plugin, Promise Machine, or external-repository file.

**Why.** The first post-commit root suite reproduced `BoundaryCurrencyTests.test_the_committed_boundary_matches_a_fresh_scan`: the eight new tracked paths advanced Horos `files_walked` from 2065 to 2073 while the generated count remained stale. Root `AGENTS.md` requires the deterministic Horos write when the tracked-tree count changes.

**Steps touched.** Step 1's Files field.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.
