# Runbook: a child or a golden retriever

## Step 1: Build and publish the beginner primer package

**Goal.** Turn the receipted study into one beginner-facing, mascot-led primer package that defines Shoggoth, the Interceptor, Hexaemeron, and Fiat, shows the first safe action, and lands only on `docs/a-child-or-a-golden-retriever`.

**Entry.** Start from `docs/a-child-or-a-golden-retriever` at `fc0374bcd2d4311a2ce7d1f710e6809e40f00c92`, with `.hexaemeron/study.md` receipted by Fiat, the supplied kit fixed at SHA-256 `e09eb107921ab52e467bae54e3e605f2e01fa258df7c12529be44fc486d71218`, the ordinary security suite waived because no Solidity changes, and no authority to merge or retarget the run to `main`.

**Exit.** Commit the receipted study and runbook copies, `docs/a-child-or-a-golden-retriever.md`, `docs/a-child-or-a-golden-retriever-source-note.md`, ADR-039, two text-free mascot source illustrations, two 1672 by 941 infographic PNGs, a six-page-or-shorter horizontal-A4 primer PDF, a one-page horizontal-A4 quick-start PDF, one deterministic builder, one focused test module, the README link, and regenerated Horos records. The Markdown page must answer the study's five opening questions and include `#the-five-minute-demo`; all generated views must use the same four definitions and lifecycle, and all image typography must come from deterministic layout rather than the image model. Prove the fixed tree with `/Users/c0rtexzer0/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/build_child_or_golden_retriever_primer.py --check --report tmp/elenchus/child-or-golden-retriever-step-1.json`, `python3 -m unittest tests.test_child_or_golden_retriever_primer`, `python3 -m unittest discover -s tests`, the applicable Imprimatur, Brevitas, Phylax, Ephoros, and Hypomnema commands, the complete Horos write/check cycle, `git diff --check`, PDF metadata/text/link checks, and a fresh Poppler render of every PDF page inspected with no clipping, overlap, missing glyph, broken hierarchy, or unreadable contrast. Push the signed, audited step through Fiat, merge its stacked pull request into the requested run branch so that branch contains the package, and stop before a pull request or merge to `main`.

**Files.** Create `docs/a-child-or-a-golden-retriever.md`, `docs/a-child-or-a-golden-retriever-study.md`, `docs/a-child-or-a-golden-retriever-runbook.md`, `docs/a-child-or-a-golden-retriever-source-note.md`, `docs/decisions/ADR-039-keep-one-source-for-the-beginner-primer.md`, `scripts/build_child_or_golden_retriever_primer.py`, `tests/test_child_or_golden_retriever_primer.py`, `docs/assets/a-child-or-a-golden-retriever-mascot-roles.png`, `docs/assets/a-child-or-a-golden-retriever-mascot-fiat.png`, `docs/assets/a-child-or-a-golden-retriever-whos-who.png`, `docs/assets/a-child-or-a-golden-retriever-fiat-flow.png`, `docs/pdf/a-child-or-a-golden-retriever.pdf`, and `docs/pdf/a-child-or-a-golden-retriever-quick-start.pdf`; update `README.md`, `.horos/boundary.json`, `.horos/candidates.json`, and `.horos/census.json`. Keep request-scoped mascot references and PDF renders under ignored temporary paths and remove the reference session after image generation.

**Tests.** Add focused assertions for the four fixed definitions, the five-minute-demo anchor, lifecycle order, README discoverability, source-note digest and prompts, required output inventory, PNG signatures and exact dimensions, PDF signatures/page bounds/title/text/links, deterministic rebuild identity, contrast ratios, no generated typography in source art, no copied kit references, no JavaScript in the PDFs, and Horos coverage of every new tracked binary. The Elenchus runner contract is test command `/Users/c0rtexzer0/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/build_child_or_golden_retriever_primer.py --check --report {report}`, report format `child-or-golden-retriever-check/v1`, report file `tmp/elenchus/child-or-golden-retriever-step-1.json`. Record actual final counts rather than predicting them; a real failure is reduced and guarded before Warden reruns this command.

**Disciplines.** phylax: the supplied ZIP and image-model pixels are untrusted inputs, so pin the archive, keep references request-scoped, generate no repository text or commands, and accept only bounded regular files. ephoros: none, because the shipped package is static and the builder's named exits are build evidence rather than unattended telemetry. metron: none, because this step makes no performance claim. elenchus: the exact JSON runner above owns any reproduced build, render, link, contrast, identity, or Horos failure and its guard. hypomnema: ADR-039 owns the expensive source/output topology, while the source note owns the kit digest, exact prompts, accepted image outputs, generation tool, and visual review.
