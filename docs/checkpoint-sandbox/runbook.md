# Support macOS and Linux checkpoint sandboxes

Assuming, unless corrected:

1. The accepted study is `.hexaemeron/study.md`, SHA-256 `fee951a00369e27a0a76667277b871cdc700a192ff1ce43ac4de487c4c31e710`, from starting commit `66f52785813a8453e7c7d54f2371aa8f6e465640`. Its five issue acceptance conditions and fourteen risk-register entries govern this delivery.
2. The installed Hexaemeron 1.6.64 / Fiat 6.70.1 remains this run's controller. Product edits never replace that active controller. Commands use Python 3.14.6, the locked test dependencies and the existing pinned cosign 3.1.3 profile.
3. The user authorised issue #1794, an apt-available Linux sandbox and the required macOS/Linux conformance coverage. Bubblewrap is already installed and works without elevation on this Linux host. Hosted macOS evidence is still due; no local result supplies it.
4. The existing repository supplies the licence, toolchain and CI scaffold. Step 1 repairs the reproduced Linux prerequisite failure while adding its smallest complete backend; Step 2 adds focused hosted coverage. No step may hand its successor a failing local suite.

The selected construction is `bubblewrap-seccomp`. It retains the macOS sandbox-exec policy and adds an explicit Linux x86_64 Bubblewrap/seccomp backend. Network denial, exact evidence and bounded execution are the capability; the study's filesystem, archive, service and untested-ABI limits remain in force.

```design-lock
schema | protasis-design-evidence/v1
sha256 | e14b0b4200982158b058b8b0059bda592fb2bda2784bff92380df492fe770256
candidate | bubblewrap-seccomp
```

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
```

The independently reviewed audit inventory contains no still-open finding requiring a new parent guard. It binds three complete source/view pairs and the independently derived empty expected-id set. There are no known-failure assignment lines. Each step still requires its exact source-bound no-known-findings inoculation record and receipt. This says nothing about the present Linux failure: Step 1 preserves its real baseline and earns a focused behavioral red/green guard before changing the product.

Every step retains its exact source specification and runs its registered Exit commands through Fiat's observed-criteria route on the signed implementation head. Commands below match the study exactly. The checked runner includes every actual changed path in addition to its requested scope. Reports belong to the observed attempt; preserve failed attempts and use the supported retry route instead of replacing accepted evidence. Elenchus's no-child contract remains unchanged; retain an inconclusive native-run verdict if that is what occurs, alongside separate real execution evidence.

Common Files for both steps include the new mirrors at `docs/checkpoint-sandbox/study.md` and `docs/checkpoint-sandbox/runbook.md` only when an authorised append-only amendment changes their source, the run's append-only audit pair under `audit/rounds/`, and necessary owner-generated release, portable-runtime and Horos outputs. Preserve historical studies, audits, source pins and receipts. New public research contains only synthetic or already-public material; never copy service checkpoints, credentials or controller state into the product.

Each step changing Hexaemeron package bytes increments its package above that step's PR base in `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `.agents/plugins/marketplace.json` and `.claude-plugin/marketplace.json`, with matching owned version expectations. The governed Fiat generation increments once for this delivery, in Step 1; retain its held frontier and every other skill's counters. Integration resolves the declared relation against the actual base. Step 2 keeps that governed generation while publishing its completed evidence.

Before recording green, regenerate the release corpus/manifest/consumer lock through their owner, portable runtime through its owner, and Horos boundary when needed and census always. Register new paths in the check map where their existing owner does not cover them. Run the selected repository checks before push and the tracked staged-tree commit gate before commits. Author and commit as `Dave Coleman <dave@wildcat.finance>`, sign with the configured kethcode SSH identity and verify every commit locally. End each contributed commit message exactly once with `Co-authored-by: Shoggoth <shoggoth@wildcat.finance>` and `Wildcat-Origin: shoggoth`.

The publication order is Sapheneia, Imprimatur, Vulgate with protected-content comparison, then Imprimatur again; apply Brevitas where its contract includes the record. Fixed generated and digest-bound evidence remains exact. Ask only if a new privilege model, broader containment, another ABI or unrelated delivery becomes necessary. Never weaken a limit, skip a positive host case, use an unsandboxed fallback, fabricate hosted execution or rewrite an earlier run to clear a gate.

## Step 1: Add Linux checkpoint network denial

**Goal.** Repair the Linux prerequisite with the selected packaged backend and independent report fixtures, while publishing the specification and proving the complete local release path.

**Entry.** Begin at the controller's exact Step 1 parent, initially `66f52785813a8453e7c7d54f2371aa8f6e465640`, after the no-known-findings inoculation receipt. The two captured Python 3.14.6 reproductions fail with network-denial-unavailable. This issue explicitly repairs that baseline failure; the entry does not claim a green Linux parent. The accepted study and fixed design select bubblewrap-seccomp.

**Exit.** Commit exact study/runbook/design copies and the executed selection evidence with clear research provenance. Add the explicit Linux x86_64 backend, retain the macOS policy and reject other ABIs or unavailable capabilities before verifier execution. Bind ABI, exact fixed argv, seccomp bytes, actual launcher and direct/exec probe results. Separate pure report validation from host preparation while preserving complete case accounting. Run the real release owner and all five pinned cosign outcomes. Implement and execute the selected product-linux and hostile-evidence resolvers before Step 2 opens; their reports must match the immutable design paths. Publish the support/refusal guide and the normal Fiat generation decision, then run its Hypomnema design bridge. The proving Exits are `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .hexaemeron/reports/step-1-hexaemeron.json` and `python3 scripts/run_checks.py --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-1-checks.json`. Both must exit zero, as must the controller-observed root and Hexaemeron implementation suites.

**Files.** Add `docs/checkpoint-sandbox/study.md`, `docs/checkpoint-sandbox/runbook.md`, `docs/checkpoint-sandbox/design-evidence.json`, `docs/checkpoint-sandbox/reports/selection/` and the minimal source/provenance records required to explain and reproduce those public measurements. Update `plugins/hexaemeron/skills/fiat/scripts/checkpoint_authority/network.py`, `demo.py` and `release_conformance.py`; a focused sibling policy module belongs in this same package only if separation makes the ABI/argv contract easier to review. Update `release.py` and conformance source/dependency inventories for every added executable source. Extend `plugins/hexaemeron/tests/test_checkpoint_authority_release.py`, `test_checkpoint_authority_release_conformance.py` and the owned release suite/corpus, and add focused `test_checkpoint_network.py` plus `checkpoint_network_design_report.py` for the two due local resolver operations. Update `docs/checkpoint-authority/release.md`, `plugins/hexaemeron/skills/fiat/references/checkpoint-authority.md`, Fiat `SKILL.md` and `EVOLUTION.md`, package manifests, marketplace/version expectations and all common generated/audit surfaces. Reuse `signatures.py` unchanged unless a demonstrated narrow compatibility defect requires its repair under the same limits. Do not change the registered test-runner or checked-runner CLI source, unrelated controller behavior or Elenchus.

**Tests.** Preserve a pure-Python behavioral guard that fails on the exact parent for the Linux selection or fixture-construction defect and passes after the fix. It may use explicit capability fixtures but cannot claim enforcement. Test closed supported/unsupported selection, missing launcher/capability, ABI and x32 rejection, policy/argv identity, launcher/verifier mutation, exact fields/types/counts, missing or altered direct/exec results, incomplete cases and ineffective-policy rejection. Execute real direct and descendant IPv4/IPv6 bind/connect denial, an allow-all negative, pinned cosign agreement, timeout/output refusal and descendant lock release. Preserve all five historical guards named by the study. Report fixtures must work without any host launcher. New design resolver operations must refuse unsupported candidates and unavailable operations, preserve source/evidence identity and refuse replacing reports. Retain actual discovered test counts. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-1-audit-guard.json`.

**Disciplines.** phylax: fixed argv, syscall ABI, tool pins, file readers and evidence admission define the security boundary. ephoros: retain bounded backend/policy/probe identity and actionable capability refusals. metron: retain existing time/output/RSS limits and measured research without a speed claim. elenchus: guard the reproduced compatibility failure and preserve actual outcomes. hypomnema: record the selected policy and rejected namespace-only construction in the existing generation ledger and support guide.

## Step 2: Prove checkpoint conformance on macOS and Linux

**Goal.** Complete independent hostile coverage and prove the released implementation on actual Ubuntu and macOS runners before integration.

**Entry.** The exact Step 1 head is green, audited and pushed, its product-linux and hostile-evidence reports pass the design gate at step:2, and Step 2 has its no-known-findings inoculation receipt. The active installed controller and selected design are unchanged. Hosted macOS/Linux acceptance remains pending.

**Exit.** Add a focused Ubuntu 24.04 x86_64 and macOS 15 arm64 workflow with the pinned Python and cosign profiles. Run the real product release path on each, retaining complete raw network/release evidence, runner/tool identity, actual checkout object and separately named event-head/merge identities. Download successful job artifacts with authenticated run/job metadata and verify matching source/fixture digests through the selected hosted-linux and hosted-macos resolver commands before integration. Missing, synthetic, skipped, stale, wrong-host or incomplete evidence refuses. Add independent weakened-policy/report mutation and hosted-evidence negative tests. Demonstrate the real local release path with unchanged workload/limits and publish the tested support/recovery instructions. The proving Exits are `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .hexaemeron/reports/step-2-hexaemeron.json` and `python3 scripts/run_checks.py --scope hexaemeron --jobs 8 --report .hexaemeron/reports/step-2-checks.json`. Both must exit zero, as must the controller-observed root and Hexaemeron implementation suites; both actual hosted design gates must pass before the completed stack can integrate.

**Files.** Add `.github/workflows/checkpoint-conformance.yml` and update its explicit interpreter/path-filter inventory in `tests/test_python_contract.py`. Extend `plugins/hexaemeron/tests/checkpoint_network_design_report.py` with the two hosted-artifact operations and add focused host-evidence validation tests. Extend `test_checkpoint_network.py`, `test_checkpoint_authority_release_conformance.py` and the owned release suite for independent hostile cases and complete accounting. Add a narrow checkpoint conformance workflow entry script only if needed to retain the exact closed host/source/tool evidence, and register that source with its owner. Update `docs/checkpoint-authority/release.md`, `docs/checkpoint-sandbox/` evidence/recovery guidance, `plugins/hexaemeron/README.md`, the checkpoint authority reference, package manifests/version expectations and all common generated/audit surfaces. Preserve the single governed generation and held frontier from Step 1. Broader CI suites and service implementations are outside the delta.

**Tests.** Execute the final owned release suite locally, preserving every positive/negative case, exact denominator, three fresh-process workload samples and tool/source bindings. Require real sandbox use and no skip on both hosted profiles. Check recorded host OS/architecture against the job and actual runtime, checkout SHA against the downloaded metadata, source and fixture digests against the product, report hashes against artifact contents, and closed report shapes. Mutations must refuse a forged successful count, substituted policy/argv/ABI, missing descendant result, allow-all filter, wrong checkout/host, omitted artifact, failed job, stale source, path escape, malformed or excess evidence, and incomplete execution. Keep trusted GitHub readback distinct from offline fixture validation. Preserve failed runs and use fresh evidence after a repair. Elenchus command: `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; format: `unittest-json-v1`; report file: `.hexaemeron/reports/step-2-audit-guard.json`.

**Disciplines.** phylax: validate hosted metadata and bounded artifact bytes before evidence admission; no workflow secrets are required. ephoros: expose the missing or failed host criterion and exact run/source identities. metron: retain the fixed workload and resource ceilings on both profiles. elenchus: independent hostile mutations and actual failing jobs receive cause analysis and guards. hypomnema: publish tested profiles, package setup, artifact recovery and the exact limits supported by completed evidence.

The final demonstration uses the owned checkpoint-authority conformance entrypoint and its released-interoperability criterion, as section 1 of the study states. It is executed separately from the registered Exit commands; a CLI fixture or a gate-interface check never counts as its execution. Final reports identify the source, actual backend and bounded positive and negative outcomes without claiming whole-host containment.

At integration, the run PR closes `wildcat-finance/skills#1794` and carries the issue's service-intake, disposable-worker-isolation and native-guard-admission dispositions, plus any genuinely new outstanding item. The original halted runs remain available for a later authorised continuation; completing this prerequisite does not advance their ledgers.
