# Runbook: Protect bare evidence tokens in Brevitas

This runbook derives from the receipted study at SHA-256 `aa17fa62c25cbf440715a77f380870024ca623db0d1abed2a52c174c1a27e42d`. It changes one lexical evidence-preservation capability and leaves the held corpus, frontier revision, frontier digest, frontier status, and Next Fiat job unchanged.

```version-relations
brevitas | plugins/brevitas/skills/brevitas/EVOLUTION.md | next-generation-after-integration-base
```

## Step 1: Protect bare digests, Git SHAs, and selectors

**Goal.** Extend Brevitas's source comparison with closed, category-specific protection for bare SHA-256-shaped digests, Git object ids, and 4-byte selectors; document and demonstrate the exact lexical boundary without advancing the held frontier.

**Entry.** Branch from run branch `fiat/372-protect-bare-evidence-tokens-in-brevitas` at `646dbff7c12202e8b9417ac51546d72b27dde5e3`, with the study receipt above, the run branch published, 70 Brevitas tests green, and the recorded direct specimen still exiting 0 after all three requested token families are removed.

**Exit.** Commit byte-identical copies of `.hexaemeron/study.md` and `.hexaemeron/runbook.md` under `docs/brevitas-evidence-tokens/`. In `protected_tokens()`, recognise exact boundary-delimited bare 64-hex digests, exact `0x` plus 8-hex selectors, boundary-delimited full 40-hex Git object ids, and 7-to-39-hex Git abbreviations only inside the study's closed Markdown-code, explicit-label, or `owner/repository@oid` contexts. Keep literal comparison case-sensitive, precedence explicit, and existing transaction-hash, address, `file:line`, numeric, diagnostic-code, and multiplicity semantics unchanged. Update the canonical evidence-preservation prose, Brevitas's governed ledger, package manifests, version-propagation expectation, and only the marketplace surfaces proved stale by a cold read. Append exactly one generation row relative to the integration base while retaining every held-frontier field and digest. Regenerate the portable Promise Machine runtime, then Horos, through their repository scripts rather than editing generated bytes. Demonstrate the known failure as guarded by running `python3 -m unittest plugins.brevitas.tests.test_brevitas.BrevitasTests.test_source_evidence_protects_new_hex_token_families -v`; prove the complete step with `python3 plugins/brevitas/tests/run_tests.py .elenchus/brevitas-372.json`, `python3 -m unittest discover -s plugins/brevitas/tests -t plugins/brevitas`, `make -C plugins/brevitas/skills/brevitas test`, `python3 scripts/portable_promise_machine.py check`, `python3 scripts/run_checks.py`, and `git diff --check`.

**Files.** Create `docs/brevitas-evidence-tokens/study.md` and `docs/brevitas-evidence-tokens/runbook.md`. Change `plugins/brevitas/skills/brevitas/scripts/brevitas.py`, `plugins/brevitas/tests/test_brevitas.py`, `plugins/brevitas/skills/brevitas/SKILL.md`, `plugins/brevitas/skills/brevitas/EVOLUTION.md`, `plugins/brevitas/AGENTS.md`, and `plugins/brevitas/README.md` only where the implemented promise makes current wording incomplete. Change `plugins/brevitas/.claude-plugin/plugin.json`, `plugins/brevitas/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and `tests/test_version_propagation.py` for the package patch. Regenerate matching files below `.agents/skills/promise-machine/runtime/`, its `MANIFEST.json`, and `.horos/boundary.json`; change no held-corpus fixture or classification.

**Tests.** Add focused cases for missing and restored literals in all three categories; lower and upper case; full and abbreviated Git ids in each admitted context; selector lengths 7, 8, and 9 bytes of hex text; Git lengths 6, 7, 39, 40, and 41; digest lengths 63, 64, and 65; left and right hexadecimal adjacency; punctuation boundaries; transaction-hash, address, selector, digest, Git, file-line, and numeric category precedence; duplicate source occurrences under the existing presence-only promise; unlabelled abbreviated hex-like words; near-miss labels and repository forms; and preservation of every existing B030 case. The red-before comparison must show assertion failures and no infrastructure error on the unfixed parent. Exact Elenchus runner contract: test command `python3 plugins/brevitas/tests/run_tests.py {report}`, report format `unittest-json-v1`, report file `.elenchus/brevitas-372.json`.

**Disciplines.** phylax: source and draft remain bounded caller-supplied text, so the new recognisers use bounded compiled regexes and add no subprocess, network, dependency, credential, or path boundary. ephoros: the finite CLI keeps `B030` path, line, category, and literal diagnostics plus the source-owned test report; no unattended product path or retained telemetry is added. metron: none, this step makes no performance claim or budget. elenchus: the observed silent deletion must fail through the exact parent-overlay runner before the smallest lexical fix is accepted, and both focused and complete suites must then pass. hypomnema: the one Brevitas ledger row records the closed grammar, rejected broad regex and Git-resolution alternatives, semantic limit, and unchanged frontier; no duplicate ADR is created.
