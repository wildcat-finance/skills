# Runbook: Forward-test Brevitas across a held engineering corpus

This runbook derives from the receipted study at SHA-256 `c5aeeeaa08710e8694f7d0b4373d8bf7c2085a1108e186ae4ad062b03f4633a1`. Every Python command runs through `mise exec python@3.13.15 -- python3`. No step adds a dependency, changes CI, edits the adjacent `wildcat/mono` checkout, or treats a model output as authority.

## Step 1: Scaffold the held-corpus contract and report runner

**Goal.** Establish the durable study/runbook copies, corpus interface documentation, and source-owned Elenchus report runner without changing a Brevitas rule or claiming frontier coverage.

**Entry.** Branch from `fiat/374-forward-test-brevitas-across-a-held-engineer` at `2e2608aa2ac62c5d556478e7f93186fddc36dce3`, with the receipted study digest above and the Brevitas baseline at 21 unit tests and three passing legacy evaluations.

**Exit.** Commit byte-identical copies of `.hexaemeron/study.md` and `.hexaemeron/runbook.md` under `docs/brevitas-held-corpus/`; document the chosen closed, digest-bound corpus interface and rejected live-CI/model-judge alternatives in Brevitas's existing ledger without changing its version, frontier fields, next job, or history rows; add a standard-library unittest reporter and a Make target that writes a complete `unittest-json-v1` report. Prove the step with `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py .elenchus/brevitas-unittest.json && mise exec python@3.13.15 -- python3 -m unittest discover -s plugins/brevitas/tests -t plugins/brevitas && mise exec python@3.13.15 -- make -C plugins/brevitas/skills/brevitas test && git diff --check`.

**Files.** `docs/brevitas-held-corpus/study.md`; `docs/brevitas-held-corpus/runbook.md`; `plugins/brevitas/skills/brevitas/evals/README.md`; `plugins/brevitas/skills/brevitas/EVOLUTION.md`; `plugins/brevitas/skills/brevitas/Makefile`; `plugins/brevitas/tests/run_tests.py`; reporter-focused files under `plugins/brevitas/tests/`.

**Tests.** Add focused cases for a complete report, assertion failure, infrastructure error, zero tests, unsafe output path, and interrupted or partial output; preserve all 21 existing unit tests and three legacy evaluations. Exact Elenchus runner contract: command `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py {report}`; report format `unittest-json-v1`; report file `.elenchus/brevitas-unittest.json`.

**Disciplines.** phylax: the reporter accepts an output path, so it must confine a regular bounded file below the worktree and use a private temporary plus atomic replacement. ephoros: none, this step adds no unattended product path and the reporter's bounded test result is evidence rather than retained telemetry. metron: none, no performance claim or budget exists. elenchus: the source-owned report contract is created here so later fixes can be classified against their parent. hypomnema: the study's chosen design and rejected alternatives must reach the existing Brevitas ledger, while the corpus README documents only the callable interface.

## Step 2: Capture and classify the five-family cross-model corpus

**Goal.** Preserve a reviewable, redistributable held corpus with at least ten qualifying outputs: `x-ray`, `solidity-auditor`, `gas`, `invariant`, and `diff-review` from each of two explicit provider/model identities.

**Entry.** Step 1's signed implementation commit, plus explicit operator approval and available authenticated access for the two named model identities. Each selected source must have a recorded licence/sensitivity decision; if that authority or access is absent, stop before any provider call or repository write and halt the Fiat run with the missing item named.

**Exit.** Add a closed versioned manifest, bounded public prompt/source excerpts, exact captured outputs, provider and full returned model ids, capture client versions, prompt/source/output digests, pre-lint human classifications, and exact protected evidence spans. Validate paths, regular-file identity, byte ceilings, unknown and duplicate fields, digests, family/model coverage, ordered spans, and the licence/secret-review record without calling a model during tests. Prove the step with `mise exec python@3.13.15 -- python3 plugins/brevitas/skills/brevitas/scripts/run_evals.py --validate-corpus-only && mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py .elenchus/brevitas-unittest.json && git diff --check`; the validator must report five families, two model identities in every family, at least ten qualifying cases, and zero unclassified or stale cases.

**Files.** `plugins/brevitas/skills/brevitas/evals/corpus.json`; qualifying case directories under `plugins/brevitas/skills/brevitas/evals/cases/`; bounded corpus-validation code under `plugins/brevitas/skills/brevitas/scripts/`; corpus-validation tests under `plugins/brevitas/tests/`; `plugins/brevitas/skills/brevitas/evals/README.md` only where the shipped interface needs clarification.

**Tests.** Add hostile fixtures for an unknown field, duplicate id, missing provider or full model id, stale digest, absolute path, parent escape, symlink escape, non-regular file, oversized input, invalid UTF-8, missing family, one-model family, duplicated or reordered protected span, and hidden/session/credential metadata; preserve a clean case for each of the five families and both model identities. Exact Elenchus runner contract: command `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py {report}`; report format `unittest-json-v1`; report file `.elenchus/brevitas-unittest.json`.

**Disciplines.** phylax: untrusted model output, manifest-controlled paths, provider access, secrets, and external source bytes cross into committed fixtures and need point-of-entry controls. ephoros: the offline validator must emit case id, family, model identity, expectation, short digests, and bounded failure codes so CI failures answer the study's four questions without printing prompts or outputs. metron: none, corpus runtime is not a performance target. elenchus: any observed validator or capture failure stops the step and receives a reproducer before repair. hypomnema: provenance, licence/sensitivity decision, classification, and protected spans live in each case manifest; raw credentials and hidden reasoning have no record home because they are forbidden.

## Step 3: Guard every confirmed Brevitas result

**Goal.** Run the current linter against every classified held case, preserve all evidence spans, and repair each reviewer-confirmed structural bypass or false positive with red-before regression evidence.

**Entry.** Step 2's signed commit with the corpus validator green, at least ten qualifying cases, five-family/two-model coverage, and every case classified before current diagnostics are compared.

**Exit.** Extend the offline evaluation runner to compare exact sorted diagnostics or the declared compression/retention result, require every protected span exactly once and in order, and refuse any unresolved confirmed bypass. For each confirmed bypass or false positive, preserve the parent result, add a focused test that fails against the parent, make the smallest rule repair, and add adjacent allowed/refused guards; if classification finds none, record zero and leave `brevitas.py` unchanged. Prove the fixed tree with `mise exec python@3.13.15 -- python3 plugins/brevitas/skills/brevitas/scripts/run_evals.py && mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py .elenchus/brevitas-unittest.json && mise exec python@3.13.15 -- python3 -m unittest discover -s plugins/brevitas/tests -t plugins/brevitas && git diff --check`; the runner must report zero stale cases, zero evidence losses, zero expectation mismatches, and zero unresolved confirmed bypasses.

**Files.** `plugins/brevitas/skills/brevitas/scripts/run_evals.py`; `plugins/brevitas/skills/brevitas/scripts/brevitas.py` only for a confirmed rule repair; held case targets and expectations under `plugins/brevitas/skills/brevitas/evals/cases/`; `plugins/brevitas/tests/`; `plugins/brevitas/skills/brevitas/SKILL.md` and `evals/README.md` only where established behaviour changed.

**Tests.** Add one captured regression plus one adjacent allowed and one adjacent refused case for every rule repair; add exact-once and relative-order tests for counterexamples, reproduction steps, and establishment limits; retain existing token-survival, evidence-exception, unit, and legacy-eval coverage. Exact Elenchus runner contract: command `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py {report}`; report format `unittest-json-v1`; report file `.elenchus/brevitas-unittest.json`.

**Disciplines.** phylax: the runner continues to consume hostile manifest and model bytes, so all Step 2 controls remain at the boundary. ephoros: stable per-case and aggregate diagnostics are the only retained signals; no raw prompt or full output is emitted. metron: none, no speed-motivated change is permitted. elenchus: every observed bypass or false positive must reproduce on the parent, be localised to a mechanism, and return `guarded` before its fix is accepted. hypomnema: behaviour reasons stay in focused tests, the per-run audit record, and the governed ledger rather than a new decision-record scheme.

## Step 4: Reconcile marketplace prose and advance the frontier

**Goal.** Cold-read the complete mutable first-party marketplace surface after behaviour settles, correct only established drift, and append exactly one valid Brevitas frontier row.

**Entry.** Step 3's signed commit, a clean held-corpus run, all step audits closed, and a recorded list of confirmed fixes, false positives, clean cases, and remaining limitations that can support the maturity decision.

**Exit.** Enumerate and cold-read all 111 mutable first-party marketplace Markdown surfaces using the repository helper; reconcile each statement against the implemented corpus and exact evidence boundary; update Brevitas's frontmatter and ledger from `brevitas-v0.2.0` to `brevitas-v1.2.0`; append exactly one evolution row; retain generation and epoch; and either record one materially evidenced next job or set `Frontier status` to `mature` with `Next Fiat job: None -- mature`. Do not change the held target before this completion decision. Regenerate the portable Promise Machine runtime after canonical source changes. Prove the step with `mise exec python@3.13.15 -- python3 scripts/portable_promise_machine.py check && mise exec python@3.13.15 -- python3 scripts/promise_machine.py check && mise exec python@3.13.15 -- python3 -m unittest discover -s tests && mise exec python@3.13.15 -- python3 -m unittest discover -s plugins/brevitas/tests -t plugins/brevitas && git diff --check`.

**Files.** `plugins/brevitas/skills/brevitas/EVOLUTION.md`; `plugins/brevitas/skills/brevitas/SKILL.md`; `plugins/brevitas/README.md`; `plugins/brevitas/AGENTS.md`; root and sibling first-party marketplace Markdown files only where the cold read proves stale wording; matching generated files below `.agents/skills/promise-machine/runtime/`; version and host-manifest surfaces required by repository parity checks.

**Tests.** Extend version/frontier and marketplace-prose assertions for the exact final state; require header/frontmatter agreement, one new history row, correct evolution arithmetic, recomputed frontier digest, unchanged generation/epoch, and a mature state with no next job or an open state with named material evidence. Exact Elenchus runner contract: command `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py {report}`; report format `unittest-json-v1`; report file `.elenchus/brevitas-unittest.json`.

**Disciplines.** phylax: the portable sync writes a generated tree, so path confinement, canonical-source ownership, and byte-identical copy checks apply. ephoros: none, this step adds no runtime or unattended path. metron: none, no performance claim exists. elenchus: any parity, version, digest, or prose failure is reproduced and guarded before correction. hypomnema: the one appended Brevitas ledger row is the standing home for the completion and successor-or-maturity decision; no duplicate ADR is created.

## Step 5: Demonstrate the offline corpus release

**Goal.** Re-run the complete prototype from the documented entrypoint, preserve a bounded verification record, and show that the delivered corpus, prose, and frontier agree.

**Entry.** Step 4's signed commit with `brevitas-v1.2.0`, the portable runtime regenerated, the marketplace surface reconciled, and every earlier step's audit closed.

**Exit.** Run the documented offline demo and every repository check covering the changed areas; write `docs/brevitas-held-corpus/verification.md` with exact commands, exits, case/family/model counts, confirmed-bypass and repair counts, protected-span result, suite counts, frontier verdict, commit identity, and explicit limits. Prove the record and tree with `mise exec python@3.13.15 -- python3 plugins/brevitas/skills/brevitas/scripts/run_evals.py && mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py .elenchus/brevitas-unittest.json && mise exec python@3.13.15 -- python3 -m unittest discover -s plugins/brevitas/tests -t plugins/brevitas && mise exec python@3.13.15 -- python3 -m unittest discover -s tests && mise exec python@3.13.15 -- python3 scripts/portable_promise_machine.py check && mise exec python@3.13.15 -- python3 scripts/promise_machine.py check && mise exec python@3.13.15 -- python3 plugins/hexaemeron/skills/phylax/scripts/phylax.py plugins tests && mise exec python@3.13.15 -- python3 plugins/hexaemeron/skills/ephoros/scripts/ephoros.py plugins tests && mise exec python@3.13.15 -- python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents/skills/promise-machine/SKILL.md .agents/skills/promise-machine/PORTABLE.md plugins docs && git diff --check`.

**Files.** `docs/brevitas-held-corpus/verification.md`; `docs/brevitas-held-corpus/study.md` and `runbook.md` only if a receipted amendment requires an appended correction; no production or frontier file may change in this demonstration step unless its prior evidence is reopened and repaired through the controller.

**Tests.** The final commands above are the release demonstration; run Imprimatur on every changed public prose file, run Brevitas with `--source` on any compressed derivative, verify every corpus digest, and retain the source-owned report as the audit-fix runner. Exact Elenchus runner contract: command `mise exec python@3.13.15 -- python3 plugins/brevitas/tests/run_tests.py {report}`; report format `unittest-json-v1`; report file `.elenchus/brevitas-unittest.json`.

**Disciplines.** phylax: rerun the mechanical boundary gate over all changed plugin and test paths and record the remaining judgement limits. ephoros: confirm the demo's bounded per-case and aggregate output answers the four study questions without sensitive material. metron: none, elapsed time is diagnostic only and no keep/revert decision is made. elenchus: any final failure stops publication and is worked from reproduction through a parent-failing guard before the affected evidence is reopened. hypomnema: the verification record holds what ran and what remains unestablished; the ledger remains the only home for the frontier decision.
