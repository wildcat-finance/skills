# Exact-head identity canary

## Purpose

This file exists only on an unmerged canary branch for issue #893. Its signed
commit proves whether the base-owned `identity` workflow publishes a successful
status against the pull request's exact head.

## Evidence boundary

A green status proves only the context, source, run link, and exact head named
in the issue runbook. It does not approve or validate product changes.

## Disposition

The canary is not product work. Close its pull request unmerged and do not add
this file to `main`.
