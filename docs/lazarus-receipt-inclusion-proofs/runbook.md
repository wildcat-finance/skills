# Runbook: prove receipts against the captured header's `receiptsRoot`

This runbook derives from the receipted study at SHA-256
`c2c1d1b7551f75aea7c993aeb2f68bf376375bb863a4291fe677dbe2b0d06ea2`.
The immutable implementation baseline is synced `main` at
`5489863196006d8e8b45799d74b56208cac65e4d`. Run Lazarus commands in Python
3.11 or later with `plugins/lazarus/requirements.lock` installed exactly.
No step adds a dependency, changes the Apache-2.0 licence, or creates a second
CI path; Step 1 verifies the existing pins, licence, workflow, and host shells
before extending them. Every step preserves the bytes and meaning of plan-v1,
plan-v2, manifest-v1, release-v1, state-fixture/v1, `goldfinch-v0`, and its
release.

The steps are dependency ordered and each is one signed, audited pull request.
The fixed cross-plugin audit runner introduced in Step 1 runs the complete
Lazarus and Ariadne unittest suites and writes the one structured report named
by each step. Root tests, Promise Machine checks, and the applicable prose and
tree lints remain additional exit gates; they are not folded into that report.

## Step 1: Scaffold the receipt-proof formats

**Goal.** Commit the accepted design records and define the versioned plan and receipt-witness contracts on the repository's existing Lazarus shell.

**Entry.** Run branch `fiat/383-prove-receipts-against-the-captured-header-s` at `5489863196006d8e8b45799d74b56208cac65e4d`, with the study digest above, Lazarus and root suites green in the locked environment, and the existing package manifests, CI workflow, dependency locks, and Apache-2.0 licence unchanged.

**Exit.** Tracked study and runbook copies under `docs/lazarus-receipt-inclusion-proofs/` preserve the receipted facts, with only relative links rebased for their tracked location. `ADR-037` records the full ordered witness choice, filtered-log completeness reason, consensus-versus-RPC boundary, version strategy, and rejected compact, debug-method, and execution-replay designs. Registered `plan-v3` and `receipt-witness-v1` schemas fix the standard `eth_getBlockReceipts` request, header and transaction identities, contiguous receipt indices, supported legacy and typed consensus fields, and explicit target-receipt and log-filter relations; closed schemas, canonical helpers, and bounded readers reject unknown versions and ambiguous inputs. Until the later steps land, capture and whole-fixture verification refuse these new versions by name. A fixed receipt-delivery runner executes both plugin suites and writes one fresh `elenchus.unittest.v1` report through the existing no-follow, worktree-bound report path. Prove the exit with `python3 plugins/lazarus/scripts/lazarus.py validate schemas`, `python3 -m unittest plugins.lazarus.tests.test_schemas plugins.lazarus.tests.test_records plugins.lazarus.tests.test_scaffold plugins.lazarus.tests.test_runner -v`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-1-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/lazarus-receipt-inclusion-proofs/study.md`, and `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/lazarus-receipt-inclusion-proofs/runbook.md`.

**Files.** Create `docs/lazarus-receipt-inclusion-proofs/{study.md,runbook.md}`, `docs/decisions/ADR-037-prove-receipts-with-a-full-ordered-witness.md`, `plugins/lazarus/schemas/{plan-v3.json,receipt-witness-v1.json}`, and `plugins/lazarus/tests/run_receipt_delivery_tests.py`; change `plugins/lazarus/scripts/lazarus.py`, `plugins/lazarus/scripts/lazarus_lib/{records.py,schemas.py}`, `plugins/lazarus/tests/{support.py,test_records.py,test_runner.py,test_scaffold.py,test_schemas.py}`, and `tests/promise_machine_coverage.json` only where source-digest propagation requires it.

**Tests.** Add exact source-copy parity with allowed link rebasing; ADR and existing shell discovery; plan-v1/v2 preservation; plan-v3 and witness positive cases; missing, extra, boolean, non-canonical quantity, receipt-type, index, transaction-identity, log, count, depth, byte, unknown-version, and schema-digest refusals; canonical round trips; and combined-runner suite/report/path cases. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-1.json`. Record the entry and exit suite counts; every entry test remains green.

**Disciplines.** phylax: the schemas, canonical readers, and report path accept untrusted bytes and paths, so closed fields, existing caps, no-follow traversal, and fresh-file creation apply. ephoros: stable version and field refusals plus complete runner counters show which boundary rejected input; no metrics backend is added. metron: none, this step defines safety caps but makes no speed claim. elenchus: malformed vectors and the cross-plugin report transport are guards for later fixes and must be red on the unfixed source where applicable. hypomnema: the wire formats and evidence vocabulary are expensive to reverse, so the ADR, schemas, and tracked study are their durable homes.

## Step 2: Reconstruct and verify the receipt trie offline

**Goal.** Turn a complete ordered receipt witness into a root-bound target receipt and an exact filtered-log relation without trusting RPC decorations.

**Entry.** The controller-receipted Step 1 head, with both new schemas registered, their unsupported runtime paths refusing by name, and the combined plugin and root suites green.

**Exit.** A bounded offline receipt verifier checks one receipt for every hash in the captured header, requires unique contiguous indices and exact block, transaction, and log identities, canonically encodes legacy or supported EIP-2718 receipts, builds the hexary trie at keys `rlp(transactionIndex)`, and compares the result with the already hash-checked header's `receiptsRoot`. Only after that equality does it prove the named receipt at index `0xbf` and compare the recorded `eth_getLogs` result with the complete ordered address/topics projection. Manifest-v2 digest-binds `receipt-witness.json`, carries the existing three evidence counts plus `receipt_trie_proved`, and derives rather than accepts that count; a positive count requires a nonzero verified receipts root. The raw witness source, named receipt, filtered logs, and RPC-only decorations remain `recorded_rpc`. `lazarus verify` reports safe block/index/count/root/digest evidence and fetches nothing. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_receipts plugins.lazarus.tests.test_manifest plugins.lazarus.tests.test_verifier plugins.lazarus.tests.test_binding -v`, `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/tests/fixtures/receipt-proof-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v0`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-2-entry.json`, and `python3 -m unittest discover -s tests -v`.

**Files.** Create `plugins/lazarus/schemas/manifest-v2.json`, `plugins/lazarus/scripts/lazarus_lib/receipts.py`, `plugins/lazarus/tests/test_receipts.py`, and `plugins/lazarus/tests/fixtures/receipt-proof-v1/`; change `plugins/lazarus/scripts/lazarus.py`, `plugins/lazarus/scripts/lazarus_lib/{binding.py,manifest.py,rlp.py,schemas.py,trieproof.py,verifier.py}`, and `plugins/lazarus/tests/{support.py,test_binding.py,test_manifest.py,test_schemas.py,test_verifier.py}`.

**Tests.** Use a small deterministic root vector and the fixed 224-receipt block vector. Mutate one status/root byte, type prefix, cumulative-gas value, bloom byte, trie index, transaction hash, block identity, address, topic, data byte, log index, log order, root nibble, component digest, or derived count and require the owning refusal. Cover receipt-set omissions, duplicates, extras, unsupported types, pre-Byzantium ambiguity, empty and embedded trie nodes, target absence, filter wildcards used by the fixed query, old manifest dispatch, offline operation, stable coordinates, and unchanged legacy fixture/release digests. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-2.json`. Record the new combined count and keep all Step 1 cases.

**Disciplines.** phylax: receipt and witness bytes are hostile input, so canonical quantities, supported types, pre-allocation count/size/depth caps, digest binding, and one offline read path apply. ephoros: verification reports safe stage, block, index, receipt/log counts, expected and computed roots, and relation count without emitting receipt or log bodies. metron: none, forward bounded verification is required but no performance improvement is claimed; structural large-vector guards prevent unbounded rescans. elenchus: every mutation first reproduces a false accept, false claim, or unclear refusal against the parent and closes with a cause-level guard. hypomnema: manifest-v2 and the receipt-root promotion rule implement the ADR; code comments are limited to canonical envelope and proof-promotion reasons.

## Step 3: Capture the complete witness safely

**Goal.** Add one bounded standard RPC request to atomic capture and publish a fixture only after the offline receipt and log relations verify.

**Entry.** The controller-receipted Step 2 head, whose offline verifier accepts only complete, canonically encoded, root-matching witnesses and whose legacy fixtures remain green.

**Exit.** `lazarus capture` accepts plan-v3, calls `eth_getBlockReceipts` once with the fixed block hash, records the response as `recorded_rpc`, derives canonical `receipt-witness.json`, and validates all 224 header slots before finalisation. The request shares existing request, elapsed-time, response-byte, component-byte, and total-byte budgets; receipt, log, topic, nesting, and field caps are enforced before allocation. No debug method or fallback is used. Capture builds in a confined stage, verifies the root and exact filtered-log projection, scans all primary and anchor secrets, and leaves the previous destination intact with no stage on transport, JSON, limit, schema, identity, root, log, secret, or final-verification failure. One structured terminal result carries a correlation identifier, safe stage, block/target identities, bounded counts, versions, roots, and fixture digest; it never carries credentials, provider URLs, receipt bodies, or log data. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_capture plugins.lazarus.tests.test_limits plugins.lazarus.tests.test_rpc plugins.lazarus.tests.test_scrub plugins.lazarus.tests.test_receipts -v`, `python3 plugins/lazarus/scripts/lazarus.py capture --plan plugins/lazarus/tests/fixtures/receipt-capture-v1/plan.json --rpc-url http://127.0.0.1:8545 --out tmp/receipt-capture-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify tmp/receipt-capture-v1`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-3-entry.json`, and `python3 -m unittest discover -s tests -v`; the focused capture command runs only while the repository fake RPC fixture is serving and never uses a live endpoint.

**Files.** Create `plugins/lazarus/tests/fixtures/receipt-capture-v1/`; change `plugins/lazarus/scripts/lazarus.py`, `plugins/lazarus/scripts/lazarus_lib/{capture.py,limits.py,rpc.py,scrub.py}`, and `plugins/lazarus/tests/{fake_rpc.py,support.py,test_capture.py,test_limits.py,test_rpc.py,test_scrub.py,test_receipts.py}`.

**Tests.** Add fake-RPC and CLI cases for the exact request method and block hash; absent, null, duplicate, reordered, missing, extra, malformed, and oversized receipts; response/request/total/time exhaustion; redirects and sanitised transport errors; primary-plus-anchor secret union; deterministic witness bytes; reversed provider field order; interruption at each staging boundary; existing-destination preservation; no-network verification; one terminal result; and no destination or stage after every refusal. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-3.json`. Record the fixed fake-RPC fixture digest and the new combined count.

**Disciplines.** phylax: endpoint, headers, provider bytes, redirects, errors, secrets, limits, staging, and finalisation are live trust boundaries controlled by existing RPC and atomic-write rules plus the new bounded method. ephoros: the terminal event answers the study's four operator questions with correlation and safe counts; only a scheduled capture/release failure merits an alert. metron: none, the observed public-node time and size are feasibility evidence only; safety budgets remain hard caps. elenchus: every transport, bounds, secret, and interruption guard is reproduced against the parent before the cause is fixed. hypomnema: no new decision record is owed; the ADR already chose the request and witness, while operator commands and failure meanings go in Lazarus documentation.

## Step 4: Carry the proof through release and Ariadne

**Goal.** Bind the new fixture evidence through release-v2 and a separately versioned Ariadne state-fixture/v2 statement while preserving both v1 contracts.

**Entry.** The controller-receipted Step 3 head, with atomic plan-v3 capture and offline manifest-v2 verification green and every legacy example byte unchanged.

**Exit.** Registered release-v2 recomputes the fixture digest, witness component, `receipts_root`, four evidence counts, statement digest, and binding checks before writing or verifying; it refuses a v1 statement for a v2 fixture and never upgrades either side implicitly. Ariadne registers `https://ariadne.wildcat.finance/state-fixture/v2` with independent `state_root` and `receipts_root` rules: a positive `proof_backed` count needs the former, a positive `receipt_trie_proved` count needs the latter, and neither root grants the other's authority. Its schema, predicate, registry, capture adapter, conformance vectors, schema-agreement tests, replay boundary, and docs move together. `capture-state-fixture` emits v1 for a verified manifest-v1 input and v2 for a verified manifest-v2 input; it reads local files only and carries no canonical-chain or provider-independence claim. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_release plugins.lazarus.tests.test_binding plugins.lazarus.tests.test_preservation_release -v`, `python3 -m unittest plugins.ariadne.tests.test_state_fixture plugins.ariadne.tests.test_capture_state_fixture plugins.ariadne.tests.test_conformance plugins.ariadne.tests.test_schema_agreement plugins.ariadne.tests.test_replay -v`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-4-entry.json`, and `python3 -m unittest discover -s tests -v`.

**Files.** Create `plugins/lazarus/schemas/release-v2.json`, `plugins/ariadne/schemas/state-fixture-v2.json`, and paired Ariadne v2 conformance fixtures under `plugins/ariadne/tests/fixtures/conformance/`; change `plugins/lazarus/scripts/lazarus_lib/{binding.py,release.py,schemas.py}`, `plugins/lazarus/tests/{support.py,test_binding.py,test_preservation_release.py,test_release.py,test_schemas.py}`, `plugins/ariadne/scripts/ariadne.py`, `plugins/ariadne/scripts/ariadne_lib/capture/state_fixture.py`, `plugins/ariadne/scripts/ariadne_lib/predicates/{__init__.py,state_fixture.py}`, `plugins/ariadne/scripts/ariadne_lib/{registry.py,replay.py}`, `plugins/ariadne/tests/{support.py,test_capture_state_fixture.py,test_cli.py,test_conformance.py,test_docs.py,test_registry.py,test_replay.py,test_schema_agreement.py,test_state_fixture.py}`, and `plugins/ariadne/docs/{capturing-a-state-fixture.md,conformance.md,state-fixture.md}`.

**Tests.** Cover v1/v2 dispatch and cross-version refusal; missing, zero, boolean, malformed, and disagreeing roots/counts; source-fixture mutation after capture; unlisted witness components; statement/release digest drift; atomic output; replay without network; schema/predicate parity; one-field conformance failures; no canonical-chain/provider-independence claim; unchanged v1 statement and release bytes; and release verify after its source fixture is unavailable. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-4.json`. Record both plugin counts and exact legacy digests.

**Disciplines.** phylax: fixture, statement, release, schema, replay, path, digest, and atomic-output boundaries are local but untrusted, so bounded no-follow reads and strict version dispatch apply. ephoros: release and statement output carry safe predicate, root, count, digest, and check names with the existing correlation value; verification remains quiet about payload bodies. metron: none, local release and predicate checks have safety bounds but no latency target. elenchus: each cross-version, count/root, digest, path, and replay failure is reduced to one parent-red mutation guard. hypomnema: release-v2 and state-fixture/v2 are public interfaces; their schemas and state-fixture guide extend the ADR without claiming Ariadne's unrelated held frontier.

## Step 5: Ship and demonstrate the Goldfinch receipt proof

**Goal.** Publish the fixed Goldfinch fixture and release, run the complete offline demonstration, and reconcile versions, ledgers, marketplace prose, and delivery evidence.

**Entry.** The controller-receipted Step 4 head, with plan/manifest/release v2 and state-fixture/v2 green, no network use in verification, and all legacy formats and examples still byte-identical.

**Exit.** `plugins/lazarus/examples/goldfinch-v1/` contains the fixed 224-receipt witness for block `0xc7da16`, target transaction index `0xbf`, all 110 target-receipt logs, and the exact five-log projection; its manifest reports the raw receipt/log sources as recorded and exactly two receipt-trie-proved relations. `goldfinch-v1-release` carries the matching state-fixture/v2 statement and verifies offline. The demo verifies fixture, statement, and release; observes the correct receipt and log relation; rejects one-byte receipt, index, log, root, count, and release mutations; and confirms the historical fixture/release digests are unchanged. Run the exact Ariadne path with a fresh `$STATEMENT` file: `python3 plugins/ariadne/scripts/ariadne.py capture-state-fixture --fixture plugins/lazarus/examples/goldfinch-v1 --name goldfinch-v1 --capture-tool lazarus --capture-version 0.2.0 --capture-command "lazarus capture goldfinch-v1" --first-capture-reason "first receipts-root-proved Goldfinch fixture" --out "$STATEMENT"`, then `python3 plugins/ariadne/scripts/ariadne.py verify "$STATEMENT"`; the shipped demo performs the same capture in a temporary directory. `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify-release plugins/lazarus/examples/goldfinch-v1-release`, and `python3 plugins/lazarus/examples/goldfinch-v1/demo.py` all exit 0 without network access.

Lazarus writer version becomes `0.2.0`; installable packages take their smallest unused patch successors, Lazarus `1.1.2` and Ariadne `1.2.2`, on both host manifests and marketplace registries. Lazarus completes this held frontier at `lazarus-v2.2.0` with exactly one evolution row and an evidence-backed new held job or `None -- mature`; Ariadne records the state-fixture behavior as generation `ariadne-v2.2.0` while retaining its grounded-agent frontier revision, digest, status, current text, and next job byte-for-byte. The complete mutable first-party marketplace inventory is cold-read and recorded in `proof.md`; affected claims are reconciled and unrelated ones are unchanged or filed through their owning queue. Promise coverage digests, portable copies, Horos boundary, public docs, manifests, and ledgers agree on the shipped tree. Prove the full exit with `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-5-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 scripts/promise_machine.py check`, the three demonstration commands above, every changed-plugin and root check required by `AGENTS.md`, and the exact Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, Agent Skills frontmatter, version-propagation, evolution-contract, marketplace-prose, Horos-currency, and `git diff --check` gates over the final tree.

**Files.** Create `plugins/lazarus/examples/{goldfinch-v1,goldfinch-v1-release}/`, `plugins/lazarus/examples/goldfinch-v1/demo.py`, `plugins/lazarus/docs/receipt-inclusion-proofs.md`, and `docs/lazarus-receipt-inclusion-proofs/proof.md`; change `plugins/lazarus/{AGENTS.md,README.md}`, `plugins/lazarus/scripts/lazarus_lib/version.py`, `plugins/lazarus/skills/lazarus/{SKILL.md,EVOLUTION.md}`, `plugins/lazarus/tests/{support.py,test_goldfinch.py,test_preservation_release.py,test_scaffold.py}`, `plugins/ariadne/{AGENTS.md,README.md}`, `plugins/ariadne/skills/ariadne/{SKILL.md,EVOLUTION.md}`, the two plugins' `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, portable skill copies and root marketplace prose found by the cold read, `.horos/boundary.json` if its tracked classifications change, `tests/promise_machine_coverage.json`, and `tests/test_version_propagation.py` only for canonical package-version constants.

**Tests.** Add the checked-in 224-receipt vector, exact target and five-log relations, deterministic fixture/statement/release rebuilds, all named one-byte mutations, v0/v1 coexistence, old digest pins, writer/package/skill version separation, marketplace and portable-copy agreement, evolution-axis rules, and offline demo/readback. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-5.json`. Record final combined and root counts, all fixture/release/statement digests, the full prose inventory, all gate exits, and the no-network proof.

**Disciplines.** phylax: the one capture used to create the committed witness retains Step 3's RPC, secret, bounds, and atomic controls; every shipped verification and demo path is offline and local-file bounded. ephoros: demo and proof record correlation, safe identities, counts, roots, versions, digests, and exact failing stage; no receipt bodies, log data, URL, or credential are published as telemetry. metron: none, record capture and verification observations only as feasibility evidence and make no provider or speed promise. elenchus: the end-to-end mutations and source-bound combined runner prove parent-red/fixed-green behavior before the final audit can close. hypomnema: the ADR, receipt guide, state-fixture guide, schemas, proof record, and exactly one row in each ledger are the durable homes; marketplace prose is reconciled only after those authorities are final.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Files: Create `docs/lazarus-receipt-inclusion-proofs/{study.md,runbook.md}`, `docs/decisions/ADR-037-prove-receipts-with-a-full-ordered-witness.md`, `plugins/lazarus/schemas/{plan-v3.json,receipt-witness-v1.json}`, and `plugins/lazarus/tests/run_receipt_delivery_tests.py`; change `plugins/lazarus/scripts/lazarus.py`, `plugins/lazarus/scripts/lazarus_lib/{capture.py,records.py,schemas.py,verifier.py}`, `plugins/lazarus/tests/{support.py,test_capture.py,test_records.py,test_runner.py,test_scaffold.py,test_schemas.py,test_verifier.py}`, and `tests/promise_machine_coverage.json` only where source-digest propagation requires it.

**Why.** Registering plan-v3 makes the existing direct-library capture and verification paths treat it as plan-v2 through their non-v1 fallback. Step 1 promises that both paths refuse the new version by name until later steps implement it. Guarding only the CLI would leave direct calls accepting the unsupported plan and would split one version rule across two layers, so the minimal complete repair adds the two owning library paths and their existing test modules.

**Steps touched.** Step 1's Files field.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Exit: Tracked study and runbook copies under `docs/lazarus-receipt-inclusion-proofs/` preserve the receipted baseline and its append-only amendments, with only relative links rebased for their tracked location. `ADR-037` records the full ordered witness choice, filtered-log completeness reason, corrected consensus-versus-RPC boundary, version strategy, and rejected compact, debug-method, execution-replay, and transaction-trie expansions. Registered `plan-v3` and `receipt-witness-v1` schemas fix the standard `eth_getBlockReceipts` request, the verified header's block identity and `receiptsRoot`, unique contiguous receipt indices, supported legacy and typed consensus fields, the target receipt index, and the exact log filter. Plan-v3 retains the transaction hash only as a recorded lookup label; the receipt witness contains no proof-bearing transaction hash or other RPC decoration. Closed schemas, canonical helpers, and bounded readers reject unknown versions and ambiguous inputs. Until later steps land, capture and whole-fixture verification refuse these new versions by name. A fixed receipt-delivery runner executes both plugin suites and writes one fresh `elenchus.unittest.v1` report through the existing no-follow, worktree-bound report path. Prove the exit with `python3 plugins/lazarus/scripts/lazarus.py validate schemas`, `python3 -m unittest plugins.lazarus.tests.test_schemas plugins.lazarus.tests.test_records plugins.lazarus.tests.test_scaffold plugins.lazarus.tests.test_runner -v`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-1-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/lazarus-receipt-inclusion-proofs/study.md`, and `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/lazarus-receipt-inclusion-proofs/runbook.md`. Complete replacement Tests: Add exact source-copy parity with allowed link rebasing; ADR and existing shell discovery; plan-v1/v2 preservation; plan-v3 and witness positive cases; plan target-label classification; witness refusal of transaction hashes and other RPC decorations; missing, extra, boolean, non-canonical quantity, receipt-type, index, log, count, depth, byte, unknown-version, and schema-digest refusals; canonical round trips; value-free bounded diagnostics; and combined-runner suite/report/path cases. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-1.json`. Record the entry and exit suite counts; every entry test remains green.

**Why.** Study amendment `dd1308ca07e98339b155fcdfa2c4585736ae1abdc9b45b5710906077432ab111` resolves `S1-R1-01`: receipt-root evidence owns consensus payloads at trie indices, while transaction-hash attribution remains recorded RPC evidence. Step 1's schemas, ADR, tracked sources, and tests must encode that boundary before offline verification is implemented.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry broken; exit broken. Step 3: entry broken; exit broken. Step 4: entry broken; exit broken. Step 5: entry broken; exit broken.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Goal: Turn a complete ordered consensus-receipt witness into a `receiptsRoot`-bound payload at the target trie index and an exact consensus-log projection, while retaining transaction-hash attribution as recorded RPC evidence. Complete replacement Exit: A bounded offline receipt verifier requires unique contiguous receipt indices starting at zero, canonically encodes each legacy or supported EIP-2718 receipt, builds the hexary trie at keys `rlp(transactionIndex)`, and compares the result with the already hash-checked header's `receiptsRoot`. Only after that equality does it prove the consensus receipt payload at index `0xbf` and compare its consensus fields with the separately recorded named receipt. It derives transaction index, block-global log index, block number, and block hash from the proved positions and verified header, then compares the full ordered `(address, topics, data)` projection and those derivable positions with the recorded `eth_getLogs` result. Transaction hashes and other RPC decorations may be cross-checked among recorded sources, but they remain `recorded_rpc` and never enter the receipt-trie-proved relation. Manifest-v2 digest-binds `receipt-witness.json`, carries the existing three evidence counts plus `receipt_trie_proved`, and derives rather than accepts that count; a positive count requires a nonzero verified receipts root. The raw witness source, named receipt, filtered logs, header transaction-hash list, and RPC-only decorations remain `recorded_rpc`. `lazarus verify` reports safe block/index/count/root/digest evidence and fetches nothing. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_receipts plugins.lazarus.tests.test_manifest plugins.lazarus.tests.test_verifier plugins.lazarus.tests.test_binding -v`, `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/tests/fixtures/receipt-proof-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v0`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-2-entry.json`, and `python3 -m unittest discover -s tests -v`. Complete replacement Tests: Use a small deterministic root vector and the fixed 224-receipt block vector. Mutate one status/root byte, type prefix, cumulative-gas value, bloom byte, trie index, address, topic, data byte, consensus-log order, root nibble, component digest, or derived count and require the owning refusal. Cover receipt-set omissions, duplicates, extras, unsupported types, pre-Byzantium ambiguity, empty and embedded trie nodes, target-index absence, filter wildcards used by the fixed query, derived position drift, old manifest dispatch, offline operation, stable coordinates, and unchanged legacy fixture/release digests. Add a coherent transaction-hash rewrite proving that the root and both receipt-trie-proved relations are unchanged and still exclude hash attribution; a one-source hash disagreement may fail recorded consistency but cannot become a proof failure or promotion. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-2.json`. Record the new combined count and keep all Step 1 cases.

**Why.** The amended study limits receipt-root evidence to consensus payloads and derivable positions. This replacement removes the original transaction-hash proof claim while retaining the complete receipt-set and filtered-log guarantees issue #383 can establish from `receiptsRoot`.

**Steps touched.** Step 2.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry broken; exit broken. Step 4: entry broken; exit broken. Step 5: entry broken; exit broken.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Goal: Add one bounded standard RPC request to atomic capture, derive only the consensus receipt witness, and publish a fixture after the scoped receipt-root and log-projection relations verify. Complete replacement Exit: `lazarus capture` accepts plan-v3, calls `eth_getBlockReceipts` once with the fixed block hash, and records the full response as `recorded_rpc`. It requires 224 unique contiguous receipt indices, checks recorded block/index/hash decorations for internal consistency without promoting them, and derives `receipt-witness.json` from consensus receipt and log fields only. The request shares existing request, elapsed-time, response-byte, component-byte, and total-byte budgets; receipt, log, topic, nesting, and field caps are enforced before allocation. No debug method or fallback is used. Capture builds in a confined stage, reconstructs the exact `receiptsRoot`, verifies the payload at index `0xbf` and the complete consensus-log projection, scans all primary and anchor secrets, and leaves the previous destination intact with no stage on transport, JSON, limit, schema, recorded-identity, root, log, secret, or final-verification failure. One structured terminal result carries a correlation identifier, safe stage, block identity, recorded target selector, bounded counts, versions, roots, relation scope, and fixture digest; it never presents a transaction hash as receipt-root-proved and never carries credentials, provider URLs, receipt bodies, or log data. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_capture plugins.lazarus.tests.test_limits plugins.lazarus.tests.test_rpc plugins.lazarus.tests.test_scrub plugins.lazarus.tests.test_receipts -v`, `python3 plugins/lazarus/scripts/lazarus.py capture --plan plugins/lazarus/tests/fixtures/receipt-capture-v1/plan.json --rpc-url http://127.0.0.1:8545 --out tmp/receipt-capture-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify tmp/receipt-capture-v1`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-3-entry.json`, and `python3 -m unittest discover -s tests -v`; the focused capture command runs only while the repository fake RPC fixture is serving and never uses a live endpoint. Complete replacement Tests: Add fake-RPC and CLI cases for the exact request method and block hash; absent, null, duplicate, reordered, missing, extra, malformed, and oversized receipts; response/request/total/time exhaustion; redirects and sanitised transport errors; primary-plus-anchor secret union; deterministic consensus-witness bytes; reversed provider field order; recorded-identity disagreement without evidence promotion; coherent transaction-hash rewriting that leaves the witness and receipt-trie-proved relations unchanged; interruption at each staging boundary; existing-destination preservation; no-network verification; one terminal result with the recorded/proved boundary; and no destination or stage after every refusal. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-3.json`. Record the fixed fake-RPC fixture digest and the new combined count.

**Why.** The capture path sees transaction-hash decorations but the receipt trie does not commit them. This replacement keeps those bytes available as recorded consistency evidence while ensuring the derived witness and every emitted proof claim contain only consensus fields and positions.

**Steps touched.** Step 3.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry broken; exit broken. Step 5: entry broken; exit broken.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Exit: Registered release-v2 recomputes the fixture digest, consensus-witness component, `receipts_root`, four evidence counts, statement digest, and binding checks before writing or verifying; it refuses a v1 statement for a v2 fixture and never upgrades either side implicitly. Its receipt-trie-proved count covers only the scoped consensus receipt and log-projection relations, and release output carries no transaction-hash proof claim. Ariadne registers `https://ariadne.wildcat.finance/state-fixture/v2` with independent `state_root` and `receipts_root` rules: a positive `proof_backed` count needs the former, a positive `receipt_trie_proved` count needs the latter, and neither root grants the other's authority. Transaction hashes and all other recorded RPC decorations remain outside the statement's proved evidence. The Ariadne schema, predicate, registry, capture adapter, conformance vectors, schema-agreement tests, replay boundary, and docs move together. `capture-state-fixture` emits v1 for a verified manifest-v1 input and v2 for a verified manifest-v2 input; it reads local files only and carries no canonical-chain, provider-independence, or transaction-hash attribution claim. Prove the exit with `python3 -m unittest plugins.lazarus.tests.test_release plugins.lazarus.tests.test_binding plugins.lazarus.tests.test_preservation_release -v`, `python3 -m unittest plugins.ariadne.tests.test_state_fixture plugins.ariadne.tests.test_capture_state_fixture plugins.ariadne.tests.test_conformance plugins.ariadne.tests.test_schema_agreement plugins.ariadne.tests.test_replay -v`, `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-4-entry.json`, and `python3 -m unittest discover -s tests -v`. Complete replacement Tests: Cover v1/v2 dispatch and cross-version refusal; missing, zero, boolean, malformed, and disagreeing roots/counts; source-fixture mutation after capture; unlisted witness components; statement/release digest drift; atomic output; replay without network; schema/predicate parity; one-field conformance failures; no canonical-chain, provider-independence, or transaction-hash proof claim; unchanged v1 statement and release bytes; coherent recorded transaction-hash changes that do not change the v2 evidence count; and release verification after its source fixture is unavailable. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-4.json`. Record both plugin counts and exact legacy digests.

**Why.** Release and Ariadne carry the verifier's evidence class, so both must preserve the amended boundary instead of turning a recorded target label into a new proof claim downstream.

**Steps touched.** Step 4.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry broken; exit broken.

### Amendment -- 2026-08-26

**What changed.** Complete replacement Goal: Publish the fixed Goldfinch fixture and release, demonstrate the scoped receipt-root and consensus-log relations offline, and reconcile versions, ledgers, marketplace prose, and delivery evidence. Complete replacement Exit: `plugins/lazarus/examples/goldfinch-v1/` contains the fixed 224-entry consensus receipt witness for block `0xc7da16`, the recorded target transaction-hash selector, the receipt payload at trie index `0xbf`, all 110 consensus logs in that payload, and the exact five-entry consensus-log projection with derivable positions. Transaction hashes and other RPC decorations remain recorded. Its manifest reports the raw block-receipt, named-receipt, filtered-log, and header transaction-list sources as recorded and exactly two receipt-trie-proved relations. `goldfinch-v1-release` carries the matching state-fixture/v2 statement and verifies offline without claiming transaction-hash attribution. The demo verifies fixture, statement, and release; observes the receipt payload and log projection; rejects one-byte consensus receipt, index, log, root, count, and release mutations; proves that a coherent transaction-hash rewrite leaves the receipt root and both proved relations unchanged; distinguishes a one-source recorded-hash disagreement from a proof failure; and confirms the historical fixture/release digests are unchanged. Run the exact Ariadne path with a fresh `$STATEMENT` file: `python3 plugins/ariadne/scripts/ariadne.py capture-state-fixture --fixture plugins/lazarus/examples/goldfinch-v1 --name goldfinch-v1 --capture-tool lazarus --capture-version 0.2.0 --capture-command "lazarus capture goldfinch-v1" --first-capture-reason "first receipts-root-proved Goldfinch fixture" --out "$STATEMENT"`, then `python3 plugins/ariadne/scripts/ariadne.py verify "$STATEMENT"`; the shipped demo performs the same capture in a temporary directory. `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify-release plugins/lazarus/examples/goldfinch-v1-release`, and `python3 plugins/lazarus/examples/goldfinch-v1/demo.py` all exit 0 without network access. Lazarus writer version becomes `0.2.0`; installable packages take their smallest unused patch successors, Lazarus `1.1.2` and Ariadne `1.2.2`, on both host manifests and marketplace registries. Lazarus completes this held frontier at `lazarus-v2.2.0` with exactly one evolution row and an evidence-backed new held job or `None -- mature`; Ariadne records the state-fixture behavior as generation `ariadne-v2.2.0` while retaining its grounded-agent frontier revision, digest, status, current text, and next job byte-for-byte. The complete mutable first-party marketplace inventory is cold-read and recorded in `proof.md`; affected claims are reconciled and unrelated ones are unchanged or filed through their owning queue. Promise coverage digests, portable copies, Horos boundary, public docs, manifests, and ledgers agree on the shipped tree. Prove the full exit with `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-5-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 scripts/promise_machine.py check`, the three demonstration commands above, every changed-plugin and root check required by `AGENTS.md`, and the exact Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, Agent Skills frontmatter, version-propagation, evolution-contract, marketplace-prose, Horos-currency, and `git diff --check` gates over the final tree. Complete replacement Tests: Add the checked-in 224-receipt vector, exact receipt payload at index `0xbf`, five-entry consensus-log projection, deterministic fixture/statement/release rebuilds, all named one-byte consensus mutations, a coherent transaction-hash rewrite that preserves both receipt-trie-proved relations, a recorded-source hash disagreement that cannot become a proof claim, v0/v1 coexistence, old digest pins, writer/package/skill version separation, marketplace and portable-copy agreement, evolution-axis rules, and offline demo/readback. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-5.json`. Record final combined and root counts, all fixture/release/statement digests, the full prose inventory, all gate exits, and the no-network proof.

**Why.** The final demonstration is where an overbroad phrase would become a public evidence claim. It must show exactly what the receipt root proves and preserve transaction-hash attribution as recorded RPC evidence on every output surface.

**Steps touched.** Step 5.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds.

### Amendment -- 2026-08-27

**What changed.** Complete replacement Exit: `plugins/lazarus/examples/goldfinch-v1/` contains the fixed 224-entry consensus receipt witness for block `0xc7da16`, the recorded target transaction-hash selector, the receipt payload at trie index `0xbf`, all 110 consensus logs in that payload, and the exact five-entry consensus-log projection with derivable positions. Transaction hashes and other RPC decorations remain recorded. Its manifest reports the raw block-receipt, named-receipt, filtered-log, and header transaction-list sources as recorded and exactly two receipt-trie-proved relations. `goldfinch-v1-release` carries the matching state-fixture/v2 statement and verifies offline without claiming transaction-hash attribution. The demo verifies fixture, statement, and release; observes the receipt payload and log projection; rejects one-byte consensus receipt, index, log, root, count, and release mutations; proves that a coherent transaction-hash rewrite leaves the receipt root and both proved relations unchanged; distinguishes a one-source recorded-hash disagreement from a proof failure; and confirms the historical fixture/release digests are unchanged. Run the exact Ariadne path with a fresh `$STATEMENT` file: `python3 plugins/ariadne/scripts/ariadne.py capture-state-fixture --fixture plugins/lazarus/examples/goldfinch-v1 --name goldfinch-v1 --capture-tool lazarus --capture-version 0.2.0 --capture-command lazarus --capture-command capture --capture-command goldfinch-v1 --first-capture-reason "first receipts-root-proved Goldfinch fixture" --out "$STATEMENT"`, then `python3 plugins/ariadne/scripts/ariadne.py verify "$STATEMENT"`; the shipped demo performs the same capture in a temporary directory. `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify-release plugins/lazarus/examples/goldfinch-v1-release`, and `python3 plugins/lazarus/examples/goldfinch-v1/demo.py` all exit 0 without network access. Lazarus writer version becomes `0.2.0`; installable packages take their smallest unused patch successors, Lazarus `1.1.2` and Ariadne `1.2.2`, on both host manifests and marketplace registries. Lazarus completes this held frontier at `lazarus-v2.2.0` with exactly one evolution row and an evidence-backed new held job or `None -- mature`; Ariadne records the state-fixture behavior as generation `ariadne-v2.2.0` while retaining its grounded-agent frontier revision, digest, status, current text, and next job byte-for-byte. The complete mutable first-party marketplace inventory is cold-read and recorded in `proof.md`; affected claims are reconciled and unrelated ones are unchanged or filed through their owning queue. Promise coverage digests, portable copies, Horos boundary, public docs, manifests, and ledgers agree on the shipped tree. Prove the full exit with `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-5-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 scripts/promise_machine.py check`, the three demonstration commands above, every changed-plugin and root check required by `AGENTS.md`, and the exact Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, Agent Skills frontmatter, version-propagation, evolution-contract, marketplace-prose, Horos-currency, and `git diff --check` gates over the final tree. Complete replacement Files: Create `plugins/lazarus/examples/{goldfinch-v1,goldfinch-v1-release}/`, `plugins/lazarus/examples/goldfinch-v1/demo.py`, `plugins/lazarus/docs/receipt-inclusion-proofs.md`, and `docs/lazarus-receipt-inclusion-proofs/{proof.md,runbook.md}`; change `plugins/lazarus/{AGENTS.md,README.md}`, `plugins/lazarus/docs/{preservation-release.md,runbook.md,study.md}`, `plugins/lazarus/scripts/lazarus_lib/{manifest.py,version.py}`, `plugins/lazarus/skills/lazarus/{SKILL.md,EVOLUTION.md}`, `plugins/lazarus/tests/fixtures/receipt-proof-v1/manifest.json`, `plugins/lazarus/tests/{support.py,test_capture.py,test_goldfinch.py,test_preservation_release.py,test_scaffold.py}`, `plugins/ariadne/{AGENTS.md,README.md}`, `plugins/ariadne/skills/ariadne/{SKILL.md,EVOLUTION.md}`, the two plugins' `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, portable skill copies and root marketplace prose found by the cold read, `.horos/boundary.json` if its tracked classifications change, `tests/promise_machine_coverage.json`, and `tests/test_version_propagation.py` only for canonical package-version constants.

**Why.** Ariadne records `capture.command` as an argv, with one `--capture-command` occurrence per word. The previous Exit quoted all three words as one element, so the shipped statement could not identify the command that produced the fixture without guessing. The tracked delivery runbook repeats that source command and must remain aligned with the receipted command. Replacing the complete Exit and adding only that tracked runbook path preserves the evidence boundary while making the recorded argv executable and unambiguous.

**Steps touched.** Step 5.

**Still holding.** Step 5: entry holds; exit holds.

### Amendment -- 2026-08-27

**What changed.** Complete replacement Exit: `plugins/lazarus/examples/goldfinch-v1/` contains the fixed 224-entry consensus receipt witness for block `0xc7da16`, the recorded target transaction-hash selector, the receipt payload at trie index `0xbf`, all 110 consensus logs in that payload, and the exact five-entry consensus-log projection with derivable positions. Transaction hashes and other RPC decorations remain recorded. Its manifest reports the raw block-receipt, named-receipt, filtered-log, and header transaction-list sources as recorded and exactly two receipt-trie-proved relations. `goldfinch-v1-release` carries the matching state-fixture/v2 statement and verifies offline without claiming transaction-hash attribution. The demo verifies fixture, statement, and release; observes the receipt payload and log projection; rejects one-byte consensus receipt, index, log, root, count, and release mutations; proves that a coherent transaction-hash rewrite leaves the receipt root and both proved relations unchanged; distinguishes a one-source recorded-hash disagreement from a proof failure; and confirms the historical fixture/release digests are unchanged. Run the exact offline build and Ariadne path with a fresh `$STATEMENT` file: first run `python3 plugins/lazarus/examples/goldfinch-v1/demo.py build-fixture --out tmp/goldfinch-v1-rebuild`; then run `python3 plugins/ariadne/scripts/ariadne.py capture-state-fixture --fixture tmp/goldfinch-v1-rebuild --name goldfinch-v1 --capture-tool lazarus --capture-version 0.2.0 --capture-command python3 --capture-command plugins/lazarus/examples/goldfinch-v1/demo.py --capture-command build-fixture --capture-command=--out --capture-command tmp/goldfinch-v1-rebuild --first-capture-reason "first receipts-root-proved Goldfinch fixture" --out "$STATEMENT"`; then run `python3 plugins/ariadne/scripts/ariadne.py verify "$STATEMENT"`. The shipped demo performs the same byte-exact build and capture in a temporary directory. `python3 plugins/lazarus/scripts/lazarus.py verify plugins/lazarus/examples/goldfinch-v1`, `python3 plugins/lazarus/scripts/lazarus.py verify-release plugins/lazarus/examples/goldfinch-v1-release`, and `python3 plugins/lazarus/examples/goldfinch-v1/demo.py` all exit 0 without network access. Lazarus writer version becomes `0.2.0`; installable packages take their smallest unused patch successors, Lazarus `1.1.2` and Ariadne `1.2.2`, on both host manifests and marketplace registries. Lazarus completes this held frontier at `lazarus-v2.2.0` with exactly one evolution row and an evidence-backed new held job or `None -- mature`; Ariadne records the state-fixture behavior as generation `ariadne-v2.2.0` while retaining its grounded-agent frontier revision, digest, status, current text, and next job byte-for-byte. The complete mutable first-party marketplace inventory is cold-read and recorded in `proof.md`; affected claims are reconciled and unrelated ones are unchanged or filed through their owning queue. Promise coverage digests, portable copies, Horos boundary, public docs, manifests, and ledgers agree on the shipped tree. Prove the full exit with `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report tmp/elenchus/lazarus-receipts-step-5-entry.json`, `python3 -m unittest discover -s tests -v`, `python3 scripts/promise_machine.py check`, the four demonstration commands above, every changed-plugin and root check required by `AGENTS.md`, and the exact Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, Agent Skills frontmatter, version-propagation, evolution-contract, marketplace-prose, Horos-currency, and `git diff --check` gates over the final tree. Complete replacement Tests: Add the checked-in 224-receipt vector, exact receipt payload at index `0xbf`, five-entry consensus-log projection, a deterministic byte-identical fixture builder from the pinned receipt-proof-v1 source components and current demo bytes, deterministic statement and release rebuilds, all named one-byte consensus mutations with guards proving each mutation materializes changed canonical bytes before verification, a coherent transaction-hash rewrite that preserves both receipt-trie-proved relations, a recorded-source hash disagreement that cannot become a proof claim, v0/v1 coexistence, old digest pins, writer/package/skill version separation, marketplace and portable-copy agreement, evolution-axis rules, and offline demo/readback. The step audit runner contract is test command `python3 plugins/lazarus/tests/run_receipt_delivery_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `tmp/elenchus/lazarus-receipts-step-5.json`. Record final combined and root counts, all fixture/release/statement digests, the full prose inventory, all gate exits, and the no-network proof.

**Why.** The prior amendment split the provenance vector correctly but still named a `lazarus capture goldfinch-v1` command that this repository does not install or implement. Replacing it with the byte-exact offline builder makes the recorded argv both runnable and truthful. The mutation guards ensure each named counterexample reaches verification with changed canonical fixture bytes instead of passing because its constructor emitted malformed input first.

**Steps touched.** Step 5.

**Still holding.** Step 5: entry holds; exit holds.
