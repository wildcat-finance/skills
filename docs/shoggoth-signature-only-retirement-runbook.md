# Runbook: retire mandatory Shoggoth co-signature and runtime-host authorship ban

```design-lock
schema | protasis-design-evidence/v1
sha256 | 36bcf19d0fd4b4ec03d86bb25c038d0eb0f9a36371f310867e0976b1ca69ea55
candidate | signature-only-retirement
```

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
```

## Step 1: Preserve the specification and draft the superseding decision

**Goal.** Commit byte-identical copies of the receipted study and runbook and add the unnumbered decision draft selected by the study.

**Entry.** Run branch `fiat/1135-retire-mandatory-shoggoth-co-signature-and` at starting commit `3cc0ad7f521985e46cf29f364a20e19fa99b64dd`, with study receipt `44c52eaa4cb22fbb4da66efe6bf93367bb52736f1f59651390cb1302f056fefa` and design receipt `36bcf19d0fd4b4ec03d86bb25c038d0eb0f9a36371f310867e0976b1ca69ea55`.

**Exit.** Create `docs/shoggoth-signature-only-retirement-study.md` and `docs/shoggoth-signature-only-retirement-runbook.md` as byte-identical copies of the receipted artefacts, and create `docs/decisions/drafts/accept-any-validly-signed-authorship.md` with stable identity `adr/accept-any-validly-signed-authorship`. The draft supersedes the live policy of ADR-016 and ADR-058 without editing either record, separates valid-signature evidence from authorship and publication authority, permits exact authenticated GitHub evidence through local or connected interfaces, and records why the hosted identity job is retired. Prove the exit with `cmp -s .hexaemeron/study.md docs/shoggoth-signature-only-retirement-study.md`, `cmp -s .hexaemeron/runbook.md docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/shoggoth-signature-only-retirement-study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/shoggoth-signature-only-retirement-study.md docs/shoggoth-signature-only-retirement-runbook.md docs/decisions/drafts/accept-any-validly-signed-authorship.md`, `python3 plugins/brevitas/skills/brevitas/scripts/brevitas.py --mode report docs/shoggoth-signature-only-retirement-runbook.md docs/decisions/drafts/accept-any-validly-signed-authorship.md`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py docs plugins`, and `git diff --check`.

**Files.** Create only `docs/shoggoth-signature-only-retirement-study.md`, `docs/shoggoth-signature-only-retirement-runbook.md`, and `docs/decisions/drafts/accept-any-validly-signed-authorship.md`, apart from the configured append-only audit record and synopsis produced by Warden.

**Tests.** No product test is added. The exact commands in Exit prove byte identity, structure, prose gates, and durable placement. Any audit repair uses `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`, schema `elenchus.unittest.v1`, report file `.elenchus/fiat-1135-step-1.json`. The path must be fresh; missing, stale, empty, malformed, skipped, or infrastructure-failed output is inconclusive.

**Disciplines.** phylax: none, because the step adds static Markdown and no executable boundary. ephoros: none, because nothing runs unattended. metron: none, because no performance claim exists. elenchus: byte identity, document structure, and durable-home checks stop the step; audit repairs use the exact runner above. hypomnema: the unnumbered draft is the standing home for the cross-cutting decision until integration assignment.

## Step 2: Make Fiat and contributor policy signature-only

**Goal.** Remove mandatory Shoggoth provenance and runtime-host identity refusals from the local Fiat path while retaining signature, topology, bounded-read, and authority gates.

**Entry.** Step 1 merged into the run branch, with the selected decision and exact specification available to Mason and Warden.

**Exit.** Remove the two mandatory trailer counts, every special Shoggoth lookalike refusal, every runtime-host author, committer, co-author, opener, account, and byline refusal, and the worker instructions that impose them. Retain local `git verify-commit`, exact commit ranges, platform `verified: true` and `reason: valid`, bounded object parsing, author/committer recording, merge attribution, and separate publication authority. Treat an authenticated connector as equivalent to local `gh` when it supplies the exact required repository, object, verification, pull-request, or mutation fields. Decouple `scripts/contributors.py` human-only recognition sets from Fiat and remove the parity gate without admitting non-human accounts to the human ranking. Update current contracts and guidance, the Fiat evolution generation without changing its held frontier, Hexaemeron package manifests by one integration-base patch, portable generated copies, boundary records if their deterministic generator changes them, and focused tests. Prove the exit with `python3 -m unittest tests.test_contributors tests.test_shoggoth_identity tests.test_host_settings plugins.hexaemeron.tests.test_hexctl -v`, `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/fiat-1135-step-2-product.json`, `python3 scripts/portable_promise_machine.py sync`, `python3 scripts/portable_promise_machine.py check`, `python3 scripts/promise_machine.py check`, `python3 scripts/run_checks.py --base 3cc0ad7f521985e46cf29f364a20e19fa99b64dd`, the required prose and phase lints over every changed authored file, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.

**Files.** Update `AGENTS.md`, `README.md`, `INSTALL.md`, `SHOGGOTH.md`, `PROMISE_MACHINE.md`, `scripts/contributors.py`, `tests/test_contributors.py`, `tests/test_shoggoth_identity.py`, `tests/test_host_settings.py`, the canonical Hexaemeron runtime contract, Fiat skill, push discipline, worker briefs, controller, evolution ledger, plugin manifests, their focused tests, Promise Machine copies, and generated portable runtime copies. Change no historical ADR, study, runbook, audit, proof, or labelled-prose fixture. Permit `.horos/boundary.json`, `.horos/candidates.json`, and `.horos/census.json` only when deterministic regeneration requires them.

**Tests.** Add red-before-green cases showing validly signed and platform-verified runtime-host authors, committers, co-authors, openers, accounts, and bylines pass; either, both, or neither Shoggoth trailer passes; invalid or missing signature evidence still refuses; contributor non-human exclusion remains; and no contributor classification reaches Fiat admission. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`, schema `elenchus.unittest.v1`, report file `.elenchus/fiat-1135-step-2.json`. The path must be fresh; missing, stale, empty, malformed, skipped, or infrastructure-failed output is inconclusive.

**Disciplines.** phylax: commit objects, GitHub or connector verification objects, and pull-request metadata remain untrusted and keep closed bounded parsing. ephoros: existing refusal messages name the exact SHA and missing verification field; no new service signal is added. metron: none, because no speed claim is made. elenchus: every reversed identity rule has a red-before-green guard while invalid signatures remain red. hypomnema: current policy moves in the canonical contracts and the Fiat generation row; historical records remain unchanged.

## Step 3: Retire the hosted identity status safely

**Goal.** Remove the base-owned identity workflow and checker only after the live repository ruleset no longer requires their status.

**Entry.** Step 2 merged into the run branch and all local signature-only guards green; live ruleset `21830871` freshly read for `wildcat-finance/skills`.

**Exit.** Capture a bounded preimage of ruleset `21830871`; remove only the `identity` required context while preserving enforcement mode, `invariants`, review requirements, bypass actors, and every unrelated field; read the exact postimage back through an authenticated GitHub route, local or connected; record repository, route capability, immutable response fields, and pre/post digests under `.hexaemeron/ruleset-identity-retirement.json`; then delete `.github/workflows/identity.yml` and `scripts/check_commit_identity.py`, remove `.claude/settings.json` only if it exists solely to suppress the withdrawn host attribution, and update or delete only their current tests and check-map ownership. Prove the exit with `python3 -m unittest tests.test_commit_identity tests.test_host_settings -v` when those modules remain, otherwise the replacement focused policy tests, `python3 scripts/run_checks.py --base 3cc0ad7f521985e46cf29f364a20e19fa99b64dd`, a fresh authenticated ruleset readback with no `identity` and unchanged unrelated fields, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`. If the preimage changed, the mutation response is partial, or readback differs beyond the one context, stop before deleting the workflow.

**Files.** Delete `.github/workflows/identity.yml` and `scripts/check_commit_identity.py`; update or delete `tests/test_commit_identity.py`, `tests/test_host_settings.py`, `tests/check-map-v1.json`, and current authored guidance that names the hosted identity status. Do not edit live rules other than ruleset `21830871`'s `identity` required context, and do not edit historical records. The run-local `.hexaemeron/ruleset-identity-retirement.json` is evidence and must not be committed.

**Tests.** Preserve hostile parsing and signature guards under their remaining owner where they still support a live promise; remove tests whose only assertion is the withdrawn host policy. Add an exact ruleset pre/post comparator fixture that fails on any unrelated-field drift. The Elenchus runner contract is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`, schema `elenchus.unittest.v1`, report file `.elenchus/fiat-1135-step-3.json`. The path must be fresh; missing, stale, empty, malformed, skipped, or infrastructure-failed output is inconclusive.

**Disciplines.** phylax: the GitHub ruleset is an external mutation boundary bound to exact repository identity and pre/post documents. ephoros: preimage, postimage, route capability, status context list, and refusal cause answer the operator's three questions without recording credentials. metron: none, because removal of one job is not a performance claim. elenchus: wrong-repository, changed-preimage, partial-response, and unrelated-field-drift fixtures guard the mutation. hypomnema: the superseding decision records why the hosted job has no remaining identity promise.

## Step 4: Demonstrate the signature-only path and reconcile every surface

**Goal.** Demonstrate the selected policy end to end on the final product tree and leave all canonical, generated, version, and public surfaces consistent.

**Entry.** Steps 1 through 3 merged into the run branch, live ruleset readback recorded without `identity`, and every earlier step green.

**Exit.** Create `docs/shoggoth-signature-only-retirement-demonstration.md` recording exact commands, counts, immutable SHAs, bounded ruleset evidence, connector/local route capability without credentials, and negative signature results. Demonstrate validly signed and platform-verified commits with runtime-host attribution and zero mandatory Shoggoth trailers, plus refusal of invalid or absent required signature evidence. Re-run portable generation, Promise Machine copies, Horos only if the tracked boundary is stale, the focused identity and Fiat tests, the complete checked runner, audit-synopsis currency, Imprimatur, Brevitas, Phylax, Ephoros, Hypomnema, and `git diff --check`; all must exit zero. Reconcile the Fiat generation relation against the integration base and keep the existing frontier revision, digest, open status, and issue #363 held job unchanged.

**Files.** Create `docs/shoggoth-signature-only-retirement-demonstration.md`; update only generated portable, Promise Machine, marketplace, boundary, and version surfaces proven stale by their canonical generators. Preserve every historical record and all unrelated files.

**Tests.** Run `python3 -m unittest tests.test_contributors tests.test_shoggoth_identity tests.test_host_settings plugins.hexaemeron.tests.test_hexctl -v`, any replacement focused hosted-policy test from Step 3, `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report .elenchus/fiat-1135-step-4-product.json`, `python3 scripts/portable_promise_machine.py check`, `python3 scripts/promise_machine.py check`, `python3 scripts/run_checks.py --base 3cc0ad7f521985e46cf29f364a20e19fa99b64dd`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, the exact prose and phase lints selected by the changed paths, and `git diff --check`. The audit-repair runner is `python3 plugins/hexaemeron/tests/run_tests.py --elenchus-report {report}`; report format `unittest-json-v1`, schema `elenchus.unittest.v1`, report file `.elenchus/fiat-1135-step-4.json`. Every report path must be fresh; missing, stale, empty, malformed, skipped, or infrastructure-failed output is inconclusive.

**Disciplines.** phylax: the demonstration stores only bounded object identities and verification fields, never credentials or raw signature material. ephoros: the proof answers which SHA passed, which exact signature result was returned, whether `identity` remains required, and which route supplied readback. metron: none, because no performance result is claimed. elenchus: the final tree must pass both positive host-attribution and negative signature specimens through the same gates. hypomnema: the demonstration is the durable proof while merge-time assignment gives the decision draft its canonical ADR number.

### Amendment -- 2026-09-06

**What changed.** Complete replacement Exit: Create `docs/shoggoth-signature-only-retirement-study.md` and `docs/shoggoth-signature-only-retirement-runbook.md` as byte-identical copies of the receipted artefacts, and create `docs/decisions/drafts/accept-any-validly-signed-authorship.md` with stable identity `adr/accept-any-validly-signed-authorship`. The draft supersedes the live policy of ADR-016 and ADR-058 without editing either record, separates valid-signature evidence from authorship and publication authority, permits exact authenticated GitHub evidence through local or connected interfaces, and records why the hosted identity job is retired. Prove the exit with `cmp -s .hexaemeron/study.md docs/shoggoth-signature-only-retirement-study.md`, `cmp -s .hexaemeron/runbook.md docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/shoggoth-signature-only-retirement-study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/shoggoth-signature-only-retirement-study.md docs/shoggoth-signature-only-retirement-runbook.md docs/decisions/drafts/accept-any-validly-signed-authorship.md`, `python3 plugins/brevitas/skills/brevitas/scripts/brevitas.py --mode report docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/brevitas/skills/brevitas/scripts/brevitas.py --mode report docs/decisions/drafts/accept-any-validly-signed-authorship.md`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py docs plugins`, and `git diff --check`.

**Why.** The installed Brevitas CLI accepts exactly one positional draft, so the original two-file invocation exited 2 before checking either file; two separate invocations preserve the intended coverage and match established repository usage.

**Steps touched.** Step 1 Exit only.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds.
