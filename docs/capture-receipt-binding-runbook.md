# Runbook: Bind the capture-profile receipt test to its artefact

Derived from `.hexaemeron/study.md`. Two steps, dependency order.

## Step 1: Commit the study and runbook

**Goal.** The study and runbook this run builds from are committed where the next study over this module will find them.
**Entry.** The run branch `fiat/574-bind-the-capture-profile-receipt-test-to-its` at `main` (`7f4264ecc26ac2149ddb99834433bee3b5dd9fdc`), clean tree, root suite 345 tests `OK (skipped=5)`, Hexaemeron suite 962/962.
**Exit.** `docs/capture-receipt-binding-study.md` and `docs/capture-receipt-binding-runbook.md` exist as committed copies of the receipted bytes; `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py` reports no defect on both; `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/capture-receipt-binding-study.md` exits 0 and the same script over the runbook exits 0; `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py README.md AGENTS.md .agents plugins docs` exits 0; `python3 -m unittest discover -s tests` reports 345 tests `OK (skipped=5)`; `python3 plugins/hexaemeron/tests/run_tests.py` reports 962/962.
**Files.** `docs/capture-receipt-binding-study.md`, `docs/capture-receipt-binding-runbook.md`, and `.horos/boundary.json` regenerated last if the two tracked paths move it.
**Tests.** No new behaviour test: the document checks and both suites are the regression net at this step. Elenchus runner contract for this step, test command `python3 tests/emit_run_observation_capture_report.py {report}`, report format `unittest-json-v1`, report file `.elenchus/capture-receipt-binding-step-1.json`.
**Disciplines.** phylax: none, two Markdown documents open no execution boundary. ephoros: none, nothing in this step runs unattended. metron: none, no behaviour changes so no measurement can move. elenchus: none, no failure is in hand at this step; the reproduction belongs to step 2's entry. hypomnema: the committed study is the standing source the step 2 comment and the audit record point back to.

## Step 2: Bind the five assertions to the tracked copies, guard the path, demonstrate

**Goal.** The five receipt tests establish issue 435's byte-identity claim from the tracked copies and their pinned digests, read no path a live run owns, and pass inside a worktree holding a foreign `.hexaemeron/study.md`.
**Entry.** Step 1's exit state, plus the reproduction: with this run's own study and runbook present at `.hexaemeron/`, `python3 -m unittest tests.test_run_observation_capture` reports 5 failures naming this delivery's study, where the same command reported `OK (skipped=5)` before those two files existed.
**Exit.** `python3 -m unittest tests.test_run_observation_capture -v` reports `test_receipted_copies_are_byte_identical_and_relocatable`, `test_receipted_copies_have_exactly_one_terminal_newline`, `test_receipted_sources_and_copies_are_digest_equal`, `test_receipted_copies_have_no_literal_newline_escape` and `test_receipted_sources_are_never_modified` as run and passed with this run's `.hexaemeron/study.md` in place, and 0 skipped; `grep -c "\.hexaemeron" tests/test_run_observation_capture.py` reports 0; the new `test_receipt_assertions_never_read_a_live_run_path` passes at the exit and fails against the step's entry tree; `python3 scripts/promise_machine.py coverage --check` exits 0; `python3 tests/emit_run_observation_capture_report.py "$(pwd -P)/.elenchus/capture-receipt-binding-step-2.json"` exits 0; `python3 -m unittest discover -s tests` passes with 0 skips of the five; `python3 plugins/hexaemeron/tests/run_tests.py` reports 962/962.
**Files.** `tests/test_run_observation_capture.py` for the five assertions, the `CARRYOVER_GUARDS` entries whose names move, and the new guard; `tests/promise_machine_coverage.json` for the module's pinned digest, which the coverage check requires once the module's bytes change; `.horos/boundary.json` regenerated last if the tracked tree moves it.
**Tests.** The five rewritten assertions keep their carryover ids C1-04, R3-01, R4-01, R5-01 and R9-03, each still failing when a tracked copy's bytes change, proved by mutating a copy in a temporary directory rather than in the worktree. R9-01's `test_detached_receipt_test_skips_only_absent_receipts` is rewritten as the new guard `test_receipt_assertions_never_read_a_live_run_path`, which scans the module's own source for a `.hexaemeron` read and is red against the step's entry tree; `CARRYOVER_GUARDS` and the union-manifest test are updated in the same commit so every id still names a test that exists. Elenchus runner contract for this step, test command `python3 tests/emit_run_observation_capture_report.py {report}`, report format `unittest-json-v1`, report file `.elenchus/capture-receipt-binding-step-2.json`.
**Demonstration.** The exit's first command is the demo path from the study's problem statement: the capture module inside this run's own worktree, with the five named tests reported as run and passed rather than skipped.
**Disciplines.** phylax: this step removes reads of a path another process owns and mutates, and the two `git show` subprocesses it keeps stay argv-pinned with no shell. ephoros: the step's only signal is the suite's own report, and the exit requires the five named tests reported as run rather than counted as an `OK` line hiding five skips. metron: none, no performance claim; the module already runs in under a second. elenchus: the failure is in hand at this step's entry, so the fix follows reproduce, reduce, fix the mechanism, guard it, and the guard is the source-level test named above. hypomnema: a comment at the binding site records why the assertion binds to tracked bytes, so the next reader does not restore the live-path read.
