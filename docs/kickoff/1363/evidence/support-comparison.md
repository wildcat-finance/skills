# Support source comparison

The support subset contains 34 concrete runtime actions at deployed source `f5a26146987926f4811b72a795d662813dedfe85` and 64 at candidate source `bea503c2736d47de7fd34130c64f10783dc35b39`. Each JSON row matches the canonical compiler action denominator by contract and selector and carries the canonical ABI signature, source location and function-body digest. The file inventories include interfaces as declarations, libraries and abstract bases separately; they contain 30 and 70 source files respectively. Imported Ownable and ERC20 runtime actions are included.

The source facts were extracted from both pinned source trees. They are preparation for the reviewed reports; they do not establish deployed code, successful tests or external token/engine behavior.

| Change | Source evidence | Consequence for action and event maps |
|---|---|---|
| Borrower identity registry added | Candidate `src/WildcatBorrowerIdentityRegistry.sol:51`, `:99`, `:126`, `:160` | Six new actions; current ArchController owner manages account factories, approved factories register accounts, and current/pending principals transfer account identity. Registry transfers do not rewrite market borrower state. |
| Mutable provider administration and deployment added | Candidate `src/providers/ManagedRoleProvider.sol:27`, `:54`; `AccessListRoleProvider.sol:35`; `MerkleRoleProvider.sol:31`; six provider factories | Provider authority is separate from hooks authority. Empty member batches emit no member event. Factory events use factory emitters; AccessList constructor member events use the new provider emitter. |
| Wrapper arithmetic changed | Deployed `src/vault/Wildcat4626Wrapper.sol:216`, `:280`; candidate `:316`, `:402`, `:442` | Deployed execution uses half-up scaling. Candidate deposits/withdrawals floor-scale, mint/redeem ceil-normalize, and verify exact scaled movement. Unchanged function selectors do not imply unchanged effects. |
| Wrapper identity and solvency changed | Deployed wrapper `:344`, `:428`; candidate wrapper `:517`, `:764`, `:954`, `:991` | Sweep uses live operational borrower; sanctions use live borrower principal. Inherited transfer/transferFrom reach the new solvency and escrow-aware sanctions override. Approve/permit do not call that override. |
| Wrapper quarantine added | Candidate wrapper `:483` | Calls market nuke with full original calldata, then moves nonzero wrapper shares to a deterministic escrow. Zero wrapper shares produce no wrapper quarantine event even though the market call occurred. |
| Wrapper factory generation routing added | Deployed `src/vault/Wildcat4626WrapperFactory.sol:28`; candidate `:142` | Undeclared rounding forwards to V1 and returns without a local WrapperDeployed. Declared unknown rounding rejects. Local floor markets require transfer-policy support and market wrapper registration. |
| ArchController and SphereX actions retain their source behavior | Both `WildcatArchController`, `SphereXConfig` and `SphereXProtectedRegisteredBase` | Membership changes, role checks and conditional NewAllowedSenderOnchain remain. Registry membership does not verify registered bytecode. SphereX assembly logs must be joined to their emit helpers. |
| Sentinel query implementation changes; action selectors/effects remain | Deployed `src/WildcatSanctionsSentinel.sol:77`, candidate `:75`; overrides `:119` and `:125` | Candidate uses a bounded assembly bool read for Chainalysis. Anyone can set only their own override namespace or create an escrow; creating an already deployed escrow returns with no event. |
| Lens split and extended | Candidate `src/lens/MarketLens.sol:55`, `MarketLensAggregator.sol:315`, `MarketData.sol:192` | Read-only staticcall facade plus core, aggregation and live helpers. Optional fields preserve absence. No non-view runtime action was added by the lens files. |

All external token, hook, market and SphereX engine event effects retain an explicit boundary in the action rows. The report producer must join source-proved market and hook emissions to these call sites. Interface-only files are listed but do not claim executable behavior. No support action is omitted merely because its direct body has no Solidity `emit`: inherited assembly logs, internal helpers and branch-specific no-event cases are recorded.
