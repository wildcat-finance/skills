# Main root-suite recovery runbook

This run implements the receipted study's `absolute-refresh` design for #1538. The base is `b9cafd196b1d8f8b74b207320836532c14e78bab`, in `issue/16777233-516380243`. The current root baseline is 1,853 tests with 19 failures and 11 errors; the completed full baseline passed 33 of 35 checks. Its two failing checks are `root-suite` and `hexaemeron-suite`. Reports remain under `.hexaemeron/`.

The first step scaffolds the recovery record and repairs every known red gate in one commit boundary. Existing layout, licence, Python 3.14.6 pin and unfiltered CI are retained and verified. A separate documentation commit could not pass the entry baseline. No first-step exit permits a red suite. The second step demonstrates the composed result and applies the issue's authorised required-check setting. The parent owns controller receipts, GitHub settings, publishing, checkpoints and integration; workers own only the role and step in their source-bound packets.

```design-lock
schema | protasis-design-evidence/v1
sha256 | e4e4533b7bc582b57a740399eb3e726f5f8e4f9e1eef5df740a18192c284f11c
candidate | absolute-refresh
```

All commands use the exact `.python-version` interpreter. `uv run --no-project --python 3.14.6 python` is the local invocation of `python3`. Commit messages preserve the two Fiat provenance trailers; every commit is locally signature-verified. Apply Sapheneia durable-record shape, Imprimatur, Vulgate and the final Imprimatur pass to every new written record. Historical audit records and receipted source bytes remain unchanged. Commit copies may correct their relative links and report their source digests.

## Step 1: Scaffold recovery and restore all failing gates

**Goal.** Commit the recovery specification and restore the corpus, packaged byte margin and gate tests together, with genuine adapter evidence and a green repository.

**Entry.** The controller's exact `branch_from` at the stated base, with the accepted study and immutable design record. Cut only the `branch` returned by `next`. The diagnosed failure is preserved in `main-ci-failure.log`, `focused-baseline.log`, `law-only-counterfactual.log` and `baseline-checks.json`; the entry is deliberately red because this step repairs that existing failure.

**Exit.** The package omits only the declared decorative portraits, repairs only their packaged image references and retains at least 5,242,880 bytes below the 26,214,400-byte cap. The law fixture resolves all four unchanged reviewed slices against the current whole-file digest. The three reviewed profiles bind the actual local executable, acquisition output and unchanged model tuples; new complete measurement and parity cohorts pass. The #1520 test checks the explicit controller binding and current runtime test-source bindings. The #1542 tests supply fixture-owned candidate records and give the same results with foreign live controller state present. The specification, design evidence, baseline explanation and proof instructions are committed. `python3 -m unittest discover -s tests` in this active worktree and `python3 scripts/run_checks.py --full --jobs 12 --format json --report .hexaemeron/step-1-checks.json` both exit zero. After staging the complete change and regenerating both Horos records, `.githooks/greenlight` exits zero for that staged tree before committing. Resolve `active-root-green`, `full-checks-green`, `corpus-check-accepted` and `package-headroom` through their exact design-matrix resolver commands; Protasis must accept `step:2` before the next step opens.

**Files.** Create `docs/main-root-suite-recovery/` with committed study, runbook, design evidence, resolvers, bound selection reports, recovery instructions and the reviewed runtime/profile acquisition record. Preserve raw adapter acquisitions in their owner-defined fixture evidence files. Create a draft package decision under `docs/decisions/drafts/` and allocate its final number against the current integration base before integration. Change the bounded law model, source spans and compact representation under `tests/fixtures/agent-instruction-v1/promise-machine-router-selection/`, its `manifest.json`, both profile records and measurement/parity evidence under `tests/fixtures/agent-instruction-v1/evidence/`, and source trust anchors in `scripts/agent_instruction.py`. Change `scripts/portable_promise_machine.py`, `tests/test_skills_sh_package.py`, `tests/test_agent_instruction_corpus.py`, `plugins/hexaemeron/tests/test_audit_synopsis_recovery.py`, and narrowly required regression cases in `tests/test_agent_instruction.py`. Create `tests/emit_main_root_recovery_report.py` for the exact Elenchus runner below. Regenerate owner-produced corpus coverage, portable copies and audit synopsis records only when their inputs change; declare the new runner and documentation in `tests/check-map-v1.json`. Regenerate `.horos/boundary.json` and `.horos/census.json` from the staged affected tree. Update the package contract documents that describe omissions, transforms and the budget. No source portrait is removed. Any newly needed product path outside these responsibilities requires a runbook amendment before editing.

**Tests.** Extend existing cases for portrait classification, omission inventory, exact image-reference transform, preserved authoritative links, refusal at the headroom boundary, the explicit controller identity, and candidate isolation. Keep the current corpus acceptance, mutation, stale-evidence and profile trust-anchor tests. Recover the old law from `49fc2caf^:PROMISE_MACHINE.md`, verify its recorded digest and unchanged uniquely located reviewed bytes, then acquire actual `measure` and `parity` reports over the complete cohort. Never rebind old counts or answers to changed input. The focused Elenchus command is `python3 tests/emit_main_root_recovery_report.py {report}`; report format is `unittest-json-v1`; report file is `.elenchus/main-root-recovery.json`. The emitter runs the three root modules `tests.test_agent_instruction`, `tests.test_agent_instruction_corpus`, `tests.test_skills_sh_package` and `plugins.hexaemeron.tests.test_audit_synopsis_recovery`, preserving runner exit status and the complete `elenchus.unittest.v1` counts. It may include new cases in those modules. Warden must distinguish each behavioural guard from a coverage-digest mismatch; unresolved guards remain explicit. Record actual counts, which can rise after a previously failed setUpClass starts running. Run the active root suite, full 12-worker checked runner and staged-tree greenlight as the exit states require.

**Disciplines.** phylax: reviewed profiles authorise subprocesses and loopback model calls, while package derivation consumes paths and bytes. ephoros: preserve source identities, adapter outcomes, package omitted/rewritten inventories, byte totals and named test failures. metron: measure actual package headroom with the fixed cap; there is no latency claim. elenchus: reproduce the known failures and prove targeted guards without substituting stale coverage for behaviour. hypomnema: document source recovery, profile review, package transform and the acceptance boundaries of #1513, #1467 and #1192.

## Step 2: Demonstrate recovery and require GitHub checks

**Goal.** Demonstrate the repaired repository, require the existing `invariants` GitHub Actions job for merges to main and preserve readback evidence.

**Entry.** Step 1's audited and pushed exact head, on the controller's prescribed child branch, with all four `step:2` conformance cells accepted. The package and corpus are green, and the first step's PR remains open as the stack's review record.

**Exit.** The documented demonstration runs the active root suite, accepted three-fixture corpus and newly built package, records the full-check report and proves the required merge setting. The parent rereads current classic protection and applicable rules, preserves any current independent setting, then requires `invariants` from GitHub Actions app id `15368`, `strict: true`, with administrator enforcement. Readback must prove those exact values; preserve an existing red PR's blocked merge state without attempting its merge. The `required-invariants` resolver passes before integration. Preserve the existing `push` to `main`, unfiltered `pull_request` and `workflow_dispatch` triggers. Stage the final documentation and owner-generated records, run the full checked runner and active-tree `.githooks/greenlight` successfully, and commit signed bytes. At final integration the parent waits for the exact final main head's `invariants` run to pass; the run cannot be reported complete beforehand.

**Files.** Extend `docs/main-root-suite-recovery/` with the demonstration, exact setting request and redacted before/after readbacks, package counts, proof commands and sibling-issue disposition evidence. Finalise the package decision's allocated ADR and its references against the current integration base; do not renumber historical records. Change `.github/workflows/repo.yml` only if an observed trigger or check-name defect requires it. Regenerate owner-produced `.horos/boundary.json`, `.horos/census.json`, portable copies and synopsis/coverage records when their inputs change; update `tests/check-map-v1.json` if an additional path needs an owner. Preserve live operational reports under `.hexaemeron/`; commit only the bounded result record needed by a reader.

**Tests.** Use `python3 tests/emit_main_root_recovery_report.py {report}` with `unittest-json-v1` and `.elenchus/main-root-recovery.json` for any fix claimed in this step. A pure demonstration/settings step introduces no pretend regression assertion; Warden reports that boundary directly. Run the exact `required-invariants` resolver from the immutable design matrix after applying and reading back protection. Keep the final active-root, corpus, package and full-check outcomes attached to their actual tree and commands. Repeat an expensive adapter cohort only when an input changes or a named failed result requires investigation. Verify the final main CI head after the one integration merge and retain its URL and conclusion in the controller completion evidence.

**Disciplines.** phylax: GitHub settings are external authority, so reread and preserve existing configuration and keep credentials outside source execution. ephoros: record the expected producer, required context, strictness, administrator enforcement, blocked red PR and exact CI head. metron: remeasure the generated package budget; no speed claim. elenchus: investigate any demonstration failure under the same runner contract; a settings readback alone is no behavioural code guard. hypomnema: publish reproduction and enforcement instructions with observed evidence and explicit remaining issue scope.

## Integration and closure

The parent pushes each audited step against its exact `pr_base`, verifies public readability, provenance and signatures, waits for the checks and saves the mandatory local checkpoint after every `done push`. Bring the stack into the run branch in controller order, preserving original signed commits. Apply any needed base-sync and ADR-allocation gates before the single run PR into `main`; never bypass a red required check. That PR closes #1538 only once its whole acceptance is met. The current corpus failure from #1513 is repaired, but its reusable reconciliation API remains open; #1192 remains open. Close #1467 only on independent proof of all its acceptance, and distinguish #1515's specification from delivered implementation. The last main CI observation, controller `verify`, final `status` and successful archive/reset complete the delivery trail.

### Amendment -- 2026-09-12

**What changed.** Complete replacement Files: Extend `docs/main-root-suite-recovery/` with the demonstration, exact setting request and redacted before/after readbacks, package counts, proof commands and sibling-issue disposition evidence. Keep the package decision unnumbered under `docs/decisions/drafts/` throughout this product step and use its stable decision identity in references. The parent assigns its final number only in the final integration composition, using Hypomnema's canonical plan, apply and replay report against the exact current integration base; no historical record is renumbered. Change `.github/workflows/repo.yml` only if an observed trigger or check-name defect requires it. Regenerate owner-produced `.horos/boundary.json`, `.horos/census.json`, portable copies and synopsis/coverage records when their inputs change; update `tests/check-map-v1.json` if an additional path needs an owner. Preserve live operational reports under `.hexaemeron/`; commit only the bounded result record needed by a reader.

**Why.** The selected Hypomnema contract assigns numbers in the final integration composition. The original Step 2 Files wording could move that work into an earlier product commit. This correction preserves the study's design and the final integration requirement.

**Steps touched.** Step 2's Files.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds.

### Amendment -- 2026-09-12

**What changed.** Complete replacement Exit: The package omits only the declared decorative portraits, repairs only their packaged image references and retains at least 5,242,880 bytes below the 26,214,400-byte cap. The law fixture resolves all four unchanged reviewed slices against the current whole-file digest. The three reviewed profiles bind the actual local executable, acquisition output and unchanged model tuples; new complete measurement and parity cohorts pass. The #1520 test checks the explicit controller binding and current runtime test-source bindings. The #1542 tests supply fixture-owned candidate records and give the same results with foreign live controller state present. The specification, design evidence, baseline explanation and proof instructions are committed. Before committing, `python3 -m unittest discover -s tests` in this active worktree, `python3 plugins/hexaemeron/tests/run_tests.py`, and every applicable generated-record and prose check exit zero. After staging the complete change and regenerating both Horos records, `.githooks/greenlight` exits zero for that staged tree before a genuine signed implementation commit. Then `python3 scripts/run_checks.py --full --jobs 12 --format json --report .hexaemeron/step-1-checks.json` must exit zero on that exact clean signed head. The suppression validator requires a clean committed source tree, so this full-run ordering preserves its contract. No implementation receipt, push or step exit is permitted until all checks pass; any repair returns through the staged-tree commit gate and full validation. Resolve `active-root-green`, `full-checks-green`, `corpus-check-accepted` and `package-headroom` through their exact design-matrix resolver commands; Protasis must accept `step:2` before the next step opens.

Complete replacement Files: Create `docs/main-root-suite-recovery/` with committed study, runbook, design evidence, resolvers, bound selection reports, recovery instructions and the reviewed runtime/profile acquisition record. Preserve raw adapter acquisitions in their owner-defined fixture evidence files. Create a draft package decision under `docs/decisions/drafts/` and allocate its final number against the current integration base before integration. Change the bounded law model, source spans and compact representation under `tests/fixtures/agent-instruction-v1/promise-machine-router-selection/`, its `manifest.json`, both profile records and measurement/parity evidence under `tests/fixtures/agent-instruction-v1/evidence/`, and source trust anchors in `scripts/agent_instruction.py`. Change `scripts/portable_promise_machine.py`, `tests/test_skills_sh_package.py`, `tests/test_agent_instruction_corpus.py`, `plugins/hexaemeron/tests/test_audit_synopsis_recovery.py`, `plugins/hexaemeron/tests/test_phylax_model_proxy.py`, and narrowly required regression cases in `tests/test_agent_instruction.py`. Create `tests/emit_main_root_recovery_report.py` for the exact Elenchus runner below. Regenerate owner-produced corpus coverage, portable copies and audit synopsis records only when their inputs change. Regenerate `docs/promise-machine/obligation-gates/evaluation-run.json`, `demonstration-run.json` and `demonstration-evidence.md` through their owner tools after the coverage input changes; declare the new runner and documentation in `tests/check-map-v1.json`. Regenerate `.horos/boundary.json` and `.horos/census.json` from the staged affected tree. Update the package contract documents that describe omissions, transforms and the budget. No source portrait is removed. Any newly needed product path outside these responsibilities requires a runbook amendment before editing.

Complete replacement Tests: Extend existing cases for portrait classification, omission inventory, exact image-reference transform, preserved authoritative links, refusal at the headroom boundary, the explicit controller identity, and candidate isolation. Keep the current corpus acceptance, mutation, stale-evidence and profile trust-anchor tests. Recover the old law from `49fc2caf^:PROMISE_MACHINE.md`, verify its recorded digest and unchanged uniquely located reviewed bytes, then acquire actual `measure` and `parity` reports over the complete cohort. Never rebind old counts or answers to changed input. The focused Elenchus command is `python3 tests/emit_main_root_recovery_report.py {report}`; report format is `unittest-json-v1`; report file is `.elenchus/main-root-recovery.json`. The emitter runs the three root modules `tests.test_agent_instruction`, `tests.test_agent_instruction_corpus`, `tests.test_skills_sh_package` and `plugins.hexaemeron.tests.test_audit_synopsis_recovery`, preserving runner exit status and the complete `elenchus.unittest.v1` counts. It also runs `plugins.hexaemeron.tests.test_phylax_model_proxy.ConformanceTests.test_skill_package_marketplace_coverage_and_portable_versions_are_exact`, `tests.test_promise_machine_contract.PromiseEvaluationGateTests` and `tests.test_obligation_gate_demonstration.DemonstrationRecordTests`. It may include new cases in those modules. The Phylax conformance test must independently require the exact declared portrait-reference transform and retain byte equality for all other content. Preserve the original obligation-gate evaluation and independently emitted old and new prompt packets. Replay the historical raw answers through the owner tally only after proving all 11 prompt files are byte-identical; keep their original model and date and record the changed whole-coverage binding. This replay is no new model observation. Warden must distinguish each behavioural guard from a coverage-digest mismatch; unresolved guards remain explicit. Record actual counts, which can rise after a previously failed setUpClass starts running. Run the active root and Hexaemeron suites, applicable generated-record and prose checks, and staged-tree greenlight before committing. Run the complete 12-worker checked runner on the resulting clean signed head as the exit requires.

**Why.** The first implementation full run found a Phylax package assertion that still required decorative references, an evaluation record bound to the changed coverage file, and a suppression validator that refuses an uncommitted source tree. These additions repair the first two failures and put the complete full check after the signed commit that the third check requires. The active root and Hexaemeron suites and staged-tree greenlight remain mandatory before that commit.

**Steps touched.** Step 1's Exit, Files and Tests.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds.

### Amendment -- 2026-09-12

**What changed.** Complete replacement Exit: The documented demonstration runs the active root suite, accepted three-fixture corpus and newly built package, records the full-check report and proves the required merge setting. The parent rereads current classic protection and applicable rules, preserves any current independent setting, then requires `invariants` from GitHub Actions app id `15368`, `strict: true`, with administrator enforcement. Readback must prove those exact values; preserve an existing red PR's blocked merge state without attempting its merge. The `required-invariants` resolver passes before integration. Preserve the existing `push` to `main`, unfiltered `pull_request` and `workflow_dispatch` triggers. Stage the final documentation and owner-generated records. Run the active root and Hexaemeron suites, applicable generated-record and prose checks, and active-tree `.githooks/greenlight` successfully before committing signed bytes. Then the full checked runner with 12 workers must pass on the exact clean signed head, including the suppression validator that requires a committed source tree. No implementation receipt, push or step exit is permitted until that full run passes; any repair returns through the staged-tree commit gate and full validation. At final integration the parent waits for the exact final main head's `invariants` run to pass; the run cannot be reported complete beforehand.

**Why.** The suppression validator requires a clean committed source tree in every step. This applies the verified Step 1 ordering to Step 2 while retaining all checks and refusing delivery until the complete full run passes.

**Steps touched.** Step 2's Exit.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds.
