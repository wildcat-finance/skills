# Main root-suite recovery

Issue [#1538](https://github.com/wildcat-finance/skills/issues/1538) restores the current corpus and package, then requires GitHub's existing `invariants` check before merging. The starting commit is `b9cafd196b1d8f8b74b207320836532c14e78bab`.

## Failure and repair

The preserved main run executed 1,853 root tests with 19 failures and 11 errors. Twenty-nine failures/errors came from the law fixture's stale whole-file digest and byte positions. One came from 27,036,319 packaged bytes exceeding the 26,214,400-byte cap. The full baseline passed 33 of 35 checks; its other failure was #1520's assumption that runtime test bindings still named the controller implementation.

The law change entered through `49fc2caf1ca0efc22e7e88fa11ced9e0b49982da` in [PR #1153](https://github.com/wildcat-finance/skills/pull/1153). Recovery reads `49fc2caf^:PROMISE_MACHINE.md`, verifies its recorded whole-file digest, and locates the identical reviewed span exactly once in the current law. All four bindings move by 4,412 bytes. [law-relocation.json](law-relocation.json) records the original and current identities and each unchanged span digest. The model's reviewed meanings, reviewers and v1 codec stay fixed.

The new profile records retain their model blobs, vocabulary, executable arguments, input scope, limits, seeds and answer vocabulary. [runtime-profile-review.json](runtime-profile-review.json) lists the changed executable, version-output and acquisition-output pins. Its raw version and identity command outputs are under `acquisitions/`. The checked measurement and parity records are generated by actual local adapter calls over the complete cohort. Historical token counts and answers are not rebound to these inputs.

The package follows `adr/omit-portable-decorative-portraits`: omit decorative PNG/WebP portraits only from generated copies, remove only their inline image references, preserve source files and authoritative links, and require five MiB of headroom. The generated manifest names every omitted file and transformed byte range.

The #1520 test now checks `run_observation_binding.controller` as the implementation identity and separately checks the six current runtime test-source bindings. The two #1542 CLI tests create their own candidate record and source copy. Production handling of unrelated Fiat design records is inherited from the starting commit.

## Recorded measurements

The complete measurement cohort accepted all 10 calls: 2,528 source tokens and 2,363 compact-plus-bootstrap tokens, a reduction of 165 for these exact inputs. The successful parity cohort accepted all 36 calls across 18 question pairs, with zero failed, refused or unknown answers. The corpus check accepted all three fixtures, 15 reviewed bindings, 14 mutations and nine questions. The expanded focused recovery runner passed 403 tests.

[package-measurement.json](package-measurement.json) records 18,239,372 runtime bytes across 1,395 files, leaving 7,975,028 bytes below the unchanged cap. The omission covers 32 packaged portraits totalling 8,788,268 source bytes; all 34 tracked portraits remain in the source. Forty-seven Markdown files carry recorded image removals. The complete package, including the runtime manifest and outer files, uses 18,668,573 bytes across 1,402 files and leaves 7,545,827 bytes of headroom. Both byte totals are enforced against the same minimum reserve. Installed verification, independent byte-range replay, authoritative link closure and the headroom boundary test passed in the focused runner.

The packaged evaluation record has a separate owner derivation because portrait removal changes whole skill-file hashes. Generation verifies the canonical record, compares all eleven source and packaged prompts byte for byte, and refuses any changed prompt. Equal prompts permit tally replay with the original answers, model and date; the package manifest records this derivation and its source/output identities. An isolated packaged evaluation check accepts the derived record. The canonical evaluation remains bound to the source tree.

The first parity attempt remains [refused](parity-attempt-1.json): 17 of 18 pairs passed, and one gptoss source call returned `WAI-E-ADAPTER.UNAVAILABLE`. The [diagnosis](parity-attempt-1-diagnosis.json) and [server excerpt](parity-attempt-1-server-excerpt.log) show the unchanged 170-second curl deadline closing its client while the server loaded that model. The next compact call and later pairs passed. Why the first load took that long is unknown. A second complete cohort passed with the same profiles, limits, inputs and answer vocabulary; it replaced no individual result inside the refused record.

The obligation-gate evaluation also binds the complete coverage file, whose agent-instruction hashes changed. [The replay record](evaluation-replay/replay.json) preserves its original model, date and answers; [the comparison](evaluation-replay/comparison.json) proves that all 11 emitted prompts are byte-identical to the starting commit. Both prompt packets and the original evaluation run are preserved beside it. The owner tally recomputed only the whole-coverage binding, with the same 55 historical classifications. This is no new model observation. The demonstration inventory was revalidated against the current coverage and evaluation run; its historical command results and counts were preserved and do not claim those commands ran on this tree.

## Reproduce

Use the exact Python in `.python-version`. Commands run from the repository root. The full checked runner requires a clean committed tree because the suppression check reads committed source. For a repair, run the active root and Hexaemeron suites, stage the complete change, regenerate both Horos records and pass `.githooks/greenlight` before the signed commit; then run the full checked runner on that clean head:

```bash
uv run --no-project --python "$(cat .python-version)" python scripts/agent_instruction.py check --root . --manifest tests/fixtures/agent-instruction-v1/manifest.json
uv run --no-project --python "$(cat .python-version)" python tests/emit_main_root_recovery_report.py .elenchus/main-root-recovery.json
uv run --no-project --python "$(cat .python-version)" python -m unittest discover -s tests
uv run --no-project --python "$(cat .python-version)" python scripts/run_checks.py --full --jobs 12 --format json --report .hexaemeron/full-completion-checks.json
```

`check` uses preserved records offline. Reacquiring measurements requires the exact reviewed local adapter identities and starts actual model calls:

```bash
uv run --no-project --python "$(cat .python-version)" python scripts/agent_instruction.py measure --root . --manifest tests/fixtures/agent-instruction-v1/manifest.json --output .hexaemeron/measurement.json
uv run --no-project --python "$(cat .python-version)" python scripts/agent_instruction.py parity --root . --manifest tests/fixtures/agent-instruction-v1/manifest.json --output .hexaemeron/parity.json
```

Preserve any refused attempt. Only after both reports pass may their generated bytes replace the two fixture evidence records and their manifest digests be recomputed. Do not alter counts, answers, correlation ids or thresholds. When source/profile identities change, review them and reacquire both complete cohorts before relying on the new corpus.

The [study](study.md), [runbook](runbook.md), [design evidence](design-evidence.json), bound selection reports and resolvers preserve the selected `absolute-refresh` design. The study copy adds only repository-relative links and its bridge to ADR-076; the controller's receipted bytes remain unchanged. Original report command fields retain their acquisition paths. `resolve_design.py` is the exact pre-build specimen: it requires the old fixture at the starting commit and the committed `runtime-identity-observations.json`. It is expected to refuse against the repaired fixture. Replay its selection in a separate checkout at the starting commit, with its outputs in a separate directory:

```bash
replay_root=$(mktemp -d)
git worktree add --detach "$replay_root" b9cafd196b1d8f8b74b207320836532c14e78bab
mkdir -p "$replay_root/.hexaemeron/issue1538-selection"
cp docs/main-root-suite-recovery/resolve_design.py docs/main-root-suite-recovery/runtime-identity-observations.json "$replay_root/.hexaemeron/issue1538-selection/"
cd "$replay_root"
uv run --no-project --python "$(cat .python-version)" python .hexaemeron/issue1538-selection/resolve_design.py --candidate absolute-refresh --criterion reviewed-bindings-preserved
```

The new report is under that checkout's `.hexaemeron/issue1538-selection/reports/`. Its command names the new invocation; the committed historical reports remain unchanged. The completion resolver instead evaluates the repaired tree and records the current results.

## Recovery and remaining work

Before changing this fixture set, preserve a Git commit containing its manifest, three router artefacts, both profiles, both generated cohort reports and source trust anchors. Restore that whole set if interrupted, then run the corpus check. Individual writes are atomic; this sequence is not a transaction. A restored fixture whose source file differs still refuses, as it should.

The immediate mismatch tracked by #1513 is repaired here. Its reusable reconciliation capability remains open: the existing reconciliation tool targets `fiat-study-runbook-phase`. [#1192](https://github.com/wildcat-finance/skills/issues/1192) still owns a relative-offset schema. #1467's omission, reference-repair and headroom criteria must be verified independently before its closure. PR #1515 remains a specification, not implementation evidence.

Step 2 owns live strict `invariants` protection tied to GitHub Actions app `15368`, administrator enforcement, a red pull request's blocked merge state and the final exact main commit's passing check. No local test result establishes that GitHub setting or a published main result.
