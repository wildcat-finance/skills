# Reviewed corpus reconciliation runbook

Implement the receipted study at `b9f8e36b8b6210bcd023a68059ecb46da3e35769` in `issue/16777233-521985670`. Preserve the v1 model, all three fixtures, 15 reviewed bindings and existing profile authority. The fresh baseline passed the corpus checker and 352 focused tests. Issue #1192 remains separate; #1467 is delivered and owes a final package-reserve verification after this work.

```design-lock
schema | protasis-design-evidence/v1
sha256 | ba069a46a51f1528dd6b8bf81dce76d7054b2df3806cbdb46342f795beaf4a3b
candidate | staged-general-v1
```

Use Python 3.14.6; locally, `python3` means `uv run --no-project --python 3.14.6 python`. Always preserve the reviewed source, historical evidence classes, signed provenance and complete corpus validation. Ask first only for a new dependency, profile capability, public/storage schema, CI setting or release rewrite outside this packet. Never bypass a gate, alter observations to gain acceptance, adopt PR #1548 or modify an unrelated worktree.

The parent owns controller receipts, publication, checkpoints, final decision assignment and integration. Workers act only on their source-bound packets. Every new written record follows Sapheneia, Imprimatur, Vulgate, final Imprimatur and applicable Brevitas checks. Generated and digest-bound records remain as their owner emits them. Decisions stay unnumbered until the parent's final canonical Hypomnema assignment.

## Step 1: Scaffold the reconciliation contract and runner

**Goal.** Commit the checked study, selection evidence, decision draft and executable reporting harness so the implementation has a reviewable contract and a green entry.

**Entry.** The controller's exact `branch_from`, the receipted study and matching immutable design record. Cut only its prescribed `branch`. The baseline corpus and 352 focused tests pass; no repair capability or crash-recovery guard is claimed yet.

**Exit.** The committed contract retains all 12 study sections, the selected candidate, raw probe limits and three pending integration gates. The runner executes the two existing corpus modules and emits their actual complete Elenchus result. Existing repository layout, licence, Python pin, hooks and CI remain valid. Run the focused command below, applicable generated/prose checks, and active-worktree `.githooks/greenlight` after staging the complete intended tree. Commit those exact bytes with a valid signature and both Fiat provenance trailers. Then `python3 scripts/run_checks.py --full --jobs 12 --format json --report .hexaemeron/step-1-checks.json` must exit zero on the exact clean signed HEAD before the implementation receipt. The suppression validator requires that clean committed tree; a failure returns through repair, staged greenlight and a new signed commit.

**Files.** Create `docs/agent-instruction-reconciliation/` with study/runbook copies, the immutable design record and selection reports, source-bound raw probes, a bounded audit-read inventory, preserved probe/resolver scripts and reproduction instructions. Keep controller originals unchanged; document any path adaptation and its source digest in committed copies. Create `docs/decisions/drafts/stage-reviewed-corpus-reconciliation.md` with the checked design bridge. Create `tests/emit_agent_instruction_reconciliation_report.py`. Register these paths in `tests/check-map-v1.json` where required. Regenerate `.horos/boundary.json` and `.horos/census.json` from the staged affected tree. No reconciliation product behavior changes in this step.

**Tests.** The Elenchus command is `python3 tests/emit_agent_instruction_reconciliation_report.py {report}`; report format is `unittest-json-v1`; report file is `.elenchus/agent-instruction-reconciliation.json`. In this scaffold, run `tests.test_agent_instruction` and `tests.test_agent_instruction_corpus`, preserving real counts and nonzero exits under `elenchus.unittest.v1`. Do not invent a new behavioral guard for a documentation/reporting step. Check Protasis study/runbook and the copied design record at design-lock, Imprimatur and Brevitas on new prose, the three required discipline lints, applicable generated-record checks, staged greenlight and the clean-HEAD full runner. Run a new report-emitter test only if a concrete reporting failure requires a guard.

**Disciplines.** phylax: the report wrapper executes fixed argv and writes a bounded requested report, without a shell or network. ephoros: report actual counts, completeness and exit status so failure cannot appear as success. metron: no speed claim; preserve the measured selection limits. elenchus: preserve the baseline and expose the exact runner contract without claiming an unfixed behavior is guarded. hypomnema: record the selected construction, its rejected alternatives and pending evidence in a draft decision and linked specification.

## Step 2: Implement and demonstrate staged reconciliation

**Goal.** Deliver prepare/apply/recover and a verified disposable demonstration that completes a moved law-span repair with fresh owner evidence.

**Entry.** Step 1's audited, pushed exact head on the controller's prescribed child branch. Its PR remains open. The design record still selects `staged-general-v1`; durable recovery and the complete demonstration remain unestablished.

**Exit.** Preparation accepts an explicit immutable baseline, one bound source and a new confined stage; it validates the accepted baseline closure, selects every fixture bound to that source, uniquely relocates unchanged spans/nodes, preserves v1 and refuses unrelated drift. It writes no live product file and makes no model call. Dependency reports distinguish ready, needs-evidence and refused, name corpus/profile/measurement/parity identities and give exact owner commands. Apply requires full staged acceptance, frozen profile authority and unchanged live dependencies; it journals the complete target set before writes, reads back all outputs and publishes manifest/coverage after their inputs. Handled faults restore the prior generated set. Killed execution leaves a journal that blocks another operation; checked, idempotent recovery restores only old/planned-new targets and refuses a third version. The edited source remains unchanged.

The demonstration exercises all six source placements and every named hostile/recovery boundary. One before-span law case completes fresh local measurement and parity, applies, and passes the full corpus checker. Preserve failed acquisition attempts and the exact accepted staged corpus, profiles and raw owner reports; no old answers or token counts are relabelled as new observations. The demonstration verifier rechecks its declared dependency hashes and owner report semantics and emits the study's closed `agent-instruction-reconciliation-verification/v1` summary. The final live corpus remains accepted, and the package retains at least 5,242,880 bytes below the unchanged 26,214,400-byte cap.

Run the focused runner, live root/applicable suites and generated/prose checks. Stage the final intended tree, pass active-worktree `.githooks/greenlight`, and create a genuine signed commit. Then `python3 scripts/run_checks.py --full --jobs 12 --format json --report .hexaemeron/step-2-checks.json` must pass on that exact clean signed HEAD before the implementation receipt. Resolve all three selected integration cells through their unchanged commands in the design record. Their reports bind real executed checks; repeat only evidence whose dependencies change before the final merge-step. The parent requires final main CI to pass on its exact merge SHA before completion.

**Files.** Extend `scripts/prove_agent_instruction_reconciliation.py`; a focused `scripts/agent_instruction_reconciliation.py` helper may own the new stages and journal. Add `tests/test_agent_instruction_reconciliation.py` and extend the Step 1 emitter to run it alongside both existing modules. Keep existing proof verbs and defaults. Extend `docs/agent-instruction-reconciliation/` with commands, refusal/recovery guidance, the demonstration and its bound evidence, and final results; update the numberless decision draft without assigning its ADR number. Keep operational reports under `.hexaemeron/`.

Only when required by changed inputs, regenerate `tests/promise_machine_coverage.json`, `docs/promise-machine/obligation-gates/evaluation-run.json`, `demonstration-run.json`, `demonstration-evidence.md`, audit synopses, portable copies and Horos records through their owners. The product corpus and profile records under `tests/fixtures/agent-instruction-v1/` remain byte-identical unless an actual owned dependency requires a new owner-emitted record; disposable shifted-source acquisitions belong with the new demonstration. A coverage rebind retains historical evaluation outcomes only after all corresponding prompt bytes are proven unchanged, with the original model/date and explicit replay status. Update the check map for the new helper, tests and evidence. Any additional product responsibility requires a checked amendment before editing.

**Tests.** Use `python3 tests/emit_agent_instruction_reconciliation_report.py {report}`, `unittest-json-v1`, and `.elenchus/agent-instruction-reconciliation.json`. Add meaningful guards for law/Horos selection, every affected sibling, exact before/after relocation, changed/missing/duplicate anchors and nodes, baseline digest or object substitution, inherited Git redirection, stale profiles and measurement/parity, stage/target races, path traversal/aliases/symlinks/special files, duplicate JSON keys, file/target/total limits, partial publication, actual process termination, retry and third-version recovery refusal. Prove selected guards fail against the unfixed behavior; distinguish a missing API or coverage hash failure from a behavioral assertion and state the exact counterfactual used.

Generate the demonstration in disposable roots using the current reviewed local profiles and owner `measure`, `parity` and `check` commands. Never widen profile authority, buy service, fetch models or add a dependency. Run `python3 scripts/prove_agent_instruction_reconciliation.py demonstrate --root . --verify docs/agent-instruction-reconciliation/demonstration.json` on the committed record. Run the three exact selected conformance resolvers from `.hexaemeron/design-evidence.json`, package/corpus currency checks, required lints, staged greenlight and the full clean-HEAD runner as the exit requires.

**Disciplines.** phylax: constrain untrusted Git/JSON/path input, staged subprocesses and publication targets with exact identities, resource limits and no shell. ephoros: emit bounded dependency states and journal phases with the failed boundary and recovery command. metron: retain the existing 600-second checker timeout, 1 MiB output/file caps, 256-target and 32 MiB staging limits; claim no speed improvement. elenchus: preserve failures, localise causes, prove behavioral guards and killed-process recovery separately from coverage checks. hypomnema: document the complete command path, evidence reuse limits, recovery, historical replay and the #1192/#1467 disposition.

The parent then merges the checked step stack in controller order, preserving audit PRs. It performs canonical final ADR assignment against the exact integration base, revalidates any changed composition, publishes one run PR with `Closes wildcat-finance/skills#1513` and the required carryover block, and merges through required checks. Post the checked completion comment, verify closure and exact final main CI, verify #1467's package reserve, then capture controller status/verify and archive/reset.

