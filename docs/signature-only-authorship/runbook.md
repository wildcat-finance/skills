# Runbook: retire the Shoggoth co-signature and the host authorship ban

Derived from the receipted study at `.hexaemeron/study.md` and the selected
design `retain-declaration`. The run starts from `main` at
`592390722f10df53658906623b15428dbfb88d8f` on branch
`fiat/1135-retire-the-shoggoth-cosignature-and-host-au`.

Step 1 merges `fiat/1135-retire-mandatory-shoggoth-co-signature-and` at
`d9114b653772dd6ba09a81d7af8ccc1943f23042`, and every later step assumes that
merged tree. Of the 73 paths the branch changed, 30 are adopted at the branch's
own bytes and 43 are regenerated, reverted to `59239072`'s bytes, or rewritten.
`python3 .hexaemeron/design-probe.py --candidate retain-declaration --surface`
prints the three lists.

No step edits a preserved record: `audit/rounds/*`, ADR-016, ADR-019, ADR-058,
every `docs/*/study.md` and `docs/*/runbook.md`, the Hexaemeron
`docs/*/proof.md` records, and
`plugins/hexaemeron/skills/imprimatur/evals/labelled-prose-v1/samples.jsonl`.
The decision record stays unnumbered and the Hypomnema allocator numbers it at
integration. No step mutates ruleset `21830871` or branch protection; the run
reads that state and reports drift rather than correcting it. A generated copy
is written by its owning command, never by hand.

Every step exit runs on a clean tree, because a dirty tree reddens the dead-code
check on its own.

```version-relations
fiat | plugins/hexaemeron/skills/fiat/EVOLUTION.md | next-generation-after-integration-base
```

```design-lock
schema | protasis-design-evidence/v1
sha256 | 7739586e0fd1c659264267ca4026ba748fb9206a7d359255a4ce8fc8e41652ef
candidate | retain-declaration
```

## Step 1: Merge the salvaged branch and prove the signature gate

**Goal.** Bring `d9114b65` onto the run branch with every conflicted artefact re-derived rather than hand-merged, commit the accepted specification, and produce the `signature-refusal-preserved` report the record blocks `step:2` on.
**Entry.** Clean run branch at `592390722f10df53658906623b15428dbfb88d8f` with the study and design lock receipted, and `d9114b653772dd6ba09a81d7af8ccc1943f23042` fetched into the worktree.
**Exit.** The merge commit carries both trees. Each of the ten conflicted paths is re-derived by its owning command or re-pinned to the one plugin version this run resolves, and none carries a conflict marker or a hand-merged hunk. `tests/prove_signature_only_refusals.py` writes a closed `protasis-design-report/v1` object with integer `exit` 0 recording that an unsigned commit, a commit altered after signing, and `77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883`, signed by GitHub web-flow key `B5690EEEBB952194`, are each still refused with the message `hexctl.py` gives today. Prove it with `cmp .hexaemeron/study.md docs/signature-only-authorship/study.md`, `cmp .hexaemeron/runbook.md docs/signature-only-authorship/runbook.md`, `git diff --check`, `python3 tests/prove_signature_only_refusals.py --candidate retain-declaration --out .hexaemeron/design-reports/retain-declaration-signature-refusal-preserved.json`, `python3 scripts/promise_machine.py check`, `python3 scripts/promise_machine.py coverage --check`, `python3 scripts/portable_promise_machine.py check`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, and `python3 scripts/run_checks.py`.
**Files.** Re-derive `.horos/boundary.json`, `.horos/candidates.json` and `.horos/census.json` through `python3 plugins/horos/skills/horos/scripts/horos.py scan . --write` and `scan . --census --write`; `.agents/plugins/marketplace.json` and `.claude-plugin/marketplace.json` through `python3 scripts/promise_machine.py sync`; and `tests/promise_machine_coverage.json` until `coverage --check` exits zero. Re-pin `plugins/hexaemeron/.claude-plugin/plugin.json`, `plugins/hexaemeron/.codex-plugin/plugin.json`, `plugins/hexaemeron/tests/test_phylax_model_proxy.py` and `tests/test_version_propagation.py` to one version rather than to either side of the conflict. Create `docs/signature-only-authorship/study.md`, `docs/signature-only-authorship/runbook.md` and `tests/prove_signature_only_refusals.py`. The merge brings the other 63 branch paths in cleanly and this step changes none of them. Permit `audit/rounds/fiat-1135-retire-the-shoggoth-cosignature-and-host-au.md` and its synopsis.
**Tests.** The prover is a new tracked script with three cases. The unsigned and altered cases build empty-tree objects in a disposable repository through `git commit-tree` and `git hash-object -t commit -w --stdin` and call `verify_local_commit` on each; the web-flow case calls it on the immutable object above, whose `%GK` is `B5690EEEBB952194` and whose `%G?` is `E`. No case supplies a fake `git` executable. The merge brings the branch's suite in whole and no other product test changes here. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-1.json`.
**Disciplines.** phylax: the prover reads an object database and spawns `git`, so its argv stays fixed, its reads bounded, and its disposable repository declines to sign. ephoros: none, because no unattended path changes in this step; `identity.yml` returns in Step 3. metron: none, because no performance claim is made. elenchus: the three refusals are the recovery gate the record blocks `step:2` on, so each case fails when its guard is removed. hypomnema: the committed study and runbook are the durable home for this specification.

## Step 2: Restore the host-set parity anchor and its promise bytes

**Goal.** Restore the classification and parity surface the merge renamed, and return the contributor-ranking promise to `59239072`'s bytes in all 19 copies.
**Entry.** Step 1's signed, audited, green head, where the merge has removed every host refusal from `hexctl.py` and renamed every `HOST_*` declaration in `scripts/contributors.py`.
**Exit.** `scripts/contributors.py` declares `HOST_IDENTITY_NAMES`, `HOST_IDENTITY_EMAILS` and `HOST_PR_LOGINS`, both predicates, `host_set_payload`, `frozensets_from_source` and `verify_host_set_parity`, and exposes `--host-set` and `--verify-host-set`. `hexctl.py` declares the same three frozensets and nothing else the merge withdrew: no `is_host_identity`, no `HOST_BYLINE_RE`, no `COAUTHOR_TRAILER` and no `ORIGIN_TRAILER`, with a comment naming `scripts/contributors.py:191` and `PROMISE_MACHINE.md:344` as the reason the three stay without a caller. The ranking promise is byte-identical to `59239072` in the canonical law and in all 18 generated plugin copies. Prove it with `python3 scripts/contributors.py --verify-host-set`, `python3 scripts/contributors.py --check`, `python3 scripts/promise_machine.py check`, `python3 scripts/promise_machine.py coverage --check`, `python3 scripts/portable_promise_machine.py check`, `git diff --no-renames 592390722f10df53658906623b15428dbfb88d8f -- PROMISE_MACHINE.md 'plugins/*/PROMISE_MACHINE.md'` printing nothing, `python3 -m unittest tests.test_contributors -v`, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 scripts/run_checks.py`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.
**Files.** Change `scripts/contributors.py`, `tests/test_contributors.py`, `plugins/hexaemeron/skills/fiat/scripts/hexctl.py` and the canonical `PROMISE_MACHINE.md`; propagate the 18 plugin copies with `python3 scripts/promise_machine.py sync` rather than by editing one. Invert the `RetiredSurfaceTests` assertion at `tests/test_ruleset_identity_retirement.py:102`, which asserts the five `HOST_*` and `is_host_*` names absent from `contributors.py`, in this step, because this step is what makes it false. Re-derive `tests/promise_machine_coverage.json`, `.horos/boundary.json` and `.horos/census.json` through their owning commands. Permit the configured audit record and synopsis.
**Tests.** Extend `tests/test_contributors.py` with a parent-red case for each parity direction: a name, email or login present in one file and absent from the other, and a `HOST_*` frozenset present in one file with no counterpart. Keep `RulesetComparatorTests` untouched. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-2.json`.
**Disciplines.** phylax: `frozensets_from_source` parses `hexctl.py` as data through the AST rather than importing it, and this step widens nothing it reads. ephoros: none, because the parity check runs from a terminal and the ranking workflow already reports its own exit. metron: none, because the parity read is one bounded AST parse. elenchus: each parity direction gets a case that fails on the parent commit. hypomnema: keeping three frozensets with no caller is the choice a later reader is most likely to undo by tidying, so Step 5 records it and the code comment carries the reason in place.

## Step 3: Restore the narrowed identity checker and its advisory status

**Goal.** Bring back `scripts/check_commit_identity.py` with its bounded reads and its 34 surviving refusals, and the workflow that publishes the advisory `identity` status under that narrower promise.
**Entry.** Step 2's signed, audited, green head, where the merge has deleted the checker, its test and its workflow, and `59239072` still holds all three in full.
**Exit.** The restored checker refuses none of the eight withdrawn rules, which are `:213` and `:265` on an ambiguous Shoggoth identity, `:215`, `:244`, `:259` and `:307` on a runtime host, and `:269` and `:273` on the two provenance trailers, all as numbered on `59239072`. Its other 34 refusals still fire, `COMMIT_COUNT_MAX`, `COMMIT_BYTES_MAX`, `COMMIT_TOTAL_BYTES_MAX`, `GIT_OUTPUT_MAX` and `GIT_TIMEOUT_SECONDS` keep their values, `_repository_path` still refuses a symlinked or non-bare path, and the file no longer imports `contributors`. Its module docstring states what the file establishes, a commit range that stayed inside `COMMIT_COUNT_MAX`, `COMMIT_BYTES_MAX` and `COMMIT_TOTAL_BYTES_MAX` and whose author and committer identities parsed, and claims nothing about who authored it. `.github/workflows/identity.yml` is byte-identical to `59239072`, so it stays base-owned under `pull_request_target` with `timeout-minutes: 5`, adds no candidate checkout, import or argument, and still filters `branches: [main]`. Prove it with `git diff --no-renames 592390722f10df53658906623b15428dbfb88d8f -- .github/workflows/identity.yml` printing nothing, `python3 -m unittest tests.test_commit_identity -v`, `python3 -m unittest tests.test_python_contract -v`, a recorded run of `python3 scripts/check_commit_identity.py --repository <bare candidate> --base <base sha> --head <head sha> --pull-request-login <login>` over one bounded range on `59239072` and again here with wall time for both, `grep -c 'raise Refusal' scripts/check_commit_identity.py` returning 34, `grep -c '^from contributors\|^import contributors' scripts/check_commit_identity.py` returning 0, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 scripts/run_checks.py`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.
**Files.** Restore `scripts/check_commit_identity.py` and `tests/test_commit_identity.py` from `59239072` and then narrow both, so no ceiling is retyped. Restore `.github/workflows/identity.yml` from `59239072` unchanged; this is an ask-first path and the restoration is the only change to it. Revert `tests/test_python_contract.py` to `59239072`'s bytes, restoring its two `"identity.yml"` rows in the same step that restores the workflow. Invert the `RetiredSurfaceTests` assertions at `tests/test_ruleset_identity_retirement.py:96`, `:97` and `:112` here, because this step is what makes them false. Re-derive `.horos/boundary.json` and `.horos/census.json` through their owning commands. Permit the configured audit record and synopsis.
**Tests.** `tests/test_commit_identity.py` returns from `59239072` with the eight withdrawn cases removed and the rest kept, and gains parent-red cases for a host author, a host committer, a host co-author trailer, a host generated-by byline, a host pull-request login, an absent Shoggoth trailer and a lookalike Shoggoth identity, each now accepted, plus a malformed author identity and each ceiling still refused. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-3.json`.
**Disciplines.** phylax: the checker reads a bare repository whose objects come from a pull request, so the fixed argv, `GIT_CONFIG_NOSYSTEM`, the devnull global config, every ceiling and the timeout are restored from the base bytes and checked rather than retyped; the privileged workflow keeps its base-owned property. ephoros: a red status must name a rule the repository still keeps, so the checker keeps printing `identity: <refusal>` to stderr with its subject commit and exiting 2, and carries only the 34 refusals that still correspond to a rule. metron: the job holds `timeout-minutes: 5` and the narrowed file removes eight refusals and adds none, so the before-and-after wall times over one bounded range are both recorded. elenchus: every withdrawn rule gets a case that fails on the parent commit by refusing, and every ceiling keeps the case that proves it still refuses. hypomnema: the narrowed promise is a claim reduction, so Step 5 records what the file now establishes and that it no longer imports `contributors`.

## Step 4: Restore the host settings file and its narrowed test

**Goal.** Keep `.claude/settings.json` in the tree, so the adopted `INSTALL.md:109` stays true, and narrow the test whose docstring cites a withdrawn rule.
**Entry.** Step 3's signed, audited, green head, where the merge has deleted `.claude/settings.json` and `tests/test_host_settings.py` while leaving `INSTALL.md:109` documenting the file.
**Exit.** `.claude/settings.json` is byte-identical to `59239072` and `INSTALL.md:109` still reads that the repository carries it with one object. `tests/test_host_settings.py` returns without the claim that Fiat refuses the host defaults by name, which stops being true, and without the ADR-016 citation for a withdrawn rule; it asserts the file's shape and its `sessionUrl` setting only. Prove it with `git diff --no-renames 592390722f10df53658906623b15428dbfb88d8f -- .claude/settings.json INSTALL.md` printing nothing, `python3 -m unittest tests.test_host_settings -v`, `python3 -m unittest tests.test_ruleset_identity_retirement -v`, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 scripts/run_checks.py`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.
**Files.** Restore `.claude/settings.json` and `tests/test_host_settings.py` from `59239072`, then narrow the test. Invert the `RetiredSurfaceTests` assertion at `tests/test_ruleset_identity_retirement.py:100` here, which is the last of the four, so that file now asserts four presences and `RulesetComparatorTests` remains untouched. Re-derive `.horos/boundary.json` and `.horos/census.json` through their owning commands. Permit the configured audit record and synopsis. Deleting `.claude/settings.json` stays out of scope and on the ask-first tier.
**Tests.** `tests/test_host_settings.py` keeps a case for the file's presence, its single object and its `sessionUrl` value, and drops every case and docstring clause resting on a refusal that no longer exists. `tests/test_ruleset_identity_retirement.py` ends this step asserting the presence of `.github/workflows/identity.yml`, `scripts/check_commit_identity.py`, `.claude/settings.json`, the five `HOST_*` and `is_host_*` names in `contributors.py`, and the `"identity.yml"` rows in `tests/test_python_contract.py`. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-4.json`.
**Disciplines.** phylax: none, because a checked-in editor settings object opens no boundary and this step widens no read. ephoros: none, because nothing here runs unattended. metron: none, because no performance claim is made. elenchus: the inverted presence assertions fail on the parent commit, which is what makes them a guard rather than a restatement. hypomnema: no Creator answer covers this file, so Step 5 records the reading and the three reasons behind it as a decision rather than leaving it discovered later.

## Step 5: Record the decisions and amend the prior specification

**Goal.** Write the five expensive-to-reverse decisions into one unnumbered record, append a dated amendment to each of the prior run's two design records, and add this run's version row.
**Entry.** Step 4's signed, audited, green head, where the implementation is complete and the branch's unnumbered draft at `docs/decisions/drafts/accept-any-validly-signed-authorship.md` is still the merged text.
**Exit.** The draft states, in this order, the withdrawal of the host authorship ban and the trailer mandate as superseding ADR-016's Decision paragraph and ADR-058's identity-script paragraph; that the two ambiguous-Shoggoth refusals answered impersonation rather than attribution and their removal is deliberate; the salvage decision of 2026-09-09 with the figures 73 changed, 30 adopted, 43 reworked; the parity anchor with no caller, naming `scripts/contributors.py:191` and `PROMISE_MACHINE.md:344`; the narrowed checker promise and that it no longer imports `contributors`; and the reading that keeps `.claude/settings.json`. It carries no assigned number, keeps the slug and first heading `accept-any-validly-signed-authorship` that `docs/shoggoth-signature-only-retirement-demonstration.md` cites, and edits no numbered record. Each of `docs/shoggoth-signature-only-retirement-study.md` and `docs/shoggoth-signature-only-retirement-runbook.md` gains one dated amendment naming the two reversals and pointing at the draft, with no existing line moved. Prove it with `python3 -m unittest tests.test_decision_records -v`, `python3 -m unittest tests.test_evolution_contract -v`, `python3 plugins/hexaemeron/skills/hypomnema/scripts/hypomnema.py docs/decisions/drafts/accept-any-validly-signed-authorship.md docs/shoggoth-signature-only-retirement-study.md docs/shoggoth-signature-only-retirement-runbook.md`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py` over each changed document, `git diff --no-renames 592390722f10df53658906623b15428dbfb88d8f -- docs/decisions/ADR-016-attribute-governed-agent-work-to-shoggoth.md docs/decisions/ADR-019* docs/decisions/ADR-058-require-base-owned-identity-and-human-review.md` printing nothing, `git diff --stat` on the two prior records showing additions only, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 scripts/run_checks.py`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.
**Files.** Rewrite `docs/decisions/drafts/accept-any-validly-signed-authorship.md`. Append to `docs/shoggoth-signature-only-retirement-study.md` and `docs/shoggoth-signature-only-retirement-runbook.md`. Add this run's row to `plugins/hexaemeron/skills/fiat/EVOLUTION.md`, whose version relation is declared above and resolved at integration, so no version token is written by hand. Re-derive `.horos/boundary.json` and `.horos/census.json` through their owning commands. Permit the configured audit record and synopsis. Do not touch ADR-016, ADR-019, ADR-052 or ADR-058, and do not pick an ADR number.
**Tests.** No product test is added. `tests/test_decision_records.py` and `tests/test_evolution_contract.py` are the existing guards over the draft's shape and the version row. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-5.json`.
**Disciplines.** phylax: none, because this step adds bounded Markdown only. ephoros: none, because no unattended path changes. metron: none, because no performance claim is made. elenchus: none, because no executable failure is repaired here. hypomnema: this step is the record, and assumption 7 of the study is the open reading behind the two amendments, so if the prior records are held to be preserved instead, both amendments become forbidden and this run writes its own records at fresh paths.

## Step 6: Demonstrate signature-only admission

**Goal.** Record the five specimens from the study's problem statement against the built tree, and enumerate the 30 adopted paths against the Creator's four answers.
**Entry.** Step 5's signed, audited, green head, with the implementation and every record complete.
**Exit.** `docs/signature-only-authorship/demonstration.md` records each specimen with its exact command, its exit status and the immutable commit it ran on: a signed commit whose author is a runtime host, carrying no provenance trailer and a `Generated with Claude Code` line, accepted by `verify_local_commit`; the same object unsigned, refused with the current message; `77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883`, refused through the `GITHUB_SIGNING_KEYS` branch for key `B5690EEEBB952194`; `python3 scripts/contributors.py --check` at exit 0 with runtime hosts still excluded from the ranking and `--verify-host-set` at exit 0; and `python3 scripts/check_commit_identity.py` accepting that host-authored commit and refusing a malformed author identity. The same document carries the 30 adopted paths, each read against the four answers with none asserting a withdrawn rule, and the grep counts for the section 2 tables: no path refusing on host identity or trailer count. It states that the published `identity` status is judged by `main`'s base-owned copy of the workflow until this run merges, so a status readback is an integration observation rather than a step exit, and that no read available here answers which commits reached `main` unsigned, naming [skills#1514](https://github.com/wildcat-finance/skills/issues/1514) as where that answer will live. Prove it with `python3 tests/prove_signature_only_refusals.py --candidate retain-declaration --out <fresh path>` re-run against this tree, `python3 scripts/contributors.py --check`, `python3 scripts/contributors.py --verify-host-set`, `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/signature-only-authorship/demonstration.md`, `python3 scripts/run_checks.py` green on a clean tree, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, and `git diff --check`.
**Files.** Create `docs/signature-only-authorship/demonstration.md`. Re-derive `.horos/boundary.json`, `.horos/candidates.json` and `.horos/census.json` through their owning commands after the last tracked edit. Permit the configured audit record and synopsis. Change no product file.
**Tests.** No product test is added; this step runs what the earlier steps built. The signed specimen is created in a disposable repository with an authorised key and synthetic host attribution, the unsigned specimen from the same empty tree, and the web-flow specimen is the immutable object named above. The audit-fix runner is `python3 tests/run_tests.py --elenchus-report {report}` with `elenchus.unittest.v1` output at `.elenchus/fiat-1135-step-6.json`.
**Disciplines.** phylax: the demonstration signs in a disposable repository and records neither credentials nor raw signature bytes. ephoros: this step answers the study's three on-call questions on the page, including the one nothing here can answer, so a later reader sees the gap rather than an implied answer. metron: the two recorded checker wall times from Step 3 are restated with the five-minute job budget they sit under. elenchus: the three signature refusals are the fail-closed posture, and the step is where their absence would stop the run. hypomnema: none, because Step 5 already holds every decision this run makes.

### Amendment -- 2026-09-09

**What changed.** Complete replacement Exit: The merge commit carries both trees. Each of the ten conflicted paths is re-derived by its owning command or re-pinned to the one plugin version this run resolves, and none carries a conflict marker or a hand-merged hunk. `tests/prove_signature_only_refusals.py` writes a closed `protasis-design-report/v1` object with integer `exit` 0 recording that an unsigned commit, a commit altered after signing, and `77bf7f15e2f857fd8fd3c2db8ad50c3f738a6883`, signed by GitHub web-flow key `B5690EEEBB952194`, are each still refused with the message `hexctl.py` gives today. Prove it with `cmp .hexaemeron/study.md docs/signature-only-authorship/study.md`, `cmp .hexaemeron/runbook.md docs/signature-only-authorship/runbook.md`, `git diff --check`, `python3 tests/prove_signature_only_refusals.py --candidate retain-declaration --out .hexaemeron/design-reports/retain-declaration-signature-refusal-preserved.json`, `python3 scripts/promise_machine.py check`, `python3 scripts/promise_machine.py coverage --check`, `python3 scripts/portable_promise_machine.py check`, `python3 plugins/horos/skills/horos/scripts/horos.py check .`, `python3 plugins/hexaemeron/skills/fiat/scripts/audit_synopsis.py --check .`, `python3 tests/run_tests.py`, `python3 plugins/hexaemeron/tests/run_tests.py`, and `python3 scripts/run_checks.py`. The last three are read against a clean snapshot of `592390722f10df53658906623b15428dbfb88d8f` measured in this step and recorded in its test summary, because that commit is already red: under the root runner bad results appear there only in `tests.test_agent_instruction`, `tests.test_agent_instruction_corpus` and `tests.test_skills_sh_package`, and under the Hexaemeron runner only in `test_audit_synopsis_recovery`. This step's tree adds no module and no further bad result to either set, and `scripts/run_checks.py`, run once the commit exists, passes every check except `root-suite` and `hexaemeron-suite`, `dead-code-suppressions-check` included. `.githooks/greenlight` records a green on suite exit zero alone, so while the base is red it cannot record one: the commit is made with `FIAT_SKIP_PRECOMMIT=1`, and the receipt, the audit record and the pull request each say so.

**Why.** The Exit as receipted read the three suite commands as pass-or-fail, and the run's own base commit fails them. On a clean snapshot of `592390722f10df53658906623b15428dbfb88d8f`, `tests/fixtures/agent-instruction-v1/manifest.json` pins `PROMISE_MACHINE.md` at `f996fb645c81a8f398a2384ed4d8da0e058a9c017ea37fbbee36e499c9ef2e15` while that commit's own `PROMISE_MACHINE.md` digests to `ae019700f378a250fe08a3d6cb68c0553ee60a410cbb6035d3c9c3cd2866b34f`, which reddens the two agent-instruction modules; `tests/test_skills_sh_package.py` is red on a byte cap its own comment block hands to framework-109, open as issue #1467; and `test_audit_synopsis_recovery` asserts more than one runtime binding sourced from `hexctl.py` after the base re-pointed those rows to per-promise modules. None of the three is this run's work and none is in its scope, so the Exit now states the baseline and requires this step to add nothing to it.

**Steps touched.** Step 1.

**Still holding.** Step 1: entry holds; exit holds. Step 2: entry holds; exit holds. Step 3: entry holds; exit holds. Step 4: entry holds; exit holds. Step 5: entry holds; exit holds. Step 6: entry holds; exit holds.
