# Runbook: Tabularium canonical-event-v3 and coverage-manifest-v3 with enums that match shipped adapter emissions

Derived from the receipted study at `.hexaemeron/study.md`. Task issue `wildcat-finance/skills#1362`. Starting commit `1d131b98a78c888b571f302e5b4c899aa2e47caf`. Run branch `fiat/1362-tabularium-canonical-event-v3-and-coverage`, integrating into `main`. Step 1 branches from the run branch; every later step branches from the step below it. Take every branch, parent and pull-request base from the controller directive.

The selected design is `superseding-releases` (study section 4): v3 schemas carry the closed three-adapter tuple table as enums plus one `oneOf` branch per adapter; `build` writes schema 3 by default and schema 2 only under `--event-schema 2`; `verify` reads 2 and 3 with a Python row validator that names the refused field; three superseding `*-v1` release directories are built from byte-identical v0 sources; the v2 schema files are corrected and marked deprecated. The four v0 release directories keep every byte.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 512a0b64fd17b5e46768e250dca7c9ace5f6c69febf4fc800c326313187cb595
candidate | superseding-releases
```

## Operating boundaries

- Always: the Tabularium suite through the checked runner and the root suite before every commit; the Imprimatur lint on every shipped Markdown file; the three v0 rebuild demos after any adapter or builder edit; the Sapheneia, Imprimatur, Vulgate, Imprimatur order on every new prose record.
- Ask first: a runtime dependency; a change to the capture manifest format; a CI change; an edit to any file under a v0 release directory; a change to the held Tabularium frontier text or Next Fiat job.
- Never: rewrite published `source.json`, `capture.json`, `events.jsonl` or `coverage.json` bytes under a v0 directory; admit an unrestricted string in v3; commit credentials; delete a failing test; claim a command ran when it did not; edit the receipted design record.

## Conventions used below

- `<run>` is the run worktree `/Users/c0rtexzer0/Documents/GitHub/skills/.claude/worktrees/frontier-jobs-ranking-871146/tmp/fiat/fiat-1362-tabularium-canonical-event-v3-and-coverage`. Every command runs from that repository root.
- `TMPDIR` is set to a directory under `/private/tmp` for every command on this host; the stock macOS value breaks two root-suite modules through a symlinked `/var`.
- The commit gate is the tracked `.githooks` directory, activated for this worktree only; `.githooks/greenlight` records a green after the suites pass, and the gate itself never runs a suite.
- Any step that adds a tracked file regenerates `.horos/boundary.json`, `.horos/candidates.json` and `.horos/census.json` after `git add` and before the commit, and only when the repository-owned scan changes them.
- The Tabularium suite command is `python3 -m unittest discover -s plugins/tabularium/tests -t plugins/tabularium`; the checked runner runs the same command as `tabularium-suite` beside the three repository lints. The root suite command is `python3 -m unittest discover -s tests`.
- Warden's exact audit runner for every step is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file named in that step's Tests field. Step 1 creates the runner from the shape of `plugins/alexandria/tests/run_tests.py`. The reporter `plugins/tabularium/tests/prove_schema_v3.py` writes design reports and is not an audit runner.
- The design record has four pending conformance cells. Their resolvers are exact strings in `.hexaemeron/design-evidence.json` and are quoted in the step that produces each report. `rejection-parity` is due before Step 4 opens, `shipped-ledgers-validate-v3` and `legacy-v0-verify` before Step 5 opens, and `suite-wall-time` before integration.
- Every unittest invocation carries `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false` so that a fixture commit never prompts for a signature.

## Step 1: Scaffold the specification, the v3 schema files and the corrected v2 files

**Goal.** Put the receipted specification and the schema documents in the tree, with document-level tests that fail while a schema disagrees with the adapter modules.

**Entry.** The run branch at `1d131b98a78c888b571f302e5b4c899aa2e47caf`, clean tree; the checked runner for scope `tabularium` is green with 137 Tabularium tests; the root suite is green under the `TMPDIR` convention.

**Exit.** All of `python3 scripts/run_checks.py --scope tabularium`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py --study docs/tabularium-schema-v3/study.md`, `python3 plugins/hexaemeron/skills/protasis/scripts/protasis.py docs/tabularium-schema-v3/runbook.md` and `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/tabularium-schema-v3/study.md docs/tabularium-schema-v3/runbook.md --max-defects 0` exit 0 from the repository root, then:
the root suite exits 0; `python3 plugins/tabularium/tests/run_tests.py --elenchus-report .elenchus/schema-v3-step-1-exit.json` exits 0 and writes a complete report with zero failures, errors and skips; `python3 scripts/plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` exits 0 on the committed step; `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:1` exits 0. The committed study and runbook are byte-identical to the receipted artefacts. `canonical-event-v3.json` and `coverage-manifest-v3.json` are draft 2020-12 documents with `schema_version` `const 3`, enums equal to the tuple table, and one `oneOf` branch per adapter; the v2 files keep their `$id`, carry `"deprecated": true` and a description naming v3 and the supersession date, and admit exactly what the v2 Python validator admitted. The extended `test_schemas.py` derives the tuple table from the three adapter modules and fails when any enum, `oneOf` branch or `const` disagrees with it. No file under an `examples/*-v0/` directory changes.

**Files.** Create `docs/tabularium-schema-v3/study.md`, `docs/tabularium-schema-v3/runbook.md`, `docs/tabularium-schema-v3/design-evidence.json`, `docs/tabularium-schema-v3/design-probe.py`, the twenty-four selection reports and `selection-observations.json` under `docs/tabularium-schema-v3/reports/`, `plugins/tabularium/schemas/canonical-event-v3.json`, `plugins/tabularium/schemas/coverage-manifest-v3.json` and `plugins/tabularium/tests/run_tests.py`. Change `plugins/tabularium/schemas/canonical-event-v2.json`, `plugins/tabularium/schemas/coverage-manifest-v2.json`, `plugins/tabularium/tests/test_schemas.py`, `plugins/tabularium/.claude-plugin/plugin.json` and `plugins/tabularium/.codex-plugin/plugin.json` (package `0.3.3` to `0.3.4`), `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`, any test that pins the Tabularium package version, and `tests/check-map-v1.json` only if a new path needs an owner. Regenerate the three `.horos/` artefacts. Warden alone appends `audit/rounds/fiat-1362-tabularium-canonical-event-v3-and-coverage.md` and its sibling synopsis.

**Tests.** Extend `plugins/tabularium/tests/test_schemas.py`: the four schema files parse and pass `check_schema` when `jsonschema` is importable and are structurally read otherwise; the v3 enums, `oneOf` branches and `const` values equal the tuple table derived from `aave_v4.py`, `euler_v1.py` and `euler_v2.py`; the v2 files are marked deprecated and admit every value the v2 Python validator admits; a schema edited to drop one tuple value is caught. Add `test_run_tests.py` covering the runner's report creation, its refusal of an existing report path and of a path outside the worktree. Expected count is observed when run, not predicted. For audit fixes the exact runner is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/schema-v3-step-1.json`.

**Disciplines.** phylax: the runner creates a report file from a caller-supplied path and must confine it to the worktree without following a symlink. ephoros: a failing tuple test names the schema file, the field and the value that disagree. metron: none, no performance claim is made in this step. elenchus: none, no failure is in hand; the tuple test is the guard a later drift would trip. hypomnema: the committed study and runbook are the record of the design; the schema files themselves are the home of the admitted vocabulary.

## Step 2: Make the library schema-version aware with a named row validator

**Goal.** Let `build` write schema 3 by default and schema 2 on request, and let `verify` read either version and refuse a bad row by name before any byte comparison.

**Entry.** Step 1's exit tree on the pushed Step 1 branch; the checked runner for scope `tabularium` and the root suite are green.

**Exit.** `python3 scripts/run_checks.py --scope tabularium` exits 0 from the repository root, then:
the root suite exits 0; `python3 plugins/tabularium/tests/run_tests.py --elenchus-report .elenchus/schema-v3-step-2-exit.json` exits 0 with a complete report and zero failures, errors and skips; `python3 plugins/tabularium/examples/aave-v4-v0/rebuild.py`, `python3 plugins/tabularium/examples/euler-v1-v0/rebuild.py` and `python3 plugins/tabularium/examples/euler-v2-v0/rebuild.py` each exit 0 and report a matching digest; `python3 plugins/tabularium/scripts/tabularium.py verify plugins/tabularium/examples/aave-v4-v0/coverage.json` exits 0 and prints the schema version in its success line; `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:2` exits 0. `tabularium_lib` exposes `SUPPORTED_EVENT_SCHEMAS` equal to the frozen set of 2 and 3 and `CURRENT_EVENT_SCHEMA` equal to 3; each adapter's `map_source` takes the schema version and emits it; `make_manifest` and `validate_manifest` take the version; `validate_event_row` refuses an unknown tuple value, a wrong version or a missing provenance field with a `TabulariumError` naming the row index and field; `verify` requires manifest `schema_version` to equal `versions.event_schema` and every row to match, and refuses any other value or a mixed release before the rebuild; `build` refuses `--event-schema` outside 2 and 3 and never writes 2 without the flag; the three v0 `rebuild.py` demos pass `--event-schema 2`. No file under an `examples/*-v0/` directory changes except the three `rebuild.py` demos.

**Files.** Change `plugins/tabularium/scripts/tabularium_lib/__init__.py`, `release_v2.py`, `builder.py`, `verifier.py`, `adapters/aave_v4.py`, `adapters/euler_v1.py`, `adapters/euler_v2.py`, `plugins/tabularium/scripts/tabularium.py`, `plugins/tabularium/examples/aave-v4-v0/rebuild.py`, `plugins/tabularium/examples/euler-v1-v0/rebuild.py`, `plugins/tabularium/examples/euler-v2-v0/rebuild.py`, `plugins/tabularium/tests/test_verify.py`, `plugins/tabularium/tests/test_builder_cli.py`, `plugins/tabularium/tests/test_aave_v4.py` and `plugins/tabularium/tests/test_euler_adapters.py`. Create `plugins/tabularium/tests/test_schema_versions.py`. Remove the unused constants at `tabularium_lib/__init__.py` lines 3 and 4 only if the dead-code check does not object; otherwise leave them and say so in the pull request.

**Tests.** Move the two existing wrong-version cases in `test_verify.py` from 3 to 4. Add cases for: build default writes 3; `--event-schema 2` reproduces each v0 release byte for byte; `--event-schema 4` refused; verify accepts 2 and 3; manifest 3 with a row at 2 refused, and the reverse; an unknown `venue`, `adapter`, `adapter_version`, `protocol_generation`, `source_api`, `evidence_class` or `mapping_rule` refused with the field named; a row missing a provenance field refused; the refusal text reaches stderr at exit 1 through the CLI. Expected count is observed when run. For audit fixes the exact runner is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/schema-v3-step-2.json`.

**Disciplines.** phylax: verify reads untrusted JSON and JSONL from a declared directory; the row validator runs after the existing duplicate-key and path-confinement checks and before the rebuild. ephoros: the success line carries the schema version and the refusal names the row and field, which answers on-call questions 1 and 2 of the study. metron: none, no performance claim; the row pass over 500 rows was measured at 93 ms in the study and no budget is claimed here. elenchus: any v0 rebuild mismatch is worked to its cause with a guard test before the fix. hypomnema: the reason v2 stays readable and the reason the flag exists are comments at the flag and the version set, not restated elsewhere.

## Step 3: Prove JSON Schema and Python parity on every shipped ledger

**Goal.** Validate every shipped row and manifest with the JSON Schema its version names, and show that three rejection fixtures are refused by both validators for the same cause.

**Entry.** Step 2's exit tree on the pushed Step 2 branch; the checked runner for scope `tabularium` and the root suite are green.

**Exit.** `python3 scripts/run_checks.py --scope tabularium` exits 0 from the repository root, then:
the root suite exits 0; `python3 plugins/tabularium/tests/run_tests.py --elenchus-report .elenchus/schema-v3-step-3-exit.json` exits 0 with a complete report and zero failures and errors, and zero skips on this host; `python3 plugins/tabularium/tests/prove_schema_v3.py --candidate superseding-releases --criterion rejection-parity --report <run>/.hexaemeron/reports/superseding-releases-rejection-parity.json` exits 0 and writes a closed `protasis-design-report/v1` object with exit 0; `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:3` exits 0. `test_schemas.py` validates every shipped `events.jsonl` row and `coverage.json` against the JSON Schema named by its `schema_version` (v0 releases against the corrected v2 files) and skips only with the reason `jsonschema is not installed`. The three committed rejection fixtures (unknown value, wrong version, malformed provenance) are refused by `jsonschema` and by `validate_event_row` with the same field named. The reporter exits 1 with the text `jsonschema is not installed` when the import fails and never writes a passing report on a skip. A fixture that either validator accepts fails the test.

**Files.** Create `plugins/tabularium/tests/prove_schema_v3.py`, `plugins/tabularium/tests/fixtures/schema-v3/unknown-value.json`, `plugins/tabularium/tests/fixtures/schema-v3/wrong-version.json` and `plugins/tabularium/tests/fixtures/schema-v3/malformed-provenance.json`. Change `plugins/tabularium/tests/test_schemas.py` and `plugins/tabularium/tests/support.py`. Regenerate the three `.horos/` artefacts.

**Tests.** In `test_schemas.py`: one parity case per shipped document set (three v0 releases at this step); one case per rejection fixture asserting both refusals and the shared field name; one case that a fixture accepted by only one validator fails; one case that the reporter refuses to write when `jsonschema` is absent, exercised by hiding the module. Expected count is observed when run. For audit fixes the exact runner is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/schema-v3-step-3.json`.

**Disciplines.** phylax: fixtures are data and are never executed; the reporter writes only below the report path it was given and refuses a symlinked component. ephoros: the unittest summary counts skips, every skip carries the named reason, and the reporter's failure text is the same string, which answers on-call question 3 of the study. metron: none, no performance claim. elenchus: a parity mismatch found here is a defect in one validator; the fixture that exposed it stays as the guard. hypomnema: none beyond the fixture names, which are the record of what is refused; no decision is made in this step.

## Step 4: Ship the superseding v1 releases and move the prose

**Goal.** Publish three schema-3 releases built from the byte-identical v0 sources, mark the v0 releases superseded in prose only, and move every document that names schema v2.

**Entry.** Step 3's exit tree on the pushed Step 3 branch; the `rejection-parity` report is bound at `step:4`; the checked runner for scope `tabularium` and the root suite are green.

**Exit.** `python3 scripts/run_checks.py --scope tabularium` and `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py plugins/tabularium/README.md plugins/tabularium/skills/tabularium/SKILL.md plugins/tabularium/docs/adding-an-adapter.md plugins/tabularium/docs/release-policy.md plugins/tabularium/examples/aave-v4-v1/README.md plugins/tabularium/examples/euler-v1-v1/README.md plugins/tabularium/examples/euler-v2-v1/README.md --max-defects 0` exit 0 from the repository root, then:
the root suite exits 0; `python3 plugins/tabularium/tests/run_tests.py --elenchus-report .elenchus/schema-v3-step-4-exit.json` exits 0 with a complete report and zero failures, errors and skips; the six `rebuild.py` demos under `examples/aave-v4-v0`, `examples/euler-v1-v0`, `examples/euler-v2-v0`, `examples/aave-v4-v1`, `examples/euler-v1-v1` and `examples/euler-v2-v1` each exit 0 with a matching digest; `python3 plugins/tabularium/scripts/tabularium.py verify plugins/tabularium/examples/aave-v4-v1/coverage.json` exits 0 and prints schema 3; `python3 plugins/tabularium/tests/prove_schema_v3.py --candidate superseding-releases --criterion shipped-ledgers-validate-v3 --report <run>/.hexaemeron/reports/superseding-releases-shipped-ledgers-validate-v3.json` and `python3 plugins/tabularium/tests/prove_schema_v3.py --candidate superseding-releases --criterion legacy-v0-verify --report <run>/.hexaemeron/reports/superseding-releases-legacy-v0-verify.json` each exit 0 and write a closed report with exit 0; `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition step:4` exits 0; `python3 scripts/plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` exits 0 on the committed step. Each v1 `source.json` has the same SHA-256 as its v0 source; each v1 `capture.json` differs from v0 only in `release` and keeps the `request` object with `"id": 1`; each v1 `coverage.json` and `events.jsonl` carries schema 3 and the release identifiers `aave-v4-mainnet-credit-window-v1`, `euler-v1-borrow-block-14531589-v1` and `euler-v2-owner-activity-1786933919-v1`. The v0 release files other than `README.md` and `rebuild.py` are byte-identical to `1d131b98`. `EVOLUTION.md` carries one new generation row `tabularium-v0.4.0` with the frontier revision, frontier SHA-256, current frontier text and Next Fiat job byte-identical to the prior row, and `SKILL.md` `metadata.version` reads `0.4.0`. The test-count figure in `plugins/tabularium/README.md` equals the observed count.

**Files.** Create `plugins/tabularium/examples/aave-v4-v1/`, `plugins/tabularium/examples/euler-v1-v1/` and `plugins/tabularium/examples/euler-v2-v1/`, each with `source.json`, `capture.json`, `coverage.json`, `events.jsonl`, `README.md`, `DATA-DICTIONARY.md` and `rebuild.py`. Change `plugins/tabularium/examples/aave-v4-v0/README.md`, `plugins/tabularium/examples/euler-v1-v0/README.md`, `plugins/tabularium/examples/euler-v2-v0/README.md` (a supersession note only), `plugins/tabularium/README.md`, `plugins/tabularium/skills/tabularium/SKILL.md`, `plugins/tabularium/skills/tabularium/EVOLUTION.md`, `plugins/tabularium/docs/adding-an-adapter.md`, `plugins/tabularium/docs/release-policy.md`, `plugins/tabularium/AGENTS.md` if it names schema v2, `plugins/tabularium/tests/test_release.py`, `plugins/tabularium/tests/test_euler_releases.py`, `plugins/tabularium/tests/test_aave_v4.py` and `plugins/tabularium/tests/test_schemas.py`. `docs/euler-preservation-study.md`, `docs/euler-preservation-runbook.md`, `docs/compound-v3-preservation.md` and `skills/tabularium/DEMONSTRATION.md` under `plugins/tabularium/` are historical and do not change. Regenerate the three `.horos/` artefacts.

**Tests.** Add release cases for the three v1 directories mirroring the existing v0 cases: rebuild matches, verify accepts, source digest equals the v0 digest, capture differs only in `release`, identifier and schema version as stated. Add a case asserting the twelve v0 data files' digests at `1d131b98` and a case that the ledger row and `SKILL.md` version agree. Extend the parity cases to the three v1 document sets. Expected count is observed when run. For audit fixes the exact runner is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/schema-v3-step-4.json`.

**Disciplines.** phylax: the v1 build writes into new directories through `write_bytes_atomic` and never overlaps a v0 path. ephoros: each v1 README names its v0 release and each v0 README its v1 successor, which answers on-call question 4 of the study. metron: none, no performance claim; the added bytes are the design's recorded trade of 1,710,643 bytes. elenchus: a v1 rebuild mismatch is worked to its cause before any fixture is edited. hypomnema: the superseding-release rule and the schema-version clause live in `release-policy.md`; the vocabulary rule lives in `adding-an-adapter.md`; the build default lives in `SKILL.md`; the ledger row names the design bridge; no new decision record is created.

## Step 5: Demonstrate the whole path and record the suite budget

**Goal.** Run the study's complete demo path from the tip of the run, produce the suite budget report, and commit the conformance evidence beside the specification.

**Entry.** Step 4's exit tree on the pushed Step 4 branch; the `shipped-ledgers-validate-v3` and `legacy-v0-verify` reports are bound at `step:5`; the checked runner for scope `tabularium` and the root suite are green.

**Exit.** `python3 scripts/run_checks.py --scope tabularium` and `python3 plugins/hexaemeron/skills/imprimatur/scripts/imprimatur.py docs/tabularium-schema-v3/demonstration.md --max-defects 0` exit 0 from the repository root, then:
every command listed under the study's success criteria exits 0 in the order given there, with the four `prove_schema_v3.py` resolvers invoked as the exact strings in the design record; `python3 plugins/tabularium/tests/prove_schema_v3.py --candidate superseding-releases --criterion suite-wall-time --report <run>/.hexaemeron/reports/superseding-releases-suite-wall-time.json` exits 0 and records a value at most 60000 milliseconds; `python3 plugins/hexaemeron/skills/protasis/scripts/design_evidence.py .hexaemeron/design-evidence.json --transition integration` exits 0; `python3 plugins/tabularium/tests/run_tests.py --elenchus-report .elenchus/schema-v3-step-5-exit.json` exits 0 with a complete report and zero failures, errors and skips; the root suite exits 0; `python3 scripts/plugin_release.py --base 1d131b98a78c888b571f302e5b4c899aa2e47caf --head HEAD` exits 0 on the committed step. `docs/tabularium-schema-v3/demonstration.md` records every command, its exit code, the observed test count, the four report digests and the six rebuild digests, and claims nothing about the truth of a mapped record. The four conformance report copies under `docs/tabularium-schema-v3/reports/` are byte-identical to the files the resolvers wrote.

**Files.** Create `docs/tabularium-schema-v3/demonstration.md` and the four conformance report copies under `docs/tabularium-schema-v3/reports/`. Change `docs/tabularium-schema-v3/design-evidence.json` only by copying the unchanged receipted record again if Step 1's copy drifted, which the exit forbids. Regenerate the three `.horos/` artefacts.

**Tests.** No new product test; add one case in `test_schemas.py` that each committed conformance report copy parses as a `protasis-design-report/v1` object with exit 0 and matches its recorded digest. Expected count is observed when run. For audit fixes the exact runner is `python3 plugins/tabularium/tests/run_tests.py --elenchus-report {report}`, report format `unittest-json-v1`, report file `.elenchus/schema-v3-step-5.json`.

**Disciplines.** phylax: none, this step reads committed files and writes one report and one document. ephoros: the demonstration lists every command with its exit code so a reader can tell which one did not run. metron: the suite budget of 60,000 ms is measured by the `suite-wall-time` resolver against the 3,650 ms baseline in the study; no other budget exists. elenchus: a failing demo command halts the step and is worked to its cause; the demonstration never records a command that did not run. hypomnema: the demonstration is the record that the demo path ran; it lives beside the specification and is not a decision record.
